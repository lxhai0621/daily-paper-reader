---
title: "MemFactory: Unified Inference & Training Framework for Agent Memory"
title_zh: MemFactory：智能体记忆的统一推理与训练框架
authors: "Ziliang Guo, Ziheng Li, Zhiyu Li"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29493v1"
tags: ["query:agent"]
score: 10.0
evidence: 使用强化学习的智能体记忆统一训练框架
tldr: "针对记忆增强型大模型智能体在训练与推理中存在的实现碎片化、缺乏统一框架等问题，本文提出了MemFactory。该框架借鉴LLaMA-Factory的成功经验，采用模块化“乐高式”架构，将记忆生命周期抽象为可插拔组件。它原生支持GRPO算法，通过环境奖励优化记忆管理策略，并兼容Memory-R1等前沿范式。实验证明，MemFactory能显著提升智能体性能，最高增幅达14.8%，为记忆驱动的AI智能体研究提供了标准化基础设施。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29493v1/fig-001.webp\", \"caption\": \"Figure 1 The overall architecture of MemFactory, illustrating the interdependencies among the Modules, Agent, Environment, and Trainer layers.\", \"page\": 4, \"index\": 1, \"width\": 929, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2603.29493v1/fig-002.webp\", \"caption\": \"Table 1 Standard interfaces supported by modules in the Module Layer.\", \"page\": 5, \"index\": 2, \"width\": 968, \"height\": 221}]"
motivation: 现有的记忆增强型智能体实现高度碎片化且任务特定，缺乏统一的框架来简化记忆操作的集成、训练和评估。
method: 提出MemFactory框架，将记忆生命周期抽象为原子化模块，并集成GRPO算法以通过多维环境奖励优化内部记忆管理策略。
result: "在MemAgent架构上的实验显示，MemFactory在域内和分布外评估集中均优于基础模型，性能提升最高达14.8%。"
conclusion: MemFactory通过提供标准化、可扩展且易用的基础设施，降低了记忆增强型智能体的开发门槛，推动了该领域的创新。
---

## 摘要
记忆增强型大语言模型（LLMs）对于开发具备长期能力的 AI 智能体至关重要。近期，应用强化学习（RL）来优化记忆操作（如提取、更新和检索）已成为一个极具前景的研究方向。然而，现有的实现方式仍然高度碎片化且针对特定任务，缺乏统一的基础设施来简化这些复杂流水线的集成、训练和评估。为填补这一空白，我们提出了 MemFactory，这是首个专为记忆增强型智能体设计的统一、高度模块化的训练与推理框架。受 LLaMA-Factory 等统一微调框架成功的启发，MemFactory 将记忆生命周期抽象为原子化、即插即用的组件，使研究人员能够通过“乐高式”架构无缝构建自定义记忆智能体。此外，该框架原生集成了群体相对策略优化（GRPO），以微调由多维环境奖励驱动的内部记忆管理策略。MemFactory 为近期前沿范式（包括 Memory-R1、RMM 和 MemAgent）提供开箱即用的支持。我们在开源的 MemAgent 架构上，利用其公开的训练和评估数据对 MemFactory 进行了实证验证。在域内和分布外评估集上，MemFactory 相比相应的基座模型均持续提升了性能，相对增益最高达 14.8%。通过提供标准化、可扩展且易于使用的基础设施，MemFactory 显著降低了准入门槛，为记忆驱动型 AI 智能体的未来创新铺平了道路。

## Abstract
Memory-augmented Large Language Models (LLMs) are essential for developing capable, long-term AI agents. Recently, applying Reinforcement Learning (RL) to optimize memory operations, such as extraction, updating, and retrieval, has emerged as a highly promising research direction. However, existing implementations remain highly fragmented and task-specific, lacking a unified infrastructure to streamline the integration, training, and evaluation of these complex pipelines. To address this gap, we present MemFactory, the first unified, highly modular training and inference framework specifically designed for memory-augmented agents. Inspired by the success of unified fine-tuning frameworks like LLaMA-Factory, MemFactory abstracts the memory lifecycle into atomic, plug-and-play components, enabling researchers to seamlessly construct custom memory agents via a "Lego-like" architecture. Furthermore, the framework natively integrates Group Relative Policy Optimization (GRPO) to fine-tune internal memory management policies driven by multi-dimensional environmental rewards. MemFactory provides out-of-the-box support for recent cutting-edge paradigms, including Memory-R1, RMM, and MemAgent. We empirically validate MemFactory on the open-source MemAgent architecture using its publicly available training and evaluation data. Across both in-domain and out-of-distribution evaluation sets, MemFactory consistently improves performance over the corresponding base models, with relative gains of up to 14.8%. By providing a standardized, extensible, and easy-to-use infrastructure, MemFactory significantly lowers the barrier to entry, paving the way for future innovations in memory-driven AI agents.

---

## 论文详细总结（自动生成）

以下是对论文《MemFactory: Unified Inference & Training Framework for Agent Memory》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：为了让大语言模型（LLM）进化为具备长期能力的 AI 智能体，记忆增强技术至关重要。目前，利用强化学习（RL）优化记忆操作（如提取、更新、检索）已成为前沿趋势。
*   **核心问题**：现有的记忆强化学习（Memory-RL）研究存在**实现碎片化**和**任务特定化**的问题。不同研究（如 Memory-R1, MemAgent, RMM）的代码库彼此孤立，缺乏一个像 LLaMA-Factory 那样统一、模块化且易用的基础设施来简化记忆智能体的构建、训练和评估。
*   **整体含义**：本文推出了 **MemFactory**，这是首个专为记忆增强型智能体设计的统一推理与训练框架，旨在降低该领域的研发门槛。

### 2. 方法论
MemFactory 采用“乐高式”的模块化架构，分为四个核心层：
*   **模块层 (Module Layer)**：将记忆生命周期抽象为原子操作。
    *   **Extractor**：从原始上下文中解析事实。
    *   **Updater**：执行增删改（ADD/DEL/UPDATE/NONE）操作维护记忆库。
    *   **Retriever**：基于语义或重排序（Rerank）检索相关记忆。
    *   **Agent Module**：支持端到端的循环记忆状态更新。
*   **智能体层 (Agent Layer)**：作为策略执行器，研究者可以像搭积木一样组合上述模块，构建自定义记忆策略。
*   **环境层 (Environment Layer)**：负责数据加载和奖励管理。支持多维奖励信号，包括格式奖励（Format Rewards）和 LLM-as-a-Judge 评分。
*   **训练层 (Trainer Layer)**：核心优化引擎。
    *   **关键技术**：原生集成 **GRPO（群体相对策略优化）** 算法。
    *   **公式原理**：GRPO 通过在一组采样响应中进行奖励归一化来计算优势（Advantage），无需额外的 Critic 网络，从而显著降低显存占用，适合长上下文的记忆训练。

### 3. 实验设计
*   **实验对象**：基于开源的 **MemAgent** 架构进行验证。
*   **基座模型**：使用了 Qwen3-1.7B 和 Qwen3-4B-Instruct。
*   **数据集/场景**：
    *   **域内（In-domain）**：`eval_50` 和 `eval_100`（基于 MemAgent 公开数据）。
    *   **分布外（OOD）**：`eval_fwe_16384`（测试模型的泛化能力）。
*   **对比基准**：对比了经过 MemFactory RL 微调后的模型与原始基座模型（Base Checkpoint）的性能差异。

### 4. 资源与算力
*   **硬件配置**：论文明确指出，整个训练和评估流水线可以在**单张 NVIDIA A800 80GB GPU** 上运行。
*   **训练细节**：每个模型训练了 250 个有效步（valid steps）。
*   **效率体现**：通过 GRPO 算法和 FlashAttention-2 的集成，证明了该框架在有限算力下开发长上下文记忆策略的可行性。

### 5. 实验数量与充分性
*   **实验规模**：针对两个不同规模的基座模型在三个测试集上进行了评估。
*   **客观性保障**：为了应对生成的不确定性，所有得分均取 **4 次独立试验的平均值 (avg@4)**。
*   **充分性**：实验涵盖了从 1.7B 到 4B 的模型，并包含了 OOD 测试，验证了框架在不同规模模型和不同任务分布下的有效性。虽然实验主要集中在 MemAgent 这一种架构上，但其目的是验证框架的优化能力而非刷榜。

### 6. 主要结论与发现
*   **性能提升**：MemFactory 显著提升了基座模型的记忆管理能力。Qwen3-1.7B 的平均性能提升了 **14.8%**；Qwen3-4B-Instruct 提升了 **7.3%**。
*   **泛化能力**：较大规模的模型（4B）在 OOD 测试集上表现出更稳健的提升，说明通过 RL 学习到的循环记忆策略具有良好的迁移性。
*   **框架有效性**：证明了即使使用简化的训练数据和单卡算力，MemFactory 也能有效优化复杂的记忆策略。

### 7. 优点
*   **高度模块化**：解耦了记忆操作，支持“即插即用”，极大方便了研究者复现和改进 SOTA 算法。
*   **显存友好**：原生支持 GRPO，解决了记忆增强型智能体在长上下文训练时显存易溢出的痛点。
*   **开箱即用**：预置了 Memory-R1、MemAgent 和 RMM 等前沿范式的实现。
*   **标准化**：为碎片化的记忆研究提供了统一的数据格式和评估接口。

### 8. 不足与局限
*   **范式覆盖有限**：虽然框架可扩展，但目前内置的成熟范式数量仍有增加空间。
*   **训练效率**：论文提到在训练效率方面仍有进一步优化的余地。
*   **实验深度**：实证部分主要聚焦于 MemAgent 架构的复现与优化，缺乏不同记忆范式（如显式记忆库 vs 隐式循环状态）在同一框架下的深度横向对比实验。

（完）
