---
title: "When to ASK: Uncertainty-Gated Language Assistance for Reinforcement Learning"
title_zh: 何时询问：面向强化学习的不确定性门控语言辅助
authors: "Juarez Monteiro, Nathan Gavenski, Gianlucca Zuin, Adriano Veloso"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02226v1"
tags: ["query:agent"]
score: 9.0
evidence: 结合语言模型与强化学习用于智能体决策
tldr: 针对强化学习（RL）在分布外（OOD）场景中表现不佳的问题，本文提出ASK框架。该框架通过蒙特卡洛丢弃法评估RL策略的不确定性，仅在不确定性超过阈值时请求语言模型（LM）提供动作建议。这种选择性调用机制在保持实时效率的同时，利用LM的先验知识增强了智能体的泛化能力。实验表明，ASK在迁移任务中表现优异，证明了神经符号集成在处理未知环境中的潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.02226v1/fig-001.webp\", \"caption\": \"TABLE IV: Downward generalization results. PPO trained on FrozenLake-8 and evaluated on smaller environments. Eval corresponds to validation performance, while Test reports final performance using fixed thresholds. Results are reported as mean ± standard deviation over 100 episodes.\", \"page\": 11, \"index\": 1, \"width\": 1076, \"height\": 1257}, {\"url\": \"assets/figures/arxiv/2604.02226v1/fig-002.webp\", \"caption\": \"TABLE III: Comparison between PPO baseline and ASK on FrozenLake. Eval corresponds to validation performance using the gating mechanism. The test corresponds to in-domain test performance. Results are reported as mean ± standard deviation over 100 episodes.\", \"page\": 10, \"index\": 2, \"width\": 1076, \"height\": 1082}, {\"url\": \"assets/figures/arxiv/2604.02226v1/fig-003.webp\", \"caption\": \"Fig. 2: In-domain performance of PPO, LM, and ASK on the FrozenLake environment across grid sizes for language model sizes ranging from 0.5B to 72B parameters. Values above markers denote mean reward over 100 episodes.\", \"page\": 4, \"index\": 3, \"width\": 621, \"height\": 208}, {\"url\": \"assets/figures/arxiv/2604.02226v1/fig-004.webp\", \"caption\": \"TABLE I: Comparison of PPO baseline and ASK on FrozenLake: same-size evaluation and downward generalization.\", \"page\": 5, \"index\": 4, \"width\": 1076, \"height\": 621}]"
motivation: 强化学习智能体在面对分布外场景时往往表现出高不确定性和随机行为，而大语言模型虽有丰富知识但计算成本高且自主规划能力有限。
method: 提出ASK框架，利用蒙特卡洛丢弃法实时监测RL策略的不确定性，并仅在必要时通过语言模型获取动作指导。
result: 在FrozenLake环境的迁移任务中，ASK实现了0.95的高奖励，显著提升了智能体在未知环境下的导航能力。
conclusion: 有效的神经符号集成需要精细的协调机制而非简单组合，模型规模和混合机制对实现成功的分布外泛化至关重要。
---

## 摘要
强化学习 (RL) 智能体通常难以应对分布外 (OOD) 场景，从而导致高不确定性和随机行为。虽然语言模型 (LM) 包含宝贵的世界知识，但大型模型计算成本高昂，阻碍了实时应用，且在自主规划方面存在局限性。我们提出了“通过知识实现自适应安全”（Adaptive Safety through Knowledge, ASK），该方法将较小的语言模型与训练好的强化学习策略相结合，在无需重新训练的情况下增强分布外泛化能力。ASK 采用蒙特卡洛丢弃法 (Monte Carlo Dropout) 来评估不确定性，并仅在不确定性超过设定阈值时才向语言模型查询动作建议。这种选择性使用在保留现有策略效率的同时，利用了语言模型在不确定情况下的推理能力。在 FrozenLake 环境的实验中，ASK 在域内没有表现出改进，但在迁移任务中展示了稳健的导航能力，实现了 0.95 的奖励。我们的研究结果表明，有效的神经符号集成需要精心的编排而非简单的组合，强调了成功的分布外泛化需要足够的模型规模和有效的混合机制。

## Abstract
Reinforcement learning (RL) agents often struggle with out-of-distribution (OOD) scenarios, leading to high uncertainty and random behavior. While language models (LMs) contain valuable world knowledge, larger ones incur high computational costs, hindering real-time use, and exhibit limitations in autonomous planning. We introduce Adaptive Safety through Knowledge (ASK), which combines smaller LMs with trained RL policies to enhance OOD generalization without retraining. ASK employs Monte Carlo Dropout to assess uncertainty and queries the LM for action suggestions only when uncertainty exceeds a set threshold. This selective use preserves the efficiency of existing policies while leveraging the language model's reasoning in uncertain situations. In experiments on the FrozenLake environment, ASK shows no improvement in-domain, but demonstrates robust navigation in transfer tasks, achieving a reward of 0.95. Our findings indicate that effective neuro-symbolic integration requires careful orchestration rather than simple combination, highlighting the need for sufficient model scale and effective hybridization mechanisms for successful OOD generalization.

---

## 论文详细总结（自动生成）

### 论文总结：When to ASK: Uncertainty-Gated Language Assistance for Reinforcement Learning

#### 1. 核心问题与整体含义
论文探讨了强化学习（RL）智能体在面对**分布外（Out-of-Distribution, OOD）**场景时的脆弱性。当智能体进入未曾见过的状态时，其策略往往会失效，表现为随机或危险的行为。虽然大语言模型（LLM）拥有丰富的世界知识和常识推理能力，可以辅助决策，但其计算成本高、实时性差，且在长程规划上不如专门的 RL 策略。
**研究动机**：如何高效地结合 RL 的执行效率与语言模型的推理能力，使智能体在熟悉环境下自主运行，在陌生（OOD）环境下寻求语言模型的“知识援助”，从而提高泛化性和安全性。

#### 2. 方法论
论文提出了 **ASK (Adaptive Safety through Knowledge)** 框架，这是一种**外在（Extrinsic）**增强方法，无需对现有 RL 策略进行重新训练。
*   **核心思想**：利用不确定性作为“门控”，仅在 RL 策略感到“困惑”时才调用语言模型。
*   **关键技术细节**：
    *   **不确定性评估**：使用**蒙特卡洛丢弃法（MC Dropout）**。在推理时对已训练的 PPO 策略进行 $N=100$ 次随机丢弃的前向传播，计算预测分布的方差（认知不确定性）和熵（偶然不确定性）。
    *   **门控机制**：设定阈值 $\tau$。当总不确定性超过 $\tau$ 时，触发语言模型查询。
    *   **提示词工程（Prompting）**：将环境状态（如坐标、周围障碍物、目标位置）转化为自然语言描述，并包含 RL 策略建议的动作（称为“自动驾驶建议”），让 LM 决定是采纳还是修正该动作。
    *   **动作执行**：若 LM 给出有效动作，则覆盖原 RL 动作；否则回退至原策略。

#### 3. 实验设计
*   **场景与数据集**：使用 **FrozenLake-v1**（冰湖导航）环境，网格大小从 4x4 到 8x8 不等。为每个尺寸创建了 300 个不同的上下文（洞穴位置不同），分为训练、验证和测试集。
*   **Benchmark 与对比方法**：
    *   **PPO 策略**：纯强化学习基准。
    *   **LM-only**：仅靠语言模型进行导航（实验证明其规划能力极差，接近 0 分）。
    *   **ASK (PPO + Qwen)**：本文方法，使用了 Qwen2.5 系列模型（规模从 0.5B 到 72B）。
*   **测试任务**：
    1.  **域内评估**：测试集与训练集地图尺寸相同。
    2.  **向下泛化（Downward Generalization）**：在 8x8 地图上训练的 PPO 策略，直接在 4x4 到 7x7 的地图上运行（模拟严重的分布偏移）。

#### 4. 资源与算力
*   **训练细节**：PPO 策略训练了 $2 \times 10^7$ 个时间步。
*   **模型来源**：使用了 HuggingFace 上的 Qwen2.5 开源权重。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或具体的训练/推理耗时。但提到使用较小的 LM（如 0.5B-7B）是为了平衡实时决策的计算开销。

#### 5. 实验数量与充分性
*   **实验规模**：每个配置在 100 个随机回合中进行评估，涵盖了 7 种不同规模的语言模型和 5 种地图尺寸。
*   **充分性**：实验设计较为全面，不仅对比了不同规模的 LM，还通过“干预率（IR）”和“覆盖率（OR）”深入分析了 LM 何时介入以及介入是否有效。
*   **客观性**：通过贝叶斯优化（Optuna）自动寻找每个模型的最佳不确定性阈值，确保了对比的公平性。

#### 6. 主要结论与发现
*   **域内无增益**：在熟悉的环境中，PPO 已经接近最优，引入 LM 反而可能因为误干预导致性能轻微下降。
*   **OOD 场景下的卓越表现**：在向下泛化任务中，纯 PPO 和纯 LM 均告失败（奖励接近 0），而 ASK（配合 32B 或 72B 模型）实现了高达 **0.95** 的奖励，证明了两者结合的强大泛化力。
*   **能力阈值效应**：语言模型在空间推理上存在明显的“规模效应”。32B 及以上规模的模型在处理分布偏移时表现出质的飞跃，而中等规模（3B-14B）模型往往因为不确定性校准差而表现不佳。

#### 7. 优点
*   **无需重训**：作为一种插件式方法，它可以直接增强现有的 RL 智能体，具有极高的实用性。
*   **效率平衡**：通过不确定性门控，避免了步步调用大模型带来的高延迟和高成本。
*   **神经符号协同**：成功地将 RL 的反应式控制与 LM 的符号化推理结合，解决了 OOD 泛化难题。

#### 8. 不足与局限
*   **环境复杂度有限**：实验仅限于离散动作空间的 FrozenLake（网格世界），尚未在连续控制或视觉输入的高维复杂环境（如机器人操作）中验证。
*   **阈值敏感性**：系统性能高度依赖于不确定性阈值 $\tau$ 的设定，且不同规模的模型需要独立调优。
*   **中等模型失效**：实验发现 3B 到 14B 的模型在 ASK 框架下表现甚至不如更小的模型，显示出模型规模与不确定性校准之间存在复杂的非线性关系。

（完）
