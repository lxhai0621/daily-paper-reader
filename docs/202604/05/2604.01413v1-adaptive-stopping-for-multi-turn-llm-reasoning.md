---
title: Adaptive Stopping for Multi-Turn LLM Reasoning
title_zh: 多轮大语言模型推理的自适应停止
authors: "Xiaofan Zhou, Huy Nguyen, Bo Yu, Chenxi Liu, Lu Cheng"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01413v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: RAG 和 ReAct 智能体的自适应停止机制
tldr: 针对多轮推理（如RAG和ReAct）中缺乏可靠停止准则的问题，本文提出了MiCP框架。这是首个将符合预测（CP）应用于多轮推理的框架，通过在不同轮次间分配误差预算，实现了在保证预测覆盖率的同时动态决定停止时机。实验证明MiCP在保持目标覆盖率的前提下，显著降低了推理成本和轮次，提升了多轮推理的可靠性与效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01413v1/fig-001.webp\", \"caption\": \"Figure 2: Empirical gold retention rate vs. the target 1 − α across five datasets on adaptive RAG and ReAct for retrieval calibration. All models maintain coverage at or above the 1 − α guarantee for all tested error rates.\", \"page\": 8, \"index\": 1, \"width\": 820, \"height\": 212}, {\"url\": \"assets/figures/arxiv/2604.01413v1/fig-002.webp\", \"caption\": \"Table 1: Average number of retrieval turns (Avg #Turns), prediction set size, and composite objective across five datasets, averaged over all α values. Bold indicates the best result per row. Lower is better for all metrics. w/o ES means no early stop.\", \"page\": 9, \"index\": 2, \"width\": 824, \"height\": 349}, {\"url\": \"assets/figures/arxiv/2604.01413v1/fig-003.webp\", \"caption\": \"Figure 3: Empirical coverage rate vs. the target 1 − α across five datasets on adaptive RAG and ReAct for the final prediction set. All models maintain coverage at or above the 1 − α guarantee for all tested error rates.\", \"page\": 9, \"index\": 3, \"width\": 817, \"height\": 192}]"
motivation: 现有多轮推理方法依赖启发式停止规则，缺乏正式的准确性保证，导致在高风险领域难以平衡推理成本与决策风险。
method: 提出MiCP框架，通过在多轮推理过程中动态分配误差预算，利用符合预测技术为自适应停止提供形式化的覆盖率保证。
result: 在单跳和多跳问答任务中，MiCP在满足预设覆盖率的同时，显著减少了推理轮次、计算成本和预测候选集的大小。
conclusion: MiCP为多轮LLM推理提供了一种兼顾效率与可靠性的自适应停止方案，并引入了联合评估覆盖率与效率的新指标。
---

## 摘要
大语言模型（LLMs）越来越多地依赖多轮推理和交互（如自适应检索增强生成 (RAG) 和 ReAct 风格的智能体）来回答难题。这些方法通过迭代地检索信息、推理或行动来提高准确性，但也引入了一个关键挑战：模型应该在何时停止？现有方法依赖于启发式停止规则或固定的轮次预算，且无法提供最终预测仍包含正确答案的形式化保证。这种局限性在金融和医疗等高风险领域尤为突出，在这些领域，不必要的轮次会增加成本和延迟，而过早停止则面临决策错误的风险。符合预测（Conformal Prediction, CP）提供了形式化的覆盖率保证，但现有的 LLM-CP 方法仅适用于单次模型输出，无法处理具有自适应停止的多轮流水线。为了填补这一空白，我们提出了带有符合预测的多轮语言模型（MiCP），这是首个用于多轮推理的 CP 框架。MiCP 在不同轮次之间分配不同的误差预算，使模型能够在保持整体覆盖率保证的同时提前停止。我们在自适应 RAG 和 ReAct 上演示了 MiCP，它在单跳和多跳问答基准测试中均达到了目标覆盖率，同时减少了轮次、推理成本和预测集大小。我们还引入了一种新指标，用于联合评估覆盖率有效性和回答效率。

## Abstract
Large Language Models (LLMs) increasingly rely on multi-turn reasoning and interaction, such as adaptive retrieval-augmented generation (RAG) and ReAct-style agents, to answer difficult questions. These methods improve accuracy by iteratively retrieving information, reasoning, or acting, but introduce a key challenge: \textbf{When should the model stop?} Existing approaches rely on heuristic stopping rules or fixed turn budgets and provide no formal guarantees that the final prediction still contains the correct answer. This limitation is particularly problematic in high-stakes domains such as finance and healthcare, where unnecessary turns increase cost and latency, while stopping too early risks incorrect decisions. Conformal prediction (CP) provides formal coverage guarantees, but existing LLM-CP methods only apply to a single model output and cannot handle multi-turn pipelines with adaptive stopping. To address this gap, we propose Multi-Turn Language Models with Conformal Prediction (MiCP), the first CP framework for multi-turn reasoning. MiCP allocates different error budgets across turns, enabling the model to stop early while maintaining an overall coverage guarantee. We demonstrate MiCP on adaptive RAG and ReAct, where it achieves the target coverage on both single-hop and multi-hop question answering benchmarks while reducing the number of turns, inference cost, and prediction set size. We further introduce a new metric that jointly evaluates coverage validity and answering efficiency.

---

## 论文详细总结（自动生成）

这是一份关于论文《Adaptive Stopping for Multi-Turn LLM Reasoning》的结构化深度分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）向多轮推理（如自适应检索增强生成 RAG、ReAct 智能体）演进，系统面临一个核心矛盾：**何时停止推理？**
*   **现状与痛点**：现有的停止准则多依赖于启发式规则（如固定轮次、简单的置信度阈值），缺乏严谨的数学保证。停得太早会导致信息不足而报错，停得太晚则增加延迟、成本，甚至引入幻觉噪声。
*   **研究目标**：将**符合预测（Conformal Prediction, CP）**引入多轮推理场景，在保证最终预测集以高概率（$1-\alpha$）包含正确答案的前提下，实现推理轮次的自适应提前停止。

### 2. 方法论：MiCP 框架
论文提出了 **MiCP (Multi-Turn Language Models with Conformal Prediction)**，其核心思想是将整体误差预算分配到各个推理轮次中。

*   **核心技术细节**：
    1.  **检索过滤（Retrieval Filtering）**：在每一轮检索中，利用 CP 校准一个相关性阈值 $\hat{q}_{ret}$，过滤掉低质量文档，确保至少保留 $1-\alpha_{ret}$ 的金标文档。
    2.  **多级误差预算分配（Error Budget Allocation）**：这是 MiCP 的核心。它不使用统一阈值，而是为每一轮 $t$ 分配特定的误差预算 $\alpha_t$。通过约束公式 $\sum_{t=0}^T (1-c_t^{ans})\alpha_t \le (1-c_{final}^{ans})\alpha$（其中 $c$ 为可回答率），确保整体覆盖率。
    3.  **自适应停止机制**：利用模型输出的归一化熵（Normalized Entropy, NE）作为不确定性信号。当某一轮的置信度超过该轮校准的阈值 $\hat{q}_t$ 时，模型提前停止。
    4.  **复合评估指标 $L$**：提出一个新指标，综合考虑平均轮次（效率）和区分可回答/不可回答问题的能力（质量），并通过网格搜索（Grid Search）优化误差分配方案。
    5.  **“无法回答”选项**：若所有轮次结束仍未达标，预测集将包含“Can’t Answer”标签，以维持覆盖率保证。

### 3. 实验设计
*   **数据集**：涵盖单跳和多跳问答任务，包括 **NQ、TriviaQA、HotpotQA、MuSiQue、2WikiMHQA**。
*   **应用场景**：自适应 RAG（Adaptive RAG）和 ReAct 智能体。
*   **基准模型（Backbones）**：Gemma-2-9B-IT、Qwen3.5-9B、GPT-4o-mini。
*   **对比方法**：
    *   **w/o ES (No Early Stop)**：不使用提前停止的基准。
    *   **Avg Iter**：仅以最小化平均轮次为目标的优化变体。
    *   **Answer Rate**：以最大化回答率为目标的优化变体。
    *   **Ours (MiCP)**：基于复合指标 $L$ 优化的完整方法。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或总训练/推理时长。
*   **参数设置**：实验中每轮采样 $M=15$ 个样本，温度设为 1.4；网格搜索步长 $G=20$；检索使用基于 Elasticsearch 的维基百科检索器。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个数据集上进行了测试，涵盖了 3 种不同架构的模型，并对比了 5 种不同的目标误差率 $\alpha \in \{0.05, 0.10, 0.15, 0.20, 0.25\}$。
*   **充分性评价**：实验设计较为充分。作者不仅验证了最终预测集的覆盖率，还验证了中间检索阶段的覆盖率（Gold Retention Rate）。通过引入 ReAct 场景，证明了框架的通用性。
*   **客观性**：作者诚实地指出了在 Gemma 模型上优化效果不如 Qwen 和 GPT 明显，并分析了原因（模型区分能力不足），体现了学术客观性。

### 6. 主要结论与发现
*   **覆盖率保证**：MiCP 在所有模型和数据集上均成功维持了预设的 $1-\alpha$ 覆盖率，证明了理论框架的有效性。
*   **效率提升**：相比不停止的基准，MiCP 显著减少了推理轮次（尤其在 2WikiMHQA 等任务上）和预测集的大小。
*   **噪声抑制**：提前停止不仅省钱，还能防止后期轮次引入的错误信息污染预测集，从而减小预测集规模。
*   **模型差异**：更强大的模型（如 Qwen3.5, GPT-4o-mini）在 MiCP 框架下表现出更好的效率-质量权衡。

### 7. 优点
*   **理论严谨**：首次将符合预测（CP）应用于多轮自适应推理，提供了形式化的数学证明。
*   **端到端校准**：不仅管“答案”，还管“检索”，实现了全流程的可靠性控制。
*   **灵活的预算分配**：通过误差预算分配解决了“简单题早停、难题多搜”的逻辑，比单一阈值更科学。
*   **新评估维度**：提出的复合指标 $L$ 为多轮推理系统的评估提供了更全面的视角。

### 8. 不足与局限
*   **采样开销**：MiCP 依赖于每轮多次采样（$M=15$）来估计不确定性，这在实时性要求极高的场景下可能带来额外的推理延迟。
*   **网格搜索成本**：优化误差分配需要进行网格搜索，虽然是在离线校准阶段完成，但随着轮次 $T$ 的增加，搜索空间会呈指数级增长。
*   **条件覆盖限制**：目前的保证是边际覆盖（Marginal Coverage），即平均意义上的保证，对于特定难度的子群体（Conditional Coverage）可能存在偏差。
*   **模型依赖性**：对于不确定性估计不准的小模型或弱模型，MiCP 的效率提升空间受限。

（完）
