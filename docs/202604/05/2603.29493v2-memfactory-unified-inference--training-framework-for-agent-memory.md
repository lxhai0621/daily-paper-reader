---
title: "MemFactory: Unified Inference & Training Framework for Agent Memory"
title_zh: MemFactory：智能体记忆的统一推理与训练框架
authors: "Ziliang Guo, Ziheng Li, Bo Tang, Feiyu Xiong, Zhiyu Li"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29493v2"
tags: ["query:agent"]
score: 10.0
evidence: 用于智能体记忆优化的强化学习
tldr: "MemFactory是首个针对记忆增强型智能体设计的统一训练与推理框架。针对当前记忆管理研究碎片化、缺乏通用基础设施的问题，该框架借鉴LLaMA-Factory的模块化理念，将记忆生命周期抽象为可插拔的原子组件。通过集成GRPO算法，MemFactory支持对记忆提取、更新和检索策略进行强化学习微调，在MemAgent等架构上实现了显著的性能提升，最高增益达14.8%。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29493v2/fig-001.webp\", \"caption\": \"Figure 1 The overall architecture of MemFactory, illustrating the interdependencies among the Modules, Agent, Environment, and Trainer layers.\", \"page\": 4, \"index\": 1, \"width\": 929, \"height\": 482}, {\"url\": \"assets/figures/arxiv/2603.29493v2/fig-002.webp\", \"caption\": \"Table 1 Standard interfaces supported by modules in the Module Layer.\", \"page\": 5, \"index\": 2, \"width\": 968, \"height\": 219}]"
motivation: 现有的记忆增强型智能体实现高度碎片化且缺乏统一的训练与推理基础设施，阻碍了复杂记忆管理策略的开发与集成。
method: 提出模块化框架MemFactory，将记忆操作抽象为“乐高式”组件，并利用组相对策略优化（GRPO）算法驱动多维度环境奖励下的记忆策略微调。
result: "在MemAgent架构上的实验验证显示，该框架在域内和分布外测试集上均优于基座模型，性能相对提升最高达14.8%。"
conclusion: MemFactory通过提供标准化、可扩展且易用的基础设施，显著降低了记忆增强型智能体的研究门槛，为未来AI智能体创新奠定了基础。
---

## 摘要
记忆增强的大语言模型（LLMs）对于开发具备强大能力的长期 AI 智能体至关重要。近年来，应用强化学习（RL）来优化记忆操作（如提取、更新和检索）已成为一个极具前景的研究方向。然而，现有的实现方式仍然高度碎片化且针对特定任务，缺乏统一的基础设施来简化这些复杂流水线的集成、训练和评估。为填补这一空白，我们提出了 MemFactory，这是首个专为记忆增强智能体设计的统一、高度模块化的训练与推理框架。受 LLaMA-Factory 等统一微调框架成功的启发，MemFactory 将记忆生命周期抽象为原子化的、即插即用的组件，使研究人员能够通过“乐高式”架构无缝构建自定义记忆智能体。此外，该框架原生集成了群体相对策略优化（GRPO），以在多维环境奖励的驱动下微调内部记忆管理策略。MemFactory 为 Memory-R1、RMM 和 MemAgent 等近期前沿范式提供开箱即用的支持。我们在开源的 MemAgent 架构上，利用其公开的训练和评估数据对 MemFactory 进行了实证验证。在域内和分布外评估集上，MemFactory 相比相应的基座模型均能持续提升性能，相对增益最高达 14.8%。通过提供标准化、可扩展且易于使用的基础设施，MemFactory 显著降低了准入门槛，为记忆驱动型 AI 智能体的未来创新铺平了道路。

## Abstract
Memory-augmented Large Language Models (LLMs) are essential for developing capable, long-term AI agents. Recently, applying Reinforcement Learning (RL) to optimize memory operations, such as extraction, updating, and retrieval, has emerged as a highly promising research direction. However, existing implementations remain highly fragmented and task-specific, lacking a unified infrastructure to streamline the integration, training, and evaluation of these complex pipelines. To address this gap, we present MemFactory, the first unified, highly modular training and inference framework specifically designed for memory-augmented agents. Inspired by the success of unified fine-tuning frameworks like LLaMA-Factory, MemFactory abstracts the memory lifecycle into atomic, plug-and-play components, enabling researchers to seamlessly construct custom memory agents via a "Lego-like" architecture. Furthermore, the framework natively integrates Group Relative Policy Optimization (GRPO) to fine-tune internal memory management policies driven by multi-dimensional environmental rewards. MemFactory provides out-of-the-box support for recent cutting-edge paradigms, including Memory-R1, RMM, and MemAgent. We empirically validate MemFactory on the open-source MemAgent architecture using its publicly available training and evaluation data. Across both in-domain and out-of-distribution evaluation sets, MemFactory consistently improves performance over the corresponding base models, with relative gains of up to 14.8%. By providing a standardized, extensible, and easy-to-use infrastructure, MemFactory significantly lowers the barrier to entry, paving the way for future innovations in memory-driven AI agents.

---

## 论文详细总结（自动生成）

### MemFactory: 智能体记忆的统一推理与训练框架论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：虽然强化学习（RL）在优化记忆增强型大模型（如记忆提取、更新、检索）方面展现出巨大潜力，但现有的研究实现高度碎片化、任务耦合度高，且缺乏一个像 LLaMA-Factory 那样统一、模块化且易用的基础设施来简化记忆智能体的开发、训练和评估。
*   **研究背景**：传统的记忆管理依赖启发式规则，难以处理长期交互中的冗余和冲突。近期虽涌现出 Memory-R1、MemAgent 等利用 RL 优化记忆策略的工作，但开发者复现或组合不同模块（如更换检索器）面临极大的工程挑战。

#### 2. 方法论
MemFactory 提出了一个四层架构，将复杂的记忆生命周期抽象为“乐高式”的原子组件：
*   **模块层 (Module Layer)**：定义了记忆操作的原子类，包括：
    *   **Extractor (提取器)**：从原始上下文解析事实和经验。
    *   **Updater (更新器)**：执行 ADD、DEL、UPDATE、NONE 等操作维护记忆库。
    *   **Retriever (检索器)**：基于语义或重排序（Rerank）获取相关记忆。
    *   **Agent Module**：支持端到端的循环记忆策略（如 MemAgent 范式）。
*   **智能体层 (Agent Layer)**：负责模块的组装与策略执行，支持训练模式（generate/rollout）和纯推理模式。
*   **环境层 (Environment Layer)**：充当数据加载器和奖励管理器，提供格式奖励、LLM-as-a-Judge 等多维度信号。
*   **训练层 (Trainer Layer)**：核心优化引擎，原生集成 **GRPO (Group Relative Policy Optimization)** 算法。GRPO 通过组内奖励归一化估计基准，无需额外的 Critic 网络，显著降低了长上下文训练时的显存开销。

#### 3. 实验设计
*   **实验对象**：以 MemAgent 架构为核心，验证框架的有效性。
*   **数据集/场景**：使用 MemAgent 公开的训练和评估数据，包括两个主任务测试集（eval_50, eval_100）和一个分布外（OOD）测试集（eval_fwe_16384）。
*   **基准模型 (Benchmark)**：对比了 Qwen3-1.7B 和 Qwen3-4B-Instruct 在 RL 微调前后的表现。
*   **对比范式**：框架原生支持并对比了 Memory-R1、MemAgent 和 RMM 等前沿记忆管理范式。

#### 4. 资源与算力
*   **硬件配置**：整个训练和评估流水线可以在 **单台 NVIDIA A800 80GB GPU** 上执行。
*   **训练细节**：每个模型训练 250 个有效步骤（valid steps）。为了提高效率，训练样本的上下文长度被简化为原始长度的约 1/3（每样本包含 50-80 个文档）。

#### 5. 实验数量与充分性
*   **实验规模**：针对两个不同规模的基座模型进行了实验，并在三个不同性质的测试集上进行了验证。
*   **客观性保障**：所有评分均采用 4 次独立试验的平均值（avg@4），以减少生成随机性带来的偏差。
*   **充分性**：实验涵盖了域内和分布外场景，验证了框架的泛化能力。虽然实验主要集中在 MemAgent 架构上，但其对 Memory-R1 和 RMM 的原生支持证明了其广泛的适用性。

#### 6. 主要结论与发现
*   **性能提升**：MemFactory 显著提升了基座模型的记忆管理能力。Qwen3-1.7B 的平均性能提升了 **14.8%**；Qwen3-4B-Instruct 提升了 **7.3%**。
*   **泛化能力**：较大规模的模型（4B）在 OOD 测试集上表现出更稳健的提升，表明 RL 优化的记忆策略具有良好的迁移性。
*   **框架价值**：证明了通过统一的模块化框架和显存高效的 GRPO 算法，可以在有限算力下实现复杂的记忆策略优化。

#### 7. 优点
*   **高度模块化**：通过“乐高式”设计，研究人员可以轻松更换提取、更新或检索模块，极大地降低了实验门槛。
*   **显存友好**：集成 GRPO 算法，解决了长上下文记忆任务中显存容易溢出的痛点。
*   **开箱即用**：预置了多种 SOTA 记忆智能体实现，方便社区快速复现和改进。

#### 8. 不足与局限
*   **范式覆盖有限**：目前仅涵盖了三种代表性范式，对于更复杂的分布式记忆或多模态记忆支持尚待开发。
*   **训练效率**：虽然支持单卡训练，但在处理极长上下文时的吞吐量仍有优化空间。
*   **小模型瓶颈**：实验显示小模型（1.7B）在 OOD 场景下的表现仍不稳定，可能需要更精细的奖励设计或更大规模的预训练支持。

（完）
