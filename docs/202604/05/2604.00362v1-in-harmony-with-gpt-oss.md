---
title: In harmony with gpt-oss
title_zh: 与 gpt-oss 协同
authors: Borislav Mavrin
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00362v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 逆向工程工具调用并构建原生智能体工具链
tldr: 本研究针对 OpenAI 未公开 gpt-oss-20b 工具和代理框架导致无法复现其评分的问题，通过逆向工程识别了模型训练分布中的原生工具调用模式。作者开发了 Harmony 代理框架，采用模型原生消息格式以减少转换损失。该方法成功实现了对 OpenAI 官方分数的首次独立复现，在 SWE Verified 和 AIME25 等基准测试中达到了与官方发布值高度一致的结果。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00362v1/fig-001.webp\", \"caption\": \"Table 4: Discovered in-distribution repo_browser tools with canonical schemas, plus container.exec as a flat tool.\", \"page\": 3, \"index\": 1, \"width\": 977, \"height\": 181}, {\"url\": \"assets/figures/arxiv/2604.00362v1/fig-002.webp\", \"caption\": \"Table 3: Schema robustness: parameter presence rates when the model is asked to call each tool by name (Step 5, 95% bootstrap CIs).\", \"page\": 3, \"index\": 2, \"width\": 984, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2604.00362v1/fig-003.webp\", \"caption\": \"Table 2: Cross-reference of text-mentioned repo_browser tools versus actual tool calls (n = 160 text samples, n = 1201 baseline samples). Tools with both text mentions and observed calls are confirmed; tools described but never called are confabulations.\", \"page\": 3, \"index\": 3, \"width\": 976, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2604.00362v1/fig-004.webp\", \"caption\": \"Figure 1: Harmony agent.\", \"page\": 5, \"index\": 4, \"width\": 786, \"height\": 779}, {\"url\": \"assets/figures/arxiv/2604.00362v1/fig-005.webp\", \"caption\": \"Figure 2: Harmony vs chat completions token usage.\", \"page\": 5, \"index\": 5, \"width\": 959, \"height\": 363}]"
motivation: 解决因 OpenAI 未公开工具定义和代理框架而导致 gpt-oss-20b 官方性能指标无法被独立复现的问题。
method: 通过逆向工程识别模型内置的工具调用先验，并构建 Harmony 代理框架以原生格式编码消息，绕过损耗较大的聊天补全转换。
result: "成功复现了官方得分，在 SWE Verified HIGH/MEDIUM 分别达到 60.4% 和 53.3%，在 AIME25 达到 91.7%。"
conclusion: 证明了通过还原模型训练分布中的原生工具和格式，可以实现对闭源模型高性能指标的精确独立复现。
---

## 摘要
尚无人能独立复现 OpenAI 发布的 gpt-oss-20b 在使用工具情况下的评分，因为原始论文既未公开工具，也未公开智能体框架（agent harness）。我们对该模型的分布内（in-distribution）工具进行了逆向工程：当在没有工具定义的情况下进行提示时，gpt-oss 仍能以极高的统计置信度调用其训练分布中的工具——这是一种强大的先验，而非幻觉。随后，我们构建了一个原生的 harmony 智能体框架（https://github.com/borislavmavrin/harmonyagent.git），该框架以模型的原生格式对消息进行编码，从而绕过了有损的聊天补全（Chat Completions）转换。结合这些工作，我们首次独立复现了 OpenAI 发布的评分：在 SWE Verified HIGH 上达到 60.4%（发布值为 60.7%），MEDIUM 为 53.3%（53.2%），以及在 AIME25 工具辅助测试中达到 91.7%（90.4%）。

## Abstract
No one has independently reproduced OpenAI's published scores for gpt-oss-20b with tools, because the original paper discloses neither the tools nor the agent harness. We reverse-engineered the model's in-distribution tools: when prompted without tool definitions, gpt-oss still calls tools from its training distribution with high statistical confidence -- a strong prior, not a hallucination. We then built a native harmony agent harness (https://github.com/borislavmavrin/harmonyagent.git) that encodes messages in the model's native format, bypassing the lossy Chat Completions conversion. Together, these yield the first independent reproduction of OpenAI's published scores: 60.4% on SWE Verified HIGH (published 60.7%), 53.3% MEDIUM (53.2%), and 91.7% on AIME25 with tools (90.4%).

---

## 论文详细总结（自动生成）

这是一份关于论文《In harmony with gpt-oss》的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
论文的核心问题是**解决 OpenAI 的开源模型 `gpt-oss-20b` 官方发布成绩无法被第三方独立复现的问题**。
*   **背景**：尽管 `gpt-oss-20b` 在发布时声称在编程（SWE-bench）和数学（AIME25）任务上表现卓越，但社区复现的分数远低于官方值（例如 SWE-bench 官方 60.7% vs 社区 26%）。
*   **动机**：作者发现这种差距并非源于模型能力不足，而是因为 OpenAI 未公开其使用的**工具集（Tools）**和**智能体框架（Agent Harness）**。该模型在训练时深度绑定了特定的“Harmony”消息格式和内置工具，使用通用的聊天接口（Chat Completions）会导致严重的性能损耗。

### 2. 论文提出的方法论
作者通过两步走战略还原了模型的真实性能：

*   **工具逆向工程（Reverse-Engineering Tools）**：
    *   **核心思想**：利用模型的“训练先验”。当不提供工具定义时，模型仍会尝试调用训练集中存在的工具（这被误认为是幻觉，实则是先验）。
    *   **流程**：
        1.  从原始日志中提取意图-推理-工具元组。
        2.  在无工具定义下诱导模型调用，识别出高频工具名（如 `container.exec`, `repo_browser.print_tree`）。
        3.  通过针对性采样确定参数架构（Schema）。
        4.  交叉验证文本描述与实际调用，过滤掉虚假的工具参数。
    *   **最终确定的工具箱**：包括目录列表（`print_tree`）、内容搜索（`search`）、文件读取（`open_file`）、补丁应用（`apply_patch`）以及 Shell 执行（`container.exec`）。

*   **Harmony 智能体框架构建**：
    *   **原生格式编码**：直接使用 Harmony 的原生 Token 格式（如 `<|start|>`, `<|message|>`），避免了 Chat Completions 转换带来的信息丢失。
    *   **多通道处理**：模型在 `analysis`（思维链）、`commentary`（工具调用）和 `final`（最终答案）三个通道上输出，框架据此进行路由和执行。
    *   **异常处理系统**：建立了一套区分“可恢复”与“终止性”错误的异常机制，允许模型在格式微偏时通过重试自我修正。

### 3. 实验设计
*   **数据集/场景**：
    *   **SWE Verified**：评估模型解决真实 GitHub 问题的能力（分为 MEDIUM 和 HIGH 两种推理强度）。
    *   **AIME 2025**：评估模型在工具辅助下的高难度数学竞赛解题能力。
*   **Benchmark 与对比对象**：
    *   对比 OpenAI 官方发布的 `gpt-oss-20b` 分数。
    *   对比社区在 SWE Leaderboard 上的现有复现分数。
    *   对比了 Harmony 格式与标准 Chat Completions 格式在 Token 消耗和上下文溢出率上的差异。

### 4. 资源与算力
*   **推理框架**：使用 `vLLM 0.14.1`。
*   **配置参数**：`tensor-parallel-size 1`（单卡并行），`max-model-len 131072`（128k 上下文）。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号（如 A100 或 H100）及总训练/推理时长，但提到该模型设计目标是适配 16GB VRAM 的消费级显卡（尽管 Agent 模式下 16GB 并不够用）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在工具发现阶段使用了超过 1200 个基准样本和 5000 多个意图元组。
    *   在最终评估中，对 SWE Verified 的 500 个实例进行了测试。
*   **统计严谨性**：所有结果均报告了 **95% Bootstrap 置信区间**（1000 次重采样），确保了结果的统计显著性。
*   **充分性评价**：实验设计较为充分，不仅复现了最终分数，还通过消融实验证明了工具定义在“系统消息”而非“开发者消息”中的重要性，以及原生格式对节省 Token 的贡献。

### 6. 论文的主要结论与发现
*   **复现成功**：通过还原工具和原生格式，作者在 SWE Verified HIGH 上达到 60.4%（官方 60.7%），在 AIME25 上达到 91.7%（官方 90.4%），首次独立证实了官方数据的真实性。
*   **框架决定论**：智能体框架（Harness）的差异会导致性能出现数十个百分点的波动。`gpt-oss-120b` 在排行榜上表现差是因为它在不断调用未定义的工具（如 `apply_patch`）。
*   **Token 效率**：Harmony 格式在处理长任务时比 Chat Completions 格式更节省 Token，尤其在高强度推理下，后者会导致严重的上下文溢出（溢出率从 2.8% 飙升至 62.2%）。

### 7. 优点
*   **方法新颖**：将模型的“幻觉”转化为逆向工程的线索，系统性地还原了闭源工具链。
*   **实用性强**：开源了 `harmonyagent` 框架，为其他研究者使用 `gpt-oss` 系列模型提供了高性能的基础设施。
*   **揭示本质**：深刻揭示了模型能力与部署框架之间的耦合关系，对评估开源模型具有指导意义。

### 8. 不足与局限
*   **模型局限性**：该方法高度依赖于对特定模型（gpt-oss）的逆向工程，其工具集可能不适用于其他厂商的开源模型。
*   **数据污染风险**：作者在文中承认了 SWE Verified 存在潜在的数据污染问题，但在实验中将其排除在讨论范围之外，这可能影响对模型真实泛化能力的判断。
*   **复杂性**：原生 Harmony 格式的实现比标准 API 复杂得多，增加了普通用户的部署门槛。

（完）
