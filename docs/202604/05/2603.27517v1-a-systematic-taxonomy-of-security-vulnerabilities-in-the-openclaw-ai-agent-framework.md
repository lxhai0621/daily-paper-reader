---
title: A Systematic Taxonomy of Security Vulnerabilities in the OpenClaw AI Agent Framework
title_zh: OpenClaw AI 智能体框架安全漏洞的系统性分类研究
authors: "Surada Suwansathit, Yuxuan Zhang, Guofei Gu"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27517v1"
tags: ["query:ma-kf"]
score: 6.0
evidence: 连接推理与执行的AI智能体框架中的安全漏洞
tldr: 本研究针对开源AI智能体框架OpenClaw，系统性地分析了190项安全漏洞，并构建了涵盖架构层级和攻击维度的分类法。研究揭示了从LLM工具调用到宿主机的远程代码执行（RCE）路径，指出了命令过滤机制的局限性以及插件分发中的安全漏洞。核心贡献在于识别了跨层级攻击的韧性，并强调了统一策略边界在AI智能体安全中的重要性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.27517v1/fig-001.webp\", \"caption\": \"Figure 3: Severity distribution by attack surface. The Exec Policy Engine dominates in volume (46 advisories, 24.2%) while the Tool Dispatch Interface and Browser Tooling surface shows the highest proportion of High-severity findings relative to its total.\", \"page\": 6, \"index\": 1, \"width\": 882, \"height\": 526}, {\"url\": \"assets/figures/arxiv/2603.27517v1/fig-002.webp\", \"caption\": \"Figure 4: Two-axis taxonomy matrix mapping OpenClaw attack surfaces (rows) to kill chain stages (columns). Filled circles indicate that the surface is exploitable at that stage.\", \"page\": 8, \"index\": 2, \"width\": 961, \"height\": 602}, {\"url\": \"assets/figures/arxiv/2603.27517v1/fig-003.webp\", \"caption\": \"Figure 1: OpenClaw system architecture with attack surfaces mapped to each component. Solid boxes represent system components; dashed orange regions denote attack surfaces from the taxonomy in Table 4.\", \"page\": 4, \"index\": 3, \"width\": 976, \"height\": 393}, {\"url\": \"assets/figures/arxiv/2603.27517v1/fig-004.webp\", \"caption\": \"Figure 5: OpenClaw Kill Chain\", \"page\": 9, \"index\": 4, \"width\": 979, \"height\": 266}, {\"url\": \"assets/figures/arxiv/2603.27517v1/fig-005.webp\", \"caption\": \"Figure 2: Advisory disclosure timeline. The two distinct waves correspond to an initial coordinated audit (Jan 31–Feb 16) and an accelerated follow-on discovery phase (Feb 18–25) that more than doubled the advisory corpus while patching from the first wave was actively in progress.\", \"page\": 5, \"index\": 5, \"width\": 882, \"height\": 439}]"
motivation: 旨在解决AI智能体框架在连接LLM推理与宿主执行环境时，所面临的独特且复杂的安全挑战。
method: 通过对OpenClaw框架的190个安全公告进行系统分类，从系统架构轴和攻击技术轴两个维度构建漏洞分类法。
result: 发现了可导致未经身份验证的远程代码执行路径，并证明了基于词法解析的命令白名单机制及插件分发渠道存在严重缺陷。
conclusion: AI智能体框架的主要结构性弱点在于缺乏统一的策略边界，导致各层独立的信任执行机制难以抵御跨层级攻击。
---

## 摘要
将大语言模型 (LLM) 推理连接到宿主执行面（如 shell、文件系统、容器和消息传递）的 AI 智能体框架，引入了在结构上与传统软件截然不同的安全挑战。我们针对开源 AI 智能体运行时 OpenClaw 的 190 条安全公告，按架构层级和信任违规类型整理，提出了一个系统性的分类体系。漏洞沿两个正交轴聚集：(1) 系统轴，反映架构层级（执行策略、网关、通道、沙箱、浏览器、插件、智能体/提示词）；(2) 攻击轴，反映对抗技术（身份欺骗、策略绕过、跨层组合、提示词注入、供应链提权）。补丁差异证据得出了三个主要发现。首先，网关和节点-宿主子系统中的三个中高危公告组合成了一条完整的未经身份验证的远程代码执行 (RCE) 路径，涵盖了从 LLM 工具调用到宿主进程的投递、利用和命令控制。其次，作为主要命令过滤机制的执行白名单依赖于一个封闭世界假设，即命令身份可以通过词法解析恢复。这一假设被 shell 换行符、busybox 多路复用和 GNU 选项缩写所失效。第三，通过插件通道分发的恶意技能在 LLM 上下文中执行了一个两阶段下载器 (dropper)，绕过了执行流水线，证明了技能分发面缺乏运行时策略强制执行。主要的结构性弱点是逐层信任强制执行而非统一的策略边界，这使得跨层攻击对局部修复具有免疫力。

## Abstract
AI agent frameworks connecting large language model (LLM) reasoning to host execution surfaces--shell, filesystem, containers, and messaging--introduce security challenges structurally distinct from conventional software. We present a systematic taxonomy of 190 advisories filed against OpenClaw, an open-source AI agent runtime, organized by architectural layer and trust-violation type. Vulnerabilities cluster along two orthogonal axes: (1) the system axis, reflecting the architectural layer (exec policy, gateway, channel, sandbox, browser, plugin, agent/prompt); and (2) the attack axis, reflecting adversarial techniques (identity spoofing, policy bypass, cross-layer composition, prompt injection, supply-chain escalation).   Patch-differential evidence yields three principal findings. First, three Moderate- or High-severity advisories in the Gateway and Node-Host subsystems compose into a complete unauthenticated remote code execution (RCE) path--spanning delivery, exploitation, and command-and-control--from an LLM tool call to the host process. Second, the exec allowlist, the primary command-filtering mechanism, relies on a closed-world assumption that command identity is recoverable via lexical parsing. This is invalidated by shell line continuation, busybox multiplexing, and GNU option abbreviation. Third, a malicious skill distributed via the plugin channel executed a two-stage dropper within the LLM context, bypassing the exec pipeline and demonstrating that the skill distribution surface lacks runtime policy enforcement. The dominant structural weakness is per-layer trust enforcement rather than unified policy boundaries, making cross-layer attacks resilient to local remediation.