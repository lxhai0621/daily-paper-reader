---
title: "MemFactory: Unified Inference & Training Framework for Agent Memory"
title_zh: MemFactory：智能体记忆的统一推理与训练框架
authors: "Ziliang Guo, Ziheng Li, Zhiyu Li"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29493v1"
tags: ["query:agent"]
score: 10.0
evidence: 利用强化学习优化智能体记忆操作
tldr: "MemFactory是首个专为记忆增强型智能体设计的统一、模块化训练与推理框架。针对现有记忆管理实现碎片化的问题，它通过“乐高式”架构抽象记忆生命周期，并集成GRPO算法优化记忆策略。该框架支持Memory-R1等前沿范式，在MemAgent基准测试中显著提升了模型性能，最高增幅达14.8%，极大降低了记忆驱动AI的研究门槛。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29493v1/fig-001.webp\", \"caption\": \"Figure 1 The overall architecture of MemFactory, illustrating the interdependencies among the Modules, Agent, Environment, and Trainer layers.\", \"page\": 4, \"index\": 1, \"width\": 929, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2603.29493v1/fig-002.webp\", \"caption\": \"Table 1 Standard interfaces supported by modules in the Module Layer.\", \"page\": 5, \"index\": 2, \"width\": 968, \"height\": 221}]"
motivation: 现有的记忆增强型大模型实现方案过于碎片化且缺乏统一的基础设施，阻碍了记忆管理策略的系统性优化与集成。
method: 提出MemFactory框架，采用模块化组件构建记忆生命周期，并利用组相对策略优化（GRPO）算法根据环境奖励微调内部记忆管理策略。
result: "在MemAgent架构上的实验表明，MemFactory在域内和分布外评估集中均优于基础模型，性能相对提升最高达14.8%。"
conclusion: MemFactory通过提供标准化且易用的基础设施，为未来记忆驱动的AI智能体创新铺平了道路。
---

## 摘要
记忆增强的大语言模型（LLMs）对于开发具备能力的长期 AI 智能体至关重要。最近，应用强化学习（RL）来优化记忆操作（如提取、更新和检索）已成为一个极具前景的研究方向。然而，现有的实现仍然高度碎片化且针对特定任务，缺乏统一的基础设施来简化这些复杂流水线的集成、训练和评估。为了填补这一空白，我们提出了 MemFactory，这是首个专门为记忆增强智能体设计的统一、高度模块化的训练与推理框架。受 LLaMA-Factory 等统一微调框架成功的启发，MemFactory 将记忆生命周期抽象为原子的、即插即用的组件，使研究人员能够通过“乐高式”架构无缝构建自定义记忆智能体。此外，该框架原生集成了群体相对策略优化（GRPO），以在多维环境奖励的驱动下微调内部记忆管理策略。MemFactory 为最近的前沿范式提供开箱即用的支持，包括 Memory-R1、RMM 和 MemAgent。我们在开源的 MemAgent 架构上，使用其公开的训练和评估数据对 MemFactory 进行了实证验证。在域内和分布外评估集中，MemFactory 相比相应的基座模型持续提升了性能，相对增益高达 14.8%。通过提供标准化、可扩展且易于使用的基础设施，MemFactory 显著降低了准入门槛，为记忆驱动的 AI 智能体的未来创新铺平了道路。

## Abstract
Memory-augmented Large Language Models (LLMs) are essential for developing capable, long-term AI agents. Recently, applying Reinforcement Learning (RL) to optimize memory operations, such as extraction, updating, and retrieval, has emerged as a highly promising research direction. However, existing implementations remain highly fragmented and task-specific, lacking a unified infrastructure to streamline the integration, training, and evaluation of these complex pipelines. To address this gap, we present MemFactory, the first unified, highly modular training and inference framework specifically designed for memory-augmented agents. Inspired by the success of unified fine-tuning frameworks like LLaMA-Factory, MemFactory abstracts the memory lifecycle into atomic, plug-and-play components, enabling researchers to seamlessly construct custom memory agents via a "Lego-like" architecture. Furthermore, the framework natively integrates Group Relative Policy Optimization (GRPO) to fine-tune internal memory management policies driven by multi-dimensional environmental rewards. MemFactory provides out-of-the-box support for recent cutting-edge paradigms, including Memory-R1, RMM, and MemAgent. We empirically validate MemFactory on the open-source MemAgent architecture using its publicly available training and evaluation data. Across both in-domain and out-of-distribution evaluation sets, MemFactory consistently improves performance over the corresponding base models, with relative gains of up to 14.8%. By providing a standardized, extensible, and easy-to-use infrastructure, MemFactory significantly lowers the barrier to entry, paving the way for future innovations in memory-driven AI agents.

---

## 论文详细总结（自动生成）

### MemFactory: 智能体记忆的统一推理与训练框架论文总结

#### 1. 论文的核心问题与研究动机
*   **核心问题**：虽然记忆增强型大语言模型（LLM）是构建长期自主智能体的关键，但现有的记忆管理研究（如利用强化学习优化记忆的提取、更新和检索）存在严重的**碎片化**问题。
*   **研究动机**：
    *   现有的记忆策略（如 Memory-R1, MemAgent）实现高度定制化，代码库孤立，难以复用或组合。
    *   缺乏像 LLaMA-Factory 那样统一、模块化且易于使用的基础设施来简化记忆智能体的开发、训练和评估流程。
    *   长上下文任务对显存要求极高，传统的强化学习算法（如 PPO）因需要额外的评论家网络（Critic）而难以在有限资源下运行。

#### 2. 核心方法论
MemFactory 提出了一个四层架构的统一框架，旨在实现“乐高式”的记忆智能体构建：
*   **模块层 (Module Layer)**：将记忆生命周期抽象为原子操作，包括：
    *   **Extractor**：从原始上下文中解析事实。
    *   **Updater**：执行增删改（ADD/DEL/UPDATE）操作维护记忆库。
    *   **Retriever**：负责语义检索或利用推理模型进行重排序（Rerank）。
    *   **Agent Module**：支持端到端的循环记忆（Recurrent Memory）策略。
*   **智能体层 (Agent Layer)**：作为策略执行器，通过组合上述模块构建复杂的记忆工作流，支持训练模式（Rollout）和纯推理模式。
*   **环境层 (Environment Layer)**：负责数据加载和多维奖励管理（如格式奖励、LLM-as-a-Judge 评分）。
*   **训练层 (Trainer Layer)**：核心优化引擎，原生集成 **GRPO (Group Relative Policy Optimization)** 算法。
    *   **关键技术**：GRPO 通过对一组采样响应的奖励进行归一化来估计优势函数（Advantage），公式为 $\hat{A}_i = \frac{r_i - \text{mean}(r)}{\text{std}(r)}$。该算法无需 Critic 网络，显著降低了显存开销。

#### 3. 实验设计
*   **实验场景**：基于 **MemAgent** 架构进行实证研究，验证框架在循环记忆策略优化上的表现。
*   **数据集/Benchmark**：
    *   使用 MemAgent 公开的训练和评估数据。
    *   **评估集**：包括两个域内测试集（eval_50, eval_100）和一个分布外（OOD）测试集（eval_fwe_16384）。
*   **对比方法**：对比了 Qwen3-1.7B 和 Qwen3-4B-Instruct 在经过 MemFactory RL 微调前后的性能表现。

#### 4. 资源与算力
*   **硬件设备**：整个训练和评估流程可以在**单张 NVIDIA A800 80GB GPU** 上完成。
*   **训练细节**：每个模型训练 250 个有效步数（steps）。为了提高效率，训练样本的上下文长度被简化为原始长度的约 1/3（每样本包含 50-80 个文档）。

#### 5. 实验数量与充分性
*   **实验规模**：针对两种不同规模的模型（1.7B 和 4B）进行了实验，涵盖了域内和跨域（OOD）场景。
*   **客观性保障**：为了应对生成的不确定性，所有评分均采用 4 次独立试验的平均值（avg@4）。
*   **充分性评价**：实验设计较为合理，验证了框架在不同规模模型上的增益以及泛化能力。虽然实证部分主要集中在 MemAgent 这一种架构上，但框架本身提供了对 Memory-R1 和 RMM 的开箱即用支持，展示了其通用性。

#### 6. 主要结论与发现
*   **性能提升**：MemFactory 显著提升了基座模型的记忆管理能力。Qwen3-1.7B 的平均得分提升了 **14.8%**，Qwen3-4B-Instruct 提升了 **7.3%**。
*   **模型规模效应**：较大的模型（4B）在 OOD 任务上表现出更强的鲁棒性和策略迁移能力，而较小模型在处理未见过的分布时性能略有下降。
*   **框架有效性**：证明了即使在有限的算力资源和简化数据下，通过统一的 RL 框架也能有效优化复杂的记忆策略。

#### 7. 优点与亮点
*   **高度模块化**：通过“乐高式”设计，研究人员可以轻松更换检索器或更新器，极大地降低了实验门槛。
*   **显存友好**：集成 GRPO 算法，解决了长上下文记忆训练中的显存瓶颈问题。
*   **生态兼容**：支持主流开源模型和推理引擎（如 vLLM），并集成了 SwanLab 等监控工具。
*   **前沿集成**：预置了多种 SOTA 记忆算法实现，方便研究者进行基准对比。

#### 8. 不足与局限
*   **实证覆盖有限**：论文的实验部分主要聚焦于 MemAgent 架构，对于 Memory-R1 和 RMM 等其他内置架构的实证对比数据较少。
*   **训练效率**：虽然支持单卡训练，但在处理极长上下文时的训练吞吐量仍有提升空间。
*   **范式覆盖**：目前仅涵盖了当前主流的几种记忆范式，对于更新颖或更复杂的异构记忆系统支持尚待扩展。

（完）
