---
title: "OntoKG: Ontology-Oriented Knowledge Graph Construction with Intrinsic-Relational Routing"
title_zh: OntoKG：基于内在-关系路由的面向本体的知识图谱构建
authors: "Yitao Li, Zhanlin Liu, Anuranjan Pandey, Muni Srikanth"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02618v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 面向本体的知识图谱构建与 LLM 引导的提取
tldr: 本研究针对现有知识图谱模式与构建过程耦合度高、难以复用的问题，提出了OntoKG框架。该框架采用“本质-关系路由”机制，将属性分类为本质属性或关系属性，从而构建出面向本体的声明式模式。通过在2026年1月的Wikidata数据上进行验证，成功构建了包含3400万节点的大规模属性图，证明了该模式在实体消歧、领域定制及大模型引导提取等任务中的独立复用价值。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.02618v1/fig-001.webp\", \"caption\": \"Table 2: Three-way agreement on 5,429 shared entities (matched by Wikidata QID) between AIDA-YAGO and CleanCoNLL. The YAML classifier has no NER-specific tuning; a complete case-level breakdown of disagreement categories is provided in the project repository.\", \"page\": 11, \"index\": 1, \"width\": 686, \"height\": 207}, {\"url\": \"assets/figures/arxiv/2604.02618v1/fig-002.webp\", \"caption\": \"Table 5: Decomposition of the education module into 9 domain facets.\", \"page\": 15, \"index\": 2, \"width\": 698, \"height\": 287}, {\"url\": \"assets/figures/arxiv/2604.02618v1/fig-003.webp\", \"caption\": \"Figure 1: Overview of the schema-centered knowledge graph ecosystem. A raw knowledge graph (left) is transformed through an intrinsic-relational classification framework into a typed property graph (right). The declarative schema (center) serves as the core artifact, enabling downstream applications including entity disambiguation, domain customization, and LLM-guided extraction.\", \"page\": 2, \"index\": 3, \"width\": 979, \"height\": 425}, {\"url\": \"assets/figures/arxiv/2604.02618v1/fig-004.webp\", \"caption\": \"Table 1: Property organization for Apple Inc. (Q312), showing selected properties from a live Wikidata query (248 total). Core and intrinsic properties map to tabular output; relational properties become typed graph edges.\", \"page\": 10, \"index\": 4, \"width\": 894, \"height\": 413}, {\"url\": \"assets/figures/arxiv/2604.02618v1/fig-005.webp\", \"caption\": \"Figure 3: Governance domain subgraph extracted by selecting three relational modules: government (green), legal (red), and politics (purple). The subgraph spans four categories—People, Knowledge, Organizations, and Events— with edge labels showing the underlying Wikidata properties (e.g., position held, political party, member of).\", \"page\": 10, \"index\": 5, \"width\": 581, \"height\": 443}, {\"url\": \"assets/figures/arxiv/2604.02618v1/fig-006.webp\", \"caption\": \"Figure 2: Bipartite view of 8 categories (top) connected to 18 cross-category relational modules with span ≥ 3 (bottom). Badges indicate category span. Two additional modules (affiliation and technology, each spanning 2 categories) are omitted for clarity. The full schema comprises 56 intrinsic modules, 18 single-category relational modules, and 20 cross-category relational modules.\", \"page\": 8, \"index\": 6, \"width\": 920, \"height\": 555}]"
motivation: 现有的知识图谱构建方法产生的模式往往与特定流程紧密耦合，导致其在下游本体分析任务中难以独立复用。
method: 核心机制是“本质-关系路由”，通过将属性分类并路由至相应的模式模块，生成可移植的声明式模式。
result: "在Wikidata数据集上实现了93.3%的类别覆盖率，生成了拥有3400万节点和6120万条边的属性图。"
conclusion: 面向本体的构建方法能够提供独立于流水线的模式，显著提升了知识图谱在多种下游应用中的灵活性和效能。
---

## 摘要
将大规模知识图谱组织为有类型的属性图需要进行结构化决策——即哪些实体成为节点，哪些属性成为边，以及由何种模式（schema）来管理这些选择。现有方法通常将这些决策嵌入到流水线代码中或进行随机的关系提取，导致生成的模式与其构建过程紧密耦合，难以在下游本体级任务中重用。我们提出了一种面向本体的方法，其中模式从一开始就是为本体分析、实体消歧、领域定制和 LLM 引导的提取而设计的，而不仅仅是图构建的副产品。其核心机制是内在-关系路由（intrinsic-relational routing），它将每个属性分类为内在属性或关系属性，并将其路由到相应的模式模块。这种路由产生了一个声明式模式，该模式可跨存储后端移植并可独立重用。我们在 2026 年 1 月的 Wikidata 转储数据上实例化了该方法。通过基于规则的清洗阶段，我们从完整数据中识别出包含 3460 万个实体的核心集，随后通过迭代的内在-关系路由将每个属性分配到 8 个类别下的 94 个模块中。在工具增强的 LLM 支持和人工审核下，该模式在已分类实体中达到了 93.3% 的类别覆盖率和 98.0% 的模块分配率。导出该模式产生了一个包含 3400 万个节点和 38 种关系类型下 6120 万条边的属性图。我们通过五个独立于构建流水线使用该模式的应用验证了面向本体的主张：本体结构分析、基准标注审计、实体消歧、领域定制以及 LLM 引导的提取。

## Abstract
Organizing a large-scale knowledge graph into a typed property graph requires structural decisions -- which entities become nodes, which properties become edges, and what schema governs these choices. Existing approaches embed these decisions in pipeline code or extract relations ad hoc, producing schemas that are tightly coupled to their construction process and difficult to reuse for downstream ontology-level tasks. We present an ontology-oriented approach in which the schema is designed from the outset for ontology analysis, entity disambiguation, domain customization, and LLM-guided extraction -- not merely as a byproduct of graph building. The core mechanism is intrinsic-relational routing, which classifies every property as either intrinsic or relational and routes it to the corresponding schema module. This routing produces a declarative schema that is portable across storage backends and independently reusable.   We instantiate the approach on the January 2026 Wikidata dump. A rule-based cleaning stage identifies a 34.6M-entity core set from the full dump, followed by iterative intrinsic-relational routing that assigns each property to one of 94 modules organized into 8 categories. With tool-augmented LLM support and human review, the schema reaches 93.3% category coverage and 98.0% module assignment among classified entities. Exporting this schema yields a property graph with 34.0M nodes and 61.2M edges across 38 relationship types. We validate the ontology-oriented claim through five applications that consume the schema independently of the construction pipeline: ontology structure analysis, benchmark annotation auditing, entity disambiguation, domain customization, and LLM-guided extraction.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **OntoKG** 的框架，旨在通过一种“面向本体”的方法构建大规模知识图谱（KG）。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的知识图谱（如 Wikidata, DBpedia, YAGO）在构建过程中，结构化决策（如哪些属性作为节点属性，哪些作为图的边）通常硬编码在复杂的流水线中，或者由大语言模型（LLM）随机提取。这导致生成的模式（Schema）与构建过程高度耦合，难以在下游任务（如本体分析、实体消歧）中独立复用。
*   **研究动机**：作者希望将模式设计为“一等公民”，使其不仅是图构建的副产品，而是一个声明式的、可移植的、且能直接指导下游应用（如 LLM 提取）的核心资产。

### 2. 方法论：内在-关系路由（Intrinsic-Relational Routing）
*   **核心思想**：通过将实体的属性分类为**内在属性（Intrinsic）**或**关系属性（Relational）**，来决定图的边界。
    *   **内在属性**：作为节点的属性（Attribute），用于表格查找（如出生日期、化学式）。
    *   **关系属性**：作为可遍历的图边（Edge），连接不同实体（如雇主、父级组织）。
*   **关键技术细节**：
    *   **声明式分类模式**：使用 YAML 文件定义 8 个大类（如人物、地点、组织等）和 94 个模块。
    *   **迭代优化算法**：采用一个闭环流程。首先进行初始分类，识别未分类实体和无模块实体，然后通过“决策决策机（Oracle）”进行模式扩展。
    *   **智能体工作流（Agentic Workflow）**：利用 LLM（Claude Opus 4.6）充当模式设计师。为了防止幻觉，为 LLM 配备了接地工具（Grounding Tools），包括本地标签查找（LMDB）、实时 SPARQL 查询和模式验证器，确保决策基于真实的图数据。
    *   **数据清洗**：针对 Wikidata 中大量的自动导入数据（如科学论文、基因数据），设计了基于规则的清洗阶段，筛选出 3460 万个核心实体。

### 3. 实验设计
*   **数据集**：2026 年 1 月的 Wikidata 全量转储数据（约 1 亿个实体）。
*   **基准测试（Benchmarks）**：
    *   **实体消歧**：在 BLINK 基准测试的受控候选子集上进行。
    *   **标注审计**：使用 AIDA-YAGO 和 CleanCoNLL 数据集。
*   **对比方法**：主要对比了 **YAGO 4.5**（目前最先进的基于 Wikidata 的结构化知识库）。

### 4. 资源与算力
*   **硬件环境**：实验主要在配备 **Apple M4 Max 芯片（16 核 CPU，48 GB RAM）** 的 MacBook Pro 上运行。
*   **效率表现**：
    *   使用 Rust 编写的分类器处理 3460 万个实体仅需约 5 分钟（约 11 万实体/秒）。
    *   全量导出为属性图 CSV 文件（9.1 GB）耗时约 11 分钟。
*   **LLM 消耗**：使用了 Claude Opus 4.6 进行模式的迭代设计和优化，但未明确说明具体的 Token 消耗总量。

### 5. 实验数量与充分性
*   **实验规模**：处理了千万级规模的真实数据，而非小型采样集。
*   **应用场景**：开展了 **5 组** 独立应用实验：
    1.  本体结构分析（双向图结构可视化）。
    2.  基准标注审计（作为第三方“裁判”纠正 NER 数据集错误）。
    3.  实体消歧（对比 YAGO 4.5）。
    4.  领域定制（将教育模块细化为 9 个子领域）。
    5.  LLM 引导提取（验证模式对信息提取的指导作用）。
*   **评价**：实验设计非常充分且具有实际应用导向，通过大规模数据验证了系统的鲁棒性和模式的复用性。

### 6. 主要结论与发现
*   **覆盖率高**：在核心实体集上实现了 93.3% 的类别覆盖率和 98.0% 的模块分配率。
*   **性能提升**：在实体消歧任务中，OntoKG 的模块化类型标注比 YAGO 4.5 的宏平均准确率高出 **2.4 个百分点**，在候选者极多的困难案例中提升更显著。
*   **模式价值**：证明了声明式模式可以独立于构建流水线，直接用于纠正人工标注错误或指导 LLM 进行结构化提取。

### 7. 优点
*   **解耦设计**：将图结构决策从代码中提取到 YAML 模式中，极大地增强了可维护性和可移植性。
*   **智能体辅助**：巧妙地结合了 LLM 的推理能力和确定性工具的验证能力，解决了大规模本体构建中的人工成本问题。
*   **高性能实现**：利用 Rust 实现了极高的处理速度，使得模式的迭代和 A/B 测试变得可行。

### 8. 不足与局限
*   **Wikidata 依赖**：虽然方法论通用，但目前的实现高度依赖 Wikidata 的 P31/P279 属性体系。
*   **模式扩展性**：对于完全不在现有模块定义内的全新关系（如从非结构化文本中新提取的关系），如何自动将其合并到现有模式中仍需进一步研究。
*   **LLM 成本**：虽然分类过程是确定性的，但模式的初始设计和迭代优化仍依赖高昂的 LLM 调用。

（完）
