---
title: "RAGShield: Detecting Numerical Claim Manipulation in Government RAG Systems"
title_zh: RAGShield：检测政府 RAG 系统中的数值主张篡改
authors: KrishnaSaiReddy Patil
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00387v2"
tags: ["query:ma-kf"]
score: 9.0
evidence: 检测RAG系统中的数值篡改并减少错误
tldr: "针对政府RAG系统在税务和福利咨询中对数值篡改极其敏感的问题，本文揭示了基于嵌入的防御手段在数值精度上的盲点。提出了RAGShield，通过模式识别提取数值，利用双向上下文传播进行实体绑定，并结合跨源注册表和时间追踪器进行验证。实验证明，RAGShield在检测IRS文档数值篡改方面达到了100%的成功率，远超传统方法。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00387v2/fig-001.webp\", \"caption\": \"TABLE XIII: Claim Extraction on Adversarial Formatting\", \"page\": 10, \"index\": 1, \"width\": 504, \"height\": 298}, {\"url\": \"assets/figures/arxiv/2604.00387v2/fig-002.webp\", \"caption\": \"TABLE XII: End-to-End LLM Evaluation (TinyLlama-1.1BChat)\", \"page\": 9, \"index\": 2, \"width\": 526, \"height\": 282}]"
motivation: 现有的基于嵌入的RAG防御系统无法识别微小但影响巨大的数值篡改，导致政府咨询系统存在严重的财务风险。
method: RAGShield通过模式匹配提取数值，利用双向上下文传播绑定实体，并建立跨源注册表和时间追踪器来验证数值的准确性。
result: "在针对真实IRS文档的430次攻击测试中，RAGShield实现了100%的检测率，而传统嵌入式防御的漏报率高达79-90%。"
conclusion: 针对高风险领域的RAG系统，必须采用专门的数值验证机制而非仅依赖语义嵌入来确保信息的准确性。
---

## 摘要
检索增强生成 (RAG) 系统已广泛部署于各联邦机构，用于面向公民的税务指导、福利资格和法律信息。在这些场景中，单个错误的数字即可导致直接的经济损失。本文证明，所有基于嵌入 (embedding) 的 RAG 防御系统都存在一个根本性的盲点：将一项税收减免额更改 50,000 美元所产生的余弦相似度高达 0.9998，这在所有已知的检测阈值下都是不可见的。通过对 174 个篡改对和两个嵌入模型的测试，平均灵敏度差距达到了 1,459 倍。该盲点在真实的美国国税局 (IRS) 文档中得到了证实。其根本原因在于嵌入编码的是主题信息，而非数值精度。RAGShield 通过直接处理提取的数值来规避这一问题：其基于模式的引擎可识别政府文本中的金额和百分比，通过双向上下文传播将每个数值与其所属实体链接（在 2,742 个真实 IRS 段落中实现了 99.8% 的实体检测率），并根据从语料库本身构建的跨源注册表验证每一项主张。此外，时间追踪器会标记超出已知政府更新计划的数值变化。在基于真实 IRS 文档内容生成的 430 次攻击中，RAGShield 实现了全量检测（攻击成功率 ASR 为 0.0%，95% 置信区间 [0%, 1%]），而基于嵌入的防御系统则漏掉了 79-90% 的相同攻击。

## Abstract
Retrieval-Augmented Generation (RAG) systems are deployed across federal agencies for citizen-facing tax guidance, benefits eligibility, and legal information, where a single incorrect number causes direct financial harm. This paper proves that all embedding-based RAG defenses share a fundamental blind spot: changing a tax deduction by $50,000 produces cosine similarity 0.9998, invisible to every known detection threshold. Across 174 manipulation pairs and two embedding models, the mean sensitivity gap is 1,459x. The blind spot is confirmed on real IRS documents.The root cause is that embeddings encode topic, not numerical precision. RAGShield sidesteps this by operating on extracted values directly: a pattern-based engine identifies dollar amounts and percentages in government text, links each value to its governing entity through two-pass context propagation (99.8% entity detection on 2,742 real IRS passages), and verifies every claim against a cross-source registry built from the corpus itself. A temporal tracker flags value changes that fall outside known government update schedules. On 430 attacks generated from real IRS document content, RAGShield detects every one (0.0% ASR, 95% CI [0%, 1%]) while embedding-based defenses miss 79-90% of the same attacks.

---

## 论文详细总结（自动生成）

这是一份关于论文《RAGShield: Detecting Numerical Claim Manipulation in Government RAG Systems》的结构化深入分析报告：

### 1. 核心问题与整体含义
*   **研究动机**：随着政府机构（如美国 IRS、GSA）广泛采用检索增强生成（RAG）系统为公民提供税务、福利和法律咨询，系统输出的数值准确性至关重要。
*   **核心问题**：论文揭示了现有基于嵌入（Embedding）的 RAG 防御系统存在一个**“数值盲点”**。由于嵌入模型侧重于语义相似度而非数值精度，篡改一个关键数值（如将免税额改变 50,000 美元）对向量空间的影响微乎其微（余弦相似度仍高达 0.9998），导致现有防御手段（如 RobustRAG、TrustRAG）完全失效。
*   **整体含义**：本文旨在通过一种非嵌入式的、基于主张级验证的框架，保护政府 RAG 系统免受内部威胁或供应链污染导致的数值篡改攻击。

### 2. 方法论：RAGShield 架构
RAGShield 放弃了依赖向量距离的防御逻辑，转而采用直接的数值验证流程：
*   **数值主张提取（Numerical Claim Extraction）**：
    *   使用正则表达式识别金额、百分比、日期等。
    *   **双向上下文传播（Two-pass Entity Resolution）**：第一遍独立检测句子实体；第二遍通过前向传播和后向填充，将孤立的数值（如“输入 $12,000”）与其所属实体（如“标准扣除额”）绑定。
*   **跨源主张验证（Cross-Source Claim Verification）**：
    *   构建一个基于 SQLite 的**主张注册表**，存储（实体、属性、数值、单位）元组。
    *   **共识机制**：将提取的数值与注册表中多个独立来源（如不同年份或不同部门的出版物）进行比对。若一致性低于阈值或存在单源冲突，则标记为“可疑”或“争议”。
*   **时间主张追踪（Temporal Claim Tracking）**：
    *   根据政府公开的更新时间表（如 IRS 通常在 10/11 月更新）建立授权变更日历。任何在非预定时间发生的数值变动都会被拦截。
*   **来源验证层（Provenance Layer）**：作为第一道防线，通过数字签名拦截未经授权的外部文档注入。

### 3. 实验设计
*   **数据集/场景**：
    *   **真实 IRS 语料库**：从 5 份官方 IRS 出版物（如 Pub 17, 501 等）中提取的 2,742 个段落。
    *   **合成语料库**：12 个政府格式段落混合 1,000 个 NQ 基准测试段落。
*   **攻击模型**：生成了 430 次针对真实内容的攻击，分为三类：T3-H（细微篡改）、T6（极小值修改 ±1）、T-TEMPORAL（使用旧年份数值）。
*   **对比方法（Benchmarks）**：
    *   无防御基准（No Defense）。
    *   纯嵌入防御（Embed-only）。
    *   学术界主流防御：RobustRAG、TrustRAG、RAGPart。

### 4. 资源与算力
*   **算力说明**：论文未详细列出大规模 GPU 集群的训练时长，因为 RAGShield 主要是基于规则和轻量级数据库的防御框架。
*   **推理开销**：作者提供了详细的延迟数据：
    *   提取引擎：~5ms/文档。
    *   注册表查询：~1ms/主张。
    *   总查询开销：约 8ms，远低于 LLM 生成（~500ms）和嵌入推理（~50ms）。
*   **模型使用**：端到端评估使用了 **TinyLlama-1.1B-Chat** 作为生成器。

### 5. 实验数量与充分性
*   **实验规模**：
    *   测试了 174 对篡改数据以证明“嵌入盲点”。
    *   在 2,742 个真实 IRS 段落上验证了实体检测率（达 99.8%）。
    *   执行了 430 次端到端攻击测试。
*   **充分性评价**：实验设计较为充分且具有高度的客观性。作者不仅在合成数据上测试，还深入分析了真实政府文档的复杂结构（如工作表、脚注），并进行了消融实验和针对自适应攻击者的鲁棒性分析，证明了该方法在真实场景下的可行性。

### 6. 主要结论与发现
*   **嵌入失效**：实证证明了数值敏感度差距高达 1,459 倍，基于嵌入的防御在数值篡改面前几乎“失明”。
*   **RAGShield 效能**：在真实 IRS 攻击中实现了 **0.0% 的攻击成功率（ASR）**，而传统防御的漏报率在 79% 到 90% 之间。
*   **财务保护**：在模拟场景中，RAGShield 将潜在的公民财务损失从 24.3 万美元降低至 0。
*   **实体绑定关键性**：双向上下文传播技术将实体检测率从 40.5% 提升至 99.8%，是处理政府复杂文档的关键。

### 7. 优点
*   **直击痛点**：精准识别并解决了 RAG 系统在处理高价值数值信息时的结构性缺陷。
*   **低开销高效率**：不依赖昂贵的 LLM 自我审计或重训练，推理延迟极低，易于集成到现有管线。
*   **可解释性强**：基于规则和跨源共识的验证结果比黑盒嵌入模型更具说服力，符合政府监管要求。

### 8. 不足与局限
*   **模式依赖**：目前的提取引擎依赖正则表达式，对于“五千美元”这类拼写形式的数字或复杂的间接引用（如“比去年增加 3%”）处理能力有限。
*   **共识假设**：系统假设“多数来源是诚实的”。如果攻击者能够控制超过 50% 的参考来源，共识机制将会失效。
*   **领域局限性**：虽然架构可迁移，但针对不同领域（如医疗、金融）需要重新编写特定的实体识别模式和时间表。
*   **自适应攻击风险**：虽然论文测试了一些规避策略，但高级攻击者可能通过改变数值格式（如使用非标准 Unicode 字符或复杂的数学重构）来绕过正则提取层。

（完）
