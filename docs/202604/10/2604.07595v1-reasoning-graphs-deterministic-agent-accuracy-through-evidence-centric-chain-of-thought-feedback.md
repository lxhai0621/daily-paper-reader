---
title: "Reasoning Graphs: Deterministic Agent Accuracy through Evidence-Centric Chain-of-Thought Feedback"
title_zh: 推理图：通过以证据为中心的思维链反馈实现确定性的智能体准确率
authors: Matthew Penaroza
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07595v1"
tags: ["query:agent"]
score: 9.0
evidence: 推理图谱将智能体的证据链式思考持久化为记忆
tldr: 针对LLM智能体在处理相似查询时重复推理且结果不稳定的问题，本文提出“推理图”结构。该方法将针对特定证据的思维链评价持久化为图边，通过证据中心化的反馈机制实现知识沉淀。结合检索图优化，系统在不微调模型的情况下，通过上下文工程显著提升了多跳问答的准确率并降低了结果方差，实现了智能体的自我进化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.07595v1/fig-001.webp\", \"caption\": \"Figure 1: Architecture overview. The agent loop (retrieve, reason, act) is augmented with two feedback graphs. The reasoning graph GR feeds evidence profiles into the agent’s context by traversing from each evidence item inward across all past evaluations (evidence-centric feedback). The retrieval graph GP feeds a pipeline planner that excludes consistently-rejected items from future candidate sets. Both graphs are updated atomically after each run. Unlike approaches that retrieve past strategies by query similarity or recency, evidence-centric feedback starts from the specific evidence items in the current candidate set.\", \"page\": 2, \"index\": 1, \"width\": 1037, \"height\": 347}, {\"url\": \"assets/figures/arxiv/2604.07595v1/fig-002.webp\", \"caption\": \"Figure 2: Reasoning graph anatomy. Forward traversal (top): from an agent through its decided and evaluated edges, reconstruct the complete chain of thought for a past run, used for auditability. Backward traversal (bottom): from an evidence item inward, aggregate evaluations from correct-outcome decisions across runs to discover cross-run patterns. This is the core operation that constructs evidence profiles and cannot be replicated with flat strategy stores.\", \"page\": 5, \"index\": 2, \"width\": 944, \"height\": 418}, {\"url\": \"assets/figures/arxiv/2604.07595v1/fig-003.webp\", \"caption\": \"Table 1: Suggested hyperparameters for experimental evaluation.\", \"page\": 15, \"index\": 3, \"width\": 940, \"height\": 529}]"
motivation: 现有的LLM智能体在每次查询时都从零开始推理，导致对相同类型问题的处理缺乏一致性且准确率受限。
method: 提出一种将证据与历史评价关联的推理图结构，通过反向遍历证据节点的评价边来提供反馈，并辅以检索图优化候选集。
result: 在多跳问答基准测试中，该方法实现了准确率的持续提升和方差的显著下降，且所有决策过程均可追溯。
conclusion: 推理图通过证据中心化的反馈机制，使冻结的语言模型能够实现自我改进，为构建确定性更强的智能体提供了新路径。
---

## 摘要
语言模型智能体在处理每个查询时都从零开始推理：每当智能体检索证据并进行审议时，思维链都会被丢弃，而下一个类似的查询在开始时没有任何先验见解。这导致了较低的准确率和较高的方差，因为同类型的查询可能会不可预测地成功或失败。我们引入了推理图（reasoning graphs），这是一种图结构，它将智能体针对每个证据的思维链持久化为结构化边，并连接到它们所评估的证据项上。与以往将提炼后的策略存储为按查询相似度索引或按近期性追加的扁平记录的记忆机制不同，推理图实现了以证据为中心的反馈：给定一个新的候选集，系统会遍历所有先前运行中针对每个证据项的所有入边评估，从而揭示该特定项之前是如何被评判的。这种从证据向内的反向遍历在结构上是一种不同于查询相似度检索的能力，因为反馈与智能体当前正在检查的具体证据挂钩，而不是与查询挂钩。我们进一步引入了检索图（retrieval graphs），这是一种补充结构，它为流水线规划器提供输入，以便在连续运行中收紧候选漏斗。这两种图共同构成了一个自我改进的反馈循环：随着连续运行，准确率上升且方差骤减，每个决策都可以通过图进行完全追溯。这种改进不需要重新训练；基础模型保持冻结，所有收益都来自于通过图遍历进行的上下文工程。我们对图结构、遍历算法和反馈机制进行了形式化，并描述了一种顺序集群评估协议，用于在多跳问答基准测试中衡量准确率收敛和方差骤减。

## Abstract
Language model agents reason from scratch on every query: each time an agent retrieves evidence and deliberates, the chain of thought is discarded and the next similar query starts with no prior insight. This produces lower accuracy and high variance, as the same type of query can succeed or fail unpredictably. We introduce reasoning graphs, a graph structure that persists an agent's per-evidence chain of thought as structured edges connected to the evidence items they evaluate. Unlike prior memory mechanisms that store distilled strategies as flat records indexed by query similarity or appended by recency, reasoning graphs enable evidence-centric feedback: given a new candidate set, the system traverses all incoming evaluation edges for each evidence item across all prior runs, surfacing how that specific item has been judged before. This backward traversal from evidence inward is a structurally different capability from query-similarity retrieval, because the feedback is tied to the specific evidence the agent is currently examining, not to the query. We further introduce retrieval graphs, a complementary structure that feeds a pipeline planner to tighten the candidate funnel over successive runs. Together, both graphs form a self-improving feedback loop: accuracy rises and variance collapses over successive runs, with every decision fully traceable through the graph. This improvement requires no retraining; the base model remains frozen and all gains come from context engineering via graph traversal. We formalize the graph structure, traversal algorithms, and feedback mechanisms, and describe a sequential cluster evaluation protocol for measuring accuracy convergence and variance collapse on multi-hop question answering benchmarks.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为“推理图”（Reasoning Graphs）的框架，旨在解决大语言模型（LLM）智能体在处理重复或相似任务时存在的“推理遗忘”和结果不一致（高方差）问题。

### 1. 核心问题与整体含义
*   **研究动机**：目前的智能体在处理每个查询时都从零开始推理。即使多次遇到相同的证据材料，它们也会丢弃之前的思维链（CoT），导致对相同证据的评价可能出现随机的偏差。
*   **核心问题**：
    1.  **准确率受限**：无法利用过往正确的推理经验。
    2.  **高方差（不确定性）**：同一类型的查询在不同运行中可能成功也可能失败，缺乏行为一致性。
*   **整体含义**：通过构建持久化的图结构记忆，将智能体对特定证据的评价“固化”下来，实现“以证据为中心”的反馈，从而让智能体在不进行模型微调的情况下实现自我进化。

### 2. 方法论
该框架由两个互补的图结构组成：
*   **推理图 (Reasoning Graph, $G_R$)**：
    *   **核心思想**：将智能体对每一个证据项（Passage）的评价（使用或拒绝、理由、结果）记录为图中的边。
    *   **以证据为中心的反馈**：当新查询检索到一组候选证据时，系统会**反向遍历**图中指向这些证据的评价边，提取出“证据概况”（Evidence Profile）。
    *   **证据概况**：包含该证据在历史正确决策中的被采用率、常见的拒绝理由和可靠性评分。这些信息会被注入到 Prompt 中，指导智能体本次的推理。
*   **检索图 (Retrieval Graph, $G_P$)**：
    *   **核心思想**：记录决策与检索到的证据之间的关系。
    *   **流水线规划器**：根据历史记录，自动识别并排除那些在特定查询类型下频繁被拒绝的“干扰项”，从而收紧候选集漏斗，减少上下文冗余。
*   **反馈循环**：每次运行后，系统原子性地写入新的决策边和评价边。随着运行次数增加，证据概况越丰富，检索漏斗越精准。

### 3. 实验设计
论文提出了一套详尽的评估协议（但需注意，该文偏向架构提案，未包含最终实验数据）：
*   **数据集**：
    *   **HotpotQA**：多跳问答，包含大量干扰文档。
    *   **MuSiQue**：更复杂的多步推理数据集。
*   **评估协议（Sequential Cluster Protocol）**：将查询按类型聚类，在每个类内顺序执行查询，观察准确率随运行次数（1, 5, 10...100次）的变化曲线。
*   **对比方法（Baselines）**：
    *   Vanilla RAG（无反馈基准）。
    *   Reflexion（基于近期性的文本反馈）。
    *   ReasoningBank（基于查询相似度的策略检索）。
    *   Ours-QC（基于查询中心化的图检索，用于消融实验）。

### 4. 资源与算力
*   **算力说明**：文中**未明确提及**具体的 GPU 型号、数量或训练时长。
*   **原因**：该方法属于“零样本/上下文工程”范畴，基础模型保持冻结（Frozen），不需要重新训练或微调，主要的计算开销在于图数据库的查询和 Prompt 增加的 Token 消耗。

### 5. 实验数量与充分性
*   **实验状态**：论文详细描述了实验的蓝图、超参数设置（如 $T_{max}=50$, $B=2000$ tokens）和消融实验设计（包括正确结果过滤器的必要性、噪声鲁棒性、跨模型迁移等）。
*   **充分性评价**：虽然设计了 8 个关键假设的验证实验，但由于文中**缺少实际运行的实验结果数据**，其有效性目前仅停留在理论推导和协议设计阶段。

### 6. 主要结论与发现（理论推导）
*   **准确率收敛**：随着证据概况的完善，智能体对证据的判断趋于正确，准确率在期望上单调递增。
*   **方差骤减（确定性）**：智能体的决策逐渐与历史多数正确信号对齐，产生“确定性准确率”，即对相同证据给出一致的判定。
*   **全过程可审计**：通过图的**正向遍历**，可以完全追溯任何一个决策背后的所有证据评价细节，便于故障诊断。

### 7. 优点
*   **反馈精准度高**：相比于传统的“查询相似度”检索，这种“以证据为中心”的反馈能直接告诉智能体“这个特定的文档以前被证明是有误导性的”，避开了向量检索的模糊性。
*   **成本优化潜力**：通过检索图排除干扰项，可以抵消注入证据概况带来的 Token 成本，甚至降低净推理成本。
*   **模型无关性**：推理图积累的知识可以跨模型迁移（例如用强模型刷图，弱模型推理）。

### 8. 不足与局限
*   **缺乏实证数据**：作为一篇论文，没有给出实际的实验结果图表是最大的硬伤。
*   **冷启动问题**：在没有任何历史评价的新领域，系统退化为普通 RAG。
*   **存储压力**：评价边随运行次数线性增长，长期运行需要复杂的清理或老化机制。
*   **身份识别依赖**：极度依赖证据的去重（如哈希匹配）。如果同一事实以不同措辞出现在不同文档中，系统无法关联它们的评价。
*   **错误强化风险**：如果初始的判定信号（Ground Truth 代理）存在系统性偏差，图结构可能会强化这种错误的推理模式。

（完）
