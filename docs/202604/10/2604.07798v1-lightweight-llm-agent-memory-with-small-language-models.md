---
title: Lightweight LLM Agent Memory with Small Language Models
title_zh: 基于小语言模型的轻量级 LLM 智能体记忆系统
authors: "Jiaquan Zhang, Chaoning Zhang, Shuxu Chen, Zhenzhen Huang, Pengcheng Zheng, Zhicheng Wang, Ping Guo, Fan Mo, Sung-Ho Bae, Jie Zou, Jiwei Wei, Yang Yang"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07798v1"
tags: ["query:agent"]
score: 10.0
evidence: LightMem 为大模型智能体模块化了记忆检索、写入和长期巩固
tldr: 针对LLM智能体在长程交互中内存管理效率与准确性难以兼得的问题，本文提出LightMem系统。该系统利用小语言模型（SLM）驱动，将内存划分为短、中、长期三层结构，并采用在线检索与离线整合分离的策略。通过两阶段检索机制，LightMem在保持低延迟的同时显著提升了记忆提取的准确性，为多用户场景下的高效智能体交互提供了轻量化解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.07798v1/fig-001.webp\", \"caption\": \"Table 2: Main results on LoCoMo across question categories. We report F1 and BLEU-1 for each category, along with the token length of the effective context. Best results within each model block are in bold.\", \"page\": 6, \"index\": 1, \"width\": 950, \"height\": 927}, {\"url\": \"assets/figures/arxiv/2604.07798v1/fig-002.webp\", \"caption\": \"Table 7: LTM update frequency, consolidation cost, and ablation results on reasoning accuracy.\", \"page\": 15, \"index\": 2, \"width\": 948, \"height\": 435}, {\"url\": \"assets/figures/arxiv/2604.07798v1/fig-003.webp\", \"caption\": \"Table 6: Node and edge types used in the LTM knowledge graph.\", \"page\": 15, \"index\": 3, \"width\": 948, \"height\": 398}, {\"url\": \"assets/figures/arxiv/2604.07798v1/fig-004.webp\", \"caption\": \"Figure 3: Ablation study on DialSim. We report F1, BLEU-1, ROUGE-L, ROUGE-2, METEOR, and SBERT similarity.\", \"page\": 8, \"index\": 4, \"width\": 950, \"height\": 287}, {\"url\": \"assets/figures/arxiv/2604.07798v1/fig-005.webp\", \"caption\": \"Figure 2: Multiple SLMs coordinate an online pathway for query-time routing and retrieval over STM/MTM, and an offline pathway that incrementally consolidates MTM into a graph-structured LTM.\", \"page\": 4, \"index\": 5, \"width\": 808, \"height\": 444}, {\"url\": \"assets/figures/arxiv/2604.07798v1/fig-006.webp\", \"caption\": \"Table 1: Structured HQ prompt used by SLM-1 for query decomposition and routing.\", \"page\": 5, \"index\": 6, \"width\": 471, \"height\": 498}]"
motivation: 现有的检索式内存系统准确率不稳定，而基于大模型的在线内存操作延迟过高，难以满足长程交互的实时性需求。
method: 提出由SLM驱动的LightMem系统，将内存分为短、中、长期三层，并结合在线两阶段检索与离线增量整合机制。
result: 在LoCoMo基准测试中F1值平均提升约2.5，且表现出极低的延迟，检索仅需83毫秒，端到端仅需581毫秒。
conclusion: LightMem证明了利用小语言模型可以构建出兼具高效率与高准确性的轻量化智能体内存系统。
---

## 摘要
尽管 LLM 智能体可以利用工具执行复杂任务，但它们仍需要记忆来维持跨轮次一致性，并在长程交互中积累可重用信息。然而，基于检索的外部记忆系统在线开销较低，但由于查询构建和候选过滤受限，其准确率不稳定。相比之下，许多系统在在线记忆操作中重复调用大模型，虽提高了准确率，但在长程交互中会累积延迟。我们提出了 LightMem，这是一个由小语言模型 (SLM) 驱动的轻量级记忆系统，旨在提升智能体记忆能力。LightMem 将记忆检索、写入和长期整合模块化，并将在线处理与离线整合分离，从而在有限计算资源下实现高效的记忆调用。我们将记忆组织为用于即时对话上下文的短期记忆 (STM)、用于可重用交互摘要的中期记忆 (MTM) 以及用于整合知识的长期记忆 (LTM)，并利用用户标识符支持多用户场景下的独立检索和增量维护。在线上，LightMem 在固定检索预算下运行，通过两阶段过程选择记忆：基于向量的粗检索和随后的语义一致性重排序。在线下，它抽象出可重用的交互证据并将其增量整合到 LTM 中。实验表明，该系统在不同模型规模上均有提升，在 LoCoMo 上的平均 F1 值提高了约 2.5，且更有效率，中值延迟较低（检索 83 毫秒；端到端 581 毫秒）。

## Abstract
Although LLM agents can leverage tools for complex tasks, they still need memory to maintain cross-turn consistency and accumulate reusable information in long-horizon interactions. However, retrieval-based external memory systems incur low online overhead but suffer from unstable accuracy due to limited query construction and candidate filtering. In contrast, many systems use repeated large-model calls for online memory operations, improving accuracy but accumulating latency over long interactions. We propose LightMem, a lightweight memory system for better agent memory driven by Small Language Models (SLMs). LightMem modularizes memory retrieval, writing, and long-term consolidation, and separates online processing from offline consolidation to enable efficient memory invocation under bounded compute. We organize memory into short-term memory (STM) for immediate conversational context, mid-term memory (MTM) for reusable interaction summaries, and long-term memory (LTM) for consolidated knowledge, and uses user identifiers to support independent retrieval and incremental maintenance in multi-user settings. Online, LightMem operates under a fixed retrieval budget and selects memories via a two-stage procedure: vector-based coarse retrieval followed by semantic consistency re-ranking. Offline, it abstracts reusable interaction evidence and incrementally integrates it into LTM. Experiments show gains across model scales, with an average F1 improvement of about 2.5 on LoCoMo, more effective and low median latency (83 ms retrieval; 581 ms end-to-end).

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **LightMem** 的轻量级大语言模型（LLM）智能体记忆系统。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：LLM 智能体在长程交互中面临“记忆困境”。现有的记忆系统主要分为两类：
    1.  **基于检索的外部记忆**：效率高，但由于查询构建简单和候选过滤不足，导致准确率不稳定且噪声多。
    2.  **基于 LLM 驱动的记忆操作**：通过反复调用大模型进行记忆读写，准确率高但在线延迟巨大，难以扩展。
*   **核心问题**：如何在保证记忆检索准确性的同时，显著降低在线处理的计算开销和延迟？
*   **整体含义**：论文提出利用**小语言模型（SLM）**来接管高频的在线记忆管理任务，并将沉重的记忆整合任务转移到离线处理，从而实现效率与效果的平衡。

### 2. 方法论：核心思想与技术细节
LightMem 的核心思想是**在线/离线解耦**以及**多层级记忆管理**。
*   **三层记忆结构**：
    *   **短期记忆 (STM)**：当前的上下文窗口，不持久化。
    *   **中期记忆 (MTM)**：存储用户相关的交互摘要，具备用户标识符（User ID）以实现逻辑隔离。
    *   **长期记忆 (LTM)**：离线整合的去隐私化语义知识，采用轻量级**图结构**组织。
*   **在线处理流程（由三个专门的 SLM 驱动）**：
    1.  **控制器 (SLM-1)**：将用户输入重写为多个“假设性查询（HQs）”，并根据意图分配检索预算。
    2.  **两阶段检索 (SLM-2)**：
        *   **阶段1（粗检索）**：基于向量相似度在元数据约束下筛选出 2K 个候选。
        *   **阶段2（精筛选）**：SLM-2 进行语义一致性重排序，将候选压缩至最终的 Top-K。
    3.  **写入器 (SLM-3)**：在每轮对话后提取关键信息，增量更新 MTM。
*   **离线整合流程**：由高性能 LLM 异步执行，将 MTM 中的碎片化信息抽象为 LTM 中的知识节点和边，支持多跳推理。

### 3. 实验设计
*   **数据集/场景**：
    *   **LoCoMo**：评估长对话中的逻辑推理（涵盖单跳、多跳、时间、开放域和对抗性任务）。
    *   **DialSim**：多方对话模拟，评估长程对话的语义一致性。
*   **Benchmark 与对比方法**：
    *   对比了 LoCoMo 原生方法、ReadAgent、MemoryBank、MemGPT 和 A-MEM。
    *   **Backbone 模型**：涵盖了从顶级模型（GPT-4o, GPT-4o-mini）到开源小模型（Qwen2.5-1.5B/3B, Llama-3.2-1B/3B）的广泛测试。

### 4. 资源与算力
*   **硬件环境**：实验在一台 **NVIDIA RTX 4090 (24GB)** GPU 上完成。
*   **模型部署**：在线控制平面的 SLM（如 Llama-3.2-1B）采用了量化部署（通过 Ollama）。
*   **训练细节**：SLM-2 使用 LoRA 进行了微调，训练数据为 2000 个构建的（查询、子图、路径）样本。

### 5. 实验数量与充分性
*   **实验规模**：论文在 2 个主流数据集上进行了详尽测试，并跨越了 6 种不同规模的 Backbone 模型。
*   **消融实验**：针对语义重排、假设性查询（HQ）、MTM 层、离线整合及图结构等关键组件均做了消融分析。
*   **客观性与公平性**：所有对比方法均在相同的检索预算和生成模型（GPT-4o-mini）下运行，并提供了 3 个随机种子的标准差及统计显著性分析（p-value），实验设计严谨、客观。

### 6. 主要结论与发现
*   **性能提升**：在 LoCoMo 基准测试中，LightMem 在各模型规模下均取得领先，F1 分数平均提升约 **2.5**。
*   **极低延迟**：检索中值延迟仅为 **83ms**，端到端中值延迟为 **581ms**，远低于基于 LLM 驱动的系统（如 A-MEM 的检索延迟高达 856ms）。
*   **语义一致性**：在 DialSim 上，SBERT 语义相似度显著提升，证明其检索到的记忆更具相关性。
*   **上下文压缩**：相比于全量回放历史，LightMem 将有效上下文长度从 ~16K tokens 压缩至 ~1K tokens，大幅降低了推理成本。

### 7. 优点与亮点
*   **模块化设计**：将记忆操作拆分为控制器、选择器和写入器，允许独立优化和弹性扩展。
*   **在线/离线分离**：巧妙地解决了“实时响应”与“深度整合”之间的矛盾。
*   **多用户支持**：通过用户标识符和元数据过滤，原生支持多用户场景下的隐私隔离。
*   **两阶段检索机制**：结合了向量检索的效率和 SLM 语义理解的精度，有效抑制了检索噪声。

### 8. 不足与局限
*   **意图分解风险**：SLM-1 在处理模糊查询时，可能会生成不准确的假设性查询（HQ），导致检索内容“焦点稀释”。
*   **离线策略探索不足**：论文主要关注在线路径，对于不同的离线整合策略（如不同的图构建算法）探讨较少。
*   **SLM 依赖性**：系统的表现高度依赖于 SLM 的微调质量，若 SLM 性能过弱，可能会成为整个记忆系统的瓶颈。

（完）
