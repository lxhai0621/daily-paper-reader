---
title: "Don't Let AI Agents YOLO Your Files: Shifting Information and Control to Filesystems for Agent Safety and Autonomy"
title_zh: 别让 AI 智能体随意处置你的文件：将信息与控制权移交给文件系统以实现智能体的安全性与自主性
authors: "Shawn Wanxiang Zhong, Junxuan Liao, Jing Liu, Mai Zheng, Andrea C. Arpaci-Dusseau, Remzi H. Arpaci-Dusseau"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13536v2"
tags: ["query:ma-kf"]
score: 9.0
evidence: 自主AI智能体与文件系统控制
tldr: 针对AI编程智能体在操作文件系统时常导致数据损坏或泄露的问题，本文在分析290份误用报告的基础上，提出了原生支持智能体的文件系统YoloFS。该系统通过暂存区隔离修改、快照支持自纠错及渐进式权限控制，在保障安全的同时提升了智能体的自主性。实验证明，YoloFS能显著减少用户干预并增强智能体的错误修复能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.13536v2/fig-001.webp\", \"caption\": \"Figure 1. Shifting information (I) and control (C) from agents to filesystems improves safety while reducing user interaction.\", \"page\": 1, \"index\": 1, \"width\": 508, \"height\": 251}, {\"url\": \"assets/figures/arxiv/2604.13536v2/fig-002.webp\", \"caption\": \"Table 3. Agent self-correction on tasks with hidden side effects. ✓ = self-corrected, ✓u= user-correctable, ✗ = fail, ? = asked user. YoloFS denotes Claude Code with YoloFS. LoC is the project size.\", \"page\": 10, \"index\": 2, \"width\": 498, \"height\": 342}, {\"url\": \"assets/figures/arxiv/2604.13536v2/fig-003.webp\", \"caption\": \"Figure 4. Taxonomy of causes (290 total). Counts can overlap.\", \"page\": 4, \"index\": 3, \"width\": 504, \"height\": 441}, {\"url\": \"assets/figures/arxiv/2604.13536v2/fig-004.webp\", \"caption\": \"Table 1. Comparison of agent frameworks.\", \"page\": 5, \"index\": 4, \"width\": 1044, \"height\": 315}]"
motivation: 现有的AI智能体在操作文件系统时缺乏对副作用的感知和控制，导致在安全性和自主性之间存在难以调和的矛盾。
method: 设计了YoloFS文件系统，利用暂存区隔离、快照机制和渐进式权限管理来提供预防性、纠正性和自主性的控制。
result: 在带有副作用的任务中，YoloFS助力智能体在多数情况下实现自纠错，并在常规任务中以更少的用户交互保持了基准成功率。
conclusion: 通过将信息和控制权转移到文件系统层，可以有效解决AI智能体在文件操作中的安全隐患并提升其独立工作能力。
---

## 摘要
AI 编程智能体直接在用户的文件系统上运行，经常导致数据损坏、文件删除和密钥泄露。目前的方法被迫在安全性与自主性之间进行权衡：不受限制的访问存在风险，而频繁的权限提示则会增加用户负担并阻碍智能体运行。为了深入理解这一问题，我们对智能体文件系统误用情况进行了首次系统性研究，分析了涵盖 13 个框架的 290 份公开报告。我们的分析表明，目前的智能体对其在文件系统上产生的影响了解有限，且缺乏足够的控制能力。因此，我们主张将这些信息和控制权移交给文件系统本身。基于这一原则，我们设计了 YoloFS，这是一种采用三种技术的智能体原生文件系统。暂存（Staging）在提交前隔离所有变更，为用户提供纠正性控制；快照（Snapshots）将这种控制权扩展到智能体，使其能够检测并纠正自身的错误；渐进式权限（Progressive permission）通过最少的人机交互来管控访问，为用户提供预防性控制。为了评估 YoloFS，我们引入了一种捕获用户-智能体-文件系统交互的新方法。在 11 个具有隐藏副作用的任务中，YoloFS 使智能体在 8 个任务中实现了自我纠正，同时保持所有影响处于暂存状态且可审查。在 112 个常规任务中，YoloFS 在保持与基准相当的成功率的同时，减少了用户交互次数。

## Abstract
AI coding agents operate directly on users' filesystems, where they regularly corrupt data, delete files, and leak secrets. Current approaches force a tradeoff between safety and autonomy: unrestricted access risks harm, while frequent permission prompts burden users and block agents. To understand this problem, we conduct the first systematic study of agent filesystem misuse, analyzing 290 public reports across 13 frameworks. Our analysis reveals that today's agents have limited information about their filesystem effects and insufficient control over them. We therefore argue for shifting this information and control to the filesystem itself.   Based on this principle, we design YoloFS, an agent-native filesystem with three techniques. Staging isolates all mutations before commit, giving users corrective control. Snapshots extend this control to agents, letting them detect and correct their own mistakes. Progressive permission provides users with preventive control by gating access with minimal interaction. To evaluate YoloFS, we introduce a new methodology that captures user-agent-filesystem interactions. On 11 tasks with hidden side effects, YoloFS enables agent self-correction in 8 while keeping all effects staged and reviewable. On 112 routine tasks, YoloFS requires fewer user interactions while matching the baseline success rate.

---

## 论文详细总结（自动生成）

这是一份关于论文《Don't Let AI Agents YOLO Your Files: Shifting Information and Control to Filesystems for Agent Safety and Autonomy》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：AI 编程智能体（如 Claude Code, Cursor, Copilot）在执行任务时，直接在用户本地文件系统上运行。由于模型的不确定性，智能体经常发生“误用”行为，包括误删文件、损坏数据、泄露 API 密钥等。
*   **研究动机**：现有的防御机制（如频繁的权限弹窗、基于字符串的命令过滤、沙箱隔离）存在严重缺陷。弹窗过多会导致用户产生“审批疲劳”并开启危险的“YOLO 模式”（全自动运行）；而过滤机制极易被绕过。
*   **整体含义**：论文提出将“信息感知”和“操作控制”从智能体层下沉到文件系统层，通过构建“智能体原生文件系统”来解决安全性与自主性之间的矛盾。

### 2. 论文提出的方法论：YoloFS
YoloFS 是一种 Linux 堆栈式文件系统（Stackable Filesystem），其核心思想是**信息透明化**与**控制权移交**。
*   **暂存机制 (Staging)**：所有写操作被重定向到一个独立的暂存层，底层文件系统保持不变。通过“平面文件存储”和“内存覆盖树”解耦内容与路径，用户可以在任务完成后审查 Diff 并决定 Commit 或 Abort。
*   **快照与非破坏性回溯 (Snapshots & Travel)**：
    *   允许智能体在执行关键步骤前创建快照。
    *   提供“Travel”功能，智能体发现错误后可回滚。与传统回滚不同，YoloFS 保留了所有分支历史（通过追加式日志），方便用户审计。
*   **渐进式权限 (Progressive Permission)**：
    *   基于路径而非命令字符串进行管控。
    *   采用“Ask-once”策略：当智能体访问未授权路径时触发弹窗，用户批准后自动生成规则，减少后续干扰。
    *   支持 Hidden（隐藏）、Deny（拒绝）、Read-only（只读）等细粒度状态。

### 3. 实验设计
*   **误用案例研究**：收集并分析了 2024-2026 年间 13 个框架的 **290 份**公开误用报告，建立了原因分类学。
*   **自纠错实验 (Self-Correction)**：设计了 **11 个**具有隐藏副作用的任务（如一个看似格式化代码的脚本实际上会删除文档）。
*   **常规任务实验 (Routine Tasks)**：包含 **112 个**日常文件操作任务（读、写、移动、跨目录访问等）。
*   **性能测试**：对比了 Ext4（基准）、OverlayFS 和 BranchFS。
*   **对比对象**：Claude Code、Codex、Copilot、Gemini。

### 4. 资源与算力
*   **算力说明**：论文未涉及模型训练，主要关注推理阶段的系统交互。
*   **硬件环境**：实验在配备 AMD EPYC 7302P（16核，3GHz）、125GB DDR4 内存和 SATA SSD 的 Ubuntu 24.04 机器上运行。
*   **模型使用**：集成了 Claude Code 2.1.45（使用 `claude-3.5-sonnet`）作为主要的智能体进行测试。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **290 份**真实案例分析提供了坚实的理论基础。
    *   **123 个**自动化测试任务覆盖了从极端陷阱到日常操作的多种场景。
    *   **性能实验**涵盖了单线程 I/O 吞吐量、元数据延迟、快照扩展性（支持数百个快照）以及真实开发者工作流（编译 Linux 内核）。
*   **充分性与公平性**：实验设计较为客观，通过构建轻量级交互式 Benchmark 框架，确保了不同智能体在相同文件系统环境下进行公平对比，并捕获了用户交互次数这一关键指标。

### 6. 论文的主要结论与发现
*   **误用根源**：智能体对文件系统效果感知模糊（信息差），且缺乏事后纠正手段（控制差）。
*   **自纠错能力**：在 11 个陷阱任务中，YoloFS 助力智能体在 **8 个**任务中实现了完全自纠错（检测到异常并自动回滚），其余 3 个任务虽未自纠错但也处于暂存状态，可由用户拦截。
*   **交互效率**：在常规任务中，YoloFS 在保持 99% 成功率的同时，显著减少了用户审批次数（平均每任务 0.4 次交互，远低于基准智能体）。
*   **性能表现**：YoloFS 的 I/O 开销极低（接近原生 Ext4），且快照性能优于 OverlayFS。

### 7. 优点
*   **范式转移**：从“防范命令”转向“管控效果”，从根本上解决了绕过过滤的问题。
*   **非破坏性设计**：Travel 功能保留了失败尝试的记录，这对 AI 审计和调试至关重要。
*   **低侵入性**：作为堆栈式文件系统，它不需要修改现有的 AI 智能体代码或底层文件系统格式。
*   **实用性强**：通过渐进式权限平衡了安全与用户体验。

### 8. 不足与局限
*   **平台限制**：目前仅实现在 Linux 平台上，对于 Windows 或 macOS 原生环境的支持尚需进一步开发。
*   **不可逆操作**：虽然文件修改可以回滚，但对于已经通过网络泄露的数据（如 `cat .env | curl ...`），文件系统层面的回滚无法撤销泄露事实，仍需依赖严格的预防性权限控制。
*   **复杂交互**：对于涉及多进程、复杂 IPC 的智能体行为，单纯的文件系统层监控可能不足以捕捉所有副作用。

（完）
