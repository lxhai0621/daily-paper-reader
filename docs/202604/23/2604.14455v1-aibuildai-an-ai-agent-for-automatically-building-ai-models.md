---
title: "AIBuildAI: An AI Agent for Automatically Building AI Models"
title_zh: AIBuildAI：一种用于自动构建 AI 模型的 AI 智能体
authors: "Ruiyi Zhang, Peijia Qin, Qi Cao, Li Zhang, Pengtao Xie"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.14455v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 自动构建AI模型的AI智能体
tldr: "针对AI模型开发过程高度依赖人工、现有AutoML工具局限性大的问题，本文提出AIBuildAI。这是一个基于大语言模型的层次化智能体系统，通过管理者协调设计、编码和调优三个子智能体，实现从任务描述到模型部署的全流程自动化。在MLE-Bench基准测试中，该系统以63.1%的奖牌率位居榜首，表现媲美资深AI工程师，显著提升了AI开发的自动化水平。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.14455v1/fig-001.webp\", \"caption\": \"Fig. 3: AIBuildAI outperforms baseline methods on image classification tasks. a, Example workflow on the alaska2-image-steganalysis task, a steganalysis challenge to predict the likelihood that an image contains a hidden message. The manager agent invokes the designer sub-agent to propose six solution candidates based on diverse architectures including EfficientNet, ResNet, ConvNeXt, Swin Transformer, and Wide-ResNet variants. The manager reviews the six plans, selects four to proceed, and invokes the coder sub-agent to implement each plan and the tuner sub-agent to optimize training parameters and obtain validation results. Based on these results, the manager invokes the designer to revise candidate 2 by removing stride surgery and reverting to vanilla EfficientNet-B4, which then passes through the coder and tuner again. The aggregator constructs a rank-based ensemble of the four models, achieving a score of 0.8101 on the private test set. b, Final solution in detail. The pipeline takes an image as input, resizes and normalizes it using ImageNet statistics. Test-time augmentation produces four views (original, H-flip, V-flip, H+V-flip). Each view is passed through four ensemble models, namely EfficientNet-V2-S with focal loss, EfficientNet-B4, ResNet50 with a learnable high-pass filter bank, and Wide-ResNet-50-2 with a dual-branch architecture. Per-model raw scores are converted to rank percentiles and combined via weighted averaging to produce a final stego score. c, Performance across 10 image classification tasks spanning natural image recognition, fine-grained species classification, medical imaging, data security, and object recognition. AIBuildAI consistently outperforms baseline systems AIRA-dojo and MLEvolve across various visual domains and evaluation metrics.\", \"page\": 6, \"index\": 1, \"width\": 945, \"height\": 1019}, {\"url\": \"assets/figures/arxiv/2604.14455v1/fig-002.webp\", \"caption\": \"Fig. 6: AIBuildAI surpasses baseline methods on temporal and tabular tasks. a, Example workflow on the hms-harmful-brain-activity-classification task, which requires classifying EEG recordings into six categories of harmful brain activity (e.g., seizure, lateralized periodic discharges). The manager agent invokes the designer sub-agent to propose six solution candidates based on diverse approaches including EfficientNet spectrogram classifiers, WaveNet on raw EEG, ResNet, TinyViT, and dual-branch hybrids. The manager reviews the plans, selects a subset to proceed, and invokes the coder and tuner sub-agents to implement and evaluate them. Based on the results, the manager invokes the designer to revise candidates 1 and 2 by adding an EEG branch with double banana montage and a 1D-CNN branch for raw signals, respectively. The coder and tuner implement and retrain the revised models. The aggregator selects the best checkpoint from candidate 1 as the final model. b, Final solution in detail. The pipeline takes spectrogram and raw EEG inputs and processes them through a dual-branch architecture. The spectrogram branch extracts four EEG regions (e.g., left lateral, right lateral), fills missing values, applies log-transform with Z-score normalization, and feeds the result into an ImageNet-pretrained EfficientNet-B0. The EEG branch applies a double banana montage and Butterworth bandpass filtering, then passes the processed signal through a separate EfficientNet-B0. A fusion MLP head combines both branches to produce 6-class probabilities. c, Performance across 3 temporal signal modeling tasks (EEG classification, neutrino detection, radio signal search) and 3 structured tabular prediction tasks (regression, materials property prediction, general tabular prediction). AIBuildAI consistently outperforms baseline systems AIRA-dojo and MLEvolve across both temporal and structured data modalities.\", \"page\": 11, \"index\": 2, \"width\": 945, \"height\": 823}, {\"url\": \"assets/figures/arxiv/2604.14455v1/fig-003.webp\", \"caption\": \"Fig. 1: Overview of AIBuildAI. a, AIBuildAI is an AI agent for automatically building AI models. It takes as input a text description of an AI task and training data, automatically designs and trains an AI model, and outputs the trained model checkpoints and inference script. AIBuildAI adopts a hierarchical agent architecture in which a manager agent oversees multiple parallel solutions, deciding which sub-agent to invoke for iteratively refining each solution repository. Sub-agents include a designer that proposes or revises modeling plans, a coder that implements and debugs training pipelines, and a tuner that runs full training and optimizes performance. Each sub-agent is itself an LLM-based agent that performs multiple rounds of LLM calls and tool interactions within a single invocation. Surrounding this core loop, a setup agent prepares the conda environment and installs the required packages at initialization, and an aggregator agent selects or ensembles the strongest candidate solutions at termination to produce the final AI model. b, AIBuildAI is applicable to a diverse range of tasks spanning multiple data modalities and problem formulations, including visual recognition, natural language processing, temporal forecasting, and structured tabular prediction.\", \"page\": 2, \"index\": 3, \"width\": 945, \"height\": 1092}, {\"url\": \"assets/figures/arxiv/2604.14455v1/fig-004.webp\", \"caption\": \"Fig. 4: AIBuildAI surpasses baseline methods on detection, segmentation, and video understanding tasks. a, Example workflow on the contrails-identify task, which aims to generate masks of aircraft condensation trails from satellite imagery. The manager agent invokes the designer sub-agent to propose seven solution candidates based on diverse segmentation architectures including UNet, FPN-ResNet, DeepLabV3+, and ConvNeXt variants. The manager reviews the plans, selects four candidates to proceed with, and invokes the coder and tuner sub-agents to implement, train and evaluate them. Based on the results, the manager invokes the designer to revise candidates 1 and 5 by adding a Lovász-Dice loss component and a fresh optimizer with cosine schedule, respectively. The coder and tuner implement and retrain the revised models. The aggregator constructs a weighted ensemble of the two strongest candidates, achieving a Dice score of 0.660 on the private test set. b, Final solution in detail. The pipeline takes a satellite image and processes it through two parallel branches. The UNet branch (EfficientNet-B4 encoder) computes ash false-color composites from selected satellite bands with per-image Z-score normalization. The DeepLabV3+ branch uses a MiT-B3 encoder, a hierarchical vision transformer, with a different set of bands and global normalization. Each model produces a per-pixel probability map indicating contrail presence. The two maps are combined with weighted averaging, thresholded, and encoded in run-length format. c, Performance across 10 visual perception tasks spanning object detection, semantic segmentation, medical imaging, character recognition, regression, and video-based activity recognition. AIBuildAI consistently outperforms baseline systems AIRA-dojo and MLEvolve across diverse spatial and temporal modalities.\", \"page\": 8, \"index\": 4, \"width\": 945, \"height\": 1050}, {\"url\": \"assets/figures/arxiv/2604.14455v1/fig-005.webp\", \"caption\": \"Fig. 2: AIBuildAI ranks first on the MLE-Bench leaderboard. a, Overall medal rate of AIBuildAI compared with 26 recent baseline methods (or method variants) on MLE-Bench as of the March 18, 2026 leaderboard. AIBuildAI achieves the top overall position with a medal rate of 63.1%, surpassing prior systems including MARS, Famou-Agent, ML-Master, Leeroo, InternAgent, R&D-Agent, AIDE, AIRA-dojo, and MLEvolve. b, Medal rates of AIBuildAI and baseline methods on the three complexity splits of MLE-Bench. AIBuildAI ranks first on both the medium- and high-complexity splits, demonstrating that its performance advantage becomes more pronounced on more challenging AI tasks.\", \"page\": 4, \"index\": 5, \"width\": 871, \"height\": 1310}, {\"url\": \"assets/figures/arxiv/2604.14455v1/fig-006.webp\", \"caption\": \"Fig. 5: AIBuildAI outperforms baseline methods on language understanding and generation tasks. a, Example workflow on the billion-word-imputation task, which aims to predict a missing word at an unknown position in a text paragraph. The manager agent invokes the designer sub-agent to propose seven solution candidates based on diverse approaches including BERT, DistilBERT, n-gram, and multi-model methods. The manager reviews the plans, selects a subset to proceed, and invokes the coder and tuner sub-agents to implement and evaluate them. Based on the results, the manager invokes the designer to revise candidate 1 by incorporating a token-level masking method with word-to-token mapping. The coder and tuner implement and retrain the revised model. The aggregator selects candidate 1 based on validation metrics, achieving a Levenshtein distance of 5.519 on the private test set. b, Final solution in detail. Given an input sentence with a missing word, the pipeline generates candidates by inserting a mask token at every possible position. Each candidate is tokenized and passed through a BERT-base-uncased model, which computes log-probabilities over the vocabulary at the masked position. The position with the highest predicted-word confidence is selected. A confidence gate then determines whether to replace the mask with the predicted word and return the reconstructed sentence, or retain the original input unchanged. c, Performance across 10 language-related tasks spanning text classification, question answering, information extraction, sequence-to-sequence generation, word imputation, and sentiment analysis. AIBuildAI consistently outperforms baseline systems, demonstrating strong capability across diverse language understanding and generation tasks.\", \"page\": 9, \"index\": 6, \"width\": 945, \"height\": 1015}]"
motivation: 传统AI模型开发过程耗时耗力且高度依赖专家经验，而现有AutoML方法仅能解决超参数优化等局部问题。
method: 提出一种层次化智能体架构，由一名管理者协调负责建模策略的设计者、负责实现与调试的编码者以及负责性能优化的调优者。
result: "在涵盖多种数据模态的MLE-Bench基准测试中，AIBuildAI以63.1%的奖牌率排名第一，超越了所有现有基准方法。"
conclusion: 层次化智能体系统能够有效自动化AI模型开发的全生命周期，为实现低门槛、少人工干预的AI开发提供了可行路径。
---

## 摘要
AI 模型是现代智能系统的基石，推动了科学、医学、金融和技术领域的进步。然而，开发高性能 AI 模型仍然是一个劳动密集型过程，需要专家从业者迭代地设计架构、工程化表示、实现训练流水线，并通过经验评估来改进方法。现有的 AutoML 方法部分减轻了这一负担，但仍局限于超参数优化和预定义搜索空间内的模型选择等狭窄方面，使得整个开发生命周期在很大程度上依赖于人类专业知识。为了填补这一空白，我们推出了 AIBuildAI，这是一种能够根据任务描述和训练数据自动构建 AI 模型的 AI 智能体。AIBuildAI 采用分层智能体架构，其中一个管理智能体协调三个专门的子智能体：负责建模策略的设计者（designer）、负责实现和调试的编码者（coder），以及负责训练和性能优化的调优者（tuner）。每个子智能体本身都是基于大语言模型（LLM）的智能体，具备多步推理和工具使用能力，从而实现了超出现有 AutoML 方法范畴的 AI 模型开发全过程端到端自动化。我们在 MLE-Bench 上评估了 AIBuildAI，这是一个包含视觉、文本、时间序列和表格模态的真实 Kaggle 风格 AI 开发任务基准。AIBuildAI 在 MLE-Bench 上排名第一，奖牌率为 63.1%，优于所有现有的基准方法，并达到了资深 AI 工程师的能力水平。这些结果表明，分层智能体系统可以自动化从任务规范到可部署模型的完整 AI 模型开发过程，为实现只需极少人工干预的广泛可及的 AI 开发提供了一条路径。

## Abstract
AI models underpin modern intelligent systems, driving advances across science, medicine, finance, and technology. Yet developing high-performing AI models remains a labor-intensive process that requires expert practitioners to iteratively design architectures, engineer representations, implement training pipelines and refine approaches through empirical evaluation. Existing AutoML methods partially alleviate this burden but remain limited to narrow aspects such as hyperparameter optimization and model selection within predefined search spaces, leaving the full development lifecycle largely dependent on human expertise. To address this gap, we introduce AIBuildAI, an AI agent that automatically builds AI models from a task description and training data. AIBuildAI adopts a hierarchical agent architecture in which a manager agent coordinates three specialized sub-agents: a designer for modeling strategy, a coder for implementation and debugging, and a tuner for training and performance optimization. Each sub-agent is itself a large language model (LLM) based agent capable of multi-step reasoning and tool use, enabling end-to-end automation of the AI model development process that goes beyond the scope of existing AutoML approaches. We evaluate AIBuildAI on MLE-Bench, a benchmark of realistic Kaggle-style AI development tasks spanning visual, textual, time-series and tabular modalities. AIBuildAI ranks first on MLE-Bench with a medal rate of 63.1%, outperforming all existing baseline methods and matching the capability of highly experienced AI engineers. These results demonstrate that hierarchical agent systems can automate the full AI model development process from task specification to deployable model, suggesting a pathway toward broadly accessible AI development with minimal human intervention.

---

## 论文详细总结（自动生成）

# AIBuildAI: 自动构建 AI 模型的层次化智能体系统

### 1. 核心问题与整体含义
开发高性能 AI 模型目前仍是一个高度依赖专家经验、劳动密集且需要反复迭代的过程（涉及架构设计、数据预处理、代码实现、超参数调优及调试）。现有的 **AutoML** 方法大多局限在预定义空间内的超参搜索，而新兴的 **LLM 编码智能体** 往往受限于单次调用（Single-shot）的复杂处理能力。
**AIBuildAI** 的提出旨在实现 AI 模型开发全生命周期的端到端自动化，通过模拟人类研究团队的协作模式，解决复杂 AI 任务中策略设计与工程实现脱节的问题。

### 2. 方法论：核心思想与技术细节
AIBuildAI 采用了一种**分层智能体架构（Hierarchical Agent Architecture）**，其核心思想是将复杂的开发任务解耦为不同职能的专业子智能体，由管理者统一调度：

*   **管理者 (Manager)：** 顶层决策者，负责维护多个并行的解决方案仓库（Repositories）。它通过长上下文推理分析执行历史，决定何时调用哪个子智能体，并负责资源的分配与优胜劣汰。
*   **设计者 (Designer)：** 负责建模策略。利用 Web 搜索获取前沿方案，根据训练反馈（如过拟合、欠拟合）修订模型架构或数据处理计划。
*   **编码者 (Coder)：** 负责代码实现与调试。将设计方案转化为可执行的 Python 代码，通过“编写-运行-报错-修复”的闭环确保流水线无误。
*   **调优者 (Tuner)：** 负责性能优化。通过监控训练日志、调整超参数和学习率策略，在有限时间内压榨模型性能。
*   **辅助组件：** 包括 **Setup 智能体**（初始化 Conda 环境和依赖）和 **Aggregator 智能体**（在任务结束前进行模型集成或选择最优提交）。

**关键技术点：** 每个子智能体本身都是一个独立的 LLM Agent，拥有专属的 Prompt、上下文和工具集，支持多轮推理和工具交互，而非简单的单次 API 调用。

### 3. 实验设计
*   **基准测试 (Benchmark)：** 使用了 **MLE-Bench**，这是目前评估 AI 工程智能体最权威的基准之一，包含 75 个真实的 Kaggle 风格任务。
*   **任务模态：** 涵盖图像分类、目标检测、语义分割、视频理解、自然语言处理（分类、问答、生成）、时间序列预测及结构化表格数据分析。
*   **对比方法：** 与 26 种基准方法进行对比，包括 **MARS、MLEvolve、AIRA-dojo、AIDE、R&D-Agent** 等当前最先进的系统。
*   **评价指标：** 采用 Kaggle 奖牌率（Medal Rate），即智能体提交的结果在人类竞赛排行榜中所处的百分位水平。

### 4. 资源与算力
*   **骨干模型：** 使用 **Claude 4.6 Opus** 作为所有智能体的底层大语言模型。
*   **硬件配置：** 单台 Linux 服务器，配备 **1 张 NVIDIA A100 GPU (80GB)**，24 个 vCPU 和 256GB RAM。
*   **时间预算：** 每个任务限制在 **24 小时** 内完成。
*   **并行度：** 每个任务初始化 **7 个并行** 的解决方案仓库。

### 5. 实验数量与充分性
*   **实验规模：** 在 75 个不同难度的任务上进行了完整测试，包括 22 个低难度、38 个中难度和 15 个高难度任务。
*   **客观性：** 实验严格遵循 MLE-Bench 的官方评估协议，使用私有测试集进行评分，确保了结果的真实性。
*   **公平性：** 对比实验中明确列出了基准方法所使用的模型（如 o3, Gemini 3 Pro）和算力资源，尽管硬件略有差异（部分基准使用 H200），但 AIBuildAI 在同等或更低硬件条件下取得了更优结果。

### 6. 主要结论与发现
*   **性能夺冠：** AIBuildAI 以 **63.1% 的总奖牌率** 位居 MLE-Bench 排行榜第一（截至 2026 年 3 月）。
*   **攻坚能力强：** 在中等和高难度任务中，AIBuildAI 的领先优势扩大，高难度任务奖牌率达到 46.67%，显著高于其他智能体。
*   **全能性：** 在视觉、文本、时间序列和表格四大领域均表现出超越基准方法的性能，证明了其通用性。
*   **分层优势：** 实验证明，将“设计-编码-调优”分离能有效避免上下文爆炸，提高复杂任务的成功率。

### 7. 优点
*   **架构解耦：** 模拟人类团队协作，使每个子智能体专注于特定领域，降低了 LLM 处理长代码和复杂逻辑的负担。
*   **多步推理：** 相比于单次生成代码的方案，其“Agent-based”操作允许在子步骤内进行多次调试和自我修正。
*   **端到端闭环：** 从环境配置到最终的模型集成（如 Rank-based Blending），实现了真正意义上的无人值守开发。

### 8. 不足与局限
*   **成本较高：** 由于采用了多智能体频繁交互和多轮 LLM 调用，Token 消耗量巨大，运行成本高于单智能体系统。
*   **通信开销：** 管理者与子智能体之间的反复沟通增加了系统延迟。
*   **算力调度局限：** 目前主要针对单 GPU 场景，对于需要大规模分布式训练（多机多卡）的超大型模型开发，其资源调度能力尚待验证。
*   **知识依赖：** 系统的上限仍受限于底层 LLM（如 Claude Opus）的参数知识，对于极度冷门或全新的 AI 库可能支持不足。

（完）
