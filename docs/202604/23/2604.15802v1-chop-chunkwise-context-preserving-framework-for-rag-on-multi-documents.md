---
title: "CHOP: Chunkwise Context-Preserving Framework for RAG on Multi Documents"
title_zh: CHOP：面向多文档 RAG 的块级上下文保留框架
authors: "Hyunseok Park, Jihyeon Kim, Jongeun Kim, Dongsik Yoon"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15802v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 块状上下文保留框架以提高RAG准确性
tldr: "针对检索增强生成（RAG）在处理多文档时因内容相似导致检索精度下降的问题，本文提出CHOP框架。该框架通过CNM-Extractor提取块级元数据（类别、名词、模型名）并利用连续性决策模块保持上下文连贯性。通过在分块中嵌入上下文感知元数据，CHOP显著减少了语义冲突，在基准测试中实现了90.77%的Top-1命中率，有效提升了知识库的检索质量。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.15802v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of the CHOP architecture comprising two components: the Continuity Decision module, which determines whether consecutive chunks are continuous, and theCNM-Extractor, which generates compact representations.When continuity holds (left), the next chunk inherits the previous CNM; when it does not (right), a new CNM is extracted. Each chunk is then prefixed with its CNM, and the combined representations are embedded to construct the vector database.\", \"page\": 2, \"index\": 1, \"width\": 1050, \"height\": 394}]"
motivation: 解决RAG系统中相似文档共存导致的检索混淆、幻觉及事实错误问题。
method: 提出包含CNM-Extractor（提取块签名）和连续性决策模块（保持上下文连贯）的CHOP框架，通过元数据前缀增强分块辨识度。
result: "在基准数据集上实现了90.77%的Top-1命中率，并显著提升了检索排名质量。"
conclusion: CHOP通过上下文感知的元数据增强，为构建高质量、可扩展的多文档RAG知识库提供了有效方案。
---

## 摘要
当相似文档共存于向量数据库中时，检索增强生成（RAG）系统的检索准确率会下降，从而导致冗余信息、幻觉和事实错误。为了缓解这一问题，我们提出了 CHOP 框架，该框架利用大语言模型（LLM）迭代评估数据块（chunk）的相关性，并通过确定其与特定主题或查询类型的关联来逐步重构文档。CHOP 集成了两个核心组件：CNM-Extractor，用于生成捕获类别、关键名词和模型名称的紧凑型块级特征签名；以及连续性决策模块（Continuity Decision Module），通过判断连续数据块是否属于同一文档流来保持上下文连贯性。通过为每个数据块添加上下文感知元数据前缀，CHOP 减少了相似文档之间的语义冲突，并增强了检索器的辨别能力。在基准数据集上的实验表明，CHOP 缓解了检索混淆，并为构建高质量知识库提供了一种可扩展的方法，实现了 90.77% 的 Top-1 命中率，并在排序质量指标上取得了显著提升。

## Abstract
Retrieval-Augmented Generation (RAG) systems lose retrieval accuracy when similar documents coexist in the vector database, causing unnecessary information, hallucinations, and factual errors. To alleviate this issue, we propose CHOP, a framework that iteratively evaluates chunk relevance with Large Language Models (LLMs) and progressively reconstructs documents by determining their association with specific topics or query types. CHOP integrates two key components: the CNM-Extractor, which generates compact per-chunk signatures capturing categories, key nouns, and model names, and the Continuity Decision Module, which preserves contextual coherence by deciding whether consecutive chunks belong to the same document flow. By prefixing each chunk with context-aware metadata, CHOP reduces semantic conflicts among similar documents and enhances retriever discrimination. Experiments on benchmark datasets show that CHOP alleviates retrieval confusion and provides a scalable approach for building high-quality knowledge bases, achieving a Top-1 Hit Rate of 90.77% and notable gains in ranking quality metrics.

---

## 论文详细总结（自动生成）

以下是对论文《CHOP: Chunkwise Context-Preserving Framework for RAG on Multi Documents》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：传统的检索增强生成（RAG）系统在处理包含大量相似文档（如不同型号但功能相近的产品手册）的知识库时，容易出现检索精度下降的问题。
*   **核心挑战**：
    *   **语义冲突**：相似文档的分块在向量空间中距离极近，导致检索器难以区分。
    *   **上下文断裂**：基于长度的硬分块会切断指代关系（如“该方法”、“图3”），导致分块失去独立解释性。
    *   **检索混淆**：无关但语义相近的片段会误导大模型，产生幻觉或事实错误。

### 2. 论文提出的方法论：CHOP 框架
CHOP（Chunkwise Context-Preserving）框架的核心思想是通过**元数据增强**和**连续性感知**来重构文档分块，确保每个分块都携带明确的上下文标签。

*   **CNM-Extractor（特征提取器）**：
    *   为每个分块提取三元组签名：**Category（类别）**、**Nouns（关键名词/功能）**、**Model（具体型号/系列）**。
    *   利用 LLM 按照严格的 JSON 格式提取这些信息，并将其作为前缀（Prefix）添加到原始文本块中。
*   **Continuity Decision Module（连续性决策模块）**：
    *   **核心逻辑**：判断相邻的两个分块 $C_i$ 和 $C_{i+1}$ 是否属于同一个文档流或主题。
    *   **算法流程**：
        1.  如果 LLM 判断为“连续”（TRUE），则 $C_{i+1}$ 直接继承 $C_i$ 的 CNM 标签，保持语义一致性。
        2.  如果判断为“不连续”（FALSE），则触发 CNM-Extractor 为新主题重新提取标签。
*   **向量化与存储**：将“CNM 前缀 + 原始文本”组合后进行 Embedding，存入向量数据库（如 ChromaDB）。这种做法在不重新训练检索模型的情况下，人为拉开了相似但不同属文档的向量距离。

### 3. 实验设计
*   **数据集**：使用 **MRAMG-Bench**（源自 ManualsLib 的产品手册基准）。研究者将其重构为连续的长文档，以模拟真实世界中缺乏明确文档边界的复杂场景。
*   **对比方法（Baselines）**：
    *   **Naive-500T**：固定长度分块（500 token，100 token 重叠）。
    *   **Cosine-Chunking**：基于句子间余弦相似度阈值的自适应分块方法。
*   **评估指标**：
    *   **检索阶段**：Hit Rate@K、MRR@K、NDCG@K。
    *   **生成阶段**：F1-score、ROUGE-L、BERTScore。

### 4. 资源与算力
*   **模型使用**：
    *   **Backbone LLM**：使用 **Gemma-12B** 作为分类和提取的核心模型（Temperature 设为 0 以保证确定性）。
    *   **Embedding 模型**：使用 OpenAI 的 `text-embedding-3-large`（3072 维）。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号、数量及处理总时长。由于涉及对每个分块进行 LLM 处理，其预处理阶段的计算开销会高于传统方法。

### 5. 实验数量与充分性
*   **实验规模**：针对检索和生成两个阶段分别进行了多组 Top-K（1, 3, 5, 10）的对比实验。
*   **充分性评价**：
    *   实验涵盖了从基础检索命中率到高级语义匹配（BERTScore）的全面指标。
    *   通过重构数据集来模拟“高重叠度”场景，针对性较强。
    *   **局限性**：实验主要集中在技术手册类文档，对于非结构化或文学类多文档的泛化能力尚待验证；消融实验部分相对薄弱，未深入探讨 CNM 三个维度各自的贡献度。

### 6. 主要结论与发现
*   **检索性能大幅提升**：CHOP 的 **Top-1 Hit Rate 达到 90.77%**，远高于 Naive 方法（81.28%）和 Cosine-Chunking（70.77%）。
*   **排序质量更优**：NDCG@10 提升了约 7.53%，说明 CHOP 不仅能找对分块，还能将其排在更靠前的位置。
*   **生成质量同步改善**：更精准的检索直接降低了生成阶段的幻觉风险，F1 和 ROUGE-L 指标均有稳定增益。
*   **连续性决策的价值**：通过继承标签，减少了因频繁调用提取器产生的噪声，维持了长文档内部的语义连贯。

### 7. 优点与亮点
*   **即插即用**：无需重新训练 Embedding 模型或微调 LLM，通过预处理端的“前缀注入”即可生效。
*   **解决指代消解**：通过在每个块前重复关键元数据，有效解决了分块后“它”、“该设备”等代词失去指向的问题。
*   **鲁棒性强**：在处理极度相似的型号（如 225B vs 226R）时，显式的模型名注入显著增强了检索器的辨别力。

### 8. 不足与局限
*   **预处理成本高**：每个分块（或每对分块）都需要调用 LLM 进行连续性判断和特征提取，对于海量文档库，索引构建的时间和 API 成本较高。
*   **模板依赖**：CNM 提取依赖于预设的 Prompt 模板，对于不符合“类别-名词-型号”结构的文档（如法律条文或创意写作），效果可能会打折扣。
*   **推理延迟**：虽然检索阶段很快，但整个系统的复杂性增加，且对 LLM 提取信息的准确性有较强依赖。

（完）
