---
title: "Graph-of-Agents: A Graph-based Framework for Multi-Agent LLM Collaboration"
title_zh: Graph-of-Agents：一种基于图的多智能体大语言模型协作框架
authors: "Sukwon Yun, Jie Peng, Pingzhi Li, Wendong Fan, Jie Chen, James Zou, Guohao Li, Tianlong Chen"
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.17148v1"
tags: ["query:ma-kf"]
score: 10.0
evidence: 基于图的多智能体LLM协作与通信框架
tldr: 针对多大型语言模型（LLM）协作中代理选择难、通信效率低等挑战，本文提出Graph-of-Agents (GoA) 框架。该框架通过模型卡片筛选相关代理，构建基于响应相关性的有向图，并利用双向消息传递机制增强和精炼代理响应，最后通过图池化聚合结果。实验证明，GoA仅需少量代理即可超越全量代理的基线模型，为多模型协作提供了高效且可扩展的解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17148v1/fig-001.webp\", \"caption\": \"Figure 1: Current multi-agent LLM pipeline and our proposed approach.(a) Given a query that spans diverse domains (e.g., biomedical + math + code), (b) selecting and organizing agents from a large pool of LLMs to form an effective multi-agent system remains a significant challenge. (c) The current Mixture-of -Agents (MoA) approach integrates all available agents, aggregates their responses, and forwards the combined output to the next layer. However, it lacks generalizability to larger agent pools (e.g., 10 or 100) and suffers from heavy intra-layer communication overhead. (d) In contrast, our proposed Graph-of Agents (GoA) addresses this challenge through a graph-based structure. In GoA, only a subset of relevant agents is selected to form the graph’s nodes, with intra-layer communication facilitated via directed message passing—flowing from more relevant agents to less relevant ones. By leveraging this graph structure, GoA achieves greater scalability and enables more efficient communication between agents, resulting in a more powerful and adaptive framework.\", \"page\": 2, \"index\": 1, \"width\": 822, \"height\": 229}, {\"url\": \"assets/figures/arxiv/2604.17148v1/fig-002.webp\", \"caption\": \"Table 1: Benchmark performance across multi-domain and domain-specific benchmarks. Singleagent baselines: General: Qwen2.5-7B-Instruct (Team, 2024), Code: Qwen2.5-Coder-7BInstruct (Hui et al., 2024), Math: Mathstral-7B-v0.1 (AI, 2024), Biomedical: Bio-Medical-Llama3-8B (Con, 2024), Finance: finance-Llama3-8B (Cheng et al., 2024), Legal: Saul-7B-Instructv1 (Colombo et al., 2024). Multi-agent baselines: Debate (Du et al., 2023b), Self-Consistency (SC) (Wang et al., 2023a), Refine (Madaan et al., 2023), ReConcile (Chen et al., 2023), MoA (Wang et al., 2024a), and Self-MoA (Li et al., 2025). Our proposed framework, GoA (Graph-of-Agents), despite using only 3 agents (i.e., top-k=3), outperforms both multi-agent baselines with 6 agents and single-agent models, demonstrating strong collaborative synergy and capability across both multidomain and domain-specific tasks. All performance is measured using zero-shot CoT in test-time.\", \"page\": 7, \"index\": 2, \"width\": 818, \"height\": 373}, {\"url\": \"assets/figures/arxiv/2604.17148v1/fig-003.webp\", \"caption\": \"Figure 2: Overall pipeline of GoA. (a) Overview: Given a query (Q) spanning diverse domains, GoA approaches multi-agent LLMs through the lens of a graph framework and produces an answer (A). (b) Node Sampling: Each agent is mapped to a model card containing domain and task information. The Meta-LLM, a general-purpose LLM, takes Q and the model cards as input and selects the most relevant agents, forming an adaptive multi-agent framework. (c) Edge Sampling: After collecting initial responses from selected agents, each agent evaluates the relevance of others (excluding itself) to generate a normalized score matrix. Edges are established in Source-to-Target and Target-to-Source directions, while low-relevance nodes are pruned using a threshold τ=0.05. (d) Message Passing: GoA first passes messages from source to target nodes, allowing lower-ranked agents to refine responses, then reverses the flow to update source nodes. (e) Graph Pooling: With updated responses structured as a graph, GoA outputs the final prediction via max or mean pooling.\", \"page\": 4, \"index\": 3, \"width\": 826, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2604.17148v1/fig-004.webp\", \"caption\": \"Figure 3: Reasoning process of (a) Mixture-of-Agents (MoA) and (b) Graph-of-Agents (GoA).\", \"page\": 9, \"index\": 4, \"width\": 788, \"height\": 425}]"
motivation: 现有多代理协作框架在代理筛选、有效通信和响应整合方面存在不足，难以应对日益增长的LLM生态。
method: 提出一种基于图的框架，通过节点采样筛选代理，并利用双向消息传递和图池化技术实现代理间的结构化协作。
result: 在MMLU、MATH等多个通用及专业领域基准测试中，GoA仅使用3个代理便优于使用6个代理的现有基线模型。
conclusion: GoA通过结构化消息传递实现了高效且可扩展的多代理协作，是解决复杂多模型调度问题的有力方案。
---

## 摘要
随着大语言模型（LLM）及其基准测试的日益增多，编排多个模型以提升任务性能的需求变得前所未有的迫切。虽然 Mixture-of-Agents (MoA) 等框架尝试协调多个 LLM，但它们在以下方面往往存在不足：(1) 选择相关智能体，(2) 促进有效的智能体间通信，以及 (3) 高效整合响应。在这项工作中，我们提出了 Graph-of-Agents (GoA)，这是一种用于建模多智能体 LLM 通信的新型基于图的框架。我们的方法首先进行节点采样，利用总结了每个模型领域、任务专长及其他特征的模型卡片，仅选择最相关的智能体。接着，我们通过相互评估响应来确定相关性排序，从而在选定智能体之间构建边。随后，执行从高相关性智能体到低相关性智能体的定向消息传递以增强其响应，接着进行反向消息传递以细化高相关性智能体的原始响应。最后，通过基于图的池化（如最大或平均池化）聚合更新后的响应，生成统一的答案。我们在多领域基准测试（MMLU、MMLU-Pro、GPQA）和特定领域基准测试（MATH、HumanEval、MedMCQA）上评估了 GoA，智能体池包含 6 个跨领域的 LLM。令人惊讶的是，GoA 仅使用 3 个选定智能体就实现了卓越性能，优于同时使用全部 6 个智能体的最新多智能体 LLM 基准。通过采用图结构，GoA 凭借结构化消息传递提供了可扩展性和有效性，使其成为应对不断增长的 LLM 种类的有力候选方案。代码见：https://github.com/UNITES-Lab/GoA。

## Abstract
With an ever-growing zoo of LLMs and benchmarks, the need to orchestrate multiple models for improved task performance has never been more pressing. While frameworks like Mixture-of-Agents (MoA) attempt to coordinate LLMs, they often fall short in terms of (1) selecting relevant agents, (2) facilitating effective intra-agent communication, and (3) integrating responses efficiently. In this work, we propose Graph-of-Agents (GoA), a new graph-based framework for modeling multi-agent LLM communication. Our approach begins with node sampling, selecting only the most relevant agents by leveraging model cards that summarize each model's domain, task specialization, and other characteristics. Next, we construct edges between the selected agents by evaluating their responses against one another to determine relevance ordering. Directed message passing is then performed from highly relevant agents to less relevant ones to enhance their responses, followed by reverse message passing to refine the original responses of the more relevant agents. Finally, the updated responses are aggregated via graph-based pooling (e.g., max or mean pooling) to produce a single, unified answer. We evaluate GoA on diverse multi-domain benchmarks (MMLU, MMLU-Pro, GPQA) and domain-specific benchmarks (MATH, HumanEval, MedMCQA), with an agent pool of 6 LLMs spanning multiple domains. Surprisingly, GoA achieves superior performance using only 3 selected agents, outperforming recent multi-agent LLM baselines that utilize all 6 agents simultaneously. By adopting a graph structure, GoA offers both scalability and effectiveness through structured message passing-positioning it as a strong candidate for navigating the challenges of the ever-growing LLM zoo. Code is available at: https://github.com/UNITES-Lab/GoA.

---

## 论文详细总结（自动生成）

这是一份关于论文《Graph-of-Agents: A Graph-based Framework for Multi-Agent LLM Collaboration》的结构化分析报告：

### 1. 论文的核心问题与整体含义
随着大语言模型（LLM）生态的爆发，如何有效协同多个专业化模型（LLM Zoo）处理复杂任务成为核心挑战。现有的多智能体协作框架（如 Mixture-of-Agents, MoA）存在三大痛点：
*   **代理选择难**：通常无差别调用所有模型，导致计算冗余并引入无关噪声。
*   **通信效率低**：多采用“多对一”的聚合方式，缺乏精细化的点对点交互。
*   **整合成本高**：简单的 Token 拼接导致计算复杂度随代理数量呈线性爆炸。
**研究动机**：提出一种基于图的框架（GoA），通过结构化的节点选择、有向消息传递和图池化，实现更高效、可扩展的多模型协同。

### 2. 论文提出的方法论：GoA 框架
GoA 将多智能体协作建模为有向图 $G=(V, E)$，核心流程分为四个阶段：
1.  **节点采样 (Node Sampling)**：利用模型卡片（Model Cards，包含领域、任务专长、参数量等元数据），由一个 Meta-LLM 根据查询（Query）从代理池中筛选出最相关的 $k$ 个代理作为图节点。
2.  **边采样 (Edge Sampling)**：选定代理生成初始响应后，相互进行评分。根据评分构建有向边，并设置阈值 $\tau$ 剪枝低相关性节点，生成加权邻接矩阵。
3.  **消息传递 (Message Passing)**：
    *   **源到目标 (Source-to-Target)**：高相关性（高分）代理将信息传递给低相关性代理，帮助后者修正错误。
    *   **目标到源 (Target-to-Source)**：低相关性代理反馈精炼后的信息给高相关性代理，实现共识优化。
4.  **图池化 (Graph Pooling)**：
    *   **Max-Pooling**：选择入度最高（最受认可）的代理响应作为最终答案。
    *   **Mean-Pooling**：由 Meta-LLM 综合所有更新后的响应，生成加权平均后的最终答案。

### 3. 实验设计
*   **数据集/基准测试**：
    *   **通用多领域**：MMLU、MMLU-Pro、GPQA。
    *   **特定领域**：MATH（数学）、HumanEval（代码）、MedMCQA（生物医学）。
*   **代理池配置**：包含 6 个 7B-8B 规模的专业模型（涵盖通用、代码、数学、生物医疗、金融、法律领域）。
*   **对比方法**：
    *   **单模型基线**：Qwen2.5-7B 等。
    *   **多模型基线**：Debate（辩论）、Self-Consistency（自一致性）、Refine（自我修正）、ReConcile、MoA、Self-MoA、DyLAN。

### 4. 资源与算力
*   **硬件设施**：论文在附录中明确提到，所有实验均在 **NVIDIA A6000 GPU** 上进行。
*   **算力细节**：未详细列出总训练时长（因为该框架主要基于推理侧的 Prompt 接口，无需重新训练模型），但提供了推理效率对比：在 MMLU-Pro 上，GoA 的延迟（Time）和 Token 消耗量仅为 MoA 的约 **40%-50%**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 6 个主流基准测试，并针对 GPT-4o 等闭源模型进行了扩展性验证。
*   **消融实验**：非常充分。作者测试了“反向消息传递”、“移除反馈机制”、“不进行评分加权”、“改变 $k$ 值”以及“调整剪枝阈值 $\tau$”等多种变体。
*   **客观性**：通过分层抽样（如 MMLU 每类 50 个样本）确保评估的代表性，并对比了相同代理数量下的不同框架性能，实验设计较为公平。

### 6. 论文的主要结论与发现
*   **“3 > 6”效应**：GoA 仅使用 **3 个** 选定代理的性能，就超过了使用全部 **6 个** 代理的 MoA 等基线模型。
*   **结构化通信的价值**：双向消息传递（尤其是从高相关性到低相关性的引导）对消除噪声、提升逻辑严密性至关重要。
*   **高效性**：通过节点采样和图池化，GoA 显著降低了 LLM 调用次数和 Token 成本，同时提升了准确率。
*   **泛化性**：该框架在开源模型（7B 级）和闭源模型（GPT-4o）上均表现出一致的增益。

### 7. 优点（亮点）
*   **动态拓扑**：不同于以往静态的代理协作，GoA 根据 Query 动态构建图结构，更具灵活性。
*   **引入模型卡片**：巧妙利用模型元数据进行预筛选，解决了“代理爆炸”问题。
*   **双向反馈机制**：借鉴了图神经网络的消息传递思想，使代理间的交互不再是简单的线性堆叠，而是深度的语义对齐。

### 8. 不足与局限
*   **Meta-LLM 依赖**：节点采样和最终聚合高度依赖 Meta-LLM 的判断力，若 Meta-LLM 选错模型，后续流程可能失效。
*   **超参数敏感性**：阈值 $\tau$ 和采样数 $k$ 需要根据不同任务手动微调，缺乏完全自动化的自适应机制。
*   **推理轮次增加**：虽然 Token 总数减少了，但由于存在多轮消息传递，可能会增加推理的串行等待时间（尽管论文称总时间下降，但在极大规模代理池下可能存在瓶颈）。

（完）
