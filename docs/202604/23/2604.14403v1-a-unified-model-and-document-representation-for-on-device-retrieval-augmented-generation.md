---
title: A Unified Model and Document Representation for On-Device Retrieval-Augmented Generation
title_zh: 一种用于端侧检索增强生成的统一模型与文档表示
authors: "Julian Killingback, Ofer Meshi, Henry Li, Hamed Zamani, Maryam Karimzadehgan"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.14403v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 管理端侧 RAG 的上下文大小与检索
tldr: 本文针对端侧检索增强生成（RAG）面临的存储与内存瓶颈，提出一种统一模型，将检索与上下文压缩功能整合。该方法通过共享文档表示，在不增加存储负担的前提下，将生成所需的上下文长度缩减至原来的1/10，同时保持了与传统RAG相当的性能。这是首个统一检索与压缩的模型，有效解决了端侧部署在隐私保护、延迟降低与资源优化方面的挑战。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.14403v1/fig-001.webp\", \"caption\": \"Table 4: Ablation study results for ECG on NQ and TriviaQA datasets across different k values.\", \"page\": 21, \"index\": 1, \"width\": 822, \"height\": 242}, {\"url\": \"assets/figures/arxiv/2604.14403v1/fig-002.webp\", \"caption\": \"Table 5: Hyperparameters for Self-Supervised Pretraining stage for models without contrastive loss (i.e. with only the generative loss). These models were used to initialize the context compression baseline models including the SmolLM model with the same architecture as the ECG model. The SmolLM model trained with MLPs was also used for the ECG ablation which does not use contrastive loss for pretraining.\", \"page\": 21, \"index\": 2, \"width\": 740, \"height\": 186}, {\"url\": \"assets/figures/arxiv/2604.14403v1/fig-003.webp\", \"caption\": \"Figure 1: Inference usage of the ECG (Embed, Compress, Generate) models.\", \"page\": 2, \"index\": 3, \"width\": 818, \"height\": 332}, {\"url\": \"assets/figures/arxiv/2604.14403v1/fig-004.webp\", \"caption\": \"Table 1: Comparison between Exact Match (EM) accuracy for ECG models (ours) and baseline models with a fixed context budget, which is the number of vectors used to represent the context documents.\", \"page\": 7, \"index\": 4, \"width\": 822, \"height\": 307}, {\"url\": \"assets/figures/arxiv/2604.14403v1/fig-005.webp\", \"caption\": \"Table 7: Hyperparameters for RAG training for the baseline Reader Models.\", \"page\": 22, \"index\": 5, \"width\": 826, \"height\": 257}, {\"url\": \"assets/figures/arxiv/2604.14403v1/fig-006.webp\", \"caption\": \"Table 8: Hyperparameters for RAG training for the baseline context compression models.\", \"page\": 22, \"index\": 6, \"width\": 740, \"height\": 185}, {\"url\": \"assets/figures/arxiv/2604.14403v1/fig-007.webp\", \"caption\": \"Table 12: Hyperparameter Search Space. We performed a grid search over the union of learning rates (LR) and weight decay (WD) values explored for both SmolLM-v2 and Gemma 3 architectures. Note that for each model we started with an expected reasonable learning rate based on initial experiments and continued to search until the best LR was not the largest or smallest LR, thus each model was usually not evaluated on each LR value. This is also why some models have substantially different starting and ending points.\", \"page\": 23, \"index\": 7, \"width\": 826, \"height\": 346}, {\"url\": \"assets/figures/arxiv/2604.14403v1/fig-008.webp\", \"caption\": \"Table 2: Performance comparison of the Exact Match (EM) accuracy between the ECG models and baseline models at a fixed performance level. Disk Space / 10k, represents the size the index/embeddings for 10k passages. For all the embedding based retrieval models and ECG models we only count the size of the embeddings as otherwise the size would be heavily dependent on the indexing approach used which is orthogonal to our contribution. The columns Context and Comp. represent the context budget and relative compression of the ECG model. *The size of BM25 is actually, 9.23e−6, but for consistent formatting we rounded it to 0.0 in the table.\", \"page\": 8, \"index\": 8, \"width\": 806, \"height\": 319}, {\"url\": \"assets/figures/arxiv/2604.14403v1/fig-009.webp\", \"caption\": \"Table 3: Performance comparison between context compression baseline and a modified context compression model with the same architecture as the ECG models using ColBERT retrievals. ECG-Reader is the ECG model used only as a reader on top of the ColBERT retrievals. We show the EM for a single document and the best EM found for documents (1-5) where the number of documents is given as k′.\", \"page\": 19, \"index\": 9, \"width\": 797, \"height\": 242}, {\"url\": \"assets/figures/arxiv/2604.14403v1/fig-010.webp\", \"caption\": \"Figure 2: Accuracy (EM) of various retriever and reader combinations on NQ across different values of k for SmolLM and Gemma models.\", \"page\": 9, \"index\": 10, \"width\": 769, \"height\": 226}]"
motivation: 传统云端RAG存在隐私风险和高延迟，而端侧部署受限于有限的内存和磁盘空间，需要平衡模型性能与资源消耗。
method: 提出一种统一模型和文档表示方法，利用同一套表示同时实现高效检索与上下文压缩，从而降低磁盘占用和生成时的内存开销。
result: 实验表明，该模型在仅使用1/10上下文长度的情况下，性能即可媲美传统RAG，且存储需求未超过多向量检索模型。
conclusion: 本研究通过整合检索与压缩模型，为受限资源环境下的端侧RAG性能优化开辟了新方向。
---

## 摘要
传统的检索增强生成（RAG）方法通常假设检索和生成过程发生在远离终端用户的强大服务器上。虽然这减轻了本地硬件的限制，但也带来了显著的缺点：数据访问的隐私担忧、持续的维护和存储成本、延迟增加以及对互联网连接的依赖。端侧 RAG 通过在本地执行整个流水线来解决这些挑战，使其成为查询财务文档、联系方式和病史等敏感个人信息的理想选择。然而，端侧部署需要在有限的内存和磁盘空间之间取得微妙的平衡。具体而言，必须限制提供给生成模型的上下文大小以管理 KV 缓存和注意力机制的内存占用，同时必须最小化存储嵌入的大小以节省磁盘空间。在这项工作中，我们提出了一种统一模型，该模型可以压缩 RAG 上下文，并利用相同的表示进行检索。与使用独立表示相比，这种方法最小化了磁盘利用率，同时显著减少了生成所需的上下文大小。在平均仅使用 1/10 上下文的情况下，我们的模型达到了传统 RAG 阅读器的性能，且与多向量检索模型相比没有增加存储需求。该方法是首个利用共享模型和表示来统一检索与上下文压缩的模型。我们相信这项工作将启发进一步整合不同的模型，以优化端侧性能。

## Abstract
Traditional Retrieval-Augmented Generation (RAG) approaches generally assume that retrieval and generation occur on powerful servers removed from the end user. While this reduces local hardware constraints, it introduces significant drawbacks: privacy concerns regarding data access, recurring maintenance and storage costs, increased latency, and the necessity of an internet connection. On-device RAG addresses these challenges by executing the entire pipeline locally, making it ideal for querying sensitive personal information such as financial documents, contact details, and medical history. However, on-device deployment necessitates a delicate balance between limited memory and disk space. Specifically, the context size provided to the generative model must be restricted to manage KV cache and attention memory usage, while the size of stored embeddings must be minimized to preserve disk space. In this work, we propose a unified model that compresses the RAG context and utilizes the same representations for retrieval. This approach minimizes disk utilization compared to using separate representations, while significantly reducing the context size required for generation. With an average of 1/10 of the context, our model matches the performance of a traditional RAG reader without increasing storage requirements compared to a multi-vector retrieval model. This approach represents the first model to unify retrieval and context compression using a shared model and representation. We believe this work will inspire further consolidation of distinct models to optimize on-device performance.

---

## 论文详细总结（自动生成）

这是一份关于论文《A Unified Model and Document Representation for On-Device Retrieval-Augmented Generation》的结构化分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在资源受限的移动设备（端侧）上高效实现检索增强生成（RAG）。
*   **研究动机**：
    *   **隐私与延迟**：云端 RAG 存在隐私泄露风险、高延迟及对网络的依赖。
    *   **端侧瓶颈**：端侧设备内存（KV Cache 占用）和磁盘空间（存储嵌入向量）极度有限。
    *   **冗余问题**：现有方法通常为“检索”和“上下文压缩”使用独立的模型和表示，导致计算和存储开销翻倍。
*   **整体含义**：本文提出了一种名为 **ECG (Embed, Compress, Generate)** 的统一模型，通过共享文档表示，同时实现高效检索、上下文压缩和响应生成，旨在打破端侧 RAG 的资源瓶颈。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：使用同一个预训练解码器模型（LLM）和同一组向量表示来完成检索和生成任务。
*   **关键技术细节**：
    *   **统一架构**：在预训练 LLM 后添加特殊 Token（`<emb>`），通过两个投影块 $\theta_{ret}$（检索分支）和 $\theta_{comp}$（压缩分支）处理隐藏状态。
    *   **共享表示**：检索用的向量直接作为压缩上下文的输入，确保磁盘只需存储一套索引。
    *   **相似度度量**：采用均值池化的 MaxSim（类似 ColBERT），支持多向量表示以提高检索精度。
    *   **训练流程**：
        1.  **自监督预训练**：利用无标签语料进行“重构”和“邻近文本预测”，同时引入对比学习损失（Contrastive Loss）来对齐检索空间。
        2.  **RAG 专项微调**：使用知识蒸馏（Knowledge Distillation）。从强大的教师模型（Reader 和 Scorer）中学习答案生成和文档排序。
    *   **动态损失缩放**：引入可学习的参数来平衡检索损失和生成损失，防止多任务训练时的梯度冲突。

### 3. 实验设计
*   **数据集**：Natural Questions (NQ) 和 Trivia QA。
*   **模型规模**：SmolLM-v2 (135M) 和 Gemma 3 (1B)，代表典型的端侧轻量级模型。
*   **Benchmark**：Exact Match (EM) 准确率。
*   **对比方法**：
    *   **参数化知识**（无检索）。
    *   **标准 RAG**：结合不同检索器（BM25, Dense, ColBERT）和标准阅读器。
    *   **带上下文压缩的 RAG**：类似 COCOM 的方法，但检索与压缩分离。

### 4. 资源与算力
*   **硬件**：
    *   **训练**：使用了 **8 台 NVIDIA H100 GPU**。
    *   **推理**：使用了 NVIDIA A100 GPU 进行评估。
*   **训练时长**：文中未明确给出具体的总训练小时数，但提到自监督阶段训练了 1 个 Epoch，RAG 微调阶段训练了 10-20 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了两种不同架构和规模的基础模型（135M 和 1B）。
    *   对比了固定上下文预算（Context Budget）和固定性能目标两种场景。
    *   **消融实验**：非常详尽，验证了对比损失、生成蒸馏、预训练、损失缩放以及负采样策略对最终性能的影响。
*   **充分性与客观性**：实验设计较为严谨，通过网格搜索优化了基线模型的超参数以确保公平对比。使用了池化（Pooling）技术处理大规模语料检索，符合学术界标准做法。

### 6. 主要结论与发现
*   **极高的压缩比**：在仅使用 **1/10** 上下文长度的情况下，ECG 模型即可匹配标准 RAG 阅读器的性能。
*   **存储优势**：通过统一表示，比“检索+压缩”分离的方案节省了 **50%** 的磁盘空间。
*   **性能超越**：在严格的内存预算下，ECG 的 EM 得分是标准 RAG 的 3 倍以上。
*   **联合训练的必要性**：消融实验证明，如果不使用动态损失缩放，模型性能会下降近一半，说明检索和生成任务的平衡至关重要。

### 7. 优点
*   **端侧友好**：直接针对端侧 RAG 的痛点（内存和磁盘），提出了切实可行的统一化方案。
*   **端到端优化**：打破了检索和生成之间的壁垒，使向量表示同时具备语义检索能力和生成所需的丰富信息。
*   **灵活性**：支持可变长度的多向量嵌入，可根据设备性能动态调整检索精度和存储成本。

### 8. 不足与局限
*   **数据集偏差**：实验主要在 Wikipedia 等公开 QA 数据集上进行，尚未在真实的端侧场景（如个人私有文档、短信、邮件）中验证其泛化能力。
*   **噪声敏感性**：实验发现增加检索文档数量（k值）有时会导致性能下降，说明模型在处理多个压缩上下文时的抗干扰能力仍有提升空间。
*   **训练复杂性**：该方法依赖于复杂的两阶段训练和多任务损失调优，对训练数据的质量和教师模型依赖较强。

（完）
