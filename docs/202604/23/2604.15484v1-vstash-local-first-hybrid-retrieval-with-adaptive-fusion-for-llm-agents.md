---
title: "vstash: Local-First Hybrid Retrieval with Adaptive Fusion for LLM Agents"
title_zh: vstash：面向大语言模型智能体的自适应融合本地优先混合检索系统
authors: Jayson Steffens
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.15484v1"
tags: ["query:agent"]
score: 10.0
evidence: 大模型智能体的本地优先文档记忆系统
tldr: 本研究提出 vstash，一个面向 LLM 智能体的本地优先混合检索系统。它在 SQLite 中集成了向量搜索与全文检索，并利用倒数排名融合（RRF）及自适应 IDF 加权优化检索质量。核心贡献在于利用向量与全文检索结果的不一致性进行自监督嵌入微调，使 33M 参数的小模型在多个 BEIR 数据集上达到或超过 110M 参数模型的性能，同时保持极低的查询延迟。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.15484v1/fig-001.webp\", \"caption\": \"Table 3: Adaptive vs fixed RRF weights on 5 BEIR datasets (NDCG@10)\", \"page\": 6, \"index\": 1, \"width\": 981, \"height\": 297}, {\"url\": \"assets/figures/arxiv/2604.15484v1/fig-002.webp\", \"caption\": \"Table 7: Hybrid RRF at scale – 1,000 ML papers, 35 topic-based queries\", \"page\": 11, \"index\": 2, \"width\": 980, \"height\": 272}, {\"url\": \"assets/figures/arxiv/2604.15484v1/fig-003.webp\", \"caption\": \"Figure 1: vstash architecture.\", \"page\": 3, \"index\": 3, \"width\": 730, \"height\": 614}, {\"url\": \"assets/figures/arxiv/2604.15484v1/fig-004.webp\", \"caption\": \"Table 8: Search latency across corpus sizes\", \"page\": 12, \"index\": 4, \"width\": 979, \"height\": 311}, {\"url\": \"assets/figures/arxiv/2604.15484v1/fig-005.webp\", \"caption\": \"Table 2: Distance-based relevance signal on BEIR\", \"page\": 5, \"index\": 5, \"width\": 640, \"height\": 196}]"
motivation: 旨在为 LLM 智能体提供一个高性能、低延迟且易于部署的本地混合检索内存系统。
method: 结合 sqlite-vec 和 FTS5，采用自适应 IDF 加权的 RRF 融合算法，并利用检索不一致性产生的信号对嵌入模型进行自监督微调。
result: "微调后的 BGE-small 模型在 BEIR 数据集上 NDCG@10 显著提升，且在 5 万个数据块规模下中值延迟仅为 20.9 毫秒。"
conclusion: 通过自监督微调和自适应融合，轻量级本地检索系统可以在保证效率的同时，达到与大型复杂模型相当的检索精度。
---

## 摘要
我们介绍了 vstash，这是一个本地优先（local-first）的文档记忆系统，它通过倒数排名融合（Reciprocal Rank Fusion, RRF）和自适应单查询 IDF 加权，将向量相似度搜索与全文关键词匹配相结合。所有数据均存储在单个 SQLite 文件中，利用 sqlite-vec 进行近似最近邻搜索，并使用 FTS5 进行关键词匹配。我们做出了四个主要贡献：(1) 通过混合检索不一致性进行自监督嵌入微调：在 SciFact、NFCorpus 和 FiQA 的 753 个 BEIR 查询中，74.5% 的查询在重向量搜索（vec=0.95, fts=0.05）与重 FTS 搜索（vec=0.05, fts=0.95）之间产生了前 10 名结果的不一致（各数据集比例分别为 63.4% / 73.4% / 86.7%，见第 5.2 节），这在无需人工标注的情况下提供了免费的训练信号。使用 MultipleNegativesRankingLoss 在 7.6 万个不一致三元组上微调 BGE-small（3300 万参数），提升了所有 5 个 BEIR 数据集的 NDCG@10（在 NFCorpus 上相比 BGE-small 基础 RRF 提升高达 +19.5%，见表 6）。在不同预处理条件下，微调后的 33M 参数流水线在 5 个数据集中的 3 个上达到或超过了已发表的 ColBERTv2（1.1 亿参数）和未经训练的 BGE-base（1.1 亿参数）的结果；在 FiQA 和 ArguAna 上表现逊于 ColBERTv2（见第 5.5 节）。(2) 带有单查询 IDF 加权的自适应 RRF：相比固定权重，在所有 5 个 BEIR 数据集上均提升了 NDCG@10（在 ArguAna 上提升高达 +21.4%），在 SciFact 上配合 BGE-small 达到了 0.7263。(3) 关于 RRF 后评分的负面结果：频率+衰减、历史增强召回以及交叉编码器（cross-encoder）重排序均未能提升 NDCG。(4) 生产级底层架构：具备完整性检查、模式版本控制、排名诊断以及基于距离的相关性信号，并在 5 个 BEIR 数据集的 50,425 个经过相关性判定的查询中进行了验证。在 5 万个分块（chunks）规模下，搜索中值延迟保持在 20.9 毫秒，且 NDCG 表现稳定。微调后的模型已在 HuggingFace 发布为 Stffens/bge-small-rrf-v2。所有代码、数据和实验均已开源。

## Abstract
We present **vstash**, a local-first document memory system that combines vector similarity search with full-text keyword matching via Reciprocal Rank Fusion (RRF) and adaptive per-query IDF weighting. All data resides in a single SQLite file using sqlite-vec for approximate nearest neighbor search and FTS5 for keyword matching.   We make four primary contributions. **(1)** Self-supervised embedding refinement via hybrid retrieval disagreement: across 753 BEIR queries on SciFact, NFCorpus, and FiQA, 74.5% produce top-10 disagreement between vector-heavy (vec=0.95, fts=0.05) and FTS-heavy (vec=0.05, fts=0.95) search (per-dataset rates 63.4% / 73.4% / 86.7%, Section 5.2), providing a free training signal without human labels. Fine-tuning BGE-small (33M params) with MultipleNegativesRankingLoss on 76K disagreement triples improves NDCG@10 on all 5 BEIR datasets (up to +19.5% on NFCorpus vs. BGE-small base RRF, Table 6). On 3 of 5 datasets, under different preprocessing, the tuned 33M-parameter pipeline matches or exceeds published ColBERTv2 results (110M params) and an untrained BGE-base (110M); on FiQA and ArguAna it underperforms ColBERTv2 (Section 5.5). **(2)** Adaptive RRF with per-query IDF weighting improves NDCG@10 on all 5 BEIR datasets versus fixed weights (up to +21.4% on ArguAna), achieving 0.7263 on SciFact with BGE-small. **(3)** A negative result on post-RRF scoring: frequency+decay, history-augmented recall, and cross-encoder reranking all failed to improve NDCG. **(4)** A production-grade substrate with integrity checking, schema versioning, ranking diagnostics, and a distance-based relevance signal validated on 50,425 relevance-judged queries across the 5 BEIR datasets.   Search latency remains 20.9 ms median at 50K chunks with stable NDCG. The fine-tuned model is published as `Stffens/bge-small-rrf-v2` on HuggingFace. All code, data, and experiments are open-source.

---

## 论文详细总结（自动生成）

以下是对论文《vstash: Local-First Hybrid Retrieval with Adaptive Fusion for LLM Agents》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大型语言模型（LLM）智能体需要持久化记忆，但现有的云端向量数据库对于开发者工具、个人知识管理及隐私敏感等“本地优先（Local-first）”场景过于沉重。
*   **研究动机**：
    *   **检索质量瓶颈**：纯向量搜索易丢失精确关键词，纯关键词搜索（FTS）缺乏语义理解。
    *   **融合难题**：如何有效结合余弦距离与 BM25 排名这类不同分布的分数。
    *   **置信度缺失**：系统难以区分“找到了相关内容”与“返回了最不无关的内容”。
    *   **时间感知**：传统 RAG 系统忽略了文档访问的历史频率和衰减。

### 2. 核心方法论
vstash 是一个基于单个 SQLite 文件的系统，集成了 `sqlite-vec`（向量搜索）和 `FTS5`（全文检索）。
*   **自适应 RRF 与 IDF 加权**：
    *   使用倒数排名融合（RRF）合并结果。
    *   **创新点**：根据查询词的平均逆文档频率（IDF）动态调整权重。高 IDF（罕见词）增加 FTS 权重，低 IDF（常见词）增加向量权重。
*   **基于“检索不一致性”的自监督微调**：
    *   利用向量搜索与 FTS 搜索结果之间的“分歧”作为训练信号。
    *   自动提取三元组（查询、正样本、硬负样本）：例如 FTS 排名高但向量排名低的块被视为向量模型的“盲点”。
    *   使用 `MultipleNegativesRankingLoss` (MNRL) 对 33M 参数的 BGE-small 模型进行微调。
*   **文档内 MMR 去重**：应用最大边界相关性（MMR）算法，在保证得分的同时增加返回结果中唯一文档的多样性。
*   **距离感知的相关性信号**：利用向量距离设定阈值（如 >0.98 为低置信度），作为智能体的“置信度”反馈。

### 3. 实验设计
*   **数据集**：主要使用 **BEIR 基准测试** 中的 5 个数据集：SciFact（生物医学）、NFCorpus（健康）、FiQA（金融）、SciDocs（计算机科学）和 ArguAna（辩论）。此外还使用了 arXiv 论文和维基百科数据。
*   **对比方法（Baselines）**：
    *   **BM25 / Elasticsearch**：传统关键词检索。
    *   **ColBERTv2 (110M)**：高性能的后期交互模型。
    *   **BGE-base (110M)**：未经微调的较大规模向量模型。
    *   **Chroma (Dense-only)**：纯向量检索对比。
*   **评估指标**：NDCG@10、Precision@k、MRR、搜索延迟（Latency）以及相关性信号的 F1 分数。

### 4. 资源与算力
*   **模型规模**：主要针对 **33M 参数** 的 BGE-small 模型进行优化。
*   **算力说明**：文中提到在 CPU 上嵌入速度约为 700 chunks/秒。
*   **训练细节**：微调过程在 7.6 万个三元组上进行，持续 2 个 epoch。**文中未明确说明具体的 GPU 型号和总训练时长**，但强调了该过程是“零成本”且高效的，用户可通过 `vstash retrain` 命令在本地完成。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 5 个 BEIR 数据集上进行了超过 **50,425 个** 经过人工标注的查询验证。
    *   进行了 **5 万个数据块** 规模的合成扩展性测试。
    *   包含消融实验（RRF vs. 纯向量 vs. 纯 FTS）、MMR 效果测试、以及针对不同评分策略的网格搜索。
*   **充分性评价**：实验设计较为全面，涵盖了从检索精度到系统延迟、再到端到端 LLM 回答质量的多个维度。对比了不同规模的模型，并诚实地记录了负面结果（如交叉编码器重排序在本地场景下无效），体现了客观性。

### 6. 主要结论与发现
*   **微调效果显著**：仅 33M 参数的微调模型在 3/5 的数据集上达到或超过了 110M 参数的 ColBERTv2 性能。在 NFCorpus 上 NDCG@10 提升了 **19.5%**。
*   **自适应融合优于固定权重**：IDF 加权在 ArguAna 数据集上带来了 **21.4%** 的性能提升。
*   **负面发现**：在 RRF 之后加入频率衰减评分、历史增强召回或交叉编码器重排序，**均未能**在标准基准上提升检索质量，反而增加了延迟。
*   **高效性**：在 5 万个分块规模下，中值延迟仅为 **20.9 毫秒**，性能随规模呈亚线性增长。

### 7. 优点
*   **自监督闭环**：提出了一种无需人工标注、利用系统内部两种检索模式的分歧来自动进化嵌入模型的方法。
*   **生产级设计**：不仅是算法研究，还包含了完整性检查、模式版本控制、代码感知分块（基于 tree-sitter）等工程化实现。
*   **轻量且强大**：证明了通过聪明的训练数据选择，小模型可以跨级挑战大模型。

### 8. 不足与局限
*   **领域依赖性**：自监督信号的提升在不同领域间存在差异。例如在 FiQA 和 ArguAna 上，由于训练分布（主要是科学/问答类）与测试分布不匹配，提升较小。
*   **相关性信号的局限**：基于距离的相关性检测在跨领域查询时非常有效（F1=0.996），但在领域内（如 NFCorpus）区分度下降（F1=0.472）。
*   **端到端评估样本较小**：在评估 LLM 最终回答质量时，每组仅使用了 30 个查询，样本量不足以支撑严谨的统计学结论。
*   **并发限制**：由于基于 SQLite 的单文件架构，在大规模并发写入场景下可能存在瓶颈。

（完）
