---
title: "EvoRAG: Making Knowledge Graph-based RAG Automatically Evolve through Feedback-driven Backpropagation"
title_zh: EvoRAG：通过反馈驱动的反向传播使基于知识图谱的 RAG 实现自动演进
authors: "Zhenbo Fu, Yuanzhe Zhang, Qiange Wang, Hao Yuan, Yuehao Xu, Enze Yi, Yanfeng Zhang, Ge Yu"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15676v1"
tags: ["query:ma-kf"]
score: 10.0
evidence: 具有反馈驱动演进的基于知识图谱的检索增强生成
tldr: 针对现有基于知识图谱的检索增强生成（KG-RAG）在处理特定任务时缺乏适应性且难以过滤低贡献知识的问题，本文提出EvoRAG框架。该框架引入反馈驱动的反向传播机制，将生成响应的反馈关联到检索路径及具体三元组，实现知识图谱的持续演化与精炼。通过建立反馈、大模型与图数据的闭环，EvoRAG显著提升了推理的准确性与鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.15676v1/fig-001.webp\", \"caption\": \"Figure 2: The overall workflow of KG-RAG.\", \"page\": 2, \"index\": 1, \"width\": 563, \"height\": 282}, {\"url\": \"assets/figures/arxiv/2604.15676v1/fig-002.webp\", \"caption\": \"Table 2: Performance comparison with KG-RAG and KGR methods. MRAG, LRAG, and KRAG are KG-RAG baselines. TransE, RotatE, CAGED, and LLM_Sim are KGR methods applied to refine the KG, whose effects are evaluated using KRAG due to its operation at the triplet-level retrieval granularity. Best results are highlighted in bold, and worst results in red. ↑ indicates the accuracy improvement over the worst result.\", \"page\": 9, \"index\": 2, \"width\": 956, \"height\": 263}]"
motivation: 现有的KG-RAG框架因预存知识与下游任务需求不匹配且缺乏动态调整机制，导致在实际应用中推理效果受限。
method: 提出一种反馈驱动的反向传播机制，通过评估检索路径对响应的效用，将反馈传播至单个三元组以实现细粒度的知识图谱优化。
result: "实验结果表明，EvoRAG在推理准确性上比现有的最先进KG-RAG框架提升了7.34%。"
conclusion: EvoRAG通过建立反馈闭环实现了知识图谱的自我演化，为提升大模型在复杂场景下的推理能力提供了有效路径。
---

## 摘要
基于知识图谱的检索增强生成（KG-RAG）已成为通过从知识图谱（KG）中检索多跳路径来增强大语言模型（LLM）推理能力的一种极具前景的范式。然而，现有的 KG-RAG 框架在现实场景中往往表现不佳，因为预先捕获的知识依赖关系并非针对下游任务或其不断变化的需求而定制。这些框架难以适应特定任务的需求，且在生成过程中缺乏过滤低贡献知识的机制。我们观察到，对生成回答的反馈为提高知识图谱质量提供了有效的监督，因为它直接反映了用户的期望，并提供了关于输出正确性和有用性的见解。然而，一个关键挑战在于如何有效地将回答层面的反馈与知识图谱中的三元组层面贡献评估及知识更新联系起来。在这项工作中，我们提出了 EvoRAG，这是一个自演进的 KG-RAG 框架，它利用对生成回答的反馈来持续优化知识图谱并提高推理准确性。EvoRAG 引入了一种反馈驱动的反向传播机制，通过衡量检索路径对回答的效用将反馈归因于这些路径，并将该效用回传至单个三元组，从而支持细粒度的知识图谱精炼，实现更具适应性和准确性的推理。通过 EvoRAG，我们建立了一个耦合反馈、大语言模型和图数据的闭环，持续增强在现实场景中的性能和鲁棒性。实验结果表明，EvoRAG 的推理准确率比最先进的 KG-RAG 框架提高了 7.34%。源代码已在 https://github.com/iDC-NEU/EvoRAG 发布。

## Abstract
Knowledge Graph-based Retrieval-Augmented Generation (KG-RAG) has emerged as a promising paradigm for enhancing LLM reasoning by retrieving multi-hop paths from KGs. However, existing KG-RAG frameworks often underperform in real-world scenarios because the pre-captured knowledge dependencies are not tailored to the downstream task or its evolving requirements. These frameworks struggle to adapt to task-specific requirements and lack mechanisms to filter low-contribution knowledge during generation. We observe that feedback on generated responses offers effective supervision for improving KG quality, as it directly reflects user expectations and provides insights into the correctness and usefulness of the output. However, a key challenge lies in effectively linking response-level feedback to triplet-level contribution evaluation and knowledge updates in the KG.   In this work, we propose EvoRAG, a self-evolving KG-RAG framework that leverages the feedback over generated responses to continuously refine the KG and enhance reasoning accuracy. EvoRAG introduces a feedback-driven backpropagation mechanism that attributes feedback to retrieved paths by measuring their utility for response and propagates this utility back to individual triplets, supporting fine-grained KG refinements towards more adaptive and accurate reasoning. Through EvoRAG, we establish a closed loop that couples feedback, LLM, and graph data, continuously enhancing the performance and robustness in real-world scenarios. Experimental results show that EvoRAG improves reasoning accuracy by $7.34\%$ over state-of-the-art KG-RAG frameworks. The source code has been made available at https://github.com/iDC-NEU/EvoRAG.

---

## 论文详细总结（自动生成）

这是一份关于论文《EvoRAG: Making Knowledge Graph-based RAG Automatically Evolve through Feedback-driven Backpropagation》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
现有的基于知识图谱的检索增强生成（KG-RAG）框架主要面临**预构建知识与下游任务需求不匹配**的问题。具体表现为：
*   **缺乏适应性**：检索到的三元组虽然语义正确，但对特定查询贡献极小（如无关事实干扰、无法捕获长程依赖）。
*   **缺乏动态性**：知识图谱通常是静态的，难以处理过时或错误的知识。
*   **粒度错位**：用户反馈通常针对整个“回答”层面（粗粒度），而知识图谱的优化需要针对单个“三元组”层面（细粒度）。

**研究动机**：利用生成回答的反馈信号（正确性、满意度等）作为监督，建立一个闭环系统，使知识图谱能够根据任务需求自动演进和精炼。

### 2. 方法论：核心思想与关键技术
EvoRAG 提出了**反馈驱动的反向传播机制**，将回答层面的反馈转化为三元组层面的权重更新：
*   **反馈驱动的反向传播（Feedback-driven Backpropagation）**：
    1.  **路径效用评估**：利用 LLM 作为评判者，根据回答反馈为检索到的“推理路径”打分（考虑支持度、忠实度和冲突度）。
    2.  **梯度传播**：定义三元组的“贡献分”为可学习参数。通过最大化检索路径的期望效用，计算损失函数梯度，并将路径层面的效用回传给组成该路径的各个三元组。
*   **反馈引导的 KG 管理**：
    1.  **关系融合（Relation Fusion）**：对于频繁出现在高分路径中的多跳连接，自动创建“快捷方式”边，缩短推理深度。
    2.  **关系抑制（Relation Suppression）**：降低低分三元组的检索优先级，减少噪声干扰。
*   **混合优先级检索**：在检索阶段同时考虑语义相关性（Query-dependent）和历史贡献分（Query-independent），平衡即时需求与长期可靠性。

### 3. 实验设计
*   **数据集**：
    *   **RGB**：评估 RAG 推理能力的新闻数据集。
    *   **MultiHop (MTH)**：侧重多文档信息整合的多跳查询。
    *   **HotpotQA (HPQ)**：维基百科背景的多跳问答。
*   **Benchmark 与对比方法**：
    *   **KG-RAG 框架**：Microsoft GraphRAG (MRAG)、LightRAG (LRAG)、KRAG。
    *   **KG 精炼方法（集成于 KRAG）**：TransE、RotatE、CAGED（深度学习类）以及 LLM_sim（大模型类）。
*   **实验设置**：模拟多用户并发访问同一知识区域的场景，构建训练集进行迭代演进，并在测试集上验证。

### 4. 资源与算力
*   **硬件环境**：2x NVIDIA RTX A6000 (48GB) GPU，2x Intel Xeon Silver 4316 CPU，503GB RAM。
*   **软件环境**：Ubuntu 20.04, PyTorch 1.13.0, vLLM 框架。
*   **模型后端**：默认使用 Qwen2.5-32B，同时也测试了 Llama-3.1-70B 和 GPT-4o-mini。
*   **训练时长**：文中未明确给出总训练小时数，但通过运行性能分析显示，在 Batch Size 为 20 时，反向传播开销仅占总运行时间的 15.35%~23.9%，具有较好的实时性。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流数据集上进行了全面对比，并在 HotpotQA 上扩展到了 5000 条查询的大规模测试（包含近 90 万实体和 100 万三元组）。
*   **消融实验**：涵盖了反馈机制、知识图谱演进、交叉验证模块等多个维度的消融。
*   **鲁棒性测试**：针对不同反馈来源（LLM、人工、Ground Truth）和不同比例的噪声反馈（10%、20% 错误率）进行了测试。
*   **客观性**：实验设计考虑了数据泄露预防，并使用了多种 LLM 后端验证通用性，实验结果较为客观、充分。

### 6. 主要结论与发现
*   **性能提升**：EvoRAG 在推理准确率（ACC）上比最先进的 KG-RAG 框架平均提高了 **7.34%**。
*   **效率优化**：通过抑制无关知识，EvoRAG 将 Prompt 长度平均缩短了 **4.6 倍**，显著降低了推理延迟（TTFT）。
*   **噪声容忍**：系统对错误反馈具有极强的鲁棒性，即使有 20% 的反馈是错误的，准确率下降也微乎其微。
*   **收敛性**：在约 6 次迭代后，系统性能趋于稳定，能够有效识别并抑制约 83% 的问题三元组。

### 7. 优点
*   **闭环演进**：首次将“反向传播”概念引入 KG-RAG 的知识管理，实现了数据与模型的协同进化。
*   **细粒度优化**：成功解决了回答级反馈与三元组级更新之间的粒度错位问题。
*   **高效性**：通过关系融合和抑制，不仅提高了准确性，还通过减少上下文冗余降低了 Token 成本。
*   **通用性**：不依赖特定的 LLM 或检索后端，具有良好的可移植性。

### 8. 不足与局限
*   **实体修正限制**：目前的演进主要集中在“关系”层面（边的权重和新增），尚未涉及实体的直接修正或事实性错误的根本性重写。
*   **反馈依赖**：虽然对噪声有容忍度，但系统性能仍高度依赖于初始反馈信号的质量。
*   **冷启动问题**：在没有任何历史反馈的全新知识领域，其表现最初等同于标准 KG-RAG，需要一定规模的查询积累才能体现优势。

（完）
