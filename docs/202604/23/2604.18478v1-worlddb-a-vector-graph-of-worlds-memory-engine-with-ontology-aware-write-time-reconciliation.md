---
title: "WorldDB: A Vector Graph-of-Worlds Memory Engine with Ontology-Aware Write-Time Reconciliation"
title_zh: WorldDB：具备本体感知写入时协调机制的向量化“世界图谱”记忆引擎
authors: Harish Santhanalakshmi Ganesan
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18478v1"
tags: ["query:agent"]
score: 10.0
evidence: 用于长期运行智能体的向量世界图记忆引擎
tldr: WorldDB 是一款针对长程智能体设计的内存引擎，旨在解决传统 RAG 向量存储在处理事实碎片化、跨会话身份丢失及矛盾处理方面的局限。它创新性地提出了“世界图”架构，将每个节点视为可递归嵌套的容器，并采用内容寻址的不可变存储和可编程边处理机制。实验表明，WorldDB 在长文本记忆评估中显著优于现有 SOTA 模型，大幅提升了时序推理和知识更新的准确性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.18478v1/fig-001.webp\", \"caption\": \"Table 7: The nine MCP tools exposed by WorldDB.\", \"page\": 11, \"index\": 1, \"width\": 814, \"height\": 278}, {\"url\": \"assets/figures/arxiv/2604.18478v1/fig-002.webp\", \"caption\": \"Table 3: LongMemEval-s comparison. Bold = best. Hydra DB and Supermemory used Gemini 3.0 Pro; Zep and full-context used GPT-4o.\", \"page\": 8, \"index\": 2, \"width\": 902, \"height\": 263}, {\"url\": \"assets/figures/arxiv/2604.18478v1/fig-003.webp\", \"caption\": \"Table 1: WorldDB node schema.\", \"page\": 4, \"index\": 3, \"width\": 940, \"height\": 328}, {\"url\": \"assets/figures/arxiv/2604.18478v1/fig-004.webp\", \"caption\": \"Table 2: Default ontology edge handlers.\", \"page\": 4, \"index\": 4, \"width\": 814, \"height\": 301}]"
motivation: 传统的向量存储和扁平图数据库在处理长程对话时，难以有效管理事实的演变、层级关系及知识冲突。
method: 提出 WorldDB 引擎，采用递归嵌套的“世界”节点、内容寻址的 Merkle 审计链以及具备写入时自动对账功能的可编程边。
result: "在 LongMemEval-s 测试中达到 96.40% 的总准确率，较此前最优的 Hydra DB 提升了 5.61 个百分点。"
conclusion: 通过将本体感知的写入时协调与递归图结构相结合，WorldDB 为长程智能体提供了更稳健、一致且可审计的持久化记忆方案。
---

## 摘要
持久化记忆是区分无状态聊天机器人与长程智能体系统的瓶颈。在扁平向量存储上进行的检索增强生成（RAG）会将事实碎片化为块，丢失跨会话身份，且缺乏对替代（supersession）或矛盾（contradiction）的原生处理机制。近期的双时态知识图谱系统（如 Graphiti、Memento、Hydra DB）虽然增加了有类型边和有效时间元数据，但图结构本身仍是扁平的：缺乏递归组合，节点上没有内容寻址的不变性，且边类型除了标签外不携带任何行为。我们提出了 WorldDB，这是一款基于三个核心承诺构建的记忆引擎：(i) 每个节点都是一个“世界”——一个拥有自身内部子图、本体范围和组合嵌入的容器，可递归至任意深度；(ii) 节点采用内容寻址且不可变，因此任何编辑都会在节点及其所有祖先节点生成新的哈希值，从而原生提供 Merkle 风格的审计追踪；(iii) 边是写入时程序——每种边类型都带有 on_insert/on_delete/on_query_rewrite 处理器（例如，supersession 关闭有效性，contradicts 保留双方，same_as 发起合并提案），因此不存在原始追加路径。在 LongMemEval-s（500 个问题，约 11.5 万 token 的对话栈）上，以 Claude Opus 4.7 作为回答者的 WorldDB 实现了 96.40% 的总准确率和 97.11% 的任务平均准确率，相比此前报道的 Hydra DB 最先进水平（90.79%）提升了 5.61 个百分点，相比 Supermemory（85.20%）提升了 11.20 个百分点，并实现了完美的单会话助手召回率，在时间推理（96.24%）、知识更新（98.72%）和偏好综合（96.67%）方面表现稳健。消融实验表明，该引擎的图层（由解析器统一的实体和有类型的 refers_to 边组成）独立于底层回答者，为任务平均准确率贡献了 7.0 个百分点。

## Abstract
Persistent memory is the bottleneck separating stateless chatbots from long-running agentic systems. Retrieval-augmented generation (RAG) over flat vector stores fragments facts into chunks, loses cross-session identity, and has no first-class notion of supersession or contradiction. Recent bitemporal knowledge-graph systems (Graphiti, Memento, Hydra DB) add typed edges and valid-time metadata, but the graph itself remains flat: no recursive composition, no content-addressed invariants on nodes, and edge types carry no behavior beyond a label. We present WorldDB, a memory engine built on three commitments: (i) every node is a world -- a container with its own interior subgraph, ontology scope, and composed embedding, recursive to arbitrary depth; (ii) nodes are content-addressed and immutable, so any edit produces a new hash at the node and every ancestor, giving a Merkle-style audit trail for free; (iii) edges are write-time programs -- each edge type ships on_insert/on_delete/on_query_rewrite handlers (supersession closes validity, contradicts preserves both sides, same_as stages a merge proposal), so no raw append path exists. On LongMemEval-s (500 questions, ~115k-token conversational stacks), WorldDB with Claude Opus 4.7 as answerer achieves 96.40% overall / 97.11% task-averaged accuracy, a +5.61pp improvement over the previously reported Hydra DB state-of-the-art (90.79%) and +11.20pp over Supermemory (85.20%), with perfect single-session-assistant recall and robust performance on temporal reasoning (96.24%), knowledge update (98.72%), and preference synthesis (96.67%). Ablations show that the engine's graph layer -- resolver-unified entities and typed refers_to edges -- contributes +7.0pp task-averaged independently of the underlying answerer.

---

## 论文详细总结（自动生成）

以下是对论文《WorldDB: A Vector Graph-of-Worlds Memory Engine with Ontology-Aware Write-Time Reconciliation》的深度结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：持久化记忆是限制大模型从“无状态聊天机器人”转向“长程智能体系统”的核心瓶颈。
*   **现有技术缺陷**：
    *   **传统 RAG**：采用扁平向量存储，导致语义碎片化（丢失跨段落依赖）、时序停滞（无法区分新旧事实）和身份漂移（无法统一同一实体的不同表述）。
    *   **现有知识图谱（如 Hydra DB, Graphiti）**：虽然引入了时间元数据，但图结构依然是扁平的，缺乏递归组合机制，且边类型仅作为标签，不具备自动处理冲突或替代逻辑的执行能力。
*   **核心目标**：构建一个具备结构一致性、可验证审计且能自动协调知识冲突的记忆引擎。

### 2. 方法论：核心思想与关键技术
WorldDB 围绕三个非标准承诺构建：
*   **递归世界（Nodes are Worlds）**：
    *   节点不再是单行记录，而是一个“世界”容器，拥有自己的内部子图、本体范围和组合嵌入。
    *   支持任意深度的递归嵌套，查询被限制在特定“世界”范围内，除非通过显式的 `refers_to` 边跨越边界。
*   **内容寻址与不可变性（Content-Addressed Immutability）**：
    *   采用 **Merkle 树结构**：节点的 ID 由其内容、子节点哈希和边哈希通过 `blake3` 算法生成。
    *   任何叶子节点的修改都会导致所有祖先节点的哈希值更新，从而原生提供加密级的审计追踪。
    *   **双时态管理**：事实的有效时间（Valid-time）存储在可变列中，而内容本身不可变。
*   **可编程的写入时边处理器（Edges as Write-Time Programs）**：
    *   **禁止原始追加**：所有写入必须通过预定义的处理器（Handler）。
    *   例如：`supersedes` 处理器在写入时会自动关闭旧事实的有效期；`contradicts` 记录冲突但不删除；`same_as` 发起合并提案。
*   **分层解析器（Tiered Resolver）**：
    *   通过精确匹配、模糊匹配（Jaro-Winkler）、音标匹配、嵌入相似度及 LLM 仲裁五个层级，确保跨会话的实体统一。
*   **混合检索与 RRF 融合**：
    *   融合了 BM25（关键词）、HNSW（向量相似度）和实体图遍历（图路径）三种路径，通过倒数排名融合（RRF）算法生成最终结果。

### 3. 实验设计
*   **数据集**：使用 **LongMemEval-s** 基准测试，包含 500 个问题，涉及约 11.5 万 token 的长程对话栈（约 50 个连续会话）。
*   **对比方法**：Hydra DB (前 SOTA)、Supermemory、Zep、Mem0 以及全上下文（Full-context）模式。
*   **评测模型**：
    *   回答者：Claude Opus 4.7
    *   提取器：Claude Haiku 4.5
    *   裁判（Judge）：Claude Sonnet 4.6
    *   嵌入模型：OpenAI text-embedding-3-small

### 4. 资源与算力
*   **算力说明**：论文未详细列出具体的 GPU 硬件型号，但提供了工程基准数据。
*   **运行效率**：
    *   500 个问题的完整评估在 8 个并行 worker 下耗时 **13.7 分钟**。
    *   **批量加载**：100 万节点 + 250 万条边在 10 分 47 秒内完成加载（约 5401 次写入/秒）。
    *   **检索延迟**：100 万节点规模下，HNSW 向量检索 P95 延迟为 3.1ms，图遍历 P95 延迟为 12.8ms。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了单会话召回、偏好综合、知识更新、时序推理和多会话推理等多个维度。
*   **消融实验**：进行了 5 组增量优化实验（v15-v19），量化了图层、摘要机制和模型升级分别带来的增益。
*   **鲁棒性测试**：
    *   **Fuzz 测试**：进行了 2 轮各 2000 次随机操作（摄取、替代、冲突等），验证了内容哈希和有效性单调性等不变性。
    *   **跨模型验证**：对比了 Claude Opus、Sonnet 和 GPT-4o 作为回答者的表现，证明了引擎本身的架构增益（+10.7pp）超过了模型升级带来的增益。
*   **评价**：实验设计全面，通过消融实验清晰地分离了算法改进与模型能力，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能突破**：WorldDB 实现了 **96.40%** 的总准确率，比之前的 SOTA (Hydra DB) 提升了 **5.61 个百分点**。
*   **核心增益点**：在**多会话推理**（+15.79pp）和**时序推理**（+5.27pp）上进步最显著，这归功于实体解析器和双时态存储机制。
*   **结构胜于模型**：消融实验显示，即使使用相同的 LLM，WorldDB 的图结构层也能比扁平 RAG 提升 10.66% 的任务平均准确率。

### 7. 优点与亮点
*   **架构创新**：将“世界”概念引入图数据库，解决了知识的层级嵌套和作用域问题。
*   **安全性与可审计性**：通过 Merkle 树和不可变内容寻址，确保了记忆库的每一条记录都有据可查，防止了静默数据损坏。
*   **写入时对账**：将逻辑下沉到存储引擎层（边处理器），保证了本体一致性，减少了应用层的负担。
*   **高性能**：在保持复杂逻辑的同时，通过 HNSW 和摘要优先查询实现了极低的检索延迟。

### 8. 不足与局限
*   **摄取开销**：由于写入时需要运行多层解析器和边处理器，单次摄取的延迟（5-15ms）高于简单的向量追加。
*   **嵌入聚合算法**：目前的“注意力池化”聚合算法是无参数的，论文承认未来需要通过对比学习训练专门的聚合模型（如 HAKG 风格）。
*   **基准覆盖**：尚未在 DMR 等其他长记忆基准上进行测试。
*   **生物模拟缺失**：尚未实现类似 Hydra DB 的生物模拟遗忘机制（如基于频率和时间的衰减）。

（完）
