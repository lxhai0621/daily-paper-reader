---
title: "Ask Only When Needed: Proactive Retrieval from Memory and Skills for Experience-Driven Lifelong Agents"
title_zh: 只在需要时询问：经验驱动型终身学习智能体的主动记忆与技能检索
authors: "Yuxuan Cai, Jie Zhou, Qin Chen, Liang He"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20572v1"
tags: ["query:agent"]
score: 10.0
evidence: 终身学习智能体从记忆中主动检索
tldr: 针对在线终身学习中智能体被动检索经验、无法识别知识缺口的问题，本文提出 ProactAgent 框架。该框架通过 ExpOnEvo 机制持续优化策略与记忆，并利用 ProactRL 将检索建模为主动决策。通过对比有无检索的任务结果，智能体学会仅在必要时检索结构化经验库（事实、情节、技能），在 SciWorld 和 AlfWorld 等任务中显著提升了成功率并降低了检索开销。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20572v1/fig-001.webp\", \"caption\": \"Figure 1: Comparison of retrieval strategies for online lifelong agents. Static initialization provides memory once at task start, failing when task dynamics shift. Continuous retrieval queries at every step, causing context overload. LLM-gated retrieval adds a separate model to judge necessity, incurring latency and cost. Proactive retrieval (ours) learns when to retrieve through paired-branch process rewards, achieving adaptive control without additional model calls.\", \"page\": 2, \"index\": 1, \"width\": 976, \"height\": 388}, {\"url\": \"assets/figures/arxiv/2604.20572v1/fig-002.webp\", \"caption\": \"Table 6: Core hyperparameters of the PROACTAGENT implementation. Annealing schedule entries are formatted as (no-retrieval fraction, learning rate warmup ratio).\", \"page\": 18, \"index\": 2, \"width\": 952, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2604.20572v1/fig-003.webp\", \"caption\": \"Table 1: Main results across three lifelong agent benchmarks. The best result are highlighted in bold.\", \"page\": 8, \"index\": 3, \"width\": 918, \"height\": 516}, {\"url\": \"assets/figures/arxiv/2604.20572v1/fig-004.webp\", \"caption\": \"Table 2: Ablation study on SciWorld. Progressive ablation (top) cumulatively removes components from the full PROACTAGENT. Component ablation (bottom) isolates individual design choices from the variant without online parameter evolution. All variants retain online D evolution.\", \"page\": 8, \"index\": 4, \"width\": 490, \"height\": 265}, {\"url\": \"assets/figures/arxiv/2604.20572v1/fig-005.webp\", \"caption\": \"Figure 2: Overview of PROACTAGENT. (a) Experience-Enhanced Online Evolution (EXPONEVO) closes the loop between acting, experience accumulation, and policy optimization. (b) EXPERIENCE BASE partitions experience into five typed stores (Mf ,Me, S+, S−, S∆), so a single query returns complementary evidence and behavioral guidance. (c) Proactive Reinforcement Learning-based Retrieval (PROACTRL) replays the shared prefix to produce a retrieval branch τ ret and a matched no-retrieval branch τno-ret; the outcome gap yields a process reward that is positive only when retrieval strictly improves the trajectory, providing step-level supervision for when and what to retrieve.\", \"page\": 4, \"index\": 5, \"width\": 976, \"height\": 389}]"
motivation: 现有终身学习智能体通常采用被动检索模式，难以在交互过程中主动识别知识缺口并获取最相关的经验指导。
method: 提出 ProactAgent 框架，结合了记忆精炼机制 ExpOnEvo 以及通过过程奖励学习主动检索决策的强化学习策略 ProactRL。
result: "在 SciWorld 和 AlfWorld 任务中分别实现 73.50% 和 71.28% 的成功率，在大幅降低检索开销的同时保持了高性能。"
conclusion: 将检索建模为主动决策能显著提升智能体在长程任务中的经验利用效率，是实现高效终身学习的关键。
---

## 摘要
在线终身学习使智能体能够通过交互积累经验，并在长程任务中持续改进。然而，现有方法通常将从过去经验中检索视为一种被动操作，仅在任务初始化或完成一个步骤后触发。因此，智能体往往无法在交互过程中识别知识缺口，并主动检索对当前决策最有用的经验。为解决这一局限性，我们提出了 ProactAgent，这是一个针对结构化经验库进行主动检索的经验驱动型终身学习框架。我们首先引入了经验增强在线演化（ExpOnEvo），通过策略更新和记忆精炼实现持续改进。经验库将历史交互组织成分类存储库，包括事实记忆、情节记忆和行为技能，从而使检索能够提供相关证据和可操作的指导。在此基础上，我们提出了基于主动强化学习的检索（ProactRL），将检索建模为一种显式的策略动作，并通过配对分支过程奖励学习何时检索以及检索什么。通过比较具有相同交互前缀但在有无检索情况下的后续执行，ProactRL 为检索决策提供了步骤级的监督，鼓励仅在能够带来更好的任务结果或更高效率时进行检索。在 SciWorld、AlfWorld 和 StuLife 上的实验表明，ProactAgent 持续提升了终身学习智能体的性能，在 SciWorld 和 AlfWorld 上分别实现了 73.50% 和 71.28% 的成功率，同时大幅降低了检索开销，并在 StuLife 上达到了与专有模型相当的性能。

## Abstract
Online lifelong learning enables agents to accumulate experience across interactions and continually improve on long-horizon tasks. However, existing methods typically treat retrieval from past experience as a passive operation, triggering it only at task initialization or after completing a step. Consequently, agents often fail to identify knowledge gaps during interaction and proactively retrieve the most useful experience for the current decision. To address this limitation, we present ProactAgent, an experience-driven lifelong learning framework for proactive retrieval over a structured experience base. We first introduce Experience-Enhanced Online Evolution (ExpOnEvo), which enables continual improvement through both policy updates and memory refinement. The experience base organizes historical interactions into typed repositories, including factual memory, episodic memory, and behavioral skills, so that retrieval can provide both relevant evidence and actionable guidance. On top of this, we propose Proactive Reinforcement Learning-based Retrieval (ProactRL), which models retrieval as an explicit policy action and learns when and what to retrieve via paired-branch process rewards. By comparing continuations from identical interaction prefixes with and without retrieval, ProactRL provides step-level supervision for retrieval decisions, encouraging retrieval only when it leads to better task outcomes or higher efficiency. Experiments on SciWorld, AlfWorld, and StuLife show that ProactAgent consistently improves lifelong agent performance, achieving success rates of 73.50\% on SciWorld and 71.28\% on AlfWorld while substantially reducing retrieval overhead, and attains performance competitive with proprietary models on StuLife.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **ProactAgent** 的框架，旨在解决在线终身学习智能体在利用历史经验时存在的“被动检索”问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的终身学习智能体在检索过去经验时通常是**被动**的。它们要么在任务开始时一次性注入记忆（静态），要么在每一步都进行检索（持续），或者依赖额外的模型来判断是否检索（门控）。
*   **研究动机**：
    *   **知识缺口识别难**：智能体无法在交互过程中主动意识到自己何时缺乏知识。
    *   **检索效率低下**：不必要的检索会导致上下文过载、延迟增加和 Token 浪费。
    *   **更新机制脱节**：现有的方法往往将“文本记忆的积累”与“模型参数的优化”分开处理，导致两者无法协同进化。

### 2. 方法论
ProactAgent 结合了两个核心组件，实现“经验驱动”的进化：

*   **ExpOnEvo (经验增强在线演化)**：
    *   **闭环机制**：智能体在环境中行动，将轨迹精炼为经验存入库中，并利用这些经验通过强化学习（RL）优化策略。
    *   **结构化经验库**：将经验分为五类：**事实记忆**（环境事实）、**情节记忆**（局部计划）、**成功技能**（成功策略）、**失败技能**（错误模式）和**对比技能**（为何 A 比 B 好的洞察）。这种分类确保检索能同时提供证据和行动指南。
*   **ProactRL (基于主动强化学习的检索)**：
    *   **检索动作化**：将检索操作 `RETRIEVE(query)` 直接建模为智能体的一个显式策略动作。
    *   **配对分支过程奖励 (Paired-branch Process Rewards)**：这是关键技术。在训练时，针对同一个交互前缀，系统会采样两个分支：一个执行检索（$\tau_{ret}$），另一个抑制检索（$\tau_{no-ret}$）。
    *   **步骤级监督**：通过对比两个分支的结果，如果检索分支表现更好（成功或更高效），则给予正向过程奖励 $\alpha$；如果检索没用甚至有害，则给予惩罚。这解决了“检索动作”在长程任务中难以进行信用分配（Credit Assignment）的问题。

### 3. 实验设计
*   **数据集/场景**：
    *   **SciWorld**：复杂的科学实验模拟环境。
    *   **AlfWorld**：基于文本的家庭环境具身任务。
    *   **StuLife**：模拟校园生活的长程终身学习任务。
*   **对比方法**：
    *   **离线基线**：SFT、静态 GRPO、ReAct。
    *   **在线基线**：在线 GRPO、AWM、Reflexion（自我反思）、MemoryBank、Mem0 等。
*   **Benchmark**：主要衡量成功率（SR）、交互轮数（Rounds）和效率指标。

### 4. 资源与算力
*   **模型使用**：
    *   **策略模型**：Qwen2.5-7B-Instruct（主实验）和 Qwen2.5-3B-Instruct（缩放实验）。
    *   **经验提取模型**：使用 Qwen3-32B 作为“老师”模型，从轨迹中提取和精炼经验。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。但提到了使用了 GRPO 算法，这通常需要较大的显存来支持多个并行 Rollout。

### 5. 实验数量与充分性
*   **实验规模**：在三个不同的基准测试上进行了全面评估，涵盖了从具身导航到科学推理的不同维度。
*   **消融实验**：非常充分。分别验证了 ExpOnEvo、ProactRL、结构化经验分类、以及冷启动阶段对最终性能的贡献。
*   **客观性**：通过“配对分支”设计，实验能够直接证明检索动作与任务结果之间的因果关系，而非简单的相关性，实验逻辑严密且公平。

### 6. 主要结论与发现
*   **性能领先**：ProactAgent 在 SciWorld (73.50%) 和 AlfWorld (71.28%) 上均显著优于所有在线和离线基线。
*   **效率极高**：相比于被动检索方法，ProactAgent 在提升成功率的同时，大幅减少了交互轮数和 Token 消耗（例如在 SciWorld 上减少了约 33% 的轮数）。
*   **模型缩放潜力**：配备主动检索的 3B 模型性能接近甚至超过了 7B 的被动检索模型，证明了“学会检索”比单纯增加参数量更有效。

### 7. 优点
*   **创新性强**：将检索从“系统插件”转变为“智能体本能”，通过 RL 学习检索时机。
*   **监督精准**：配对分支奖励机制巧妙地解决了检索决策缺乏即时反馈的难题。
*   **结构化记忆**：五种类型的经验划分比传统的单一向量数据库更符合人类解决问题的逻辑。

### 8. 不足与局限
*   **环境重放依赖**：ProactRL 依赖于“前缀重放”（Prefix Replay）来生成对比分支，这在某些实时或不可逆的现实世界环境中可能难以实现。
*   **提取开销**：经验的精炼和提取依赖于更强大的模型（Qwen3-32B），这在完全自主的端到端部署中可能产生额外的离线计算负担。
*   **长期维护**：论文未深入讨论当经验库增长到极大规模时，如何进行有效的记忆遗忘或压缩。

（完）
