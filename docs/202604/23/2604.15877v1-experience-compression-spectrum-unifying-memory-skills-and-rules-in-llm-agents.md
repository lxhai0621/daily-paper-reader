---
title: "Experience Compression Spectrum: Unifying Memory, Skills, and Rules in LLM Agents"
title_zh: 经验压缩光谱：统一大语言模型智能体中的记忆、技能与规则
authors: "Xing Zhang, Guanghui Wang, Yanwei Cui, Wei Qiu, Ziyuan Li, Bing Zhu, Peiyang He"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15877v1"
tags: ["query:agent"]
score: 9.0
evidence: 统一长周期智能体部署中的记忆、技能和规则
tldr: 针对LLM智能体长期部署中的经验管理瓶颈，本文提出“经验压缩光谱”框架，将记忆、技能和规则统一为不同压缩倍率的知识形态。研究发现现有系统普遍缺乏跨层级自适应压缩能力，即存在“缺失的对角线”。通过分析20多个系统，本文揭示了社区间的孤立现状，并为构建高效、全光谱的智能体学习系统提供了设计原则与方向。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.15877v1/fig-001.webp\", \"caption\": \"Figure 1. The Experience Compression Spectrum. Existing agent learning systems map onto a single axis from raw traces to abstract rules. Memory systems cluster at Level 1, skill systems at Level 2, with Level 3 largely empty. A small number of cross-level systems (dashed) bridge Levels 1–2 but none support adaptive level selection. Compression ratios are approximate.\", \"page\": 2, \"index\": 1, \"width\": 952, \"height\": 428}, {\"url\": \"assets/figures/arxiv/2604.15877v1/fig-002.webp\", \"caption\": \"Table 2. Representative systems mapped onto the Experience Compression Spectrum. Level indicates primary output granularity. Mechanism indicates the core compression method. Lifecycle indicates explicit support for knowledge maintenance (versioning, conflict detection, deprecation). Notably, no system supports adaptive level selection—all operate at a predetermined compression level.\", \"page\": 4, \"index\": 2, \"width\": 492, \"height\": 471}]"
motivation: 智能体记忆与技能发现社区长期缺乏交流，导致海量交互经验难以被高效、多层次地转化和利用。
method: 提出经验压缩光谱（ECS）框架，根据压缩率将经验划分为情节记忆、程序化技能和陈述性规则三个维度。
result: 揭示了现有系统仅在固定压缩水平运行且缺乏跨层级转换的现状，并量化了压缩率与迁移性、特异性之间的权衡。
conclusion: 论文指出了全光谱智能体学习系统的设计原则，强调了解决跨层级压缩和知识生命周期管理是未来发展的关键。
---

## 摘要
随着大语言模型（LLM）智能体扩展到长周期、多会话的部署场景，高效管理积累的经验已成为关键瓶颈。智能体记忆系统和智能体技能发现都在应对这一挑战——即从交互轨迹中提取可重用的知识——然而，对22篇核心论文中1,136条参考文献的引用分析显示，跨社区引用率低于1%。我们提出了“经验压缩光谱”（Experience Compression Spectrum），这是一个统一的框架，将记忆、技能和规则定位在压缩率递增的单一轴线上（情境记忆为5-20倍，程序性技能为50-500倍，陈述性规则为1,000倍以上），从而直接降低上下文消耗、检索延迟和计算开销。将20多个系统映射到该光谱上发现，每个系统都在固定的、预设的压缩级别上运行，没有一个系统支持自适应的跨级别压缩，我们将这一空白称为“缺失的对角线”。我们进一步表明，仅靠专业化是不够的——两个社区都在没有交流解决方案的情况下独立解决共同的子问题——评估方法与压缩级别紧密耦合，可迁移性随压缩程度增加而提高，但以牺牲特异性为代价，且知识生命周期管理在很大程度上仍被忽视。我们阐述了可扩展的全光谱智能体学习系统的开放性问题和设计原则。

## Abstract
As LLM agents scale to long-horizon, multi-session deployments, efficiently managing accumulated experience becomes a critical bottleneck. Agent memory systems and agent skill discovery both address this challenge -- extracting reusable knowledge from interaction traces -- yet a citation analysis of 1,136 references across 22 primary papers reveals a cross-community citation rate below 1%. We propose the \emph{Experience Compression Spectrum}, a unifying framework that positions memory, skills, and rules as points along a single axis of increasing compression (5--20$\times$ for episodic memory, 50--500$\times$ for procedural skills, 1,000$\times$+ for declarative rules), directly reducing context consumption, retrieval latency, and compute overhead. Mapping 20+ systems onto this spectrum reveals that every system operates at a fixed, predetermined compression level -- none supports adaptive cross-level compression, a gap we term the \emph{missing diagonal}. We further show that specialization alone is insufficient -- both communities independently solve shared sub-problems without exchanging solutions -- that evaluation methods are tightly coupled to compression levels, that transferability increases with compression at the cost of specificity, and that knowledge lifecycle management remains largely neglected. We articulate open problems and design principles for scalable, full-spectrum agent learning systems.

---

## 论文详细总结（自动生成）

这篇论文《Experience Compression Spectrum: Unifying Memory, Skills, and Rules in LLM Agents》对大语言模型（LLM）智能体如何管理长期交互经验进行了深入的架构性思考。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：随着 LLM 智能体从单次演示转向长期、多会话的实际部署，它们积累了海量的交互轨迹（Traces）。这些原始数据会迅速撑爆上下文窗口，并导致检索成本激增。
*   **核心矛盾**：目前存在两个平行的研究社区——“智能体记忆（Memory）”和“技能发现（Skill Discovery）”。作者通过对 22 篇核心论文的 1,136 条参考文献进行分析，发现这两个社区的跨社区引用率低于 1%，处于严重的割裂状态。
*   **整体含义**：论文旨在打破这种孤立，提出一个统一的框架，将记忆、技能和规则视为对原始经验进行不同程度“压缩”的产物，从而为构建可扩展的智能体学习系统提供理论指导。

### 2. 方法论：经验压缩光谱 (ECS)
*   **核心思想**：将经验管理定义为一个压缩函数 $C_L: T \to K_L$，将交互轨迹 $T$ 映射为不同层级 $L$ 的知识。
*   **四个压缩层级**：
    *   **Level 0 (原始轨迹)**：1:1 压缩，包含完整日志，重用性极低。
    *   **Level 1 (情节记忆)**：5–20× 压缩，提取“发生了什么”（如：用户偏好、特定事件），重用性中等。
    *   **Level 2 (程序化技能)**：50–500× 压缩，提取“如何操作”（如：代码片段、工作流模板），重用性高。
    *   **Level 3 (陈述性规则)**：1000×+ 压缩，提取“决策原则”（如：领域无关的约束、策略），重用性最高。
*   **关键技术洞察**：
    *   **缺失的对角线**：作者发现现有系统（如 Mem0, Voyager 等）都固定在单一层级运行，缺乏在不同层级间自适应转换（如将多次记忆固化为技能）的能力。
    *   **权衡关系**：压缩率越高，知识的通用性（Transferability）越强，但特异性（Specificity）越弱。

### 3. 实验设计与分析
*   **分析对象**：论文并非传统的实验型论文，而是**元分析（Meta-analysis）与系统综述**。它调研了 2023 年以来发布的 20 多个代表性系统（包括 Mem0, Voyager, SkillRL, ExpeL 等）。
*   **基准数据来源**：引用并汇总了多个已有基准测试的结果（如 ALFWorld, SpreadsheetBench, BrowseComp, SWE-bench 等）来验证压缩层级与性能的关系。
*   **对比维度**：通过对比 L1（记忆检索）与 L2（技能提取）在相同任务下的表现，量化了高层级压缩在下游任务中的增益。

### 4. 资源与算力
*   **算力说明**：由于本文属于框架提案和综述性质，**文中未提供具体的 GPU 型号、数量或训练时长**。
*   **资源消耗讨论**：论文从理论上计算了存储开销。例如，L1 系统存储 1000 个片段需 ~500K token，而压缩至 L3 仅需 ~500 token，实现了 1000 倍的检索开销削减。

### 5. 实验数量与充分性
*   **数据规模**：分析了 1,136 条参考文献，详细映射了 20 多个主流智能体系统。
*   **充分性评价**：作为一篇观点性/综述性论文（Position Paper），其证据链条较为充分。它通过汇总多个独立研究（如 SkillRL 相比 L1 提升 68.5pp）来支持其“高层级压缩更有效”的论点。实验分析客观地揭示了当前领域的研究空白（尤其是 L3 自动化提取的缺失）。

### 6. 主要结论与发现
*   **社区孤立**：记忆与技能社区几乎互不引用，导致重复造轮子。
*   **压缩优势**：高层级压缩（L2/L3）在跨领域迁移和降低计算开销方面具有显著优势。
*   **评估耦合**：目前的评估方法与压缩层级深度绑定（L1 看 QA 准确率，L2 看任务成功率），缺乏统一的效率指标。
*   **生命周期管理缺失**：现有系统大多只管“存”，不管“删”或“更新”，缺乏冲突检测和知识过时处理机制。

### 7. 优点
*   **统一性**：首次将零散的记忆、技能、规则研究整合进一个连续的压缩光谱中。
*   **前瞻性**：提出了“缺失的对角线”概念，指明了未来自适应智能体系统的发展方向（即在闲暇时间进行经验固化/压缩）。
*   **实用原则**：为开发者提供了设计原则，如“层级无关的压缩核心”和“双向升降级机制”。

### 8. 不足与局限
*   **缺乏实证系统**：论文提出了框架，但并未实现一个完整覆盖 L1-L3 且能自适应切换的“全光谱”原型系统。
*   **层级离散化**：虽然称为“光谱”，但文中定义的四个层级仍是离散的，实际应用中知识的界限可能更模糊。
*   **L3 提取难题**：论文承认自动化提取 L3 规则（陈述性知识）在技术上仍面临因果推断和元评估的巨大挑战。

（完）
