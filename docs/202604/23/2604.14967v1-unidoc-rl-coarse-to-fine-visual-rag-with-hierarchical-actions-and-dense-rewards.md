---
title: "UniDoc-RL: Coarse-to-Fine Visual RAG with Hierarchical Actions and Dense Rewards"
title_zh: UniDoc-RL：具有层级化动作与稠密奖励的由粗及精视觉检索增强生成
authors: "Jun Wang, Shuo Tan, Zelong Sun, Tiancheng Gu, Yongle Zhao, Ziyong Feng, Kaicheng Yang, Cewu Lu"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14967v1"
tags: ["query:mmkqa"]
score: 9.0
evidence: 结合强化学习智能体的视觉RAG
tldr: "针对现有视觉RAG系统在复杂推理中缺乏细粒度语义的问题，本文提出UniDoc-RL框架。该框架将视觉信息获取建模为分层动作空间的序列决策过程，使LVLM智能体能从粗粒度文档检索逐步细化到区域裁剪。通过基于GRPO的密集多奖励机制进行端到端训练，UniDoc-RL在多个基准测试中显著提升了性能，最高比现有RL方法高出17.7%。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.14967v1/fig-001.webp\", \"caption\": \"Figure 1: Three critical factors for Visual RAG. UniDoc-RL address these challenges through the (a) Precise Selection action to bridge the semantic gap between coarse retrieval and reasoning, and an (b) Active Visual Perception action to focus on information-dense regions, both optimized via a (c) Dense multi-reward mechanism.\", \"page\": 2, \"index\": 1, \"width\": 858, \"height\": 356}, {\"url\": \"assets/figures/arxiv/2604.14967v1/fig-002.webp\", \"caption\": \"Table 1: Main Results. The best performance are marked in bold. SlideVQA and ViDoSeek mainly focus on reasoning type, while MMLongBench focuses on the visual type of reference content. OCR-based ( ) RAG and purely visual ( ) RAG are evaluated with the same prompt and setting.\", \"page\": 7, \"index\": 2, \"width\": 885, \"height\": 456}, {\"url\": \"assets/figures/arxiv/2604.14967v1/fig-003.webp\", \"caption\": \"Figure 6: Case Study for UniDoc-RL.\", \"page\": 14, \"index\": 3, \"width\": 898, \"height\": 594}, {\"url\": \"assets/figures/arxiv/2604.14967v1/fig-004.webp\", \"caption\": \"Figure 2: Overview of UniDoc-RL. (a)(b)(c) demonstrates the “Search-Select-Perceive” coarse-to-fine action space. (d) is the specially designed reward for UniDoc-RL. (e) shows the interaction process between model and external environment, as well as the implementation of the GRPO algorithm.\", \"page\": 4, \"index\": 4, \"width\": 879, \"height\": 504}, {\"url\": \"assets/figures/arxiv/2604.14967v1/fig-005.webp\", \"caption\": \"Figure 8: Prompt for Training and Testing.\", \"page\": 16, \"index\": 5, \"width\": 855, \"height\": 829}, {\"url\": \"assets/figures/arxiv/2604.14967v1/fig-006.webp\", \"caption\": \"Figure 7: Prompt for Reward Model.\", \"page\": 16, \"index\": 6, \"width\": 872, \"height\": 398}]"
motivation: 现有的视觉RAG系统依赖通用检索信号，忽略了复杂推理所需的细粒度视觉语义。
method: 提出一种统一的强化学习框架，通过分层动作空间实现从文档检索到区域裁剪的由粗到精视觉感知，并结合密集多奖励机制进行优化。
result: "在三个基准测试中均优于现有最先进模型，相比之前的强化学习方法实现了高达17.7%的性能提升。"
conclusion: UniDoc-RL证明了通过分层决策和密集奖励对LVLM进行端到端训练，能有效提升视觉RAG系统的精准信息获取与推理能力。
---

## 摘要
检索增强生成（RAG）通过外部视觉知识扩展了大语言视觉模型（LVLMs）。然而，现有的视觉 RAG 系统通常依赖于通用的检索信号，忽略了复杂推理所需的细粒度视觉语义。为了解决这一局限性，我们提出了 UniDoc-RL，这是一个统一的强化学习框架，其中 LVLM 智能体共同执行检索、重排序、主动视觉感知和推理。UniDoc-RL 将视觉信息获取建模为一个具有层级化动作空间的序列决策问题。具体而言，它将视觉证据从粗粒度的文档检索逐步细化为细粒度的图像选择和主动区域裁剪，使模型能够抑制无关内容并关注信息密集区域。为了实现有效的端到端训练，我们引入了一种稠密多奖励方案，为每个动作提供任务感知的监督。基于组相对策略优化（GRPO），UniDoc-RL 在不依赖独立价值网络的情况下，使智能体行为与多个目标保持一致。为了支持这种训练范式，我们构建了一个包含高质量推理轨迹和细粒度动作标注的综合数据集。在三个基准测试上的实验表明，UniDoc-RL 持续超越了最先进的基准模型，相比之前的基于强化学习的方法提升高达 17.7%。

## Abstract
Retrieval-Augmented Generation (RAG) extends Large Vision-Language Models (LVLMs) with external visual knowledge. However, existing visual RAG systems typically rely on generic retrieval signals that overlook the fine-grained visual semantics essential for complex reasoning. To address this limitation, we propose UniDoc-RL, a unified reinforcement learning framework in which an LVLM agent jointly performs retrieval, reranking, active visual perception, and reasoning. UniDoc-RL formulates visual information acquisition as a sequential decision-making problem with a hierarchical action space. Specifically, it progressively refines visual evidence from coarse-grained document retrieval to fine-grained image selection and active region cropping, allowing the model to suppress irrelevant content and attend to information-dense regions. For effective end-to-end training, we introduce a dense multi-reward scheme that provides task-aware supervision for each action. Based on Group Relative Policy Optimization (GRPO), UniDoc-RL aligns agent behavior with multiple objectives without relying on a separate value network. To support this training paradigm, we curate a comprehensive dataset of high-quality reasoning trajectories with fine-grained action annotations. Experiments on three benchmarks demonstrate that UniDoc-RL consistently surpasses state-of-the-art baselines, yielding up to 17.7% gains over prior RL-based methods.

---

## 论文详细总结（自动生成）

### UniDoc-RL: 具有层级化动作与稠密奖励的由粗及精视觉 RAG 论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
现有的视觉检索增强生成（Visual RAG）系统在处理复杂文档（如带有图表、表格的扫描件）时面临三大挑战：
*   **检索不精准**：通用检索器依赖浅层匹配，难以捕捉复杂推理所需的细粒度语义，导致模型产生幻觉。
*   **视觉利用率低**：模型通常被动接收整张高分辨率图像，其中包含大量冗余背景噪声，浪费了上下文容量。
*   **优化困难**：现有的强化学习（RL）方法多采用稀疏的最终结果奖励，难以对检索、裁剪等中间决策步骤进行有效的信用分配（Credit Assignment）。

**UniDoc-RL** 的动机在于将视觉 RAG 建模为一个**由粗到精的序列决策问题**，使模型能够像人类一样主动搜索、筛选并放大关键视觉区域。

#### 2. 方法论：核心思想与技术细节
UniDoc-RL 提出了一个统一的强化学习框架，核心包含以下三个维度：

*   **层级化动作空间（Hierarchical Action Space）**：
    1.  **Image Search（搜索）**：调用外部工具进行粗粒度候选文档检索。
    2.  **Precise Selection（选择）**：利用 LVLM 自身的语义理解能力，从候选集中精准挑选（重排序）最相关的图像，过滤文档级噪声。
    3.  **Visual Perception（感知）**：通过生成 `<bbox>` 坐标进行主动裁剪（Crop）和缩放（Zoom），聚焦于信息密集的局部区域。
*   **稠密多奖励机制（Dense Multi-Rewards）**：
    为了解决中间步骤的监督问题，设计了五种奖励：
    *   **检索奖励 ($r_{ir}$)**：基于 NDCG 评估搜索结果的排名质量。
    *   **选择奖励 ($r_{sel}$)**：评估 LVLM 挑选图像的准确性。
    *   **感知奖励 ($r_{crop}$)**：基于预测框与真实区域的交并比（IoU）评估定位精度。
    *   **模式奖励 ($r_{pat}$)**：确保模型遵循 XML 标签格式。
    *   **结果奖励 ($rans$)**：由强大的奖励模型（RM）评估最终答案的正确性。
*   **算法流程**：
    采用 **GRPO（组相对策略优化）** 算法。模型首先通过高质量轨迹数据进行监督微调（SFT）作为冷启动，随后在 RL 阶段通过组内相对优势进行策略更新，无需独立的价值网络（Value Network）。

#### 3. 实验设计
*   **数据集/场景**：
    *   **SlideVQA**：多图幻灯片问答，侧重多跳推理。
    *   **ViDoSeek**：视觉文档搜索与推理。
    *   **MMLongBench**：长文档理解，涵盖图表、表格等多种视觉类型。
*   **Benchmark 与对比方法**：
    *   对比了 **OCR-based RAG**（将图像转为文本再处理）和 **Purely Visual RAG**。
    *   基准模型包括：Vanilla RAG、ReAct、Search-R1(-VL) 以及最直接的竞争对手 **VRAG-RL**。
    *   基础模型使用 Qwen2.5-VL-3B 和 7B。

#### 4. 资源与算力
*   **硬件**：实验使用了 **8 台 NVIDIA A100 80G GPU**。
*   **软件框架**：使用 `llama-factory` 进行 SFT，使用 `verl` 框架进行 GRPO 训练。
*   **训练细节**：SFT 阶段 cutoff 长度为 40,000，训练 3 个 Epoch；RL 阶段组大小（Group Size）为 5，Actor 学习率为 1e-6。文中未明确给出总训练时长，但给出了详细的超参数配置。

#### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流基准测试上进行了全面评估，涵盖了 3B 和 7B 两种参数规模。
*   **消融实验**：针对“动作空间（选择 vs 感知）”和“奖励组成（各项奖励的增益）”进行了详细的消融研究。
*   **分析深度**：提供了检索召回率提升分析、动作频率分析（SFT vs RL vs Teacher）以及定性案例研究。
*   **客观性**：实验对比了 OCR 路径与视觉路径，并统一了 Prompt 和设置，确保了对比的公平性。

#### 6. 主要结论与发现
*   **性能飞跃**：UniDoc-RL 在所有基准测试中均达到 SOTA，相比之前的 RL 方法（VRAG-RL），在 3B 和 7B 模型上分别实现了 **17.5% 和 17.7%** 的大幅提升。
*   **主动感知的价值**：RL 训练显著提高了模型执行“主动裁剪”的频率和精度，使其从被动阅读转变为主动寻找证据。
*   **选择动作的必要性**：LVLM 驱动的选择动作能有效弥补外部检索器的语义鸿沟，显著提升了地面真值（Ground-truth）图像的召回率。

#### 7. 优点
*   **拟人化决策**：由粗到精的“搜索-选择-感知”流程符合人类处理复杂文档的认知逻辑。
*   **稠密监督**：通过多维度奖励解决了长链条推理中的信用分配问题，使中间步骤可优化。
*   **高质量数据贡献**：构建并开源了包含细粒度动作标注的推理轨迹数据集，对社区具有重要价值。

#### 8. 不足与局限
*   **推理延迟**：由于引入了多轮交互（搜索、选择、裁剪、推理），系统的端到端推理延迟会高于单次推理的 RAG 系统。
*   **教师模型依赖**：训练轨迹的合成高度依赖于极强大的教师模型（如 Qwen3-VL-235B），对于缺乏此类资源的研究者来说，数据复现成本较高。
*   **计算开销**：处理长上下文（40k tokens）和多轮视觉输入对显存和算力有较高要求。

（完）
