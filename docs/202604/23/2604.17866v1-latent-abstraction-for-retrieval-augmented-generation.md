---
title: Latent Abstraction for Retrieval-Augmented Generation
title_zh: 检索增强生成的潜空间抽象
authors: "Ha Lan N. T, Minh-Anh Nguyen, Dung D. Le"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17866v1"
tags: ["query:ma-kf"]
score: 10.0
evidence: 具有潜空间检索的统一 RAG 架构
tldr: 针对现有RAG系统依赖文本查询且检索器与生成器分离的局限，本文提出LAnR框架。该框架将编码、检索和生成统一在单个LLM的潜空间内，利用隐藏状态生成检索向量，并结合基于答案熵的MLP控制头实现自适应检索。实验证明，LAnR在多个QA基准测试中优于传统方法，在提升准确性的同时显著优化了推理效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17866v1/fig-001.webp\", \"caption\": \"Figure 1: Comparison between conventional RAG and LAnR for multi-hop QA. Conventional RAG performs explicit reasoning at each hop, including generating intermediate text, forming search queries, and deciding whether to continue retrieval. In contrast, LAnR operates in latent space: a special token [PRED] produces query vectors from hidden states, while a lightweight entropy-aware MLP controls the retrieval process, enabling more efficient and integrated reasoning.\", \"page\": 2, \"index\": 1, \"width\": 826, \"height\": 458}, {\"url\": \"assets/figures/arxiv/2604.17866v1/fig-002.webp\", \"caption\": \"Table 2: Results on HotpotQA and Musique\", \"page\": 8, \"index\": 2, \"width\": 577, \"height\": 423}, {\"url\": \"assets/figures/arxiv/2604.17866v1/fig-003.webp\", \"caption\": \"Figure 2: Overview of LAnR. Queries are injected into the LLM and combined with a [PRED] token to form a latent query from hidden representations. This latent query is used for retrieval and to decide whether further retrieval is needed via a lightweight MLP Retrieval Control Head. The LLM then generates the answer from the retrieved context.\", \"page\": 4, \"index\": 3, \"width\": 824, \"height\": 454}, {\"url\": \"assets/figures/arxiv/2604.17866v1/fig-004.webp\", \"caption\": \"Figure 3: Entropy distribution on HotPotQA under varying numbers of supporting documents.\", \"page\": 5, \"index\": 4, \"width\": 829, \"height\": 575}]"
motivation: 现有RAG系统因检索器与生成器架构分离且依赖文本查询，限制了LLM表征能力的发挥。
method: 提出LAnR框架，在LLM潜空间内统一执行检索与生成，并利用隐藏状态和MLP控制头实现自适应检索。
result: 在六个QA基准测试中，LAnR在单跳和多跳设置下均优于现有方法，且推理效率更高。
conclusion: 通过在潜空间内实现检索与生成的深度集成，可以有效提升RAG系统的性能与运行效率。
---

## 摘要
检索增强生成 (RAG) 已成为利用外部知识增强大语言模型 (LLM)、缓解幻觉并提高事实准确性的标准方法。然而，现有系统依赖于在每一步跳转中生成自然语言查询，并在检索器和生成器之间保持严格的架构分离，这阻碍了它们充分利用 LLM 的表征能力。我们提出了 LAnR (Latent Abstraction for RAG)，这是一个统一的框架，其中单个 LLM 完全在其自身的潜空间内共同执行编码、检索和生成。LAnR 不生成文本查询，而是从指定 [PRED] 标记的隐藏状态中产生稠密检索向量，并使用它们与来自同一模型的编码文档表征进行匹配。此外，LAnR 使用这些隐藏状态之上的轻量级 MLP 控制头，自适应地决定何时已检索到足够的证据，从而消除了独立的检索器和显式的标记级停止推理。这一设计源于我们的实证观察，即答案标记的熵能够可靠地指示检索的充分性。在涵盖单跳和多跳设置的六个问答 (QA) 基准测试上的广泛实验表明，LAnR 优于现有的 RAG 方法，同时通过减少检索调用次数和更紧密的模型集成实现了更高的推理效率。

## Abstract
Retrieval-Augmented Generation (RAG) has become a standard approach for enhancing large language models (LLMs) with external knowledge, mitigating hallucinations, and improving factuality. However, existing systems rely on generating natural language queries at each hop and maintaining a strict architectural separation between retriever and generator, preventing them from leveraging the full representational capacity of the LLM. We propose \textbf{LAnR} (Latent Abstraction for RAG), a unified framework in which a single LLM jointly performs encoding, retrieval, and generation entirely within its own latent space. Rather than generating textual queries, LAnR produces dense retrieval vectors from the hidden states of a designated \texttt{[PRED]} token and uses them to match against encoded document representations from the same model. Furthermore, LAnR adaptively decides when sufficient evidence has been retrieved using a lightweight MLP control head over those same hidden states, eliminating both the separate retriever and explicit token-level stopping reasoning. This design is motivated by our empirical observation that answer token entropy reliably signals retrieval sufficiency. Extensive experiments on six QA benchmarks spanning single-hop and multi-hop settings demonstrate that LAnR outperforms existing RAG methods, while achieving improved inference efficiency through reduced number of retrieval calls and tighter model integration.

---

## 论文详细总结（自动生成）

这篇论文提出了 **LAnR (Latent Abstraction for RAG)**，旨在通过将检索和生成统一在大语言模型（LLM）的潜空间（Latent Space）中，解决传统检索增强生成（RAG）系统的效率和表征瓶颈。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的 RAG 系统通常将检索器（Retriever）和生成器（Generator）视为独立的模块，依赖于生成显式的自然语言查询（Textual Queries）进行交互。
*   **研究动机**：
    *   **架构分离**：独立训练的检索器无法充分利用 LLM 强大的内部表征能力。
    *   **推理开销**：生成文本查询和显式的推理步骤（如“思考：我需要搜索...”）增加了推理延迟和计算成本。
    *   **离散限制**：文本查询会将丰富的语义信息压缩为离散的标记，可能导致信息丢失。
*   **整体含义**：论文探索 LLM 是否能直接在隐藏状态（Hidden States）中进行检索和判断检索充分性，从而实现更紧密的“检索-生成”一体化。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：取消显式的文本查询，利用 LLM 内部的特殊标记表征作为检索向量，并使用轻量级分类头控制检索流程。
*   **关键技术细节**：
    1.  **潜空间查询构建（Latent Query Construction）**：在输入序列末尾添加一个特殊标记 `[PRED]`。该标记在经过 Transformer 层处理后的隐藏状态 $h_{[PRED]}$ 直接作为稠密检索向量 $q$。
    2.  **统一表征空间**：使用同一个 LLM 对外部文档进行编码（取文档最后一个标记的隐藏状态作为向量），确保查询向量和文档向量在同一语义空间内。
    3.  **熵引导的检索控制（Entropy-Guided Control）**：
        *   **实证发现**：作者发现随着检索到的金标准文档增加，模型生成答案首个标记的**预测熵（Entropy）**会显著下降。
        *   **MLP 控制头**：在 $h_{[PRED]}$ 之上训练一个轻量级的多层感知机（MLP），预测当前证据是否足以回答问题（二分类：停止或继续检索）。
    4.  **多轮自适应检索**：在多跳推理中，模型会根据已检索到的内容动态更新 `[PRED]` 的表征，并采用对比学习目标（Contrastive Loss）引导模型寻找尚未获取的证据。

### 3. 实验设计
*   **数据集**：主要针对复杂的多跳问答任务，使用了 **HotpotQA** 和 **Musique**。
*   **基准模型（Backbones）**：Qwen-2.5-3B 和 Qwen-2.5-7B（包括 Base 和 Instruct 版本）。
*   **对比方法**：
    *   **无检索**：直接生成（Direct Generation）、监督微调（SFT）。
    *   **单跳检索**：Naive RAG。
    *   **多跳/迭代检索**：Search-o1、IRCoT、ReSearch、Search-R1、AutoRefine 等当前最先进的（SOTA）方法。
*   **评估指标**：准确匹配率（Exact Match, EM）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **训练细节**：提到使用了 NQ 和 HotpotQA 的合并训练集进行微调，并采用了 ANCE 策略定期刷新文档索引。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流多跳 QA 基准上进行了详尽对比，涵盖了从 3B 到 7B 不同规模的模型。
*   **分析实验**：
    *   **熵分布分析**：通过可视化不同证据量下的熵变化，验证了控制头设计的合理性。
    *   **消融/对比**：对比了 Base 和 Instruct 版本的表现，证明了 LAnR 在不同底座模型上的普适性。
*   **充分性评价**：实验设计较为客观，通过与多种最新的迭代检索方法对比，证明了其在多跳逻辑下的优越性。但实验主要集中在 QA 任务，对其他 RAG 场景（如长文本摘要或代码生成）覆盖较少。

### 6. 主要结论与发现
*   **性能领先**：LAnR 在 HotpotQA 和 Musique 上均刷新了 SOTA 记录，优于依赖文本查询的 Search-R1 和 AutoRefine。
*   **潜空间优势**：证明了 LLM 隐藏状态能够比文本查询更有效地捕捉检索意图。
*   **自适应能力**：基于熵信号的 MLP 控制头能有效减少冗余检索，提升了推理效率。
*   **统一性**：单一模型同时充当检索器、判断器和生成器是可行且高效的。

### 7. 优点
*   **高效性**：消除了生成中间查询文本的步骤，显著降低了推理延迟和 Token 消耗。
*   **深度集成**：检索和生成在同一表征空间内演进，减少了模块间的信息损失。
*   **无需显式推理链**：通过潜空间抽象实现了类似“隐式思维链”的效果，使模型更专注于任务本身。

### 8. 不足与局限
*   **训练成本**：需要对 LLM 进行专门的对比学习微调，不像传统的 RAG 那样可以“开箱即用”地接入任何现成检索器。
*   **索引维护**：由于检索依赖于 LLM 自身的隐藏状态，如果模型更新，整个外部知识库的向量索引也需要重新编码。
*   **泛化风险**：实验主要集中在 Wikipedia 域内的 QA 任务，对于极大规模或跨域知识库的检索稳定性尚待验证。
*   **硬件细节缺失**：缺乏具体的算力消耗报告，难以评估其训练成本的经济性。

（完）
