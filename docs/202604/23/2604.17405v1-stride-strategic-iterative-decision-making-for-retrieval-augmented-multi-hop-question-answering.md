---
title: "STRIDE: Strategic Iterative Decision-Making for Retrieval-Augmented Multi-Hop Question Answering"
title_zh: STRIDE：面向检索增强多跳问答的策略性迭代决策
authors: "Wei Chen, Lili Zhao, Zhi Zheng, HuiJun Hou, Tong Xu"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17405v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 检索增强多跳问答的战略迭代决策
tldr: 针对多跳问答中存在的词义歧义和逻辑依赖缺失问题，本文提出STRIDE框架。该框架通过分离战略规划、动态控制和落地执行，利用Meta-Planner构建抽象推理骨架以延迟实体落地，并由Supervisor协调具备依赖感知的子问题执行。此外，STRIDE-FT微调框架进一步提升了开源模型的推理能力。实验证明STRIDE在复杂查询下具有更高的准确性和鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17405v1/fig-001.webp\", \"caption\": \"Figure 1: Challenges in current iterativeRAG. Top: Premature entity grounding leading to cascading errors due to lexical ambiguity. Bottom: Rigid scheduling that fails to account for the rich dependency structures among sub-questions.\", \"page\": 2, \"index\": 1, \"width\": 454, \"height\": 370}, {\"url\": \"assets/figures/arxiv/2604.17405v1/fig-002.webp\", \"caption\": \"Table 1: Evaluation results on multi-hop QA benchmarks. EM, F1, P, and R denote Exact Match, F1 score, Precision, and Recall respectively. Best and second-best results within each model group are highlighted in bold and underlined, respectively.\", \"page\": 7, \"index\": 2, \"width\": 1002, \"height\": 306}, {\"url\": \"assets/figures/arxiv/2604.17405v1/fig-003.webp\", \"caption\": \"Figure 2: Overview of STRIDE, which structures the reasoning process into strategy, control, and execution.\", \"page\": 4, \"index\": 3, \"width\": 939, \"height\": 631}]"
motivation: 现有MHQA方法因过早关注表面实体导致词义歧义，且忽略了推理步骤间的逻辑依赖，导致执行不协调且易产生误差传播。
method: 提出STRIDE框架，通过Meta-Planner生成抽象推理骨架，并由Supervisor进行依赖感知的动态调度，配合专门的执行模块完成事实提取与推理。
result: 实验表明STRIDE实现了更稳健准确的推理，且STRIDE-FT微调框架在无需人工标注或强教师模型的情况下显著增强了开源大模型的能力。
conclusion: STRIDE通过解耦规划、控制与执行，并引入动态决策机制，有效解决了多跳问答中的逻辑协调和歧义挑战。
---

## 摘要
多跳问答（MHQA）通过检索和推理分散在多个文档中的证据，能够对复杂查询提供准确的答案。现有的 MHQA 方法主要依赖于迭代式检索增强生成，但存在以下两个主要问题：1）现有方法过早地关注表面实体而非底层的推理结构，使得问题分解极易受到词义歧义的影响；2）现有方法忽视了推理步骤之间的逻辑依赖关系，导致执行过程缺乏协调。为了解决这些问题，我们提出了 STRIDE 框架，该框架将战略规划、动态控制和落地执行进行分离。其核心在于，元规划器（Meta-Planner）首先构建一个实体无关的推理骨架，以捕捉查询的抽象逻辑，从而将实体落地推迟到推理结构建立之后，这减轻了由过早词汇承诺引起的歧义错误。随后，监督器（Supervisor）以依赖感知的方式编排子问题的执行，在可能的情况下实现高效并行，并在必要时进行顺序协调。通过动态决定是检索新证据还是从现有事实中进行推断，该框架避免了冗余查询和错误传播，同时融合跨分支信息并重构失败的查询以增强鲁棒性。落地的实体提取和逻辑推理被委托给专门的执行模块，通过检索与推理的显式分离确保了忠实性。我们进一步提出了 STRIDE-FT，这是一个模块化的微调框架，它利用 STRIDE 自生成的执行轨迹，既不需要人工标注，也不需要更强大的教师模型。实验表明，STRIDE 实现了鲁棒且准确的推理，而 STRIDE-FT 有效地提升了开源大语言模型（LLMs）的性能。

## Abstract
Multi-hop question answering (MHQA) enables accurate answers to complex queries by retrieving and reasoning over evidence dispersed across multiple documents. Existing MHQA approaches mainly rely on iterative retrieval-augmented generation, which suffer from the following two major issues. 1) Existing methods prematurely commit to surface-level entities rather than underlying reasoning structures, making question decomposition highly vulnerable to lexical ambiguity. 2) Existing methods overlook the logical dependencies among reasoning steps, resulting in uncoordinated execution. To address these issues, we propose STRIDE, a framework that separates strategic planning, dynamic control, and grounded execution. At its core, a Meta-Planner first constructs an entity-agnostic reasoning skeleton to capture the abstract logic of the query, thereby deferring entity grounding until after the reasoning structure is established, which mitigates disambiguation errors caused by premature lexical commitment. A Supervisor then orchestrates sub-question execution in a dependency-aware manner, enabling efficient parallelization where possible and sequential coordination when necessary. By dynamically deciding whether to retrieve new evidence or infer from existing facts, it avoids redundant queries and error propagation, while fusing cross-branch information and reformulating failed queries to enhance robustness. Grounded fact extraction and logical inference are delegated to specialized execution modules, ensuring faithfulness through explicit separation of retrieval and reasoning. We further propose STRIDE-FT, a modular fine-tuning framework that uses self-generated execution trajectories from STRIDE, requiring neither human annotations nor stronger teacher models. Experiments show that STRIDE achieves robust and accurate reasoning, while STRIDE-FT effectively enhances open-source LLMs.

---

## 论文详细总结（自动生成）

### 论文总结：STRIDE —— 面向检索增强多跳问答的策略性迭代决策

#### 1. 核心问题与整体含义（研究动机和背景）
多跳问答（MHQA）要求系统能够跨多个文档检索并整合信息。现有的迭代式检索增强生成（RAG）方法存在两个核心缺陷：
*   **过早的实体绑定（Premature Entity Grounding）：** 现有方法在分解问题时直接锁定具体实体名，容易受到词义歧义的影响（例如将电影《林肯》误认为总统林肯），导致后续检索步步错。
*   **僵化的执行逻辑：** 现有方法通常采用固定的顺序执行，忽略了子问题之间复杂的逻辑依赖（如并行关系、分叉合并关系），导致效率低下且缺乏容错能力。

**STRIDE** 的提出旨在通过模拟人类决策层级（战略规划、动态控制、具体执行），解决多跳推理中的歧义和协调问题。

#### 2. 方法论：核心思想与关键技术
STRIDE 将推理过程解耦为三个紧密协作的层级：
*   **战略层（Strategy Layer - Meta-Planner）：** 
    *   首先生成**通用策略（General Strategy）**：一个与具体实体无关的抽象推理骨架（如“识别作品->找到作者->找到作者国籍”）。
    *   随后将其实例化为**具体计划（Concrete Plan）**。这种“推理先行、实体后行”的设计有效缓解了词义歧义。
*   **控制层（Control Layer - Supervisor）：** 
    *   作为动态调度器，维护执行状态（已解决、待处理、失败记录）。
    *   根据逻辑依赖决定子问题的执行顺序（支持并行或顺序），并动态选择操作：**检索（Retrieve）**、**重写（Rewrite，针对检索失败）**或**直接回答（Answer，基于已有事实推理）**。
*   **执行层（Execution Layer）：** 
    *   **提取器（Extractor）：** 从文档中提取原子事实。
    *   **推理器（Reasoner）：** 基于事实进行逻辑推导。
*   **STRIDE-FT（模块化微调）：** 
    *   利用 STRIDE 自身生成的执行轨迹进行自监督学习。
    *   对 Meta-Planner 使用 **DPO（直接偏好优化）**，对其他模块使用 **SFT（有监督微调）**，无需人工标注或强教师模型。

#### 3. 实验设计
*   **数据集：** 2WikiMultihopQA、HotpotQA、MuSiQue（每个数据集随机采样 1000 个问题）。
*   **Benchmark：** 统一的文档库（包含金标文档和干扰文档），并额外测试了 5 万规模的大型噪声语料库。
*   **对比方法：** 
    *   非检索方法：CoT。
    *   单步 RAG：RAG w/ CoT。
    *   迭代 RAG：SelfAsk、Iter-RetGen、GenGround、DualRAG。
*   **模型底座：** Qwen3-8B（开源代表）、GPT-4o-mini（闭源代表）。

#### 4. 资源与算力
*   **硬件：** 使用了 NVIDIA A6000 GPU。
*   **训练时长：** 整个微调过程（STRIDE-FT）总计耗时约 **2 小时**。
*   **参数设置：** 使用 LoRA 技术进行微调（Rank=8），训练 2 个 Epoch。

#### 5. 实验数量与充分性
论文进行了多维度的实验，具有较高的充分性和客观性：
*   **主实验：** 覆盖 3 个主流数据集和 2 种不同规模的 LLM。
*   **消融实验：** 分别验证了框架组件（Meta-Planner, Supervisor 等）和微调模块（MP-FT, R-FT 等）的有效性。
*   **鲁棒性分析：** 在 5 万文档的大规模噪声环境下测试性能。
*   **深度分析：** 针对不同推理跳数（Hops）的性能表现。
*   **效率分析：** 统计了 Token 消耗量和推理耗时。
*   **错误归因：** 详细分析了规划错误、检索错误和执行错误的占比。

#### 6. 主要结论与发现
*   **性能领先：** STRIDE 在所有数据集上均优于现有迭代 RAG 方法，尤其在复杂的 MuSiQue 数据集上提升显著。
*   **微调增益：** STRIDE-FT 显著提升了开源模型（Qwen3-8B）的能力，使其在多个指标上接近甚至超过了 GPT-4o-mini。
*   **高效率：** 相比 DualRAG，STRIDE 减少了 54%-71% 的 Token 消耗，推理速度提升了 60%-80%。
*   **鲁棒性强：** 在噪声语料库和高跳数推理场景下，STRIDE 的性能下降幅度最小，表现出极强的稳定性。

#### 7. 优点
*   **架构创新：** 首次将抽象逻辑规划与具体实体检索分离，从根本上解决了多跳问答中的歧义传播问题。
*   **动态调度：** Supervisor 能够识别何时需要检索、何时只需推理，避免了冗余操作。
*   **自给自足：** STRIDE-FT 证明了开源模型可以通过自身生成的优质轨迹实现自我进化，降低了对昂贵标注或闭源模型的依赖。
*   **可解释性：** 显式的规划和执行轨迹使推理过程高度透明。

#### 8. 不足与局限
*   **模型依赖：** 尽管 STRIDE-FT 有所改善，但框架性能仍受限于底座模型的指令遵循能力，若底座模型极弱，可能无法生成有效的初始轨迹。
*   **最大迭代限制：** 实验中设置了最大 5 次迭代，对于极深层次（如 10 跳以上）的推理可能仍存在瓶颈。
*   **检索器性能：** 框架效果仍部分取决于底层检索器（如 Contriever）的召回率，若检索器完全无法找回相关文档，Supervisor 的重写机制也难以发挥作用。

（完）
