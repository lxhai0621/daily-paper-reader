---
title: Stateless Decision Memory for Enterprise AI Agents
title_zh: 面向企业级 AI 智能体的无状态决策记忆
authors: Vasundra Srinivasan
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20158v1"
tags: ["query:agent"]
score: 9.0
evidence: 长周期决策智能体的无状态决策记忆
tldr: 针对受监管行业对AI Agent在确定性、可审计性和水平扩展方面的严格要求，本文指出传统有状态记忆架构的局限性，并提出确定性投影记忆（DPM）。DPM通过追加式事件日志和任务条件投影，在保持无状态特性的同时，显著提升了长周期决策的准确性与推理连贯性。实验表明，DPM在压缩比20倍时表现优于摘要式记忆，且推理速度提升7-15倍，为企业级AI应用提供了更可靠、更易审计的架构选择。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20158v1/fig-001.webp\", \"caption\": \"Figure 4: DPM minus Summ-only on each decision-alignment axis as a function of the compression ratio (ρ). At ρ≈2 (loose budget) and ρ≈5 (moderate) the architectures are empirically indistinguishable; at ρ≈20 (tight) the DPM advantage emerges at Cohen’s h>1.1 on FRP and RCS. Bars are 95% paired-bootstrap CIs.\", \"page\": 11, \"index\": 1, \"width\": 896, \"height\": 527}, {\"url\": \"assets/figures/arxiv/2604.20158v1/fig-002.webp\", \"caption\": \"Table 3: Determinism under temperature-zero replay. Edit distance is normalized characterlevel Levenshtein on the 200-character prefix. Hash uniqueness near 1.0 indicates the full surface bytes differ across replays even when the prefix is stable.\", \"page\": 10, \"index\": 2, \"width\": 885, \"height\": 251}, {\"url\": \"assets/figures/arxiv/2604.20158v1/fig-003.webp\", \"caption\": \"Figure 3: Byte-hash uniqueness and 200-character-prefix edit distance across 10 replays per cell. Both architectures inherit residual API-level nondeterminism at temperature zero. The prefix is stable on long surfaces where the projection has room to converge; the tail remains variable, producing unique full-surface hashes.\", \"page\": 10, \"index\": 3, \"width\": 943, \"height\": 325}, {\"url\": \"assets/figures/arxiv/2604.20158v1/fig-004.webp\", \"caption\": \"Table 1: DPM versus Summ-only across four decision-alignment axes at three budgets. n=10 cases per cell, paired permutation test (10,000 resamples). At tight budget DPM improves FRP and RCS with large effect sizes; at moderate and loose budgets the architectures are indistinguishable on the gated axes.\", \"page\": 8, \"index\": 4, \"width\": 633, \"height\": 375}, {\"url\": \"assets/figures/arxiv/2604.20158v1/fig-005.webp\", \"caption\": \"Figure 2: Decision-alignment axes by budget. Asterisks mark permutation-significant deltas (∗∗∗ p<0.001, ∗∗ p<0.01, ∗ p<0.05). At generous budgets the architectures are empirically equivalent; at tight budgets DPM improves FRP and RCS with Cohen’s h above 1.\", \"page\": 9, \"index\": 5, \"width\": 943, \"height\": 246}, {\"url\": \"assets/figures/arxiv/2604.20158v1/fig-006.webp\", \"caption\": \"Figure 1: Enterprise properties supported by each memory architecture family. DPM satisfies all four by construction. Stateful architectures require retrofits that compound with architectural sophistication.\", \"page\": 5, \"index\": 6, \"width\": 943, \"height\": 342}]"
motivation: 受监管行业要求AI系统具备确定性回放、可审计性和水平扩展能力，而现有的有状态记忆架构在设计上违反了这些核心系统属性。
method: 提出确定性投影记忆（DPM），利用追加式事件日志并在决策时执行单次任务条件投影，以实现无状态且高效的决策支持。
result: 在20倍压缩比下，DPM的事实精度和推理连贯性显著优于摘要式架构，且决策速度提升了7至15倍，同时大幅减少了LLM调用次数。
conclusion: 无状态性是企业级AI架构的关键需求，DPM证明了在满足监管合规性的同时，可以实现比传统检索或摘要方案更优的长周期决策性能。
---

## 摘要
尽管过去十年中出现了日益复杂的有状态记忆架构，但在受监管领域（如核保、理赔裁定、税务稽查）中，长程决策智能体的企业级部署仍以检索增强流水线为主。我们认为这反映了一个隐含的需求：受监管的部署高度依赖四项系统属性（确定性回放、可审计的依据、多租户隔离以及用于水平扩展的无状态性），而有状态架构在设计上就违背了这些属性。我们提出了确定性投影记忆（Deterministic Projection Memory, DPM）：它由一个仅追加的事件日志和决策时的一次任务条件投影组成。在三种记忆预算下的十个受监管决策案例中，DPM 在预算充裕时与基于摘要的记忆表现相当，而在预算受限时表现显著优于后者：在 20 倍压缩率下，DPM 将事实精准度提高了 0.52（Cohen's h=1.17, p=0.0014），推理连贯性提高了 0.53（h=1.13, p=0.0034），配对置换检验，n=10。此外，在预算受限时，DPM 的速度快 7-15 倍，在决策时仅需调用一次大语言模型（LLM），而非 N 次。一项在零温度下对每个案例进行 10 次回放的确定性研究表明，两种架构都继承了残余的 API 级非确定性，但这种不对称性是结构性的：DPM 仅暴露一次非确定性调用，而摘要法则暴露了 N 次复合调用。审计面也遵循同样的“1 对 N”模式：在 LongHorizon-Bench 上，DPM 每次决策记录两次 LLM 调用，而摘要法则记录 83-97 次。最后，我们提出了 TAMS（一种供从业者选择架构的启发式方法），并对企业运行条件下有状态记忆的失效进行了分析。本文的贡献在于论证了无状态性是解释企业偏好性能较弱但可回放的检索流水线的核心属性，并证明了 DPM 可以在不付出检索所带来的决策性能代价的情况下实现这一属性。

## Abstract
Enterprise deployment of long-horizon decision agents in regulated domains (underwriting, claims adjudication, tax examination) is dominated by retrieval-augmented pipelines despite a decade of increasingly sophisticated stateful memory architectures. We argue this reflects a hidden requirement: regulated deployment is load-bearing on four systems properties (deterministic replay, auditable rationale, multi-tenant isolation, statelessness for horizontal scale), and stateful architectures violate them by construction. We propose Deterministic Projection Memory (DPM): an append-only event log plus one task-conditioned projection at decision time. On ten regulated decisioning cases at three memory budgets, DPM matches summarization-based memory at generous budgets and substantially outperforms it when the budget binds: at a 20x compression ratio, DPM improves factual precision by +0.52 (Cohen's h=1.17, p=0.0014) and reasoning coherence by +0.53 (h=1.13, p=0.0034), paired permutation, n=10. DPM is additionally 7-15x faster at binding budgets, making one LLM call at decision time instead of N. A determinism study of 10 replays per case at temperature zero shows both architectures inherit residual API-level nondeterminism, but the asymmetry is structural: DPM exposes one nondeterministic call; summarization exposes N compounding calls. The audit surface follows the same one-versus-N pattern: DPM logs two LLM calls per decision while summarization logs 83-97 on LongHorizon-Bench. We conclude with TAMS, a practitioner heuristic for architecture selection, and a failure analysis of stateful memory under enterprise operating conditions. The contribution is the argument that statelessness is the load-bearing property explaining enterprise's preference for weaker but replayable retrieval pipelines, and that DPM demonstrates this property is attainable without the decisioning penalty retrieval pays.

---

## 论文详细总结（自动生成）

这篇论文由 Vasundra Srinivasan（斯坦福大学工程学院，AI 架构师）撰写，探讨了企业级 AI 智能体在受监管领域（如金融、医疗、法律）部署时，记忆架构所面临的系统性挑战，并提出了一种创新的无状态记忆方案。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **学术与工业的脱节**：学术界倾向于开发日益复杂的“有状态”记忆架构（如 MemGPT、分层摘要、图记忆），但企业级部署仍普遍采用简单的检索增强生成（RAG）。
*   **企业级核心需求**：受监管领域的决策系统必须满足四个关键属性：**确定性回放**（可重现决策）、**可审计的依据**（推理链可追溯）、**多租户隔离**（防止数据泄露）和**水平扩展的无状态性**。
*   **有状态架构的缺陷**：传统的有状态记忆通过不断更新中间状态来运行，这导致了路径依赖。在回放时，由于 LLM 的随机性，多次中间调用会产生复合误差，且审计面极大（需要记录每一次状态更新），难以满足监管要求。

### 2. 论文提出的方法论：确定性投影记忆 (DPM)
DPM 借鉴了分布式系统中的“事件溯源”（Event Sourcing）模式，核心思想是将记忆处理从“过程式更新”转变为“决策时投影”。
*   **核心组件**：
    1.  **仅追加事件日志 (Append-only Event Log)**：将智能体轨迹中的所有原始事件（文档块、工具输出、用户消息）按顺序存入不可变的日志中。
    2.  **任务条件投影 (Task-conditioned Projection)**：在决策时刻，仅调用一次 LLM，将整个日志、任务规范和记忆预算作为输入，生成结构化的记忆视图。
*   **关键技术细节**：
    *   **单次调用**：在决策前不进行任何摘要或记忆压缩，避免了信息的逐级丢失。
    *   **结构化输出**：投影结果固定分为“事实（Facts）”、“推理（Reasoning）”和“合规说明（Compliance Notes）”三个部分。
    *   **确定性控制**：在温度为 0 的条件下运行，通过减少 LLM 调用次数（从 N 次降为 1 次）来最小化 API 层的非确定性漂移。

### 3. 实验设计
*   **数据集/场景**：使用 **LongHorizon-Bench**，涵盖两个受监管领域：抵押贷款资格审核（依据 ECOA/Reg B 法规）和保险理赔裁定。
*   **Benchmark**：采用四轴决策对齐框架进行评分：事实精准度 (FRP)、推理连贯性 (RCS)、决策准确性 (EDA) 和合规重建 (CRR)。
*   **对比方法**：
    *   **Summ-only (有状态基准)**：增量摘要架构，每增加一个事件就更新一次运行摘要。
    *   **DPM (本文方法)**：在三种记忆预算（紧凑 20x 压缩、中等 5x 压缩、宽松 2x 压缩）下进行对比。

### 4. 资源与算力
*   **模型使用**：实验全部基于 Anthropic 的 `claude-haiku-4-5-20251001` 模型。
*   **算力说明**：论文未提及具体的 GPU 型号或训练时长，因为该研究侧重于推理架构的设计而非模型训练。实验通过 API 调用完成，重点对比了 DPM 与基准方法在推理延迟和成本上的差异。

### 5. 实验数量与充分性
*   **实验规模**：每个实验单元包含 10 个案例（n=10），总共进行了多组不同预算下的对比实验。
*   **统计严谨性**：使用了配对置换检验（10,000 次重采样）和 Cohen’s h 效应量分析。
*   **确定性研究**：对每个案例进行了 10 次重复回放，以测量字节级的哈希稳定性和编辑距离。
*   **评价**：虽然样本量（n=10）在机器学习领域不算大，但考虑到长程决策案例构建的复杂性和统计检验的严谨性，实验对于验证架构属性是充分且客观的。

### 6. 主要结论与发现
*   **性能优势**：在记忆预算充足时，DPM 与摘要法表现相当；但在**高压缩比（20x）下，DPM 显著优于摘要法**（事实精准度提升 0.52，推理连贯性提升 0.53）。
*   **效率提升**：DPM 的速度比有状态基准快 **7-15 倍**，成本也大幅降低，因为它将 N 次 LLM 调用简化为 1 次。
*   **系统属性**：DPM 成功实现了无状态性，将审计面从几十个 LLM 调用缩减为 2 个（投影+决策），极大地降低了企业运维和合规审计的负担。
*   **确定性**：虽然 API 仍存在残余随机性，但 DPM 的结构化设计使其回放稳定性远高于需要多次复合调用的有状态架构。

### 7. 优点
*   **工程实用性极强**：直接解决了企业 AI 落地中最头疼的“不可解释”和“不可重现”问题。
*   **架构简洁**：通过“日志+投影”的模式，规避了复杂的记忆管理逻辑。
*   **成本效益**：显著减少了中间步骤的 Token 消耗和延迟。
*   **提出 TAMS 启发式准则**：为从业者提供了清晰的架构选择建议（根据压缩比和合规需求决定是否使用 DPM）。

### 8. 不足与局限
*   **上下文窗口限制**：DPM 依赖于单次调用的上下文容量。如果轨迹长度超过模型窗口（如超过 100 万 token），则必须引入分层投影，这会削弱其无状态优势。
*   **API 漂移**：即便 DPM 减少了调用次数，只要底层模型 API 不是 100% 字节确定性的，就无法实现完美的比特级回放（除非私有化部署并固定权重）。
*   **领域覆盖**：实验仅限于金融和保险，尚未在医疗（HIPAA 限制）或税务等更复杂的长周期领域验证。
*   **样本量限制**：n=10 的样本量虽然在统计上显著，但仍需更大规模的工业数据验证。

（完）
