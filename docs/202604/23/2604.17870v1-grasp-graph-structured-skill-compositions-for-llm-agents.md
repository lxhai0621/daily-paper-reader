---
title: "GraSP: Graph-Structured Skill Compositions for LLM Agents"
title_zh: GraSP：面向大语言模型智能体的图结构化技能组合
authors: "Tianle Xia, Lingxiang Hu, Yiding Sun, Ming Xu, Lan Xu, Siying Wang, Wei Xu, Jie Jiang"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17870v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: LLM智能体的可执行技能图架构，用于选择和组合技能
tldr: 针对LLM智能体在技能过多时性能下降的“技能编排”瓶颈，本文提出GraSP架构。它通过将扁平技能集编译为具有因果依赖的有向无环图（DAG），并引入节点级验证与局部修复机制，实现了高效的技能组合与执行。在多个基准测试中，GraSP显著提升了任务成功率并减少了执行步数，证明了结构化编排是提升智能体可靠性的关键。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17870v1/fig-001.webp\", \"caption\": \"Table 1: Main results across four interactive benchmarks. R = average reward/score (↑), S = average environment steps (↓). Best in bold, second-best underlined. For ALFWorld and WebShop, R is average reward (0–100). For ScienceWorld, R is average score (0–100). For InterCode, R is success rate (%). Results averaged over 3 runs.\", \"page\": 6, \"index\": 1, \"width\": 854, \"height\": 1117}, {\"url\": \"assets/figures/arxiv/2604.17870v1/fig-002.webp\", \"caption\": \"Figure 4: Skill quantity and quality. (a) Flat execution peaks around M=3 then drops; GRASP is robust to over-retrieval and remains above flat-at-optimum even at M=8. (b) When skill quality drops from High to Low, GRASP loses only ∼5% vs. ∼9% for flat execution.\", \"page\": 7, \"index\": 2, \"width\": 432, \"height\": 335}, {\"url\": \"assets/figures/arxiv/2604.17870v1/fig-003.webp\", \"caption\": \"Figure 1: Overview of GraSP (GRASP). Top: Flat skill execution (left) treats skills as a sequential chain where any failure invalidates the entire suffix at O(N) cost; GRASP (right) compiles skills into a typed DAG with explicit dependencies, enabling O(dh) local repair. Bottom: The four-stage GRASP pipeline: (1) Retrieve selects a focused subset of skills from a large library conditioned on experience memory; (2) Compile organizes them into a verified DAG with typed edges (state, data, order); (3) Execute & Verify traverses the graph, checking pre/postconditions at every node; (4) Repair patches only the failed subgraph while preserving verified progress. When retrieval confidence is low, the system falls back to ReAct.\", \"page\": 3, \"index\": 3, \"width\": 950, \"height\": 533}, {\"url\": \"assets/figures/arxiv/2604.17870v1/fig-004.webp\", \"caption\": \"Table 3: Default hyperparameters for GRASP. Values are shared across all eight LLM backbones and four benchmarks unless stated otherwise.\", \"page\": 13, \"index\": 4, \"width\": 829, \"height\": 339}]"
motivation: 现有的LLM智能体在面对大量技能时，由于缺乏显式的因果依赖和结构化编排机制，导致技能检索越多性能反而越差。
method: 提出一种可执行技能图架构，将技能转化为带类型的DAG，通过节点验证和五种局部修复算子实现高效的执行与重规划。
result: "在ALFWorld等四个基准测试中，GraSP在所有配置下均优于ReAct等基线，最高提升19分奖励并减少41%的环境步数。"
conclusion: 结构化的技能编排而非单纯增加技能库规模，是提升LLM智能体执行可靠性和应对复杂任务的核心。
---

## 摘要
大语言模型（LLM）智能体的技能生态系统已迅速成熟，但最近的基准测试表明，为智能体提供更多技能并不能单调地提升性能——包含 2-3 个技能的精简集合优于详尽的文档，而过多的技能反而会损害性能。瓶颈已从技能的可用性转向了技能编排：智能体需要的不是更多技能，而是一种能够以显式因果依赖关系来选择、组合和执行技能的结构化机制。我们提出了 GraSP，这是首个可执行的技能图架构，它在技能检索与执行之间引入了一个编译层。GraSP 将扁平的技能集转换为具有“前提-效果”边的有类型有向无环图（DAG），通过节点级验证进行执行，并利用五种有类型算子执行局部有界修复——将重新规划的复杂度从 O(N) 降低到 O(d^h)。在 ALFWorld、ScienceWorld、WebShop 和 InterCode 等基准测试以及八种 LLM 主干模型上，GraSP 在所有配置下均优于 ReAct、Reflexion、ExpeL 和扁平技能基线，在最强基线的基础上将奖励提升了多达 19 点，同时减少了高达 41% 的环境步数。GraSP 的优势随任务复杂度的增加而扩大，并且对技能过度检索和质量下降均具有鲁棒性，这证实了结构化编排（而非更大的技能库）是实现可靠智能体执行的关键。

## Abstract
Skill ecosystems for LLM agents have matured rapidly, yet recent benchmarks show that providing agents with more skills does not monotonically improve performance -- focused sets of 2-3 skills outperform comprehensive documentation, and excessive skills actually hurt. The bottleneck has shifted from skill availability to skill orchestration: agents need not more skills, but a structural mechanism to select, compose, and execute them with explicit causal dependencies. We propose GraSP, the first executable skill graph architecture that introduces a compilation layer between skill retrieval and execution. GraSP transforms flat skill sets into typed directed acyclic graphs (DAGs) with precondition-effect edges, executes them with node-level verification, and performs locality-bounded repair through five typed operators -- reducing replanning from O(N) to O(d^h). Across ALFWorld, ScienceWorld, WebShop, and InterCode with eight LLM backbones, GraSP outperforms ReAct, Reflexion, ExpeL, and flat skill baselines in every configuration, improving reward by up to +19 points over the strongest baseline while cutting environment steps by up to 41%. GraSP's advantage grows with task complexity and is robust to both skill over-retrieval and quality degradation, confirming that structured orchestration -- not larger skill libraries -- is the key to reliable agent execution.

---

## 论文详细总结（自动生成）

以下是对论文《GraSP: Graph-Structured Skill Compositions for LLM Agents》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：尽管LLM智能体的技能库（Skill Library）日益丰富，但研究发现技能并非“越多越好”。当提供超过3-4个技能时，智能体性能往往会因认知负荷过重、幻觉增加而下降（即“少即是多”现象）。
*   **研究动机**：作者认为瓶颈已从“技能可用性”转向“**技能编排（Orchestration）**”。现有的扁平化技能调用方式缺乏显式的因果依赖，导致一旦中间步骤失败，智能体必须进行全局重规划（复杂度 $O(N)$），效率低下且极易出错。

### 2. 方法论：GraSP 架构
GraSP（Graph-Structured Skill Compositions）在技能检索与执行之间引入了一个**编译层**，核心流程分为四个阶段：
1.  **记忆增强检索（Memory-conditioned Retrieval）**：结合语义匹配和历史成功经验，计算检索置信度，筛选出最相关的技能子集。
2.  **DAG 编译（DAG Compilation）**：将扁平技能集转化为有向无环图（DAG）。节点为技能调用，边分为三类：
    *   **状态边（State）**：前置条件满足关系。
    *   **数据边（Data）**：输出到输入的参数传递。
    *   **顺序边（Order）**：基于经验的软优先级约束。
3.  **验证执行与局部修复（Verified Execution & Local Repair）**：按拓扑顺序执行，每个节点都有预设的验证器（Verifier）。若失败，利用**五种局部修复算子**（REBIND, INSERT PREREQ, SUBSTITUTE, REWIRE, BYPASS）在局部子图内修复，将重规划复杂度降至 $O(d^h)$。
4.  **置信度路由（Confidence-based Routing）**：若检索置信度过低，系统会自动回退到反应式控制（如 ReAct），确保鲁棒性。

### 3. 实验设计
*   **数据集/场景**：涵盖了四种交互式基准测试：
    *   **ALFWorld**：家庭模拟任务。
    *   **ScienceWorld**：科学实验任务。
    *   **WebShop**：在线购物导航。
    *   **InterCode**：Bash 代码执行。
*   **对比方法**：
    *   **ReAct**：基础的思考-行动循环。
    *   **Reflexion**：带自我反思的迭代。
    *   **ExpeL**：基于经验学习的方法。
    *   **ReAct + Skills**：将技能作为扁平工具调用的基线。
*   **主干模型**：测试了包括 DeepSeek V3.2、GPT-4.1、Claude-4-Sonnet、GLM-5、Gemini 2.5 Pro、o4 Mini、Qwen3-235B 和 Kimi-K2.5 在内的 8 种主流 LLM。

### 4. 资源与算力
*   论文**未明确说明**具体的硬件训练算力（如 GPU 型号或数量）。
*   由于该研究主要基于 API 调用各种 LLM 主干模型进行推理实验，且核心创新点在于推理侧的架构编排而非模型预训练，因此算力消耗主要集中在推理阶段的 API Token 消耗。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个基准测试和 8 个模型上进行了 48 组配置实验，每组实验重复 3 次取平均值。
*   **消融实验**：详细测试了记忆模块、DAG 编译、局部修复和路由机制的各自贡献。
*   **鲁棒性测试**：专门设计了针对“技能过度检索”和“技能质量下降”的压力测试。
*   **充分性评价**：实验设计非常充分且客观。通过跨模型、跨领域的对比，证明了 GraSP 的提升是架构性的，而非依赖于特定模型的特性。

### 6. 主要结论与发现
*   **全面领先**：GraSP 在所有配置下均优于所有基线，奖励值最高提升 19 点。
*   **高效性**：相比 ReAct，GraSP 最多可减少 41% 的环境交互步数，显著降低了推理成本。
*   **复杂度优势**：任务越复杂（长路径任务），GraSP 的优势越明显，验证了局部修复机制的有效性。
*   **编排胜过数量**：实验证明，即使技能库质量较低或检索冗余，结构化的图编排也能通过“编译过滤”和“局部修复”保持性能稳定。

### 7. 优点
*   **结构化创新**：首次提出可执行技能图架构，填补了检索与执行之间的“编译”空白。
*   **局部修复代数**：定义的五种修复算子为智能体错误恢复提供了形式化的解决路径。
*   **高鲁棒性**：置信度路由机制解决了结构化规划在不确定环境下的僵化问题。
*   **普适性强**：在 8 种不同架构和规模的模型上均表现出色。

### 8. 不足与局限
*   **DAG 限制**：目前的架构基于有向无环图，无法直接处理需要循环（Loop）执行的任务（如反复测量直到达标）。
*   **编译开销**：虽然减少了环境步数，但 DAG 编译阶段本身需要额外的 LLM 调用，对于极简单的任务可能存在过度设计的嫌疑。
*   **多模态扩展**：目前主要在文本交互环境中验证，尚未在视觉导航等复杂多模态物理环境中进行大规模测试。

（完）
