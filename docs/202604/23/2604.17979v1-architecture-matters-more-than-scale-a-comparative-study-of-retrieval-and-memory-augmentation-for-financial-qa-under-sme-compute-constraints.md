---
title: "Architecture Matters More Than Scale: A Comparative Study of Retrieval and Memory Augmentation for Financial QA Under SME Compute Constraints"
title_zh: 架构比规模更重要：中小企业计算约束下金融问答的检索与记忆增强比较研究
authors: "Jianan Liu, Jing Yang, Xianyou Li, Weiran Yan, Yichao Wu, Penghao Liang, Mengwei Yuan"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17979v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 金融问答中检索和记忆增强架构的比较研究
tldr: 本研究针对中小企业在有限算力下的金融问答需求，探讨了模型架构对性能的影响。通过在本地8B参数模型上对比RAG、结构化长期记忆等四种架构，发现架构选择比模型规模更关键：结构化记忆擅长确定性计算，而RAG在对话式推理中表现更优。研究最终提出了一个平衡精度与效率的混合部署框架，为资源受限环境下的金融AI落地提供了实用路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17979v1/fig-001.webp\", \"caption\": \"Fig. 1. Unified architecture overview of the four evaluated systems. All methods share the same input (Q + D) and evaluation layer; they differ in how context is selected, stored, and injected into the prompt. Conversational mode additions (bottom panel) apply across all architectures when evaluating ConvFinQA.\", \"page\": 4, \"index\": 1, \"width\": 1076, \"height\": 550}, {\"url\": \"assets/figures/arxiv/2604.17979v1/fig-002.webp\", \"caption\": \"Fig. 4. Judge versus close confusion matrices across architectures in ConvFinQA. The top-right cell corresponds to fluency–accuracy divergence (Judge = Correct, Close = Wrong). Mem0-Augmented exhibits the largest divergence, indicating that memory-heavy architectures produce fluent but numerically incorrect responses more frequently. Counts and percentages are shown per cell.\", \"page\": 9, \"index\": 2, \"width\": 1076, \"height\": 275}]"
motivation: 中小企业在缺乏大规模算力资源的情况下，亟需探索如何在受限环境下实现高效、准确且合规的金融AI推理。
method: 在本地部署的8B参数模型上，系统对比了基准LLM、RAG、结构化长期记忆及记忆增强对话推理四种架构在FinQA和ConvFinQA上的表现。
result: 实验发现结构化记忆显著提升了显式运算任务的精度，而检索式方法在处理具有隐式参考特征的对话式推理时表现更佳。
conclusion: 架构优化在受限环境下比单纯扩大模型规模更有效，建议采用动态选择推理策略的混合框架以平衡准确性、可审计性与基础设施效率。
---

## 摘要
人工智能（AI）和大型语言模型（LLM）的快速普及正在通过实现报告、决策支持和自动推理的自然语言接口，改变金融分析领域。然而，关于不同的基于 LLM 的推理架构在实际金融工作流中的表现，尤其是在中小企业（SME）面临的成本、准确性和合规性约束下，目前的实证理解仍然有限。中小企业通常在严苛的基础设施限制下运营，缺乏云端 GPU 预算、专门的 AI 团队以及 API 规模的推理能力，这使得架构效率成为首要关注点。为了确保实际相关性，我们引入了一个明确的中小企业约束评估设置，其中所有实验均使用本地托管的 8B 参数指令微调模型进行，无需云端规模的基础设施。这一设计在现实的部署环境中隔离了架构选择的影响。我们在 FinQA 和 ConvFinQA 基准测试中系统地比较了四种推理架构：基准 LLM、检索增强生成（RAG）、结构化长期记忆以及记忆增强对话推理。结果揭示了一种一致的架构反转现象：结构化记忆提高了确定性、操作数显式任务的精度，而基于检索的方法在对话式、引用隐式场景中优于以记忆为中心的方法。基于这些发现，我们提出了一个混合部署框架，该框架动态选择推理策略，以平衡数值准确性、可审计性和基础设施效率，为资源受限环境下的金融 AI 应用提供了一条切实可行的路径。

## Abstract
The rapid adoption of artificial intelligence (AI) and large language models (LLMs) is transforming financial analytics by enabling natural language interfaces for reporting, decision support, and automated reasoning. However, limited empirical understanding exists regarding how different LLM-based reasoning architectures perform across realistic financial workflows, particularly under the cost, accuracy, and compliance constraints faced by small and medium-sized enterprises (SMEs). SMEs typically operate within severe infrastructure constraints, lacking cloud GPU budgets, dedicated AI teams, and API-scale inference capacity, making architectural efficiency a first-class concern. To ensure practical relevance, we introduce an explicit SME-constrained evaluation setting in which all experiments are conducted using a locally hosted 8B-parameter instruction-tuned model without cloud-scale infrastructure. This design isolates the impact of architectural choices within a realistic deployment environment. We systematically compare four reasoning architectures: baseline LLM, retrieval-augmented generation (RAG), structured long-term memory, and memory-augmented conversational reasoning across both FinQA and ConvFinQA benchmarks. Results reveal a consistent architectural inversion: structured memory improves precision in deterministic, operand-explicit tasks, while retrieval-based approaches outperform memory-centric methods in conversational, reference-implicit settings. Based on these findings, we propose a hybrid deployment framework that dynamically selects reasoning strategies to balance numerical accuracy, auditability, and infrastructure efficiency, providing a practical pathway for financial AI adoption in resource-constrained environments.

---

## 论文详细总结（自动生成）

这篇论文《Architecture Matters More Than Scale: A Comparative Study of Retrieval and Memory Augmentation for Financial QA Under SME Compute Constraints》对中小企业（SME）在有限算力下如何优化金融问答系统进行了深入探讨。以下是详细的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：金融分析需要极高的数值推理精度，但中小企业面临“算力贫困”：缺乏大规模云端 GPU 预算、API 成本高昂且有合规性限制。
*   **核心问题**：在模型规模受限（如 8B 参数）的现实场景下，不同的推理架构（检索增强 RAG vs. 长期记忆增强）对金融问答性能的影响究竟如何？
*   **整体含义**：论文挑战了“规模至上”的观念，证明在受限环境下，**架构选择对性能的影响远超模型规模的提升**。

### 2. 方法论：核心思想与关键技术
论文对比了四种统一架构，并引入了符号化处理：
*   **四种架构**：
    1.  **Baseline LLM**：直接使用指令微调模型进行推理。
    2.  **RAG (检索增强生成)**：将文档分解为事实块，基于余弦相似度动态检索最相关的 Top-k 事实。
    3.  **Mem0-Augmented (非结构化记忆)**：将历史对话和上下文作为自由文本存储在持久化存储中。
    4.  **Structured Mem0 (结构化记忆)**：将表格数据转化为“实体-属性-值”的元组，通过模式验证减少操作数歧义。
*   **关键技术细节**：
    *   **符号化数值解码 (Symbolic Numerical Decoding)**：在输出端应用后处理，提取最后一个数值并进行比例归一化（如分数转百分比），以消除格式干扰。
    *   **对话历史注入**：在对话式任务中，将所有历史 Q/A 对拼接到当前 Prompt 中。
    *   **混合路由框架 (Hybrid Routing)**：根据任务类型（单轮 vs. 多轮）动态选择最适合的架构。

### 3. 实验设计
*   **数据集**：
    *   **FinQA**：专注于单轮、基于财务报表的复杂数值推理。
    *   **ConvFinQA**：多轮对话式金融分析，涉及隐式引用和跨轮次实体跟踪。
*   **对比方法**：上述四种架构在相同基座模型下的表现。
*   **评估指标**：精确匹配 (EM)、容差匹配 (Close)、LLM 裁判评分 (Judge Correct)、延迟 (Latency) 和 Token 消耗量。

### 4. 资源与算力
*   **模型**：本地托管的 **Llama 3.1 (8B)** 指令微调版。
*   **推理引擎**：使用 **Ollama** 在本地服务器运行。
*   **算力细节**：文中明确指出实验是在“商品化硬件”（Commodity Hardware）上进行的，以模拟中小企业的真实环境。虽然未列出具体的 GPU 型号（如 A100 或 RTX 4090），但强调了**不使用云端大规模基础设施**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   FinQA 验证集：492 个样本。
    *   ConvFinQA 验证集：500 个对话，共 1,490 个轮次。
*   **充分性**：实验设计非常全面，包含了 95% 置信区间分析、随对话轮次增加的性能衰减分析、级联错误率分析以及 Token 经济学分析。通过跨数据集的对比，揭示了架构表现的“反转现象”，实验逻辑严密且客观。

### 6. 主要结论与发现
*   **架构反转现象**：
    *   在**确定性、操作数显式**的任务（FinQA）中，**结构化记忆**表现最好，因为它能稳定操作数选择。
    *   在**对话式、引用隐式**的任务（ConvFinQA）中，**RAG** 表现更好，因为动态检索允许模型在后续轮次中纠正早期的解析错误。
*   **流畅度-准确度背离**：记忆增强架构生成的回答听起来更专业（Judge 评分高），但数值准确率反而更低，这警示开发者不能仅依赖 LLM 裁判。
*   **成本效益**：RAG 在保持高准确度的同时，Token 消耗远低于持久化记忆架构。
*   **路由优势**：使用简单的“任务路由”策略（单轮用结构化记忆，多轮用 RAG）可在不增加成本的情况下提升 2.9% 的综合准确率。

### 7. 优点
*   **实用性极强**：直接针对中小企业的痛点，提供了可落地的技术路线图。
*   **洞察深刻**：提出了“不确定性分解”理论，解释了为什么持久化记忆在对话中可能导致错误累积。
*   **评估严谨**：引入符号化归一化，排除了模型因格式问题（如 0.12 vs 12%）导致的评分误差。

### 8. 不足与局限
*   **模型单一性**：实验仅基于 Llama 3.1 8B，虽然具有代表性，但未验证其他同规模模型（如 Mistral 或 Qwen）是否存在相同的架构反转规律。
*   **性能瓶颈**：所有架构在 ConvFinQA 上的准确率都卡在 50-55% 左右，显示出无监督架构在处理复杂对话实体跟踪时的局限性。
*   **领域覆盖**：主要基于公开财报数据，对于非结构化程度更高的金融文本（如财报电话会议记录）的泛化能力有待验证。

（完）
