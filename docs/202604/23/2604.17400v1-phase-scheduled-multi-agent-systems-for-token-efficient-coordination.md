---
title: Phase-Scheduled Multi-Agent Systems for Token-Efficient Coordination
title_zh: 面向高效 Token 协作的相位调度多智能体系统
authors: Mohit Dubey
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17400v1"
tags: ["query:agent"]
score: 9.0
evidence: 多智能体系统中的令牌高效协作和上下文共享
tldr: 针对大语言模型多智能体系统因并行执行和全量上下文共享导致的 Token 浪费，本文提出 PSMAS 框架。该框架将智能体激活建模为圆周流形上的连续控制，通过相位调度和上下文压缩，在保持性能的同时显著降低 Token 消耗，为高效多智能体协作提供了新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17400v1/fig-001.webp\", \"caption\": \"Figure 2: Sweep-field analysis. Top: four operating regimes; the velocity-failure boundary (red band) is sharp, confirming Theorem 7.3. Bottom: theory–empirical gap at small ε reflects summarisation overhead, which does not affect the scheduling gain.\", \"page\": 6, \"index\": 1, \"width\": 407, \"height\": 445}, {\"url\": \"assets/figures/arxiv/2604.17400v1/fig-002.webp\", \"caption\": \"Figure 1: PSMAS overview. (a) Agents on S1 by topological phase; sweep φ(t) (red) activates only A3 (within ε). (b) Dependency DAG with TPA phase labels derived by eq. (2). (c) Active agents receive full context; idle agents receive a compressed summary of length αL. Scheduling and compression are decoupled (§6): scheduling controls the idle fraction; compression controls the residual idle cost.\", \"page\": 4, \"index\": 2, \"width\": 1049, \"height\": 415}]"
motivation: 现有多智能体系统因缺乏时间维度的协调，导致所有智能体同时激活并共享冗余上下文，造成严重的 Token 资源浪费。
method: 提出 PSMAS 框架，利用圆周流形上的全局扫描信号按相位激活智能体，并对非激活状态的智能体实施上下文压缩。
result: "在多个基准测试中，PSMAS 平均减少了 27.3% 的 Token 消耗，且性能损失控制在 2.1% 以内，优于现有的路由基准。"
conclusion: 通过引入时间维度的相位调度，可以在不显著牺牲性能的情况下大幅提升多智能体系统的 Token 利用效率。
---

## 摘要
由大语言模型驱动的多智能体系统（MAS）面临严重的 Token 低效问题，这主要源于两个复合因素：(i) 非结构化的并行执行，即所有智能体不顾输入就绪情况同时激活；(ii) 无限制的上下文共享，即每个智能体都会接收全部累积的上下文，而不考虑其相关性。现有的缓解策略——如静态剪枝、分层分解和学习型路由——将协作视为结构分配问题，却从根本上忽略了其时间维度。我们提出了相位调度多智能体系统（PSMAS），该框架将智能体激活重新概念化为对基于圆流形建模的共享注意力空间的连续控制。每个智能体 i 根据任务依赖拓扑被分配一个固定角相位 theta_i（范围为 [0, 2*pi]）；全局扫描信号 phi(t) 以速度 omega 旋转，仅激活处于角度窗口 epsilon 内的智能体。空闲状态的智能体接收压缩后的上下文摘要，从而减少每步的 Token 消耗。我们在 LangGraph 上实现了 PSMAS，并在四个结构化基准测试（HotPotQA-MAS、HumanEval-MAS、ALFWorld-Multi、WebArena-Coord）和两个非结构化对话场景中进行了评估，并证明了扫描动力学的稳定性、收敛性和最优性。PSMAS 实现了 27.3% 的平均 Token 减少量（范围为 21.4-34.8%），同时将任务性能保持在全激活基线的 2.1 个百分点以内（p < 0.01，n = 500），且在 Token 减少量上比最强的学习型路由基线高出 5.6 个百分点，同时性能下降减少了 2.0 个百分点。至关重要的是，我们表明调度和压缩是独立的增益来源：仅调度一项就贡献了 18-20 个百分点的减少量，且在压缩退化高达 alpha = 0.40 时仍表现稳健。

## Abstract
Multi-agent systems (MAS) powered by large language models suffer from severe token inefficiency arising from two compounding sources: (i) unstructured parallel execution, where all agents activate simultaneously irrespective of input readiness; and (ii) unrestricted context sharing, where every agent receives the full accumulated context regardless of relevance. Existing mitigation strategies - static pruning, hierarchical decomposition, and learned routing - treat coordination as a structural allocation problem and fundamentally ignore its temporal dimension. We propose Phase-Scheduled Multi-Agent Systems (PSMAS), a framework that reconceptualizes agent activation as continuous control over a shared attention space modeled on a circular manifold.   Each agent i is assigned a fixed angular phase theta_i in the range [0, 2*pi], derived from the task dependency topology; a global sweep signal phi(t) rotates at velocity omega, activating only agents within an angular window epsilon. Idle agents receive compressed context summaries, reducing per-step token consumption. We implement PSMAS on LangGraph, evaluate on four structured benchmarks (HotPotQA-MAS, HumanEval-MAS, ALFWorld-Multi, WebArena-Coord) and two unstructured conversational settings, and prove stability, convergence, and optimality results for the sweep dynamics. PSMAS achieves a mean token reduction of 27.3 percent (range 21.4-34.8 percent) while maintaining task performance within 2.1 percentage points of a fully activated baseline (p < 0.01, n = 500 per configuration), and outperforms the strongest learned routing baseline by 5.6 percentage points in token reduction with 2.0 percentage points less performance drop. Crucially, we show that scheduling and compression are independent sources of gain: scheduling alone accounts for 18-20 percentage points of reduction, robust to compression degradation up to alpha = 0.40.

---

## 论文详细总结（自动生成）

这是一份关于论文《Phase-Scheduled Multi-Agent Systems for Token-Efficient Coordination》（面向高效 Token 协作的相位调度多智能体系统）的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：大语言模型（LLM）驱动的多智能体系统（MAS）在处理复杂任务时存在严重的 **Token 低效**问题。这种低效源于两个方面：一是**非结构化并行**（所有智能体同时激活，无论输入是否就绪）；二是**无限制上下文共享**（每个智能体都接收全量上下文，包含大量无关信息）。
*   **核心痛点**：现有的优化方法（如静态剪枝、分层分解、学习型路由）主要关注结构分配，忽略了协作中的**时间维度**。
*   **整体含义**：本文提出 **PSMAS** 框架，将智能体协作建模为圆周流形（$S^1$）上的连续控制问题，通过“相位调度”在时间轴上精确控制智能体的激活状态，从而在不显著损失性能的前提下大幅降低 Token 消耗。

### 2. 方法论
*   **核心思想**：将智能体激活视为对共享注意力空间的几何控制。
*   **关键技术细节**：
    *   **圆周流形（$S^1$）建模**：将协作空间设想为一个圆环，每个智能体 $i$ 被分配一个固定的角相位 $\theta_i \in [0, 2\pi)$。
    *   **相位分配（Phase Assignment）**：
        *   **拓扑相位分配（TPA）**：根据任务依赖的有向无环图（DAG）进行拓扑排序，均匀分配相位。
        *   **加权相位分配（WPA）**：根据智能体的预期推理延迟比例分配相位间距。
    *   **扫描信号（Sweep Signal）**：引入全局信号 $\phi(t) = (\omega t) \mod 2\pi$，以速度 $\omega$ 旋转。只有当智能体相位处于扫描窗口 $\epsilon$ 内时才被激活。
    *   **上下文交付策略**：激活状态的智能体接收全量上下文；空闲状态的智能体仅接收由轻量级模型（如 Mistral-7B）生成的压缩摘要。
    *   **解耦原理**：证明了“调度增益”（减少激活频率）与“压缩增益”（减少空闲时的 Token）是相互独立的，调度是核心贡献。

### 3. 实验设计
*   **数据集/场景**：
    *   **结构化基准**：HotPotQA-MAS（多跳问答）、HumanEval-MAS（代码生成）、ALFWorld-Multi（具身任务）、WebArena-Coord（网页导航）。
    *   **非结构化场景**：AgentDebate（自由辩论）、ResearchBench（开放式研究助理）。
*   **对比方法（Baselines）**：
    *   全激活（Full Activation）、静态剪枝（Static Pruning）、随机调度（Random Sched.）。
    *   **强基线**：AgentPrune（图剪枝）、HuggingGPT（LLM 路由）、RouterLLM（基于 MLP 的自适应路由）。

### 4. 资源与算力
*   **模型使用**：使用 Mistral-7B-Instruct-v0.2 作为摘要生成后端，通过 vLLM 框架部署。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或总训练时长。但提到摘要生成的平均延迟为 1.18 秒，且 PSMAS 作为一个中间件层集成在 LangGraph 中，主要涉及推理侧的调度控制。

### 5. 实验数量与充分性
*   **实验规模**：每种配置进行了 $n=500$ 组实验，结果具有统计学意义（$p < 0.01$）。
*   **消融实验**：针对压缩率 $\alpha$、扫描速度 $\omega$、窗口大小 $\epsilon$ 进行了详尽的消融研究。
*   **充分性评价**：实验设计非常充分。不仅覆盖了多种任务类型（从代码到具身智能），还对比了最先进的自适应路由方法，并专门验证了在依赖关系不明（非结构化）场景下的鲁棒性。

### 6. 主要结论与发现
*   **Token 减省**：PSMAS 实现了平均 **27.3%** 的 Token 减少（最高达 34.8%）。
*   **性能保持**：任务性能下降控制在 **2.1 个百分点**以内，优于所有同类 Token 优化基线。
*   **优于路由**：相比最强的 RouterLLM，PSMAS 在 Token 减少上多出 5.6%，且性能损失更小，证明了连续时间控制优于离散路由决策。
*   **理论验证**：证明了系统在延迟噪声下的稳定性，以及向全激活固定点收敛的性质。

### 7. 优点
*   **理论严密**：引入动力系统和几何控制理论（如 Kuramoto 模型），为多智能体调度提供了坚实的数学基础，而非单纯的启发式规则。
*   **解耦分析**：清晰地界定了调度和压缩各自的贡献，回应了“增益是否全靠压缩”的质疑。
*   **易于集成**：作为 LangGraph 的中间件实现，仅需约 80 行 Python 代码即可集成，实用性强。

### 8. 不足与局限
*   **依赖图准确性**：在非结构化场景下，如果初始的依赖图推断错误，会导致相位错位（Phase Misalignment），从而影响性能。
*   **摘要开销**：虽然节省了 Token，但引入了额外的摘要生成延迟（约 1.2 秒），在对实时性要求极高的场景下可能受限。
*   **动态性限制**：对于任务中途发生剧烈变化的动态依赖关系，目前的静态相位分配可能需要频繁重新计算。

（完）
