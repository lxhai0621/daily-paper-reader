---
title: "To Know is to Construct: Schema-Constrained Generation for Agent Memory"
title_zh: 知即建构：面向智能体记忆的图式约束生成
authors: "Lei Zheng, Weinan Song, Daili Li, Yanming Yang"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20117v1"
tags: ["query:agent"]
score: 10.0
evidence: 用于智能体记忆系统的模式约束生成
tldr: 针对智能体记忆系统中检索噪声及生成式访问的结构性幻觉问题，本文提出SCG-MEM架构。该方法受建构主义启发，将记忆访问重构为模式约束生成，通过动态认知模式严格限制LLM解码以确保生成有效键值，并结合同化与顺应机制实现记忆更新与多跳推理，显著增强了长程记忆的准确性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20117v1/fig-001.webp\", \"caption\": \"Figure 4: Impact of Hop Count across Categories. Performance consistently peaks at hop-1 across all categories, demonstrating the critical value of one-step associative propagation. Multi-Hop exhibits the largest relative gain from hop-0 to hop-1, as direct schema matching alone cannot bridge disjoint conversation sessions. However, hop-2 uniformly degrades performance, with Temporal reasoning suffering the most severe decline, indicating that excessive propagation introduces semantically drifted noise.\", \"page\": 7, \"index\": 1, \"width\": 496, \"height\": 423}, {\"url\": \"assets/figures/arxiv/2604.20117v1/fig-002.webp\", \"caption\": \"Figure 3: Impact of Retrieval Size k. Performance exhibits a consistent rise-then-fall pattern across all categories, peaking around k = 35. Insufficient retrieval (k < 20) misses relevant context, while excessive retrieval (k > 40) introduces noise that degrades reasoning precision.\", \"page\": 7, \"index\": 2, \"width\": 502, \"height\": 426}, {\"url\": \"assets/figures/arxiv/2604.20117v1/fig-003.webp\", \"caption\": \"Figure 2: The SCG-Mem Framework. (A) Evolutionary Schema Construction: New dialogue turns are processed via dual pathways— Assimilation grounds inputs to existing schema nodes through constrained decoding, while Accommodation expands the Prefix Trie with novel concepts via free generation. (B) Relational Topology Construction: Co-occurring concepts within each turn are linked in an Associative Graph, with edge weights computed by accumulated IDF products to capture semantic coupling strength. (C) Constructive Recall: Given a query, the system first activates seed concepts via schema-constrained decoding (guaranteeing k̂ ∈ S), then performs associative propagation over the graph to gather contextually relevant memory entries for response generation.\", \"page\": 3, \"index\": 3, \"width\": 938, \"height\": 454}, {\"url\": \"assets/figures/arxiv/2604.20117v1/fig-004.webp\", \"caption\": \"Table 1: Experimental results on the LoCoMo benchmark across different foundation models. Since SCG-MEM requires access to tokenlevel probability distributions for schema-constrained decoding, we evaluate on open-weights models (Qwen 2.5 and Llama 3.2). SCG-MEM consistently outperforms the strongest baseline A-MEM, demonstrating the efficacy of structural constraints even on smaller-scale models. (Best results in bold).\", \"page\": 6, \"index\": 4, \"width\": 1044, \"height\": 649}, {\"url\": \"assets/figures/arxiv/2604.20117v1/fig-005.webp\", \"caption\": \"Figure 1: Comparison of Memory Access Paradigms. (a) Dense Retrieval: Encodes queries and memory into vectors and retrieves top-k entries via similarity matching. While structurally safe (k̂ ∈ S), it suffers from the semantic gap where nearest neighbors may be contextually irrelevant. (b) Unconstrained Generative Memory: Directly prompts the LLM to generate memory keys. This approach risks Structural Hallucination—producing semantically plausible but non-existent keys (e.g., “Concept X” /∈ S), leading to lookup failures. (c) SCG-MEM: Constrains LLM decoding via a Cognitive Schema (Prefix Trie), guaranteeing that all generated keys are valid (k̂ ∈ S). The Associative Graph then enables multi-hop traversal to gather contextually relevant neighbors.\", \"page\": 2, \"index\": 5, \"width\": 986, \"height\": 242}]"
motivation: 传统基于检索的记忆系统易受语义重叠干扰产生噪声，而直接生成记忆键值则面临生成不存在条目的结构性幻觉风险。
method: 提出SCG-MEM架构，通过动态认知模式约束解码过程，并利用关联图谱与同化/顺应机制实现记忆的结构化组织与更新。
result: 在LoCoMo长程记忆基准测试中，SCG-MEM在所有评估类别上均显著优于现有的基于检索的基准方法。
conclusion: 研究表明，基于模式约束的生成式记忆访问能有效消除结构性幻觉，是提升智能体长程认知能力的有效途径。
---

## 摘要
建构主义认识论认为，知识是主动建构的，而非被动复制的。尽管大语言模型（LLMs）具有生成特性，但现有的大多数智能体记忆系统仍基于稠密检索。然而，稠密检索严重依赖于句子内的语义重叠或实体匹配。因此，嵌入向量往往无法区分语义相似但语境不同的实例，从而因检索到语境不匹配的条目而引入大量噪声。相反，直接采用开放式生成进行记忆访问存在“结构性幻觉”的风险，即模型生成了记忆中并不存在的记忆键，导致查找失败。受此认识论启发，我们假设记忆从根本上是由认知图式组织的，而有效的召回必须是在这些图式结构内进行的生成过程。为了实现这一点，我们提出了 SCG-MEM，一种图式约束的生成式记忆架构。SCG-MEM 将记忆访问重新表述为图式约束生成。通过维护动态认知图式，我们严格约束 LLM 的解码过程，使其仅生成有效的记忆条目键，从而为防止结构性幻觉提供了形式化保证。为了支持长期适应，我们通过同化（将输入关联到现有图式）和顺应（用新概念扩展图式）来模拟记忆更新。此外，我们构建了一个关联图，通过激活传播实现多跳推理。在 LoCoMo 基准测试上的实验表明，SCG-MEM 在所有类别上的性能均显著优于基于检索的基准模型。

## Abstract
Constructivist epistemology argues that knowledge is actively constructed rather than passively copied. Despite the generative nature of Large Language Models (LLMs), most existing agent memory systems are still based on dense retrieval. However, dense retrieval heavily relies on semantic overlap or entity matching within sentences. Consequently, embeddings often fail to distinguish instances that are semantically similar but contextually distinct, introducing substantial noise by retrieving context-mismatched entries. Conversely, directly employing open-ended generation for memory access risks "Structural Hallucination" where the model generates memory keys that do not exist in the memory, leading to lookup failures. Inspired by this epistemology, we posit that memory is fundamentally organized by cognitive schemas, and valid recall must be a generative process performed within these schematic structures. To realize this, we propose SCG-MEM, a schema-constrained generative memory architecture. SCG-MEM reformulates memory access as Schema-Constrained Generation. By maintaining a dynamic Cognitive Schema, we strictly constrain LLM decoding to generate only valid memory entry keys, providing a formal guarantee against structural hallucinations. To support long-term adaptation, we model memory updates via assimilation (grounding inputs into existing schemas) and accommodation (expanding schemas with novel concepts). Furthermore, we construct an Associative Graph to enable multi-hop reasoning through activation propagation. Experiments on the LoCoMo benchmark show that SCG-MEM substantially improves performance across all categories over retrieval-based baselines.

---

## 论文详细总结（自动生成）

### 论文总结：SCG-MEM —— 面向智能体记忆的图式约束生成

#### 1. 核心问题与整体含义（研究动机和背景）
论文针对自主智能体（Autonomous Agents）的长程记忆访问提出了新的解决方案。
*   **研究背景**：现有的记忆系统主要基于**稠密检索（Dense Retrieval）**，依赖语义相似度匹配。
*   **核心痛点**：
    1.  **检索噪声**：语义相似的实体在不同语境下含义不同，导致检索到无关上下文。
    2.  **结构性幻觉（Structural Hallucination）**：若让大模型直接生成记忆索引键（Keys），模型可能会生成记忆库中根本不存在的键，导致查找彻底失败。
*   **研究动机**：受皮亚杰（Piaget）的建构主义认识论启发，认为记忆不是被动复制而是主动建构的。论文主张记忆应由“认知图式”组织，召回过程应是在结构约束下的生成过程。

#### 2. 方法论：核心思想与关键技术
论文提出了 **SCG-MEM** 架构，将记忆访问重构为**图式约束生成（Schema-Constrained Generation）**。

*   **核心组件**：
    *   **认知图式（Cognitive Schema）**：将记忆条目提取为离散概念（Keywords），并构建为**前缀树（Prefix Trie）**。该树定义了智能体知识的边界。
    *   **约束解码（Constrained Decoding）**：在 LLM 生成记忆键时，通过前缀树对 Token 概率分布进行掩码处理（Masking），强制模型只能生成树中存在的有效路径，从而在数学上消除结构性幻觉。
    *   **进化图式构建（Evolutionary Schema Construction）**：
        *   **同化（Assimilation）**：将新输入关联到现有图式节点。
        *   **顺应（Accommodation）**：当发现新概念时，通过自由生成扩展前缀树，更新知识边界。
    *   **关联图（Associative Graph）**：在图式之上构建加权无向图，边权重基于 **IDF（逆文档频率）乘积**计算，用于捕捉概念间的语义耦合强度。
*   **召回流程**：
    1.  **图式激活**：通过约束束搜索（Beam Search）生成种子概念。
    2.  **关联传播**：在关联图上进行随机游走（Activation Propagation），获取隐含的相关上下文。
    3.  **上下文重构**：将激活的概念映射回原始文本，合成最终回答。

#### 3. 实验设计
*   **数据集与 Benchmark**：使用 **LoCoMo** 基准测试，包含超长对话（平均 9K token，最高 35 个会话）。
*   **评估类别**：单跳检索（Single-Hop）、多跳推理（Multi-Hop）、时序更新（Temporal）和对抗性查询（Adversarial）。
*   **对比方法**：
    *   **LoCoMo (Full Context)**：全量上下文输入。
    *   **ReadAgent**：基于摘要压缩和交互查找。
    *   **MemoryBank**：基于遗忘曲线的动态更新。
    *   **MemGPT**：分层上下文管理。
    *   **A-MEM**：基于智能体笔记和稠密检索的关联记忆。
*   **基础模型**：Qwen2.5 (1.5B, 3B) 和 Llama 3.2 (1B, 3B)。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练/推理时长。
*   **实现细节**：提到所有模型均使用 Hugging Face Transformers 库在本地部署，并以**全精度（Full Precision）**运行，以便进行精确的 Logit 操作（这是约束解码所必需的）。

#### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同规模的开源模型上进行了完整测试，涵盖了长程记忆的四个核心维度。
*   **消融实验**：
    *   验证了“认知约束”的必要性（移除后多跳性能下降 39.5%）。
    *   验证了“进化更新”的必要性（移除后时序任务性能显著下降）。
*   **参数敏感性分析**：详细探讨了检索数量 $k$（在 35 左右达到最优）和关联跳数（Hop-1 效果最好，Hop-2 引入噪声）的影响。
*   **充分性评价**：实验设计较为全面，通过跨模型、跨任务的对比验证了方法的鲁棒性，消融实验有效地证明了各组件的贡献。

#### 6. 主要结论与发现
*   **性能飞跃**：SCG-MEM 在所有类别上均显著优于最强基准 A-MEM。在 Qwen2.5 3B 上，单跳任务 F1 提升了 146.7%，多跳任务提升了 126.6%。
*   **消除幻觉**：通过前缀树约束，成功解决了生成式记忆访问中的结构性幻觉问题。
*   **关联优势**：相比扁平的向量检索，基于图式的关联传播能更好地连接跨会话的碎片化信息。
*   **动态适应**：同化与顺应机制使智能体能够有效追踪随时间变化的客观事实。

#### 7. 优点（亮点）
*   **理论创新**：将经典的建构主义心理学理论（皮亚杰图式理论）成功引入 LLM 记忆架构。
*   **方案闭环**：通过前缀树将“存在性（是否存在该记忆）”与“关联性（记忆间如何联系）”解耦，既保证了生成的安全性，又保留了生成的灵活性。
*   **抗干扰能力强**：在对抗性任务中表现优异，证明了其语义映射的准确性。

#### 8. 不足与局限
*   **模型依赖性**：该方法需要访问模型的 **Token 级概率分布（Logits）**，这意味着它目前无法直接应用于 GPT-4 等闭源 API 模型。
*   **计算开销**：虽然消除了检索噪声，但维护动态前缀树和进行约束解码会增加额外的计算复杂性，文中未详细讨论推理延迟。
*   **模型规模限制**：实验主要集中在 1B-3B 的小规模模型上，尚未在大规模模型（如 70B+）上验证其扩展性。
*   **存储效率**：未深入探讨记忆长期增长后的压缩与清理机制。

（完）
