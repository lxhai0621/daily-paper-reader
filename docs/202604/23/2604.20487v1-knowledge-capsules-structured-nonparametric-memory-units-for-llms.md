---
title: "Knowledge Capsules: Structured Nonparametric Memory Units for LLMs"
title_zh: 知识胶囊：大语言模型的结构化非参数化记忆单元
authors: "Bin Ju, Shenfeng Weng, Danying Zhou, Kunkai Su, Rongkai Xu"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20487v1"
tags: ["query:ma-kf"]
score: 10.0
evidence: 用于改进RAG架构的结构化非参数化记忆单元
tldr: 针对大语言模型知识更新成本高及RAG在长文本和多跳推理中不稳定的问题，本文提出“知识胶囊”（Knowledge Capsules）。这是一种结构化的非参数化记忆单元，通过外部键值注入（KVI）框架将关系知识直接编译为注意力机制可用的KV表示。该方法无需更新参数，使外部知识能直接参与模型计算，在多个问答基准测试中表现优于RAG和GraphRAG。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20487v1/fig-001.webp\", \"caption\": \"Table 1: Positioning of KVI along the knowledge integration axis. Prior work improves retrieval, structure, or memory, but remains at the token level. KVI integrates knowledge directly into attention memory.\", \"page\": 6, \"index\": 1, \"width\": 931, \"height\": 473}, {\"url\": \"assets/figures/arxiv/2604.20487v1/fig-002.webp\", \"caption\": \"Figure 3: Unified proxy hallucination rate (↑ is worse) across TruthfulQA [39] and FEVER [40]. Left/middle: TruthfulQA MC1/MC2 likelihood proxies mapped as 100− MC proxy (%) (conditional log-likelihood over multiple-choice targets; not official TruthfulQA script scores). Right: FEVER veracity task mapped as 100− label accuracy, where accuracy is computed by parsing the first occurrence of SUPPORTS/REFUTES/NOT ENOUGH INFO in the full model output and comparing to gold (label-only; not the full evidence-based FEVER submission scorer).\", \"page\": 15, \"index\": 2, \"width\": 879, \"height\": 227}, {\"url\": \"assets/figures/arxiv/2604.20487v1/fig-003.webp\", \"caption\": \"Table 2: Main QA performance: exact match (%, 95% bootstrap CI in brackets). Bold indicates the best EM per dataset.\", \"page\": 13, \"index\": 3, \"width\": 962, \"height\": 346}, {\"url\": \"assets/figures/arxiv/2604.20487v1/fig-004.webp\", \"caption\": \"Figure 2: Architecture of the External KVI framework.\", \"page\": 8, \"index\": 4, \"width\": 922, \"height\": 752}, {\"url\": \"assets/figures/arxiv/2604.20487v1/fig-005.webp\", \"caption\": \"Figure 1: Comparison between GraphRAG and the proposed KVI framework under a multihop, constraint-intensive query: “SFTSV distribution in China in 2012.” GraphRAG retrieves relevant evidence via graph traversal but ultimately aggregates it within the prompt, where knowledge competes at the token level under standard attention dynamics, often resulting in incomplete or constraint-violating answers due to the lack of explicit enforcement of temporal or relational conditions. The figure highlights the fundamental difference between token-level context augmentation (GraphRAG) and memory-level knowledge integration (KVI).\", \"page\": 4, \"index\": 5, \"width\": 832, \"height\": 542}]"
motivation: 传统的RAG通过上下文扩展引入知识，存在Token竞争、长文本处理不稳定及多跳推理能力弱等局限。
method: 提出知识胶囊及KVI框架，将从语料中提取的结构化关系知识转化为注意力机制兼容的KV表示，实现内存级的知识交互。
result: 在多个问答基准测试中，该方法在稳定性、准确性以及处理长上下文和多跳推理任务上均显著优于RAG和GraphRAG。
conclusion: 知识胶囊通过将知识集成从上下文层面转向记忆交互层面，为LLM提供了一种高效、稳定且无需重训的知识扩展方案。
---

## 摘要
大语言模型 (LLMs) 将知识编码在参数权重中，这使得在不进行重新训练的情况下更新或扩展知识的成本极高。检索增强生成 (RAG) 通过将检索到的文本附加到输入中来缓解这一限制，但它纯粹通过上下文扩展运行，外部知识在注意力机制中作为 token 进行竞争。因此，其影响是间接的且通常不稳定，特别是在长上下文和多跳推理场景中。我们提出了知识胶囊 (Knowledge Capsules)，这是一种结构化的非参数化记忆单元，用于表示归一化的关系知识，并可以使用冻结的基础模型直接从文档语料库中构建。我们没有将知识作为文本注入，而是引入了一种外部键值注入 (KVI) 框架，该框架将胶囊编译为与注意力机制兼容的键值表示，使外部知识能够直接参与模型的注意力计算。通过将知识集成从上下文层面的增强转向记忆层面的交互，所提出的框架在多个问答 (QA) 基准测试中始终优于 RAG 和 GraphRAG，在长上下文和多跳推理中表现出更高的稳定性和准确性，且无需任何参数更新。

## Abstract
Large language models (LLMs) encode knowledge in parametric weights, making it costly to update or extend without retraining. Retrieval-augmented generation (RAG) mitigates this limitation by appending retrieved text to the input, but operates purely through context expansion, where external knowledge competes as tokens within the attention mechanism. As a result, its influence is indirect and often unstable, particularly in long context and multi hop reasoning scenarios. We propose Knowledge Capsules, structured nonparametric memory units that represent normalized relational knowledge and can be constructed directly from document corpora using a frozen base model. Instead of injecting knowledge as text, we introduce an External Key Value Injection (KVI) framework that compiles capsules into attention-compatible key value representations, enabling external knowledge to directly participate in the model's attention computation. By shifting knowledge integration from context-level augmentation to memory level interaction, the proposed framework consistently outperforms RAG and GraphRAG across multiple QA benchmarks, with improved stability and accuracy in long context and multi hop reasoning, while requiring no parameter updates.

---

## 论文详细总结（自动生成）

这是一份关于论文《Knowledge Capsules: Structured Nonparametric Memory Units for LLMs》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：大语言模型（LLM）的知识存储在参数权重中，更新成本高。现有的检索增强生成（RAG）虽然能引入外部知识，但属于“上下文扩展”范式，即外部知识以Token形式进入输入序列。
*   **研究动机**：在RAG中，外部知识必须与格式Token、无关上下文在注意力机制中竞争，导致其影响是间接且不稳定的，尤其在长文本和多跳推理中容易失效。
*   **整体含义**：本文试图将外部知识从“Token层面的上下文”提升到“记忆层面的键值（KV）表示”，让外部知识直接参与Transformer的注意力计算，从而提高推理的稳定性和准确性。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：提出**知识胶囊（Knowledge Capsules）**和**外部键值注入（KVI）框架**。将知识表示为结构化的非参数化单元，并将其预编译为模型可直接读取的KV张量。
*   **关键技术细节**：
    *   **知识胶囊表示**：定义为四元组 $C = (S, R, O, I)$，分别代表主体、关系、客体和来源标识符。
    *   **两级记忆架构**：
        1.  **符号层**：以JSON/图结构存储，易于解释和扩展。
        2.  **连续层**：利用冻结的LLM将胶囊编译为KV张量（KV Bank），包含“实体锚点KV”和“三元组KV”。
    *   **双通道注入机制**：
        1.  **KV通道**：将检索到的胶囊KV张量直接拼接在注意力机制的Cache中。
        2.  **Prompt通道**：提供原始文本证据用于生成时的“落地”（Grounding）。
    *   **计算公式**：在每一层注意力计算中，全量KV为：$K_{full} = [K_{ext}; K_{prompt}]$，$V_{full} = [V_{ext}; V_{prompt}]$。

### 3. 实验设计
*   **数据集/场景**：
    *   **问答基准**：Natural Questions (NQ, 单跳)、HotpotQA (多跳)、MedHopQA (生物医学多跳)。
    *   **幻觉评估**：TruthfulQA、FEVER。
*   **对比方法（Baselines）**：
    *   **LLM**：无检索的基座模型。
    *   **RAG**：稠密检索 + 文本Prompt。
    *   **GraphRAG**：图引导检索 + 文本Prompt。
    *   **KV Prefix**：稠密检索 + 原始文本直接转KV注入（消融实验）。
*   **基座模型**：Qwen2.5-7B-Instruct 和 Mistral-7B-Instruct-v0.3。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的GPU型号、数量或总训练时长。
*   **实现细节**：提到使用了冻结（Frozen）的模型进行推理和KV编译，这意味着不需要进行参数微调（Training-free）。实验中提到了使用FAISS进行索引，以及通过HTTP端点提供推理服务。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从通用问答到专业医疗领域的三个主要QA数据集，以及两个幻觉检测数据集。
*   **充分性**：
    *   **消融实验**：通过对比“无图引导”和“无KV注入”的变体，验证了各组件的必要性。
    *   **跨模型验证**：在Qwen和Mistral两个主流开源模型上均取得了提升，证明了方法的通用性。
    *   **统计显著性**：报告了95%的自助法（Bootstrap）置信区间，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **KVI优于RAG**：在所有基准测试中，KVI的表现均优于传统的RAG和GraphRAG，尤其在多跳推理（如MedHopQA）中优势巨大。
*   **KV注入需要结构化**：简单的将文本转KV注入（KV Prefix）效果极差，甚至不如基座模型，说明只有经过结构化处理的“知识胶囊”才能有效作为记忆单元。
*   **稳定性提升**：KVI通过内存级交互，减少了Token竞争带来的干扰，使模型在处理复杂约束（如时间、空间条件）时更稳定。

### 7. 优点
*   **无需训练**：完全基于冻结模型，部署成本低，且能即时更新知识。
*   **内存级融合**：解决了RAG中外部知识被稀释的问题，使外部知识成为“一等公民”参与推理。
*   **可解释性与溯源性**：通过符号层的来源标识符（I），模型生成的每一条知识都可以追溯到原始文档。

### 8. 不足与局限
*   **提取质量依赖**：KVI的效果高度依赖于初始三元组提取的准确性，提取错误会直接传播到KV记忆中。
*   **实体识别限制**：检索过程依赖实体锚点，对于缺乏明确实体或需要高度抽象推理的查询，效果可能受限。
*   **内存开销**：虽然KV表示比原始文本紧凑，但注入额外的KV张量仍会增加推理时的显存占用。
*   **评估范围**：主要集中在结构化答案（如实体、ID）的问答上，在开放式长文生成上的表现仍需进一步验证。

（完）
