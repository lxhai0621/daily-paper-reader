---
title: "Mesh Memory Protocol: Semantic Infrastructure for Multi-Agent LLM Systems"
title_zh: 网格记忆协议：多智能体大语言模型系统的语义基础设施
authors: Hongwei Xu
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19540v1"
tags: ["query:agent"]
score: 9.0
evidence: 跨会话智能体间认知协作与记忆
tldr: 针对多智能体LLM系统在长周期协作中面临的认知状态共享与整合难题，本文提出了网格记忆协议（MMP）。该协议作为语义层基础设施，通过CAT7模式、SVAF评估机制、跨智能体谱系追踪和重混存储四大原语，实现了智能体间字段级的精细化协作、来源可追溯性及持久化记忆。MMP已在多个生产环境中部署，有效提升了多智能体系统的集体智能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.19540v1/fig-001.webp\", \"caption\": \"Figure 1: MMP’s 8-layer architecture. Layers 0–3 (Protocol Infrastructure) carry identity, transport, connection, and memory. Layers 4–7 (Mesh Cognition) carry coupling (SVAF), synthetic memory, xMesh (per-agent Liquid Neural Network), and application (where agents reason on the remix subgraph). The semantic-infrastructure contribution of this paper — CAT7 (§3.1), SVAF (§3.2), lineage (§3.3), remix (§3.4) — operates at Layers 3 and 4, with CMBs held in L3 Memory and SVAF evaluation performed in L4 Coupling.\", \"page\": 7, \"index\": 1, \"width\": 976, \"height\": 611}, {\"url\": \"assets/figures/arxiv/2604.19540v1/fig-002.webp\", \"caption\": \"Figure 2: MMP mesh topology across three Claude Code sessions on two machines — claude-code-mac (macOS, CTO role), claude-strategic-win and claude-research-win (Windows, COO and CMO roles). Each session runs a sym-mesh-channel MCP server as a distinct mesh peer with its own identity and meshmem (MMP §2.4: agent-to-agent, not device-to-device). Transport between machines is Bonjour mDNS on LAN with optional WebSocket relay for WAN. The in-flight CAT7 CMB shown is the one captured in Listing 1 — claude-research-win emitting via sym_observe, claude-code-mac receiving, running neural SVAF evaluation (decision='aligned'), and storing the remix with source set to the fusion of both peer node names and populated lineage.\", \"page\": 13, \"index\": 2, \"width\": 976, \"height\": 535}]"
motivation: 现有的多智能体协作缺乏一种语义协议，使智能体难以在跨会话的长周期任务中实时共享、评估和整合彼此的认知状态。
method: 提出MMP协议，包含CAT7标准模式、SVAF字段级评估、基于内容哈希的谱系追踪以及仅存储评估后理解的重混机制。
result: MMP已在三个参考部署中投入生产运行，支持自主智能体作为网格节点进行跨网络协作并实现集体智能。
conclusion: MMP填补了多智能体通信中语义基础设施的空白，为实现跨会话的复杂认知协作提供了标准化的协议框架。
---

## 摘要
大语言模型（LLM）智能体团队越来越多地协作完成跨越数天或数周的任务：例如在多日数据生成冲刺中，生成器、审查器和审计器智能体在重叠批次上实时协调；专家在会话重启后继续推进发现；产品决策在多轮审查中不断累积。这要求智能体在跨会话的实时过程中共享、评估并结合彼此的认知状态。我们将这种协作称为“跨会话智能体间认知协作”，它有别于并行的智能体执行。为了实现这一目标，必须同时解决三个问题：（P1）每个智能体逐字段决定接受同伴的哪些内容，而不是接受或拒绝整条消息；（P2）每项主张都可追溯至来源，以便返回的主张能被识别为接收者自身先前思考的回响；（P3）能够在会话重启后存续的记忆，其相关性取决于其存储方式，而非检索方式。这些是智能体通信语义层的协议级属性，有别于底层工具访问和任务委派协议。我们将这一缺失的协议层称为“语义基础设施”，而网格记忆协议（MMP）对其进行了规范。四个可组合的原语协同工作：CAT7，每个认知记忆块（CMB）的固定七字段模式；SVAF，根据接收者的角色索引锚点评估每个字段并实现 P1；智能体间谱系，作为内容哈希键的父级和祖先携带并实现 P2；以及重混（remix），仅存储接收者对每个接受的 CMB 经角色评估后的理解，而非原始的同伴信号，从而实现 P3。MMP 已完成规范、发布并在三个参考部署的生产环境中运行，其中每个会话都运行一个作为网格对等节点的自主智能体，拥有自己的身份和记忆，并与网络中的其他智能体协作以实现集体智能。

## Abstract
Teams of LLM agents increasingly collaborate on tasks spanning days or weeks: multi-day data-generation sprints where generator, reviewer, and auditor agents coordinate in real time on overlapping batches; specialists carrying findings forward across session restarts; product decisions compounding over many review rounds. This requires agents to share, evaluate, and combine each other's cognitive state in real time across sessions. We call this cross-session agent-to-agent cognitive collaboration, distinct from parallel agent execution. To enable it, three problems must be solved together. (P1) Each agent decides field by field what to accept from peers, not accept or reject whole messages. (P2) Every claim is traceable to source, so returning claims are recognised as echoes of the receiver's own prior thinking. (P3) Memory that survives session restarts is relevant because of how it was stored, not how it is retrieved. These are protocol-level properties at the semantic layer of agent communication, distinct from tool-access and task-delegation protocols at lower layers. We call this missing protocol layer "semantic infrastructure," and the Mesh Memory Protocol (MMP) specifies it. Four composable primitives work together: CAT7, a fixed seven-field schema for every Cognitive Memory Block (CMB); SVAF, which evaluates each field against the receiver's role-indexed anchors and realises P1; inter-agent lineage, carried as parents and ancestors of content-hash keys and realising P2; and remix, which stores only the receiver's own role-evaluated understanding of each accepted CMB, never the raw peer signal, realising P3. MMP is specified, shipped, and running in production across three reference deployments, where each session runs an autonomous agent as a mesh peer with its own identity and memory, collaborating with other agents across the network for collective intelligence.

---

## 论文详细总结（自动生成）

### 论文总结：网格记忆协议（Mesh Memory Protocol, MMP）

#### 1. 核心问题与整体含义（研究动机和背景）
论文指出，当前多智能体大语言模型（LLM）系统在处理跨越数天或数周的长周期协作任务（如多日数据生成、复杂产品决策）时存在系统性失败。现有的协议（如 MCP, A2A）主要解决工具访问和任务委派，但在**语义层面的认知协作**上存在空白。
具体表现为三个核心挑战：
*   **P1（字段级准入）：** 智能体无法针对消息中的特定语义字段进行选择性接受，只能整条接收或拒绝。
*   **P2（信号级谱系）：** 缺乏信号来源追踪，导致智能体无法识别“回声效应”（即误将自己之前的观点当成他人的独立见解）。
*   **P3（写入时过滤）：** 现有记忆系统多在检索时过滤，导致重启会话后需要重放大量原始历史，缺乏经过评估的持久化状态。
**MMP 协议**旨在构建一种“语义基础设施”，使智能体能够实时共享、评估和整合彼此的认知状态。

#### 2. 方法论：核心思想与关键技术
MMP 协议由四个核心原语组成，工作在 8 层架构的语义层（第 3-4 层）：
*   **CAT7（标准语义模式）：** 规定每个认知记忆块（CMB）必须包含 7 个固定字段：焦点（focus）、议题（issue）、意图（intent）、动机（motivation）、承诺（commitment）、视角（perspective）和情绪（mood）。这种固定模式确保了跨领域的通用评估。
*   **SVAF（符号-向量注意力融合）：** 接收端通过计算输入信号与自身角色锚点之间的余弦距离（漂移量 $\delta_f$），结合角色权重（$\alpha_f$）进行评估。评估结果分为：冗余、对齐、受限、拒绝四种状态。
*   **跨智能体谱系（Lineage）：** 使用有向无环图（DAG）记录每个 CMB 的父级和祖先。通过内容哈希键实现 $O(1)$ 复杂度的回声检测。
*   **重混（Remix）：** 写入时过滤机制。智能体不存储原始信号，而是存储经过自身角色评估后的“重混”版本。这确保了记忆在存储时即具备相关性。

#### 3. 实验设计
论文通过三种形式的参考部署验证了协议：
*   **生产级插件：** 开发了 `sym-mesh-channel`（Claude 插件），使 Claude Code 智能体能够通过 MCP 服务器进行结构化协作。
*   **跨域应用：** 在 iOS 音乐应用 MeloTune 中集成，验证了 CAT7 模式在软件开发与音乐消费跨领域间的语义耦合能力。
*   **操作性部署（核心实验）：** 运行了一个包含 3 个角色（执行、质量审查、合规审计）的 14 波次训练数据生成冲刺任务。
*   **对比基准：** 论文对 8 种现有的记忆底座（如 MemGPT, Mem0, Reflexion 等）进行了属性审计，对比它们在 P1/P2/P3 属性上的支持程度。

#### 4. 资源与算力
*   **训练算力：** 文中提到 SVAF 机制在 23.7 万个样本上进行了预训练，达到了 78.7% 的三分类准确率。
*   **运行算力：** 实际部署运行在 Claude Opus 4.7 模型之上。
*   **硬件细节：** 论文未详细列出训练 SVAF 所使用的具体 GPU 型号、数量或具体训练时长，主要侧重于协议的逻辑实现和生产环境中的运行表现。

#### 5. 实验数量与充分性
*   **实验规模：** 核心实验是一个 14 波次的生产冲刺，产生了 2998 条保留叙述。
*   **充分性评价：** 实验更倾向于“存在性证明”（Existence Proof），证明了协议在 $N=3$ 的小规模集群中是可行的且能解决跨会话重启问题。
*   **客观性：** 论文诚实地指出，目前的实验尚未在更大规模（$N \ge 10$）或跨不同模型供应商（如 GPT 与 Claude 混合）的网格中进行压力测试。

#### 6. 主要结论与发现
*   **协作收敛：** 通过谱系追踪，智能体能够在无需人工干预的情况下，通过“停止-诊断-修复”循环解决方法论分歧（观察 1）。
*   **无感知重启：** 智能体在清空对话历史重启后，能仅依靠“重混记忆”快速恢复工作状态，无需重放历史记录（观察 2）。
*   **结构化扩展：** 证明了 CAT7 头部与自定义 Body 的分离设计既保证了通用评估，又支持特定任务的复杂数据交换（观察 3）。

#### 7. 优点
*   **粒度精细：** 实现了字段级（Field-level）而非消息级的认知准入，更符合人类团队协作的逻辑。
*   **架构创新：** 提出的“写入时过滤”机制从根本上解决了长周期任务中上下文窗口溢出和检索噪声问题。
*   **互操作性：** 协议开源且提供了 Node.js 和 Swift 的参考实现，并已通过 Anthropic 的插件审核，具备实际落地能力。

#### 8. 不足与局限
*   **规模化挑战：** 尚未验证在超大规模智能体集群中的收敛速度和计算开销。
*   **供应商依赖：** 实验主要基于 Claude 系列模型，跨模型的 Token 化和工具调用差异可能影响协议表现。
*   **对抗性风险：** 虽然协议考虑了安全因素，但尚未进行针对性的红队测试，以验证其在恶意智能体注入虚假认知信号时的鲁棒性。
*   **权重调节：** 目前的角色权重（$\alpha_f$）多为静态配置，缺乏动态学习或随话题自动调整的机制。

（完）
