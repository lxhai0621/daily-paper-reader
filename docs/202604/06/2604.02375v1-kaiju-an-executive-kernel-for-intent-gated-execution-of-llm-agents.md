---
title: "KAIJU: An Executive Kernel for Intent-Gated Execution of LLM Agents"
title_zh: KAIJU：用于大语言模型智能体意图门控执行的执行内核
authors: "Cormac Guerin, Frank Guerin"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.02375v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 自主 LLM 智能体的执行内核与工具调度
tldr: 针对ReAct架构在延迟、上下文增长及安全性方面的局限，本文提出KAIJU系统。它通过解耦推理与执行层，引入“意图门控执行（IGX）”安全范式和“执行内核”抽象，实现工具的并行调度与依赖感知注入。KAIJU在复杂并行任务中表现出显著的性能优势，并提供了ReAct难以实现的系统级行为保障。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.02375v1/fig-001.webp\", \"caption\": \"Table 2. Execution gate variables.\", \"page\": 7, \"index\": 1, \"width\": 969, \"height\": 381}, {\"url\": \"assets/figures/arxiv/2604.02375v1/fig-002.webp\", \"caption\": \"Figure 1. Per-query latency across 40 queries (10 per category) at four complexity levels. Small markers: individual queries. Large markers: category mean. All modes run through the same system with identical tools, IGX, and audit. Computational queries require real-time astronomical data and multi-step computation.\", \"page\": 10, \"index\": 2, \"width\": 989, \"height\": 474}, {\"url\": \"assets/figures/arxiv/2604.02375v1/fig-003.webp\", \"caption\": \"Table 4. Observed latency across 40 queries (10 per category).\", \"page\": 9, \"index\": 3, \"width\": 462, \"height\": 265}]"
motivation: 传统的ReAct代理存在串行延迟高、上下文膨胀及易受提示词注入攻击等安全性与效率瓶颈。
method: 提出解耦推理与执行的系统架构，利用执行内核进行并行调度，并通过IGX机制基于范围、意图、影响和许可进行安全授权。
result: 实验显示KAIJU在简单查询中因规划开销略有延迟，但在中等复杂度任务中趋于平稳，且在需要并行数据采集的计算查询中具有结构性优势。
conclusion: KAIJU通过系统级抽象实现了比单纯提示词工程更强的行为保证和执行效率，为复杂LLM代理任务提供了更可靠的框架。
---

## 摘要
基于 ReAct 的工具调用型大语言模型自主智能体存在三个局限性：串行延迟、上下文二次方增长，以及易受提示词注入和幻觉的影响。近期研究趋向于将规划与执行分离，但在各种情况下，模型仍与执行机制耦合。我们为大语言模型智能体引入了一种系统级抽象，将智能体工作流的执行与大语言模型推理层解耦。我们定义了两个核心抽象：(1) 意图门控执行 (IGX)，一种在执行时强制执行意图的安全范式；(2) 执行内核，负责管理调度、工具分发、依赖解析、故障处理和安全。在 KAIJU 中，大语言模型预先进行规划，通过依赖感知的参数注入来乐观地并行调度工具。工具通过 IGX 基于四个独立变量进行授权：范围、意图、影响和许可（外部审批）。KAIJU 支持三种自适应执行模式（Reflect、nReflect 和 Orchestrator），提供适用于复杂调查、深度分析或研究的渐进式细粒度执行控制。针对 ReAct 基准的实验评估表明，由于规划开销，KAIJU 在简单查询上存在延迟劣势，在复杂度适中时趋于一致，而在需要并行数据采集的计算查询上具有结构性优势。除了延迟之外，这种分离还强制执行了仅靠提示词无法实现的各种行为保证。

## Abstract
Tool-calling autonomous agents based on large language models using ReAct exhibit three limitations: serial latency, quadratic context growth, and vulnerability to prompt injection and hallucination. Recent work moves towards separating planning from execution but in each case the model remains coupled to the execution mechanics. We introduce a system-level abstraction for LLM agents which decouples the execution of agent workflows from the LLM reasoning layer. We define two first-class abstractions: (1) Intent-Gated Execution (IGX), a security paradigm that enforces intent at execution, and (2) an Executive Kernel that manages scheduling, tool dispatch, dependency resolution, failures and security. In KAIJU, the LLM plans upfront, optimistically scheduling tools in parallel with dependency-aware parameter injection. Tools are authorised via IGX based on four independent variables: scope, intent, impact, and clearance (external approval). KAIJU supports three adaptive execution modes (Reflect, nReflect, and Orchestrator), providing progressively finer-grained execution control apt for complex investigation and deep analysis or research. Empirical evaluation against a ReAct baseline shows that KAIJU has a latency penalty on simple queries due to planning overhead, convergence at moderate complexity, and a structural advantage on computational queries requiring parallel data gathering. Beyond latency, the separation enforces behavioural guarantees that ReAct cannot match through prompting alone. Code available at https://github.com/compdeep/kaiju

---

## 论文详细总结（自动生成）

### KAIJU：用于大语言模型智能体意图门控执行的执行内核 —— 论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
传统的 LLM 智能体主要依赖 **ReAct**（推理-行动）模式，即“思考-调用工具-观察结果-再思考”的循环。这种模式在处理复杂任务时存在三个核心瓶颈：
*   **串行延迟**：工具调用通常是顺序的，导致总耗时随步骤线性增加。
*   **上下文爆炸**：每一轮对话都会携带之前的工具结果，导致上下文以 $O(n^2)$ 的速度增长，容易超出模型处理窗口并增加成本。
*   **安全性脆弱**：安全约束（如“不要删除文件”）通常写在提示词（Prompt）中，容易被提示词注入攻击绕过，且模型在失败时可能产生幻觉或放弃任务。

**KAIJU** 的提出旨在通过**解耦推理层与执行层**，建立一个类似操作系统的“执行内核”，将复杂的调度、依赖解析和安全门控从 LLM 的对话逻辑中剥离出来。

#### 2. 核心方法论
KAIJU 引入了两个关键的系统级抽象：

*   **执行内核（Executive Kernel）**：
    *   **预先规划**：LLM 仅负责生成一个有向无环图（DAG）形式的计划。
    *   **并行调度**：内核根据 DAG 的依赖关系并行触发工具，无需 LLM 逐个干预。
    *   **依赖感知参数注入**：通过 `param_refs` 机制，下游工具可以直接引用上游工具的输出字段，实现了无需 LLM 参与的数据流传递。
    *   **自适应模式**：提供 **Reflect**（波次间反射）、**nReflect**（每 N 个节点反射）和 **Orchestrator**（逐节点观察）三种模式，平衡速度与精度。

*   **意图门控执行（IGX）安全范式**：
    内核在执行每个工具前，会根据四个独立变量进行确定性授权检查：
    1.  **Scope（范围）**：允许调用的工具白名单。
    2.  **Intent（意图）**：调用者的权限等级（由系统设定，而非 LLM 自行决定）。
    3.  **Impact（影响）**：工具操作的破坏性等级（只读、写入、删除）。
    4.  **Clearance（许可）**：向外部 HTTP 端点请求的实时资源级授权。
    *注：门控决策对 LLM 不可见，防止模型通过试探来绕过安全策略。*

#### 3. 实验设计
*   **对比方法**：以带有原生并行函数调用功能的 **ReAct** 架构作为基准（Baseline）。
*   **实验场景**：
    1.  **40 组自定义查询**：涵盖简单（Simple）、目标明确（Targeted）、复杂（Complex）和计算型（Computational）四个难度等级。
    2.  **计算天文学基准**：10 个需要实时天文数据、多步计算和跨源合成的极难问题。
    3.  **GAIA 验证集**：选取了 127 个纯文本问题，评估多步推理和工具使用的准确性。
*   **模型使用**：推理/规划使用 GPT-4.1，执行/反射使用更轻量的 GPT-4.1-mini。

#### 4. 资源与算力
*   论文**未明确说明**具体的本地 GPU 型号或训练时长。
*   实验主要基于商业 API（OpenAI GPT 系列）进行。
*   成本估算基于 Sonnet 级别的定价（$3/M 输入，$15/M 输出），并假设本地工具执行开销约为 100ms。

#### 5. 实验数量与充分性
*   实验涵盖了从基础延迟测试到复杂科学计算的多个维度，总计超过 160 组测试用例。
*   **充分性**：通过对比三种不同的 DAG 执行模式（Reflect, nReflect, Orchestrator），展示了系统在不同配置下的表现。
*   **客观性**：所有模式均在同一套系统框架下运行，使用相同的工具集、安全门控和审计日志，确保了对比的公平性。

#### 6. 主要结论与发现
*   **延迟优势**：在简单任务中，KAIJU 因规划开销略慢于 ReAct；但在复杂任务中，KAIJU 的并行性使其速度大幅领先（如复杂查询下 nReflect 模式 9.5s vs ReAct 28.9s）。
*   **可靠性**：在计算天文学测试中，KAIJU 完成率为 100%，而 ReAct 因上下文溢出或中途放弃仅为 80%。
*   **上下文效率**：KAIJU 将上下文复杂度从 $O(n^2k)$ 降低到接近线性 $O(nk)$，有效避免了长任务中的性能退化。
*   **安全性**：IGX 机制在不依赖提示词的情况下，实现了亚毫秒级的确定性安全拦截。

#### 7. 优点
*   **结构化解耦**：将不稳定的 LLM 推理与确定性的系统执行分离，提高了系统的健壮性。
*   **并行效率**：通过 DAG 调度显著降低了多步骤任务的墙钟时间（Wall-clock time）。
*   **防御深度**：IGX 提供了四层防御，且决策过程对模型屏蔽，彻底消除了针对安全策略的自适应攻击面。
*   **自动修复**：内置微规划器（Micro-planner）处理工具失败，无需用户干预。

#### 8. 不足与局限
*   **规划开销**：对于只需 1-2 步的简单查询，预先规划反而增加了不必要的延迟。
*   **规划质量依赖**：如果初始规划器（Planner）能力不足导致步骤缺失，虽然反射循环可以补偿，但会增加额外的 LLM 调用成本。
*   **双模型瓶颈**：使用较弱的模型进行反射和聚合可能会在极高精度的任务中遇到质量天花板。
*   **动态匹配风险**：对于 Shell 等通用工具，依赖模式匹配来识别“影响等级”可能存在漏报风险。

（完）
