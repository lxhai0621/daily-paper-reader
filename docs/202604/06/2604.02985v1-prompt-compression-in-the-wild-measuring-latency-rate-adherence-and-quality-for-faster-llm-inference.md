---
title: "Prompt Compression in the Wild: Measuring Latency, Rate Adherence, and Quality for Faster LLM Inference"
title_zh: 实际应用中的提示词压缩：衡量延迟、压缩率遵循度及质量以实现更快的大语言模型推理
authors: "Cornelius Kummer, Lena Jurkschat, Michael Färber, Sahar Vahdati"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02985v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 通过提示词压缩管理RAG系统中的长上下文
tldr: "针对RAG系统中长上下文导致的LLM推理延迟问题，本文对提示词压缩技术进行了首次大规模系统性研究。通过在多种开源模型和GPU硬件上进行3万次查询测试，评估了压缩开销与解码速度之间的权衡。研究发现LLMLingua在特定条件下可实现18%的端到端加速且保持质量稳定，并能辅助模型迁移至低端显卡。此外，研究还发布了用于预测延迟平衡点的开源分析器，为实际部署提供指导。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.02985v1/fig-001.webp\", \"caption\": \"Fig. 1. Compression latency in dependence on the compression ratio (a) and on the executing hardware (b). With LLMLingua-2, latency is independent of the compression ratio and reduced to max. ∼3s for the longest possible prompts (48K). Compression latency and model inference time percentage of LLMLingua-2 (left) and the LLMLingua2-small variant (right) in dependence of compression hardware for a prompt size of 4,000 tokens.\", \"page\": 6, \"index\": 1, \"width\": 728, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2604.02985v1/fig-002.webp\", \"caption\": \"Fig. 3. Speed-up for the generation of a single token (Time to First Token) for all tested target models under prompt compression with LLMLingua-2 on an Nvidia A100 GPU. The compression ratio of 1 marks the baseline, meaning no compression was applied to the prompt.\", \"page\": 8, \"index\": 2, \"width\": 714, \"height\": 436}, {\"url\": \"assets/figures/arxiv/2604.02985v1/fig-003.webp\", \"caption\": \"Fig. 4. Response quality of LLMLingua-2 compressed LongBench prompts, using different LLMs, compared to the uncompressed baseline dissected into task types.\", \"page\": 9, \"index\": 3, \"width\": 723, \"height\": 457}]"
motivation: 旨在解决RAG系统中长提示词带来的高推理延迟和计算成本问题，探究提示词压缩在实际应用中的真实收益与开销权衡。
method: 通过在不同GPU和模型上进行数万次实验，分离压缩开销与解码延迟，系统评估了压缩方法在性能、质量和内存占用方面的表现。
result: "实验表明LLMLingua在硬件与压缩比匹配时可提升18%的端到端速度，且在摘要、代码生成等任务中质量保持稳定。"
conclusion: 提示词压缩的有效性高度依赖于硬件和参数配置，开发者应利用本文提供的分析器来确定其在特定场景下是否能带来实际加速。
---

## 摘要
随着语言模型在信息检索（IR）——特别是检索增强生成（RAG）系统中的广泛应用，底层大语言模型（LLM）的延迟成为了关键瓶颈，因为检索段落的长上下文会导致提示词过长，从而增加计算量。提示词压缩旨在减小输入提示词的大小，同时力求保持下游任务的性能，已成为加速大语言模型推理的一种高性价比且低延迟的方法。然而，其有效性取决于生成过程中的额外预处理时间是否能被更快的解码速度所抵消。我们展示了针对这一权衡的首次系统性、大规模研究，涵盖了多个开源大语言模型和三类 GPU，进行了数千次运行和 30,000 次查询。我们的评估将压缩开销与解码延迟分开，同时追踪输出质量和内存使用情况。研究发现，当提示词长度、压缩率和硬件能力良好匹配时，LLMLingua 可实现高达 18% 的端到端加速，且在摘要、代码生成和问答任务中的响应质量在统计学上保持不变。然而，在此运行窗口之外，压缩步骤会占据主导地位并抵消收益。我们还表明，有效的压缩可以显著减少内存使用，足以将工作负载从数据中心级 GPU 卸载到消费级显卡，而延迟仅增加 0.3 秒。我们的开源分析器可以预测每种模型-硬件配置的延迟盈亏平衡点，为提示词压缩何时能带来实际收益提供实践指导。

## Abstract
With the wide adoption of language models for IR -- and specifically RAG systems -- the latency of the underlying LLM becomes a crucial bottleneck, since the long contexts of retrieved passages lead large prompts and therefore, compute increase. Prompt compression, which reduces the size of input prompts while aiming to preserve performance on downstream tasks, has established itself as a cost-effective and low-latency method for accelerating inference in large language models. However, its usefulness depends on whether the additional preprocessing time during generation is offset by faster decoding. We present the first systematic, large-scale study of this trade-off, with thousands of runs and 30,000 queries across several open-source LLMs and three GPU classes. Our evaluation separates compression overhead from decoding latency while tracking output quality and memory usage. LLMLingua achieves up to 18% end-to-end speed-ups, when prompt length, compression ratio, and hardware capacity are well matched, with response quality remaining statistically unchanged across summarization, code generation, and question answering tasks. Outside this operating window, however, the compression step dominates and cancels out the gains. We also show that effective compression can reduce memory usage enough to offload workloads from data center GPUs to commodity cards, with only a 0.3s increase in latency. Our open-source profiler predicts the latency break-even point for each model-hardware setup, providing practical guidance on when prompt compression delivers real-world benefits.

---

## 论文详细总结（自动生成）

这是一份关于论文《Prompt Compression in the Wild: Measuring Latency, Rate Adherence, and Quality for Faster LLM Inference》的结构化分析报告：

### 1. 核心问题与整体含义
*   **研究动机**：随着检索增强生成（RAG）系统的普及，长上下文（Long Context）导致的推理延迟成为瓶颈。提示词压缩（Prompt Compression）被视为一种降低延迟和成本的手段，但学术界往往忽略了**压缩过程本身的计算开销**。
*   **核心问题**：提示词压缩在实际应用中是否真的能带来端到端的加速？压缩带来的预处理开销是否会抵消解码阶段节省的时间？
*   **研究背景**：现有的压缩研究多关注输出质量，缺乏对延迟、内存占用和硬件适配性的系统性评估。

### 2. 方法论
论文主要评估了“硬压缩”（Hard Compression）方法，即直接在文本/Token层面进行删减，使其兼容黑盒 API。
*   **核心思想**：通过小型模型识别并移除提示词中的冗余信息，保留关键语义。
*   **关键技术**：
    *   **LLMLingua**：基于困惑度（Perplexity）的令牌剪枝。使用小型解码器模型（如 GPT-2 Small 或 LLaMA-7B）计算 Token 的自信息量，移除低信息量的 Token。
    *   **LLMLingua-2**：基于分类的压缩。在编码器模型（如 XLM-RoBERTa）后添加线性分类层，通过数据蒸馏训练模型直接预测每个 Token 是否应保留。
*   **算法流程**：输入原始提示词 -> 压缩模型推理 -> 算法后处理（Token 选取） -> 生成压缩提示词 -> 目标 LLM 推理。

### 3. 实验设计
*   **数据集**：使用 **LongBench** 综合基准测试，涵盖单/多文档问答、摘要、代码生成、合成任务和少样本学习。
*   **目标模型**：
    *   开源模型：Mistral-7B-v0.3, Llama-3.1-70B。
    *   闭源 API：GPT-3.5 Turbo, GPT-4o mini。
*   **推理框架**：Hugging Face Transformers, vLLM, TGI。
*   **对比基准**：未压缩的原始提示词（Baseline）。
*   **评估维度**：端到端延迟（E2E Latency）、首字时间（TTFT）、压缩率遵循度（Rate Adherence）、下游任务质量、GPU 显存占用。

### 4. 资源与算力
论文在三种不同等级的硬件上进行了详尽测试：
1.  **数据中心级**：Nvidia A100 (40 GB)，用于大规模推理和 70B 模型测试。
2.  **旧款消费级**：Nvidia GeForce GTX 1080 Ti (11 GB)，模拟资源受限的本地部署。
3.  **移动端/统一内存架构**：Apple M1 Pro (16 GB)，评估在个人工作站上的可行性。
*   **实验规模**：总计进行了超过 **30,000 次查询**和数千次实验运行。

### 5. 实验数量与充分性
*   **充分性**：实验设计非常全面。它不仅对比了不同模型大小，还对比了不同的推理后端（如 vLLM 与 Transformers 的巨大差异），并考虑了硬件性能对压缩开销的影响。
*   **客观性**：论文诚实地指出了压缩在许多场景下（如使用 vLLM 或 API 时）可能导致负优化，没有刻意夸大压缩的效果。
*   **公平性**：通过多次重复实验（12次运行，排除前4次热身）并提供 95% 置信区间，确保了数据的可靠性。

### 6 主要结论与发现
*   **加速效果有限**：在优化过的框架（如 vLLM）中，压缩往往因预处理开销过大而导致端到端延迟增加。仅在未优化的框架或提示词极长（>10k tokens）且压缩率极高（>4x）时，才能看到约 **18%** 的端到端加速。
*   **LLMLingua-2 优于一代**：LLMLingua-2 的延迟不随压缩率变化，且在压缩率遵循度上远好于基于困惑度的一代方法。
*   **显存节省显著**：压缩可减少高达 **75%** 的峰值显存占用，使得原本需要 A100 的长文本任务可以在 1080 Ti 等低端显卡上运行。
*   **质量损失视任务而定**：摘要和问答任务对压缩较鲁棒；但代码生成、合成任务（如段落检索）和少样本学习在压缩后性能大幅下降。

### 7. 优点
*   **系统性解耦**：首次将“压缩开销”与“解码加速”分开测量，揭示了提示词压缩在工程实践中的真实瓶颈。
*   **实用性强**：发布了开源的 **Profiler（分析器）**，帮助开发者预测在特定硬件和模型组合下，提示词达到多长时使用压缩才具有“盈亏平衡点”。
*   **跨平台覆盖**：涵盖了从 HPC 到移动端 SoC 的多种硬件，具有很强的现实参考价值。

### 8. 不足与局限
*   **方法覆盖面**：主要集中在 LLMLingua 系列，未深入对比其他新兴的压缩技术（如基于摘要的压缩或软提示词压缩）。
*   **任务局限性**：实验显示在结构化要求高的任务（如代码、数学）中，硬压缩会导致语义断裂，限制了其通用性。
*   **框架依赖**：结论高度依赖于推理后端的实现（如 vLLM 的 Prefill 优化），随着推理框架技术的进步，本文的部分延迟结论可能需要更新。

（完）
