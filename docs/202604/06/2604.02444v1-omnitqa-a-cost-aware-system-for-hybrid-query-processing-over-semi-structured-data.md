---
title: "OmniTQA: A Cost-Aware System for Hybrid Query Processing over Semi-Structured Data"
title_zh: OmniTQA：一种面向半结构化数据混合查询处理的成本感知系统
authors: "Nima Shahbazi, Seiji Maekawa, Nikita Bhutani, Estevam Hruschka"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02444v1"
tags: ["query:ma-kf"]
score: 10.0
evidence: 结构化与半结构化数据的混合查询处理
tldr: OmniTQA是一个针对半结构化数据（结构化属性与自由文本共存）的成本感知型混合查询处理框架。它将LLM语义推理视为一级查询算子，并与传统关系算子整合为有向无环图（DAG）。通过数据感知规划、算子重排序和双引擎执行架构，该系统在降低LLM推理成本和延迟的同时，显著提升了复杂查询的准确率，在多项基准测试中表现优于现有方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.02444v1/fig-001.webp\", \"caption\": \"Table 3: The OmniTQA Operator Universe\", \"page\": 6, \"index\": 1, \"width\": 1083, \"height\": 327}, {\"url\": \"assets/figures/arxiv/2604.02444v1/fig-002.webp\", \"caption\": \"Table 4: Ablation Study\", \"page\": 11, \"index\": 2, \"width\": 1056, \"height\": 193}, {\"url\": \"assets/figures/arxiv/2604.02444v1/fig-003.webp\", \"caption\": \"Figure 4: Accuracy comparison of OmniTQA vs. baselines evaluated with Gemini-3-Flash.\", \"page\": 10, \"index\": 3, \"width\": 1047, \"height\": 268}, {\"url\": \"assets/figures/arxiv/2604.02444v1/fig-004.webp\", \"caption\": \"Figure 3: Illustration of the OmniTQA planning phase for the UEFA Soccer database and the query “Did the player who achieved the UEFA Men’s Player of the Year 2021 win the UCL championship?”. OmniTQA first constructs a query-aware data preview ?̂?Q (shown in blue) to ground\", \"page\": 5, \"index\": 4, \"width\": 1050, \"height\": 386}]"
motivation: 现有Text-to-SQL系统难以处理企业数据库中结构化数据与非结构化文本共存的混合模式查询。
method: 将语义推理集成到关系算子中，通过数据感知规划优化算子顺序，并采用双引擎架构动态路由任务。
result: 在多个基准测试中，OmniTQA在准确性和成本效率上均优于现有的符号化、语义化及混合基准模型。
conclusion: OmniTQA证明了通过将LLM推理与传统数据库优化技术深度融合，可以高效且低成本地解决半结构化数据的复杂查询问题。
---

## 摘要
虽然大语言模型的最新进展显著提升了 Text-to-SQL 和表格问答系统的性能，但大多数现有方法都假设所有与查询相关的信息都显式地表示在结构化模式中。在实践中，许多企业数据库包含混合模式，其中结构化属性与自由格式的文本字段共存，这要求系统能够对这两类信息进行推理。为了应对这一挑战，我们推出了 OmniTQA，这是一个面向结构化和半结构化数据的成本感知混合查询处理框架。OmniTQA 将语义推理视为一等查询算子，将基于大语言模型的语义操作与经典关系算子无缝集成到一个可执行的有向无环图中。为了管理大语言模型推理的高延迟和高成本，它通过数据感知规划扩展了经典查询优化，结合原子查询分解和算子重排序，以最小化语义工作负载。该框架还具有双引擎执行架构，可在关系数据库和 LLM 模块之间动态路由任务，并利用算子感知批处理来实现高效扩展。在多种结构化和半结构化表格问答基准测试上的广泛实验表明，OmniTQA 在准确性和成本效益方面始终优于现有的符号、语义和混合基线。这些提升在复杂查询、大型表格和多关系模式中尤为显著。

## Abstract
While recent advances in large language models have significantly improved Text-to-SQL and table question answering systems, most existing approaches assume that all query-relevant information is explicitly represented in structured schemas. In practice, many enterprise databases contain hybrid schemas where structured attributes coexist with free-form textual fields, requiring systems to reason over both types of information. To address this challenge, we introduce OmniTQA, a cost-aware hybrid query processing framework that operates over both structured and semi-structured data. OmniTQA treats semantic reasoning as a first-class query operator, seamlessly integrating LLM-based semantic operations with classical relational operators into an executable directed acyclic graph. To manage the high latency and cost of LLM inference, it extends classical query optimization with data-aware planning, combining atomic query decomposition and operator reordering to minimize semantic workload. The framework also features a dual-engine execution architecture that dynamically routes tasks between a relational database and an LLM module, using operator-aware batching to scale efficiently. Extensive experiments across a diverse suite of structured and semi-structured table question answering benchmarks demonstrate that OmniTQA consistently outperforms existing symbolic, semantic, and hybrid baselines in both accuracy and cost efficiency. These gains are particularly pronounced for complex queries, large tables and multi-relation schemas.

---

## 论文详细总结（自动生成）

### OmniTQA：面向半结构化数据混合查询处理的成本感知系统

#### 1. 核心问题与整体含义
传统的 Text-to-SQL 系统和表格问答（TQA）方法通常假设所有查询相关信息都显式存储在结构化列中。然而，现实中的企业数据库往往是**半结构化**的：关键实体、谓词或关联关系常被“埋藏”在自由格式的文本字段中。
*   **研究动机**：现有方法要么无法处理文本列中的隐含信息（如 Text-to-SQL），要么在处理大规模表格时成本极高且难以扩展（如直接使用 LLM 读表）。
*   **核心目标**：开发一个能够同时处理结构化属性和非结构化文本、且具备成本感知能力的混合查询系统。

#### 2. 方法论
OmniTQA 的核心思想是将 **LLM 语义推理视为一级查询算子**，并将其与传统关系算子（SQL）整合进一个统一的可执行有向无环图（DAG）中。

*   **三阶段架构**：
    1.  **预处理**：进行查询无关的模式清洗与剪枝，以及查询感知的“数据预览”（结合语义搜索和随机采样），为规划器提供上下文。
    2.  **规划与优化**：
        *   **原子分解**：将自然语言查询分解为最小可执行单元（算子），确保每个步骤逻辑单一。
        *   **混合算子模型**：定义了关系算子（SCAN, FILTER, JOIN 等）和语义算子（语义 MAP, 语义 FILTER, 语义 JOIN, 语义 AGG）。
        *   **成本感知优化**：利用成本模型 $Cost(G) = \sum (w_{sys} \cdot C_{sys} + w_{llm} \cdot C_{llm})$ 指导算子重排序。核心策略是**谓词迁移**与**语义延迟执行**，即尽可能先用廉价的关系算子过滤数据，减少昂贵的 LLM 调用。
        *   **计划多样化**：生成 $K$ 个候选执行计划，以应对模式映射的歧义性。
    3.  **执行与整合**：
        *   **双引擎架构**：关系算子路由至 SQL 引擎，语义算子路由至 LLM 模块。
        *   **算子感知批处理**：针对不同语义算子采用不同批处理策略（如 JOIN 采用块嵌套循环，AGG 采用递归 Map-Reduce），以解决 LLM 上下文限制并提高并行度。
        *   **结果整合**：通过 LLM-as-a-Judge 或多数投票从多个计划结果中选出最终答案。

#### 3. 实验设计
*   **数据集**：涵盖了 12 个基准测试，包括 RepairTQA（S1-S5, M1-M2 子集）、WikiSQL、WikiTableQuestions、FeTaQA、HybridQA 和 TAT-QA。这些数据集覆盖了长表、多表、复杂组合查询及半结构化文本等多种挑战。
*   **对比方法**：
    *   **纯 LLM 类**：Direct-LLM。
    *   **Text-to-SQL 类**：NL2SQL, Plan-of-SQLs。
    *   **混合类**：H-Star, Weaver。
*   **评估指标**：准确率（Acc@1, Acc@6, Majority Voting, LLM-as-a-Judge）和 Token 消耗成本。

#### 4. 资源与算力
*   **硬件环境**：高性能服务器，配置为 AMD EPYC 7R32 (48核 CPU)，192GB RAM，以及 **8 台 NVIDIA A100 GPU**。
*   **模型使用**：评估了 Gemini-3-Flash-Preview（默认）、GPT-5-Mini 和 Qwen3:30B（通过 Ollama 本地部署）。
*   **训练/运行时间**：文中未明确给出总训练时长（主要为推理过程），但提到通过批处理和并行执行来优化延迟。

#### 5. 实验数量与充分性
*   **实验规模**：在 12 个数据集上进行了大规模测试，每个数据集抽取 50-100 个样本。
*   **消融实验**：非常充分，分别验证了查询感知预览、模式剪枝、算子模型选择、计划优化和计划多样化对性能的影响。
*   **公平性**：所有对比方法均使用相同的底层 LLM，并针对不同复杂度的任务（单表 vs 多表，短表 vs 长表）进行了细分对比，实验设计客观且具有代表性。

#### 6. 主要结论与发现
*   **性能领先**：OmniTQA 在所有半结构化数据集上均显著优于基线，尤其在复杂的多表推理（RepairTQA-M2）中，准确率提升高达 48%。
*   **成本高效**：通过优化策略，OmniTQA 在保持高准确率的同时，Token 成本远低于其他混合方法，单计划执行成本与 Direct-LLM 相当。
*   **鲁棒性**：系统表现出对底层 LLM 家族的通用性，且在数据被篡改的对抗性测试中，表现出更强的“数据扎根”能力而非依赖模型先验知识。
*   **瓶颈定位**：错误分析显示，主要的失败源于“规划阶段”（如属性投影缺失或幻觉），而非执行阶段。

#### 7. 优点
*   **系统性融合**：将 LLM 推理深度集成进数据库查询优化器，而非简单的 Pipeline 串联。
*   **成本感知设计**：通过谓词下推和语义延迟，科学地平衡了查询精度与推理开销。
*   **原子化算子**：这种设计提高了计划的可解释性，并允许更细粒度的并行化和错误修复。
*   **处理长表能力强**：算子感知批处理有效解决了大表无法塞入 LLM 窗口的问题。

#### 8. 不足与局限
*   **规划器依赖**：系统性能高度依赖于初始生成的逻辑计划质量，若规划阶段出现属性遗漏，后续优化也无法挽回。
*   **延迟问题**：尽管有批处理，但涉及大量语义算子的复杂查询在实时性要求极高的场景下可能仍存在延迟挑战。
*   **领域值敏感**：在缺乏特定领域知识预览的情况下，规划器可能会在过滤常量（Filter constants）上产生幻觉。
*   **多样化开销**：生成 $K$ 个计划虽然提升了准确率，但也成倍增加了 Token 消耗。

（完）
