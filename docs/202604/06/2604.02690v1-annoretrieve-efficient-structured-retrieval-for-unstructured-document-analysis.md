---
title: "AnnoRetrieve: Efficient Structured Retrieval for Unstructured Document Analysis"
title_zh: AnnoRetrieve：面向非结构化文档分析的高效结构化检索
authors: "Teng Lin, Yuyu Luo, Nan Tang"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02690v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 非结构化文档分析的结构化检索
tldr: AnnoRetrieve 提出了一种针对非结构化文档的高效检索新范式，旨在解决传统向量检索语义粗糙且过度依赖大模型（LLM）的问题。该系统通过 SchemaBoot 自动生成文档标注模式，并利用结构化语义检索（SSR）引擎替代昂贵的向量比对，实现了基于标注的精确匹配与推理。实验证明，该方法在保持高准确率的同时，显著降低了 LLM 调用频率和计算成本。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.02690v1/fig-001.webp\", \"caption\": \"Figure 2: The pipeline of the AnnoRetrieve system, comprising a pre-annotation stage for schema-guided document annotation and a structured semantic retrieval stage for answering complex user queries. In the pre-annotation stage, Schemapboot generates and optimizes a multi-granularity schema, which is paired with models like GliNER2 to extract attribute-value pair annotations from raw documents. The retrieval framework translates natural language queries into SQL, filters documents via type and schema constraints, and constructs structured tables to enable hybrid semantic-structured reasoning, yielding precise answers to complex questions.\", \"page\": 4, \"index\": 1, \"width\": 898, \"height\": 550}, {\"url\": \"assets/figures/arxiv/2604.02690v1/fig-002.webp\", \"caption\": \"Table 3: Ablation Study on WikiText Dataset.\", \"page\": 6, \"index\": 2, \"width\": 898, \"height\": 239}, {\"url\": \"assets/figures/arxiv/2604.02690v1/fig-003.webp\", \"caption\": \"Table 1: Dataset Characteristics.\", \"page\": 6, \"index\": 3, \"width\": 848, \"height\": 184}, {\"url\": \"assets/figures/arxiv/2604.02690v1/fig-004.webp\", \"caption\": \"Table 2: Overall Performance Comparison (Average across all queries).\", \"page\": 6, \"index\": 4, \"width\": 698, \"height\": 239}]"
motivation: 传统的嵌入式向量搜索由于语义匹配粒度粗且需频繁调用 LLM 进行后处理，导致非结构化文档分析成本高昂且效率低下。
method: 系统集成了自动诱导文档标注模式的 SchemaBoot，以及将语义理解与结构化查询执行统一、无需向量嵌入的结构化语义检索（SSR）引擎。
result: 在三个真实数据集上的实验表明，AnnoRetrieve 在显著降低 LLM 调用次数和检索成本的同时，依然保持了极高的检索精度。
conclusion: 该研究通过智能结构化手段，为低成本、高精度且可扩展的文档分析建立了一种优于传统向量和图检索方法的新范式。
---

## 摘要
非结构化文档在企业和网络数据中占据主导地位，但其缺乏显式组织结构，阻碍了精确的信息检索。当前主流的检索方法，特别是基于嵌入（embedding）的向量搜索，依赖于粗粒度的语义相似度，导致了高昂的计算成本以及频繁调用大语言模型（LLM）进行后处理。为了解决这一关键问题，我们提出了 AnnoRetrieve，这是一种从嵌入转向结构化标注的新型检索范式，实现了精确的、标注驱动的语义检索。我们的系统通过在自动诱导的模式（schemas）上进行轻量级结构化查询，取代了昂贵的向量比较，从而显著减少了 LLM 的使用量和整体成本。该系统集成了两项协同的核心创新：SchemaBoot，通过多粒度模式发现和基于约束的优化自动生成文档标注模式，为标注驱动的检索奠定了基础，并消除了人工模式设计的需求；以及结构化语义检索（SSR），作为核心检索引擎，它将语义理解与结构化查询执行相统一。通过利用标注结构而非向量嵌入，SSR 实现了精确的语义匹配，能够无缝完成属性值提取、表格生成以及基于 SQL 的渐进式推理，而无需依赖 LLM 的干预。这种标注驱动的范式克服了传统基于向量的方法（粗粒度匹配且严重依赖 LLM）以及基于图的方法（计算开销高）的局限性。在三个真实世界数据集上的实验证实，AnnoRetrieve 在保持高准确率的同时，显著降低了 LLM 的调用频率和检索成本。AnnoRetrieve 通过智能结构化，为高性价比、精确且可扩展的文档分析建立了一种新范式。

## Abstract
Unstructured documents dominate enterprise and web data, but their lack of explicit organization hinders precise information retrieval. Current mainstream retrieval methods, especially embedding-based vector search, rely on coarse-grained semantic similarity, incurring high computational cost and frequent LLM calls for post-processing. To address this critical issue, we propose AnnoRetrieve, a novel retrieval paradigm that shifts from embeddings to structured annotations, enabling precise, annotation-driven semantic retrieval. Our system replaces expensive vector comparisons with lightweight structured queries over automatically induced schemas, dramatically reducing LLM usage and overall cost. The system integrates two synergistic core innovations: SchemaBoot, which automatically generates document annotation schemas via multi-granularity pattern discovery and constraint-based optimization, laying a foundation for annotation-driven retrieval and eliminating manual schema design, and Structured Semantic Retrieval (SSR), the core retrieval engine, which unifies semantic understanding with structured query execution; by leveraging the annotated structure instead of vector embeddings, SSR achieves precise semantic matching, seamlessly completing attribute-value extraction, table generation, and progressive SQL-based reasoning without relying on LLM interventions. This annotation-driven paradigm overcomes the limitations of traditional vector-based methods with coarse-grained matching and heavy LLM dependency and graph-based methods with high computational overhead. Experiments on three real-world datasets confirm that AnnoRetrieve significantly lowers LLM call frequency and retrieval cost while maintaining high accuracy. AnnoRetrieve establishes a new paradigm for cost-effective, precise, and scalable document analysis through intelligent structuring.

---

## 论文详细总结（自动生成）

### 论文总结：AnnoRetrieve：面向非结构化文档分析的高效结构化检索

#### 1. 核心问题与研究动机
*   **核心问题**：如何高效、精确地从海量非结构化文档（如法律合同、企业报告）中检索并提取结构化信息。
*   **研究背景**：
    *   **传统向量检索（Retrieve-then-Extract）**：依赖语义相似度，匹配粒度粗，返回结果包含大量无关信息，导致后续必须频繁调用昂贵的大语言模型（LLM）进行二次提取，成本高且效率低。
    *   **全量提取（Extract-All-then-Query）**：如知识图谱方法，虽然支持复杂查询，但前期构建成本巨大，难以扩展到大规模文档集。
*   **研究目标**：提出一种“标注驱动”的新范式，通过预先对文档进行轻量级结构化标注，将复杂的语义检索转化为高效的结构化查询，从而降低 LLM 调用成本并提升精度。

#### 2. 方法论
AnnoRetrieve 采用“先结构化，后检索”的策略，核心包含三个模块：
*   **SchemaBoot（自动模式诱导）**：
    *   **核心思想**：无需人工设计，自动从文档集中发现最优的标注模式（Schema）。
    *   **技术细节**：通过多粒度模式挖掘生成候选集，并利用**约束多目标优化（基于 NSGA-II 算法）**来筛选最优模式 $S^*$。优化目标包括文档覆盖率（Coverage）、区分度（Discriminative power）、标注一致性（Consistency）以及与历史查询的语义对齐度。
*   **Annotator（结构化标注生成）**：
    *   利用预训练的实体抽取模型（如 GliNER2）根据诱导出的模式对文档进行离线标注，生成“属性-值”对索引。
*   **SSR（结构化语义检索引擎）**：
    *   **核心思想**：将自然语言查询解析为 SQL 语句，直接在结构化索引上执行。
    *   **关键技术**：引入了 `EXTRACT` 操作符，允许在单条 SQL 执行计划中同时处理已标注字段和未标注的原始文本。该引擎在检索路径上**无需调用 LLM**，而是通过确定性的文本扫描或正则匹配完成精确过滤。

#### 3. 实验设计
*   **数据集**：
    1.  **LCR (Legal Case Reports)**：法律案例报告，侧重长文本和复杂属性提取。
    2.  **WikiText**：维基百科页面，涉及多领域、半结构化文本，用于测试连接（Join）操作。
    3.  **SWDE (Structured Web Data Extraction)**：网页数据，测试对噪声和模板化布局的处理能力。
*   **Benchmark 与查询类型**：每个数据集构建 50 个查询，涵盖简单投影选择、逻辑组合查询、多表连接查询以及渐进式推理查询。
*   **对比方法**：包括 VectorDB (Chroma/Pinecone)、Graph RAG (Neo4j+LLM)、ZenDB、Palimpsest、QUEST、ClosedIE 以及直接使用 GPT-4 提示词的方法。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确给出**具体的 GPU 型号、数量或总训练时长。
*   **模型使用**：提到了使用 GliNER2 进行实体抽取，以及在 Schema 生成和基准对比中使用了 GPT-4。系统强调其优势在于减少了在线检索时的 LLM Token 消耗。

#### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同领域的真实数据集上进行了测试，涵盖了从 100 到 10,000 份文档的扩展性分析。
*   **实验维度**：
    *   **总体性能对比**：对比了 F1 分数、LLM 成本和延迟。
    *   **消融实验**：分别验证了 SchemaBoot 和 SSR 引擎的贡献。
    *   **扩展性测试**：分析了随文档数量增加时的延迟变化。
    *   **模式质量分析**：对比了自动生成模式与人工标注模式的对齐度。
*   **评价**：实验设计较为全面，对比了当前最先进的各类 RAG 和数据库系统，指标选取（F1、Token 成本、延迟）符合工业界关注的核心痛点，具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **高精度**：AnnoRetrieve 在所有数据集上的 F1 分数均显著优于基准方法（如在 WikiText 上达到 0.87，而 GPT-4 为 0.72）。
*   **低成本**：相比于 Graph RAG 和直接调用 LLM，AnnoRetrieve 的 Token 消耗降低了数倍甚至数十倍。
*   **高效推理**：在处理复杂的多表连接（Join）查询时，SSR 引擎通过 SQL 优化显著减少了中间结果的提取开销。
*   **模式有效性**：SchemaBoot 诱导出的模式在覆盖率和检索性能上甚至优于 LLM 直接生成的模式。

#### 7. 优点
*   **范式创新**：打破了传统向量检索的局限，提出了“结构化优先”的检索思路。
*   **自动化程度高**：SchemaBoot 解决了非结构化数据分析中最头疼的模式设计问题。
*   **成本可控**：将昂贵的在线 LLM 推理转变为廉价的离线标注和在线结构化查询，极具商业落地价值。
*   **推理能力强**：通过 SQL 逻辑实现了比向量搜索更严谨的复杂推理。

#### 8. 不足与局限
*   **离线开销**：虽然在线检索快且省钱，但前期的离线标注（SchemaBoot + Annotator）仍需要一定的计算资源投入。
*   **模型依赖**：系统的最终精度高度依赖于底层抽取模型（如 GliNER2）的质量，如果抽取模型在特定领域表现不佳，会影响索引质量。
*   **动态更新挑战**：论文未详细讨论当文档库频繁更新或模式需要变更时，重新标注的增量成本。
*   **应用限制**：对于完全无法结构化的纯感性/创意类文本检索，该方法的优势可能不如事实类、属性类查询明显。

（完）
