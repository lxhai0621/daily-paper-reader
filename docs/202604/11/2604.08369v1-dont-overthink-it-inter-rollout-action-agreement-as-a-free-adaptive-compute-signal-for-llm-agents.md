---
title: "Don't Overthink It: Inter-Rollout Action Agreement as a Free Adaptive-Compute Signal for LLM Agents"
title_zh: 别想太多：展开间动作一致性作为大语言模型智能体的免费自适应计算信号
authors: Khushal Sethi
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08369v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 在智能体时间步中自适应分配LLM调用
tldr: 本研究针对LLM智能体在推理时计算资源分配不均的问题，提出了TrACE。这是一种无需训练的自适应计算控制器，通过衡量多次采样间动作的一致性来动态分配计算量。在简单步骤中，高一致性触发快速决策；在复杂步骤中，低一致性则增加采样次数。实验表明，TrACE在保持与固定预算自一致性方法相当的准确率时，显著降低了计算开销，是首个应用于多步序列决策任务的无需训练的自适应计算方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.08369v1/fig-001.webp\", \"caption\": \"Figure 1: Compute–accuracy frontier. Each point is one (condition, benchmark) pair. TrACE-4 and TrACE-8 (red, pink) match the accuracy of SC-4 and SC-8 (blue) at substantially fewer LLM calls, Pareto-dominating the self-consistency baseline in both settings. Error bars on MiniHouse are 95% bootstrap CIs; GSM8K bars are Wald CIs from binomial data (no per-task data available for bootstrap, see §6).\", \"page\": 4, \"index\": 1, \"width\": 747, \"height\": 257}, {\"url\": \"assets/figures/arxiv/2604.08369v1/fig-002.webp\", \"caption\": \"Figure 2: Left: Agreement is a reliable success signal. Right: TrACE call counts are task-adaptive.\", \"page\": 7, \"index\": 2, \"width\": 832, \"height\": 376}, {\"url\": \"assets/figures/arxiv/2604.08369v1/fig-003.webp\", \"caption\": \"Figure 3: τhigh ablation (TrACE-4, kmax = 4). Three thresholds are evaluated: 0.50 (commit at bare majority), 0.75 (default), and 1.00 (require unanimity, degenerates to SC-4). The dotted line shows SC-4 accuracy and call count for reference. TrACE with τ = 0.75 achieves matched accuracy at substantially fewer calls than either extreme. τ = 0.50 underspends and slightly loses accuracy; τ = 1.00 recovers SC-4 accuracy and cost.\", \"page\": 8, \"index\": 3, \"width\": 812, \"height\": 260}]"
motivation: 现有的推理时计算缩放方法对所有决策步骤统一分配预算，忽略了任务难度的差异，导致计算资源在简单步骤上的浪费。
method: 提出TrACE控制器，通过在每个时间步采样少量候选动作并计算其一致性，动态决定是立即执行还是增加采样以降低不确定性。
result: "在GSM8K和MiniHouse基准测试中，TrACE在达到与固定采样方法相同精度的前提下，减少了33%至65%的LLM调用次数。"
conclusion: 动作一致性是衡量步骤难度的可靠信号，利用该信号实现的自适应计算能在不牺牲性能的情况下大幅提升LLM智能体的推理效率。
---

## 摘要
推理时计算缩放（Inference-time compute scaling）已成为提高大语言模型（LLM）智能体可靠性的强大技术，但现有方法通常统一分配计算资源：无论决策步骤的难度如何，每个步骤都获得相同的预算。我们引入了 TrACE（Trajectorical Adaptive Compute via agrEement，通过一致性实现的轨迹自适应计算），这是一种无需训练的控制器，通过测量展开（rollout）间的动作一致性，在智能体的时间步中自适应地分配 LLM 调用。在每一步，TrACE 采样一小组候选下一步动作，并测量模型选择同一动作的一致性。高一致性预示着决策简单，控制器会立即提交；低一致性则预示着不确定性，控制器会在提交多数投票动作之前采样额外的展开，直到达到可配置的上限。该方法无需学习组件、外部验证器或人类标签。我们在涵盖单步推理（GSM8K, n=50）和多步家庭导航（MiniHouse, n=30）的两个基准测试上，使用在 CPU 上运行的 Qwen 2.5 3B Instruct 模型，将 TrACE 与贪婪解码及固定预算自一致性（SC-4, SC-8）进行了对比评估。结果显示，TrACE-4 在 GSM8K 和 MiniHouse 上分别减少了 33% 和 39% 的 LLM 调用，且准确率与 SC-4 持平；TrACE-8 则分别减少了 55% 和 65% 的调用，且准确率与 SC-8 持平。我们进一步证明，展开间的一致性是步骤级成功的可靠信号，验证了核心假设：模型自身的输出一致性编码了难度信息，且无需训练即可被利用。TrACE 是首个在多步顺序决策任务上进行评估的、针对 LLM 智能体的无需训练、逐时间步自适应计算控制器。

## Abstract
Inference-time compute scaling has emerged as a powerful technique for improving the reliability of large language model (LLM) agents, but existing methods apply compute uniformly: every decision step receives the same budget regardless of its difficulty. We introduce TrACE (Trajectorical Adaptive Compute via agrEement), a training-free controller that allocates LLM calls adaptively across agent timesteps by measuring inter-rollout action agreement. At each step, TrACE samples a small set of candidate next actions and measures how consistently the model commits to the same action. High agreement signals an easy decision; the controller commits immediately. Low agreement signals uncertainty; the controller samples additional rollouts up to a configurable cap before committing to the plurality action. No learned components, no external verifier, and no human labels are required. We evaluate TrACE against greedy decoding and fixed-budget self-consistency (SC-4, SC-8) on two benchmarks spanning single-step reasoning (GSM8K, n=50) and multi-step household navigation (MiniHouse, n=30), using a Qwen 2.5 3B Instruct model running on CPU. TrACE-4 matches SC-4 accuracy while using 33% fewer LLM calls on GSM8K and 39% fewer on MiniHouse. TrACE-8 matches SC-8 accuracy with 55% fewer calls on GSM8K and 65% fewer on MiniHouse. We further show that inter-rollout agreement is a reliable signal of step-level success, validating the core hypothesis that the model's own output consistency encodes difficulty information that can be exploited without training. TrACE is the first training-free, per-timestep adaptive-compute controller for LLM agents to be evaluated on multi-step sequential decision tasks.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **TrACE** 的方法，旨在解决大语言模型（LLM）智能体在推理过程中计算资源分配效率低下的问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：目前的 LLM 智能体在执行任务时，通常对每个决策步骤分配固定的计算预算（如贪婪解码或固定次数的采样）。然而，任务中不同步骤的难度差异巨大：有些步骤显而易见，而有些则充满歧义。
*   **核心问题**：如何在不进行额外训练、不使用外部验证器的情况下，让智能体能够“自适应”地分配推理计算量——在简单步骤上快速决策，在困难步骤上“深思熟虑”。

### 2. 方法论：TrACE 控制器
TrACE（Trajectorical Adaptive Compute via agrEement）的核心思想是利用**模型输出的一致性**作为难度的代理信号。
*   **核心流程**：
    1.  **初始采样**：在每个时间步 $t$，先以一定的温度（Temperature）独立采样 $k_{init}$（默认 2）个候选动作。
    2.  **计算一致性 ($\alpha_t$)**：计算这组动作中出现频率最高的动作（众数动作）所占的比例。
    3.  **自适应决策**：
        *   如果一致性 $\alpha_t \ge \tau_{high}$（阈值，默认 0.75），认为模型很笃定，立即执行该动作。
        *   如果一致性低于阈值，则逐个增加采样，直到一致性达标或达到预设的最大采样上限 $k_{max}$（如 4 或 8），最后执行众数动作。
*   **关键技术**：该方法完全基于行为一致性，无需模型自我报告置信度，也无需训练任何奖励模型（PRM/ORM）。

### 3. 实验设计
*   **数据集/场景**：
    *   **GSM8K**：小学数学应用题，用于测试单步推理中的泛化能力（抽取 50 题）。
    *   **MiniHouse**：论文作者开发的轻量级文本环境，模拟家庭导航和物体操作任务（30 个任务，每个任务 3 轮随机种子）。
*   **对比方法（Baselines）**：
    *   **Greedy**：贪婪解码（k=1）。
    *   **Self-Consistency (SC-4, SC-8)**：固定预算的自一致性采样，每步分别固定调用 4 次或 8 次 LLM。
*   **评估指标**：任务准确率与平均每任务 LLM 调用次数（计算成本）。

### 4. 资源与算力
*   **硬件**：实验在 **Apple M 系列 CPU** 上运行（32GB RAM），使用 8 个推理线程。
*   **模型**：使用 **Qwen 2.5 3B Instruct** 模型，通过 `llama-cpp-python` 加载 Q4_K_M 量化版本（GGUF 格式）。
*   **算力消耗**：论文强调了其 CPU 友好性。在 MiniHouse 任务中，SC-8 耗时约 40 分钟，而 TrACE-8 仅需约 14 分钟，壁挂时间减少了 65%。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 140 组评估（50 个 GSM8K 任务 + 30 个 MiniHouse 任务 × 3 个种子）。
*   **消融实验**：对阈值 $\tau_{high}$ 进行了消融研究（测试了 0.50, 0.75, 1.00 三个水平），验证了默认参数的合理性。
*   **充分性评价**：实验设计较为客观，通过帕累托前沿（Pareto Frontier）展示了性能与成本的权衡。但受限于算力，样本量（n=30/50）和模型规模（3B）相对较小。

### 6. 主要结论与发现
*   **高效性**：TrACE 在保持与固定预算自一致性（SC）相同准确率的前提下，显著减少了 LLM 调用次数。在 GSM8K 上减少了 **33-55%**，在 MiniHouse 上减少了 **39-65%**。
*   **信号有效性**：实验证明，步骤间的一致性与最终任务的成功率高度相关，高一致性步骤更有可能出现在成功的轨迹中。
*   **帕累托占优**：在计算-准确率曲线上，TrACE 明显优于传统的固定预算方法。

### 7. 优点
*   **零成本部署**：无需训练、无需标注数据、无需外部验证模型，可直接应用于任何开源 LLM。
*   **即插即用**：作为推理时的控制器，逻辑简单，计算开销极低。
*   **显著提速**：在不损失精度的前提下，大幅缩短了智能体的响应时间。

### 8. 不足与局限
*   **模型规模限制**：仅在 3B 模型上验证，尚未确认该结论是否能完美推广到 70B 或更大规模的模型。
*   **任务复杂度**：MiniHouse 相对简单，且 3B 模型在多步规划上存在性能瓶颈（准确率天花板较低）。
*   **开放式生成挑战**：对于代码编写或长文本生成等动作空间巨大的任务，简单的字符串一致性匹配（Canonicaliser）可能失效。
*   **超参数依赖**：虽然提供了默认阈值，但在不同任务中可能需要微调 $\tau_{high}$ 以达到最优平衡。

（完）
