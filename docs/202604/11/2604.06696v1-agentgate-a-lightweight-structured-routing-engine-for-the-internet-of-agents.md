---
title: "AgentGate: A Lightweight Structured Routing Engine for the Internet of Agents"
title_zh: AgentGate：面向智能体互联网的轻量级结构化路由引擎
authors: "Yujun Cheng, Enfang Cui, Hao Qin, Zhiyuan Liang, Qi Xu"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06696v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 用于AI智能体调度的结构化路由引擎
tldr: 针对“智能体互联网”中请求分发面临的延迟、隐私和成本挑战，本文提出 AgentGate，一种轻量级结构化路由引擎。它将路由建模为受限决策问题，通过动作决策和结构化落地两阶段实现高效分发。通过对 3B-7B 参数的轻量级模型进行候选感知微调，AgentGate 在资源受限环境下实现了高性能的智能体调度，为构建高效、隐私友好的智能体系统提供了可行方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06696v1/fig-001.webp\", \"caption\": \"Fig. 1. Overview of the proposed agent network architecture and representative routing task examples handled by AgentGate.\", \"page\": 2, \"index\": 1, \"width\": 494, \"height\": 421}, {\"url\": \"assets/figures/arxiv/2604.06696v1/fig-002.webp\", \"caption\": \"Fig. 3. Baseline comparison on the AgentDNS runtime benchmark. The left panel reports routing metrics, and the right panel reports escalation precision and recall.\", \"page\": 8, \"index\": 2, \"width\": 671, \"height\": 247}, {\"url\": \"assets/figures/arxiv/2604.06696v1/fig-003.webp\", \"caption\": \"Fig. 2. Detailed architecture of the AgentGate framework, illustrating the two-stage structured routing pipeline, heuristic fallback mechanisms, and confidenceaware backend selection.\", \"page\": 4, \"index\": 3, \"width\": 888, \"height\": 484}]"
motivation: 随着智能体系统向跨平台演进，如何在满足延迟、隐私和成本约束下实现高效的请求分发成为亟待解决的系统性问题。
method: 将路由分解为动作决策和结构化落地两个阶段，并采用候选感知的监督微调和难负样本策略优化轻量级模型。
result: 实验表明，经过优化的 3B-7B 模型在路由基准测试中表现出色，其性能差异主要体现在动作预测和结构化输出的质量上。
conclusion: 结构化路由是实现资源受限环境下高效、隐私感知智能体系统的可行路径，轻量级模型通过特定优化可胜任复杂的调度任务。
---

## 摘要
AI 智能体系统的快速发展正在催生一个新兴的“智能体互联网”（Internet of Agents），其中专用智能体运行在本地设备、边缘节点、私有服务和云平台之上。尽管最近的研究在智能体命名、发现和交互方面取得了进展，但在延迟、隐私和成本约束下，高效的请求调度仍然是一个开放的系统性问题。在本文中，我们提出了 AgentGate，这是一个用于候选感知的智能体调度的轻量级结构化路由引擎。AgentGate 不将路由视为不受限的文本生成，而是将其建模为一个受限决策问题，并将其分解为两个阶段：动作决策和结构化落地（structural grounding）。第一阶段确定查询是否应触发单智能体调用、多智能体规划、直接响应或安全升级；第二阶段则将所选动作实例化为可执行输出，如目标智能体、结构化参数或多步计划。为了使紧凑型模型适应这一场景，我们进一步开发了一种面向路由的微调方案，包含候选感知监督和难负样本（hard negative examples）。在包含多个 3B-7B 开源权重模型的精选路由基准测试上的实验表明，紧凑型模型在受限环境下可以提供具有竞争力的路由性能，且模型差异主要体现在动作预测、候选选择和结构化落地质量上。这些结果表明，结构化路由是构建高效且隐私感知的智能体系统的一个可行设计点，特别是在必须在资源受限的部署条件下做出路由决策时。

## Abstract
The rapid development of AI agent systems is leading to an emerging Internet of Agents, where specialized agents operate across local devices, edge nodes, private services, and cloud platforms. Although recent efforts have improved agent naming, discovery, and interaction, efficient request dispatch remains an open systems problem under latency, privacy, and cost constraints. In this paper, we present AgentGate, a lightweight structured routing engine for candidate-aware agent dispatch. Instead of treating routing as unrestricted text generation, AgentGate formulates it as a constrained decision problem and decomposes it into two stages: action decision and structural grounding. The first stage determines whether a query should trigger single-agent invocation, multi-agent planning, direct response, or safe escalation, while the second stage instantiates the selected action into executable outputs such as target agents, structured arguments, or multi-step plans. To adapt compact models to this setting, we further develop a routing-oriented fine-tuning scheme with candidate-aware supervision and hard negative examples. Experiments on a curated routing benchmark with several 3B--7B open-weight models show that compact models can provide competitive routing performance in constrained settings, and that model differences are mainly reflected in action prediction, candidate selection, and structured grounding quality. These results indicate that structured routing is a feasible design point for efficient and privacy-aware agent systems, especially when routing decisions must be made under resource-constrained deployment conditions.

---

## 论文详细总结（自动生成）

这是一份关于论文《AgentGate: A Lightweight Structured Routing Engine for the Internet of Agents》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着“智能体互联网”（Internet of Agents, IoA）的兴起，大量专用智能体分布在本地、边缘和云端。现有的多智能体系统通常依赖强大的云端大模型（LLM）进行集中调度，这带来了**高延迟、高昂的 API 成本以及隐私泄露风险**。
论文的核心问题是：如何在资源受限的边缘设备上，利用轻量级模型（3B-7B）实现高效、安全且结构化的请求分发（路由）。AgentGate 旨在充当一个“网关”，决定请求是本地处理、调用特定智能体、进行多步规划，还是因安全原因拒绝执行。

### 2. 方法论：核心思想与技术细节
AgentGate 将路由任务从“不受限的文本生成”转变为“受限的结构化决策问题”，核心流程分为两个阶段：
*   **第一阶段：动作决策（Action Decision）**
    *   模型从受限动作空间 $Y$ 中选择其一：单智能体调用（`y_call`）、多智能体规划（`y_plan`）、直接响应（`y_direct`）或安全升级/拒绝（`y_escalate`）。
    *   这一阶段作为早期过滤器，识别无需执行的请求（如违反隐私或无匹配工具）。
*   **第二阶段：结构化落地（Structural Grounding）**
    *   基于第一阶段的决策，将动作实例化。如果是调用，则选择目标智能体并生成符合 Schema 的参数；如果是规划，则生成有序的执行计划。
*   **关键技术：**
    *   **候选感知微调（Candidate-Aware Fine-Tuning）：** 使用包含候选智能体元数据的提示词进行监督微调，并引入“难负样本”（如语义重叠的工具、诱导性顺序查询）来强化决策边界。
    *   **置信度感知混合路由：** 计算动作和结构的置信度分数 $\gamma_{eff}$。若低于阈值 $\tau$，则将请求回退（Fallback）至云端强大模型处理。
    *   **启发式保障机制：** 引入基于规则的敏感词过滤和槽位填充补偿，提升系统鲁棒性。

### 3. 实验设计
*   **数据集/场景：** 构建了名为 **AgentDNS_Runtime** 的精选基准测试，包含 3,200 个实例（2400 训练/400 验证/400 测试）。涵盖餐饮、出行、医疗、气象、订票等 10 多个服务领域。
*   **对比方法：**
    1.  **Rule-based：** 基于词法触发和元数据匹配的规则系统。
    2.  **Retrieve-rank：** 先分类动作，再通过语义匹配对智能体进行排序。
    3.  **Generic Tool-Calling：** 直接使用大模型的原生工具调用能力（不进行两阶段分解）。
*   **评估指标：** 动作准确率、智能体选择准确率、参数完全匹配（Arg EM）、计划完全匹配（Plan EM）、JSON 合法性、以及安全升级的精确率/召回率。

### 4. 资源与算力
*   **硬件：** 实验在一台配备单块 **NVIDIA GeForce RTX 5090 GPU** 的服务器上完成。
*   **训练技术：** 采用了基于 **LoRA** 的参数高效微调（PEFT）技术，以适应边缘部署需求。
*   **模型规模：** 评估了 3B 到 7B 参数量的开源模型（如 Qwen2.5, Mistral, Llama2, Phi-3.5）。

### 5. 实验数量与充分性
*   **实验规模：** 论文对比了 5 种不同的模型底座，并在 3,200 个样本的基准上进行了测试。
*   **消融与诊断：** 进行了微调 vs. 基础模型的对比实验，以及针对系统延迟、吞吐量和显存占用的性能分析。
*   **充分性评价：** 实验设计较为充分，涵盖了从算法精度到系统工程性能（TTFT、QPS、显存）的全面评估。通过引入“难负样本”，实验在区分模型真实能力方面具有较好的客观性。

### 6. 主要结论与发现
*   **轻量级模型可行性：** 经过结构化微调的 3B-7B 模型在受限路由任务中表现优异，**Qwen2.5-7B** 在精度、安全性和部署成本之间达到了最佳平衡。
*   **两阶段分解的必要性：** 相比于直接的工具调用，两阶段设计显著提升了复杂场景下的可控性和准确性。
*   **微调的作用点：** 微调对“动作预测”的提升有限（预训练模型已有较强先验），但显著增强了**“结构化落地”的合规性**（如参数格式对齐）。
*   **系统瓶颈：** 实验发现“结构化落地”阶段（第二阶段）是主要的延迟来源，占总路由时间的绝大部分。

### 7. 优点
*   **系统性强：** 不仅关注模型精度，还考虑了边缘部署的实际约束（延迟、隐私、回退机制）。
*   **结构化设计：** 将开放式生成转化为受限决策，极大地提高了输出的可解释性和下游执行的可靠性。
*   **安全意识：** 明确将“安全升级（Escalate）”作为核心路由动作，体现了对隐私和授权问题的重视。

### 8. 不足与局限
*   **基准测试规模：** 虽然涵盖了多领域，但 3,200 个样本对于模拟极其复杂的“智能体互联网”环境可能仍显不足。
*   **动态性挑战：** 实验主要在静态候选集上进行，对于智能体频繁上线/下线的动态发现场景探讨较少。
*   **回退策略简化：** 目前的云端回退主要基于简单的置信度阈值，缺乏更复杂的成本-收益权衡模型。
*   **应用限制：** 该方法高度依赖于智能体元数据（AgentDNS）的质量，如果元数据描述模糊，路由效果会大幅下降。

（完）
