---
title: Weakly Supervised Distillation of Hallucination Signals into Transformer Representations
title_zh: 将幻觉信号弱监督蒸馏至 Transformer 表示中
authors: "Shoaib Sadiq Salehmohamed, Jinal Prashant Thakkar, Hansika Aredla, Shaik Mohammed Omar, Shalmali Ayachit"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06277v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 将幻觉信号蒸馏到Transformer表示中以进行内部检测
tldr: 本研究针对大语言模型幻觉检测依赖外部验证（如检索或辅助模型）的问题，提出了一种将外部监督信号蒸馏至模型内部表示的方法。通过结合子串匹配、语义相似度和LLM评判的弱监督框架，在SQuAD数据集上构建了包含隐藏状态的训练集。研究训练了多种探测分类器，证明了仅凭模型内部激活即可在推理阶段实现高效的幻觉检测，为提升模型可靠性提供了新途径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06277v1/fig-001.webp\", \"caption\": \"Table 3: Final held-out test-set results (5,000 rows) on the held-out test dataset. Here, τ∗ is the threshold that maximizes F1 on the evaluation sweep.\", \"page\": 14, \"index\": 1, \"width\": 1044, \"height\": 198}, {\"url\": \"assets/figures/arxiv/2604.06277v1/fig-002.webp\", \"caption\": \"Table 1: 5-fold cross-validation performance (mean ± std) across the five trained architectures (M0–M4). All probes are trained on yjudge labels. Higher AUC/F1/Acc and lower ECE are better.\", \"page\": 14, \"index\": 2, \"width\": 1030, \"height\": 198}, {\"url\": \"assets/figures/arxiv/2604.06277v1/fig-003.webp\", \"caption\": \"Table 2: Single-fold validation metrics from best-checkpoint analysis. Here, τ∗ denotes the threshold maximizing validation F1.\", \"page\": 14, \"index\": 3, \"width\": 925, \"height\": 198}, {\"url\": \"assets/figures/arxiv/2604.06277v1/fig-004.webp\", \"caption\": \"Table 6: Output-level similarity baselines and best probe result on the held-out 5,000-row test set.\", \"page\": 15, \"index\": 4, \"width\": 682, \"height\": 209}, {\"url\": \"assets/figures/arxiv/2604.06277v1/fig-005.webp\", \"caption\": \"Figure 1: PR (top left), ROC (top right), and predicted logit distribution (bottom) from full train/development-pool diagnostic inference. These plots are intended for qualitative comparison only and do not reflect held-out test evaluation.\", \"page\": 17, \"index\": 5, \"width\": 979, \"height\": 788}, {\"url\": \"assets/figures/arxiv/2604.06277v1/fig-006.webp\", \"caption\": \"Table 7: Diagnostic metrics computed on the full training-data evaluation run (eval 10, 10k samples). These are not held-out test metrics and should not be interpreted as final generalization performance.\", \"page\": 17, \"index\": 6, \"width\": 620, \"height\": 198}]"
motivation: 旨在解决现有幻觉检测方法在推理阶段对外部参考答案或检索系统的强依赖问题，探索能否仅通过模型内部激活进行检测。
method: 利用弱监督信号标注生成结果，并训练五种不同结构的探测分类器（从MLP到Transformer）来学习模型各层隐藏状态中的幻觉特征。
result: 实验表明Transformer架构的探测器表现最佳，且在保持高检测精度的同时，推理延迟仅为毫秒级，对整体生成吞吐量影响极小。
conclusion: 幻觉检测信号可以成功蒸馏到Transformer的内部表示中，支持在无需外部验证的情况下实现高效的实时幻觉监测。
---

## 摘要
现有的针对大语言模型（LLMs）的幻觉检测方法在推理时依赖外部验证，需要标准答案、检索系统或辅助评判模型。我们探究是否可以在训练期间将这种外部监督蒸馏到模型自身的表示中，从而在推理时仅通过内部激活实现幻觉检测。我们提出了一种弱监督框架，结合了三种互补的接地（grounding）信号：子串匹配、句子嵌入相似度以及大语言模型作为评判者的判定，从而在无需人工标注的情况下将生成的回答标记为有依据的或幻觉的。利用该框架，我们基于 SQuAD v2 构建了一个包含 15,000 个样本的数据集（10,500 个训练/开发样本和 5,000 个独立测试样本），其中每个示例都将 LLaMA-2-7B 生成的回答与其完整的逐层隐藏状态和结构化幻觉标签配对。随后，我们直接在这些隐藏状态上训练了五个探测分类器：ProbeMLP (M0)、LayerWiseMLP (M1)、CrossLayerTransformer (M2)、HierarchicalTransformer (M3) 和 CrossLayerAttentionTransformerV2 (M4)，并将外部接地信号仅视为训练时的监督。我们的核心假设是，幻觉检测信号可以被蒸馏到 Transformer 表示中，从而在推理时无需任何外部验证即可实现内部检测。结果支持了这一假设。基于 Transformer 的探测器实现了最强的辨别能力，其中 M2 在五折平均 AUC/F1 上表现最佳，而 M3 在单折验证和留出测试集评估中均表现最佳。我们还对推理效率进行了基准测试：探测器的延迟范围在 0.15 到 5.62 毫秒（批处理）以及 1.55 到 6.66 毫秒（单样本）之间，而端到端生成加探测器的吞吐量保持在约每秒 0.231 次查询，表明实际开销可以忽略不计。

## Abstract
Existing hallucination detection methods for large language models (LLMs) rely on external verification at inference time, requiring gold answers, retrieval systems, or auxiliary judge models. We ask whether this external supervision can instead be distilled into the model's own representations during training, enabling hallucination detection from internal activations alone at inference time.   We introduce a weak supervision framework that combines three complementary grounding signals: substring matching, sentence embedding similarity, and an LLM as a judge verdict to label generated responses as grounded or hallucinated without human annotation. Using this framework, we construct a 15000-sample dataset from SQuAD v2 (10500 train/development samples and a separate 5000-sample test set), where each example pairs a LLaMA-2-7B generated answer with its full per-layer hidden states and structured hallucination labels.   We then train five probing classifiers: ProbeMLP (M0), LayerWiseMLP (M1), CrossLayerTransformer (M2), HierarchicalTransformer (M3), and CrossLayerAttentionTransformerV2 (M4), directly on these hidden states, treating external grounding signals as training-time supervision only. Our central hypothesis is that hallucination detection signals can be distilled into transformer representations, enabling internal detection without any external verification at inference time.   Results support this hypothesis. Transformer-based probes achieve the strongest discrimination, with M2 performing best on 5-fold average AUC/F1, and M3 performing best on both single-fold validation and held-out test evaluation. We also benchmark inference efficiency: probe latency ranges from 0.15 to 5.62 ms (batched) and 1.55 to 6.66 ms (single sample), while end-to-end generation plus probe throughput remains approximately 0.231 queries per second, indicating negligible practical overhead.

---

## 论文详细总结（自动生成）

这是一份关于论文《Weakly Supervised Distillation of Hallucination Signals into Transformer Representations》的结构化深度总结：

### 1. 核心问题与整体含义
该研究针对大语言模型（LLM）的**幻觉检测**问题。目前主流的检测方法（如检索增强、LLM-as-a-judge、对比困惑度等）在推理阶段高度依赖外部资源（标准答案、辅助模型或多次前向传播），这导致了高昂的计算成本和延迟。
**核心动机**：探索能否将外部监督信号在训练阶段“蒸馏”到模型自身的内部表示中，从而在推理时仅通过分析模型的**隐藏状态（Hidden States）**，无需任何外部参考即可实时检测幻觉。

### 2. 方法论
论文提出了一种将外部弱监督信号转化为内部探测能力的方法：
*   **弱监督标签框架**：结合三种信号自动标注数据：
    1.  **混合语义标签**：通过子串匹配、MiniLM 句子嵌入相似度（阈值 $\tau=0.72$）和针对不确定问题的拒绝回答（Abstention）启发式规则进行标注。
    2.  **LLM 评判标签**：使用 Mistral-7B-Instruct 作为评判者，通过结构化 Prompt 判断生成内容是否受上下文支持。
*   **隐藏状态提取**：使用 LLaMA-2-7B 生成回答，提取每一层（32层）、每个解码步骤（最多96步）的隐藏状态，形成张量 $\tilde{H} \in \mathbb{R}^{32 \times 96 \times 4096}$。
*   **探测分类器（Probes）**：设计了五种架构来学习这些张量与幻觉标签之间的映射：
    *   **M0 (ProbeMLP)**：全局平均池化后接 MLP。
    *   **M1 (LayerWiseMLP)**：逐层处理后融合，用于分析各层贡献。
    *   **M2 (CrossLayerTransformer)**：对层和 Token 进行全局自注意力建模。
    *   **M3 (HierarchicalTransformer)**：先进行层内 Token 建模，再进行跨层建模（分层结构）。
    *   **M4 (CrossLayerAttentionTransformerV2)**：引入门控机制和多查询跨层注意力。

### 3. 实验设计
*   **数据集**：基于 **SQuAD v2** 构建。包含 15,000 个样本（10,500 用于训练/开发，5,000 作为严格的留出测试集）。
*   **基准测试（Benchmark）**：
    *   **内部对比**：对比五种不同复杂度的探测器架构。
    *   **外部对比**：对比传统的输出层相似度基准（如 Cosine Similarity, Token F1 等）。
*   **评估指标**：AUC-ROC（主要指标）、F1 分数、准确率（Acc）和预期校准误差（ECE）。

### 4. 资源与算力
*   **硬件**：实验在单台 **NVIDIA A100 GPU** 上完成。
*   **训练细节**：使用 AdamW 优化器，余弦学习率调度，训练 30 个 Epoch，Batch Size 为 8。
*   **数据规模**：每个样本的隐藏状态张量约为 25MB（float16 精度），总数据集包含完整的逐层激活信息。

### 5. 实验数量与充分性
*   **实验规模**：进行了 5 折分层交叉验证以确保鲁棒性，并进行了独立的留出测试集评估。
*   **分析维度**：除了性能指标，还进行了推理效率基准测试（延迟与吞吐量）、层重要性分析、标签一致性分析（Judge vs. Hybrid）以及表示空间的可视化（PCA/t-SNE）。
*   **评价**：实验设计较为客观、公平。通过留出测试集证明了模型的泛化能力，且对比了传统基线方法，验证了内部表示探测的优越性。

### 6. 主要结论与发现
*   **幻觉可检测性**：实验证实幻觉信号确实编码在 Transformer 的隐藏状态中。即使是简单的 M0 模型也能获得非随机的检测效果。
*   **架构表现**：Transformer 架构（M2, M3）显著优于 MLP。**M3（分层 Transformer）**在留出测试集上表现最强（AUC 0.8577），且校准度最好。
*   **层级分布**：幻觉信号在**深层网络**中更为集中，反映了上下文不一致性在模型前向传播中的累积。
*   **极低开销**：探测器的推理延迟仅为 0.15-6.66 毫秒，相对于 LLaMA-2 约 4.3 秒的生成时间，开销几乎可以忽略不计（吞吐量保持不变）。

### 7. 优点
*   **推理高效性**：将复杂的验证逻辑前置到训练期，推理阶段实现了“零外部依赖”的实时监测。
*   **早期预警潜力**：由于隐藏状态在 Token 正式输出前即可获得，该方法理论上支持在幻觉发生前进行拦截。
*   **数据贡献**：发布了首个配对完整隐藏状态与多信号幻觉标签的大规模数据集。

### 8. 不足与局限
*   **弱监督噪声**：标签依赖于 Mistral-7B 的判断和启发式规则，可能存在模型偏见或标注错误。
*   **泛化范围有限**：实验仅限于 LLaMA-2-7B 和 SQuAD 领域（维基百科），跨模型和跨领域的迁移效果尚待验证。
*   **解码策略单一**：仅使用了贪婪解码（Greedy Decoding），未探讨采样（Sampling）等策略下隐藏状态的变化。
*   **长度限制**：对隐藏状态进行了 96 个 Token 的截断，可能丢失长文本后期的幻觉信号。

（完）
