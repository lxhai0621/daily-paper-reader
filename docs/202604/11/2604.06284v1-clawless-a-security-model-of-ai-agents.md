---
title: "ClawLess: A Security Model of AI Agents"
title_zh: ClawLess：一种 AI 智能体安全模型
authors: "Hongyi Lu, Nian Liu, Shuai Wang, Fengwei Zhang"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06284v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 具有形式化验证策略的自主AI智能体安全框架
tldr: 针对大语言模型驱动的自主智能体在信息检索和代码执行中存在的安全风险，本文提出了 ClawLess 安全框架。该框架在假设智能体可能具有对抗性的最坏情况下，通过形式化验证的细粒度安全策略（涵盖实体、信任范围和权限）来约束其行为。利用用户态内核和基于 BPF 的系统调用拦截技术，ClawLess 实现了动态策略的强制执行，为智能体提供了超越传统训练或提示词约束的底层安全保障。
source: arxiv
selection_source: fresh_fetch
motivation: 现有的通过训练或提示词规范 AI 智能体行为的方法缺乏根本性的安全保证，难以应对智能体自主执行任务带来的安全威胁。
method: 提出一种形式化验证的细粒度安全模型，通过用户态内核和 BPF 系统调用拦截技术，在运行时动态强制执行安全策略。
result: 成功构建了一个能够将形式化安全模型转化为具体执行规则的框架，确保了无论智能体内部设计如何，其行为均受到严格监管。
conclusion: ClawLess 为自主 AI 智能体提供了一种稳健的安全防御机制，通过底层系统级拦截实现了对潜在对抗性智能体的有效管控。
---

## 摘要
由大语言模型驱动的自主 AI 智能体能够进行推理、规划并执行复杂任务，但其自主检索信息和运行代码的能力带来了显著的安全风险。现有方法试图通过训练或提示词来规范智能体行为，但这无法提供根本性的安全保障。我们提出了 ClawLess，这是一个在最坏情况威胁模型下对 AI 智能体强制执行形式化验证策略的安全框架，在该模型中，智能体本身可能是对抗性的。ClawLess 针对系统实体、信任范围和权限形式化了一个细粒度的安全模型，用以表达能够适应智能体运行时行为的动态策略。这些策略被转化为具体的安全规则，并通过一个增强了基于 BPF 的系统调用拦截功能的用户空间内核来执行。该方法将形式化安全模型与实际执行相结合，无论智能体的内部设计如何，都能确保安全性。

## Abstract
Autonomous AI agents powered by Large Language Models can reason, plan, and execute complex tasks, but their ability to autonomously retrieve information and run code introduces significant security risks. Existing approaches attempt to regulate agent behavior through training or prompting, which does not offer fundamental security guarantees. We present ClawLess, a security framework that enforces formally verified policies on AI agents under a worst-case threat model where the agent itself may be adversarial. ClawLess formalizes a fine-grained security model over system entities, trust scopes, and permissions to express dynamic policies that adapt to agents' runtime behavior. These policies are translated into concrete security rules and enforced through a user-space kernel augmented with BPF-based syscall interception. This approach bridges the formal security model with practical enforcement, ensuring security regardless of the agent's internal design.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **ClawLess** 的安全框架，旨在为大语言模型（LLM）驱动的自主 AI 智能体提供底层的安全保障。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：自主 AI 智能体（如 OpenClaw, Claude Code）具备推理、规划和执行代码的能力，但这种高度自治性带来了严重的安全性挑战。
*   **研究动机**：
    *   **信任边界模糊**：智能体从多样化且不可信的来源检索数据，容易受到间接提示词注入（Indirect Prompt Injection）等攻击。
    *   **权限与可用性的权衡**：传统“最小权限原则”难以应用于行为不可预测的智能体。
    *   **现有防御失效**：基于训练或提示词（Prompting）的防御缺乏根本性保证；传统的沙箱（如 Docker）依赖单体内核，存在逃逸风险。
*   **整体含义**：论文提出在“最坏情况”威胁模型下（即假设智能体本身可能已被恶意控制），通过形式化验证的策略和底层系统拦截来强制约束智能体行为。

### 2. 论文提出的方法论
ClawLess 的核心思想是将**形式化安全模型**与**运行时动态监控**相结合。
*   **形式化安全模型**：
    *   **实体（Entities）**：定义系统中的文件、进程、套接字、设备等。
    *   **范围（Scopes）**：定义三个隔离层级：沙箱（外部脚本）、智能体（Agent 本身）、监控器（Monitor）。
    *   **权限（Permissions）**：包括 Read, Write, NoExecute, Append, Visible 等。特别提出了 `Visible` 权限，允许智能体引用凭据（如 API Key）而无法读取其具体内容。
    *   **线性时序逻辑（LTL）**：引入时间维度（如 $\square$ 总是, $\diamond$ 有时），实现动态策略。例如：*“如果智能体读取了敏感文件，则未来禁止其开启对外 Socket”*。
*   **关键技术细节**：
    *   **策略编译器**：将抽象的形式化规则映射为具体的 Linux 系统调用（Syscalls）。
    *   **用户态内核（User-space Kernel）**：采用类似 gVisor 的架构，在应用和宿主内核之间建立信任层。
    *   **BPF 系统调用拦截**：利用 eBPF（raw_tracepoint）在内核态高效拦截并验证系统调用，支持动态更新策略而无需重启。
    *   **SMT 验证**：使用 Z3 求解器验证安全策略的一致性，防止配置冲突。

### 3. 实验设计
*   **场景与对比**：
    *   论文主要通过**架构对比**和**策略验证**来展示其优越性。
    *   对比了四种容器技术：标准容器（Docker）、用户态内核（gVisor）、虚拟化（Kata）和机密容器（CoCo）。
    *   分析了不同技术在硬件独立性、内核兼容性、互操作性、部署难度和安全性方面的表现。
*   **Benchmark**：论文未提及使用特定的 AI 性能数据集，而是侧重于系统安全属性的定性分析和 CVE 漏洞历史数据的对比（如 Docker 过去十年的 CVE 数量）。

### 4. 资源与算力
*   **算力说明**：文内**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **原因分析**：由于 ClawLess 是一个系统级安全框架，其核心在于系统调用拦截和形式化逻辑验证，而非 LLM 模型本身的训练，因此算力消耗主要集中在运行时监控的 CPU 开销和 SMT 求解器的计算上。

### 5. 实验数量与充分性
*   **实验规模**：论文侧重于**理论证明和系统架构设计**。它展示了如何将形式化模型转化为 BPF 处理程序（如 `on_read` 示例代码），并给出了权限映射表。
*   **充分性评价**：
    *   从**形式化验证**角度看，逻辑严密，涵盖了实体、权限和时序逻辑。
    *   从**工程实现**角度看，提供了关键的 BPF 实现逻辑。
    *   **不足之处**：缺乏大规模的性能开销测试（如拦截系统调用带来的延迟）和针对真实恶意智能体攻击的实测防御成功率数据。

### 6. 论文的主要结论与发现
*   **结论**：AI 智能体安全不能依赖于智能体的“自我约束”，必须由外部系统强制执行。
*   **发现**：
    *   用户态内核在可用性和安全性之间达到了最佳平衡，适合作为智能体的运行环境。
    *   通过 LTL 能够有效解决静态权限模型过于死板的问题，在保证安全的同时提升了智能体的可用性。
    *   `Visible` 权限是处理智能体凭据泄露风险的有效创新。

### 7. 优点
*   **防御深度**：不依赖于 LLM 的内部设计，即使模型被完全“洗脑”或存在 0-day 漏洞，底层系统拦截依然有效。
*   **动态性**：利用 LTL 处理非确定性行为，比传统的静态 ACL 更灵活。
*   **形式化保证**：通过 SMT 求解器确保了安全策略在逻辑上是无冲突且完备的。

### 8. 不足与局限
*   **性能开销**：用户态内核和频繁的 BPF 拦截可能会带来显著的 I/O 和计算延迟，文中未给出量化数据。
*   **信任根依赖**：框架假设用户态内核（如 gVisor）和硬件是绝对可信的，但实际中这些组件也可能存在漏洞。
*   **范围限制**：明确排除了拒绝服务攻击（DoS）、微架构攻击（如 Spectre/Meltdown）以及智能体软件栈本身的漏洞。
*   **应用复杂性**：编写和维护复杂的形式化策略对普通开发者来说门槛较高。

（完）
