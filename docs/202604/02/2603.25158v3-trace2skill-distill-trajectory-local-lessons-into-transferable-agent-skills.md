---
title: "Trace2Skill: Distill Trajectory-Local Lessons into Transferable Agent Skills"
title_zh: Trace2Skill：将轨迹局部经验提炼为可迁移的智能体技能
authors: "Jingwei Ni, Yihao Liu, Xinpeng Liu, Yutao Sun, Mengyu Zhou, Pengyu Cheng, Dexin Wang, Erchao Zhao, Xiaoxi Jiang, Guanjun Jiang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25158v3"
tags: ["query:agent"]
score: 9.0
evidence: 为大模型智能体配备领域特定技能
tldr: Trace2Skill是一个旨在提升大语言模型（LLM）智能体领域技能的框架。针对手动编写技能效率低和自动生成技能易过拟合的问题，该框架模仿人类专家，通过并行分析多样化的执行轨迹，提取局部经验并将其分层整合为统一、无冲突的技能库。实验证明，该方法在电子表格、视觉问答和数学推理等任务中表现优异，且生成的技能具有极强的跨模型迁移能力和泛化性，无需参数微调。
selection_source: fresh_fetch
motivation: 现有的自动技能生成方法往往依赖浅层知识或过度拟合局部轨迹，导致生成的技能碎片化且缺乏泛化性。
method: 采用并行子智能体分析多样化执行轨迹，通过归纳推理将提取的局部经验分层整合为统一、声明式的技能目录。
result: 在电子表格、VisionQA和数学推理等领域显著超越强基线，且小模型生成的技能能大幅提升大模型的性能。
conclusion: 复杂的智能体经验可以被封装为高度可迁移、无需参数更新的声明式技能，为提升LLM智能体能力提供了高效路径。
---

## 摘要
为大语言模型（LLM）智能体配备领域特定技能对于处理复杂任务至关重要。然而，手动编写技能造成了严重的扩展性瓶颈。相反，自动技能生成往往产生脆弱或碎片化的结果，因为它要么依赖于浅层的参数化知识，要么顺序地过拟合于不可泛化的轨迹局部经验。为克服这一问题，我们推出了 Trace2Skill，该框架模拟了人类专家编写技能的方式：通过全面分析广泛的执行经验，将其提炼为一份单一且综合的指南。Trace2Skill 并非顺序地对单个轨迹做出反应，而是派遣一组并行的子智能体来分析多样化的执行池。它提取特定于轨迹的经验，并通过归纳推理将其分层整合进一个统一、无冲突的技能目录中。Trace2Skill 既支持深化现有的人工编写技能，也支持从零开始创建新技能。在电子表格、视觉问答（VisionQA）和数学推理等具有挑战性的领域进行的实验表明，Trace2Skill 相比于包括 Anthropic 官方 xlsx 技能在内的强基准模型有显著提升。至关重要的是，这种基于轨迹的演化不仅是记忆任务实例或模型特定的怪癖：演化出的技能可以跨 LLM 规模迁移，并能泛化到分布外（OOD）设置。例如，由 Qwen3.5-35B 在其自身轨迹上演化出的技能，在 WikiTableQuestions 任务上将 Qwen3.5-122B 智能体的表现提升了高达 57.65 个绝对百分点。最终，我们的结果表明，复杂的智能体经验可以被打包成高度可迁移的声明式技能——无需参数更新，无需外部检索模块，且可以使用参数量小至 35B 的开源模型。

## Abstract
Equipping Large Language Model (LLM) agents with domain-specific skills is critical for tackling complex tasks. Yet, manual authoring creates a severe scalability bottleneck. Conversely, automated skill generation often yields fragile or fragmented results because it either relies on shallow parametric knowledge or sequentially overfits to non-generalizable trajectory-local lessons. To overcome this, we introduce Trace2Skill, a framework that mirrors how human experts author skills: by holistically analyzing broad execution experience before distilling it into a single, comprehensive guide. Instead of reacting sequentially to individual trajectories, Trace2Skill dispatches a parallel fleet of sub-agents to analyze a diverse pool of executions. It extracts trajectory-specific lessons and hierarchically consolidates them into a unified, conflict-free skill directory via inductive reasoning. Trace2Skill supports both deepening existing human-written skills and creating new ones from scratch. Experiments in challenging domains, such as spreadsheet, VisionQA and math reasoning, show that Trace2Skill significantly improves upon strong baselines, including Anthropic's official xlsx skills. Crucially, this trajectory-grounded evolution does not merely memorize task instances or model-specific quirks: evolved skills transfer across LLM scales and generalize to OOD settings. For example, skills evolved by Qwen3.5-35B on its own trajectories improved a Qwen3.5-122B agent by up to 57.65 absolute percentage points on WikiTableQuestions. Ultimately, our results demonstrate that complex agent experience can be packaged into highly transferable, declarative skills -- requiring no parameter updates, no external retrieval modules, and utilizing open-source models as small as 35B parameters.

---

## 论文详细总结（自动生成）

以下是对论文《Trace2Skill: Distill Trajectory-Local Lessons into Transferable Agent Skills》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：为大语言模型（LLM）智能体配备领域特定技能（Skills）是解决复杂任务的关键。然而，手动编写技能难以扩展，而现有的自动生成方法通常存在两个缺陷：一是依赖模型自身的参数化知识（缺乏领域细节）；二是采用顺序更新模式（Sequential Updates），容易导致技能库碎片化、过拟合局部轨迹且难以检索。
*   **核心目标**：模拟人类专家编写技能的过程——通过全面分析广泛的执行经验，将其提炼为一份单一、综合、可迁移的声明式（Declarative）指南，从而解决技能的扩展性、泛化性和迁移性问题。

### 2. 方法论
Trace2Skill 框架包含三个阶段，核心思想是“并行分析”与“层次化归纳”：
*   **阶段 1：轨迹生成（Trajectory Generation）**：让固定参数的智能体在任务集上运行，收集成功（$T^+$）和失败（$T^-$）的执行轨迹池。
*   **阶段 2：并行多智能体补丁提议（Parallel Multi-Agent Patch Proposal）**：
    *   派遣大量子智能体并行处理轨迹。
    *   **成功分析师（$A^+$）**：识别贡献成功的通用模式。
    *   **错误分析师（$A^-$）**：采用“智能体循环”（Agentic Loop），通过检查文件、对比真值、尝试修复来定位根因，确保补丁的准确性。
    *   每个分析师独立提出技能补丁（Patches），互不干扰，保持多样性。
*   **阶段 3：无冲突补丁整合（Conflict-Free Consolidation）**：
    *   **层次化合并**：将成百上千的补丁通过 LLM 进行层次化归纳推理。
    *   **归纳推理**：优先保留在多个独立轨迹中重复出现的模式（视为通用规律），剔除偶然的、特定于实例的修改。
    *   **程序化校验**：通过格式检查和冲突检测，确保最终生成的技能目录（包含 Markdown 文档和辅助脚本）逻辑一致且可直接执行。

### 3. 实验设计
*   **实验场景与数据集**：
    *   **电子表格领域**：SpreadsheetBench（包含验证集、Soft/Hard 评分）。
    *   **数学推理**：DAPO-Math、AIME 2026。
    *   **视觉问答（VQA）**：DocVQA（处理文档图像、表格等）。
*   **Benchmark 与对比方法**：
    *   **基准**：No Skill（无技能）、Human-Written（Anthropic 官方专家编写技能）、Parametric（LLM 仅凭自身知识生成的草稿）。
    *   **对比范式**：ReasoningBank（基于检索的经验库）、顺序编辑（Sequential Editing）。
*   **跨模型验证**：测试了 Qwen3.5-35B 和 Qwen3.5-122B 之间的技能迁移性（例如 35B 编写的技能给 122B 使用）。
*   **分布外（OOD）测试**：在电子表格技能基础上，直接测试其在 WikiTableQuestions 上的表现。

### 4. 资源与算力
*   **算力消耗**：
    *   **轨迹生成**：使用 122B 模型生成 200 条长轨迹（50+ 轮次）耗时少于 2 GPU-hours。
    *   **技能演化**：在 8 张 A800 GPU 节点上，并行整合流程约需 **3 分钟**，而传统的顺序更新（Seq-B=1）则需 **60 分钟**。
*   **模型使用**：主要基于开源的 Qwen3.5-35B-A3B 和 Qwen3.5-122B-A10B 混合专家模型（MoE）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三个迥异的领域（表格、数学、多模态），并针对每个领域进行了 ID（分布内）和 OOD（分布外）评估。
*   **消融实验**：非常充分。对比了成功/错误分析师的贡献、Agentic 循环与单次 LLM 调用的差异、并行与顺序合并的效果、以及技能文档与检索式存储的优劣。
*   **客观性**：所有实验均采用三组随机种子取平均值，并使用了各 Benchmark 的官方评估指标，确保了对比的公平性。

### 6. 主要结论与发现
*   **卓越的迁移性**：小模型（35B）演化出的技能可以显著提升大模型（122B）的性能。在 WikiTQ 任务中，35B 演化的技能让 122B 提升了 **57.65%**。
*   **超越人类专家**：在某些设定下，自动演化的技能优于 Anthropic 官方编写的专家技能。
*   **并行优于顺序**：并行整合不仅效率提升 20 倍，且通过全局归纳有效避免了顺序更新中的“上下文漂移”和过拟合问题。
*   **能力解耦**：发现模型的“执行能力”与“归纳分析能力”并不完全对等（如 35B 在 DocVQA 执行上很强，但作为技能作者的表现较弱）。

### 7. 优点
*   **架构优雅**：将复杂的经验转化为简单的声明式 Markdown 目录，无需参数微调，无需外部检索模块，极易部署。
*   **鲁棒性高**：通过层次化合并提取“普遍规律”，使技能具有极强的泛化能力，能应对未见过的任务分布。
*   **自包含性**：整个流程（生成、分析、整合）均可由同一个开源模型完成，摆脱了对闭源商业模型的依赖。

### 8. 不足与局限
*   **因果归因困难**：目前采用整体整合，难以精确量化单个补丁对最终性能提升的边际贡献。
*   **动态修剪缺失**：生成的技能目录可能包含冗余信息，目前尚无机制在推理时动态追踪并修剪无效的技能片段。
*   **模型门槛**：虽然 35B 模型表现良好，但在极高难度的归纳任务中，较小模型的逻辑反思能力仍可能限制技能的上限。

（完）
