---
title: "KG-CMI: Knowledge graph enhanced cross-Mamba interaction for medical visual question answering"
title_zh: KG-CMI：用于医疗视觉问答的知识图谱增强型跨 Mamba 交互框架
authors: "Xianyao Zheng, Hong Yu, Hui Cui, Changming Sun, Xiangyu Li, Ran Su, Leyi Wei, Jia Zhou, Junbo Wang, Qiangguo Jin"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00601v1"
tags: ["query:mmkqa"]
score: 9.0
evidence: 知识图谱增强的医疗视觉问答跨模态交互
tldr: 针对医疗视觉问答（Med-VQA）中领域知识利用不足及分类方法难以处理自由回答的问题，本文提出KG-CMI框架。该框架通过知识图谱增强跨模态Mamba交互，利用细粒度对齐、知识嵌入和多任务学习模块，有效整合专业医学知识并提升对开放式问题的处理能力，在多个主流数据集上达到领先水平。
selection_source: fresh_fetch
motivation: 现有Med-VQA方法难以充分利用专业医学知识，且受限于预定义答案集，无法有效处理多样化的自由形式回答。
method: 提出KG-CMI框架，包含细粒度特征对齐、知识图谱嵌入、跨模态交互表示及自由回答增强的多任务学习模块。
result: 在VQA-RAD、SLAKE和OVQA三个权威数据集上的实验结果表明，该方法性能优于现有的最先进模型。
conclusion: KG-CMI通过整合医学知识图谱和改进跨模态交互，显著提升了医疗视觉问答的准确性和对开放式问题的适应性。
---

## 摘要
医疗视觉问答（Med-VQA）是临床决策支持和远程医疗中一项至关重要的多模态任务。最近的方法未能充分利用特定领域的医学知识，导致难以将医学图像中的病灶特征与关键诊断标准准确关联。此外，基于分类的方法通常依赖于预定义的答案集。将 Med-VQA 视为简单的分类问题限制了其适应多样化自由形式答案的能力，并可能忽略这些答案中的详细语义信息。为了应对这些挑战，我们提出了一个知识图谱增强的跨 Mamba 交互（KG-CMI）框架，该框架由细粒度跨模态特征对齐（FCFA）模块、知识图谱嵌入（KGE）模块、跨模态交互表示（CMIR）模块以及自由形式答案增强的多任务学习（FAMT）模块组成。KG-CMI 通过图结构有效地整合专业医学知识，学习图像和文本的跨模态特征表示，建立了病灶特征与疾病知识之间的关联。此外，FAMT 利用来自开放式问题的辅助知识，提升了模型处理开放式 Med-VQA 的能力。实验结果表明，KG-CMI 在三个 Med-VQA 数据集（即 VQA-RAD、SLAKE 和 OVQA）上优于现有的最先进方法。此外，我们还进行了可解释性实验，以进一步验证该框架的有效性。

## Abstract
Medical visual question answering (Med-VQA) is a crucial multimodal task in clinical decision support and telemedicine. Recent methods fail to fully leverage domain-specific medical knowledge, making it difficult to accurately associate lesion features in medical images with key diagnostic criteria. Additionally, classification-based approaches typically rely on predefined answer sets. Treating Med-VQA as a simple classification problem limits its ability to adapt to the diversity of free-form answers and may overlook detailed semantic information in those answers. To address these challenges, we propose a knowledge graph enhanced cross-Mamba interaction (KG-CMI) framework, which consists of a fine-grained cross-modal feature alignment (FCFA) module, a knowledge graph embedding (KGE) module, a cross-modal interaction representation (CMIR) module, and a free-form answer enhanced multi-task learning (FAMT) module. The KG-CMI learns cross-modal feature representations for images and texts by effectively integrating professional medical knowledge through a graph, establishing associations between lesion features and disease knowledge. Moreover, FAMT leverages auxiliary knowledge from open-ended questions, improving the model's capability for open-ended Med-VQA. Experimental results demonstrate that KG-CMI outperforms existing state-of-the-art methods on three Med-VQA datasets, i.e., VQA-RAD, SLAKE, and OVQA. Additionally, we conduct interpretability experiments to further validate the framework's effectiveness.

---

## 论文详细总结（自动生成）

这是一份关于论文《KG-CMI: Knowledge graph enhanced cross-Mamba interaction for medical visual question answering》的结构化分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
医疗视觉问答（Med-VQA）旨在根据医学图像回答临床问题，对临床决策支持和远程医疗至关重要。
*   **核心挑战**：
    1.  **语义关联难**：难以将医学图像中细微的病灶特征与复杂的医学诊断标准准确关联。
    2.  **领域知识缺失**：现有模型缺乏对专业医学知识（如解剖学、疾病诊断标准）的深度整合。
    3.  **答案形式受限**：传统方法多将其视为闭集分类问题，难以处理多样化的自由形式（开放式）回答，且容易忽略答案中的深层语义。
*   **研究目标**：提出一个能够整合结构化医学知识、实现高效跨模态交互并增强开放式回答能力的统一框架。

### 2. 方法论：核心思想与关键技术
论文提出了 **KG-CMI** 框架，由四个核心模块组成：
*   **细粒度跨模态特征对齐 (FCFA)**：
    *   使用 ViT 提取图像特征，RoBERTa 提取文本特征。
    *   引入 **QQ-Former (Question-aware Q-Former)**，通过自注意力、交叉注意力和双向对比学习（CMCL），强化图像区域与文本 Token 之间的语义一致性。
*   **知识图谱嵌入 (KGE)**：
    *   利用预构建的医学知识图谱（包含器官、疾病及其关系）。
    *   通过 **图注意力网络 (GAT)** 聚合结构化知识，并利用交叉注意力机制根据问题动态检索相关的医学先验知识。
*   **跨模态交互表示 (CMIR)**：
    *   核心是 **跨 Mamba (CMM) 块**。相比 Transformer 的平方复杂度，CMM 利用 Mamba 的选择性扫描机制实现线性复杂度的跨模态建模。
    *   通过交错融合视觉、文本和知识图谱特征，捕捉深层跨模态相关性。
*   **自由形式答案增强的多任务学习 (FAMT)**：
    *   包含一个**分类头**（处理闭集预测）和一个**辅助生成头 (AHead)**。
    *   辅助头采用 T5 解码器，在训练阶段通过生成自由形式答案来强化模型对复杂语义的理解，提升开放式问题的表现。

### 3. 实验设计
*   **数据集**：
    1.  **SLAKE**：支持中英双语，涵盖多种器官。
    2.  **VQA-RAD**：由医学专家标注的放射学图像数据集。
    3.  **OVQA**：专注于骨科领域的临床生成数据集。
*   **对比方法**：
    *   **预训练模型**：M2I2, MUMC, M3AE, UniCLAM。
    *   **知识增强方法**：MEVF, MPR, LaPA。
    *   **架构优化方法**：MMQ, MQAT, VQAMix, VG-CALF, CCIS-MVQA。
*   **评价指标**：准确率 (ACC)，针对生成任务使用了 BLEU, METEOR, CIDEr。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA GeForce RTX 4090** GPU。
*   **软件**：PyTorch 框架。
*   **训练细节**：使用 AdamW 优化器，初始学习率 $5 \times 10^{-6}$，训练 50 个 epoch，batch size 为 8。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流基准数据集上进行了全面测试。
*   **消融实验**：非常详尽，分别验证了 QQ-Former、对比学习 (CMCL)、知识图谱 (KGE)、跨 Mamba (CMM) 和辅助头 (AHead) 的有效性。
*   **参数敏感性分析**：对损失函数权重 ($\alpha, \beta$)、交互权重 ($\eta, \theta$)、QQ-Former 层数、CMM 块数量以及 GAT 层数均做了对比实验。
*   **客观性**：所有实验均运行 5 次并取平均值及标准差，进行了双尾 t 检验以确保结果的统计显著性（p < 0.05）。

### 6. 主要结论与发现
*   **性能领先**：KG-CMI 在三个数据集上均达到了 SOTA 水平。特别是在 **OVQA 开放式问题**上，比之前的最佳模型 M3AE 提升了 **10.12%**。
*   **知识图谱的作用**：引入结构化医学知识显著提升了模型在复杂推理任务中的表现。
*   **Mamba 的优势**：CMM 模块在保持较低计算量（FLOPs 降低约 24%）的同时，比传统的 Transformer 交叉注意力机制表现更好。
*   **多任务学习的增益**：通过辅助生成任务训练，模型在分类任务中的鲁棒性得到了显著增强。

### 7. 优点（亮点）
*   **高效交互**：首次在 Med-VQA 中引入跨 Mamba 交互，解决了长序列建模的计算瓶颈。
*   **深度融合**：不仅对齐了图文特征，还动态引入了外部医学知识图谱，弥补了数据驱动模型的常识缺失。
*   **可解释性强**：通过 Grad-CAM 可视化证明模型能准确聚焦于病灶区域（如骨折点），符合临床诊断逻辑。
*   **多任务协同**：巧妙地利用生成式任务作为辅助，提升了判别式任务（分类）的性能。

### 8. 不足与局限
*   **细微结构混淆**：在处理极其微小的解剖结构或复杂的空间位置关系时（如 Case 4），模型仍可能出现“位置混淆”或“存在性判断错误”。
*   **知识图谱依赖**：模型的推理能力高度依赖于预构建知识图谱的质量和覆盖范围，对于图谱之外的罕见病例可能表现受限。
*   **推理延迟**：虽然 Mamba 具有线性复杂度，但框架包含多个复杂模块（如 T5 解码器辅助训练），虽然推理时去掉了辅助头，但整体架构仍较重。

（完）
