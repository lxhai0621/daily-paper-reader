---
title: A Multi-agent AI System for Deep Learning Model Migration from TensorFlow to JAX
title_zh: 一种用于将深度学习模型从 TensorFlow 迁移到 JAX 的多智能体 AI 系统
authors: "Stoyan Nikolov, Bernhard Konrad, Moritz Gronbach, Niket Kumar, Ann Yan, Varun Singh, Yaning Liang, Parthasarathy Ranganathan"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27296v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 用于自动代码迁移的多智能体 AI 系统
tldr: 本研究针对深度学习模型从 TensorFlow 迁移到 JAX 的高成本手动维护难题，开发了一个基于多智能体的 AI 系统。该系统结合静态分析与 AI 指令生成迁移计划，利用编排器、编码器及示例手册实现自动化迁移，并引入 AI 评审机制确保代码质量。实验证明，该系统在真实商业场景中将迁移效率提升了 6.4 至 8 倍，为框架迁移和代码转换提供了高效的自动化方案。
selection_source: fresh_fetch
motivation: 随着深度学习框架的快速迭代，手动维护和跨框架迁移大量模型源代码已成为一项极具挑战且耗时的工作。
method: 提出一种多智能体系统，通过 AI 规划器结合静态分析生成计划，并利用编排器、编码器及 AI 评审员协同完成代码转换与质量校验。
result: 在大型超大规模计算环境的真实案例中，该系统实现了 6.4 倍到 8 倍的迁移速度提升。
conclusion: 该 AI 驱动的自动化迁移方法不仅显著提高了开发效率，且其技术路径可推广至其他框架迁移及通用代码转换任务。
---

## 摘要
基于 AI 的产品及其底层模型的快速发展，推动了深度学习框架的不断创新。Google 一直在数十种产品中率先应用机器学习。维护不同机器学习框架和版本中的大量模型源代码是一项重大挑战。到目前为止，维护和迁移工作主要由人类专家手动完成。我们描述了一个构建的基于 AI 的多智能体系统，用于支持将基于 TensorFlow 的深度学习模型自动迁移为基于 JAX 的模型。我们做出了三个主要贡献：首先，我们展示了结合静态分析与 AI 指令的 AI 规划器如何为极其复杂的代码组件创建迁移计划，并由编排器和编码器组合，利用 AI 生成的基于示例的剧本可靠地执行。其次，我们定义了质量指标和基于 AI 的评判器，在待评估代码缺乏测试且必须遵守严格风格和依赖要求的情况下加速开发。第三，我们证明了该系统如何在大型超大规模计算环境下的商业真实用例中加速代码迁移。我们的方法显著缩短了深度学习模型的迁移时间（加速 6.4 到 8 倍），并创造了一个 AI 有效支持其自身开发工作流的良性循环。我们预期本文描述的技术和方法可以推广到其他框架迁移和通用的代码转换任务中。

## Abstract
The rapid development of AI-based products and their underlying models has led to constant innovation in deep learning frameworks. Google has been pioneering machine learning usage across dozens of products. Maintaining the multitude of model source codes in different ML frameworks and versions is a significant challenge. So far the maintenance and migration work was done largely manually by human experts. We describe an AI-based multi-agent system that we built to support automatic migration of TensorFlow-based deep learning models into JAX-based ones. We make three main contributions: First, we show how an AI planner that uses a mix of static analysis with AI instructions can create migration plans for very complex code components that are reliably followed by the combination of an orchestrator and coders, using AI-generated example-based playbooks. Second, we define quality metrics and AI-based judges that accelerate development when the code to evaluate has no tests and has to adhere to strict style and dependency requirements. Third, we demonstrate how the system accelerates code migrations in a large hyperscaler environment on commercial real-world use-cases. Our approach dramatically reduces the time (6.4x-8x speedup) for deep learning model migrations and creates a virtuous circle where effectively AI supports its own development workflow. We expect that the techniques and approaches described here can be generalized for other framework migrations and general code transformation tasks.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个由 Google 开发的多智能体 AI 系统，旨在解决将大规模深度学习模型从 TensorFlow (TF) 迁移到 JAX/Flax 的复杂工程难题。

### 1. 论文的核心问题与整体含义
*   **研究动机**：随着 AI 技术的更迭，Google 内部正从传统的 TensorFlow 框架转向更适合 TPU 训练和推理的 JAX 框架。然而，由于两者在架构（有状态 vs 无状态）、执行范式（静态图 vs 函数式编程）上存在根本差异，手动迁移数千个生产级模型需要耗费数百人年的专家工作量。
*   **核心问题**：通用的 AI 编程助手（如 Gemini CLI、Claude Code）在处理跨多个文件、具有复杂依赖关系且需遵循严格内部规范的大型代码库迁移时，往往会出现幻觉、风格不统一或无法生成可构建代码的问题。

### 2. 论文提出的方法论
系统采用了**多智能体协作架构**，通过分工明确的角色来降低单个任务的复杂度：
*   **剧本（Playbooks）体系**：构建了分层级的 Markdown 指导文档，包括通用指令、代码风格、任务特定（TF 到 JAX 的 API 映射）以及客户特定（基于“黄金示例”自动生成的团队规范）剧本。
*   **规划器智能体（Planner Agent）**：结合静态分析工具（Kythe）递归发现代码依赖，生成包含多个离散步骤的 JSON 格式迁移计划。它负责确定哪些文件需要修改，并确保迁移顺序从“叶子节点”开始。
*   **编排器智能体（Orchestrator Agent）**：作为“大脑”，将大计划拆分为可管理的子任务块，动态注入相关剧本，并根据编码器的反馈处理失败、重试或跳过逻辑。
*   **编码器智能体（Coder Agent）**：基于 ReAct 模式，拥有读写文件、搜索代码、运行构建和单元测试的工具权限。它在每个步骤后生成摘要存入“存储库（Memory Bank）”，以维持长程上下文。
*   **质量评估（LLM Judge）**：引入基于框架无关清单（Checklist）的 LLM 评审机制，通过“盲审”迁移后的代码是否满足功能需求（如：是否包含特定的损失函数、层结构是否正确），而非简单的语法对比。

### 3. 实验设计
*   **数据集/场景**：
    *   **开源模型**：32 个开源模型用于初期系统调试和回归测试。
    *   **Google 内部模型**：选取了 6 个具有代表性的 YouTube 生产级模型，涵盖小型（约 500 行）、中型（约 2k 行）到大型（约 3k 行主代码 + 13k 行依赖，包含数百个指标和层）。
*   **对比方法（Baseline）**：
    1.  `single_agent_baseline`：无特定适配的单个编码智能体。
    2.  `single_agent_yt_specific`：带有剧本但无规划/编排的单个智能体。
    3.  `multi_agent`：有规划/编排但无客户特定剧本。
    4.  `multi_agent_yt_specific`：全功能多智能体系统。
*   **评估指标**：清单得分（Checklist Score，由 Gemini 3 Pro 评分）和人类专家迁移的时间节省率。

### 4. 资源与算力
*   **模型使用**：系统核心使用了 **Gemini 3 Pro** 模型（温度设定为 0.2）。
*   **算力细节**：文中**未明确说明**运行这些智能体所消耗的具体 GPU/TPU 数量或总推理时长。但提到多智能体配置的计算成本比单智能体高出约 3-5 倍。

### 5. 实验数量与充分性
*   **实验规模**：针对 6 个核心测试模型，每种配置运行了超过 10 次（N > 10），以减少随机性影响。
*   **充分性评价**：实验设计较为充分，包含了消融实验（验证剧本和多智能体架构的各自贡献）。虽然测试模型数量（6个）看似不多，但这些模型代表了极高复杂度的真实工业场景，且通过多次运行证明了统计学上的显著性（p < 0.02）。

### 6. 论文的主要结论与发现
*   **性能提升**：全功能多智能体系统在所有复杂度模型上均表现最优。
*   **效率飞跃**：相比人类专家手动迁移，该系统实现了 **6.4 倍到 8 倍的加速**。
*   **复杂度相关性**：模型越复杂，多智能体系统的优势越明显。对于简单模型，单智能体与多智能体差距不大；但对于大型模型，单智能体往往无法完成任务。
*   **知识蒸馏价值**：仅需 2 个“黄金示例”生成的客户特定剧本，就能显著提升迁移质量。

### 7. 优点
*   **混合驱动规划**：结合了确定性的静态分析（Kythe）和灵活的 AI 推理，解决了纯 AI 难以处理的大规模依赖发现问题。
*   **闭环验证**：编码器具备运行构建和测试的能力，确保生成的代码不仅“看起来对”，而且“能跑通”。
*   **自动化剧本生成**：提出了一种从现有迁移案例中自动提取规则的方法，形成了 AI 辅助 AI 开发的良性循环。

### 8. 不足与局限
*   **调试成本**：虽然代码迁移快，但由于 JAX 的无状态特性，迁移后可能存在细微的数值差异或训练不稳定，这部分“调试税”仍需人类专家介入。
*   **评估局限**：LLM Judge 虽然高效，但不能完全替代数值等效性测试（Numerical Parity），对于极高精度的模型可能存在漏检。
*   **环境依赖**：系统深度集成在 Google 内部工具链（如 Google3 仓库、Kythe 索引）中，外部开发者难以直接复现整套流程。

（完）
