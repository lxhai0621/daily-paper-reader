---
title: "Trace2Skill: Distill Trajectory-Local Lessons into Transferable Agent Skills"
title_zh: Trace2Skill：将轨迹局部经验提炼为可迁移的智能体技能
authors: "Jingwei Ni, Yihao Liu, Xinpeng Liu, Yutao Sun, Mengyu Zhou, Pengyu Cheng, Dexin Wang, Xiaoxi Jiang, Guanjun Jiang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25158v2"
tags: ["query:agent"]
score: 9.0
evidence: LLM智能体的自动化技能生成
tldr: Trace2Skill是一个旨在提升大语言模型智能体领域技能的框架。针对手动编写技能效率低和自动生成技能易碎片化的问题，该框架模仿人类专家，通过并行分析多样化的执行轨迹，提取局部经验并归纳整合为统一、无冲突的技能库。实验证明，该方法在电子表格、视觉问答和数学推理等任务中显著提升了性能，且生成的技能具有极强的跨模型和跨领域迁移能力，无需参数更新即可实现显著的性能跨越。
selection_source: fresh_fetch
motivation: 现有的智能体技能生成方法要么依赖浅层知识，要么过度拟合局部轨迹，导致生成的技能脆弱且难以泛化。
method: 采用并行子智能体分析多样化执行轨迹，通过归纳推理将提取的局部经验层级化地整合为统一的声明式技能目录。
result: 在多个复杂领域显著优于强基准模型，且由较小模型生成的技能能大幅提升更大模型在分布外任务上的表现。
conclusion: 复杂的智能体经验可以被封装为高度可迁移的声明式技能，无需参数更新或外部检索即可实现显著的性能提升。
---

## 摘要
为大语言模型（LLM）智能体配备特定领域技能对于解决复杂任务至关重要。然而，手动编写技能造成了严重的扩展性瓶颈。相反，自动化的技能生成往往产生脆弱或碎片化的结果，因为其要么依赖于浅层的参数化知识，要么顺序地过拟合于不可泛化的轨迹局部经验。为克服这一问题，我们提出了 Trace2Skill，该框架模仿了人类专家编写技能的方式：通过全面分析广泛的执行经验，然后将其提炼为一份单一且详尽的指南。Trace2Skill 并非顺序地对单个轨迹做出反应，而是派遣一组并行的子智能体来分析多样化的执行池。它提取特定于轨迹的经验，并通过归纳推理将其分层整合进一个统一、无冲突的技能目录中。Trace2Skill 既支持深化现有的人工编写技能，也支持从零开始创建新技能。在电子表格、VisionQA 和数学推理等具有挑战性的领域进行的实验表明，Trace2Skill 显著优于强基准模型，包括 Anthropic 官方的 xlsx 技能。至关重要的是，这种基于轨迹的演化不仅仅是记忆任务实例或特定模型的特性：演化出的技能可以跨 LLM 规模迁移，并能泛化到分布外（OOD）设置。例如，由 Qwen3.5-35B 在其自身轨迹上演化出的技能，在 WikiTableQuestions 上将 Qwen3.5-122B 智能体的表现提升了高达 57.65 个绝对百分点。最终，我们的结果表明，复杂的智能体经验可以被打包成高度可迁移的声明式技能——无需参数更新，无需外部检索模块，且可以使用参数量小至 35B 的开源模型。

## Abstract
Equipping Large Language Model (LLM) agents with domain-specific skills is critical for tackling complex tasks. Yet, manual authoring creates a severe scalability bottleneck. Conversely, automated skill generation often yields fragile or fragmented results because it either relies on shallow parametric knowledge or sequentially overfits to non-generalizable trajectory-local lessons. To overcome this, we introduce Trace2Skill, a framework that mirrors how human experts author skills: by holistically analyzing broad execution experience before distilling it into a single, comprehensive guide. Instead of reacting sequentially to individual trajectories, Trace2Skill dispatches a parallel fleet of sub-agents to analyze a diverse pool of executions. It extracts trajectory-specific lessons and hierarchically consolidates them into a unified, conflict-free skill directory via inductive reasoning. Trace2Skill supports both deepening existing human-written skills and creating new ones from scratch. Experiments in challenging domains, such as spreadsheet, VisionQA and math reasoning, show that Trace2Skill significantly improves upon strong baselines, including Anthropic's official xlsx skills. Crucially, this trajectory-grounded evolution does not merely memorize task instances or model-specific quirks: evolved skills transfer across LLM scales and generalize to OOD settings. For example, skills evolved by Qwen3.5-35B on its own trajectories improved a Qwen3.5-122B agent by up to 57.65 absolute percentage points on WikiTableQuestions. Ultimately, our results demonstrate that complex agent experience can be packaged into highly transferable, declarative skills -- requiring no parameter updates, no external retrieval modules, and utilizing open-source models as small as 35B parameters.

---

## 论文详细总结（自动生成）

### Trace2Skill：将轨迹局部经验提炼为可迁移的智能体技能 —— 论文深度总结

#### 1. 核心问题与研究动机
*   **核心问题**：如何高效、自动地为大语言模型（LLM）智能体构建高质量、可泛化的领域特定技能（如标准作业程序 SOP）。
*   **研究背景**：
    *   **手动编写瓶颈**：人工撰写技能难以扩展，且针对特定模型或任务的优化成本极高。
    *   **现有自动方法缺陷**：
        *   基于参数化知识生成：缺乏领域细节和常见陷阱的深度理解。
        *   在线顺序更新：容易产生碎片化的技能库，导致检索困难，且容易过拟合于局部轨迹，缺乏全局视角。
*   **研究动机**：模仿人类专家“先广泛实践、后总结提炼”的模式，通过并行分析大量执行轨迹，将局部经验归纳为统一、无冲突且可迁移的声明式技能。

#### 2. 方法论：Trace2Skill 框架
Trace2Skill 包含三个主要阶段，旨在实现从经验到技能的整体式提炼：
*   **阶段 1：轨迹生成 (Trajectory Generation)**
    *   使用初始技能（人工编写或 LLM 草拟）运行智能体，收集成功（$T^+$）和失败（$T^-$）的执行轨迹。
*   **阶段 2：并行多智能体补丁提议 (Parallel Multi-Agent Patch Proposal)**
    *   派遣一组并行的子智能体（分析师）独立处理每条轨迹。
    *   **成功分析师 ($A^+$)**：识别贡献成功的通用模式。
    *   **错误分析师 ($A^-$)**：采用 **ReAct 风格的智能体循环**，通过检查文件、对比真值和验证修复方案来定位根本原因，确保补丁的因果准确性。
*   **阶段 3：无冲突补丁整合 (Conflict-Free Patch Consolidation)**
    *   **层级化合并**：采用对数级深度的层级结构，将数百个独立补丁逐步合并。
    *   **归纳推理**：通过识别在不同轨迹中反复出现的“普遍模式”来提取通用原则，丢弃偶然的、不可泛化的修改。
    *   **程序化守卫**：通过冲突检测和格式校验，确保最终生成的技能目录（SKILL.md 及辅助资源）逻辑一致且可直接运行。

#### 3. 实验设计
*   **实验场景与数据集**：
    *   **电子表格 (Spreadsheet)**：SpreadsheetBench-Verified（200 样本演化，200 样本测试）。
    *   **分布外 (OOD) 泛化**：WikiTableQuestions (WikiTQ)。
    *   **数学推理**：DAPO-Math, AIME 2026。
    *   **视觉问答 (VQA)**：DocVQA。
*   **对比方法与基准 (Benchmarks)**：
    *   **No Skill**：无技能输入。
    *   **Human-Written**：Anthropic 官方提供的 xlsx 专家技能。
    *   **Parametric**：仅靠 LLM 内部知识生成的草案。
    *   **ReasoningBank**：基于检索的经验库方法。
    *   **Sequential Editing**：在线顺序更新模式。
*   **跨模型验证**：测试 35B 模型生成的技能是否能提升 122B 模型的表现（反之亦然）。

#### 4. 资源与算力
*   **算力消耗**：
    *   **轨迹生成**：使用 122B 模型生成 200 条长轨迹（50+ 轮次）耗时小于 **2 GPU-hours**。
    *   **并行整合**：在 8 张 A800 GPU 节点上，Trace2Skill 的并行整合仅需 **~3 分钟**，而顺序更新模式（Seq-B=1）需 60 分钟，效率提升约 20 倍。
*   **模型使用**：主要基于开源的 Qwen3.5-35B-A3B 和 Qwen3.5-122B-A10B 混合专家模型（MoE）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了电子表格、数学、视觉三个迥异的领域，进行了数百次轨迹分析。
*   **充分性与客观性**：
    *   实验设计了严谨的**跨模型规模迁移**和**跨领域 OOD 测试**，证明了技能的通用性而非简单的记忆。
    *   进行了多项消融实验（如智能体化分析 vs. 单次 LLM 调用、并行 vs. 顺序、提炼 vs. 检索），对比维度全面。
    *   所有结果均基于 3 个随机种子的平均值，具有较高的统计可信度。

#### 6. 主要结论与发现
*   **显著提升**：Trace2Skill 在电子表格任务上显著超越了人工编写的强基准。
*   **极强的迁移性**：由 35B 小模型演化出的技能，在 WikiTQ 任务上能将 122B 大模型的表现提升 **57.65%**。这表明经验可以被提炼为与模型规模无关的通用知识。
*   **并行优于顺序**：整体式的并行整合不仅效率更高，且通过归纳推理有效避免了顺序更新中的“漂移”和过拟合问题。
*   **技能优于检索**：单一的声明式技能文档在性能和便携性上均优于需要外部检索模块的经验库（ReasoningBank）。
*   **分析深度至关重要**：带有验证循环的“智能体化错误分析”生成的补丁质量远高于单次 LLM 调用的结果。

#### 7. 优点与亮点
*   **方法论创新**：将技能演化视为一个“归纳推理”过程，而非简单的补丁堆叠。
*   **高效性**：充分利用并行计算，极大地缩短了技能迭代周期。
*   **无需微调**：完全在推理侧实现，无需更新模型参数，生成的技能是纯文本/脚本，易于分发和共享。
*   **开源友好**：证明了 35B 规模的开源模型即可胜任复杂的技能提炼任务，降低了技术门槛。

#### 8. 不足与局限
*   **因果量化缺失**：目前尚无法精确量化单个补丁对最终性能提升的边际贡献。
*   **动态剪枝不足**：缺乏在推理过程中动态追踪技能各部分利用率的机制，无法自动删除无效或干扰性的技能片段。
*   **能力鸿沟**：在 VQA 任务中发现，虽然 35B 模型执行任务不错，但作为“技能作者”的能力较弱，说明提炼技能需要比执行任务更高的反思能力。

（完）
