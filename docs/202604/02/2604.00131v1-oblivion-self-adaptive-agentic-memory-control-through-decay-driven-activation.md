---
title: "Oblivion: Self-Adaptive Agentic Memory Control through Decay-Driven Activation"
title_zh: Oblivion：通过衰减驱动激活实现的自适应智能体记忆控制
authors: "Ashish Rana, Chia-Chien Hung, Qumeng Sun, Julian Martin Kunkel, Carolin Lawrence"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.00131v1"
tags: ["query:agent"]
score: 10.0
evidence: 通过衰减驱动激活的自适应智能体记忆控制
tldr: 针对LLM智能体在长周期任务中因“始终开启”的检索和扁平化存储导致的干扰与高延迟问题，本文提出Oblivion框架。该框架借鉴人类记忆的衰减与激活机制，将遗忘视为可访问性的降低而非物理删除。通过解耦读写路径，Oblivion能根据智能体不确定性决定何时检索，并根据贡献度强化记忆，实现了分层记忆组织，显著提升了复杂场景下的推理效率与准确性。
selection_source: fresh_fetch
motivation: 现有LLM智能体采用扁平化存储和全量检索模式，随着历史记录增长，容易产生严重的干扰和计算延迟。
method: 提出一种衰减驱动的自适应内存控制框架，通过解耦读写路径，利用不确定性触发检索并根据响应贡献度强化特定记忆。
result: 在长周期交互基准测试中，该方法成功实现了内存访问与强化的动态平衡，有效降低了冗余访问并提升了任务表现。
conclusion: 研究证明了模拟人类遗忘机制的内存控制是提升LLM智能体在复杂、动态环境下推理能力的关键。
---

## 摘要
人类记忆通过选择性遗忘进行适应：经验随时间推移变得难以获取，但可以通过强化或上下文线索重新激活。相比之下，记忆增强型大语言模型（LLM）智能体依赖“始终开启”的检索和“扁平化”的记忆存储，随着历史记录的增长，会导致严重的干扰和延迟。我们引入了 Oblivion，这是一个记忆控制框架，它将遗忘视为由衰减驱动的可访问性降低，而非显式的删除。Oblivion 将记忆控制解耦为读路径和写路径。读路径根据智能体的不确定性和记忆缓冲区的充足性来决定何时查阅记忆，从而避免冗余的始终开启式访问。写路径通过强化对生成响应有贡献的记忆，来决定需要加强的内容。两者结合实现了一种分层记忆组织，在保持持久的高层策略的同时，根据需要动态加载细节。我们在静态和动态的长时程交互基准测试上进行了评估。结果表明，Oblivion 能够动态调整记忆访问和强化，在不断变化的上下文中平衡学习与遗忘，强调了记忆控制对于有效的 LLM 智能体推理至关重要。源代码可在 https://github.com/nec-research/oblivion 获取。

## Abstract
Human memory adapts through selective forgetting: experiences become less accessible over time but can be reactivated by reinforcement or contextual cues. In contrast, memory-augmented LLM agents rely on "always-on" retrieval and "flat" memory storage, causing high interference and latency as histories grow. We introduce Oblivion, a memory control framework that casts forgetting as decay-driven reductions in accessibility, not explicit deletion. Oblivion decouples memory control into read and write paths. The read path decides when to consult memory, based on agent uncertainty and memory buffer sufficiency, avoiding redundant always-on access. The write path decides what to strengthen, by reinforcing memories contributing to forming the response. Together, this enables hierarchical memory organization that maintains persistent high-level strategies while dynamically loading details as needed. We evaluate on both static and dynamic long-horizon interaction benchmarks. Results show that Oblivion dynamically adapts memory access and reinforcement, balancing learning and forgetting under shifting contexts, highlighting that memory control is essential for effective LLM-agentic reasoning. The source code is available at https://github.com/nec-research/oblivion.

---

## 论文详细总结（自动生成）

### 论文总结：Oblivion —— 基于衰减驱动激活的自适应智能体记忆控制

#### 1. 核心问题与整体含义（研究动机和背景）
现有的记忆增强型大语言模型（LLM）智能体在处理长周期（Long-horizon）任务时面临三大挑战：
*   **检索冗余（Always-on Retrieval）：** 无论是否需要，智能体在每轮对话都会检索记忆，导致计算浪费。
*   **记忆扁平化（Flat Memory）：** 存储系统无法区分高层策略与底层细节，通常采用简单的“硬删除”来应对上下文限制，导致重要信息丢失。
*   **干扰与延迟：** 随着历史记录增长，无关信息的堆积会产生“大海捞针”难题，干扰推理并增加延迟。

**研究动机：** 借鉴人类认知心理学中的“遗忘”机制——遗忘并非物理删除，而是**可访问性的降低**。记忆随时间衰减，但可通过强化或上下文线索重新激活。

#### 2. 方法论：核心思想与关键技术
Oblivion 框架的核心是将记忆管理视为一个**控制问题**，通过**读/写路径解耦**实现自适应控制：

*   **分层记忆表示：**
    *   **L1（动态程序记忆）：** 驻留在缓冲区的元索引，包含类别摘要、访问统计和处理策略。
    *   **L2（语义记忆）：** 存储带时间戳的事实陈述。
    *   **L3（抢占式情节记忆）：** 存储经过转化的交互片段，将过去的回馈转化为行动建议。
*   **读路径（激活机制）：**
    *   **衰减器（Decayer）：** 利用**艾宾浩斯遗忘曲线公式** $R_t(c) = \exp(-n_t(c) / S_t(c))$ 计算保留分数。同时通过 LLM 评判和嵌入相似度进行**不确定性估计**，仅在当前缓冲区不足以回答问题时触发检索。
    *   **激活器（Activator）：** 采用基于 **DAG（有向无环图）的查询扩展**，将复杂查询分解为多步子查询，从持久存储中加载 L2/L3 记忆。
*   **写路径（强化机制）：**
    *   **识别器（Recognizer）：** 进行“信用分配”，识别哪些记忆条目对生成当前响应有贡献。
    *   **记忆管理器：** 仅强化被使用的记忆（提高其稳定性 $S_t$），未被使用的记忆则自然衰减。

#### 3. 实验设计
*   **数据集/场景：**
    *   **LONG MEM EVAL：** 静态基准测试，包含约 100K 上下文的多会话对话，侧重于长文本 QA。
    *   **GOOD AILTM：** 动态交互基准测试，包含约 120K 上下文，评估智能体在持续对话中保留、修正和更新信息的能力。
*   **对比方法：**
    *   **Direct（直接访问）：** 如 Full Context（全上下文模式）。
    *   **Memory（记忆增强）：** 包括 LME-RFT、Ever-Memos（基于 OS 的记忆系统）和 Beyond Prompts（类似 MemGPT 的扁平检索系统）。
*   **模型后端：** Phi-4-mini (4B), Qwen3-30B, GPT-4o-mini, GPT-4.1-mini。

#### 4. 资源与算力
论文**未明确说明**具体的 GPU 型号、数量或总训练时长。由于该框架主要是一个**推理时（Inference-time）控制层**，不涉及大规模模型微调，其算力消耗主要体现在 API 调用次数和本地小模型的推理延迟上。文中提供了推理延迟（Latency）和吞吐量（TPS）的对比数据。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了静态 QA（500 个问题）和动态交互（33 个测试场景），并在 2K、32K、120K 等不同记忆跨度下进行了测试。
*   **消融实验：** 针对分层记忆（L1/L2/L3）的组合进行了详细测试，验证了每一层对性能的贡献。
*   **分析深度：** 探讨了衰减温度参数 $T$ 对记忆保留分布的影响，并进行了成本和延迟分析。
*   **评价：** 实验设计较为充分，对比了多种主流记忆框架，且在不同规模的开源和闭源模型上均有验证，结果具有较好的客观性和说服力。

#### 6. 主要结论与发现
*   **性能领先：** Oblivion 在静态和动态任务中均优于直接检索和现有的记忆增强基准。
*   **成本效益：** 在 120K 长上下文场景下，Oblivion 相比全上下文模式**降低了约 73% 的 Token 成本**，且延迟显著低于其他记忆增强系统。
*   **遗忘的价值：** 证明了通过衰减降低无关信息的可访问性，能有效缓解“迷失在中间（Lost-in-the-middle）”现象，提升推理准确度。

#### 7. 优点与亮点
*   **认知启发：** 成功将心理学中的遗忘曲线和读写解耦概念引入 LLM 智能体。
*   **自适应性：** 能够根据查询的不确定性动态决定是否检索，避免了“始终开启”检索带来的冗余。
*   **分层架构：** L1-L3 的分层设计兼顾了宏观策略和微观事实，比扁平化存储更符合复杂任务需求。
*   **开源贡献：** 提供了源代码，便于社区复现和进一步研究。

#### 8. 不足与局限
*   **模型依赖：** 框架中的不确定性估计和记忆提取高度依赖 LLM 本身的能力，使用极小规模模型时性能可能受限。
*   **参数敏感：** 衰减温度 $T$ 需要针对不同任务进行调优，缺乏一种完全自动化的参数确定机制。
*   **覆盖范围：** 实验主要集中在文本交互，尚未在多模态、多语言或极大规模（如百万级）上下文场景下进行验证。
*   **计算开销：** 虽然节省了 Token 成本，但多步查询扩展（DAG）和 LLM-as-a-judge 的判断过程会引入额外的推理延迟。

（完）
