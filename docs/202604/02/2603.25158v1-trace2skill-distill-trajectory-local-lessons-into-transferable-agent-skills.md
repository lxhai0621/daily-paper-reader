---
title: "Trace2Skill: Distill Trajectory-Local Lessons into Transferable Agent Skills"
title_zh: Trace2Skill：将轨迹局部经验提炼为可迁移的智能体技能
authors: "Jingwei Ni, Yihao Liu, Xinpeng Liu, Yutao Sun, Mengyu Zhou, Pengyu Cheng, Dexin Wang, Xiaoxi Jiang, Guanjun Jiang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25158v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 通过轨迹分析为大模型智能体自动生成技能
tldr: 针对 LLM 智能体技能获取中手动编写效率低和自动生成易过拟合的问题，本文提出 Trace2Skill 框架。该框架模拟专家行为，通过并行分析大量执行轨迹，提取局部经验并利用归纳推理将其层级化整合为统一、无冲突的技能库。实验证明，Trace2Skill 能显著提升智能体在复杂任务中的表现，且生成的技能具有极强的跨模型迁移能力和 OOD 泛化性，无需参数更新或外部检索。
selection_source: fresh_fetch
motivation: 现有的自动技能生成方法往往因过度拟合局部轨迹或依赖浅层知识，导致生成的技能碎片化且缺乏泛化能力。
method: 采用并行子智能体分析多样化轨迹，提取特定教训并通过归纳推理将其层级化整合为统一、无冲突的声明式技能目录。
result: 在电子表格、VisionQA 和数学推理等挑战性领域显著优于强基线，且小模型生成的技能能大幅提升大模型的性能。
conclusion: 复杂的智能体经验可以被提炼为高度可迁移的声明式技能，为提升 LLM 智能体的领域专业能力提供了一种高效且通用的新路径。
---

## 摘要
为大语言模型（LLM）智能体配备特定领域技能对于处理复杂任务至关重要。然而，手动编写技能会产生严重的可扩展性瓶颈。相反，自动技能生成往往会产生脆弱或碎片化的结果，因为它要么依赖于浅层的参数化知识，要么顺序地过拟合于不可泛化的轨迹局部经验。为了克服这一问题，我们提出了 Trace2Skill，这是一个模仿人类专家编写技能方式的框架：通过全面分析广泛的执行经验，然后将其提炼为一份单一且全面的指南。Trace2Skill 并非顺序地对单个轨迹做出反应，而是派遣一组并行的子智能体来分析多样化的执行池。它提取特定于轨迹的经验，并通过归纳推理将其分层整合进一个统一、无冲突的技能目录中。Trace2Skill 既支持深化现有的人工编写技能，也支持从零开始创建新技能。在电子表格、VisionQA 和数学推理等具有挑战性的领域进行的实验表明，Trace2Skill 相比于包括 Anthropic 官方 xlsx 技能在内的强基线模型有显著提升。至关重要的是，这种基于轨迹的演化不仅仅是记忆任务实例或模型特定的怪癖：演化出的技能可以跨 LLM 规模迁移，并能泛化到分布外（OOD）设置。例如，由 Qwen3.5-35B 在其自身轨迹上演化出的技能，在 WikiTableQuestions 任务上将 Qwen3.5-122B 智能体的表现提升了高达 57.65 个绝对百分点。最终，我们的结果表明，复杂的智能体经验可以被打包成高度可迁移的声明式技能——无需参数更新，无需外部检索模块，且可以使用小至 35B 参数的开源模型。

## Abstract
Equipping Large Language Model (LLM) agents with domain-specific skills is critical for tackling complex tasks. Yet, manual authoring creates a severe scalability bottleneck. Conversely, automated skill generation often yields fragile or fragmented results because it either relies on shallow parametric knowledge or sequentially overfits to non-generalizable trajectory-local lessons. To overcome this, we introduce Trace2Skill, a framework that mirrors how human experts author skills: by holistically analyzing broad execution experience before distilling it into a single, comprehensive guide. Instead of reacting sequentially to individual trajectories, Trace2Skill dispatches a parallel fleet of sub-agents to analyze a diverse pool of executions. It extracts trajectory-specific lessons and hierarchically consolidates them into a unified, conflict-free skill directory via inductive reasoning. Trace2Skill supports both deepening existing human-written skills and creating new ones from scratch. Experiments in challenging domains, such as spreadsheet, VisionQA and math reasoning, show that Trace2Skill significantly improves upon strong baselines, including Anthropic's official xlsx skills. Crucially, this trajectory-grounded evolution does not merely memorize task instances or model-specific quirks: evolved skills transfer across LLM scales and generalize to OOD settings. For example, skills evolved by Qwen3.5-35B on its own trajectories improved a Qwen3.5-122B agent by up to 57.65 absolute percentage points on WikiTableQuestions. Ultimately, our results demonstrate that complex agent experience can be packaged into highly transferable, declarative skills -- requiring no parameter updates, no external retrieval modules, and utilizing open-source models as small as 35B parameters.

---

## 论文详细总结（自动生成）

### 论文总结：Trace2Skill —— 将轨迹局部经验提炼为可迁移的智能体技能

#### 1. 核心问题与整体含义（研究动机和背景）
大语言模型（LLM）智能体在处理复杂领域任务时，极度依赖结构化的“技能”（如操作指南、SOP）。然而，目前获取技能面临两大困境：
*   **手动编写瓶颈**：专家编写技能耗时耗力，难以覆盖海量的细分领域。
*   **自动生成缺陷**：现有的自动方法通常采用“顺序更新”模式（即每看到一个失败案例就修改一次技能），这导致技能库变得碎片化、过拟合于特定轨迹，且容易在多次修改中产生逻辑冲突。
**Trace2Skill 的核心动机**是模拟人类专家的学习方式：通过并行观察大量执行案例，进行全局性的归纳推理，最终提炼出一份统一、严谨且具备泛化能力的技能文档。

#### 2. 方法论：核心思想与关键技术
Trace2Skill 提出了一个三阶段的离线演化框架：
1.  **轨迹生成（Trajectory Generation）**：让固定参数的智能体在任务集上并行运行，收集成功（$T^+$）和失败（$T^-$）的执行轨迹。
2.  **并行多智能体补丁提议（Parallel Patch Proposal）**：
    *   **成功分析员（$A^+$）**：识别成功轨迹中的高效模式。
    *   **错误分析员（$A^-$）**：采用**代理式（Agentic）多轮交互**模式，通过检查文件、对比真值、尝试修复来定位根本原因，并提出针对性的技能补丁。
3.  **无冲突补丁整合（Conflict-Free Consolidation）**：
    *   **层级化合并**：将数百个局部补丁通过 LLM 进行层级化合并（类似树状结构）。
    *   **归纳推理**：在合并过程中，LLM 被要求识别“普遍模式”（即在多个独立补丁中重复出现的建议），丢弃偶然性的细节，从而确保技能的泛化性。
    *   **程序化校验**：通过格式检查和冲突检测（如避免对同一行进行冲突修改）确保最终技能文档的可用性。

#### 3. 实验设计
*   **实验场景与数据集**：
    *   **电子表格操作**：SpreadsheetBench（核心测试集），并使用 WikiTableQuestions 进行分布外（OOD）泛化测试。
    *   **数学推理**：DAPO-Math 和 AIME 2026。
    *   **视觉问答（VQA）**：DocVQA（涉及表单、发票等复杂图像理解）。
*   **对比基线（Baselines）**：
    *   **No Skill**：无技能输入。
    *   **Human-Written**：Anthropic 官方提供的专家编写技能。
    *   **Parametric**：仅依靠 LLM 自身知识生成的初始技能。
    *   **ReasoningBank**：基于检索的经验库方法（RAG 模式）。
    *   **Sequential Editing**：在线顺序更新模式。

#### 4. 资源与算力
*   **模型使用**：主要使用开源的 Qwen3.5-122B-A10B 和 Qwen3.5-35B-A3B。
*   **算力消耗**：
    *   **轨迹生成**：在 122B 模型上生成 200 条长轨迹（50+ 轮交互）仅需不到 2 GPU-hours。
    *   **技能演化**：在 8 张 A800 GPU 节点上，Trace2Skill 的并行整合仅需约 **3 分钟**，而传统的顺序更新（Seq-B=1）需要约 60 分钟，效率提升 20 倍。

#### 5. 实验数量与充分性
*   **实验规模**：论文在三个截然不同的领域（表格、数学、视觉）进行了验证，涵盖了从 35B 到 122B 的跨模型测试。
*   **客观性与公平性**：
    *   采用了 3 个随机种子取平均值。
    *   进行了详尽的消融实验，对比了“代理式分析 vs. 单次 LLM 调用”、“并行整合 vs. 顺序更新”、“技能文档 vs. 检索记忆”。
    *   **跨模型验证**是其亮点：证明了小模型（35B）生成的技能可以显著提升大模型（122B）的表现，排除了简单的“记忆模型特性”的可能性。

#### 6. 主要结论与发现
*   **性能卓越**：Trace2Skill 生成的技能显著优于人类专家编写的技能，尤其在处理复杂工具调用（如 openpyxl 库的怪癖）时。
*   **极强的迁移性**：技能不仅能跨模型规模迁移，还能从电子表格编辑任务迁移到 Wikipedia 表格问答任务（OOD 泛化）。
*   **小模型也能胜任**：35B 规模的模型足以作为“技能作者”，在某些任务上将 122B 模型的表现提升了 57.65%。
*   **整合胜过检索**：将经验提炼为单一文档的效果优于在推理时检索零散的记忆片段。

#### 7. 优点
*   **架构无关**：生成的技能是纯文本（Markdown），无需更新模型参数，可直接适配任何智能体框架。
*   **高效并行**：解决了在线学习中串行更新的延迟和漂移问题。
*   **深度诊断**：通过 Agentic 错误分析，确保了技能补丁是基于事实而非幻觉。

#### 8. 不足与局限
*   **因果归因缺失**：目前无法精确量化技能文档中某一个特定补丁对最终性能提升的边际贡献。
*   **动态修剪不足**：缺乏一种机制来识别并删除技能文档中随着时间推移变得冗余或无效的部分。
*   **领域覆盖**：虽然涵盖了三个领域，但在更开放、更动态的 Web 浏览或长程软件工程任务中的表现尚待验证。

（完）
