---
title: "InconLens: Interactive Visual Diagnosis of Behavioral Inconsistencies in LLM-based Agentic Systems"
title_zh: InconLens：基于大语言模型（LLM）的智能体系统行为不一致性交互式可视化诊断
authors: "Shuo Yan, Xiaolin Wen, Shaolun Ruan, Yanjie Zhang, Jiaming Mi, Yushi Sun, Huamin Qu, Rui Sheng"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28106v1"
tags: ["query:agent"]
score: 9.0
evidence: 诊断基于大语言模型的智能体系统行为不一致性
tldr: 针对基于大语言模型（LLM）的智能体系统在处理复杂任务时存在的行为不一致性问题（即相同输入下结果不同），本文提出了可视化分析系统 InconLens。该系统通过引入“信息节点”作为中间抽象，实现了多轮运行轨迹的语义对齐与对比。研究通过案例分析和专家访谈证明，InconLens 能有效帮助开发者识别行为分歧点、发现潜在失败模式，并提升系统的可靠性与稳定性。
selection_source: fresh_fetch
motivation: LLM 智能体的随机性导致相同输入下表现不一，且现有工具难以对比冗长且无结构的运行日志。
method: 提出 InconLens 系统，利用“信息节点”抽象关键里程碑，支持跨运行轨迹的语义对齐与交互式可视化诊断。
result: 案例研究和专家访谈表明，该系统能显著提高开发者识别分歧点和分析失败模式的效率。
conclusion: InconLens 为提升 LLM 智能体系统的稳定性和可靠性提供了有效的可视化分析手段。
---

## 摘要
基于大语言模型（LLM）的智能体系统在通过自主规划、推理以及与外部环境交互来处理复杂多步任务方面展现出日益增长的潜力。然而，LLM 生成的随机性引入了内在的行为不一致性：在相同的输入下，同一个智能体可能在一次执行中成功，但在另一次执行中失败。诊断此类不一致性仍然是开发者面临的主要挑战，因为智能体的执行日志通常冗长、非结构化，且难以在多次运行之间进行比较。现有的调试和评估工具主要侧重于检查单次执行，对于理解智能体行为在多次重复运行中如何以及为何发生分歧的支持有限。为了应对这一挑战，我们推出了 InconLens，这是一个可视化分析系统，旨在支持基于 LLM 的智能体系统的交互式诊断，特别关注跨运行的行为分析。InconLens 引入了信息节点作为一种中间抽象，用于捕捉多次执行中共享的规范化信息里程碑，从而实现对多次运行中智能体推理轨迹的语义对齐和检查。我们通过详细的案例研究展示了 InconLens 的有效性，并通过专家访谈进一步验证了其可用性和分析价值。我们的结果表明，InconLens 使开发者能够更高效地识别分歧点，揭示潜在的失败模式，并获得关于提高智能体系统可靠性和稳定性的可操作见解。

## Abstract
Large Language Model (LLM)-based agentic systems have shown growing promise in tackling complex, multi-step tasks through autonomous planning, reasoning, and interaction with external environments. However, the stochastic nature of LLM generation introduces intrinsic behavioral inconsistency: the same agent may succeed in one execution but fail in another under identical inputs. Diagnosing such inconsistencies remains a major challenge for developers, as agent execution logs are often lengthy, unstructured, and difficult to compare across runs. Existing debugging and evaluation tools primarily focus on inspecting single executions, offering limited support for understanding how and why agent behaviors diverge across repeated runs. To address this challenge, we introduce InconLens, a visual analytics system designed to support interactive diagnosis of LLM-based agentic systems with a particular focus on cross-run behavioral analysis. InconLens introduces information nodes as an intermediate abstraction that captures canonical informational milestones shared across executions, enabling semantic alignment and inspection of agent reasoning trajectories across multiple runs. We demonstrate the effectiveness of InconLens through a detailed case study and further validate its usability and analytical value via expert interviews. Our results show that InconLens enables developers to more efficiently identify divergence points, uncover latent failure modes, and gain actionable insights into improving the reliability and stability of agentic systems.

---

## 论文详细总结（自动生成）

### 论文总结：InconLens - 基于 LLM 的智能体系统行为不一致性交互式可视化诊断

#### 1. 核心问题与整体含义
*   **研究动机**：基于大语言模型（LLM）的智能体（Agent）系统具有随机性。在相同输入下，智能体可能在一次运行中成功，而在另一次中失败，这种“行为不一致性”给开发者带来了巨大的调试压力。
*   **核心挑战**：智能体执行日志通常极其冗长、非结构化，且不同运行轨迹（Trajectories）之间难以进行语义对齐和直接对比。现有工具多侧重于单次运行的检查，缺乏对跨运行（Cross-run）分歧原因的深入分析支持。
*   **研究目标**：开发一个可视化分析系统，帮助开发者识别多次运行中的分歧点，揭示潜在失败模式，并提供改进系统可靠性的见解。

#### 2. 方法论
InconLens 引入了**“信息节点”（Information Nodes）**作为中间语义抽象层，将底层日志转化为高层任务里程碑。
*   **核心思想**：不关注具体的动作（如“运行 Python 脚本”），而关注获取了什么信息（如“提取了数据库架构”），以此实现不同运行轨迹的对齐。
*   **技术流程**：
    1.  **协调者消息整理**：针对中心化多智能体系统，提取协调者（Orchestrator）的消息，过滤底层执行噪声。
    2.  **语义变化驱动的分段**：利用 Embedding 向量计算相邻消息的余弦相似度，识别语义突变点作为候选状态转换边界。
    3.  **候选节点提取**：利用 LLM 对分段后的文本进行自然语言摘要，生成候选信息节点。
    4.  **人工介入微调**：开发者对自动生成的节点进行确认、重命名、合并或拆分，并定义节点间的依赖关系。
*   **可视化组件**：
    *   **任务摘要视图**：提供多次运行的完成状态概览。
    *   **信息节点视图**：使用 **Sankey 图（桑基图）** 展示任务流向，通过颜色（绿/红/橙）标记成功、失败和恢复路径。
    *   **动作视图**：对齐展示不同运行在特定节点转换时的动作序列（如 Web、Coder、Terminal 等智能体的调用顺序）。
    *   **日志视图**：提供原始证据的追溯。

#### 3. 实验设计
*   **实验场景**：选择了 **MagenticOne**（微软开发的通用多智能体系统）作为诊断对象。
*   **具体案例**：**投资组合再平衡（Investment Portfolio Rebalancing）**。该任务涉及文件读取、网页搜索、汇率查询、代码生成及执行，逻辑复杂且容易出错。
*   **评估方法**：
    *   **形成性研究**：与 5 名经验丰富的 AI 开发者进行半结构化访谈，确定设计需求。
    *   **案例研究**：详细展示专家如何利用系统发现智能体因 reCAPTCHA 验证码拦截而陷入死循环，并提出策略改进建议。
    *   **专家访谈**：邀请 5 名未参与设计的专家（P1-P5）进行任务测试，并使用 **NASA-TLX** 量表评估认知负荷。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **原因分析**：由于该系统属于可视化诊断工具，主要消耗在于调用 LLM API（用于生成摘要、分析错误和评估节点状态）以及前端渲染，而非模型训练。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 1 个深度案例分析。
    *   邀请了 5 位专家进行系统评估。
    *   在形成性研究阶段咨询了 5 位开发者。
*   **充分性评价**：对于可视化（Visualization）和人机交互（HCI）领域的论文，这种“需求分析 + 系统设计 + 案例研究 + 专家访谈”的模式是标准且充分的。实验通过定性反馈和 NASA-TLX 定量评分证明了系统的有效性，但在大规模自动化测试方面尚有提升空间。

#### 6. 主要结论与发现
*   **有效性**：InconLens 能显著降低开发者对比冗长日志的认知负担，快速定位首次发生分歧的关键节点。
*   **诊断价值**：系统帮助专家发现了智能体在网页搜索失败时缺乏“策略切换”（如从网页搜索转为脚本抓取）的问题。
*   **认知负荷**：NASA-TLX 结果显示，用户在精神需求、努力程度和挫败感方面的得分均较低，证明系统易于上手且工作流合理。

#### 7. 优点
*   **语义抽象创新**：提出的“信息节点”概念成功解决了异构执行轨迹难以对齐的痛点。
*   **人机协作**：系统并非全自动，而是通过“LLM 推荐 + 人类确认”的模式，保证了诊断的准确性和灵活性。
*   **多层级工作流**：从宏观摘要到微观日志的递进式设计，符合开发者的调试逻辑。

#### 8. 不足与局限
*   **幻觉风险**：系统依赖 LLM 来评估信息节点的完成状态，可能存在模型幻觉导致的误判。
*   **路径分歧限制**：对于路径极其发散、完全没有共同里程碑的开放式任务，提取通用信息节点可能非常困难。
*   **架构局限**：目前主要针对中心化（Centralized）多智能体系统，对去中心化或点对点架构的支持需进一步扩展。

（完）
