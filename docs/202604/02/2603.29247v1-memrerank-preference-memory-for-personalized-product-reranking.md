---
title: "MemRerank: Preference Memory for Personalized Product Reranking"
title_zh: MemRerank：用于个性化商品重排序的偏好记忆框架
authors: "Zhiyuan Peng, Xuyang Wu, Huaixiao Tou, Yi Fang, Yi Gong"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29247v1"
tags: ["query:agent"]
score: 9.0
evidence: 强化学习用于智能体记忆优化
tldr: 针对基于大语言模型的购物助手在处理冗长且含噪声的购买历史时效果不佳的问题，本文提出了 MemRerank 框架。该框架通过强化学习训练的存储提取器，将用户历史蒸馏为简洁的偏好记忆，用于个性化商品重排序。实验表明，该方法在 1-in-5 选择任务中显著优于原始历史和现有基准，为电商智能体的个性化提供了有效方案。
selection_source: fresh_fetch
motivation: 传统的将原始购买历史直接加入提示词的方法因噪声大、长度长且相关性差，难以有效实现个性化重排序。
method: 提出 MemRerank 框架，利用强化学习以重排序性能为监督信号，将用户历史蒸馏为简洁、独立于查询的偏好记忆。
result: 在 1-in-5 选择任务中，MemRerank 相比无记忆或原始历史基准，准确率最高提升了 10.61 个百分点。
conclusion: 显式的偏好记忆是构建高效、实用的个性化电商智能体系统的关键组件。
---

## 摘要
基于大语言模型（LLM）的购物智能体日益依赖长购买历史和多轮交互来实现个性化，但由于噪声、长度和相关性不匹配，直接将原始历史记录附加到提示词中往往效果不佳。我们提出了 MemRerank，这是一个偏好记忆框架，它将用户购买历史提炼为简洁且独立于查询的信号，用于个性化商品重排序。为了研究这一问题，我们构建了一个端到端的基准测试和评估框架，其核心是一个基于 LLM 的“五选一”（1-in-5）选择任务，该任务同时衡量记忆质量和下游重排序效用。我们进一步利用强化学习（RL）训练记忆提取器，并以下游重排序性能作为监督信号。在两个基于 LLM 的重排序器上进行的实验表明，MemRerank 始终优于无记忆、原始历史和现成的记忆基准，在“五选一”准确率上最高提升了 10.61 个绝对百分点。这些结果表明，显式偏好记忆是代理式电子商务系统中实现个性化的实用且有效的构建模块。

## Abstract
LLM-based shopping agents increasingly rely on long purchase histories and multi-turn interactions for personalization, yet naively appending raw history to prompts is often ineffective due to noise, length, and relevance mismatch. We propose MemRerank, a preference memory framework that distills user purchase history into concise, query-independent signals for personalized product reranking. To study this problem, we build an end-to-end benchmark and evaluation framework centered on an LLM-based \textbf{1-in-5} selection task, which measures both memory quality and downstream reranking utility. We further train the memory extractor with reinforcement learning (RL), using downstream reranking performance as supervision. Experiments with two LLM-based rerankers show that MemRerank consistently outperforms no-memory, raw-history, and off-the-shelf memory baselines, yielding up to \textbf{+10.61} absolute points in 1-in-5 accuracy. These results suggest that explicit preference memory is a practical and effective building block for personalization in agentic e-commerce systems.

---

## 论文详细总结（自动生成）

以下是对论文《MemRerank: Preference Memory for Personalized Product Reranking》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：在基于大语言模型（LLM）的电商智能体中，如何有效利用用户冗长的历史购买记录来实现个性化推荐。
*   **研究动机**：
    *   **噪声与长度限制**：直接将原始购买历史放入 Prompt 会引入大量无关噪声，且容易超出 LLM 的上下文窗口限制。
    *   **相关性失配**：原始历史记录往往与当前查询（Query）的直接相关性较弱，导致模型难以提取有效的偏好信号。
    *   **缺乏结构化**：现有的记忆框架（如 Mem0）多为通用设计，缺乏针对电商场景（跨品类与品类内偏好）的深度优化。

### 2. 方法论：MemRerank 框架
*   **核心思想**：将用户的购买历史“蒸馏”为简洁、结构化且独立于查询的**偏好记忆（Preference Memory）**，并利用强化学习（RL）优化提取过程。
*   **关键技术细节**：
    *   **两阶段流程**：首先由“记忆提取器”生成偏好摘要，随后将其输入“重排序器”进行商品选择。
    *   **双重记忆结构**：
        *   **品类内记忆（Within-category）**：捕捉用户在特定品类（如电子产品）下的具体功能偏好、品牌倾向等。
        *   **跨品类记忆（Cross-category）**：捕捉用户的通用购物风格（如预算敏感度、对品质的要求）。
    *   **强化学习训练（GRPO）**：使用组相对策略优化（Group Relative Policy Optimization）算法训练提取器。
        *   **奖励函数（Reward Function）**：由**格式奖励**（确保输出符合特定标签格式）和**重排序奖励**（如果下游重排序器选对了正确商品，则给予正向反馈）组成。
    *   **提示词工程**：设计了半结构化、基于证据（Evidence-grounded）的提取提示词，要求模型在提取偏好时引用原始元数据片段。

### 3. 实验设计
*   **数据集**：基于 **Amazon-Review-2023** 和 **Amazon-C4** 构建。
*   **Benchmark 任务**：**1-in-5 选择任务**。即给定 1 个正样本和 4 个同品类的硬负样本（Hard Negatives），要求模型选出最相关的一个。
*   **对比方法**：
    *   **无记忆基准**：仅使用 Query 和候选商品。
    *   **原始历史注入**：直接在 Prompt 中添加原始购买记录。
    *   **现成记忆工具**：如 Mem0、MR.Rec。
    *   **强模型提取**：使用未经 RL 训练的 Qwen2.5-7B 或 GPT-5.2 生成记忆。

### 4. 资源与算力
*   **模型使用**：
    *   **提取器**：Qwen2.5-7B-Instruct（作为 RL 训练的基础模型）。
    *   **重排序器**：GPT-4.1-mini 和 o4-mini。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及训练总时长。但提到使用了 GRPO 算法，这通常需要一定的分布式计算资源来支持多样本并行采样。

### 5. 实验数量与充分性
*   **实验规模**：主要在“电子产品（Electronics）”类别上进行深入实验（包含 905 个训练实例、194 个开发实例和 194 个测试实例）。
*   **消融实验**：
    *   对比了三种不同的提取提示词（固定维度 vs 自由发现 vs 半结构化）。
    *   分析了购买历史长度（10 到 40 个商品）对性能的影响。
    *   验证了“思考标签（Think Tag）”对推理能力的提升。
*   **客观性**：实验涵盖了多种主流 LLM 作为重排序器，并对比了最新的学术界和工业界记忆框架，评估较为客观。但实验主要集中在单一品类，跨品类的泛化性验证略显不足。

### 6. 主要结论与发现
*   **性能显著提升**：MemRerank 在 o4-mini 重排序器上比无记忆基准提升了 **10.61%** 的准确率。
*   **RL 训练至关重要**：经过 RL 优化的提取器显著优于未经训练的提取器，甚至优于使用更强大的 GPT-5.2 提取的记忆。
*   **提示词设计的微妙平衡**：半结构化（提供示例但不强制）且要求提供证据的提示词效果最好，过于僵硬或过于自由的提示词都会降低性能。
*   **原始历史的负面影响**：对于某些模型（如 GPT-4.1-mini），直接注入过长的原始历史反而会引入噪声，导致性能下降。

### 7. 优点与亮点
*   **闭环优化**：通过 RL 将下游任务（重排序）的准确性直接反馈给上游任务（记忆提取），实现了端到端的性能对齐。
*   **高效性**：将冗长的历史压缩为几百个 Token 的偏好摘要，极大地节省了推理时的上下文成本。
*   **可解释性**：通过引入证据片段（Evidence Snippets）和思考标签，使得个性化推荐的过程更加透明。

### 8. 不足与局限
*   **领域覆盖有限**：实验主要集中在电子产品类别，不同品类（如服装、食品）的偏好特征差异巨大，其通用性有待验证。
*   **规模限制**：目前仅在 1-in-5 的重排序场景下测试，尚未在更大规模（如从 100 个候选物中排序）的端到端检索场景中验证。
*   **数据依赖**：依赖于高质量的商品元数据和评论，如果用户历史数据稀疏或元数据缺失，效果可能会大打折扣。
*   **实时性挑战**：虽然记忆是独立于查询的，但针对每个用户进行 RL 微调或大规模提取在工业级实时系统中仍面临计算压力。

（完）
