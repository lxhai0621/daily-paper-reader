---
title: "APEX-EM: Non-Parametric Online Learning for Autonomous Agents via Structured Procedural-Episodic Experience Replay"
title_zh: APEX-EM：通过结构化程序-情节式经验回放实现自主智能体的非参数化在线学习
authors: "Pratyay Banerjee, Masud Moshtaghi, Ankit Chadha"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29093v1"
tags: ["query:agent"]
score: 10.0
evidence: 自主智能体的持久程序性记忆
tldr: 针对LLM智能体在处理结构相似任务时缺乏持久记忆、需重复推导的问题，本文提出APEX-EM非参数化在线学习框架。该框架通过结构化经验表示（涵盖计划、错误分析及评分）和PRGII工作流，实现了经验的自动积累、检索与重用。其独特的混合检索机制支持跨领域结构迁移，在代码生成和复杂问答等多个基准测试中显著提升了智能体的性能，表现优于现有SOTA方法。
selection_source: fresh_fetch
motivation: 旨在解决LLM智能体无法持久化存储和复用过往解决问题的程序化经验，导致在相似任务上效率低下的问题。
method: 引入结构化经验表示与PRGII工作流，结合语义搜索、结构签名匹配和DAG遍历实现双重结果经验存储与检索。
result: 在KGQAGen-10k上准确率提升48.3个百分点，在BigCodeBench上提升29.4个百分点，均大幅超越基准模型及现有记忆增强方法。
conclusion: APEX-EM证明了通过非参数化的结构化经验回放，智能体无需修改模型权重即可实现高效的在线学习和跨领域知识迁移。
---

## 摘要
基于大语言模型（LLM）的自主智能体缺乏持久的程序性记忆：即使之前已经解决过结构完全相同的任务，它们仍会从头开始推导解决方案。我们提出了 APEX-EM，这是一个非参数化在线学习框架，它在不修改模型权重的情况下积累、检索和重用结构化程序计划。APEX-EM 引入了：(1) 一种结构化经验表示，编码了每次执行的完整程序-情节轨迹——包括规划步骤、中间产物、带有错误分析的迭代历史以及质量评分；(2) 一种“规划-检索-生成-迭代-摄取”（PRGII）工作流，其中任务验证器提供多维奖励信号；(3) 一种具有双重结果的经验记忆，采用结合了语义搜索、结构特征匹配和计划有向无环图（DAG）遍历的混合检索方式——从而实现在词汇无重叠但操作结构相似的任务之间的跨领域迁移。成功的经验作为正向上下文示例；失败的经验则作为带有结构化错误标注的负向示例。我们使用 Claude Sonnet 4.5 和 Opus 4.5 在 BigCodeBench、KGQAGen-10k 和 Humanity's Last Exam (HLE) 上进行了评估。在 KGQAGen-10k 上，APEX-EM 达到了 89.6% 的准确率，而无记忆情况下为 41.3%（提升 48.3 个百分点），超过了真值检索（oracle-retrieval）的上限（84.9%）。在 BigCodeBench 上，其成功率从 53.9% 的基准提升至 83.3%（提升 29.4 个百分点），在可比的冻结骨干网络条件下，超过了 MemRL 11.0 个百分点的增益（注：我们在分析中对骨干网络的差异进行了控制）。在 HLE 上，实体图检索从 25.2% 提升至 48.0%（提升 22.8 个百分点）。消融实验表明，各组件的价值取决于具体任务：丰富的评判反馈对代码生成的影响微乎其微，但对结构化查询至关重要（提升 10.3 个百分点），而二值信号迭代可以部分补偿较弱的反馈。

## Abstract
LLM-based autonomous agents lack persistent procedural memory: they re-derive solutions from scratch even when structurally identical tasks have been solved before. We present \textbf{APEX-EM}, a non-parametric online learning framework that accumulates, retrieves, and reuses structured procedural plans without modifying model weights. APEX-EM introduces: (1) a \emph{structured experience representation} encoding the full procedural-episodic trace of each execution -- planning steps, artifacts, iteration history with error analysis, and quality scores; (2) a \emph{Plan-Retrieve-Generate-Iterate-Ingest} (PRGII) workflow with Task Verifiers providing multi-dimensional reward signals; and (3) a \emph{dual-outcome Experience Memory} with hybrid retrieval combining semantic search, structural signature matching, and plan DAG traversal -- enabling cross-domain transfer between tasks sharing no lexical overlap but analogous operational structure. Successful experiences serve as positive in-context examples; failures as negative examples with structured error annotations.   We evaluate on BigCodeBench~\cite{zhuo2025bigcodebench}, KGQAGen-10k~\cite{zhang2025kgqagen}, and Humanity's Last Exam~\cite{phan2025hle} using Claude Sonnet 4.5 and Opus 4.5. On KGQAGen-10k, APEX-EM achieves 89.6\% accuracy versus 41.3\% without memory (+48.3pp), surpassing the oracle-retrieval upper bound (84.9\%). On BigCodeBench, it reaches 83.3\% SR from a 53.9\% baseline (+29.4pp), exceeding MemRL's~\cite{memrl2025} +11.0pp gain under comparable frozen-backbone conditions (noting backbone differences controlled for in our analysis). On HLE, entity graph retrieval reaches 48.0\% from 25.2\% (+22.8pp). Ablations show component value is task-dependent: rich judge feedback is negligible for code generation but critical for structured queries (+10.3pp), while binary-signal iteration partially compensates for weaker feedback.

---

## 论文详细总结（自动生成）

以下是对论文《APEX-EM: Non-Parametric Online Learning for Autonomous Agents via Structured Procedural-Episodic Experience Replay》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：当前的 LLM 智能体在程序层面是“无状态”的。即使面对结构完全相同的任务，它们也会从头开始推导方案，无法积累和复用过往的执行经验。
*   **研究动机**：
    *   **现有记忆机制的局限**：如 MemRL 等系统存储的是扁平的文本摘要或标量 Q 值，丢失了执行过程中的程序结构（如规划步骤、错误轨迹）。
    *   **缺乏负面经验利用**：多数系统仅存储成功案例，忽略了失败案例作为“避坑指南”的价值。
    *   **跨领域迁移困难**：基于语义相似度的检索在词汇不重叠但逻辑结构相似的任务间（如体育统计 vs. 财务报表分析）表现不佳。

### 2. 方法论：APEX-EM 框架
APEX-EM 引入了一种非参数化的在线学习机制，核心思想是通过外部存储和检索结构化经验来提升性能，而无需微调模型权重。

*   **程序化知识图谱 (PKG)**：一种分层图结构，包含实体（Entity）、经验（Experience）、子任务（Sub-Task）、操作（Operation）和任务主题（TaskTopic）五种节点。
*   **结构化经验表示**：每个经验包含五个反射层：
    1.  **目标反射**：任务目标与约束。
    2.  **程序反射**：可复用的步骤模板。
    3.  **证据反射**：执行过程中使用的工具输出或事实。
    4.  **错误注册表**：结构化的错误分类与根因分析。
    5.  **补丁反射**：针对错误的具体修复逻辑。
*   **PRGII 工作流**：
    1.  **Plan（规划）**：分解任务并提取“结构签名”（抽象操作序列，如 `[实体解析 -> 过滤 -> 聚合]`）。
    2.  **Retrieve（检索）**：混合检索策略，结合语义搜索、结构签名匹配（利用最长公共子序列 LCS）和图遍历。
    3.  **Generate（生成）**：基于检索到的正向（成功）和负向（失败）示例生成代码或查询。
    4.  **Iterate（迭代）**：通过验证器进行自我修正。
    5.  **Ingest（摄取）**：由“教师模型”评分并存入 PKG，实现闭环学习。

### 3. 实验设计
*   **数据集与场景**：
    *   **BigCodeBench (BCB)**：复杂的 Python 代码生成，涉及多库调用。
    *   **KGQAGen-10k**：基于 Wikidata 的多步结构化查询（SPARQL）生成。
    *   **Humanity’s Last Exam (HLE)**：跨学科的专家级知识推理。
*   **对比基准 (Baselines)**：
    *   无记忆基准（No Memory）。
    *   标准检索增强生成（RAG、Self-RAG）。
    *   现有智能体记忆系统：Reflexion、Mem0、MemP 以及当前的 SOTA 方法 **MemRL**。
*   **评估指标**：末轮成功率 (SR) 和累积成功率 (CSR)。

### 4. 资源与算力
*   **模型骨干**：使用了 **Claude 4.5 Sonnet** 和 **Claude 4.5 Opus** 作为智能体核心。
*   **嵌入模型**：Amazon Titan Embed v2。
*   **算力说明**：论文**未明确说明**具体的 GPU 硬件型号、数量或总训练时长。由于该方法是非参数化的（无需训练权重），其主要开销在于推理时的多轮 LLM 调用和向量数据库检索。

### 5. 实验数量与充分性
*   **实验规模**：在三个不同领域的基准测试上进行了评估，每个实验运行 10 个 Epoch 以观察在线学习曲线。
*   **消融实验**：非常充分。分别测试了有无评判反馈（Judge）、有无迭代、不同检索策略（仅语义 vs. 仅实体图 vs. 混合）对结果的影响。
*   **客观性**：通过在相同冻结骨干网络（Frozen-backbone）条件下对比 MemRL，并设置了 7:3 的训练/迁移划分，验证了系统的泛化能力。

### 6. 主要结论与发现
*   **性能飞跃**：在 KGQA 任务上，准确率从 41.3% 提升至 89.6%（+48.3pp），甚至超过了拥有真值子图的 Oracle 上限。
*   **跨领域迁移**：结构签名匹配允许智能体在词汇完全不同的任务间迁移逻辑模式，解决了语义检索的失效问题。
*   **效率提升**：随着经验积累，智能体解决任务所需的迭代次数显著减少（KGQA 任务减少了 37-43%），实现了从“自我修正”到“经验引导”的转变。
*   **任务依赖性**：发现丰富的反馈对结构化查询至关重要，但对代码生成（已有编译器报错）增益较小。

### 7. 优点
*   **结构化深度**：相比之前的扁平记忆，PKG 提供了更深层的因果链条和程序逻辑。
*   **双重结果学习**：显式利用失败案例作为负面约束，有效防止智能体重复同样的错误。
*   **非参数化优势**：无需昂贵的微调，即插即用，且能通过更新 PKG 实时更新智能体的“世界知识”。

### 8. 不足与局限
*   **模型依赖**：实验高度依赖 Claude 4.5 等顶级模型，在较弱模型上的表现尚待验证。
*   **分类体系限制**：结构签名的分类法（Taxonomy）目前是人工设计的，可能无法覆盖所有复杂的程序模式。
*   **推理延迟**：PRGII 工作流涉及多步规划、检索和可能的多次迭代，在实时性要求极高的场景下可能存在延迟挑战。
*   **收敛性证明**：作为一种经验驱动的方法，缺乏像强化学习那样的形式化收敛保证。

（完）
