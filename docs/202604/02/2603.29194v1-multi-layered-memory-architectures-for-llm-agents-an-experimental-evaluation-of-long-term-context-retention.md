---
title: "Multi-Layered Memory Architectures for LLM Agents: An Experimental Evaluation of Long-Term Context Retention"
title_zh: 大语言模型智能体的多层记忆架构：长期上下文保留的实验评估
authors: "Sunil Tiwari, Payal Fofadiya"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29194v1"
tags: ["query:agent"]
score: 10.0
evidence: 用于长期上下文保留的多层记忆架构
tldr: 针对长程对话系统中语义漂移和记忆留存不稳定的挑战，本文提出了一种多层记忆框架。该架构将对话历史分解为工作、情景和语义三层，并引入自适应检索门控与留存正则化机制。实验表明，该方法在提升长效记忆留存和推理稳定性的同时，显著降低了上下文开销和错误记忆率，为LLM智能体提供了高效的记忆管理方案。
selection_source: fresh_fetch
motivation: 解决长程对话系统在长会话中面临的语义漂移、记忆留存不稳定以及上下文无限增长导致的计算效率低下问题。
method: 构建包含工作、情景和语义层的多层记忆架构，并采用自适应检索门控和留存正则化技术来管理对话历史。
result: "在LOCOMO等基准测试中取得了46.85%的成功率，显著提升了长期留存率，并将错误记忆率降至5.1%，上下文使用量减少了约41.6%。"
conclusion: 多层记忆架构能有效平衡记忆深度与计算效率，显著增强了LLM智能体在受限上下文预算下的长期记忆能力和推理稳定性。
---

## 摘要
长程对话系统在长会话中常面临语义漂移和记忆保留不稳定的问题。本文提出了一种多层记忆框架，该框架通过自适应检索门控和保留正则化，将对话历史分解为工作层、情节层和语义层。该架构在控制跨会话漂移的同时，保持了有界的上下文增长和计算效率。在 LOCOMO、LOCCO 和 LoCoMo 上的实验表明，该方法提升了性能，实现了 46.85 的成功率、0.618 的整体 F1 分数（其中多跳 F1 为 0.594）以及 56.90% 的六周期保留率，同时将虚假记忆率降低至 5.1%，并将上下文使用量减少至 58.40%。结果证实，在受限的上下文预算下，该架构增强了长期保留能力和推理稳定性。

## Abstract
Long-horizon dialogue systems suffer from semanticdrift and unstable memory retention across extended sessions. This paper presents a Multi-Layer Memory Framework that decomposes dialogue history into working, episodic, and semantic layers with adaptive retrieval gating and retention regularization. The architecture controls cross-session drift while maintaining bounded context growth and computational efficiency. Experiments on LOCOMO, LOCCO, and LoCoMo show improved performance, achieving 46.85 Success Rate, 0.618 overall F1 with 0.594 multi-hop F1, and 56.90% six-period retention while reducing false memory rate to 5.1% and context usage to 58.40%. Results confirm enhanced long-term retention and reasoning stability under constrained context budgets.

---

## 论文详细总结（自动生成）

### 论文总结：大语言模型智能体的多层记忆架构

#### 1. 核心问题与研究背景
论文针对长程对话系统（Long-horizon dialogue systems）在处理超长会话时面临的三大挑战：**语义漂移（Semantic Drift）**、**记忆留存不稳定**以及**上下文窗口限制**。
*   **研究动机**：传统的对话历史处理方式（如直接拼接）会导致计算开销呈平方级增长，且容易产生记忆干扰，导致模型丢失角色一致性或出现事实错误。
*   **核心问题**：如何在受限的内存预算下，构建一种既能保持长期语义稳定性，又不增加推理成本的分层记忆结构。

#### 2. 论文提出的方法论（MLMF 框架）
论文提出了一种**多层记忆框架（Multi-Layer Memory Framework, MLMF）**，其核心思想是将记忆解耦为三个功能层：
*   **工作记忆层（Working Memory）**：保留最近的对话片段，采用有界窗口编码，确保短期任务信号的活跃度。
*   **情景记忆层（Episodic Memory）**：通过递归融合机制生成会话摘要，平衡信息的抽象度与细节保留。
*   **语义记忆层（Semantic Memory）**：将情景摘要映射为结构化的实体-事件图（Entity-Event Graphs），维护稳定的角色属性和长期事实。

**关键技术细节：**
*   **自适应检索门控（Adaptive Retrieval Gating）**：利用 Softmax 机制根据当前输入动态计算各层记忆的权重，实现精准检索。
*   **留存正则化（Retention Regularization）**：引入正则化项 $L_{ret}$，通过惩罚相邻会话间语义嵌入的剧烈变化，抑制语义漂移。
*   **优化目标**：将生成损失与留存损失统一优化（$L = L_{gen} + \lambda L_{ret}$），确保回复流畅性的同时兼顾记忆一致性。

#### 3. 实验设计
论文在三个主流的长程对话基准数据集上进行了评估：
*   **LOCOMO**：侧重于超长会话（平均 588 轮）中的角色一致性和时间连续性。
*   **LOCCO**：用于量化跨越多个时间周期的记忆衰减。
*   **LoCoMo**：评估实体跟踪、多跳推理、因果和时间推理能力。
*   **对比方法**：包括 HiAgent（分层工作记忆）、Memory OS（三级存储操作系统）、Truth-maintained Agent（带真实性校验的记忆）以及多种上下文压缩技术（如 KV 缓存剔除）。

#### 4. 资源与算力
论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）及训练时长。实验提到所有测试均在相同的解码设置和受限上下文预算下进行，以确保公平性。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了三个不同侧重点的基准数据集，并针对每个数据集的关键指标（成功率、F1、留存率、上下文效率）进行了详尽对比。
*   **消融实验**：专门设计了消融组，分别移除语义层、情景层、正则化项和自适应门控，验证了各组件的必要性。
*   **客观性**：所有结果均为 5 次独立运行的平均值，并进行了配对 t 检验（$p < 0.01$），证明了结果的统计显著性。实验设计较为客观、全面。

#### 6. 主要结论与发现
*   **性能提升**：MLMF 在 LOCOMO 上取得了 46.85 的成功率（高于基准的 42.00），在 LoCoMo 上的多跳推理 F1 达到 0.594。
*   **长期留存**：在 LOCCO 六周期测试中，记忆留存率达到 56.90%（比对比方法提升约 8.65%）。
*   **错误控制**：虚假记忆率（False Memory Rate）降至 5.1%，显著低于同类架构。
*   **效率优化**：上下文使用量减少至 58.40%，解码吞吐量提升至 10.4 倍，证明了该架构在资源受限环境下的优越性。

#### 7. 优点与亮点
*   **结构化解耦**：通过三层记忆设计，巧妙地解决了短期交互与长期抽象之间的矛盾。
*   **稳定性机制**：引入留存正则化项是本文的一大亮点，从优化目标层面直接约束了语义漂移。
*   **高效性**：在提升记忆质量的同时，显著降低了 Token 使用量和推理延迟，具有较强的工程实用价值。

#### 8. 不足与局限
*   **领域局限性**：实验主要集中在对话系统，尚未在代码生成、长文档分析等其他长上下文任务中验证通用性。
*   **图构建开销**：虽然论文声称效率提升，但语义记忆层中“实体-事件图”的动态构建和冲突解决（RESOLVE 算法）在超大规模知识库下的扩展性有待进一步观察。
*   **算力细节缺失**：缺乏具体的算力消耗数据，使得其他研究者难以评估其训练成本。

（完）
