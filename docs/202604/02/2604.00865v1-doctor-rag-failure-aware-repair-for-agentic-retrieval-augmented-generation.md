---
title: "Doctor-RAG: Failure-Aware Repair for Agentic Retrieval-Augmented Generation"
title_zh: Doctor-RAG：针对智能体检索增强生成的故障感知修复
authors: "Shuguang Jiao, Chengkai Huang, Shuhan Qi, Xuan Wang, Yifan Li, Lina Yao"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00865v1"
tags: ["query:ma-kf"]
score: 10.0
evidence: 智能体检索增强生成的故障感知修复
tldr: 针对智能体检索增强生成（Agentic RAG）在处理复杂多跳问答时易出现推理失败且重试成本高的问题，本文提出Doctor-RAG（DR-RAG）框架。该框架通过故障诊断与定位，将错误归类并锁定最早失效点，随后仅在失效点进行局部修复并复用已有前缀。实验表明，DR-RAG在提升回答准确率的同时，显著降低了推理成本。
selection_source: fresh_fetch
motivation: 现有的Agentic RAG在推理失败时通常需要重新运行整个流程，导致巨大的计算开销和冗余推理。
method: 提出一种统一的诊断与修复框架，通过轨迹级错误定位和基于工具条件的局部修复，实现对推理过程的最小化干预。
result: 在多个多跳问答基准测试中，DR-RAG在提高准确率的同时，相比全量重跑策略大幅减少了Token消耗。
conclusion: 显式分离错误归因与修正，并结合前缀复用技术，是实现高效且精准的Agentic RAG故障修复的有效途径。
---

## 摘要
智能体检索增强生成（Agentic RAG）已成为多跳问答和复杂知识推理中广泛采用的范式，其中检索和推理在推理阶段交织进行。随着推理轨迹变长，故障变得越来越普遍。现有方法通常通过停留在诊断分析或重新运行整个检索-推理流水线来处理此类故障，这导致了巨大的计算开销和冗余推理。在本文中，我们提出了 Doctor-RAG (DR-RAG)，这是一个统一的诊断与修复框架，通过显式的错误定位和前缀重用，以最小成本干预来纠正 Agentic RAG 中的故障。DR-RAG 将故障处理分解为两个连续阶段：(i) 轨迹级故障诊断与定位，将错误归因于覆盖门控分类体系，并识别推理轨迹中的最早故障点；(ii) 工具调节的局部修复，仅在诊断出的故障点进行干预，同时最大限度地重用已验证的推理前缀和检索到的证据。通过显式地将错误归因与纠正分离，DR-RAG 实现了精确的错误定位，从而避免了昂贵的全流水线重运行，并实现了有针对性的高效修复。我们在三个多跳问答基准测试、多个 Agentic RAG 基准线和不同的主干模型上对 DR-RAG 进行了评估。实验结果表明，与基于重运行的修复策略相比，DR-RAG 在显著降低推理 Token 消耗的同时，大幅提高了答案准确性。

## Abstract
Agentic Retrieval-Augmented Generation (Agentic RAG) has become a widely adopted paradigm for multi-hop question answering and complex knowledge reasoning, where retrieval and reasoning are interleaved at inference time. As reasoning trajectories grow longer, failures become increasingly common. Existing approaches typically address such failures by either stopping at diagnostic analysis or rerunning the entire retrieval-reasoning pipeline, which leads to substantial computational overhead and redundant reasoning. In this paper, we propose Doctor-RAG (DR-RAG), a unified diagnose-and-repair framework that corrects failures in Agentic RAG through explicit error localization and prefix reuse, enabling minimal-cost intervention. DR-RAG decomposes failure handling into two consecutive stages: (i) trajectory-level failure diagnosis and localization, which attributes errors to a coverage-gated taxonomy and identifies the earliest failure point in the reasoning trajectory; and (ii) tool-conditioned local repair, which intervenes only at the diagnosed failure point while maximally reusing validated reasoning prefixes and retrieved evidence. By explicitly separating error attribution from correction, DR-RAG enables precise error localization, thereby avoiding expensive full-pipeline reruns and enabling targeted, efficient repair. We evaluate DR-RAG across three multi-hop question answering benchmarks, multiple agentic RAG baselines, and different backbone models. Experimental results demonstrate that DR-RAG substantially improves answer accuracy while significantly reducing reasoning token consumption compared to rerun-based repair strategies.

---

## 论文详细总结（自动生成）

以下是对论文《Doctor-RAG: Failure-Aware Repair for Agentic Retrieval-Augmented Generation》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：智能体检索增强生成（Agentic RAG）通过动态生成查询、调用检索器和多步推理，在处理复杂知识推理任务中表现优异。
*   **核心问题**：随着推理轨迹（Trajectory）变长，系统失效（Failure）变得不可避免。
*   **研究动机**：现有处理失效的方法存在两个极端：一是仅停留在诊断层面（如 RAGChecker），不提供自动修复；二是采取“全量重跑”（Rerun）策略（如 RAG-Critic），丢弃所有中间过程重新执行。这导致了巨大的计算开销、推理冗余，且无法复用已验证的正确推理前缀。
*   **核心目标**：探索如何在发现失效后，以最小的计算成本定位并修复 Agentic RAG 的推理轨迹。

### 2. 方法论：Doctor-RAG (DR-RAG) 框架
DR-RAG 将失效处理建模为“诊断-定位-局部修复”的闭环过程，核心包含两个阶段：

*   **阶段一：分类体系约束的错误诊断与定位**
    *   **覆盖率门控（Coverage-Gated）**：首先评估已检索文档是否足以回答问题（$Cov=1$ 或 $Cov<1$）。这作为一个门控信号，将错误空间划分为不同的候选集。
    *   **错误分类**：
        *   **$Cov=1$ 时**：分为格式错误（Format Error）和推理逻辑错误（Reasoning Error）。
        *   **$Cov<1$ 时**：分为格式错误、检索器错误（Retriever Error，查询对但没搜到）和搜索诱导错误（Search Error，推理错导致查询错）。
    *   **最早失效点定位（Localization）**：识别轨迹中第一个偏离正确路径的动作索引 $k^\dagger$，将 $k^\dagger$ 之前的步骤视为可复用的“有效前缀”。

*   **阶段二：工具调节的局部修复（Tool-Conditioned Local Repair）**
    *   **前缀复用**：保留 $k^\dagger$ 之前的轨迹，仅从失效点开始干预。
    *   **修复算子**：
        *   **格式修复**：仅重写最终答案。
        *   **推理修复**：截断轨迹，基于已有证据重新推理。
        *   **检索修复**：重写查询并增加 Top-K 检索深度。
        *   **搜索诱导修复**：生成高层解决方案计划，重新规划后续的推理与检索路径。

### 3. 实验设计
*   **数据集**：使用了三个经典的多跳问答（Multi-hop QA）基准测试：**HotpotQA**、**2WikiMultihopQA** 和 **MuSiQue**。
*   **智能体基准线（Baselines）**：集成了三种代表性的 Agentic RAG 范式：**ReAct**、**Search-o1** 和 **Search-R1**。
*   **对比修复策略**：
    *   **Rerun**：全量重跑。
    *   **Step-wise Retry**：逐步验证并重试。
    *   **RAG-Critic**：基于诊断的重规划（但仍需重跑大部分流程）。
*   **主干模型**：评估了 Qwen3-8B、LLaMA-3.1-8B-Instruct 和 Qwen3-4B，验证了跨模型规模的泛化性。

### 4. 资源与算力
*   **硬件环境**：实验在 **4 张 NVIDIA RTX Pro 6000 GPU** 上运行。
*   **推理框架**：使用 **vLLM** 进行高效推理。
*   **训练时长**：文中未提及模型微调或训练时长，因为该框架主要作为一种**推理时（Inference-time）**的即插即用修复策略，不涉及参数更新。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个数据集 × 3 种 Agent 框架 × 3 种主干模型，形成了详尽的对比矩阵。
*   **消融实验**：分别验证了“错误分类体系”和“失效定位”的必要性，证明两者缺一不可。
*   **可靠性分析**：对比了自动诊断与人工标注的一致性（准确率在 60% 左右），并进行了 Oracle（先验已知错误点）上限实验。
*   **客观性**：实验不仅关注准确率（EM/F1），还引入了 Token 消耗和推理时间作为效率指标，评估维度全面且公平。

### 6. 主要结论与发现
*   **性能提升**：DR-RAG 在所有数据集和模型上均显著优于 Rerun 策略。例如在 Qwen3-8B + ReAct 组合下，HotpotQA 的 EM 提升了 25.8%。
*   **成本降低**：相比 Rerun，DR-RAG 的 Token 消耗降低了约 25%-40%，推理时间也大幅缩短。
*   **修复难度**：证据覆盖不足（$Cov<1$）时的修复难度远高于证据充足（$Cov=1$）的情况。
*   **模型规模**：DR-RAG 对小参数模型（如 4B）的提升效果尤为显著，说明局部修复能有效弥补小模型推理稳定性的不足。

### 7. 优点与亮点
*   **首创性**：首次将 Agentic RAG 的失效处理形式化为“最小成本干预”问题，而非盲目重试。
*   **前缀复用机制**：通过定位最早失效点，最大限度保留了已有的计算成果，极具工程实用价值。
*   **细粒度诊断**：提出的覆盖率门控分类体系比传统的 RAG 评估更符合智能体交互的逻辑。

### 8. 不足与局限
*   **诊断准确率瓶颈**：自动诊断的准确率目前约为 60%，诊断错误会导致修复策略失效或引入新错误。
*   **复杂任务限制**：在极高难度的多跳任务（如 MuSiQue）中，修复成功率依然较低，说明局部修复难以解决根本性的逻辑能力缺失。
*   **依赖 LLM 裁判**：诊断过程本身依赖 LLM 的判断力，若主干模型本身较弱，诊断的可靠性会随之下降。
*   **应用场景**：目前仅在多跳问答任务上验证，尚未在更广泛的工具调用（如 API 操作、代码生成）场景中测试。

（完）
