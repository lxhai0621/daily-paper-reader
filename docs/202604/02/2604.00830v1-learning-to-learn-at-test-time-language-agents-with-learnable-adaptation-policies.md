---
title: "Learning to Learn-at-Test-Time: Language Agents with Learnable Adaptation Policies"
title_zh: 学习在测试时学习：具有可学习自适应策略的语言智能体
authors: "Zhanzhi Lou, Hui Chen, Yibo Li, Qian Wang, Bryan Hooi"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00830v1"
tags: ["query:agent"]
score: 9.0
evidence: 语言智能体在测试时优化性能的可学习自适应策略
tldr: 本研究针对语言智能体在推理阶段的测试时学习（TTL）问题，指出传统方法依赖手工设计的固定策略。为此，作者提出了Meta-TTL框架，通过双层优化将适应策略的发现转化为可学习过程。该框架利用进化搜索在多样化任务上优化策略，使智能体能根据历史经验更有效地修正错误。实验证明，Meta-TTL在Jericho和WebArena-Lite基准测试中显著优于手工基准，展现了强大的泛化能力。
selection_source: fresh_fetch
motivation: 现有的测试时学习方法依赖于人工设计的固定适应策略，无法针对特定任务环境实现最优的性能提升。
method: 提出Meta-TTL框架，采用双层优化结构，通过外层循环的进化搜索在多种任务上迭代优化内层循环的适应策略。
result: 在Jericho和WebArena-Lite基准测试的分布内及分布外设置下，Meta-TTL均显著优于传统的手工设计基准。
conclusion: 学习到的适应策略能够编码可迁移的策略，从而在未见过的任务分布中实现比人工设计更强的泛化性能。
---

## 摘要
测试时学习（TTL）使语言智能体能够在推理阶段通过与环境的重复交互来迭代优化其性能。TTL 的核心是一个自适应策略，它根据先前回合的经验更新执行者策略（actor policy），从而改善未来的行为。现有方法依赖于固定的、手工设计的自适应策略，而不是针对下游改进对其进行优化。我们认为，最优的自适应策略应该从任务环境中学习，而不是基于人类直觉进行手工设计。为此，我们引入了 Meta-TTL，这是一个将有效自适应策略的发现表述为双层优化问题的框架。在该框架内，内循环执行标准的 TTL 过程，衡量候选自适应策略在连续回合中帮助智能体纠正错误的有效性。在智能体性能的指导下，外循环在多样化的训练任务分布上采用进化搜索，以迭代地优化自适应策略。我们在 Jericho 和 WebArena-Lite 上评估了 Meta-TTL，涵盖了分布内（ID）和分布外（OOD）设置，并使用了多种元智能体骨干网络。两个基准测试的结果表明，Meta-TTL 一致优于手工设计的基准方法，这表明优化后的自适应策略编码了可迁移的策略，能够泛化到训练任务分布之外。

## Abstract
Test-Time Learning (TTL) enables language agents to iteratively refine their performance through repeated interactions with the environment at inference time. At the core of TTL is an adaptation policy that updates the actor policy based on experience from previous episodes, thereby improving future behavior. Existing methods rely on fixed, hand-crafted adaptation policies rather than optimizing them for downstream improvement. We argue that optimal adaptation policies should be learned from task environments, not hand-engineered based on human intuition. To achieve this, we introduce Meta-TTL, a framework that formulates the discovery of effective adaptation policies as a bi-level optimization problem. Within this framework, the inner loop executes the standard TTL process, measuring how effectively a candidate adaptation policy helps an agent correct errors across sequential episodes. Guided by the agent's performance, the outer loop employs evolutionary search over a diverse distribution of training tasks to iteratively refine the adaptation policy. We evaluate Meta-TTL on Jericho and WebArena-Lite across both in-distribution (ID) and out-of-distribution (OOD) settings, using multiple meta-agent backbones. Results on both benchmarks show that Meta-TTL consistently outperforms hand-crafted baselines, suggesting that the optimized adaptation policy encodes transferable strategies that generalize beyond the training task distribution.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning to Learn-at-Test-Time: Language Agents with Learnable Adaptation Policies》的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：尽管大语言模型（LLM）智能体具备强大的零样本能力，但在面对陌生环境时，往往难以通过尝试和错误进行自我改进。
*   **核心问题**：现有的“测试时学习”（Test-Time Learning, TTL）方法（如 Reflexion）通常依赖于**人工预设、固定不变**的自适应策略（即如何根据失败经验更新下一步行动的提示词）。作者认为，最优的自适应策略应当是从任务环境中“学习”出来的，而非基于人类直觉手工设计。
*   **整体含义**：论文提出了 **Meta-TTL** 框架，将寻找有效自适应策略的过程建模为一个**双层优化（Bi-level Optimization）**问题，旨在通过元学习（Meta-Learning）自动发现能够跨任务迁移的自适应规则。

### 2. 方法论
*   **核心思想**：通过“学习如何学习”，将自适应策略（Adaptation Policy）实例化为一个可优化的“元提示词”（Meta-Prompt），该提示词指导元智能体如何分析历史轨迹并重写执行者（Actor）的系统提示词。
*   **双层优化结构**：
    *   **内层循环（Inner Loop）**：标准的 TTL 过程。智能体在特定任务上进行多轮尝试（Episodes），元智能体根据历史经验更新执行者的提示词，目标是最大化该任务的学习曲线下面积（W-AUC）。
    *   **外层循环（Outer Loop）**：进化元训练。在训练任务分布上，利用进化搜索（Evolutionary Search）迭代优化元提示词。
*   **关键技术细节**：
    *   **进化搜索**：包含提案（Proposal）、局部验证（Local Validation）和全局验证（Global Validation）阶段。
    *   **专家池（Expert Pool）**：为每个验证任务维护表现最好的元提示词，最终选择综合表现最优（通过 z-score 标准化评分）的作为最终策略。
    *   **W-AUC 指标**：采用加权学习曲线下面积，给予后期回合更高的权重，以奖励持续的改进。

### 3. 实验设计
*   **数据集/场景**：
    *   **Jericho**：交互式小说游戏（文本冒险类），具有密集的奖励信号。
    *   **WebArena-Lite**：真实网页导航任务，具有稀疏的二元奖励（成功/失败）。
*   **测试设置**：分为分布内（ID）和分布外（OOD）任务，以验证策略的泛化性。
*   **对比方法（Baselines）**：
    *   **Static**：无自适应。
    *   **Reflexion**：经典的手工设计反馈方法。
    *   **Memory Agent**：基于记忆积累的方法。
    *   **Naive Meta-Agent**：使用未经过元优化的初始元提示词。
*   **模型骨干**：执行者统一使用 Gemini 3 Flash；元智能体分别测试了 Gemini 3 Flash、GLM-5 和 GPT-5。

### 4. 资源与算力
*   **算力说明**：文中未明确提及具体的 GPU 型号和数量。
*   **训练时长**：论文附录提到，针对 Jericho 任务，使用 GPT-5 作为元智能体的优化过程经历了 26 次迭代，耗时约 **27 小时**。
*   **推理成本**：由于是基于提示词的优化，测试时不需要梯度计算，但多回合交互和元智能体的调用会增加推理阶段的 Token 消耗。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个截然不同的基准测试（游戏 vs 网页），并针对三种不同的顶级 LLM 骨干网络进行了独立实验。
*   **充分性**：
    *   **泛化性测试**：通过 OOD 任务验证了学习到的策略不是简单的过拟合，而是掌握了通用的自适应逻辑。
    *   **消融与分析**：提供了详细的学习曲线对比、优化轨迹分析以及定性的案例研究。
*   **客观性**：使用了标准化的 W-AUC 指标，并在 Jericho 任务中引入了 z-score 标准化来消除不同游戏难度差异带来的偏差，实验设计较为严谨。

### 6. 论文的主要结论与发现
*   **性能提升**：Meta-TTL 在所有设置下均优于手工基准。在 Jericho ID 任务上，平均得分从 50.4 提升至 110.8（约 120% 提升）；在 WebArena 上也有显著增长。
*   **OOD 泛化**：优化后的元提示词在未见过的任务上依然有效，证明了“自适应策略”本身是可迁移的。
*   **涌现策略**：通过进化学习，元提示词自动演化出了诸如**显式信用分配（Credit Assignment）**、**受控的探索管理**以及**结构化诊断报告**等高级策略。
*   **任务差异**：在奖励密集的任务（Jericho）中，优化效果比奖励稀疏的任务（WebArena）更明显，因为前者提供了更细粒度的反馈信号。

### 7. 优点
*   **框架创新**：将 TTL 提升到了元学习的高度，摆脱了对人工设计提示词的依赖。
*   **可解释性**：学习到的自适应策略以自然语言（元提示词）形式存在，人类可以直接阅读并理解智能体学到了哪些“学习技巧”。
*   **灵活性**：作为一种“插件式”方法，它可以跨不同的模型骨干使用，无需微调模型权重。

### 8. 不足与局限
*   **搜索成本**：外层的进化搜索过程依赖于大量的任务尝试，对于 API 调用成本较高的模型来说，训练代价较大。
*   **奖励依赖**：在极度稀疏的奖励环境下（如 WebArena），进化搜索的效率会受到挑战，可能需要更长的训练时间或更强的提案模型。
*   **上下文限制**：随着尝试回合增加，历史轨迹的长度可能会超出 LLM 的上下文窗口，论文未深入讨论长程记忆的压缩问题。

（完）
