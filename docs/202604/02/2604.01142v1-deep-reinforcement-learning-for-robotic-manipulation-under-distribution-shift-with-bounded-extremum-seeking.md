---
title: Deep Reinforcement Learning for Robotic Manipulation under Distribution Shift with Bounded Extremum Seeking
title_zh: 基于有界极值寻优的分布偏移下机器人操纵深度强化学习
authors: "Shaifalee Saxena, Rafael Fierro, Alexander Scheinker"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01142v1"
tags: ["query:agent"]
score: 9.0
evidence: 用于机器人操作智能体的深度强化学习
tldr: 针对机器人操纵任务在测试环境与训练分布不一致（分布偏移）时性能下降的问题，本文提出一种结合深度强化学习（DDPG）与有界极值搜索（Bounded ES）的混合控制框架。该方法利用RL实现快速操纵，并通过Bounded ES增强系统在目标变化或摩擦力波动等分布外（OOD）情况下的鲁棒性，显著提升了机器人在复杂接触任务中的泛化能力。
selection_source: fresh_fetch
motivation: 传统的强化学习策略在面对机器人操纵中的分布偏移（如目标变化或动力学差异）时往往表现不佳，缺乏鲁棒性。
method: 提出一种混合控制器，将预训练的深度确定性策略梯度（DDPG）策略与有界极值搜索（Bounded ES）算法相结合。
result: 在具有时变目标和空间变化摩擦斑块等分布外设置的推动及取放任务中，该控制器表现出更强的适应性。
conclusion: 结合强化学习的高效性与极值搜索的鲁棒性，是解决机器人操纵中分布偏移问题的有效途径。
---

## 摘要
强化学习在机器人操纵中展现出强大的性能，但当测试条件与训练分布不一致时，学习到的策略性能往往会下降。这种局限性在诸如推挤和取放等接触密集型任务中尤为显著，因为目标、接触条件或机器人动力学的改变可能导致系统在推理阶段出现分布偏移。在本文中，我们研究了一种将强化学习与有界极值寻优相结合的混合控制器，以提高此类条件下的鲁棒性。在所提出的方法中，深度确定性策略梯度（DDPG）策略在标准条件下针对机器人推挤和取放任务进行训练，并在部署时与有界极值寻优相结合。强化学习策略提供快速的操纵行为，而有界极值寻优则确保当运行条件偏离训练分布时，整体控制器对时间变化具有鲁棒性。我们在多种分布外设置下对所得控制器进行了评估，包括随时间变化的目标和空间变化的摩擦区域。

## Abstract
Reinforcement learning has shown strong performance in robotic manipulation, but learned policies often degrade in performance when test conditions differ from the training distribution. This limitation is especially important in contact-rich tasks such as pushing and pick-and-place, where changes in goals, contact conditions, or robot dynamics can drive the system out-of-distribution at inference time. In this paper, we investigate a hybrid controller that combines reinforcement learning with bounded extremum seeking to improve robustness under such conditions. In the proposed approach, deep deterministic policy gradient (DDPG) policies are trained under standard conditions on the robotic pushing and pick-and-place tasks, and are then combined with bounded ES during deployment. The RL policy provides fast manipulation behavior, while bounded ES ensures robustness of the overall controller to time variations when operating conditions depart from those seen during training. The resulting controller is evaluated under several out-of-distribution settings, including time-varying goals and spatially varying friction patches.

---

## 论文详细总结（自动生成）

以下是对论文《Deep Reinforcement Learning for Robotic Manipulation under Distribution Shift with Bounded Extremum Seeking》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：深度强化学习（DRL）在机器人操纵任务（如推挤、取放）中表现出色，但其策略极易受到**分布偏移（Distribution Shift）**的影响。当测试环境的动力学特性（如摩擦力变化）或任务目标（如时变目标）与训练环境不一致时，预训练策略的性能会剧烈下降。
*   **研究动机**：接触密集型任务具有高度非线性和混合动力学特征，难以精确建模。现有的域随机化或元学习方法计算成本高，且难以覆盖所有可能的部署场景。因此，需要一种在推理阶段（Inference-time）能够实时在线调整、且无需重新训练的鲁棒控制方案。

### 2. 方法论
论文提出了一种**混合控制器架构（ES-DRL）**，结合了 DRL 的快速响应能力和有界极值寻优（Bounded ES）的鲁棒自适应能力。
*   **核心思想**：利用 RL 策略处理任务的“入口阶段”（如接近物体、建立接触、形成抓取），因为这些行为需要复杂的序列决策；一旦建立接触，则切换到 Bounded ES 进行“操纵阶段”的在线微调，以应对环境的不确定性。
*   **关键技术细节**：
    *   **DRL 部分**：采用深度确定性策略梯度（DDPG）算法，在标准 MuJoCo 环境中训练目标条件（Goal-conditioned）策略。
    *   **Bounded ES 部分**：这是一种无模型（Model-free）的自适应控制方法。它通过在控制输入中加入微小的正弦扰动（Dither），根据标量性能信号（如物体与目标的距离）的反馈，实时估计未知代价函数的梯度并更新动作。其“有界”特性确保了控制量和参数更新率在安全范围内。
    *   **切换机制（Supervisor）**：引入一个基于接触状态的开关变量 $\beta$。当末端执行器未接触物体时，由 RL 主导；一旦检测到接触（Contact Flag），控制权移交给 ES，并以 RL 最后的动作为 ES 的初始值。

### 3. 实验设计
*   **实验场景**：基于 Gymnasium-Robotics 的 Fetch 机器人基准测试，包括 **FetchPush**（推挤）和 **FetchPickAndPlace**（取放）。
*   **分布外（OOD）设置**：
    1.  **空间变化摩擦力**：将桌面分为三个不同摩擦系数（$\mu=0.8, 1.2, 1.5$）的区域，而训练仅在均匀摩擦面上进行。
    2.  **时变目标（2D/3D）**：目标不再固定，而是沿圆形或三维曲线轨迹移动。
*   **对比方法（Benchmark）**：
    *   **RL-only**：仅使用预训练的 DDPG 策略。
    *   **ES-only**：仅使用有界极值寻优（从随机初始位置开始）。
    *   **ES-DRL（本文方法）**：结合两者的混合控制器。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或训练总时长。
*   **训练参数**：文中提到了 DDPG 的超参数，如 Replay Buffer 大小为 $10^6$，Batch Size 为 256，Actor/Critic 学习率均为 $1 \times 10^{-4}$，每个训练周期（Epoch）包含 100 个 Episode。

### 5. 实验数量与充分性
*   **实验规模**：展示了训练过程中的成功率曲线（约 50 个 Epoch 达到收敛）。在测试阶段，针对摩擦力变化和时变目标分别进行了多组定性轨迹分析和定量对比。
*   **充分性评价**：实验设计较为直观地展示了分布偏移下的失效模式。通过对比 RL-only 和 ES-only 的失败案例，有力地证明了混合控制器的必要性。但论文缺乏大规模重复实验的统计显著性分析（如多个随机种子的平均成功率及标准差）。

### 6. 主要结论与发现
*   **RL 的局限性**：预训练的 RL 策略在进入高摩擦区域或面对移动目标时，无法维持有效的接触，导致物体脱离控制。
*   **ES 的局限性**：纯 ES 算法在没有先验知识的情况下，难以在广阔的搜索空间中发现初始的推挤方向或复杂的抓取动作。
*   **混合优势**：ES-DRL 成功结合了二者。RL 提供了快速的“任务切入”，而 Bounded ES 在接触后通过在线反馈补偿了摩擦力偏差和目标移动带来的误差。在 3D 目标跟踪任务中，ES-DRL 的跟踪精度显著优于 RL-only。

### 7. 优点
*   **无需微调**：在部署阶段不需要对神经网络进行梯度更新或微调，降低了计算负担和灾难性遗忘的风险。
*   **理论保障**：Bounded ES 具有收敛性证明，能够处理非光滑、不可微的系统，增强了控制系统的稳定性。
*   **互补性强**：巧妙地利用了接触传感器作为切换触发点，解决了 ES 搜索效率低和 RL 泛化性差的痛点。

### 8. 不足与局限
*   **切换逻辑单一**：目前的切换是单向的（RL $\rightarrow$ ES）。如果 ES 在操纵过程中丢失了接触，系统无法自动切回 RL 模式重新尝试抓取（作者在结论中提到这是未来的改进方向）。
*   **实验覆盖面有限**：仅测试了摩擦力和目标位置的偏移，未探讨物体质量、形状变化或机器人关节动力学改变等更复杂的分布偏移。
*   **实时性挑战**：虽然 ES 是在线更新，但在极高频的工业控制场景下，正弦扰动的频率选择和收敛速度可能成为瓶颈。

（完）
