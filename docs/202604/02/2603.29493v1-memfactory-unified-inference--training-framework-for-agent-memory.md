---
title: "MemFactory: Unified Inference & Training Framework for Agent Memory"
title_zh: MemFactory：智能体记忆的统一推理与训练框架
authors: "Ziliang Guo, Ziheng Li, Zhiyu Li"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29493v1"
tags: ["query:agent"]
score: 10.0
evidence: 用于强化学习优化智能体记忆操作的统一框架
tldr: "MemFactory是首个针对记忆增强型智能体设计的统一推理与训练框架。针对现有记忆管理实现碎片化且缺乏通用基础设施的问题，该框架借鉴LLaMA-Factory的模块化理念，将记忆生命周期抽象为可插拔的原子组件。它原生集成GRPO算法，支持通过环境奖励优化记忆策略，并兼容Memory-R1等前沿范式。实验表明，MemFactory在MemAgent架构上实现了最高14.8%的性能提升，显著降低了开发门槛。"
selection_source: fresh_fetch
motivation: 现有的记忆增强型大模型实现方案高度碎片化且缺乏统一的基础设施，限制了复杂记忆流水线的集成、训练与评估。
method: 提出模块化的MemFactory框架，将记忆操作抽象为“乐高式”组件，并利用GRPO算法实现多维环境奖励驱动的记忆管理策略微调。
result: "在MemAgent架构的实证评估中，MemFactory在域内和分布外测试集上均表现优异，相对基础模型最高提升了14.8%的性能。"
conclusion: MemFactory通过提供标准化、可扩展且易用的基础设施，为未来记忆驱动型AI智能体的研究与创新奠定了基础。
---

## 摘要
记忆增强的大语言模型（LLMs）对于开发具备强大能力的长期 AI 智能体至关重要。近期，应用强化学习（RL）来优化记忆操作（如提取、更新和检索）已成为一个极具前景的研究方向。然而，现有的实现方式仍然高度碎片化且针对特定任务，缺乏统一的基础设施来简化这些复杂流水线的集成、训练和评估。为填补这一空白，我们提出了 MemFactory，这是首个专为记忆增强智能体设计的统一、高度模块化的训练与推理框架。受 LLaMA-Factory 等统一微调框架成功的启发，MemFactory 将记忆生命周期抽象为原子化的、即插即用的组件，使研究人员能够通过“乐高式”架构无缝构建自定义记忆智能体。此外，该框架原生集成了群体相对策略优化（GRPO），以在多维环境奖励的驱动下微调内部记忆管理策略。MemFactory 为 Memory-R1、RMM 和 MemAgent 等近期前沿范式提供开箱即用的支持。我们在开源的 MemAgent 架构上，利用其公开的训练和评估数据对 MemFactory 进行了实证验证。在域内和分布外评估集上，MemFactory 相比相应的基座模型均能持续提升性能，相对增益最高达 14.8%。通过提供标准化、可扩展且易于使用的基础设施，MemFactory 显著降低了准入门槛，为记忆驱动型 AI 智能体的未来创新铺平了道路。

## Abstract
Memory-augmented Large Language Models (LLMs) are essential for developing capable, long-term AI agents. Recently, applying Reinforcement Learning (RL) to optimize memory operations, such as extraction, updating, and retrieval, has emerged as a highly promising research direction. However, existing implementations remain highly fragmented and task-specific, lacking a unified infrastructure to streamline the integration, training, and evaluation of these complex pipelines. To address this gap, we present MemFactory, the first unified, highly modular training and inference framework specifically designed for memory-augmented agents. Inspired by the success of unified fine-tuning frameworks like LLaMA-Factory, MemFactory abstracts the memory lifecycle into atomic, plug-and-play components, enabling researchers to seamlessly construct custom memory agents via a "Lego-like" architecture. Furthermore, the framework natively integrates Group Relative Policy Optimization (GRPO) to fine-tune internal memory management policies driven by multi-dimensional environmental rewards. MemFactory provides out-of-the-box support for recent cutting-edge paradigms, including Memory-R1, RMM, and MemAgent. We empirically validate MemFactory on the open-source MemAgent architecture using its publicly available training and evaluation data. Across both in-domain and out-of-distribution evaluation sets, MemFactory consistently improves performance over the corresponding base models, with relative gains of up to 14.8%. By providing a standardized, extensible, and easy-to-use infrastructure, MemFactory significantly lowers the barrier to entry, paving the way for future innovations in memory-driven AI agents.

---

## 论文详细总结（自动生成）

以下是对论文《MemFactory: Unified Inference & Training Framework for Agent Memory》的深度结构化总结：

### 1. 研究动机与核心问题
*   **研究背景**：为了让大语言模型（LLM）进化为具备长期记忆和个性化能力的 AI 智能体，记忆增强技术（如 RAG、启发式更新）至关重要。近期研究显示，利用强化学习（RL）优化记忆操作（提取、更新、检索）比传统启发式方法更有效。
*   **核心问题**：现有的“记忆-强化学习”（Memory-RL）研究高度碎片化，不同算法（如 Memory-R1, MemAgent, RMM）的实现代码深度耦合特定任务，缺乏像 LLaMA-Factory 那样统一、模块化且易用的基础设施，导致研究门槛高、复现困难。

### 2. 方法论：核心思想与技术细节
MemFactory 借鉴了“乐高式”模块化设计，将记忆生命周期抽象为四个核心层：
*   **模块层 (Module Layer)**：定义了原子化的记忆操作。
    *   **Extractor**：从原始上下文中解析事实和见解。
    *   **Updater**：执行增删改查（ADD, DEL, UPDATE, NONE）操作维护记忆库。
    *   **Retriever**：基于语义或重排序（Rerank）检索相关记忆。
    *   **Agent Module**：支持端到端的循环记忆策略（如 MemAgent 范式）。
*   **智能体层 (Agent Layer)**：作为策略执行器，研究者可以灵活组合上述模块构建自定义智能体。支持训练模式（rollout）和纯推理模式。
*   **环境层 (Environment Layer)**：负责数据加载和奖励计算。提供 `MemoryBankEnv`（长期记忆）和 `LongcontextEnv`（短时上下文）两种环境，支持格式奖励和 LLM-as-a-Judge 等多维评价。
*   **训练层 (Trainer Layer)**：核心优化引擎。
    *   **关键算法**：原生集成 **GRPO（群体相对策略优化）**。该算法通过组内奖励归一化估计基准，无需额外的 Critic 模型，显著降低了显存占用，适合长上下文训练。

### 3. 实验设计
*   **实验对象**：以开源的 **MemAgent** 架构为基础进行验证。
*   **基座模型**：Qwen3-1.7B 和 Qwen3-4B-Instruct。
*   **数据集/Benchmark**：使用 MemAgent 公开的训练和评估集。
    *   **主任务测试**：`eval_50` 和 `eval_100`（不同文档密度的任务）。
    *   **分布外（OOD）测试**：`eval_fwe_16384`（测试模型的泛化能力）。
*   **对比方法**：对比了基座模型（Base checkpoint）与经过 MemFactory RL 微调后的模型性能。

### 4. 资源与算力
*   **硬件配置**：论文明确指出，整个训练和评估流水线可以在 **单台 NVIDIA A800 80GB GPU** 上运行。
*   **训练细节**：每个模型训练 250 个有效步（valid steps）。
*   **算力评价**：该框架对硬件要求较低，具有极高的可复现性和科研友好性。

### 5. 实验数量与充分性
*   **实验规模**：针对两个不同规模的基座模型在三个测试集上进行了实验。
*   **客观性保障**：所有评分均基于 **4 次独立实验的平均值（avg@4）**，以减少生成随机性带来的偏差。
*   **充分性**：实验涵盖了域内（In-domain）和分布外（OOD）场景，验证了框架在提升性能和泛化性方面的有效性。虽然实验主要集中在 MemAgent 架构上，但框架本身已内置了对 Memory-R1 和 RMM 的支持。

### 6. 主要结论与发现
*   **性能提升**：MemFactory 显著提升了模型的记忆管理能力。Qwen3-1.7B 的平均性能提升了 **14.8%**；Qwen3-4B-Instruct 提升了 **7.3%**。
*   **模型规模效应**：较大的模型（4B）在 OOD 测试集上表现更稳健，显示出更好的策略迁移能力。
*   **框架有效性**：证明了即使在有限算力和简化训练数据下，通过统一框架进行 RL 优化也能显著增强长上下文任务的处理效果。

### 7. 优点与亮点
*   **统一性与模块化**：首个将记忆增强智能体的训练、推理和评估统一化的框架，极大降低了开发门槛。
*   **开箱即用**：内置支持多种 SOTA 记忆算法（Memory-R1, RMM, MemAgent），方便研究者直接对比。
*   **显存高效**：通过集成 GRPO 算法和 FlashAttention-2，解决了长上下文训练中显存易溢出的痛点。

### 8. 不足与局限
*   **范式覆盖有限**：虽然支持三大主流范式，但对于更复杂的分布式记忆或多智能体协同记忆的覆盖尚在完善中。
*   **训练效率**：论文提到在训练效率方面仍有进一步优化的空间。
*   **实验深度**：目前的实证研究主要集中在 MemAgent 这一种架构上，对于 Memory-R1 等其他内置范式的横向对比实验数据在文中体现较少。

（完）
