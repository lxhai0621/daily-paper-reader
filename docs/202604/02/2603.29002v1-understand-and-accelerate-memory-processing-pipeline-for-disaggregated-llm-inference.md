---
title: Understand and Accelerate Memory Processing Pipeline for Disaggregated LLM Inference
title_zh: 理解并加速解耦式大语言模型推理的内存处理流水线
authors: "Zifan He, Rui Ma, Yizhou Sun, Jason Cong"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.29002v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 加速长上下文大模型和RAG的内存处理
tldr: 针对长文本大模型推理中内存处理开销巨大的问题，本文提出了一个统一的四阶段内存处理流水线框架。通过系统分析发现，稀疏注意力、RAG等优化手段在GPU上存在显著的性能瓶颈。研究者提出了一种GPU-FPGA异构加速方案，将不规则且受内存限制的操作卸载到FPGA，从而显著提升了推理速度并降低了能耗，为未来异构硬件设计提供了重要参考。
selection_source: fresh_fetch
motivation: "现代大模型长文本优化技术在推理中产生了高达22%-97%的内存处理开销，且计算特征具有强异构性。"
method: 提出将内存处理统一为四个步骤，并利用GPU-FPGA异构系统，将稀疏、不规则及内存受限的操作卸载至FPGA执行。
result: 在AMD和NVIDIA平台上，该方案相比纯GPU基准实现了1.04至2.2倍的加速，并降低了1.11至4.7倍的能耗。
conclusion: 异构系统是解决大模型内存处理瓶颈的有效方向，对未来高效推理硬件的设计具有指导意义。
---

## 摘要
现代大语言模型（LLM）日益依赖高效的长上下文处理和生成机制，包括稀疏注意力、检索增强生成（RAG）以及压缩上下文内存，以支持复杂的推理。我们展示了这些优化可以统一为一个四步内存处理流水线：准备内存、计算相关性、检索以及应用于推理。通过系统性的性能分析，我们发现在 LLM 推理中存在 22%-97% 的内存处理开销，且其计算特性具有极强的异构性。基于这一发现，我们认为异构系统非常适合加速内存处理，从而优化端到端推理。我们在 GPU-FPGA 系统上演示了这一方法，将稀疏、不规则且访存受限的操作卸载到 FPGA，同时在 GPU 上保留计算密集型操作。在 AMD MI210 GPU 和 Alveo U55C FPGA 上的评估结果显示，在多种 LLM 推理优化场景下，我们的系统比 GPU 基准快 1.04~2.2 倍，且能耗降低 1.11~4.7 倍（在 NVIDIA A100 上也有类似结果）。这些结果确立了异构系统作为高效 LLM 内存处理的一个实际方向，并为未来的异构硬件设计提供了参考。

## Abstract
Modern large language models (LLMs) increasingly depends on efficient long-context processing and generation mechanisms, including sparse attention, retrieval-augmented generation (RAG), and compressed contextual memory, to support complex reasoning. We show that these optimizations can be unified into a four-step memory processing pipeline: Prepare Memory, Compute Relevancy, Retrieval, and Apply to Inference. Through systematic profiling, we identify a 22%-97% memory processing overhead in LLM inference and strong heterogeneity in its computational characteristics. Motivated by this insight, we argue that \textbf{heterogeneous systems} are well-suited to accelerate memory processing and thus end-to-end inference. We demonstrate this approach on a GPU-FPGA system by offloading sparse, irregular, and memory-bounded operations to FPGAs while retaining compute-intensive operations on GPUs. Evaluated on an AMD MI210 GPU and an Alveo U55C FPGA, our system is $1.04\sim2.2\times$ faster and requires $1.11\sim4.7\times$ less energy across multiple LLM inference optimizations than the GPU baseline (similar results hold on NVIDIA A100). These results establish heterogeneous systems as a practical direction for efficient LLM memory processing and inform future heterogeneous hardware design.

---

## 论文详细总结（自动生成）

### 论文总结：理解并加速解耦式大语言模型推理的内存处理流水线

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）向长上下文（128k至1M+ tokens）演进，KV缓存的存储和访问成为了性能瓶颈。虽然业界提出了稀疏注意力（Sparse Attention）、检索增强生成（RAG）和压缩上下文内存等优化技术，但这些技术在减少计算量的同时，引入了巨大的**内存处理（Memory Processing）**开销。
*   **痛点**：在长文本推理中，内存处理占总延迟的22%至97%。
*   **异构性**：这些操作（如Top-k搜索、BM25评分）通常是访存受限（Memory-bound）且计算模式不规则的，而GPU更擅长处理规则的计算密集型任务。
*   **目标**：通过异构系统（GPU+FPGA）协同工作，解决LLM推理中内存处理的效率问题。

#### 2. 论文提出的方法论
论文的核心思想是将多样化的LLM优化技术抽象为一个统一的**四阶段内存处理流水线**，并根据计算特性进行异构映射：
*   **统一流水线框架**：
    1.  **Prepare Memory（准备内存）**：预处理原始内存为紧凑或结构化格式（如线性投影、池化）。
    2.  **Compute Relevancy（计算相关性）**：为内存条目分配重要性分数（如内积、BM25）。
    3.  **Retrieval（检索）**：根据分数选择子集（如Top-k、阈值筛选）。
    4.  **Apply to Inference（应用于推理）**：将检索内容整合回解码过程。
*   **关键技术细节**：
    *   **异构任务分配**：将计算密集型、规则访问的操作保留在GPU；将稀疏、不规则、访存受限的操作（如相关性计算和检索）卸载到FPGA。
    *   **FPGA内核设计**：采用流式数据流（Streaming Dataflow）架构，利用FPGA内部的高带宽SRAM（BRAM/URAM）和HBM构建三级存储层次，实现细粒度的计算-通信重叠。
    *   **通信优化**：利用PCIe P2P（Peer-to-Peer）技术绕过系统内存，直接在GPU和FPGA之间交换索引和向量数据。

#### 3. 实验设计
*   **场景与方法对比**：
    *   **稀疏注意力**：对比了 DeepSeek Attention、SeerAttention-R、LServe。
    *   **RAG**：对比了单阶段（DRAGIN, FLARE, FS-RAG）和两阶段（带Reranker）检索。
    *   **压缩/合成内存**：对比了 Titans/HMT（Memory as Context）和 MemAgent。
*   **数据集**：Wikipedia dump（用于RAG）、长文本推理任务（最高支持1M tokens序列）。
*   **Benchmark**：以纯GPU实现（基于vLLM、TileLang等优化内核）作为基准。

#### 4. 资源与算力
*   **硬件配置**：
    *   **GPU**：AMD Instinct MI210 (64GB HBM2e, 1.6 TB/s带宽)。
    *   **FPGA**：AMD Alveo U55C (16GB HBM2, 460 GB/s带宽)。
    *   **CPU**：AMD EPYC 7v13 (64核)。
    *   **补充验证**：论文还通过性能预估分析了在 NVIDIA A100 上的表现。
*   **软件环境**：ROCm 6.3, Vitis HLS 2024.2, Vivado 2024.2。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从4K到1M tokens的不同序列长度，测试了从单Batch到Batch 32的扩展性。
*   **维度**：包括端到端延迟、内存处理阶段延迟、能耗（J/token 或 J/request）以及PCIe通信开销分析。
*   **客观性**：实验不仅展示了加速效果，也诚实地指出了FPGA在超大规模Batch（如MemAgent Batch > 2）或超长序列（>1M tokens导致FPGA HBM瓶颈）时的性能回退点，体现了科学的客观性。

#### 6. 主要结论与发现
*   **性能提升**：异构系统相比纯GPU基准实现了 **1.04× 至 2.2×** 的端到端加速。
*   **能效优势**：由于FPGA运行功率远低于GPU且处理不规则任务效率更高，系统能耗降低了 **1.11× 至 4.7×**。
*   **瓶颈转移**：通过卸载，原本在GPU上耗时最长的相关性计算和检索阶段得到了显著压缩，使内存处理不再是长文本推理的绝对瓶颈。
*   **异构必要性**：证明了单一硬件架构难以同时兼顾LLM的稠密计算和稀疏内存处理。

#### 7. 优点
*   **系统性强**：首次提出了统一的四阶段内存处理模型，能够涵盖稀疏注意力、RAG、TTT等多种前沿技术。
*   **软硬结合**：不仅有理论分析，还有基于商用FPGA和GPU的实际落地验证，具有很强的工程参考价值。
*   **能效卓越**：在当前大模型推理成本高昂的背景下，提供了极具吸引力的低功耗替代方案。

#### 8. 不足与局限
*   **硬件代差**：实验使用的 U55C FPGA（16nm）在工艺上落后于 MI210 GPU（6nm），虽然结果依然领先，但未能在同代工艺下对比。
*   **开发复杂度**：FPGA内核需要定制化设计，虽然论文提到了设计自动化，但目前对于新算法的适配仍存在较高的工程门槛。
*   **扩展性限制**：PCIe带宽在处理超大规模向量传输时可能成为潜在瓶颈，未来可能需要更高速的互联技术（如CXL）。
*   **Batch Size敏感**：对于某些计算密集型任务（如MemAgent），当Batch Size增大到一定程度后，GPU的吞吐量优势会反超FPGA。

（完）
