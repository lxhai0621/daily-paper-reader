---
title: Compressing Code Context for LLM-based Issue Resolution
title_zh: 面向基于大语言模型的问题修复的代码上下文压缩
authors: "Haoxiang Jia, Earl T. Barr, Sergey Mechtaev"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28119v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 压缩代码上下文以管理上下文窗口
tldr: 本研究针对LLM在处理GitHub问题时因代码上下文过长导致的成本高和噪声干扰问题，提出了一种新型压缩框架。该框架包含基于遗传算法和增量调试的OCD算法，用于提取最小修复上下文，并据此训练了轻量化模型SWEzze。SWEzze能在推理时过滤冗余信息并保留关键修复要素。实验表明，该方法在显著降低Token成本的同时，有效提升了LLM的问题解决率，实现了效率与性能的平衡。
selection_source: fresh_fetch
motivation: 解决LLM在修复GitHub问题时，因大规模代码输入带来的高昂成本以及冗余噪声对模型修复能力的干扰。
method: 提出OCD算法提取最小修复上下文，并训练轻量化模型SWEzze在推理阶段对代码进行语义感知的压缩。
result: "在SWE-bench上实现了约6倍的稳定压缩率，减少了51.8%-71.3%的Token预算，并提升了5.0%-9.2%的修复成功率。"
conclusion: SWEzze框架在压缩比、修复效果和推理延迟之间取得了最优平衡，证明了任务感知压缩对提升LLM代码处理能力的有效性。
---

## 摘要
大语言模型（LLMs）现已具备解决真实 GitHub 问题的能力。然而，当前的方法往往对代码上下文进行过度近似，并面临两个复合问题：处理海量输入的高昂成本，以及由于噪声充斥上下文窗口并干扰模型捕捉修复信号而导致的低效。现有的压缩技术未能解决这一矛盾：通用压缩器会损害代码的语义完整性，而特定于代码的工具则缺乏对代码结构和任务上下文的感知，无法保留关键的补丁要素。为此，我们提出了一个由两个组件组成的新型框架。首先是 Oracle 引导的代码蒸馏（OCD），这是一种结合了遗传搜索和增量调试（delta debugging）的上下文蒸馏算法，能够系统地将代码上下文缩减为最小充分子序列，仅保留成功修复所需的要素。我们利用这些蒸馏数据微调了 SWEzze，这是一个轻量级模型，能够在推理时学习压缩代码上下文，在过滤噪声和对抗干扰的同时保留修复要素。在 SWE-bench Verified 基准测试中，针对三种前沿 LLM 的评估结果显示，SWEzze 在不同模型上保持了约 6 倍的稳定压缩率，相对于未压缩设置减少了 51.8%-71.3% 的总 Token 预算，将问题修复率提高了 5.0%-9.2%，并且与最先进的上下文压缩基准相比，在有效性、压缩比和延迟之间实现了最佳的整体平衡。

## Abstract
Large Language Models (LLMs) are now capable of resolving real-world GitHub issues. However, current approaches overapproximate the code context and suffer from two compounding problems: the prohibitive cost of processing massive inputs, and low effectiveness as noise floods the context window and distracts the model from the bug-fixing signal. Existing compression techniques fail to resolve this tension: generic compressors compromise the semantic integrity of code, while code-specific tools lack awareness of code structure and task context to preserve essential patch ingredients. To address this, we propose a novel framework consisting of two components. First, Oracle-guided Code Distillation (OCD), a context distillation algorithm that combines genetic search and delta debugging to systematically reduce code contexts to their minimal sufficient subsequence - retaining only the ingredients required for a successful fix. We use this distilled data to fine-tune SWEzze, a lightweight model that learns to compress code context at inference time, filtering noise and combating distraction while preserving fix ingredients. Evaluated on SWE-bench Verified across three frontier LLMs, SWEzze maintains a stable compression rate of about 6 times across models, reduces the total token budget by 51.8%-71.3% relative to the uncompressed setting, improves issue resolution rates by 5.0%-9.2%, and delivers the best overall balance among effectiveness, compression ratio, and latency compared with state-of-the-art context compression baselines.

---

## 论文详细总结（自动生成）

### 论文总结：面向基于大语言模型的问题修复的代码上下文压缩

#### 1. 核心问题与研究动机
*   **核心问题**：在利用大语言模型（LLM）解决 GitHub 真实问题（Issue Resolution）时，现有的上下文构建方法存在“过度近似”的问题。
*   **研究动机**：
    *   **高昂成本**：处理包含大量文件、定义和调用链的海量输入会导致极高的推理开销。
    *   **噪声干扰**：无关代码会充斥上下文窗口，分散模型的注意力，导致修复效果下降（即“迷失在中间”或被噪声误导）。
    *   **现有技术局限**：通用压缩器会破坏代码语义；而现有的代码压缩器往往基于词法相似度，容易丢弃那些虽然与 Bug 描述不相似但对构造补丁至关重要的“修复要素”（Fix Ingredients）。

#### 2. 方法论
该论文提出了一个包含数据蒸馏和推理压缩两阶段的框架：
*   **Oracle 引导的代码蒸馏 (OCD)**：
    *   **核心思想**：通过一个计算代价高昂的“先验知识（Oracle）”来识别“最小充分上下文（MSC）”。
    *   **关键技术**：
        1.  **搜索空间表示**：将代码分解为文件、函数、块三个层级的结构单元，确保压缩后的代码在语法上基本完整。
        2.  **两阶段搜索策略**：首先利用**遗传算法 (GA)** 在指数级的组合空间中搜索能使模型成功修复问题的代码子集；随后利用**分层增量调试 (HDD)** 对该子集进行最小化剔除，直到删除任何一个单元都会导致修复失败（达到 1-最小化）。
        3.  **优先级引导**：利用测试覆盖率、补丁重合度和符号重合度为搜索提供启发式引导。
*   **SWEzze 压缩模型**：
    *   **核心思想**：利用 OCD 产生的高质量数据训练一个轻量级的推理模型。
    *   **技术细节**：基于 `Qwen3-Reranker-0.6B`，使用 **LoRA** 参数高效微调技术。模型学习将初始上下文映射到 MSC，在推理时通过对代码段进行评分并按预算贪婪选择，实现无需 Oracle 介入的实时压缩。

#### 3. 实验设计
*   **数据集**：
    *   **训练集**：基于 `SWE-Smith` 数据集，通过 OCD 蒸馏出 3,157 个实例的最小充分上下文。
    *   **测试集**：`SWE-bench Verified`（包含 500 个真实的 Python 项目问题）。
*   **下游修复模型（LLMs）**：GPT-5.2、DeepSeek-V3.2、Qwen3-Coder-Next。
*   **对比方法（Baselines）**：
    *   无压缩（No_compression）
    *   无上下文（No_context，仅提供 Bug 位置）
    *   通用压缩器：LLMLingua-2
    *   代码专用压缩器：LongCodeZip、SWE-Pruner

#### 4. 资源与算力
*   **训练细节**：使用了 LoRA 微调（Rank=16, Alpha=32），训练 3 个 Epoch，有效 Batch Size 为 16。
*   **算力说明**：文中提到训练在“消费级 GPU 显存（commodity GPU memory）”内即可完成，使用了混合精度（bfloat16）和梯度检查点技术。
*   **实验成本**：作者提到整个实验的总成本超过了 **2800 美元**（主要用于调用前沿 LLM 的 API 进行评估和 OCD 蒸馏）。

#### 5. 实验数量与充分性
*   **实验规模**：在 500 个真实问题上针对 3 种不同的顶级 LLM 进行了全量对比实验。
*   **消融实验**：对 OCD 中的遗传算法（GA）进行了消融分析，证明不使用 GA 会导致成功最小化的实例减少 52.7%。
*   **客观性**：实验涵盖了不同架构、不同厂商的模型，并使用了 UpSet 图分析了不同方法解决问题的交集，证明了 SWEzze 的提升具有普适性且覆盖了更多难题。

#### 6. 主要结论与发现
*   **性能提升**：SWEzze 在三种模型上将问题修复率提高了 **5.0%–9.2%**。
*   **成本削减**：实现了约 **6 倍** 的稳定压缩率，总 Token 预算减少了 **51.8%–71.3%**。
*   **效率平衡**：虽然推理速度慢于极轻量级的 LLMLingua-2，但比 LongCodeZip 快约 **4 倍**，在效果、压缩比和延迟之间取得了最佳平衡。
*   **核心发现**：对于代码修复任务，保留“修复充分性”比单纯追求“压缩率”或“语义相似度”更重要。

#### 7. 优点
*   **任务感知性强**：通过 OCD 捕获了代码间的深层功能依赖，而非表面的文本相似性。
*   **模型无关性**：在一种模型（Qwen3-30B）上蒸馏的数据，能够显著提升其他模型（如 GPT-5.2）的修复表现。
*   **结构保护**：采用分层占位符（如 `# ... N lines omitted`）而非暴力截断，维持了代码的骨架。

#### 8. 不足与局限
*   **语义识别精度**：在处理具有相同签名或高度相似逻辑的重写函数时，模型有时难以精确区分哪个才是真正的修复要素。
*   **语法破坏风险**：尽管采用了分层处理，但在极端压缩下仍可能产生碎片化的上下文，影响 LLM 对整体逻辑的理解。
*   **数据分布偏差**：OCD 依赖于现有的测试套件和正确补丁，如果训练数据中的 Bug 类型与测试集差异巨大，压缩效果可能会受限。

（完）
