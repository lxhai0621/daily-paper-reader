---
title: "SCPatcher: Automated Smart Contract Code Repair via Retrieval-Augmented Generation and Knowledge Graph"
title_zh: SCPatcher：通过检索增强生成与知识图谱实现的自动化智能合约代码修复
authors: "Xiaoqi Li, Shipeng Ye, Wenkai Li, Zongwei Li"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00687v1"
tags: ["query:ma-kf"]
score: 10.0
evidence: 将检索增强生成与知识图谱相结合
tldr: 针对智能合约部署后不可更改且漏洞修复困难的问题，本文提出SCPatcher框架。该框架结合检索增强生成（RAG）与知识图谱，利用从5000个以太坊合约中提取的语义网络作为外部知识库，增强大语言模型的推理能力。通过两阶段修复策略（知识引导修复与思维链推理），SCPatcher在漏洞修复率和编译通过率上均显著优于现有方法，为智能合约安全提供了高效的自动化修复方案。
selection_source: fresh_fetch
motivation: 智能合约漏洞会导致巨大经济损失，但现有工具多侧重于检测而非自动修复。
method: 构建基于5000个合约的知识图谱，并结合检索增强生成与思维链推理实现两阶段自动化修复。
result: "实验显示SCPatcher实现了81.5%的整体修复率和91.0%的编译通过率，性能大幅领先现有技术。"
conclusion: 结合知识图谱与大语言模型的RAG方法能有效提升智能合约漏洞修复的准确性和可靠性。
---

## 摘要
由于代码部署后的不可篡改性，智能合约漏洞可能导致巨大的经济损失。虽然现有工具可以检测漏洞，但无法有效地修复它们。在本文中，我们提出了 SCPatcher，这是一个结合了检索增强生成与知识图谱的自动化智能合约修复框架。我们从 5,000 个经过验证的以太坊合约中构建了一个知识图谱，提取函数级关系以构建语义网络。该图谱作为外部知识库，增强了大语言模型的推理能力，并实现了精确的漏洞补丁修复。我们引入了一种两阶段修复策略：首先是初始的知识引导修复，随后是针对复杂漏洞的思维链（Chain-of-Thought）推理。在多种漏洞合约集上的评估结果显示，SCPatcher 实现了 81.5% 的整体修复率和 91.0% 的编译通过率，显著优于现有方法。

## Abstract
Smart contract vulnerabilities can cause substantial financial losses due to the immutability of code after deployment. While existing tools detect vulnerabilities, they cannot effectively repair them. In this paper, we propose SCPatcher, a framework that combines retrieval-augmented generation with a knowledge graph for automated smart contract repair. We construct a knowledge graph from 5,000 verified Ethereum contracts, extracting function-level relationships to build a semantic network. This graph serves as an external knowledge base that enhances Large Language Model reasoning and enables precise vulnerability patching. We introduce a two-stage repair strategy, initial knowledge-guided repair followed by Chain-of-Thought reasoning for complex vulnerabilities. Evaluated on a diverse set of vulnerable contracts, SCPatcher achieves 81.5\% overall repair rate and 91.0\% compilation pass rate, substantially outperforming existing methods.

---

## 论文详细总结（自动生成）

### SCPatcher：通过检索增强生成与知识图谱实现的自动化智能合约代码修复

#### 1. 核心问题与整体含义
*   **研究动机**：智能合约一旦部署在区块链上便不可更改，漏洞会导致巨大的经济损失（如2025年损失超过20亿美元）。
*   **核心挑战**：虽然现有工具（如Slither、Oyente）能检测漏洞，但修复工作仍依赖人工，耗时且易错。大语言模型（LLM）虽有代码生成潜力，但在处理特定领域的安全模式时容易产生“幻觉”，生成语法正确但逻辑错误的补丁。
*   **研究目标**：提出SCPatcher框架，通过结合知识图谱（KG）和检索增强生成（RAG），为LLM提供结构化的外部知识，从而实现精确、自动化的智能合约漏洞修复。

#### 2. 方法论
SCPatcher的核心思想是利用结构化知识引导LLM生成补丁，主要包含以下关键技术：
*   **知识图谱构建**：从Etherscan抓取5,000个已验证合约，利用静态分析工具Slither提取实体（合约、函数、变量等）及其关系（调用、拥有、返回等），构建语义网络。使用CodeBERT将节点嵌入为向量，存入向量数据库。
*   **混合检索与多阶段重排序**：
    *   **语义检索**：通过向量相似度（L2距离）检索Top-50候选补丁。
    *   **语法过滤**：剔除与目标函数签名不兼容的候选者。
    *   **信任评分（Trust Scoring）**：基于全局使用频率（GUF）对候选者进行加权评分，公式为：$S_{final} = S_{sem} / \ln(f_{freq} + \epsilon)$。
    *   **多样性去重**：从不同的语义克隆组中选择Top-k，确保上下文丰富。
*   **两阶段修复策略**：
    *   **第一阶段（知识引导）**：将检索到的高质量代码模式作为Prompt输入DeepSeek-V3生成补丁。
    *   **第二阶段（CoT细化）**：若第一阶段修复失败（未通过Slither验证），则启动思维链（Chain-of-Thought）推理，引导模型逐步分析逻辑依赖并重新生成。
*   **自动化验证**：所有生成的补丁均通过Slither进行静态检查，确保漏洞被消除且未引入新问题。

#### 3. 实验设计
*   **数据集**：
    *   **知识库**：5,000个以太坊验证合约。
    *   **测试集**：200个包含重入、整数溢出、访问控制、时间戳篡改、未检查调用返回值等5类典型漏洞的合约（源自SmartBugs及开源仓库）。
*   **Benchmark与对比方法**：
    *   **DirectFix**：基于直觉的直接修复。
    *   **LogicRepair**：基于推理的逻辑修复。
    *   **SelfRefine**：基于自我反馈的迭代修复。
*   **评估指标**：编译通过率（CPR）、有效修复率（ERR，指编译通过中的修复成功率）、整体修复率（ORR）。

#### 4. 资源与算力
*   **硬件环境**：单台机器，配备 NVIDIA GeForce RTX 4070Ti GPU (12GB) 和 Intel i7-12700 CPU。
*   **模型**：主要调用 DeepSeek-V3。
*   **训练/运行时间**：文中未明确提及具体的训练或推理总时长，但由于是基于RAG的框架，主要开销在于知识图谱的预构建和在线检索推理。

#### 5. 实验数量与充分性
*   **实验规模**：针对200个真实漏洞合约进行了测试，涵盖了主流漏洞类型。
*   **消融实验**：对比了纯LLM生成、标准RAG、无CoT的SCPatcher以及完整版SCPatcher，验证了各模块（尤其是KG和CoT）的有效性。
*   **敏感性分析**：测试了检索数量 $K$（1, 3, 5）对性能的影响，发现 $K=3$ 时效果最优。
*   **客观性保障**：通过代码哈希对测试集和知识库进行了严格去重，防止数据泄露。实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **性能卓越**：SCPatcher实现了 **81.5% 的整体修复率（ORR）** 和 **91.0% 的编译通过率（CPR）**，比基准方法提升了12%至36%。
*   **外部知识的必要性**：消融实验显示，标准RAG（无结构化知识）表现极差（28% ORR），证明了领域特定、结构化的知识图谱在修复任务中的关键作用。
*   **自我修正的局限**：实验发现，没有外部引导的自我迭代（如SelfRefine）容易导致过度修正，性能反而不如知识引导的方法。

#### 7. 优点
*   **首创性**：据作者称，这是首个系统性结合RAG与知识图谱进行智能合约修复的研究。
*   **结构化语义增强**：不同于传统的文本检索，它利用代码的函数级关系和信任度指标，显著降低了LLM的幻觉。
*   **两阶段策略**：结合了“模式匹配（RAG）”与“逻辑推理（CoT）”的优点，处理复杂漏洞的能力更强。

#### 8. 不足与局限
*   **静态分析依赖**：修复效果高度依赖于Slither的检测能力，可能存在“补丁过拟合”风险（即通过了静态检测但逻辑仍有误）。
*   **粒度限制**：目前的知识图谱主要在函数级别，对于涉及跨合约调用或深层业务逻辑的复杂漏洞，修复能力可能受限。
*   **动态验证缺失**：目前仅通过静态工具验证，缺乏动态执行和测试用例的覆盖，无法完全保证行为的正确性。

（完）
