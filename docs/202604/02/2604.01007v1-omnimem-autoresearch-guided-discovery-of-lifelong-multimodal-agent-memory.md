---
title: "OmniMem: Autoresearch-Guided Discovery of Lifelong Multimodal Agent Memory"
title_zh: OmniMem：自动研究引导的终身多模态智能体记忆发现
authors: "Jiaqi Liu, Zipeng Ling, Shi Qiu, Yanqing Liu, Siwei Han, Peng Xia, Haoqin Tu, Zeyu Zheng, Cihang Xie, Charles Fleming, Mingyu Ding, Huaxiu Yao"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01007v1"
tags: ["query:agent"]
score: 10.0
evidence: 终身AI智能体的多模态记忆框架
tldr: "本研究针对AI智能体在长周期运行中多模态记忆保留与检索的瓶颈，利用自主研究流水线开发了OmniMem框架。该流水线在无人工干预下执行了约50次实验，涵盖架构修改、Bug修复和提示词工程。OmniMem在LoCoMo和Mem-Gallery基准测试中分别实现了411%和214%的F1值提升，证明了自主研究在处理复杂系统设计空间时优于传统AutoML的能力。"
selection_source: fresh_fetch
motivation: 多模态智能体在长期记忆的组织与检索方面存在瓶颈，且其庞大的设计空间难以通过人工或传统AutoML有效探索。
method: 部署自主研究流水线，通过自动诊断故障、修改架构、修复Bug及优化提示词，迭代发现并构建了统一的多模态记忆框架OmniMem。
result: "OmniMem在LoCoMo和Mem-Gallery基准上均达到SOTA水平，F1分数较初始基准分别大幅提升了411%和214%。"
conclusion: 自主研究流水线能够有效处理超越超参数调优的复杂AI系统设计任务，为多模态记忆及其他AI领域的自动化发现提供了新范式。
---

## 摘要
AI 智能体越来越多地在长周期内运行，但其保留、组织和召回多模态经验的能力仍是一个关键瓶颈。构建有效的终身记忆需要探索涵盖架构、检索策略、提示工程和数据流水线的庞大设计空间；这一空间过于庞大且相互关联，手动探索或传统的自动机器学习（AutoML）难以有效应对。我们部署了一个自主研究流水线来发现 OmniMem，这是一个用于终身 AI 智能体的统一多模态记忆框架。从一个朴素的基准（LoCoMo 上的 F1=0.117）开始，该流水线在两个基准测试中自主执行了约 50 次实验，诊断故障模式、提出架构修改并修复数据流水线漏洞，整个内环过程无需人工干预。最终系统在两个基准测试上均达到了当前最优水平（SOTA），相对于初始配置，LoCoMo 上的 F1 分数提升了 411%（0.117 提升至 0.598），Mem-Gallery 上的 F1 分数提升了 214%（0.254 提升至 0.797）。关键在于，最具影响力的发现并非超参数调整：漏洞修复（+175%）、架构变更（+44%）和提示工程（特定类别下 +188%）各自的贡献均超过了所有超参数调优的累积贡献，展示了从根本上超越传统 AutoML 能力范围的实力。我们提供了六种发现类型的分类法，并确定了使多模态记忆特别适合自动研究的四种属性，为将自主研究流水线应用于其他 AI 系统领域提供了指导。代码可在 https://github.com/aiming-lab/OmniMem 获取。

## Abstract
AI agents increasingly operate over extended time horizons, yet their ability to retain, organize, and recall multimodal experiences remains a critical bottleneck. Building effective lifelong memory requires navigating a vast design space spanning architecture, retrieval strategies, prompt engineering, and data pipelines; this space is too large and interconnected for manual exploration or traditional AutoML to explore effectively. We deploy an autonomous research pipeline to discover OmniMem, a unified multimodal memory framework for lifelong AI agents. Starting from a naïve baseline (F1=0.117 on LoCoMo), the pipeline autonomously executes ${\sim}50$ experiments across two benchmarks, diagnosing failure modes, proposing architectural modifications, and repairing data pipeline bugs, all without human intervention in the inner loop. The resulting system achieves state-of-the-art on both benchmarks, improving F1 by +411% on LoCoMo (0.117$\to$0.598) and +214% on Mem-Gallery (0.254$\to$0.797) relative to the initial configurations. Critically, the most impactful discoveries are not hyperparameter adjustments: bug fixes (+175%), architectural changes (+44%), and prompt engineering (+188\% on specific categories) each individually exceed the cumulative contribution of all hyperparameter tuning, demonstrating capabilities fundamentally beyond the reach of traditional AutoML. We provide a taxonomy of six discovery types and identify four properties that make multimodal memory particularly suited for autoresearch, offering guidance for applying autonomous research pipelines to other AI system domains. Code is available at this https://github.com/aiming-lab/OmniMem.

---

## 论文详细总结（自动生成）

以下是对论文《OmniMem: Autoresearch-Guided Discovery of Lifelong Multimodal Agent Memory》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：AI 智能体在长周期运行中，如何有效保留、组织和召回跨模态（文本、图像、音频、视频）的经验。
*   **研究背景**：构建终身多模态记忆系统涉及极其庞大的设计空间（包括架构设计、检索策略、提示工程和数据流水线）。
*   **动机**：
    *   **人工局限**：人类研究员手动迭代速度慢，且容易忽略组件间的复杂耦合。
    *   **传统 AutoML 局限**：仅能优化数值超参数，无法进行代码理解、Bug 诊断、架构重构或跨组件推理。
    *   **目标**：利用自主研究（Autoresearch）流水线，在无需人工干预的情况下，从零开始发现并优化出一个高性能的多模态记忆框架。

### 2. 方法论：核心思想与关键技术
OmniMem 是通过 **AutoResearchClaw**（一个 23 阶段的自主研究流水线）自动发现的，其核心架构遵循三大原则：
*   **选择性摄入（Selective Ingestion）**：
    *   **冗余过滤**：利用轻量级感知编码器（如 CLIP 视觉特征、VAD 语音检测、Jaccard 文本重叠）在存储前剔除重复信息。
    *   **多模态原子单元（MAU）**：将记忆表示为 $M = \langle s, e, p, \tau, m, \ell \rangle$（摘要、嵌入、原始数据指针、时间戳、模态、结构链接），实现“热存储”（元数据）与“冷存储”（原始大文件）的分离。
*   **渐进式检索与混合搜索（Progressive Retrieval）**：
    *   **混合搜索**：结合了 FAISS 稠密向量检索和 BM25 稀疏关键词匹配。流水线自主发现“集合并集（Set-union）”合并策略优于传统的得分加权融合。
    *   **金字塔扩展机制**：根据 Token 预算分阶段加载信息：Level 1（摘要）→ Level 2（全文/详细描述）→ Level 3（原始多模态证据）。
*   **知识图谱增强（KG-Augmented）**：
    *   自动提取 7 类实体（人、地点、事件等）和关系，构建全局知识图谱。
    *   通过 $h$-hop 邻域扩展解决需要跨会话推理的多跳查询问题。

### 3. 实验设计
*   **数据集/基准（Benchmarks）**：
    1.  **LoCoMo**：长周期对话记忆基准，包含单跳、多跳、时间推理等 5 类问题。
    2.  **Mem-Gallery**：多模态长对话记忆基准，涉及图像接地（Grounded images）和社交互动推理。
*   **对比方法**：
    *   包括 **MemVerse**（分层多模态图记忆）、**Mem0**（动态事实提取）、**Claude-Mem**（商用嵌入记忆）、**A-MEM**（智能体管理记忆）、**MemGPT**（类操作系统内存管理）以及 **SimpleMem**（本文的初始基准）。
*   **评估指标**：主要使用 Token 级别的 F1 分数、精确匹配（EM）和 BLEU 分数。

### 4. 资源与算力
*   **算力消耗**：论文提到自主研究流水线在 **约 72 小时的墙钟时间（Wall-clock time）** 内完成了约 50 次实验。
*   **硬件设备**：文中提到流水线具备自动检测硬件（GPU 类型、显存）的能力，并在实验中使用了 8 个并行 worker 来提升吞吐量。
*   **模型后端**：使用了 GPT-4o、GPT-4o-mini、GPT-5.1（预研版本）等多种大语言模型作为推理后端。
*   **明确说明**：文中**未明确给出**具体的 GPU 型号（如 A100 或 H100）的具体数量，但强调了其搜索效率远高于人类研究员（人类完成同等工作量约需 4 周）。

### 5. 实验数量与充分性
*   **实验规模**：流水线在 LoCoMo 上进行了 9 次成功迭代，在 Mem-Gallery 上进行了 39 次实验（涵盖 7 个阶段）。
*   **充分性**：
    *   **消融实验**：针对金字塔扩展、BM25 混合检索、LLM 摘要等核心组件进行了详细的消融分析。
    *   **跨模型验证**：在 5 种不同的 LLM 后端上验证了 OmniMem 的泛化性。
    *   **效率测试**：对比了不同系统在查询吞吐量（QPS）和延迟上的表现。
*   **客观性**：实验采用了自动化的“Proceed/Pivot/Iterate”决策机制，并包含自动回滚（Revert）失败尝试的逻辑，减少了人为偏见。

### 6. 主要结论与发现
*   **性能飞跃**：OmniMem 在 LoCoMo 上 F1 提升了 **411%**，在 Mem-Gallery 上提升了 **214%**，均创下 SOTA。
*   **非参数化发现的价值**：研究发现，对性能贡献最大的是 **Bug 修复（+175%）**、**架构变更（+44%）** 和 **提示工程（+188%）**，而非传统的超参数微调。
*   **自主研究的优势**：流水线能够自主诊断出如“API 缺少 response_format 参数”或“时间戳损坏”等深层系统问题并自动修复。
*   **多模态适配性**：多模态记忆领域因其具有即时标量反馈、模块化架构和快速迭代周期，被证明非常适合自主研究范式。

### 7. 优点与亮点
*   **全自动闭环**：展示了从朴素基准到 SOTA 系统的全自动进化过程，无需人工编写模板。
*   **系统性创新**：提出了 MAU 和金字塔检索机制，有效平衡了存储成本、检索精度和上下文窗口限制。
*   **自愈能力**：流水线具备代码级自愈功能，能处理 API 错误、依赖冲突和输出格式不匹配。
*   **高效性**：通过并行化检索-生成流水线，吞吐量比最强基准快 3.5 倍。

### 8. 不足与局限
*   **依赖高性能 LLM**：自主研究流水线本身高度依赖于底层 LLM（如 GPT-4o）的推理能力，若后端模型能力不足，可能无法发现复杂的架构改进。
*   **隐私风险**：终身记忆系统涉及大量个人多模态数据，论文虽有提及但未深入探讨在加密环境下的自主优化。
*   **基准局限**：虽然在两个主流基准上表现优异，但在更极端的长文本（如数百万 Token）或更复杂的实时具身智能场景下的表现尚待验证。
*   **成本波动**：虽然节省了人力，但大规模调用高性能 LLM API 进行自主搜索的经济成本较高。

（完）
