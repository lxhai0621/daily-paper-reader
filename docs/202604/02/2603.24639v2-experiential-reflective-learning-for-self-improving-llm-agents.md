---
title: Experiential Reflective Learning for Self-Improving LLM Agents
title_zh: 针对自我改进型 LLM 智能体的经验反思学习
authors: "Marc-Antoine Allard, Arnaud Teinturier, Victor Xing, Gautier Viaud"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24639v2"
tags: ["query:agent"]
score: 9.0
evidence: 利用过去交互和启发式方法的自改进LLM智能体
tldr: "针对LLM智能体难以利用过去经验的问题，本文提出经验反思学习（ERL）框架。该框架通过反思任务轨迹生成可迁移的启发式策略，并在测试时动态检索相关经验注入上下文。在Gaia2基准测试中，ERL比ReAct基线成功率提升7.8%，证明了提取抽象启发式经验能有效增强智能体的自我提升与环境适应能力。"
selection_source: fresh_fetch
motivation: 现有LLM智能体无法从过去交互中学习，导致在处理新任务时缺乏环境适应能力且重复犯错。
method: 提出ERL框架，通过反思任务执行过程生成启发式经验，并在推理时动态检索相关策略以指导执行。
result: 在Gaia2基准上，ERL显著提升了成功率和可靠性，且表现优于传统的少样本轨迹提示方法。
conclusion: 从单次尝试中提取可迁移的启发式策略是实现智能体高效自我改进和环境适应的有效手段。
---

## 摘要
大语言模型（LLM）的最新进展使得开发能够进行复杂推理和多步问题解决的自主智能体成为可能。然而，这些智能体难以适应专门的环境，且未能利用过去的交互经验，无论积累了多少经验，在面对新任务时往往都从零开始。我们提出了经验反思学习（Experiential Reflective Learning, ERL），这是一种简单的自我改进框架，通过经验学习实现快速的环境适应。ERL 通过反思任务轨迹和结果来生成启发式策略，捕捉可跨任务迁移的、具有操作性的经验教训。在测试阶段，系统根据当前任务检索相关的启发式策略，并将其注入智能体的上下文以指导执行。在 Gaia2 基准测试中，ERL 相比 ReAct 基线将成功率提高了 7.8%，在任务完成可靠性方面有显著提升，并优于先前的经验学习方法。通过系统的消融实验，我们发现选择性检索至关重要，且启发式策略比少样本轨迹提示提供了更具迁移性的抽象。这些结果表明，通过反思单次尝试的经验来提取可迁移的启发式策略，能够实现有效的智能体自我改进。

## Abstract
Recent advances in large language models (LLMs) have enabled the development of autonomous agents capable of complex reasoning and multi-step problem solving. However, these agents struggle to adapt to specialized environments and do not leverage past interactions, approaching each new task from scratch regardless of their accumulated experience. We introduce Experiential Reflective Learning (ERL), a simple self-improvement framework that enables rapid environment adaptation through experiential learning. ERL reflects on task trajectories and outcomes to generate heuristics, capturing actionable lessons that transfer across tasks. At test time, relevant heuristics are retrieved based on the current task and injected into the agent's context to guide execution. On the Gaia2 benchmark, ERL improves success rate by 7.8% over a ReAct baseline, with large gains in task completion reliability, and outperforms prior experiential learning methods. Through systematic ablations, we find that selective retrieval is essential and that heuristics provide more transferable abstractions than few-shot trajectory prompting. These results demonstrate that reflecting on single-attempt experiences to extract transferable heuristics enables effective agent self-improvement.

---

## 论文详细总结（自动生成）

以下是对论文《Experiential Reflective Learning for Self-Improving LLM Agents》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管 LLM 智能体在复杂推理上表现出色，但它们在面对特定环境（如专用工具、领域规范）时缺乏适应能力。现有的智能体通常“过目即忘”，无法从过去的交互中积累经验，导致在相似任务中重复犯错。
*   **核心挑战**：微调（Fine-tuning）成本高且不支持持续学习；现有的经验学习方法（如 ExpeL、AutoGuide）往往需要对同一任务进行多次尝试（对比成功与失败）才能提取知识，且在推理时检索效率低下或缺乏针对性。
*   **整体含义**：本文提出 **ERL（经验反思学习）** 框架，旨在通过对单次任务轨迹的“反思”生成抽象的启发式策略（Heuristics），并在新任务中动态注入相关策略，实现无需参数更新的自我改进。

### 2. 方法论
ERL 包含两个核心阶段：
*   **启发式生成（Heuristic Generation）**：
    *   智能体在完成任务后（无论成功或失败），会根据任务描述、执行轨迹（推理步骤、工具调用）和环境反馈（Binary Reward）进行反思。
    *   生成**结构化启发式**：包含“分析”（定位失败断点或成功关键点）和“学习指南”（明确触发条件 Trigger 和建议动作 Action）。
*   **检索增强执行（Retrieval-augmented Execution）**：
    *   面对新任务时，系统不再盲目注入所有经验，而是使用 LLM 作为评分器，根据任务相似性、经验多样性和指南信息量，从启发式池中检索 Top-k（实验中 k=20）条策略。
    *   将这些策略注入智能体的系统提示词（System Prompt）中，指导其后续执行。

### 3. 实验设计
*   **数据集/场景**：
    *   **Gaia2**：核心基准，包含 12 个应用和 101 个工具。实验专注于“搜索（Search）”和“执行（Execution）”分片。
    *   **$\tau^2$-bench**：用于评估双控制（智能体与用户协作）场景下的表现，涵盖航空、零售、电信领域。
*   **Benchmark 与对比方法**：
    *   **基线**：标准的 ReAct 智能体框架。
    *   **对比方法**：ExpeL（对比学习）、AutoGuide（上下文感知指南）、Few-shot（直接提供原始轨迹作为示例）。
*   **实验设置**：利用 Gaia2 的“宇宙（Universes）”机制，在 8 个独立数据分区上积累经验，在 2 个完全隔离的分区上测试，确保无知识泄露。

### 4. 资源与算力
*   **模型使用**：实验主要基于 OpenAI 的 API。智能体骨干模型为 **GPT-5-mini**，启发式检索模型为 **GPT-5.2**，嵌入模型使用了 **Qwen3-Embedding-0.6B**。
*   **算力说明**：文中**未明确提及**具体的物理 GPU 型号或训练时长，因为该方法是“无参数（Parameter-free）”的，主要开销在于 API 调用。
*   **成本分析**：论文提供了详细的 Token 消耗统计。ERL 相比基线增加了约 85% 的输入 Token（主要由于注入了约 20k Token 的启发式策略），最终导致 API 总成本增加了约 **40%**。

### 5. 实验数量与充分性
*   **实验规模**：在 Gaia2 上进行了多次运行（3-run average），并计算了 Pass@3 和 Pass^3（可靠性指标）。
*   **消融实验**：
    *   对比了 LLM 检索、Embedding 检索与随机选择的效果。
    *   分析了启发式数量 $k$ 对性能的影响。
    *   探讨了仅从“成功经验”学习与仅从“失败经验”学习的差异。
    *   测试了在无环境奖励信号（智能体自我评估结果）下的鲁棒性。
*   **充分性评价**：实验设计较为严谨，通过跨基准（Gaia2 和 $\tau^2$-bench）验证了泛化性，消融实验覆盖了框架的核心变量，客观地展示了方法的优劣。

### 6. 主要结论与发现
*   **性能提升**：ERL 在 Gaia2 上比 ReAct 基线成功率提高 **7.8%**，且显著优于 ExpeL 和 AutoGuide。
*   **可靠性增强**：ERL 在 Pass^3 指标上提升显著（执行类 +8.3%，搜索类 +10.6%），说明经验能让智能体表现更稳定。
*   **抽象胜于原始**：启发式策略比原始轨迹（Few-shot）更具迁移性，因为原始轨迹包含过多特定数据的噪音。
*   **反馈来源差异**：**失败经验**对搜索类任务帮助最大（提供负面约束），而**成功经验**对执行类任务更有利（强化证明有效的步骤）。
*   **检索至关重要**：LLM 检索优于 Embedding 检索，且盲目增加启发式数量会导致性能下降（存在非线性关系）。

### 7. 优点
*   **高效学习**：支持从单次尝试中学习，不需要像强化学习或某些反思方法那样反复重试同一任务。
*   **可解释性强**：生成的启发式策略是自然语言形式，人类可读且易于调试。
*   **即插即用**：无需对模型进行微调，适用于闭源模型，且能随着经验积累持续进化。

### 8. 不足与局限
*   **推理开销**：注入大量启发式策略显著增加了上下文长度，导致推理成本和延迟上升。
*   **依赖奖励信号**：虽然在无奖励时仍有提升，但准确的环境反馈对生成高质量启发式至关重要。
*   **复杂交互限制**：在 $\tau^2$-bench 的电信领域表现不佳，说明该方法在处理高度随机的用户协作和组合式子任务时仍有局限。
*   **冲突处理**：目前尚未深入探讨当启发式池中出现相互矛盾的指南时，系统应如何消解冲突。

（完）
