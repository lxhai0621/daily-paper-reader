---
title: "PAR$^2$-RAG: Planned Active Retrieval and Reasoning for Multi-Hop Question Answering"
title_zh: PAR$^2$-RAG：面向多跳问答的规划式主动检索与推理
authors: "Xingyu Li, Rongguang Wang, Yuying Wang, Mengqing Guo, Chenyang Li, Tao Sheng, Sujith Ravi, Dan Roth"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.29085v1"
tags: ["query:ma-kf"]
score: 10.0
evidence: 用于多跳问答的规划式主动检索与推理
tldr: "针对大语言模型在多跳问答中因检索路径偏差或规划僵化导致的性能瓶颈，本文提出 PAR$^2$-RAG 框架。该方法采用“覆盖与承诺分离”的策略，通过两阶段流程实现：首先进行广度优先锚定以构建高召回率证据前沿，随后通过带证据充分性控制的深度优先迭代进行精炼。实验表明，该方法在多个基准测试上显著优于现有 SOTA 模型，准确率提升高达 23.5%，有效增强了复杂推理能力。"
selection_source: fresh_fetch
motivation: 现有的多跳问答方法在迭代检索时易陷入错误路径，或因静态规划无法根据中间证据灵活调整。
method: 提出一种两阶段框架，先通过广度优先锚定确保证据覆盖，再利用深度优先精炼和证据充分性控制进行推理。
result: "在四个 MHQA 基准测试中，PAR$^2$-RAG 的准确率比 IRCoT 高出 23.5%，检索指标 NDCG 提升了 10.5%。"
conclusion: PAR$^2$-RAG 通过分离覆盖与承诺，有效解决了多跳问答中的检索漂移和规划僵化问题，显著提升了复杂推理任务的性能。
---

## 摘要
大语言模型（LLMs）在多跳问答（MHQA）中仍表现脆弱，这类任务需要通过检索和推理整合跨文档的证据。迭代检索系统可能会因锁定在早期的低召回轨迹并放大下游误差而失败，而仅含规划的方法则可能产生静态查询集，无法在中间证据变化时进行自适应。我们提出了 \textbf{Planned Active Retrieval and Reasoning RAG (PAR$^2$-RAG)}，这是一个将 \emph{覆盖范围} 与 \emph{承诺

## Abstract
Large language models (LLMs) remain brittle on multi-hop question answering (MHQA), where answering requires combining evidence across documents through retrieval and reasoning. Iterative retrieval systems can fail by locking onto an early low-recall trajectory and amplifying downstream errors, while planning-only approaches may produce static query sets that cannot adapt when intermediate evidence changes. We propose \textbf{Planned Active Retrieval and Reasoning RAG (PAR$^2$-RAG)}, a two-stage framework that separates \emph{coverage} from \emph{commitment}. PAR$^2$-RAG first performs breadth-first anchoring to build a high-recall evidence frontier, then applies depth-first refinement with evidence sufficiency control in an iterative loop. Across four MHQA benchmarks, PAR$^2$-RAG consistently outperforms existing state-of-the-art baselines, compared with IRCoT, PAR$^2$-RAG achieves up to \textbf{23.5\%} higher accuracy, with retrieval gains of up to \textbf{10.5\%} in NDCG.

---

## 论文详细总结（自动生成）

这篇论文提出了 **PAR$^2$-RAG**，旨在解决大语言模型（LLM）在处理多跳问答（Multi-Hop QA, MHQA）时，因检索路径偏差或规划僵化导致的性能瓶颈。

以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：多跳问答要求系统从多个文档中收集证据并进行逻辑整合。现有的 RAG 方法存在两大痛点：
    1.  **过早承诺（Premature Commitment）**：迭代检索（如 ReAct, IRCoT）通常采用深度优先的贪婪模式，一旦早期检索到干扰信息，错误会随推理链放大，难以修复。
    2.  **规划僵化**：仅依赖初始规划的方法（如查询分解）产生的是静态查询集，无法根据中间检索到的新证据灵活调整方向。
*   **核心目标**：通过“分离覆盖与承诺”（Separating Coverage from Commitment）的策略，提高多跳推理的鲁棒性。

### 2. 论文提出的方法论：PAR$^2$-RAG
该框架由两个协调阶段组成，涉及五个核心代理（Agent）：
*   **第一阶段：覆盖锚定（Coverage Anchoring - 广度优先）**
    *   **核心思想**：在正式推理前，先构建一个高召回率的证据池。
    *   **流程**：**规划器（Planner）**将复杂问题分解为多个互补的子查询；**检索器（Retriever）**执行这些查询并获取候选文档，经去重合并后形成“锚定上下文”（$C_{anchor}$）。这确保了“桥接事实”在推理开始前就已就绪。
*   **第二阶段：迭代链精炼（Iterative Chain Refinement - 深度优先）**
    *   **核心思想**：在锚定证据池的基础上，进行受控的、自适应的推理链构建。
    *   **流程**：
        1.  **生成器（Writer/Generator）**基于当前上下文生成初步步骤响应。
        2.  **证据充分性控制器（ESC）**判断当前证据是否足以回答问题。
        3.  若不足，**查询公式化器（Query Formulator）**重写针对性的后续查询进行补充检索；若充足，则输出最终答案。
    *   **关键机制**：ESC 充当“门控”，防止在证据不足时强行推理。

### 3. 实验设计
*   **数据集**：使用了四个具有挑战性的 MHQA 基准测试：
    *   **MuSiQue** & **2WikiMultiHopQA**：侧重于多步骤证据组合。
    *   **MoreHopQA** & **FRAMES**：侧重于生成式推理和端到端的事实性。
*   **对比方法（Baselines）**：
    *   **非检索方法**：直接推理、Chain-of-Thought (CoT)。
    *   **迭代检索方法**：ReAct、IRCoT（强基准）。
    *   **消融变体**：仅使用覆盖锚定（Coverage Anchor）、仅使用迭代链（Iterative Chain）。
*   **评估指标**：答案准确率（LLM 评判）、检索质量（Recall@k, NDCG@k, All-Pass）。

### 4. 资源与算力
*   **模型使用**：实验使用了多种 OpenAI 模型，包括 **GPT-4.1**、**GPT-o4-mini**、**GPT-o3**，以及最新的 **GPT-5.2**（用于扩展实验）。
*   **检索后端**：使用 OpenSearch 进行混合检索，并使用 `e5-mistral-7b-instruct` 进行重排序（Reranking）。
*   **算力说明**：文中**未明确提及**具体的 GPU 硬件型号、数量或训练时长。由于该方法属于“无训练”（Training-free）的 Agent 框架，其开销主要体现在推理时的 API 调用次数和 Token 消耗上。

### 5. 实验数量与充分性
*   **实验规模**：每个数据集随机采样 500 个查询进行评估。
*   **充分性**：
    *   涵盖了从非推理到强推理（Reasoning models）的不同生成设置。
    *   进行了**步数鲁棒性实验**（3, 5, 7, 10 步），验证了模型在不同检索预算下的稳定性。
    *   使用了最新的 GPT-5.2 模型进行验证，证明了方法在更强基础模型上的持续增益。
*   **客观性**：通过对比模块化变体，清晰地展示了“广度锚定”和“深度精炼”各自的贡献及其互补性。

### 6. 论文的主要结论与发现
*   **性能领先**：PAR$^2$-RAG 在所有基准测试中均取得 SOTA。相比 IRCoT，在 MuSiQue 上的准确率提升了 **23.5%**，检索 NDCG 提升了 **10.5%**。
*   **机制验证**：实验证明“先广度覆盖、后深度承诺”的策略能有效减少推理漂移。
*   **模型能力与瓶颈**：当生成模型推理能力较弱时，迭代精炼贡献更大；当生成模型极强（如推理模型）时，初始证据覆盖范围（Recall）成为决定性能的关键。

### 7. 优点
*   **架构优雅**：将复杂的检索推理任务解构为“覆盖”与“承诺”两个逻辑清晰的阶段。
*   **自适应性强**：ESC 控制器允许系统根据证据质量动态决定停止或继续，比固定步数的检索更灵活。
*   **检索质量高**：通过规划器预先扩展搜索边界，显著提升了多跳路径中关键“桥接事实”的召回率。

### 8. 不足与局限
*   **成本与延迟**：作为多 Agent 框架，PAR$^2$-RAG 需要多次调用 LLM（规划、生成、控制、重写），在实际工业应用中可能面临较高的 API 成本和响应延迟。
*   **依赖控制器质量**：ESC 的判断准确性至关重要，如果控制器误判证据已充足，仍会导致回答错误。
*   **实验样本量**：虽然覆盖了四个数据集，但每个数据集仅采样 500 条，对于极大规模的生产环境验证可能略显不足。

（完）
