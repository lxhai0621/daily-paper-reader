---
title: "ATP-Bench: Towards Agentic Tool Planning for MLLM Interleaved Generation"
title_zh: ATP-Bench：迈向多模态大语言模型交错生成的智能体工具规划
authors: "Yinuo Liu, Zi Qian, Heng Zhou, Jiahao Zhang, Yajie Zhang, Zhihang Li, Mengyu Zhou, Erchao Zhao, Xiaoxi Jiang, Guanjun Jiang"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29902v1"
tags: ["query:mmkqa"]
score: 9.0
evidence: 多模态检索增强生成的智能体工具规划
tldr: "本研究针对多模态大模型（MLLM）在交错文本图像生成中难以兼顾事实性与创造性的挑战，提出了“智能体工具规划”范式。通过引入包含7,702个QA对的ATP-Bench基准测试，以及无需参考答案的多智能体评测系统（MAM），系统评估了模型自主调用工具生成交错响应的能力。研究揭示了现有模型在复杂规划上的不足，为未来交错生成技术的发展提供了重要参考。"
selection_source: fresh_fetch
motivation: 旨在解决当前多模态模型在交错生成中无法统一事实性与创造性，且缺乏自主决定何时、何地及如何调用工具的能力。
method: 提出了ATP-Bench基准测试和多智能体评测系统（MAM），通过大规模人工验证的查询和意图分类来评估模型的工具规划精度。
result: 实验表明，即使是顶尖的MLLM在连贯的交错规划方面仍面临巨大挑战，且不同模型间的工具使用行为差异显著。
conclusion: 该研究定义了智能体工具规划的新范式，并为推动多模态大模型在视觉关键任务中的交错生成能力提供了行动指南。
---

## 摘要
文本与图像交错生成代表了多模态大语言模型（MLLM）的一个重要前沿，为传达复杂信息提供了一种更直观的方式。当前的范式依赖于图像生成或检索增强，但通常将两者视为互斥的路径，未能将真实性与创造性统一起来。我们认为，该领域的下一个里程碑是智能体工具规划（Agentic Tool Planning），即模型作为中央控制器，自主决定何时、何地以及调用哪些工具，从而为视觉关键型查询生成交错式响应。为了系统地评估这一范式，我们推出了 ATP-Bench，这是一个包含 8 个类别、25 种视觉关键意图、共 7,702 个问答对（包括 1,592 个 VQA 对）的新型基准测试，其特点是包含经过人工验证的查询和标准答案。此外，为了独立于端到端执行和不断变化的工具后端来评估智能体规划，我们提出了一个多智能体 MLLM-as-a-Judge (MAM) 系统。MAM 在不需要参考标准答案的情况下，评估工具调用的精确度，识别遗漏的工具使用机会，并评估整体响应质量。我们对 10 种最先进的 MLLM 进行了广泛实验，结果表明模型在连贯的交错规划方面面临挑战，且在工具使用行为上表现出显著差异，这突显了巨大的改进空间，并为推进交错生成提供了可操作的指导。数据集和代码可在 https://github.com/Qwen-Applications/ATP-Bench 获取。

## Abstract
Interleaved text-and-image generation represents a significant frontier for Multimodal Large Language Models (MLLMs), offering a more intuitive way to convey complex information. Current paradigms rely on either image generation or retrieval augmentation, yet they typically treat the two as mutually exclusive paths, failing to unify factuality with creativity. We argue that the next milestone in this field is Agentic Tool Planning, where the model serves as a central controller that autonomously determines when, where, and which tools to invoke to produce interleaved responses for visual-critical queries. To systematically evaluate this paradigm, we introduce ATP-Bench, a novel benchmark comprising 7,702 QA pairs (including 1,592 VQA pairs) across eight categories and 25 visual-critical intents, featuring human-verified queries and ground truths. Furthermore, to evaluate agentic planning independent of end-to-end execution and changing tool backends, we propose a Multi-Agent MLLM-as-a-Judge (MAM) system. MAM evaluates tool-call precision, identifies missed opportunities for tool use, and assesses overall response quality without requiring ground-truth references. Our extensive experiments on 10 state-of-the-art MLLMs reveal that models struggle with coherent interleaved planning and exhibit significant variations in tool-use behavior, highlighting substantial room for improvement and providing actionable guidance for advancing interleaved generation. Dataset and code are available at https://github.com/Qwen-Applications/ATP-Bench.

---

## 论文详细总结（自动生成）

以下是对论文《ATP-Bench: Towards Agentic Tool Planning for MLLM Interleaved Generation》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：多模态大语言模型（MLLM）的“交错生成”（Interleaved Generation）旨在同时产出文本和图像序列。目前主要存在两种范式：一是基于生成的（如扩散模型），虽具创造性但缺乏事实依据；二是基于检索的（RAG），虽具事实性但无法按需创作。
*   **核心问题**：现有模型通常将检索和生成视为互斥路径，缺乏一种能够像“智能体”一样自主规划、决定在何时何地调用何种工具（检索、生成、编辑等）来优化响应的能力。
*   **研究意义**：提出了**智能体工具规划（Agentic Tool Planning）**范式，将模型定位为中央控制器，动态编排多种视觉工具以解决复杂的视觉关键型（Visual-Critical）查询。

### 2. 方法论
*   **核心思想**：通过构建一个包含多种工具调用能力的基准测试（ATP-Bench）和一个无需参考答案的自动化评测系统（MAM），来衡量模型在交错生成中的规划能力。
*   **ATP-Bench 构建**：
    *   **分类体系**：涵盖学术、手册、食谱、时尚、装修、产品、旅游、百科 8 大类，细分为 25 种视觉关键意图。
    *   **数据规模**：7,702 个 QA 对（含 1,592 个 VQA 对），通过“查询收集-标准答案生成-人工精修”三阶段完成。
*   **工具箱（Toolkit）定义**：
    1.  **Reference**：引用上下文中的已有图像。
    2.  **Diffusion**：合成新图像（用于创意或概念展示）。
    3.  **Search**：通过搜索引擎获取真实世界图像。
    4.  **Code**：编写代码生成图表或数学绘图。
    5.  **Edit**：对已有图像进行局部修改或标注。
*   **MAM 评测系统（核心技术细节）**：
    *   **Precision Inspector（精度检查员）**：评估工具调用的必要性、工具选择正确性、语义位置及参数准确性。
    *   **Recall Inspector（召回检查员）**：识别模型本该使用工具但仅提供纯文本的“遗漏机会”。
    *   **Chief Judge（主裁判）**：综合上述报告，给出 0-100 的最终评分（Final Score）。

### 3. 实验设计
*   **评测对象**：选取了 10 种最先进的 MLLM，包括 Claude 4.5/4、Gemini 3 Pro、GPT-5、GPT-4o、Qwen3-VL-Plus、Qwen2.5-VL-72B、LLaMA-3.2-11B 等。
*   **实验场景**：在 8 个垂直领域进行零样本（Zero-shot）测试，并对部分模型进行了少样本（Few-shot）消融实验。
*   **对比指标**：最终得分（FS）、成功率（SR）、遗漏图像数（MI）、工具采用率、以及基于 Ground Truth 的 F1 分数。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的训练算力（如 GPU 型号或数量）。由于该研究侧重于基准测试构建和模型推理评测，而非模型训练，其主要开销在于调用各模型 API 进行推理以及人工标注的成本。

### 5. 实验数量与充分性
*   **实验规模**：对 10 个模型在 7,702 个样本上进行了全面测试，涵盖了从闭源顶尖模型到开源主流模型的梯队。
*   **消融与验证**：
    *   进行了**法官模型一致性实验**（更换 MAM 系统背后的模型，如 Claude/Gemini/GPT 互测）。
    *   进行了**人工一致性研究**（400 个样本的人机对比），证明 MAM 系统与人类判断的高度一致性（84%-88%）。
    *   探讨了工具边界定义对模型行为的影响。
*   **客观性**：通过多智能体协作评测，减少了单一模型作为法官的偏见，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **性能排行**：**Gemini 3 Pro** 在所有指标上均处于领先地位（FS: 79.88），表现出极强的工具规划和执行稳定性。
*   **任务难度**：学术和百科类任务相对简单；**旅游（Travel）**和**装修（Renovation）**最难，模型在这些领域极易遗漏工具调用或执行错误。
*   **模型行为差异**：
    *   顶尖模型（如 Gemini, Claude）规划谨慎且精准。
    *   开源模型（如 LLaMA-3.2）倾向于不调用工具（遗漏率高），而 InternVL 则容易产生格式错误的无效调用。
*   **工具偏好**：Diffusion 是最常被调用的非引用工具，而 Code 和 Edit 的调用门槛最高。

### 7. 优点
*   **范式创新**：首次将“智能体工具规划”引入交错生成领域，打破了检索与生成的二元对立。
*   **评测框架完备**：MAM 系统解决了交错生成难以用传统指标（如 CLIP Score）衡量逻辑规划的问题，且无需依赖 Ground Truth。
*   **数据质量高**：结合了自动化生成与严格的专家级人工校验，确保了基准测试的权威性。

### 8. 不足与局限
*   **模态限制**：目前仅关注文本和图像，未涉及音频、视频等更丰富的多模态交错生成。
*   **工具集规模**：仅定义了 5 种核心工具，对于更复杂的现实场景（如交互式操作、实时数据反馈）覆盖不足。
*   **评测依赖性**：MAM 系统本身依赖于高性能 MLLM 作为法官，若法官模型存在系统性偏见，可能会影响评分结果。
*   **端到端执行偏差**：虽然论文评估了规划能力，但实际执行效果（如生成图像的审美质量）受限于后端工具（如特定的扩散模型），规划好并不代表最终视觉效果一定完美。

（完）
