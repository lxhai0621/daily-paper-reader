---
title: "To Memorize or to Retrieve: Scaling Laws for RAG-Considerate Pretraining"
title_zh: 记忆还是检索：兼顾 RAG 的预训练缩放法则
authors: "Karan Singh, Michael Yu, Varun Gangal, Zhuofu Tao, Sachin Kumar, Emmy Liu, Steven Y. Feng"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00715v1"
tags: ["query:ma-kf"]
score: 10.0
evidence: 检索增强生成的预训练缩放法则
tldr: 本研究探讨了预训练参数知识与检索增强生成（RAG）非参数知识之间的权衡。通过训练不同规模的OLMo-2模型并改变预训练数据量与检索库大小，作者提出了一个三维缩放定律框架，涵盖模型参数、预训练Token数和检索库规模。研究揭示了在固定数据预算下，如何优化预训练与检索资源的分配，为构建高效的可扩展语言模型系统提供了定量指导。
selection_source: fresh_fetch
motivation: 旨在量化理解预训练阶段获得的参数化知识与推理阶段检索到的非参数化知识之间的关系及资源分配权衡。
method: 系统性地训练了30M至3B参数的模型，通过改变预训练数据规模和检索库大小，并利用三维缩放框架对性能进行建模。
result: 检索在各种模型规模下均能显著提升性能，且其边际效用受模型规模、任务类型及预训练饱和程度的共同影响。
conclusion: 提出了一个能够估算预训练与检索之间最优数据分配的缩放流形，为设计兼顾RAG性能的预训练策略提供了实践指南。
---

## 摘要
检索增强生成 (RAG) 通过在推理阶段为知识密集型场景提供相关上下文，提升了语言模型 (LM) 的性能。然而，在预训练期间获得的参数化知识与通过检索获取的非参数化知识之间的关系仍不明确，尤其是在固定数据预算的情况下。在这项工作中，我们系统地研究了在广泛的模型和数据规模下，预训练语料库大小与检索库大小之间的权衡。我们在多达 100B token 的 DCLM 数据上训练了参数量从 30M 到 3B 不等的基于 OLMo-2 的语言模型，同时改变预训练数据规模（参数量的 1-150 倍）和检索库大小（1-20 倍），并在涵盖推理、科学问答和开放域问答的多样化基准测试中评估了其性能。我们发现，在各种模型规模下，检索相比于仅依赖参数的基线模型均能持续提升性能。此外，我们引入了一个三维缩放框架，将性能建模为模型大小、预训练 token 数和检索语料库大小的函数。这种缩放流形使我们能够估算固定数据预算在预训练和检索之间的最优分配，揭示了检索的边际效用强烈依赖于模型规模、任务类型以及预训练的饱和程度。我们的研究结果为理解检索何时以及如何补充预训练提供了定量基础，并为可扩展语言建模系统设计中的数据资源分配提供了实践指导。

## Abstract
Retrieval-augmented generation (RAG) improves language model (LM) performance by providing relevant context at test time for knowledge-intensive situations. However, the relationship between parametric knowledge acquired during pretraining and non-parametric knowledge accessed via retrieval remains poorly understood, especially under fixed data budgets. In this work, we systematically study the trade-off between pretraining corpus size and retrieval store size across a wide range of model and data scales. We train OLMo-2-based LMs ranging from 30M to 3B parameters on up to 100B tokens of DCLM data, while varying both pretraining data scale (1-150x the number of parameters) and retrieval store size (1-20x), and evaluate performance across a diverse suite of benchmarks spanning reasoning, scientific QA, and open-domain QA. We find that retrieval consistently improves performance over parametric-only baselines across model scales and introduce a three-dimensional scaling framework that models performance as a function of model size, pretraining tokens, and retrieval corpus size. This scaling manifold enables us to estimate optimal allocations of a fixed data budget between pretraining and retrieval, revealing that the marginal utility of retrieval depends strongly on model scale, task type, and the degree of pretraining saturation. Our results provide a quantitative foundation for understanding when and how retrieval should complement pretraining, offering practical guidance for allocating data resources in the design of scalable language modeling systems.

---

## 论文详细总结（自动生成）

以下是对论文《To Memorize or to Retrieve: Scaling Laws for RAG-Considerate Pretraining》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了在大语言模型（LLM）开发中一个长期被忽视的资源分配问题：**在固定数据预算下，应该将多少数据用于预训练（转化为参数知识），将多少数据留作检索库（作为非参数知识）？**
传统的缩放法则（Scaling Laws）假设所有数据都应被模型“消化”进权重中。然而，检索增强生成（RAG）提供了一种替代方案。作者旨在量化预训练数据规模、模型参数量与检索库规模之间的三维关系，为设计高效的 RAG 系统提供理论指导。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将模型性能建模为三个变量的函数：模型参数量 ($N$)、预训练 Token 数 ($D$) 和检索库 Token 数 ($R$)。
*   **三维缩放公式**：作者在 Chinchilla 缩放法则的基础上引入了对数增益项：
    $$L(N, D, R) = A N^{-\alpha} + B D^{-\beta} - C \log(1 + \eta R) + L_0$$
    其中，$A, \alpha, B, \beta$ 捕捉参数化缩放特征，$C$ 代表检索的最大潜在收益，$\eta$ 代表检索收益的饱和速度，$L_0$ 是不可约损失。
*   **关键流程**：
    1.  **模型训练**：基于 OLMo-2 架构训练不同规模的模型。
    2.  **索引构建**：使用 Qwen3-Embedding-8B 模型对 DCLM 语料库的留出部分进行向量化，利用 FAISS 构建不同规模（1B-20B tokens）的检索索引。
    3.  **RAG 评估**：在推理时检索 Top-5 片段作为上下文，通过困惑度（PPL）和准确率评估性能。

### 3. 实验设计
*   **模型规模**：涵盖 30M、136M、233M、728M、1B 和 3B 参数。
*   **数据集**：预训练使用 100B Token 的 DCLM 数据；检索库同样源自 DCLM 的独立切片。
*   **Benchmark**：涵盖 10 个主流基准测试，分为三类：
    *   **推理**：PIQA, CommonsenseQA, HellaSwag, StrategyQA。
    *   **科学/开放域问答**：SciQ, OpenBookQA, ARC (Easy/Challenge), Natural Questions, SimpleQA。
*   **对比方法**：对比了纯参数化基线（$R=0$）与不同 $D$ 和 $R$ 组合下的 RAG 模型。

### 4. 资源与算力
*   **硬件**：使用 NVIDIA H100 GPU，每个训练任务配置 8 张显卡。
*   **训练细节**：采用 FSDP 数据并行，混合精度训练，上下文长度为 4096。
*   **时长**：文中未明确给出总训练小时数，但提到模型训练涵盖了从 1 倍到 150 倍参数量的 Token 规模（最高达 100B tokens）。

### 5. 实验数量与充分性
*   **实验规模**：作者进行了极其详尽的实验，包括 6 种模型规模、多种预训练数据梯度和多种检索库规模。
*   **充分性**：实验不仅拟合了缩放曲线，还进行了留一法（LOMO）交叉验证以测试公式的预测能力。
*   **客观性**：通过 27 组独立运行（3 种模型规模 × 3 个随机种子）验证了结果的稳定性，并探讨了检索查询构造（如加入金标准答案）对结果的影响，实验设计严谨且公平。

### 6. 主要结论与发现
*   **检索的替代效应**：检索可以作为预训练的有效替代。当预训练程度超过一定阈值（约每参数 4.14 个 Token）后，检索的边际效用显著增加，1 个检索 Token 可以抵消多个预训练 Token 的贡献。
*   **规模依赖性**：小模型从检索中获益最大；随着模型规模增加，检索带来的绝对性能提升（边际收益）逐渐减小。
*   **任务差异性**：知识密集型任务（如问答）对检索非常敏感，而推理密集型任务（如数学、逻辑）受检索影响较小，甚至可能因检索到无关信息而受损。
*   **非单调性**：在固定数据预算下，性能随检索分配比例的变化呈现非单调趋势，存在一个最优的分配平衡点。

### 7. 优点
*   **开创性视角**：首次将检索库规模纳入缩放法则的定量分析，解决了“数据该存放在权重里还是数据库里”的工程难题。
*   **实用指导**：为开发者提供了计算最优数据分配的数学工具，有助于在有限算力和数据下优化系统设计。
*   **度量科学**：坚持使用困惑度（PPL）而非不稳定的准确率作为主要拟合指标，确保了缩放曲线的平滑和可预测性。

### 8. 不足与局限
*   **检索组件固定**：实验使用了固定的检索器（Qwen3）和简单的 Top-k 策略，未探讨重排序（Reranking）或自适应检索等高级技术对缩放法则的影响。
*   **模型规模上限**：实验最高仅达到 3B 参数，虽然对小模型（SLM）极具参考价值，但在超大规模模型（如 70B+）上的适用性仍需验证。
*   **任务覆盖**：对于长文本理解、多轮对话等复杂场景的覆盖不足。
*   **推理成本未量化**：虽然检索在训练时“免费”，但在推理时会增加延迟和计算开销，论文未将推理成本纳入最终的优化公式。

（完）
