---
title: "EffiSkill: Agent Skill Based Automated Code Efficiency Optimization"
title_zh: EffiSkill：基于智能体技能的自动化代码效率优化
authors: "Zimu Wang, Yuling Shi, Mengfan Li, Zijun Liu, Jie M. Zhang, Chengcheng Wan, Xiaodong Gu"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27850v1"
tags: ["query:agent"]
score: 9.0
evidence: 基于LLM智能体的便携式优化工具箱
tldr: "针对大语言模型在代码效率优化中缺乏可重用知识的问题，本文提出了 EffiSkill 框架。该框架将代码优化过程建模为可重用的智能体技能（包括算子技能和元技能），通过从大规模代码对中挖掘优化模式构建技能库。在推理阶段，EffiSkill 无需运行时反馈，通过诊断、检索和规划实现高效优化。实验表明，该方法在 EffiBench-X 基准测试上显著优于现有基线，提升了 3.69% 至 12.52%，证明了机制级技能复用的有效性。"
selection_source: fresh_fetch
motivation: 现有 LLM 代码优化方法多为单次重写或简单检索，缺乏对可重用优化知识的显式提取，限制了其泛化能力。
method: EffiSkill 通过挖掘大规模代码对构建包含算子和元技能的技能库，并利用该库对新程序进行无执行反馈的诊断、检索与规划优化。
result: "在 EffiBench-X 基准测试中，EffiSkill 相比最强基线在不同模型和语言设置下实现了 3.69% 至 12.52% 的成功率提升。"
conclusion: 将优化机制建模为可重用技能为无执行代码优化提供了有效基础，且该技能库可作为提升智能体工作流效率的通用资源。
---

## 摘要
代码效率是软件质量的一个核心维度，然而如何利用大语言模型（LLMs）来优化程序仍具挑战。先前的方法多采用单次重写、示例检索或基于提示的搜索，但未能显式提取可重用的优化知识，从而限制了在特定实例之外的泛化能力。在本文中，我们提出了 EffiSkill，这是一个代码效率优化框架，旨在为基于 LLM 的智能体构建便携式优化工具箱。其核心思想是将反复出现的“由慢到快”转换建模为可重用的智能体技能，这些技能兼顾了具体的转换机制与高层优化策略。EffiSkill 采用两阶段设计：第一阶段从大规模慢速/快速程序对中挖掘算子技能（Operator Skills）和元技能（Meta Skills）以构建技能库；第二阶段通过无执行诊断、技能检索、方案组合及候选生成，将该库应用于未知程序，且无需运行时反馈。在 EffiBench-X 上的结果显示，EffiSkill 实现了更高的优化成功率，在不同模型和语言设置下较最强基准提升了 3.69 至 12.52 个百分点。研究结果表明，机制层面的技能重用为无执行代码优化奠定了坚实基础，且所得技能库可作为更广泛智能体工作流的可重用资源。

## Abstract
Code efficiency is a fundamental aspect of software quality, yet how to harness large language models (LLMs) to optimize programs remains challenging. Prior approaches have sought for one-shot rewriting, retrieved exemplars, or prompt-based search, but they do not explicitly distill reusable optimization knowledge, which limits generalization beyond individual instances.   In this paper, we present EffiSkill, a framework for code-efficiency optimization that builds a portable optimization toolbox for LLM-based agents. The key idea is to model recurring slow-to-fast transformations as reusable agent skills that capture both concrete transformation mechanisms and higher-level optimization strategies. EffiSkill adopts a two-stage design: Stage I mines Operator and Meta Skills from large-scale slow/fast program pairs to build a skill library; Stage II applies this library to unseen programs through execution-free diagnosis, skill retrieval, plan composition, and candidate generation, without runtime feedback.   Results on EffiBench-X show that EffiSkill achieves higher optimization success rates, improving over the strongest baseline by 3.69 to 12.52 percentage points across model and language settings. These findings suggest that mechanism-level skill reuse provides a useful foundation for execution-free code optimization, and that the resulting skill library can serve as a reusable resource for broader agent workflows.

---

## 论文详细总结（自动生成）

### 论文总结：EffiSkill —— 基于智能体技能的自动化代码效率优化

#### 1. 核心问题与整体含义（研究动机和背景）
代码效率是软件质量的核心，但利用大语言模型（LLM）进行代码优化面临挑战。现有方法（如单次重写、示例检索或搜索）通常停留在**实例级别**，即针对特定代码进行修改，而未能提取出**可重用的优化知识**。这导致模型难以在不同任务间泛化。
**EffiSkill** 的研究动机在于：性能优化往往遵循重复的模式（如算法替换、状态压缩、减少常数因子）。论文提出将这些模式建模为“**智能体技能（Agent Skills）**”，构建一个便携式的优化工具箱，使 LLM 能够从机制层面而非表面模仿层面进行代码优化。

#### 2. 方法论：核心思想与关键技术
EffiSkill 采用两阶段设计，旨在实现**无执行反馈（Execution-free）**的推理优化：

*   **第一阶段：离线技能挖掘（Stage I: Skill Mining）**
    1.  **轨迹提取**：利用 LLM 分析大规模“慢/快”程序对，生成包含问题简述、慢速审计、快速审计和差异总结的结构化轨迹。
    2.  **特征抽象与聚类**：将轨迹抽象为包含优化类型、复杂度变化等信息的“签名”，在词法和语义空间进行混合聚类。
    3.  **技能蒸馏**：将每个聚类提炼为**算子技能（Operator Skills）**（具体的转换机制）和**元技能（Meta Skills）**（如何选择和组合算子的程序化逻辑），最终形成“技能卡（Skill Cards）”。
*   **第二阶段：技能引导优化（Stage II: Skill-Guided Optimization）**
    1.  **诊断**：对输入程序进行瓶颈分析，生成优化简报。
    2.  **检索与规划**：根据诊断结果从库中检索相关技能，并由元技能组合成多个优化方案（Plans）。
    3.  **候选生成**：根据规划生成多个优化后的代码候选，整个过程无需在推理时反复执行代码获取反馈。

#### 3. 实验设计
*   **数据集/Benchmark**：
    *   **挖掘阶段**：使用 PIE、Mercury、DeepMind Code Contests 等数据集构建了 900 对 Python 和 900 对 C++ 的慢/快代码对。
    *   **评估阶段**：使用 **EffiBench-X** 基准测试，包含 623 个优化任务。
*   **对比方法**：
    *   **提示词类**：直接指令（Instruction）、思维链（CoT）。
    *   **检索增强类**：RAG、FasterPy。
    *   **搜索与微调类**：SBLLM（基于搜索）、EffiCoder（基于微调）。
*   **评估指标**：**OPT@k**（在 k 个候选者中，至少有一个通过测试且运行时间减少 10% 以上的比例）。

#### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **AMD EPYC 7302 CPU** (16核, 3.0 GHz) 和 **112 GB RAM** 的服务器上运行。
*   **模型支持**：使用了 **GPT-5-mini** 和 **Qwen3-Coder-30B-A3B-Instruct** 作为骨干模型。
*   **训练/挖掘时长**：文中未明确给出挖掘技能库的具体耗时，但强调了推理阶段是“无执行”的，这显著降低了部署时的计算开销。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 Python 和 C++ 两种主流语言，使用了两种不同规模和来源的 LLM 骨干模型。
*   **消融实验**：针对“无检索”、“随机技能”和“无多方案规划”进行了对比，验证了每个模块的必要性。
*   **充分性评价**：实验设计较为充分。通过 Top-k 成功率增长曲线、任务级胜负分析以及优化幅度分布图，多维度证明了方法的有效性。实验遵循了严格的测试集隔离原则，确保了结果的客观性。

#### 6. 主要结论与发现
*   **性能领先**：EffiSkill 在所有设置下均优于最强基线。在 C++ 上，OPT@8 相比基线提升了 **12.03% 至 12.52%**；在 Python 上提升了 **4.98% 至 8.67%**。
*   **技能库特征**：挖掘出的技能库包含 29 个算子技能，涵盖实现优化、代数重构、动态规划压缩等 5 大类。其中前 5 个核心技能覆盖了约 70% 的使用场景。
*   **跨语言一致性**：尽管技能是从特定语言对中挖掘的，但其抽象出的优化机制在不同编程语言间具有良好的通用性。

#### 7. 优点与亮点
*   **知识显式化**：将模糊的优化经验转化为可阅读、可重用的“技能卡”，提高了优化过程的可解释性。
*   **无执行推理**：与依赖反复运行代码的搜索类方法相比，EffiSkill 在推理时更高效，适用于无法提供运行环境的生产场景（如代码审查工具）。
*   **模块化设计**：技能库可以作为“插件”集成到更广泛的智能体工作流中。

#### 8. 不足与局限
*   **领域局限性**：实验主要集中在**竞赛编程（Competitive Programming）**任务上。这类任务逻辑清晰、瓶颈明显，而工业级软件优化涉及复杂的外部依赖、硬件架构和多文件上下文，其有效性尚待验证。
*   **依赖挖掘质量**：技能库的质量高度依赖于初始“慢/快”代码对的质量以及 LLM 在挖掘阶段的分析能力。
*   **静态局限**：虽然无执行推理是优点，但在处理某些极度依赖运行时数据的性能问题（如缓存缺失、分支预测）时，缺乏动态反馈可能会限制其优化深度。

（完）
