---
title: "Navigating the Mirage: A Dual-Path Agentic Framework for Robust Misleading Chart Question Answering"
title_zh: 穿越幻象：一种用于鲁棒误导性图表问答的双路径智能体框架
authors: "Yanjie Zhang, Yafei Li, Rui Sheng, Zixin Chen, Yanna Lin, Huamin Qu, Lei Chen, Yushi Sun"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28583v1"
tags: ["query:mmkqa"]
score: 9.0
evidence: 用于多模态图表问答的智能体框架
tldr: 针对视觉语言模型在处理误导性图表时的脆弱性，本文提出 ChartCynics 框架。该框架采用“怀疑论”推理范式，通过双路径解耦感知与验证：诊断视觉路径捕捉结构异常，OCR 数据路径确保数值准确。结合 Oracle-Informed SFT 和 Deception-Aware GRPO 优化，显著提升了模型识别视觉陷阱的能力，在多个基准测试中大幅超越现有模型，为可信图表解析提供了新方案。
selection_source: fresh_fetch
motivation: 现有的视觉语言模型容易受到误导性图表（如坐标轴反转、数据扭曲）的欺骗，难以准确识别视觉陷阱。
method: 提出一种双路径智能体框架，通过视觉诊断和 OCR 数据路径解耦感知，并利用两阶段协议优化推理与对抗对齐。
result: "在基准测试中，该方法比 Qwen3-VL-8B 基座模型提升了约 29% 的准确率，性能超越了先进的闭源模型。"
conclusion: 研究证明专门的智能体工作流能显著增强开源模型的鲁棒性，是实现可靠图表解释的有效途径。
---

## 摘要
尽管视觉语言模型（VLMs）取得了成功，但由于误导性图表具有欺骗性的视觉结构和扭曲的数据表示，它们仍然是一个重大挑战。我们提出了 ChartCynics，这是一个旨在通过“怀疑式”推理范式揭示视觉欺骗的智能体双路径框架。与整体模型不同，ChartCynics 将感知与验证解耦：诊断视觉路径（Diagnostic Vision Path）通过策略性的感兴趣区域（ROI）裁剪捕捉结构异常（如反转轴），而 OCR 驱动的数据路径（OCR-Driven Data Path）则确保了数值基础。为了解决跨模态冲突，我们引入了一个通过两阶段协议优化的智能体总结器（Agentic Summarizer）：用于推理蒸馏的 Oracle-Informed SFT 和用于对抗对齐的 Deception-Aware GRPO。该流水线有效地惩罚了视觉陷阱并增强了逻辑一致性。在两个基准测试上的评估表明，ChartCynics 分别达到了 74.43% 和 64.55% 的准确率，相比 Qwen3-VL-8B 基座模型实现了约 29% 的绝对性能提升，优于最先进的商业模型。我们的结果表明，专门的智能体工作流可以赋予较小的开源模型卓越的鲁棒性，为可靠的图表解释奠定了新的基础。

## Abstract
Despite the success of Vision-Language Models (VLMs), misleading charts remain a significant challenge due to their deceptive visual structures and distorted data representations. We present ChartCynics, an agentic dual-path framework designed to unmask visual deception via a "skeptical" reasoning paradigm. Unlike holistic models, ChartCynics decouples perception from verification: a Diagnostic Vision Path captures structural anomalies (e.g., inverted axes) through strategic ROI cropping, while an OCR-Driven Data Path ensures numerical grounding. To resolve cross-modal conflicts, we introduce an Agentic Summarizer optimized via a two-stage protocol: Oracle-Informed SFT for reasoning distillation and Deception-Aware GRPO for adversarial alignment. This pipeline effectively penalizes visual traps and enforces logical consistency. Evaluations on two benchmarks show that ChartCynics achieves 74.43% and 64.55% accuracy, providing an absolute performance boost of ~29% over the Qwen3-VL-8B backbone, outperforming state-of-the-art proprietary models. Our results demonstrate that specialized agentic workflows can grant smaller open-source models superior robustness, establishing a new foundation for trustworthy chart interpretation.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **ChartCynics** 的双路径智能体框架，旨在解决视觉语言模型（VLMs）在面对“误导性图表”（如坐标轴反转、比例扭曲等）时容易产生视觉顺从（Visual Sycophancy）并落入陷阱的问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉语言模型在处理标准图表时表现良好，但在面对故意设计的**误导性图表**（Misleading Charts）时，往往会被直观的视觉趋势（如线条的物理斜率）误导，而忽略了真实的数值逻辑。
*   **研究动机**：
    *   **视觉陷阱**：例如，当 Y 轴反转（0 在顶部）时，模型会根据视觉上的“下降”趋势判断数值减少，即使数值实际上在增加。
    *   **现有方法局限**：端到端模型容易产生视觉偏见；纯 OCR 路径则丢失了空间布局语义，导致实体对齐错误。
    *   **目标**：建立一种“怀疑式”推理范式，将视觉感知与数值验证解耦，提升模型在复杂欺骗场景下的鲁棒性。

### 2. 方法论
ChartCynics 采用双路径架构，并通过两阶段训练协议进行优化：
*   **核心思想**：模仿人类审计员的行为，先独立观察结构异常，再核对原始数据，最后进行冲突裁决。
*   **关键技术细节**：
    1.  **诊断视觉路径 (Diagnostic Vision Path)**：利用 ROI 裁剪技术提取标题、图例、坐标轴等关键区域，由“诊断智能体”进行**盲测**（不看问题只看图表结构），识别是否存在轴反转或非零基准等异常。
    2.  **OCR 驱动的数据路径 (Data Path)**：将图表序列化为 Markdown 格式，提取数值事实，并根据数据标签的完整性设定“信任等级”。
    3.  **智能体融合与侦探思维链 (D-CoT)**：通过 5 步推理法（感知审计、数值锚定、欺骗映射、完整性检查、陷阱拒绝）整合两路信息。
*   **优化策略**：
    *   **Oracle-Informed SFT**：利用大模型（Qwen3-32B）作为教师，将不可见的原始数据转化为可见的逻辑推理痕迹进行蒸馏。
    *   **Deception-Aware GRPO (强化学习)**：使用组相对策略优化，设计了**非对称奖励函数**——正确回答给 +1.0，但如果落入“误导性陷阱”则重罚 -2.0，强制模型学会怀疑视觉幻象。

### 3. 实验设计
*   **数据集**：
    *   **MC (Misleading ChartQA)**：主要的误导性图表基准测试。
    *   **CDCC (Curated Deceptive Chart Collection)**：包含 110 个专家验证的真实世界欺骗案例。
    *   **MSMB (Mixed Benchmark)**：混合了标准图表和误导性图表，用于测试模型是否会产生“过度怀疑”。
*   **对比方法**：
    *   **闭源模型**：GPT-4o-mini, Gemini 1.5 Pro, Gemini 3.1 Pro。
    *   **开源基座**：Qwen3-VL-8B。
    *   **专门模型/策略**：ChartMoE、基于表格的 QA、图表重绘技术。

### 4. 资源与算力
*   **算力设备**：使用了 **4 × NVIDIA A800 (80GB)** GPU 集群。
*   **模型基座**：主要基于 **Qwen3-VL-8B** 进行微调和实验。
*   **辅助工具**：使用了 `nemotron-graphic-elements-v1` 进行 ROI 提取，使用 `LlamaParse` (GPT-4o 驱动) 进行 OCR 解析。

### 5. 实验数量与充分性
*   **实验规模**：论文在三个不同的基准测试上进行了评估，涵盖了从合成数据到真实世界案例的多种场景。
*   **消融实验**：详细对比了“仅视觉”、“仅 OCR”、“无 SFT/GRPO”等多种变体，验证了双路径和强化学习的必要性。
*   **充分性评价**：实验设计较为全面，不仅关注准确率，还通过 WM（误导错误率）和 WO（其他错误率）深入分析了错误来源。通过 MSMB 数据集验证了模型在标准图表上的泛化能力，证明了该方法不会损害基础性能。

### 6. 主要结论与发现
*   **性能飞跃**：ChartCynics 使 Qwen3-VL-8B 的准确率从 45.57% 提升至 **74.43%**，提升幅度达 29%，甚至超越了 Gemini 3.1 Pro 等大型闭源模型。
*   **陷阱抑制**：该框架显著降低了模型因视觉欺骗而犯错的比例（WM 显著下降）。
*   **通用性**：即使在非误导性的标准图表上，ChartCynics 的表现也优于专门的图表解析模型（如 ChartMoE），证明了结构化诊断对普通图表理解也有帮助。

### 7. 亮点
*   **解耦设计**：通过“盲测”诊断智能体有效避免了确认偏误（Confirmation Bias）。
*   **强化学习创新**：引入非对称惩罚机制（GRPO），针对性地解决了模型“视觉顺从”的顽疾。
*   **实用性**：提供了一套完整的、可解释的 5 步侦探推理流程，增强了模型输出的可信度。

### 8. 不足与局限
*   **OCR 依赖**：数据路径高度依赖高质量的 OCR 解析（如 LlamaParse），如果 OCR 环节出错，后续推理可能受阻。
*   **推理开销**：作为一种智能体框架，涉及多次模型调用和 ROI 裁剪，推理延迟和计算成本高于单次前向传播的端到端模型。
*   **覆盖范围**：虽然涵盖了常见的误导手段，但对于极其复杂或新颖的视觉艺术化变形图表，其分类分类学（Taxonomy）可能仍需扩展。

（完）
