---
title: "Internal APIs Are All You Need: Shadow APIs, Shared Discovery, and the Case Against Browser-First Agent Architectures"
title_zh: 内部 API 就是你所需的一切：影子 API、共享发现以及反对浏览器优先的智能体架构
authors: "Lewis Tham, Nicholas Mac Gregor Garcia, Jungpil Hahn"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00694v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 通过共享路由图将内部API与自主智能体集成
tldr: 针对当前自主智能体在网页交互中面临的DOM解析慢、鲁棒性差等问题，本文提出Unbrowse系统。该系统通过构建共享路由图，利用网站现有的内部API（影子API）替代传统的浏览器自动化操作。Unbrowse能从流量中被动学习路由并缓存，使智能体能直接通过API获取数据。实验显示，该方法相比Playwright自动化提速显著，并引入了基于x402协议的微支付模型以激励生态建设，为“智能体网页”架构提供了新思路。
selection_source: fresh_fetch
motivation: 现有的浏览器优先架构导致智能体在处理面向人类设计的网页时效率低下、过程脆弱且存在大量重复的逆向工程劳动。
method: 提出Unbrowse系统，通过共享路由图被动学习并索引网站内部API，实现从繁琐的浏览器自动化向直接API调用的转变。
result: 在94个域名的实测中，缓存执行速度比Playwright自动化平均快3.6倍，中位数提速达5.4倍，部分请求可在100毫秒内完成。
conclusion: 利用内部API构建共享发现机制是提升智能体网页交互效率的关键，结合微支付模型可实现自纠错且经济合理的智能体生态系统。
---

## 摘要
自主智能体越来越多地与 Web 交互，但大多数网站仍是为人类浏览器设计的——这是新兴的“智能体化网络”（Agentic Web）必须解决的根本性不匹配。智能体必须重复浏览页面、检查 DOM 并逆向工程可调用路由，这一过程缓慢、脆弱，且在不同智能体之间冗余重复。我们观察到，每个现代网站在其用户界面背后都已经暴露了内部 API（有时称为影子 API），即驱动网站自身功能的第一方端点。我们提出了 Unbrowse，这是一个共享路由图，它将基于浏览器的路由发现转化为这些可调用第一方接口的集体维护索引。该系统从真实的浏览流量中被动学习路由，并通过直接 API 调用提供缓存路由。在针对 94 个域名的等效信息检索任务的单主机实时网络基准测试中，完全预热的缓存执行平均耗时 950 毫秒，而 Playwright 浏览器自动化耗时 3,404 毫秒（平均加速 3.6 倍，中位数加速 5.4 倍），缓存良好的路由可在 100 毫秒内完成。三路径执行模型（本地缓存、共享图或浏览器回退）确保了系统的自愿性和自我修正。通过 x402 协议建立的三层微支付模型对图查询收取单次查询搜索费（第 3 层），对发现文档收取一次性安装费（第 1 层），并为选择加入的网站所有者提供可选的单次执行费（第 2 层）。所有层级都基于理性采用的必要条件：只有当总费用低于浏览器重新发现的预期成本时，智能体才会使用共享图。

## Abstract
Autonomous agents increasingly interact with the web, yet most websites remain designed for human browsers -- a fundamental mismatch that the emerging ``Agentic Web'' must resolve. Agents must repeatedly browse pages, inspect DOMs, and reverse-engineer callable routes -- a process that is slow, brittle, and redundantly repeated across agents. We observe that every modern website already exposes internal APIs (sometimes called \emph{shadow APIs}) behind its user interface -- first-party endpoints that power the site's own functionality. We present Unbrowse, a shared route graph that transforms browser-based route discovery into a collectively maintained index of these callable first-party interfaces. The system passively learns routes from real browsing traffic and serves cached routes via direct API calls. In a single-host live-web benchmark of equivalent information-retrieval tasks across 94 domains, fully warmed cached execution averaged 950\,ms versus 3{,}404\,ms for Playwright browser automation (3.6$\times$ mean speedup, 5.4$\times$ median), with well-cached routes completing in under 100\,ms. A three-path execution model -- local cache, shared graph, or browser fallback -- ensures the system is voluntary and self-correcting. A three-tier micropayment model via the x402 protocol charges per-query search fees for graph lookups (Tier~3), a one-time install fee for discovery documentation (Tier~1), and optional per-execution fees for site owners who opt in (Tier~2). All tiers are grounded in a necessary condition for rational adoption: an agent uses the shared graph only when the total fee is lower than the expected cost of browser rediscovery.

---

## 论文详细总结（自动生成）

这篇论文由 Lewis Tham 等人撰写，探讨了如何通过利用网站现有的“内部 API”（影子 API）来彻底改变自主智能体（Autonomous Agents）与万维网交互的方式。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：目前的 Web 智能体主要模仿人类行为（打开浏览器、解析 DOM、模拟点击），这导致了交互速度慢、鲁棒性差（UI 变动即失效）以及极高的计算成本（LLM 需处理大量 DOM 文本）。
*   **核心矛盾**：不同智能体在访问同一网站时，都在重复进行昂贵的“逆向工程”和页面解析工作，这是一种巨大的资源浪费。
*   **整体含义**：论文提出了 **Unbrowse** 系统，主张智能体不应“浏览”网页，而应直接调用驱动网页功能的内部 API。通过构建一个“共享路由图”（Shared Route Graph），将个别智能体的发现转化为集体的公共知识库，从而实现“智能体化网络”（Agentic Web）的高效基础设施。

### 2. 方法论
*   **核心思想**：将浏览器发现过程转化为一个可集体维护、可搜索的 API 索引。
*   **三路径执行模型**：
    1.  **路径 1（本地缓存）**：优先检查本地是否已有该任务的路由缓存，实现瞬时响应。
    2.  **路径 2（共享图）**：若本地无缓存，则查询云端共享路由图，通过 **x402 协议**支付微小的费用下载“技能包”（包含 API 模式、认证描述等），然后在本地直接调用 API。
    3.  **路径 3（浏览器回退）**：若图中无匹配路由，则启动 **Kuri**（一个轻量级 Zig 原生 CDP 代理）运行无头浏览器，被动捕获网络流量，自动提取 API 路由并发布到共享图中。
*   **经济模型（x402 协议）**：
    *   **第 1 层（安装费）**：一次性支付给路由贡献者。
    *   **第 2 层（执行费）**：可选，支付给选择加入该生态的网站所有者。
    *   **第 3 层（搜索费）**：支付给维护索引的平台。
*   **关键公式**：智能体采用该系统的必要条件是：`共享图总费用 < 浏览器重新发现的预期成本`（包括延迟、算力、Token 消耗及失败重试成本）。

### 3. 实验设计
*   **数据集/场景**：涵盖 94 个真实域名，包括政府门户、SaaS 工具、电商、医疗、金融、媒体和社交网络。
*   **任务类型**：等效的信息检索任务（例如：获取产品价格、提取新闻摘要等）。
*   **Benchmark**：对比了 **Playwright**（主流浏览器自动化框架）与 **Unbrowse**（缓存 API 调用）。
*   **对比维度**：执行延迟（毫秒）、加速比、冷启动成本、任务成功率。

### 4. 资源与算力
*   **硬件环境**：实验在单台 Apple MacBook Pro (M4 Max 芯片, 64GB 统一内存) 上完成。
*   **网络环境**：新加坡住宅宽带（约 28 Mbps 下行）。
*   **算力说明**：文中未提及大规模 GPU 集群训练，因为该系统侧重于推理和执行效率。它通过减少浏览器运行（每个实例约需 500MB RAM）和 LLM Token 消耗（无需解析 DOM）来节省算力。

### 5. 实验数量与充分性
*   **实验规模**：针对 94 个域名进行了预热缓存测试，针对 20 个新域名进行了冷启动测试。
*   **充分性**：实验涵盖了不同类别的网站，并使用了 3 次运行取平均值的方法以减少网络波动干扰。
*   **客观性**：对比是在相同的硬件和网络条件下进行的，且要求两种方法输出语义等效的结构化数据，确保了对比的公平性。但实验主要在单一地理位置（新加坡）运行，可能存在地域性的网络延迟偏差。

### 6 主要结论与发现
*   **显著提速**：在预热缓存情况下，Unbrowse 平均耗时 **950ms**，而 Playwright 需 **3404ms**。平均加速 **3.6 倍**，中位数加速达 **5.4 倍**。
*   **极端性能**：部分优化良好的路由可在 **100ms** 内完成，最高加速比达 **30 倍**。
*   **成本优势**：一旦安装了路由技能，后续任务的成本可降低 **90-96%**。
*   **冷启动代价**：首次发现路由平均需 **12.4s**，但该成本通过 3-5 次复用即可摊平。

### 7. 优点
*   **效率革命**：跳过了 DOM 解析和 UI 交互，极大地提升了智能体的响应速度。
*   **激励相容**：通过微支付模型解决了“谁来维护 API 路由”的动力问题，并为网站所有者提供了新的变现渠道。
*   **架构解耦**：三路径模型保证了系统的鲁棒性，即使 API 失效也能自动回退到浏览器模式重新学习。

### 8. 不足与局限
*   **反爬虫挑战**：虽然 API 调用更快，但仍需面对高级的 Web 应用防火墙（WAF）和机器人检测。
*   **动态认证**：对于需要复杂人工交互（如 OAuth、验证码）的认证流程，自动化提取仍有难度。
*   **安全风险**：共享图中可能存在恶意贡献者发布的虚假路由（虽然有本地执行和验证机制作为缓冲）。
*   **法律边界**：非官方 API 的使用在某些司法管辖区可能处于法律灰色地带（尽管论文提出了网站所有者分成模型来缓解这一问题）。

（完）
