---
title: "CirrusBench: Evaluating LLM-based Agents Beyond Correctness in Real-World Cloud Service Environments"
title_zh: CirrusBench：在真实云服务环境中评估超越正确性的大语言模型智能体
authors: "Yi Yu, Guangquan Hu, Chenghuang Shen, Xingyan Liu, Jing Gu, Hangyi Sun, Junzhuo Ma, Weiting Liu, Jianfeng Liu, Mingyue Pu, Yu Wang, Zhengdong Xiao, Rui Xie, Longjiu Luo, Qianrong Wang, Gurong Cui, Honglin Qiao, Wenlian Lu"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28569v1"
tags: ["query:agent"]
score: 9.0
evidence: 在真实云服务环境中评估基于大语言模型的智能体
tldr: 针对现有LLM智能体评估基准多依赖合成环境且忽视效率的问题，本文提出CirrusBench。该框架基于真实云服务工单数据，保留了复杂的多轮逻辑链和工具依赖。除了执行正确性，它还引入了以客户为中心的效率指标（如归一化效率指数）。实验表明，现有顶尖模型在处理复杂真实任务时效率不足，为未来智能体开发指明了方向。
selection_source: fresh_fetch
motivation: 现有基准缺乏真实云服务环境的复杂性、不可预测性以及对解决效率的关注。
method: 构建基于真实云服务工单的评估框架，并引入归一化效率指数和多轮延迟等以客户为中心的衡量指标。
result: 实验发现即使是顶尖模型在复杂多轮任务中也难以达到实际客户服务所需的高效率标准。
conclusion: CirrusBench填补了真实场景下智能体评估的空白，强调了在追求正确性的同时提升解决效率的重要性。
---

## 摘要
大语言模型（LLM）日益增强的智能体能力使其能够部署在真实应用中，例如云服务。在这些场景中，客户与助手之间的交互表现出极高的技术复杂性和长程依赖性，使得鲁棒性和解决效率对客户满意度至关重要。然而，现有的 LLM 智能体基准测试主要依赖于合成环境，无法捕捉真实客户输入的多样性和不可预测性，且往往忽略了真实部署中必不可少的解决效率。为了弥补这一差距，我们推出了 CirrusBench，这是一个基于真实云服务工单数据的创新评估框架。CirrusBench 保留了技术服务环境中固有的复杂多轮逻辑链和真实的工具依赖关系。除了执行正确性之外，我们还引入了全新的“以客户为中心”的指标来定义智能体的成功，通过归一化效率指数（Normalized Efficiency Index）和多轮延迟（Multi-Turn Latency）等指标量化服务质量，从而明确衡量解决效率。使用该框架进行的实验表明，尽管最先进的模型展示了强大的推理能力，但它们在复杂的真实多轮任务中经常表现挣扎，且无法满足客户服务所需的高效率标准，这为 LLM 智能体在实际技术服务应用中的未来发展指明了关键方向。CirrusBench 评估框架已发布于：https://github.com/CirrusAI。

## Abstract
The increasing agentic capabilities of Large Language Models (LLMs) have enabled their deployment in real-world applications, such as cloud services, where customer-assistant interactions exhibit high technical complexity and long-horizon dependencies, making robustness and resolution efficiency critical for customer satisfaction. However, existing benchmarks for LLM-based agents largely rely on synthetic environments that fail to capture the diversity and unpredictability of authentic customer inputs, often ignoring the resolution efficiency essential for real-world deployment. To bridge this gap, we introduce CirrusBench, a novel evaluation framework distinguished by its foundation in real-world data from authentic cloud service tickets. CirrusBench preserves the intricate multi-turn logical chains and realistic tool dependencies inherent to technical service environments. Moving beyond execution correctness, we introduce novel Customer-Centric metrics to define agent success, quantifying service quality through metrics such as the Normalized Efficiency Index and Multi-Turn Latency to explicitly measure resolution efficiency. Experiments utilizing our framework reveal that while state-of-the-art models demonstrate strong reasoning capabilities, they frequently struggle in complex, realistic multi-turn tasks and fail to meet the high-efficiency standards required for customer service, highlighting critical directions for the future development of LLM-based agents in practical technical service applications. CirrusBench evaluation framework is released at: https://github.com/CirrusAI

---

## 论文详细总结（自动生成）

### CirrusBench：在真实云服务环境中评估超越正确性的大语言模型智能体

#### 1. 核心问题与整体含义
*   **研究背景**：随着大语言模型（LLM）能力的提升，其作为智能体（Agent）被广泛部署于云服务等复杂技术领域。
*   **核心痛点**：
    1.  **环境失真**：现有基准测试多依赖合成数据或角色扮演模拟，无法还原真实客户交互中的多样性、不可预测性和“噪声”。
    2.  **评估维度单一**：现有评估侧重于任务完成的“正确性”，而忽略了对客户满意度至关重要的“解决效率”。在真实场景中，一个正确但过程极其冗长的方案往往被视为失败。
*   **研究目的**：构建一个基于真实世界云服务工单的评估框架，旨在衡量智能体在复杂、多轮交互中的鲁棒性与解决效率。

#### 2. 方法论
*   **核心思想**：利用真实历史工单构建任务集，并引入“以客户为中心”的效率指标。
*   **数据构建流程**：
    1.  **脱敏与去噪**：对 PII 信息进行脱敏，剔除冗余的寒暄。
    2.  **工具模拟（Tool Mock）**：采用“记录与重放”（Record-and-Replay）方法，将历史真实的工具调用及其结果标准化，使评估无需连接实时后端 API 即可复现。
    3.  **关键点标注（Checkpoint Annotation）**：标注出涉及决策、意图识别或工具调用的关键轮次，并分类回复类型（如解决方案、询问、澄清等）。
    4.  **知识增强**：为每个任务关联最多 5 份相关的官方技术文档（SOP、手册）。
*   **动态评估算法**：
    *   **单轮子任务评估**：评估工具调用意识（TIA）、选择准确性（TSA）、执行有效性（TEV）及回复解决能力（RR）。
    *   **动态多轮评估**：如果智能体给出的回答偏离了标准路径，系统会通过“流利度检查器”（Fluency Checker）判断该偏离是否逻辑连贯。若连贯，则基于新状态继续对话；若不连贯或失败，则终止评估。

#### 3. 实验设计
*   **数据集/场景**：
    *   包含 1,500 个无需工具的任务和 425 个需调用工具的任务。
    *   涵盖 20 个真实云服务类别（如服务器、域名注册、短信服务、网站问题等）。
    *   输入 Token 长度跨度大（520 至 37,000+ Token），平均约 1.1 万 Token。
*   **对比方法（Benchmark）**：
    *   评估了多个 SOTA 模型系列：**Qwen 系列**（Qwen3-235B, Qwen3-Max）、**GPT 系列**（GPT-4o, GPT-5 预览版等）、**DeepSeek 系列**（V3.2, R1）。
    *   区分了“思考型”（Thinking/Reasoning）模型与普通指令微调模型。

#### 4. 资源与算力
*   **算力说明**：论文未明确提及模型训练所需的 GPU 型号、数量或时长。
*   **评估成本**：由于实验主要通过 API 调用进行，重点在于推理阶段的评估。论文提到为了平衡成本与准确性，选用了 DeepSeek-V3.2 作为评估器（Evaluator）的底座模型。

#### 5. 实验数量与充分性
*   **实验规模**：对 9 种主流大模型进行了全面测试，涵盖了 Pass@1 和 Pass@2 成功率。
*   **分析维度**：进行了多维度的深入分析，包括输入长度影响、交互轮数（Checkpoints）影响、服务类别差异以及回复类型分析。
*   **客观性**：通过人工专家对 141 条标注进行校验，证明自动评估器的准确率达 91.49%，确保了实验结果的可靠性。

#### 6. 论文的主要结论与发现
*   **工具集成是核心瓶颈**：所有模型在涉及工具调用的任务中表现均显著差于纯对话任务。
*   **“思考”模型的双刃剑效应**：显式推理模型（如 R1）虽然在某些逻辑任务上有优势，但往往带来极高的延迟（AMTL 显著增加），且在真实工具调用场景中，成功率提升并不成比例，甚至可能下降。
*   **长程交互的脆弱性**：随着交互轮数增加，错误累积效应明显，模型维持逻辑一致性的能力大幅下降。
*   **过早结案倾向**：模型在“询问”和“澄清”类型任务上表现最差，倾向于在信息不足时直接给出（可能错误的）解决方案，缺乏主动获取信息的策略。

#### 7. 优点
*   **高生态效度**：数据完全源自真实工单，保留了原始输入的“高熵”特征（如包含系统噪声的 OCR 文本）。
*   **评估维度创新**：引入了 **NEI（归一化效率指数）** 和 **MTL（多轮延迟）**，填补了智能体评估中“效率”维度的空白。
*   **动态路径支持**：评估框架允许模型偏离预设路径，更符合真实对话的动态特性。

#### 8. 不足与局限
*   **多模态处理简化**：将截图统一通过 OCR 转为文本处理，虽然降低了评估复杂度，但可能丢失了 UI 布局等视觉语义信息。
*   **裁判模型偏见**：尽管经过校验，但评估高度依赖于另一个 LLM 作为裁判，可能存在“自我偏好”或特定模型的评分偏见。
*   **行业局限性**：虽然在云服务领域非常深入，但其指标和任务构造是否能完美迁移到医疗、法律等其他专业咨询领域仍需验证。

（完）
