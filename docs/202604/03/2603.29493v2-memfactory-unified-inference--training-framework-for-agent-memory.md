---
title: "MemFactory: Unified Inference & Training Framework for Agent Memory"
title_zh: MemFactory：智能体记忆的统一推理与训练框架
authors: "Ziliang Guo, Ziheng Li, Bo Tang, Feiyu Xiong, Zhiyu Li"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29493v2"
tags: ["query:agent"]
score: 10.0
evidence: 使用强化学习的智能体记忆统一框架
tldr: "MemFactory是首个针对记忆增强型智能体设计的统一训练与推理框架。针对当前记忆管理研究碎片化且缺乏通用工具的问题，该框架借鉴LLaMA-Factory的模块化理念，将记忆生命周期抽象为可插拔的原子组件，并原生集成GRPO算法以优化记忆策略。实验证明，MemFactory能显著提升智能体性能，最高增益达14.8%，为记忆驱动的AI研究提供了标准化的基础设施。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29493v2/fig-001.webp\", \"caption\": \"Figure 1 The overall architecture of MemFactory, illustrating the interdependencies among the Modules, Agent, Environment, and Trainer layers.\", \"page\": 4, \"index\": 1, \"width\": 929, \"height\": 482}, {\"url\": \"assets/figures/arxiv/2603.29493v2/fig-002.webp\", \"caption\": \"Table 1 Standard interfaces supported by modules in the Module Layer.\", \"page\": 5, \"index\": 2, \"width\": 968, \"height\": 219}]"
motivation: 现有的记忆增强型智能体实现高度碎片化且缺乏统一的训练与推理框架，阻碍了复杂记忆管理策略的开发与评估。
method: 设计了类似“乐高”的模块化架构，将记忆操作抽象为原子组件，并利用组相对策略优化（GRPO）算法微调记忆管理策略。
result: "在MemAgent架构上的验证显示，该框架在多种评估集下均优于基座模型，性能相对提升最高达14.8%。"
conclusion: MemFactory通过提供标准化且易用的基础设施，显著降低了记忆增强型智能体的开发门槛，推动了该领域的创新。
---

## 摘要
记忆增强型大语言模型（LLMs）对于开发具备强大能力的长期 AI 智能体至关重要。近期，应用强化学习（RL）来优化记忆操作（如提取、更新和检索）已成为一个极具前景的研究方向。然而，现有的实现方式仍然高度碎片化且针对特定任务，缺乏统一的基础设施来简化这些复杂流水线的集成、训练和评估。为填补这一空白，我们提出了 MemFactory，这是首个专为记忆增强型智能体设计的统一、高度模块化的训练与推理框架。受 LLaMA-Factory 等统一微调框架成功的启发，MemFactory 将记忆生命周期抽象为原子化的即插即用组件，使研究人员能够通过“乐高式”架构无缝构建自定义记忆智能体。此外，该框架原生集成了群体相对策略优化（GRPO），以在多维环境奖励的驱动下微调内部记忆管理策略。MemFactory 为 Memory-R1、RMM 和 MemAgent 等近期前沿范式提供开箱即用的支持。我们在开源的 MemAgent 架构上，利用其公开的训练和评估数据对 MemFactory 进行了实证验证。在域内和分布外评估集上，MemFactory 相比相应的基座模型均能持续提升性能，相对增益最高达 14.8%。通过提供标准化、可扩展且易于使用的基础设施，MemFactory 显著降低了准入门槛，为记忆驱动型 AI 智能体的未来创新铺平了道路。

## Abstract
Memory-augmented Large Language Models (LLMs) are essential for developing capable, long-term AI agents. Recently, applying Reinforcement Learning (RL) to optimize memory operations, such as extraction, updating, and retrieval, has emerged as a highly promising research direction. However, existing implementations remain highly fragmented and task-specific, lacking a unified infrastructure to streamline the integration, training, and evaluation of these complex pipelines. To address this gap, we present MemFactory, the first unified, highly modular training and inference framework specifically designed for memory-augmented agents. Inspired by the success of unified fine-tuning frameworks like LLaMA-Factory, MemFactory abstracts the memory lifecycle into atomic, plug-and-play components, enabling researchers to seamlessly construct custom memory agents via a "Lego-like" architecture. Furthermore, the framework natively integrates Group Relative Policy Optimization (GRPO) to fine-tune internal memory management policies driven by multi-dimensional environmental rewards. MemFactory provides out-of-the-box support for recent cutting-edge paradigms, including Memory-R1, RMM, and MemAgent. We empirically validate MemFactory on the open-source MemAgent architecture using its publicly available training and evaluation data. Across both in-domain and out-of-distribution evaluation sets, MemFactory consistently improves performance over the corresponding base models, with relative gains of up to 14.8%. By providing a standardized, extensible, and easy-to-use infrastructure, MemFactory significantly lowers the barrier to entry, paving the way for future innovations in memory-driven AI agents.

---

## 论文详细总结（自动生成）

## MemFactory: 智能体记忆的统一推理与训练框架论文总结

### 1. 核心问题与研究动机
*   **核心问题**：尽管记忆增强型大语言模型（LLM）是构建长期 AI 智能体的关键，但现有的记忆管理研究（如通过强化学习优化记忆的提取、更新和检索）存在**高度碎片化**的问题。
*   **研究背景**：
    *   早期的 RAG 或启发式规则难以处理长期的记忆冗余和冲突。
    *   新兴的强化学习（RL）方法（如 Memory-R1, MemAgent）虽然效果显著，但代码实现通常与特定任务深度耦合，缺乏统一的基础设施。
    *   **动机**：借鉴 LLaMA-Factory 在模型微调领域的成功，开发一个模块化、标准化的框架，以降低记忆增强型智能体的开发、训练和评估门槛。

### 2. 方法论
MemFactory 采用“乐高式”的模块化设计，将复杂的记忆生命周期拆解为四个核心层：
*   **Module Layer（模块层）**：定义了原子化的记忆操作，包括：
    *   **Extractor**：从原始上下文中解析事实和经验。
    *   **Updater**：执行增（ADD）、删（DEL）、改（UPDATE）等操作维护记忆库。
    *   **Retriever**：负责语义检索及基于推理模型的重排序（Rerank）。
    *   **Agent Module**：支持端到端的循环记忆策略（如 MemAgent 范式）。
*   **Agent Layer（智能体层）**：作为策略执行器，将上述模块组合成完整的智能体，支持训练模式（generate/rollout）和纯推理模式。
*   **Environment Layer（环境层）**：负责数据加载和多维奖励计算（包括格式奖励和 LLM-as-a-Judge 评分）。
*   **Trainer Layer（训练层）**：核心优化引擎，原生集成 **GRPO（Group Relative Policy Optimization）** 算法。
    *   **技术细节**：GRPO 通过组内奖励归一化来估计基准线，无需额外的 Critic 网络，极大地节省了显存，适合长上下文的记忆训练任务。

### 3. 实验设计
*   **数据集/场景**：使用了 MemAgent 发布的公开训练和评估数据集，专注于长文本处理任务。
*   **Benchmark**：
    *   **eval_50 / eval_100**：主任务评估集。
    *   **eval_fwe_16384**：分布外（OOD）评估集，用于测试泛化能力。
*   **对比方法**：以 **Qwen3-1.7B** 和 **Qwen3-4B-Instruct** 作为基座模型，对比其原始 Checkpoint 与经过 MemFactory RL 微调后的性能表现。

### 4. 资源与算力
*   **硬件环境**：实验明确指出整个训练和评估流水线可以在**单张 NVIDIA A800 80GB GPU** 上运行。
*   **训练细节**：
    *   每个模型训练 250 个有效步数（valid steps）。
    *   为了提高效率，对训练样本进行了简化（每样本包含 50-80 个文档），约为原始长度的三分之一。

### 5. 实验数量与充分性
*   **实验规模**：针对两个不同规模的模型在三个测试集上进行了对比。
*   **客观性与公平性**：
    *   所有评分均采用 **avg@4**（4 次独立试验的平均值），以减少 LLM 生成随机性带来的偏差。
    *   实验涵盖了域内和分布外（OOD）场景，验证了框架的稳健性。
*   **充分性评价**：实验设计较为客观，证明了框架在有限算力下的有效性。但对比的基座模型种类较少（仅限于 Qwen3 系列），且尚未在大规模集群上展示扩展性。

### 6. 主要结论与发现
*   **性能提升**：MemFactory 显著提升了基座模型的记忆管理能力。Qwen3-1.7B 的平均性能提升了 **14.8%**，Qwen3-4B-Instruct 提升了 **7.3%**。
*   **模型规模效应**：较大的模型（4B）在 OOD 任务上表现更稳定，能够更好地将学习到的记忆策略迁移到未见过的设置中。
*   **框架有效性**：验证了通过 GRPO 优化记忆策略的可行性，证明了统一框架可以复现并超越现有的 SOTA 记忆范式。

### 7. 优点
*   **高度模块化**：通过标准接口实现“即插即用”，极大地方便了研究者组合不同的提取、更新和检索策略。
*   **显存优化**：集成 GRPO 算法，解决了记忆增强任务中上下文窗口占用大、显存易溢出的痛点。
*   **生态兼容**：开箱即用支持 Memory-R1、RMM 和 MemAgent 等主流算法，具有很强的实用价值。

### 8. 不足与局限
*   **范式覆盖**：虽然支持了主流范式，但对于一些极新兴或极其复杂的记忆架构（如多级分层记忆）可能仍需额外开发。
*   **训练效率**：尽管单卡可行，但在处理超长序列时的吞吐量仍有优化空间。
*   **实验广度**：目前的实证主要集中在 MemAgent 任务上，未来需在更多样化的智能体任务（如多步规划、复杂工具调用）中验证其通用性。

（完）
