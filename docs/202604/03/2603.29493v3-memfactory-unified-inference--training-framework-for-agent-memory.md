---
title: "MemFactory: Unified Inference & Training Framework for Agent Memory"
title_zh: MemFactory：智能体记忆的统一推理与训练框架
authors: "Ziliang Guo, Ziheng Li, Bo Tang, Feiyu Xiong, Zhiyu Li"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29493v3"
tags: ["query:agent"]
score: 10.0
evidence: 强化学习优化的智能体记忆操作统一框架
tldr: MemFactory是一个专为记忆增强型智能体设计的统一推理与训练框架。针对现有记忆管理实现碎片化且缺乏标准的问题，该框架借鉴LLaMA-Factory的成功经验，将记忆生命周期抽象为模块化、可插拔的组件。它原生集成GRPO算法，支持通过强化学习优化记忆提取与检索策略，并兼容Memory-R1等前沿范式，显著降低了高性能记忆智能体的开发门槛。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29493v3/fig-001.webp\", \"caption\": \"Figure 1 The overall architecture of MemFactory, illustrating the interdependencies among the Modules, Agent, Environment, and Trainer layers.\", \"page\": 4, \"index\": 1, \"width\": 929, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2603.29493v3/fig-002.webp\", \"caption\": \"Table 1 Standard interfaces supported by modules in the Module Layer.\", \"page\": 5, \"index\": 2, \"width\": 968, \"height\": 219}]"
motivation: 现有的记忆增强型智能体实现高度碎片化且缺乏统一的基础设施，导致记忆管理策略的集成、训练和评估效率低下。
method: 提出一种类似“乐高”的模块化架构，将记忆操作抽象为原子组件，并利用组相对策略优化（GRPO）算法对记忆管理策略进行微调。
result: "在MemAgent架构上的实验验证显示，该框架相比基础模型实现了显著的性能提升，最高相对增益达14.8%。"
conclusion: MemFactory通过提供标准化、可扩展且易用的基础设施，为记忆驱动型AI智能体的未来创新铺平了道路。
---

## 摘要
记忆增强的大语言模型（LLMs）对于开发具备强大能力的长期 AI 智能体至关重要。近年来，应用强化学习（RL）来优化记忆操作（如提取、更新和检索）已成为一个极具前景的研究方向。然而，现有的实现方式仍然高度碎片化且针对特定任务，缺乏统一的基础设施来简化这些复杂流水线的集成、训练和评估。为填补这一空白，我们提出了 MemFactory，这是首个专为记忆增强智能体设计的统一、高度模块化的训练与推理框架。受 LLaMA-Factory 等统一微调框架成功的启发，MemFactory 将记忆生命周期抽象为原子化的、即插即用的组件，使研究人员能够通过“乐高式”架构无缝构建自定义记忆智能体。此外，该框架原生集成了群体相对策略优化（GRPO），以在多维环境奖励的驱动下微调内部记忆管理策略。MemFactory 为 Memory-R1、RMM 和 MemAgent 等近期前沿范式提供开箱即用的支持。我们在开源的 MemAgent 架构上，利用其公开的训练和评估数据对 MemFactory 进行了实证验证。在各个评估集上，MemFactory 的平均性能优于相应的基座模型，相对提升最高达 14.8%。通过提供标准化、可扩展且易于使用的基础设施，MemFactory 显著降低了准入门槛，为记忆驱动型 AI 智能体的未来创新铺平了道路。

## Abstract
Memory-augmented Large Language Models (LLMs) are essential for developing capable, long-term AI agents. Recently, applying Reinforcement Learning (RL) to optimize memory operations, such as extraction, updating, and retrieval, has emerged as a highly promising research direction. However, existing implementations remain highly fragmented and task-specific, lacking a unified infrastructure to streamline the integration, training, and evaluation of these complex pipelines. To address this gap, we present MemFactory, the first unified, highly modular training and inference framework specifically designed for memory-augmented agents. Inspired by the success of unified fine-tuning frameworks like LLaMA-Factory, MemFactory abstracts the memory lifecycle into atomic, plug-and-play components, enabling researchers to seamlessly construct custom memory agents via a "Lego-like" architecture. Furthermore, the framework natively integrates Group Relative Policy Optimization (GRPO) to fine-tune internal memory management policies driven by multi-dimensional environmental rewards. MemFactory provides out-of-the-box support for recent cutting-edge paradigms, including Memory-R1, RMM, and MemAgent. We empirically validate MemFactory on the open-source MemAgent architecture using its publicly available training and evaluation data. Across the evaluation sets, MemFactory improves performance over the corresponding base models on average, with relative gains of up to 14.8%. By providing a standardized, extensible, and easy-to-use infrastructure, MemFactory significantly lowers the barrier to entry, paving the way for future innovations in memory-driven AI agents.

---

## 论文详细总结（自动生成）

以下是对论文《MemFactory: Unified Inference & Training Framework for Agent Memory》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：尽管记忆增强型大语言模型（LLM）是构建长期自主智能体的关键，但目前的记忆管理研究（如提取、更新、检索）存在**实现碎片化**和**任务特定化**的问题。
*   **研究动机**：现有的记忆优化方法（如 Memory-R1, MemAgent）通常耦合在特定的代码库中，缺乏一个像 LLaMA-Factory 那样统一、模块化且支持强化学习（RL）训练的基础设施，导致研究人员难以复现、组合或扩展不同的记忆策略。

### 2. 方法论
MemFactory 提出了一个四层模块化架构，旨在实现“乐高式”的智能体构建：
*   **模块层 (Module Layer)**：将记忆生命周期抽象为原子操作，包括：
    *   **Extractor**：从原始上下文中解析事实和经验。
    *   **Updater**：执行增删改（ADD/DEL/UPDATE/NONE）操作维护记忆库。
    *   **Retriever**：负责语义检索或通过推理模型进行重排序（Rerank）。
    *   **Agent Module**：支持端到端的循环记忆策略（如 MemAgent）。
*   **智能体层 (Agent Layer)**：作为策略执行器，负责加载预训练模型、执行 Rollout（生成交互轨迹）和推理。
*   **环境层 (Environment Layer)**：充当数据加载器和奖励管理器，提供格式奖励和“LLM-as-a-Judge”等多维奖励信号。
*   **训练层 (Trainer Layer)**：核心优化引擎，原生集成 **GRPO（群体相对策略优化）** 算法。
    *   **关键技术**：GRPO 通过在一组采样响应中进行奖励归一化来计算优势（Advantage），无需额外的 Critic 网络，显著降低了长上下文训练时的显存开销。

### 3. 实验设计
*   **实验场景**：基于 **MemAgent** 架构进行验证，该架构将记忆视为跨文本段落的循环状态。
*   **数据集**：使用 MemAgent 公开的训练和评估集，包括两个主任务集（eval_50, eval_100）和一个分布外（OOD）测试集（eval_fwe_16384）。
*   **对比方法**：将经过 MemFactory RL 微调后的模型与原始基座模型（Base Checkpoint）进行对比。
*   **基座模型**：选用 Qwen3-1.7B 和 Qwen3-4B-Instruct。

### 4. 资源与算力
*   **硬件设备**：整个训练和评估流水线可以在**单张 NVIDIA A800 80GB GPU** 上运行。
*   **训练细节**：每个模型训练 250 个有效步（steps）。为了适配单卡显存，研究者对训练样本进行了简化，将上下文长度缩减至原有的约 1/3（每样本包含 50-80 个文档）。

### 5. 实验数量与充分性
*   **实验规模**：针对两个不同规模的基座模型在三个测试集上进行了测试，每组实验均采用 4 次独立试验的平均分（avg@4）。
*   **充分性评价**：实验涵盖了主任务和 OOD 任务，验证了框架的泛化能力。虽然主要针对 MemAgent 范式进行了深入实证，但框架本身已内置了对 Memory-R1 和 RMM 的支持，展示了较好的覆盖面。不过，论文中未提供针对 Memory-R1 等其他范式的详细对比曲线。

### 6. 主要结论与发现
*   **性能提升**：MemFactory 显著提升了基座模型的记忆管理能力。Qwen3-1.7B 的平均性能提升了 **14.8%**；Qwen3-4B-Instruct 提升了 **7.3%**。
*   **模型规模影响**：较大规模的模型（4B）在 RL 微调后表现更稳健，不仅在主任务上进步，在 OOD 任务上也实现了正向迁移；而较小模型（1.7B）在 OOD 任务上出现了轻微性能下降。
*   **可行性验证**：证明了在有限算力（单卡）下，通过统一框架和 GRPO 算法优化复杂的长上下文记忆策略是切实可行的。

### 7. 优点
*   **高度模块化**：首个将记忆生命周期标准化的框架，支持“即插即用”地更换提取器或检索器。
*   **训练效率高**：通过集成 GRPO 解决了记忆增强智能体训练中显存占用过高的痛点。
*   **开箱即用**：预置了多种前沿记忆算法（Memory-R1, RMM 等）的实现，极大降低了研究门槛。

### 8. 不足与局限
*   **范式覆盖有限**：虽然支持三大主流范式，但对于更复杂的、涉及外部工具调用的记忆交互支持尚待加强。
*   **训练效率仍有提升空间**：论文提到在处理极长序列时的训练速度仍有优化余地。
*   **小模型泛化风险**：实验显示小模型在经过特定任务的 RL 优化后，可能存在分布外泛化能力减弱的风险。

（完）
