---
title: "ReLope: KL-Regularized LoRA Probes for Multimodal LLM Routing"
title_zh: ReLope：用于多模态大语言模型路由的 KL 正则化 LoRA 探针
authors: "Yaopei Zeng, Congchao Wang, Blake JianHang Chen, Lu Lin"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24787v1"
tags: ["query:mmkqa"]
score: 7.0
evidence: 多模态大语言模型路由与性能平衡
tldr: 本研究针对多模态大模型（MLLM）路由中性能与成本平衡的问题，发现传统探测路由在处理视觉输入时效果不佳。为此，作者提出了两种改进方案：Attention Probe通过注意力机制聚合隐藏状态以恢复正确性信号，而ReLope则利用KL正则化的LoRA适配器学习路由感知表示。实验证明，这些方法能显著提升路由效率，证明了优化隐藏状态质量对多模态路由的重要性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.24787v1/fig-001.webp\", \"caption\": \"Table 2: AUC (%) comparison of routing methods on five multimodal benchmarks: MMMU, AOKVQA, ScienceQA, ChartQA, and MathVision. Results are reported for three backbone MLLMs: Qwen2.5-VL-7B-Instruct, Gemma312B, and Phi-4-Multimodal-Instruct. Higher values indicate better routing quality.\", \"page\": 6, \"index\": 1, \"width\": 929, \"height\": 754}, {\"url\": \"assets/figures/arxiv/2603.24787v1/fig-002.webp\", \"caption\": \"Figure 2: Overall system accuracy as a function of routing ratio. The routing ratio denotes the percentage of queries routed to the large model in a hybrid MLLM system. Higher curves indicate better routing strategies that achieve stronger trade-offs bewteen accuracy and cost by sending only the most difficult queries to the large model.\", \"page\": 7, \"index\": 2, \"width\": 940, \"height\": 199}, {\"url\": \"assets/figures/arxiv/2603.24787v1/fig-003.webp\", \"caption\": \"Figure 3: Ablation studies of ReLope. (a) Effect of the LoRA rank r. (b) Effect of the transformer layer l used for probe placement. (c) Effect of the VIB coefficient β. Results are reported as AUC on five multimodal benchmarks.\", \"page\": 8, \"index\": 3, \"width\": 934, \"height\": 245}, {\"url\": \"assets/figures/arxiv/2603.24787v1/fig-004.webp\", \"caption\": \"Figure 1: ReLope pipeline. Given an input sample (x, y), the MLLM fθ produces hidden states Z(l−1) at layer l−1. A lightweight LoRA adapter θLoRA is inserted into the model to obtain adapted hidden states Z̃(l). The adapted last-token representation z = z̃ (l) n is then used as the routing feature. It is fed into the probe gϕ to predict correctness ŷ for routing. In parallel, two linear heads map z to (µ, logσ2), which parameterize a Gaussian posterior for a variational bottleneck. The training objective combines cross-entropy loss with a KL regularizer, encouraging the learned representation to preserve correctness-related information while filtering out task-irrelevant variation.\", \"page\": 4, \"index\": 4, \"width\": 936, \"height\": 229}]"
motivation: 传统探测路由在多模态场景下因视觉输入干扰了隐藏状态中的正确性信号，导致路由性能显著下降。
method: 提出了Attention Probe聚合隐藏状态，以及ReLope通过LoRA适配器和KL正则化来学习更具辨别力的路由表示。
result: 实验结果显示，所提方法在多模态路由任务上一致优于现有基准方法。
conclusion: 提升隐藏状态的质量是实现多模态大模型高效路由的关键所在。
---

## 摘要
路由已成为平衡大语言模型（LLM）系统性能与成本的一种极具前景的策略，该系统通常结合了轻量级模型与功能强大但昂贵的大型模型。最近的研究表明，探针路由（probe routing）通过利用隐藏状态预测小模型的正确性，在纯文本 LLM 中提供了一种有效的解决方案。然而，我们观察到，当这些探针应用于多模态大语言模型（MLLM）时，其性能会大幅下降。通过实证分析，我们发现视觉输入的引入削弱了隐藏状态中正确性信号的可分性，使得使用标准探针设计难以提取这些信号。为了应对这一挑战，我们引入了两种互补的方法来改进 MLLM 中的探针路由。首先，我们提出了注意力探针（Attention Probe），它根据注意力分数聚合前一层的隐藏状态，以恢复分布式的正确性信号。其次，我们提出了 KL 正则化 LoRA 探针（ReLope），它插入了一个轻量级的 LoRA 适配器并应用 KL 正则化项，以学习路由感知的表示。全面的实验表明，我们的方法始终优于基准模型，这表明提高隐藏状态的质量是实现 MLLM 有效路由的关键。我们的代码可在 https://github.com/Spinozaaa/ReLope 获取。

## Abstract
Routing has emerged as a promising strategy for balancing performance and cost in large language model (LLM) systems that combine lightweight models with powerful but expensive large models. Recent studies show that \emph{probe routing}, which predicts the correctness of a small model using its hidden states, provides an effective solution in text-only LLMs. However, we observe that these probes degrade substantially when applied to multimodal LLMs (MLLMs). Through empirical analysis, we find that the presence of visual inputs weakens the separability of correctness signals in hidden states, making them harder to extract using standard probe designs. To address this challenge, we introduce two complementary approaches for improving probe routing in MLLMs. First, we propose the \emph{Attention Probe}, which aggregates hidden states from the preceding layer based on attention scores to recover distributed correctness signals. Second, we present the \emph{KL-Regularized LoRA Probe (ReLope)}, which inserts a lightweight LoRA adapter and applies a KL regularizer to learn routing-aware representations. Comprehensive experiments show that our methods consistently outperform baselines, suggesting that improving the quality of hidden states is key to effective routing in MLLMs. Our code is available at https://github.com/Spinozaaa/ReLope.