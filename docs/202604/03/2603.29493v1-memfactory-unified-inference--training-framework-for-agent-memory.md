---
title: "MemFactory: Unified Inference & Training Framework for Agent Memory"
title_zh: MemFactory：智能体记忆的统一推理与训练框架
authors: "Ziliang Guo, Ziheng Li, Zhiyu Li"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29493v1"
tags: ["query:agent"]
score: 10.0
evidence: 使用强化学习优化记忆操作的统一智能体记忆框架
tldr: "MemFactory是首个专为记忆增强型智能体设计的统一推理与训练框架。针对记忆管理研究碎片化的问题，它采用模块化设计将记忆操作抽象为可插拔组件，并集成GRPO算法优化管理策略。该框架支持多种前沿范式，在实验中最高提升性能14.8%，显著降低了开发门槛并提供了标准化的基础设施。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29493v1/fig-001.webp\", \"caption\": \"Figure 1 The overall architecture of MemFactory, illustrating the interdependencies among the Modules, Agent, Environment, and Trainer layers.\", \"page\": 4, \"index\": 1, \"width\": 929, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2603.29493v1/fig-002.webp\", \"caption\": \"Table 1 Standard interfaces supported by modules in the Module Layer.\", \"page\": 5, \"index\": 2, \"width\": 968, \"height\": 221}]"
motivation: 现有的记忆增强型智能体实现高度碎片化且缺乏统一的基础设施，导致集成、训练和评估过程异常复杂。
method: 提出一种模块化的“乐高式”架构，将记忆生命周期抽象为原子组件，并利用GRPO算法根据环境奖励优化内部记忆管理策略。
result: "在MemAgent基准测试中，MemFactory在域内和分布外评估中均优于基础模型，实现了最高14.8%的相对性能增益。"
conclusion: MemFactory通过提供标准化、可扩展且易用的基础设施，显著降低了技术门槛，为未来记忆驱动的AI智能体创新铺平了道路。
---

## 摘要
记忆增强的大语言模型（LLMs）对于开发具备强大能力的长期 AI 智能体至关重要。近年来，应用强化学习（RL）来优化记忆操作（如提取、更新和检索）已成为一个极具前景的研究方向。然而，现有的实现方式仍然高度碎片化且针对特定任务，缺乏统一的基础设施来简化这些复杂流水线的集成、训练和评估。为填补这一空白，我们推出了 MemFactory，这是首个专为记忆增强智能体设计的统一、高度模块化的训练与推理框架。受 LLaMA-Factory 等统一微调框架成功的启发，MemFactory 将记忆生命周期抽象为原子化的即插即用组件，使研究人员能够通过“乐高式”架构无缝构建自定义记忆智能体。此外，该框架原生集成了群体相对策略优化（GRPO），以在多维环境奖励的驱动下微调内部记忆管理策略。MemFactory 为 Memory-R1、RMM 和 MemAgent 等近期前沿范式提供开箱即用的支持。我们在开源的 MemAgent 架构上，利用其公开的训练和评估数据对 MemFactory 进行了实证验证。在域内和分布外评估集上，MemFactory 相比相应的基座模型均能持续提升性能，相对增益最高达 14.8%。通过提供标准化、可扩展且易于使用的基础设施，MemFactory 显著降低了准入门槛，为记忆驱动型 AI 智能体的未来创新铺平了道路。

## Abstract
Memory-augmented Large Language Models (LLMs) are essential for developing capable, long-term AI agents. Recently, applying Reinforcement Learning (RL) to optimize memory operations, such as extraction, updating, and retrieval, has emerged as a highly promising research direction. However, existing implementations remain highly fragmented and task-specific, lacking a unified infrastructure to streamline the integration, training, and evaluation of these complex pipelines. To address this gap, we present MemFactory, the first unified, highly modular training and inference framework specifically designed for memory-augmented agents. Inspired by the success of unified fine-tuning frameworks like LLaMA-Factory, MemFactory abstracts the memory lifecycle into atomic, plug-and-play components, enabling researchers to seamlessly construct custom memory agents via a "Lego-like" architecture. Furthermore, the framework natively integrates Group Relative Policy Optimization (GRPO) to fine-tune internal memory management policies driven by multi-dimensional environmental rewards. MemFactory provides out-of-the-box support for recent cutting-edge paradigms, including Memory-R1, RMM, and MemAgent. We empirically validate MemFactory on the open-source MemAgent architecture using its publicly available training and evaluation data. Across both in-domain and out-of-distribution evaluation sets, MemFactory consistently improves performance over the corresponding base models, with relative gains of up to 14.8%. By providing a standardized, extensible, and easy-to-use infrastructure, MemFactory significantly lowers the barrier to entry, paving the way for future innovations in memory-driven AI agents.

---

## 论文详细总结（自动生成）

以下是对论文《MemFactory: Unified Inference & Training Framework for Agent Memory》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：虽然记忆增强型大语言模型（LLMs）是构建长期 AI 智能体的关键，但现有的记忆管理研究（如利用强化学习优化记忆的提取、更新和检索）存在**高度碎片化**的问题。
*   **研究动机**：目前的实现通常针对特定任务且代码耦合严重，缺乏像 LLaMA-Factory 那样统一、模块化的基础设施，导致研究人员在集成、训练和评估不同的记忆策略时面临极高的工程门槛。

### 2. 方法论
*   **核心思想**：借鉴“乐高式”架构，将复杂的记忆生命周期抽象为原子化的、可插拔的组件，构建一个统一的训练与推理框架。
*   **四层架构设计**：
    *   **模块层 (Module Layer)**：定义了记忆操作的原子类，包括 **Extractor**（提取事实）、**Updater**（管理增删改）、**Retriever**（语义检索及重排序）和 **Agent Module**（支持端到端循环记忆）。
    *   **智能体层 (Agent Layer)**：作为策略执行者，负责组装模块、加载预训练模型并生成交互轨迹（Rollout）。
    *   **环境层 (Environment Layer)**：充当数据加载器和奖励管理器，将原始数据标准化，并提供多维奖励信号（如格式奖励、LLM-as-a-Judge 评分）。
    *   **训练层 (Trainer Layer)**：核心优化引擎，原生集成 **GRPO（群体相对策略优化）** 算法。GRPO 通过组内奖励归一化估计基准，无需额外的 Critic 网络，显著降低了长上下文训练时的显存开销。

### 3. 实验设计
*   **数据集与场景**：使用 MemAgent 提供的公开数据集，涵盖长文本处理和多轮对话场景。
*   **Benchmark**：
    *   **域内测试**：`eval_50` 和 `eval_100`（分别包含 50 和 100 个文档的上下文）。
    *   **分布外（OOD）测试**：`eval_fwe_16384`（测试模型在未见过的长上下文分布下的泛化能力）。
*   **对比方法**：以 Qwen3-1.7B 和 Qwen3-4B-Instruct 为基座模型，对比其原始版本与经过 MemFactory RL 优化后的性能。
*   **支持范式**：框架内置了对 Memory-R1、MemAgent 和 RMM 等前沿算法的开箱即用支持。

### 4. 资源与算力
*   **硬件设备**：实验在**单张 NVIDIA A800 80GB GPU** 上完成。
*   **训练细节**：每个模型训练 250 个有效步（steps）。为了提高效率，训练时将上下文长度简化为原始数据的约 1/3（每样本 50-80 个文档）。
*   **结论**：该框架证明了在有限的单卡算力下，也能实现复杂的 Memory-RL 策略优化。

### 5. 实验数量与充分性
*   **实验规模**：针对两种不同参数规模的模型（1.7B 和 4B）在三个独立测试集上进行了验证。
*   **客观性保障**：所有评分均采用 **avg@4**（对每个问题进行 4 次独立试验并取平均值），以减少 LLM 生成随机性带来的偏差。
*   **充分性**：实验覆盖了从基础性能提升到 OOD 泛化能力的评估，虽然实验组数不算庞大，但对于验证框架的有效性和复现性而言是充分且客观的。

### 6. 主要结论与发现
*   **性能提升**：MemFactory 显著提升了基座模型的记忆管理能力。Qwen3-1.7B 的平均性能提升了 **14.8%**，Qwen3-4B-Instruct 提升了 **7.3%**。
*   **模型规模效应**：较大规模的模型（4B）在 OOD 任务上表现更稳健，能够更好地将学习到的记忆策略迁移到未知场景。
*   **框架有效性**：验证了通过 GRPO 优化内部记忆管理策略（如决定何时覆盖或压缩记忆）比单纯的启发式规则更有效。

### 7. 优点
*   **高度模块化**：标准化的接口设计允许研究人员像搭积木一样组合不同的提取器、更新器和检索器。
*   **降低门槛**：统一了数据格式和训练流程，极大简化了 Memory-RL 研究的工程实现。
*   **显存优化**：通过集成 GRPO 算法，解决了记忆增强智能体在长上下文训练时显存容易溢出的痛点。

### 8. 不足与局限
*   **范式覆盖有限**：目前仅集成了几种代表性算法，对于更复杂的分布式记忆或多模态记忆支持尚待扩展。
*   **训练效率**：虽然支持单卡训练，但在处理极长轨迹时的吞吐量仍有优化空间。
*   **评估维度**：实验主要集中在 MemAgent 任务上，未来需要更多样化的基准测试（如复杂推理、多工具调用）来验证其通用性。

（完）
