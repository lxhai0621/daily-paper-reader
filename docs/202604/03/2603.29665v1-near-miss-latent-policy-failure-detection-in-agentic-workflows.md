---
title: "Near-Miss: Latent Policy Failure Detection in Agentic Workflows"
title_zh: 险些失误：智能体工作流中的潜在策略失效检测
authors: "Ella Rabinovich, David Boaz, Naama Zwerdling, Ateret Anaby-Tavor"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29665v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 检测智能体工作流中的潜在策略失败
tldr: "本文研究了智能体工作流中的“险些出错”（Near-Miss）现象，即智能体虽达成正确结果但绕过了必要的策略检查。作者提出一种基于ToolGuard框架的新指标，通过将自然语言策略转化为可执行代码来分析智能体轨迹的决策合理性。在航空公司基准测试中发现，即使结果正确，仍有8-17%的案例存在潜在策略失效，揭示了当前仅关注最终状态评估的局限性，强调了过程审计的重要性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29665v1/fig-001.webp\", \"caption\": \"Figure 1: Canceling a reservation upon a customer request. Assuming accurate information, this workflow leads to the same outcome either with reservation details check (the tool call encircled by the dashed line) or without it (following the bypassing dashed path).\", \"page\": 2, \"index\": 1, \"width\": 461, \"height\": 502}, {\"url\": \"assets/figures/arxiv/2603.29665v1/fig-002.webp\", \"caption\": \"Figure 4: On the left: Latent failure distribution by mutating tool: update_reservation_flights() is the tool that is invoked most frequently without ensuring that the system state complies with policies, such as flight status and seats availability. Additional tool — update_reservation_passengers() — is missing from the chart since near-misses were not observed for this function. On the right: the distribution of read-only tools causing near-misses: get_flight_status() is the tool most frequently bypassed by agents; indeed, our manual inspection reveals that flights are often updated without verifying that their status is \\\"available\\\" as required by policies. Consistently with Table 2, slightly higher amount of latent failures is observed in closed models (blue) than in open models (orange).\", \"page\": 7, \"index\": 2, \"width\": 954, \"height\": 211}, {\"url\": \"assets/figures/arxiv/2603.29665v1/fig-003.webp\", \"caption\": \"Table 2: Performance results of three closed (top) and three open (bottom) models on the τ2-verified Airlines benchmark: total failure ratio is computed as the failure to meet the expected DB state after simulation completion; policy violation is detected by the ToolGuard library as explicit failure to follow guidelines. We further report the number of simulations involving mutating tool calls (out of the total of 200), near-miss ratio out of the 200 total, and, since latent failure is inherently relevant only to cases altering the system state – out of simulations with mutating tool calls, in brackets. All experiments are conducted with code generated by (the best performing, see Section 4.2) Claude-Sonnet4, and two best results in a column are boldfaced (the lower, the better).\", \"page\": 7, \"index\": 3, \"width\": 902, \"height\": 290}, {\"url\": \"assets/figures/arxiv/2603.29665v1/fig-004.webp\", \"caption\": \"Figure 2: Schematic ReAct agentic flow using ToolGuard – cancellation eligibility is verified prior to cancel_reservation() invocation. Contrary to this approach, if policy adherence is left for the agent’s best-effort, three possible outcomes exist: (1) agent explicitly validates the policy by fetching and inspecting reservation details (the desired path), (2) agent bypasses policy validation, which results in policy violation (captured by the benchmark since differs from the ground-truth state), and (3) agent bypasses policy validation, yet the flow results in a valid outcome since the policy is not violated (near-miss, the subject of this study).\", \"page\": 4, \"index\": 4, \"width\": 950, \"height\": 413}, {\"url\": \"assets/figures/arxiv/2603.29665v1/fig-005.webp\", \"caption\": \"Figure 3: Near-miss detection in a completed task trajectory: mutating tool call with arguments args (MTC(args)) detected → load and simulate MTC’s guard code → if read-only tool call (RO) found, we search the trajectory history for precisely this, or any alternative read-only tool invocation, that obtains the same required information → if not found, assert latent failure to follow the policies.\", \"page\": 5, \"index\": 5, \"width\": 950, \"height\": 306}]"
motivation: 传统的基于最终状态的评估方法无法检测到智能体因偶然因素绕过策略检查而导致的潜在失效（Near-Miss）。
method: 利用ToolGuard框架将自然语言策略转换为可执行代码，通过分析智能体的工具调用轨迹来评估其决策是否符合策略要求。
result: "实验显示在多个主流大模型中，有8-17%的成功任务实际上包含了未触发策略检查的潜在失效。"
conclusion: 评估智能体系统必须超越最终结果，引入能够审计决策过程合规性的指标，以识别并修复隐蔽的策略违规风险。
---

## 摘要
用于业务流程自动化的智能体系统通常需要遵守管理系统状态条件更新的策略。评估基于大语言模型（LLM）的智能体工作流中的策略合规性，通常是通过将最终系统状态与预定义的基准真相（ground truth）进行比较来完成的。虽然这种方法可以检测到显式的策略违规，但它可能会忽略一类更微妙的问题：智能体绕过了必要的策略检查，却由于有利的外部环境而得出了正确的结果。我们将此类情况称为“险些失误”（near-misses）或“潜在失效”（latent failures）。在这项工作中，我们引入了一种新颖的指标，用于检测智能体对话轨迹中的潜在策略失效。基于将自然语言策略转换为可执行防御代码的 ToolGuard 框架，我们的方法分析智能体轨迹，以确定智能体的工具调用决策是否基于充分的信息。我们在 $τ^2$-verified Airlines 基准测试上，针对多种当代开源和闭源 LLM 智能体评估了我们的方法。结果显示，在涉及状态变更工具调用的轨迹中，即使最终结果与预期的基准真相状态一致，仍有 8-17% 的轨迹发生了潜在失效。这些发现揭示了当前评估方法中的一个盲点，并强调了开发不仅评估最终结果、还要评估导致结果的决策过程的指标的必要性。

## Abstract
Agentic systems for business process automation often require compliance with policies governing conditional updates to the system state. Evaluation of policy adherence in LLM-based agentic workflows is typically performed by comparing the final system state against a predefined ground truth. While this approach detects explicit policy violations, it may overlook a more subtle class of issues in which agents bypass required policy checks, yet reach a correct outcome due to favorable circumstances. We refer to such cases as $\textit{near-misses}$ or $\textit{latent failures}$. In this work, we introduce a novel metric for detecting latent policy failures in agent conversations traces. Building on the ToolGuard framework, which converts natural-language policies into executable guard code, our method analyzes agent trajectories to determine whether agent's tool-calling decisions where sufficiently informed.   We evaluate our approach on the $τ^2$-verified Airlines benchmark across several contemporary open and proprietary LLMs acting as agents. Our results show that latent failures occur in 8-17% of trajectories involving mutating tool calls, even when the final outcome matches the expected ground-truth state. These findings reveal a blind spot in current evaluation methodologies and highlight the need for metrics that assess not only final outcomes but also the decision process leading to them.

---

## 论文详细总结（自动生成）

### 论文总结：险些失误（Near-Miss）——智能体工作流中的潜在策略失效检测

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）智能体被广泛应用于业务流程自动化，确保其遵循公司政策（Policies）变得至关重要。目前的评估方法主要依赖于**基于参考的评估**，即比较智能体操作后的最终环境状态与基准真相（Ground Truth）是否一致。
*   **研究动机**：作者发现这种“以结果论英雄”的方法存在严重盲点。智能体可能在没有执行必要策略检查的情况下，“碰巧”达到了正确的结果（例如：未核实用户资格就取消了订单，而该用户恰好符合资格）。
*   **核心概念**：论文将这种现象定义为**“险些失误”（Near-Miss）**或**“潜在失效”（Latent Failure）**。这种行为在非对抗性场景下看似无害，但在面对恶意用户或复杂边界情况时，会演变成严重的合规性风险或安全漏洞。

#### 2. 方法论：核心思想与算法流程
论文提出了一种检测潜在策略失效的新指标，其核心思想是**审计智能体的决策过程是否具备充分的信息支持**。
*   **核心框架（ToolGuard）**：利用 ToolGuard 框架将自然语言描述的业务政策自动编译为可执行的 Python 防御代码（Guard Code）。
*   **检测流程**：
    1.  **识别变更工具调用（MTC）**：当智能体尝试调用会改变系统状态的工具（如 `cancel_reservation`）时，触发检测。
    2.  **模拟防御逻辑**：运行该工具对应的防御代码。防御代码通常需要调用只读工具（RO，如 `get_reservation_details`）来获取决策依据。
    3.  **轨迹回溯与匹配**：系统检查智能体在调用 MTC 之前的对话历史中，是否已经调用过该 RO 工具或能提供同等信息的替代工具。
    4.  **判定潜在失效**：如果防御代码运行需要某项信息，而智能体在调用变更工具前从未获取过该信息，则判定为“险些失误”，即使最终结果正确。
*   **历史搜索技术**：对比了“基于 LLM 的直接搜索”和“基于 LLM 生成代码进行搜索”两种方式，发现后者在准确率上表现更优。

#### 3. 实验设计
*   **数据集/场景**：使用了 **$\tau^2$-verified Airlines** 基准测试。这是一个经过人工修正的航空公司业务场景，包含复杂的政策文档（如取消政策、升舱规则等）和数据库。
*   **对比方法（智能体模型）**：
    *   **闭源模型**：GPT-5-chat、Claude-3.5-Sonnet、Gemini-1.5-Pro。
    *   **开源模型**：GPT-oss-120b、Kimi-K2.5、Qwen2.5-72b。
*   **评估指标**：总失败率、显式策略违规率、以及论文提出的**潜在失效率（NMR）**。

#### 4. 资源与算力
*   论文**未明确说明**具体的硬件算力（如 GPU 型号或数量）。
*   由于该研究侧重于推理阶段的评估而非模型训练，其算力消耗主要集中在 1200 次模拟实验的 LLM API 调用以及 ToolGuard 防御代码的生成与执行上。

#### 5. 实验数量与充分性
*   **实验规模**：针对 6 个模型，每个模型在 50 个任务上进行 4 次重复实验，总计 **1200 次模拟轨迹**。
*   **验证充分性**：作者对其中 400 条轨迹进行了人工标注，作为“潜在失效”的基准真相，用以验证自动检测算法的精确率（Precision）和召回率（Recall）。
*   **客观性**：实验涵盖了当前最先进的开源和闭源模型，并针对 $\tau^2$-bench 中原本缺失的工具进行了补充，确保了实验的公平性和基准测试的完整性。

#### 6. 主要结论与发现
*   **普遍存在的盲点**：在最终结果正确的成功轨迹中，竟然有 **8% 到 17%** 的案例实际上是“潜在失效”。这意味着现有的评估指标显著高估了智能体的可靠性。
*   **模型表现差异**：GPT-oss-120b 在潜在失效控制上表现最好（8.6%），而 GPT-5-chat 在涉及变更工具的轨迹中潜在失效比例最高（17.3%）。
*   **高危工具分布**：`update_reservation_flights` 是最容易触发潜在失效的工具，智能体经常在不检查航班状态（`get_flight_status`）的情况下直接进行更新。
*   **过程审计的必要性**：实验证明，仅仅匹配数据库状态不足以验证智能体的合规性，必须对决策路径进行审计。

#### 7. 优点
*   **视角独特**：首次系统性地定义并量化了智能体工作流中的“险些失误”现象，填补了安全评估的空白。
*   **方法严谨**：结合了程序化验证（防御代码）与自然语言处理，比纯 LLM 评估更具确定性和透明度。
*   **实用性强**：提出的指标可以直接集成到现有的智能体开发流水线中，用于离线评估或在线监控。

#### 8. 不足与局限
*   **领域局限性**：实验仅在航空公司（Airlines）单一领域进行，尚未在零售、医疗等其他复杂领域广泛验证。
*   **依赖防御代码质量**：检测的准确性高度依赖于 ToolGuard 生成的 Python 代码是否准确还原了政策逻辑。如果防御代码本身有误，检测结果将失效。
*   **计算开销**：在推理过程中引入轨迹回溯和代码执行会增加额外的计算延迟和成本。
*   **静态政策假设**：目前的方法假设政策是静态且明确的，对于模糊或动态变化的政策处理能力有限。

（完）
