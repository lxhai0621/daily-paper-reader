---
title: "Squeez: Task-Conditioned Tool-Output Pruning for Coding Agents"
title_zh: Squeez：面向编程智能体的任务条件化工具输出剪枝
authors: Ádám Kovács
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.04979v1"
tags: ["query:agent"]
score: 9.0
evidence: 管理智能体上下文及代码智能体的工具输出剪枝
tldr: 针对编程智能体在处理冗长工具输出时效率低下的问题，本文提出了任务条件下的工具输出剪枝方法。研究者构建了一个包含1.1万余个样本的基准数据集，并利用LoRA技术微调了Qwen 3.5 2B模型。该模型能根据当前任务提取最关键的证据块，在保留核心信息的同时大幅减少Token消耗，显著提升了智能体处理长文本的效率和准确性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04979v1/fig-001.webp\", \"caption\": \"Table 5: Representative held-out examples. The fine-tuned model is strongest on precise hits, compact failure blocks, and true negatives; its residual errors are usually adjacent over-selection.\", \"page\": 5, \"index\": 1, \"width\": 950, \"height\": 255}, {\"url\": \"assets/figures/arxiv/2604.04979v1/fig-002.webp\", \"caption\": \"Figure 1: System overview. The benchmark stores grounded spans over raw tool output, then derives a generative training target. At evaluation, predicted lines are compared to gold spans using recall, F1, and compression.\", \"page\": 2, \"index\": 2, \"width\": 410, \"height\": 633}]"
motivation: 编程智能体在执行任务时经常需要处理冗长的工具输出，但其中仅有极小部分对下一步决策有用，导致计算资源浪费和上下文干扰。
method: 提出了一种基于任务条件的剪枝方法，通过微调轻量级模型Qwen 3.5 2B，从原始工具输出中提取与当前查询相关的最小连续证据块。
result: "该模型在移除92%输入Token的情况下，达到了0.86的召回率和0.80的F1值，性能远超大型零样本模型和启发式基准。"
conclusion: 任务条件下的工具输出剪枝是提升编程智能体效率的有效手段，轻量级微调模型在这一特定任务上表现优于通用大模型。
---

## 摘要
编程智能体经常需要处理冗长的工具观测结果，尽管每次观测中只有一小部分对下一步操作至关重要。我们研究了任务条件化的工具输出剪枝：给定一个特定的查询和一个工具输出，返回智能体下一步应检查的最小逐字证据块。我们引入了一个包含 11,477 个样本的基准测试集，该基准基于 SWE-bench 仓库交互和合成的多生态系统工具输出构建，并包含一个经过人工筛选的 618 样本测试集。我们使用 LoRA 对 Qwen 3.5 2B 进行了微调，并将其与更大的零样本模型以及启发式剪枝基准进行了比较。我们的模型在移除 92% 输入 token 的情况下，达到了 0.86 的召回率和 0.80 的 F1 分数，在召回率上比零样本 Qwen 3.5 35B A3B 高出 11 个百分点，并大幅领先于所有启发式基准。

## Abstract
Coding agents repeatedly consume long tool observations even though only a small fraction of each observation matters for the next step. We study task-conditioned tool-output pruning: given a focused query and one tool output, return the smallest verbatim evidence block the agent should inspect next. We introduce a benchmark of 11,477 examples built from SWE-bench repository interactions and synthetic multi-ecosystem tool outputs, with a manually curated 618-example test set. We fine-tune Qwen 3.5 2B with LoRA and compare it against larger zero-shot models and heuristic pruning baselines. Our model reaches 0.86 recall and 0.80 F1 while removing 92% of input tokens, outperforming zero-shot Qwen 3.5 35B A3B by 11 recall points and all heuristic baselines by a wide margin.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Squeez** 的方法，旨在解决编程智能体（Coding Agents）在处理冗长工具输出时的效率瓶颈。以下是对该论文的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：编程智能体在解决问题时会产生大量的工具观测结果（如文件读取、日志、Git 记录、测试输出等）。虽然这些输出可能长达数千行，但通常只有极小一部分对智能体的下一步决策至关重要。
*   **核心问题**：如何实现“任务条件化的工具输出剪枝”？即给定一个具体的查询（Query）和一个原始工具输出（Tool Output），如何准确提取出智能体下一步所需的最小逐字证据块（Verbatim Evidence Block），从而减少 Token 消耗并提高推理效率。

### 2. 方法论
*   **核心思想**：将剪枝任务定义为一个抽取式任务，通过微调轻量级大模型，使其学习在特定任务背景下识别关键信息，而非简单的文本压缩或摘要。
*   **关键技术细节**：
    *   **任务定义**：输入为对 $(q, o)$，其中 $q$ 是简短的任务查询，$o$ 是原始工具输出。输出为一组行索引区间 $Y = \{(s_1, e_1), \dots, (s_k, e_k)\}$。
    *   **模型选择**：选用 **Qwen 3.5 2B** 作为基础模型，因其在代码和推理任务上表现出色且体积小巧，适合作为智能体工作流中的轻量级过滤器。
    *   **微调技术**：使用 **LoRA**（低秩自适应）进行微调。
    *   **数据生成管线**：利用强大的教师模型（GPT-OSS-120B）自动生成查询和对应的证据跨度（Spans）。为了保证质量，标签会映射回原始文本以确保“逐字提取”。
    *   **部署形式**：模型被设计为可以作为 CLI 过滤器或通过 vLLM 提供服务，能够无缝嵌入现有的智能体框架（如 Claude Code）。

### 3. 实验设计
*   **数据集**：构建了一个包含 **11,477 个样本** 的基准数据集。
    *   **数据来源**：一部分源自 SWE-bench 的真实仓库交互（9,205 个），另一部分是针对多种编程生态（TypeScript, Go, Rust 等）合成的工具输出（2,272 个）。
    *   **工具覆盖**：涵盖 27 种工具类型，包括 `read file`, `grep`, `git log`, `pytest`, `docker build` 等。
*   **测试集**：包含 618 个经过**人工精心筛选**的样本，排除了重复、琐碎或错误的标注。
*   **对比方法**：
    *   **零样本大模型**：Qwen 3.5 35B A3B、Kimi K2、未微调的 Qwen 3.5 2B。
    *   **启发式基准**：BM25（取前 10% 行）、First-N（前 10%）、Last-N（后 10%）和随机选择。

### 4. 资源与算力
*   **硬件使用**：使用了 **1 台 NVIDIA A100 80GB GPU**。
*   **训练参数**：微调进行了 3 个 Epoch，最大序列长度为 20,000 Token，每台设备的 Batch Size 为 8，梯度累积步数为 4。
*   **训练时长**：文中未明确给出具体的训练总时长，但考虑到 2B 模型规模和 1 万余条数据，在 A100 上通常可在数小时内完成。

### 5. 实验数量与充分性
*   **实验规模**：实验涵盖了 27 种不同的工具家族，测试集经过人工校验，具有较高的可信度。
*   **充分性**：研究对比了从 2B 到 35B 不同规模的模型，并加入了传统的检索算法（BM25）和简单的位置启发式算法。
*   **客观性**：通过引入“负样本”（即查询与输出不匹配的情况）来测试模型的鲁棒性，防止模型盲目提取内容。实验结果不仅报告了召回率，还报告了 F1 分数和压缩率，评估维度较为全面。

### 6. 主要结论与发现
*   **性能卓越**：微调后的 Squeez-2B 在移除 **92%** 输入 Token 的情况下，达到了 **0.86 的召回率** 和 **0.80 的 F1 分数**。
*   **以小胜大**：2B 规模的微调模型在召回率上比 18 倍体积的 35B 零样本模型高出 11 个百分点。
*   **任务特定监督的必要性**：通用大模型在处理重复日志或 Git 历史时容易选择语义接近但错误的块，而 Squeez 学习到了特定工具的提取策略。
*   **负样本处理**：Squeez 在处理无相关信息的负样本时表现远好于大模型（80% 的正确空输出率 vs 35B 模型的 7%）。

### 7. 优点
*   **实用性强**：模型体积小、推理快，可作为 CLI 工具直接管道连接（Piped）工具输出，对现有智能体架构改动极小。
*   **高压缩比**：在保持高召回率的同时大幅削减了上下文长度，能显著降低后续长文本处理的成本和幻觉风险。
*   **高质量数据集**：开源了包含多种编程语言和工具类型的标注数据集，填补了该细分领域的空白。

### 8. 不足与局限
*   **评估局限**：目前的评估集中在“证据提取”的准确性上，尚未直接测量剪枝对智能体最终解决 Bug（如 SWE-bench 最终得分）的端到端影响。
*   **指标偏差**：使用跨度重叠（Span Overlap）作为指标可能无法捕捉到所有合理的剪枝决策（有时存在多种同样有效的剪枝方式）。
*   **工具噪声**：某些工具（如 `grep` 和 `lint`）的输出在剪枝后依然存在一定的噪声，提取质量仍有提升空间。

（完）
