---
title: "MemFactory: Unified Inference & Training Framework for Agent Memory"
title_zh: MemFactory：智能体记忆的统一推理与训练框架
authors: "Ziliang Guo, Ziheng Li, Bo Tang, Feiyu Xiong, Zhiyu Li"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29493v4"
tags: ["query:agent"]
score: 10.0
evidence: 使用强化学习优化记忆操作的记忆增强智能体统一框架
tldr: "MemFactory是首个针对记忆增强型智能体设计的统一推理与训练框架。针对当前记忆管理实现碎片化且缺乏标准的问题，它将记忆生命周期抽象为模块化、原子化的组件，支持类似“乐高”式的灵活构建。该框架集成了GRPO算法，通过环境奖励优化记忆提取与检索策略，并原生支持Memory-R1等前沿范式。实验证明，MemFactory能显著提升模型性能，最高增益达14.8%，极大降低了开发高性能长短期记忆智能体的门槛。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29493v4/fig-001.webp\", \"caption\": \"Figure 1 The overall architecture of MemFactory, illustrating the interdependencies among the Modules, Agent, Environment, and Trainer layers.\", \"page\": 4, \"index\": 1, \"width\": 929, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2603.29493v4/fig-002.webp\", \"caption\": \"Table 1 Standard interfaces supported by modules in the Module Layer.\", \"page\": 5, \"index\": 2, \"width\": 968, \"height\": 219}]"
motivation: 旨在解决记忆增强型智能体在开发过程中缺乏统一、模块化且易于训练的基础设施这一现状。
method: 通过将记忆生命周期抽象为原子化组件，并集成GRPO算法，实现对记忆管理策略的强化学习优化。
result: "在MemAgent架构上的验证表明，该框架能有效提升模型表现，相对基础模型最高获得14.8%的性能增长。"
conclusion: MemFactory为记忆驱动型AI智能体提供了标准化且可扩展的工具，显著降低了该领域的研究与应用门槛。
---

## 摘要
记忆增强的大语言模型（LLMs）对于开发具备长期能力的 AI 智能体至关重要。近期，应用强化学习（RL）来优化记忆操作（如提取、更新和检索）已成为一个极具前景的研究方向。然而，现有的实现仍然高度碎片化且针对特定任务，缺乏统一的基础设施来简化这些复杂流水线的集成、训练和评估。为了填补这一空白，我们提出了 MemFactory，这是首个专为记忆增强智能体设计的统一、高度模块化的训练与推理框架。受 LLaMA-Factory 等统一微调框架成功的启发，MemFactory 将记忆生命周期抽象为原子化的、即插即用的组件，使研究人员能够通过“乐高式”架构无缝构建自定义记忆智能体。此外，该框架原生集成了群体相对策略优化（GRPO），以在多维环境奖励的驱动下微调内部记忆管理策略。MemFactory 为近期前沿范式（包括 Memory-R1、RMM 和 MemAgent）提供开箱即用的支持。我们在开源的 MemAgent 架构上，利用其公开的训练和评估数据对 MemFactory 进行了实证验证。在各个评估集上，MemFactory 相比对应的基础模型平均提升了性能，相对增益最高达 14.8%。通过提供标准化、可扩展且易于使用的基础设施，MemFactory 显著降低了准入门槛，为未来记忆驱动型 AI 智能体的创新铺平了道路。

## Abstract
Memory-augmented Large Language Models (LLMs) are essential for developing capable, long-term AI agents. Recently, applying Reinforcement Learning (RL) to optimize memory operations, such as extraction, updating, and retrieval, has emerged as a highly promising research direction. However, existing implementations remain highly fragmented and task-specific, lacking a unified infrastructure to streamline the integration, training, and evaluation of these complex pipelines. To address this gap, we present MemFactory, the first unified, highly modular training and inference framework specifically designed for memory-augmented agents. Inspired by the success of unified fine-tuning frameworks like LLaMA-Factory, MemFactory abstracts the memory lifecycle into atomic, plug-and-play components, enabling researchers to seamlessly construct custom memory agents via a "Lego-like" architecture. Furthermore, the framework natively integrates Group Relative Policy Optimization (GRPO) to fine-tune internal memory management policies driven by multi-dimensional environmental rewards. MemFactory provides out-of-the-box support for recent cutting-edge paradigms, including Memory-R1, RMM, and MemAgent. We empirically validate MemFactory on the open-source MemAgent architecture using its publicly available training and evaluation data. Across the evaluation sets, MemFactory improves performance over the corresponding base models on average, with relative gains of up to 14.8%. By providing a standardized, extensible, and easy-to-use infrastructure, MemFactory significantly lowers the barrier to entry, paving the way for future innovations in memory-driven AI agents.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **MemFactory**，这是首个为记忆增强型 AI 智能体设计的统一推理与训练框架。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：虽然记忆增强型大语言模型（LLM）在长期对话和个性化任务中至关重要，且利用强化学习（RL）优化记忆操作（如提取、更新、检索）已成为前沿趋势，但目前该领域的研究高度**碎片化**。
*   **研究背景**：现有的记忆强化学习实现（如 Memory-R1, MemAgent, RMM）通常与特定任务深度耦合，缺乏标准化的基础设施。研究人员在复现或组合不同记忆模块时面临巨大的工程障碍。
*   **目标**：效仿 LLaMA-Factory 在模型微调领域的成功，打造一个“乐高式”的、模块化的统一框架，以简化记忆智能体的开发、训练和评估流程。

### 2. 方法论
MemFactory 采用了四层模块化架构：
*   **模块层 (Module Layer)**：将记忆生命周期抽象为原子操作。
    *   **Extractor（提取器）**：从原始上下文中解析事实和经验。
    *   **Updater（更新器）**：执行 CRUD 操作（增加、删除、更新、无操作），保持记忆库简洁准确。
    *   **Retriever（检索器）**：支持语义检索及基于推理模型的重排序（Rerank）。
    *   **Agent Module**：支持端到端的循环记忆策略（如 MemAgent 范式）。
*   **智能体层 (Agent Layer)**：作为策略执行器，通过组合上述原子模块构建自定义智能体，支持训练模式（生成轨迹）和纯推理模式。
*   **环境层 (Environment Layer)**：负责数据加载和奖励计算。提供 `MemoryBankEnv`（长期记忆库）和 `LongcontextEnv`（短文境记忆）两种环境，并集成格式奖励和 LLM-as-a-Judge 评估。
*   **训练层 (Trainer Layer)**：核心优化引擎。
    *   **关键技术**：原生集成 **GRPO（群体相对策略优化）**。该算法通过组内奖励归一化计算优势（Advantage），无需额外的 Critic 模型，显著降低了长上下文训练时的显存开销。

### 3. 实验设计
*   **实验对象**：以开源的 **MemAgent** 架构为基准。
*   **基础模型**：Qwen3-1.7B 和 Qwen3-4B-Instruct。
*   **数据集**：使用 MemAgent 公开的训练和评估数据。
    *   **主任务评估**：`eval_50` 和 `eval_100`（不同长度的文档集）。
    *   **分布外（OOD）评估**：`eval_fwe_16384`（测试模型的泛化能力）。
*   **对比方法**：对比了各基础模型在经过 MemFactory RL 微调前后的性能表现。

### 4. 资源与算力
*   **硬件环境**：整个训练和评估流水线可以在**单张 NVIDIA A800 80GB GPU** 上运行。
*   **训练细节**：每个模型训练 250 个有效步骤（valid steps）。为了提高效率，训练时将原始上下文长度缩减至约三分之一（每样本 50-80 个文档）。
*   **可复现性**：论文强调了该框架对算力需求较低，具有很高的可复现性。

### 5. 实验数量与充分性
*   **实验规模**：针对两种不同规模的模型（1.7B 和 4B）进行了实验，涵盖了主任务和 OOD 任务。
*   **客观性**：所有评分均采用 **avg@4**（每道题进行 4 次独立试验后取平均值），以减少生成随机性带来的偏差。
*   **充分性**：实验涵盖了从基础性能提升到跨领域泛化的验证，虽然实验组数不算庞大，但针对核心模块的验证逻辑清晰，对比公平（使用相同的训练/测试集）。

### 6. 主要结论与发现
*   **性能提升**：MemFactory 显著提升了模型的记忆管理能力。Qwen3-1.7B 的平均性能提升了 **14.8%**；Qwen3-4B-Instruct 提升了 **7.3%**。
*   **模型规模效应**：较大的模型（4B）在 OOD 任务上表现更稳健，能够更好地将学到的循环记忆策略迁移到未见过的设置中。
*   **框架有效性**：证明了通过 GRPO 优化记忆策略的路径是行之有效的，且 MemFactory 能够成功复现并改进现有的 SOTA 记忆范式。

### 7. 优点
*   **高度模块化**：通过“乐高式”设计，研究者可以像拼积木一样组合 Extractor、Updater 和 Retriever。
*   **降低门槛**：统一了数据格式和训练接口，使非工程背景的研究员也能快速开展 Memory-RL 研究。
*   **资源友好**：集成了 FlashAttention-2 和 GRPO，使得在单卡上训练长上下文记忆策略成为可能。
*   **开箱即用**：预置了 Memory-R1、MemAgent 和 RMM 等前沿算法的实现。

### 8. 不足与局限
*   **范式覆盖有限**：目前仅涵盖了几种代表性的记忆范式，对于更复杂的分布式记忆或多模态记忆支持尚待开发。
*   **训练效率**：虽然支持单卡训练，但在处理极长上下文时的训练吞吐量仍有优化空间。
*   **实验深度**：论文主要侧重于框架的展示，缺乏更深层次的消融实验（例如不同奖励函数对记忆策略的具体影响分析）。

（完）
