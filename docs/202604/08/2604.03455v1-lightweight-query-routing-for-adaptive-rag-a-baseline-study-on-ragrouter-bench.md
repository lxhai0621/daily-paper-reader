---
title: "Lightweight Query Routing for Adaptive RAG: A Baseline Study on RAGRouter-Bench"
title_zh: 面向自适应 RAG 的轻量级查询路由：基于 RAGRouter-Bench 的基准研究
authors: "Prakhar Bansal, Shivangi Agarwal"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.03455v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 检索增强生成的查询路由与策略选择
tldr: "本研究针对检索增强生成（RAG）中不同策略的成本与能力平衡问题，在RAGRouter-Bench基准上首次系统评估了轻量级分类器路由。通过对比15种分类器与特征组合，发现TF-IDF结合SVM表现最优，在实现93.2%准确率的同时节省了28.1%的Token成本。研究揭示了词汇特征在预测查询复杂度方面的优势，并分析了不同领域的路由难度差异，为自适应RAG提供了高效的基准参考。"
source: arxiv
selection_source: fresh_fetch
motivation: 旨在解决RAG系统中如何根据查询类型自动选择最优检索策略以平衡生成质量与Token成本的实际效率问题。
method: 在RAGRouter-Bench上评估了5种经典分类器与3种特征（TF-IDF、MiniLM嵌入、手工特征）组合而成的15种轻量级路由方案。
result: "TF-IDF结合SVM的配置效果最佳，宏F1值达0.928，且相比始终使用最昂贵策略可节省28.1%的Token。"
conclusion: 词汇特征在查询路由中优于语义嵌入，该研究为自适应RAG建立了可复现的基准，并指出了未来语料感知路由的研究方向。
---

## 摘要
检索增强生成（RAG）流水线涵盖了广泛的检索策略，这些策略在 Token 成本和能力方面存在显著差异。为每个查询选择合适的策略是一个实际的效率问题，然而目前尚未有路由分类器在 RAGRouter-Bench 上进行训练。RAGRouter-Bench 是最近发布的一个基准测试集，包含跨越四个知识领域的 7,727 个查询，每个查询都被标注为三种典型查询类型之一：事实型、推理型和摘要型。我们在该基准上首次对基于轻量级分类器的路由进行了系统性评估。我们在三种特征方案（即 TF-IDF、MiniLM 句子嵌入和手工构建的结构化特征）下评估了五种经典分类器，共产生了 15 种分类器-特征组合。我们的最佳配置（TF-IDF 结合 SVM）实现了 0.928 的宏平均 F1 分数和 93.2% 的准确率，同时相比于始终使用最昂贵的范式，模拟实现了 28.1% 的 Token 节省。词汇 TF-IDF 特征的表现比语义句子嵌入高出 3.1 个宏 F1 点，这表明表面关键词模式是查询类型复杂度的强预测指标。领域级分析显示，医疗查询最难路由，而法律查询最易处理。这些结果建立了一个可复现的查询端基准，并突显了语料库感知路由需要弥补的差距。

## Abstract
Retrieval-Augmented Generation pipelines span a wide range of retrieval strategies that differ substantially in token cost and capability. Selecting the right strategy per query is a practical efficiency problem, yet no routing classifiers have been trained on RAGRouter-Bench \citep{wang2026ragrouterbench}, a recently released benchmark of $7,727$ queries spanning four knowledge domains, each annotated with one of three canonical query types: factual, reasoning, and summarization. We present the first systematic evaluation of lightweight classifier-based routing on this benchmark. Five classical classifiers are evaluated under three feature regimes, namely, TF-IDF, MiniLM sentence embeddings \citep{reimers2019sbert}, and hand-crafted structural features, yielding 15 classifier feature combinations. Our best configuration, TF-IDF with an SVM, achieves a macro-averaged F1 of $\mathbf{0.928}$ and an accuracy of $\mathbf{93.2\%}$, while simulating $\mathbf{28.1\%}$ token savings relative to always using the most expensive paradigm. Lexical TF-IDF features outperform semantic sentence embeddings by $3.1$ macro-F1 points, suggesting that surface keyword patterns are strong predictors of query-type complexity. Domain-level analysis reveals that medical queries are hardest to route and legal queries most tractable. These results establish a reproducible query-side baseline and highlight the gap that corpus-aware routing must close.

---

## 论文详细总结（自动生成）

这篇论文针对检索增强生成（RAG）系统的效率优化问题，在最新的 RAGRouter-Bench 基准测试集上首次建立了轻量级查询路由的基准性能。以下是对该论文的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：RAG 系统包含多种检索策略（如 NaiveRAG 到 IterativeRAG），其 Token 成本和处理能力差异巨大（最高达 3.5 倍差距）。如何根据查询（Query）的复杂度，自动将其路由到“最便宜且足够用”的检索范式，是提升 RAG 系统实用性的关键。
*   **研究背景**：虽然已有 RAGRouter-Bench 这一包含 7,727 个查询、跨 4 个领域的基准数据集，但此前尚未有人在该数据集上训练并评估分类器。本研究旨在填补这一空白，探索仅凭“查询文本”特征能达到多高的路由准确率。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：将路由问题转化为一个三分类任务（事实型、推理型、摘要型），并根据预测类型映射到相应的 RAG 范式。
*   **特征工程（三种方案）**：
    1.  **词汇特征 (TF-IDF)**：提取 1-gram 和 2-gram，限制 3000 个特征词。
    2.  **语义特征 (MiniLM)**：使用 22M 参数的轻量级模型 `all-MiniLM-L6-v2` 生成 384 维句子嵌入。
    3.  **结构化特征 (Hand-crafted)**：手动提取 23 维特征，包括查询长度、疑问词类型（who/what 等）、否定词、命名实体计数、从句计数等。
*   **分类器选择**：评估了逻辑回归 (LR)、支持向量机 (SVM)、随机森林 (RF)、K-近邻 (KNN) 和多层感知机 (MLP) 五种经典模型。
*   **路由映射逻辑**：
    *   `single_hop` (事实型) $\rightarrow$ NaiveRAG (1.4x 成本)
    *   `multi_hop` (推理型) $\rightarrow$ HybridRAG (2.8x 成本)
    *   `summary` (摘要型) $\rightarrow$ IterativeRAG (3.5x 成本)

### 3. 实验设计
*   **数据集 (RAGRouter-Bench)**：涵盖四个领域：MuSiQue (维基百科)、QuALITY (文学)、UltraDomain (法律) 和 GraphRAG-Bench (医疗)。
*   **对比基准**：
    *   **15 种组合**：5 种分类器 $\times$ 3 种特征集。
    *   **Majority Baseline**：始终预测出现频率最高的类别（事实型）。
    *   **Perfect-label Reference**：假设分类完全正确时的理想成本节省。
*   **评估指标**：宏平均 F1 (Macro-F1)、准确率 (Accuracy) 和模拟 Token 节省率。

### 4. 资源与算力
*   **算力说明**：论文强调了“轻量级”。MiniLM 模型仅 22M 参数，可在 **CPU** 上运行，无需 GPU。
*   **训练细节**：文中未提及具体的 GPU 型号或训练时长，因为所选模型（SVM, RF 等）在标准 CPU 环境下即可在数秒或数分钟内完成训练。

### 5. 实验数量与充分性
*   **实验规模**：进行了 15 组核心对比实验，并采用了 **5 折分层交叉验证** 以确保结果的稳定性。
*   **充分性评价**：实验覆盖了从简单统计到深度学习嵌入的多种特征，并进行了跨领域的细分分析（Domain Breakdown），实验设计客观、公平，能够代表查询端路由的基准水平。

### 6 主要结论与发现
*   **最佳配置**：**TF-IDF + SVM** 表现最好，准确率达 **93.2%**，宏 F1 达 **0.928**。
*   **词汇胜过语义**：TF-IDF 的效果比 MiniLM 语义嵌入高出 3.1 个 F1 点。这表明在 RAG 路由中，表面的关键词模式（如“summarize”、“compare”）是判断查询复杂度的强信号。
*   **成本节省**：最佳模型相比始终使用最贵的 IterativeRAG 可节省 **28.1%** 的 Token 成本（达到了理想状态下 35.2% 节省额的 79%）。
*   **领域差异**：法律查询最容易路由（F1=0.967），而医疗查询最难（F1=0.803），因为医疗查询的词汇模式在不同复杂度下更为相似。

### 7. 优点与亮点
*   **实用性强**：专注于轻量级方案，无需调用昂贵的 LLM 即可完成路由决策。
*   **首次基准化**：为 RAGRouter-Bench 提供了第一个可复现的查询端基准。
*   **揭示关键洞察**：指出了词汇特征在查询分类任务中相对于密集向量嵌入的优越性。

### 8. 不足与局限
*   **忽略语料库信号**：分类仅基于查询文本。原基准论文指出，最优策略往往取决于查询与语料库的交互（如语料库的连通性、密度等），本研究未涉及语料侧特征。
*   **映射简化**：将查询类型直接映射到特定 RAG 范式的做法过于简化，实际应用中不同范式的表现可能因数据集而异。
*   **泛化性未知**：实验仅限于 RAGRouter-Bench 的四个领域，模型在分布外（OOD）数据上的表现尚未测试。

（完）
