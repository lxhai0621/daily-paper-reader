---
title: "HYVE: Hybrid Views for LLM Context Engineering over Machine Data"
title_zh: HYVE：针对机器数据的大语言模型上下文工程混合视图
authors: "Jian Tan, Fan Bu, Yuqing Gao, Dev Khanolkar, Jason Mackay, Boris Sobolev, Lei Jin, Li Zhang"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05400v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 针对长机器数据负载的上下文工程
tldr: 针对大语言模型在处理冗长且结构复杂的机器数据（如日志、JSON）时表现不佳的问题，本文提出了 HYVE 框架。该框架借鉴数据库管理原理，通过预处理将原始数据转化为混合列式和行式视图，并仅向模型展示最相关部分，结合后处理阶段的语义合成，显著降低了 Token 消耗并提升了任务准确率，为机器数据上下文工程提供了新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05400v1/fig-001.webp\", \"caption\": \"Fig. 1. HYVE architecture. The preprocessor parses input strings with embedded structured segments into hybrid tabular views; the request-scoped datastore retains the full data; and the postprocessor inspects LLM outputs and, when needed, queries the datastore to enrich the response.\", \"page\": 1, \"index\": 1, \"width\": 529, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2604.05400v1/fig-002.webp\", \"caption\": \"Fig. 2. Slot-filling tasks over 700 network telemetry samples. Top: baseline GPT-4.1 consumes ∼5000 tokens per request; HYVE reduces usage by 53%. Bottom: baseline GPT-4.1 fails on 30.6% of samples (scores ≤2); HYVE achieves almost perfect scores on all samples.\", \"page\": 2, \"index\": 2, \"width\": 516, \"height\": 456}, {\"url\": \"assets/figures/arxiv/2604.05400v1/fig-003.webp\", \"caption\": \"Fig. 3. Line chart over three years of USD exchange-rate data (778 points per series). Baseline GPT-4.1 truncates output arrays to 40–120 points, collapsing detailed time series into sparse straight-line segments. HYVE recovers all 778 points through data backfilling.\", \"page\": 2, \"index\": 3, \"width\": 516, \"height\": 447}, {\"url\": \"assets/figures/arxiv/2604.05400v1/fig-004.webp\", \"caption\": \"Fig. 6. Per-sample distributions on the Anom dataset (n=797). HYVE improves reasoning quality while reducing token consumption through the anomaly operator’s high-signal summarization.\", \"page\": 13, \"index\": 4, \"width\": 516, \"height\": 456}, {\"url\": \"assets/figures/arxiv/2604.05400v1/fig-005.webp\", \"caption\": \"TABLE II ABLATION STUDY OF HYVE WITH GPT-4.1\", \"page\": 13, \"index\": 5, \"width\": 523, \"height\": 491}]"
motivation: 大语言模型在处理长且嵌套的重复性机器数据时存在性能脆弱、Token 消耗大且效率低下的挑战。
method: HYVE 通过预处理识别重复结构并将其存储在数据仓库中，生成混合视图供模型调用，并在后处理阶段通过 SQL 增强进行信息恢复。
result: "实验表明 HYVE 减少了 50-90% 的 Token 使用量，并将图表生成准确率提升了高达 132%，同时大幅降低了延迟。"
conclusion: HYVE 为处理包含大规模机器数据的提示词提供了一种实用的、接近无限上下文窗口的高效解决方案。
---

## 摘要
机器数据是现代计算系统可观测性和诊断的核心，以日志、指标、遥测追踪和配置快照的形式出现。当提供给大语言模型（LLM）时，这些数据通常以自然语言和结构化负载（如 JSON 或 Python/AST 字面量）的混合形式呈现。然而，LLM 在处理此类输入时仍然表现脆弱，特别是当输入较长、嵌套较深且包含大量重复结构时。我们提出了 HYVE（混合视图），这是一个受数据库管理原则启发、针对包含大规模机器数据负载输入的 LLM 上下文工程框架。HYVE 在模型调用前后集成了协调的预处理和后处理，其核心是一个通过模式（schema）信息增强的请求作用域数据存储。在预处理阶段，HYVE 检测原始输入中的重复结构，将其物化到数据存储中，并将其转换为混合的列式和行式视图，最后仅向 LLM 暴露最相关的表示。在后处理阶段，HYVE 要么直接返回模型输出，要么查询数据存储以恢复省略的信息，或者执行受限的额外 LLM 调用以进行 SQL 增强的语义合成。我们在涵盖知识问答、图表生成、异常检测和多步网络故障排除的多种真实工作负载上评估了 HYVE。在这些基准测试中，HYVE 在保持或提高输出质量的同时，将 Token 使用量减少了 50-90%。在结构化生成任务中，它将图表生成的准确率提高了高达 132%，并将延迟降低了高达 83%。总之，对于以大规模机器数据负载为主的提示词，HYVE 为实现有效的无限上下文窗口提供了一种实用的近似方案。

## Abstract
Machine data is central to observability and diagnosis in modern computing systems, appearing in logs, metrics, telemetry traces, and configuration snapshots. When provided to large language models (LLMs), this data typically arrives as a mixture of natural language and structured payloads such as JSON or Python/AST literals. Yet LLMs remain brittle on such inputs, particularly when they are long, deeply nested, and dominated by repetitive structure.   We present HYVE (HYbrid ViEw), a framework for LLM context engineering for inputs containing large machine-data payloads, inspired by database management principles. HYVE surrounds model invocation with coordinated preprocessing and postprocessing, centered on a request-scoped datastore augmented with schema information. During preprocessing, HYVE detects repetitive structure in raw inputs, materializes it in the datastore, transforms it into hybrid columnar and row-oriented views, and selectively exposes only the most relevant representation to the LLM. During postprocessing, HYVE either returns the model output directly, queries the datastore to recover omitted information, or performs a bounded additional LLM call for SQL-augmented semantic synthesis.   We evaluate HYVE on diverse real-world workloads spanning knowledge QA, chart generation, anomaly detection, and multi-step network troubleshooting. Across these benchmarks, HYVE reduces token usage by 50-90% while maintaining or improving output quality. On structured generation tasks, it improves chart-generation accuracy by up to 132% and reduces latency by up to 83%. Overall, HYVE offers a practical approximation to an effectively unbounded context window for prompts dominated by large machine-data payloads.

---

## 论文详细总结（自动生成）

以下是对论文《HYVE: Hybrid Views for LLM Context Engineering over Machine Data》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对大语言模型（LLM）在处理**机器数据**（如日志、指标、遥测追踪、JSON 配置等）时的三大痛点：
*   **Token 爆炸：** 嵌套的键值对和重复的模式（Schema）迅速耗尽上下文窗口。
*   **上下文腐烂（Context Rot）：** 关键信号被淹没在海量负载中，导致模型偏离指令。
*   **推理能力弱：** LLM 在处理长序列数值和类别数据（如趋势分析、异常检测）时表现不稳定。
**核心目标：** 提出一个名为 **HYVE** 的框架，借鉴数据库管理原则，通过“结构发现、信息保留、可查询表示”来优化 LLM 的上下文工程，实现对超长机器数据的高效处理。

### 2. 方法论：核心思想与关键技术
HYVE 在模型调用前后增加了预处理和后处理环节，核心组件包括：
*   **预处理（Preprocessing）：**
    *   **鲁棒解析器：** 能够处理畸形 JSON、Python 字面量、嵌套字符串编码等复杂格式。
    *   **混合视图（Hybrid Views）：** 
        *   **列式视图（Column-oriented）：** 将重复的 Schema 提取为列，便于 LLM 进行数值分析和提取。
        *   **行式视图（Row-oriented）：** 保留完整记录，便于实体间的关联推理。
    *   **参考感知排名（Ranking）：** 使用 BM25 算法根据用户查询对数据进行排名，仅在 Prompt 中保留最相关的部分（截断），其余存入数据仓库。
*   **请求作用域数据存储（Datastore）：** 使用内存数据库（DuckDB）存储全量原始数据及其 Schema。
*   **后处理（Postprocessing）：**
    *   **模式 1（直接输出）：** 仅靠可见上下文即可回答。
    *   **模式 2（模板回填）：** LLM 生成 JSON 模板和 SQL，系统从数据库查询数据并自动回填（解决图表生成中的截断问题）。
    *   **模式 3（SQL 增强合成）：** LLM 调用 SQL 工具查询数据库，系统将结果反馈给 LLM 进行最终语义合成。

### 3. 实验设计
*   **数据集/场景：** 涵盖了 11 个真实世界工作负载，包括：
    *   **网络工程 QA（Cert-QA）：** 3524 个认证考试问题。
    *   **故障排除（Runbook）：** 260 个真实支持流程。
    *   **图表生成（Line/Bar）：** 将遥测数据转化为可视化 JSON。
    *   **异常检测（Anom）：** 识别高延迟节点。
    *   **公共基准：** TOON-QA（针对 JSON 数据的检索准确性测试）。
*   **对比方法（Baselines）：** 
    *   标准 GPT-4.1 和 GPT-5（直接将原始 JSON 序列化输入）。
*   **评估指标：** LLM-as-a-judge 评分（1-5 分）、数据相似度（nDTW/余弦相似度）、精确匹配（ExactMatch）、Token 使用量和延迟。

### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 训练算力（如型号、数量）。
*   **原因：** HYVE 主要是一个推理侧的框架，依赖于现有的 LLM API（如 OpenAI GPT 系列）和 Cisco 内部的 DNM 模型。
*   **本地计算：** 框架在请求作用域内使用内存中的 DuckDB，其计算开销相对于 LLM 调用可以忽略不计。

### 5. 实验数量与充分性
*   **实验规模：** 进行了 11 组不同维度的基准测试，样本量从几十个到几千个不等（如 Canvas 任务达 4096 个样本）。
*   **消融实验：** 针对排名（Ranking）、截断（Truncation）、编码格式（TOON vs JSON）以及后处理模式进行了详细的消融研究。
*   **充分性：** 实验覆盖了从简单检索到复杂多步推理的多种场景，对比了不同能力的模型（GPT-4.1 vs GPT-5），结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **Token 节省：** 在处理大型机器数据负载时，Token 使用量减少了 **50-90%**。
*   **质量提升：** 在图表生成任务中，准确率提升了 **132%**（解决了 LLM 自动截断长数组的问题）。
*   **延迟降低：** 结构化生成任务的延迟降低了高达 **83%**。
*   **模型增强：** 即使是较弱的模型（GPT-4.1），在配合 HYVE 后，在某些任务上的表现也能接近或超过更强的模型（GPT-5）。

### 7. 优点
*   **数据保真度：** 采用可恢复的压缩而非 LLM 摘要，确保信息不丢失。
*   **混合视图设计：** 巧妙地结合了列式和行式存储的优点，对齐了 LLM 的推理优势。
*   **工程实用性：** 作为一个“包装器”存在，不改变 LLM 接口，易于集成到现有生产环境。
*   **鲁棒性：** 专门设计的解析器能处理现实中不规范的机器数据。

### 8. 不足与局限
*   **短期记忆限制：** HYVE 是请求作用域的，仅提供短期记忆，不涉及跨会话的长期记忆管理。
*   **SQL 依赖：** 系统的上限受限于 LLM 生成准确 SQL 的能力（虽然论文通过 Gradual Disclosure 优化了这一点）。
*   **延迟权衡：** 在某些需要额外 LLM 调用进行语义合成的场景（模式 3），可能会增加一定的响应时间。
*   **应用范围：** 主要针对结构化/半结构化机器数据，对于纯非结构化文本的压缩效果有限。

（完）
