---
title: Learning to Edit Knowledge via Instruction-based Chain-of-Thought Prompting
title_zh: 通过基于指令的思维链提示学习知识编辑
authors: "Jinhu Fu, Yan Bai, Longzhu He, Yihang Lou, Yanxiao Zhao, Li Sun, Sen Su"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05540v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 整合结构化事实三元组与非结构化事实信息
tldr: 针对大语言模型知识编辑中泛化性差和仅限于结构化数据的局限，本文提出CoT2Edit框架。该方法利用Agent生成包含思维链（CoT）的高质量指令数据，涵盖结构化与非结构化信息，并通过SFT和GRPO算法训练模型。推理阶段结合RAG动态检索编辑事实。实验证明，该方法在六个场景下均表现出卓越的泛化能力，为实时、多样的知识更新提供了高效解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05540v1/fig-001.webp\", \"caption\": \"Figure 1: Revealing the deficiencies in existing knowledge editing approaches.\", \"page\": 2, \"index\": 1, \"width\": 919, \"height\": 413}, {\"url\": \"assets/figures/arxiv/2604.05540v1/fig-002.webp\", \"caption\": \"Table 1: Editing performance of CoT2Edit against baselines on five benchmarks (editing full datasets).\", \"page\": 7, \"index\": 2, \"width\": 939, \"height\": 621}, {\"url\": \"assets/figures/arxiv/2604.05540v1/fig-003.webp\", \"caption\": \"Figure 2: The framework of our proposed CoT2Edit. (I) Construct editing instructions by prompting LLM agents to generate reasoning chains from editing corpora. (II) Generate more instruct data by LLM agents using entity relations from HotpotQA. (III) Train the model via SFT on Phase (I) data to learn targeted response patterns. (IV) Optimize the model using GRPO to enhance generalization on the combined (I)(II) data. (V) Deploy RAG to retrieve relevant edited facts during inference, ensuring accurate question answering.\", \"page\": 4, \"index\": 3, \"width\": 947, \"height\": 521}]"
motivation: 现有知识编辑方法在泛化能力上表现不佳，且主要局限于结构化三元组，难以处理现实世界中多样的非结构化信息。
method: 提出CoT2Edit框架，通过Agent生成思维链指令数据，结合监督微调（SFT）和群体相对策略优化（GRPO）训练模型，并在推理时利用RAG检索编辑后的知识。
result: 在三种开源语言模型上的实验表明，该方法仅需一轮训练即可在六种不同的知识编辑场景中展现出强大的泛化性能。
conclusion: 通过引入思维链推理和强化学习，CoT2Edit有效提升了大型语言模型在处理结构化与非结构化知识编辑任务时的灵活性和准确性。
---

## 摘要
大语言模型（LLMs）可以通过知识编辑有效处理过时信息。然而，当前的方法面临两个关键局限性：（I）泛化能力差：大多数方法只是僵化地注入新知识，而无法确保模型能有效地利用这些知识解决实际问题。（II）适用范围窄：当前方法主要关注结构化的事实三元组，忽略了现实世界中普遍存在的各种非结构化形式的事实信息（如新闻、文章）。为了应对这些挑战，我们提出了一种新范式：通过思维链（CoT）推理教 LLMs 进行知识编辑（CoT2Edit）。我们首先利用语言模型智能体针对结构化和非结构化编辑数据生成思维链，构建高质量的指令数据。随后，通过有监督微调（SFT）和群体相对策略优化（GRPO）训练模型对编辑后的知识进行推理。在推理阶段，我们集成了检索增强生成（RAG），以动态检索相关的已编辑事实，实现实时知识编辑。实验结果表明，我们的方法仅需在三个开源语言模型上进行一轮训练，即可在六种不同的知识编辑场景中实现强大的泛化能力。代码可在 https://github.com/FredJDean/CoT2Edit 获取。

## Abstract
Large language models (LLMs) can effectively handle outdated information through knowledge editing. However, current approaches face two key limitations: (I) Poor generalization: Most approaches rigidly inject new knowledge without ensuring that the model can use it effectively to solve practical problems. (II) Narrow scope: Current methods focus primarily on structured fact triples, overlooking the diverse unstructured forms of factual information (e.g., news, articles) prevalent in real-world contexts. To address these challenges, we propose a new paradigm: teaching LLMs to edit knowledge via Chain of Thoughts (CoTs) reasoning (CoT2Edit). We first leverage language model agents for both structured and unstructured edited data to generate CoTs, building high-quality instruction data. The model is then trained to reason over edited knowledge through supervised fine-tuning (SFT) and Group Relative Policy Optimization (GRPO). At inference time, we integrate Retrieval-Augmented Generation (RAG) to dynamically retrieve relevant edited facts for real-time knowledge editing. Experimental results demonstrate that our method achieves strong generalization across six diverse knowledge editing scenarios with just a single round of training on three open-source language models. The codes are available at https://github.com/FredJDean/CoT2Edit.

---

## 论文详细总结（自动生成）

这篇论文提出了 **CoT2Edit**，一种通过基于指令的思维链（Chain-of-Thought, CoT）提示来学习知识编辑的新范式。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLMs）存在知识陈旧或错误的问题，现有的知识编辑方法（如参数修改法或简单上下文法）存在两大局限：
    1.  **泛化性差**：模型往往只是机械地记住了新事实（如三元组），但在语义等价的表达或需要多步推理的场景下表现不佳。
    2.  **适用范围窄**：现有方法大多针对结构化的“事实三元组”（主语-谓语-宾语），难以处理现实世界中广泛存在的非结构化信息（如新闻报道、长文章）。
*   **研究动机**：通过引入思维链推理，教模型“如何利用新知识进行推理”，而非仅仅“记住新知识”，从而提升编辑的灵活性和泛化能力。

### 2. 方法论
CoT2Edit 包含三个关键阶段：
*   **高质量指令数据构建**：
    *   利用 LLM Agent（如 ChatGPT/DeepSeek）对结构化（MQuAKE）和非结构化（MQuAKE-uns）数据生成多步推理的思维链。
    *   **数据增强**：从 HotpotQA 中提取实体关系，通过 Agent 合成额外的指令数据，并利用维基百科进行 SPARQL 校验，确保数据准确性。
*   **两阶段模型训练**：
    *   **阶段 1：有监督微调（SFT）**：让模型学习基本的推理格式和任务结构（冷启动）。
    *   **阶段 2：群体相对策略优化（GRPO）**：首次将 GRPO 强化学习应用于知识编辑。通过设置“格式奖励”和“准确性奖励”，引导模型生成正确的推理路径并避免幻觉。
*   **推理阶段（RAG + CoT）**：
    *   集成检索增强生成（RAG），使用 Contriver 模型动态检索与问题相关的已编辑事实。
    *   模型基于检索到的事实，通过训练好的 CoT 能力进行推理并给出最终答案。

### 3. 实验设计
*   **基础模型**：Llama3-8B, Falcon3-10B, Qwen3-14B。
*   **数据集**：
    *   **结构化**：ZsRE, CounterFact, MQuAKE。
    *   **非结构化**：CounterFact-uns, WikiUpdate。
    *   **分布外（OOD）泛化**：ConvSent（情感编辑任务）。
*   **对比方法（Baselines）**：包括参数修改类（ROME, MEMIT, PMET, AlphaEdit, GLAME）、上下文类（IKE, SKEME）以及训练类（LTE, EditCoT）共 10 种主流方法。
*   **评价指标**：编辑成功率（Edit succ.）、改写鲁棒性（Para.）、局部性（Loc.）、邻域成功率（Nei.）。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA A800 GPU**。
*   **训练时长**：
    *   SFT 阶段：约 1 小时。
    *   GRPO 阶段：Llama3/Falcon3 约 1 小时，Qwen3-14B 约 2 小时。
    *   总训练流程在 4 小时内即可完成。
*   **推理效率**：相比于复杂的 EditCoT，CoT2Edit 的推理速度快了一倍以上，且无需在每次编辑时修改参数。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个标准基准测试集上进行了全面评估，涵盖了从 1k 到 20k 不同规模的事实编辑。
*   **消融实验**：验证了 SFT 和 GRPO 的必要性，结果显示缺失任何一环都会导致性能显著下降。
*   **充分性评价**：实验设计非常充分且客观。作者不仅对比了编辑效果，还测试了模型在通用任务（如 MMLU, SST）上的性能保持情况，证明了该方法不会导致模型通用能力的坍塌。

### 6. 主要结论与发现
*   **卓越的泛化性**：CoT2Edit 在单次训练后，即可在未见过的 OOD 任务（如情感编辑）上取得领先，准确率比现有方法高出约 11%。
*   **非结构化处理能力**：在非结构化知识编辑（WikiUpdate）中表现优异，准确率达到 80% 以上，远超传统方法。
*   **语义鲁棒性**：在改写（Rephrase）和邻域成功率（Neighborhood Success）指标上显著优于参数修改类方法，证明了思维链推理的有效性。
*   **稳定性**：随着编辑事实数量增加（至 20k），CoT2Edit 的性能保持稳定，而参数修改类方法（如 MEMIT）会出现明显的性能崩溃。

### 7. 优点
*   **范式创新**：将知识编辑从“参数记忆”转变为“基于检索事实的推理”，极大地扩展了编辑的边界。
*   **训练高效**：只需一轮训练即可适配多种场景，无需针对每个新事实反复微调或定位参数。
*   **可解释性强**：通过 `<think>` 标签输出推理过程，使用户能够直观看到模型是如何利用新知识的。
*   **格式兼容**：同时支持结构化三元组和自然语言描述的非结构化事实。

### 8. 不足与局限
*   **指令集覆盖面**：目前的指令数据主要集中在多步推理和非结构化问答，对于更复杂的逻辑推理或特定领域的专业知识编辑，可能需要更丰富的指令集。
*   **推理开销**：虽然比 EditCoT 快，但由于需要生成思维链，其推理延迟仍略高于直接输出答案的简单方法。
*   **检索依赖**：性能在一定程度上受限于 RAG 阶段检索的准确性，如果检索不到相关事实，编辑将失效。

（完）
