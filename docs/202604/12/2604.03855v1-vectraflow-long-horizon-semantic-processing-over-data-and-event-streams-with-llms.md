---
title: "VectraFlow: Long-Horizon Semantic Processing over Data and Event Streams with LLMs"
title_zh: VectraFlow：基于大语言模型的数据与事件流长周期语义处理
authors: "Shu Chen, Junhan Liu, Deepti Raghavan, Ugur Cetintemel"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03855v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 非结构化流的长程语义处理
tldr: VectraFlow是一个语义流数据处理引擎，旨在解决现有LLM框架缺乏状态化推理以及传统复杂事件处理系统无法处理非结构化文本的问题。它通过扩展传统关系算子并引入基于LLM的语义事件模式算子，实现了对非结构化文档流的长周期、状态化语义处理。该系统结合了LLM提取与NFA时间规则匹配，支持在临床文档等流数据上进行高效的端到端时间模式检测，填补了语义理解与流式推理之间的空白。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.03855v1/fig-001.webp\", \"caption\": \"Figure 3: VectraFlow interactive interface. (a) NL & Config View: Natural language to executable pipeline compilation. (b) Query Processing View: Per-operator traces and intermediate outputs. (c) Report View: End-to-end execution metrics and LLM profiling.\", \"page\": 4, \"index\": 1, \"width\": 1056, \"height\": 246}, {\"url\": \"assets/figures/arxiv/2604.03855v1/fig-002.webp\", \"caption\": \"Figure 1: VectraFlow Data and Event Processing Architecture.\", \"page\": 2, \"index\": 2, \"width\": 1052, \"height\": 496}]"
motivation: 现有的LLM框架多为无状态且单次触发，而传统的复杂事件处理系统难以处理非结构化文本流中的长周期状态化推理需求。
method: 提出了VectraFlow引擎，通过LLM驱动的语义算子扩展传统关系代数，并结合NFA技术实现对非结构化流的复杂事件模式匹配。
result: 该系统支持在临床文档流上进行实时查询，实现了从原始文本提取到复杂时间模式检测的端到端处理，并提供灵活的性能权衡。
conclusion: VectraFlow成功将LLM的语义能力与流式数据流引擎结合，为非结构化数据流的长周期状态化推理提供了有效的技术框架。
---

## 摘要
监控连续数据中的有意义信号，日益需要对非结构化流进行长周期、有状态的推理。然而，目前的 LLM 框架仍保持无状态和单次（one-shot）特性，而传统的复杂事件处理（CEP）系统虽然具备时间模式检测能力，但通常假设事件流是结构化且有类型的，导致无法处理非结构化文本。我们展示了 VectraFlow，一个语义流式数据流引擎，旨在解决上述两个问题。VectraFlow 扩展了传统的关联算子，使其能够在自由文本流上进行由 LLM 驱动的执行，并提供了一系列连续语义算子——包括 filter、map、aggregate、join、group-by 和 window。每个算子在基于 LLM、基于嵌入（embedding）以及混合实现中，都具有可配置的吞吐量与准确度权衡。在此基础上，语义事件模式算子将复杂事件处理提升至非结构化文档流，结合了基于 LLM 的事件提取与基于 NFA 的时间规则匹配，从而实现对语义事件序列的有状态推理。在本次演示中，用户将通过 VectraFlow 的实时查询界面，在临床文档流上构建语义流水线。与会者将把自然语言意图编译为可执行的算子图，检查中间状态输出，并观察从原始文本到匹配事件队列的端到端时间模式检测。

## Abstract
Monitoring continuous data for meaningful signals increasingly demands long-horizon, stateful reasoning over unstructured streams. However, today's LLM frameworks remain stateless and one-shot, and traditional Complex Event Processing (CEP) systems, while capable of temporal pattern detection, assume structured, typed event streams that leave unstructured text out of reach. We demonstrate VectraFlow, a semantic streaming dataflow engine, to address both gaps. VectraFlow extends traditional relational operators with LLM-powered execution over free-text streams, offering a suite of continuous semantic operators -- filter, map, aggregate, join, group-by, and window -- each with configurable throughput-accuracy tradeoffs across LLM-based, embedding-based, and hybrid implementations. Building on this, a semantic event pattern operator lifts complex event processing to unstructured document streams, combining LLM-based event extraction with NFA-based temporal rule matching for stateful reasoning over sequences of semantic events. In this demonstration, users will interact with VectraFlow's live query interface to compose semantic pipelines over clinical document streams. Attendees will compile natural language intents into executable operator graphs, inspect intermediate stateful outputs, and observe end-to-end temporal pattern detection, from raw text to matched event cohorts.

---

## 论文详细总结（自动生成）

### 论文总结：VectraFlow —— 基于大语言模型的数据与事件流长周期语义处理

#### 1. 核心问题与研究动机
*   **研究背景**：现代应用（如临床监控、欺诈检测）需要对非结构化数据流进行长周期、有状态的推理。
*   **核心问题**：
    *   **LLM 框架的局限性**：现有的 LLM 管道（如 Palimpzest, DocETL）多为无状态且单次触发（one-shot），难以处理随时间演变的序列信号。
    *   **传统 CEP 系统的局限性**：复杂事件处理（CEP）系统虽擅长时间模式检测，但要求输入必须是结构化、有类型的事件流，无法直接处理非结构化文本。
*   **研究目标**：开发一个既能理解非结构化语义，又能进行长周期时间模式匹配的流式处理引擎。

#### 2. 方法论
VectraFlow 是一个语义流式数据流引擎，其核心架构分为三层：
*   **流式数据流引擎层**：将计算建模为算子的有向无环图（DAG），记录在其中连续流动。
*   **语义算子层**：
    *   **扩展传统算子**：提供语义化的 Filter、Map、Aggregate、Join 等，支持 LLM 驱动的执行。
    *   **创新算子**：如 `sem_window`（基于语义偏移调整边界）、`sem_groupby`（动态演变分类）和 `cont_rag`（持续更新检索上下文）。
*   **语义模式算子 (`sem_pattern`)**：
    *   **核心思想**：将语义解释与时间验证分离。
    *   **执行流程**：
        1.  **LLM 提取器**：从非结构化文本中提取带时间戳的“语义事件”（Typed Tuples）。
        2.  **NFA 规则检测器**：使用非确定性有限自动机（NFA）在提取的事件序列上评估 CEP 规则（支持顺序、与/或、否定、时间窗口等）。
*   **自然语言层**：通过 Agent 循环将用户的自然语言意图编译为可执行的算子图。

#### 3. 实验设计
*   **数据集/场景**：
    *   **MiDe22 数据集**：用于测试 `sem_groupby` 算子在推文流上的聚类性能。
    *   **MIMIC-IV 临床笔记**：包含 256 份临床文档，用于测试 `sem_pattern` 在复杂医疗事件序列（如出院后未随访）中的检测能力。
*   **对比方法（Benchmark）**：
    *   **Baseline**：全上下文 LLM 提示词（将所有历史文本堆叠给 LLM 判断）。
    *   **Baseline (+ RAG)**：结合检索增强的全上下文判断。
    *   **VectraFlow (`sem_pattern`)**：提取事件后由 NFA 匹配。
    *   **VectraFlow (`sem_pattern + RAG`)**：在提取阶段加入 RAG 优化。
*   **评估指标**：F1 分数、ARI、纯度（Purity）、吞吐量（tuples/s）以及 Token 消耗量。

#### 4. 资源与算力
*   **模型使用**：
    *   **GPT-4o-mini**：部署在私有 Azure 服务器上（用于处理敏感的 MIMIC-IV 数据）。
    *   **Qwen3-8B / Qwen3-4B**：本地部署。
*   **算力细节**：文中未明确给出具体的 GPU 型号和数量，但提到在本地部署 Qwen 模型时，全上下文 Baseline 方案因上下文过长导致了 **OOM（显存溢出）**，而 VectraFlow 方案则运行正常。

#### 5. 实验数量与充分性
*   **实验规模**：针对 `sem_pattern` 评估了 5 种复杂的时间模式，涉及 256 份临床笔记。
*   **充分性评价**：
    *   **客观性**：对比了不同规模的模型（从 4B 到 GPT-4 级别）和不同的实现策略（LLM vs. Embedding vs. Hybrid），展示了性能与成本的权衡。
    *   **局限性**：作为一篇 4 页的 Demo 论文，实验样本量（256 份笔记）相对较小，主要用于验证框架的可行性和效率优势，而非大规模工业级压力测试。

#### 6. 主要结论与发现
*   **效率提升**：`sem_pattern` 显著降低了 Token 消耗（从 14.6M 降至 3.1M-5.7M），避免了长文本导致的显存溢出。
*   **准确性增强**：解耦“语义提取”与“时间推理”比直接让 LLM 在长文本中寻找模式更准确（F1 分数从 0.675 提升至 0.84 左右）。
*   **权衡灵活性**：通过不同的算子实现（如基于 Embedding 的快速分组 vs 基于 LLM 的精细分组），用户可以根据需求平衡吞吐量和语义精度。

#### 7. 优点
*   **架构创新**：成功统一了 LLM 的语义理解能力与 CEP 的状态化推理能力。
*   **实用性强**：提供了自然语言到管道的编译器，降低了非专家用户构建复杂流处理任务的门槛。
*   **透明度高**：交互式界面允许用户检查中间状态和 NFA 匹配证据，解决了 LLM “黑盒”推理难以调试的问题。

#### 8. 不足与局限
*   **延迟风险**：尽管优化了 Token 消耗，但流式处理中频繁调用 LLM 仍可能带来较高的端到端延迟，不适用于毫秒级实时场景。
*   **数据依赖**：语义提取的质量高度依赖于 Prompt 的设计和 LLM 的抽取能力，若提取阶段出错，下游 NFA 无法纠正。
*   **实验覆盖面**：目前仅在医疗和社交媒体数据上验证，对于更复杂、高并发的工业传感器流等场景尚待观察。

（完）
