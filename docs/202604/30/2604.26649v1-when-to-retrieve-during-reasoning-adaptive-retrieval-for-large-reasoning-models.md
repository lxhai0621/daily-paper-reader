---
title: "When to Retrieve During Reasoning: Adaptive Retrieval for Large Reasoning Models"
title_zh: 何时在推理过程中进行检索：面向大推理模型的自适应检索
authors: "Dongxin Guo, Jikun Wu, Siu Ming Yiu"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26649v1"
tags: ["query:ma-kf"]
score: 10.0
evidence: 大推理模型多步推理过程中的自适应检索框架
tldr: 针对大语言模型在长链条推理中与传统RAG预检索模式不匹配的问题，本文提出ReaLM-Retrieve框架。该框架通过步骤级不确定性检测、自适应检索干预策略及高效集成机制，实现了在推理过程中按需获取外部证据。实验表明，该方法在多跳推理任务上显著提升了准确率并大幅减少了检索次数，为复杂推理任务提供了高效的检索增强方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26649v1/fig-001.webp\", \"caption\": \"Figure 2: F1 score by number of reasoning hops on MuSiQue.\", \"page\": 8, \"index\": 1, \"width\": 463, \"height\": 772}, {\"url\": \"assets/figures/arxiv/2604.26649v1/fig-002.webp\", \"caption\": \"Figure 1: Overview of ReaLM-Retrieve’s adaptive retrieval approach. (a) The temporal mismatch: traditional RAG retrieves\", \"page\": 4, \"index\": 2, \"width\": 1062, \"height\": 637}]"
motivation: 传统RAG在推理开始前提供上下文，难以满足大模型在多步推理过程中动态、精准获取证据的需求。
method: 引入ReaLM-Retrieve框架，利用步骤级不确定性检测和自适应干预策略来决定何时进行检索，并优化了集成效率。
result: "在MuSiQue等基准测试中，该方法比标准RAG平均提升10.1%的F1值，同时比固定间隔检索减少了47%的调用次数。"
conclusion: ReaLM-Retrieve成功解决了推理模型与检索系统的失配问题，在推理密集型任务中实现了效率与精度的最优权衡。
---

## 摘要
DeepSeek-R1 和 OpenAI o1 等大推理模型生成跨越数千个 token 的长思维链，但它们与检索增强生成（RAG）的集成在根本上仍不匹配。当前的 RAG 系统优化了在推理开始前提供上下文，而推理模型则需要在多步推理链中注入证据。我们引入了 ReaLM-Retrieve，这是一个推理感知的检索框架，通过三项关键创新解决了这种不匹配：（1）步级不确定性检测器，在推理步粒度而非 token 或句子级别识别知识差距；（2）检索干预策略，学习外部证据何时能最大限度地使正在进行的推理受益；（3）效率优化的集成机制，与朴素集成相比，将单次检索开销降低了 3.2 倍。在 MuSiQue、HotpotQA 和 2WikiMultiHopQA 上的实验表明，ReaLM-Retrieve 在答案 F1 值上比标准 RAG 平均提高了 10.1% 的绝对值（三个基准测试的范围为 9.0-11.8%），同时与 IRCoT 等固定间隔方法相比，检索调用次数减少了 47%（所有改进在 p<0.01 下均显著，配对自助法）。在需要 2-4 跳推理的挑战性 MuSiQue 基准测试中，我们的方法达到了 71.2% 的 F1 值，平均每个问题仅需 1.8 次检索调用。分析表明，ReaLM-Retrieve 还提高了检索质量本身，在支持证据上实现了 81.3% 的 Recall@5，且精确率和 MRR 始终高于固定间隔基准，为推理密集型检索任务建立了新的最先进效率-准确率权衡。

## Abstract
Large reasoning models such as DeepSeek-R1 and OpenAI o1 generate extended chains of thought spanning thousands of tokens, yet their integration with retrieval-augmented generation (RAG) remains fundamentally misaligned. Current RAG systems optimize for providing context before reasoning begins, while reasoning models require evidence injection during multi-step inference chains. We introduce ReaLM-Retrieve, a reasoning-aware retrieval framework that addresses this mismatch through three key innovations: (1) a step-level uncertainty detector that identifies knowledge gaps at reasoning-step granularity rather than token or sentence level; (2) a retrieval intervention policy that learns when external evidence maximally benefits ongoing reasoning; and (3) an efficiency-optimized integration mechanism that reduces per-retrieval overhead by 3.2x compared to naive integration. Experiments on MuSiQue, HotpotQA, and 2WikiMultiHopQA demonstrate that ReaLM-Retrieve achieves on average 10.1% absolute improvement in answer F1 over standard RAG (range: 9.0-11.8% across the three benchmarks) while reducing retrieval calls by 47% compared to fixed-interval approaches like IRCoT (all improvements significant at p<0.01, paired bootstrap). On the challenging MuSiQue benchmark requiring 2-4 hop reasoning, our method achieves 71.2% F1 with an average of only 1.8 retrieval calls per question. Analysis shows that ReaLM-Retrieve also improves retrieval quality itself, achieving 81.3% Recall@5 with consistently higher precision and MRR than fixed-interval baselines on supporting evidence, establishing new state-of-the-art efficiency-accuracy trade-offs for reasoning-intensive retrieval tasks.

---

## 论文详细总结（自动生成）

这篇论文针对大推理模型（LRM）在长链条推理过程中对外部知识的需求，提出了一个名为 **ReaLM-Retrieve** 的自适应检索框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：DeepSeek-R1、OpenAI o1 等大推理模型能生成数万 token 的思维链（CoT），在逻辑推理上表现出色，但在处理需要外部事实知识的任务时仍存在幻觉。
*   **核心矛盾**：
    *   **时间错位**：传统 RAG 在推理开始前检索一次，无法预见长推理链中途出现的知识缺口。
    *   **粒度错位**：现有迭代检索（如 IRCoT）按句子或 token 触发，而推理模型以“推理步骤”（逻辑单元）为操作单位。
    *   **信号缺失**：o1 等模型不提供 token 概率，导致基于概率触发的检索方法（如 FLARE）失效。
    *   **效率低下**：在长推理链中频繁检索会导致巨大的延迟和成本。

### 2. 方法论：核心思想与关键技术
ReaLM-Retrieve 的核心思想是**在推理步骤级别检测不确定性，并学习最优的检索干预策略**。
*   **推理步分割**：使用轻量级 Transformer 分类器将长推理链划分为逻辑连贯的“推理步”（平均约 127 tokens），而非简单的句子分割。
*   **推理步不确定性评分 (RSUS)**：综合三个信号：
    1.  **口头化不确定性 ($U_{verb}$)**：提示模型自我评估结论的置信度。
    2.  **实体熵 ($U_{ent}$)**：分析推理步中命名实体在语料库中的覆盖稀疏度。
    3.  **一致性信号 ($U_{cons}$)**：采样多个推理分支并测量其结论的一致性。
*   **学习检索干预策略 ($\pi_\theta$)**：将检索决策建模为上下文多臂老虎机问题，通过 REINFORCE 算法训练策略网络，决定何时检索、如何构建查询以及如何集成上下文。
*   **效率优化集成**：
    *   **隐式压缩**：仅保留检索文档中与当前推理最相关的句子，减少 73% 的上下文扩展。
    *   **投机缓存**：根据当前内容预判下一步可能的查询并并行检索。
    *   **KV 缓存保留**：针对开源模型优化，避免重复计算已有的推理前缀。

### 3. 实验设计
*   **数据集**：MuSiQue（2-4 跳推理）、HotpotQA（2 跳推理）、2WikiMultiHopQA（跨文档推理）。
*   **对比方法**：
    *   无检索（闭卷推理）。
    *   标准 RAG（单次预检索）。
    *   **IRCoT**（固定间隔、按句检索）。
    *   **FLARE**（基于概率触发）。
    *   **Self-RAG**（基于特殊 token 触发）。
    *   **Search-R1**（基于强化学习的搜索决策）。
*   **实验模型**：DeepSeek-R1 (32B/671B)、QwQ-32B-Preview。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA A100 80GB GPU**。
*   **训练细节**：干预策略训练了 50,000 步；总训练时长约为 **2,400 GPU 小时**（包括数据收集和策略优化）。
*   **推理优化**：使用 vLLM 框架和张量并行技术。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流多跳 QA 基准上进行了全面测试，涵盖了从 2 跳到 4 跳的不同难度。
*   **消融实验**：非常充分。分别验证了不确定性评分的三个组件、策略网络的不同部分以及效率优化机制的贡献。
*   **泛化性测试**：在不同架构的模型（R1 vs QwQ）和跨数据集（HotpotQA 训练，MuSiQue 测试）上验证了策略的通用性。
*   **客观性**：使用了配对自助法（Paired Bootstrap）进行显著性检验（p < 0.01），确保改进并非偶然。

### 6. 主要结论与发现
*   **精度与效率双赢**：在 MuSiQue 上达到 71.2% F1 值，比 IRCoT 提升 5.8%，且检索调用次数减少了 **47%**。
*   **推理步粒度的优越性**：相比于按句检索，按推理步检索能更精准地捕捉知识缺口，尤其在 4 跳等复杂任务中优势更明显。
*   **检索质量提升**：自适应触发不仅减少了次数，还提高了检索结果的准确率（Recall@5 达到 81.3%）。
*   **缩短推理链**：及时的证据注入能帮助模型更快跳出错误的推理路径，从而减少总生成的 token 数。

### 7. 优点
*   **适配性强**：不依赖 token 概率或内部状态，可直接应用于 o1 等“黑盒”API 模型。
*   **系统级优化**：不仅关注算法，还通过投机缓存和 KV 缓存保留等手段解决了 RAG 在长推理中的延迟瓶颈。
*   **理论完备**：将检索决策形式化为顺序决策问题，并结合了模型自省与外部实体信号。

### 8. 不足与局限
*   **黑盒模型性能略低**：在无法获取丰富内部信号的 API 模型上，性能比开源模型低 2-3% F1。
*   **语料库依赖**：约 43% 的失败案例源于语料库本身缺失信息，自适应检索无法解决“巧妇难为无米之炊”的问题。
*   **长链退化**：当推理链极长（超过 3 万 token）或检索注入超过 3 次时，分割器和不确定性检测的准确度会有所下降。
*   **计算开销**：虽然减少了检索次数，但步骤分割和 RSUS 计算仍为推理过程增加了约 8% 的额外计算负担。

（完）
