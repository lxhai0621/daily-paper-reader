---
title: "MiMIC: Mitigating Visual Modality Collapse in Universal Multimodal Retrieval While Avoiding Semantic Misalignment"
title_zh: MiMIC：在缓解通用多模态检索中视觉模态崩溃的同时避免语义失配
authors: "Juan Li, Chuanghao Ding, Xujie Zhang, Cam-Tu Nguyen"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21326v1"
tags: ["query:cmv"]
score: 8.5
evidence: 通用多模态检索的语义对齐
tldr: 本研究针对通用多模态检索（UMR）中存在的视觉模态崩溃（模型过度依赖文本而忽略视觉特征）和语义失配（语义相关内容在空间中距离过远）问题，提出了MiMIC模型。MiMIC通过引入解码器融合（fusion-in-decoder）架构实现高效的多模态集成，并结合单模态混合与随机标题丢弃策略增强训练鲁棒性。实验证明，该方法在WebQA+和EVQA+等数据集上显著优于现有的早融合和晚融合基线模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.21326v1/fig-001.webp\", \"caption\": \"Figure 2: Different Fusion Strategies. (a) Late Fusion. Use separate image and text encoder (e.g., UniVL-DR). (b) Early Fusion. Concatenates image and text embeddings, which are then fed into the LM. (e.g., Marvel)\", \"page\": 2, \"index\": 1, \"width\": 456, \"height\": 317}, {\"url\": \"assets/figures/arxiv/2604.21326v1/fig-002.webp\", \"caption\": \"Table 3: Retrieval Performance on WebQA+ (based on a mixed-modal corpus D{T,IV ,IV C}) and EVQA+.\", \"page\": 7, \"index\": 2, \"width\": 941, \"height\": 350}, {\"url\": \"assets/figures/arxiv/2604.21326v1/fig-003.webp\", \"caption\": \"Figure 3: Embeddings of T2I Queries, Image Documents (IV C or IV ) and Text Documents (T ).\", \"page\": 3, \"index\": 3, \"width\": 456, \"height\": 528}, {\"url\": \"assets/figures/arxiv/2604.21326v1/fig-004.webp\", \"caption\": \"Figure 7: (left) The encoding of different modalities under our architecture. (right) The multi-modal representation in training and inference phase.\", \"page\": 5, \"index\": 4, \"width\": 456, \"height\": 282}]"
motivation: 现有的通用多模态检索方法在处理多模态数据时，容易出现视觉模态崩溃或语义失配的问题。
method: 提出MiMIC模型，采用解码器融合架构，并引入单模态混合与随机标题丢弃的鲁棒训练策略。
result: 在WebQA+和EVQA+数据集上的实验结果显示，MiMIC在多项指标上均优于早融合和晚融合的基线方法。
conclusion: MiMIC通过创新的架构和训练策略，有效缓解了视觉模态崩溃并避免了语义失配，提升了多模态检索的准确性。
---

## 摘要
通用多模态检索（UMR）旨在将不同模态（如视觉和文本）映射到一个共享的嵌入空间，以进行多模态检索。现有的 UMR 方法大致可分为两类：早期融合方法（如 Marvel），它将视觉特征投影到语言模型（LM）空间以与文本模态集成；以及后期融合方法（如 UniVL-DR），它使用独立的编码器对视觉和文本输入进行编码，并通过加法获得融合嵌入。我们的初步研究发现，Marvel 表现出视觉模态崩溃，其特征是模型倾向于忽略视觉特征，而过度依赖文本线索。相比之下，虽然 UniVL-DR 受此问题影响较小，但它更容易受到语义失配的影响，即语义相关的内容在嵌入空间中相距较远。为了应对这些挑战，我们提出了 MiMIC，它引入了两项关键创新：(1) 用于有效多模态集成的解码器内融合（fusion-in-decoder）架构，以及 (2) 通过单模态混入（single modality mixin）和随机标题丢弃（random caption dropout）进行的鲁棒训练。在 WebQA+ 和 EVQA+ 数据集上的实验表明（其中文档或查询中的图像可能缺少标题），MiMIC 一致优于早期融合和后期融合基准模型。

## Abstract
Universal Multimodal Retrieval (UMR) aims to map different modalities (e.g., visual and textual) into a shared embedding space for multi-modal retrieval. Existing UMR methods can be broadly divided into two categories: early-fusion approaches, such as Marvel, which projects visual features into the language model (LM) space for integrating with text modality, and late-fusion approaches, such as UniVL-DR, which encode visual and textual inputs using separate encoders and obtain fused embeddings through addition. Our pilot study reveals that Marvel exhibits visual modality collapse, which is characterized by the model's tendency to disregard visual features while depending excessively on textual cues. In contrast, although UniVL-DR is less affected by this issue, it is more susceptible to semantic misalignment, where semantically related content is positioned far apart in the embedding space. To address these challenges, we propose MiMIC, which introduces two key innovations: (1) a fusion-in-decoder architecture for effective multimodal integration, and (2) robust training through single modality mixin and random caption dropout. Experiments on the WebQA+ and EVQA+ datasets, where image in documents or queries might lack captions, indicate that MiMIC consistently outperforms both early- and late-fusion baselines.