---
title: "Ghost in the Agent: Redefining Information Flow Tracking for LLM Agents"
title_zh: 代理中的幽灵：重新定义大语言模型（LLM）代理的信息流追踪
authors: "Yuandao Cai, Wensheng Tang, Cheng Wen, Shengchao Qin"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23374v1"
tags: ["query:agent"]
score: 9.0
evidence: 与工具和存储交互的LLM智能体信息流跟踪
tldr: 针对LLM智能体面临的间接提示注入等安全威胁，本文提出NeuroTaint框架。由于传统污点分析难以处理LLM的概率性推理，该框架通过离线审计执行轨迹，结合语义转换、因果影响和持久化上下文来追踪信息流。在TaintBench等基准测试中，NeuroTaint在检测传播路径方面显著优于现有方法，为智能体安全提供了有效的审计手段。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.23374v1/fig-001.webp\", \"caption\": \"Figure 2: NeuroTaint workflow. The agent executes normally while NeuroTaint records source, memory, retrieval, and sink\", \"page\": 6, \"index\": 1, \"width\": 985, \"height\": 409}, {\"url\": \"assets/figures/arxiv/2604.23374v1/fig-002.webp\", \"caption\": \"Figure 4: Threshold sensitivity on TaintBench. Each panel\", \"page\": 11, \"index\": 2, \"width\": 506, \"height\": 527}, {\"url\": \"assets/figures/arxiv/2604.23374v1/fig-003.webp\", \"caption\": \"Figure 3: DCPG cross-session provenance restoration. Session A stores source-derived content and persists the taint state;\", \"page\": 7, \"index\": 3, \"width\": 1002, \"height\": 286}, {\"url\": \"assets/figures/arxiv/2604.23374v1/fig-004.webp\", \"caption\": \"Figure 1: Three TaintBench cases used in §2. Each panel shows a benchmark source, a sink, and the provenance relation that\", \"page\": 3, \"index\": 4, \"width\": 994, \"height\": 396}, {\"url\": \"assets/figures/arxiv/2604.23374v1/fig-005.webp\", \"caption\": \"Table 13: Sink catalogue: tool names across all evaluated frameworks.\", \"page\": 16, \"index\": 5, \"width\": 936, \"height\": 344}, {\"url\": \"assets/figures/arxiv/2604.23374v1/fig-006.webp\", \"caption\": \"Table 12: Source catalogue: tool names across all evaluated frameworks.\", \"page\": 16, \"index\": 6, \"width\": 936, \"height\": 237}]"
motivation: 传统污点分析无法应对LLM智能体中基于自然语言推理的概率性信息流和语义转换。
method: NeuroTaint通过离线审计执行轨迹，利用语义证据、因果推理和持久化上下文追踪污点传播。
result: 在TaintBench等多个基准测试中，NeuroTaint在检测源到汇点的传播方面显著优于FIDES等基准。
conclusion: NeuroTaint证明了结合语义与因果推理的离线审计是保障LLM智能体信息流安全的有效途径。
---

## 摘要
自主大语言模型（LLM）代理正越来越多地被部署，通过与外部工具、API 和存储器交互来执行复杂任务。然而，处理不可信的外部数据使这些代理面临严重的安全威胁，例如间接提示注入和未经授权的工具执行。保护这些系统需要有效的信息流追踪。然而，为程序内存状态设计的传统污点分析在应用于 LLM 时从根本上失效了，因为 LLM 中的数据传播是由概率性的自然语言推理驱动的。在本文中，我们提出了 NeuroTaint，这是第一个专门针对 LLM 代理独特信息流特征量身定制的全面污点追踪框架。我们的核心见解是，LLM 代理中的污点传播不仅必须被理解为显式的内容传输，还必须被理解为语义转换、对决策的因果影响以及通过存储器实现的跨会话持久性。因此，NeuroTaint 离线审计执行轨迹，利用语义证据、因果推理和持久上下文追踪，重建从不可信源（sources）到特权汇（sinks）的溯源，而不是仅仅依赖于精确的字符串匹配或预定义的源-汇路径。使用 TaintBench（我们包含 400 个场景、涵盖 20 个真实世界代理框架的基准测试）进行的广泛评估表明，NeuroTaint 在源-汇传播检测方面显著优于 FIDES（一种针对 LLM 代理的信息流控制（IFC）风格基准）。我们进一步表明，NeuroTaint 在包括 InjecAgent 和 ToolEmu 在内的既有代理安全基准测试中依然有效，同时以适度的额外审计成本进行离线运行。

## Abstract
Autonomous Large Language Model (LLM) agents are increasingly deployed to conduct complex tasks by interacting with external tools, APIs, and memory stores. However, processing untrusted external data exposes these agents to severe security threats, such as indirect prompt injection and unauthorized tool execution. Securing these systems requires effective information flow tracking. Yet, traditional taint analysis that is designed for program memory states fundamentally fails when applied to LLMs, where data propagation is governed by probabilistic natural language reasoning. In this paper, we present NeuroTaint, the first comprehensive taint tracking framework tailored for the unique information flow characteristics of LLM agents. Our key insight is that taint propagation in LLM agents must be understood not only as explicit content transfer, but also as semantic transformation, causal influence on decisions, and cross-session persistence through memory. NeuroTaint therefore audits execution traces offline to reconstruct provenance from untrusted sources to privileged sinks using semantic evidence, causal reasoning, and persistent context tracking, rather than relying on exact string matches or pre-defined source-sink paths alone. Extensive evaluation using TaintBench, our 400-scenario benchmark spanning 20 real-world agent frameworks, shows that NeuroTaint substantially outperforms FIDES, an information-flow-control (IFC)-style baseline for LLM agents, in source-sink propagation detection. We further show that NeuroTaint remains effective on established agent-security benchmarks, including InjecAgent and ToolEmu, while operating offline with modest additional auditing cost.

---

## 论文详细总结（自动生成）

### 论文总结：Ghost in the Agent: Redefining Information Flow Tracking for LLM Agents

#### 1. 论文的核心问题与整体含义
随着大语言模型（LLM）代理（Agents）在自动化任务中的普及，它们频繁与外部工具、API和存储器交互。这种开放性引入了严重的**安全风险**，尤其是**间接提示注入（Indirect Prompt Injection）**。攻击者可以通过不可信的外部数据（如恶意网页、邮件）劫持代理的推理过程，导致敏感信息泄露或非法工具执行。
传统的**污点分析（Taint Analysis）**技术在 LLM 环境下失效，原因有三：
*   **语义转换（C1）**：LLM 会对输入进行改写、总结，导致传统的字符串匹配失效。
*   **隐式控制影响（C2）**：不可信数据可能不直接出现在输出中，但会改变代理的选择逻辑（如“如果是恶意指令，则执行删除”）。
*   **异步溯源重用（C3）**：污点可能存入长期记忆（向量数据库），在数天后的不同会话中被触发。

本文提出了 **NeuroTaint**，这是首个专门为 LLM 代理设计的、基于溯源（Provenance）的离线污点追踪框架。

#### 2. 方法论
NeuroTaint 的核心思想是将污点传播视为一种**语义和因果的演化过程**，而非简单的字节拷贝。其核心流程包括：

*   **动态上下文溯源图 (DCPG)**：
    *   作为系统的骨干，记录数据从源（Source）到汇（Sink）的流向。
    *   **关键技术**：支持跨会话持久化。当代理调用存储工具时，NeuroTaint 将污点标签序列化为元数据（`_nt_taint`）随内容存入数据库；检索时重新激活标签，解决了 C3 挑战。
*   **混合语义追踪器 (Hybrid Semantic Tracker)**：
    *   针对显式内容传播（C1），采用四层级联检测：
        1.  **Canary 匹配**：注入唯一 UUID，检测精确提取。
        2.  **LCS 匹配**：计算最长公共子序列，检测部分片段拷贝。
        3.  **语义嵌入相似度**：利用 `all-MiniLM-L6-v2` 模型计算余弦相似度，检测改写和翻译。
        4.  **多片段覆盖**：将长文档分块，检测隐藏在大量正常文本中的微小恶意载荷。
*   **汇点驱动的因果分析器 (Sink-Driven Causal Analyzer)**：
    *   针对隐式控制（C2），在汇点触发时进行**反事实推理（Counterfactual Reasoning）**。
    *   **流程**：将可疑源内容替换为中性占位符（如“未找到相关信息”），重新询问 LLM。如果代理不再执行该汇点动作，则判定存在因果影响。

#### 3. 实验设计
*   **自建基准测试 TaintBench**：包含 400 个场景，涵盖 20 个主流开源代理框架（如 LangChain, AutoGen, CrewAI, Semantic Kernel, LlamaIndex 等）。
*   **外部基准**：在 InjecAgent 和 ToolEmu 两个已有的安全基准上验证迁移能力。
*   **对比方法**：**FIDES**（一种基于信息流控制 IFC 的 LLM 代理基准，主要依赖路径标签和预定义规则）。
*   **评估指标**：精确率（Precision）、召回率（Recall）和 F1 分数。

#### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 型号或数量。
*   **审计成本**：由于是离线审计框架，其开销主要在于推理。实验显示，平均每个执行单元的审计耗时约为 **0.25 秒**。
*   **Token 消耗**：因果分析器平均每次查询消耗约 **457 tokens**。
*   **模型使用**：主要使用 `gpt-4o-mini` 作为代理模型，并在讨论部分对比了 `gpt-4o`、`claude-3-5-sonnet` 等强模型。

#### 5. 实验数量与充分性
*   **实验规模**：进行了 400 组 TaintBench 实验，每组实验运行 5 次并取多数投票结果以应对 LLM 的随机性。
*   **消融实验**：详细分析了四个检测层级（Canary, String, Semantic, Coverage）以及因果分析器的各自贡献。
*   **鲁棒性分析**：对 6 种不同的阈值（语义、字符串等）进行了敏感度分析，并测试了不同强度的 LLM 作为执行引擎对结果的影响。
*   **充分性评价**：实验覆盖了从单代理到多代理、从内存检索到浏览器操作的多种架构，实验设计客观、公平，对比基准选择合理。

#### 6. 主要结论与发现
*   **性能卓越**：在 TaintBench 上，NeuroTaint 的 F1 分数达到 **0.928**，而基准方法 FIDES 仅为 **0.522**。
*   **隐式流检测**：因果分析器成功识别了 92.5% 的隐式控制攻击，这类攻击传统方法几乎完全无法检测。
*   **模型影响**：更强大的模型（如 Claude 3.5 Opus）虽然能拒绝部分不安全动作，但无法替代溯源审计；NeuroTaint 在强模型生成的轨迹上依然保持高效。
*   **二级评审价值**：将 NeuroTaint 作为第一级过滤器，再配合 LLM 进行二级人工/自动评审，可以达到近乎完美的精确率。

#### 7. 优点
*   **定义清晰**：首次系统化定义了 LLM 代理中的三类信息流传播模式。
*   **溯源完整性**：通过 DCPG 解决了跨会话、跨存储边界的污点断裂问题。
*   **非侵入性**：作为离线审计层，不需要修改 LLM 内部权重或实时干预推理过程，易于集成到现有框架（如 LangChain 回调）。

#### 8. 不足与局限
*   **离线限制**：目前主要用于事后审计，无法在攻击发生瞬间进行实时拦截。
*   **语义衰减风险**：如果攻击载荷经过多次极端改写或高度稀释，语义相似度可能低于阈值导致漏报。
*   **策略依赖**：检测效果高度依赖于用户对“源”和“汇”的定义，若策略配置不当（如未标记某个敏感 API 为 Sink），则无法触发审计。
*   **多模态缺失**：目前仅支持文本信息流，尚未涵盖图像、音频等跨模态注入攻击。

（完）
