---
title: Efficient Agent Evaluation via Diversity-Guided User Simulation
title_zh: 通过多样性引导的用户模拟实现高效智能体评估
authors: "Itay Nakash, George Kour, Ateret Anaby-Tavor"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21480v1"
tags: ["query:agent"]
score: 9.0
evidence: 评估基于大语言模型的面向客户智能体
tldr: 针对大语言模型智能体评估中线性模拟效率低且难以发现深层故障的问题，本文提出 DIVERT 框架。该框架采用基于快照的分支轨迹方法，在关键决策点保存状态并复用对话前缀，通过多样性引导的用户模拟探索不同交互路径。实验表明，DIVERT 在提升计算效率的同时，能更有效地发现智能体的潜在故障并扩大任务覆盖范围。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.21480v1/fig-001.webp\", \"caption\": \"Figure 3: Task Failure Counts across Domains. Heatmaps show the number of tasks with at least one failure (out of N ) as a function of number of branches (x-axis) and rollout iterations (y-axis) with GPT-OSS-120B as agent. The vertical black line separates the baseline setting (left, no branches) from branch-based evaluation (right). Failure coverage increases with additional branches.\", \"page\": 6, \"index\": 1, \"width\": 946, \"height\": 246}, {\"url\": \"assets/figures/arxiv/2604.21480v1/fig-002.webp\", \"caption\": \"Figure 5: Cumulative number of unique tasks with failures as a function of total token cost under varying branch budgets, shown for Airline, Retail, and Telecom using GPT-OSS-120B as both agent and user model. Branch-based configurations consistently achieve higher failure coverage for the same token budget, with steeper early gains and delayed saturation compared to linear rollouts.\", \"page\": 15, \"index\": 2, \"width\": 941, \"height\": 205}, {\"url\": \"assets/figures/arxiv/2604.21480v1/fig-003.webp\", \"caption\": \"Figure 1: Standard Rollout Evaluation vs. DIVERT. Left: Conventional evaluation repeatedly rolls out full conversations from the beginning, often producing low-impact interactions and failing to explore deeper failure modes. Right: Our snapshot-based evaluation framework stores intermediate conversation states and branches at critical junctions with directed, diverse user responses, enabling more efficient exploration and improved discovery of failures (a full example is provided in Appendix A).\", \"page\": 2, \"index\": 3, \"width\": 946, \"height\": 565}, {\"url\": \"assets/figures/arxiv/2604.21480v1/fig-004.webp\", \"caption\": \"Figure 4: Example Full Rollout + DIVERT (Appendix Example). The conversation prefix is identical up to the selected junction. The junction chooser identifies the turn where the agent lacks verifiable insurance proof. DIVERT replaces the original user message with a directed alternative that provides an insurance confirmation number, producing a divergent continuation. Left: the linear rollout escalates and does not cancel the reservation (policy respected). Right: the branched continuation leads to cancellation despite the system record indicating no insurance (new failure uncovered).\", \"page\": 10, \"index\": 4, \"width\": 941, \"height\": 898}, {\"url\": \"assets/figures/arxiv/2604.21480v1/fig-005.webp\", \"caption\": \"Table 1: Varying rollouts and branches configurations on Airline domain (GPT-OSS-120B). RO, Err/100K, Fail C., and T denote Rollouts, Errors found per 100K tokens, Task Failure Count, and Total Tokens, respectively. Increasing branches consistently improves failure discovery efficiency (Err/100k) and coverage unique failure count.\", \"page\": 5, \"index\": 5, \"width\": 456, \"height\": 383}, {\"url\": \"assets/figures/arxiv/2604.21480v1/fig-006.webp\", \"caption\": \"Figure 2: Errors Discovery Rate. Number of failed trajectories per 100K agent tokens for GPT-OSS-120B and Gemini-2.5-Flash across increasing branch budgets. The notation 8+K denotes 8 full rollouts with K additional mid-trajectory branches using DIVERT. The dashed horizontal line marks the corresponding baseline, namely linear rollout using the same total number of trajectories without branching.\", \"page\": 5, \"index\": 6, \"width\": 931, \"height\": 250}]"
motivation: 传统的线性蒙特卡洛模拟在评估多轮对话智能体时存在计算冗余，且难以覆盖罕见但关键的用户行为路径。
method: 提出 DIVERT 框架，通过在关键决策点保存快照并利用多样性引导的用户响应进行分支探索，实现对交互空间的系统性覆盖。
result: 实验证明 DIVERT 在单位 Token 消耗下能发现更多故障，并显著提升了识别出故障的任务集合范围。
conclusion: DIVERT 是一种高效且覆盖面广的智能体评估方法，通过减少冗余计算和增强路径多样性，为提升 LLM 智能体的可靠性提供了有力工具。
---

## 摘要
大语言模型 (LLMs) 正越来越多地被部署为面向客户的智能体，但由于随机的多轮交互，评估其可靠性仍然具有挑战性。当前的评估协议依赖于完整智能体-用户对话的线性蒙特卡洛展开 (Monte Carlo rollouts) 来估计成功率。然而，这种方法计算效率低下，会重复生成相同的早期前缀，且往往无法发现由罕见用户行为引起的深层故障模式。我们引入了 DIVERT (Diversity-Induced Evaluation via Branching of Trajectories)，这是一个高效的、基于快照的、覆盖率引导的用户模拟框架，用于系统地探索智能体与用户的交互。DIVERT 在关键决策点捕获完整的智能体-环境状态，并从这些快照中恢复执行，从而实现共享对话前缀的重用并减少冗余计算。在每个交汇点，该框架使用有针对性的、诱导多样性的用户响应进行分支，从而允许对替代交互路径进行定向探索。通过将评估重点放在语义多样且探索不足的轨迹上，DIVERT 提高了效率和覆盖率。实证结果表明，与标准的线性展开协议相比，它在单位 token 下能发现更多故障，同时扩大了识别出故障的任务集。

## Abstract
Large language models (LLMs) are increasingly deployed as customer-facing agents, yet evaluating their reliability remains challenging due to stochastic, multi-turn interactions. Current evaluation protocols rely on linear Monte Carlo rollouts of complete agent-user conversations to estimate success. However, this approach is computationally inefficient, repeatedly regenerating identical early prefixes, and often fails to uncover deep failure modes that arise from rare user behaviors.   We introduce DIVERT (Diversity-Induced Evaluation via Branching of Trajectories), an efficient, snapshot-based, coverage-guided user simulation framework for systematic exploration of agent-user interactions. DIVERT captures the full agent-environment state at critical decision points and resumes execution from these snapshots, enabling reuse of shared conversation prefixes and reducing redundant computation. From each junction, the framework branches using targeted, diversity-inducing user responses, allowing directed exploration of alternative interaction paths.   By focusing evaluation on semantically diverse and underexplored trajectories, DIVERT improves both efficiency and coverage. Empirical results show that it discovers more failures per token compared to standard linear rollout protocols, while expanding the set of tasks on which failures are identified.

---

## 论文详细总结（自动生成）

以下是对论文《Efficient Agent Evaluation via Diversity-Guided User Simulation》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：随着大语言模型（LLM）被广泛部署为面向客户的智能体，如何评估其在多轮交互中的可靠性成为难题。
*   **核心挑战**：
    1.  **计算冗余**：现有的评估协议（如线性蒙特卡洛展开）每次都从头开始模拟，导致大量重复的早期对话前缀（如登录、身份验证）消耗了不必要的 Token。
    2.  **覆盖率不足**：标准的用户模拟器通常表现得过于“配合”，难以触发隐藏在罕见用户行为背后的深层故障模式。
*   **整体含义**：论文提出了一种名为 **DIVERT** 的框架，通过“快照+分支”的机制，在关键决策点探索多样化的交互路径，旨在提高智能体评估的效率和故障发现的深度。

### 2. 论文提出的方法论：DIVERT 框架
DIVERT 放弃了从初始状态重复运行的线性模式，转而采用树状结构探索。其核心流程包含四个阶段：
*   **初始展开与状态快照（Snapshotting）**：在初始模拟过程中，保存完整的智能体-环境状态（包括对话历史、工具数据库状态、随机种子等）。
*   **交汇点选择（Junction Selection）**：利用一个基于 LLM 的“交汇点选择器”（Junction Chooser），分析轨迹并识别出哪些用户回合最有可能通过改变响应来显著影响后续智能体行为。
*   **多样性引导的用户响应生成**：
    *   在选定的交汇点，生成 $K$ 个候选用户响应。
    *   **多样性筛选**：使用嵌入模型（Sentence-Transformers）计算候选响应与原始响应的余弦相似度，选择相似度最低（即差异最大）的一个。
    *   **意图保持**：通过提示词约束，确保生成的响应在改变语义的同时仍符合用户的初始任务意图。
*   **基于快照的恢复与执行**：从快照点加载状态，注入新的用户响应并继续执行，从而避免了重新生成前缀的成本。

### 3. 实验设计
*   **数据集/场景**：使用了 **$\tau$-bench** 集合，涵盖三个现实服务领域：**Airline（航空）**、**Retail（零售）**和 **Telecom（电信）**。
*   **Benchmark**：以标准的线性蒙特卡洛展开（Linear Rollout）作为基准。
*   **对比方法与模型**：
    *   **评估对象（Agent）**：OpenAI GPT-OSS-120B 和 Gemini-2.5-Flash。
    *   **用户模拟器**：主要使用 GPT-OSS-120B。
    *   **对比维度**：在相同的 Token 预算下，对比不同分支数量（Branching budget）对故障发现的影响。

### 4. 资源与算力
*   **模型使用**：使用了闭源 API 模型（Gemini 系列）和开源/权重开放模型（GPT-OSS-120B）。
*   **本地算力**：论文提到相似度计算（嵌入推理）使用了轻量级的 `all-MiniLM-L6-v2` 模型，在 **MacBook Pro (Apple M1 Pro CPU)** 上即可高效运行。
*   **训练时长**：由于本文关注的是**评估框架**而非模型训练，因此未涉及训练时长。实验主要涉及推理成本，论文详细统计了 Token 消耗。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 3 个领域，共 278 个任务。
    *   进行了数百次重复运行，生成了数千条轨迹。
    *   包含**消融实验**（验证交汇点选择、多样性生成等组件的有效性）。
    *   包含**异构模型实验**（交叉更换 Agent 和 Simulator 模型）。
*   **充分性评价**：实验设计较为全面，不仅关注了效率（每 100K Token 发现的错误数），还关注了覆盖率（发现错误的唯一任务数），并进行了意图保持的后验分析（LLM-as-a-judge），论证了实验的客观性和公平性。

### 6. 论文的主要结论与发现
*   **效率提升**：DIVERT 在单位 Token 消耗下发现的故障数量显著高于线性展开。即使只增加一个分支，也能提升故障发现率。
*   **覆盖面扩大**：通过定向分支，DIVERT 能够触发线性模拟难以触及的状态，从而在更多唯一的任务中发现智能体的失效点。
*   **成本优势**：框架引入的额外开销（交汇点选择和候选生成）仅占总评估成本的 **0.08% - 0.2%**，而节省的 Agent Token 远超此开销。
*   **结构化优势**：DIVERT 创造了大量精确重复的前缀，这为 KV-Cache 复用提供了巨大的潜力（前缀重合率从 0.5% 提升至最高 58%）。

### 7. 优点
*   **创新性**：将智能体评估从“线性采样”转向“树状探索”，充分利用了对话的结构化特性。
*   **实用性强**：通过快照机制直接减少了昂贵的 API 调用成本，对工业界评估大规模智能体具有直接参考价值。
*   **即插即用**：该框架不依赖于特定的用户模拟策略（如对抗性攻击），可以与现有的任何模拟器结合。

### 8. 不足与局限
*   **依赖选择器质量**：交汇点的选择高度依赖于“交汇点选择器”LLM 的推理能力，如果选择器无法识别关键决策点，效率提升可能受限。
*   **意图漂移风险**：虽然论文进行了意图检查，但在极长路径或多次分支后，如何保证用户行为依然严格符合原始 Backstory 仍是一个挑战。
*   **环境状态限制**：快照机制要求环境（如数据库、API 模拟器）必须支持状态回滚或序列化，这在某些复杂的真实生产环境中可能难以实现。

（完）
