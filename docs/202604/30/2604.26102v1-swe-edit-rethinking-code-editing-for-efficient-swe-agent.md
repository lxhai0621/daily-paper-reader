---
title: "SWE-Edit: Rethinking Code Editing for Efficient SWE-Agent"
title_zh: SWE-Edit：重新思考高效软件工程智能体（SWE-Agent）的代码编辑
authors: "Yikai Zhang, Jiaxin Pei, Kenan Li, Maoquan Wang, Jin Pan, Yu Kang, Shengyu Fu, Elsie Nallipogu, Junjie Hu, Yufan Huang, Zijian Jin"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.26102v1"
tags: ["query:agent"]
score: 9.0
evidence: 将代码编辑分解为子智能体以管理上下文窗口耦合问题
tldr: 针对软件工程智能体中代码查看与编辑耦合导致的上下文冗余问题，本文提出 SWE-Edit 框架。该框架将代码编辑分解为负责提取代码的 Viewer 和执行修改的 Editor 两个子智能体，使主智能体专注于推理。通过 GRPO 训练 Qwen3-8B 模型以自适应选择编辑模式，在提升解决率的同时显著降低了推理成本，并提出了新的代码编辑基准。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26102v1/fig-001.webp\", \"caption\": \"Figure 1. Overview of the proposed SWE-Edit framework architecture. The figure illustrates the dual optimization mechanism, demonstrating how optimization occurs simultaneously at both the scaffolding level (coordinating components and context) and the model level (refining the underlying models).\", \"page\": 2, \"index\": 1, \"width\": 890, \"height\": 522}, {\"url\": \"assets/figures/arxiv/2604.26102v1/fig-002.webp\", \"caption\": \"Figure 4. PR-Edit benchmark scores correlate with downstream agent performance, enabling efficient editor model selection without full SWE-bench evaluation.\", \"page\": 8, \"index\": 2, \"width\": 478, \"height\": 329}, {\"url\": \"assets/figures/arxiv/2604.26102v1/fig-003.webp\", \"caption\": \"Figure 5. Training dynamics for fixed vs. adaptive format selection. The y-axis is validation reward (normalized match) and the x-axis is the rollout step. While fixed find-replace starts higher (simpler format, easier to learn), adaptive training surpasses it by learning when to invoke whole-file rewrite.\", \"page\": 9, \"index\": 3, \"width\": 492, \"height\": 415}, {\"url\": \"assets/figures/arxiv/2604.26102v1/fig-004.webp\", \"caption\": \"Figure 2. Adaptive editing mode selection. The editor analyzes task characteristics to choose between find-replace (token-efficient but matching-sensitive) and whole-file rewrite (robust but costly), enabling optimal strategy selection based on edit scope and complexity.\", \"page\": 5, \"index\": 4, \"width\": 886, \"height\": 406}]"
motivation: 现有的代码编辑接口将代码查看、规划与执行混杂在单一上下文窗口中，导致无关信息堆积并降低智能体性能。
method: 提出 SWE-Edit 框架，通过 Viewer 和 Editor 子智能体解耦上下文，并利用 GRPO 训练模型实现自适应编辑模式选择。
result: "在 SWE-bench Verified 上，该方法将解决率提升了 2.1%，同时将推理成本降低了 17.9%。"
conclusion: 解耦代码编辑流程并优化编辑模式选择能有效提升软件工程智能体的效率与准确性。
---

## 摘要
大语言模型智能体在软件工程任务上取得了显著进展，但当前方法面临一个根本性的上下文耦合问题：标准代码编辑界面将代码检查、修改规划和编辑执行混杂在单个上下文窗口中，迫使智能体在探索性查看与严格格式化的编辑生成之间交替进行。这导致无关信息累积并降低了智能体的性能。为解决这一问题，我们提出了 SWE-Edit，它将代码编辑分解为两个专门的子智能体：一个根据需求提取任务相关代码的查看器（Viewer），以及一个根据高层规划执行修改的编辑器（Editor）——这使得主智能体能够专注于推理，同时将上下文密集型操作委托给干净的上下文窗口。我们进一步研究了高效编辑模型的构成要素：观察到普遍的“查找并替换”格式容易出错，我们使用 GRPO 训练 Qwen3-8B 以自适应地选择编辑模式，从而比单一格式基线提高了编辑效率。在 SWE-bench Verified 上，SWE-Edit 将解决率提高了 2.1%，同时将推理成本降低了 17.9%。我们还提出了一个代码编辑基准测试，能够可靠地预测下游智能体的性能，为编辑模型的选择提供实际指导。我们的代码已在 https://github.com/microsoft/SWE-Edit 公开。

## Abstract
Large language model agents have achieved remarkable progress on software engineering tasks, yet current approaches suffer from a fundamental context coupling problem: the standard code editing interface conflates code inspection, modification planning, and edit execution within a single context window, forcing agents to interleave exploratory viewing with strictly formatted edit generation. This causes irrelevant information to accumulate and degrades agent performance. To address this, we propose SWE-Edit, which decomposes code editing into two specialized subagents: a Viewer that extracts task-relevant code on demand, and an Editor that executes modifications from high-level plans--allowing the main agent to focus on reasoning while delegating context-intensive operations to clean context windows. We further investigate what makes an effective editing model: observing that the prevalent find-and-replace format is error-prone, we train Qwen3-8B with GRPO to adaptively select editing modes, yielding improved editing efficiency over single-format baselines. On SWE-bench Verified, SWE-Edit improves resolved rate by 2.1% while reducing inference cost by 17.9%. We additionally propose a code editing benchmark that reliably predicts downstream agentic performance, providing practical guidance for editing model selection. Our code is publicly available at https://github.com/microsoft/SWE-Edit.

---

## 论文详细总结（自动生成）

### 论文总结：《SWE-Edit: Rethinking Code Editing for Efficient SWE-Agent》

#### 1. 核心问题与整体含义
论文针对基于大语言模型（LLM）的软件工程智能体（SWE-Agents）中存在的**“上下文耦合问题”（Context Coupling Problem）**提出了解决方案。
*   **研究动机**：目前的智能体界面将代码检查（查看）、修改规划（推理）和编辑执行（生成格式化指令）混杂在同一个上下文窗口中。这导致智能体在探索代码库时积累大量无关信息，造成上下文污染，进而降低推理精度并增加推理成本。
*   **核心目标**：通过解耦“查看”与“编辑”职能，提升智能体在处理复杂软件工程任务时的效率、可靠性及成本效益。

#### 2. 方法论
SWE-Edit 提出了两层优化架构：

*   **脚手架层面（Scaffolding Optimization）**：将代码编辑接口分解为两个专门的子智能体。
    *   **Viewer（查看器）**：接收自然语言查询，从完整文件中仅提取与任务相关的代码块（Logical Blocks）返回给主智能体，从而保持主智能体上下文的整洁。
    *   **Editor（编辑器）**：接收主智能体的高层自然语言修改指令，负责将其转化为严格格式化的代码变更（如查找-替换或全文件重写）。这使得主智能体无需关注繁琐的格式细节。
*   **模型层面（Model Optimization）**：
    *   **自适应编辑策略**：研究发现单一的编辑格式（如仅查找-替换）在处理复杂修改时易出错。
    *   **GRPO 训练**：使用群组相对策略优化（GRPO）算法训练 Qwen3-8B 模型。模型学习根据任务复杂度自适应选择“查找-替换”（Token 效率高）或“全文件重写”（鲁棒性强）模式。
    *   **奖励机制**：采用“规范化匹配奖励”（Normalized Match Reward），在去除注释和规范化空格后对比生成结果与真实值。

#### 3. 实验设计
*   **数据集/场景**：
    *   **SWE-bench Verified**：包含 500 个真实的 GitHub 问题，是目前评估 SWE 智能体的主流基准。
    *   **PR-Edit Benchmark**：论文自建的中间基准，包含 500 个从开源 PR 中提取的编辑实例，用于快速评估编辑器的准确性。
*   **对比方法**：
    *   **Baseline**：采用 Anthropic 风格的 OpenHands 架构（目前最强的基准之一）。
    *   **消融实验**：分别测试“Baseline + Viewer”、“Baseline + Editor”以及完整的“SWE-Edit”。
    *   **泛化性测试**：将主智能体更换为 Kimi-K2、MiniMax-M2.1 和 GLM-4.7 等不同厂商的推理模型。

#### 4. 资源与算力
*   **训练细节**：使用了 **Slime** 框架进行 GRPO 后训练。
*   **训练规模**：在 3.5K 实例上进行训练（2.8K 训练集），运行了 520 个 rollout 步，每步采样 32 个实例，每个实例生成 8 个候选输出。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号（如 A100 或 H100）及总训练时长，但提到使用了较小的 8B 参数模型（Qwen3-8B）作为编辑器，旨在证明小模型经过针对性训练后可胜任专门职能。

#### 5. 实验数量与充分性
*   **实验规模**：主实验在 SWE-bench Verified 上进行了 3 次独立运行并取平均值，以减少随机性。
*   **维度覆盖**：
    *   **消融实验**：清晰展示了 Viewer 降低成本和 Editor 提升成功率的各自贡献。
    *   **模型泛化**：验证了该架构在多种闭源/开源推理模型上的有效性。
    *   **相关性分析**：证明了 PR-Edit 基准得分与下游任务解决率之间存在强相关性。
*   **客观性**：实验设计较为公平，基线模型选用了当前领域内的强力配置，且通过多次运行提供了标准差分析。

#### 6. 主要结论与发现
*   **性能提升**：在 SWE-bench Verified 上，SWE-Edit 将解决率（Resolved Rate）提升了 **2.1%**（达到 72.0%）。
*   **成本降低**：推理成本显著降低了 **17.9%**，主要归功于 Viewer 减少了主智能体的输入 Token 量。
*   **可靠性增强**：编辑成功率提升了 **3.5%**，且在不同推理模型上均表现出极高的稳定性（编辑成功率普遍从 70%-80% 提升至 95% 以上）。
*   **关键发现**：小模型（8B）通过强化学习可以获得比肩甚至超越大模型（如 GPT-5 级别）的专门编辑能力，且成本仅为后者的几分之一。

#### 7. 优点
*   **架构优雅**：通过简单的子智能体解耦解决了困扰长上下文智能体的“信息过载”问题。
*   **实用性强**：不仅提升了准确率，还大幅降低了实际部署成本，具有很高的工程参考价值。
*   **中间基准**：提出的 PR-Edit 为开发者提供了一个比跑完整个 SWE-bench（耗时且昂贵）更快捷的评估手段。

#### 8. 不足与局限
*   **Viewer 的潜在风险**：Viewer 如果在提取代码时过度剪裁（Over-pruning），可能会导致主智能体因信息缺失而做出错误决策（虽然文中提到平均 7.49 次调用可缓解此问题）。
*   **训练闭环有限**：目前的编辑器训练是基于静态的 Ground-truth，而非在智能体互动的动态反馈回路中进行端到端训练。
*   **算力细节缺失**：未披露具体的训练资源消耗，不便于其他研究者评估复现成本。

（完）
