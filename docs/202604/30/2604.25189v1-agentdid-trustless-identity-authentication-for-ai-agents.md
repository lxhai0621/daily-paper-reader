---
title: "AgentDID: Trustless Identity Authentication for AI Agents"
title_zh: AgentDID：面向 AI 智能体的无须信任身份认证
authors: "Minghui Xu, Xiaoyu Liu, Yihao Guo, Chunchi Liu, Yue Zhang, Xiuzhen Cheng"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25189v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 自主AI智能体的身份认证与状态验证
tldr: 针对AI智能体自主性强、生命周期短且状态动态变化的特点，本文提出了AgentDID，一个基于去中心化标识符（DID）和可验证凭证（VC）的无信任身份认证框架。该框架解决了智能体自我管理身份、大规模并发认证及动态执行状态验证的挑战，通过挑战-响应机制确保交互时的状态有效性，为大规模AI智能体生态提供了可扩展的身份安全保障。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.25189v1/fig-001.webp\", \"caption\": \"Fig. 2. A DID Document for the AI agent.\", \"page\": 6, \"index\": 1, \"width\": 528, \"height\": 708}, {\"url\": \"assets/figures/arxiv/2604.25189v1/fig-002.webp\", \"caption\": \"Fig. 3. Example VC for static agent attributes.\", \"page\": 6, \"index\": 2, \"width\": 528, \"height\": 579}, {\"url\": \"assets/figures/arxiv/2604.25189v1/fig-003.webp\", \"caption\": \"Fig. 4. The workflow for identity authentication and state verification.\", \"page\": 7, \"index\": 3, \"width\": 509, \"height\": 283}, {\"url\": \"assets/figures/arxiv/2604.25189v1/fig-004.webp\", \"caption\": \"Fig. 8. System throughput under increasing concurrency. The x-axis denotes the number of concurrent Holder–Verifier pairs, and the y-axis reports the system throughput.\", \"page\": 10, \"index\": 4, \"width\": 469, \"height\": 341}, {\"url\": \"assets/figures/arxiv/2604.25189v1/fig-005.webp\", \"caption\": \"Fig. 6. Latency breakdown across protocol phases under increasing concurrency. The x-axis denotes the number of concurrently authenticated agent pairs, and the y-axis reports the corresponding latency.\", \"page\": 10, \"index\": 5, \"width\": 469, \"height\": 349}, {\"url\": \"assets/figures/arxiv/2604.25189v1/fig-006.webp\", \"caption\": \"Fig. 7. Latency of context hash computation under varying context sizes. The hash computation latency scales linearly with the size of the context memory (R2 = 0.9996), remaining at the millisecond level even for large contexts, indicating that context consistency verification introduces negligible overhead.\", \"page\": 10, \"index\": 6, \"width\": 469, \"height\": 351}, {\"url\": \"assets/figures/arxiv/2604.25189v1/fig-007.webp\", \"caption\": \"Fig. 5. Example probe task for availability verification.\", \"page\": 8, \"index\": 7, \"width\": 523, \"height\": 316}, {\"url\": \"assets/figures/arxiv/2604.25189v1/fig-008.webp\", \"caption\": \"TABLE I COVERAGE OF REQUIREMENTS FOR AI AGENT INTERACTIONS.\", \"page\": 4, \"index\": 8, \"width\": 529, \"height\": 268}]"
motivation: 现有的身份管理机制无法满足AI智能体自主创建、生命周期短以及与执行状态紧密耦合的动态认证需求。
method: 提出AgentDID框架，利用W3C标准的DID和VC实现去中心化身份管理，并引入挑战-响应机制来验证智能体的实时执行状态。
result: 实验表明AgentDID在多智能体并发场景下具有良好的吞吐量表现，能够实现高效且可扩展的身份认证与状态验证。
conclusion: AgentDID为去中心化环境下的AI智能体交互提供了可靠的身份与状态验证方案，支持大规模智能体群体的安全协作。
---

## 摘要
AI 智能体是能够按需实例化、跨平台迁移，并在无需持续人工监督的情况下与其他智能体或服务交互的自主实体。在此类环境中，身份对于在可能缺乏先前信任关系的智能体之间建立可靠的交互语义至关重要。然而，现有的身份与访问管理机制是为人类用户或静态机器设计的，它们假设了中心化注册、持久标识符以及稳定的执行上下文。这些假设并不适用于 AI 智能体，因为其身份是自我管理的、短周期的，并且与其执行状态和能力紧密耦合。我们研究了 AI 智能体的身份认证与状态验证问题，并识别出三个挑战：(1) 支持自主创建智能体的自我管理身份；(2) 实现大规模并发交互下的身份认证；(3) 验证智能体的动态执行状态，例如其上下文和能力在交互时是否仍然有效。为了应对这些挑战，我们提出了 AgentDID，这是一个用于身份认证和状态验证的去中心化框架。AgentDID 利用去中心化标识符 (DID) 和可验证凭证 (VC)，使智能体能够管理自己的身份并在没有中心化控制的情况下跨系统进行认证。为了解决基于静态凭证方法的局限性，AgentDID 引入了一种挑战-响应机制，允许验证者在交互时验证智能体的执行条件。我们按照 W3C 标准实现了 AgentDID，并通过多个并发智能体的吞吐量实验对其进行了评估。结果表明，该系统实现了可扩展的身份认证和状态验证，展示了其支持大规模 AI 智能体群体的潜力。

## Abstract
AI agents are autonomous entities that can be instantiated on demand, migrate across platforms, and interact with other agents or services without continuous human supervision. In such environments, identity is critical for establishing reliable interaction semantics among agents that may lack prior trust relationships. However, existing identity and access management mechanisms are designed for human users or static machines, assuming centralized enrollment, persistent identifiers, and stable execution contexts. These assumptions do not hold for AI agents, whose identities are self-managed, short-lived, and tightly coupled with their execution state and capabilities. We study the problem of identity authentication and state verification for AI agents and identify three challenges: (1) supporting self-managed identities for autonomously created agents, (2) enabling authentication under large-scale, concurrent interactions, and (3) verifying agents' dynamic execution state, such as whether their context and capabilities remain valid at interaction time. To address these challenges, we present AgentDID, a decentralized framework for identity authentication and state verification. AgentDID leverages decentralized identifiers (DIDs) and verifiable credentials (VCs), enabling agents to manage their own identities and authenticate across systems without centralized control. To address the limitations of static credential-based approaches, AgentDID introduces a challenge-response mechanism that allows verifiers to validate an agent's execution conditions at interaction time. We implement AgentDID in compliance with W3C standards and evaluate it through throughput experiments with multiple concurrent agents. Results show that the system achieves scalable identity authentication and state verification, demonstrating its potential to support large populations of AI agents.

---

## 论文详细总结（自动生成）

### 论文结构化总结：AgentDID

#### 1. 核心问题与整体含义（研究动机和背景）
随着 AI 智能体（AI Agents）在企业和个人应用中的爆发式增长，现有的身份与访问管理（IAM）系统（如 OAuth 2.0、SAML、FIDO2）面临严峻挑战。传统系统假设身份是中心化管理的、长效的且主要针对人类或静态机器。然而，AI 智能体具有**自主性、跨平台迁移性、生命周期短**以及**执行状态动态变化**等特征。
论文识别了三个核心挑战：
*   **(C1) 自我管理身份**：智能体需要自主生成身份，不依赖单一提供商。
*   **(C2) 大规模并发认证**：智能体交互频率极高，中心化架构易成为性能瓶颈。
*   **(C3) 动态状态验证**：仅验证身份（“你是谁”）不足够，还需验证其当前状态（“你现在是否有能力/上下文执行此任务”）。

#### 2. 方法论：核心思想与关键技术
AgentDID 提出了一个基于区块链和 W3C 标准的去中心化框架，核心流程如下：
*   **身份生成（Identity Generation）**：
    *   **DID 注册**：Controller 为智能体生成管理密钥对和操作密钥对，并在区块链上注册 DID 及其文档（DID Document）。
    *   **VC 签发**：Issuer 验证智能体的来源（Provenance）、能力（Capabilities，通过 Benchmark 测试）和合规性，签发可验证凭证（VC）。
*   **身份认证（Identity Authentication）**：
    *   采用挑战-响应机制，验证者（Verifier）发送 Nonce，持有者（Holder）生成包含 VC 的可验证表达（VP）并签名。
*   **动态状态验证（State Verification）—— 核心创新**：
    *   **就绪探测（Readiness Probe）**：通过标准化的探测任务（如工具调用、文本处理）验证智能体当前是否在线、推理引擎是否正常及响应延迟。
    *   **上下文一致性检查（Context Consistency Check）**：通过对序列化后的交互历史进行哈希（Hash）计算并签名对比，确保双方在长对话中上下文同步，防止因实例重启或缓存失效导致的状态丢失。

#### 3. 实验设计
*   **实验场景**：在以太坊 Sepolia 测试网上进行实测，模拟真实区块链环境。
*   **技术栈**：Python 3.11 (Web3.py) + Node.js (ethr-did-resolver) + LangChain/LangGraph。
*   **底层模型**：使用通义千问（Qwen-Turbo, Qwen-Plus）作为智能体的推理引擎。
*   **Benchmark 与对比**：
    *   **功能对比**：与传统 IAM 及现有研究（如 BlockA2A 等）在 SSI 支持、并发扩展性、状态验证三个维度进行理论对比。
    *   **性能测试**：从 1v1 到 50v50 的并发规模，测量延迟（Latency）和吞吐量（TPS）。
    *   **微观基准**：测试不同上下文长度（最高 40MB/10M tokens）下的哈希计算开销。

#### 4. 资源与算力
*   **硬件环境**：虚拟机配置为 24 核 Intel Xeon Silver 4214R CPU，64 GB 内存，运行 Ubuntu 22.04。
*   **算力说明**：论文主要关注身份框架的协议开销和区块链交互，**未涉及模型训练**，因此未列出 GPU 训练时长。推理过程通过 DashScope API 调用云端模型。

#### 5. 实验数量与充分性
*   **实验规模**：进行了 100 轮 DID 注册实验以统计 Gas 费和延迟；在并发测试中涵盖了 6 个不同的并发梯度（1 到 50 对智能体）。
*   **充分性评价**：实验设计较为充分，涵盖了从链上成本、链下处理延迟到大规模并发吞吐量的全方位评估。通过微观基准测试验证了长上下文哈希的可行性，证明了方案在极端情况下的鲁棒性。

#### 6. 主要结论与发现
*   **经济可行性**：在以太坊主网模拟成本下，单次 DID 注册费用低于 1 美元，VC 存储开销仅约 1.23 KB。
*   **性能表现**：总协议延迟稳定在 13.5 秒左右（主要受限于区块链查询和 LLM API 响应），且延迟不随并发量增加而显著恶化。
*   **可扩展性**：系统吞吐量随并发量线性增长，在 50v50 场景下达到 3.25 TPS，证明了去中心化架构处理大规模智能体群体的潜力。
*   **状态验证开销**：上下文哈希计算在毫秒级，对整体性能影响极小。

#### 7. 优点（亮点）
*   **首创性**：首次在去中心化身份框架中引入了针对 AI 智能体的“动态状态验证”机制。
*   **标准化**：严格遵循 W3C 的 DID 和 VC 标准，具有良好的互操作性。
*   **安全性**：通过形式化证明（Theorem 1）论证了框架在防御身份伪造和状态伪造方面的安全性。
*   **实用性**：结合了 LLM-as-a-Judge 和加密哈希，解决了智能体交互中“幻觉”或状态丢失的检测问题。

#### 8. 不足与局限
*   **隐私风险**：虽然实现了去中心化，但在 VC 交换和状态探测过程中可能泄露智能体的任务模式或能力细节，论文提到隐私保护将作为未来工作。
*   **区块链依赖**：身份解析仍依赖区块链响应，在网络拥塞时可能导致认证延迟增加。
*   **实验环境限制**：实验主要在测试网（Sepolia）进行，真实主网的 Gas 波动和确认时间可能会对用户体验产生更大影响。
*   **应用范围**：目前主要针对文本类智能体，对于多模态或具身智能体的状态验证（如物理环境一致性）尚未涉及。

（完）
