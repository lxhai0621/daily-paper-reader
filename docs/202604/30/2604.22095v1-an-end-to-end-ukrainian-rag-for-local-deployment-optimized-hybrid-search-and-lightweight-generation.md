---
title: An End-to-End Ukrainian RAG for Local Deployment. Optimized Hybrid Search and Lightweight Generation
title_zh: 一种用于本地部署的端到端乌克兰语 RAG 系统：优化的混合搜索与轻量化生成
authors: "Mykola Trokhymovych, Yana Oliinyk, Nazarii Nyzhnyk"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.22095v1"
tags: ["query:ma-kf"]
score: 10.0
evidence: 端到端 RAG 架构与实现
tldr: 本研究针对乌克兰语文档问答，开发了一套高效的端到端检索增强生成（RAG）系统，并在UNLP 2026共享任务中获得第二名。该系统采用两阶段搜索流程进行文档检索，并结合在合成数据上微调的专用乌克兰语模型生成准确答案。通过模型压缩技术，该架构实现了在资源受限硬件上的本地轻量化部署，证明了在严格计算限制下也能保持高质量且可验证的问答性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.22095v1/fig-001.webp\", \"caption\": \"Figure 3: Prompt template used for synthetic question generation. Note: examples appear in Ukrainian in the actual prompt.\", \"page\": 7, \"index\": 1, \"width\": 472, \"height\": 1304}, {\"url\": \"assets/figures/arxiv/2604.22095v1/fig-002.webp\", \"caption\": \"Figure 2: Prompt template for answer generation with grounding (translated from Ukrainian).\", \"page\": 7, \"index\": 2, \"width\": 473, \"height\": 725}]"
motivation: 旨在为乌克兰语文档问答开发一种能在资源受限硬件上本地部署的高效、准确的RAG系统。
method: 采用优化的两阶段混合搜索流程进行页面检索，并结合经过合成数据微调及压缩处理的轻量化乌克兰语生成模型。
result: 该系统在UNLP 2026共享任务中荣获第二名，在严格的计算限制下表现出卓越的性能。
conclusion: 证明了通过优化检索和模型压缩，可以在低算力环境下实现高质量、可验证的特定语言AI问答服务。
---

## 摘要
本文介绍了一种专为乌克兰语文档问答构建的高效检索增强生成（RAG）系统，该系统在 UNLP 2026 共享任务中获得了第二名。我们的解决方案采用了一个定制的两阶段搜索流水线来检索相关的文档页面，并配合一个在合成数据上进行微调的专用乌克兰语语言模型，以生成准确且有据可依的回答。最后，我们对模型进行了压缩以实现轻量化部署。在严格的计算限制下进行的评估表明，我们的架构证明了在资源受限的硬件上本地实现高质量、可验证的 AI 问答是可行的，且无需牺牲准确性。

## Abstract
This paper presents a highly efficient Retrieval-Augmented Generation (RAG) system built specifically for Ukrainian document question answering, which achieved 2nd place in the UNLP 2026 Shared Task. Our solution features a custom two-stage search pipeline that retrieves relevant document pages, paired with a specialized Ukrainian language model fine-tuned on synthetic data to generate accurate, grounded answers. Finally, we compress the model for lightweight deployment. Evaluated under strict computational limits, our architecture demonstrates that high-quality, verifiable AI question answering can be achieved locally on resource-constrained hardware without sacrificing accuracy.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种专为乌克兰语文档问答（QA）设计的端到端检索增强生成（RAG）系统，该系统在 UNLP 2026 共享任务中荣获第二名。

### 1. 论文的核心问题与整体含义
论文的核心挑战在于如何在**严格的硬件限制**（单块 P100 GPU、完全离线、9小时推理时限）下，构建一个针对**中等资源语言（乌克兰语）**的高精度问答系统。由于通用大模型在处理西里尔字母时存在分词效率低、知识过时及幻觉问题，作者旨在通过优化的检索流程和轻量化生成模型，实现可验证的、本地部署的乌克兰语 RAG 方案。

### 2. 论文提出的方法论
系统采用模块化流水线，主要包含以下关键技术：

*   **数据准备：** 使用 `pymupdf4llm` 进行布局感知的 PDF 提取，将文档转换为保留结构的 Markdown 格式。
*   **两阶段混合检索：**
    *   **第一阶段（文档级）：** 结合稠密检索（Perplexity 0.6B 嵌入模型）和稀疏检索（BM25）。若两者结果不一致，则调用 `jina-reranker-v3` 进行重排序，以确定唯一的候选文档。
    *   **第二阶段（页面级）：** 基于 Markdown 结构进行语法分块（每块 <500 字符）。利用倒数排名融合（RRF）结合向量搜索与 BM25 评分，最后使用自定义微调的 `bge-reranker-v2-m3` 交叉编码器选出最相关的页面。
*   **合成数据增强：** 利用 GPT-4o-mini 从原始 PDF 中自动生成约 7,000 个高质量的乌克兰语多选题（MCQ），用于模型微调。
*   **轻量化生成：** 选用专为乌克兰语优化的 `MamayLM-12B` 模型，通过 LoRA 技术进行微调，使其能够同时输出答案选项和来源页码（如 "A 2"）。
*   **本地部署优化：** 采用 4-bit GGUF 量化技术，确保 12B 参数模型能在 P100 GPU 上高效运行。

### 3. 实验设计
*   **数据集：** 使用 UNLP 2026 共享任务提供的开发集（461 个问题，41 份文档）和隐藏测试集（240 多份文档，涉及新领域）。
*   **Benchmark：** 比赛官方排行榜（分为 Public 和 Private 两部分）。
*   **评估指标：** 加权平均分，包括：
    1.  多选题答案准确率（权重 0.5）。
    2.  文档识别准确率（权重 0.25）。
    3.  页码接近度评分（权重 0.25，仅在文档识别正确时计算）。

### 4. 资源与算力
*   **硬件环境：** 比赛规定在单块 **NVIDIA P100 GPU** 上进行离线推理。
*   **训练细节：** 
    *   重排序模型（Reranker）训练了 5 个 Epoch。
    *   生成模型（LLM）使用 LoRA 训练了 2 个 Epoch（先用合成数据，再用开发集数据）。
*   **推理时长：** 整个测试集推理过程严格控制在 9 小时以内。

### 5. 实验数量与充分性
作者在开发集上进行了本地验证，并利用 7,000 条合成数据进行了充分的微调实验。虽然论文未展示大规模的消融实验表格，但其两阶段检索的准确率（文档检索近乎完美，页面 Recall@3 达 0.92）以及最终在比赛中取得的第二名成绩，客观证明了该方案的有效性和鲁棒性。

### 6. 主要结论与发现
*   **检索精度至上：** 在 RAG 架构中，检索的精确度比生成模型的原始能力更重要，因为错误的上下文直接导致错误的答案。
*   **混合检索的优势：** 结合语义（稠密）与关键词（稀疏）的混合搜索在处理特定领域文档时显著优于单一方法。
*   **本地化模型更优：** 针对特定语言（如乌克兰语）优化的分词器和预训练模型（如 MamayLM）能显著提升计算效率和准确性。
*   **量化可行性：** 4-bit 量化在大幅降低内存占用的同时，对问答准确性的影响在可接受范围内。

### 7. 优点
*   **针对性强：** 专门解决了乌克兰语在通用 LLM 中分词效率低下的痛点。
*   **架构高效：** 两阶段检索策略极大地缩小了搜索范围，平衡了延迟与精度。
*   **实用性高：** 证明了在旧款、资源受限的硬件（P100）上部署高性能 RAG 系统的可能性。
*   **数据闭环：** 通过合成数据生成解决了低资源语言标注数据不足的问题。

### 8. 不足与局限
*   **多模态缺失：** 为了节省算力，系统忽略了 PDF 中的图像和图表，可能丢失关键视觉信息。
*   **提取风险：** 依赖于 Markdown 转换，对于极少数布局极其复杂的 PDF 页面，解析可能会失败。
*   **硬件依赖：** 虽然针对 P100 优化，但若要进一步提升性能，可能需要更现代的量化技术（如 AWQ 或 GPTQ），而这些技术在旧架构上支持有限。
*   **幻觉风险：** 尽管有 RAG 约束，但在处理敏感领域（如医药）时，仍存在模型产生幻觉的潜在风险，需人工审核。

（完）
