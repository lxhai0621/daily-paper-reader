---
title: "KISS Sorcar: A Stupidly-Simple General-Purpose and Software Engineering AI Assistant"
title_zh: KISS Sorcar：一个极简的通用型及软件工程 AI 助手
authors: Koushik Sen
date: 2026-04-26
pdf: "https://arxiv.org/pdf/2604.23822v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 用于软件工程的自主AI智能体框架
tldr: "KISS Sorcar是一个基于极简框架（约1850行代码）的通用软件工程AI助手，旨在解决LLM在实际开发中面临的上下文限制、错误累积和代码质量差等问题。它采用五层代理架构，集成了ReAct执行、自动总结、Git隔离和工具调用等功能。通过优先考虑输出质量而非响应速度，并利用自身进行迭代开发，该工具在Terminal Bench 2.0上取得了62.2%的通过率，优于Claude Code和Cursor等主流工具。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.23822v1/fig-001.webp\", \"caption\": \"Figure 1: Screenshot of KISS Sorcar running as a VS Code extension. The sidebar shows the agent’s chat interface with real-time budget tracking, while the editor displays the code being modified.\", \"page\": 2, \"index\": 1, \"width\": 829, \"height\": 521}, {\"url\": \"assets/figures/arxiv/2604.23822v1/fig-002.webp\", \"caption\": \"Table 1: Terminal Bench 2.0 aggregate results (89 tasks, 5 trials each, Claude Opus 4.6).\", \"page\": 7, \"index\": 2, \"width\": 448, \"height\": 298}]"
motivation: 针对现有AI助手在长程任务中存在的上下文受限、错误难以修复及生成代码质量低等痛点。
method: 提出一种五层代理架构，结合预算跟踪、自动摘要、Git工作区隔离以及通过运行检查器和测试进行的自我验证机制。
result: "在Terminal Bench 2.0基准测试中，KISS Sorcar以62.2%的通过率超越了Claude Code和Cursor Composer 2。"
conclusion: 证明了通过极简框架和严谨的自我验证流程，可以构建出比复杂商业系统更高效且可靠的软件工程助手。
---

## 摘要
大语言模型能够以惊人的流畅度生成代码并调用工具，但将其部署为实用的软件工程助手时仍面临一些顽固的差距：有限的上下文窗口、导致整个会话脱轨的单一错误、陷入死胡同的智能体、AI 废话（AI slop），以及难以审查或撤销的生成更改。我们提出了 KISS Sorcar，这是一个基于 KISS Agent 框架构建的通用助手和集成开发环境（IDE），该框架是一个代码量约为 1,850 行的极简 AI 智能体框架。该框架通过强大的系统提示词和五层智能体层级结构来解决这些差距，每一层都恰好增加一个关注点：受预算跟踪的 ReAct 执行、通过摘要实现的跨子会话自动延续、带有并行子智能体的代码和浏览器工具、具有历史召回功能的持久多轮对话，以及 git 工作树隔离（使每个任务都在自己的分支上运行）。为了评估 KISS 智能体框架的能力，我们将 KISS Sorcar 实现为一个免费、开源的 Visual Studio Code 扩展，它可以在本地运行并有效处理长程任务，同时支持浏览器自动化、多模态输入和 Docker 容器。在这项研究中，我们刻意将输出质量置于延迟之上：给予前沿模型充足的时间来验证其自身输出——运行代码检查器（linters）、类型检查器和测试——这显著减少了困扰那些速度更快但不够彻底的智能体的低质量代码。整个系统是在 4.5 个月内使用其自身构建的，提供了一个持续的压力测试，其中智能体引入的任何错误都会立即损害其自身的工作能力。在 Terminal Bench 2.0 上，KISS Sorcar 配合 Claude Opus 4.6 实现了 62.2% 的总通过率，优于 Claude Code (58%) 和 Cursor Composer 2 (61.7%)。

## Abstract
Large language models can generate code and call tools with remarkable fluency, yet deploying them as practical software engineering assistants still expose stubborn gaps: finite context windows, single mistakes that derail entire sessions, agents that get stuck in dead ends, AI slop, and generated changes that are difficult to review or revert.   We present KISS Sorcar, a general-purpose assistant and integrated development environment (IDE) built on top of the KISS Agent Framework, a stupidly-simple AI agent framework of roughly 1,850 lines of code. The framework addresses these gaps using a robust system prompt and through a five-layer agent hierarchy in which each layer adds exactly one concern: budget-tracked ReAct execution, automatic continuation across sub-sessions via summarization, coding, and browser tools with parallel sub-agents, persistent multi-turn chat with history recall, and git worktree isolation so every task runs on its own branch.   To assess the power of the KISS agent framework, we implemented KISS Sorcar as a free, open-source Visual Studio Code extension that runs locally and effectively for long-horizon tasks, and supports browser automation, multimodal input, and Docker containers.   In this research, we deliberately prioritize output quality over latency: giving a frontier model adequate time to validate its own output -- running linters, type checkers, and tests -- dramatically reduces the low-quality code that plagues faster but less thorough agents. The entire system was built using itself in 4.5 months, providing a continuous stress test in which any agent-introduced bug immediately impairs its own ability to work. On Terminal Bench 2.0, KISS Sorcar achieves a 62.2% overall pass rate with Claude Opus 4.6, comparing favorably to Claude Code (58%) and Cursor Composer 2 (61.7).

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **KISS Sorcar** 的 AI 软件工程助手及其背后的 **KISS Agent Framework**。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：尽管大语言模型（LLM）在代码生成上表现出色，但在实际软件工程应用中仍存在五大痛点：上下文窗口有限、单一错误导致会话崩溃、智能体陷入死胡同、生成内容充斥“AI 废话”（AI slop），以及生成的更改难以审查或撤销。
*   **核心理念**：遵循“KISS（Keep It Simple, Stupid）”原则，主张通过极简的架构（约 1,850 行代码）和严谨的工程纪律（系统提示词），而非复杂的算法或大规模微调，来构建高性能的自主智能体。

### 2. 方法论：核心思想与关键技术
KISS Sorcar 采用**五层继承架构**，每一层只解决一个核心关注点：
1.  **KISS Agent（执行层）**：实现基础的 ReAct 循环，支持原生函数调用，并具备严格的步骤、Token 和预算跟踪功能。
2.  **Relentless Agent（持续层）**：通过“结构化摘要”解决上下文溢出。当达到限制时，智能体会总结进度并开启新会话继续任务。
3.  **Sorcar Agent（工具层）**：集成代码编辑、Shell 执行、浏览器自动化（Playwright）和并行子智能体执行。
4.  **Chat Sorcar Agent（对话层）**：管理持久化多轮对话，支持历史记录召回和任务元数据持久化。
5.  **Worktree Sorcar Agent（隔离层）**：**核心创新点**。为每个任务创建独立的 Git Worktree（分支），确保智能体的修改不污染用户的主工作区，支持一键合并或撤销。

**关键技术细节**：
*   **质量优先策略**：宁愿增加延迟也要让模型运行 Linter、类型检查器和测试。
*   **系统提示词工程**：包含执行心态（冷静、严谨）、预检规则（修改前必读）、代码风格（禁止冗余抽象）、测试指令（100% 分支覆盖、禁止使用 Mock）等详细规范。

### 3. 实验设计
*   **数据集/场景**：使用 **Terminal Bench 2.0**，包含 89 个复杂的终端编程任务（如构建编译器、服务器配置、机器学习模型训练等）。
*   **对比方法**：与 **Claude Code** (Anthropic 官方工具) 和 **Cursor Composer 2** (知名 AI IDE) 进行对比。
*   **评估模型**：统一使用 Claude Opus 4.6 作为底层模型。
*   **自举测试（Self-hosting）**：该系统在 4.5 个月内通过“自己构建自己”的方式进行开发，任何引入的 Bug 都会直接阻碍智能体后续的开发工作，形成了极强的压力测试。

### 4. 资源与算力
*   **算力设备**：实验在一台 **2025 款 MacBook Air 15"** 上本地运行。
*   **推理成本**：论文详细记录了成本，单次试验的中位成本为 **0.45 美元**，平均成本为 **0.90 美元**。
*   **训练时长**：该项目**未进行模型微调或训练**，完全基于现有的前沿模型 API。

### 5. 实验数量与充分性
*   **实验规模**：针对 89 个任务，每个任务进行 5 次独立试验，总计 **445 次试验**。
*   **充分性**：实验覆盖了系统编程、安全、数据工程等多个领域。作者还讨论了基准测试中的“作弊”现象，并强调其结果是“无作弊且诚实”的。
*   **客观性**：通过 5 次试验的平均值（pass@any, pass@all）来衡量，而非取最高分，体现了实验的严谨性。

### 6. 主要结论与发现
*   **性能领先**：KISS Sorcar 实现了 **62.2%** 的总通过率，优于 Claude Code (58%) 和 Cursor Composer 2 (61.7%)。
*   **架构胜过复杂性**：证明了简单的分层架构配合强大的系统提示词，可以超越经过大规模强化学习（RL）微调的专用模型。
*   **质量反馈循环**：强制运行测试和检查显著减少了低质量代码的产生。
*   **专家时代**：作者认为，未来 3-5 年，能够熟练驾驭前沿模型并具备领域专业知识的“专家”将成为软件开发的主导者。

### 7. 优点
*   **极简主义**：代码量极小（1,850 行），易于理解、维护和二次开发。
*   **安全性与可撤销性**：Git Worktree 隔离机制解决了 AI 助手乱改代码且难以恢复的行业难题。
*   **透明度**：实时显示 Token 消耗和美元成本，让用户对 AI 的开销有明确预期。
*   **自我进化**：通过 `USER_PREFS.md` 自动记录并学习用户的编码偏好，实现跨会话的持续改进。

### 8. 不足与局限
*   **延迟较高**：由于坚持“质量优先”，在生成代码后会运行大量验证步骤，响应速度慢于追求即时反馈的助手。
*   **模型依赖**：高度依赖 Claude Opus 4.6 等顶级模型，在较弱的模型上表现可能会大幅下降。
*   **多媒体限制**：在 Terminal Bench 中，涉及图形界面（GUI）或复杂多媒体处理的任务仍无法有效解决。
*   **摘要膨胀风险**：虽然目前未遇到，但对于超长周期的任务，自动生成的摘要可能会变得过于庞大。

（完）
