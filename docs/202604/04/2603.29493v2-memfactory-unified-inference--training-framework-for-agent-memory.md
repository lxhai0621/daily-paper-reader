---
title: "MemFactory: Unified Inference & Training Framework for Agent Memory"
title_zh: MemFactory：智能体记忆的统一推理与训练框架
authors: "Ziliang Guo, Ziheng Li, Bo Tang, Feiyu Xiong, Zhiyu Li"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29493v2"
tags: ["query:agent"]
score: 10.0
evidence: 使用强化学习的智能体记忆统一框架
tldr: "MemFactory是首个专为记忆增强型智能体设计的统一推理与训练框架。针对记忆管理研究碎片化的问题，它采用模块化设计将记忆操作抽象为可插拔组件，并集成GRPO算法优化策略。该框架支持多种前沿范式，在实验中实现了最高14.8%的性能提升，显著降低了开发门槛并推动了记忆驱动AI的标准化。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29493v2/fig-001.webp\", \"caption\": \"Figure 1 The overall architecture of MemFactory, illustrating the interdependencies among the Modules, Agent, Environment, and Trainer layers.\", \"page\": 4, \"index\": 1, \"width\": 929, \"height\": 482}, {\"url\": \"assets/figures/arxiv/2603.29493v2/fig-002.webp\", \"caption\": \"Table 1 Standard interfaces supported by modules in the Module Layer.\", \"page\": 5, \"index\": 2, \"width\": 968, \"height\": 219}]"
motivation: 现有的记忆增强型智能体实现高度碎片化且缺乏统一的基础设施，导致集成、训练和评估过程极其复杂。
method: 提出一种模块化的“乐高式”架构，将记忆生命周期抽象为原子组件，并利用GRPO算法根据环境奖励优化记忆管理策略。
result: "在MemAgent架构上的实验显示，该框架在域内和分布外评估中均优于基础模型，最高实现14.8%的性能增益。"
conclusion: MemFactory通过提供标准化、可扩展且易用的基础设施，为未来记忆驱动的AI智能体创新铺平了道路。
---

## 摘要
记忆增强的大语言模型（LLMs）对于开发具备强大能力的长期 AI 智能体至关重要。近年来，应用强化学习（RL）来优化记忆操作（如提取、更新和检索）已成为一个极具前景的研究方向。然而，现有的实现方式仍然高度碎片化且针对特定任务，缺乏统一的基础设施来简化这些复杂流水线的集成、训练和评估。为填补这一空白，我们推出了 MemFactory，这是首个专为记忆增强智能体设计的统一、高度模块化的训练与推理框架。受 LLaMA-Factory 等统一微调框架成功的启发，MemFactory 将记忆生命周期抽象为原子化的即插即用组件，使研究人员能够通过“乐高式”架构无缝构建自定义记忆智能体。此外，该框架原生集成了群体相对策略优化（GRPO），以在多维环境奖励的驱动下微调内部记忆管理策略。MemFactory 为 Memory-R1、RMM 和 MemAgent 等近期前沿范式提供开箱即用的支持。我们在开源的 MemAgent 架构上，利用其公开的训练和评估数据对 MemFactory 进行了实证验证。在域内和分布外评估集上，MemFactory 相比相应的基座模型均能持续提升性能，相对增益最高达 14.8%。通过提供标准化、可扩展且易于使用的基础设施，MemFactory 显著降低了准入门槛，为记忆驱动型 AI 智能体的未来创新铺平了道路。

## Abstract
Memory-augmented Large Language Models (LLMs) are essential for developing capable, long-term AI agents. Recently, applying Reinforcement Learning (RL) to optimize memory operations, such as extraction, updating, and retrieval, has emerged as a highly promising research direction. However, existing implementations remain highly fragmented and task-specific, lacking a unified infrastructure to streamline the integration, training, and evaluation of these complex pipelines. To address this gap, we present MemFactory, the first unified, highly modular training and inference framework specifically designed for memory-augmented agents. Inspired by the success of unified fine-tuning frameworks like LLaMA-Factory, MemFactory abstracts the memory lifecycle into atomic, plug-and-play components, enabling researchers to seamlessly construct custom memory agents via a "Lego-like" architecture. Furthermore, the framework natively integrates Group Relative Policy Optimization (GRPO) to fine-tune internal memory management policies driven by multi-dimensional environmental rewards. MemFactory provides out-of-the-box support for recent cutting-edge paradigms, including Memory-R1, RMM, and MemAgent. We empirically validate MemFactory on the open-source MemAgent architecture using its publicly available training and evaluation data. Across both in-domain and out-of-distribution evaluation sets, MemFactory consistently improves performance over the corresponding base models, with relative gains of up to 14.8%. By providing a standardized, extensible, and easy-to-use infrastructure, MemFactory significantly lowers the barrier to entry, paving the way for future innovations in memory-driven AI agents.

---

## 论文详细总结（自动生成）

以下是对论文《MemFactory: Unified Inference & Training Framework for Agent Memory》的结构化深度总结：

### 1. 核心问题与研究背景
*   **核心问题**：尽管记忆增强型大语言模型（LLM）智能体在处理长期上下文和个性化交互方面至关重要，但目前的记忆管理研究（如提取、更新、检索）存在严重的**碎片化**问题。
*   **研究背景**：现有的记忆优化方法（如 Memory-R1, MemAgent, RMM）通常针对特定任务高度定制，代码库彼此孤立，缺乏一个像 LLaMA-Factory 那样统一、模块化且易于扩展的基础设施，导致研究人员在集成、训练和评估不同记忆策略时面临巨大的工程障碍。

### 2. 方法论：核心思想与技术细节
MemFactory 提出了一个四层模块化架构，旨在实现记忆智能体的“乐高式”构建：
*   **模块层 (Module Layer)**：将记忆生命周期抽象为原子操作。
    *   **Extractor**：从原始上下文中解析事实和见解。
    *   **Updater**：执行 ADD（添加）、DEL（删除）、UPDATE（更新）或 NONE 操作，维护记忆库的一致性。
    *   **Retriever**：支持语义检索及基于推理模型的重排序（Rerank）。
    *   **Agent Module**：支持端到端的循环记忆策略（如 MemAgent 范式）。
*   **智能体层 (Agent Layer)**：作为策略执行器，通过组合上述原子模块构建复杂的记忆工作流，支持训练模式（生成轨迹）和推理模式。
*   **环境层 (Environment Layer)**：负责数据加载和多维奖励计算（如格式奖励、LLM-as-a-Judge 评分）。
*   **训练层 (Trainer Layer)**：核心优化引擎，原生集成 **GRPO (Group Relative Policy Optimization)** 算法。
    *   **GRPO 优势**：通过组内奖励归一化估计基准，无需额外的 Critic 网络，显著降低了长上下文训练时的显存开销。

### 3. 实验设计
*   **场景与架构**：基于 **MemAgent** 架构进行实证研究。
*   **数据集/Benchmark**：
    *   使用 MemAgent 公开的训练和评估数据。
    *   **测试集**：包含两个主任务集（`eval_50`, `eval_100`）和一个分布外（OOD）测试集（`eval_fwe_16384`）。
*   **对比方法**：以 **Qwen3-1.7B** 和 **Qwen3-4B-Instruct** 作为基座模型，对比其原始 Checkpoint 与经过 MemFactory RL 微调后的性能差异。

### 4. 资源与算力
*   **硬件环境**：整个训练和评估流水线可以在**单张 NVIDIA A800 80GB GPU** 上运行。
*   **训练细节**：每个模型训练 250 个有效步数（steps）。为了提高效率，训练样本的上下文长度被简化为原始长度的约 1/3（每样本 50-80 个文档）。

### 5. 实验数量与充分性
*   **实验规模**：针对两个不同规模的基座模型在三个代表性测试集上进行了实验。
*   **客观性与公平性**：
    *   所有评分均采用 **avg@4**（即每个问题进行 4 次独立试验并取平均值），以减少生成随机性的影响。
    *   实验涵盖了域内（In-domain）和分布外（OOD）场景，验证了框架的泛化能力。
    *   虽然实验组数不算庞大，但针对核心指标（性能增益、模型规模影响）提供了清晰的对比。

### 6. 主要结论与发现
*   **性能显著提升**：MemFactory 成功优化了记忆策略。Qwen3-1.7B 在平均得分上实现了 **14.8%** 的相对增长；Qwen3-4B-Instruct 实现了 **7.3%** 的提升。
*   **模型规模效应**：较大的模型（4B）在 RL 微调后表现出更稳健的泛化能力，尤其是在 OOD 测试集上依然保持增长；而较小模型（1.7B）在 OOD 任务上出现了轻微性能下降。
*   **框架有效性**：证明了通过 GRPO 优化记忆管理策略（如“覆盖”或“压缩”历史事实）比传统的启发式规则更有效。

### 7. 优点与亮点
*   **高度模块化**：通过解耦记忆操作，研究人员可以像搭积木一样快速复现或组合不同的记忆算法（如将 Memory-R1 的检索器换成 RMM 的重排序器）。
*   **低硬件门槛**：支持单卡训练长上下文记忆策略，对学术界和中小型团队非常友好。
*   **开箱即用**：预置了 Memory-R1、MemAgent 和 RMM 等前沿算法的实现，极大降低了入门门槛。
*   **监控集成**：原生支持 SwanLab，可视化训练过程中的奖励分布和生成轨迹。

### 8. 不足与局限
*   **范式覆盖有限**：目前仅涵盖了三种代表性范式，对于更复杂的分布式记忆或多模态记忆支持尚待开发。
*   **训练效率**：尽管使用了 GRPO，但在处理极长上下文（如 128k+）时的训练吞吐量仍有提升空间。
*   **小模型泛化风险**：实验显示小模型在经过 RL 强化后可能在 OOD 任务上存在过拟合或性能波动风险。

（完）
