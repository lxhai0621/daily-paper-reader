---
title: "MemFactory: Unified Inference & Training Framework for Agent Memory"
title_zh: MemFactory：智能体记忆的统一推理与训练框架
authors: "Ziliang Guo, Ziheng Li, Bo Tang, Feiyu Xiong, Zhiyu Li"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29493v3"
tags: ["query:agent"]
score: 10.0
evidence: 利用强化学习优化智能体框架中的记忆操作
tldr: "MemFactory是一个专为记忆增强型智能体设计的统一推理与训练框架。针对现有记忆管理实现碎片化且缺乏标准的问题，它借鉴LLaMA-Factory的模块化理念，将记忆生命周期抽象为可插拔的原子组件。该框架原生支持GRPO算法，通过环境奖励优化记忆策略，并兼容Memory-R1等前沿范式。实验证明，MemFactory能显著提升智能体性能，最高增幅达14.8%，有效降低了记忆驱动型AI的研究门槛。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29493v3/fig-001.webp\", \"caption\": \"Figure 1 The overall architecture of MemFactory, illustrating the interdependencies among the Modules, Agent, Environment, and Trainer layers.\", \"page\": 4, \"index\": 1, \"width\": 929, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2603.29493v3/fig-002.webp\", \"caption\": \"Table 1 Standard interfaces supported by modules in the Module Layer.\", \"page\": 5, \"index\": 2, \"width\": 968, \"height\": 219}]"
motivation: 现有的记忆增强型智能体实现高度碎片化且缺乏统一的基础设施，导致记忆操作的集成、训练和评估过程极其复杂。
method: 采用模块化“乐高式”架构抽象记忆生命周期，并集成GRPO算法以实现基于多维环境奖励的记忆管理策略微调。
result: "在MemAgent架构上的实验表明，该框架相比基础模型实现了显著的性能提升，最高相对增益达14.8%。"
conclusion: MemFactory通过提供标准化且易用的基础设施，为记忆驱动型AI智能体的开发与创新提供了高效的工具支持。
---

## 摘要
记忆增强型大语言模型（LLMs）对于开发具备长期能力的 AI 智能体至关重要。近期，应用强化学习（RL）来优化记忆操作（如提取、更新和检索）已成为一个极具前景的研究方向。然而，现有的实现方式仍然高度碎片化且针对特定任务，缺乏统一的基础设施来简化这些复杂流水线的集成、训练和评估。为填补这一空白，我们提出了 MemFactory，这是首个专为记忆增强型智能体设计的统一、高度模块化的训练与推理框架。受 LLaMA-Factory 等统一微调框架成功的启发，MemFactory 将记忆生命周期抽象为原子化的、即插即用的组件，使研究人员能够通过“乐高式”架构无缝构建自定义记忆智能体。此外，该框架原生集成了群体相对策略优化（GRPO），以微调由多维环境奖励驱动的内部记忆管理策略。MemFactory 为近期前沿范式（包括 Memory-R1、RMM 和 MemAgent）提供开箱即用的支持。我们在开源的 MemAgent 架构上，利用其公开的训练和评估数据对 MemFactory 进行了实证验证。在各评估集上，MemFactory 相比相应的基座模型平均提升了性能，相对增益最高达 14.8%。通过提供标准化、可扩展且易于使用的基础设施，MemFactory 显著降低了准入门槛，为记忆驱动型 AI 智能体的未来创新铺平了道路。

## Abstract
Memory-augmented Large Language Models (LLMs) are essential for developing capable, long-term AI agents. Recently, applying Reinforcement Learning (RL) to optimize memory operations, such as extraction, updating, and retrieval, has emerged as a highly promising research direction. However, existing implementations remain highly fragmented and task-specific, lacking a unified infrastructure to streamline the integration, training, and evaluation of these complex pipelines. To address this gap, we present MemFactory, the first unified, highly modular training and inference framework specifically designed for memory-augmented agents. Inspired by the success of unified fine-tuning frameworks like LLaMA-Factory, MemFactory abstracts the memory lifecycle into atomic, plug-and-play components, enabling researchers to seamlessly construct custom memory agents via a "Lego-like" architecture. Furthermore, the framework natively integrates Group Relative Policy Optimization (GRPO) to fine-tune internal memory management policies driven by multi-dimensional environmental rewards. MemFactory provides out-of-the-box support for recent cutting-edge paradigms, including Memory-R1, RMM, and MemAgent. We empirically validate MemFactory on the open-source MemAgent architecture using its publicly available training and evaluation data. Across the evaluation sets, MemFactory improves performance over the corresponding base models on average, with relative gains of up to 14.8%. By providing a standardized, extensible, and easy-to-use infrastructure, MemFactory significantly lowers the barrier to entry, paving the way for future innovations in memory-driven AI agents.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **MemFactory**，一个专为记忆增强型智能体（Memory-augmented Agents）设计的统一推理与训练框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：虽然利用强化学习（RL）优化智能体的记忆操作（如提取、更新、检索）已成为前沿趋势，但现有的研究（如 Memory-R1, MemAgent, RMM）实现高度碎片化，代码库孤立且任务耦合度高。
*   **研究动机**：缺乏一个像 LLaMA-Factory 那样标准化、模块化且易于使用的基础设施，导致研究人员难以复现、组合或改进不同的记忆策略。MemFactory 旨在填补这一空白，降低记忆驱动型智能体的开发门槛。

### 2. 方法论：核心思想与技术细节
MemFactory 采用了高度模块化的“乐高式”架构，分为四个核心层：
*   **模块层 (Module Layer)**：将记忆生命周期抽象为原子操作。
    *   **Extractor**：从原始上下文中解析事实和经验。
    *   **Updater**：执行 ADD（添加）、DEL（删除）、UPDATE（修改）或 NONE 操作，保持记忆库简洁。
    *   **Retriever**：支持语义检索（Naive）和基于大推理模型的重排序（Rerank）。
    *   **Agent Module**：支持端到端的循环记忆策略（如 MemAgent 范式）。
*   **智能体层 (Agent Layer)**：作为策略执行器，通过组合上述模块构建自定义智能体，支持训练模式（生成轨迹）和纯推理模式。
*   **环境层 (Environment Layer)**：负责数据加载和奖励计算。提供 `MemoryBankEnv`（长期记忆）和 `LongcontextEnv`（短期上下文）两种环境，并支持格式奖励和“LLM-as-a-Judge”等多维奖励信号。
*   **训练层 (Trainer Layer)**：核心优化引擎。
    *   **关键算法**：原生集成 **GRPO (Group Relative Policy Optimization)**。该算法通过组内奖励归一化计算优势（Advantage），无需额外的 Critic 模型，显著降低了显存占用，适合长上下文任务。

### 3. 实验设计
*   **实验场景**：基于开源的 **MemAgent** 架构进行验证。
*   **数据集/Benchmark**：使用 MemAgent 公开的训练和评估数据，包括两个主任务测试集（`eval_50`, `eval_100`）和一个分布外（OOD）测试集（`eval_fwe_16384`）。
*   **对比方法**：对比了基座模型（Base Checkpoint）与经过 MemFactory RL 微调后的模型性能。
*   **基座模型**：选用 Qwen3-1.7B 和 Qwen3-4B-Instruct。

### 4. 资源与算力
*   **硬件设备**：整个训练和评估流水线可以在 **单张 NVIDIA A800 80GB GPU** 上运行。
*   **训练细节**：每个模型训练 250 个有效步（valid steps）。为了提高效率，训练时将上下文长度简化为原始长度的三分之一（每样本约 50-80 个文档）。

### 5. 实验数量与充分性
*   **实验规模**：针对两个不同规模的基座模型在三个测试集上进行了对比。
*   **客观性保障**：所有评分均采用 **avg@4**（即每个问题进行 4 次独立试验后取平均值），以减少生成随机性带来的偏差。
*   **充分性评价**：实验涵盖了主任务和 OOD 任务，验证了框架的泛化能力。虽然实验组数不算庞大，但针对核心模块的验证较为直接且具有代表性。

### 6. 主要结论与发现
*   **性能提升**：MemFactory 显著提升了模型表现。Qwen3-1.7B 的平均得分相对提升了 **14.8%**；Qwen3-4B-Instruct 提升了 **7.3%**。
*   **模型规模效应**：较大的模型（4B）在 OOD 测试集上表现更稳健，显示出更好的记忆策略迁移能力；而较小模型（1.7B）虽然在主任务提升明显，但在 OOD 任务上略有下降。
*   **框架有效性**：证明了即使在有限算力和简化数据下，通过统一框架进行 RL 微调也能有效优化复杂的记忆管理策略。

### 7. 优点与亮点
*   **高度模块化**：通过标准化的接口（generate, rollout, inference），研究人员可以像搭积木一样更换检索器或更新器。
*   **低硬件门槛**：集成 GRPO 算法，使得在单卡上训练长上下文记忆策略成为可能。
*   **开箱即用**：预置了 Memory-R1、MemAgent 和 RMM 等前沿算法的实现，具有极高的实用价值。

### 8. 不足与局限
*   **范式覆盖有限**：目前仅涵盖了少数几种代表性记忆范式，未来仍需扩展更多复杂的记忆架构。
*   **训练效率**：论文提到在训练效率方面仍有提升空间。
*   **小模型泛化风险**：实验显示小模型在经过特定任务 RL 训练后，可能存在分布外（OOD）性能退化的风险。

（完）
