---
title: "MemFactory: Unified Inference & Training Framework for Agent Memory"
title_zh: MemFactory：智能体记忆的统一推理与训练框架
authors: "Ziliang Guo, Ziheng Li, Zhiyu Li"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29493v1"
tags: ["query:agent"]
score: 10.0
evidence: 用于强化学习优化智能体记忆操作的统一框架
tldr: MemFactory是首个针对记忆增强型智能体设计的统一推理与训练框架。针对当前记忆管理研究碎片化且缺乏通用工具的问题，该框架借鉴LLaMA-Factory的模块化理念，将记忆生命周期抽象为可插拔的原子组件，并集成GRPO算法以优化记忆策略。它支持多种前沿记忆模型，在实验中显著提升了智能体性能，为记忆驱动的AI研究提供了标准化的基础设施。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29493v1/fig-001.webp\", \"caption\": \"Figure 1 The overall architecture of MemFactory, illustrating the interdependencies among the Modules, Agent, Environment, and Trainer layers.\", \"page\": 4, \"index\": 1, \"width\": 929, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2603.29493v1/fig-002.webp\", \"caption\": \"Table 1 Standard interfaces supported by modules in the Module Layer.\", \"page\": 5, \"index\": 2, \"width\": 968, \"height\": 221}]"
motivation: 现有的记忆增强型智能体实现方式高度碎片化且缺乏统一的训练与推理框架，限制了复杂记忆流水线的开发与评估。
method: 采用模块化的“乐高式”架构抽象记忆操作，并原生集成GRPO算法，通过多维环境奖励对内部记忆管理策略进行微调。
result: "在MemAgent基准测试中，MemFactory在域内和分布外数据集上均表现优异，相比基础模型实现了最高14.8%的性能提升。"
conclusion: MemFactory通过提供标准化且易用的基础设施，显著降低了记忆增强型智能体的开发门槛，推动了该领域的创新。
---

## 摘要
记忆增强型大语言模型（LLMs）对于开发具备强大能力的长期 AI 智能体至关重要。近期，应用强化学习（RL）来优化记忆操作（如提取、更新和检索）已成为一个极具前景的研究方向。然而，现有的实现方式仍然高度碎片化且针对特定任务，缺乏统一的基础设施来简化这些复杂流水线的集成、训练和评估。为填补这一空白，我们提出了 MemFactory，这是首个专为记忆增强型智能体设计的统一、高度模块化的训练与推理框架。受 LLaMA-Factory 等统一微调框架成功的启发，MemFactory 将记忆生命周期抽象为原子化的即插即用组件，使研究人员能够通过“乐高式”架构无缝构建自定义记忆智能体。此外，该框架原生集成了群体相对策略优化（GRPO），以在多维环境奖励的驱动下微调内部记忆管理策略。MemFactory 为 Memory-R1、RMM 和 MemAgent 等近期前沿范式提供开箱即用的支持。我们在开源的 MemAgent 架构上，利用其公开的训练和评估数据对 MemFactory 进行了实证验证。在域内和分布外评估集上，MemFactory 相比相应的基座模型均能持续提升性能，相对增益最高达 14.8%。通过提供标准化、可扩展且易于使用的基础设施，MemFactory 显著降低了准入门槛，为记忆驱动型 AI 智能体的未来创新铺平了道路。

## Abstract
Memory-augmented Large Language Models (LLMs) are essential for developing capable, long-term AI agents. Recently, applying Reinforcement Learning (RL) to optimize memory operations, such as extraction, updating, and retrieval, has emerged as a highly promising research direction. However, existing implementations remain highly fragmented and task-specific, lacking a unified infrastructure to streamline the integration, training, and evaluation of these complex pipelines. To address this gap, we present MemFactory, the first unified, highly modular training and inference framework specifically designed for memory-augmented agents. Inspired by the success of unified fine-tuning frameworks like LLaMA-Factory, MemFactory abstracts the memory lifecycle into atomic, plug-and-play components, enabling researchers to seamlessly construct custom memory agents via a "Lego-like" architecture. Furthermore, the framework natively integrates Group Relative Policy Optimization (GRPO) to fine-tune internal memory management policies driven by multi-dimensional environmental rewards. MemFactory provides out-of-the-box support for recent cutting-edge paradigms, including Memory-R1, RMM, and MemAgent. We empirically validate MemFactory on the open-source MemAgent architecture using its publicly available training and evaluation data. Across both in-domain and out-of-distribution evaluation sets, MemFactory consistently improves performance over the corresponding base models, with relative gains of up to 14.8%. By providing a standardized, extensible, and easy-to-use infrastructure, MemFactory significantly lowers the barrier to entry, paving the way for future innovations in memory-driven AI agents.

---

## 论文详细总结（自动生成）

### MemFactory：智能体记忆的统一推理与训练框架论文总结

#### 1. 核心问题与整体含义
论文针对当前**记忆增强型大语言模型（LLM）智能体**在开发过程中的碎片化问题提出了解决方案。虽然利用强化学习（RL）优化记忆操作（如提取、更新、检索）已成为提升智能体长期上下文处理能力的关键方向，但现有研究（如 Memory-R1, MemAgent 等）的代码实现高度定制化且互不兼容。MemFactory 的核心目标是提供一个类似于 LLaMA-Factory 的**统一、模块化基础设施**，旨在降低记忆驱动型智能体的开发、训练和评估门槛。

#### 2. 方法论
MemFactory 采用了“乐高式”的模块化架构，将复杂的记忆生命周期抽象为四个核心层：
*   **模块层 (Module Layer)：** 定义了原子化的记忆操作，包括**提取器 (Extractor)**、**更新器 (Updater)**、**检索器 (Retriever)** 和**智能体模块 (Agent Module)**。这些组件通过标准化的接口（generate, rollout, inference）实现即插即用。
*   **智能体层 (Agent Layer)：** 作为策略执行器，负责组合不同的原子模块以构建特定的记忆策略，并生成交互轨迹。
*   **环境层 (Environment Layer)：** 充当数据加载器和奖励管理器。它将原始数据标准化，并提供多维奖励信号（如格式奖励、LLM-as-a-Judge 评分）。
*   **训练层 (Trainer Layer)：** 核心优化引擎，原生集成了**群体相对策略优化 (GRPO)** 算法。
    *   **关键技术细节：** GRPO 通过对一组采样响应进行奖励归一化来计算优势值（Advantage），无需额外的 Critic 网络，显著降低了显存占用，适合长上下文的记忆训练任务。

#### 3. 实验设计
*   **实验场景：** 重点验证了 **MemAgent**（一种基于循环状态的记忆架构）在 MemFactory 框架下的训练效果。
*   **数据集：** 使用了 MemAgent 公开的训练和评估数据。
*   **Benchmark（基准测试）：** 
    *   **主任务：** `eval_50` 和 `eval_100`（测试长上下文处理能力）。
    *   **分布外（OOD）任务：** `eval_fwe_16384`（测试模型的泛化能力）。
*   **对比方法：** 将经过 MemFactory RL 微调后的模型与原始基座模型（Base Checkpoint）进行对比。
*   **实验模型：** Qwen3-1.7B 和 Qwen3-4B-Instruct。

#### 4. 资源与算力
*   **硬件设备：** 论文明确指出整个训练和评估流水线可以在**单台 NVIDIA A800 80GB GPU** 上执行。
*   **训练细节：** 每个模型训练了 250 个有效步数（valid steps）。为了适配显存，研究者对训练样本进行了简化，将上下文长度缩减至原始长度的约 1/3（每样本包含 50-80 个文档）。

#### 5. 实验数量与充分性
*   **实验规模：** 针对两个不同规模的基座模型（1.7B 和 4B）进行了实验，涵盖了域内和分布外共三个测试集。
*   **客观性保障：** 所有评分均基于 **4 次独立试验的平均值 (avg@4)**，以减少生成随机性带来的偏差。
*   **充分性评价：** 实验设计较为合理，验证了框架在不同规模模型上的有效性及 OOD 泛化性。但实证部分主要集中在 MemAgent 这一种范式上，对于框架宣称支持的其他范式（如 Memory-R1, RMM）缺乏同等深度的对比实验。

#### 6. 主要结论与发现
*   **性能提升：** MemFactory 显著提升了基座模型的记忆管理能力。Qwen3-1.7B 的平均性能提升了 **14.8%**，Qwen3-4B-Instruct 提升了 **7.3%**。
*   **泛化能力：** 较大规模的模型（4B）在 OOD 测试集上表现出更稳健的增长，证明了 RL 微调后的记忆策略具有良好的迁移性。
*   **低门槛验证：** 证明了在有限算力（单 GPU）下，通过统一框架优化复杂的记忆策略是切实可行的。

#### 7. 优点
*   **高度模块化：** 抽象出的“原子操作”极大地方便了研究者组合不同的记忆策略。
*   **算法集成：** 原生支持 GRPO，解决了记忆增强型智能体在长上下文训练时显存溢出的痛点。
*   **开箱即用：** 预置了多种前沿记忆范式（Memory-R1, RMM 等）的实现，具有很强的实用价值。

#### 8. 不足与局限
*   **实验覆盖面：** 实证研究主要聚焦于 MemAgent 范式，未对 Memory-R1 和 RMM 进行详尽的消融实验或对比分析。
*   **训练效率：** 作者承认在训练效率方面仍有提升空间。
*   **模型规模限制：** 实验主要在较小规模模型（1.7B, 4B）上进行，尚未验证该框架在超大规模模型（如 70B+）上的扩展性表现。

（完）
