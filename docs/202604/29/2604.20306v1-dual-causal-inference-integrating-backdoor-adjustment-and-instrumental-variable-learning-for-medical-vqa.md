---
title: "Dual Causal Inference: Integrating Backdoor Adjustment and Instrumental Variable Learning for Medical VQA"
title_zh: 双重因果推理：集成后门调整与工具变量学习的医学视觉问答
authors: "Zibo Xu, Qiang Li, Ke Lu, Jin Wang, Weizhi Nie, Yuting Su"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20306v1"
tags: ["query:mmkqa"]
score: 9.0
evidence: 医疗多模态问答中的因果推理
tldr: 提出双重因果推理框架，以提高医疗视觉问答的可靠性和诊断推理能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20306v1/fig-001.webp\", \"caption\": \"TABLE I COMPARISON OF ACCURACY WITH SOTA METHODS ON THREE MEDVQA DATASETS. QUESTIONS ARE CATEGORIZED INTO OPEN AND CLOSED TYPES. “OVERALL” REFERS TO THE ACCURACY ACROSS THE ENTIRE DATASET. THE SECOND-BEST RESULTS ARE UNDERLINED.\", \"page\": 7, \"index\": 1, \"width\": 854, \"height\": 358}, {\"url\": \"assets/figures/arxiv/2604.20306v1/fig-002.webp\", \"caption\": \"Fig. 4. Hyperparameter sensitivity of the dictionary size K.\", \"page\": 8, \"index\": 2, \"width\": 507, \"height\": 294}]"
motivation: 医疗多模态问答中的因果推理。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
医学视觉问答（MedVQA）旨在根据复杂的医学图像和问题生成临床可靠的答案。然而，现有方法往往过拟合于表层的跨模态相关性，忽略了多模态医学数据中嵌入的内在偏差。因此，模型容易受到跨模态混杂效应的影响，严重阻碍了其提供可靠诊断推理的能力。为了解决这一局限性，我们提出了一种用于 MedVQA 的新型双重因果推理（DCI）框架。据我们所知，DCI 是首个集成后门调整（BDA）和工具变量（IV）学习的统一架构，旨在共同处理可观测和不可观测的混杂因素。具体而言，我们构建了一个结构因果模型（SCM），其中可观测的跨模态偏差（例如频繁的视觉和文本共现）通过 BDA 得到缓解，而不可观测的混杂因素则利用从共享潜空间学习到的 IV 进行补偿。为了保证 IV 的有效性，我们设计了互信息约束，以最大化其对融合多模态表示的依赖性，同时最小化其与不可观测混杂因素及目标答案的关联。通过这种双重机制，DCI 提取了捕捉真实因果关系的去混杂表示。在 SLAKE、SLAKE-CP、VQA-RAD 和 PathVQA 四个基准数据集上的广泛实验表明，我们的方法始终优于现有方法，特别是在分布外（OOD）泛化方面。此外，定性分析证实，DCI 通过显式解耦真实的因果效应与虚假的跨模态捷径，显著增强了跨模态推理的可解释性和鲁棒性。

## Abstract
Medical Visual Question Answering (MedVQA) aims to generate clinically reliable answers conditioned on complex medical images and questions. However, existing methods often overfit to superficial cross-modal correlations, neglecting the intrinsic biases embedded in multimodal medical data. Consequently, models become vulnerable to cross-modal confounding effects, severely hindering their ability to provide trustworthy diagnostic reasoning. To address this limitation, we propose a novel Dual Causal Inference (DCI) framework for MedVQA. To the best of our knowledge, DCI is the first unified architecture that integrates Backdoor Adjustment (BDA) and Instrumental Variable (IV) learning to jointly tackle both observable and unobserved confounders. Specifically, we formulate a Structural Causal Model (SCM) where observable cross-modal biases (e.g., frequent visual and textual co-occurrences) are mitigated via BDA, while unobserved confounders are compensated using an IV learned from a shared latent space. To guarantee the validity of the IV, we design mutual information constraints that maximize its dependence on the fused multimodal representations while minimizing its associations with the unobserved confounders and target answers. Through this dual mechanism, DCI extracts deconfounded representations that capture genuine causal relationships. Extensive experiments on four benchmark datasets, SLAKE, SLAKE-CP, VQA-RAD, and PathVQA, demonstrate that our method consistently outperforms existing approaches, particularly in out-of-distribution (OOD) generalization. Furthermore, qualitative analyses confirm that DCI significantly enhances the interpretability and robustness of cross-modal reasoning by explicitly disentangling true causal effects from spurious cross-modal shortcuts.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **DCI（Dual Causal Inference，双重因果推理）** 的框架，旨在解决医学视觉问答（MedVQA）中的数据偏差和虚假相关性问题。以下是对该论文的深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 MedVQA 模型往往依赖于医学图像与问题之间的**虚假统计相关性**（如特定的提问方式总是对应特定答案），而非真正的临床逻辑推理。
*   **研究动机**：医学数据中存在大量的**混杂因素（Confounders）**，包括：
    *   **可观测混杂因素**：如数据集中某些病理与解剖区域的频繁共现、偏向性的问题模板。
    *   **不可观测混杂因素**：如潜在的患者特征、未记录的采集设备差异或隐藏的数据集对齐间隙。
*   **目标**：通过因果推理技术，显式地消除这些混杂效应，使模型能够捕捉真实的因果关系，提高在临床场景下的鲁棒性和可解释性。

### 2. 方法论
DCI 是首个将**后门调整（BDA）**与**工具变量（IV）**学习集成到统一架构中的 MedVQA 框架。
*   **结构因果模型 (SCM)**：构建了一个包含图像(V)、问题(Q)、多模态特征(X)、答案(A)以及混杂因素(C)的因果图。
*   **后门调整 (BDA) 模块**：
    *   针对**可观测偏差**。构建了“视觉-文本联合混杂因素字典”，通过注意力机制对这些偏差进行分层和边缘化处理。
    *   利用 **NWGM（归一化加权几何平均）** 近似计算因果干预概率 $P(A|do(V,Q))$，从而切断混杂因素对输入变量的影响。
*   **工具变量 (IV) 学习模块**：
    *   针对**不可观测偏差**。从潜在空间中合成一个潜工具变量 $I$。
    *   **互信息 (MI) 约束**：设计了三个约束条件：
        1.  **相关性**：最大化 $I$ 与融合特征 $X$ 的互信息。
        2.  **无混杂性**：通过 CLUB 估计器最小化 $I$ 与不可观测混杂因素 $C$ 的关联。
        3.  **排他性约束**：确保 $I$ 仅通过 $X$ 影响答案 $A$，而非直接产生影响。
*   **训练策略**：采用多任务损失函数，结合交叉熵损失、互信息瓶颈损失和因果一致性正则化进行端到端训练。

### 3. 实验设计
*   **数据集**：
    *   **VQA-RAD**：放射学图像数据集。
    *   **SLAKE**：双语医学问答数据集。
    *   **SLAKE-CP**：专门用于评估脱偏能力的偏差敏感变体（分布外 OOD 测试）。
    *   **PathVQA**：大规模病理图像数据集。
*   **对比方法 (Benchmarks)**：对比了包括 MEVF-BAN、PubMedCLIP、M3AE、PMC-CLIP、MUMC 以及最新的因果推理方法 CIMB-MVQA 等十余种 SOTA 模型。

### 4. 资源与算力
*   **硬件**：使用了 **2 张 NVIDIA RTX 4090 GPU**。
*   **软件/优化器**：基于 PyTorch 实现，使用 AdamW 优化器，批大小为 32，学习率为 $1 \times 10^{-4}$。
*   **训练细节**：应用了线性预热（Warm-up）策略，视觉和文本编码器初始化自预训练的 PMC-CLIP。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流基准数据集上进行了全面测试，涵盖了开放式和封闭式问题。
*   **消融实验**：非常充分。分别验证了 BDA 和 IV 模块的独立贡献、不同字典构建方式的影响、以及各项互信息损失组件的必要性。
*   **超参数敏感性**：分析了混杂因素字典大小 $K$ 对性能的影响。
*   **客观性**：通过 SLAKE-CP 数据集进行了严苛的分布外（OOD）泛化测试，证明了模型并非靠记忆训练集分布获胜，实验设计公平且具有说服力。

### 6. 主要结论与发现
*   **性能领先**：DCI 在所有数据集上均刷新了 SOTA 记录。在 VQA-RAD 上达到 82.5% 的准确率，在 SLAKE 上达到 88.6%。
*   **OOD 鲁棒性**：在 SLAKE-CP 上的表现显著优于传统模型和现有的因果方法，证明了其在处理数据分布偏移时的强大能力。
*   **减少幻觉**：定性分析显示，DCI 能有效缓解“基于问题的幻觉”（即不看图瞎猜），使模型的注意力图更集中于真实的病灶区域。

### 7. 优点
*   **理论严谨**：首次结合了 BDA 和 IV 两种互补的因果技术，同时处理显性和隐性混杂因素。
*   **动态更新**：视觉字典采用动量更新机制，能随训练过程捕捉演化的多模态交互。
*   **可解释性强**：通过因果解耦，使模型推理过程更符合医学逻辑，注意力机制的指向更具临床意义。

### 8. 不足与局限
*   **字典依赖**：BDA 模块的效果在一定程度上取决于混杂因素字典的构建质量和大小（$K$ 值），这可能需要针对不同领域进行微调。
*   **计算复杂度**：引入互信息估计（如 CLUB）和双重因果模块增加了训练阶段的计算开销。
*   **应用限制**：虽然在放射和病理图像上表现良好，但在极少数极端长尾分布或全新医学模态下的表现仍有待验证。

（完）
