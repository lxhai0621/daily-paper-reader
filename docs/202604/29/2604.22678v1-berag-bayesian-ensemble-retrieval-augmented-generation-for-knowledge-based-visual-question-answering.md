---
title: "BERAG: Bayesian Ensemble Retrieval-Augmented Generation for Knowledge-based Visual Question Answering"
title_zh: BERAG：面向基于知识的视觉问答的贝叶斯集成检索增强生成
authors: "Jinghong Chen, Jingbiao Mei, Guangyu Yang, Bill Byrne"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.22678v1"
tags: ["query:mmkqa"]
score: 10.0
evidence: 用于知识库视觉问答的贝叶斯集成RAG
tldr: 针对检索增强生成（RAG）中长上下文导致的“迷失在中间”和计算开销大等问题，本文提出BERAG框架。该方法不再拼接文档，而是将语言模型分别基于单个文档进行条件化，并利用贝叶斯法则在生成过程中逐令牌更新文档后验概率作为集成权重。在知识库视觉问答任务中，BERAG显著提升了性能，实现了更好的归因、更快的解码并有效缓解了长上下文信息遗忘问题。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.22678v1/fig-001.webp\", \"caption\": \"Table 1: VQA Performance on E-VQA and Infoseek as evaluated by BEM and VQA accuracy, respectively. Systems are ranked by VQA performance. We report Recall@5 as a performance indicator of the retriever used for each system, and note that the best performance is not necessarily achieved at K = 5. ∗ denotes performance after reranking. + denotes SoTA systems. Our systems use PreFLMR-L for retrieval. To ensure comparability, we report system performance as evaluated on the M2KR setup (Lin et al., 2024) with 150K passages for E-VQA and 200K passages for Infoseek. Best results for our SFT, DPO, and BEFT systems are taken from Table 2.\", \"page\": 6, \"index\": 1, \"width\": 767, \"height\": 486}, {\"url\": \"assets/figures/arxiv/2604.22678v1/fig-002.webp\", \"caption\": \"Table 2: VQA performance of BEFT, SFT, and DPO models for different retrieval Top-K, where documents are retrieved by PreFLMR-L. E-VQA is evaluated with Bert Exact Match (BEM) and Infoseek with Exact Match (EM) following the official evaluation script. Qwen2-VL-7B-Instruct without fine-tuning and GPT-4o-mini gives low scores on Infoseek under Exact Match even when prompted to generate short answers and so we do not report them. BEFT models consistently generate better answers with longer context, making use of the higher recall rate at large K. The 32K context window for Qwen2-VL-7B-Instruct is exceeded at K > 30 on E-VQA and K > 15 on Infoseek under regular decoding. OOL denotes Out Of context Length. Bold denotes best performance at the operating Top-K.\", \"page\": 6, \"index\": 2, \"width\": 825, \"height\": 413}, {\"url\": \"assets/figures/arxiv/2604.22678v1/fig-003.webp\", \"caption\": \"Table 3: Results on SlideVQA. Evidence Selection EM (ES EM) measures evidence selection accuracy, and QA Score is measured with Exact Match (QA EM). ∗ denotes Token F1 as QA EM is not reported. We note F1 is generally higher than EM.\", \"page\": 7, \"index\": 3, \"width\": 439, \"height\": 281}, {\"url\": \"assets/figures/arxiv/2604.22678v1/fig-004.webp\", \"caption\": \"Figure 3: An example image panel from the MMNeedle benchmark for 4× 4 image panel (i.e., N = 4). Assuming that this is the first image panel in the provided context and given the query “A blue commercial airliner is parked on the airport tarmac near a terminal area.”, the model is expected to generate “1, 3, 2”.\", \"page\": 13, \"index\": 4, \"width\": 854, \"height\": 857}, {\"url\": \"assets/figures/arxiv/2604.22678v1/fig-005.webp\", \"caption\": \"Figure 1: VQA scores with different ground-truth document (GT doc) position in the retrieval list, evaluated on the same set of 256 randomly sampled E-VQA questions for which the GT doc is in the Top-20 list of PreFLMRL retrieval. GT@1-4 indicates that the GT doc is moved to the Top-4 via swapping if needed, and vice versa. The BEFT model (red) is unaffected by document ordering.\", \"page\": 8, \"index\": 5, \"width\": 461, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2604.22678v1/fig-006.webp\", \"caption\": \"Figure 2: The inference procedure for Bayesian Ensemble Retrieval Augmented Generation (BERAG). Given query x and K documents, BERAG maintains K independent branches of next-token likelihood computation, each conditioning on a document singleton zk. These branches are weighted by the document posterior, which is computed via Baye’s rule using the document prior distribution and likelihood of previously generated tokens.\", \"page\": 12, \"index\": 6, \"width\": 862, \"height\": 536}]"
motivation: 传统的RAG拼接策略会导致长上下文信息遗忘、计算成本高昂且难以准确归因文档贡献。
method: 提出BERAG框架，通过贝叶斯法则逐令牌更新单个文档的后验概率作为集成权重，并配合BEFT微调方法。
result: 在视觉问答和多模态大海捞针测试中表现优异，有效解决了“迷失在中间”问题并支持通过文档剪枝加速推理。
conclusion: BERAG为大规模文档集合的检索增强生成提供了一种高效、可解释且具备概率重排序能力的全新范式。
---

## 摘要
检索增强生成（RAG）在问答中的一种常用方法是将文档拼接成单个上下文，并将其传递给语言模型以生成答案。虽然这种策略很简单，但它可能会掩盖单个文档的贡献，导致归因困难，并引发“迷失在中间”（lost-in-the-middle）效应，即长上下文中的相关信息被忽略。拼接的扩展性也很差：计算成本随上下文长度呈二次方增长，当上下文中包含视觉数据（如视觉问答）时，这一问题尤为严重。通过限制上下文长度来缓解这些问题的尝试可能会因为阻止模型从深度检索提供的更高召回率中获益而进一步限制性能。我们提出了贝叶斯集成检索增强生成（BERAG）以及贝叶斯集成微调（BEFT），这是一种 RAG 框架，其中语言模型以单个检索到的文档为条件，而不是单个组合上下文。BERAG 将文档后验概率视为集成权重，并在生成过程中使用贝叶斯规则逐标记更新它们。这种方法实现了概率重排序、并行内存使用和清晰的文档贡献归因，使其非常适合大型文档集合。我们主要在基于知识的视觉问答任务上评估了 BERAG 和 BEFT，在这些任务中，模型必须在长且不完美的检索列表上进行推理。结果显示，与标准 RAG 相比有显著改进，包括在文档视觉问答和多模态“大海捞针”基准测试中的强劲增长。我们还证明了 BERAG 缓解了“迷失在中间”效应。文档后验可用于检测依据不足并触发拒绝回答，而文档剪枝实现了比标准 RAG 更快的解码。

## Abstract
A common approach to question answering with retrieval-augmented generation (RAG) is to concatenate documents into a single context and pass it to a language model to generate an answer. While simple, this strategy can obscure the contribution of individual documents, making attribution difficult and contributing to the ``lost-in-the-middle'' effect, where relevant information in long contexts is overlooked. Concatenation also scales poorly: computational cost grows quadratically with context length, a problem that becomes especially severe when the context includes visual data, as in visual question answering. Attempts to mitigate these issues by limiting context length can further restrict performance by preventing models from benefiting from the improved recall offered by deeper retrieval. We propose Bayesian Ensemble Retrieval-Augmented Generation (BERAG), along with Bayesian Ensemble Fine-Tuning (BEFT), as a RAG framework in which language models are conditioned on individual retrieved documents rather than a single combined context. BERAG treats document posterior probabilities as ensemble weights and updates them token by token using Bayes' rule during generation. This approach enables probabilistic re-ranking, parallel memory usage, and clear attribution of document contribution, making it well-suited for large document collections. We evaluate BERAG and BEFT primarily on knowledge-based visual question answering tasks, where models must reason over long, imperfect retrieval lists. The results show substantial improvements over standard RAG, including strong gains on Document Visual Question Answering and multimodal needle-in-a-haystack benchmarks. We also demonstrate that BERAG mitigates the ``lost-in-the-middle'' effect. The document posterior can be used to detect insufficient grounding and trigger deflection, while document pruning enables faster decoding than standard RAG.

---

## 论文详细总结（自动生成）

### BERAG: 贝叶斯集成检索增强生成论文总结

#### 1. 论文的核心问题与整体含义
本文针对传统检索增强生成（RAG）中的**拼接策略（ConcatRAG）**存在的缺陷提出了改进方案。在处理基于知识的视觉问答（KB-VQA）时，传统方法将多个检索到的文档拼接成一个超长上下文，这导致了：
*   **“迷失在中间”（Lost-in-the-Middle）效应**：模型倾向于忽略长文本中间位置的信息。
*   **计算开销巨大**：计算量随上下文长度呈二次方增长，显存占用高。
*   **归因困难**：难以量化单个文档对生成答案的具体贡献。
*   **召回率限制**：为了节省算力，通常限制检索深度（K值较小），导致模型无法利用更深层检索带来的高召回率。

#### 2. 方法论
论文提出了 **BERAG（Bayesian Ensemble RAG）** 框架及其配套的微调方法 **BEFT（Bayesian Ensemble Fine-Tuning）**。
*   **核心思想**：不再将所有文档拼接，而是让语言模型分别基于**单个文档**进行条件化生成，并利用贝叶斯法则在生成过程中动态调整各文档的权重。
*   **关键技术细节**：
    *   **贝叶斯集成推理**：在生成每一个 Token 时，通过贝叶斯公式更新文档的**后验概率**。该概率反映了在给定已生成文本的情况下，某个文档作为支撑证据的可能性。
    *   **文档先验（Document Prior）**：使用一个轻量级的 MLP 头部，基于查询和文档的嵌入向量预测初始相关性权重。
    *   **并行处理**：由于各文档是独立条件化的，可以在显存允许的情况下并行处理多个短上下文，而非一个超长上下文。
    *   **BEFT 微调**：采用端到端的监督微调，损失函数基于加权的极大似然估计，使模型学会识别并利用最相关的文档。

#### 3. 实验设计
*   **数据集/场景**：
    *   **E-VQA & Infoseek**：标准的基于百科知识的视觉问答基准。
    *   **SlideVQA**：多页文档（PPT）视觉问答，要求跨页面推理。
    *   **MMNeedle (Multimodal Needle-in-a-Haystack)**：多模态“大海捞针”实验，测试模型在大量干扰图像中定位关键信息的能力。
*   **对比方法**：
    *   **基准模型**：Qwen2-VL-7B-Instruct, GPT-4o-mini。
    *   **训练策略对比**：SFT（标准拼接微调）、DPO（直接偏好优化）。
    *   **领域 SoTA**：MuKA, EchoSight, Reflectiva 等。

#### 4. 资源与算力
*   **硬件**：实验中提到了使用 **NVIDIA A100-80G** GPU。
*   **训练时长**：
    *   MMNeedle 任务的微调（基于 LoRA）在单台 A100 上约需 **2 小时**。
    *   推理阶段，BERAG 支持在单台 A100 上进行 Top-K=50 的并行推理。
*   **模型规模**：主要基于 Qwen2-VL-7B 和 PreFLMR-L (442M) 检索器。

#### 5. 实验数量与充分性
论文进行了多维度的充分实验：
*   **扩展性实验**：测试了 K 值从 1 到 50 的性能变化，证明了 BERAG 在长上下文下的优越性。
*   **鲁棒性实验**：通过人为改变金标准文档的位置，验证了其对“迷失在中间”效应的免疫力。
*   **功能性实验**：包括拒绝回答（Deflection）机制的准确性测试和推理速度（解码延迟）分析。
*   **评价**：实验覆盖了从文本检索到多模态推理的多个维度，对比了当前最强的开源与闭源模型，结果具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **性能突破**：在 E-VQA 和 Infoseek 上显著超过了 DPO 和 SFT 基准，且在 K=50 时性能持续增长，而传统方法在 K>20 后性能开始下降。
*   **解决遗忘问题**：BERAG 的集成机制使其对文档顺序完全不敏感，彻底解决了“迷失在中间”的问题。
*   **推理加速**：通过**后验概率剪枝（Top-P Pruning）**，BERAG 在处理大量文档时的解码速度比传统拼接 RAG 更快（例如 K=50 时快约 4.5 倍）。
*   **可靠归因**：文档后验概率提供了一种天然的解释性工具，可用于检测模型是否在“瞎编”（即当所有文档后验概率均低时触发拒绝回答）。

#### 7. 优点
*   **架构优雅**：将 RAG 建模为概率集成问题，理论框架严谨且具有良好的数学解释性。
*   **显存友好**：通过并行化短上下文处理，规避了 Transformer 对长序列处理的二次方复杂度。
*   **实用性强**：支持文档剪枝加速和自动拒绝回答，非常适合部署于对可靠性要求高的实际场景。

#### 8. 不足与局限
*   **跨文档推理限制**：目前的公式主要基于单个文档的边际化（Marginalization），虽然在 SlideVQA 上表现良好，但理论上对于需要**深度融合**多个文档细微线索的极复杂推理任务，可能存在建模上限。
*   **非训练无关**：BERAG 需要通过 BEFT 进行微调才能达到最佳效果，无法像某些方法那样直接应用于任意现成的 LLM。
*   **工程实现复杂**：为了实现真正的吞吐量优势，需要对推理引擎（如 Prefill 阶段的 Query 重复编码优化）进行深度定制。

（完）
