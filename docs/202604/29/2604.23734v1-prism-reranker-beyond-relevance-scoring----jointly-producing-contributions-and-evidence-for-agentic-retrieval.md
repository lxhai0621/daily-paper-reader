---
title: "Prism-Reranker: Beyond Relevance Scoring -- Jointly Producing Contributions and Evidence for Agentic Retrieval"
authors: Dun Zhang
date: 2026-04-26
pdf: "https://arxiv.org/pdf/2604.23734v1"
tags: ["query:ma-kf"]
score: 10.0
evidence: 为智能体检索和RAG生成证据和贡献陈述的重排序模型
tldr: Prism-Reranker 是一系列基于 Qwen3.5 的重排序模型（0.8B-9B），旨在解决传统重排序仅提供相关性分数而导致 RAG 和智能体上下文冗余的问题。该模型不仅输出相关性判断，还生成贡献摘要和精简的证据片段。通过混合蒸馏与监督微调，并利用 LLM 评委重标注数据，Prism-Reranker 在 BEIR-QA 等基准测试中表现优异，显著提升了检索效率和质量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.23734v1/fig-001.webp\", \"caption\": \"Table 1: NDCG@10 (%) on 9 BEIR datasets. All rerankers operate on the same top-100 candidates from jina-embeddings-v3. Baseline numbers are taken from [32]; Avg. is computed over the 9 datasets listed here. For jina-reranker-v3, we report the random-ordering variant (R), which is its best configuration. ‡The commercial teacher API was evaluated on 7 of the 9 datasets only; NQ and HotpotQA were skipped due to query-volume cost. We compare the teacher to our students on the shared 7-dataset subset in the text below; the 9-dataset Avg. column is left blank for this row to avoid a misleading comparison. NFC = NFCorpus, SF = SciFact, SD = SCIDOCS, FQA = FiQA, TC = TREC-COVID, TCH = Touché, DBP = DBPedia, NQ = Natural Questions, HQA = HotpotQA.\", \"page\": 11, \"index\": 1, \"width\": 779, \"height\": 509}, {\"url\": \"assets/figures/arxiv/2604.23734v1/fig-002.webp\", \"caption\": \"Table 3: NDCG@10 (%) averaged within each benchmark group. All four distilled variants share the same Qwen3-Reranker-0.6B backbone, teacher, and training data; only the distillation loss differs. Best per column in bold. MTEB: 18 datasets; PosIR-zh / PosIR-en: 31 Chinese / English domain-stratified datasets each; Overall: unweighted mean over all 80 datasets.\", \"page\": 25, \"index\": 2, \"width\": 848, \"height\": 189}, {\"url\": \"assets/figures/arxiv/2604.23734v1/fig-003.webp\", \"caption\": \"Table 4: Per-dataset NDCG@10 (%) on the 18 MTEB retrieval datasets. Same settings as table 3. L, P, R denote Llist, Lpoint, Lrank; the first column is the non-distilled backbone.\", \"page\": 26, \"index\": 3, \"width\": 741, \"height\": 538}, {\"url\": \"assets/figures/arxiv/2604.23734v1/fig-004.webp\", \"caption\": \"Figure 5: Compression statistics of the evidence field on the held-out dev set (∼470 yes-labeled pairs per model, cl100k tokens). (a) Per-pair compression-ratio distribution |evidence|/|document|, clipped at 2.0 for display; the dashed line marks r = 1 (no compression). (b) Per-pair scatter of evidence length against document length; the dashed diagonal marks the 1:1 no-compression line.\", \"page\": 15, \"index\": 4, \"width\": 924, \"height\": 316}, {\"url\": \"assets/figures/arxiv/2604.23734v1/fig-005.webp\", \"caption\": \"Figure 2: Data construction pipeline. Open-source IR corpora and live web pages retrieved through Tavily and Exa—for both natural-language and keyword-style queries—are scored by a commercial rerank API (the teacher) and balanced across a 6×8 length-by-score grid. A 5-judge LLM ensemble then issues a binary positive/negative tag by 3-of-5 majority vote; positives receive structured <contribution>/<evidence> targets generated by DeepSeek-V4-Pro, while negatives carry the single token no as the SFT target. The teacher score serves as the point-wise regression target on every pair; the binary tag determines only which SFT target text is used.\", \"page\": 7, \"index\": 5, \"width\": 943, \"height\": 519}, {\"url\": \"assets/figures/arxiv/2604.23734v1/fig-006.webp\", \"caption\": \"Figure 4: Joint distribution of training pairs over teacher score y (rows, six equal-width bins) and document token length (columns, eight log-spaced bins). Before balancing, short and high-scoring documents dominate (Hnorm = 0.977, CV = 0.40). After balancing, every cell is capped, raising entropy to Hnorm = 0.996 (CV = 0.15) while retaining ∼ 85% of the original pairs.\", \"page\": 10, \"index\": 6, \"width\": 926, \"height\": 337}, {\"url\": \"assets/figures/arxiv/2604.23734v1/fig-007.webp\", \"caption\": \"Table 2: Contribution and evidence quality. LLM scores are 1–5; fidelity is [0, 1]; label and format are accuracy / score in [0, 1]. lbl = label_match, fmt = format_score, fid = entity_fidelity, c-acc = contribution_accuracy, c-cov = contribution_coverage, e-fth = evidence_faithfulness, e-sc = evidence_self_contained, e-con = evidence_concision, lang = language_consistency. †DeepSeek-V4-Flash was prompted in this run to emit only contribution and evidence (no relevance gate), so the lbl/fmt columns are not measured here.\", \"page\": 14, \"index\": 7, \"width\": 918, \"height\": 420}, {\"url\": \"assets/figures/arxiv/2604.23734v1/fig-008.webp\", \"caption\": \"Figure 3: Inter-annotator agreement among LLM judges, measured as Cohen’s κ on N=3,507 shared examples. (a) Pairwise κ over the final 5-judge panel; the largest pairwise κ is 0.82. By Landis–Koch this is still “almost perfect” agreement in absolute terms—the panel is the relatively most decorrelated 5-subset of the candidate pool, not a decorrelated panel in any absolute sense. (b) Pairwise κ over the 7-model candidate pool. (c) Independence ranking by mean off-diagonal κ (lower is more relatively independent within this pool); blue bars mark the five judges retained for the ensemble, gray bars mark the two candidates dropped because they cluster too tightly with judges already selected.\", \"page\": 9, \"index\": 8, \"width\": 981, \"height\": 380}, {\"url\": \"assets/figures/arxiv/2604.23734v1/fig-009.webp\", \"caption\": \"Figure 1: Three outputs from one forward pass. The same LM head is reused for (i) reading the calibrated relevance score s(q, d) = σ(ℓyes − ℓno) at the prompt boundary, and (ii) autoregressively generating the <contribution> and <evidence> fields when the first decoded token is yes. Irrelevant pairs stop after the single no token, so generation cost is paid only on positives.\", \"page\": 5, \"index\": 9, \"width\": 869, \"height\": 479}]"
motivation: 传统的标量相关性评分迫使智能体将整篇文档存入上下文，造成 Token 浪费和噪声干扰。
method: 采用混合目标训练，结合商业重排序器蒸馏与贡献/证据目标的监督微调，并使用 LLM 评委集成进行数据重标注。
result: "在 BEIR-QA 任务和证据质量评估中表现出色，其中 4B 版本相比基座模型在 NDCG@10 上提升了 1.54。"
conclusion: Prism-Reranker 证明了重排序器可以同时提供评分、摘要和证据提取能力，为智能体检索提供了更高效的工具。
---

## Abstract
Modern retrieval pipelines increasingly serve downstream consumers like retrieval-augmented generation (RAG) and autonomous agents that need more than a scalar relevance score. A reranker that only tells the caller "how relevant" forces the agent to dump entire documents into the language-model context, wasting tokens on tangential passages and boilerplate. We introduce Prism-Reranker, a family of reranker models built on Qwen3.5 at four sizes (0.8B, 2B, 4B, 9B) that goes beyond scalar scoring. In addition to the standard yes/no relevance judgement, whenever the verdict is yes the model emits (i) a contribution statement summarizing how the document helps the query, and (ii) an evidence passage: a self-contained rewrite that preserves every query-relevant signal while discarding noise. Prism-Reranker is trained with a hybrid objective combining point-wise distillation from a strong commercial reranker API with supervised fine-tuning on contribution and evidence targets. We curate training data from KaLM-Embedding's open-source aggregation, augmented with real web documents retrieved via commercial search APIs for open-domain queries and LLM-synthesized variants, and rewrite a portion of queries into keyword-style reformulations to adapt the model to agent-issued traffic. To reconcile inconsistent labels across open corpora and obtain crisp binary supervision, we relabel data with an LLM-as-Judge ensemble aggregating votes from five frontier LLMs. On a QA subset of BEIR and on an LLM-judged evaluation of contribution and evidence quality, Prism-Reranker attains solid results across all four sizes. We further show that the same recipe extends existing LLM-based rerankers, augmenting Qwen3-Reranker-4B with contribution and evidence capabilities while improving its average BEIR-QA NDCG@10 by +1.54 over the base model. Model weights, training recipe, and evaluation suite are released.

---

## 论文详细总结（自动生成）

以下是对论文《Prism-Reranker: Beyond Relevance Scoring — Jointly Producing Contributions and Evidence for Agentic Retrieval》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的重排序器（Reranker）仅输出一个标量相关性分数。在检索增强生成（RAG）和自主智能体（Agent）场景下，这导致了三个痛点：
    1.  **上下文浪费**：智能体被迫将整篇文档存入上下文，其中包含大量无关信息。
    2.  **噪声干扰**：网页抓取内容包含导航栏、广告等噪声，标量分数无法区分信号与噪声。
    3.  **缺乏规划信号**：智能体需要知道文档“贡献了什么”来决定下一步规划，而不仅仅是“是否相关”。
*   **整体含义**：论文提出了 **Prism-Reranker** 模型家族，旨在单次前向传播中同时输出：相关性分数、贡献陈述（Contribution）和精简证据（Evidence），从而为智能体提供更高效、可操作的检索信号。

### 2. 方法论
*   **核心思想**：将重排序任务从单纯的“评分”扩展为“评分+生成”的多任务学习，通过蒸馏商业模型的分数并结合结构化文本的监督微调（SFT）。
*   **关键技术细节**：
    *   **模型架构**：基于 Qwen3.5 骨干网络，涵盖 0.8B、2B、4B、9B 四种尺寸。
    *   **输出格式**：模型首先输出 `yes/no` 标记（用于计算分数），若为 `yes`，则继续生成 `<contribution>`（一句话摘要）和 `<evidence>`（去冗余的原文重写）。
    *   **训练目标**：混合损失函数 $L = \gamma_{point} L_{point} + \gamma_{sft} L_{sft}$。
        *   $L_{point}$：点对点蒸馏，将学生模型输出的概率与商业老师模型（Teacher API）的连续分数进行均方误差（MSE）回归。
        *   $L_{sft}$：对结构化文本目标进行交叉熵损失计算。
    *   **数据管道**：
        *   **来源**：整合 KaLM-Embedding 开源数据集、通过 Tavily/Exa 获取的真实网页文档。
        *   **查询改写**：使用 DeepSeek-V3.2 将 30% 的查询改写为关键词风格，模拟智能体流量。
        *   **LLM 评委集成**：使用 5 个顶尖 LLM（DeepSeek、Qwen、Gemini、Claude、GPT）进行投票，解决开源数据标注不一致的问题，获取高质量二元标签。
        *   **长度-分数平衡**：通过 6x8 网格采样，确保训练数据在文档长度和相关性分数上分布均匀。

### 3. 实验设计
*   **数据集/场景**：
    *   **相关性排序**：BEIR 基准测试中的 QA 子集（包含 9 个数据集，如 NFCorpus, SciFact, HotpotQA 等）。
    *   **生成质量**：自定义评估集，涵盖 9 个维度（格式、实体忠实度、贡献准确性、证据简洁度等）。
*   **Benchmark 与对比方法**：
    *   对比了 Jina-Reranker (v2/v3)、BGE-Reranker、Qwen3-Reranker 等开源模型。
    *   对比了匿名商业重排序 Teacher API。
    *   在生成质量上对比了 DeepSeek-V4-Flash。

### 4. 资源与算力
*   **GPU 型号与配置**：
    *   **0.8B、2B、4B 版本**：在单张 **NVIDIA RTX 4090 (24GB)** 上训练。
    *   **9B 版本**：在单张 **NVIDIA A800 (80GB)** 上训练。
*   **训练细节**：采用 LoRA 微调（r=64 或 32），序列长度支持达 10,240 tokens。训练时长约为 1-3 个 epoch。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 9 个 BEIR 数据集上验证了排序性能。
    *   在 80 个数据集（MTEB 和 PosIR）上进行了蒸馏损失函数的消融实验。
    *   对约 470 个测试样本进行了详尽的 LLM 维度评分。
*   **充分性与公平性**：实验设计较为充分。通过消融实验证明了点对点 MSE 蒸馏在交叉编码器（Cross-encoder）中的优越性；使用了相同的候选池（Top-100）确保对比公平。作为独立研究者的工作，其覆盖面和深度达到了较高水平。

### 6. 主要结论与发现
1.  **多任务增益**：Prism-Reranker 在提供额外生成能力的同时，保持了稳健的排序性能。
2.  **上下文压缩**：生成的证据字段中位数压缩率约为 **0.5**（即缩减了一半长度），且长度随原文增长呈亚线性增长，有效节省了 Token。
3.  **现有模型增强**：通过该方法微调 Qwen3-Reranker-4B，其 BEIR-QA 的 NDCG@10 提升了 **1.54**，证明了该配方对已有重排序器的增强作用。
4.  **无需推理链**：研究发现对于重排序任务，直接输出结果优于思维链（CoT）推理，因为推理可能导致分数极化并增加开销。

### 7. 优点
*   **接口创新**：首次提出将“贡献”和“证据”集成到重排序输出中，直接对齐智能体需求。
*   **数据质量高**：通过 5-LLM 集成标注和长度-分数平衡技术，构建了高质量的训练分布。
*   **实用性强**：模型尺寸适中（最高 9B），可在消费级 GPU 上运行，且单次前向传播完成多项任务，推理效率高。

### 8. 不足与局限
*   **排序性能差距**：相比最顶尖的商业 Teacher API，学生模型在排序指标上仍有 2.8-5.3 点的差距，这可能是由于多任务负担分散了模型容量。
*   **幻觉风险**：虽然实体忠实度高（>96%），但仅靠 SFT 无法完全杜绝幻觉，未来可能需要强化学习（RL）进一步优化。
*   **缺乏端到端验证**：论文尚未在真实的智能体下游任务（如 Open-domain QA 的最终准确率）中测量证据字段带来的端到端收益。
*   **语言覆盖**：主要集中在中文和英文，对低资源语言的覆盖和表现尚不明确。

（完）
