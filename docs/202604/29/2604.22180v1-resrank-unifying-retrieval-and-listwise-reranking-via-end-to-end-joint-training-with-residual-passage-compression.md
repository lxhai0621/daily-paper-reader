---
title: "ResRank: Unifying Retrieval and Listwise Reranking via End-to-End Joint Training with Residual Passage Compression"
title_zh: ResRank：通过残差段落压缩的端到端联合训练统一检索与列表重排序
authors: "Xiaojie Ke, Shuai Zhang, Liansheng Sun, Yongjin Wang, Hengjun Jiang, Xiangkun Liu, Cunxin Gu, Jian Xu, Guanjun Jiang"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.22180v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 统一检索和重排序以提高RAG效率和质量
tldr: ResRank是一个统一的检索与重排序框架，旨在解决基于LLM的列表重排序中存在的“信息丢失”和高推理延迟问题。该方法通过Encoder-LLM将段落压缩为单个嵌入向量，并结合残差连接与余弦相似度评分机制，实现了无需自回归生成的端到端联合训练。实验证明，ResRank在保持甚至超越现有重排序效果的同时，显著提升了推理效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.22180v1/fig-001.webp\", \"caption\": \"TABLE I: Results (nDCG@10) of reranking top-100 passages on the BEIR benchmark. Ret. denotes the first-stage retrieval model. The overall best result is in bold; the best within each block is underlined.\", \"page\": 7, \"index\": 1, \"width\": 1064, \"height\": 363}, {\"url\": \"assets/figures/arxiv/2604.22180v1/fig-002.webp\", \"caption\": \"TABLE II: Results (nDCG@10) of reranking top-100 passages on TREC DL. Ret. denotes the first-stage retrieval model. Best overall in bold; best per block underlined.\", \"page\": 7, \"index\": 2, \"width\": 396, \"height\": 394}, {\"url\": \"assets/figures/arxiv/2604.22180v1/fig-003.webp\", \"caption\": \"Fig. 2: Effectiveness vs. efficiency trade-off on TREC DL19 and Covid. ResRank achieves the highest nDCG@10 while processing only 100 tokens (one per passage) and generating zero output tokens.\", \"page\": 8, \"index\": 3, \"width\": 520, \"height\": 567}, {\"url\": \"assets/figures/arxiv/2604.22180v1/fig-004.webp\", \"caption\": \"Fig. 3: Ablation study summary. BEIR Avg. and TREC DL Avg. (mean of DL19 and DL20) for the full model and each ablation variant. Every component contributes to the final performance.\", \"page\": 8, \"index\": 4, \"width\": 486, \"height\": 333}, {\"url\": \"assets/figures/arxiv/2604.22180v1/fig-005.webp\", \"caption\": \"Fig. 1: Architecture overview of ResRank. Each passage is independently compressed into a single embedding by the Encoder-LLM and directly fed into the Reranker-LLM alongside tokenized instruction and query. The reranker contextualizes all inputs through causal attention, producing hidden states at each passage position. A residual connection combines the original encoder embedding with the reranker’s contextualized hidden state to form the fused passage embedding. The global aggregation embedding at the [EOS] position captures the holistic ranking context, and final relevance scores are computed via cosine similarity between the global aggregation embedding and each fused passage embedding, entirely eliminating autoregressive decoding.\", \"page\": 4, \"index\": 5, \"width\": 990, \"height\": 558}, {\"url\": \"assets/figures/arxiv/2604.22180v1/fig-006.webp\", \"caption\": \"TABLE V: Ablation study results (nDCG@10). Each row removes one component from the full ResRank model.\", \"page\": 9, \"index\": 6, \"width\": 1064, \"height\": 184}]"
motivation: 现有的基于LLM的列表重排序方法受限于长文本输入导致的性能下降和极高的推理延迟，难以在工业界大规模部署。
method: 提出ResRank框架，利用编码器将段落压缩为单一向量，通过残差连接融合上下文信息，并采用一步余弦相似度评分代替传统的自回归解码。
result: 在TREC和BEIR等多个基准数据集上，ResRank在仅处理每个段落一个Token且无需生成Token的情况下，达到了与现有方法相当或更优的排序效果。
conclusion: ResRank通过创新的压缩表示和端到端联合训练策略，成功在检索重排序的有效性与效率之间取得了更好的平衡。
---

## 摘要
基于大语言模型（LLM）的列表重排序（listwise reranking）已成为信息检索中实现最先进排序效果的主流范式。然而，由于需要将完整的段落文本输入 LLM，这引入了两个关键瓶颈：“迷失在中间”（lost in the middle）现象随着输入长度增加而降低排序质量，且推理延迟随序列长度呈超线性增长，使其在工业部署中不切实际。本文提出了 ResRank，这是一个从根本上解决这两个挑战的统一检索-重排序框架。受将视觉输入投影为紧凑 token 表示的多模态 LLM 启发，ResRank 采用 Encoder-LLM 将每个候选段落压缩为单个嵌入（embedding），然后将其与查询文本一起输入 Reranker-LLM 进行列表排序。为了缓解压缩表示空间与排序空间之间的失配，我们引入了一种残差连接结构，将编码器嵌入与来自重排序器的上下文隐藏状态相结合。此外，我们用一步式基于余弦相似度的评分机制取代了传统的自回归解码，从而彻底消除了生成瓶颈。ResRank 通过精心设计的双阶段、多任务、端到端联合优化策略进行训练，同时训练编码器和重排序器，在显著降低训练复杂度的同时，实现了检索与重排序之间的学习目标对齐。在 TREC Deep Learning 和八个 BEIR 基准数据集上的广泛实验表明，ResRank 与现有方法相比，在无需生成 token 且每个段落仅处理一个 token 的情况下，实现了具有竞争力或更优的排序效果，在效果与效率之间达成了根本性的更好平衡。

## Abstract
Large language model (LLM) based listwise reranking has emerged as the dominant paradigm for achieving state-of-the-art ranking effectiveness in information retrieval. However, its reliance on feeding full passage texts into the LLM introduces two critical bottlenecks: the "lost in the middle" phenomenon degrades ranking quality as input length grows, and the inference latency scales super-linearly with sequence length, rendering it impractical for industrial deployment. In this paper, we present ResRank, a unified retrieval-reranking framework that fundamentally addresses both challenges. Inspired by multimodal LLMs that project visual inputs into compact token representations, ResRank employs an Encoder-LLM to compress each candidate passage into a single embedding, which is then fed alongside the query text into a Reranker-LLM for listwise ranking. To alleviate the misalignment between the compressed representation space and the ranking space, we introduce a residual connection structure that combines encoder embeddings with contextualized hidden states from the reranker. Furthermore, we replace the conventional autoregressive decoding with a one-step cosine-similarity-based scoring mechanism, eliminating the generation bottleneck entirely. ResRank is trained through a carefully designed dual-stage, multi-task, end-to-end joint optimization strategy that simultaneously trains the encoder and reranker, achieving learning objective alignment between retrieval and reranking while substantially reducing training complexity. Extensive experiments on TREC Deep Learning and eight BEIR benchmark datasets demonstrate that ResRank achieves competitive or superior ranking effectiveness compared to existing approaches while requiring zero generated tokens and processing only one token per passage, yielding a fundamentally better balance between effectiveness and efficiency.