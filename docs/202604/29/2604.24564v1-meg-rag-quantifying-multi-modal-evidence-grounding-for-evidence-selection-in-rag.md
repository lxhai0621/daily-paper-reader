---
title: "MEG-RAG: Quantifying Multi-modal Evidence Grounding for Evidence Selection in RAG"
title_zh: MEG-RAG：用于 RAG 证据选择的多模态证据落地量化
authors: "Xihang Wang, Zihan Wang, Chengkai Huang, Quan Z. Sheng, Lina Yao"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24564v1"
tags: ["query:mmkqa"]
score: 10.0
evidence: 多模态检索增强生成与证据选择
tldr: 针对多模态检索增强生成中难以区分证据语义支持度的问题，本文提出MEG指标，通过语义确定性锚点量化证据贡献。基于此构建MEG-RAG框架，训练多模态重排序器以对齐高价值语义内容。实验证明该方法在M2RAG基准上显著提升了生成准确性与一致性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.24564v1/fig-001.webp\", \"caption\": \"Figure 2: Ablation study on Teacher Model Selection. We compare the performance (CIDEr) of MEG-RAG trained with different teacher models against the Jina Reranker baseline.\", \"page\": 5, \"index\": 1, \"width\": 506, \"height\": 257}, {\"url\": \"assets/figures/arxiv/2604.24564v1/fig-002.webp\", \"caption\": \"Table 1: Main comparison of MEG-RAG against baselines on the𝑀2𝑅𝐴𝐺 benchmark tasks across multiple MLLMs. (B-N = Bleu-N, R-N = Rouge-N, R-L = Rouge-L). Bold indicates the best performance. Naive@𝑘 directly uses the top-𝑘 retrieved documents without reranking, while Jina@𝑘 and MEG-RAG@𝑘 select the top-𝑘 documents after reranking.\", \"page\": 4, \"index\": 2, \"width\": 1056, \"height\": 688}, {\"url\": \"assets/figures/arxiv/2604.24564v1/fig-003.webp\", \"caption\": \"Figure 1: Overview of the MEG-RAG framework. (1) Dataset Construction computes MEG scores to quantify the utility of multimodal documents. (2) Reranker Training learns a multimodal reranker using the constructed MEG dataset with a hybrid objective combining document relevance classification and pairwise ranking. (3) MEG-RAG Inference retrieves candidate documents, reranks them to select the top-𝐾 results, and generates the final answer based on the selected evidence.\", \"page\": 3, \"index\": 3, \"width\": 1056, \"height\": 661}]"
motivation: 现有的多模态检索增强生成系统难以区分检索证据是提供实质语义支持还是仅具有表面相关性。
method: 提出MEG指标利用语义确定性锚点量化证据贡献，并据此训练多模态重排序器以优化证据选择。
result: 在M2RAG基准测试中，MEG-RAG显著优于现有基准模型，并在不同教师模型下表现出强鲁棒性。
conclusion: 通过基于语义接地而非令牌概率分布来优先处理高价值内容，能有效提升多模态生成的准确性。
---

## 摘要
多模态检索增强生成（MRAG）解决了多模态大语言模型（MLLM）的关键局限性，如幻觉和知识过时。然而，当前的多模态检索增强生成系统难以区分检索到的多模态数据是真正支持答案的语义核心，还是仅提供表面上的相关性。现有指标通常依赖于基于位置的启发式置信度，无法捕捉多模态实体的核心信息密度。为此，我们提出了多模态证据落地（MEG），这是一种能够量化检索证据贡献的语义感知指标。与标准的置信度度量不同，MEG 利用语义确定性锚定（Semantic Certainty Anchoring），专注于能够更好捕捉答案语义核心的高 IDF 信息承载标记。基于 MEG，我们引入了 MEG-RAG 框架，该框架训练一个多模态重排序器，使检索到的证据与真实答案的语义锚点对齐。通过基于语义落地而非标记概率分布来优先处理高价值内容，MEG-RAG 提高了生成输出的准确性和多模态一致性。在 M$^2$RAG 基准测试上的广泛实验表明，MEG-RAG 始终优于强基准模型，并在不同的教师模型中表现出鲁棒的泛化能力。

## Abstract
Multimodal Retrieval-Augmented Generation (MRAG) addresses key limitations of Multimodal Large Language Models (MLLMs), such as hallucination and outdated knowledge. However, current MRAG systems struggle to distinguish whether retrieved multimodal data truly supports the semantic core of an answer or merely provides superficial relevance. Existing metrics often rely on heuristic position-based confidence, which fails to capture the informational density of multimodal entities. To address this, we propose Multi-modal Evidence Grounding (MEG), a semantic-aware metric that quantifies the contribution of retrieved evidence. Unlike standard confidence measures, MEG utilizes Semantic Certainty Anchoring, focusing on high-IDF information-bearing tokens that better capture the semantic core of the answer. Building on MEG, we introduce MEG-RAG, a framework that trains a multimodal reranker to align retrieved evidence with the semantic anchors of the ground truth. By prioritizing high-value content based on semantic grounding rather than token probability distributions, MEG-RAG improves the accuracy and multimodal consistency of generated outputs. Extensive experiments on the M$^2$RAG benchmark show that MEG-RAG consistently outperforms strong baselines and demonstrates robust generalization across different teacher models.

---

## 论文详细总结（自动生成）

### MEG-RAG: 用于 RAG 证据选择的多模态证据落地量化 —— 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
多模态检索增强生成（MRAG）旨在通过引入外部多模态证据（文本、图像等）来缓解多模态大语言模型（MLLM）的幻觉、知识过时等问题。然而，现有的 MRAG 系统面临一个核心挑战：**难以区分检索到的证据是提供了实质性的语义支持，还是仅仅具有表面上的相关性**。
目前的重排序（Rerank）方法多依赖语义相似度或启发式的置信度分数，无法准确量化证据对生成正确答案的真实贡献。因此，研究者需要一种能够衡量“证据落地”（Evidence Grounding）程度的指标，以筛选出真正高价值的信息。

#### 2. 方法论：核心思想与关键技术
论文提出了 **MEG-RAG** 框架，其核心是通过量化证据对模型生成信心的提升程度来筛选文档。

*   **核心指标：多模态证据落地（MEG）**
    *   **定义**：MEG 衡量在给定查询 $x$ 和正确答案 $y$ 的情况下，加入文档 $d_i$ 后模型生成概率的变化。公式表示为：$MEG(d_i|x) = S_\phi(y|x, d_i) - S_\phi(y|x)$。
    *   **语义确定性锚定（Semantic Certainty Anchoring）**：为了避免“the”、“is”等无意义停用词干扰置信度计算，该方法利用 **逆文档频率（IDF）** 识别信息量大的标记（Tokens）作为锚点。只有高 IDF 的词会被纳入置信度 $S_\phi$ 的计算（取对数概率的平均值）。
*   **多任务重排序器优化**
    *   **混合损失函数**：训练一个多模态重排序器 $f_\theta$，结合了两种损失：
        1.  **分类损失（CE Loss）**：判断文档是否相关（基于 MEG 分数设定正负样本阈值）。
        2.  **排序损失（RankNet Loss）**：优化文档间的相对顺序，确保 MEG 分数高的文档排在前面。
    *   **最终目标**：$L_{hybrid} = \alpha \cdot L_{CE} + (1-\alpha) \cdot L_{RankNet}$。

#### 3. 实验设计
*   **数据集与场景**：使用 **$M^2RAG$** 综合基准，涵盖三个任务：
    1.  **多模态问答（MMQA）**：合成多模态信息回答问题。
    2.  **图像描述（Image Captioning）**：结合检索上下文生成描述。
    3.  **事实核查（Fact Verification）**：验证多模态声明的真实性。
*   **对比基准（Baselines）**：
    *   **Naive-MRAG**：直接使用检索到的 Top-K 文档。
    *   **Jina Reranker**：目前主流的多模态重排序模型。
*   **评估模型（MLLMs）**：LLaVA-NeXT、InternVL-3.5、Qwen2.5-VL 以及专门微调过的 MM-RAIT-Qwen2.5-VL。

#### 4. 资源与算力
*   **硬件环境**：使用了 **4 张 NVIDIA A100 (80GB) GPU**。
*   **训练细节**：
    *   训练时长：3 个 Epoch。
    *   优化器：AdamW，学习率 2e-5。
    *   批大小（Batch Size）：每张显卡 64。
    *   数据集规模：构建了包含 62,000 个三元组（正负样本 1:1）的平衡训练集。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 4 种不同的 MLLM 骨干模型上进行了测试，涵盖了 3 种主流多模态任务。
*   **消融实验**：
    *   验证了混合损失函数（CE + RankNet）的必要性。
    *   测试了不同“教师模型”（如 Qwen2.5-VL vs Llama-3.2-Vision）生成 MEG 信号对结果的影响。
*   **敏感性分析**：对损失权重 $\alpha$ 和 MEG 标签阈值 $\tau$ 进行了网格搜索。
*   **评价**：实验设计较为全面，不仅验证了性能提升，还通过跨任务实验证明了该重排序器具有很强的**泛化能力**（仅在 MMQA 上训练，但在其他任务上也表现优异）。

#### 6. 主要结论与发现
*   **性能领先**：MEG-RAG 在所有任务和模型上均显著优于 Jina Reranker。例如，在 MMQA 任务中，CIDEr 指标相比基准提升了约 7%~16%。
*   **跨模型鲁棒性**：重排序器的效果并不依赖于特定的“教师模型”，使用不同架构的教师模型生成的 MEG 分数训练出的学生模型效果相近。
*   **语义聚焦**：通过语义锚定过滤掉低信息量 token，能更有效地捕捉证据对事实落地的真实贡献。

#### 7. 优点（亮点）
*   **指标创新**：MEG 指标从信息论角度（类似 PMI）量化证据价值，比单纯的相似度更符合生成任务的需求。
*   **计算高效**：相比于需要计算全词表分布的 KL 散度，MEG 仅需计算正确答案锚点的概率，计算开销低。
*   **即插即用**：作为一个独立的重排序模块，可以无缝集成到任何现有的 MRAG 流程中。

#### 8. 不足与局限
*   **任务覆盖**：虽然在三个任务上表现良好，但训练数据主要源于 MMQA，对于更复杂的多模态推理（如多步推理或长视频检索）的适用性尚待验证。
*   **阈值依赖**：MEG 标签的划分依赖于人工设定的阈值 $\tau$，在不同领域的数据集上可能需要重新调整以获得最佳效果。
*   **模态偏差**：论文未深入探讨图像证据与文本证据在 MEG 计算中的权重差异，可能存在某种模态主导的情况。

（完）
