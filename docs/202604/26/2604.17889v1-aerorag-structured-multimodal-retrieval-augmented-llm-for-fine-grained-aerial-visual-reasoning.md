---
title: "AeroRAG: Structured Multimodal Retrieval-Augmented LLM for Fine-Grained Aerial Visual Reasoning"
title_zh: AeroRAG：面向细粒度航空视觉推理的结构化多模态检索增强大语言模型
authors: "Junxiao Xue, Quan Deng, Tingqi Hu, Meicong Si, Xinyi Yin, Yunyun Shi, Xuecheng Wu"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17889v1"
tags: ["query:mmkqa"]
score: 10.0
evidence: 场景图引导的多模态检索增强生成
tldr: 针对多模态大模型在航空影像中难以处理微小目标、数量及复杂关系的问题，本文提出AeroRAG框架。该框架通过将图像转化为包含类别、位置和关系的结构化场景图，并利用检索增强生成（RAG）技术提取关键语义块辅助LLM推理。实验证明，该方法在航空及通用视觉问答任务中显著优于现有模型，为细粒度视觉推理提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17889v1/fig-001.webp\", \"caption\": \"TABLE I THE RECALL AND F1-SCORES OF DIFFERENT METHODS ON THE AUG TEST SET. THE BEST RESULTS ARE HIGHLIGHTED IN BOLD. THE SECOND-BEST RESULTS ARE INDICATED WITH UNDERLINES.\", \"page\": 5, \"index\": 1, \"width\": 1040, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2604.17889v1/fig-002.webp\", \"caption\": \"TABLE II THE RECALL AND F1-SCORES OF DIFFERENT METHODS ON THE VG-150 TEST SET.\", \"page\": 5, \"index\": 2, \"width\": 1040, \"height\": 269}, {\"url\": \"assets/figures/arxiv/2604.17889v1/fig-003.webp\", \"caption\": \"Fig. 2. Overview of the enhanced multimodal RAG-LLM framework for visual question answering, comprising three core components: (1) Multimodal RAG Construction enabling structured visual encoding through knowledge graph generation, (2) Semantic-Enhanced Prompt Generation for cross-modal context alignment, and (3) LLM-Based VQA Engine integrating domain knowledge for response synthesis with visual grounding.\", \"page\": 3, \"index\": 3, \"width\": 1054, \"height\": 464}, {\"url\": \"assets/figures/arxiv/2604.17889v1/fig-004.webp\", \"caption\": \"Fig. 1. The qualitative comparison on challenging aerial VQA examples. Existing MLLMs often miss small objects or confuse counts and spatial relations, whereas our proposed multimodal RAG framework provides more accurate and visually grounded answers.\", \"page\": 1, \"index\": 4, \"width\": 512, \"height\": 337}]"
motivation: 传统多模态模型的密集视觉Token难以有效表征航空场景中的微小目标、精确数量及复杂的空间语义关系。
method: 提出一种基于场景图引导的检索增强生成框架，将图像转换为结构化知识并检索查询相关的语义片段输入LLM进行推理。
result: 在AUG航空数据集和VG-150基准上均优于六种强基线模型，特别是在密集场景和关系敏感型推理任务中提升显著。
conclusion: 结构化检索是构建面向部署且具备扎实推理能力的视觉问答系统的一种实用且有效的技术方向。
---

## 摘要
尽管多模态大语言模型（MLLMs）近期取得了进展，但在航空场景中实现可靠的视觉问答（VQA）仍然具有挑战性。在此类场景中，任务关键型证据通常由小物体、明确的数量、粗略的位置以及物体间的关系承载，而传统的密集视觉标记（visual-token）表示与这些结构化语义的对齐效果并不理想。为了解决这种接口不匹配问题，我们提出了 AeroRAG，这是一种用于视觉问答的场景图引导的多模态检索增强生成框架。该框架首先将输入图像转换为结构化视觉知识，包括物体类别、数量、空间位置和语义关系，然后检索与查询相关的语义块，为基于文本的大语言模型构建简洁的提示词。我们的方法并非依赖于对密集视觉标记的直接推理，而是在感知与语言推理之间引入了一个更明确的中间接口。在 AUG 航空数据集和通用领域 VG-150 基准测试上的实验表明，该方法相比六个强 MLLM 基线模型均有持续提升，其中在密集航空场景和关系敏感型推理中提升最为显著。我们进一步在 VQAv2 上评估了该框架，以验证所提出的接口与标准视觉推理设置保持兼容。这些结果表明，结构化检索是面向部署和落地视觉推理系统的一个实用设计方向。

## Abstract
Despite recent progress in multimodal large language models (MLLMs), reliable visual question answering in aerial scenes remains challenging. In such scenes, task-critical evidence is often carried by small objects, explicit quantities, coarse locations, and inter-object relations, whereas conventional dense visual-token representations are not well aligned with these structured semantics. To address this interface mismatch, we propose AeroRAG, a scene-graph-guided multimodal retrieval-augmented generation framework for visual question answering. The framework first converts an input image into structured visual knowledge, including object categories, quantities, spatial locations, and semantic relations, and then retrieves query-relevant semantic chunks to construct compact prompts for a text-based large language model. Rather than relying on direct reasoning over dense visual tokens, our method introduces a more explicit intermediate interface between perception and language reasoning. Experiments on the AUG aerial dataset and the general-domain VG-150 benchmark show consistent improvements over six strong MLLM baselines, with the largest gains observed in dense aerial scenes and relation-sensitive reasoning. We further evaluate the framework on VQAv2 to verify that the proposed interface remains compatible with standard visual reasoning settings. These results suggest that structured retrieval is a practical design direction for deployment-oriented and grounded visual reasoning systems.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **AeroRAG** 的新型框架，旨在解决多模态大语言模型（MLLM）在处理复杂航空影像时面临的细粒度推理难题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的多模态大模型（如 GPT-4o、LLaVA）在处理航空影像时，往往难以准确识别微小物体、统计精确数量、定位空间位置以及理解物体间的复杂关系。
*   **研究动机**：作者认为这源于“接口不匹配（Interface Mismatch）”。传统模型依赖密集的视觉 Token（Visual Tokens），而航空场景的推理更依赖稀疏但关键的结构化语义（如类别、坐标、关系）。在密集场景下，直接从原始视觉 Token 中提取这些细粒度信息非常困难，导致模型容易产生幻觉或推理错误。

### 2. 方法论
AeroRAG 提出了一个“感知-检索-推理”的三阶段框架，将视觉推理转化为结构化知识检索任务：
*   **多模态 RAG 构建模块**：
    *   使用 **Faster R-CNN** 提取物体建议框，并获取类别、数量和精确坐标。
    *   引入 **3×3 网格划分**，将精确坐标转化为易于文本描述的粗略空间位置。
    *   利用 **原型嵌入网络（PENET）** 构建场景图（Scene Graph），识别物体间的语义关系（如“在...之上”、“靠近”）。
    *   将这些信息转化为文本块（Chunks）并存入向量数据库。
*   **语义增强提示词模块**：
    *   根据用户查询（Query），通过余弦相似度从数据库中检索前 $k$ 个最相关的视觉语义块。
    *   将检索到的结构化上下文与用户问题组合成一个紧凑的文本提示词。
*   **LLM 视觉问答模块**：
    *   使用纯文本大模型（如 **Qwen-2-72B-Instruct**）基于提供的结构化证据进行推理并生成答案。

### 3. 实验设计
*   **数据集**：
    *   **AUG**：专门的航空城市影像数据集，包含 77 类物体和 63 种关系，特点是物体极小且密集。
    *   **VG-150**：通用领域的视觉基因组子集，用于验证通用性。
    *   **VQAv2**：标准视觉问答基准，用于验证与主流任务的兼容性。
*   **对比方法（Baselines）**：对比了 6 个强力模型，包括 **GPT-4o**、**Qwen-VL-Max**、**Gemini 1.5 Flash**、**InternVL2.5**、**LAMM** 和 **QvQ-72B**。
*   **评估指标**：Recall（召回率）和 F1-score（针对类别、数量、位置、关系四个维度），以及 VQA 准确率。

### 4. 资源与算力
*   **训练阶段**：使用 **2 张 NVIDIA RTX 3090 GPU** 训练场景图生成（SGG）模型，训练 20 个 Epoch。
*   **推理阶段**：部署在拥有 **8 张 NVIDIA RTX 3090 GPU** 的服务器上。
*   **模型细节**：推理后端使用了 Qwen-2-72B-Instruct。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从专用航空领域到通用领域的三个大型数据集，实验设计较为全面。
*   **消融实验**：针对检索块数量 $k$ 进行了专门研究（$k \in \{1, 2, 4, 8, 16\}$），发现 $k=4$ 时性能最优，证明了信息量与噪声之间的平衡关系。
*   **客观性**：通过与当前最顶尖的闭源（GPT-4o）和开源（InternVL2.5）模型对比，证明了其在特定细粒度任务上的优越性，实验结果具有说服力。

### 6. 主要结论与发现
*   **结构化接口的优越性**：在 AUG 航空数据集上，AeroRAG 在位置和关系推理方面的表现远超所有基线模型。
*   **密集场景优势**：在物体密集的航空场景中，将视觉信息显式化为结构化文本，比让模型直接处理视觉 Token 更有效。
*   **通用性**：即使在非航空的通用数据集（VG-150, VQAv2）上，该框架依然保持了领先或极具竞争力的性能。

### 7. 优点
*   **即插即用（Pluggable）**：无需对底层 LLM 进行昂贵的多模态预训练或微调，部署成本低。
*   **可解释性强**：检索到的结构化文本块可以作为推理依据，方便用户核实模型是否“看对”了事实。
*   **解决小目标难题**：通过专门的检测器和场景图，绕过了传统 MLLM 对低分辨率小目标感知模糊的问题。

### 8. 不足与局限
*   **感知瓶颈**：系统的性能高度依赖于前端检测器和场景图生成模型的准确性。如果检测器漏掉物体，后续 LLM 无法通过“补救”看到该物体。
*   **计算延迟**：相比于端到端的 MLLM，这种多阶段（检测-建库-检索-推理）的流水线可能会带来更高的推理延迟，影响实时性。
*   **动态场景限制**：目前主要针对静态图像，尚未探讨在动态视频或实时航空监控中的应用。

（完）
