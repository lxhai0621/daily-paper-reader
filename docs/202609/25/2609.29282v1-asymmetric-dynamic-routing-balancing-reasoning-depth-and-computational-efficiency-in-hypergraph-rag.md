---
title: "Asymmetric Dynamic Routing: Balancing Reasoning Depth and Computational Efficiency in Hypergraph RAG"
title_zh: 非对称动态路由：在超图RAG中平衡推理深度与计算效率
authors: "Qi Sun, Yijia Zhang, Xingliang Hou, Caibo Li, Qiang Li, Yu Guo"
date: 2026-09-24
pdf: "https://arxiv.org/pdf/2609.29282v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 面向RAG的意图条件化动态路由与层次知识图检索
tldr: "现有基于图或超图的RAG多采用静态遍历，忽视查询复杂度，造成简单查询算力冗余、复杂推理上下文缺口。本文提出非对称动态路由ADR，在分层知识图上用轻量结构化分类器按意图调度三类非对称拓扑遍历算子：局部事实锚定、自底向上邻接扩散、自顶向下洞见接地，实现跨层双向信息流。在五个领域语料上，ADR保持较强推理表现，并将提示词token消耗最多降低48.7%、端到端查询延迟降低45.3%。这为查询自适应的Hypergraph RAG提供了质量与效率更优的权衡方案。"
source: arxiv
selection_source: fresh_fetch
motivation: 现有结构化RAG不区分查询复杂度，统一采用静态遍历，导致简单查询计算冗余、复杂推理上下文缺口，即静态检索谬误。
method: 提出ADR，在分层知识图上用轻量分类器按意图调度局部事实锚定、自底向上扩散与自顶向下接地三类非对称遍历算子。
result: "在五个领域语料上，ADR保持较强推理表现，提示词token消耗最多降低48.7%，端到端查询延迟降低45.3%。"
conclusion: ADR为查询自适应的Hypergraph RAG提供质量与效率更优的权衡，并验证动态路由对结构化检索的有效性。
---

## 摘要
虽然基于图和基于超图的检索增强生成（RAG）显著减轻了大语言模型（LLMs）中的幻觉，但现有的基于结构的RAG系统通常无论查询复杂度如何都采用静态遍历策略。我们将这种“静态检索谬误”视为简单查询计算冗余以及复杂推理任务认知上下文缺口的主要来源。为了平衡推理质量与推理效率，我们提出非对称动态路由（ADR），一种在分层知识图谱上运行的意图条件化检索框架。ADR采用轻量级结构化分类器，在三种非对称拓扑遍历算子之间动态调度查询：局部事实锚定、自底向上邻接扩散与自顶向下洞察落地，它们共同实现跨分层知识层的双向信息流动。在五个领域特定语料库上进行的大量实证评估表明，ADR在保持强推理性能的同时，将提示词元消耗最多降低48.7%，并将端到端查询延迟降低45.3%，为查询自适应的超图RAG带来了有利的质量—效率权衡。

## Abstract
While graph-based and hypergraph-based Retrieval-Augmented Generation (RAG) significantly mitigate hallucinations in Large Language Models (LLMs), existing structure-based RAG systems typically adopt static traversal strategies regardless of the query complexity. We identify this ``static retrieval fallacy'' as a primary source of computational redundancy for simple queries and cognitive context gaps for complex reasoning tasks. To balance reasoning quality and inference efficiency, we propose Asymmetric Dynamic Routing (ADR), an intent-conditioned retrieval framework operating over hierarchical knowledge graphs. ADR employs a lightweight structured classifier to dynamically dispatch queries among three asymmetric topological traversal operators: localized fact anchoring, bottom-up adjacency diffusion, and top-down insight grounding, which collectively enable bidirectional information flow across hierarchical knowledge layers. Extensive empirical evaluations across five domain-specific corpora demonstrate that ADR maintains strong reasoning performance while reducing prompt token consumption by up to 48.7\% and end-to-end query latency by 45.3\%, yielding a favorable quality--efficiency trade-off for query-adaptive Hypergraph RAG.

---

## 论文详细总结（自动生成）

# 论文总结：非对称动态路由（ADR）——在超图 RAG 中平衡推理深度与计算效率

## 1. 核心问题与整体含义

- **研究动机**：图/超图 RAG 通过结构化知识表示缓解 LLM 幻觉，但现有结构型 RAG 通常对子图扩展半径、遍历深度采用统一静态配置，不区分查询复杂度。
- **核心问题**：作者将这种缺陷定义为“静态检索谬误”（Static Retrieval Fallacy），并指出它导致两类相反问题：
  - **过度检索造成上下文污染**：简单事实查询被不必要多跳扩散，引入语义无关信息，降低信噪比、增加 prefill 开销，并可能诱发联想幻觉。
  - **检索不足造成认知缺口**：抽象、跨文档复杂查询需要宏观综合，但局部拓扑扩展难以桥接不同抽象层，导致证据碎片化和推理浅层化。
- **整体含义**：论文提出 ADR，在分层知识图/超图上进行意图条件化的动态路由，用单遍检索替代多轮 agentic 循环，以同时提升复杂推理覆盖并降低简单查询冗余。其意义在于为查询自适应的 Hypergraph RAG 提供质量—效率更优的权衡。

## 2. 方法论

### 2.1 核心思想

- ADR 根据查询意图/复杂度，动态选择检索入口层、遍历方向和扩展范围。
- 它运行在双超图知识框架 \(G=(H_K,H_D)\) 上：
  - \(H_K\)：事实超图，保存细粒度实体与事实关系，包括成对边和高阶事实超边。
  - \(H_D\)：深层洞见超图，保存高层抽象结构及其语义关联。
  - 高阶事实超边通过映射 \(\pi\) 关联到 \(H_D\) 中的洞见顶点；反向映射 \(\pi^{-1}\) 用于将高层洞见落地到事实证据。
- 三类非对称遍历算子共同实现跨层双向信息流：局部事实锚定、自底向上邻接扩散、自顶向下洞见接地。

### 2.2 意图路由与保守回退

- 使用 LLM 意图分类器 \(f_\phi\) 将查询分为三类：
  - **简单**：直接事实、定义、实体关系，适合局部事实检索。
  - **中等**：需要多事实关系或程序性推理，需要局部证据加更广上下文关联。
  - **复杂**：需要主题综合、跨域比较或因果推理，需要抽象语义结构与事实证据支撑。
- 分类器输出类别 \(y\) 和置信度 \(c_y\)。
- **保守回退原则**：若 \(c_y \ge \gamma\)，接受预测；否则统一回退到“中等”轨迹。默认 \(\gamma=0.75\)。中等轨迹提供中间检索范围，降低不确定查询被路由到极端轨迹的风险。

### 2.3 三类非对称遍历算子

- **算子 1：局部事实锚定（简单查询）**
  - 在 \(H_K\) 中选取与查询嵌入余弦相似度最高的 \(k_e\) 个事实实体作为锚点。
  - 检索与这些锚点直接关联的事实边，形成 \(E_{sub}\)。
  - 检索在 \(E_{sub}\) 终止，不做邻域扩展或跨层遍历，近似零跳检索。

- **算子 2：自底向上邻接扩散（中等查询）**
  - 对 \(E_{sub}\) 中 \(|e|\ge 3\) 的高阶事实超边，通过 \(\pi\) 映射到 \(H_D\) 中的洞见顶点集合 \(U_{seed}\)。
  - 从 \(U_{seed}\) 出发，在 \(H_D\) 中做一次邻接扩展，得到相关深层洞见边 \(E_{up}\)。
  - 最终将 \(E_{sub}\) 的细粒度事实内容与 \(E_{up}\) 的综合洞见共同作为上下文。

- **算子 3：自顶向下洞见接地（复杂查询）**
  - 从 \(H_D\)
