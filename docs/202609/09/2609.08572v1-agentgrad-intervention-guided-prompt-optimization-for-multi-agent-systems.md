---
title: "AgentGrad: Intervention-guided Prompt Optimization for Multi Agent Systems"
title_zh: AgentGrad：面向多智能体系统的干预引导提示优化
authors: "Jaewon Chu, Jinwoo Seo, Jaewon Cho, Jeehye Na, Yunyang Xiong, Youngdae Kim, Hyunwoo J. Kim"
date: 2026-09-08
pdf: "https://arxiv.org/pdf/2609.08572v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 利用干预引导文本梯度优化多智能体系统的提示词，改进梯度提取与聚合过程
tldr: "针对多智能体系统的提示优化，文本梯度方法虽为主流，但梯度提取时不验证目标修改能否修复失败，且缺乏智能体级监督；聚合时随机拼接，易混入无关失败模式。AgentGrad通过顺序干预逐一修改智能体行为，定位能修复失败的关键智能体，并以其修正输出提供细粒度监督；再将语义相似梯度聚类并抽象为广义梯度。在五个MAS基准上取得最优性能，平均优化时间较次快基线降低60%，为多智能体自动提示优化确立了更高效、可泛化的范式。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.08572v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1439, \"height\": 596, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.08572v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 702, \"height\": 834, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.08572v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1436, \"height\": 343, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.08572v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 583, \"height\": 371, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.08572v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1432, \"height\": 1058, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.08572v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1429, \"height\": 278, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.08572v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1427, \"height\": 280, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.08572v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 736, \"height\": 279, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.08572v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1240, \"height\": 295, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.08572v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1451, \"height\": 314, \"label\": \"Table\"}]"
motivation: 现有文本梯度方法提取时未验证目标修改能否修复失败且缺乏智能体级监督；聚合时随机混合无关模式，导致提示更新低效且难以泛化。
method: AgentGrad逐个干预智能体，定位能修复失败的目标智能体，以其修正输出为监督提取细粒度梯度，再对语义相似梯度聚类并抽象为广义梯度。
result: "在五个MAS基准上取得最优性能，平均优化时间较次快基线降低60%。"
conclusion: AgentGrad以顺序干预与语义抽象提升提示优化精度和效率，为多智能体系统提示自动调优提供了新方案。
---

## 摘要
基于大型语言模型（LLM）的多智能体系统（MAS）通过采用专门的多个智能体实现了强大的性能，但其性能取决于每个智能体的提示设计。对于MAS提示优化，使用自然语言反馈指导提示更新的文本梯度方法已成为主流范式。在本文中，我们识别了现有文本梯度方法在梯度提取和梯度聚合两个阶段的局限性。在梯度提取中，先前的工作选择目标提示时未验证修改它是否能解决失败，并且在没有对应智能体中间输出的智能体级监督的情况下推导梯度。在梯度聚合中，个体梯度被随机分组并拼接，常常混合不相关的失败模式，产生无法泛化的提示。为解决这些局限性，我们提出了**AgentGrad**，一个基于顺序干预和语义文本梯度抽象的多智能体系统提示优化框架。对于每次失败，顺序干预一次只修改一个智能体的行为，以识别其修改能解决失败的目标智能体。目标智能体的修改后输出随后作为智能体级监督，用于提取细粒度梯度。语义文本梯度抽象将语义相似的梯度聚类，以防止混合不相关的失败模式，并将每个聚类抽象为捕获共享修正模式的广义梯度。实验结果表明，AgentGrad在五个MAS基准上实现了最先进的性能，并且与次快基线相比，平均将墙钟优化时间减少了$2.5\times$。

## Abstract
Large language model (LLM)-based multi-agent systems (MAS) achieve strong performance by employing specialized multiple agents, yet their performance depends on the prompt design of each agent. For MAS prompt optimization, textual gradient methods that guide prompt updates using natural-language feedback have emerged as a leading paradigm. In this paper, we identify limitations in two stages of existing textual gradient approaches: gradient extraction and gradient aggregation. In gradient extraction, previous works select a target prompt without verifying whether modifying it resolves the failure, and derive gradients without agent-level supervision over the corresponding agent's intermediate output. In gradient aggregation, individual gradients are randomly grouped and concatenated, often mixing unrelated failure modes and producing prompts that fail to generalize. To address these limitations, we propose \textbf{AgentGrad}, a prompt optimization framework for multi-agent systems based on sequential intervention and semantic textual gradient abstraction. For each failure, sequential intervention modifies the behavior of one agent at a time to identify the target agent whose modification resolves the failure. The modified output of the target agent then serves as agent-level supervision for extracting a fine-grained gradient. Semantic textual gradient abstraction clusters semantically similar gradients to prevent mixing unrelated failure modes, and abstracts each cluster into a generalized gradient that captures the shared corrective pattern. Experimental results show that AgentGrad achieves state-of-the-art performance across five MAS benchmarks and reduces wall-clock optimization time by $2.5\times$ on average compared to the next-fastest baseline.

---

## 论文详细总结（自动生成）

# AgentGrad：面向多智能体系统的干预引导提示优化 — 论文总结

## 1. 论文的核心问题与整体含义

- **背景与动机**：LLM 驱动的多智能体系统（MAS）通过多个专业智能体协作来处理复杂任务，其整体性能高度依赖每个智能体的提示词（prompt）设计。自动提示优化（Automatic Prompt Optimization, APO）因此成为提升 MAS 性能的关键方向，其中"文本梯度法"（textual gradient）——用自然语言反馈指导提示词更新——已成为主流范式。
- **核心问题**：论文指出现有文本梯度方法存在两大阶段性的系统缺陷：
  - **梯度提取阶段**：
    - **目标选择缺乏验证**：未验证修改某个提示词是否真的能修复对应的失败（failure），导致"归因不当"；
    - **缺乏智能体级监督**：只使用系统级输出与真实标签的对比，无法获知哪个中间智能体的输出需要修正，梯度信号粗糙。
  - **梯度聚合阶段**：将样本级梯度随机分组并直接拼接，可能混合互不相关的失败模式，使提示更新方向不连贯、泛化能力差。
- **整体含义**：AgentGrad希望通过"顺序干预"（sequential intervention）定位真正导致失败的目标智能体，并借其"干预修正后的输出"作为智能体级伪标签来提取细粒度梯度；再通过"语义文本梯度抽象"（semantic textual gradient abstraction）将相似梯度的共同纠正模式提炼成有泛化力的统一更新方向。该方法面向实际部署中的性能与效率双重挑战。

## 2. 论文提出的方法论

- **总体框架**：AgentGrad 将提示优化建模为一个有预算（rollout 次数）约束下的搜索问题，并采用"梯度提取—梯度聚合—提示更新—验证"的迭代流程。

- **关键组件一：顺序干预（Sequential Intervention）**
  - 对每个失败样本，按**逆执行顺序**逐个智能体注入提示（hint），观察单独修正该智能体后整个系统能否产出正确结果。
  - 定义目标智能体为：仅修改它即可在干预下解决该失败的那个智能体。
  - 干预通过向当前提示中追加 hint 实现，hint 根据真实标签或约束构造，仅训练时使用，推理时不含 hint。
  - 逆序处理的依据是：失败通常更集中于较后的智能体，因此可减少平均干预次数。
  - 对无法被任何单智能体 hint 修复的失败，本轮跳过，下轮重新评估。

- **关键组件二：智能体级监督与梯度提取**
  - 对归因到智能体 $n$ 的失败样本，记录其在同一输入下与原始失败输出相同上下文中的活动：
    - 原始输出：$\hat{y}_n^i = \pi_n(x_n^i; p_n)$
    - 干预后输出：$\tilde{y}_n^i = \pi_n(x_n^i; p_n, H)$
  - 将 $\tilde{y}_n^i$ 视为伪标签，通过 LLM 梯度提取器生成样本级文本梯度：
    - $\delta_n^i = \mathrm{LLM}_{\nabla}(p_n, x_n^i, \hat{y}_n^i, \tilde{y}_n^i)$
  - 该梯度描述"如何修改 $p_n$ 才能令其输出 $\tilde{y}_n^i$"（而非笼统地"让系统输出正确结果"），实现了直接、精细的智能体级监督。

- **关键组件三：语义文本梯度抽象**
  - 对每个智能体的所有样本级梯度 $\Omega_n$，用一个 LLM 聚合器同时完成：
    - **语义聚类**：将修正方向相近的梯度归入同一语义 minibatch $D_n^j$；
    - **抽象**：将每个簇抽象成一条广义梯度 $\bar{\delta}_n^j$，捕获簇内共有的校正模式。
  - 簇大小下界采用周期性调度（如 5→3→1→5…），在粗粒度和细粒度修正之间切换，以兼顾广谱性与特异性。

- **提示更新与验证**
  - 按簇大小降序（先大后小）用 LLM 提示优化器生成候选新提示 $p_n^{\text{new}}$。
  - 候选提示先在语义 minibatch $D_n^j$ 上验证，若改善则再到验证集 $D_{\text{val}}$ 上验证，两层均通过才会被接受更新。

- **算法流程**：每一轮优化中从训练集取失败样本 → 逆序干预以划分目标智能体及失败子集 → 为每个目标智能体提取样本级梯度 → 语义抽象成广义梯度 → 分层验证并更新提示词，重复直至 rollout 预算耗尽。

## 3. 实验设计

- **数据集 / 场景（共 5 个 MAS 基准）**：
  - **HotpotQA**：多跳问答
  - **HoVer**：多跳事实核查（claim verification）
  - **IFBench**：指令跟随
  - **PUPA**：隐私保护的委派（涉及敏感信息脱敏）
  - **MATH**：数学推理
- **多智能体结构与数据来源**：前四个采用 GEPA[10] 公开的多智能体系统设置与数据划分；MATH 采用 MACM[6] 的多智能体设置。

- **模型与 backbone**：
  - 闭源模型：GPT-5-mini
  - 开源模型：Qwen3-8B
  - 注意：task LLM 和所有优化器组件均使用相同的 backbone，避免额外偏差。

- **对比方法**：
  - **Baseline（No PO）**：不做提示优化。
  - **MIPROv2**：多阶段 LM 程序的指令与示例联合优化（贝叶斯优化）。
  - **TextGrad**：基于自然语言"反传"的文本梯度方法。
  - **GEPA**：结合轨迹级反思与进化搜索的提示优化方法。

- **迁移实验**：将优化后的提示词直接用于同领域未见过的基准：
  - HotpotQA → 2WikiMultiHopQA
  - HoVer → EX-FEVER
  - PUPA → PUPA-TNB
  - IFBench → IFEval
  - MATH → OlympiadBench

## 4. 资源与算力

- 文中**未明确说明 GPU 型号、数量、训练时长**等硬件资源信息。
- 唯一与算力相关的量化指标是**墙钟优化时间（wall-clock time）**：AgentGrad 在五个基准上都耗时最少，平均 136 分钟，较次快基线 GEPA（平均 337 分钟）快约 2.5 倍（基准间速度提升 1.4×~3.2×）；相比 TextGrad（平均 647 分钟）快 4.7 倍。
- 实验所用模型虽有 GPT-5-mini 与 Qwen3-8B，但未报告具体使用的 GPU 环境。

## 5. 实验数量与充分性

- **实验组数**：
  - 主结果实验：2 种 backbone × 5 个基准 = 10 张指标表；
  - 消融实验：2 个基准（HotpotQA, PUPA）上逐级增加 TI（目标识别）、AS（智能体级监督）、STGA（语义抽象），共 5 组配置；
  - 优化轨迹实验：2 个基准上的验证曲线；
  - 转移性实验：5 组跨目标基准的零样本迁移；
  - 效率对比实验：5 个基准的墙钟时间；
  - 质量控制分析：minibatch/validation 改进率对比与消融（图 4）。
  - 另有定性案例分析（PUPA 脱敏场景）。
- **充分性与客观性评估**：
  - **充分**：覆盖了多任务类型、两大模型族（开源/闭源）、多角度消融、优化轨迹和迁移性，验证较全面；性能指标均报告三个随机种子的均值±标准误，主结果表中 AgentGrad 在多数基准上性能领先且方差较小。
  - **客观**：与近期代表性基线在同一多智能体系统和数据划分下对比，并让优化器与任务使用相同 backbone，降低不公平因素。
  - **仍需注意**：消融实验只覆盖了两个基准（HotpotQA、PUPA），且未给出统计显著性检验；优化时间对比主要基于单次/较少次重复，未见标准误差。因此，虽然实验相当充分，但在统计严谨性上仍有提升空间。

## 6. 主要结论与发现

- AgentGrad 在五个 MAS 基准上的 GPT-5-mini 和 Qwen3-8B 设置均超过所有基线，达到 SOTA；相对 no-PO 基线的平均提升：
  - GPT-5-mini：+11.76
  - Qwen3-8B：+9.67
- 在 HotpotQA 上优势尤为明显（GPT-5-mini 下 73.89 vs GEPA 68.33），优化曲线的早期收敛速度也大幅领先：约 1000 次 rollout 达到约 70% 性能，而 GEPA 需约 6000 次。
- 训练效率显著：AgentGrad 是最快的方法，平均较次快基线提速约 2.5 倍，且没有牺牲性能——效率与性能同步提升。
- 优化后的提示词迁移到未见过的同领域基准时，AgentGrad 仍保持最好成绩，说明其学习到的更新方向具有一定泛化能力而非过度拟合训练集。
- 消融表明：目标识别（TI）、智能体级监督（AS）、语义抽象（STGA）三个组件均有正面贡献，且分工不同——TI 和 AS 提升单样本梯度质量（提高 minibatch 改进比），STGA 提升更新泛化能力（提高验证改进比）。
- 定性示例表明，AgentGrad 能将分散的样本级具体错误（如漏掉机构名、城市名、看似虚构但仍敏感的姓名）抽象为更通用的脱敏策略，从而更新相应智能体提示。

## 7. 优点

- **问题剖析精准**：明确提出文本梯度方法在"提取"和"聚合"两个环节的缺陷，并分别设计针对性解决方案，逻辑清晰。
- **方法论创新**：
  - "顺序干预 + 目标归因"把失败分析工具引入提示优化，实现了可验证的智能体级定位；
  - 用"干预后的输出"作为伪标签，不依赖额外的显式损失函数，提供了直接的智能体级监督信号；
  - 语义聚类与抽象把零散且可能互相冲突的样本级更新信号合并为语义连贯的广义梯度，在提高泛化性的同时也降低了优化模块调用次数。
- **工程实用性强**：在保持 SOTA 精度的同时大幅压缩墙钟时间（2.5× 提速）；只修改训练流程，推理时不加任何 hint，不影响部署链路。
- **实验设计较完善**：跨 5 类任务、2 种模型、多种评估维度（主结果、消融、轨迹、迁移、效率、质性分析），并给出统计误差。
- **迁移测试是一个加分项**，反映了优化结果在真实场景中的可用性，区别于许多只报告域内指标的论文。

## 8. 不足与局限

- **实验覆盖的局限**：
  - 消融只在 HotpotQA 与 PUPA 上做，未覆盖 MATH、HoVer、IFBench，不能充分证明三组件贡献在所有任务类型中的普遍性；
  - 转移性测试同域但未涉及更大跨度的域转移（如从问答迁移到数学或指令跟随）。
- **统计严谨性**：
  - 主结果只报告均值±标准误，未进行显著性检验（如配对 t-test 或置信区间），因此某些差距较小（如 HoVer 上 AgentGrad 与 TextGrad/GEPA 之差）的稳健性存疑；
  - 墙钟时间的对比缺少多次重复与方差报告，速度结论的稳健性有待更强验证。
- **依赖假设**：
  - 顺序干预假设"单个智能体的独立修正足以修复失败"，而对那些需要多智能体协同修正的问题，本方法会将其视为不可解决案例越过，可能导致部分深层次问题得不到处理；
  - 干预 hint 的构造依赖任务知识（如真实标签、约束描述），不同任务需要专业知识来设计，自动化程度有限。
- **局限性其他方面**：
  - LLM 聚合器同时执行聚类与抽象，聚类数由模型自行决定，存在不稳定或不可控风险；
  - 软性簇大小下限的周期调度看似有价值，但没有单独消融验证该调度的必要性；
  - 论文未讨论计算资源细节，使复现与横向比较的难度略有上升；
  - 某些表结果自相矛盾或异常需要解释：如 TextGrad 在 MATH/GPT-5-mini 上表现与 no-PO 完全相同（76.48），而在 Qwen3-8B 上 GEPA 在 IFBench 上甚至低于 baseline，提示这些方法在部分设置下可能"更新失败"或"退化"——论文未分析原因。

**（完）**
