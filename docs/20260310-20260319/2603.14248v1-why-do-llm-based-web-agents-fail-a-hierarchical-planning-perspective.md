---
title: Why Do LLM-based Web Agents Fail? A Hierarchical Planning Perspective
title_zh: 基于大语言模型（LLM）的网络智能体为何失败？分层规划视角
authors: "Mohamed Aghzal, Gregory J. Stein, Ziyu Yao"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14248v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 分析网络智能体推理和落地的分层规划框架
tldr: 本研究针对 LLM 网络智能体在复杂长程任务中可靠性不足的问题，提出了一个分层规划框架，从高层规划、底层执行和重新规划三个维度对失败原因进行深入分析。通过对比 PDDL 和自然语言规划，研究发现虽然结构化规划能产生更优策略，但底层执行阶段的感知接地和自适应控制才是制约性能的核心瓶颈。该框架为诊断和改进网络智能体提供了系统性的方法论。
selection_source: fresh_fetch
motivation: 现有的评估方法主要关注端到端的成功率，无法深入揭示 LLM 网络智能体在处理复杂任务时具体的失败环节。
method: 提出一种分层规划框架，将智能体行为分解为高层规划、底层执行和重新规划，并对比了 PDDL 与自然语言规划的表现。
result: 实验表明 PDDL 规划在策略制定上优于自然语言，但底层执行阶段的感知与控制能力是导致任务失败的主要瓶颈。
conclusion: 要实现人类水平的可靠性，网络智能体不仅需要强大的高层推理能力，更需在感知接地和自适应控制方面取得突破。
---

## 摘要
基于大语言模型（LLM）的网络智能体越来越多地被用于网页导航，但在现实的长程任务中，其可靠性仍远低于人类。现有的评估主要关注端到端的成功率，对于失败发生的原因提供的见解有限。我们提出了一个分层规划框架，从三个层面（即高层规划、底层执行和重新规划）分析网络智能体，从而实现对推理、接地（grounding）和恢复能力的基于过程的评估。我们的实验表明，结构化的规划领域定义语言（PDDL）规划比自然语言（NL）规划能产生更简洁且目标导向的策略，但底层执行仍然是主要的瓶颈。这些结果表明，要达到人类水平的可靠性，不仅需要提高高层推理能力，改进感知接地和自适应控制也至关重要。这种分层视角为诊断和推进 LLM 网络智能体提供了原则性的基础。

## Abstract
Large language model (LLM) web agents are increasingly used for web navigation but remain far from human reliability on realistic, long-horizon tasks. Existing evaluations focus primarily on end-to-end success, offering limited insight into where failures arise. We propose a hierarchical planning framework to analyze web agents across three layers (i.e., high-level planning, low-level execution, and replanning), enabling process-based evaluation of reasoning, grounding, and recovery. Our experiments show that structured Planning Domain Definition Language (PDDL) plans produce more concise and goal-directed strategies than natural language (NL) plans, but low-level execution remains the dominant bottleneck. These results indicate that improving perceptual grounding and adaptive control, not only high-level reasoning, is critical for achieving human-level reliability. This hierarchical perspective provides a principled foundation for diagnosing and advancing LLM web agents.

---

## 论文详细总结（自动生成）

### 基于大语言模型（LLM）的网络智能体为何失败？分层规划视角

#### 1. 论文的核心问题与整体含义
尽管基于 LLM 的网络智能体在自动化网页导航方面取得了进展，但在处理现实、长程（long-horizon）任务时，其可靠性仍远低于人类。现有的评估方法主要依赖端到端的成功率，这像是一个“黑盒”，无法揭示失败的具体根源：是任务理解错了？高层策略不对？还是底层 UI 操作无法落地？
本研究旨在通过**分层规划（Hierarchical Planning）**的视角，将智能体的行为拆解为高层规划、底层执行和重新规划三个阶段，从而系统性地诊断 LLM 网络智能体的失败原因。

#### 2. 论文提出的方法论
论文提出了一个三层评估框架，核心思想是将复杂的网页任务结构化：
*   **高层规划（High-level Planning）：** 将自然语言指令分解为一系列抽象子目标（Subgoals）。研究对比了两种表示方式：
    *   **自然语言（NL）：** 自由文本描述步骤。
    *   **PDDL（规划领域定义语言）：** 使用结构化的谓词、前提条件和效果来定义动作。
*   **底层执行（Low-level Execution）：** 将每个子目标转化为具体的网页交互序列（如点击、输入、滚动）。
*   **后置条件检查（Postcondition Checking）：** 引入“LLM 作为裁判（LLM-as-Judge）”机制，在每个子目标完成后验证环境状态是否符合预期，作为反馈信号。
*   **重新规划（Replanning）：** 当子目标执行失败时，智能体需决定是局部调整还是全局重排。

#### 3. 实验设计
*   **数据集与场景：** 使用了 **Mind2Web-Live** 基准测试，包含 104 个在真实、动态网站上执行的任务。
*   **模型：** 评估了三个主流模型：**gpt-5-nano**（主要模型）、**claude-haiku-4.5** 和 **gemini-flash-2.5**。
*   **动作空间设置：**
    *   **扩展空间（Expanded）：** 包含 `google_search`、`goto` 等抽象动作。
    *   **原始空间（Primitive）：** 仅限底层的 `click`、`type` 等，分为“动作对象预测”和“动作 ID 选择”两种模式。
*   **对比维度：** 比较了人类专家编写的计划与 LLM 生成的计划（NL vs. PDDL）在对齐度、执行成功率和效率上的差异。

#### 4. 资源与算力
论文**未明确说明**具体的硬件算力（如 GPU 型号或数量）。由于实验主要基于 OpenAI、Anthropic 和 Google 的 API 进行推理，研究重点在于模型输出的逻辑分析而非模型训练，因此不涉及大规模本地集群的训练时长。

#### 5. 实验数量与充分性
*   **实验规模：** 针对 104 个真实网页任务进行了多轮测试，涵盖了 3 种模型、2 种高层表示法和 3 种动作空间配置。
*   **充分性：** 实验设计较为全面，不仅有端到端成功率，还细化到了子目标完成率、计划对齐度（Perfect Match, Partial, Missing 等）以及错误模式分析（如重复、幻觉链接、冗余动作）。
*   **客观性：** 为了验证“LLM 作为裁判”的可靠性，作者对 50 个样本进行了人工校验，结果显示人工与 LLM 的一致性在 82%-86% 之间，证明了自动化评估的有效性。

#### 6. 论文的主要结论与发现
*   **PDDL 优于自然语言：** 使用 PDDL 结构化表示的高层计划比 NL 计划更简洁、目标导向，且与人类专家计划的对齐度更高。
*   **底层执行是核心瓶颈：** 即使给智能体提供完美的人类高层计划，其底层执行的成功率依然很低（gpt-5-nano 仅为 36.4%）。失败主因是**感知落地（Perceptual Grounding）**不足，如无法识别 UI 元素、产生幻觉链接或陷入无效的重复操作。
*   **重新规划显著提升性能：** 仅需一轮基于环境反馈的重新规划，就能大幅提高子目标完成率和最终任务成功率。
*   **模型差异：** gpt-5-nano 表现最优；gemini-flash-2.5 在底层执行上最弱，容易产生冗余动作。

#### 7. 优点
*   **诊断性强：** 突破了单一的成功率指标，提供了“过程评估”的方法论，能精准定位智能体是在哪个环节“掉链子”。
*   **引入形式化方法：** 首次将 PDDL 这种经典的符号规划语言引入 LLM 网络智能体的评估，证明了结构化约束对推理的帮助。
*   **细粒度错误分析：** 对底层执行的失败模式（如 Out-of-domain 导航、Redundant actions）进行了量化分类，具有很高的工程参考价值。

#### 8. 不足与局限
*   **标注依赖：** 高层计划的对齐度评估依赖于人工标注的参考计划，这限制了其在大规模无标注任务上的扩展性。
*   **多模态缺失：** 实验主要基于文本 DOM 表示，未充分探讨视觉信息（如截图）在解决感知落地瓶颈中的潜力。
*   **模型规模限制：** 实验主要集中在各家的“轻量级”模型（nano/haiku/flash），未对比超大规模模型（如 GPT-4o 或 Claude 3.5 Sonnet）在相同框架下的表现。
*   **重新规划深度：** 实验仅限制在一轮重新规划，未探讨多轮迭代或更复杂的恢复策略。

（完）
