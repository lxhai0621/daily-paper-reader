---
title: AI Planning Framework for LLM-Based Web Agents
title_zh: 基于大语言模型（LLM）的 Web 智能体 AI 规划框架
authors: "Orit Shahnovsky, Rotem Dror"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12710v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 将智能体架构映射到传统规划范式以处理Web任务
tldr: 本研究针对基于大语言模型（LLM）的网页智能体在任务规划中缺乏透明度的问题，提出了一种将网页任务视为序列决策过程的AI规划框架。通过将现代智能体架构映射到传统规划范式（如BFS、DFS），并引入五项评估轨迹质量的新指标及包含794条人工标注轨迹的数据集，为诊断系统故障和优化架构选择提供了理论支持与实证依据。
selection_source: fresh_fetch
motivation: 现有的LLM网页智能体运作机制如同黑盒，导致难以诊断其规划失败的具体原因或理解其复杂的决策逻辑。
method: 将网页任务形式化为序列决策过程，并将不同智能体架构映射到传统规划算法，同时提出五种新的评估指标和人工标注数据集。
result: 实验表明逐步执行智能体在整体成功率上更接近人类水平，而预先全量规划智能体在元素准确率等技术指标上表现更优。
conclusion: 该框架和多维度评估指标证明了根据特定应用约束选择合适智能体架构的必要性，为智能体优化提供了新路径。
---

## 摘要
开发用于 Web 任务的自主智能体是人工智能领域的核心挑战。虽然大语言模型（LLM）智能体能够解释复杂的用户请求，但它们通常作为黑盒运行，导致难以诊断其失败原因或规划逻辑。本文通过将 Web 任务正式视为序列决策过程来填补这一空白。我们引入了一种分类法，将现代智能体架构映射到传统的规划范式：逐步（Step-by-Step）智能体对应广度优先搜索（BFS），树搜索（Tree Search）智能体对应最佳优先树搜索，预先全规划（Full-Plan-in-Advance）智能体对应深度优先搜索（DFS）。该框架允许对上下文漂移和不连贯的任务分解等系统故障进行原则性诊断。为了评估这些行为，我们提出了五种新型评估指标，用于在简单成功率之外评估轨迹质量。我们利用来自 WebArena 基准测试的 794 条人工标注轨迹的新数据集来支持这一分析。最后，我们通过将基准逐步智能体与新型预先全规划实现进行比较，验证了我们的评估框架。结果表明，虽然逐步智能体与人类黄金轨迹的一致性更高（总体成功率为 38%），但预先全规划智能体在元素准确率（89%）等技术指标上表现优异，这证明了在根据特定应用约束选择合适的智能体架构时，我们提出的指标具有必要性。

## 速览
**TLDR**：提出了一个正式的规划框架，用于诊断LLM网页智能体中上下文漂移等故障。 \
**Motivation**：将智能体架构映射到传统规划范式的分类法。 \
**Method**：方法与实现细节请参考摘要与正文。 \
**Result**：结果与对比结论请参考摘要与正文。 \
**Conclusion**：总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。

---

## Abstract
Developing autonomous agents for web-based tasks is a core challenge in AI. While Large Language Model (LLM) agents can interpret complex user requests, they often operate as black boxes, making it difficult to diagnose why they fail or how they plan. This paper addresses this gap by formally treating web tasks as sequential decision-making processes. We introduce a taxonomy that maps modern agent architectures to traditional planning paradigms: Step-by-Step agents to Breadth-First Search (BFS), Tree Search agents to Best-First Tree Search, and Full-Plan-in-Advance agents to Depth-First Search (DFS). This framework allows for a principled diagnosis of system failures like context drift and incoherent task decomposition. To evaluate these behaviors, we propose five novel evaluation metrics that assess trajectory quality beyond simple success rates. We support this analysis with a new dataset of 794 human-labeled trajectories from the WebArena benchmark. Finally, we validate our evaluation framework by comparing a baseline Step-by-Step agent against a novel Full-Plan-in-Advance implementation. Our results reveal that while the Step-by-Step agent aligns more closely with human gold trajectories (38% overall success), the Full-Plan-in-Advance agent excels in technical measures such as element accuracy (89%), demonstrating the necessity of our proposed metrics for selecting appropriate agent architectures based on specific application constraints.

---

## 论文详细总结（自动生成）

### 基于大语言模型（LLM）的 Web 智能体 AI 规划框架论文总结

#### 1. 论文的核心问题与整体含义
*   **研究动机**：尽管基于 LLM 的 Web 智能体在处理复杂任务上表现出色，但其内部规划逻辑往往是“黑盒”，难以诊断失败原因（如上下文漂移、任务分解不连贯）。
*   **核心问题**：现有的评估体系（如 WebArena）多采用二元化的“成功/失败”指标，忽略了执行过程的质量。论文旨在通过将 Web 任务形式化为序列决策过程，建立 LLM 智能体与传统 AI 规划理论之间的联系，并提供一套细粒度的评估框架。

#### 2. 论文提出的方法论
*   **核心思想**：将现代 LLM 智能体架构映射到传统规划范式，建立统一的分类法：
    *   **逐步执行智能体 (Step-by-Step)**：对应**广度优先搜索 (BFS)**，每步仅考虑当前状态的最优动作，视野深度为 1。
    *   **树搜索智能体 (Tree Search)**：对应**最佳优先树搜索**，维护搜索树并利用启发式价值函数评估路径。
    *   **预先全量规划智能体 (Full-Plan-in-Advance)**：对应**深度优先搜索 (DFS)**，在执行前生成完整动作序列 $\tau = (a_1, a_2, ..., a_n)$。
*   **关键技术细节**：
    *   **环境表示**：使用辅助功能树（Accessibility Tree）简化 DOM，过滤视觉噪音。
    *   **预先规划实现**：智能体首先生成带编号的步骤计划，并在后续每步执行中将该计划作为“外部记忆”放入 Prompt，以抵抗上下文漂移。
    *   **LLM-as-a-Judge**：利用 LLM 作为语义评判员，判断智能体动作与人类黄金轨迹在语义上是否等价。

#### 3. 实验设计
*   **数据集/场景**：使用 **WebArena** 基准测试，涵盖 5 个领域：电子商务（OneStopShop）、社交媒体（Reddit）、协作开发（GitLab）、内容管理（CMS）和地图导航（OpenStreetMap）。
*   **对比方法**：
    *   **基准组**：WebArena 官方的逐步执行（Step-by-Step）智能体。
    *   **实验组**：论文新实现的预先全量规划（Full-Plan-in-Advance）智能体。
*   **模型选择**：两者均使用 **GPT-4o-mini**，设置 Temperature 为 1.0 以鼓励探索。

#### 4. 资源与算力
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **实现工具**：执行层基于 **Playwright** 浏览器自动化工具；评估逻辑使用 Python 编写，统计分析采用 IBM SPSS 27。由于该研究主要基于 Prompting（提示工程）而非模型微调，算力消耗主要集中在 API 调用上。

#### 5. 实验数量与充分性
*   **实验规模**：对 WebArena 全部 **812 个任务**进行了测试。
*   **数据贡献**：人工标注了 **794 条人类黄金轨迹**作为基准数据集。
*   **充分性评价**：实验设计较为充分且客观。通过在 5 个不同复杂度的 Web 领域进行对比，并引入 5 项新指标（恢复率、重复率、步骤成功率、部分成功率、元素准确率），多维度地分析了不同规划范式的优劣，而非单纯追求成功率。

#### 6. 论文的主要结论与发现
*   **性能对比**：逐步执行智能体总体成功率略高（38.41% vs 36.29%），且与人类轨迹的一致性更强（步骤成功率 82% vs 58%）。
*   **技术优势**：预先全量规划智能体在**元素准确率**（89%）上表现更好，且**重复动作更少**，但在应对动态变化和意外弹窗时表现僵化。
*   **失败诊断**：发现许多失败并非因为任务不可解，而是由于智能体陷入死循环（重复动作）或格式错误导致的“早停”。
*   **架构选择建议**：动态、不可预测的环境（如 DevOps 面板）适合逐步执行；结构化、逻辑固定的环境（如企业 ERP、CMS 发布）适合预先规划。

#### 7. 优点
*   **理论联系实际**：首次将 LLM 智能体行为与经典 AI 规划算法（BFS/DFS）进行形式化映射，提升了领域理论深度。
*   **高质量数据集**：提供了 794 条人工标注的 Web 操作轨迹，填补了 Web 智能体过程评估数据的空白。
*   **评估维度创新**：提出的“恢复率”和“重复率”等指标能有效量化智能体的鲁棒性和效率。

#### 8. 不足与局限
*   **模型单一性**：实验主要基于 GPT-4o-mini，未测试其他开源模型（如 Llama 3）或更强大的模型（如 GPT-4o）在规划上的差异。
*   **预先规划智能体的局限**：论文实现的预先规划智能体属于基准水平，未加入复杂的重规划（Replanning）机制，导致其在偏离计划时表现较差。
*   **LLM 评判偏差**：虽然使用了 LLM-as-a-Judge，但语义等价性的判断仍可能存在模型固有的偏见或误差。

（完）
