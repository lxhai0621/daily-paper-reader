---
title: "KEPo: Knowledge Evolution Poison on Graph-based Retrieval-Augmented Generation"
title_zh: KEPo：基于图的检索增强生成中的知识演化投毒
authors: "Qizhi Chen, Chao Qi, Yihong Huang, Muquan Li, Rongzheng Wang, Dongyang Zhang, Ke Qin, Shuang Liang"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11501v2"
tags: ["query:ma-kf"]
score: 9.0
evidence: 基于图的检索增强生成以及从外部数据库构建知识图谱
tldr: 本研究针对基于图的检索增强生成（GraphRAG）系统的安全性展开。由于GraphRAG将外部数据重组为知识图谱，传统RAG攻击手段往往失效。为此，作者提出了KEPo攻击方法，通过伪造从原始事实到毒化知识的“知识演化路径”，诱导LLM将错误信息视为最终结论。在多目标攻击中，KEPo通过连接多个攻击语料库实现相互增强。实验证明，KEPo在单目标和多目标攻击中均达到了SOTA水平，揭示了GraphRAG潜在的安全风险。
selection_source: fresh_fetch
motivation: 传统RAG攻击难以突破GraphRAG将文本重组为图结构的防御机制，需开发专门针对图结构的毒化方法。
method: 通过构建包含毒化知识的虚假事件，并伪造从真实事实到该事件的演化路径，从而在知识图谱中植入误导性逻辑。
result: 在多个数据集上的实验显示，KEPo在单目标和多目标攻击场景下的成功率均显著优于现有方法，达到了SOTA水平。
conclusion: 研究揭示了GraphRAG在面对结构化知识演化攻击时的脆弱性，强调了增强图检索系统安全性的必要性。
---

## 摘要
基于图的检索增强生成（GraphRAG）通过从外部数据库构建知识图谱（KG）来增强大语言模型（LLM）生成的时效性和准确性。然而，这种对外部数据的依赖引入了新的攻击面。攻击者可以将投毒文本注入数据库，从而操纵 LLM 针对其选择的查询生成有害的目标响应。现有研究主要集中在攻击传统的 RAG 系统，但这些方法对 GraphRAG 无效。这种鲁棒性源于 GraphRAG 的知识图谱抽象机制，它在检索前将注入的文本重新组织成图，从而使 LLM 能够基于重构的上下文而非原始投毒段落进行推理。为了揭示 GraphRAG 中潜在的安全漏洞，我们提出了知识演化投毒（KEPo），这是一种专门为 GraphRAG 设计的新型投毒攻击方法。对于每个目标查询，KEPo 首先根据目标答案生成一个包含投毒知识的有害事件。通过伪造事件背景并构建从原始事实到有害事件的知识演化路径，它随后对知识图谱进行投毒，并误导 LLM 将投毒知识视为最终结果。在多目标攻击场景中，KEPo 进一步连接多个攻击语料库，使它们的投毒知识能够相互增强，同时扩大投毒社区的规模，从而放大攻击效果。在多个数据集上的实验结果表明，KEPo 在单目标和多目标攻击中均达到了最先进的攻击成功率，显著优于以往的方法。

## 速览
**TLDR**：本研究针对基于图的检索增强生成（GraphRAG）系统的安全性展开。由于GraphRAG将外部数据重组为知识图谱，传统RAG攻击手段往往失效。为此，作者提出了KEPo攻击方法，通过伪造从原始事实到毒化知识的“知识演化路径”，诱导LLM将错误信息视为最终结论。在多目标攻击中，KEPo通过连接多个攻击语料库实现相互增强。实验证明，KEPo在单目标和多目标攻击中均达到了SOTA水平，揭示了GraphRAG潜在的安全风险。 \
**Motivation**：传统RAG攻击难以突破GraphRAG将文本重组为图结构的防御机制，需开发专门针对图结构的毒化方法。 \
**Method**：通过构建包含毒化知识的虚假事件，并伪造从真实事实到该事件的演化路径，从而误导知识图谱的构建与推理。 \
**Result**：在多个数据集上的实验表明，KEPo在单目标和多目标攻击场景下的成功率均显著优于现有方法。 \
**Conclusion**：研究揭示了GraphRAG在面对精心设计的知识演化攻击时存在严重的安全漏洞。

---

## Abstract
Graph-based Retrieval-Augmented Generation (GraphRAG) constructs the Knowledge Graph (KG) from external databases to enhance the timeliness and accuracy of Large Language Model (LLM) generations. However, this reliance on external data introduces new attack surfaces. Attackers can inject poisoned texts into databases to manipulate LLMs into producing harmful target responses for attacker-chosen queries. Existing research primarily focuses on attacking conventional RAG systems. However, such methods are ineffective against GraphRAG. This robustness derives from the KG abstraction of GraphRAG, which reorganizes injected text into a graph before retrieval, thereby enabling the LLM to reason based on the restructured context instead of raw poisoned passages. To expose latent security vulnerabilities in GraphRAG, we propose Knowledge Evolution Poison (KEPo), a novel poisoning attack method specifically designed for GraphRAG. For each target query, KEPo first generates a toxic event containing poisoned knowledge based on the target answer. By fabricating event backgrounds and forging knowledge evolution paths from original facts to the toxic event, it then poisons the KG and misleads the LLM into treating the poisoned knowledge as the final result. In multi-target attack scenarios, KEPo further connects multiple attack corpora, enabling their poisoned knowledge to mutually reinforce while expanding the scale of poisoned communities, thereby amplifying attack effectiveness. Experimental results across multiple datasets demonstrate that KEPo achieves state-of-the-art attack success rates for both single-target and multi-target attacks, significantly outperforming previous methods.

---

## 论文详细总结（自动生成）

### 论文总结：KEPo —— 针对基于图的检索增强生成（GraphRAG）的知识演化投毒攻击

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的检索增强生成（RAG）系统容易受到数据投毒攻击，但新兴的**基于图的 RAG（GraphRAG）**通过将非结构化文本抽象为知识图谱（KG）并进行社区摘要，展现出了较强的鲁棒性。传统攻击手段（如语义替换、提示词注入）在 GraphRAG 的图构建过程中往往会被过滤或形成孤立的低权重节点，导致攻击失效。
*   **研究动机**：为了揭示 GraphRAG 潜在的安全漏洞，作者提出了一种名为 **KEPo（Knowledge Evolution Poison）** 的攻击方法。其核心动机是利用 LLM 对“知识随时间演化”逻辑的信任，通过伪造从真实事实到毒化结论的演化路径，诱导系统将错误信息视为最新的、修正后的事实。

#### 2. 方法论：核心思想与关键技术
KEPo 的核心思想是**伪造知识演化路径**，使投毒内容在逻辑上与现有知识无缝衔接。
*   **知识演化伪造（Single-Target Attack）**：
    1.  **锚点识别**：从原始查询和答案中提取真实事实（$f_t$）和时间锚点（$t$）。
    2.  **演化路径构建**：利用“伪造器（Fabricator）”LLM 生成从真实事实 $f_t$ 到目标毒化事实 $f^*_{t+\Delta t}$ 的演化逻辑（例如：由于 2024 年的新研究，之前的结论被修正）。
    3.  **背景回填**：生成演化路径的起点背景 $f^*_{t-\Delta t}$，进一步增强语料的连贯性。
    4.  **降低困惑度（C-PPL）**：通过这种平滑过渡，降低投毒文本相对于原始知识库的条件困惑度，使其更容易被整合进知识图谱的高权重社区。
*   **多目标跨子图协同攻击（Multi-Target Attack）**：
    1.  **相似性聚类**：计算多个攻击目标的语义相似度，选择最相关的语料对。
    2.  **关键节点连接**：识别各投毒子图中的度中心性（Degree Centrality）节点，通过伪造关系将它们连接，形成更大规模的“毒化社区”，从而提升在 GraphRAG 检索阶段的排名。

#### 3. 实验设计
*   **数据集**：
    *   **GraphRAG-Bench**：包含 Story（多跳故事推理）和 Medical（临床医学推理）两个子集，专门用于评估图检索系统。
    *   **MuSiQue**：经典的多跳问答数据集。
    *   **HotpotQA & NQ**：用于补充验证的公共数据集。
*   **对比方法（Baselines）**：
    *   **PoisonedRAG**：针对传统 RAG 的首个投毒方法。
    *   **CorruptRAG**：改进的 RAG 投毒方法。
    *   **GRAG-Poison**：此前专门针对 GraphRAG 的攻击尝试。
*   **评估框架**：在 **GraphRAG (Nano)**、**LightRAG** 和 **HippoRAG 2** 三种主流图检索框架上进行测试，涵盖全局搜索（Global Search）和局部搜索（Local Search）模式。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件型号（如 GPU 数量或型号）以及具体的训练/索引时长。
*   **模型使用**：实验主要依赖 API 调用，使用了 **GPT-4o** 作为评估器（Evaluator），并测试了 **Gemini-2.5**、**Claude-4**、**Qwen3-14B**、**Llama-3.1-8B** 等多种模型作为生成器（Generator）和伪造器（Fabricator）。

#### 5. 实验数量与充分性
*   **实验规模**：作者在 5 个数据集上进行了广泛测试，涵盖了单目标和多目标攻击场景。
*   **消融实验**：针对知识演化路径的不同阶段（源状态事实、演化路径等）进行了消融实验，证明了完整演化链条的必要性。
*   **防御测试**：测试了查询改写、指令忽略、提示词检测等多种防御手段，证明了 KEPo 的隐蔽性。
*   **客观性与公平性**：实验对比了多种 GraphRAG 变体和不同参数规模的 LLM 后端，结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **GraphRAG 并非绝对安全**：虽然它能防御简单的文本注入，但对于具有**结构化逻辑**和**时间演化特征**的投毒攻击非常脆弱。
*   **KEPo 性能卓越**：在所有测试的 GraphRAG 框架中，KEPo 的攻击成功率（ASR）和条件攻击成功率（CASR）均显著高于基准方法，平均提升幅度巨大。
*   **检索排名优势**：通过伪造演化路径，投毒文本在知识图谱中的 Hits@10 排名显著提高，使其更容易进入 LLM 的上下文窗口。
*   **现有防御失效**：目前的通用 RAG 防御手段难以识别这种逻辑自洽且符合百科风格的演化文本。

#### 7. 优点（亮点）
*   **攻击视角新颖**：不同于以往的语义干扰，KEPo 利用了知识的“时效性”和“演化性”这一逻辑漏洞。
*   **针对性强**：专门针对 GraphRAG 的图构建机制（实体提取、社区摘要）设计，通过增强节点连接度来操纵检索权重。
*   **协同效应**：多目标攻击中的跨子图连接策略有效地利用了图结构的社区特性。

#### 8. 不足与局限
*   **黑盒假设的局限**：虽然论文声称是黑盒攻击，但攻击者仍需通过多次查询原始系统来获取“原始答案”以构建演化路径，这在现实中可能会触发频率限制或异常检测。
*   **生成成本**：构建高质量、逻辑自洽的演化路径对“伪造器”LLM 的推理能力有一定要求，攻击成本高于简单的文本拼接。
*   **防御研究较薄弱**：论文虽然证明了现有防御无效，但未能提出针对此类“逻辑演化”攻击的有效防御建议。

（完）
