---
title: Test-Time Strategies for More Efficient and Accurate Agentic RAG
title_zh: 提升智能体 RAG 效率与准确性的测试时策略
authors: "Brian Zhang, Deepti Guntur, Zhiyang Zuo, Abhinav Sharma, Shreyas Chaudhari, Wenlong Zhao, Franck Dernoncourt, Puneet Mathur, Ryan Rossi, Nedim Lipka"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12396v1"
tags: ["query:ma-kf"]
score: 10.0
evidence: 提高智能体RAG准确性的测试时策略
tldr: 针对 Agentic RAG 系统在处理复杂多跳问题时存在的重复检索和上下文整合不足等效率与准确性问题，本文提出了两种推理时优化策略：上下文增强模块和去重模块。通过在 Search-R1 框架中集成这些模块，研究显著提升了检索效率并减少了推理轮次，在 HotpotQA 等数据集上实现了更高的准确率和更低的 Token 消耗。
selection_source: fresh_fetch
motivation: 现有的迭代式 Agentic RAG 系统在处理复杂问题时存在重复检索和检索结果整合效果差导致的效率低下问题。
method: 引入了上下文增强模块以更好地整合检索信息，以及去重模块以过滤已检索文档并替换为次优相关文档。
result: "在 HotpotQA 数据集上，该方法使精确匹配（EM）得分提升了 5.6%，同时将检索轮次减少了 10.5%。"
conclusion: 通过在推理阶段优化上下文整合与检索去重，可以显著提升 Agentic RAG 系统的准确性与运行效率。
---

## 摘要
检索增强生成 (RAG) 系统在处理复杂的、多跳问题时面临挑战，而像 Search-R1 (Jin et al., 2025) 这样以迭代方式运行的智能体框架已被提出以应对这些复杂性。然而，此类方法可能会引入效率低下的问题，包括对先前已处理信息的重复检索，以及在当前生成提示词中有效情境化检索结果的挑战。这些问题可能导致不必要的检索轮次、次优的推理、不准确的答案以及增加的 Token 消耗。在本文中，我们研究了对 Search-R1 流线的测试时修改，以缓解这些已识别的缺点。具体而言，我们探索了两个组件及其组合的集成：一个用于将检索文档中的相关信息更好地整合到推理中的情境化模块，以及一个用次相关的文档替换先前已检索文档的去重模块。我们使用 HotpotQA (Yang et al., 2018) 和 Natural Questions (Kwiatkowski et al., 2019) 数据集评估了我们的方法，报告了精确匹配 (EM) 分数、LLM 作为裁判 (LLM-as-a-Judge) 的答案正确性评估以及平均轮次。我们表现最佳的变体（利用 GPT-4.1-mini 进行情境化）与 Search-R1 基准相比，EM 分数提高了 5.6%，轮次减少了 10.5%，证明了答案准确性和检索效率的提升。

## 速览
**TLDR**：针对 Agentic RAG 系统在处理复杂多跳问题时存在的重复检索和上下文整合不足等效率与准确性问题，本文提出了两种推理时优化策略：上下文增强模块和去重模块。通过在 Search-R1 框架中集成这些模块，研究显著提升了检索效率并减少了推理轮次，在 HotpotQA 等数据集上实现了更高的准确率和更低的 Token 消耗。 \
**Motivation**：现有的迭代式 Agentic RAG 系统在处理复杂问题时存在重复检索和检索结果整合效果差导致的效率低下问题。 \
**Method**：引入了上下文增强模块以更好地整合检索信息，以及去重模块以过滤已检索文档并替换为次优相关文档。 \
**Result**：在 HotpotQA 数据集上，该方法使精确匹配（EM）得分提升了 5.6%，同时将检索轮次减少了 10.5%。 \
**Conclusion**：通过在推理阶段优化上下文整合与检索去重，可以显著提升 Agentic RAG 系统的准确性与运行效率。

---

## Abstract
Retrieval-Augmented Generation (RAG) systems face challenges with complex, multihop questions, and agentic frameworks such as Search-R1 (Jin et al., 2025), which operates iteratively, have been proposed to address these complexities. However, such approaches can introduce inefficiencies, including repetitive retrieval of previously processed information and challenges in contextualizing retrieved results effectively within the current generation prompt. Such issues can lead to unnecessary retrieval turns, suboptimal reasoning, inaccurate answers, and increased token consumption.   In this paper, we investigate test-time modifications to the Search-R1 pipeline to mitigate these identified shortcomings. Specifically, we explore the integration of two components and their combination: a contextualization module to better integrate relevant information from retrieved documents into reasoning, and a de-duplication module that replaces previously retrieved documents with the next most relevant ones. We evaluate our approaches using the HotpotQA (Yang et al., 2018) and the Natural Questions (Kwiatkowski et al., 2019) datasets, reporting the exact match (EM) score, an LLM-as-a-Judge assessment of answer correctness, and the average number of turns.   Our best-performing variant, utilizing GPT-4.1-mini for contextualization, achieves a 5.6% increase in EM score and reduces the number of turns by 10.5% compared to the Search-R1 baseline, demonstrating improved answer accuracy and retrieval efficiency.

---

## 论文详细总结（自动生成）

以下是对论文《Test-Time Strategies for More Efficient and Accurate Agentic RAG》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：智能体检索增强生成（Agentic RAG）系统（如 Search-R1）通过迭代推理和检索来解决复杂的多跳问题。
*   **核心问题**：现有的 Agentic RAG 在推理过程中存在两大缺陷：
    1.  **信息遗忘（Information Forgetting）**：模型难以保留前几轮检索的信息，导致重复生成相同的查询语句。
    2.  **信息提取无效（Ineffective Extraction）**：模型难以从检索到的长文档中精准识别关键信息，导致推理路径偏移。
*   **研究目的**：探索在**测试时（Inference-time/Test-time）**无需重新训练模型的情况下，通过模块化策略提升 RAG 系统的准确性与检索效率。

### 2. 方法论
论文提出了三种基于测试时的改进模块，直接作用于 Search-R1 的推理循环：
*   **情境化模块（Contextualization）**：
    *   引入一个外部强模型（如 GPT-4o-mini）作为“阅读器”。
    *   在每一轮检索后，该模块提取文档中与原始问题相关的简洁信息，并维护一个**持久化内存缓存（Memory Cache）**。
    *   主模型在下一轮推理时，同时接收新提取的信息和历史缓存，从而减少冗余并强化长程记忆。
*   **去重模块（De-duplication）**：
    *   记录所有已检索文档的 ID。
    *   如果当前检索返回了已见过的文档，系统会自动将其过滤，并强制提取检索列表中排名靠后的、未见过的新文档。
    *   核心思想是强制增加信息的多样性，防止模型陷入重复检索的死循环。
*   **混合模式（Hybrid）**：顺序结合上述两个模块，先去重再进行情境化提取。

### 3. 实验设计
*   **数据集**：HotpotQA（多跳推理）和 Natural Questions (NQ) 的验证集。
*   **实验规模**：从上述数据集中随机抽取 500 个样本进行评估（以平衡 API 调用成本）。
*   **基准模型（Benchmark）**：
    *   Qwen2.5-7b Search-R1-base (PPO 训练版) 作为主要基线。
    *   对比了 Qwen2.5-3b 的不同版本（PPO 和 GRPO）。
*   **评估指标**：
    *   **EM (Exact Match)**：精确匹配得分。
    *   **LLM Match**：使用 GPT-4o-mini 作为裁判，评估语义一致性（解决 EM 过于严格的问题，如 "2" 与 "Two"）。
    *   **平均检索轮次（Avg. # searches）**：衡量效率。

### 4. 资源与算力
*   **模型运行**：使用 HuggingFace Transformers 库运行 Qwen2.5-7b 模型。
*   **检索器**：使用 E5 稠密检索器对 2018 年维基百科转储（Wikipedia dump）进行检索。
*   **外部 API**：调用 OpenAI 的 GPT-4o-mini 进行情境化处理和 LLM-as-a-Judge 评估。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号、数量或推理总时长。由于是测试时策略且不涉及训练，算力消耗主要集中在模型推理和 API 调用上。

### 5. 实验数量与充分性
*   **实验组设置**：对比了基线、情境化、去重、混合四种方案，并在两个主流数据集上进行了验证。
*   **充分性评价**：
    *   **优点**：引入了 LLM Match 指标，比单纯的 EM 更客观地反映了 RAG 的真实表现；通过 95% 置信区间分析了检索轮次与准确率的关系。
    *   **局限**：每组实验仅使用 500 个样本，样本量相对较小，可能存在一定的随机性偏差。

### 6. 主要结论与发现
*   **情境化模块表现最佳**：在 EM 分数上提升了 **5.6%**，同时将平均检索轮次减少了 **10.5%**。这证明了简洁的信息表示能显著提升推理效率。
*   **去重模块的副作用**：虽然去重能略微提升准确率，但会**增加**检索轮次。原因是当模型无法从初始文档提取信息时，去重强制其寻找新文档，导致模型为了完成任务而进行了更多无效搜索。
*   **负相关性**：实验发现 EM 分数随检索轮次的增加而下降，说明需要更多检索轮次的问题本身难度更高，且更容易在长链推理中出错。

### 7. 优点
*   **即插即用**：无需对基础模型进行昂贵的微调或强化学习训练。
*   **效率与精度双赢**：通过情境化模块成功降低了 Token 消耗（减少轮次）并提升了答案质量。
*   **深入的错误分析**：揭示了 Agentic RAG 重复检索的根源在于“提取能力不足”而非单纯的“检索不到”。

### 8. 不足与局限
*   **外部依赖**：情境化模块依赖于高性能的外部 LLM API，这在实际部署中会引入额外的延迟和成本。
*   **模型敏感度**：实验发现较小的模型（如 3b）在遵循 Agentic RAG 的格式（如 `<thought>` 标签）方面表现较差，限制了该策略在小模型上的应用。
*   **评估规模**：500 个样本的子集可能无法完全代表复杂 QA 场景的所有边缘情况。

（完）
