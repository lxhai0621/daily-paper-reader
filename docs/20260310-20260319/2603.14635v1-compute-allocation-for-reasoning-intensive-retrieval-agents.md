---
title: Compute Allocation for Reasoning-Intensive Retrieval Agents
title_zh: 推理密集型检索智能体的计算分配
authors: "Sreeja Apparaju, Nilesh Gupta"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14635v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 推理密集型检索流水线与重排序深度
tldr: 随着智能体运行时间增长，其记忆库不断扩大，使得推理密集型检索变得至关重要。本研究探讨了在查询扩展和重排序阶段如何分配计算资源。通过在BRIGHT基准测试上使用Gemini系列模型，研究发现重排序阶段从更强的模型和更深的候选池中获益显著，而查询扩展的收益则迅速递减。此外，推理时间思考对两阶段的提升均有限。研究建议应将计算资源集中在重排序阶段，而非均匀分布。
selection_source: fresh_fetch
motivation: 针对长程智能体中推理密集型检索的高昂推理成本，探索如何优化计算资源在查询扩展与重排序阶段的分配。
method: 利用BRIGHT基准和Gemini模型，通过改变模型容量、推理思考时间和重排序深度，系统评估不同阶段的性能表现。
result: 重排序阶段显著受益于更强的模型和更深的候选池，而查询扩展在轻量级模型之外收益微小，且推理时间思考提升有限。
conclusion: 检索流水线的计算资源应优先集中于重排序阶段，而非在各阶段间平均分配。
---

## 摘要
随着智能体在长周期内运行，其内存存储持续增长，使得检索成为获取相关信息的关键。许多智能体查询需要推理密集型检索，其中查询与相关文档之间的联系是隐式的，需要通过推理来衔接。大语言模型（LLM）增强的流水线通过查询扩展和候选重排序来解决这一问题，但引入了显著的推理成本。我们利用 BRIGHT 基准测试和 Gemini 2.5 模型系列，研究了推理密集型检索流水线中的计算分配。我们在查询扩展和重排序阶段改变了模型容量、推理时思考以及重排序深度。我们发现，重排序从更强大的模型（NDCG@10 提升 7.5）和更深的候选池（k 从 10 增加到 100 时提升 21%）中获益显著，而查询扩展在轻量级模型之外表现出收益递减（从弱模型到强模型 NDCG@10 仅提升 1.1）。推理时思考在任何阶段提供的改进都微乎其微。这些结果表明，计算资源应集中在重排序上，而不是在流水线各阶段之间均匀分布。

## 速览
**TLDR**：研究如何在记忆库不断增长的智能体检索和重排序阶段优化计算分配。 \
**Motivation**：长程智能体推理密集型检索中的计算分配。 \
**Method**：方法与实现细节请参考摘要与正文。 \
**Result**：结果与对比结论请参考摘要与正文。 \
**Conclusion**：总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。

---

## Abstract
As agents operate over long horizons, their memory stores grow continuously, making retrieval critical to accessing relevant information. Many agent queries require reasoning-intensive retrieval, where the connection between query and relevant documents is implicit and requires inference to bridge. LLM-augmented pipelines address this through query expansion and candidate re-ranking, but introduce significant inference costs. We study computation allocation in reasoning-intensive retrieval pipelines using the BRIGHT benchmark and Gemini 2.5 model family. We vary model capacity, inference-time thinking, and re-ranking depth across query expansion and re-ranking stages. We find that re-ranking benefits substantially from stronger models (+7.5 NDCG@10) and deeper candidate pools (+21% from $k$=10 to 100), while query expansion shows diminishing returns beyond lightweight models (+1.1 NDCG@10 from weak to strong). Inference-time thinking provides minimal improvement at either stage. These results suggest that compute should be concentrated on re-ranking rather than distributed uniformly across pipeline stages.

---

## 论文详细总结（自动生成）

以下是对论文《Compute Allocation for Reasoning-Intensive Retrieval Agents》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着长程智能体（Long-running agents）运行时间的增加，其记忆库不断膨胀，检索相关信息变得至关重要。传统的基于嵌入（Embedding）的检索依赖表面语义重叠，难以处理**推理密集型（Reasoning-intensive）**查询（即查询与文档之间无直接词汇重叠，需通过逻辑推理建立联系，如“实习生”与“灵活时间项目”的匹配）。虽然目前已有利用大语言模型（LLM）进行查询扩展（QE）和重排序（RR）的流水线，但其推理成本高昂。本研究的核心问题是：**在有限的计算预算下，应如何在流水线的不同阶段（查询扩展 vs. 重排序）分配计算资源以获得最优检索性能？**

### 2. 论文提出的方法论
研究构建了一个三阶段的检索流水线，并系统性地改变各阶段的计算投入：
*   **查询扩展 (QE)：** 使用 LLM 将原始查询 $q$ 扩展为包含隐式约束和领域术语的新查询 $q_{exp}$。
*   **初始检索：** 使用 BM25 算法从大规模语料库中检索候选列表。选择 BM25 是因为它能较好地处理 LLM 生成的分布外（OOD）查询。
*   **列表式重排序 (RR)：** 使用 LLM 对初始检索到的前 $k$ 个候选文档进行推理，生成最终的排名列表。
*   **变量控制：** 研究通过改变 **模型容量**（Gemini 2.5 Flash-Lite 到 Pro）、**推理时间思考**（开启或关闭“Thinking”模式）以及 **重排序深度**（$k \in \{10, 20, 50, 100\}$）来观察性能变化。

### 3. 实验设计
*   **数据集/基准：** 使用 **BRIGHT** 基准测试，该测试包含 12 个领域的查询，要求多步推理和隐式约束满足。
*   **对比模型：** 采用了 Gemini 2.5 模型家族的四个变体：Flash-Lite（极轻量）、Flash-no-think（中型，无思考）、Flash-think（中型，动态思考）、Pro（大型，带思考）。
*   **评估指标：** NDCG@10（排名质量）、Recall@100（检索上限）、每查询成本（$）和延迟（秒）。
*   **消融实验：** 分别针对查询扩展模型、重排序模型强度、以及重排序候选池深度 $k$ 进行了独立消融。

### 4. 资源与算力
*   **算力说明：** 论文未明确提及具体的 GPU 型号、数量或训练时长。由于该研究侧重于推理阶段的计算分配，其实验主要基于 API 调用（Gemini 系列模型）。
*   **成本记录：** 论文详细记录了不同配置下的经济成本（如 Flash-Lite 扩展仅需 $0.0018/查询，而 Pro 重排序可能高达 $0.17/查询）和时间延迟。

### 5. 实验数量与充分性
*   **实验规模：** 实验覆盖了 BRIGHT 数据集的所有 12 个子领域，并对 QE 和 RR 两个阶段进行了多维度的交叉对比。
*   **充分性：** 实验设计较为全面，不仅对比了模型大小，还探讨了新兴的“推理模型（Thinking mode）”在检索任务中的表现。通过控制变量法，清晰地展示了性能随计算量增加的边际效应。
*   **客观性：** 使用了标准化的基准测试和统一的模型家族，确保了对比的公平性。

### 6. 主要结论与发现
*   **查询扩展收益递减：** 从无增强到轻量级模型（Flash-Lite）提升巨大（NDCG@10 +14.35），但进一步升级到 Pro 模型收益极微（仅 +1.1），且成本增加 27 倍。
*   **重排序是性能核心：** 重排序阶段对模型强度高度敏感，Pro 模型比 Flash-Lite 提升了 7.5 NDCG@10。
*   **重排序深度至关重要：** 将候选池 $k$ 从 10 增加到 100，NDCG@10 稳定提升约 20%，是高杠杆的计算分配方式。
*   **“思考”模式在检索中表现不佳：** 在 QE 阶段，开启 Thinking 模式反而可能因“过度思考”导致召回率下降；在 RR 阶段，Thinking 带来的微小提升（<1 NDCG）无法抵消其高昂的延迟和成本。

### 7. 优点
*   **实用性强：** 为构建长程智能体记忆系统的开发者提供了明确的成本优化指南。
*   **视角独特：** 挑战了“更强的模型在所有阶段都更好”的直觉，提出了非对称的资源分配策略。
*   **发现新颖：** 揭示了当前流行的“推理/思考型模型”在传统检索增强任务中的局限性。

### 8. 不足与局限
*   **基准单一：** 仅在 BRIGHT 一个基准上进行了测试，其结论在其他类型的检索任务（如纯事实检索）中的普适性有待验证。
*   **模型家族局限：** 实验仅限于 Gemini 2.5 系列，未测试 OpenAI o1 或开源模型（如 Llama、DeepSeek）的表现。
*   **检索算法单一：** 初始检索仅使用了 BM25，未探讨向量检索（Dense Retrieval）或混合检索在这一框架下的表现。

（完）
