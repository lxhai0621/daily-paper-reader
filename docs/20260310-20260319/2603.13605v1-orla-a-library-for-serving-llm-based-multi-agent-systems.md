---
title: "Orla: A Library for Serving LLM-Based Multi-Agent Systems"
title_zh: Orla：一个用于服务基于大语言模型的多智能体系统的库
authors: "Rana Shahout, Hayder Tirmazi, Minlan Yu, Michael Mitzenmacher"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.13605v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 用于构建和协调基于LLM的多智能体系统与工作流的库
tldr: Orla 是一个专为 LLM 多智能体系统设计的推理服务库。针对现代智能体应用中复杂的工作流编排难题，Orla 通过将请求执行与工作流策略分离，提供了阶段映射器、工作流编排器和内存管理器三大核心机制。它作为现有推理引擎的上层服务层，实现了跨模型和后端的资源调度与 KV 缓存优化，在保证灵活性的同时显著提升了多步骤智能体任务的执行效率并降低了成本。
selection_source: fresh_fetch
motivation: 当前开发者在构建智能体应用时，往往需要手动编写复杂的编排代码来连接推理引擎和工具，缺乏高效且通用的服务层抽象。
method: Orla 提出了由阶段映射器、工作流编排器和内存管理器组成的架构，实现了对多阶段工作流的自动化调度、资源管理和跨边界 KV 缓存优化。
result: 在客服工作流等数据集上的评估显示，Orla 相比 vLLM 基准显著降低了延迟和成本，并通过缓存管理有效缩短了首字响应时间。
conclusion: Orla 为多智能体系统的构建提供了一种新型服务层抽象，通过优化资源映射和内存管理，显著提升了复杂 LLM 工作流的运行效率。
---

## 摘要
我们介绍了 Orla，这是一个用于构建和运行基于大语言模型（LLM）的智能体系统的库。现代智能体应用由结合了多个 LLM 推理步骤、工具调用和异构基础设施的工作流组成。目前，开发人员通常通过手动将编排代码与 LLM 服务引擎和工具执行逻辑相结合来构建这些系统。Orla 提供了一种通用的抽象，将请求执行与工作流级策略分离。它作为现有 LLM 推理引擎之上的服务层：开发人员定义由阶段（stages）组成的工作流，而 Orla 负责管理这些阶段如何在不同模型和后端之间进行映射、执行和协调。它通过三种机制提供智能体级控制：阶段映射器（stage mapper），负责将每个阶段分配给合适的模型和后端；工作流编排器（workflow orchestrator），负责调度阶段并管理其资源和上下文；以及内存管理器（memory manager），负责跨工作流边界管理推理状态（如 KV 缓存）。我们通过一个展示了其多项能力的客户支持工作流来演示 Orla。我们在两个数据集上对 Orla 进行了评估，结果表明，与单模型 vLLM 基准相比，阶段映射改善了延迟和成本，而工作流级缓存管理减少了首字延迟（time-to-first-token）。

## 速览
**TLDR**：Orla是一个管理复杂LLM智能体工作流编排和执行策略的库。 \
**Motivation**：用于提供基于LLM的多智能体系统服务的库。 \
**Method**：方法与实现细节请参考摘要与正文。 \
**Result**：结果与对比结论请参考摘要与正文。 \
**Conclusion**：总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。

---

## Abstract
We introduce Orla, a library for constructing and running LLM-based agentic systems. Modern agentic applications consist of workflows that combine multiple LLM inference steps, tool calls, and heterogeneous infrastructure. Today, developers typically build these systems by manually composing orchestration code with LLM serving engines and tool execution logic. Orla provides a general abstraction that separates request execution from workflow-level policy. It acts as a serving layer above existing LLM inference engines: developers define workflows composed of stages, while Orla manages how those stages are mapped, executed, and coordinated across models and backends. It provides agent-level control through three mechanisms: a stage mapper, which assigns each stage to an appropriate model and backend; a workflow orchestrator, which schedules stages and manages their resources and context; and a memory manager, which manages inference state such as the KV cache across workflow boundaries. We demonstrate Orla with a customer support workflow that exercises many of its capabilities. We evaluate Orla on two datasets, showing that stage mapping improves latency and cost compared to a single-model vLLM baseline, while workflow-level cache management reduces time-to-first-token.

---

## 论文详细总结（自动生成）

### 论文总结：Orla - 用于服务基于 LLM 的多智能体系统的库

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）应用向智能体（Agentic Systems）演进，推理任务已从单次请求转变为复杂的工作流：包含多个推理步骤、工具调用及跨模型的协作。
*   **现有痛点**：目前的推理引擎（如 vLLM、SGLang）主要针对单次请求的吞吐量和延迟进行优化，而开发者在构建多智能体系统时，必须手动编写复杂的编排代码来连接推理引擎、工具执行和上下文管理。
*   **研究目标**：Orla 旨在提供一个通用的服务层抽象，将“请求执行”与“工作流策略”分离，通过在现有推理引擎之上构建编排层，自动化管理多阶段任务的资源映射、调度和内存优化。

#### 2. 方法论：核心思想与关键技术
Orla 的核心思想是将智能体工作流建模为有向无环图（DAG），并通过三个关键组件进行管理：
*   **阶段映射器 (Stage Mapper)**：
    *   负责将工作流中的每个阶段（Stage）分配给最合适的模型和后端。
    *   支持**显式映射**（开发者指定）和**动态路由**（如 `OneBitStageMapper`，利用轻量级 LLM 判断任务复杂度并路由至轻量或重量模型）。
*   **工作流编排器 (Workflow Orchestrator)**：
    *   **调度**：基于 DAG 依赖关系，支持 FCFS（先来先服务）或基于优先级的调度。
    *   **资源与上下文管理**：在阶段间自动传递输出结果，构造后续阶段的 Prompt，并在工具调用等待期间保持后端利用率。
*   **内存管理器 (Memory Manager)**：
    *   **工作流级 KV 缓存优化**：不同于传统引擎的请求级 LRU 策略，Orla 根据工作流边界决定缓存寿命。
    *   策略包括：在相同后端的连续阶段间保留缓存（减少重复 Prefill）、在跨后端或工作流结束时刷新缓存。

#### 3. 实验设计
论文通过实际工作流和基准数据集验证了 Orla 的有效性：
*   **数据集/场景**：
    1.  **SWE-bench Lite**：包含 2,294 个请求，用于评估代码修复任务中的阶段映射（Stage Mapping）。
    2.  **DAG-MATH-Formatted-CoT**：包含 2,894 个请求，用于评估多阶段数学推理中的 KV 缓存管理。
    3.  **客户支持工作流**：一个包含分类、路由、政策检查和回复的演示场景。
*   **Benchmark 与对比方法**：
    *   **基准 (Baseline)**：使用单模型（Qwen3-8B）运行在 vLLM 上的配置。
    *   **对比实验**：Orla 的异构映射（4B 与 8B 模型组合）vs. 单一 8B 模型；工作流级缓存刷新 vs. 逐请求缓存刷新。

#### 4. 资源与算力
*   **硬件环境**：1 台服务器，配置 2 颗 AMD EPYC 7313 16 核处理器（共 64 核），528 GB 系统内存。
*   **GPU 资源**：1 块 **NVIDIA RTX PRO 6000 Blackwell Max-Q** 工作站显卡（96 GB VRAM）。
*   **软件栈**：使用 vLLM 和 SGLang 作为后端引擎，支持 OpenAI 兼容的 HTTP API。
*   **模型分配**：在映射实验中，8B 模型分配 60% GPU 显存，4B 模型分配 35%。

#### 5. 实验数量与充分性
*   **实验规模**：论文针对两个主流基准数据集进行了数千次请求的测试（2,294 + 2,894 次），样本量充足。
*   **充分性评价**：实验涵盖了延迟（Wall-clock time）、成本（Cost）和首字延迟（TTFT）三个核心维度。虽然对比的基准相对单一（主要对比 vLLM），但针对“工作流级优化”这一核心贡献，实验设计是客观且具有说服力的。

#### 6. 主要结论与发现
*   **性能提升**：在 SWE-bench 任务中，通过阶段映射（将 41.6% 的简单请求路由至 4B 模型），Orla 将**墙钟时间缩短了 38%**，**平均完成时间缩短了 60%**。
*   **成本降低**：由于使用了更廉价的轻量级模型处理简单任务，推理**成本降低了 35%**。
*   **延迟优化**：工作流级的 KV 缓存管理显著改善了 TTFT（首字延迟）的分布，避免了阶段间冗余的 Prefill 计算。

#### 7. 优点
*   **解耦设计**：将编排逻辑与底层推理引擎解耦，支持插件化接入 SGLang、vLLM 甚至 Ollama（CPU 环境）。
*   **智能体感知 (Agent-aware)**：填补了现有推理引擎无法感知工作流结构的空白，特别是在 KV 缓存的生命周期管理上非常高效。
*   **易用性**：提供了简洁的 Python/Go 风格 API，允许开发者通过几行代码定义复杂的 DAG 依赖和调度策略。

#### 8. 不足与局限
*   **动态路由策略较简单**：目前提供的 `OneBitStageMapper` 依赖于另一个 LLM 调用来判断复杂度，这本身会引入额外的延迟和开销。
*   **单机局限性**：实验主要在单机单 GPU 环境下完成，未深入探讨在大规模分布式集群或跨地域后端下的通信开销和调度挑战。
*   **模型版本模糊**：文中提到的“Qwen3”模型在当前公开市场尚未主流化（可能为作者内部版本或对未来版本的预演），这可能影响其他研究者在当前环境下的复现精确度。

（完）
