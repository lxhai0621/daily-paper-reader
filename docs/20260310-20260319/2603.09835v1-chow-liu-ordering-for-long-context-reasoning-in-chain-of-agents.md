---
title: Chow-Liu Ordering for Long-Context Reasoning in Chain-of-Agents
title_zh: Chain-of-Agents 中用于长上下文推理的 Chow-Liu 排序
authors: "Naman Gupta, Vaibhav Singh, Arun Iyer, Kirankumar Shiragur, Pratham Grover, Ramakrishna B. Bairi, Ritabrata Maiti, Sankarshan Damle, Shachee Mishra Gupta, Rishikesh Maurya, Vageesh D. C"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09835v1"
tags: ["query:agent"]
score: 9.0
evidence: 在多智能体系统中使用有界共享内存管理长上下文查询
tldr: 针对长文本推理中的 Chain-of-Agents (CoA) 框架，本文研究了分块处理顺序对信息损耗的影响。由于 CoA 采用有限共享内存的顺序处理模式，分块顺序决定了信息瓶颈的质量。作者提出利用 Chow-Liu 树学习分块间的依赖结构，并通过广度优先遍历确定处理顺序。实验证明，该方法在多个长文本基准测试中显著优于默认顺序和基于语义分数的排序，有效提升了回答准确性。
selection_source: fresh_fetch
motivation: 顺序多智能体推理框架在处理长文本时存在信息瓶颈，导致最终推理结果高度依赖于文本块的处理顺序。
method: 采用 Chow-Liu 树学习文本块间的概率依赖关系，并利用树的广度优先遍历生成能减少信息损失的处理序列。
result: 在三个长文本基准测试中，该方法在答案相关性和精确匹配准确率上均优于默认文档顺序和语义评分排序。
conclusion: 优化长文本分块的处理顺序是提升顺序推理框架性能的关键，而基于依赖结构的 Chow-Liu 排序是一种有效的解决方案。
---

## 摘要
诸如代理链 (Chain-of-Agents, CoA) 之类的顺序多代理推理框架，通过将输入分解为块并使用基于大语言模型 (LLM) 的工作代理顺序处理这些块来处理长上下文查询，这些代理从受限的共享内存中读取并更新信息。从概率角度来看，CoA 旨在近似能够对整个长上下文进行联合推理的模型的条件分布。CoA 通过隐状态分解实现这一点，其中仅在代理之间传递先前处理证据的有界摘要。由此产生的有界内存近似引入了有损信息瓶颈，使得最终的证据状态本质上取决于块的处理顺序。在这项工作中，我们研究了长上下文推理中的块排序问题。我们利用著名的 Chow-Liu 树来学习一种依赖结构，该结构优先处理强相关的块。实验表明，对生成的树进行广度优先遍历所产生的块排序减少了代理间的信息损失，并且在三个长上下文基准测试中，在答案相关性和精确匹配准确度方面，始终优于默认的文档块排序和基于语义评分的排序。

## Abstract
Sequential multi-agent reasoning frameworks such as Chain-of-Agents (CoA) handle long-context queries by decomposing inputs into chunks and processing them sequentially using LLM-based worker agents that read from and update a bounded shared memory. From a probabilistic perspective, CoA aims to approximate the conditional distribution corresponding to a model capable of jointly reasoning over the entire long context. CoA achieves this through a latent-state factorization in which only bounded summaries of previously processed evidence are passed between agents. The resulting bounded-memory approximation introduces a lossy information bottleneck, making the final evidence state inherently dependent on the order in which chunks are processed.   In this work, we study the problem of chunk ordering for long-context reasoning. We use the well-known Chow-Liu trees to learn a dependency structure that prioritizes strongly related chunks. Empirically, we show that a breadth-first traversal of the resulting tree yields chunk orderings that reduce information loss across agents and consistently outperform both default document-chunk ordering and semantic score-based ordering in answer relevance and exact-match accuracy across three long-context benchmarks.

---

## 论文详细总结（自动生成）

这篇论文针对长文本推理中的“代理链”（Chain-of-Agents, CoA）框架，提出了一种基于概率图模型理论的文本块排序优化方法。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：大语言模型（LLM）在处理超长文本时受限于上下文窗口。CoA 框架通过将长文本切分为块（Chunks），让多个代理顺序处理并更新一个“有界共享内存（摘要）”来解决此问题。
*   **核心问题**：由于共享内存的大小是有限的，顺序处理本质上是一个**有损压缩**过程。信息的保留高度依赖于处理顺序。如果相关的证据块在序列中相隔太远，早期的关键信息可能在后续步骤中被遗忘或覆盖。
*   **研究动机**：现有的方法通常采用默认的文档顺序或简单的语义相关性排序，忽略了文本块之间复杂的相互依赖关系。本文旨在寻找一种最优排序，以最小化推理过程中的信息损失。

### 2. 方法论：核心思想与算法流程
*   **核心思想**：将文本块视为相互依赖的随机变量，利用 **Chow-Liu 树**（一种经典的概率算法，用于寻找最接近联合分布的树状结构）来建模块间的全局依赖关系。
*   **关键技术细节**：
    1.  **相似度建模**：使用嵌入向量（Embedding）的余弦相似度作为互信息（Mutual Information）的代理指标，构建一个以文本块为节点、相似度为边权的完全图。
    2.  **构建 Chow-Liu 树**：通过最大权重生成树（MWST）算法，从图中提取出捕获最强两两依赖关系的树结构。
    3.  **确定根节点**：选择与用户查询（Query）语义最相似的文本块作为树的根节点。
    4.  **生成排序（CL-ORDER）**：从根节点开始进行**广度优先遍历（BFS）**。这种遍历方式能确保语义和统计上相关的块在处理序列中尽可能靠近。
*   **算法流程**：编码文本块 -> 计算两两相似度 -> 构建最大生成树 -> 确定查询相关的根节点 -> BFS 遍历生成序列 -> 交付 CoA 顺序执行。

### 3. 实验设计
*   **数据集/基准测试**：
    *   **HELMET (LongQA)**：长文档问答。
    *   **$\infty$BENCH**：包含书籍问答和多选题（MC）。
    *   **NarrativeQA**：筛选出上下文超过 256K token 的极长文本查询。
*   **对比方法**：
    1.  **DEFAULT**：文档默认的自然顺序。
    2.  **DENSE**：基于查询相似度的简单降序排列（类似标准 RAG 的重排序）。
*   **评估指标**：Ragas 答案相关性评分（由 LLM 担任裁判）和精确匹配（EM）准确率。
*   **实验模型**：GPT-4.1、GPT-4.1-mini、Qwen-3-14B。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：由于该方法属于推理侧的排序优化，不涉及模型微调（Fine-tuning）。实验中使用了 OpenAI 的 `text-embedding-3-large` 和 Qwen 的开源嵌入模型进行向量化。CoA 设置中每个块的大小为 8K token，内存预算也是 8K token。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种不同规模和类型的 LLM（闭源旗舰、闭源轻量、开源模型），并在 3 个主流长文本基准上进行了测试。
*   **消融实验**：
    *   测试了不同的嵌入模型（OpenAI vs. Qwen）。
    *   对比了不同的评分函数（语义向量 vs. 词法 BM25）。
    *   对比了不同的遍历策略（BFS 树遍历 vs. 全图贪婪 DFS 遍历）。
*   **充分性评价**：实验设计较为全面，通过跨模型和跨数据集的对比验证了方法的鲁棒性，消融实验有效地证明了“全局依赖建模”优于“局部贪婪搜索”。

### 6. 主要结论与发现
*   **性能提升**：CL-ORDER 在所有配置下均优于默认顺序和语义排序。在多选题任务中，相比默认顺序提升了 **10.68%**，相比语义排序提升了 **6.89%**。
*   **模型敏感度**：较小的模型（如 GPT-4.1-mini 和 Qwen-3-14B）从优化排序中获益更多，说明更强的模型本身具有一定的抗干扰能力，但排序优化仍能提供增量收益。
*   **结构重要性**：基于树的全局结构（Chow-Liu）比局部的链式依赖（DFS）更有效，因为它能更好地防止推理路径偏离核心上下文。

### 7. 优点
*   **理论严谨**：将启发式的排序问题转化为概率图模型中的结构学习问题，具有坚实的数学基础。
*   **即插即用**：无需重新训练模型，仅通过预处理阶段的计算即可显著提升长文本推理能力。
*   **效率平衡**：构建最大生成树的计算开销远小于 LLM 推理开销，在大规模应用中具有可行性。

### 8. 不足与局限
*   **代理指标限制**：使用嵌入相似度作为互信息的代理可能无法完全捕捉复杂的逻辑依赖（例如时间顺序或因果关系）。
*   **计算复杂度**：构建完全图需要计算 $O(N^2)$ 的相似度矩阵，当文本块数量 $N$ 极大时，预处理开销会增加。
*   **任务覆盖面**：实验主要集中在问答（QA）任务上，对于长文本摘要、代码库分析等其他长上下文场景的效果尚待验证。
*   **超参数依赖**：块大小（8K）和内存预算是固定的，未探讨这些参数与排序策略之间的交互影响。

（完）
