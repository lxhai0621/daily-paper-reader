---
title: Deep Tabular Research via Continual Experience-Driven Execution
title_zh: 基于持续经验驱动执行的深度表格研究
authors: "Junnan Dong, Chuang Zhou, Zheng Yuan, Yifei Yu, Qiufeng Wang, Yinghui Li, Siyu An, Di Yin, Xing Sun, Feiyue Huang"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09151v2"
tags: ["query:ma-kf"]
score: 9.0
evidence: 针对非结构化表格推理和知识发现的智能体框架
tldr: 针对大型语言模型在处理具有复杂层级和非规范布局的非结构化表格时面临的长程分析难题，本文提出了“深度表格研究（DTR）”框架。该框架将表格推理视为闭环决策过程，通过构建层次化元图映射搜索空间、引入期望感知策略优化路径选择，并利用孪生结构化记忆实现经验的持续积累与精炼，显著提升了复杂表格任务的推理效率和准确性。
selection_source: fresh_fetch
motivation: 大型语言模型在处理具有层级表头和非规范布局的复杂非结构化表格时，难以进行有效的多步推理和长程分析。
method: 提出一种基于智能体的闭环决策框架，通过层次化元图、期望感知选择策略和孪生结构化记忆实现策略规划与执行的分离。
result: 在多个挑战性的非结构化表格基准测试中，该方法显著优于现有模型，验证了其在长程表格推理任务中的有效性。
conclusion: 将表格推理建模为持续经验驱动的执行过程，并分离高层规划与底层执行，是解决深度表格研究任务的关键。
---

## 摘要
大语言模型在处理非结构化表格上的复杂长程分析任务时经常面临困难，这些表格通常具有层级化和双向表头以及非规范布局。我们将这一挑战形式化为深度表格研究（DTR），它需要对相互依赖的表格区域进行多步推理。为了解决 DTR，我们提出了一种新颖的智能体框架，将表格推理视为一个闭环决策过程。我们精心设计了耦合的查询与表格理解，用于路径决策和操作执行。具体而言：(i) DTR 首先构建一个层级元图以捕获双向语义，将自然语言查询映射到操作级搜索空间；(ii) 为了在该空间中导航，我们引入了一种期望感知选择策略，优先处理高效用的执行路径；(iii) 至关重要的是，历史执行结果被合成到孪生结构化记忆中（即参数化更新和抽象文本），从而实现持续优化。在具有挑战性的非结构化表格基准测试上的广泛实验验证了该方法的有效性，并强调了在长程表格推理中将战略规划与底层执行分离的必要性。

## Abstract
Large language models often struggle with complex long-horizon analytical tasks over unstructured tables, which typically feature hierarchical and bidirectional headers and non-canonical layouts. We formalize this challenge as Deep Tabular Research (DTR), requiring multi-step reasoning over interdependent table regions. To address DTR, we propose a novel agentic framework that treats tabular reasoning as a closed-loop decision-making process. We carefully design a coupled query and table comprehension for path decision making and operational execution. Specifically, (i) DTR first constructs a hierarchical meta graph to capture bidirectional semantics, mapping natural language queries into an operation-level search space; (ii) To navigate this space, we introduce an expectation-aware selection policy that prioritizes high-utility execution paths; (iii) Crucially, historical execution outcomes are synthesized into a siamese structured memory, i.e., parameterized updates and abstracted texts, enabling continual refinement. Extensive experiments on challenging unstructured tabular benchmarks verify the effectiveness and highlight the necessity of separating strategic planning from low-level execution for long-horizon tabular reasoning.

---

## 论文详细总结（自动生成）

### 论文总结：基于持续经验驱动执行的深度表格研究 (DTR)

#### 1. 核心问题与整体含义（研究动机和背景）
传统的表格问答（TableQA）主要集中在结构规整、表头扁平的简单表格上。然而，现实世界中的表格（如 Excel 电子表格）通常是非结构化的，具有层级化和双向表头、合并单元格及缺失值等复杂属性。
*   **核心挑战**：大型语言模型（LLM）在处理这类表格时，难以进行长程（Long-horizon）、多步的分析任务（如趋势分析、跨区域比较、递归聚合）。
*   **研究动机**：现有的“表格转文本”推理模式受限于 Token 长度且难以处理精确数值运算；而简单的“代码生成”模式在面对复杂表头时搜索空间巨大，且容易产生错误累积。
*   **DTR 定义**：论文将此类任务定义为“深度表格研究（Deep Tabular Research）”，强调需要协调数据获取、计算和分析综合的闭环决策过程。

#### 2. 论文提出的方法论
DTR 框架将表格推理视为一个**闭环决策过程**，核心思想是**将宏观战略规划与微观操作执行分离**。

*   **关键技术组件**：
    1.  **表格理解与元图构建**：提取表格的显式/隐式元信息，识别双向层级表头，并构建层次化元图 $G_T$ 来捕获实体间的包含和层级关系。
    2.  **查询引导的操作映射**：将自然语言查询分解为原子操作（如 CLEAN, FILTER, GROUP, AGG 等），并构建操作依赖图。
    3.  **期望感知路径选择**：引入类似强化学习中 UCB（置信区间上界）的评分机制。公式为：
        $$E(\pi) = \hat{R}(\pi) + \alpha \cdot P(\pi) \sqrt{\frac{\log \sum_{\pi'} N(\pi')}{1 + N(\pi)}}$$
        其中 $\hat{R}(\pi)$ 是历史估计回报，$P(\pi)$ 是结构先验，$N(\pi)$ 是执行次数。该机制平衡了对已知高效路径的“利用”和对潜在路径的“探索”。
    4.  **孪生结构化记忆（Siamese Memory）**：
        *   **参数化反馈**：记录执行成功率、耗时、输出格式一致性等量化信号。
        *   **抽象经验反馈**：由 LLM 总结执行失败的原因或优化策略（如“聚合前应先过滤”），用于指导后续路径的修正。

#### 3. 实验设计
*   **数据集/场景**：
    *   **DTR-Bench**：论文自建的基准，包含 500 个场景驱动的问答对，涵盖 8 种用户画像（如投资者、社会研究员、数据分析师），侧重长程统计分析。
    *   **RealHitBench**：现有的真实世界层级表格基准，包含事实核查、数值推理、结构理解、数据分析和图表生成五类任务。
*   **对比方法**：
    *   **表格专用模型**：TableGPT2-7B, TableLLM-7B。
    *   **通用 LLM**：DeepSeek-V3, GPT-4o。
    *   **智能体/工作流框架**：ST-Raptor, TreeThinker, Code Loop（迭代调试代码的基线）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练/推理的总时长。
*   **模型使用**：实验主要基于 DeepSeek-V3 和 Qwen3 (1.7B & 4B) 作为底层 Backbone 进行推理测试。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个大型基准（DTR-Bench 和 RealHitBench）上进行了全量对比。
    *   进行了**消融实验**，验证了元信息提取、查询分解、期望感知和记忆模块的各自贡献。
    *   进行了 **Prompt 策略分析**（如 [THINK]+[CODE] 模式的有效性）。
    *   进行了**效率分析**，探讨了 LLM 调用次数（Budget）与准确率之间的边际效应。
*   **充分性评价**：实验设计较为全面，不仅关注准确率，还评估了分析深度、可行性、美观度（针对图表）以及推理成本。通过 10 个 Batch 的路径演化可视化，客观展示了模型学习和收敛的过程。

#### 6. 主要结论与发现
*   **性能领先**：DTR 在所有维度上均优于现有基准，尤其在非结构化表格的长程分析任务中表现突出。
*   **效率平衡**：DTR 在平均 4.78 次 LLM 调用时达到性能甜点区，比盲目增加迭代次数的 Code Loop 框架更高效且稳定。
*   **经验价值**：引入历史执行经验（尤其是失败经验的抽象总结）能显著降低代码错误率，并帮助模型在复杂搜索空间中快速锁定最优路径。

#### 7. 优点
*   **任务定义前瞻性**：首次形式化了“深度表格研究”这一更符合现实办公场景的任务。
*   **决策机制科学**：借鉴强化学习的探索-利用博弈，解决了 LLM 在复杂规划时容易陷入局部最优或死循环的问题。
*   **解耦设计**：将高层逻辑规划与底层代码执行分离，增强了系统的鲁棒性和可解释性。

#### 8. 不足与局限
*   **Backbone 依赖**：实验显示 DTR 在强模型（如 DeepSeek-V3）上提升显著，但在极小模型（如 Qwen3-1.7B）上表现依然受限，说明框架对基础推理能力有一定门槛。
*   **实时性挑战**：虽然比树搜索方法快，但多次 LLM 调用和闭环反馈仍带来一定的推理延迟，可能不适用于超大规模实时表格处理。
*   **偏差风险**：抽象经验的总结依赖于 LLM 的自我反思能力，若 LLM 对错误原因判断失误，可能会引导错误的策略更新。

（完）
