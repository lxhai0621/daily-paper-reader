---
title: "DocSage: An Information Structuring Agent for Multi-Doc Multi-Entity Question Answering"
title_zh: DocSage：一种用于多文档多实体问答的信息结构化智能体
authors: "Teng Lin, Yizhang Zhu, Zhengxuan Zhang, Yuyu Luo, Nan Tang"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11798v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 集成动态模式发现与结构化信息
tldr: "针对多文档多实体问答（MDMEQA）中逻辑追踪难、检索不精准等问题，本文提出DocSage智能体框架。该框架通过动态模式发现、结构化信息提取及模式感知推理，将非结构化文本转化为关系表，利用SQL索引和关系连接实现跨文档证据链构建。实验表明，DocSage在准确率上显著优于现有长文本模型和RAG系统，提升幅度超过27%。"
selection_source: fresh_fetch
motivation: 现有RAG框架在处理多文档多实体问答时，因缺乏模式感知和高效的跨文档证据整合能力，导致关键事实遗漏和推理不准确。
method: 提出包含模式发现、结构化提取和模式感知推理三个核心模块的智能体框架，将非结构化文本转化为关系表进行SQL驱动的精准推理。
result: "在两个MDMEQA基准测试中，DocSage相比最先进的长文本LLM和RAG系统，准确率提升了27%以上。"
conclusion: DocSage通过结构化信息处理有效解决了多文档复杂实体关系的推理难题，为提升大模型在复杂问答场景下的表现提供了新思路。
---

## 摘要
多文档多实体问答（MDMEQA）本质上要求模型能够追踪分布在碎片化文档中多个实体之间的隐式逻辑。然而，现有的大语言模型（LLMs）和检索增强生成（RAG）框架面临严重的局限性：标准 RAG 基于向量相似度的粗粒度检索往往会遗漏关键事实；基于图的 RAG 无法有效整合碎片化的复杂关系网络；且两者都缺乏模式感知（schema awareness），导致跨文档证据链构建不足以及实体关系推断不准确。为了应对这些挑战，我们提出了 DocSage，这是一个集成动态模式发现、结构化信息提取和具有错误保证的模式感知关系推理的端到端智能体框架。DocSage 通过三个核心模块运行：(1) 模式发现模块，动态推断特定于查询的最小可连接模式，以捕获关键实体和关系；(2) 提取模块，将非结构化文本转换为语义连贯的关系表，并通过错误感知纠正机制进行增强，以减少提取错误；(3) 推理模块，在结构化表上执行多跳关系推理，利用模式感知高效对齐跨文档实体并聚合证据。这种智能体设计具有三个关键优势：通过 SQL 驱动的索引实现精确的事实定位；通过关系表自然支持跨文档实体连接；以及通过结构化表示减轻 LLM 的注意力扩散。在两个 MDMEQA 基准测试上的评估表明，DocSage 的性能显著优于最先进的长上下文 LLM 和 RAG 系统，准确率分别提升了 27% 以上。

## 速览
**TLDR**：针对多文档多实体问答（MDMEQA）中证据链构建难和关系推断不准的问题，本文提出DocSage智能体框架。该框架通过动态模式发现、结构化信息提取及模式感知推理，将非结构化文本转化为关系表，利用SQL索引实现精准事实定位与跨文档实体关联。实验表明，DocSage在准确率上比现有长文本模型和RAG系统提升了27%以上，有效增强了复杂关系网络的整合能力。 \
**Motivation**：现有RAG框架在处理多文档多实体问答时，因缺乏模式感知和结构化整合能力，难以构建完整的跨文档证据链并准确推断实体关系。 \
**Method**：提出包含模式发现、结构化提取与模式感知推理三个核心模块的智能体框架，将非结构化数据转化为关系表并利用SQL进行精准推理。 \
**Result**：在两个MDMEQA基准测试中，DocSage的准确率显著优于最先进的长文本LLM和RAG系统，性能提升幅度超过27%。 \
**Conclusion**：通过将非结构化文本结构化并引入模式感知推理，DocSage有效解决了复杂多文档环境下的实体对齐与证据聚合难题。

---

## Abstract
Multi-document Multi-entity Question Answering inherently demands models to track implicit logic between multiple entities across scattered documents. However, existing Large Language Models (LLMs) and Retrieval-Augmented Generation (RAG) frameworks suffer from critical limitations: standard RAG's vector similarity-based coarse-grained retrieval often omits critical facts, graph-based RAG fails to efficiently integrate fragmented complex relationship networks, and both lack schema awareness, leading to inadequate cross-document evidence chain construction and inaccurate entity relationship deduction. To address these challenges, we propose DocSage, an end-to-end agentic framework that integrates dynamic schema discovery, structured information extraction, and schema-aware relational reasoning with error guarantees. DocSage operates through three core modules: (1) A schema discovery module dynamically infers query-specific minimal joinable schemas to capture essential entities and relationships; (2) An extraction module transforms unstructured text into semantically coherent relational tables, enhanced by error-aware correction mechanisms to reduce extraction errors; (3) A reasoning module performs multi-hop relational reasoning over structured tables, leveraging schema awareness to efficiently align cross-document entities and aggregate evidence. This agentic design offers three key advantages: precise fact localization via SQL-powered indexing, natural support for cross-document entity joins through relational tables, and mitigated LLM attention diffusion via structured representation. Evaluations on two MDMEQA benchmarks demonstrate that DocSage significantly outperforms state-of-the-art long-context LLMs and RAG systems, achieving more than 27% accuracy improvements respectively.

---

## 论文详细总结（自动生成）

### 论文总结：DocSage - 多文档多实体问答的信息结构化智能体

#### 1. 核心问题与研究动机
*   **核心问题**：多文档多实体问答（MDMEQA）要求模型在分散的非结构化文档中追踪多个实体间的隐式逻辑。
*   **研究背景与动机**：
    *   **现有 RAG 的局限**：标准 RAG 依赖向量相似度，容易遗漏关键事实；图 RAG 在处理大规模碎片化关系时效率低下。
    *   **LLM 的局限**：长文本 LLM 存在“注意力稀释”现象，难以在超长上下文中精准定位实体关系。
    *   **缺乏模式感知**：现有方法缺乏显式的结构化模式（Schema），导致证据链构建不完整，推理不准确。
*   **整体含义**：DocSage 旨在通过将非结构化文本动态转化为“查询相关的关系型数据库”，利用结构化数据的严谨性来解决复杂的多跳推理问题。

#### 2. 方法论：核心思想与技术细节
DocSage 是一个端到端的智能体框架，包含三个核心模块：
*   **交互式模式发现模块（ASK 算法）**：
    *   **核心思想**：不依赖预定义模板，通过与文档集的交互对话动态推断最小可连接模式 $S_q$。
    *   **流程**：生成初始模式假设 $\rightarrow$ 识别不确定性信号（如实体对齐冲突、逻辑矛盾、关系缺失） $\rightarrow$ 生成澄清问题并检索证据 $\rightarrow$ 迭代更新模式直至收敛。
*   **逻辑感知结构化提取模块（CLEAR 机制）**：
    *   **核心思想**：将文本提取为元组，并引入错误感知纠正机制。
    *   **技术细节**：使用 LoRA 增强的校准器评估提取置信度；执行跨记录逻辑一致性检查（如函数依赖、时间约束、外键完整性）；对低置信度或违反逻辑的元组启动纠正工作流。
*   **模式引导的关系推理模块**：
    *   **核心思想**：将自然语言查询编译为优化的 SQL 语句。
    *   **流程**：在构建的高质量数据库上执行 SQL $\rightarrow$ 证据溯源（将结果映射回原始文档块） $\rightarrow$ 综合生成带证据支撑的最终答案。

#### 3. 实验设计
*   **数据集/场景**：
    *   **MEBench**：包含 4,780 个问题，涵盖比较（Comparison）、统计（Statistics）和关系（Relationship）三大类。
    *   **Loong**：针对长文本（10K 到 250K token），包含定位、比较、聚类和推理链四种任务。
*   **对比方法（Baselines）**：
    *   原生 LLM：GPT-4o。
    *   检索增强方法：标准 RAG（GPT-4o + RAG）。
    *   结构化/图增强方法：GraphRAG、StructRAG。
*   **评估指标**：准确率（Accuracy）、平均分（Avg Score）及完美率（Perfect Rate）。

#### 4. 资源与算力
*   **模型使用**：主要使用 GPT-4o 和 Qwen3 作为核心 LLM，提取模块使用了 Mistral-7B。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及训练/推理的总时长。文中提到了对 Mistral-7B 进行了轻量级的 LoRA 微调，这通常需要中等规模的算力支持（如 A100 或 H100 级 GPU）。

#### 5. 实验数量与充分性
*   **实验规模**：在两个主流 MDMEQA 基准上进行了全面测试，涵盖了从短文本到超长文本（250K tokens）、从少量实体到大量实体（>100个）的多种场景。
*   **消融实验**：针对模式发现、结构化提取、SQL 推理、CLEAR 逻辑检查等每个核心组件都做了消融对比。
*   **充分性评价**：实验设计较为充分，通过不同难度等级（Set1/2/3）和不同任务类型的划分，客观地展示了模型在极端情况下的鲁棒性，对比实验涵盖了当前最先进的同类框架。

#### 6. 主要结论与发现
*   **性能飞跃**：DocSage 在 MEBench 和 Loong 上的准确率比现有最强基准提升了 **27% 以上**。
*   **鲁棒性强**：随着实体密度和文档长度增加，传统 RAG 和 LLM 性能大幅下降，而 DocSage 的性能保持相对稳定（如在 MEBench Set3 中仍保持 87.9% 的准确率）。
*   **结构化的价值**：实验证明，将复杂逻辑下放到 SQL 引擎处理，能有效避免 LLM 的幻觉和注意力扩散。

#### 7. 优点
*   **精准定位**：通过 SQL 驱动的索引，解决了传统向量检索容易遗漏关键事实的问题。
*   **逻辑严密**：引入了符号逻辑约束（CLEAR），为 LLM 的提取结果提供了统计学和逻辑学上的双重保证。
*   **可解释性**：答案可以精确溯源至数据库元组及原始文档位置，增强了结果的可信度。

#### 8. 不足与局限
*   **计算成本**：作为一个多阶段、迭代式的智能体框架，其推理延迟和 API 调用成本（尤其是多次调用 GPT-4o）显著高于标准 RAG。
*   **模型依赖**：框架的效果高度依赖于底层模型（如模式发现和提取模型）的基础能力。
*   **噪声敏感度**：虽然有纠错机制，但在面对极端矛盾或高度专业化且充满噪声的文本时，初始模式的构建可能仍会受到干扰。

（完）
