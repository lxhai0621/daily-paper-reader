---
title: "AEGIS: No Tool Call Left Unchecked -- A Pre-Execution Firewall and Audit Layer for AI Agents"
title_zh: AEGIS：不放过任何工具调用——面向 AI 智能体的执行前防火墙与审计层
authors: "Aojie Yuan, Zhiyuan Su, Yue Zhao"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12621v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 用于AI智能体工具调用和外部集成的预执行防火墙与审计层
tldr: AEGIS是一个针对AI智能体的预执行防火墙和审计层，旨在解决当前智能体在调用外部工具（如数据库、Shell、网络等）时缺乏安全管控的问题。它通过深度字符串提取、风险扫描和策略验证三阶段流水线，在工具执行前拦截并过滤高风险调用，并提供基于加密签名的审计追踪。该系统支持14种主流框架，在保证极低延迟的同时，能有效阻止恶意攻击并支持人工审批流程。
selection_source: fresh_fetch
motivation: 现有的AI智能体框架在执行模型生成的工具调用前缺乏有效的拦截和安全控制机制，导致无法在副作用发生前阻止潜在的恶意操作。
method: 提出了一种三阶段预执行流水线，包括从工具参数中提取深度字符串、内容风险扫描以及可组合的策略验证，并结合了基于加密哈希链的审计机制。
result: "在48个攻击实例中实现了100%的拦截率，良性调用的误报率仅为1.2%，且中值延迟仅增加8.3毫秒。"
conclusion: AEGIS证明了为AI智能体部署低延迟、高可靠且跨框架通用的预执行安全中继层是切实可行的。
---

## 摘要
AI 智能体越来越多地通过外部工具采取行动：它们查询数据库、执行 shell 命令、读写文件以及发送网络请求。然而，在目前大多数智能体技术栈中，模型生成的工具调用被直接交给执行层，中间缺乏与框架无关的控制点。执行后的可观测性可以记录这些操作，但无法在副作用发生前阻止它们。我们提出了 AEGIS，一种面向 AI 智能体的执行前防火墙和审计层。AEGIS 介入工具执行路径并应用三阶段流水线：(i) 从工具参数中进行深度字符串提取，(ii) 内容优先的风险扫描，以及 (iii) 可组合的策略验证。高风险调用可以挂起以待人工审批，所有决策都记录在基于 Ed25519 签名和 SHA-256 哈希链的防篡改审计轨迹中。在当前的实现中，AEGIS 通过轻量级集成支持跨 Python、JavaScript 和 Go 的 14 种智能体框架。在包含 48 个攻击实例的精选套件上，AEGIS 在执行前拦截了套件中的所有攻击；在 500 个良性工具调用中，其误报率为 1.2%；在 1,000 次连续拦截中，其中值延迟增加了 8.3 毫秒。现场演示将展示对良性、恶意和需人工介入的工具调用的端到端拦截，使与会者能够观察实时拦截、审批工作流和审计轨迹生成。这些结果表明，面向 AI 智能体的执行前调解是切实可行、低开销且可直接部署的。

## 速览
**TLDR**：AEGIS作为一个安全层，在执行前审计和验证AI智能体发出的工具调用。 \
**Motivation**：AI智能体调用外部工具的预执行防火墙。 \
**Method**：方法与实现细节请参考摘要与正文。 \
**Result**：结果与对比结论请参考摘要与正文。 \
**Conclusion**：总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。

---

## Abstract
AI agents increasingly act through external tools: they query databases, execute shell commands, read and write files, and send network requests. Yet in most current agent stacks, model-generated tool calls are handed to the execution layer with no framework-agnostic control point in between. Post-execution observability can record these actions, but it cannot stop them before side effects occur. We present AEGIS, a pre-execution firewall and audit layer for AI agents. AEGIS interposes on the tool-execution path and applies a three-stage pipeline: (i) deep string extraction from tool arguments, (ii) content-first risk scanning, and (iii) composable policy validation. High-risk calls can be held for human approval, and all decisions are recorded in a tamper-evident audit trail based on Ed25519 signatures and SHA-256 hash chaining. In the current implementation, AEGIS supports 14 agent frameworks across Python, JavaScript, and Go with lightweight integration. On a curated suite of 48 attackinstances, AEGIS blocks all attacks in the suite before execution; on 500 benign tool calls, it yields a 1.2% false positive rate; and across 1,000 consecutive interceptions, it adds 8.3 ms median latency. The live demo will show end-to-end interception of benign, malicious, and human-escalated tool calls, allowing attendees to observe real-time blocking, approval workflows, and audit-trail generation. These results suggest that pre-execution mediation for AI agents can be practical, low-overhead, and directly deployable.

---

## 论文详细总结（自动生成）

以下是对论文《AEGIS: No Tool Call Left Unchecked — A Pre-Execution Firewall and Audit Layer for AI Agents》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：AI 智能体（Agents）通过调用外部工具（数据库、Shell、文件系统等）产生现实世界的副作用。然而，现有的智能体技术栈在模型生成工具调用与执行层之间缺乏有效的拦截点。
*   **核心问题**：现有的可观测性平台（如 Langfuse、Arize）多为“事后记录”，无法在破坏性操作（如 SQL 注入、数据删除）发生前进行拦截。
*   **整体含义**：AEGIS 填补了这一空白，提出了一种**执行前（Pre-Execution）防火墙和审计层**，旨在将 AI 智能体视为“不可信主体”，在工具调用执行前进行实时风险扫描、策略验证和人工审批。

### 2. 方法论：核心思想与关键技术
AEGIS 采用框架无关（Framework-agnostic）的设计，核心由四个部分组成：
*   **SDK 拦截层**：通过运行时插桩（Monkey-patching）拦截 LLM SDK 的响应。支持 Python、JS、Go 等 14 种主流框架（如 LangChain, CrewAI, LlamaIndex），实现零代码侵入集成。
*   **三阶段执行前流水线**：
    1.  **深度字符串提取（Extract）**：递归提取工具参数中的字符串（深度达 32 层），防止通过嵌套结构规避检测。
    2.  **内容优先风险扫描（Scan）**：针对 7 大类（SQL 注入、路径遍历、Shell 注入、提示词注入、敏感文件访问、数据外泄、PII 泄露）的 22 种模式进行匹配。
    3.  **可组合策略验证（Policy）**：使用 JSON Schema 进行策略校验，支持自然语言编写策略并转化为规则，决策结果分为：允许（Allow）、拦截（Block）或待定（Pending）。
*   **人工参与（Human-in-the-loop）**：高风险调用会被挂起，由管理员在“合规驾驶舱”（Compliance Cockpit）中手动审批。
*   **防篡改审计轨迹**：利用 Ed25519 签名和 SHA-256 哈希链技术，确保所有决策记录不可回溯修改。

### 3. 实验设计
*   **攻击覆盖测试**：构建了一个包含 **48 个攻击实例** 的精选套件，涵盖 SQLi、路径遍历、深度规避等 7 类攻击向量。
*   **误报率测试**：从生产级工作流中采样 **500 个良性工具调用**（如正常的 SQL 查询、文件读取）。
*   **性能测试**：在本地部署环境下进行 **1,000 次连续拦截**，测量端到端延迟。
*   **对比对象**：虽然 AgentDojo 和 ToolEmu 是评估类系统而非运行时拦截系统，论文仍将其作为攻击检测覆盖范围的参考对比。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号或训练时长。
*   **系统开销**：由于 AEGIS 主要基于规则匹配和轻量级签名，其核心逻辑在 CPU 上即可高效运行。实验中提到的 8.3 ms 延迟包含了 SDK 提取、HTTP 往返和策略评估的时间。

### 5. 实验数量与充分性
*   **实验规模**：
    *   48 个攻击单元测试（100% 拦截）。
    *   500 个良性样本测试（1.2% 误报）。
    *   1,000 次延迟采样。
*   **充分性评价**：实验设计较为**客观且具有针对性**，涵盖了安全防护的关键指标（安全性、可用性、性能）。但相比于大规模工业级基准，48 个攻击实例的样本量较小，主要用于验证系统原型和已知攻击模式的有效性。

### 6. 主要结论与发现
*   **高效拦截**：AEGIS 成功拦截了测试集中的所有 48 次攻击，包括深达 20 层的嵌套规避攻击。
*   **极低开销**：中值延迟仅为 **8.3 毫秒**（P99 为 23.1 毫秒），相对于 LLM 动辄数秒的推理延迟，该开销几乎可以忽略不计（<1%）。
*   **低误报**：在良性测试中误报率仅为 1.2%，主要集中在复杂的 SQL 谓词误判，可通过服务器端覆盖规则解决。
*   **实用性**：证明了在不改变现有智能体代码的前提下，部署一个通用的执行前安全层是完全可行的。

### 7. 优点
*   **框架无关性**：支持 14 种框架，极大地降低了部署门槛。
*   **执行前控制**：真正实现了在“副作用”产生前的防御，优于传统的事后日志系统。
*   **深度防御**：递归提取技术有效解决了恶意载荷隐藏在深层嵌套参数中的问题。
*   **合规性保障**：防篡改哈希链为企业审计提供了强有力的技术支撑。

### 8. 不足与局限
*   **规则局限性**：目前主要依赖正则表达式和模式匹配，可能无法识别未知的（Zero-day）攻击变体。
*   **SQL 误报风险**：对于复杂的合法 SQL 查询，基于模式的扫描容易产生误报。
*   **评估集规模**：攻击套件规模较小，尚未在更大规模、更动态的对抗性基准（如完整的 AgentDojo 全集）上进行全面压力测试。
*   **绕过风险**：如果攻击者能够绕过被插桩的 SDK 直接调用底层 API，AEGIS 将失效。

（完）
