---
title: "Schema First Tool APIs for LLM Agents: A Controlled Study of Tool Misuse, Recovery, and Budgeted Performance"
title_zh: 面向 LLM 智能体的模式优先工具 API：关于工具误用、恢复及受限预算下性能的受控研究
authors: "Akshey Sigdel, Rista Baral"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.13404v1"
tags: ["query:ma-kf"]
score: 10.0
evidence: LLM 智能体的基于 Schema 的工具 API 和验证
tldr: 本研究探讨了工具接口设计对LLM智能体可靠性的影响，重点分析了基于模式（Schema）的工具契约和结构化验证诊断在严格预算下的作用。通过在软件工程沙盒中对比自由文本、JSON Schema及其增强版，研究发现模式化接口能显著减少接口误用，提高契约遵守度。尽管在受限本地推理下语义动作质量仍是瓶颈，但该研究为优化智能体工具调用提供了重要的实验数据和设计参考。
selection_source: fresh_fetch
motivation: 探究工具接口的规范化设计（如Schema）是否能提升LLM智能体在受限交互预算下的可靠性与错误恢复能力。
method: 在软件工程沙盒中对比评估了自由文本、JSON Schema以及带结构化诊断的JSON Schema三种不同形式的工具接口表现。
result: 实验表明模式化接口显著降低了接口层面的格式误用，但未能有效改善语义层面的逻辑错误，且在受限本地模型下任务成功率极低。
conclusion: 接口形式化虽能增强智能体对调用契约的遵守，但语义动作质量和推理时间限制仍是制约智能体性能的主要瓶颈。
---

## 摘要
工具使用已成为现代 LLM 智能体的核心，但接口设计很少被作为独立的实验变量进行研究。本文研究了基于模式（schema）的工具契约和结构化验证诊断是否能在严格的交互预算下提高可靠性。我们评估了三种保持相同工具语义和信息内容的条件：自由格式文档、JSON Schema 规范以及带有结构化诊断的 JSON Schema。我们实现了一个包含日志、指标、配置和仓库任务的确定性软件工程沙盒，并使用一个开源本地模型、三个随机种子、三种接口条件和四种预算评估了一项全交叉试点研究。我们报告了最终任务成功率、接口误用、执行失败、语义误用、恢复行为以及开销。在该试点研究中，所有条件下的成功率均为零，而模式条件减少了接口误用，但未能减少语义误用。证据支持一个精确的解释，即接口形式化提高了对契约的遵守程度，但在受限的本地推理下，语义动作质量和对超时敏感的任务仍然是主要的瓶颈。

## 速览
**TLDR**：研究基于模式的工具契约和结构化诊断如何提高LLM智能体的可靠性和工具使用。 \
**Motivation**：基于模式的工具API和LLM智能体的结构化验证。 \
**Method**：方法与实现细节请参考摘要与正文。 \
**Result**：结果与对比结论请参考摘要与正文。 \
**Conclusion**：总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。

---

## Abstract
Tool use has become central to modern LLM agents, yet interface design is rarely isolated as an experimental variable. This paper studies whether schema based tool contracts and structured validation diagnostics improve reliability under strict interaction budgets. We evaluate three conditions that preserve identical tool semantics and information content: free form documentation, JSON Schema specifications, and JSON Schema with structured diagnostics.   We implement a deterministic software engineering sandbox with logs, metrics, configurations, and repository tasks, and evaluate a fully crossed pilot with one open local model, three seeds, three interface conditions, and four budgets. We report end task success, interface misuse, execution failures, semantic misuse, recovery behavior, and overhead. In this pilot, success remains zero across conditions, while schema conditions reduce interface misuse but not semantic misuse. The evidence supports a precise interpretation that interface formalization improves contract adherence, but semantic action quality and timeout sensitive tasks remain dominant bottlenecks under constrained local inference.

---

## 论文详细总结（自动生成）

### 论文总结：面向 LLM 智能体的模式优先工具 API

#### 1. 核心问题与研究动机
*   **核心问题**：探讨工具接口（API）的设计形式（如自然语言文档 vs. 结构化 Schema）以及验证反馈的质量，如何影响 LLM 智能体在受限交互预算下的可靠性、错误恢复能力及最终任务成功率。
*   **研究动机**：在现代 LLM 智能体应用中，工具调用的可靠性至关重要。目前的评估往往忽略了“接口表示”这一变量。作者认为，借鉴传统软件工程中的“显式契约（Explicit Contracts）”和“结构化诊断（Structured Diagnostics）”，可能在不改变模型权重的情况下，低成本地提升智能体的性能。

#### 2. 方法论
*   **核心思想**：通过控制变量法，隔离接口的“表示形式”，同时保持工具的“语义内容”和“环境反馈”完全一致。
*   **三种接口条件**：
    *   **(A) 自由格式文档 (Free-form)**：使用自然语言描述工具参数、类型和示例。
    *   **(B) 模式优先规范 (Schema-first)**：使用严格的 JSON Schema 定义工具契约。
    *   **(C) 模式 + 结构化诊断**：在 B 的基础上，当调用出错时返回精确到字段级的结构化错误信息（类似编译器诊断）。
*   **关键技术细节**：
    *   **信息等效性**：从同一个“规范契约”源自动生成 A 和 B/C 两种描述，确保模型获得的信息量相同，仅格式不同。
    *   **失败分类学**：将失败分为**接口误用**（格式/契约错误）、**执行失败**（运行时前提不满足，如文件不存在）和**语义误用**（逻辑错误，如选错工具）。
    *   **确定性沙盒**：构建了一个包含日志、指标、配置和代码仓库的软件工程环境，确保实验结果可重复。

#### 3. 实验设计
*   **场景与任务**：基于软件工程诊断任务，包括日志分析、配置修复、代码调试和补丁生成。
*   **Benchmark**：自建的确定性软件工程沙盒。
*   **对比维度**：在四个不同的交互预算（步骤限制 $B \in \{3, 5, 8, 12\}$）下，对比上述三种接口条件。
*   **评估指标**：任务成功率 (S)、接口误用率 (I)、执行失败率 (E)、恢复概率 (R)、语义误用率 (M) 和 Token 开销 (O)。

#### 4. 资源与算力
*   **模型**：使用了开源本地模型 **Qwen2.5-0.5B-Instruct**（GGUF 量化版）。
*   **算力说明**：论文未详细列出具体的 GPU 型号或训练时长，实验主要集中在推理阶段。由于使用的是 0.5B 的极小规模模型，推理开销相对较低，但在实验中遇到了基础设施相关的模型超时问题。

#### 5. 实验数量与充分性
*   **实验规模**：这是一项试点研究（Pilot Study），包含 8 个任务实例，每个任务在 3 个随机种子下运行，涵盖 3 种接口条件和 4 种预算。
*   **数据量**：共产生了 276 条有效运行记录和 2208 条步骤记录。
*   **充分性与客观性**：
    *   **优点**：实验设计极其严谨，采用了全交叉（Fully Crossed）设计，并预设了统计分析协议和失败分类。
    *   **局限**：由于仅使用了一个极小规模的模型（0.5B），且任务成功率在所有条件下均为零，实验结果更多地反映了“诊断性”趋势，而非普适性的性能结论。

#### 6. 主要结论与发现
*   **Schema 能减少格式错误**：使用 JSON Schema（条件 B/C）显著降低了接口误用（格式错误），证明了形式化契约有助于模型遵守调用规范。
*   **语义瓶颈依然存在**：尽管接口误用减少了，但语义误用（逻辑错误）在 Schema 条件下反而有所增加。这表明接口规范化无法弥补模型本身的推理和规划能力不足。
*   **成功率受限**：在 0.5B 模型下，无论接口如何优化，最终任务成功率均为零。这说明在受限的本地推理环境下，语义动作质量和超时问题是比接口格式更严重的瓶颈。
*   **诊断信息的价值**：结构化诊断有助于识别错误位置，但在模型能力极弱时，无法有效转化为成功的恢复行为。

#### 7. 优点
*   **方法论严谨**：首次严格区分了接口表示与信息含量，为 LLM 工具调用研究提供了高标准的实验范式。
*   **细粒度分析**：通过失败分类学，清晰地揭示了“遵守格式”与“完成任务”之间的鸿沟。
*   **开源贡献**：发布了完整的沙盒环境、工具契约生成器和评估框架，具有很高的复现价值。

#### 8. 不足与局限
*   **地板效应（Floor Effect）**：由于选用的模型能力太弱，导致成功率为零，无法观察到接口优化对最终任务达成率的实际贡献。
*   **模型单一**：仅测试了一个超小规模模型，结果可能无法泛化到更大规模（如 7B+）或闭源模型（如 GPT-4）。
*   **任务难度与环境**：部分任务受限于基础设施超时，可能掩盖了模型在更稳定环境下的表现。

（完）
