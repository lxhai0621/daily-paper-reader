---
title: "EPOCH: An Agentic Protocol for Multi-Round System Optimization"
title_zh: EPOCH：一种用于多轮系统优化的智能体协议
authors: "Zhanlin Liu, Yitao Li, Munirathnam Srikanth"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09049v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 用于多轮系统优化和规划的智能体协议
tldr: 针对当前自主智能体在系统优化中缺乏统一协议且难以追踪的问题，本文提出了EPOCH。这是一个面向多轮系统优化的工程协议，将优化分为基准构建和迭代自我改进两个阶段。通过角色约束的阶段划分（规划、执行、评估）和标准化的执行接口，EPOCH实现了对提示词、代码及模型配置的协同优化，显著提升了优化过程的稳定性、可追溯性和可复现性。
selection_source: fresh_fetch
motivation: 现有的自主优化方法多为特定任务设计，缺乏统一的协议来管理多轮自我改进并确保评估的完整性。
method: 提出包含基准构建与迭代改进两阶段的协议，通过角色约束阶段和标准化命令接口实现结构化执行。
result: 实验表明EPOCH在多种任务中具有实用性，能够有效支持面向生产环境的自主改进工作流。
conclusion: EPOCH为异构环境下的多轮系统优化提供了一个稳定、可追溯且通用的工程框架。
---

## 摘要
自主智能体正越来越多地被用于通过迭代执行和反馈来改进提示词、代码和机器学习系统。然而，现有的方法通常被设计为针对特定任务的优化循环，而非用于建立基准和管理受追踪的多轮自我改进的统一协议。我们介绍了 EPOCH，这是一种用于异构环境下多轮系统优化的工程协议。EPOCH 将优化组织为两个阶段：基准构建和迭代自我改进。它通过角色受限的阶段进一步结构化每一轮优化，将规划、实现和评估分离开来，并通过规范的命令接口和轮级追踪来标准化执行。这种设计实现了跨提示词、模型配置、代码和基于规则的组件的协同优化，同时保持了评估的稳定性、可复现性、可追溯性和完整性。在各种任务中的实证研究说明了 EPOCH 在面向生产的自主改进工作流中的实用性。

## 速览
**TLDR**：EPOCH是一个针对多轮系统优化的工程协议，旨在解决现有自主代理优化方法缺乏统一标准和可追溯性的问题。它将优化过程分为基准构建和迭代自改进两个阶段，并通过角色约束的阶段（规划、实施、评估）来规范执行。该协议支持对提示词、代码和模型配置的协同优化，显著提升了生产环境下自主改进工作流的稳定性和可复现性。 \
**Motivation**：现有的自主代理优化方法通常是针对特定任务设计的，缺乏统一的协议来管理多轮自改进过程中的基准建立和状态追踪。 \
**Method**：提出了EPOCH协议，将优化分为基准构建和迭代改进两阶段，并利用角色约束阶段（规划、执行、评估）和标准化接口实现系统化管理。 \
**Result**：实验表明，EPOCH能够有效协调提示词、代码和模型配置的优化，同时保证了评估的稳定性、可追溯性和完整性。 \
**Conclusion**：EPOCH为生产导向的自主改进工作流提供了一个实用且稳健的框架，增强了异构环境下系统优化的工程化能力。

---

## Abstract
Autonomous agents are increasingly used to improve prompts, code, and machine learning systems through iterative execution and feedback. Yet existing approaches are usually designed as task-specific optimization loops rather than as a unified protocol for establishing baselines and managing tracked multi-round self-improvement. We introduce EPOCH, an engineering protocol for multi-round system optimization in heterogeneous environments. EPOCH organizes optimization into two phases: baseline construction and iterative self-improvement. It further structures each round through role-constrained stages that separate planning, implementation, and evaluation, and standardizes execution through canonical command interfaces and round-level tracking. This design enables coordinated optimization across prompts, model configurations, code, and rule-based components while preserving stability, reproducibility, traceability, and integrity of evaluation. Empirical studies in various tasks illustrate the practicality of EPOCH for production-oriented autonomous improvement workflows.

---

## 论文详细总结（自动生成）

以下是对论文《EPOCH: An Agentic Protocol for Multi-Round System Optimization》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前的自主智能体（Autonomous Agents）虽然在提示词优化、代码修复和超参数搜索等特定任务中表现出色，但通常被设计为**特定任务的闭环**，缺乏一个通用的、可工程化的协议来管理从“建立基准”到“多轮自我改进”的全过程。
*   **研究背景**：在生产环境中，系统性能往往由代码、配置、提示词和规则等多个交织组件决定。现有的方法缺乏明确的角色分离、规范的执行接口和轮级（Round-level）状态追踪，导致优化过程难以复现、难以审计且容易出现评估偏差（如数据泄露）。
*   **整体含义**：EPOCH 旨在为异构环境下的系统优化提供一套类似 OODA（观察-判断-决策-行动）循环的标准化工程协议，确保优化过程的稳定性、可追溯性和评估完整性。

### 2. 论文提出的方法论
EPOCH 将优化过程抽象为两个阶段和六个受限角色：
*   **核心思想**：通过角色分离（将“提出改进方案”与“评估改进效果”分离）和标准化接口，将非结构化的编辑过程转变为可审计的状态转换历史。
*   **第一阶段：基准构建（Phase I: Baseline Construction）**
    *   **Seed Planner**：分析任务规范，设计初始系统架构和评估接口。
    *   **Baseline Executor**：将设计转化为可运行的代码，生成初始度量指标。
*   **第二阶段：多轮自我改进（Phase II: Multi-Round Self-Improvement）**
    *   **Orchestrator（编排者）**：管理控制流、预算、重试机制和状态转换。
    *   **Investigator（调查者）**：分析当前状态和训练集反馈，提出改进假设（受限于训练数据）。
    *   **Executor（执行者）**：在允许的动作空间内（如修改代码、调整参数）实施更改。
    *   **Reviewer（评审者）**：在标准评估接口上验证候选状态，决定接受、拒绝或重试。
*   **关键技术细节**：
    *   **轮级追踪**：记录每轮的变更证据、指标增量（Delta）和决策逻辑。
    *   **评估完整性**：强制执行训练/评估集分离、泄露检查和确定性执行接口。

### 3. 实验设计
论文并未追求在单一任务上达到 SOTA，而是通过四个异构场景验证协议的通用性：
*   **代码改进（Fibonacci Calculator）**：优化 CLI 工具的执行效率。目标是在 2 秒内计算 $fib(10^6)$。
*   **超参数微调（MNIST 分类）**：使用 MobileNetV2 冻结卷积层，仅优化分类头的优化器类型和学习率。
*   **提示词微调（SST-2 情感分类）**：使用 `gpt-4.1-nano` 模型，在不改变模型的前提下优化系统提示词和 Few-shot 示例。
*   **基于规则的优化（Iris 分类）**：优化手写的符号化决策规则（如阈值和优先级）。
*   **对比基准**：实验主要对比了各任务从 Baseline 到多轮优化后的性能提升轨迹，并展示了协议在面对性能下降时的“拒绝”和“重试”行为。

### 4. 资源与算力
*   **算力说明**：文中**未明确说明**具体的硬件算力（如 GPU 型号、数量或总训练时长）。
*   **模型使用**：提到了使用 `gpt-4.1-nano` 进行提示词优化实验，以及在 MNIST 任务中使用了预训练的 MobileNetV2。
*   **实验规模**：实验多采用小规模数据集（如 MNIST 选取的 200 张训练图，SST-2 选取的 20 个样本），侧重于展示协议逻辑而非大规模训练。

### 5. 实验数量与充分性
*   **实验数量**：共进行了 4 组代表性任务实验，每组实验展示了 3-5 轮的优化轨迹。
*   **充分性与客观性**：
    *   **充分性**：实验覆盖了代码、参数、提示词和规则四种完全不同的优化对象，足以证明协议的**通用性**。
    *   **客观性**：实验详细记录了“拒绝（Reject）”和“终止（Terminate）”的情况，证明协议并非盲目接受所有更改，体现了评估的严谨性。
    *   **局限性**：实验样本量较小，主要在受控的小规模环境下运行，缺乏在大规模复杂生产系统中的压力测试。

### 6. 论文的主要结论与发现
*   **协议有效性**：EPOCH 能够成功将异构优化任务统一在同一套编排逻辑下，实现稳定的性能提升。
*   **自动终止与重试**：协议能识别收益递减并自动终止（如 Fibonacci 任务），或在性能退化时尝试不同策略（如 MNIST 任务中从 Adam 切换到 SGD）。
*   **可追溯性**：通过轮级追踪，系统演进的每一步都有据可查，解决了自主智能体优化过程中的“黑盒”问题。

### 7. 优点
*   **高度结构化**：将复杂的智能体协作流程标准化，非常适合工业界集成到 CI/CD 工作流中。
*   **角色解耦**：通过 Investigator 和 Reviewer 的分离，有效降低了过拟合和数据泄露的风险。
*   **多模态支持**：不局限于 LLM 提示词，同样适用于传统机器学习参数和硬编码规则。

### 8. 不足与局限
*   **实验规模较小**：目前的实证研究多基于简单任务和小数据集，在大规模分布式系统或超长链路任务中的表现尚待验证。
*   **单目标优化**：目前主要针对单一目标的迭代，尚未深入探讨多智能体在复杂子系统间的协同优化（尽管结论中提到了这是未来方向）。
*   **协议开销**：引入多角色和多阶段可能会增加推理成本和执行延迟，论文未对协议本身的性能开销进行量化分析。

（完）
