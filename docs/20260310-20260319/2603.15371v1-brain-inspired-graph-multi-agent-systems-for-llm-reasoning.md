---
title: Brain-Inspired Graph Multi-Agent Systems for LLM Reasoning
title_zh: 类脑启发式图多智能体系统用于大语言模型推理
authors: "Guangfu Hao, Yuming Dai, Xianzhe Qin, Shan Yu"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15371v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 组织为图节点的多个智能体协作系统
tldr: 针对大语言模型在复杂多步推理中的局限性，本文受人类认知“全局工作空间理论”启发，提出脑启发图多智能体系统（BIGMAS）。该系统将专用智能体组织为动态有向图节点，通过中心化共享空间进行协作。通过GraphDesigner构建任务拓扑及Orchestrator全局调度，BIGMAS在Game24等复杂任务上显著提升了推理性能，证明了多智能体架构设计与模型规模化推理的互补性。
selection_source: fresh_fetch
motivation: 现有的LLM和LRM在处理极高复杂度任务时仍面临准确率崩溃，单纯依靠模型层面的推理规模化已不足以解决复杂多步推理难题。
method: 提出BIGMAS系统，利用动态图拓扑组织智能体，并通过中心化共享工作空间和全局编排器克服反应式方法的局部视野瓶颈。
result: 在Game24、Six Fives和伦敦塔等任务上的实验表明，BIGMAS在多种前沿模型上均优于ReAct和思维树等现有基准。
conclusion: 多智能体架构设计能提供与模型层面推理增强相正交的互补增益，是提升复杂推理能力的有效途径。
---

## 摘要
大语言模型（LLMs）在广泛的语言任务中展现了卓越的能力，但复杂的多步推理仍然是一个根本性的挑战。虽然配备了扩展思维链机制的大型推理模型（LRMs）表现出优于标准 LLMs 的性能，但在足够复杂的任务上，这两类模型仍会遭遇准确率崩塌，这表明仅靠扩展模型层面的推理是不够的。受人类认知全局工作空间理论的启发，我们提出了类脑启发式图多智能体系统（BIGMAS）。在该系统中，专门的 LLM 智能体被组织为动态构建的有向图中的节点，并完全通过一个集中的共享工作空间进行协作。一个问题自适应的图设计器（GraphDesigner）构建特定任务的智能体拓扑结构，而全局编排器（Orchestrator）利用完整的共享状态进行路由决策，克服了反应式方法中的局部视图瓶颈。在 Game24、Six Fives 和 Tower of London 任务上针对六种前沿 LLMs 的实验表明，BIGMAS 持续提升了标准 LLMs 和 LRMs 的推理性能，优于包括 ReAct 和思维树（Tree of Thoughts）在内的现有多智能体基准，证明了多智能体架构设计提供了与模型层面推理增强相正交的互补增益。

## Abstract
Large Language Models (LLMs) have demonstrated remarkable capabilities across a wide range of language tasks, yet complex multi-step reasoning remains a fundamental challenge. While Large Reasoning Models (LRMs) equipped with extended chain-of-thought mechanisms demonstrate improved performance over standard LLMs, both model types still suffer from accuracy collapse on sufficiently complex tasks, suggesting that scaling model-level reasoning alone is insufficient. Inspired by the global workspace theory of human cognition, we propose Brain-Inspired Graph Multi-Agent Systems (BIGMAS), in which specialized LLM agents are organized as nodes in a dynamically constructed directed graph and coordinate exclusively through a centralized shared workspace. A problem-adaptive GraphDesigner constructs task-specific agent topologies, while a global Orchestrator leverages the complete shared state for routing decisions, overcoming the local-view bottleneck of reactive approaches. Experiments on Game24, Six Fives, and Tower of London across six frontier LLMs demonstrate that BIGMAS consistently improves reasoning performance for both standard LLMs and LRMs, outperforming existing multi-agent baselines including ReAct and Tree of Thoughts, showing that multi-agent architectural design provides complementary gains orthogonal to model-level reasoning enhancements.

---

## 论文详细总结（自动生成）

### 类脑启发式图多智能体系统（BIGMAS）论文总结

#### 1. 论文的核心问题与整体含义
*   **研究动机**：尽管大语言模型（LLMs）和大型推理模型（LRMs，如 OpenAI o1/o3、DeepSeek-R1）在推理任务上取得了显著进步，但研究发现，当问题复杂度超过一定阈值时，这些模型均会出现“准确率崩塌（Accuracy Collapse）”。这表明单纯依靠模型层面的规模化（Scaling）和思维链（CoT）扩展已遇到瓶颈。
*   **核心问题**：能否通过设计一种多智能体架构，将认知负荷分布到专门的组件中，并外部化中间推理状态，从而解决复杂的多步推理难题？
*   **整体含义**：论文受人类认知的“全局工作空间理论（Global Workspace Theory, GWT）”启发，提出了一种动态构建、全局协作的多智能体框架 BIGMAS，旨在提供一种与模型自身推理能力增强相正交的结构化解决方案。

#### 2. 论文提出的方法论
BIGMAS 框架通过三个阶段解决推理问题：
*   **核心思想**：模拟大脑中分布式专门处理器通过中央工作空间进行动态协作的机制。
*   **关键技术细节**：
    *   **阶段 1：问题自适应图设计（Graph Design）**：由 `GraphDesigner` 智能体分析具体问题，动态生成一个有向智能体图 $G$（包含节点角色、拓扑结构）和工作空间模式（Schema）。
    *   **阶段 2：中心化共享工作空间（Shared Workspace）**：所有智能体节点不进行点对点通信，而是通过一个分为四个分区（只读上下文、读写工作区、系统元数据、答案存储）的中心化空间进行交互，确保全局状态一致性。
    *   **阶段 3：全局编排与执行（Orchestration & Execution）**：
        *   **节点执行协议**：智能体通过结构化指令（append/update/replace）修改工作空间。
        *   **自纠错循环**：输出需经过验证，失败则触发带错误反馈的重新生成。
        *   **全局编排器（Orchestrator）**：基于完整的工作空间状态和执行历史进行路由决策，克服了传统反应式方法（如 ReAct）的局部视野限制。
    *   **阶段 4：答案提取**：若达到步数上限未完成，由 `FallbackResolver` 从工作空间提取最佳候选答案。

#### 3. 实验设计
*   **数据集/场景**：
    1.  **Game24**：算术推理，使用 4 个数字组合成 24。
    2.  **Six Fives**：约束表达式生成，使用 6 个数字 5 达到目标值。
    3.  **Tower of London (ToL)**：顺序规划任务，测试计划与执行功能。
*   **Benchmark 模型**：测试了 6 种前沿模型，包括 DeepSeek-V3.2、Claude 4.5 Sonnet、Gemini 2.5 Pro、GPT-5 及其对应的推理版本（+thinking）。
*   **对比方法**：
    *   **Base LLM**：直接单次调用推理。
    *   **ReAct**：反应式推理与行动循环。
    *   **Tree of Thoughts (ToT)**：思维树搜索。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **推理成本分析**：论文详细分析了**Token 消耗**。结果显示，节点执行（Node Execution）占据了总 Token 消耗的 46.4%–55.5%，而图设计和编排路由的开销相对可控。

#### 5. 实验数量与充分性
*   **实验规模**：每个任务随机采样 100 个实例，涵盖了不同的难度梯度。
*   **充分性评价**：实验设计较为充分。通过在 6 种不同的前沿模型（涵盖标准 LLM 和 LRM）上进行测试，证明了该框架的普适性。同时，通过与 ReAct 和 ToT 的对比，验证了动态拓扑和全局工作空间的优越性。此外，论文还进行了图拓扑分析、节点角色分布分析和 Token 消耗分析，从多个维度解释了系统的工作机制。

#### 6. 论文的主要结论与发现
*   **性能提升**：BIGMAS 在所有模型和任务上均显著提升了准确率。例如，Claude 4.5 (+thinking) 在伦敦塔任务上的准确率从 57% 提升至 93%。
*   **正交增益**：多智能体架构提供的增益与模型本身的推理增强（如 CoT）是互补的。即使是已经具备强大推理能力的 LRM，在 BIGMAS 框架下仍有显著提升。
*   **动态适应性**：`GraphDesigner` 能根据任务复杂度自动调整图的深度和节点角色分布（如规划任务中 Analyzer 和 Optimizer 节点比例显著增加）。
*   **失败模式**：失败的运行通常伴随着更多的路由决策次数，这表明路由计数可以作为识别推理陷入死循环的早期信号。

#### 7. 优点
*   **理论支撑扎实**：基于认知科学的全局工作空间理论，将生物学原理成功转化为工程架构。
*   **动态拓扑设计**：突破了现有框架固定拓扑的限制，实现了“一题一图”的自适应协作。
*   **全局视野**：通过中心化工作空间解决了多智能体协作中的信息碎片化和局部视图瓶颈问题。
*   **鲁棒性强**：引入了自纠错循环和多策略解析，提高了系统在面对模型输出格式波动时的稳定性。

#### 8. 不足与局限
*   **Token 成本较高**：相比单次推理，多智能体协作显著增加了 Token 消耗，虽然在复杂任务上是值得的，但在简单任务上性价比不高。
*   **任务覆盖范围**：目前仅在组合推理和规划任务上验证，尚未在开源问答、代码生成或数学竞赛等更广泛的领域进行测试。
*   **缺乏记忆机制**：`GraphDesigner` 在设计图时没有利用历史经验，每次都是独立设计，缺乏跨任务的学习能力。
*   **超参数固定**：步数预算 $T_{max}$ 和纠错限制 $R$ 是固定的，未能根据问题难度动态调整。

（完）
