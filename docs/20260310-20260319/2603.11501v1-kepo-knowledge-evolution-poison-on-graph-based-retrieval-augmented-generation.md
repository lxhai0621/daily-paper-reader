---
title: "KEPo: Knowledge Evolution Poison on Graph-based Retrieval-Augmented Generation"
title_zh: KEPo：基于图的检索增强生成的知识演化投毒
authors: "Qizhi Chen, Chao Qi, Yihong Huang, Muquan Li, Rongzheng Wang, Dongyang Zhang, Ke Qin, Shuang Liang"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11501v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 基于知识图谱抽象的图RAG
tldr: 本研究针对基于图的检索增强生成（GraphRAG）系统的安全性，提出了名为KEPo的知识演化投毒攻击方法。由于GraphRAG将文本重构为知识图谱，传统RAG攻击手段往往失效。KEPo通过伪造从原始事实到毒性事件的知识演化路径，诱导大模型将错误知识视为最终结果。实验证明，该方法在单目标和多目标攻击中均达到了最先进的成功率，揭示了GraphRAG潜在的安全漏洞。
selection_source: fresh_fetch
motivation: 传统的RAG攻击方法难以突破GraphRAG的图抽象重构机制，因此需要一种能利用图结构特征的新型投毒手段。
method: KEPo通过构建从真实事实到毒性事件的伪造演化路径来污染知识图谱，并在多目标场景下通过连接攻击语料增强攻击效果。
result: 实验结果表明，KEPo在多个数据集上的单目标和多目标攻击成功率均达到SOTA水平，显著优于现有攻击方法。
conclusion: 该研究揭示了GraphRAG在面对结构化知识演化攻击时的脆弱性，为未来提升图增强生成系统的安全性提供了重要参考。
---

## 摘要
基于图的检索增强生成 (GraphRAG) 通过从外部数据库构建知识图谱 (KG) 来增强大语言模型 (LLM) 生成的时效性和准确性。然而，这种对外部数据的依赖引入了新的攻击面。攻击者可以将投毒文本注入数据库，从而操纵 LLM 针对攻击者选择的查询生成有害的目标响应。现有研究主要集中在攻击传统的 RAG 系统，但此类方法对 GraphRAG 无效。这种鲁棒性源于 GraphRAG 的 KG 抽象机制，它在检索前将注入的文本重新组织成图，从而使 LLM 能够基于重构的上下文而非原始投毒段落进行推理。为了揭示 GraphRAG 中潜在的安全漏洞，我们提出了知识演化投毒 (KEPo)，这是一种专门为 GraphRAG 设计的新型投毒攻击方法。对于每个目标查询，KEPo 首先根据目标答案生成一个包含投毒知识的有害事件。通过虚构事件背景并伪造从原始事实到有害事件的知识演化路径，它随后对 KG 进行投毒，并误导 LLM 将投毒知识视为最终结果。在多目标攻击场景中，KEPo 进一步连接多个攻击语料库，使它们的投毒知识能够相互增强，同时扩大投毒社区的规模，从而放大攻击效果。在多个数据集上的实验结果表明，KEPo 在单目标和多目标攻击中均实现了最先进的攻击成功率，显著优于以往的方法。

## 速览
**TLDR**：本研究针对基于图的检索增强生成（GraphRAG）系统的安全性，提出了名为KEPo的知识演化投毒攻击方法。由于GraphRAG将文本重构为知识图谱，传统RAG攻击手段往往失效。KEPo通过伪造从原始事实到毒性事件的知识演化路径，诱导大模型将错误知识视为最终推理结果。实验证明，该方法在单目标和多目标攻击中均达到了最先进的成功率，揭示了GraphRAG潜在的安全漏洞。 \
**Motivation**：传统RAG攻击方法在面对GraphRAG的图抽象和重构机制时效果不佳，难以直接操纵其检索推理过程。 \
**Method**：KEPo通过构建包含毒性知识的虚假事件背景，并伪造从真实事实到该事件的演化路径，从而在知识图谱中植入误导性信息。 \
**Result**：在多个数据集上的实验表明，KEPo在单目标和多目标攻击场景下均显著优于现有方法，实现了极高的攻击成功率。 \
**Conclusion**：研究揭示了GraphRAG在处理经过精心设计的演化式投毒数据时存在严重的安全隐患。

---

## Abstract
Graph-based Retrieval-Augmented Generation (GraphRAG) constructs the Knowledge Graph (KG) from external databases to enhance the timeliness and accuracy of Large Language Model (LLM) generations.However,this reliance on external data introduces new attack surfaces.Attackers can inject poisoned texts into databases to manipulate LLMs into producing harmful target responses for attacker-chosen queries.Existing research primarily focuses on attacking conventional RAG systems.However,such methods are ineffective against GraphRAG.This robustness derives from the KG abstraction of GraphRAG,which reorganizes injected text into a graph before retrieval,thereby enabling the LLM to reason based on the restructured context instead of raw poisoned passages.To expose latent security vulnerabilities in GraphRAG,we propose Knowledge Evolution Poison (KEPo),a novel poisoning attack method specifically designed for GraphRAG.For each target query,KEPo first generates a toxic event containing poisoned knowledge based on the target answer.By fabricating event backgrounds and forging knowledge evolution paths from original facts to the toxic event,it then poisons the KG and misleads the LLM into treating the poisoned knowledge as the final result.In multi-target attack scenarios,KEPo further connects multiple attack corpora,enabling their poisoned knowledge to mutually reinforce while expanding the scale of poisoned communities,thereby amplifying attack effectiveness.Experimental results across multiple datasets demonstrate that KEPo achieves state-of-the-art attack success rates for both single-target and multi-target attacks,significantly outperforming previous methods.

---

## 论文详细总结（自动生成）

以下是对论文《KEPo: Knowledge Evolution Poison on Graph-based Retrieval-Augmented Generation》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：研究如何针对**基于图的检索增强生成（GraphRAG）**系统实施有效的投毒攻击。
*   **研究背景**：传统的 RAG 系统直接检索原始文本片段，容易受到简单的投毒攻击。而 GraphRAG 通过构建知识图谱（KG）并进行社区摘要，具有更强的鲁棒性。现有的攻击方法（如语义替换、提示词注入）在 GraphRAG 的图抽象和重构机制面前往往失效，因为它们生成的毒性文本难以融入图结构，或因逻辑冲突导致检索排名极低。
*   **研究动机**：揭示 GraphRAG 看似稳固的图结构背后存在的安全漏洞，即通过伪造逻辑连贯的“知识演化”过程，可以欺骗图构建算法和 LLM 生成器。

### 2. 方法论：核心思想与关键技术
KEPo（Knowledge Evolution Poison）的核心思想是**伪造从真实事实到毒性结论的演化路径**，使投毒信息在逻辑和结构上与现有知识库深度融合。

*   **知识演化伪造（Knowledge Evolution Forgery）**：
    1.  **锚点识别**：从目标查询和原始正确答案中提取“事实锚点”和“时间锚点”。
    2.  **路径伪造**：利用 LLM（Fabricator）生成一段逻辑链条，描述知识如何从原始事实演变为攻击者想要的目标答案（例如：从“2000年认为A是主流”演变为“2024年新研究发现B才是结果”）。
    3.  **背景虚构**：为演化路径添加权威来源（如“2024年国际肿瘤报告”）和虚构的初始背景，以增强可信度。
*   **数学原理（条件困惑度 C-PPL）**：
    *   论文通过公式推导证明，直接注入冲突事实会导致高困惑度，难以被集成。而通过构建演化路径 $L$，可以显著降低毒性文本相对于原始知识的条件困惑度，使其在图构建过程中获得更高的权重和更好的连接性。
*   **多目标协同攻击（Multi-target Coordinated Attack）**：
    *   在攻击多个目标时，KEPo 会计算不同目标答案之间的语义相似度，选择相似度高的语料对。
    *   通过在不同毒性子图的中心节点（Degree Centrality）之间建立虚构联系，形成大规模的“毒性社区”，从而在 GraphRAG 的全局搜索中占据优势。

### 3. 实验设计
*   **数据集**：
    *   **GraphRAG-Bench**：包含 Story（复杂叙事推理）和 Medical（临床医学决策）两个子集。
    *   **通用 QA 数据集**：MuSiQue（多跳问答）、HotpotQA、NQ。
*   **评估框架**：对比了四种检索架构：Naive RAG、GraphRAG（微软方案）、LightRAG（轻量级双层检索）、HippoRAG 2（受神经生物学启发的长效记忆框架）。
*   **对比方法（Baselines）**：
    *   PoisonedRAG（首个 RAG 投毒工作）。
    *   CorruptRAG（针对 RAG 的改进投毒）。
    *   GRAG-Poison（专门针对 GraphRAG 的初步尝试）。
*   **评估指标**：ASR（攻击成功率）和 CASR（条件攻击成功率，即在模型原本能答对的情况下攻击成功的概率）。

### 4. 资源与算力
*   **模型使用**：实验使用了多种 LLM 作为 Fabricator（生成攻击文本）、Generator（GraphRAG 核心）和 Evaluator（评估攻击是否成功）。包括 **GPT-4o, Gemini-2.5, GPT-4o-mini, Qwen3-14B, Llama-3.1-8B, Claude-sonnet** 等。
*   **算力说明**：论文**未明确说明**具体的 GPU 硬件型号、数量或训练时长。由于该攻击属于推理侧的黑盒投毒，主要开销在于调用 LLM API 生成攻击语料和运行 GraphRAG 检索流程，而非模型训练。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 5 个主流数据集和 4 种不同的检索框架。
    *   针对 Global Search（全局搜索）和 Local Search（局部搜索）两种模式分别进行了测试。
    *   进行了**消融实验**，验证了演化路径中不同阶段（起始事实、演化过程、终点事实）的重要性。
    *   测试了不同 Fabricator 和 Generator 组合下的表现，以及不同攻击文本长度的影响。
*   **充分性与客观性**：实验设计较为全面，不仅对比了 SOTA 方法，还针对现有的防御手段（如查询改写、提示词检测）进行了鲁棒性测试。实验结果显示 KEPo 在各种设置下均表现稳定，具有较强的说服力。

### 6. 主要结论与发现
*   **KEPo 性能卓越**：在所有 GraphRAG 框架上，KEPo 的攻击成功率显著高于基准方法，平均 ASR 提升幅度巨大（在某些场景下比传统方法高出 20%-40%）。
*   **GraphRAG 的脆弱性**：GraphRAG 虽然能过滤无意义的提示词注入，但对于逻辑自洽、结构连贯的“结构化投毒”非常脆弱。
*   **防御失效**：现有的通用 RAG 防御策略（如查询改写、指令忽略）对 KEPo 几乎无效，因为 KEPo 改变的是底层知识图谱的拓扑结构和逻辑链条，而非表层的提示词。
*   **多目标增强**：通过连接相关的毒性子图，可以有效提升毒性信息在全局检索中的排名。

### 7. 优点（亮点）
*   **攻击视角新颖**：不同于以往的语义扰动，KEPo 从“知识演化”的角度切入，利用了 LLM 对时间顺序和逻辑连贯性的偏好。
*   **理论支撑**：引入信息论中的条件困惑度（C-PPL）来解释为什么演化路径能提高投毒文本的集成度。
*   **系统性强**：不仅考虑了单目标攻击，还设计了针对大规模知识库的多目标协同攻击策略。

### 8. 不足与局限
*   **依赖高质量 Fabricator**：攻击效果高度依赖于用于生成攻击文本的 LLM 的逻辑生成能力。如果 Fabricator 较弱，生成的演化路径可能不够自然。
*   **黑盒假设的局限**：虽然论文声称是黑盒攻击，但仍需通过多次查询原始系统来获取“原始答案”以确定事实锚点，这在某些高频监控的系统中可能会触发异常检测。
*   **计算成本**：相比于简单的文本注入，KEPo 需要多次调用 LLM 来构造复杂的演化路径和多目标连接，攻击成本相对较高。

（完）
