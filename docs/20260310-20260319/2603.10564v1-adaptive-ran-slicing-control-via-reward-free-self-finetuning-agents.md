---
title: Adaptive RAN Slicing Control via Reward-Free Self-Finetuning Agents
title_zh: 基于无奖励自我微调智能体的自适应 RAN 切片控制
authors: "Yuanhao Li, Haozhe Wang, Geyong Min, Nektarios Georgalas, Wang Miao"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10564v1"
tags: ["query:agent"]
score: 9.0
evidence: 将经验内化到参数中以绕过有限上下文窗口
tldr: 本研究针对AI原生网络中生成式AI模型在连续控制任务中的局限性，提出了一种无需手工奖励函数的自微调框架。该框架通过双视角反思机制将交互经验转化为偏好数据集，并将其蒸馏至模型参数中，克服了长上下文退化问题。在动态RAN切片任务中，该方法在频谱效率、服务质量和稳定性方面均优于传统强化学习和现有大模型智能体，展示了生成式智能体在复杂网络控制中的巨大潜力。
selection_source: fresh_fetch
motivation: 旨在解决生成式AI在连续控制任务中面临的上下文窗口受限、缺乏显式奖励信号以及长上下文性能退化等核心架构挑战。
method: 提出一种自微调框架，通过双视角反思机制生成自主语言反馈并构建偏好数据集，将长周期交互经验蒸馏至模型参数中。
result: 实验表明，该框架在动态RAN切片任务的采样效率、系统稳定性和多目标优化性能上均显著超越了传统强化学习和现有LLM智能体。
conclusion: 研究证明了自我改进的生成式智能体能够有效处理复杂的连续控制任务，为构建未来AI原生网络基础设施开辟了新方向。
---

## 摘要
生成式人工智能（AI）模型与 AI 原生网络系统的集成，为实现自主和自适应控制提供了一条变革性路径。然而，此类模型在连续控制任务中的应用受到其固有架构限制的阻碍，包括有限的上下文窗口、缺乏显式奖励信号以及长上下文的退化。本文认为，实现稳健连续控制的关键在于使智能体能够通过将经验蒸馏到其参数中来内化经验，而不是依赖于基于提示（prompt）的记忆。为此，我们提出了一种新颖的自我微调框架，使智能体系统能够通过与环境的直接交互进行持续学习，从而绕过对人工设计奖励的需求。我们的框架实现了一种双视角反思机制，该机制生成自主的语言反馈，以从交互历史中构建偏好数据集。随后的基于偏好的微调过程将长时程经验蒸馏到模型参数中。我们在动态无线接入网（RAN）切片任务上评估了我们的方法，这是一个具有挑战性的多目标控制问题，需要在波动的网络条件下解决频谱效率、服务质量和重构稳定性之间的剧烈权衡。实验结果表明，我们的框架在样本效率、稳定性和多指标优化方面优于标准的强化学习（RL）基准和现有的基于大语言模型（LLM）的智能体。这些发现展示了自我改进的生成式智能体在连续控制任务中的潜力，为未来的 AI 原生网络基础设施铺平了道路。

## Abstract
The integration of Generative AI models into AI-native network systems offers a transformative path toward achieving autonomous and adaptive control. However, the application of such models to continuous control tasks is impeded by intrinsic architectural limitations, including finite context windows, the lack of explicit reward signals, and the degradation of the long context. This paper posits that the key to unlocking robust continuous control is enabling agents to internalize experience by distilling it into their parameters, rather than relying on prompt-based memory. To this end, we propose a novel self-finetuning framework that enables agentic systems to learn continuously through direct interaction with the environment, bypassing the need for handcrafted rewards. Our framework implements a bi-perspective reflection mechanism that generates autonomous linguistic feedback to construct preference datasets from interaction history. A subsequent preference-based fine-tuning process distills long-horizon experiences into the model's parameters. We evaluate our approach on a dynamic Radio Access Network (RAN) slicing task, a challenging multi-objective control problem that requires the resolution of acute trade-offs between spectrum efficiency, service quality, and reconfiguration stability under volatile network conditions. Experimental results show that our framework outperforms standard Reinforcement Learning (RL) baselines and existing Large Language Model (LLM)-based agents in sample efficiency, stability, and multi-metric optimization. These findings demonstrate the potential of self-improving generative agents for continuous control tasks, paving the way for future AI-native network infrastructure.

---

## 论文详细总结（自动生成）

### 论文总结：基于无奖励自我微调智能体的自适应 RAN 切片控制

#### 1. 核心问题与研究动机
*   **研究背景**：随着 6G 时代的到来，网络架构正向“AI 原生”转变。无线接入网（RAN）切片作为核心任务，需要在频谱效率、服务质量（QoS）和重构稳定性之间取得平衡。
*   **核心问题**：
    1.  **强化学习（RL）的瓶颈**：传统 RL 依赖复杂的人工奖励函数设计（Reward Engineering），在多目标优化中极难调优。
    2.  **大语言模型（LLM）的局限**：虽然 LLM 具备推理能力，但在连续控制任务中受限于有限的上下文窗口（Context Window）和长上下文退化问题，难以实现持久的持续学习。
*   **研究目的**：提出一种无需人工奖励函数、能将交互经验“内化”到模型参数中（而非仅依赖 Prompt 记忆）的自我改进智能体框架。

#### 2. 方法论
该论文提出了一套结合语义推理与参数微调的架构，核心组件包括：
*   **反思性马尔可夫决策过程（R-MDP）**：将传统 MDP 扩展为包含状态、动作、步骤级反思（Reflection）、决策分析和环境反馈向量的五元组，使决策过程具备语义属性。
*   **智能体-反思者（Actor-Reflector, AR）框架**：
    *   **Actor（执行者）**：基于轻量级 LLM，根据当前状态和历史生成动作及即时反思。
    *   **Reflector（反思者）**：在轨迹完成后，利用更强大的 LLM 对全量历史进行回顾，识别错误并提出改进建议。
*   **双视角反思机制**：结合了 Actor 的局部步骤级反馈（即时调整）和 Reflector 的全局轨迹级反思（长期策略优化）。
*   **Refine-from-Reflection (RfR) 微调框架**：
    *   将反思后的轨迹转化为“偏好数据集”。
    *   利用 **KTO（Kahneman-Tversky Optimization）** 算法进行微调。KTO 相比 DPO 能更好地处理不平衡的偏好数据，将经验直接蒸馏至模型参数中。
    *   引入 **Refine-rollout**：对负面样本进行多次采样，生成更多正面示例以提高样本效率。

#### 3. 实验设计
*   **实验场景**：自定义的 Python RAN 切片模拟器（基于 ns-3 引擎），模拟 6G 环境下的动态资源分配。
*   **任务目标**：最大化平均频谱效率（SE）、最小化重构次数（C）和最小化 QoS 违规次数（V）。
*   **Benchmark 与对比方法**：
    *   **传统 RL 基准**：DQN（基于值）、SAC（最大熵/离策）、PPO（策略梯度/同策）。
    *   **LLM 智能体基准**：Reflexion（基于 Prompt 的自我反思框架）。
*   **模型配置**：Actor 使用 **Qwen3-4B**，Reflector 使用 **DeepSeek-R1**。

#### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **模型规模**：提到了使用了 4B 规模的轻量级模型作为 Actor，这暗示了该方案在推理侧具有一定的轻量化潜力，但微调过程仍需标准的大模型训练环境。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   对比了 3 种主流 RL 算法在 80 轮训练（共 1600 条轨迹）下的表现。
    *   展示了所提方法在仅 1 次迭代、极少量环境交互下的收敛过程。
    *   进行了 6 轮 KTO 迭代的消融分析，展示了奖励收敛曲线。
*   **充分性评价**：实验涵盖了多目标优化的核心指标，对比了不同范式的算法。虽然场景集中在 RAN 切片，但对于验证“自我微调”这一核心贡献是充分的。不过，缺乏在不同网络拓扑或更大规模切片场景下的泛化性测试。

#### 6. 主要结论与发现
*   **性能优越**：所提框架在综合效用（Utility）上显著超过所有 RL 基准和 Reflexion。
*   **稳定性极佳**：重构次数比 PPO 减少了 59%，比 Reflexion 减少了 28.4%，证明了内化经验后的策略更加稳健。
*   **样本效率极高**：RL 算法需要上千次交互才能勉强收敛，而该方法通过单次轨迹的自我挖掘即可达到更优性能。
*   **克服上下文限制**：通过参数微调而非 Prompt 堆叠，成功解决了 LLM 在长周期任务中的性能退化问题。

#### 7. 亮点
*   **无需奖励函数**：完全依赖 LLM 的语义理解和环境反馈向量进行自我评价，解决了 RL 最头疼的奖励设计问题。
*   **经验内化**：通过 KTO 微调将短期记忆转化为长期参数知识，这是 LLM 智能体走向实用化（处理长任务）的关键一步。
*   **双模型协作**：利用强模型（Reflector）带弱模型（Actor）的思路，兼顾了推理成本和学习质量。

#### 8. 不足与局限
*   **推理延迟**：尽管使用了 4B 模型，但 LLM 的推理速度仍远慢于传统 RL 的神经网络，可能难以满足 6G 毫秒级的实时控制需求（作者在结论中也提到了这一点）。
*   **算力细节缺失**：未提供微调阶段的资源消耗数据，难以评估其部署成本。
*   **泛化风险**：实验仅在单一模拟器环境下进行，LLM 在面对未见过的极端网络故障或异构协议时的鲁棒性有待验证。
*   **依赖强模型**：Reflector（如 DeepSeek-R1）的性能直接决定了学习上限，若强模型产生幻觉，微调后的 Actor 可能会继承错误的偏好。

（完）
