---
title: "Governed Memory: A Production Architecture for Multi-Agent Workflows"
title_zh: 受治理记忆：一种面向多智能体工作流的生产级架构
authors: Hamed Taheri
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17787v1"
tags: ["query:ma-kf"]
score: 10.0
evidence: 多智能体工作流的共享记忆与治理架构
tldr: 针对企业级多智能体工作流中存在的内存孤岛、治理碎片化及冗余上下文等挑战，本文提出了“受控内存”（Governed Memory）架构。该架构通过双重内存模型、分层治理路由、反射约束检索及闭环模式生命周期，实现了跨智能体的共享内存与统一治理。实验证明，该系统在提高召回率和治理合规性的同时，显著降低了Token消耗，目前已在Personize.ai投入生产。
selection_source: fresh_fetch
motivation: 解决企业AI多智能体协作中缺乏共享内存和统一治理导致的效率低下、数据孤岛及质量退化问题。
method: 提出一种结合原子事实与模式化属性的双重内存模型，并引入分层治理路由、反射约束检索及AI辅助的闭环模式管理机制。
result: "实验显示该架构实现了99.6%的事实召回率、100%的治理合规性，并减少了50%的Token消耗，在LoCoMo基准测试中达到74.8%的准确率。"
conclusion: 受控内存架构在不损失检索质量的前提下，有效解决了多智能体系统的治理与效率难题，具有极高的生产应用价值。
---

## 摘要
企业级 AI 在工作流中部署了数十个自主智能体节点，每个节点都作用于相同的实体，但缺乏共享记忆和统一治理。我们识别了由这种记忆治理差距引起的五个结构性挑战：跨智能体工作流的记忆孤岛；跨团队和工具的治理碎片化；下游系统无法使用的非结构化记忆；自主多步执行中的冗余上下文传递；以及缺乏反馈循环导致的隐性质量下降。我们提出了 Governed Memory，这是一个共享记忆与治理层，通过四种机制解决这一差距：结合了开集原子事实与模式强制类型属性的双重记忆模型；具有渐进式上下文传递的分层治理路由；具有实体范围隔离的反射约束检索；以及包含 AI 辅助创作和自动化逐属性精炼的闭环模式生命周期。我们通过受控实验（N=250，五种内容类型）验证了每种机制：互补双模态覆盖实现了 99.6% 的事实召回率；治理路由准确率达 92%；渐进式传递减少了 50% 的 Token 消耗；在 500 次对抗性查询中实现了零跨实体泄露；100% 的对抗性治理合规性；且输出质量在每个实体约七个受控记忆时达到饱和。在 LoCoMo 基准测试中，该架构实现了 74.8% 的总准确率，证实了治理和模式强制执行不会造成检索质量损失。该系统已在 Personize.ai 投入生产。

## 速览
**TLDR**：针对企业级多智能体工作流中存在的内存孤岛、治理碎片化和冗余等挑战，本文提出了“受控内存”（Governed Memory）架构。该架构通过结合原子事实与模式化属性的双重内存模型、分层治理路由、反射约束检索和闭环模式生命周期，实现了跨智能体的共享内存与统一治理。实验证明，该系统在提高召回率和合规性的同时，显著降低了Token消耗，目前已在Personize.ai投入生产。 \
**Motivation**：企业级AI多智能体协作中缺乏共享内存和统一治理，导致数据孤岛、冗余执行及质量难以监控等结构性挑战。 \
**Method**：提出一种包含双重内存模型、分层治理路由、反射约束检索及AI辅助模式优化的共享内存与治理层架构。 \
**Result**：实验显示该架构实现了99.6%的事实召回率、50%的Token减量及100%的治理合规性，且在LoCoMo基准测试中达到74.8%的准确率。 \
**Conclusion**：受控内存架构在不损失检索质量的前提下，有效解决了多智能体系统的治理与效率问题，具备极高的生产应用价值。

---

## Abstract
Enterprise AI deploys dozens of autonomous agent nodes across workflows, each acting on the same entities with no shared memory and no common governance. We identify five structural challenges arising from this memory governance gap: memory silos across agent workflows; governance fragmentation across teams and tools; unstructured memories unusable by downstream systems; redundant context delivery in autonomous multi-step executions; and silent quality degradation without feedback loops. We present Governed Memory, a shared memory and governance layer addressing this gap through four mechanisms: a dual memory model combining open-set atomic facts with schema-enforced typed properties; tiered governance routing with progressive context delivery; reflection-bounded retrieval with entity-scoped isolation; and a closed-loop schema lifecycle with AI-assisted authoring and automated per-property refinement. We validate each mechanism through controlled experiments (N=250, five content types): 99.6% fact recall with complementary dual-modality coverage; 92% governance routing precision; 50% token reduction from progressive delivery; zero cross-entity leakage across 500 adversarial queries; 100% adversarial governance compliance; and output quality saturation at approximately seven governed memories per entity. On the LoCoMo benchmark, the architecture achieves 74.8% overall accuracy, confirming that governance and schema enforcement impose no retrieval quality penalty. The system is in production at Personize.ai.

---

## 论文详细总结（自动生成）

### 论文总结：Governed Memory（受控记忆）—— 面向多智能体工作流的生产级架构

#### 1. 核心问题与整体含义（研究动机和背景）
在企业级 AI 应用中，通常会部署数十个自主智能体（Agent）节点来处理不同的工作流（如销售、支持、研发）。然而，这些智能体在作用于相同实体（如客户、公司）时，面临**“记忆治理差距”**，具体表现为：
*   **记忆孤岛**：不同工作流之间的信息不互通。
*   **治理碎片化**：各团队使用的合规规则、品牌语气等缺乏统一管理和版本控制。
*   **非结构化死胡同**：纯文本记忆难以被下游 CRM 或自动化系统直接查询利用。
*   **上下文冗余**：在多步自主执行中，重复注入相同的政策信息浪费 Token 并干扰模型注意力。
*   **质量退化**：缺乏反馈循环，无法检测模式漂移或提取准确率下降。

该论文提出了 **Governed Memory** 架构，旨在为多智能体系统提供一个共享的、受治理的记忆层。

#### 2. 方法论：核心思想与关键技术
该架构分为四个层次，通过以下核心技术实现：
*   **双重记忆模型 (Dual Memory Model)**：
    *   **开集原子事实 (Open-Set Facts)**：提取自包含、消解了指代关系的原子事实，存储为向量嵌入。
    *   **模式强制属性 (Schema-Enforced Properties)**：根据预定义的组织模式（Schema）提取带类型的属性值（如数字、日期、布尔值），并附带置信度评分。
*   **分层治理路由 (Tiered Governance Routing)**：
    *   **快速模式**：基于嵌入相似度和关键词匹配（约 850ms），无需 LLM 调用。
    *   **全量模式**：两阶段 LLM 分析，精确选择关键和补充治理变量。
    *   **渐进式上下文传递**：记录会话状态，仅向智能体注入新增的（Delta）治理信息，减少冗余。
*   **反射约束检索 (Reflection-Bounded Retrieval)**：
    *   引入 LLM 反射循环（通常限制在 2 轮内），判断证据是否完整，若不完整则生成后续查询。
    *   **实体隔离**：通过 CRM 键（如 Email、ID）在检索前进行硬过滤，确保多租户环境下的数据安全。
*   **闭环模式生命周期 (Closed-Loop Schema Lifecycle)**：
    *   提供 AI 辅助的模式创作工具，并根据提取质量的反馈自动精炼属性定义。

#### 3. 实验设计
*   **数据集与场景**：
    *   **主要语料库**：250 个合成样本，涵盖通话记录、文档、电子邮件、转录和聊天五种类型。
    *   **对抗性数据集**：500 次对抗性查询用于验证实体隔离；50 个场景用于验证治理合规性。
    *   **冲突数据集**：30 对包含陈旧和新鲜信息的冲突事实。
*   **Benchmark**：
    *   **LoCoMo**：长效对话记忆基准测试，包含 272 个会话和 1542 个问题。
*   **对比方法**：
    *   对比了“无记忆”、“原始记忆（RAG）”、“开集+治理”以及“全受控记忆”四种配置。
    *   在 LoCoMo 上与 Mem0、Zep、OpenAI 内置记忆等系统进行了横向对比。

#### 4. 资源与算力
*   论文**未明确说明**具体的硬件算力（如 GPU 型号或数量）。
*   由于该架构是作为生产级服务（Personize.ai）部署的，其核心依赖于 LLM API（如 OpenAI 的 `text-embedding-3-small` 和 GPT 系列模型）。
*   实验中提到的延迟数据（如快速路由约 850ms，全量路由 2-5s）反映了其在生产环境中的实时处理能力。

#### 5. 实验数量与充分性
*   **实验规模**：共进行了 15 组针对性实验（E1-E15），涵盖了从提取质量、路由精度到 Token 节省率等多个维度。
*   **充分性**：实验设计较为全面，不仅有基础的召回率测试，还包含了消融实验（验证双模态的互补性）、压力测试（对抗性治理）和长效记忆基准测试。
*   **客观性**：作者承认合成数据集可能存在“上限偏差”，因此引入了 LoCoMo 这一外部基准进行验证，并提供了详细的错误分析和诊断模式。

#### 6. 主要结论与发现
*   **高性能提取**：实现了 99.6% 的事实召回率，双模态模型（开集+模式化）具有显著的互补性。
*   **治理有效性**：治理路由准确率达 92%，且在对抗性测试中实现了 100% 的合规性。
*   **效率提升**：渐进式上下文传递在多步任务中平均减少了 50.3% 的 Token 消耗。
*   **安全性**：通过 CRM 键过滤实现了零跨实体数据泄露。
*   **质量饱和点**：发现当每个实体的受控记忆达到约 7 条时，输出质量趋于饱和，这为生产部署提供了参考基准。
*   **基准表现**：在 LoCoMo 上达到 74.8% 的准确率，优于多个现有的记忆系统。

#### 7. 优点
*   **架构完整性**：不仅关注检索（RAG），更关注记忆的写入治理、类型化存储和生命周期管理。
*   **生产导向**：解决了 Token 成本、多租户隔离和下游系统集成等实际工程痛点。
*   **双模态设计**：兼顾了非结构化洞察的灵活性和结构化数据的严谨性。
*   **渐进式交付**：巧妙地解决了长上下文模型在处理冗余信息时的注意力退化问题。

#### 8. 不足与局限
*   **启发式门控**：质量门控（如指代消解评分）依赖于模式匹配而非深度语义分析，可能存在误判。
*   **自评偏差**：评估过程大量依赖“LLM-as-judge”，尽管有评分准则约束，仍可能存在模型偏见。
*   **并发冲突未验证**：实验主要验证了时间顺序上的写冲突，未测试多个智能体同时写入同一实体时的并发冲突解决。
*   **隐私保护**：脱敏机制主要基于正则匹配，对于非标准格式或上下文相关的隐私信息可能存在漏检风险。

（完）
