---
title: "Open Biomedical Knowledge Graphs at Scale: Construction, Federation, and AI Agent Access with Samyama Graph Database"
title_zh: 大规模开放生物医学知识图谱：基于 Samyama 图数据库的构建、联邦化与 AI 智能体访问
authors: "Madhulatha Mandarapu, Sandeep Kunkunuru"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15080v2"
tags: ["query:ma-kf"]
score: 9.0
evidence: 构建大规模生物医学知识图谱供AI智能体访问
tldr: "本研究针对生物医学知识碎片化问题，基于高性能Rust图数据库Samyama构建了三个大规模开源知识图谱（Pathways、Clinical Trials、Drug Interactions）。研究提出了一套可复现的ETL流程，实现了跨图谱联邦查询，并引入了模式驱动的MCP服务器，使AI智能体能高效访问图数据。在BiomedQA基准测试中，该方法将LLM回答准确率提升至98%，显著优于传统方法，为生物医学研究提供了高效的数据集成与智能问答方案。"
selection_source: fresh_fetch
motivation: 旨在解决生物医学知识分散在多个孤立数据库中、难以统一集成和高效查询的问题。
method: 基于Rust开发的Samyama图数据库，通过可复现的ETL流程构建大规模知识图谱，并利用模式驱动的MCP服务器为AI智能体提供工具化访问。
result: "成功构建了包含790万节点和2800万条边的联邦图谱，在药理学问答测试中达到98%的准确率，且查询响应时间在毫秒级。"
conclusion: 该研究证明了高性能图数据库结合模式驱动的智能体接口，能有效提升大规模生物医学知识的集成效率与AI问答的可靠性。
---

## 摘要
生物医学知识分散在孤立的数据库中——Reactome 用于通路，STRING 用于蛋白质相互作用，ClinicalTrials.gov 用于研究注册，DrugBank 用于药物词汇，DGIdb 用于药物-基因相互作用，SIDER 用于副作用。我们展示了三个开源生物医学知识图谱——Pathways KG（来自 5 个数据源的 118,686 个节点和 834,785 条边）、Clinical Trials KG（来自 5 个数据源的 7,774,446 个节点和 26,973,997 条边）以及 Drug Interactions KG（来自 3 个数据源的 32,726 个节点和 191,970 条边）——这些图谱均构建于用 Rust 编写的高性能图数据库 Samyama 之上。我们的贡献有三个方面。首先，我们描述了一种可重复的 ETL 模式，用于从异构公共数据源构建大规模知识图谱，包括跨源去重、批量加载（Python Cypher 和 Rust 原生加载器）以及便携式快照导出。其次，我们展示了跨知识图谱的联邦化：将所有三个快照加载到单个图租户中，实现了跨数据集的基于属性的连接。第三，我们引入了用于大语言模型（LLM）智能体访问的模式驱动型 MCP 服务器生成，并在新的 BiomedQA 基准测试（40 个药理学问题）上进行了评估：领域特定的 MCP 工具实现了 98% 的准确率，而 text-to-Cypher 为 0%，独立 GPT-4o 为 75%。所有数据源均采用开放许可。合并后的联邦图（790 万个节点，2800 万条边）在商用云硬件上加载约需 3 分钟，跨知识图谱查询在 80 毫秒至 4 秒内完成。

## Abstract
Biomedical knowledge is fragmented across siloed databases -- Reactome for pathways, STRING for protein interactions, ClinicalTrials.gov for study registries, DrugBank for drug vocabularies, DGIdb for drug-gene interactions, SIDER for side effects. We present three open-source biomedical knowledge graphs -- Pathways KG (118,686 nodes, 834,785 edges from 5 sources), Clinical Trials KG (7,774,446 nodes, 26,973,997 edges from 5 sources), and Drug Interactions KG (32,726 nodes, 191,970 edges from 3 sources) -- built on Samyama, a high-performance graph database written in Rust.   Our contributions are threefold. First, we describe a reproducible ETL pattern for constructing large-scale KGs from heterogeneous public data sources, with cross-source deduplication, batch loading (Python Cypher and Rust native loaders), and portable snapshot export. Second, we demonstrate cross-KG federation: loading all three snapshots into a single graph tenant enables property-based joins across datasets. Third, we introduce schema-driven MCP server generation for LLM agent access, evaluated on a new BiomedQA benchmark (40 pharmacology questions): domain-specific MCP tools achieve 98% accuracy vs. 0% for text-to-Cypher and 75% for standalone GPT-4o.   All data sources are open-license. The combined federated graph (7.9M nodes, 28M edges) loads in approximately 3 minutes on commodity cloud hardware, and cross-KG queries complete in 80ms-4s.

---

## 论文详细总结（自动生成）

这篇论文介绍了一项关于大规模生物医学知识图谱（KG）构建、联邦化及 AI 智能体访问的研究。以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：生物医学知识高度碎片化，分布在 Reactome、STRING、ClinicalTrials.gov 等多个孤立数据库中。研究人员通常需要手动编写脚本跨源引用，过程缓慢、易错且不可复现。
*   **研究动机**：现有的生物医学 KG（如 Hetionet、Bio2RDF）存在更新困难、查询性能低或仅针对单一数据源等局限。
*   **整体含义**：本文旨在利用高性能 Rust 原生图数据库 Samyama，构建一套可复现、可联邦化查询、且能让 AI 智能体通过自然语言高效访问的大规模生物医学知识体系。

### 2. 论文提出的方法论
*   **核心思想**：通过标准化的 ETL 流程构建三个独立的 KG 快照，利用 Samyama 数据库的联邦能力实现跨图谱连接，并通过 Model Context Protocol (MCP) 为 LLM 提供模式驱动的工具访问。
*   **关键技术细节**：
    *   **五阶段 ETL 模式**：下载（支持断点续传）、解析与过滤（针对人类物种 ID 9606）、去重（跨阶段追踪实体）、批量加载（Python Cypher 或 Rust 原生加载器）、快照导出（.sgsnap 便携格式）。
    *   **跨 KG 联邦化**：不采用加载时物理合并，而是基于共享属性（如 UniProt ID、DrugBank ID、基因名）进行“属性连接”（Property-based joins），使多个独立快照在同一租户内实现逻辑关联。
    *   **模式驱动的 MCP 服务器**：自动从图数据库模式（Schema）生成 LLM 专用工具。LLM 不直接生成 Cypher 语句，而是调用预定义的参数化工具（如 `pathway_members`），从而避免幻觉。

### 3. 实验设计
*   **数据集**：整合了 13 个开放许可的数据源，构建了三个 KG：
    1.  **Pathways KG**：11.9 万节点，83.5 万条边（Reactome, STRING, GO 等）。
    2.  **Clinical Trials KG**：777 万节点，2700 万条边（ClinicalTrials.gov, MeSH, PubMed 等）。
    3.  **Drug Interactions KG**：3.3 万节点，19.2 万条边（DrugBank, DGIdb, SIDER）。
*   **Benchmark**：引入了 **BiomedQA** 基准测试，包含 7 个类别的 40 个药理学问题。
*   **对比方法**：
    1.  **Standalone GPT-4o**：仅依靠模型内置知识回答。
    2.  **Text-to-Cypher (GPT-4o)**：模型根据模式生成 Cypher 查询语句。
    3.  **MCP Tools (本文方法)**：模型调用由 Samyama 自动生成的领域特定工具。

### 4. 资源与算力
*   **硬件环境**：AWS g4dn.4xlarge 实例（16 vCPU AMD EPYC, 62 GB RAM, NVIDIA A10G GPU）。
*   **性能表现**：
    *   **加载速度**：合并后的联邦图（7.9M 节点，28M 边）从快照加载仅需约 **3 分钟**（181 秒）。
    *   **构建速度**：Rust 原生加载器在 1 秒内即可完成 Drug Interactions KG 的构建。
    *   **查询延迟**：简单查询约 80-100 毫秒，复杂的跨 KG 联邦查询（如 6 跳查询）在 3.6-4 秒内完成。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从分子生物学到临床试验的 13 个主流数据库，数据量级达到千万级，具有较强的代表性。
*   **充分性评价**：
    *   **系统性能实验**：对比了 Python 加载器与 Rust 加载器的效率，测试了不同复杂度的查询延迟，实验较为充分。
    *   **问答实验**：BiomedQA 虽然仅包含 40 个问题，但覆盖了 7 个关键药理学类别，且对比了当前主流的 LLM 访问模式（RAG vs. Text-to-SQL/Cypher），结果具有说服力。
    *   **验证环境**：在 AWS 云端和 Mac Mini M4 本地环境均进行了验证，展示了良好的可移植性。

### 6. 论文的主要结论与发现
*   **MCP 优于 Text-to-Cypher**：在 BiomedQA 中，MCP 工具达到 **98%** 的准确率，而 Text-to-Cypher 准确率为 **0%**（主要由于模型生成的 Cypher 语句存在属性名幻觉或模式不匹配）。
*   **高性能联邦化可行性**：证明了在商用硬件上，通过属性连接实现千万级规模的跨 KG 联邦查询是切实可行的，且响应时间在秒级。
*   **Rust 引擎优势**：Samyama 的 Rust 原生实现显著提升了数据摄取和处理的吞吐量。

### 7. 优点
*   **工程实现卓越**：提供了可复现的 ETL 流程和便携式快照，极大降低了生物医学 KG 的部署门槛。
*   **AI 接入方案创新**：通过 MCP 协议将 LLM 的角色从“查询生成者”转变为“工具调用者”，有效解决了图查询中的幻觉问题。
*   **开源贡献**：所有代码、快照和基准测试均开源，对社区具有很高的实用价值。

### 8. 不足与局限
*   **快照时效性**：快照是静态的，而源数据库（如临床试验注册）是动态更新的，需要定期重新导出。
*   **内存依赖**：合并后的图谱需要约 33GB RAM，虽然在云端易实现，但在低配个人电脑上运行受限。
*   **实体对齐简化**：采用属性连接而非深度的实体解析（Entity Resolution），可能存在因标识符不统一导致的连接遗漏（如某些蛋白缺少 UniProt ID）。
*   **联邦范围限制**：目前联邦查询要求所有快照加载到同一租户，尚不支持跨远程租户的分布式联邦查询。

（完）
