---
title: "CLAG: Adaptive Memory Organization via Agent-Driven Clustering for Small Language Model Agents"
title_zh: CLAG：面向小语言模型智能体的基于智能体驱动聚类的自适应记忆组织
authors: "Taeyun Roh, Wonjune Jang, Junha Jung, Jaewoo Kang"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15421v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 智能体驱动的聚类内存组织
tldr: 针对小语言模型（SLM）智能体在全局内存检索中易受无关上下文干扰的问题，本文提出CLAG框架。该框架通过SLM驱动的聚类机制主动组织内存，将记忆分配至语义一致的簇并生成描述性配置。通过两阶段检索流程，CLAG有效减少了跨主题干扰，提升了内存密度和检索精度，在多个QA数据集上显著增强了SLM智能体的性能与鲁棒性。
selection_source: fresh_fetch
motivation: 传统的全局内存检索容易引入无关干扰，这对处理能力有限且对上下文敏感的小语言模型（SLM）尤为不利。
method: 提出一种基于聚类的内存组织框架，利用SLM驱动的路由和配置生成实现内存的局部化演化与两阶段精准检索。
result: 在多个问答数据集和三种SLM骨干模型上的实验表明，CLAG在保持轻量高效的同时，显著提升了回答质量和系统鲁棒性。
conclusion: CLAG通过结构化的内存管理有效解决了SLM智能体的知识稀释问题，为轻量化智能体的高效记忆检索提供了新方案。
---

## 摘要
大语言模型（LLM）智能体严重依赖外部记忆来支持知识重用和复杂的推理任务。然而，大多数记忆系统将经验存储在单一的全局检索池中，这可能会逐渐稀释或破坏存储的知识。这一问题在小语言模型（SLM）中尤为突出，因为它们极易受到无关上下文的影响。我们提出了 CLAG，一种基于聚类的智能体记忆框架，其中 SLM 智能体通过聚类主动组织记忆。CLAG 采用 SLM 驱动的路由，将传入的记忆分配到语义一致的簇中，并自主生成特定于簇的配置文件（包括主题摘要和描述性标签），从而将每个簇建立为一个自包含的功能单元。通过在这些结构化的邻域内进行局部演化，CLAG 有效地减少了跨主题干扰并增强了内部记忆密度。在检索过程中，该框架利用两阶段过程，首先通过配置文件过滤相关簇，从而排除干扰项并缩小搜索空间。在多个问答数据集上使用三种 SLM 骨干网络进行的实验表明，CLAG 在保持轻量和高效的同时，相比之前的智能体记忆系统，一致地提高了回答质量和鲁棒性。

## Abstract
Large language model agents heavily rely on external memory to support knowledge reuse and complex reasoning tasks. Yet most memory systems store experiences in a single global retrieval pool which can gradually dilute or corrupt stored knowledge. This problem is especially pronounced for small language models (SLMs), which are highly vulnerable to irrelevant context. We introduce CLAG, a CLustering-based AGentic memory framework where an SLM agent actively organizes memory by clustering. CLAG employs an SLM-driven router to assign incoming memories to semantically coherent clusters and autonomously generates cluster-specific profiles, including topic summaries and descriptive tags, to establish each cluster as a self-contained functional unit. By performing localized evolution within these structured neighborhoods, CLAG effectively reduces cross-topic interference and enhances internal memory density. During retrieval, the framework utilizes a two-stage process that first filters relevant clusters via their profiles, thereby excluding distractors and reducing the search space. Experiments on multiple QA datasets with three SLM backbones show that CLAG consistently improves answer quality and robustness over prior memory systems for agents, remaining lightweight and efficient.

---

## 论文详细总结（自动生成）

以下是对论文《CLAG: Adaptive Memory Organization via Agent-Driven Clustering for Small Language Model Agents》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 AI 智能体记忆系统通常将所有交互记录存储在一个“全局检索池”中。随着记忆规模增长，检索空间变大，容易引入语义相似但任务无关的干扰信息（Distractors）。
*   **研究背景**：这一问题在**小语言模型（SLM）**上尤为严重，因为 SLM 对无关上下文的鲁棒性较差，极易受到干扰而导致推理失败。
*   **整体含义**：论文提出了一种名为 **CLAG** 的框架，通过“智能体驱动的聚类”将记忆组织成语义一致的局部邻域，从而减少跨主题干扰，提升 SLM 在长程任务中的记忆质量和检索效率。

### 2. 方法论：核心思想与关键技术
CLAG 的核心思想是将记忆管理从“静态全局存储”转变为“动态结构化组织”，主要包含三个阶段：
*   **智能体驱动路由（Agentic Routing）**：
    *   新记忆进入时，首先通过向量相似度筛选出 Top-K 个候选簇。
    *   由 SLM 智能体根据簇的“语义配置文件”（Profile）决定将其分配到哪个簇，或创建新簇。
    *   **自适应机制**：当某个簇的大小超过阈值时，会自动触发 K-Means 拆分，保持簇的细粒度。
*   **局部化演化（Localized Evolution）**：
    *   记忆的更新、链接生成和压缩仅在所属的簇内进行。
    *   这种设计模仿人类认知中的“图式（Schema）”理论，确保新信息只修正相关的知识块，而不干扰无关领域，提高了记忆密度。
*   **簇感知的两阶段检索（Two-Stage Retrieval）**：
    *   **第一阶段（粗筛）**：SLM 根据查询（Query）评估各簇的配置文件，选择相关的簇子集。
    *   **第二阶段（精选）**：仅在选定的簇内进行细粒度的向量检索。
    *   **意义**：通过预过滤排除了大量潜在的干扰项，显著缩小了搜索空间。

### 3. 实验设计
*   **数据集**：
    *   **LoCoMo**：超长对话记忆基准（平均 300 轮对话，9K Token）。
    *   **HotpotQA**：多跳推理任务，并人工加入了大量干扰文档。
    *   **BioASQ**：生物医学领域专业问答，用于测试领域自适应能力。
*   **骨干模型（Backbones）**：Qwen3-0.6B、Llama-3.2-1B-Instruct、DeepSeek-R1-Distill-Qwen-1.5B。
*   **对比方法（Baselines）**：
    *   标准 RAG。
    *   **A-mem**：基于智能体的动态记忆管理。
    *   **GAM**：通用智能体记忆（基于 Deep Research 思想）。
    *   **MemoryOS**：受操作系统启发的层级记忆架构。

### 4. 资源与算力
*   **硬件**：实验在单张 **NVIDIA A100 GPU** 上进行。
*   **效率指标**：论文详细对比了延迟（Latency）。例如，在 Qwen3-0.6B 上，CLAG 的端到端延迟约为 514ms，远低于 GAM（约 17.9s），略高于标准 RAG（约 290ms）。
*   **训练时长**：文中未明确提及具体的训练时长，因为该方法主要依赖于预训练模型的 Prompt 引导和在线聚类，而非大规模二次微调。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种不同架构的 SLM、3 个跨领域的基准数据集。
*   **分析维度**：
    *   **消融实验**：验证了“局部演化”和“两阶段检索”的各自贡献。
    *   **聚类策略对比**：对比了智能体驱动聚类与纯几何聚类（K-Means/Cosine）的效果。
    *   **分类表现**：针对 LoCoMo 的多跳、时间、对抗等 5 个子类别进行了详细拆解。
*   **评价**：实验设计较为充分且客观，通过跨模型和跨领域的对比证明了框架的普适性，尤其是在资源受限的 SLM 场景下具有很强的说服力。

### 6. 主要结论与发现
*   **性能提升**：CLAG 在所有数据集和模型上均优于现有基准。在 BioASQ 这种专业领域，其检索 F1 分数甚至比基准高出一个数量级。
*   **鲁棒性**：通过两阶段检索，CLAG 有效抑制了干扰项，解决了 SLM 容易被无关上下文“带偏”的痛点。
*   **效率平衡**：CLAG 在保持高精度的同时，计算开销远低于其他复杂的智能体记忆系统（如 GAM），适合端侧部署。

### 7. 优点
*   **结构化创新**：将无序的全局池转变为有序的语义簇，符合认知心理学原理。
*   **针对性强**：精准捕捉到了 SLM 对干扰项敏感的弱点，并给出了轻量级的解决方案。
*   **局部演化**：通过限制更新范围，既保证了记忆的持续进化，又避免了全局计算带来的高昂成本。

### 8. 不足与局限
*   **Prompt 依赖**：系统性能高度依赖于 Prompt 的设计，不同版本的模型可能需要重新调优 Prompt。
*   **分布偏移风险**：如果输入数据出现剧烈的主题漂移，智能体路由的准确性可能会下降。
*   **隐私与治理**：论文未深入讨论长期存储用户记忆带来的隐私保护和数据删除策略（尽管作者在结论中提到了这一点）。
*   **冷启动**：在记忆初期（数据量极小时），聚类结构的建立可能不够稳定。

（完）
