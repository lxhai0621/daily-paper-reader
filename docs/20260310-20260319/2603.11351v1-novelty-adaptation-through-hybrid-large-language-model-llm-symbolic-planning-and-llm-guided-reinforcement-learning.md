---
title: Novelty Adaptation Through Hybrid Large Language Model (LLM)-Symbolic Planning and LLM-guided Reinforcement Learning
title_zh: 通过混合大语言模型 (LLM)-符号规划与 LLM 引导的强化学习实现新颖性适应
authors: "Hong Lu, Pierrick Lorang, Timothy R. Duggan, Jivko Sinapov, Matthias Scheutz"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.11351v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 用于开放世界自主智能体规划与推理的神经符号架构
tldr: 本文提出一种结合符号规划、强化学习与大语言模型（LLM）的神经符号架构，旨在解决自主智能体在动态环境中因缺乏算子而无法应对新颖物体的问题。该方法利用LLM的常识推理识别缺失算子并生成RL奖励函数，从而引导智能体学习新控制策略。实验表明，该方法在机器人领域的算子发现与学习性能上显著优于现有技术。
selection_source: fresh_fetch
motivation: 传统符号规划器在面对缺乏相应操作算子的新颖物体时，无法生成有效的执行计划。
method: 提出一种集成LLM、符号规划和强化学习的架构，利用LLM识别缺失算子并生成RL奖励函数。
result: 该方法在连续机器人领域的算子发现和控制策略学习效率上均超越了当前最先进的方法。
conclusion: 通过结合LLM的常识推理与符号规划及强化学习，能够显著提升智能体在动态环境中的新颖性适应能力。
---

## 摘要
在动态开放世界环境中，自主智能体经常遇到新颖性，这阻碍了它们寻找实现目标计划的能力。具体而言，当机器人的规划领域缺乏使其能够与环境中新颖物体进行适当交互的算子时，传统的符号规划器无法生成计划。我们提出了一种神经符号架构，该架构集成了符号规划、强化学习和大语言模型 (LLM)，以学习如何处理新颖物体。特别是，我们利用 LLM 的常识推理能力来识别缺失的算子，使用符号 AI 规划器生成计划，并编写奖励函数以引导强化学习智能体学习新识别算子的控制策略。我们的方法在连续机器人领域的算子发现和算子学习方面均优于现有最先进的方法。

## Abstract
In dynamic open-world environments, autonomous agents often encounter novelties that hinder their ability to find plans to achieve their goals. Specifically, traditional symbolic planners fail to generate plans when the robot's planning domain lacks the operators that enable it to interact appropriately with novel objects in the environment. We propose a neuro-symbolic architecture that integrates symbolic planning, reinforcement learning, and a large language model (LLM) to learn how to handle novel objects. In particular, we leverage the common sense reasoning capability of the LLM to identify missing operators, generate plans with the symbolic AI planner, and write reward functions to guide the reinforcement learning agent in learning control policies for newly identified operators. Our method outperforms the state-of-the-art methods in operator discovery as well as operator learning in continuous robotic domains.

---

## 论文详细总结（自动生成）

### 论文总结：通过混合 LLM-符号规划与 LLM 引导的强化学习实现新颖性适应

#### 1. 核心问题与研究动机
*   **核心问题**：在动态开放世界中，自主机器人常遇到预定义规划领域（PDDL）中未包含的新颖物体（如新出现的盖子、抽屉）。传统的符号规划器因缺乏相应的操作算子（Operators）而失效，而纯强化学习（RL）在连续动作空间中通过随机探索来发现新算子的效率极低。
*   **研究动机**：利用大语言模型（LLM）蕴含的常识推理能力来识别缺失的算子结构，并生成密集的奖励函数，从而加速机器人对新颖物体的适应过程。

#### 2. 方法论
该论文提出了一种“计划-学习-执行”（Plan-Learn-Execute）循环架构，核心组件包括：
*   **混合 LLM-符号规划器**：
    *   当符号规划失败时，调用 LLM（如 GPT-o3）根据当前状态和目标建议缺失的 PDDL 算子（包含前提条件和效果）。
    *   使用符号规划器进行“前瞻搜索”，验证添加该算子后是否能找到通往目标的路径，从而过滤 LLM 的幻觉。
*   **LLM 引导的子目标学习**：
    *   将算子的效果分解为有序的子目标课程（Curriculum）。
    *   利用 LLM（如 GPT-o4-mini）为每个子目标编写 Python 形式的密集奖励函数。
*   **并行 RL 与启发式淘汰**：
    *   为每个子目标启动多个带有不同奖励函数候选者的 PPO 智能体。
    *   定期评估性能，剔除表现最差的候选者（类似遗传算法），最终保留最优策略作为新算子的执行器。

#### 3. 实验设计
*   **实验场景**：基于 `mimicgen` 模拟环境设置了四个难度递增的机器人操纵任务：
    1.  **厨房（简单）**：处理挡住锅的新盖子。
    2.  **螺母组装（中等）**：在圆柱销上取下螺母。
    3.  **咖啡机-盒子（中等）**：从新盒子中取出咖啡荚。
    4.  **咖啡机-抽屉（困难）**：打开关闭的抽屉并取出咖啡荚。
*   **对比基准（Benchmarks）**：
    *   **Operator Discovery (OD)**：通过随机探索发现可规划状态的传统混合方法。
    *   **LEAGUE-Sparse**：仅在完成所有效果后给予稀疏奖励。
    *   **Reward Machine (RM)**：仅提供子目标完成后的阶梯奖励，无 LLM 引导的密集奖励。

#### 4. 资源与算力
*   **硬件环境**：使用了一台 Ubuntu 20.04 服务器，配置为 24 核 Intel i9-12900K CPU 和 **NVIDIA GeForce RTX 4090 GPU**。
*   **训练细节**：每个算子学习实验运行 10 个随机种子。RL 智能体在每个阶段的探索步数上限设定为 100 万步（针对算子发现）或特定的时间步间隔（针对奖励函数淘汰）。

#### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同领域进行了测试，每个领域运行 10 次实验以确保统计显著性。
*   **充分性评价**：实验设计较为充分，涵盖了从简单到极难（随机探索几乎不可能成功）的梯度。通过 Wilcoxon 符号秩检验验证了所提方法在成功率和进度指标上均显著优于基准方法（p < 0.05）。

#### 6. 主要结论与发现
*   **算子识别**：混合 LLM-符号规划器能 100% 准确识别缺失算子，而传统 OD 方法在复杂任务（如抽屉任务）中完全失败。
*   **学习效率**：LLM 生成的密集奖励函数显著加速了连续控制策略的学习，使智能体在困难任务中达到 90% 以上的成功率。
*   **鲁棒性**：并行启动多个奖励函数候选者并进行淘汰，有效解决了 LLM 生成代码可能存在的错误或低效问题。

#### 7. 优点
*   **神经符号结合**：巧妙地将 LLM 的常识、符号规划的逻辑严谨性和 RL 的底层控制能力结合在一起。
*   **自动化程度高**：实现了从发现问题（规划失败）到解决问题（学习新技能）的闭环，无需人工干预定义新奖励。
*   **解决冷启动问题**：通过 LLM 引导，解决了 RL 在连续空间中因奖励稀疏导致的探索难题。

#### 8. 不足与局限
*   **感知假设**：假设机器人拥有完美的谓词检测能力（如能自动识别“抽屉已打开”），未考虑视觉感知误差。
*   **模拟与现实差距**：实验完全在模拟器中进行，尚未在物理机器人上验证 Sim-to-Real 的迁移效果。
*   **谓词依赖**：方法依赖于现有的谓词集合，如果新颖性超出了现有谓词的描述范围（例如需要发明全新的谓词），该系统可能失效。
*   **成本考虑**：频繁调用高性能 LLM（如 GPT-o3）可能产生较高的 API 使用成本。

（完）
