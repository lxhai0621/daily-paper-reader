---
title: Improving Search Agent with One Line of Code
title_zh: 仅需一行代码改进搜索智能体
authors: "Jian Li, Dongsheng Chen, Zhenhua Xu, Yizhang Jin, Jiafu Wu, Chengjie Wang, Xiaotong Yuan, Yabiao Wang"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.10069v1"
tags: ["query:agent"]
score: 9.0
evidence: 基于工具的智能体强化学习，用于搜索智能体
tldr: "本研究针对工具型强化学习（TARL）中搜索智能体训练不稳定的问题，识别出 GRPO 算法中存在的重要性采样分布偏移（ISDD）现象。为此提出了 SAPO 算法，通过引入条件式 token 级 KL 约束，在不阻断梯度流的前提下防止分布过度偏移。该方法仅需一行代码修改，在多个 QA 基准测试中显著提升了模型性能，相比 Search-R1 获得 10.6% 的绝对增长，展现了极高的实用价值。"
selection_source: fresh_fetch
motivation: 解决工具型强化学习中 GRPO 算法因重要性采样分布偏移（ISDD）导致的训练崩溃和梯度失效问题。
method: 提出 SAPO 算法，通过一行代码实现的条件式 token 级 KL 约束来选择性惩罚分布偏移，确保训练稳定性。
result: "在 7 个 QA 基准测试中，SAPO 相比 Search-R1 实现了 10.6% 的绝对性能提升，并在多种模型规模上表现稳健。"
conclusion: SAPO 证明了通过极简的算法改进即可有效解决强化学习中的分布偏移问题，显著增强了搜索智能体的自主信息检索能力。
---

## 摘要
基于工具的智能体强化学习（TARL）已成为一种极具前景的范式，用于训练搜索智能体自主地与外部工具交互，以进行多轮信息寻取过程。然而，我们发现了一个导致灾难性模型崩溃的关键训练不稳定性：重要性采样分布偏移（ISDD）。在广泛采用的 TARL 算法——群体相对策略优化（GRPO）中，ISDD 表现为重要性采样比率的急剧下降，这会使梯度更新失效并引发不可逆的训练失败。为解决这一问题，我们提出了搜索智能体策略优化（SAPO），通过条件性的 Token 级 KL 约束来稳定训练。与忽略分布差异的硬截断（hard clipping）不同，SAPO 选择性地惩罚当前策略与旧策略之间的 KL 散度。至关重要的是，这种惩罚仅应用于策略发生过度偏移且概率较低的正向 Token，从而在防止分布偏移的同时保留梯度流。值得注意的是，SAPO 仅需对标准 GRPO 进行一行代码修改，确保了即时部署性。在七个问答（QA）基准测试上的广泛实验表明，SAPO 相比 Search-R1 实现了 +10.6% 的绝对提升（+31.5% 相对提升），并在不同模型规模（1.5B、14B）和系列（Qwen、LLaMA）中均获得了一致的增益。

## Abstract
Tool-based Agentic Reinforcement Learning (TARL) has emerged as a promising paradigm for training search agents to interact with external tools for a multi-turn information-seeking process autonomously. However, we identify a critical training instability that leads to catastrophic model collapse: Importance Sampling Distribution Drift(ISDD). In Group Relative Policy Optimization(GRPO), a widely adopted TARL algorithm, ISDD manifests as a precipitous decline in the importance sampling ratios, which nullifies gradient updates and triggers irreversible training failure. To address this, we propose \textbf{S}earch \textbf{A}gent \textbf{P}olicy \textbf{O}ptimization (\textbf{SAPO}), which stabilizes training via a conditional token-level KL constraint. Unlike hard clipping, which ignores distributional divergence, SAPO selectively penalizes the KL divergence between the current and old policies. Crucially, this penalty is applied only to positive tokens with low probabilities where the policy has shifted excessively, thereby preventing distribution drift while preserving gradient flow. Remarkably, SAPO requires only one-line code modification to standard GRPO, ensuring immediate deployability. Extensive experiments across seven QA benchmarks demonstrate that SAPO achieves \textbf{+10.6\% absolute improvement} (+31.5\% relative) over Search-R1, yielding consistent gains across varying model scales (1.5B, 14B) and families (Qwen, LLaMA).

---

## 论文详细总结（自动生成）

### 论文总结：《Improving Search Agent with One Line of Code》

#### 1. 核心问题与整体含义（研究动机和背景）
本文针对**工具型智能体强化学习（TARL）**在训练搜索智能体（Search Agent）时的不稳定性问题展开研究。研究者发现，目前主流的算法（如 DeepSeek 提出的 **GRPO**）在处理多轮搜索任务时，容易陷入**重要性采样分布偏移（ISDD）**的困境。
*   **现象**：随着训练进行，当前策略与旧策略的差异过大，导致重要性采样比率（IS Ratio）趋近于零。
*   **后果**：梯度更新失效，模型无法从高奖励的路径中学习，最终导致训练崩溃或性能退化。
*   **背景**：搜索智能体需要交替进行推理和搜索，这种多步交互放大了分布偏移的风险。

#### 2. 方法论：核心思想与关键技术
论文提出了 **SAPO（Search Agent Policy Optimization）**，其核心思想是通过引入**条件式 Token 级 KL 约束**来替代或补充传统的硬截断（Hard Clipping）机制。
*   **核心公式**：在 GRPO 的目标函数中增加了一个辅助惩罚项 $\gamma KL[\pi_\theta \| \pi_{old}]$。
*   **关键细节（条件触发）**：该 KL 惩罚并非无差别应用，而是具有**选择性**：
    1.  **优势条件**：仅针对优势值 $\hat{A}_t > 0$ 的 Token（即模型认为表现好的动作）。
    2.  **阈值触发**：仅当重要性采样比率 $r_t$ 低于设定阈值 $\tau$（默认 1.0）时触发。
*   **实现方式**：该方法在代码实现上极其简洁，仅需在损失函数计算中增加一行逻辑判断，即可在不阻断梯度流的前提下，强制让当前策略向旧策略的高概率正向动作靠拢，从而防止分布“漂移”过远。

#### 3. 实验设计
*   **数据集/场景**：涵盖了 7 个主流 QA 基准测试，分为两类：
    *   **单跳（Single-hop）**：Natural Questions (NQ), TriviaQA, PopQA。
    *   **多跳（Multi-hop）**：HotpotQA, 2WikiMultihopQA, Musique, Bamboogle。
*   **Benchmark 与对比方法**：
    *   **无检索方法**：直接生成、SFT、R1-Instruct。
    *   **标准 RAG**：Naive RAG, IRCoT。
    *   **搜索智能体**：Search-R1 (Baseline), Search-o1, ReSearch, ZeroSearch, O2-Searcher, AutoRefine, InForage, CriticSearch 等。

#### 4. 资源与算力
*   **框架**：基于 VeRL 强化学习框架实现。
*   **模型规模**：测试了 Qwen2.5 系列（1.5B, 3B, 7B, 14B）以及 LLaMA-3.2-3B。
*   **具体算力说明**：文中**未明确列出**具体的 GPU 型号（如 A100/H100）数量及总训练时长。但提到了训练超参数：Batch Size 为 256，微批次（Micro batch）为 64，总训练步数为 420 步，组采样大小 $G=10$。

#### 5. 实验数量与充分性
*   **实验规模**：实验设计非常充分且具有说服力。
*   **维度覆盖**：
    *   **主实验**：在 7 个数据集上验证了 SAPO 显著优于现有所有 SOTA 方法。
    *   **消融实验**：对比了无 KL、无条件 KL、仅比例约束 KL 以及 SAPO 完整版，证明了“条件触发”的必要性。
    *   **扩展性分析（Scaling Laws）**：验证了从 1.5B 到 14B 模型性能随规模增长的趋势。
    *   **泛化性验证**：在 Qwen 和 LLaMA 两个完全不同的模型家族上均取得了显著提升。
*   **客观性**：通过 Exact Match (EM) 和 F1 分数双重指标评估，且在相同检索器和知识库下进行公平对比。

#### 6. 主要结论与发现
*   **性能飞跃**：SAPO 在 7 个基准测试中平均达到 44.2% 的准确率，相比 Search-R1 实现了 **10.6% 的绝对提升**（相对提升 31.5%）。
*   **多步推理优势**：在复杂的“多跳”任务中提升尤为明显（如 HotpotQA 提升 14.7%），说明 SAPO 有效解决了长序列交互中的训练不稳定性。
*   **稳定性验证**：实验曲线显示，SAPO 能有效维持重要性采样比率在健康区间，避免了 GRPO 后期常见的熵崩溃和奖励下降现象。

#### 7. 优点
*   **极简高效**：仅需“一行代码”修改，无需引入额外的评论家网络（Critic Network）或复杂的奖励模型，部署成本极低。
*   **理论扎实**：深入分析了 ISDD 现象及其对梯度流的影响，针对性地解决了强化学习在 Agent 场景下的核心痛点。
*   **通用性强**：不依赖于特定的模型架构，对 Base 模型和 Instruct 模型均有效。

#### 8. 不足与局限
*   **任务局限性**：实验主要集中在基于文本检索的问答任务（QA），尚未验证其在代码执行、数学证明或多模态工具调用等更广泛 Agent 场景下的表现。
*   **奖励函数依赖**：目前仍依赖于基于规则的奖励（如 F1 分数），对于没有标准答案的开放式搜索任务，如何定义有效的奖励信号仍是挑战。
*   **超参数敏感性**：虽然论文给出了推荐值 $\tau=1.0$，但在极端不同的任务分布下，该阈值可能仍需微调。

（完）
