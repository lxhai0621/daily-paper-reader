---
title: "POaaS: Minimal-Edit Prompt Optimization as a Service to Lift Accuracy and Cut Hallucinations on On-Device sLLMs"
title_zh: POaaS：旨在提升端侧小语言模型准确率并减少幻觉的最小编辑提示词优化服务
authors: "Jungwoo Shim, Dae Won Kim, Sun Wook Kim, Soo Young Kim, Myungcheol Lee, Jae-geun Cha, Hyunhwa Choi"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16045v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 针对小模型的自动提示词优化以减少幻觉
tldr: 针对端侧小模型因用户输入不规范导致的幻觉问题，本文提出POaaS。这是一种最小编辑提示词优化服务，通过轻量级专家模块在严格约束下优化输入。相比传统搜索型APO，POaaS在保持低开销的同时显著提升了sLLM的准确率和事实性，是端侧模型优化的实用方案。
selection_source: fresh_fetch
motivation: 现有的提示词优化方法生成的冗长指令在端侧小模型上会浪费上下文并降低准确率。
method: 利用轻量级专家模块对查询进行清理、改写和事实补充，并结合保守的跳过策略和长度约束。
result: "在Llama-3.2-3B等模型上，POaaS显著提升了准确率和事实性，在干扰场景下最高恢复了7.4%的性能。"
conclusion: 针对端侧sLLM，基于单次查询的保守优化比重型搜索类APO更具实践价值。
---

## 摘要
小语言模型 (sLLMs) 越来越多地部署在端侧，而不完善的用户提示词（如拼写错误、意图不明或上下文缺失）可能会引发事实错误和幻觉。现有的自动提示词优化 (APO) 方法是为云端大语言模型设计的，依赖于通常会产生冗长、结构化指令的搜索；当在端侧受限环境下执行时，即同一个小模型必须同时充当优化器和求解器，这些流水线可能会浪费上下文，甚至损害准确率。我们提出了 POaaS，这是一个最小编辑提示词优化层，它将每个查询路由到轻量级专家（清洗器 Cleaner、改写器 Paraphraser、事实添加器 Fact-Adder），并在严格的偏移和长度约束下合并它们的输出，同时对格式良好的提示词采用保守的跳过策略。在 Llama-3.2-3B-Instruct 和 Llama-3.1-8B-Instruct 的严格固定模型设置下，POaaS 提升了任务准确率和事实性，而代表性的 APO 基准方法反而使其下降；在 Token 删除和混淆的情况下，POaaS 的恢复率最高可达 +7.4%。总的来说，对于端侧 sLLM，针对每个查询的保守优化是重搜索型 APO 的一种实用替代方案。

## Abstract
Small language models (sLLMs) are increasingly deployed on-device, where imperfect user prompts--typos, unclear intent, or missing context--can trigger factual errors and hallucinations. Existing automatic prompt optimization (APO) methods were designed for large cloud LLMs and rely on search that often produces long, structured instructions; when executed under an on-device constraint where the same small model must act as optimizer and solver, these pipelines can waste context and even hurt accuracy. We propose POaaS, a minimal-edit prompt optimization layer that routes each query to lightweight specialists (Cleaner, Paraphraser, Fact-Adder) and merges their outputs under strict drift and length constraints, with a conservative skip policy for well-formed prompts. Under a strict fixed-model setting with Llama-3.2-3B-Instruct and Llama-3.1-8B-Instruct, POaaS improves both task accuracy and factuality while representative APO baselines degrade them, and POaaS recovers up to +7.4% under token deletion and mixup. Overall, per-query conservative optimization is a practical alternative to search-heavy APO for on-device sLLMs.

---

## 论文详细总结（自动生成）

### 论文总结：POaaS —— 针对端侧小语言模型的最小编辑提示词优化服务

#### 1. 核心问题与研究动机
*   **背景**：随着小语言模型（sLLMs）在手机、浏览器等端侧设备的广泛部署，用户输入的非规范性（如拼写错误、意图模糊、上下文缺失）成为影响模型表现的关键因素，极易引发事实错误和“幻觉”。
*   **核心问题**：现有的自动提示词优化（APO）方法（如 OPRO、PromptWizard）主要为云端大模型设计，依赖重型迭代搜索，生成的指令冗长且结构复杂。当这些方法应用于端侧受限环境（即由小模型自身担任优化器和求解器）时，往往会浪费上下文窗口，甚至因指令过载导致准确率下降。
*   **研究动机**：开发一种轻量级、实时、保守的优化层，在不引入重型搜索的前提下，通过微调提示词来提升 sLLM 的鲁棒性和事实性。

#### 2. 方法论：POaaS 核心思想与技术细节
POaaS 在用户提示词和目标模型之间插入了一个“微服务”风格的优化层，核心流程如下：
*   **提示词分析与路由（CPU 启发式）**：利用轻量级算法计算输入提示词的拼写错误（Typo）、完整性（Completeness）、流畅度（Fluency）和清晰度（Clarity）得分。
*   **保守跳过策略（Skip Logic）**：如果提示词质量已足够高，系统将直接跳过优化，以避免不必要的重写导致意图偏移。
*   **角色化专家模块（Specialists）**：
    *   **Cleaner**：修复拼写和语法错误。
    *   **Paraphraser**：提升表达的清晰度和流畅度。
    *   **Fact-Adder**：识别关键实体并添加简洁的事实背景（最多3条，总计不超过120 tokens）。
*   **漂移控制与合并（Drift-Controlled Merging）**：
    *   使用词法相似度（如 Jaccard 相似度、SequenceMatcher）计算编辑前后的“漂移量”。
    *   设定严格的长度比例限制（$\rho_{max} = 2.4$）和漂移阈值，确保优化后的提示词不偏离用户原始意图。
    *   采用分层防御机制（Guardrails）过滤掉可能包含答案泄露或误导性信息的编辑。

#### 3. 实验设计
*   **数据集与场景**：
    *   **任务准确率**：BBH（综合推理）、GSM8K（数学推理）、CommonsenseQA（常识问答）。
    *   **事实性/幻觉**：HaluEval、HalluLens、FActScore。
    *   **输入降级场景**：模拟真实人类错误，对输入进行 5%、10%、15% 的 Token 删除（Deletion）和随机混淆（Mixup）。
*   **Benchmark 与对比方法**：
    *   **对比方法**：No Optimization（原始输入）、EvoPrompt、OPRO、PromptWizard。
    *   **实验设置**：所有方法均在相同的 3B/8B 模型上运行，即 APO 方法必须使用小模型作为其内部的优化器和评估器。
*   **目标模型**：Llama-3.2-3B-Instruct 和 Llama-3.1-8B-Instruct。

#### 4. 资源与算力
*   **训练阶段**：专家模块使用 LoRA 适配器在冻结的 Llama 后端上微调（r=16, α=32），训练 3 个 epoch。
*   **推理阶段**：
    *   **CPU**：负责启发式评分、路由逻辑和漂移检查。
    *   **GPU**：负责运行 vLLM 驱动的专家 LoRA 模块。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号和数量，但强调了其设计目标是“端侧友好”。在 3B 模型测试中，POaaS 平均每条查询增加约 0.95 秒的延迟，注入约 48 个额外 Token。

#### 5. 实验数量与充分性
*   **实验规模**：每个基准测试随机采样 500 个样本，总计涵盖了 6 个主流基准数据集。
*   **维度覆盖**：涵盖了干净输入和 6 种不同强度的噪声干扰，实验维度完整。
*   **消融实验**：详细测试了去掉 Cleaner、Paraphraser 或 Fact-Adder 后的表现，验证了各模块的互补性。
*   **公平性**：通过固定解码参数（Temperature=0.2, Top-p=0.9）和相同的生成预算（512 tokens），确保了对比的客观性。

#### 6. 主要结论与发现
*   **性能提升**：在干净输入下，POaaS 稳定提升了准确率和事实性；而传统的 APO 方法在小模型上普遍表现不佳，甚至导致性能大幅下降（如 PromptWizard 在 BBH 任务上使准确率从 42.2% 降至 10.4%）。
*   **鲁棒性恢复**：在输入存在 15% 噪声的情况下，POaaS 相比无优化基准最高恢复了 **7.4%** 的准确率，证明了其在处理非规范输入方面的卓越能力。
*   **效率优势**：相比 APO 方法需要数分钟的离线搜索时间，POaaS 的在线优化开销极低，且增加的 Token 数量远少于 APO 生成的长指令。

#### 7. 优点
*   **容量感知设计**：充分考虑了小模型的计算限制，避免了复杂指令带来的负面影响。
*   **最小编辑原则**：通过漂移控制和保守跳过策略，极大地降低了优化过程中的意图丢失风险。
*   **即插即用**：作为服务层存在，无需对目标模型进行重新训练或针对特定任务进行离线搜索。

#### 8. 不足与局限
*   **语言局限性**：目前仅针对英语进行了调优，多语言支持（如中韩日文）需要重新构建启发式评分和专家训练集。
*   **阈值敏感性**：路由和漂移控制的阈值主要依靠经验设定，可能需要针对不同模型进行微调。
*   **专家覆盖面**：目前的三个专家模块虽然通用，但在特定专业领域（如医疗、法律）可能需要更具针对性的知识插件。

（完）
