---
title: "One Supervisor, Many Modalities: Adaptive Tool Orchestration for Autonomous Queries"
title_zh: 一个主管，多种模态：面向自主查询的自适应工具编排
authors: "Mayank Saini, Arit Kumar Bishwas"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11545v2"
tags: ["query:ma-kf"]
score: 9.0
evidence: 用于自主多模态查询处理和工具编排的智能体AI框架
tldr: 本研究提出了一种自主多模态查询处理的智能体框架，通过中央监督者动态分解任务并协调文本、图像、音频等多种模态工具。该框架利用自适应路由策略而非预设决策树，显著提升了处理效率并降低了成本，为多模态AI部署提供了高效的编排方案。
selection_source: fresh_fetch
motivation: 旨在解决多模态查询处理中工具协调复杂、效率低下及成本高昂的问题。
method: 采用中央监督者架构，结合RouteLLM和SLM辅助的模态分解，实现动态任务委派与结果合成。
result: "在2847个查询测试中，该框架减少了72%的响应时间、85%的重复工作和67%的成本，且保持了准确率。"
conclusion: 智能化的中央编排机制能从根本上优化多模态AI部署的经济性与运行效率。
---

## 摘要
我们提出了一种用于自主多模态查询处理的智能体 AI 框架，该框架协调了涵盖文本、图像、音频、视频和文档模态的专业工具。一个核心主管（Supervisor）动态地分解用户查询，将子任务委派给适合相应模态的工具（例如目标检测、OCR、语音转录），并通过自适应路由策略而非预定义的决策树来综合结果。对于纯文本查询，该框架通过 RouteLLM 使用学习型路由；而对于非文本路径，则使用 SLM 辅助的模态分解。在涵盖 15 个任务类别的 2,847 个查询上进行的评估显示，与匹配的分层基准相比，我们的框架在保持准确率相当的情况下，获得准确答案的时间缩短了 72%，对话返工减少了 85%，成本降低了 67%。这些结果表明，智能化的集中编排从根本上改善了多模态 AI 部署的经济性。

## Abstract
We present an agentic AI framework for autonomous multimodal query processing that coordinates specialized tools across text, image, audio, video, and document modalities. A central Supervisor dynamically decomposes user queries, delegates subtasks to modality-appropriate tools (e.g., object detection, OCR, speech transcription), and synthesizes results through adaptive routing strategies rather than predetermined decision trees. For text-only queries, the framework uses learned routing via RouteLLM, while non-text paths use SLM-assisted modality decomposition. Evaluated on 2,847 queries across 15 task categories, our framework achieves 72% reduction in time-to-accurate-answer, 85% reduction in conversational rework, and 67% cost reduction compared to the matched hierarchical baseline while maintaining accuracy parity. These results demonstrate that intelligent centralized orchestration fundamentally improves multimodal AI deployment economics.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为“One Supervisor, Many Modalities”的智能体 AI 框架，旨在解决多模态查询处理中的效率与成本平衡问题。以下是对该论文的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：现代 AI 系统需要在处理复杂多模态请求（文本、图像、音视频、文档）的同时，兼顾**运行成本**和**实时响应速度**。
*   **现有方案缺陷**：
    *   **单体大模型（Monolithic LLMs）**：如 GPT-4，虽然强大但对简单任务而言成本过高。
    *   **分层路由（Hierarchical Routing）**：依赖预设的决策树，极其脆弱，一旦查询偏离预期模式就会导致系统崩溃或需要昂贵的全局重启。
*   **研究动机**：开发一种能够自主分解任务、动态委派工具并具备局部修复能力的集中式编排框架，以取代僵化的预设逻辑。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：引入一个中央“监督者”（Supervisor），它不依赖固定路径，而是根据工具的**类型化接口规范**（输入/输出签名、前置/后置条件、延迟先验）动态构建执行图。
*   **关键技术细节**：
    *   **状态管理（State Management）**：基于 LangGraph 的 `StateGraph` 模型，使用结构化状态对象 `Squery` 封装查询、成本层级、上下文、附件和会话元数据，确保智能体切换时信息零损耗。
    *   **两阶段查询分解**：
        1.  **启发式检测**：通过文件扩展名和 MIME 类型识别附件模态。
        2.  **语义分析**：利用轻量级 SLM（如 Phi-3.5-mini）预测执行标签（如音频、视觉、复杂任务等）。
    *   **自适应路由策略**：
        *   **文本路径**：集成 RouteLLM，在强模型（GPT-4o）和弱模型（如 LLaMA-3）之间进行成本感知路由。
        *   **非文本路径（Couplet Framework）**：将传统感知模型（如 YOLO、CLIP、Whisper）与 SLM 结合。感知模型负责高效解码，SLM 负责上下文合成。
    *   **五层存储架构**：包括短期记忆、全历史、模态特定记忆、相关上下文（向量检索）和压缩上下文，实现跨模态的信息检索与整合。
    *   **局部修复机制**：当某个工具失败时，监督者能动态选择替代工具或请求用户澄清，而非重启整个流水线。

### 3. 实验设计
*   **数据集/场景**：使用了涵盖 15 个任务类别的 **2,847 个查询**，包括 MMLU（文本推理）、VQA-v2（视觉问答）、PDF 文档分析、音频转录、视频分析等。
*   **基准测试（Benchmark）**：
    *   **Monolithic LLM**：全量使用 GPT-4 处理。
    *   **Hierarchical Routing**：基于预设决策树的匹配分层基准。
    *   **主流框架**：如 AutoGen 和 LangGraph 的标准实现。
*   **对比指标**：获得准确答案的时间（TTA）、对话返工率（Rework Rate）、每查询成本、吞吐量和准确率。

### 4. 资源与算力
*   **模型使用**：文中提到了 GPT-4o、Claude-3.5、LLaMA-3-70B、Mixtral-8x7B、Phi-3.5-mini 以及专用模型 YOLO、CLIP、Whisper、Tesseract。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。实验主要集中在推理侧的编排效率，而非从头训练大模型。

### 5. 实验数量与充分性
*   **实验规模**：2,847 组实验样本对于评估编排框架而言非常充分。
*   **消融实验**：论文进行了详细的消融研究，分别验证了记忆层、验证机制、并行执行和 Couplet Framework 对系统性能的贡献。
*   **客观性**：通过计算 p 值（如 p<0.001）证明了结果的统计显著性，并使用了 95% 的威尔逊置信区间（Wilson CI），实验设计严谨、公平。

### 6. 主要结论与发现
*   **效率大幅提升**：相比分层基准，TTA（响应时间）缩短了 **72%**，对话返工减少了 **85%**。
*   **成本显著降低**：通过智能路由和专用工具替代昂贵的 LLM 推理，成本降低了 **67%**。
*   **性能保持**：在大幅优化效率的同时，准确率保持在 ±1% 的误差范围内（约 99.2%）。
*   **感知任务优势**：使用专用模型（如 YOLO）处理视觉任务的速度（180ms/帧）远快于端到端 LLM 视觉方案（2.4s/帧）。

### 7. 优点
*   **动态性与鲁棒性**：摆脱了硬编码的路由逻辑，能够处理未预见的查询模式和模态组合。
*   **Couplet 架构创新**：巧妙结合了传统 CV/语音模型的高效性和 LLM 的推理能力，实现了“术业有专攻”。
*   **精细的记忆管理**：五层记忆架构有效解决了长对话中的上下文丢失和跨模态干扰问题。
*   **局部修复**：显著提升了用户体验，减少了因小错误导致的用户重复输入。

### 8. 不足与局限
*   **编排开销**：虽然减少了执行时间，但监督者本身的推理（尤其是多次调用 SLM 进行分解和验证）在极高吞吐量场景下可能成为瓶颈。
*   **工具集成门槛**：目前 Couplet Framework 支持的模型集是固定的，扩展新工具仍需一定的开发工作。
*   **路由保守性**：在某些模糊场景下，系统可能会为了保险而过度升级到昂贵模型，导致成本优化未达极限。
*   **长会话压力**：随着会话极度延长，记忆检索和压缩的计算开销会显著增加。

（完）
