---
title: "TRUST-SQL: Tool-Integrated Multi-Turn Reinforcement Learning for Text-to-SQL over Unknown Schemas"
title_zh: TRUST-SQL：面向未知模式下 Text-to-SQL 的工具集成多轮强化学习
authors: "Ai Jian, Xiaoyun Zhang, Wanrou Du, Jingqing Ruan, Jiangbo Pei, Weipeng Zhang, Ke Zeng, Xunliang Cai"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16448v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 用于在未知数据库模式上进行推理的工具集成自主智能体
tldr: 针对现实中数据库模式庞大且未知的挑战，本文提出TRUST-SQL框架。该框架将Text-to-SQL建模为部分可观测马尔可夫决策过程，通过四阶段协议引导智能体主动探索元数据。结合创新的双轨GRPO强化学习策略，有效解决了信用分配问题。实验表明，该方法在无需预加载模式的情况下，性能显著优于基础模型，并能媲美甚至超越依赖模式预填的强基准模型。
selection_source: fresh_fetch
motivation: 传统的Text-to-SQL研究假设已知完整模式，但在处理包含海量噪声元数据的真实企业级数据库时，这种假设并不成立。
method: 提出一种集成工具的四阶段协议，并采用双轨GRPO策略，通过令牌级掩码优势将探索奖励与执行结果分离，优化强化学习过程。
result: "在五个基准测试中，4B和8B模型变体分别实现了30.6%和16.6%的平均绝对性能提升，且在未知模式下表现优于部分已知模式的基准。"
conclusion: TRUST-SQL证明了通过结构化工具集成和改进的强化学习策略，智能体可以在不预知数据库模式的情况下实现高效且准确的SQL生成。
---

## 摘要
Text-to-SQL 解析在“全模式假设”下取得了显著进展。然而，这一前提在现实世界的企业环境中往往失效，因为那里的数据库包含数百张表以及海量的噪声元数据。智能体必须主动识别并验证相关的子集，而不是预先注入完整的模式，这便产生了我们在本研究中探讨的“未知模式”场景。为此，我们提出了 TRUST-SQL（通过工具在未知模式下进行真实推理）。我们将该任务建模为一个部分可观测马尔可夫决策过程（POMDP），其中我们的自主智能体采用结构化的四阶段协议，将推理建立在经过验证的元数据之上。至关重要的是，该协议为我们新颖的双轨 GRPO（Dual-Track GRPO）策略提供了结构边界。通过应用 Token 级的掩码优势，该策略将探索奖励与执行结果隔离开来，以解决信用分配问题，相比标准 GRPO 实现了 9.9% 的相对提升。在五个基准测试上的广泛实验表明，TRUST-SQL 的 4B 和 8B 变体相较于其基础模型，平均绝对提升分别达到了 30.6% 和 16.6%。值得注意的是，尽管在完全没有预加载元数据的情况下运行，我们的框架仍能持续匹配或超越依赖模式预填充的强基准模型。

## Abstract
Text-to-SQL parsing has achieved remarkable progress under the Full Schema Assumption. However, this premise fails in real-world enterprise environments where databases contain hundreds of tables with massive noisy metadata. Rather than injecting the full schema upfront, an agent must actively identify and verify only the relevant subset, giving rise to the Unknown Schema scenario we study in this work. To address this, we propose TRUST-SQL (Truthful Reasoning with Unknown Schema via Tools). We formulate the task as a Partially Observable Markov Decision Process where our autonomous agent employs a structured four-phase protocol to ground reasoning in verified metadata. Crucially, this protocol provides a structural boundary for our novel Dual-Track GRPO strategy. By applying token-level masked advantages, this strategy isolates exploration rewards from execution outcomes to resolve credit assignment, yielding a 9.9% relative improvement over standard GRPO. Extensive experiments across five benchmarks demonstrate that TRUST-SQL achieves an average absolute improvement of 30.6% and 16.6% for the 4B and 8B variants respectively over their base models. Remarkably, despite operating entirely without pre-loaded metadata, our framework consistently matches or surpasses strong baselines that rely on schema prefilling.

---

## 论文详细总结（自动生成）

以下是对论文《TRUST-SQL: Tool-Integrated Multi-Turn Reinforcement Learning for Text-to-SQL over Unknown Schemas》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
传统的 Text-to-SQL 研究大多基于**“全模式假设”（Full Schema Assumption）**，即假设数据库的所有表结构（Schema）已预先加载到模型的上下文窗口中。然而，在现实企业环境中，数据库往往包含数百张表和海量噪声元数据，且模式经常变动。预加载所有模式不仅超出上下文限制，还会引入干扰导致模型产生幻觉。
本文提出了**“未知模式”（Unknown Schema）**设定，要求智能体像人类专家一样，通过与数据库交互主动探索、检索并验证必要的元数据，从而完成 SQL 生成。

### 2. 方法论：核心思想与关键技术
论文提出了 **TRUST-SQL** 框架，其核心由两部分组成：
*   **四阶段交互协议（EPGC）**：将任务建模为部分可观测马尔可夫决策过程（POMDP）。
    1.  **Explore（探索）**：调用工具查询数据库元数据。
    2.  **Propose（提议）**：强制性的认知检查点，要求智能体提交已验证的模式片段，以抑制幻觉。
    3.  **Generate（生成）**：基于验证后的模式生成候选 SQL 并观察执行结果。
    4.  **Confirm（确认）**：提交最终 SQL 答案。
*   **双轨 GRPO（Dual-Track GRPO）**：针对多轮交互中的**信用分配（Credit Assignment）**难题，改进了 DeepSeek 的 GRPO 算法。
    *   **双轨优化**：将轨迹分为“模式轨道”（优化元数据检索质量）和“全轨道”（优化最终执行准确率）。
    *   **令牌级掩码优势（Token-level Masked Advantages）**：通过掩码技术，确保探索奖励仅影响探索阶段的 Token，执行奖励影响全局 Token，从而实现模式接地（Grounding）与 SQL 生成的协同优化。

### 3. 实验设计
*   **数据集**：
    *   **BIRD-Dev**：大规模、跨领域的复杂数据库基准。
    *   **Spider-Test**：标准 Text-to-SQL 基准。
    *   **鲁棒性变体**：Spider-Syn（同义词替换）、Spider-DK（隐性领域知识）、Spider-Realistic（消除歧义）。
    *   **Spider 2.0 (SQLite)**：更具挑战性的企业级复杂场景。
*   **对比方法**：
    *   **单轮模型**：OmniSQL, SQL-R1。
    *   **多轮强化学习模型**：MTIR-SQL, SQL-Trail。
    *   **基础模型**：Qwen3-4B/8B。
*   **评估指标**：执行准确率（Execution Accuracy, EX%）。

### 4. 资源与算力
论文详细披露了训练配置：
*   **硬件**：使用 NVIDIA A100 GPU。
*   **训练阶段**：
    *   **SFT 阶段**：16张 A100，耗时 6.5（4B）至 12（8B）小时。
    *   **RL 阶段**：4B 模型使用 8张 A100（同步模式），8B 模型使用 32张 A100（异步模式），耗时 40-60 小时。
*   **数据量**：SFT 阶段约 9.2k 样本，RL 阶段约 11.6k 样本。

### 5. 实验数量与充分性
论文进行了多维度的实验，具有较高的充分性和客观性：
*   **主实验**：在 5 个标准基准上对比了不同参数规模的模型。
*   **消融实验**：验证了 λ 参数（平衡探索与生成）、奖励函数设计（耦合 vs 解耦）、SFT 预热的必要性。
*   **测试时缩放分析**：探讨了训练/推理轮数预算对性能的影响，以及 Pass@K 随采样次数的增长趋势。
*   **案例研究**：通过具体实例对比了“未知模式”与“模式预填”下的推理逻辑差异。

### 6. 主要结论与发现
*   **性能飞跃**：在未知模式下，TRUST-SQL 4B 和 8B 变体相较于基础模型分别实现了 **30.6% 和 16.6%** 的平均绝对提升。
*   **超越基准**：尽管 TRUST-SQL 不预加载任何模式，其表现仍能匹配或超越那些拥有“全模式”特权的强基准模型。
*   **双轨优势**：Dual-Track GRPO 相比标准 GRPO 在 BIRD-Dev 上实现了 9.9% 的相对性能提升。
*   **模式预填的局限**：实验发现，对于训练后的 TRUST-SQL，预填模式反而可能因引入噪声而导致性能轻微下降。

### 7. 优点
*   **现实针对性强**：直接解决企业级数据库中模式不可知、规模巨大的痛点。
*   **算法创新**：双轨 GRPO 有效解决了长轨迹强化学习中的信用分配问题，避免了探索与生成信号的混淆。
*   **高效率**：相比于 CHESS 等依赖复杂 Prompt 工程的方法，TRUST-SQL 在推理延迟和 Token 消耗上降低了百倍以上。
*   **抑制幻觉**：通过 Propose 阶段的强制验证，将幻觉率降低了约 9.4 倍。

### 8. 不足与局限
*   **推理开销**：多轮交互自然比单轮翻译产生更高的延迟和成本（尽管已大幅优化）。
*   **方言限制**：目前主要针对 SQLite 进行评估，尚未在 PostgreSQL 或 MySQL 等其他 SQL 方言上广泛验证。
*   **固定步数**：训练时使用了固定的最大轮数（10轮），对于极端复杂的数据库，可能需要更动态的步数调整机制。
*   **依赖 SFT 预热**：实验表明，如果没有高质量的 SFT 数据引导，强化学习容易陷入“奖励作弊”（如一次性查询所有表）。

（完）
