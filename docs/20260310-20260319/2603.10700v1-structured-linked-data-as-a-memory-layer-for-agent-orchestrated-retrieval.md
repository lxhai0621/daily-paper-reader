---
title: Structured Linked Data as a Memory Layer for Agent-Orchestrated Retrieval
title_zh: 结构化链接数据作为智能体编排检索的记忆层
authors: "Andrea Volpini, Elie Raad, Beatrice Gamba, David Riccitelli"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10700v1"
tags: ["query:ma-kf"]
score: 10.0
evidence: 将结构化链接数据和知识图谱集成到智能体RAG中
tldr: 本研究探讨了结构化链接数据在增强检索增强生成（RAG）系统中的作用。针对传统RAG忽略文档间结构化元数据和链接关系的问题，作者提出了一种增强型实体页面格式，结合了Schema.org标记、llms.txt风格指令和多跳链接遍历。实验表明，这种方法在多个领域显著提升了检索准确率和回答质量，为智能体驱动的知识检索提供了更有效的内存层。
selection_source: fresh_fetch
motivation: 传统的RAG系统通常将文档视为扁平文本，忽略了知识图谱提供的结构化元数据和实体间的链接关系。
method: 研究通过对比实验测试了三种文档表示形式和两种检索模式，重点评估了包含导航辅助和实体互联的增强型智能体优化实体页面。
result: "增强型实体页面格式使标准RAG的准确率提升了29.6%，全智能体流水线的准确率提升了29.8%，且在准确性和完整性上得分最高。"
conclusion: 结构化链接数据和优化的实体页面设计能显著增强智能体在复杂检索任务中的推理能力和信息获取质量。
---

## 摘要
检索增强生成 (RAG) 系统通常将文档视为扁平文本，忽略了知识图谱提供的结构化元数据和链接关系。在本文中，我们研究了结构化链接数据（特别是 Schema.org 标记和由链接数据平台提供的可解引用实体页面）是否能提高标准和智能体化 RAG 系统的检索准确率和回答质量。我们在四个领域（编辑、法律、旅游、电子商务）进行了受控实验，使用 Vertex AI Vector Search 2.0 进行检索，并使用 Google 智能体开发工具包 (ADK) 进行智能体推理。我们的实验设计测试了七种条件：三种文档表示形式（纯 HTML、带有 JSON-LD 的 HTML 以及增强型智能体优化实体页面）与两种检索模式（标准 RAG 和具有多跳链接遍历的智能体化 RAG）交叉，外加一个增加了丰富导航示能 (navigational affordances) 和实体互联的 Enhanced+ 条件。结果显示，虽然仅靠 JSON-LD 标记带来的提升有限，但我们结合了 llms.txt 风格智能体指令、面包屑导航和神经搜索能力的增强型实体页面格式取得了显著收益：标准 RAG 的准确率提升了 29.6%，完整智能体流水线的准确率提升了 29.8%。具有更丰富导航示能的 Enhanced+ 变体获得了最高的绝对分数（准确率：4.85/5，完整性：4.55/5），尽管其相对于基础增强格式的增量收益在统计上并不显著。我们发布了数据集、评估框架和增强型实体页面模板，以支持研究的可复现性。

## 速览
**TLDR**：本研究探讨了结构化链接数据在增强检索增强生成（RAG）系统中的作用。针对传统RAG忽略文档间结构化元数据和链接关系的问题，作者提出了一种增强型实体页面格式，结合了Schema.org标记、llms.txt风格指令和多跳链接遍历。实验表明，这种优化的内存层在多个领域显著提升了检索准确率和回答质量，为智能体驱动的知识检索提供了新范式。 \
**Motivation**：传统的RAG系统通常将文档视为扁平文本，忽略了知识图谱提供的结构化元数据和实体间的链接关系。 \
**Method**：通过对比实验测试了三种文档表示（HTML、JSON-LD、增强型实体页面）和两种检索模式（标准RAG与多跳智能体检索）。 \
**Result**：增强型实体页面格式使标准RAG的准确率提升了29.6%，智能体流水线的准确率提升了29.8%，达到最高4.85/5的评分。 \
**Conclusion**：结构化链接数据和优化的实体页面设计能显著增强智能体在复杂检索任务中的推理能力和信息获取质量。

---

## Abstract
Retrieval-Augmented Generation (RAG) systems typically treat documents as flat text, ignoring the structured metadata and linked relationships that knowledge graphs provide. In this paper, we investigate whether structured linked data, specifically Schema.org markup and dereferenceable entity pages served by a Linked Data Platform, can improve retrieval accuracy and answer quality in both standard and agentic RAG systems. We conduct a controlled experiment across four domains (editorial, legal, travel, e-commerce) using Vertex AI Vector Search 2.0 for retrieval and the Google Agent Development Kit (ADK) for agentic reasoning. Our experimental design tests seven conditions: three document representations (plain HTML, HTML with JSON-LD, and an enhanced agentic-optimized entity page) crossed with two retrieval modes (standard RAG and agentic RAG with multi-hop link traversal), plus an Enhanced+ condition that adds rich navigational affordances and entity interlinking. Our results reveal that while JSON-LD markup alone provides only modest improvements, our enhanced entity page format, incorporating llms.txt-style agent instructions, breadcrumbs, and neural search capabilities, achieves substantial gains: +29.6% accuracy improvement for standard RAG and +29.8% for the full agentic pipeline. The Enhanced+ variant, with richer navigational affordances, achieves the highest absolute scores (accuracy: 4.85/5, completeness: 4.55/5), though the incremental gain over the base enhanced format is not statistically significant. We release our dataset, evaluation framework, and enhanced entity page templates to support reproducibility.

---

## 论文详细总结（自动生成）

### 论文总结：结构化链接数据作为智能体编排检索的记忆层

#### 1. 核心问题与整体含义
*   **研究动机**：当前的检索增强生成（RAG）系统普遍将网页或文档视为“扁平文本”（Flat Text），在向量化过程中忽略了网页中嵌入的结构化元数据（如 Schema.org）和实体间的链接关系。
*   **核心问题**：论文探讨了如何利用**结构化链接数据**（Structured Linked Data）作为智能体的“外部记忆层”，以解决传统 RAG 在处理复杂、多跳（Multi-hop）查询时准确性不足和上下文缺失的问题。
*   **背景**：随着 AI 驱动的搜索引擎（如 Google AI Mode）兴起，优化内容以供智能体理解和推理（即 SEO 3.0 或生成引擎优化 GEO）变得至关重要。

#### 2. 方法论
论文提出了一种将知识图谱（KG）与智能体推理相结合的架构，核心思想是**“链接物质化”**（Link Materialization）：
*   **增强型实体页面（Enhanced Entity Page）**：设计了一种专门为 LLM 智能体优化的页面格式，包含：
    *   **自然语言摘要**：基于结构化数据生成。
    *   **嵌入式 JSON-LD**：提供完整的 Schema.org 类型定义。
    *   **可见的实体导航**：包含可解引用的 URI，方便智能体跳转。
    *   **llms.txt 风格指令**：为智能体提供显式的操作指南。
    *   **面包屑导航与神经搜索技能参考**：增强层级上下文。
*   **智能体编排（Agentic RAG）**：基于 Google ADK 构建，采用 ReAct 循环，具备三种工具：
    1.  `search_documents`：传统的向量搜索。
    2.  `follow_entity_link`：通过 HTTP 内容协商获取链接实体的 JSON-LD 数据。
    3.  `search_knowledge_graph`：直接对知识图谱进行神经搜索。

#### 3. 实验设计
*   **场景与数据集**：涵盖四个垂直领域：编辑（WordLift 博客）、法律（Express Legal Funding）、旅游（SalzburgerLand）、电子商务（BlackBriar）。共包含 158 个实体和 349 个测试查询。
*   **对比方法（7 种条件）**：
    *   **C1-C3（标准 RAG）**：分别测试纯 HTML、HTML+JSON-LD、增强实体页面。
    *   **C4-C6（智能体 RAG）**：在上述三种格式上运行具备链接遍历能力的智能体。
    *   **C6+（Enhanced+）**：在 C6 基础上增加更丰富的导航示能（如 Wikidata 风格的属性表）。
*   **Benchmark**：以基于知识图谱生成的 Ground Truth 为基准，评估准确性（Accuracy）、完整性（Completeness）和接地性（Grounding）。

#### 4. 资源与算力
*   **算力说明**：论文未明确列出具体的 GPU 型号或训练时长。
*   **技术栈**：实验完全在 **Google Cloud** 上运行。使用了 **Vertex AI Vector Search 2.0** 作为检索后端，**Gemini 2.5 Flash** 用于答案生成，**Gemini 3.0 Flash** 作为独立评委进行自动化评估。

#### 5. 实验数量与充分性
*   **实验规模**：共进行了 **2,443 次** 独立的查询评估。
*   **充分性**：实验设计较为严谨，采用了 3x2 因子设计，并进行了统计显著性检验（配对 t 检验及 Bonferroni 校正）。
*   **客观性**：通过跨四个不同特征的领域（如电商领域事实丰富，旅游领域关系复杂）来验证泛化性，实验结果显示了不同领域下结构化数据的不同贡献度，具有较高的客观性。

#### 6. 主要结论与发现
*   **JSON-LD 的局限性**：在扁平化的 RAG 流程中，仅添加 JSON-LD 标记对准确率的提升非常微小（仅 +0.17）。
*   **增强页面的显著效果**：增强型实体页面在标准 RAG 中使准确率提升了 **29.6%**，在智能体流水线中提升了 **29.8%**。
*   **智能体的补偿作用**：当文档结构较差（纯 HTML）时，智能体通过链接遍历能显著提升效果（+0.74）；但当文档已经过“增强页面”优化时，智能体的额外增益会变小，说明良好的内容结构可以降低推理成本。
*   **SEO 3.0 范式**：未来的搜索优化应从“针对排名优化”转向“针对推理和行动优化”。

#### 7. 优点
*   **前瞻性**：将语义网（Semantic Web）的经典理念与现代 Agentic AI 结合，为 RAG 系统的演进提供了清晰路径。
*   **实用性**：提出的“增强型实体页面”模板和 `llms.txt` 指令具有很强的实操性，可直接应用于现有的 Web 开发。
*   **开源贡献**：发布了完整的数据集、评估框架和模板，支持学术复现。

#### 8. 不足与局限
*   **截断风险**：论文提到向量数据库对长文档有截断限制，JSON-LD 效果不佳可能部分归因于其在 HTML 中的位置靠后被截断，而非数据本身无用。
*   **评估闭环风险**：Ground Truth 是从知识图谱生成的，而增强页面也是基于同一知识图谱构建的，这可能导致实验结果在某种程度上倾向于“文本匹配”而非纯粹的推理能力提升。
*   **模型单一性**：生成和评估均依赖 Gemini 家族模型，可能存在模型家族内部的偏见，未来需引入更多样化的 LLM（如 GPT-4, Claude）进行交叉验证。

（完）
