---
title: "Distilling Reasoning Without Knowledge: A Framework for Reliable LLMs"
title_zh: 无知识推理蒸馏：一种构建可靠大语言模型的框架
authors: "Auksarapak Kietkajornrit, Jad Tarifi, Nima Asgharbeygi"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14458v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 将计划与检索分离以减少幻觉
tldr: 针对大语言模型在处理实时或冲突信息时事实性不可靠的问题，本文提出一种模块化框架，将规划与检索、合成解耦。通过教师-学生蒸馏，训练轻量级规划器生成不含具体知识的抽象推理步骤和事实请求。该方法在SEAL-0基准上显著提升了准确率并降低了延迟，为构建可靠的事实检索型LLM提供了新思路。
selection_source: fresh_fetch
motivation: 现有的检索增强或工具调用模型往往依赖隐式规划，导致工具使用效率低下且容易产生幻觉。
method: 提出一种将规划、检索和合成显式分离的框架，通过蒸馏学习不包含具体事实答案的结构化推理路径。
result: 在挑战性基准SEAL-0上的实验表明，该框架在准确性和推理延迟方面均优于单体模型和基于提示的工具增强框架。
conclusion: 显式学习的规划结构对于提高事实检索型大语言模型的可靠性和效率至关重要。
---

## 摘要
当答案依赖于即时信息或冲突信息时，大语言模型（LLMs）在事实性问答任务中仍然表现得不够可靠。尽管检索增强和使用工具的 LLM 减少了幻觉，但它们通常依赖隐式规划，导致工具使用效率低下。我们提出了一个模块化框架，将规划与事实检索及答案合成明确分离。通过教师-学生框架训练一个轻量级学生规划器，以生成由抽象推理步骤和可搜索事实请求组成的结构化分解。监督信号仅包含规划轨迹和事实请求，不提供事实性答案或检索到的证据。在推理阶段，规划器生成计划，而经过提示工程优化的模块则执行检索和响应合成。我们在 SEAL-0 上评估了所提框架，这是一个针对搜索增强型 LLM 的极具挑战性的基准测试。结果表明，与单体推理模型和基于提示的工具增强框架相比，有监督规划提高了准确性并降低了延迟，证明了显式学习的规划结构对于构建可靠的事实性 LLM 至关重要。

## 速览
**TLDR**：引入模块化框架，通过将推理与事实检索分离来提高RAG系统的可靠性。 \
**Motivation**：在RAG和工具调用LLM中将规划与事实检索分离。 \
**Method**：方法与实现细节请参考摘要与正文。 \
**Result**：结果与对比结论请参考摘要与正文。 \
**Conclusion**：总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。

---

## Abstract
Fact-seeking question answering with large language models (LLMs) remains unreliable when answers depend on up-to-date or conflicting information. Although retrieval-augmented and tool-using LLMs reduce hallucinations, they often rely on implicit planning, leading to inefficient tool usage. We propose a modular framework that explicitly separates planning from factual retrieval and answer synthesis. A lightweight student planner is trained via a teacher-student framework to generate structured decompositions consisting of abstract reasoning steps and searchable fact requests. The supervision signals contain only planning traces and fact requests, without providing factual answers or retrieved evidence. At inference, the planner produces plans, while prompt-engineered modules perform retrieval and response synthesis. We evaluate the proposed framework on SEAL-0, an extremely challenging benchmark for search-augmented LLMs. Results show that supervised planning improves both accuracy and latency compared to monolithic reasoning models and prompt-based tool-augmented frameworks, demonstrating that explicitly learned planning structures are essential for reliable fact-seeking LLMs.

---

## 论文详细总结（自动生成）

以下是对论文《Distilling Reasoning Without Knowledge: A Framework for Reliable LLMs》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：大语言模型（LLM）在处理需要即时信息或长尾知识的问题时，常因内部参数知识过时或缺失而产生“幻觉”。
*   **核心挑战**：现有的检索增强生成（RAG）或工具调用模型通常将“推理”与“事实检索”耦合在一起，导致模型在不确定时倾向于编造事实，且隐式规划导致工具使用效率低下、错误难以溯源。
*   **整体含义**：本文提出了一种**显式解耦规划与知识**的框架。其核心理念是：通过蒸馏学习“如何寻找答案的逻辑结构”，而不是学习“答案本身”，从而构建一个不依赖内部事实记忆、高度可靠且可解释的智能体系统。

### 2. 方法论
该框架将推理过程分为四个独立模块：**规划（Planning）、检索（Retrieval）、事实提取（Extraction）和答案聚合（Aggregation）**。

*   **核心思想：无知识推理蒸馏**
    *   **教师模型（Teacher）**：使用高性能模型（如 GPT-5.2）生成结构化的规划轨迹。要求教师模型仅输出抽象推理步骤和原子级事实请求（JSON格式），严禁直接给出答案。
    *   **学生规划器（Student Planner）**：使用轻量级模型（Qwen 3-8B）进行微调。训练数据仅包含“问题 -> 规划路径”，不包含任何事实性证据或最终答案。这确保了学生模型只学习“提问和拆解的逻辑”，而不内化事实。
*   **关键技术细节**：
    *   **结构化计划**：计划包含 `required_information`（原子查询，区分 web 搜索和逻辑计算）和 `reasoning_steps`（抽象步骤）。
    *   **依赖解析**：支持占位符（如 `<RESULT_1>`），允许后续查询依赖于前序步骤的检索结果。
    *   **迭代执行**：推理时，规划器生成 JSON 计划，由专门的提取模块从搜索结果中提取简洁事实，最后由聚合模块合成最终答案。

### 3. 实验设计
*   **数据集/场景**：
    *   **训练集**：1,596 个问题（900 个合成事实问题、100 个 HotpotQA 难题、596 个 FreshQA 实时问题）。
    *   **评估基准（Benchmark）**：**SEAL-0**。这是一个极具挑战性的基准，专门针对搜索增强型 LLM 设计，包含 111 个即便是最强闭源模型（如 GPT-4o）在联网状态下也经常得零分的问题。
*   **对比方法**：
    1.  **单体模型（Monolithic）**：直接使用 Qwen 3-8B 进行原生推理（不联网）。
    2.  **提示词框架（Prompted）**：使用 Qwen 3-8B 作为规划器，但通过 Prompt 引导而非微调（分为开启和关闭内部推理两种模式）。
    3.  **本文框架（Student Planner）**：经过微调的 8B 学生规划器。

### 4. 资源与算力
*   **硬件**：单台 **NVIDIA L40S GPU (48GB VRAM)**。
*   **训练细节**：采用 **QLoRA（4-bit 量化）** 进行参数高效微调。
*   **参数设置**：有效 Batch Size 为 16，学习率 5e-5，使用余弦调度，训练直至验证集损失收敛。
*   **推理**：所有生成均采用确定性解码（Deterministic Decoding）以确保可重复性。

### 5. 实验数量与充分性
*   **实验规模**：论文对比了四种主要配置在 SEAL-0 上的表现，并提供了详细的延迟分解和定性案例分析。
*   **充分性评价**：
    *   **客观性**：使用了 SEAL-0 这一公认的“硬核”基准，避免了在简单任务上刷分的嫌疑。
    *   **公平性**：对比了相同基座模型在不同模式（原生、Prompt、微调）下的表现，证明了性能提升源于规划能力的增强而非模型规模。
    *   **局限性**：实验主要集中在 SEAL-0 上，样本量（111个）相对较小，且缺乏在更广泛、更简单的 RAG 任务上的泛化性验证。

### 6. 主要结论与发现
*   **性能突破**：在 SEAL-0 上，微调后的学生规划器达到了 **10.8% 的准确率**，远高于单体模型的 1.8% 和 Prompt 模式的 6.3%。这一结果在同类开源模型中处于领先地位。
*   **延迟优化**：学生规划器的平均延迟（27.8s）显著低于单体推理（159.9s）和 Prompt 模式（41.1s-107.9s）。这是因为显式规划减少了模型陷入“推理循环”的概率。
*   **规划的重要性**：实验证明，显式学习规划结构比单纯增加搜索功能或内部推理能力更能提高事实性问答的可靠性。

### 7. 优点
*   **解耦设计**：通过“无知识蒸馏”，有效解决了参数记忆干扰事实检索的问题，使模型更易于调试和控制。
*   **高效性**：使用 8B 小模型实现了超越大模型的规划质量，降低了部署成本。
*   **鲁棒性**：引入了 JSON 修复模块和显式依赖处理，增强了智能体在复杂多步推理中的稳定性。

### 8. 不足与局限
*   **外部依赖**：框架的最终表现高度依赖于外部搜索引擎（SerpAPI）的质量。如果搜索结果本身存在偏差或噪声，错误会传播至最终答案。
*   **评估范围**：SEAL-0 虽然难度高，但覆盖面窄。目前尚不清楚该框架在非事实性任务（如创意写作或开放式对话）中的表现。
*   **实时性瓶颈**：尽管模型推理变快了，但端到端延迟仍受限于多次外部 API 调用和网页提取过程，难以满足极高实时性的场景。

（完）
