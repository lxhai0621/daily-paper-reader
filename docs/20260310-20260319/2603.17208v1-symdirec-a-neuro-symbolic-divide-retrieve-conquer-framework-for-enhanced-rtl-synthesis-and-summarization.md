---
title: "SYMDIREC: A Neuro-Symbolic Divide-Retrieve-Conquer Framework for Enhanced RTL Synthesis and Summarization"
title_zh: SYMDIREC：一种用于增强 RTL 综合与摘要生成的神经符号分治-检索-整合框架
authors: "Prashanth Vijayaraghavan, Apoorva Nitsure, Luyao Shi, Charles Mackin, Ashutosh Jadhav, David Beymer, Ehsan Degan, Vandana Mukherjee"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.17208v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 用于 RTL 综合与推理的神经符号 RAG 框架
tldr: 针对大语言模型在处理寄存器传输级（RTL）硬件设计任务时面临的语法严苛、监督数据有限及结构精度不足等挑战，本文提出了一种名为 SYMDIREC 的神经符号框架。该框架采用“分而治之”的策略，将 RTL 任务分解为符号化子目标，结合微调的检索器获取相关代码，并通过 LLM 推理生成验证后的输出。SYMDIREC 在无需微调 LLM 的情况下，显著提升了 Verilog 和 VHDL 的合成与摘要性能。
selection_source: fresh_fetch
motivation: 现有大语言模型在 RTL 任务中缺乏符号化规划能力，导致生成的硬件描述语言在结构精度和语法对齐上表现欠佳。
method: 提出一种神经符号框架，通过将 RTL 任务分解为符号子目标、检索相关代码并利用 LLM 进行推理组装来实现高效设计。
result: "在 Verilog 和 VHDL 任务中，该方法将合成任务的 Pass@1 提高了约 20%，并将摘要任务的 ROUGE-L 指标提升了 15-20%。"
conclusion: 符号化引导与检索增强生成的结合能有效提升大语言模型在硬件自动化设计领域的结构化生成与理解能力。
---

## 摘要
寄存器传输级 (RTL) 综合与摘要生成是硬件设计自动化的核心，但由于严格的 HDL 语法、有限的监督以及与自然语言的弱对齐，对于大语言模型 (LLM) 而言仍具挑战性。现有的提示词和检索增强生成 (RAG) 方法尚未结合符号规划，限制了它们的结构精度。我们引入了 SYMDIREC，这是一个神经符号框架，它将 RTL 任务分解为符号子目标，通过微调后的检索器检索相关代码，并通过 LLM 推理组装经过验证的输出。SYMDIREC 在无需 LLM 微调的情况下支持 Verilog 和 VHDL，在综合任务上的 Pass@1 率比提示词和 RAG 基准高出约 20%，在摘要生成任务上的 ROUGE-L 提升了 15-20%，证明了符号引导在 RTL 任务中的优势。

## Abstract
Register-Transfer Level (RTL) synthesis and summarization are central to hardware design automation but remain challenging for Large Language Models (LLMs) due to rigid HDL syntax, limited supervision, and weak alignment with natural language. Existing prompting and retrieval-augmented generation (RAG) methods have not incorporated symbolic planning, limiting their structural precision. We introduce SYMDIREC, a neuro-symbolic framework that decomposes RTL tasks into symbolic subgoals, retrieves relevant code via a fine-tuned retriever, and assembles verified outputs through LLM reasoning. Supporting both Verilog and VHDL without LLM fine-tuning, SYMDIREC achieves ~20% higher Pass@1 rates for synthesis and 15-20% ROUGE-L improvements for summarization over prompting and RAG baselines, demonstrating the benefits of symbolic guidance in RTL tasks.

---

## 论文详细总结（自动生成）

以下是对论文《SYMDIREC: A Neuro-Symbolic Divide-Retrieve-Conquer Framework for Enhanced RTL Synthesis and Summarization》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在电子设计自动化（EDA）领域，寄存器传输级（RTL）的**综合**（自然语言转代码）与**摘要生成**（代码转自然语言）是核心任务。尽管大语言模型（LLM）在通用代码生成上表现出色，但在处理 Verilog 和 VHDL 等硬件描述语言（HDL）时面临三大挑战：
*   **语法严苛性**：HDL 对结构和时序逻辑要求极高，微小错误会导致综合失败。
*   **数据稀缺**：相比于 Python/C++，高质量且带有标注的 RTL 数据较少。
*   **语义对齐弱**：自然语言描述与底层硬件逻辑（如布尔方程、数据流）之间存在巨大的语义鸿沟。
现有的检索增强生成（RAG）或提示词工程（如 CoT）缺乏对硬件功能意图的**符号化建模**，导致生成的结构精度不足。

### 2. 方法论：SYMDIREC 框架
论文提出了一种名为 **SYMDIREC**（神经符号分治-检索-整合）的框架，其核心思想是将复杂的 RTL 任务分解为受符号逻辑引导的子任务。
*   **分（Divide - 符号化分解）**：利用 LLM 将高层任务分解为多个功能子模块（如计数器、多路复用器）。每个模块不仅有自然语言描述，还包含**符号逻辑表达式**（如布尔逻辑或状态转移方程），作为中间支架。
*   **检（Retrieve - 符号化检索）**：设计了一个双编码器检索器，在自建的 **RTL-IR 数据集**上进行微调。检索时同时输入“文本描述 + 符号逻辑”，通过联合嵌入空间匹配最相关的 RTL 代码片段或摘要。
*   **治（Conquer - 验证与组装）**：LLM 充当“验证者”，根据符号意图对检索到的候选片段进行评分和筛选，最后将验证过的片段组装成完整的 RTL 模块或功能摘要，确保命名一致性和逻辑连贯。

### 3. 实验设计
*   **数据集**：
    *   **Verilog-Eval**：包含 156 个来自 HDLBits 的功能任务，配有测试平台（Testbench）。
    *   **VHDL-Eval**：包含 202 个 VHDL 任务，用于评估跨语言泛化能力。
    *   **RTL-IR**：作者自建的包含约 5 万条记录的数据集，涵盖文本-代码对、功能等效代码（FEC）等。
*   **基准测试（Benchmark）**：使用 **Pass@1**（合成任务的通过率）和 **ROUGE-L**（摘要任务的文本相似度）。
*   **对比方法**：包括 Vanilla Prompting、CoDes（基于描述的链式思考）、ReAct（迭代推理）、VRAG（通用 RAG）、以及领域专用模型 RTLCoder 和 CodeV。

### 4. 资源与算力
*   **硬件**：检索器的微调在 **2 台 NVIDIA V100 GPU** 上完成。
*   **软件/工具**：使用 **Milvus** 向量数据库进行高效检索，**LangChain** 进行提示词编排。
*   **推理效率**：在并行处理下，每个任务的平均周转时间约为 5-10 秒。

### 5. 实验数量与充分性
*   **实验规模**：针对 Verilog 和 VHDL 两种语言分别进行了详尽测试。
*   **消融实验**：研究了子组件数量（$N$）、检索深度（$k$）以及符号逻辑对性能的影响。
*   **客观性**：每组实验运行 5 次并取平均值和标准差，使用了配对 t 检验（p < 0.01）来验证统计显著性。实验设计涵盖了从零样本到有监督微调的多种梯度，对比相对公平。

### 6. 主要结论与发现
*   **性能大幅提升**：在合成任务中，SYMDIREC 的 Pass@1 比强 RAG 基准高出约 **20%**；在摘要任务中，ROUGE-L 提升了 **15-20%**。
*   **符号逻辑的关键作用**：相比仅使用自然语言检索，加入符号逻辑支架能显著减少检索噪声，提高子模块的匹配精度。
*   **跨语言有效性**：该框架在 Verilog 和 VHDL 上表现均衡，证明了其作为通用 RTL 辅助工具的潜力。
*   **分治策略的边界**：实验发现子组件分解数量 $N$ 在 4-6 之间最优，过度分解会导致语义碎片化，反而降低性能。

### 7. 优点
*   **神经符号结合**：巧妙地利用符号逻辑弥补了 LLM 在严谨硬件逻辑上的短板。
*   **无需微调大模型**：仅通过微调轻量级检索器和优化提示词流程即达到了超越全量微调模型的效果，成本更低。
*   **模块化推理**：通过“分-检-治”流程，提高了生成过程的可解释性和可调试性。

### 8. 不足与局限
*   **符号生成的依赖性**：如果 LLM 在第一阶段生成的符号表达式有误，会直接导致后续检索失败（错误分析显示约 8-10% 的错误源于此）。
*   **单文件限制**：目前仅支持单文件 RTL 设计，无法处理具有复杂跨文件依赖的大型层次化硬件项目。
*   **组装风险**：在“治”阶段，LLM 偶尔仍会犯信号连接错误或进位传播逻辑错误（约 6-8% 的失败案例）。

（完）
