---
title: "Demand-Driven Context: A Methodology for Building Enterprise Knowledge Bases Through Agent Failure"
title_zh: 需求驱动上下文：一种通过智能体失败构建企业知识库的方法论
authors: "Raj Navakoti, Saideep Navakoti"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.14057v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 通过智能体失败案例构建企业知识库
tldr: 本研究针对大模型智能体在企业任务中因缺乏领域知识而失败的问题，提出了“需求驱动上下文”（DDC）方法。该方法借鉴测试驱动开发理念，以智能体失败为信号，按需提取并固化解决问题所需的最小必要知识。通过零售订单履约案例验证，DDC能有效避免传统方法带来的知识冗余，并提出20-30个循环即可实现知识收敛，为构建高效、精准的企业知识库提供了新路径。
selection_source: fresh_fetch
motivation: 传统自上而下或自下而上的知识工程方法存在知识冗余或无法捕获隐性知识的缺陷，导致智能体难以应对复杂的企业特定任务。
method: 提出一种以问题为导向的DDC方法，通过让智能体在实际任务中“索要”缺失的上下文，实现对领域知识的精准提取与建模。
result: 在零售订单履约的SRE事件管理实验中，仅通过9个循环就构建了包含46个实体的实用知识库，验证了该方法的收敛性与高效性。
conclusion: DDC方法通过将智能体失败转化为知识获取动力，为企业级大模型应用的知识治理和规模化落地提供了一套可行的架构方案。
---

## 摘要
大语言模型智能体展现出了专家级的推理能力，但由于缺乏领域知识（如术语、操作流程、系统间依赖关系以及主要以“部落知识”形式存在的机构决策），在企业特定任务上经常失败。目前的方法分为两类：自顶向下的知识工程（在智能体使用之前记录领域知识）和自底向上的自动化（智能体从任务经验中学习）。两者都存在根本性的局限：自顶向下的努力会产生臃肿且未经测试的知识库；而自底向上的方法无法获取仅存在于人类头脑中的知识。我们提出了需求驱动上下文（DDC），这是一种问题优先的方法论，它将智能体的失败作为策划哪些领域知识的主要信号。受测试驱动开发（TDD）的启发，DDC 反转了知识工程：DDC 不再是先策划知识并寄希望于其有用，而是给智能体实际问题，让它们索取所需的上下文，并仅策划成功所需的最小知识。我们描述了该方法论及其实体元模型，并提出了一个收敛假设，即 20-30 个问题周期即可产生足以胜任特定领域角色的知识库。我们通过零售订单履约中的一个实际案例演示了 DDC，其中针对 SRE 事件管理智能体的 9 个周期产生了一个包含 46 个实体的可重用知识库。最后，我们提出了一种适用于企业采用的扩展架构，包含半自动化策划和人工治理。

## Abstract
Large language model agents demonstrate expert-level reasoning, yet consistently fail on enterprise-specific tasks due to missing domain knowledge -- terminology, operational procedures, system interdependencies, and institutional decisions that exist largely as tribal knowledge. Current approaches fall into two categories: top-down knowledge engineering, which documents domain knowledge before agents use it, and bottom-up automation, where agents learn from task experience. Both have fundamental limitations: top-down efforts produce bloated, untested knowledge bases; bottom-up approaches cannot acquire knowledge that exists only in human heads. We present Demand-Driven Context (DDC), a problem-first methodology that uses agent failure as the primary signal for what domain knowledge to curate. Inspired by Test-Driven Development, DDC inverts knowledge engineering: instead of curating knowledge and hoping it is useful, DDC gives agents real problems, lets them demand the context they need, and curates only the minimum knowledge required to succeed. We describe the methodology, its entity meta-model, and a convergence hypothesis suggesting that 20-30 problem cycles produce a knowledge base sufficient for a given domain role. We demonstrate DDC through a worked example in retail order fulfillment, where nine cycles targeting an SRE incident management agent produce a reusable knowledge base of 46 entities. Finally, we propose a scaling architecture for enterprise adoption with semi-automated curation and human governance.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为**需求驱动上下文（Demand-Driven Context, DDC）**的方法论，旨在解决大语言模型（LLM）智能体在处理企业特定任务时因缺乏“内部隐性知识”而失效的问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：LLM 具备强大的通用推理能力，但在面对企业内部系统、特定术语、操作流程和历史决策（即“部落知识”，Tribal Knowledge）时表现极差。
*   **研究动机**：
    *   **传统自上而下（Top-down）**的知识工程试图在应用前穷举所有文档，导致知识库臃肿、过时且难以发现真正有用的信息。
    *   **自底向上（Bottom-up）**的自动化方法（如 Reflexion, ExpeL）侧重于优化智能体的执行策略，但无法凭空产生只存在于专家头脑中的领域知识。
    *   **RAG 的局限**：RAG 只能检索已有的文档，如果知识未被记录或已过时，RAG 则无能为力。

### 2. 方法论（DDC）
*   **核心思想**：借鉴软件工程中的**测试驱动开发（TDD）**理念。TDD 是“先写失败的测试，再写代码”；DDC 是“**先让智能体在真实问题中失败，再根据失败暴露的缺口精准补充知识**”。
*   **关键流程（9步循环）**：
    1.  **引入真实问题**：给智能体一个具体的业务难题。
    2.  **创建沙盒**：隔离工作区。
    3.  **初始尝试**：智能体在当前知识下尝试解决（通常会失败）。
    4.  **识别缺口**：智能体生成“信息清单”，列出解决该问题所需的术语定义、系统逻辑等。
    5.  **人工填补**：领域专家针对清单提供最小化的必要答案。
    6.  **再次尝试**：智能体利用新补充的上下文重新执行。
    7.  **人工验证**：专家审核输出，若不正确则进入修正循环。
    8.  **知识沉淀**：将验证后的知识转化为结构化实体。
    9.  **记录日志**：用于分析知识库的增长和收敛情况。
*   **实体元模型**：知识以 Markdown 文件存储，包含 YAML 元数据，分为 `system`（系统）、`jargon`（术语）、`process`（流程）、`capability`（能力）等类型。
*   **收敛假设**：随着循环次数增加，新创建的实体会呈幂律衰减，预计 20-30 个循环后，知识库即可覆盖该领域角色的大部分需求。

### 3. 实验设计
*   **场景设置**：零售电子商务订单履约（Order Fulfillment）领域。
*   **目标任务**：SRE（站点可靠性工程）事件管理，要求智能体诊断生产环境故障（如订单积压、区域性部署错误）。
*   **对比基准**：对比了智能体在“零上下文（Zero Context）”与“DDC 迭代后上下文”下的表现。
*   **实验过程**：进行了 9 个完整的 DDC 循环，模拟了从队列阻塞到跨区域配置错误等多种真实故障场景。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **模型使用**：文中提到了使用 LLM 智能体，但未指明具体的模型版本（如 GPT-4 或 Claude 3.5）。由于该方法侧重于上下文工程而非模型微调，其算力消耗主要集中在推理阶段。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 **9 组**连续的循环实验。
*   **充分性评价**：
    *   **初步验证有效**：实验展示了新实体数量从最初的 8 个下降到后期的 2 个，复用率从 0% 上升到 75%，初步支持了收敛假设。
    *   **局限性**：9 组实验对于验证“20-30 次循环达到完全收敛”的长期假设尚显不足。实验场景虽然具有代表性，但属于模拟（Synthetic）环境，而非真实的生产实时数据。

### 6. 主要结论与发现
*   **失败是高质量信号**：智能体的失败能精准定位企业知识库中最缺失、最有价值的部分。
*   **知识复用性强**：经过少量循环后，新问题往往可以通过已有实体的组合来解决。
*   **效率提升**：平均每个循环仅需 30 分钟的人工干预，9 个循环共 4.5 小时即构建了一个包含 46 个实体的实用知识库。
*   **纠错循环至关重要**：实验发现智能体在知识不足时会产生“幻觉”（如虚构不存在的系统），人工干预能有效阻止错误知识进入永久知识库。

### 7. 优点
*   **按需驱动，避免冗余**：只记录“有用”的知识，解决了传统知识管理中“文档写了没人看”的痛点。
*   **结构化与机器友好**：采用带有 YAML 的 Markdown 格式，既方便人类阅读，又方便智能体解析和建立实体关联。
*   **可扩展性**：提出了基于 Pull Request（PR）机制的分布式协作架构，允许跨团队并行构建知识库。

### 8. 不足与局限
*   **高度依赖专家**：DDC 依然需要领域专家投入时间回答问题，在专家极度匮乏的企业中可能存在落地瓶颈。
*   **实验规模有限**：收敛假设（20-30次）尚未在更大规模、更多样化的真实企业环境中得到统计学意义上的验证。
*   **主观性风险**：不同专家对同一问题的知识建模可能存在差异，论文虽提到 PR 治理，但未给出冲突解决的具体算法。
*   **静态知识局限**：对于变化极快的动态数据（如实时库存数值），DDC 这种结构化实体建模可能不如实时 API 调用有效。

（完）
