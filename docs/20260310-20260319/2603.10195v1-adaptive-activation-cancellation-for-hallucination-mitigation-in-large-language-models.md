---
title: Adaptive Activation Cancellation for Hallucination Mitigation in Large Language Models
title_zh: 大语言模型中缓解幻觉的自适应激活抵消
authors: "Eric Yocam, Varghese Vaidyan, Gurcan Comert, Paris Kalathas, Yong Wang, Judith L. Mwakalonge"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.10195v1"
tags: ["query:ma-kf"]
score: 7.0
evidence: 通过激活取消减轻幻觉
tldr: 针对大语言模型的事实性幻觉问题，本文提出自适应激活消除（AAC）框架。该方法借鉴信号处理中的噪声消除思路，将幻觉相关的神经激活视为残差流中的结构化干扰，通过线性探测识别“幻觉节点”并在推理时利用置信度加权的钩子进行实时抑制。实验表明，AAC在不损害模型通用能力和流畅度的前提下，显著提升了多个模型在TruthfulQA等基准上的事实准确性，且无需外部知识或微调。
selection_source: fresh_fetch
motivation: 旨在解决大语言模型生成事实错误文本的问题，同时避免微调或额外推理开销带来的通用能力受损。
method: 通过层级线性探测识别幻觉相关的神经激活节点，并在推理过程中使用置信度加权的前向钩子实时抑制这些干扰信号。
result: 在OPT、Phi-3和LLaMA 3等模型上一致提升了事实准确性，且在WikiText-103和MMLU基准上实现了零性能退化。
conclusion: 研究证明了精准的神经元级抑制能有效缓解幻觉，同时完美保留模型的语言流畅度和通用推理能力。
---

## 摘要
大语言模型经常生成流畅但事实错误的文本。我们提出了自适应激活抵消（AAC），这是一个实时推理框架，它将与幻觉相关的神经激活视为 Transformer 残差流中的结构化干扰，并明确类比了信号处理中的经典自适应噪声抵消。该框架通过逐层线性探测识别幻觉节点（H-Nodes），并在自回归生成过程中使用置信度加权的前向钩子（forward hook）对其进行抑制——无需外部知识、无需微调，且无需额外的推理次数。在 TruthfulQA 和 HaluEval 上对 OPT-125M、Phi-3-mini 和 LLaMA 3-8B 进行评估，结果表明该实时钩子是唯一在所有三个规模上都能持续提高下游准确率的干预方法。关键在于，该方法具有严格的“手术式”特征：在所有三个模型规模上，WikiText-103 的困惑度（perplexity）和 MMLU 推理准确率均保持 0.0% 的零退化，这一特性使 AAC 区别于那些以牺牲流畅度或通用能力来换取事实性提升的干预手段。在 LLaMA 3-8B 规模上，该钩子还带来了正向的生成级增益（MC1 +0.04；MC2 +0.003；Token-F1 +0.003），同时实现的探测空间选择性比 ITI 基准高出 5.94 倍至 3.5 倍——这证明了针对性的神经元级抑制可以同时提高事实准确性并保留模型能力。

## 速览
**TLDR**：针对大语言模型的事实性幻觉，本文提出自适应激活消除（AAC）框架。该方法将幻觉激活视为残差流中的干扰，通过线性探测识别“幻觉节点”并在推理时利用置信度加权钩子进行实时抑制。实验证明，AAC在不损害模型通用能力和流畅度的前提下，显著提升了TruthfulQA等基准的事实准确性，实现了高效且无损的幻觉缓解。 \
**Motivation**：旨在解决大语言模型生成内容虽流畅但事实错误的问题，且现有干预手段往往会损害模型的通用推理能力或语言流畅度。 \
**Method**：借鉴经典噪声消除理论，通过层级线性探测识别幻觉节点，并在生成过程中使用置信度加权的前向钩子实时抑制这些激活。 \
**Result**：在不同规模模型上均提升了事实准确性，且在WikiText-103困惑度和MMLU推理准确度上实现了零性能退化。 \
**Conclusion**：AAC证明了通过精准的神经元级抑制，可以在不牺牲模型原有能力的情况下有效缓解幻觉，为实时推理干预提供了高效路径。

---

## Abstract
Large Language Models frequently generate fluent but factually incorrect text. We propose Adaptive Activation Cancellation (AAC), a real-time inference-time framework that treats hallucination-associated neural activations as structured interference within the transformer residual stream, drawing an explicit analogy to classical adaptive noise cancellation from signal processing. The framework identifies Hallucination Nodes (H-Nodes) via layer-wise linear probing and suppresses them using a confidence-weighted forward hook during auto-regressive generation -- requiring no external knowledge, no fine-tuning, and no additional inference passes. Evaluated across OPT-125M, Phi-3-mini, and LLaMA 3-8B on TruthfulQA and HaluEval, the real-time hook is the only intervention that consistently improves downstream accuracy on all three scales. Critically, the method is strictly surgical: WikiText-103 perplexity and MMLU reasoning accuracy are preserved at exactly 0.0% degradation across all three model scales, a property that distinguishes AAC from interventions that trade fluency or general capability for factual improvement. On the LLaMA 3-8B scale, the hook additionally yields positive generation-level gains (MC1 +0.04; MC2 +0.003; Token-F1 +0.003) while achieving probe-space selectivity 5.94x - 3.5x higher than the ITI baseline -- demonstrating that targeted neuron-level suppression can simultaneously improve factual accuracy and preserve model capability.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为**自适应激活抵消（Adaptive Activation Cancellation, AAC）**的框架，旨在通过干预大语言模型（LLM）内部的神经激活来缓解幻觉问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：LLM 经常生成流畅但事实错误的内容（幻觉）。现有的缓解方法（如 RAG、知识编辑、后期验证）通常需要外部知识库、重新训练模型或额外的推理开销，且往往会损害模型的通用语言能力（如推理准确度下降或困惑度上升）。
*   **研究动机**：作者借鉴经典信号处理中的**自适应噪声抵消（ANC）**理论，将幻觉视为 Transformer 残差流中的“结构化干扰信号”。研究目标是开发一种无需微调、无需外部知识、且能保持模型原始能力的实时推理干预方法。

### 2. 方法论：核心思想与技术细节
AAC 框架分为三个阶段：
1.  **离线探测训练（Offline Probing）**：
    *   在标注数据集上提取模型各层的隐藏状态。
    *   使用 L2 正则化逻辑回归训练线性探测器，识别最能区分“事实”与“幻觉”的层（通常在模型深度的 50% 左右）。
2.  **幻觉节点（H-Nodes）识别与基准构建**：
    *   根据探测器的权重，识别出对幻觉贡献最大的前 K 个神经元（H-Nodes）。
    *   计算这些节点在正确样本上的激活分布，设定 80% 分位数为“正常基准（Baseline）”。
3.  **实时前向钩子（Real-time Forward Hook）**：
    *   在推理生成过程中，通过 `forward_hook` 拦截指定层的激活。
    *   **自适应衰减公式**：如果探测器预测当前 token 产生幻觉的置信度较高，则计算 H-Nodes 超过基准的“多余激活”，并根据置信度加权进行扣除（抵消）。
    *   该过程是动态的，仅在模型表现出幻觉倾向时进行“手术式”干预。

### 3. 实验设计
*   **模型规模**：跨越了三个量级，包括 OPT-125M、Phi-3-mini (3.8B) 和 LLaMA 3-8B。
*   **数据集**：主要使用 **TruthfulQA**（用于探测训练和生成评估）和 **HaluEval**（用于跨基准泛化评估）。
*   **对比方法（Baselines）**：
    *   **ITI (Inference-Time Intervention)**：通过改变激活方向进行干预。
    *   **DoLA (Decoding by Contrasting Layers)**：通过对比不同层的逻辑输出进行解码。
    *   **五种 AAC 变体**：包括均值基准、分位数基准、傅里叶变换变体等。
*   **评估指标**：MC1/MC2 准确率、Token-F1、选择性比率（Selectivity）、WikiText-103 困惑度（PPL）以及 MMLU 任务准确率。

### 4. 资源与算力
*   **算力说明**：论文中**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到使用了 `device-map` 进行模型加载，涉及 float32、float16 和 bfloat16 等精度。由于该方法不需要微调，主要的计算开销集中在离线提取激活和训练简单的逻辑回归探测器上，推理时的额外开销极低（仅增加了一个线性探测和简单的向量减法）。

### 5. 实验数量与充分性
*   **实验规模**：做了大量的消融实验和对比实验，包括：
    *   **全层扫描**：分析了所有模型每一层的探测准确率。
    *   **分位数扫描**：测试了不同阈值对干预效果的影响。
    *   **策略对比**：对比了 6 种不同的抵消策略。
    *   **能力测试**：专门测试了干预对通用能力（MMLU/PPL）的影响。
*   **充分性评价**：实验设计较为全面，覆盖了从 1.25 亿到 80 亿参数的模型，验证了方法的扩展性。通过引入“选择性比率”这一指标，客观地衡量了干预是精准打击幻觉还是误伤了正确知识。

### 6. 主要结论与发现
*   **中层涌现**：幻觉的可区分性在模型约 50% 深度处达到峰值，这是一种跨规模的架构特性。
*   **手术式精准**：AAC 是唯一在所有测试模型上实现 **0.0% 能力退化** 的方法。在提升事实性的同时，WikiText 困惑度和 MMLU 分数完全不受影响。
*   **实时钩子的必要性**：只有在生成过程中实时应用钩子才能提升下游准确率，事后（Post-hoc）修改单次前向传播的激活对最终生成结果无效。
*   **规模效应**：在 LLaMA 3-8B 上，AAC 显著优于 ITI，探测空间选择性高出 3.5 倍至 5.94 倍。

### 7. 优点
*   **非侵入性**：无需修改模型参数，无需微调，易于部署。
*   **极高安全性**：通过置信度加权和分位数门控，极大地降低了对正常生成任务的干扰。
*   **理论支撑**：将信号处理中的噪声消除理论引入 LLM 解释性研究，提供了新颖的视角。
*   **诊断价值**：识别出的 H-Nodes 具有语义一致性（如特定文化偏见、名人事实错误），有助于理解模型幻觉的来源。

### 8. 不足与局限
*   **领域内依赖**：探测器目前依赖于特定基准（如 TruthfulQA）的分布，跨领域的泛化能力（如从 TruthfulQA 到 HaluEval）随模型规模增大而有所下降。
*   **基准限制**：所测试的模型规模（最高 8B）在 TruthfulQA 上的基准表现仍处于较低水平，尚需在 70B 或更大规模模型上验证其 MC1/MC2 的提升潜力。
*   **多义性陷阱**：在 Phi-3-mini 等中等规模模型中，神经元的高度多义性（Polysemanticity）导致干预的选择性略有下降，显示出稀疏激活干预在特定参数区间面临挑战。

（完）
