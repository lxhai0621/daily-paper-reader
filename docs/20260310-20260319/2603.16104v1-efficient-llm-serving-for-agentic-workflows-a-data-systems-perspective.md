---
title: "Efficient LLM Serving for Agentic Workflows: A Data Systems Perspective"
title_zh: 面向智能体工作流的高效 LLM 推理服务：数据系统视角
authors: "Noppanat Wadlom, Junyi Shen, Yao Lu"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16104v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 从数据系统角度通过缓存优化智能体工作流
tldr: 智能体工作流由相互依赖的LLM调用组成，存在大量提示词和中间结果冗余。现有系统如vLLM仅优化单次推理，忽视了跨调用依赖。本文提出Helium框架，从数据系统视角将工作流建模为查询计划，将LLM调用视为一级算子。通过主动缓存和缓存感知调度，Helium实现了跨提示词和KV状态的高效复用，显著提升了智能体服务的端到端性能。
selection_source: fresh_fetch
motivation: 现有的LLM推理系统主要针对单次调用进行优化，无法有效处理智能体工作流中普遍存在的跨调用冗余和依赖关系。
method: 引入Helium框架，将智能体工作流视为查询计划，并结合主动缓存与缓存感知调度技术来最大化KV缓存的复用。
result: 在多种工作负载下，Helium相比最先进的智能体服务系统实现了高达1.56倍的加速。
conclusion: 针对智能体工作流进行端到端的系统级优化是实现高效、可扩展LLM服务的关键。
---

## 摘要
智能体工作流由一系列相互依赖的大语言模型（LLM）调用组成，已成为现代 AI 系统中的主流工作负载。由于推测性执行和并行探索，这些工作流在重叠的提示词和中间结果中表现出大量的冗余。现有的 LLM 推理服务系统（如 vLLM）专注于优化单个推理调用，而忽略了跨调用的依赖关系，导致了显著的效率低下。本文从数据系统的视角重新思考了 LLM 和智能体推理服务，并引入了 Helium。Helium 是一个感知工作流的推理服务框架，它将智能体工作负载建模为查询计划，并将 LLM 调用视为一等算子。Helium 集成了主动缓存和感知缓存的调度，以最大限度地提高提示词、KV 状态和工作流之间的复用。通过这些技术，Helium 将经典的查询优化原则与 LLM 推理服务相结合，在各种工作负载上相比最先进的智能体推理服务系统实现了高达 1.56 倍的加速。我们的结果表明，跨工作流的端到端优化对于构建可扩展且高效的基于 LLM 的智能体至关重要。

## Abstract
Agentic workflows are composed of sequences of interdependent Large Language Model (LLM) calls, and they have become a dominant workload in modern AI systems. These workflows exhibit extensive redundancy from overlapping prompts and intermediate results due to speculative and parallel exploration. Existing LLM serving systems, such as vLLM, focus on optimizing individual inference calls and overlook cross-call dependencies, leading to significant inefficiencies. This paper rethinks LLM and agent serving from a data systems perspective and introduces Helium, a workflow-aware serving framework that models agentic workloads as query plans and treats LLM invocations as first-class operators. Helium integrates proactive caching and cache-aware scheduling to maximize reuse across prompts, KV states, and workflows. Through these techniques, Helium bridges classic query optimization principles with LLM serving, achieving up to 1.56x speedup over state-of-the-art agent serving systems on various workloads. Our results demonstrate that end-to-end optimization across workflows is essential for scalable and efficient LLM-based agents.

---

## 论文详细总结（自动生成）

这篇论文提出了 **Helium**，一个从数据系统视角出发、专门为智能体工作流（Agentic Workflows）设计的 LLM 推理服务框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：智能体工作流通常由一系列相互依赖的 LLM 调用组成（如并行探索、多轮辩论、迭代优化）。现有的 LLM 推理引擎（如 vLLM）主要优化单次推理调用，对跨调用的依赖关系和提示词冗余缺乏感知；而传统的分布式数据系统（如 Spark）将 LLM 视为黑盒 UDF，无法利用 LLM 特有的 KV 缓存优化。
*   **研究动机**：智能体工作流中存在大量重叠的提示词前缀和可复用的中间结果。作者认为应将智能体工作流视为一种“查询计划（Query Plan）”，利用数据库领域的查询优化原则来消除冗余并优化调度。

### 2. 方法论：Helium 框架
Helium 将 LLM 调用视为“一等算子（First-class Operators）”，核心思想是**感知工作流的全局优化**。
*   **逻辑计划优化**：
    *   **算子剪枝与公共子图消除（CSE）**：识别并合并工作流中结构相同、输入相同的子图，确保重复计算只执行一次。
    *   **提示词缓存替换**：对于确定性算子（如贪婪采样的 LLM），若输入已处理过，则将 LLM 算子替换为轻量级的 `CacheFetch` 算子，直接获取结果。
*   **物理执行与调度**：
    *   **模板化基数树（Templated Radix Tree, TRT）**：一种新型数据结构，用于建模提示词的全局前缀结构和算子间的依赖关系。
    *   **主动 KV 缓存管理**：不同于现有系统的被动缓存，Helium 会在执行前识别静态前缀并预热（Pre-warm）GPU 内存中的 KV 缓存。
    *   **感知缓存的调度算法**：基于成本模型（以“Token Step”为时间单位）的贪心算法。它在最大化并行度（Makespan 优化）的同时，通过调整执行顺序来最大化同一 Worker 上的前缀复用。

### 3. 实验设计
*   **数据集与场景**：
    *   **微观基准测试**：涵盖 Map-Reduce、多智能体辩论（Debate）、自我反思（Reflection）、迭代精炼（Iterative）、并行链（Parallel）五种典型模式。使用 MMLU、TAT-QA、Amazon Reviews 数据集。
    *   **端到端测试**：构建了一个复杂的“金融交易（Trading）”工作流，包含 19 个智能体和 88 个 LLM 算子，模拟真实的金融分析场景。
*   **对比方法（Baselines）**：
    *   **推理引擎**：vLLM（原生顺序执行）。
    *   **编排框架**：LangGraph、AgentScope。
    *   **学术前沿系统**：Parrot（感知语义变量）、KVFlow（感知工作流的缓存管理）、OpWise（算子级并行）。

### 4. 资源与算力
*   **硬件环境**：一台配备 2 张 **NVIDIA H100 NVL (94GB)** GPU 的服务器，CPU 为 AMD EPYC 9554 (64核)。
*   **模型使用**：主要使用 **Qwen3-8B** 和 **Qwen3-14B**，敏感性分析中还测试了 Llama-3.1-8B 和 Qwen3-32B。
*   **训练时长**：本文侧重于推理服务优化，不涉及模型训练。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的详尽实验，包括 7 种原始工作流组合、1 个大规模端到端案例研究。
*   **消融实验**：分别验证了 KV 缓存、计划剪枝、提示词缓存和感知缓存调度（CAS）的贡献。
*   **敏感性分析**：测试了不同 Batch Size（8 到 80）、并行分支数、辩论轮数、Worker 数量、模型大小以及内存容量限制下的表现。
*   **客观性**：通过 MILP（混合整数线性规划）求解器计算了理论最优调度方案，证明 Helium 的调度算法已接近理论极限（差距仅 0.9%）。实验设计严谨，对比了多种 SOTA 系统，具有较高的说服力。

### 6. 主要结论与发现
*   **性能提升**：Helium 在原始工作流上比现有 SOTA 系统快 **1.56 倍**；在复杂的金融交易工作流中，相比 LangGraph 提升了 **1.49 倍**，相比原生 vLLM 提升了近 **40 倍**。
*   **缓存效率**：通过全局调度，Helium 的前缀缓存命中率显著高于其他系统（在 Qwen3-8B 上比 LangGraph 高出 37.3%）。
*   **调度优势**：全局感知调度比局部启发式调度（如 LSPF）更有效，能显著降低长尾延迟（P95 延迟降低了约 28%）。

### 7. 优点与亮点
*   **视角独特**：首次系统性地从数据库查询优化的角度重新定义智能体工作流推理，将 LLM 调用从“黑盒”变为“白盒”。
*   **主动性**：提出的“主动缓存”和“全局 TRT 调度”改变了现有系统只能被动响应请求的现状。
*   **工程落地性强**：提供了 Python DSL，且在 vLLM 基础上实现，易于集成到现有 AI 基础设施中。
*   **内存高效**：相比 SGLang 等系统，Helium 的调度元数据内存占用极低（仅为前者的几十分之一）。

### 8. 不足与局限
*   **动态控制流限制**：目前的 DAG 抽象对包含复杂条件循环（Looping）和动态分支（Dynamic Mapping）的工作流支持有限，虽然文中提到可以通过集成控制流原语解决，但尚未完全展开。
*   **外部 API 依赖**：实验主要集中在 LLM 计算上，未深入探讨当智能体调用具有不可预测延迟的外部工具（如 Web 搜索）时，成本模型如何保持准确。
*   **同质模型假设**：目前的优化假设工作流使用相同的基座模型，对于混合使用多种不同尺寸模型的异构场景，优化空间尚待挖掘。

（完）
