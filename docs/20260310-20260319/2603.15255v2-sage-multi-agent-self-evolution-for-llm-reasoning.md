---
title: "SAGE: Multi-Agent Self-Evolution for LLM Reasoning"
title_zh: SAGE：大语言模型推理的多智能体自我演化
authors: "Yulin Peng, Xinxin Zhu, Chenxing Wei, Nianbo Zeng, Leilei Wang, Ying Tiffany He, F. Richard Yu"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15255v2"
tags: ["query:ma-kf"]
score: 9.0
evidence: 用于推理的多智能体自我进化
tldr: SAGE是一个多智能体自我进化框架，旨在提升大语言模型的推理能力。针对现有方法依赖人工标注或缺乏规划控制的问题，SAGE通过挑战者、规划者、执行者和评论者四个角色的协同演化，实现了从少量种子数据出发的闭环自我训练。实验证明，该方法在数学和代码生成任务上显著提升了模型性能，为复杂推理任务提供了高效的自我进化方案。
selection_source: fresh_fetch
motivation: 现有的强化学习推理方法过度依赖人工标注，且自我博弈缺乏显式规划和质量控制，导致长程推理不稳定。
method: 提出SAGE框架，通过挑战者生成任务、规划者制定步骤、执行者求解、评论者过滤质量，实现四种角色的闭环协同进化。
result: "在Qwen-2.5-7B模型上，SAGE在LiveCodeBench和OlympiadBench上分别实现了8.9%和10.7%的性能提升。"
conclusion: SAGE通过多智能体协作和严格的质量控制，证明了在极少人工干预下实现模型推理能力持续进化的可行性。
---

## 摘要
具有可验证奖励的强化学习提升了大语言模型（LLMs）的推理能力，但许多方法仍依赖于大规模的人工标注数据集。虽然自我博弈（self-play）减少了这种依赖，但它往往缺乏显式的规划和强大的质量控制，限制了在长程多步推理中的稳定性。我们提出了 SAGE（用于广义推理演化的自我演化智能体），这是一个闭环框架，其中四个智能体：挑战者（Challenger）、规划者（Planner）、求解者（Solver）和评论者（Critic），仅使用少量的种子集，从共享的 LLM 主干网络中共同演化。挑战者不断生成难度递增的任务；规划者将每个任务转换为结构化的多步计划；求解者遵循该计划生成答案，其正确性由外部验证器确定。评论者对生成的题目和计划进行评分和过滤，以防止课程漂移（curriculum drift）并保持训练信号的质量，从而实现稳定的自我训练。在数学和代码生成基准测试中，SAGE 在不同模型规模上均实现了持续的提升，使 Qwen-2.5-7B 模型在 LiveCodeBench 上提升了 8.9%，在 OlympiadBench 上提升了 10.7%。

## Abstract
Reinforcement learning with verifiable rewards improves reasoning in large language models (LLMs), but many methods still rely on large human-labeled datasets. While self-play reduces this dependency, it often lacks explicit planning and strong quality control, limiting stability in long-horizon multi-step reasoning. We present SAGE (Self-evolving Agents for Generalized reasoning Evolution), a closed-loop framework where four agents: Challenger, Planner, Solver, and Critic, co-evolve from a shared LLM backbone using only a small seed set. The Challenger continuously generates increasingly difficult tasks; the Planner converts each task into a structured multi-step plan; and the Solver follows the plan to produce an answer, whose correctness is determined by external verifiers. The Critic scores and filters both generated questions and plans to prevent curriculum drift and maintain training signal quality, enabling stable self-training. Across mathematics and code-generation benchmarks, SAGE delivers consistent gains across model scales, improving the Qwen-2.5-7B model by 8.9% on LiveCodeBench and 10.7% on OlympiadBench.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **SAGE**（Self-evolving Agents for Generalized reasoning Evolution），一个旨在提升大语言模型（LLM）推理能力的**多智能体自我演化框架**。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **核心问题**：现有的强化学习（RL）方法（如 DeepSeek-R1）虽然能提升推理能力，但高度依赖大规模人工标注数据。现有的“自我博弈”（Self-play）方法虽减少了数据依赖，但往往缺乏显式的规划（Planning）和严格的质量控制，导致在处理长程、多步推理任务时表现不稳定，且容易出现“课程漂移”（生成的任务质量下降）。
*   **整体含义**：SAGE 旨在通过一个闭环的多智能体协作系统，仅利用极少量的种子数据（Seed Set），实现模型在数学和代码领域的自主进化。

### 2. 方法论：核心思想与技术细节
SAGE 在一个共享的 LLM 主干网络上实例化了四个专门的智能体角色，通过**Task-Relative REINFORCE++** 算法进行端到端联合训练：
*   **四个角色**：
    1.  **挑战者 (Challenger)**：基于种子题目生成难度递增的新任务及其验证器（如单元测试或标准答案）。
    2.  **规划者 (Planner)**：将任务分解为结构化的多步执行计划。
    3.  **求解者 (Solver)**：遵循计划生成最终答案。
    4.  **评论者 (Critic)**：对生成的题目质量和计划的逻辑性进行评分和过滤。
*   **关键技术**：
    *   **闭环演化**：挑战者因生成“求解者失败但题目本身高质量”的任务而获得奖励（难度奖励）；求解者因通过外部验证器（Verifier）而获得奖励。
    *   **质量过滤机制**：评论者设定阈值（如 $\alpha=0.7$），只有通过质量检查的任务才会被加入训练池，防止数据污染。
    *   **奖励设计**：结合了格式奖励（Format Reward）、质量得分（Critic Score）、难度奖励（Difficulty Reward）和外部验证正确性（Ground-truth Correctness）。
    *   **优势归一化**：采用按角色归一化的优势函数（Per-role Advantage Normalization），平衡不同智能体之间异构目标的更新。

### 3. 实验设计
*   **数据集/场景**：
    *   **数学推理**：GSM8K、MATH（分布内 ID）；AIME 2024/2025、OlympiadBench、AMC 2023（分布外 OOD/竞赛级）。
    *   **代码生成**：HumanEval+、MBPP+（ID）；LiveCodeBench v1-v5（OOD）。
*   **对比基准 (Baselines)**：
    *   原始模型：Qwen-2.5-3B/7B-Instruct, Qwen-3-4B-Base。
    *   训练基线：**Absolute-Zero-Reasoning (AZR)**（自我博弈推理）和 **Multi-Agent Evolve (MAE)**（多智能体演化）。

### 4. 资源与算力
*   **框架**：基于 **VeRL** 强化学习框架实现。
*   **微调技术**：使用 **LoRA**（Rank=128, Alpha=256），学习率为 3e-6。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量及总训练时长，但提到训练步数为 200 步，批量大小（Batch Size）为 128。

### 5. 实验数量与充分性
*   **实验规模**：在 3 种不同规模和版本的模型（3B、7B、4B）上进行了验证。
*   **消融实验**：分别移除了挑战者训练、求解者训练和评论者训练，验证了每个角色的必要性。
*   **充分性评价**：实验设计较为充分，涵盖了从基础推理到竞赛级难度的多个维度，并对比了最新的自我演化基线。通过 ID 和 OOD 的对比，客观地展示了模型泛化能力的提升。

### 6. 主要结论与发现
*   **性能显著提升**：SAGE 在所有模型规模上均取得领先。例如，在 Qwen-2.5-7B 上，LiveCodeBench 提升了 **8.9%**，OlympiadBench 提升了 **10.7%**。
*   **OOD 泛化能力强**：相比于其他方法，SAGE 在未见过的竞赛级题目上表现出更强的鲁棒性。
*   **自主数据扩展**：在 250 步训练中，题目池从 500 个种子扩展到了超过 20,000 个有效任务，实现了 18 倍的自主增长。
*   **训练动态**：观察到模型在 100-140 步左右达到性能峰值，随后可能出现对自我生成数据的过拟合（过度专业化）。

### 7. 优点（亮点）
*   **角色分工明确**：引入“规划者”和“评论者”解决了纯自我博弈中逻辑链条断裂和质量失控的问题。
*   **极低数据依赖**：仅需 500 条种子数据即可启动，展示了迈向“零人工数据”演化的潜力。
*   **稳定性高**：通过格式奖励和质量过滤，有效缓解了强化学习在长文本生成中的崩溃问题。

### 8. 不足与局限
*   **领域局限性**：目前仅适用于数学和代码等具有**可验证奖励（Verifiable Rewards）**的领域，难以直接推广到文学创作或主观问答等缺乏客观验证标准的领域。
*   **种子数据依赖**：虽然需求量小，但仍需初始种子集来引导演化方向。
*   **过拟合风险**：长期自我演化可能导致模型在特定分布的任务上过拟合，需要更精细的早停策略或课程多样性控制。

（完）
