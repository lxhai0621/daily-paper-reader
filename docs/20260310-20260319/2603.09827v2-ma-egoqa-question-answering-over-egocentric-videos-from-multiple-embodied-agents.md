---
title: "MA-EgoQA: Question Answering over Egocentric Videos from Multiple Embodied Agents"
title_zh: MA-EgoQA：基于多个具身智能体第一视角视频的问答
authors: "Kangsan Kim, Yanlai Yang, Suji Kim, Woongyeong Yeo, Youngwan Lee, Mengye Ren, Sung Ju Hwang"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09827v2"
tags: ["query:ma-kf"]
score: 9.0
evidence: 多个具身智能体之间的协作与通信
tldr: 随着多具身智能体协作场景的兴起，如何理解并整合多个第一视角视频流成为关键。本文定义了多智能体长时程第一视角视频理解的新问题，并推出了MA-EgoQA基准测试，包含1.7k个涵盖社交、协作等五类任务的问题。同时提出了EgoMAS基准模型，通过共享内存和动态检索来处理多流信息。研究表明现有模型在多智能体系统级理解上仍有巨大提升空间。
selection_source: fresh_fetch
motivation: 旨在解决多具身智能体协作中，如何有效压缩、整合并理解来自多个第一视角视频流的海量感官信息。
method: 提出了MA-EgoQA基准测试及EgoMAS模型，后者利用跨智能体共享内存和智能体维度的动态检索机制进行问答。
result: 实验显示现有模型在处理多路第一视角视频流时表现不佳，难以实现有效的系统级记忆构建与推理。
conclusion: 该研究强调了开发能够同时处理并整合多个智能体视角的系统级理解能力的必要性。
---

## 摘要
随着具身模型变得日益强大，未来人类将在工作场所或家中与多个具身 AI 智能体进行协作。为了确保人类用户与多智能体系统之间更好的沟通，并行解释来自智能体的输入信息并为每个查询引用适当的上下文至关重要。现有的挑战包括：如何有效地压缩和传输视频形式的大量个体感官输入，以及如何正确聚合多个第一视角视频以构建系统级记忆。在这项工作中，我们首先正式定义了一个新颖的问题，即理解从具身智能体同时采集的多个长时程第一视角视频。为了促进这一方向的研究，我们引入了 MultiAgent-EgoQA (MA-EgoQA)，这是一个旨在系统评估现有模型在我们设定场景下表现的基准测试。MA-EgoQA 提供了 1.7k 个针对多第一视角流的独特问题，涵盖五个类别：社交互动、任务协调、心智理论（theory-of-mind）、时间推理和环境交互。我们进一步为 MA-EgoQA 提出了一个名为 EgoMAS 的简单基准模型，该模型利用了具身智能体间的共享记忆和针对单个智能体的动态检索。通过在 MA-EgoQA 上对多种基准模型和 EgoMAS 进行全面评估，我们发现当前的方法无法有效处理多个第一视角流，这凸显了未来在跨智能体系统级理解方面取得进展的必要性。代码和基准测试可在 https://ma-egoqa.github.io 获取。

## 速览
**TLDR**：随着多具身智能体协作场景的兴起，如何理解并整合多个第一视角视频流成为关键。本文定义了多智能体长时程第一视角视频理解的新问题，并推出了MA-EgoQA基准测试，包含1.7k个涵盖社交、协作等五类任务的问题。同时提出了EgoMAS基准模型，通过共享内存和动态检索来处理多流信息。研究表明现有模型在多智能体系统级理解上仍有巨大提升空间。 \
**Motivation**：旨在解决多具身智能体协作中，如何有效压缩、整合并理解来自多个第一视角视频流的海量感官信息。 \
**Method**：提出了MA-EgoQA基准测试及EgoMAS模型，后者利用跨智能体共享内存和智能体维度的动态检索机制进行问答。 \
**Result**：实验显示现有模型在处理多路第一视角视频流时表现不佳，难以实现有效的系统级记忆构建与推理。 \
**Conclusion**：该研究强调了开发能够同时处理并整合多个智能体视角的系统级理解能力的必要性。

---

## Abstract
As embodied models become powerful, humans will collaborate with multiple embodied AI agents at their workplace or home in the future. To ensure better communication between human users and the multi-agent system, it is crucial to interpret incoming information from agents in parallel and refer to the appropriate context for each query. Existing challenges include effectively compressing and communicating high volumes of individual sensory inputs in the form of video and correctly aggregating multiple egocentric videos to construct system-level memory. In this work, we first formally define a novel problem of understanding multiple long-horizon egocentric videos simultaneously collected from embodied agents. To facilitate research in this direction, we introduce MultiAgent-EgoQA (MA-EgoQA), a benchmark designed to systemically evaluate existing models in our scenario. MA-EgoQA provides 1.7k questions unique to multiple egocentric streams, spanning five categories: social interaction, task coordination, theory-of-mind, temporal reasoning, and environmental interaction. We further propose a simple baseline model for MA-EgoQA named EgoMAS, which leverages shared memory across embodied agents and agent-wise dynamic retrieval. Through comprehensive evaluation across diverse baselines and EgoMAS on MA-EgoQA, we find that current approaches are unable to effectively handle multiple egocentric streams, highlighting the need for future advances in system-level understanding across the agents. The code and benchmark are available at https://ma-egoqa.github.io.

---

## 论文详细总结（自动生成）

以下是对论文《MA-EgoQA: Question Answering over Egocentric Videos from Multiple Embodied Agents》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在多具身智能体（Multi-Embodied Agents）协作的环境中，理解并整合来自多个智能体、长达数天的并行第一视角（Egocentric）视频流，以实现系统级的问答与决策支持。
*   **研究动机**：
    *   **协作趋势**：未来人类将与多个 AI 智能体共同生活或工作，智能体间需要共享经验。
    *   **现有局限**：现有的视频问答（Video QA）基准测试大多针对单智能体或短视频，无法处理多路并行、超长时程（如 7 天）且需要跨视角融合的复杂场景。
    *   **技术挑战**：如何高效压缩海量视频数据、如何构建跨智能体的系统级记忆、以及如何从碎片化的信息中检索出正确答案。

### 2. 方法论：EgoMAS 模型
论文提出了一个无需训练（Training-free）的基准模型 **EgoMAS**，其核心思想是构建“共享记忆”并进行“动态检索”：
*   **基于事件的共享记忆（Event-based Shared Memory）**：
    *   将每个智能体每 10 分钟的视频生成简短描述。
    *   中央管理器将这些描述整合为结构化的 **4W1H** 字段（时间 When、地点 Where、人物 Who、事件 What、方式 How），形成全局一致的系统级记忆。
*   **智能体维度动态检索（Agent-wise Dynamic Retrieval）**：
    *   **第一步（系统级检索）**：根据用户查询，利用 BM25 算法从共享记忆中检索出最相关的 $n$ 个系统事件。
    *   **第二步（智能体级检索）**：基于检索到的系统上下文，生成针对特定智能体的子查询，从该智能体的原始记忆中提取更细粒度的细节。
    *   **第三步（响应生成）**：整合系统级和智能体级的检索结果，由大模型生成最终答案。

### 3. 实验设计
*   **数据集与基准测试（MA-EgoQA）**：
    *   基于 **EgoLife** 数据集：包含 6 个人在共享房屋中生活 7 天的视频，总计 266 小时。
    *   **1.7k 个问答对**，涵盖五大类：社交互动（SI）、任务协调（TC）、心智理论（ToM）、时间推理（TR）、环境交互（EI）。
    *   **构建流程**：GPT-4o/5 生成候选 -> 自动化过滤（剔除无上下文可答、单智能体可答、模型偏见样本） -> 人工验证。
*   **对比方法**：
    *   **全文本/视频拼接基准**：Gemini-2.5-Flash (1M context), GPT-5, Qwen2.5-VL 等。
    *   **RAG 基准**：VideoRAG, EgoRAG, BM25, DPR 等。
    *   **Oracle**：提供生成问题时所用的原始上下文作为参考。

### 4. 资源与算力
*   **训练算力**：论文未明确说明训练算力，因为提出的 EgoMAS 是**无需训练**的推理框架，主要依赖现有的预训练大模型（如 Gemini, Qwen, GPT-5）进行推理。
*   **推理效率**：文中提到了推理延迟（Latency）。EgoMAS 在 Qwen2.5VL-7B 骨干网络下，平均每个查询的推理时间约为 **1.3 秒**，远低于全文本拼接方法（如 Gemini 需 20 秒以上）。

### 5. 实验数量与充分性
*   **实验规模**：对比了 **16 种** 不同的基准模型（包括闭源最强模型和开源模型）。
*   **消融实验**：
    *   验证了共享记忆结构（4W1H vs. 纯摘要 vs. 图结构）。
    *   测试了不同检索器（BM25 vs. 稠密检索器）的影响。
    *   分析了超参数（检索数量 $n, k$ 和阈值 $\tau$）的敏感性。
*   **充分性评价**：实验设计较为客观、公平。通过“单智能体记忆 vs. 多智能体记忆”的对比实验，有力证明了该基准测试确实需要跨智能体的信息融合能力。

### 6. 主要结论与发现
*   **任务难度极高**：即使是目前最强的 Gemini-2.5-Flash，在全量输入下的准确率也仅为 36.93%，远低于 Oracle（83.80%）。
*   **心智理论（ToM）最难**：所有模型在 ToM 类别上的表现普遍最差，说明模型难以推断智能体的隐性意图和错误信念。
*   **检索优于拼接**：在处理超长视频流时，智能检索（如 EgoMAS）比简单地将所有信息塞入长上下文窗口更有效且更节省 Token。
*   **多智能体融合瓶颈**：随着回答问题所需涉及的智能体数量增加，模型表现显著下降。

### 7. 优点与亮点
*   **首创性**：首次定义了多具身智能体长时程视频理解问题，填补了该领域的空白。
*   **高质量基准**：通过严格的“单智能体过滤”确保了问题必须通过整合多个视角才能回答，具有极高的区分度。
*   **高效框架**：EgoMAS 提出的 4W1H 结构化记忆和两阶段检索机制，为处理海量多路视频数据提供了务实的工程思路。

### 8. 不足与局限
*   **场景单一**：目前仅基于 EgoLife 一个数据集（居家场景），缺乏工业、户外或极端环境下的泛化验证。
*   **性能差距**：EgoMAS 虽然优于现有基准，但距离人类水平或 Oracle 表现仍有巨大鸿沟（约 42% 的差距）。
*   **模态利用**：实验显示，加入视频帧有时会引入噪声反而降低某些类别的准确率，如何更优雅地融合视觉与文本模态仍需研究。

（完）
