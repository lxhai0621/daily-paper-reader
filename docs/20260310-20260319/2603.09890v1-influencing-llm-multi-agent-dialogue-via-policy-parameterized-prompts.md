---
title: Influencing LLM Multi-Agent Dialogue via Policy-Parameterized Prompts
title_zh: 通过策略参数化提示词影响大语言模型多智能体对话
authors: "Hongbo Bo, Jingyu Hu, Weiru Liu"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09890v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 在多智能体对话策略中将提示优化为动作
tldr: 本研究针对大语言模型多智能体系统缺乏系统性策略控制的问题，提出了一种“提示词即动作”的创新框架。该方法通过将提示词参数化，根据智能体当前状态动态构建包含五个组件的轻量级策略，无需额外训练即可引导对话行为。实验通过响应性、立场转变等五个指标评估了其在公共讨论场景下的有效性。结果证明，该机制能显著影响对话动态，为多智能体社会模拟研究提供了简单高效的控制手段。
selection_source: fresh_fetch
motivation: 现有大语言模型多智能体系统主要依赖临时性提示词，缺乏一种基于策略的系统性方法来控制和引导智能体的对话行为。
method: 提出一种将提示词视为动作的框架，通过五个核心组件根据智能体当前状态动态构建参数化提示词，实现无需训练的轻量级策略控制。
result: 在不同场景的实验中，该方法成功通过响应性、反驳程度和立场转变等指标证明了其对对话动态的有效影响。
conclusion: 策略参数化提示词为影响多智能体对话过程提供了一种简单且有效的机制，对社会模拟等领域的研究具有重要意义。
---

## 摘要
大语言模型（LLMs）已成为多智能体系统的一种新范式。然而，现有关于基于 LLM 的多智能体行为的研究依赖于临时（ad hoc）提示词，缺乏原则性的策略视角。与强化学习不同，我们研究了“提示词即动作”（prompt-as-action）是否可以被参数化，从而构建一种由一系列状态-动作对组成的轻量级策略，在无需训练的情况下影响对话行为。我们的框架将提示词视为 LLM 执行的动作，并根据智能体的当前状态，通过五个组件动态构建提示词。为了测试参数化控制的有效性，我们基于五个指标评估了对话流：响应性、反驳、证据使用、非重复性和立场转变。我们在两个与公众相关的讨论场景中使用不同的 LLM 驱动智能体进行了实验，结果表明提示词参数化可以影响对话动态。这一结果表明，策略参数化提示词提供了一种简单且有效的机制来影响对话过程，这将有助于多智能体系统在社会模拟方向的研究。

## Abstract
Large Language Models (LLMs) have emerged as a new paradigm for multi-agent systems. However, existing research on the behaviour of LLM-based multi-agents relies on ad hoc prompts and lacks a principled policy perspective. Different from reinforcement learning, we investigate whether prompt-as-action can be parameterized so as to construct a lightweight policy which consists of a sequence of state-action pairs to influence conversational behaviours without training. Our framework regards prompts as actions executed by LLMs, and dynamically constructs prompts through five components based on the current state of the agent. To test the effectiveness of parameterized control, we evaluated the dialogue flow based on five indicators: responsiveness, rebuttal, evidence usage, non-repetition, and stance shift. We conduct experiments using different LLM-driven agents in two discussion scenarios related to the general public and show that prompt parameterization can influence the dialogue dynamics. This result shows that policy-parameterised prompts offer a simple and effective mechanism to influence the dialogue process, which will help the research of multi-agent systems in the direction of social simulation.

---

## 论文详细总结（自动生成）

这篇论文由布里斯托大学的研究团队撰写，已被 AAMAS 2026 接收。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：大语言模型（LLM）多智能体系统（MAS）在社会模拟中表现出巨大潜力，但现有研究大多依赖于“临时性（ad hoc）”的提示词（Prompts）。
*   **核心痛点**：缺乏一种原则性的、系统性的框架来定义和优化智能体之间的通信策略。这导致智能体行为难以预测、通信模式难以优化，且研究结论难以在不同任务间迁移。
*   **研究动机**：探索是否可以将“提示词”视为强化学习中的“动作（Action）”，通过参数化的方式构建轻量级策略，从而在无需重新训练模型的情况下，精确引导多智能体对话的行为。

### 2. 方法论：策略参数化提示词框架
*   **核心思想**：将提示词分解为可调参数的组件，通过动态构建提示词来控制智能体的输出风格和内容。
*   **关键组件（五元组）**：
    1.  **T (Task & Persona)**：任务描述与人格设定，决定智能体的立场。
    2.  **M (Memory)**：对话历史记忆，确保上下文连贯性。
    3.  **D (Knowledge Base)**：外部知识库，通过 RAG（检索增强生成）获取事实依据。
    4.  **R (Rule Template)**：规则模板，分为三级：`None`（无约束）、`Light`（简单结构/长度限制）、`Struct`（深度推理结构，如提取支持/反对点）。
    5.  **W (Weights)**：权重向量，控制智能体对 T、M、D 的依赖程度（分为低、中、高三档信号）。
*   **动态调整机制**：
    *   **时间趋势更新**：对话早期侧重知识（D），后期侧重对话历史（M）。
    *   **行为反馈修正**：若智能体在前一轮未引用证据或未响应对方，则自动调高相应组件的权重。

### 3. 实验设计
*   **实验场景**：
    1.  **土地资源利用（Land）**：涉及农民、环保人士、社区代表。
    2.  **教育资源分配（Education）**：涉及农村教师、城市家长、政策制定者。
*   **智能体配置**：采用异质化模型底座，分别由 **Qwen3-8B**、**Llama3-8B** 和 **Mistral-7B** 驱动。
*   **评估指标**：
    *   **响应性 (Resp.)**：是否回应了上一轮发言。
    *   **反驳程度 (Rebuttal)**：是否表达了反对立场。
    *   **非重复性 (Non-rep.)**：与自身历史发言的差异度。
    *   **证据使用 (Evid.)**：是否引用了外部知识库内容。
    *   **立场转变 (Stance)**：发言与原始人格设定的语义相似度。
*   **对比实验**：对比了不同规则模板（None/Light/Struct）、不同权重配置以及同质化 vs 异质化模型底座的表现。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **资源消耗分析**：由于该方法属于“无需训练（Training-free）”的轻量级策略，其主要算力消耗在于 LLM 的推理过程。实验涉及 10 轮对话、多个 Query 和多次重复实验，推理开销主要取决于所选的 7B/8B 参数模型。

### 5. 实验数量与充分性
*   **实验规模**：
    *   每个场景设计了 5 个核心议题（Query）。
    *   每个议题进行 5 次独立运行，每次运行包含 10 轮对话。
    *   进行了详细的**消融实验**（针对 T、M、D 组件的排列组合）。
    *   进行了**权重敏感性分析**和**自适应权重测试**。
*   **充分性评价**：实验设计较为全面，不仅验证了框架的有效性，还探讨了模型多样性（异质化底座）对结果的影响。通过 LLM-as-a-Judge（使用 Llama3 作为裁判）和 Embedding 相似度计算，评估方式相对客观。

### 6. 主要结论与发现
*   **提示词控制有效性**：通过参数化提示词，可以系统地调节智能体的反驳频率、证据引用率等行为，证明了“提示词即策略”的可行性。
*   **规则模板的权衡**：`Struct` 规则能显著减少重复，但可能抑制证据的自然使用；`Light` 规则在促进证据引用方面效果最佳。
*   **权重的影响**：强化人格权重（W_T）会显著增加对话中的冲突（反驳率）并增强立场稳定性。
*   **模型多样性的价值**：使用不同厂商的 LLM 作为底座（异质化）比使用单一模型（同质化）能产生更丰富、更具互动性的对话动态。

### 7. 优点与亮点
*   **轻量且高效**：无需昂贵的模型微调，仅通过提示词工程的结构化管理即可实现复杂的行为控制。
*   **可解释性强**：将模糊的提示词转化为具有明确物理意义的参数（权重、规则），使社会模拟过程更具可控性和可观察性。
*   **自适应能力**：引入了基于对话进程和行为反馈的动态权重调整，模拟了人类在讨论中策略转移的过程。

### 8. 不足与局限
*   **模型规模限制**：实验主要集中在 7B/8B 规模的开源模型上，未验证该策略在超大规模模型（如 GPT-4o）或更小规模模型上的泛化能力。
*   **裁判模型偏差**：虽然使用了独立的裁判模型，但 LLM 评估本身可能存在偏见（如对特定格式的偏好）。
*   **场景覆盖面**：目前仅限于公共政策讨论场景，对于需要高度协作（如代码编写）或强竞争（如博弈游戏）的场景，该策略的有效性尚待验证。

（完）
