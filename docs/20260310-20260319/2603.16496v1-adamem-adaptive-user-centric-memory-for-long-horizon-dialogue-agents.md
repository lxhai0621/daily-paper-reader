---
title: "AdaMem: Adaptive User-Centric Memory for Long-Horizon Dialogue Agents"
title_zh: AdaMem：面向长程对话智能体的自适应以用户为中心的记忆系统
authors: "Shannan Yan, Jingchen Ni, Leqi Zheng, Jiajun Zhang, Peixi Wu, Dacheng Yin, Jing Lyu, Chun Yuan, Fengyun Rao"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16496v1"
tags: ["query:agent"]
score: 10.0
evidence: 长周期对话智能体的自适应用户中心记忆
tldr: 针对长程对话智能体在记忆检索中存在的语义依赖过强、缺乏连贯性及粒度固定等问题，本文提出 AdaMem 框架。该框架通过整合工作记忆、情景记忆、人格记忆和图记忆，构建了多维度的用户中心记忆系统。在推理阶段，AdaMem 采用问题驱动的自适应检索路径和角色专业化流水线，实现了对复杂长程交互的高效建模。实验证明，AdaMem 在 LoCoMo 和 PERSONAMEM 基准测试中均达到 SOTA 水平。
selection_source: fresh_fetch
motivation: 现有记忆系统过度依赖语义相似度且缺乏时序因果连贯性，难以适应不同问题的动态粒度需求。
method: 提出一种自适应记忆框架，通过多维记忆组织（工作、情景、人格、图）和问题驱动的动态检索路径来增强长程对话能力。
result: 在 LoCoMo 和 PERSONAMEM 两个长程推理与用户建模基准测试中，AdaMem 均取得了当前最先进的性能表现。
conclusion: AdaMem 通过多层次记忆结构与自适应检索机制，有效提升了对话智能体在长周期交互中的个性化理解与推理能力。
---

## 摘要
大语言模型（LLM）智能体日益依赖外部记忆来支持长程交互、个性化辅助和多步推理。然而，现有的记忆系统仍面临三个核心挑战：它们往往过度依赖语义相似性，这可能会遗漏对以用户为中心的理解至关重要的证据；它们经常将相关的经历存储为孤立的片段，削弱了时间与因果的连贯性；且它们通常使用静态的记忆粒度，无法很好地适应不同问题的需求。我们提出了 AdaMem，一种面向长程对话智能体的自适应以用户为中心的记忆框架。AdaMem 将对话历史组织为工作记忆、情景记忆、人格记忆和图记忆，使系统能够在统一框架内保留近期上下文、结构化的长期经历、稳定的用户特征以及关系感知的连接。在推理时，AdaMem 首先解析目标参与者，然后构建一个以问题为条件的检索路径，该路径仅在需要时将语义检索与关系感知图扩展相结合，最后通过角色专门化的流水线进行证据合成和响应生成，从而产生答案。我们在 LoCoMo 和 PERSONAMEM 基准测试上评估了 AdaMem，用于长程推理和用户建模。实验结果表明，AdaMem 在这两个基准测试上均达到了最先进的性能。代码将在论文被接收后发布。

## Abstract
Large language model (LLM) agents increasingly rely on external memory to support long-horizon interaction, personalized assistance, and multi-step reasoning. However, existing memory systems still face three core challenges: they often rely too heavily on semantic similarity, which can miss evidence crucial for user-centric understanding; they frequently store related experiences as isolated fragments, weakening temporal and causal coherence; and they typically use static memory granularities that do not adapt well to the requirements of different questions. We propose AdaMem, an adaptive user-centric memory framework for long-horizon dialogue agents. AdaMem organizes dialogue history into working, episodic, persona, and graph memories, enabling the system to preserve recent context, structured long-term experiences, stable user traits, and relation-aware connections within a unified framework. At inference time, AdaMem first resolves the target participant, then builds a question-conditioned retrieval route that combines semantic retrieval with relation-aware graph expansion only when needed, and finally produces the answer through a role-specialized pipeline for evidence synthesis and response generation. We evaluate AdaMem on the LoCoMo and PERSONAMEM benchmarks for long-horizon reasoning and user modeling. Experimental results show that AdaMem achieves state-of-the-art performance on both benchmarks. The code will be released upon acceptance.

---

## 论文详细总结（自动生成）

以下是对论文《AdaMem: Adaptive User-Centric Memory for Long-Horizon Dialogue Agents》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对长程对话智能体（Long-Horizon Dialogue Agents）在记忆管理上的三个核心局限提出了挑战：
*   **语义依赖过强**：现有系统过度依赖语义相似性检索，容易忽略与查询词不直接相关但对理解用户至关重要的稳定偏好或行为模式。
*   **记忆碎片化**：将经历存储为孤立片段，削弱了事件间的时序和因果连贯性，难以重建事件发展的全貌。
*   **静态记忆粒度**：采用固定长度的分块或粗粒度摘要，无法根据不同问题的需求（如事实查询 vs. 复杂推理）动态调整检索深度。

**AdaMem** 的提出旨在构建一个**自适应的、以用户为中心**的记忆框架，通过多层次记忆组织和动态检索路径来解决上述问题。

### 2. 方法论：核心思想与技术细节
AdaMem 采用了一种多维记忆结构和基于角色分工的智能体流水线：

*   **四种记忆结构**：
    *   **工作记忆 (Working Memory)**：有限容量的 FIFO 缓冲区，保留近期对话上下文。
    *   **情景记忆 (Episodic Memory)**：长期结构化记录，包括事件、事实、属性和主题摘要。
    *   **人格记忆 (Persona Memory)**：从情景记忆中提炼的稳定用户特质和偏好。
    *   **图记忆 (Graph Memory)**：异构图结构，连接消息、主题、事实和属性，支持关系感知检索。
*   **记忆构建流程**：
    1.  **归一化写入**：Memory Agent 将每条消息解析为包含主题、态度、事实和属性的标准化记录。
    2.  **整合与刷新**：当工作记忆满时，将旧消息合并至情景记忆，并触发主题重组、人格刷新和图同步。
*   **自适应检索与生成**：
    1.  **目标解析**：确定问题指向的是用户、助手还是两者。
    2.  **路径规划**：根据问题类型（如时序、因果、属性）决定是否启动图扩展及检索深度。
    3.  **证据融合**：结合语义检索、图检索、时序先验和事实置信度进行综合评分。
    4.  **多智能体协作**：Research Agent 负责迭代搜索与证据整合，Working Agent 负责最终答案生成。

### 3. 实验设计
*   **数据集/基准**：
    *   **LoCoMo**：评估长程推理能力（单跳、多跳、时序推理及开放域问答）。
    *   **PERSONAMEM**：评估用户建模能力（属性召回、偏好演化跟踪、新场景泛化等）。
*   **对比方法**：对比了 5 种代表性记忆框架：MemGPT、A-Mem、Mem0、LangMem 和 Zep。
*   **评估指标**：F1 分数、BLEU-1 和准确率（Accuracy）。

### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA RTX A800 GPU** 上运行。
*   **模型**：
    *   闭源模型：GPT-4.1-mini, GPT-4o-mini。
    *   开源模型：Qwen3-4B-Instruct, Qwen3-30B-A3B-Instruct。
*   **训练/运行细节**：文中未提及具体的训练时长（主要基于推理框架），但详细记录了推理时的超参数（如 Top-K=10，检索迭代次数 Li=2，嵌入模型使用 all-MiniLM-L6-v2）。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流基准上进行了全面测试，并针对 GPT-4 系列和 Qwen3 系列模型进行了跨模型验证。
*   **消融实验**：针对图记忆、融合模块、多智能体协作分别进行了消融，并研究了模型规模、检索 Top-K 值和迭代次数对性能的影响。
*   **客观性与公平性**：实验设置了固定的温度系数（0）以确保可重复性，并对比了多种 SOTA 方法，涵盖了从系统级设计到图结构的多种记忆范式，实验设计较为客观、充分。

### 6. 主要结论与发现
*   **性能领先**：AdaMem 在 LoCoMo 上相比之前最好的方法实现了显著提升（GPT-4.1-mini 下 F1 提升 4.4%，GPT-4o-mini 下提升 12.8%）。
*   **时序推理优势**：在时序问题类别中，AdaMem 的 F1 分数提升高达 23.4%，证明了图记忆和自适应路径规划在处理复杂时间线方面的有效性。
*   **泛化能力**：在 PERSONAMEM 的“新场景泛化”任务中，准确率相对提升了 27.3%，显示出强大的用户建模能力。
*   **效率平衡**：虽然 AdaMem 的延迟略高于 Mem0 等简单系统，但其通过适度的计算开销换取了大幅的推理质量提升。

### 7. 优点（亮点）
*   **多层次记忆协同**：将短期上下文与长期结构化知识（人格、图、情景）有机结合，解决了记忆碎片化问题。
*   **自适应检索路径**：不再使用单一的检索策略，而是根据问题类型动态决定是否使用图扩展，提高了检索的精准度。
*   **角色专门化**：通过 Memory/Research/Working Agent 的分工，减少了记忆维护与答案生成之间的干扰。

### 8. 不足与局限
*   **系统复杂度**：多智能体协作和多维记忆维护增加了系统的实现难度和 Token 消耗。
*   **依赖上游解析**：系统性能高度依赖于初始的消息理解（归一化写入），如果实体链接或时序归一化在解析阶段出错，后续检索难以纠正。
*   **推理延迟**：由于存在迭代搜索和路径规划，端到端推理延迟（约 4.7 秒）高于部分轻量级基准方法。

（完）
