---
title: "Context-Length Robustness in Question Answering Models: A Comparative Empirical Study"
title_zh: 问答模型中的上下文长度鲁棒性：一项比较实证研究
authors: "Trishita Dhara, Siddhesh Sheth"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15723v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 问答模型中上下文长度鲁棒性的实证研究
tldr: 本研究针对大语言模型在长且多噪声上下文中的鲁棒性进行了实证研究。通过在SQuAD和HotpotQA基准上系统性增加无关上下文，研究发现模型性能随长度增加而下降，且多跳推理任务（HotpotQA）的准确率下降幅度几乎是单跨度提取任务（SQuAD）的两倍。该研究揭示了任务类型对上下文鲁棒性的显著影响，强调了在长文档应用中评估此类鲁棒性的重要性。
selection_source: fresh_fetch
motivation: 旨在探究大语言模型在处理包含大量无关信息的长上下文时，其问答性能的鲁棒性及任务依赖性。
method: 通过在保持答案相关信号不变的情况下，系统性地增加无关上下文长度，对比评估模型在SQuAD和HotpotQA上的表现。
result: 随着上下文长度增加，模型性能普遍下降，且多跳推理任务的准确率降幅约为单跨度提取任务的两倍。
conclusion: 多跳推理对上下文稀释更为敏感，建议在评估长文档或检索增强生成应用的模型可靠性时，应明确考察上下文长度鲁棒性。
---

## 摘要
大语言模型越来越多地被部署在相关信息嵌入长且多噪声上下文的场景中。尽管如此，在不同的问答任务中，模型对不断增长的上下文长度的鲁棒性仍未得到充分理解。在这项工作中，我们利用两个广泛使用的基准测试（SQuAD 和 HotpotQA），对大语言模型的上下文长度鲁棒性进行了受控实证研究。我们通过系统地增加无关上下文的数量，同时保留包含答案的信号，来评估模型准确率随总上下文长度变化的情况。这使我们能够将上下文长度的影响与任务难度的变化隔离开来。我们的结果显示，随着上下文长度的增加，性能会出现一致的下降，且与单跨度抽取任务相比，多跳推理任务的性能下降幅度明显更大。特别是，在等效的上下文扩展下，HotpotQA 的准确率下降几乎是 SQuAD 的两倍。这些发现强调了鲁棒性在不同任务间的差异，并表明多跳推理特别容易受到上下文稀释的影响。我们认为，在评估模型可靠性时，应明确评估上下文长度鲁棒性，特别是对于涉及长文档或检索增强生成的应用。

## Abstract
Large language models are increasingly deployed in settings where relevant information is embedded within long and noisy contexts. Despite this, robustness to growing context length remains poorly understood across different question answering tasks. In this work, we present a controlled empirical study of context-length robustness in large language models using two widely used benchmarks: SQuAD and HotpotQA.   We evaluate model accuracy as a function of total context length by systematically increasing the amount of irrelevant context while preserving the answer-bearing signal. This allows us to isolate the effect of context length from changes in task difficulty. Our results show a consistent degradation in performance as context length increases, with substantially larger drops observed on multi-hop reasoning tasks compared to single-span extraction tasks. In particular, HotpotQA exhibits nearly twice the accuracy degradation of SQuAD under equivalent context expansions.   These findings highlight task-dependent differences in robustness and suggest that multi-hop reasoning is especially vulnerable to context dilution. We argue that context-length robustness should be evaluated explicitly when assessing model reliability, especially for applications involving long documents or retrieval-augmented generation.

---

## 论文详细总结（自动生成）

以下是对论文《Context-Length Robustness in Question Answering Models: A Comparative Empirical Study》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：研究大语言模型（LLMs）在处理包含大量无关信息的长上下文时，其问答（QA）性能的**鲁棒性**如何随长度增加而变化。
*   **研究动机**：在检索增强生成（RAG）和长文档分析等实际应用中，模型常面临“信号”被淹没在大量“噪声”中的情况。现有研究往往将任务难度与上下文长度混为一谈，本研究旨在通过受控实验，**隔离出上下文长度这一单一变量**，探究其对不同类型推理任务（单跳 vs 多跳）的影响。

### 2. 方法论
*   **核心思想**：采用“受控上下文扩展”法。保持包含答案的关键信号（Signal）不变，通过填充无关的干扰项（Distractors）来系统性地增加总上下文长度。
*   **关键技术细节**：
    *   **信号段构建**：从原始上下文中提取包含正确答案的 256 字符窗口。
    *   **干扰项注入**：从同数据集的其他实例中抽取 128 字符的无关片段作为干扰项，并排除包含答案字符串的片段以防泄露。
    *   **长度设置**：设定 256、512、1024、2048 四个目标长度。
*   **数学建模**：
    *   将准确率定义为长度 $L$ 的函数 $Acc_M(L)$。
    *   提出**信噪比视角**：假设注意力机制在无先验情况下均匀分布，信号获得的注意力质量与 $1/L$ 成正比。对于需要 $k$ 步推理的多跳任务，成功概率按 $(1/L)^k$ 衰减，解释了为何多跳任务对长度更敏感。

### 3. 实验设计
*   **数据集**：
    *   **SQuAD**：代表单跨度答案提取任务（局部证据提取）。
    *   **HotpotQA**：代表多跳推理任务（需整合多个文档的信息）。
*   **对比模型**：使用了 OpenAI 的 **gpt-4.1**（即 GPT-4o）和 **gpt-4.1-mini**，以观察不同参数规模模型的表现差异。
*   **评估指标**：精确匹配（Exact Match, EM）。
*   **实验设置**：解码温度设为 0（确定性输出），每个数据集随机抽取 200 个样本进行评估。

### 4. 资源与算力
*   **算力说明**：文中**未明确提及**具体的 GPU 型号、数量或训练时长。
*   **实现方式**：由于评估的是闭源模型（GPT-4 系列），实验主要通过 API 调用完成。作者提到了使用了指数退避算法处理速率限制，并对结果进行了缓存以防重复计算。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 16 组核心配置实验（2 种模型 × 2 个数据集 × 4 种长度），每组配置包含 200 个样本。
*   **充分性评价**：
    *   **客观性**：通过固定信号段和随机种子，确保了不同长度间的对比是公平的。
    *   **统计学支持**：报告了 95% 的自助法（Bootstrap）置信区间，证明了 HotpotQA 的性能下降在统计上是显著的。
    *   **局限性**：200 个样本的规模相对较小，且仅测试了最高 2048 字符的长度，对于目前动辄 128k 甚至更长上下文的模型来说，测试范围较窄。

### 6. 主要结论与发现
*   **性能普遍下降**：随着无关上下文增加，所有模型的准确率均呈下降趋势。
*   **任务依赖性显著**：多跳推理（HotpotQA）对上下文稀释极其敏感，准确率下降幅度（约 25-27%）几乎是单跳提取（SQuAD，约 2-6%）的**数倍**。
*   **模型规模的影响**：虽然大模型（gpt-4.1）的绝对准确率更高，但其随长度增加而退化的**模式与小模型高度一致**，说明增加参数量并不能消除这种结构性的鲁棒性问题。
*   **推理复杂性是瓶颈**：干扰项对需要跨片段整合信息的任务干扰更大。

### 7. 优点与亮点
*   **变量隔离**：实验设计精巧，成功排除了任务难度、答案歧义等干扰因素，纯粹考察了“长度/噪声”对模型的影响。
*   **理论解释**：引入了简单的概率衰减模型，为实验观察到的多跳任务性能骤降提供了直观的数学解释。
*   **实用性**：研究结论直接提醒了 RAG 开发者，在处理复杂推理任务时，简单的“长上下文”并不能替代精准的检索。

### 8. 不足与局限
*   **长度覆盖不足**：实验最高仅到 2048 字符，未能覆盖现代 LLM 的超长上下文窗口（如 32k+）。
*   **模型多样性欠缺**：仅测试了 GPT-4 系列，未包含 Claude、Gemini 或开源模型（如 Llama），可能存在模型家族偏差。
*   **噪声类型单一**：仅使用了无关文本作为噪声，未考虑矛盾信息、误导性信息等更复杂的对抗性噪声。
*   **评估指标单一**：Exact Match 过于严格，可能无法捕捉到模型在长上下文中虽然没答对但推理逻辑部分正确的“部分正确”情况。

（完）
