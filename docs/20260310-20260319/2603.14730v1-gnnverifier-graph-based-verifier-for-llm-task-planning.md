---
title: "GNNVerifier: Graph-based Verifier for LLM Task Planning"
title_zh: GNNVerifier：基于图的大语言模型任务规划验证器
authors: "Yu Hao, Qiuyu Wang, Cheng Yang, Yawen Li, Zhiqiang Zhang, Chuan Shi"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14730v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 基于图的LLM任务规划验证器及幻觉检测
tldr: 针对大语言模型在任务规划中容易产生幻觉且难以识别结构性错误的问题，本文提出了GNNVerifier。该方法将规划方案表示为包含丰富属性的有向图，利用图神经网络（GNN）进行结构化评估和细粒度错误定位。通过自动生成的扰动数据进行训练，GNNVerifier能有效指导LLM进行局部修正，显著提升了复杂任务规划的质量和可靠性。
selection_source: fresh_fetch
motivation: 现有的基于LLM的验证器容易被虚假叙述误导，且难以检测步骤间的类型不匹配或依赖断裂等结构性错误。
method: 将规划建模为有向图并利用GNN生成全局合理性评分及局部风险评分，从而引导LLM对错误区域进行针对性编辑。
result: 在多种数据集和LLM骨干网络上的实验证明，GNNVerifier在识别和纠正规划错误方面均取得了显著的性能提升。
conclusion: 通过引入图结构表征和GNN评估机制，该研究为增强自主智能体任务规划的准确性提供了一种有效的新范式。
---

## 摘要
大语言模型（LLMs）促进了自主智能体的发展。作为此类智能体的核心组件，任务规划旨在将复杂的自然语言请求分解为具体的、可解决的子任务。由于 LLM 生成的规划经常容易出现幻觉，且对长上下文提示词敏感，最近的研究引入了规划验证器来识别和纠正潜在缺陷。然而，大多数现有方法仍然依赖 LLM 作为验证器，通过额外的提示进行规划审查或自我反思。基于 LLM 的验证器可能会被看似合理的叙述所误导，并且难以检测由跨步骤结构关系引起的故障，例如类型不匹配、中间体缺失或依赖关系断裂。为了解决这些局限性，我们提出了一种用于 LLM 任务规划的基于图的验证器。具体而言，所提出的方法包含四个主要组件：首先，我们将规划表示为具有丰富属性的有向图，其中节点表示子任务，边编码执行顺序和依赖约束。其次，图神经网络（GNN）执行结构评估和诊断，生成用于规划采纳的图级合理性评分，以及用于定位错误区域的节点/边级风险评分。第三，我们从地面真值（ground truth）规划图中构建可控扰动，并自动生成具有细粒度标注的训练数据。最后，在 GNN 验证器反馈的指导下，当图级评分不足时，我们使 LLM 能够进行局部编辑（例如工具替换或插入）以纠正规划。在不同数据集、骨干 LLM 和规划器上的广泛实验表明，我们的 GNNVerifier 在提高规划质量方面取得了显著收益。我们的数据和代码可在 https://github.com/BUPT-GAMMA/GNNVerifier 获取。

## Abstract
Large language models (LLMs) facilitate the development of autonomous agents. As a core component of such agents, task planning aims to decompose complex natural language requests into concrete, solvable sub-tasks. Since LLM-generated plans are frequently prone to hallucinations and sensitive to long-context prom-pts, recent research has introduced plan verifiers to identify and correct potential flaws. However, most existing approaches still rely on an LLM as the verifier via additional prompting for plan review or self-reflection. LLM-based verifiers can be misled by plausible narration and struggle to detect failures caused by structural relations across steps, such as type mismatches, missing intermediates, or broken dependencies. To address these limitations, we propose a graph-based verifier for LLM task planning. Specifically, the proposed method has four major components: Firstly, we represent a plan as a directed graph with enriched attributes, where nodes denote sub-tasks and edges encode execution order and dependency constraints. Secondly, a graph neural network (GNN) then performs structural evaluation and diagnosis, producing a graph-level plausibility score for plan acceptance as well as node/edge-level risk scores to localize erroneous regions. Thirdly, we construct controllable perturbations from ground truth plan graphs, and automatically generate training data with fine-grained annotations. Finally, guided by the feedback from our GNN verifier, we enable an LLM to conduct local edits (e.g., tool replacement or insertion) to correct the plan when the graph-level score is insufficient. Extensive experiments across diverse datasets, backbone LLMs, and planners demonstrate that our GNNVerifier achieves significant gains in improving plan quality. Our data and code is available at https://github.com/BUPT-GAMMA/GNNVerifier.

---

## 论文详细总结（自动生成）

以下是对论文《GNNVerifier: Graph-based Verifier for LLM Task Planning》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**大语言模型（LLM）在任务规划中的可靠性问题**。虽然 LLM 能将复杂请求分解为子任务，但常因幻觉或长上下文干扰产生看似合理实则不可执行的规划（如工具类型不匹配、步骤缺失、依赖断裂）。
*   **现有局限**：目前的验证方法多依赖 LLM 自我反思，容易被流畅的叙述误导，且难以识别跨步骤的**结构性错误**。
*   **核心目标**：引入图神经网络（GNN）作为外部验证器，利用图结构表征规划步骤间的逻辑关系，从而精准识别并定位规划中的结构性缺陷。

### 2. 方法论：核心思想与技术细节
GNNVerifier 框架包含四个关键步骤：
*   **属性图构建（Attributed Plan Graph）**：将规划转换为有向图。节点表示工具调用（包含工具语义、步骤文本、I/O类型等特征）；边表示执行顺序和依赖（包含接口兼容性、路径共现频率等特征）。
*   **GNN 编码与评分**：使用多层有向边感知 GNN 编码图结构。
    *   **图级评分**：评估整个规划的全局合理性。
    *   **节点/边级风险评分**：识别具体的错误工具选择（节点风险）或缺失的中间步骤（边风险）。
*   **基于扰动的监督学习**：由于缺乏细粒度的错误标注数据，研究者通过对真实规划图进行“替换工具（Wrong Tool）”和“删除/压缩步骤（Missing Step）”的操作，自动生成带标注的负样本。
*   **两阶段训练策略**：
    *   **第一阶段**：学习图级评分，通过排序损失（Ranking Loss）区分规划质量。
    *   **第二阶段**：冻结部分参数，微调节点和边预测头，学习精准的错误定位。
*   **验证引导的局部修正**：当图级评分低于阈值时，将高风险区域及候选工具反馈给 LLM，限制其仅在特定位置进行“替换”或“插入”操作，避免全局重写带来的新错误。

### 3. 实验设计
*   **数据集**：使用了 **TaskBench**（包含 HuggingFace、Multimedia、DailyLife 三个领域）和 **UltraTool**（大规模工具使用场景）。
*   **评估指标**：Node-F1（节点准确率）、Link-F1（依赖边准确率）和 Accuracy（全图完全匹配的成功率）。
*   **对比方法**：
    *   **基础规划器**：Direct（直接生成）、ReAct（推理-动作交替）、GNN4Plan（图引导规划）。
    *   **验证基准**：Raw（无验证）、Refine（自我修正）、VeriCoder（基于提示的验证）、VeriPlan（基于形式化检查的验证）。
*   **骨干模型**：GPT-4o 和 Qwen3-235B。

### 4. 资源与算力
*   **硬件设施**：实验使用了 **8 张 NVIDIA A800-40G GPU**。
*   **模型细节**：GNN 验证器为 3 层结构，隐藏层维度为 1024。
*   **训练细节**：使用了 E5-large 作为文本编码器，学习率为 2e-5，Batch Size 为 512。文中未明确给出具体的总训练时长，但提到了早停机制（Patience=5）。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个数据集、2 种主流 LLM、3 种不同规划范式上进行了交叉验证，实验覆盖面广。
*   **消融实验**：针对 GNN 结构、两阶段训练、节点/边特征、反馈信号类型等进行了详尽的消融分析（共 8 组变体），验证了各组件的必要性。
*   **客观性**：通过 t-SNE 可视化展示了 GNN 嵌入空间的区分度，并进行了错误类型分布分析（Error Analysis），实验设计严谨且具有说服力。

### 6. 主要结论与发现
*   **性能提升显著**：相比最强的基线方法（VeriPlan），GNNVerifier 在任务级准确率（Acc）上实现了 **10.96% 至 19.93%** 的相对提升。
*   **结构意识的重要性**：消融实验显示，移除 GNN 结构建模（改为纯 MLP）会导致性能大幅下降，甚至低于无验证器的状态，证明了结构化推理在规划验证中的核心作用。
*   **纠错能力**：该方法能有效减少“工具错误”和“步骤缺失”两类主要幻觉，尤其在复杂的多步依赖任务中表现优异。

### 7. 优点
*   **创新性表征**：将规划验证从纯文本理解转向图结构诊断，有效解决了 LLM 难以察觉的隐性逻辑错误。
*   **细粒度反馈**：不仅给出“对错”，还能定位“哪里错”，并提供候选工具，极大降低了 LLM 修正时的搜索空间。
*   **自动化训练**：通过扰动算子解决了高质量负样本匮乏的问题，使验证器具有很强的鲁棒性。

### 8. 不足与局限
*   **依赖外部知识库**：验证器的效果高度依赖于预定义的工具依赖图（Dependency Graph）和接口定义，对于动态变化或未定义的工具环境适应性受限。
*   **静态验证限制**：目前主要进行静态结构验证，尚未整合工具执行后的实时反馈（如 API 返回的错误信息）。
*   **计算开销**：相比纯 Prompt 方法，需要额外训练和运行一个 GNN 模型，增加了系统部署的复杂性。

（完）
