---
title: "Verified Multi-Agent Orchestration: A Plan-Execute-Verify-Replan Framework for Complex Query Resolution"
title_zh: 验证式多智能体编排：一种用于复杂查询解析的“规划-执行-验证-重规划”框架
authors: "Xing Zhang, Yanwei Cui, Guanghui Wang, Wei Qiu, Ziyuan Li, Fangwei Han, Yajing Huang, Hengzhi Qiu, Bing Zhu, Peiyang He"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11445v2"
tags: ["query:ma-kf"]
score: 10.0
evidence: 具有计划-执行-验证循环的多智能体编排
tldr: 本研究提出VMAO框架，通过“规划-执行-验证-重规划”循环协调多智能体解决复杂查询。该框架将任务分解为有向无环图（DAG）并并行执行，利用LLM验证器评估结果完整性，并根据反馈动态调整计划。实验表明，VMAO在市场调研任务中显著提升了答案完整性和来源质量，证明了编排级验证在多智能体质量保证中的有效性。
selection_source: fresh_fetch
motivation: 针对复杂查询，现有的单智能体或简单多智能体系统难以保证结果的完整性和质量。
method: 提出一种基于DAG任务分解、并行执行、LLM验证及自适应重规划的闭环多智能体编排框架。
result: 在专家策划的市场调研测试中，答案完整性从3.1提升至4.2，来源质量从2.6提升至4.1。
conclusion: 编排层面的验证机制是提升多智能体系统输出质量和可靠性的有效手段。
---

## 摘要
我们提出了验证式多智能体编排（VMAO），这是一个通过验证驱动的迭代循环来协调基于大语言模型（LLM）的专业智能体的框架。针对复杂查询，我们的系统将其分解为子问题的有向无环图（DAG），通过特定领域的智能体并行执行，利用基于 LLM 的评估来验证结果的完整性，并进行自适应重规划以弥补差距。主要贡献包括：(1) 在子问题 DAG 上实现具有自动上下文传播的依赖感知并行执行；(2) 采用基于 LLM 的验证器作为编排级协调信号的验证驱动自适应重规划；(3) 可配置的停止条件，用于平衡回答质量与资源消耗。在 25 个专家策划的市场研究查询中，与单智能体基准相比，VMAO 将回答完整性从 3.1 提升至 4.2，来源质量从 2.6 提升至 4.1（1-5 分制），证明了编排级验证是多智能体质量保证的有效机制。

## 速览
**TLDR**：本研究提出VMAO框架，通过“规划-执行-验证-重规划”循环协调多智能体解决复杂查询。该框架将任务分解为有向无环图（DAG）并并行执行，利用LLM验证器评估结果完整性，并根据反馈动态调整计划。实验表明，VMAO在市场调研任务中显著提升了答案完整性和来源质量，证明了编排级验证在多智能体质量保证中的有效性。 \
**Motivation**：针对复杂查询，现有的单智能体或简单多智能体系统难以保证答案的完整性和质量。 \
**Method**：提出一种基于DAG任务分解、并行执行、LLM验证及自适应重规划的闭环多智能体编排框架。 \
**Result**：在专家策划的市场调研测试中，VMAO将答案完整性从3.1提升至4.2，来源质量从2.6提升至4.1。 \
**Conclusion**：编排层面的验证机制是提升多智能体系统输出质量和可靠性的有效手段。

---

## Abstract
We present Verified Multi-Agent Orchestration (VMAO), a framework that coordinates specialized LLM-based agents through a verification-driven iterative loop. Given a complex query, our system decomposes it into a directed acyclic graph (DAG) of sub-questions, executes them through domain-specific agents in parallel, verifies result completeness via LLM-based evaluation, and adaptively replans to address gaps. The key contributions are: (1) dependency-aware parallel execution over a DAG of sub-questions with automatic context propagation, (2) verification-driven adaptive replanning that uses an LLM-based verifier as an orchestration-level coordination signal, and (3) configurable stop conditions that balance answer quality against resource usage. On 25 expert-curated market research queries, VMAO improves answer completeness from 3.1 to 4.2 and source quality from 2.6 to 4.1 (1-5 scale) compared to a single-agent baseline, demonstrating that orchestration-level verification is an effective mechanism for multi-agent quality assurance.

---

## 论文详细总结（自动生成）

### 论文结构化深入总结：验证式多智能体编排 (VMAO)

#### 1. 论文的核心问题与整体含义
论文针对复杂查询（如市场调研）中信息分散、分析维度多样且需要高可靠性的挑战，提出了 **VMAO (Verified Multi-Agent Orchestration)** 框架。
*   **研究动机**：现有的多智能体系统（如辩论式或角色扮演式）虽然提升了推理能力，但缺乏结构化的任务分解、原则性的质量验证以及自适应的精炼机制。在生产环境中，如何在没有持续人工干预的情况下确保输出的完整性和可靠性是核心痛点。
*   **核心含义**：通过引入“编排级验证”信号，将多智能体协作从简单的顺序执行转变为闭环的迭代优化过程。

#### 2. 论文提出的方法论
VMAO 采用 **“规划-执行-验证-重规划-综合” (Plan-Execute-Verify-Replan-Synthesize)** 的五阶段循环：
*   **规划 (Plan)**：将复杂查询分解为**有向无环图 (DAG)** 形式的子问题。每个子问题包含 ID、智能体类型、依赖关系、优先级和验证标准。
*   **执行 (Execute)**：根据 DAG 依赖关系进行并行执行。支持自动上下文传播，即上游结果会自动作为下游任务的背景信息。
*   **验证 (Verify)**：使用独立的 LLM 验证器评估每个子问题的结果，输出状态（完成/部分/未完成）、完整性评分（0-1）及缺失点。
*   **重规划 (Replan)**：根据验证反馈，决定是重试低分任务、引入新的子问题来填补空白，还是合并多次尝试的结果。
*   **综合 (Synthesize)**：当满足停止条件时，进行层次化综合。先按智能体类型分组总结，再集成最终答案并标注来源。
*   **智能体分类法**：将智能体分为三层：数据采集层（RAG、Web、金融、竞争对手）、分析层（推理、原始数据处理）和输出层（文档、可视化）。

#### 3. 实验设计
*   **数据集/场景**：使用了 25 个由专家策划的**市场调研查询**，涵盖四个类别：绩效分析、竞争情报、财务调查和战略评估。
*   **Benchmark**：采用 1-5 分制的“完整性 (Completeness)”和“来源质量 (Source Quality)”作为核心指标。
*   **对比方法**：
    1.  **Single-Agent**：单个推理智能体访问所有工具。
    2.  **Static Pipeline**：预定义的智能体顺序执行（RAG -> Web -> 金融 -> 分析 -> 综合），无验证和重规划。
    3.  **VMAO (本文方法)**：完整的动态编排框架。

#### 4. 资源与算力
*   **模型使用**：主要使用 **Claude Sonnet 4.5** 进行执行，**Claude Haiku 4.5** 作为降级备份，**Claude Opus 4.5** 用于验证和评估。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号和数量。
*   **消耗统计**：VMAO 平均每个查询消耗约 **850K tokens**，执行时间约 **900 秒**（15 分钟）。相比之下，单智能体仅需 100K tokens 和 165 秒。

#### 5. 实验数量与充分性
*   **实验规模**：共 25 组复杂查询实验。虽然样本量不算巨大，但每个查询的复杂度极高（涉及 8-12 个子问题，5+ 种智能体）。
*   **评估客观性**：采用了 LLM 自动评分与人类专家审核相结合的方式。为了减少偏见，评估模型（Opus）比执行模型（Sonnet）更强大。
*   **充分性评价**：实验覆盖了不同维度的市场调研任务，并进行了不同编排方法的对比。但缺乏针对单个组件（如仅 DAG 并行而不重规划）的详细消融实验。

#### 6. 论文的主要结论与发现
*   **性能提升**：VMAO 在完整性上从 3.1 提升至 4.2（+35%），在来源质量上从 2.6 提升至 4.1（+58%）。
*   **任务敏感性**：在“战略评估”等开放式、多维度任务中提升最显著（完整性提升 53%），而在数据源明确的简单任务中提升较小。
*   **重规划价值**：大多数重规划行为是针对失败工具调用或搜索结果不足的“重试”，证明了执行层面的不确定性是导致质量下降的主因。

#### 7. 优点
*   **闭环验证机制**：将验证提升到编排层面，解耦了协调逻辑与具体的智能体实现。
*   **高效并行**：基于 DAG 的调度在保证依赖关系的同时最大化了执行效率。
*   **可配置性强**：提供了包括 Token 预算、迭代次数、完整性阈值在内的多种停止条件，便于平衡成本与质量。
*   **模块化工具集成**：通过 Model Context Protocol (MCP) 实现工具的微服务化，易于扩展。

#### 8. 不足与局限
*   **成本高昂**：VMAO 的 Token 消耗是单智能体的 8.5 倍，延迟也显著增加，不适合实时性要求高的场景。
*   **验证局限性**：验证器主要评估“完整性”而非“事实准确性”，无法完全杜绝幻觉或事实错误。
*   **评估偏差风险**：执行模型和评估模型属于同一家族（Claude），可能存在系统性偏见。
*   **样本量限制**：25 个查询的测试集规模较小，且未报告置信区间。

（完）
