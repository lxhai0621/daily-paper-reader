---
title: "Open Biomedical Knowledge Graphs at Scale: Construction, Federation, and AI Agent Access with Samyama Graph Database"
title_zh: 大规模开放生物医学知识图谱：基于 Samyama 图数据库的构建、联邦化与 AI 智能体访问
authors: "Madhulatha Mandarapu, Sandeep Kunkunuru"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15080v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 生物医学知识图谱的构建及智能体访问
tldr: 本研究针对生物医学数据碎片化问题，基于高性能Rust图数据库Samyama构建了Pathways和Clinical Trials两个大规模开源知识图谱。通过可重复的ETL流程实现异构数据集成，支持跨图谱联邦查询以解决复杂生物医学问题，并引入模型上下文协议（MCP）使AI智能体能通过自然语言访问图数据，显著提升了数据整合效率与可访问性。
selection_source: fresh_fetch
motivation: 解决生物医学数据库高度碎片化导致的研究人员手动整合数据效率低下且难以复现的问题。
method: 利用Samyama图数据库开发了可重复的ETL模式，实现跨源去重、联邦查询及基于MCP协议的AI智能体自动接口生成。
result: 成功构建了包含近800万节点和2800万边的联邦图谱，在普通硬件上仅需76秒加载，复杂跨图查询响应时间仅为2.1秒。
conclusion: 该研究为大规模生物医学知识图谱的构建、集成及AI驱动的自然语言交互提供了一套高效、开源且可扩展的解决方案。
---

## 摘要
生物医学知识分散在孤立的数据库中——Reactome 用于通路，STRING 用于蛋白质相互作用，Gene Ontology 用于功能注释，ClinicalTrials.gov 用于研究注册，以及其他数十个数据库。研究人员通常从每个来源下载扁平文件并编写定制脚本进行交叉引用，这一过程缓慢、易错且不可重复。我们展示了两个开源生物医学知识图谱——Pathways KG（包含来自 5 个数据源的 118,686 个节点和 834,785 条边）和 Clinical Trials KG（包含来自 5 个数据源的 7,774,446 个节点和 26,973,997 条边）——它们构建于用 Rust 编写的高性能图数据库 Samyama 之上。我们的贡献有三个方面：首先，我们描述了一种可重复的 ETL 模式，用于从异构公共数据源构建大规模知识图谱（KG），包括跨源去重、批量 Cypher 加载和便携式快照导出。其次，我们展示了跨 KG 联邦化：将两个快照加载到单个图租户中，可以实现跨数据集的基于属性的连接（join），从而回答诸如“哪些生物通路被目前处于乳腺癌 3 期临床试验的药物所破坏？”之类的问题——这是任何一个单独的 KG 都无法回答的查询。第三，我们引入了模式驱动的 MCP 服务器生成：每个 KG 通过模型上下文协议（Model Context Protocol）自动为大语言模型（LLM）智能体公开类型化工具，从而实现对图查询的自然语言访问，而无需手动编写工具。所有数据源均采用开放许可（CC BY 4.0, CC0, OBO）。快照、ETL 代码和 MCP 配置均已公开。合并后的联邦图（789 万个节点，2780 万条边）在商用硬件（Mac Mini M4，16GB RAM）上加载仅需 76 秒，而标志性的跨 KG 查询——“哪些通路被乳腺癌 3 期试验中的药物所破坏？”——在 2.1 秒内即可返回经过验证的结果。

## Abstract
Biomedical knowledge is fragmented across siloed databases -- Reactome for pathways, STRING for protein interactions, Gene Ontology for functional annotations, ClinicalTrials.gov for study registries, and dozens more. Researchers routinely download flat files from each source and write bespoke scripts to cross-reference them, a process that is slow, error-prone, and not reproducible. We present two open-source biomedical knowledge graphs -- Pathways KG (118,686 nodes, 834,785 edges from 5 sources) and Clinical Trials KG (7,774,446 nodes, 26,973,997 edges from 5 sources) -- built on Samyama, a high-performance graph database written in Rust.   Our contributions are threefold. First, we describe a reproducible ETL pattern for constructing large-scale KGs from heterogeneous public data sources, with cross-source deduplication, batch Cypher loading, and portable snapshot export. Second, we demonstrate cross-KG federation: loading both snapshots into a single graph tenant enables property-based joins across datasets, answering questions like ``Which biological pathways are disrupted by drugs currently in Phase~3 trials for breast cancer?'' -- a query that neither KG can answer alone. Third, we introduce schema-driven MCP server generation: each KG automatically exposes typed tools for LLM agents via the Model Context Protocol, enabling natural-language access to graph queries without manual tool authoring.   All data sources are open-license (CC~BY~4.0, CC0, OBO). Snapshots, ETL code, and MCP configurations are publicly available. The combined federated graph (7.89M nodes, 27.8M edges) loads in 76 seconds on commodity hardware (Mac Mini M4, 16GB RAM), and the signature cross-KG query -- ``which pathways are disrupted by drugs in Phase~3 breast cancer trials?'' -- returns validated results in 2.1 seconds.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种在大规模环境下构建、集成生物医学知识图谱（KG）并使其可供 AI 智能体访问的系统化方案。以下是对该论文的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
生物医学知识目前高度碎片化，散布在如 Reactome（通路）、STRING（蛋白质相互作用）、ClinicalTrials.gov（临床试验）等多个孤立数据库中。研究人员通常需要手动下载扁平文件并编写定制脚本进行交叉引用，这导致研究过程**缓慢、易错且不可重复**。论文旨在通过高性能图数据库 Samyama 构建一个可扩展、可复现的集成框架，解决异构数据源的整合难题，并为 AI 智能体提供自然语言查询接口。

### 2. 论文提出的方法论
论文提出了一套基于 Rust 编写的 Samyama 图数据库的完整技术栈：
*   **可重复的 ETL 模式**：分为五个阶段：下载（支持断点续传）、解析与过滤（针对人类物种过滤）、去重（跨阶段追踪实体）、批量加载（通过 Cypher 语句批量写入）以及快照导出（生成便携的 `.sgsnap` 文件）。
*   **跨图谱联邦化（Cross-KG Federation）**：采用“基于属性的连接”而非预先合并。将多个 KG 快照加载到同一个数据库租户中，利用共享标识符（如 UniProt ID、DrugBank ID）在查询时进行动态关联。
*   **模式驱动的 MCP 服务器生成**：利用模型上下文协议（Model Context Protocol, MCP），系统能根据图谱模式（Schema）自动为大语言模型（LLM）生成类型化工具。AI 智能体无需手动编写代码即可通过自然语言调用图查询功能。

### 3. 实验设计
*   **数据集**：构建了两个核心图谱：
    *   **Pathways KG**：整合了 Reactome、STRING、GO、WikiPathways 和 UniProt，包含约 11.9 万节点。
    *   **Clinical Trials KG**：整合了 ClinicalTrials.gov、MeSH、RxNorm、OpenFDA 和 PubMed，包含约 777 万节点。
*   **Benchmark（基准测试）**：主要测试了构建性能（加载时间）、查询延迟（特别是跨图谱的 6 跳复杂查询）以及联邦查询的准确性。
*   **对比对象**：文中对比了 Bio2RDF（RDF/SPARQL 复杂度高）、Hetionet（静态数据集，无更新管线）和 Clinical Knowledge Graph（基于 Neo4j，侧重单一数据源）。

### 4. 资源与算力
*   **硬件环境**：实验在一台商用 **Mac Mini M4**（10 核 CPU，16GB RAM）上完成。
*   **算力消耗**：论文未提及 GPU 使用，因为 Samyama 是基于 Rust 的 CPU 优化型图数据库。
*   **时间消耗**：完整联邦图谱（2780 万条边）的加载仅需 **76 秒**；复杂的跨图查询在 **2.1 秒**内完成。

### 5. 实验数量与充分性
*   **实验规模**：论文展示了从数据摄取、快照导入到复杂查询的全流程性能数据。
*   **充分性评价**：实验涵盖了性能基准测试（表 7、表 8）和功能验证（联邦查询结果的生物学一致性）。虽然实验主要在单一硬件平台上进行，但其展示的近 3000 万规模边的处理能力对于生物医学研究场景而言具有较强的说服力。

### 6. 论文的主要结论与发现
*   **高性能集成**：Samyama 数据库在处理千万级规模的生物医学图谱时表现出极高的效率，远超传统基于扁平文件或复杂 RDF 的方法。
*   **联邦查询的可行性**：通过属性连接（Property Joins）可以有效桥接分子生物学（通路）与转化医学（临床试验），回答如“哪些通路被乳腺癌 3 期药物破坏”等跨领域问题。
*   **AI 访问便捷性**：MCP 协议显著降低了 LLM 访问结构化知识图谱的门槛，实现了自然语言到复杂 Cypher 查询的自动映射。

### 7. 优点（亮点）
*   **极致性能**：利用 Rust 语言特性，实现了极高的摄取速度和查询响应。
*   **便携性与复现性**：通过 `.sgsnap` 快照机制，研究人员可以秒级部署复杂的知识图谱。
*   **前瞻性集成**：引入 MCP 协议，紧跟 AI Agent 发展趋势，解决了知识图谱“好建难用”的问题。
*   **开源贡献**：所有代码、ETL 管线和数据快照均已开源。

### 8. 不足与局限
*   **数据时效性**：快照是静态的，而临床试验等数据更新频繁，需要定期重新导出。
*   **标识符覆盖率**：联邦查询的质量高度依赖于标识符（如 UniProt ID）的归一化质量，部分缺失 ID 的实体无法参与连接。
*   **内存依赖**：合并后的图谱需要约 8GB 内存，虽然在现代 PC 上不是问题，但在更低端的嵌入式或移动设备上可能受限。
*   **冗余问题**：基于属性的连接会导致数据库中存在重复节点（如两个图谱中各有一个相同的蛋白质节点），增加了存储开销。

（完）
