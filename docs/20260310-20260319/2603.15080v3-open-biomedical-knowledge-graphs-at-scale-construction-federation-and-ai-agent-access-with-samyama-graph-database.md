---
title: "Open Biomedical Knowledge Graphs at Scale: Construction, Federation, and AI Agent Access with Samyama Graph Database"
title_zh: 大规模开放生物医学知识图谱：基于 Samyama 图数据库的构建、联邦化与 AI 智能体访问
authors: "Madhulatha Mandarapu, Sandeep Kunkunuru"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15080v3"
tags: ["query:ma-kf"]
score: 9.0
evidence: 构建大规模生物医学知识图谱供AI智能体访问
tldr: 针对生物医学知识碎片化问题，本文基于高性能 Rust 图数据库 Samyama 构建了三个大规模开源知识图谱（Pathways、Clinical Trials、Drug Interactions）。研究提出了一套可复用的 ETL 流程，实现了跨图谱联邦查询，并引入了模式驱动的 MCP 服务，使 AI 智能体能高效访问图数据。在 BiomedQA 基准测试中，该方法显著提升了 LLM 的回答准确率，展示了在大规模生物医学数据集成与智能问答方面的卓越性能。
selection_source: fresh_fetch
motivation: 旨在解决生物医学知识分散在多个孤立数据库中，导致数据集成困难且难以被 AI 智能体高效利用的现状。
method: 基于 Rust 开发的 Samyama 图数据库，通过可复用的 ETL 流程构建大规模知识图谱，并利用模式驱动的 MCP 服务为 LLM 提供精准的数据访问接口。
result: "构建的联邦图谱包含 790 万节点和 2800 万条边，在 BiomedQA 测试中，MCP 辅助的智能体准确率达 98%，远超传统 text-to-Cypher 和独立 LLM。"
conclusion: 研究表明，高性能图数据库与模式驱动的智能体接口相结合，能有效实现大规模生物医学知识的集成、联邦查询与高精度智能问答。
---

## 摘要
生物医学知识分散在孤立的数据库中——Reactome 用于通路，STRING 用于蛋白质相互作用，ClinicalTrials.gov 用于研究注册，DrugBank 用于药物词汇，DGIdb 用于药物-基因相互作用，SIDER 用于副作用。我们介绍了三个开源生物医学知识图谱——Pathways KG（包含来自 5 个数据源的 118,686 个节点和 834,785 条边）、Clinical Trials KG（包含来自 5 个数据源的 7,774,446 个节点和 26,973,997 条边）以及 Drug Interactions KG（包含来自 3 个数据源的 32,726 个节点和 191,970 条边）——这些图谱均构建于用 Rust 编写的高性能图数据库 Samyama 之上。我们的贡献主要体现在三个方面：首先，我们描述了一种可重复的 ETL 模式，用于从异构公共数据源构建大规模知识图谱，支持跨源去重、批量加载（Python Cypher 和 Rust 原生加载器）以及便携式快照导出。其次，我们展示了跨知识图谱的联邦化：将所有三个快照加载到单个图租户中，实现了跨数据集的基于属性的连接。第三，我们引入了用于大语言模型（LLM）智能体访问的模式驱动型 MCP 服务器生成，并在新的 BiomedQA 基准测试（40 个药理学问题）上进行了评估：领域特定的 MCP 工具实现了 98% 的准确率，而模式感知的 text-to-Cypher 准确率为 85%，独立 GPT-4o 为 75%，且模式错误率为零。所有数据源均采用开放许可。合并后的联邦图（790 万个节点，2800 万条边）在商用云硬件上加载约需 3 分钟，单知识图谱查询在 80-100 毫秒内完成，跨知识图谱联邦连接在 1-4 秒内完成。

## Abstract
Biomedical knowledge is fragmented across siloed databases -- Reactome for pathways, STRING for protein interactions, ClinicalTrials.gov for study registries, DrugBank for drug vocabularies, DGIdb for drug-gene interactions, SIDER for side effects. We present three open-source biomedical knowledge graphs -- Pathways KG (118,686 nodes, 834,785 edges from 5 sources), Clinical Trials KG (7,774,446 nodes, 26,973,997 edges from 5 sources), and Drug Interactions KG (32,726 nodes, 191,970 edges from 3 sources) -- built on Samyama, a high-performance graph database written in Rust.   Our contributions are threefold. First, we describe a reproducible ETL pattern for constructing large-scale KGs from heterogeneous public data sources, with cross-source deduplication, batch loading (Python Cypher and Rust native loaders), and portable snapshot export. Second, we demonstrate cross-KG federation: loading all three snapshots into a single graph tenant enables property-based joins across datasets. Third, we introduce schema-driven MCP server generation for LLM agent access, evaluated on a new BiomedQA benchmark (40 pharmacology questions): domain-specific MCP tools achieve 98% accuracy vs. 85% for schema-aware text-to-Cypher and 75% for standalone GPT-4o, with zero schema errors.   All data sources are open-license. The combined federated graph (7.9M nodes, 28M edges) loads in approximately 3 minutes on commodity cloud hardware, with single-KG queries completing in 80-100ms and cross-KG federation joins in 1-4s

---

## 论文详细总结（自动生成）

以下是对论文《Open Biomedical Knowledge Graphs at Scale: Construction, Federation, and AI Agent Access with Samyama Graph Database》的深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：生物医学知识高度碎片化，散落在如 Reactome（通路）、STRING（蛋白质相互作用）、ClinicalTrials.gov（临床试验）等数十个孤立的数据库中。
*   **研究动机**：
    *   **集成困难**：研究人员通常需要手动下载扁平文件并编写临时脚本进行交叉引用，过程缓慢、易错且不可重复。
    *   **规模挑战**：现有生物医学知识图谱（KG）要么是静态的（如 Hetionet），要么缺乏高效的更新和访问机制。
    *   **AI 接入障碍**：大语言模型（LLM）在直接生成图查询语句（Text-to-Cypher）时经常出现模式幻觉（Schema Hallucinations）和语法错误。

### 2. 方法论
*   **核心思想**：基于 Rust 编写的高性能图数据库 **Samyama**，构建三个大规模开源 KG，并通过**联邦化（Federation）**和**模式驱动的 MCP（Model Context Protocol）**实现高效集成与 AI 访问。
*   **关键技术细节**：
    *   **可复用的 ETL 模式**：采用“下载 -> 解析过滤 -> 去重 -> 批量加载 -> 快照导出”的五阶段流程。利用 Rust 原生加载器实现极高的摄取速度。
    *   **跨图谱联邦化**：不进行物理上的预合并，而是将多个 KG 快照加载到同一个数据库租户中，利用共享标识符（如 UniProt ID、DrugBank ID、基因名）在查询时进行基于属性的连接（Property-based joins）。
    *   **模式驱动的 MCP 服务器**：自动根据图谱模式生成 LLM 智能体可调用的“工具”（Typed Tools）。LLM 不再直接写 Cypher 语句，而是通过填充参数调用预定义的工具函数。

### 3. 实验设计
*   **数据集**：
    *   **Pathways KG**：5 个来源（Reactome, STRING 等），11.8万节点，83.4万边。
    *   **Clinical Trials KG**：5 个来源（ClinicalTrials.gov, MeSH 等），777万节点，2697万边。
    *   **Drug Interactions KG**：3 个来源（DrugBank, DGIdb, SIDER），3.2万节点，19.1万边。
*   **Benchmark**：引入了 **BiomedQA**，一个包含 40 个药理学问题的基准测试，涵盖 7 个类别。
*   **对比方法**：
    1.  **独立 GPT-4o**：无外部数据库访问。
    2.  **模式感知 Text-to-Cypher**：GPT-4o 根据提供的 Schema 生成 Cypher 查询。
    3.  **MCP Tools**：LLM 调用由 Samyama 自动生成的领域特定工具。

### 4. 资源与算力
*   **硬件环境**：AWS g4dn.4xlarge 实例。
*   **配置细节**：16 vCPU (AMD EPYC), 62 GB RAM, NVIDIA A10G GPU。
*   **性能数据**：
    *   全量联邦图谱（7.9M 节点，28M 边）加载时间约 **3 分钟**。
    *   内存占用约 **33 GB**。
    *   Rust 原生加载器在 **928 毫秒**内完成了 Drug Interactions KG 的构建。

### 5. 实验数量与充分性
*   **实验组数**：进行了构建性能测试、查询延迟测试（涵盖单图查询和跨图联邦查询）以及 BiomedQA 准确率对比实验。
*   **充分性与客观性**：实验覆盖了从底层数据库性能到上层 AI 应用的全链路。BiomedQA 虽然规模较小（40 题），但针对性强，对比了当前 LLM 接入数据的三种主流模式，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能卓越**：Samyama 的 Rust 引擎在处理千万级规模的图数据时，单图查询延迟在 100ms 以内，复杂的跨图联邦连接在 1-4s 内完成。
*   **AI 访问精度提升**：使用 MCP 工具模式的准确率达到 **98%**，显著高于 Text-to-Cypher（85%）和独立 LLM（75%）。
*   **消除幻觉**：模式驱动的 MCP 强制执行类型检查，实现了 **0 模式错误**，证明了“工具选择”优于“查询生成”。

### 7. 优点
*   **工程化程度高**：提供了可重复的 ETL 流程和便携式快照（.sgsnap），极大降低了生物医学 KG 的部署门槛。
*   **联邦机制灵活**：通过属性连接避免了复杂的预合并过程，使得不同来源的 KG 可以像插件一样按需组合。
*   **AI 友好**：率先将 MCP 协议应用于大规模生物医学图谱，为 AI 智能体提供了可靠的数据访问层。

### 8. 不足与局限
*   **数据时效性**：快照是静态的，对于 ClinicalTrials.gov 这种频繁更新的数据源，需要定期重新导出快照。
*   **标识符依赖**：联邦查询的质量高度依赖于不同数据源之间 ID 归一化的覆盖率。
*   **内存门槛**：全量联邦图谱需要至少 32GB 以上的内存，在低配硬件上无法运行。
*   **分布式限制**：目前的联邦化仍需在单一租户内完成，尚不支持跨物理节点的分布式图查询。

（完）
