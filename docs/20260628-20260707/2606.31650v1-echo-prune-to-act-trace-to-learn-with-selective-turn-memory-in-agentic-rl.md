---
title: "ECHO: Prune to act, trace to learn with selective turn memory in agentic RL"
title_zh: ECHO：通过选择性回合记忆在智能体强化学习中剪枝行动、追踪学习
authors: "Zijun Xie, Binbin Zheng, Enlei Gong, Jihua Liu, Yuyang You, Lingfeng Liu, Jiayao Tang, Guanqun Zhao, Aoqi Hu, Zeyu Chen"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2606.31650v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 选择性回合记忆管理智能体长上下文和有界窗口
tldr: "现有长程语言智能体因上下文窗口限制，常截断或压缩历史观测，导致细粒度证据丢失和强化学习信用分配困难。ECHO提出选择性回合记忆框架，将每个环境回合压缩为紧凑记录，通过源索引重建策略上下文，并将成功结果的正奖励路由到支持性证据和选择动作。在BrowseComp-Plus上，ECHO以43.4%保留准确率超越GRPO(28.9%)和SUPO(36.1%)，且使用更少回合和轨迹。该方法在多个任务上展现了零样本泛化能力。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.31650v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1621, \"height\": 461, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31650v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1625, \"height\": 369, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31650v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1617, \"height\": 564, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31650v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1588, \"height\": 1019, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31650v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1621, \"height\": 460, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31650v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1614, \"height\": 565, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.31650v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1578, \"height\": 447, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31650v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1636, \"height\": 530, \"label\": \"Table\"}]"
motivation: 现有截断或压缩历史的方法导致证据丢失，且强化学习无法将成功奖励追溯至支持性证据。
method: ECHO压缩每个环境回合为紧凑记录，基于源索引选择构建策略上下文，并利用索引将正奖励路由至相应证据和动作。
result: "BrowseComp-Plus上ECHO达43.4%准确率，优于GRPO和SUPO，且使用更少回合和轨迹，零样本泛化至多任务。"
conclusion: ECHO通过选择性回合记忆和源索引重建，同时解决历史压缩与可追溯学习，显著提升长程智能体性能与泛化能力。
---

## 摘要
长视野语言智能体必须在有限上下文窗口内重复与工具交互、积累证据并做出决策。现有的上下文管理方法通过截断遥远历史、将过去回合折叠为摘要或选择紧凑记忆状态来使得这种展开可行。然而，这些突破引入了两个相互耦合的限制。首先，随着回合数增加，历史观测被逐步移除或压缩为压缩状态，使得策略更难重用细粒度证据。其次，一旦原始回合不再可溯源，基于结果的强化学习失去了将策略更新与支持成功最终答案的证据对齐的显式路径。为此，我们提出ECHO，一种选择性回合记忆框架，通过基于源索引的重建联合解决历史坍塌和可追踪学习。具体来说，ECHO将每个完成的环境回合压缩为紧凑记忆记录，通过从这些记录中选择来重建有界策略上下文，并重用所选源索引将正向结果信用路由到支持成功答案的证据和选择动作。在BrowseComp-Plus上，ECHO达到43.4%的留出准确率，优于GRPO（28.9%）和滚动摘要基线SUPO（36.1%），同时使用比SUPO更少的回合和更低的轨迹量（图1）。此外，训练后的策略在密集和MoE骨干网络上，跨多目标问答、代码生成和深度信息检索基准提升了零样本泛化能力。

## Abstract
Long-horizon language agents must repeatedly interact with tools, accumulate evidence, and make decisions under bounded context windows. Existing context-management methods make such rollouts feasible by truncating distant history, folding past turns into summaries, or selecting compact memory states. However, these breakthroughs introduce two coupled limitations. First, as the number of turns grows, historical observations are progressively removed or collapsed into compressed states, making it harder for the policy to reuse fine-grained evidence. Second, once the original turns are no longer source-addressable, outcome-based RL loses an explicit path for aligning policy updates with the evidence that supported a successful final answer. To this end, we propose ECHO, a selective turn-memory framework that jointly addresses history collapse and traceable learning through source-indexed reconstruction. Specifically, ECHO compresses each completed environment turn into a compact memory record, reconstructs bounded policy contexts by selecting from these records, and reuses the selected source indices to route positive outcome credit to the evidence and selection actions that support successful answers. On BrowseComp-Plus, ECHO reaches 43.4% held-out accuracy, outperforming GRPO (28.9%) and the rolling-summary baseline SUPO (36.1%), while using fewer turns and lower trajectory volume than SUPO (Figure 1). Additionally, the trained policy improves zero-shot generalization across multi-objective QA, code generation, and deep information-seeking benchmarks on both dense and MoE backbones.

---

## 论文详细总结（自动生成）

# 论文总结：ECHO：通过选择性回合记忆在智能体强化学习中剪枝行动、追踪学习

## 1. 核心问题与整体含义（研究动机与背景）

- **核心问题**：长程语言智能体在与工具交互、积累证据和做出决策时，面临有限上下文窗口的严重瓶颈。现有方法（如截断历史、折叠摘要、选择收缩记忆）虽能使长程交互可行，但引入了两个耦合限制：
  - **历史坍塌**：随着回合数增加，细粒度证据被逐步移除或压缩，降低了策略重用证据的能力。
  - **信用分配困难**：一旦原始回合不可溯源，基于最终结果的强化学习无法将成功奖励正确分配给支持性历史证据，导致策略更新与有用证据脱钩。
- **整体含义**：现有滚动摘要方法（如SUPO）虽能延长交互，但常导致回合激增、轨迹膨胀和训练变慢。这表明仅延长交互是不够的，还需要一种可追踪的上下文重建机制，记录哪些证据被选择并重用。

## 2. 论文提出的方法论：ECHO框架

### 核心思想
ECHO提出一种**选择性回合记忆框架**，通过基于源索引的重建，同时解决历史坍塌和可追踪学习。其核心是将每个完成的环境回合压缩为紧凑记忆记录，通过策略从这些记录中选择有用的记录来重建有界策略上下文，并利用所选源索引引导信用分配。

### 关键技术细节
1.  **记忆存储（Prune to Act）**：
    - 每个回合u_i完成后，生成一个紧凑的局部发现s_i，形成记忆记录e_i = (i, α_i, n_i)，其中i为源回合索引，α_i为动作摘要，m_i为发现。
    - 记忆集M_j = {e_i}_{i≤K_j}是非坍塌的集合，每个记录独立链接到原始回合。

2.  **上下文重建（Bounded Context Reconstruction）**：
    - 当上下文预算超限时，策略执行**记忆选择**：从当前记忆集中选择有用的记录（asel_j），输出源索引集合Isel_j。
    - 选择结果结合自动保留的最新K个回合，构成重建的上下文：c_j,t = x ⊕ render(M_j[Isel_j]) ⊕ Φ_local(H_loc_j,t; B)。

3.  **信用分配（Trace to Learn）**：
    - 创建硬信用掩码μ_q，标记以下token：最终答案段、被选中的证据回合的token、其记忆发现token、以及所有记忆选择操作token。
    - 仅对具有正群体相对优势的轨迹应用此掩码，实现目标性信用路由：
      - L_ECHO(θ) = Σ_q [ (A+ * μ_q) * log π_θ(y_q | y_{<q}, c_q) ]，其中A+ = max(A, 0)。

### 算法流程（文字说明）
- **训练时**：智能体在有限上下文窗口下执行多回合交互，每当上下文超限，触发记忆选择→重建上下文→记录选择掩码→保存段。轨迹结束后，利用掩码和优势值进行梯度更新。
- **零样本测试时**：使用训练好的策略，直接应用记忆选择与重建机制，无需额外训练。

## 3. 实验设计

### 数据集与场景
- **主要训练与评估**：**BrowseComp-Plus**，一个长程工具使用QA基准，需迭代搜索、证据检查和答案验证。训练集747例，验证集83例。
- **零样本泛化测试**：
  - **多目标QA**：2/4/8/16个目标，基于HotpotQA, 2WikiMultihopQA等构建，使用LLM-as-a-Judge评估。
  - **代码生成**：CodeGym（合成交互环境）和LoCoBench-Agent（真实软件工程）。
  - **深度信息检索**：GAIA, Humanity's Last Exam (HLE), Frames。

### 对比方法
- **GRPO（基线）**：标准多回合强化学习方法。
- **SUPO（滚动摘要基线）**：递归折叠旧历史为摘要以延长交互。
- **ECHO及其变体**：含/不含可追踪信用分配、含/不含回合级重要性采样、含/不含总结等。

### 评估指标
- 主要：留出集Pass@1准确率。
- 辅助：平均回合数、轨迹体积、生成时间等。

## 4. 资源与算力

- **算力描述**：所有实验在**4个节点**上运行，每个节点**8块GPU**，总计**32 GPU**。采用完全异步的环境生成流程。
- **未明确细节**：论文未明确指出GPU的具体型号（如A100, H100）、训练总时长（小时数）、批量大小等，也没有报告总计算量（如GPU-hours）。原文仅提及使用了“4节点、8GPU/节点”的配置。

## 5. 实验数量与充分性

### 实验组数
- **主实验**：在BrowseComp-Plus上对比三种主要方法（GRPO, SUPO, ECHO），并跟踪训练动态（回合数、轨迹体积）。
- **消融实验**：两组主要消融：
  - **记忆组件**：比较学习型选择 vs 静态Top-K检索，以及总结发现 vs 完整观察。
  - **信用分配**：移除了可追踪掩码或替换为回合级重要性采样。
- **零样本泛化**：在6个不同任务/子集上评估（多目标QA×4, 代码生成×2, 深度检索×3）。
- **架构鲁棒性**：在Qwen3-30B-A3B-Instruct（MoE骨干）上重复训练动态，并评估零样本性能。

### 充分性与公平性
- **充分性**：实验覆盖了核心性能（准确率）、训练行为（回合数、轨迹体积）、消融验证和零样本迁移，较为全面。
- **公平性**：所有方法使用完全相同的主干、工具环境、预算和采样配置（8 rollout/提示），且压缩机制对齐（如均采用32k上下文及最多5次压缩）。消融实验逐一剥离关键组件，逻辑清晰。
- **偏差风险**：
  - 主要实验仅针对文本型工具使用智能体，未覆盖GUI、具身或多智能体场景。
  - 零样本泛化的任务选择可能带有偶然性，未做更广泛的任务覆盖。
  - 部分基准（如CodeGym）经过筛选以排除过易或过难样本，可能引入选择偏差。

## 6. 论文的主要结论与发现

- **性能提升**：ECHO在BrowseComp-Plus上达到**43.4%** 留出准确率，显著高于GRPO（28.9%）和SUPO（36.1%），且使用更少的回合（45.3 vs 62.5）和更低的轨迹体积（3.13 vs 4.18）。
- **训练稳定性**：ECHO避免SUPO后期出现的回合激增和轨迹膨胀问题，保持稳定的学习曲线。
- **零样本泛化**：训练后的策略在密集和MoE骨干上均表现出较强的零样本泛化能力（平均分40.2% vs GRPO 33.6%），尤其在证据密集型任务（如16目标QA、Frames）上提升明显。
- **消融发现**：
  - 学习型选择比静态Top-K检索更有效。
  - 紧凑的回合发现与原始完整观察性能相当，表明信息压缩是有效的。
  - 可追踪信用分配是提升核心性能的关键 — 移除后准确率下降且训练不稳定。
- **架构鲁棒性**：采用MoE骨干（Qwen3-30B-A3B-Instruct）时，ECHO仍保持优势，而SUPO训练后期进入塌缩状态（准确率降至13.3%，回合和轨迹体积激增）。

## 7. 优点

1.  **问题驱动的方法创新**：
    - 从实际挑战（历史坍塌与信用缺失）出发，提出选择性回合记忆与源索引重建的统一解决方案，动机明确。
    - 提出了“上下文管理多回合强化学习”的统一接口（M, Φ），为比较不同策略提供了框架。

2.  **精巧的信用分配机制**：
    - 直接利用上下文重建过程中产生的源索引（I_sel_j）来引导信用路由，使“剪枝”与“学习”天然对齐。
    - 仅对正优势的轨迹应用掩码，避免了信用噪声，且无需额外的估计或模型。

3.  **实验设计严谨且充分**：
    - 消融实验明确支持了各核心组件的必要性（如学习型选择、可追踪信用）。
    - 零样本泛化测试覆盖多任务领域，且验证了架构鲁棒性（稠密 vs MoE），增强了结论的外部有效性。

4.  **效率和扩展性**：
    - ECHO在使用更少回合和轨迹体积的情况下获得更高准确率，表明其鼓励了更高效的搜索而非冗余探求。
    - 方法在MoE架构下表现更稳定，显示其适应稀疏模型的潜力。

## 8. 不足与局限

1.  **信用分配的理论近似**：
    - 作者承认使用“最终轨迹近似”：仅对最终重建上下文中选中的源回合分配信用，未递归追踪这些回合的完整依赖链。对于深层嵌套依赖，可能低估历史贡献。

2.  **实验覆盖范围有限**：
    - 主要聚焦于**文本型工具使用智能体**（搜索、打开页面）。对GUI智能体、具身智能体或多智能体系统的泛化性尚未验证。
    - 零样本泛化的任务数量仍然有限，且主要来自信息检索和问答领域，未覆盖更广泛的行业应用（如金融、医疗）。

3.  **计算开销与效率**：
    - 源索引记忆选择引入了额外的前向计算（每次上下文超限时需要生成选择动作）。论文未详细分析其对训练速度和推理效率的具体影响。

4.  **超参数敏感性**：
    - 方法涉及多个超参数（如保留最新回合数K=3、最大选择数S=8、上下文预算B=32k），论文未充分讨论这些参数对性能的敏感性。例如，不同B值或不同选择策略（如允许动态调整S）可能产生不同效果。

5.  **模型幻觉与可靠性问题**：
    - 论文未分析ECHO在长程交互中是否容易因错误记忆或选择而引入幻觉风险。对于需要高度事实准确性的应用场景，该机制的可能失败模式尚未研究。

（完）
