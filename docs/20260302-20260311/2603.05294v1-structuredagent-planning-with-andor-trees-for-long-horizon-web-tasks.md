---
title: "STRUCTUREDAGENT: Planning with AND/OR Trees for Long-Horizon Web Tasks"
title_zh: STRUCTUREDAGENT：基于与/或树的长程网络任务规划
authors: "ELita Lobo, Xu Chen, Jingjing Meng, Nan Xi, Yang Jiao, Chirag Agarwal, Yair Zick, Yan Gao"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.05294v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 针对长周期任务的使用与/或树的层级规划
tldr: 针对大型语言模型在处理长程复杂网络任务时存在的规划能力弱、记忆受限及贪婪行为等问题，本文提出了 STRUCTUREDAGENT 框架。该框架引入了基于动态 AND/OR 树的在线分层规划器和结构化记忆模块，通过高效搜索和候选方案追踪，显著提升了任务成功率和约束满足度，并提供了可解释的规划过程，方便调试与人工干预。
selection_source: fresh_fetch
motivation: 现有的网络智能体在处理长程任务时，常因上下文记忆有限、规划能力不足及贪婪决策导致任务过早终止或失败。
method: 提出一种分层规划框架，利用动态 AND/OR 树进行高效搜索，并结合结构化记忆模块来追踪和维护候选解决方案。
result: 在 WebVoyager、WebArena 及自定义购物基准测试中，该方法在长程网页浏览任务上的表现显著优于标准 LLM 智能体。
conclusion: STRUCTUREDAGENT 通过结构化规划和记忆机制，有效增强了智能体在复杂网络环境下的决策能力、约束满足度及可解释性。
---

## 摘要
大语言模型（LLMs）的最新进展使得用于顺序决策的智能体系统成为可能。这些智能体必须感知环境，跨多个时间步进行推理，并采取优化长期目标的行动。然而，由于用于追踪历史的上下文内存有限、规划能力薄弱以及导致过早终止的贪婪行为，现有的网络智能体在复杂、长程任务中表现不佳。为了应对这些挑战，我们提出了 STRUCTUREDAGENT，这是一个分层规划框架，包含两个核心组件：（1）一个使用动态与/或树进行高效搜索的在线分层规划器；（2）一个追踪并维护候选解决方案的结构化记忆模块，旨在提高信息寻求任务中的约束满足度。该框架还能生成可解释的分层计划，从而简化调试过程，并在需要时促进人工干预。我们在 WebVoyager、WebArena 和自定义购物基准测试上的结果表明，与标准的基于 LLM 的智能体相比，STRUCTUREDAGENT 提升了在长程网络浏览任务中的性能。

## Abstract
Recent advances in large language models (LLMs) have enabled agentic systems for sequential decision-making. Such agents must perceive their environment, reason across multiple time steps, and take actions that optimize long-term objectives. However, existing web agents struggle on complex, long-horizon tasks due to limited in-context memory for tracking history, weak planning abilities, and greedy behaviors that lead to premature termination. To address these challenges, we propose STRUCTUREDAGENT, a hierarchical planning framework with two core components: (1) an online hierarchical planner that uses dynamic AND/OR trees for efficient search and (2) a structured memory module that tracks and maintains candidate solutions to improve constraint satisfaction in information-seeking tasks. The framework also produces interpretable hierarchical plans, enabling easier debugging and facilitating human intervention when needed. Our results on WebVoyager, WebArena, and custom shopping benchmarks show that STRUCTUREDAGENT improves performance on long-horizon web-browsing tasks compared to standard LLM-based agents.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **STRUCTUREDAGENT** 的分层规划框架，旨在解决大型语言模型（LLM）智能体在处理复杂、长程（Long-Horizon）网络任务时的局限性。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
现有的网络智能体在处理复杂任务（如跨多个网页的购物、信息检索）时面临四大挑战：
*   **上下文记忆有限**：难以追踪长路径中的备选方案。
*   **规划能力薄弱**：无法构建和执行跨越长周期的复杂计划。
*   **缺乏错误恢复机制**：一旦某个步骤失败，往往无法回溯或尝试替代方案。
*   **贪婪决策**：容易在未完全满足用户约束的情况下过早终止任务。

### 2. 方法论：核心思想与关键技术
STRUCTUREDAGENT 结合了分层规划和结构化记忆，核心组件包括：

*   **动态 AND/OR 树规划器**：
    *   **AND 节点**：代表必须全部完成的子目标（顺序或并行）。
    *   **OR 节点**：代表实现目标的多种替代策略，增强了系统的容错性。
    *   **Action 节点**：底层的原子操作（点击、输入、滚动等）。
*   **改进的贪婪深度优先搜索（Modified Greedy DFS）**：
    *   框架通过维护一个 DFS 栈来动态构建树。
    *   引入了三种执行状态：**进入（ENTERING）**、**退出（EXITING）**和**失败（FAILED）**。
    *   支持**节点修复（Node Repair）**和**剪枝（Pruning）**：当某个路径失败时，智能体可以回溯并尝试 OR 节点下的其他分支，或通过 LLM 修正计划。
*   **结构化记忆模块（Structured Memory）**：
    *   专门针对信息寻求任务，以表格形式追踪候选实体及其满足的约束条件。
    *   防止智能体在探索过程中遗忘之前发现的潜在选项。
*   **职责分离**：框架负责维护树结构和遍历逻辑，LLM 仅作为“高层控制器”负责局部的节点扩展、修复和完成度检查。

### 3. 实验设计
论文在三个主要基准测试上进行了评估：
*   **Custom Complex Shopping (Amazon)**：包含 60 个长程购物任务，涉及复杂的自然语言约束（通常需要 10-30 步）。
*   **WebVoyager**：包含 129 个真实世界的跨网站任务。
*   **WebArena**：在 630 个任务上进行评估，涵盖地图、购物、Reddit 和 GitLab 等场景。
*   **对比方法**：包括 AgentOccam（强基线）、BasicClaudeAction（带历史记录的朴素智能体）、SteP（基于堆栈的策略）和 WebarenaReplication。
*   **评估指标**：任务成功率（Success Rate），采用 LLM-as-a-Judge（GPT-4, Gemini, Kimi）和人工标注相结合的方式。

### 4. 资源与算力
*   **模型支持**：主要使用 **Claude 3.5 Sonnet** 和 **Claude 3.7** 作为后端模型，同时也验证了 **Kimi-k2** 的泛化能力。
*   **算力细节**：文中**未明确说明**具体的 GPU 训练算力或训练时长，因为该框架主要基于推理侧的规划（Inference-time planning），不涉及大规模的模型微调。实验主要通过 API 调用完成。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了超过 800 个测试任务，跨越了多个不同的网络环境和任务类型。
*   **充分性**：实验设计较为充分。作者不仅对比了多个基线模型，还进行了跨模型家族（Claude vs. Kimi）的验证，并引入了人工评估以弥补 LLM 自动评分可能存在的偏差。
*   **客观性**：通过展示轨迹长度分布和失败案例分析，客观地反映了该方法在增加成功率的同时所付出的时间成本。

### 6. 主要结论与发现
*   **性能提升**：在 Amazon 购物任务中，STRUCTUREDAGENT 的成功率比 AgentOccam 高出约 14%；在 WebArena 综合任务中，性能提升了约 6%-20%。
*   **长程任务优势**：任务越复杂、约束越多，该框架的优势越明显。
*   **模型规模效应**：即使使用更强大的模型（如 Claude 3.7），分层规划依然能带来显著的性能增益。
*   **结构化记忆的作用**：在多约束的检索任务中，结构化记忆能显著提升约束满足度，但在简单任务中可能引入噪声。

### 7. 优点
*   **高可解释性**：生成的 AND/OR 树清晰地展示了智能体的决策逻辑，便于人类理解和干预（Human-in-the-loop）。
*   **鲁棒的错误处理**：通过 OR 节点和节点修复机制，能够从执行错误中恢复，而不是直接崩溃。
*   **动态适应性**：能够根据网页反馈实时调整计划，而不是死板地执行预设序列。

### 8. 不足与局限
*   **效率与成本**：由于涉及更多的规划步骤和回溯，该方法的轨迹通常比基线更长，导致**推理延迟更高**且 **API 调用成本增加**。
*   **简单任务的开销**：在非常简单的任务中，分层规划可能显得过于沉重，甚至因为过度约束而导致性能轻微下降。
*   **依赖 LLM 质量**：节点扩展和修复的质量高度依赖于底层 LLM 的推理能力，如果模型在理解 HTML 结构上存在缺陷，规划器也会受限。

（完）
