---
title: A Dynamic Self-Evolving Extraction System
title_zh: 一种动态自演化抽取系统
authors: "Moin Amin-Naseri, Hannah Kim, Estevam Hruschka"
date: 2026-03-06
pdf: "https://arxiv.org/pdf/2603.06915v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 自演化提取系统，通过三元组填充知识库
tldr: 针对特定领域信息抽取中术语更新快、长尾数据难处理的问题，本文提出DySECT系统。该系统通过大模型提取三元组构建自演化知识库，并利用图推理增强知识，再通过反馈机制优化模型抽取能力，形成知识与抽取相互促进的闭环，实现系统的持续自我进化。
selection_source: fresh_fetch
motivation: 传统信息抽取方法难以应对特定领域（如医疗、法律）中不断变化的专业术语和复杂的结构化知识推理需求。
method: 提出DySECT框架，利用大模型构建自扩展知识库，并通过图推理与反馈循环（如提示词微调或合成数据训练）实现抽取性能的持续提升。
result: 成功建立了一个知识抽取与知识库构建的共生闭环，使系统能够随着使用过程不断积累领域知识并自动优化抽取精度。
conclusion: 动态自演化的闭环机制是提升大模型在专业领域信息抽取准确性、实时性与适应性的有效途径。
---

## 摘要
从原始文本中提取结构化信息是许多自然语言处理（NLP）应用的基础组件，包括文档检索、排序和相关性评估。高质量的抽取通常需要特定领域的准确性、对专业分类体系的最新理解，以及整合新兴术语和稀有离群值的能力。在医疗、法律和人力资源等许多领域，抽取模型还必须适应不断变化的术语，并从结构化知识的显式推理中获益。我们提出了 DySECT，一个动态自演化抽取与策展工具包，它在使用过程中不断改进。该系统利用大语言模型（LLM）提取的三元组，增量式地填充一个多功能、自扩展的知识库（KB）。知识库通过整合概率知识和基于图的推理进一步丰富自身，逐渐积累领域概念和关系。丰富后的知识库随后通过提示微调、相关少样本示例采样或使用基于知识库生成的合成数据进行微调，反馈给 LLM 抽取器。最终，该系统形成了一个共生的闭环循环，其中抽取不断改进知识，而知识又不断改进抽取。

## Abstract
The extraction of structured information from raw text is a fundamental component of many NLP applications, including document retrieval, ranking, and relevance estimation. High-quality extractions often require domain-specific accuracy, up-to-date understanding of specialized taxonomies, and the ability to incorporate emerging jargon and rare outliers. In many domains--such as medical, legal, and HR--the extraction model must also adapt to shifting terminology and benefit from explicit reasoning over structured knowledge. We propose DySECT, a Dynamic Self-Evolving Extraction and Curation Toolkit, which continually improves as it is used. The system incrementally populates a versatile, self-expanding knowledge base (KB) with triples extracted by the LLM. The KB further enriches itself through the integration of probabilistic knowledge and graph-based reasoning, gradually accumulating domain concepts and relationships. The enriched KB then feeds back into the LLM extractor via prompt tuning, sampling of relevant few-shot examples, or fine-tuning using KB-derived synthetic data. As a result, the system forms a symbiotic closed-loop cycle in which extraction continuously improves knowledge, and knowledge continuously improves extraction.

---

## 论文详细总结（自动生成）

这是一份关于论文《A Dynamic Self-Evolving Extraction System》（DySECT）的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
在医疗、法律、人力资源等专业领域，信息抽取（IE）面临术语更新快、领域知识专业化、长尾数据多以及模式（Schema）不断演化等挑战。传统的 IE 系统通常依赖静态的标注数据集或人工设计的规则，难以适应动态变化的环境。
**核心问题**：如何构建一个能够随着使用过程自动学习、无需频繁人工干预且能持续优化抽取性能的系统？
**整体含义**：论文提出了 **DySECT** 框架，通过建立“抽取-知识库构建-反馈优化”的闭环，使大语言模型（LLM）在抽取过程中不断丰富知识库，而知识库反过来指导 LLM 进行更精准、更全面的抽取，实现系统的自演化。

### 2. 方法论：核心思想与关键技术
DySECT 由三个核心组件构成的闭环系统：

*   **抽取步骤（Extraction Step）**：利用 LLM 根据初始提示词从原始文本中提取 `(主体, 关系, 客体)` 三元组。
*   **知识库增长（Knowledge Base Growth）**：
    *   **知识整合**：基于 Theo 框架构建知识库。使用 **KNN 聚类** 对语义相似的节点进行分组，并由 LLM 生成抽象标签（如将具体乐器归类为“音乐流派”），构建层级结构。
    *   **概率置信度建模**：为每个三元组计算置信度 $C(t)$。
        *   **聚合置信度**：采用保守的 Noisy-OR 模型结合观测频率 $f_i$ 和收缩因子 $\lambda$：$C_{agg}(t) = 1 - \prod (1 - \lambda c_i)^{f_i}$。
        *   **互斥约束**：若检测到 $m(t)$ 个互斥实例，则惩罚置信度：$C(t) = \frac{C_{agg}(t)}{m(t) + 1}$。
    *   **主动获取**：通过 LLM 自动发现新的概念实例和关系实例，扩展知识边界。
*   **反馈机制（Feedback Mechanisms）**：
    *   **提示词增强**：将知识库中的高置信度抽象概念、长尾示例注入 Prompt。
    *   **正/负反馈模式**：正向（Encouraging）引导模型关注类似模式；负向（Prohibitive）告知模型哪些已提取，避免冗余，转向挖掘新信息。
    *   **合成数据微调**：将结构化知识转化为自然语言文本，用于模型的轻量级微调。

### 3. 实验设计
*   **数据集**：使用 **DocRED**（大规模文档级关系抽取数据集，源自维基百科）。
*   **Benchmark**：以各模型在无知识库反馈下的初始抽取表现为基准（Baseline）。
*   **对比方法**：
    *   **模型对比**：评估了 GPT-4.1、GPT-4.1-mini、LLaMA-3.3 70B 和 Kimi K2.5 四种主流 LLM。
    *   **模式对比**：对比了“无反馈”、“正向反馈（Positive）”和“负向反馈（Negative）”在多次迭代下的表现。
*   **评价指标**：召回率（Recall）和平均提取三元组数量。

### 4. 资源与算力
论文**未明确说明**具体的算力资源（如 GPU 型号、数量或具体训练时长）。由于该系统主要依赖 LLM 的 API 调用（如 GPT-4）或推理，且提到的微调是“可选的”且属于“轻量级”，因此实验重点在于闭环逻辑的有效性而非大规模算力消耗。

### 5. 实验数量与充分性
*   **实验规模**：针对 4 种不同的模型架构进行了测试，涵盖了闭环演化的 0 到 2 次迭代。
*   **充分性**：实验涵盖了从闭环的有效性验证到不同反馈策略（正/负）的对比。
*   **客观性**：通过在标准数据集 DocRED 上进行量化评估，并展示了系统在 Wikipedia 实际案例中的演化过程，实验设计较为客观。但实验主要集中在召回率的提升，对精确率（Precision）的详细量化波动描述相对较少。

### 6. 主要结论与发现
*   **性能显著提升**：在第一轮迭代中，仅通过提示词注入知识库抽象概念，各模型的召回率就提升了 **5-8%**。
*   **持续进化**：随着迭代次数增加（Iter-2），召回率进一步提升。例如 GPT-4.1 在正向反馈下召回率从 22.80% 提升至 37.03%。
*   **模型无关性**：无论是闭源模型（GPT, Kimi）还是开源模型（LLaMA），均能从该闭环系统中获益。
*   **知识库的引导作用**：知识库不仅是存储器，其生成的“层级抽象”能有效引导模型识别之前忽略的复杂关系。

### 7. 优点（亮点）
*   **闭环自演化**：打破了传统 IE 系统静态 Retraining 的限制，实现了“越用越聪明”。
*   **显式且可解释**：与纯参数化的模型不同，DySECT 的知识存储在可编辑、可检查的符号化知识库中，支持人工干预和纠偏。
*   **概率与逻辑结合**：引入了置信度计算和互斥约束，有效缓解了 LLM 抽取中的幻觉和噪声问题。
*   **灵活的反馈**：提供了正向引导和负向去重的双重策略，增强了探索新知识的能力。

### 8. 不足与局限
*   **错误累积风险**：尽管有置信度建模，但如果初始抽取存在系统性偏差，错误的知识可能会在闭环中被强化（Self-reinforcement of bias）。
*   **计算延迟**：闭环中的聚类、LLM 总结标签和多次迭代会增加单次任务的总推理时长和成本。
*   **评估维度单一**：实验重点在于召回率的提升，对于知识库规模扩大后是否会引入逻辑冲突或检索干扰缺乏深度探讨。
*   **领域迁移验证**：虽然声称适用于医疗、法律等，但主要实验仍基于通用的 DocRED 数据集，缺乏在极端专业领域（如高度复杂的法律条文）的实测数据。

（完）
