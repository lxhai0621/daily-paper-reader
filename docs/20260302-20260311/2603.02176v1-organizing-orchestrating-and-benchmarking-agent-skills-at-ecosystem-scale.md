---
title: "Organizing, Orchestrating, and Benchmarking Agent Skills at Ecosystem Scale"
title_zh: 在生态系统规模下组织、编排和基准测试智能体技能
authors: "Hao Li, Chunjiang Mu, Jianhao Chen, Siyue Ren, Zhiyao Cui, Yiqun Zhang, Lei Bai, Shuyue Hu"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.02176v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 通过DAG流水线进行技能选择、编排和执行的框架
tldr: 针对Claude智能体技能生态快速扩张带来的管理与扩展难题，本文提出AgentSkillOS框架。该框架通过能力树递归分类实现高效技能管理，并利用DAG流水线进行技能编排与执行。研究还构建了涵盖五大领域的基准测试，实验证明结构化组合是释放大规模技能潜力的关键，显著优于传统调用方式。
selection_source: fresh_fetch
motivation: 旨在解决在大规模智能体技能生态中，如何有效组织、检索和协同调用海量技能以完成复杂任务的挑战。
method: 提出了AgentSkillOS框架，包含基于能力树的技能管理阶段和基于有向无环图（DAG）的技能编排执行阶段。
result: 实验表明树状检索在大规模场景下接近最优选择，且DAG编排在任务完成质量上显著优于传统的扁平化调用。
conclusion: 结构化的技能组织与编排是提升智能体在大规模生态中处理复杂任务能力的核心要素。
---

## 摘要
Claude 智能体技能的快速激增提出了一个核心问题：如何有效地利用、管理和扩展智能体技能生态系统。在本文中，我们提出了 AgentSkillOS，这是首个用于技能选择、编排和生态系统级管理的原则性框架。AgentSkillOS 包含两个阶段：(i) 技能管理，通过节点级递归分类将技能组织成能力树，以实现高效发现；(ii) 任务解决，通过基于有向无环图（DAG）的流水线检索、编排并执行多种技能。为了评估智能体调用技能的能力，我们构建了一个包含 30 个富含制品（artifact-rich）任务的基准测试，涵盖五个类别：数据计算、文档创建、动态视频、视觉设计和网页交互。我们使用基于大语言模型（LLM）的两两比较评估来衡量任务输出的质量，并通过 Bradley-Terry 模型汇总结果以生成统一的质量评分。在三个技能生态系统规模（200 到 20 万个技能）上的实验表明，基于树的检索能有效逼近最优（oracle）技能选择，且即使在给定相同技能集的情况下，基于 DAG 的编排也显著优于原生的扁平化调用。我们的研究结果证实，结构化组合是释放技能潜力的关键。我们的 GitHub 仓库地址为：https://github.com/ynulihao/AgentSkillOS。

## Abstract
The rapid proliferation of Claude agent skills has raised the central question of how to effectively leverage, manage, and scale the agent skill ecosystem. In this paper, we propose AgentSkillOS, the first principled framework for skill selection, orchestration, and ecosystem-level management. AgentSkillOS comprises two stages: (i) Manage Skills, which organizes skills into a capability tree via node-level recursive categorization for efficient discovery; and (ii) Solve Tasks, which retrieves, orchestrates, and executes multiple skills through DAG-based pipelines. To evaluate the agent's ability to invoke skills, we construct a benchmark of 30 artifact-rich tasks across five categories: data computation, document creation, motion video, visual design, and web interaction. We assess the quality of task outputs using LLM-based pairwise evaluation, and the results are aggregated via a Bradley-Terry model to produce unified quality scores. Experiments across three skill ecosystem scales (200 to 200K skills) show that tree-based retrieval effectively approximates oracle skill selection, and that DAG-based orchestration substantially outperforms native flat invocation even when given the identical skill set. Our findings confirm that structured composition is the key to unlocking skill potential. Our GitHub repository is available at:https://github.com/ynulihao/AgentSkillOS.

---

## 论文详细总结（自动生成）

这篇论文针对大规模智能体技能生态系统的管理与应用提出了系统性的解决方案。以下是对该论文的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着 Claude 等智能体技能（Skills）数量的爆炸式增长（截至2026年2月已超过28万个），如何从海量、去中心化且质量参差不齐的技能池中**高效检索**、**合理管理**并**协同编排**多个技能来解决复杂任务，成为了智能体领域的核心挑战。现有的智能体通常只能处理单一技能或少量的扁平化调用，难以应对大规模生态系统带来的“信息过载”和“技能孤岛”问题。

### 2. 方法论：AgentSkillOS 框架
论文提出了 **AgentSkillOS**，这是首个针对生态规模技能设计的原则性框架，分为两个核心阶段：
*   **阶段一：技能管理（能力树构建）**
    *   **递归分类**：利用 LLM 将技能按功能递归划分为节点级层次结构。若节点技能数超过阈值，则继续分裂，最终形成“能力树”。
    *   **频率队列与休眠索引**：维护一个基于使用频率（如安装量）的队列，将高频技能纳入活跃树，低频技能放入基于向量检索的“休眠索引”，平衡了检索效率与覆盖面。
*   **阶段二：任务解决（检索与编排）**
    *   **任务驱动检索**：通过 LLM 沿能力树层级向下搜索，结合向量建议进行剪枝、去重和排序，筛选出 Top-M 个候选技能。
    *   **基于 DAG 的编排**：将复杂任务分解为子任务，并根据三种策略构建有向无环图（DAG）：
        1.  **质量优先**：增加准备和精炼步骤。
        2.  **效率优先**：最大化并行执行。
        3.  **简洁优先**：仅保留核心必要节点。
    *   **多技能执行**：按 DAG 层级顺序执行，自动管理节点间的数据流和依赖关系。

### 3. 实验设计
*   **数据集/场景**：构建了一个包含 **30 个任务** 的基准测试，涵盖五大领域：数据计算、文档创建、动态视频、视觉设计、网页交互。
*   **Benchmark 特性**：
    *   **富制品输出**：要求生成 PDF、PPTX、视频、HTML 等真实文件，而非简单的文本回复。
    *   **两两比较评估**：使用 LLM（Claude 4.5 Opus）作为裁判，对不同系统的输出进行双向盲测（消除位置偏见）。
    *   **Bradley-Terry 模型**：将两两胜负结果转化为统一的 0-100 分排名分。
*   **对比方法**：对比了 AgentSkillOS（三种策略）、原生 Claude Code（全量池、检索池、Oracle 技能池）以及无技能的 Vanilla 基线。

### 4. 资源与算力
*   **模型使用**：框架的构建、检索和编排使用了 **claude-opus-4.5**；节点执行使用了 **claude-sonnet-4.5**。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。由于该研究侧重于推理侧的框架设计和编排逻辑，而非模型本身的预训练，其资源消耗主要体现在 API 调用成本和推理延迟上。

### 5. 实验数量与充分性
*   **实验规模**：在 **200、1K、200K** 三种不同规模的技能生态下进行了测试。
*   **消融实验**：通过对比“Oracle 技能+扁平调用”与“Oracle 技能+DAG 编排”，验证了结构化编排的独立贡献。
*   **充分性评价**：实验设计较为充分，覆盖了从极小规模到极大规模的演进过程。通过 30 个跨领域任务和多维度的 DAG 结构分析，客观地证明了框架在处理复杂、多步骤任务时的优越性。

### 6. 主要结论与发现
*   **结构化胜过扁平化**：即使提供完全相同的技能集，基于 DAG 的编排性能也远超原生的扁平化调用。
*   **树状检索的有效性**：基于能力树的检索能够以极低的代价逼近“Oracle（人工预选）”的技能选择水平。
*   **规模扩展性**：AgentSkillOS 在 20 万规模的技能池中依然保持高性能，而原生方法在技能池扩大时性能显著下降（由于上下文窗口限制和检索噪声）。

### 7. 优点
*   **系统性强**：首次提出了从管理到执行的全链路技能操作系统概念。
*   **评估严谨**：引入 Bradley-Terry 模型和富制品评估，比传统的 Pass@1 或文本相似度更贴近真实应用。
*   **策略灵活**：提供的三种编排策略（质量/效率/简洁）满足了不同用户场景的需求。

### 8. 不足与局限
*   **成本与延迟**：基于 DAG 的多步执行和多次 LLM 检索/编排会显著增加任务的完成时间和 API 成本。
*   **依赖底层模型**：框架的效果高度依赖于底层 LLM（如 Claude 4.5）的推理和分类能力，在弱模型上的表现尚未验证。
*   **动态演化不足**：目前能力树的更新机制相对静态，对于实时快速更迭的技能生态，自动化的技能发现和质量自动过滤仍有提升空间。

（完）
