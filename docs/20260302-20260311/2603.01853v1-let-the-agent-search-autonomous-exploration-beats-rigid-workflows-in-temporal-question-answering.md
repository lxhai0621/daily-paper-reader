---
title: "Let the Agent Search: Autonomous Exploration Beats Rigid Workflows in Temporal Question Answering"
title_zh: 让智能体去搜索：时序问答中自主探索优于僵化工作流
authors: "Xufei Lv, Jiahui Yang, Yifu Gao, Linbo Qiao, Houde Liu"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01853v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 用于时序知识图谱问答的自主智能体
tldr: 针对时序知识图谱问答（TKGQA）中复杂的多步推理和时间约束，本文提出AT2QA。该方法摒弃了传统的固定检索工作流和昂贵的微调，通过赋予大模型自主决策权，利用通用搜索工具与知识图谱进行迭代交互。实验表明，这种自主探索模式在零样本下显著优于现有SOTA，尤其在处理多目标查询时表现卓越。
selection_source: fresh_fetch
motivation: 现有的时序问答方法受限于僵化的检索流程或高昂的微调成本，难以应对复杂多变的时序推理场景。
method: 提出AT2QA，一种无需训练的自主智能体，通过通用搜索工具与时序知识图谱进行动态交互和迭代检索。
result: "在MultiTQ数据集上，AT2QA实现了88.7%的Hits@1，比此前最优方法提升了10.7%，在多目标查询上提升达20.1%。"
conclusion: 研究证明，赋予大模型自主探索的权力比使用预定义的固定工作流在解决时序问答任务上更为有效。
---

## 摘要
时序知识图谱问答（TKGQA）要求在时间约束下进行多步推理。先前基于大语言模型（LLM）的方法通常依赖于僵化的、手工设计的检索工作流，或昂贵的有监督微调。我们展示了仅通过赋予现成 LLM 自主性（即让其决定下一步操作），即使在严格的零样本（zero-shot）设置下也能获得显著提升。基于这一见解，我们提出了 AT2QA，这是一种用于时序问答的自主、免训练智能体，它通过通用搜索工具与时序知识图谱进行迭代交互，以实现动态检索。在 MultiTQ 上的实验表明了巨大的改进：AT2QA 达到了 88.7% 的 Hits@1（比之前的 SOTA 提升了 10.7%），其中包括在具有挑战性的多目标查询上提升了 20.1%，这表明智能体自主性在时序问答中可以有力地超越微调。代码和完整的采样轨迹集可在 https://github.com/AT2QA-Official-Code/AT2QA-Official-Code 获取。

## Abstract
Temporal Knowledge Graph Question Answering (TKGQA) demands multi-hop reasoning under temporal constraints. Prior approaches based on large language models (LLMs) typically rely on rigid, hand-crafted retrieval workflows or costly supervised fine-tuning. We show that simply granting an off-the-shelf LLM autonomy, that is, letting it decide what to do next, already yields substantial gains even in a strict zero-shot setting. Building on this insight, we propose AT2QA, an autonomous, training-free agent for temporal question answering that iteratively interacts with the temporal knowledge graph via a general search tool for dynamic retrieval. Experiments on MultiTQ demonstrate large improvements: AT2QA achieves 88.7% Hits@1 (+10.7% over prior SOTA), including a +20.1% gain on challenging multi-target queries, showing that agentic autonomy can decisively outperform fine-tuning for temporal question answering. Code and the full set of sampled trajectories are available on https://github.com/AT2QA-Official-Code/AT2QA-Official-Code

---

## 论文详细总结（自动生成）

这是一份关于论文《Let the Agent Search: Autonomous Exploration Beats Rigid Workflows in Temporal Question Answering》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在时序知识图谱问答（TKGQA）中处理复杂的多步推理和严苛的时间约束，同时避免现有方法中常见的“错误累积”和“高昂训练成本”问题。
*   **研究背景**：
    *   **传统方法局限**：基于嵌入（Embedding）的方法像黑盒，缺乏语义理解；基于语义解析的方法难以泛化。
    *   **现有 LLM 方法局限**：大多采用**僵化的手工工作流**（如预定义的分解步骤），一旦中间检索出错，错误会迅速传播且无法纠正；或者依赖**昂贵的微调**（SFT/RL），扩展性差。
*   **核心动机**：作者发现，现代 LLM（如 DeepSeek-V3）本身已具备极强的自主规划能力。与其用死板的流程限制它，不如给它一个好用的搜索工具，让它自主决定“下一步该搜什么”。

### 2. 论文提出的方法论：AT2QA
AT2QA（Autonomous and Training-free Agent）是一个**自主且免训练**的智能体框架，核心包含两个部分：

*   **工具增强的推理框架（Tool-Augmented Reasoning）**：
    *   **结构化搜索工具**：提供一个 `<search>` 接口，支持语义检索（向量相似度）与符号过滤（时间窗口 `[start, end]`、实体过滤、关系过滤）的结合。
    *   **迭代交互与自纠错**：智能体遵循 `Thought -> Action -> Observation` 的循环。如果检索到的证据与假设冲突，智能体可以自主回滚、调整时间窗口或重写查询语句，实现**系统级自纠错**。
*   **免训练经验挖掘（Training-Free Experience Mining）**：
    *   借鉴 **GRPO（群体相对策略优化）** 的思想，但不更新模型参数。
    *   从模型自身生成的成功轨迹中，利用规则奖励（是否得到正确答案）筛选出高质量的推理路径。
    *   通过 LLM 评估这些路径的“启发价值”，构建一个精简的 Few-shot 示例库，在推理时引导模型稳定发挥。

### 3. 实验设计
*   **数据集**：使用 **MultiTQ**，这是目前最大、最具挑战性的多粒度时序问答基准（包含约 56,000 个测试问题），涵盖单步、多步、比较（First/Last）等多种时序逻辑。
*   **对比方法**：
    *   **传统嵌入法**：EmbedKGQA, CronKGQA, MultiQA。
    *   **基于 LLM 的静态工作流/微调法**：ARI, TempAgent, MemoTime, RTQA, 以及经过大规模微调的 PoK 和 Temp-R1。
*   **评价指标**：Hits@1（即 Exact Match，精确匹配率）。

### 4. 资源与算力
*   **算力使用**：论文**未明确说明**具体的 GPU 训练算力（如型号或数量），因为该方法的核心卖点是 **Training-free（免训练）**。
*   **推理成本**：作者提到在 MultiTQ 全量数据集（5.6万个问题）上运行 AT2QA 的 API 总成本约为 **150 美元**（基于 DeepSeek 官方 API 价格）。
*   **模型底座**：主要实验基于 **DeepSeek-V3.2**，消融实验测试了 Qwen3-Max, DeepSeek-R1 和 Kimi-2.5。

### 5. 实验数量与充分性
*   **实验规模**：在超过 5 万个测试样本上进行了全量评估，样本量非常充分。
*   **消融实验**：
    *   测试了不同 LLM 底座的泛化性。
    *   验证了搜索工具中“时间窗口”、“结构化过滤”和“时序排序”等组件的必要性。
    *   进行了 **Pass@k 分析**，证明了模型内部潜藏着解决难题的能力，只是需要被正确触发。
*   **客观性**：通过轨迹分析（CDF 曲线）量化了智能体的“自纠错”和“自验证”行为，而非仅仅给出最终分数，实验论证较为严密、客观。

### 6. 论文的主要结论与发现
*   **自主性胜过微调**：AT2QA 在零样本（Zero-shot）下就超过了许多经过微调的 SOTA 模型。最终 Hits@1 达到 **88.7%**，比此前最优方法提升了 **10.7%**。
*   **多目标查询的突破**：在最难的“多目标”问题上，AT2QA 提升了 **20.1%**，证明了自主探索在处理复杂逻辑时的优越性。
*   **能力诱导而非注入**：实验证明，解决复杂时序问题的能力已存在于强 LLM 的参数中，通过“经验挖掘”和“自主搜索”即可诱导出来，无需昂贵的梯度更新。

### 7. 优点与亮点
*   **范式创新**：挑战了“复杂任务必须依赖复杂工作流”的固有思维，证明了“给 LLM 自由”反而效果更好。
*   **高解释性**：AT2QA 提供完整的工具调用轨迹和事实证据链，每一步推理都清晰可查。
*   **低成本高效率**：无需微调，仅靠 Prompt 和工具调用即可实现超越 SOTA 的性能，极具实用价值。
*   **鲁棒的自纠错**：通过迭代机制有效解决了 RAG 系统中常见的幻觉和错误传播问题。

### 8. 不足与局限
*   **推理延迟**：由于需要多轮迭代交互（最高可达 20 轮），其推理延迟和 API 调用次数显著高于单次生成的 RAG 或固定工作流。
*   **对底座模型依赖强**：虽然方法是通用的，但其自主性高度依赖于底座 LLM 的指令遵循和逻辑规划能力，在较弱的模型上效果可能大打折扣。
*   **搜索效率**：在极大规模图谱上，频繁的迭代搜索可能带来检索压力，需要更高效的索引机制支持。

（完）
