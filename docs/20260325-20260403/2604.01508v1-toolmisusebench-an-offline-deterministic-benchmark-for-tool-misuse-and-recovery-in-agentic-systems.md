---
title: "ToolMisuseBench: An Offline Deterministic Benchmark for Tool Misuse and Recovery in Agentic Systems"
title_zh: ToolMisuseBench：一个用于评估智能体系统中工具误用与恢复的离线确定性基准测试
authors: "Akshey Sigdel, Rista Baral"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01508v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 智能体系统中工具误用与恢复的基准
tldr: ToolMisuseBench 是一个针对智能体系统工具误用与恢复能力的离线确定性基准测试。它涵盖了 CRUD、检索和调度等环境，通过注入可重现的故障（如参数无效、接口漂移），在严格的步骤和重试预算下评估智能体的恢复质量。该基准包含 6800 个任务，旨在解决智能体在操作层面而非语言理解层面的失效问题，为提升系统鲁棒性提供了评估框架。
source: arxiv
selection_source: fresh_fetch
motivation: 尽管语言理解能力很强，但工具调用智能体常因参数无效、接口漂移和恢复机制薄弱等操作性原因导致失败。
method: 提出了包含 6800 个任务的离线基准，通过在多种环境中注入可重现故障，并在限定预算下评估智能体的错误处理和恢复效率。
result: 基准测试结果显示，感知模式的方法在特定故障恢复上有所提升，但在授权受限和硬故障设置下的整体成功率仍然有限。
conclusion: 该研究为评估和优化智能体在面对工具调用错误时的鲁棒性及恢复策略提供了标准化的工具和数据集。
---

## 摘要
即使在语言理解能力很强的情况下，使用工具的智能体也经常因操作性原因而失败。常见原因包括无效参数、接口漂移、恢复能力弱以及低效的重试行为。我们推出了 ToolMisuseBench，这是一个离线确定性基准测试，用于在明确的步骤、调用和重试预算下评估工具的误用与恢复。该基准测试涵盖了 CRUD、检索、文件和调度环境，并支持可重现的故障注入。它报告了成功率、无效调用行为、策略违规、恢复质量以及预算效率。我们发布了一个包含 6800 个任务的公开数据集和一个可复现的评估流水线。基准测试结果显示，模式感知（schema-aware）方法在针对特定故障的恢复方面有所提升，但在发布的授权和硬故障设置下，整体成功率仍然有限。

## Abstract
Tool using agents often fail for operational reasons even when language understanding is strong. Common causes include invalid arguments, interface drift, weak recovery, and inefficient retry behavior. We introduce ToolMisuseBench, an offline deterministic benchmark for evaluating tool misuse and recovery under explicit step, call, and retry budgets. The benchmark covers CRUD, retrieval, file, and scheduling environments with replayable fault injection. It reports success, invalid call behavior, policy violations, recovery quality, and budgeted efficiency. We release a public dataset with 6800 tasks and a reproducible evaluation pipeline. Baseline results show fault specific recovery gains for schema aware methods, while overall success remains limited under the released authorization and hard failure settings.

---

## 论文详细总结（自动生成）

以下是对论文《ToolMisuseBench: An Offline Deterministic Benchmark for Tool Misuse and Recovery in Agentic Systems》的结构化深入总结：

### 1. 核心问题与研究动机
*   **研究背景**：尽管大语言模型（LLM）在理解能力上表现强劲，但在实际工作流中，智能体（Agent）常因操作性原因导致工具调用失败，如参数无效、接口漂移（Interface Drift）、恢复机制薄弱和重试逻辑低效。
*   **核心问题**：现有的基准测试（如 HELM、AgentBench）多侧重于通用能力，缺乏对工具调用可靠性、故障归因及在严格预算（步骤、调用次数、重试次数）下恢复能力的深入评估。
*   **研究动机**：建立一个离线、确定性且可重现的基准测试，用于隔离并分析智能体在面对模式错误、接口变动及环境不稳定时的表现。

### 2. 方法论与核心思想
*   **确定性模拟器**：构建了涵盖 CRUD（增删改查）、检索、文件管理和日程调度四种环境的离线模拟器。通过固定随机种子，确保每个任务序列完全可重现。
*   **声明式故障注入（Fault Injection）**：
    *   **故障类型**：包括模式漂移（Schema Drift）、速率限制（Rate Limit）、超时（Timeout）、授权失败（Authorization）及对抗性错误重写（Adversarial Error Rewriting）。
    *   **触发逻辑**：支持针对特定工具、特定调用次数或参数模式触发故障。
*   **预算感知评估**：引入了显式的预算限制（最大步骤、调用数、重试数）。
    *   **核心公式**：定义了预算成功率 $S(k)$，即在调用次数不超过 $k$ 的情况下任务成功的比例。这防止了智能体通过无限重试来掩盖不稳定性。
*   **评估指标**：除了最终成功率，还记录无效调用率、策略违规、恢复质量（Recovery Quality）和恢复所需时间。

### 3. 实验设计
*   **数据集规模**：共 6800 个任务，分为训练集（5000）、开发集（800）和公开测试集（1000）。
*   **实验场景**：跨越四个领域（CRUD、检索、文件、调度），确保了指令文本和初始状态的多样性。
*   **对比方法（Baselines）**：
    1.  **确定性启发式策略（Heuristic）**：基础调用逻辑。
    2.  **基于模式的修复（Schema Repair）**：针对参数错误增加自动修复逻辑。
    3.  **策略感知修复（Policy Aware）**：在模式修复基础上增加轻量级策略约束层。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或训练时长。
*   **实现细节**：该基准测试基于 Python 开发，强调离线本地执行，不依赖外部运行时服务，因此其评估过程对算力要求相对较低，主要消耗在于运行智能体逻辑。

### 5. 实验数量与充分性
*   **实验规模**：在 1000 个测试任务上进行了完整评估，并针对不同故障类型进行了细分分析（Per-fault analysis）。
*   **充分性评价**：实验设计较为充分，通过对比不同故障设置（如超时 vs. 授权失败）下的恢复率，揭示了现有方法的局限性。
*   **客观性**：采用确定性流水线和统一的观察接口，确保了不同智能体之间的比较是公平且可审计的。

### 6. 主要结论与发现
*   **恢复能力的局限性**：模式感知方法在处理“超时”和“模式漂移”故障时有显著恢复提升（恢复率约 0.5），但在“授权失败”和“速率限制”面前表现为零成功率。
*   **效率不等于可靠性**：启发式基线虽然调用次数较少，但无法处理任何故障；修复类方法虽然增加了调用次数，但在复杂故障组合下，整体成功率仍停留在 0.25 左右。
*   **瓶颈在于规划**：预算成功率曲线（AUC）在不同智能体间表现持平，表明当前的瓶颈在于高层规划和策略决策，而非简单的重试限制。

### 7. 优点与亮点
*   **确定性与可重现性**：通过任务种子和声明式故障计划，解决了智能体评估中常见的随机性问题。
*   **细粒度归因**：能够明确区分失败是源于调用构造错误、策略违规还是恢复能力不足。
*   **离线安全**：全离线环境避免了在线 API 调用产生的成本和隐私风险。
*   **预算敏感**：将资源消耗（调用次数）直接纳入成功率计算，符合工业界对 Agent 成本控制的需求。

### 8. 不足与局限
*   **环境合成化**：虽然受控性强，但模拟环境与真实的生产系统（如复杂的网络抖动、动态权限变更）仍有差距。
*   **基线多样性有限**：目前仅测试了简单的启发式和修复逻辑，缺乏对最先进 LLM（如 GPT-4, Claude 3.5）在特定提示工程下的深度评估。
*   **成本模型简化**：尚未建模真实的货币成本（Token 计费）或异构服务的延迟差异。
*   **应用限制**：主要针对工具调用层，对于长程逻辑推理和多智能体协作的覆盖较少。

（完）
