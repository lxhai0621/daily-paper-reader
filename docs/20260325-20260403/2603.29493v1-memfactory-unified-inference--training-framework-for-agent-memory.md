---
title: "MemFactory: Unified Inference & Training Framework for Agent Memory"
title_zh: MemFactory：智能体记忆的统一推理与训练框架
authors: "Ziliang Guo, Ziheng Li, Zhiyu Li"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29493v1"
tags: ["query:agent"]
score: 10.0
evidence: 强化学习优化智能体记忆的统一框架
tldr: "MemFactory是首个针对记忆增强型智能体设计的统一推理与训练框架。针对现有记忆管理实现碎片化且缺乏通用工具的问题，它借鉴LLaMA-Factory的模块化理念，将记忆生命周期抽象为可插拔的原子组件。该框架原生集成GRPO算法，支持利用环境奖励优化记忆策略，并兼容Memory-R1等前沿范式。实验证明，MemFactory在MemAgent架构上实现了最高14.8%的性能提升，显著降低了开发门槛。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29493v1/fig-001.webp\", \"caption\": \"Figure 1 The overall architecture of MemFactory, illustrating the interdependencies among the Modules, Agent, Environment, and Trainer layers.\", \"page\": 4, \"index\": 1, \"width\": 929, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2603.29493v1/fig-002.webp\", \"caption\": \"Table 1 Standard interfaces supported by modules in the Module Layer.\", \"page\": 5, \"index\": 2, \"width\": 968, \"height\": 221}]"
motivation: 现有的记忆增强型大模型实现方案高度碎片化且缺乏统一的基础设施，阻碍了复杂记忆流水线的集成、训练与评估。
method: 提出模块化的MemFactory框架，将记忆操作抽象为“乐高式”组件，并集成GRPO算法以实现多维环境奖励驱动的记忆管理策略微调。
result: "在MemAgent架构的实证评估中，该框架在域内和分布外测试集上均表现优异，性能相对基础模型提升了高达14.8%。"
conclusion: MemFactory通过提供标准化、可扩展且易用的基础设施，为记忆驱动型AI智能体的研究与创新提供了强有力的支持。
---

## 摘要
记忆增强型大语言模型（LLM）对于开发具备长期能力的 AI 智能体至关重要。近期，应用强化学习（RL）来优化记忆操作（如提取、更新和检索）已成为一个极具前景的研究方向。然而，现有的实现仍然高度碎片化且针对特定任务，缺乏统一的基础设施来简化这些复杂流水线的集成、训练和评估。为填补这一空白，我们提出了 MemFactory，这是首个专为记忆增强型智能体设计的统一、高度模块化的训练与推理框架。受 LLaMA-Factory 等统一微调框架成功的启发，MemFactory 将记忆生命周期抽象为原子化、即插即用的组件，使研究人员能够通过“乐高式”架构无缝构建自定义记忆智能体。此外，该框架原生集成了群体相对策略优化（GRPO），以在多维环境奖励的驱动下微调内部记忆管理策略。MemFactory 为 Memory-R1、RMM 和 MemAgent 等近期前沿范式提供开箱即用的支持。我们在开源的 MemAgent 架构上，利用其公开的训练和评估数据对 MemFactory 进行了实证验证。在域内和分布外评估集上，MemFactory 相比相应的基座模型均能持续提升性能，相对增益最高达 14.8%。通过提供标准化、可扩展且易于使用的基础设施，MemFactory 显著降低了准入门槛，为记忆驱动型 AI 智能体的未来创新铺平了道路。

## Abstract
Memory-augmented Large Language Models (LLMs) are essential for developing capable, long-term AI agents. Recently, applying Reinforcement Learning (RL) to optimize memory operations, such as extraction, updating, and retrieval, has emerged as a highly promising research direction. However, existing implementations remain highly fragmented and task-specific, lacking a unified infrastructure to streamline the integration, training, and evaluation of these complex pipelines. To address this gap, we present MemFactory, the first unified, highly modular training and inference framework specifically designed for memory-augmented agents. Inspired by the success of unified fine-tuning frameworks like LLaMA-Factory, MemFactory abstracts the memory lifecycle into atomic, plug-and-play components, enabling researchers to seamlessly construct custom memory agents via a "Lego-like" architecture. Furthermore, the framework natively integrates Group Relative Policy Optimization (GRPO) to fine-tune internal memory management policies driven by multi-dimensional environmental rewards. MemFactory provides out-of-the-box support for recent cutting-edge paradigms, including Memory-R1, RMM, and MemAgent. We empirically validate MemFactory on the open-source MemAgent architecture using its publicly available training and evaluation data. Across both in-domain and out-of-distribution evaluation sets, MemFactory consistently improves performance over the corresponding base models, with relative gains of up to 14.8%. By providing a standardized, extensible, and easy-to-use infrastructure, MemFactory significantly lowers the barrier to entry, paving the way for future innovations in memory-driven AI agents.

---

## 论文详细总结（自动生成）

### MemFactory: 智能体记忆的统一推理与训练框架论文总结

#### 1. 论文的核心问题与整体含义
*   **研究背景**：为了让大语言模型（LLM）进化为具备长期能力的自主智能体，必须赋予其维护长期上下文、积累历史经验和个性化行为的能力。
*   **核心问题**：虽然利用强化学习（RL）优化记忆操作（提取、更新、检索）已成为前沿趋势（如 Memory-R1, MemAgent），但现有实现高度碎片化、任务耦合度高，且缺乏像 LLaMA-Factory 那样统一的基础设施来简化记忆流水线的集成、训练和评估。
*   **整体含义**：本文推出了 **MemFactory**，这是首个专为记忆增强型智能体设计的模块化、统一的训练与推理框架，旨在降低 Memory-RL 研究的工程门槛。

#### 2. 论文提出的方法论
MemFactory 借鉴了“乐高式”的模块化设计，将框架分为四个核心层：
*   **模块层 (Module Layer)**：将记忆生命周期抽象为原子操作。
    *   **Extractor**：从原始上下文中解析事实和经验。
    *   **Updater**：执行增删改（ADD, DEL, UPDATE, NONE）操作维护记忆库。
    *   **Retriever**：负责语义检索及基于推理模型的重排序（Rerank）。
    *   **Agent Module**：支持端到端的循环记忆策略（如 MemAgent 范式）。
*   **智能体层 (Agent Layer)**：作为策略执行器，通过组合上述原子模块构建自定义智能体，支持 `generate`（训练生成）、`rollout`（环境交互）和 `inference`（纯推理）接口。
*   **环境层 (Environment Layer)**：负责数据加载与奖励管理。提供 `MemoryBankEnv`（长期记忆库）和 `LongcontextEnv`（短期长上下文）两种环境，并支持格式奖励和 LLM-as-a-Judge 等多维奖励信号。
*   **训练层 (Trainer Layer)**：核心优化引擎。
    *   **关键技术**：原生集成 **GRPO (Group Relative Policy Optimization)** 算法。
    *   **算法流程**：针对每个输入采样一组（G个）候选响应，通过组内奖励归一化计算优势函数（Advantage），从而在无需 Critic 模型的情况下实现高效策略更新，显著降低显存占用。

#### 3. 实验设计
*   **实验场景**：基于 **MemAgent** 架构进行实证验证，该架构将记忆视为跨文本段落的循环状态。
*   **基座模型**：Qwen3-1.7B 和 Qwen3-4B-Instruct。
*   **数据集/Benchmark**：
    *   使用 MemAgent 公开的训练集进行微调。
    *   测试集包括两个主任务集（`eval_50`, `eval_100`）和一个分布外（OOD）测试集（`eval_fwe_16384`）。
*   **对比方法**：对比了各基座模型在 RL 微调前后的性能表现。

#### 4. 资源与算力
*   **硬件设备**：整个训练和评估流水线可以在 **单张 NVIDIA A800 80GB GPU** 上运行。
*   **训练细节**：每个模型训练 250 个有效步数（valid steps），集成了 FlashAttention-2 以处理长上下文并减少显存消耗。

#### 5. 实验数量与充分性
*   **实验规模**：针对两个不同规模的模型（1.7B 和 4B）在三个测试集上进行了评估。
*   **客观性保障**：所有评分均采用 4 次独立试验的平均值（avg@4），以减少生成随机性带来的偏差。
*   **充分性**：实验涵盖了域内和分布外（OOD）场景，验证了框架的泛化能力。虽然目前主要针对 MemAgent 架构进行了深入实验，但框架本身已内置了对 Memory-R1 和 RMM 的支持，展示了较强的通用性。

#### 6. 论文的主要结论与发现
*   **性能提升**：MemFactory 显著提升了基座模型的记忆管理能力。Qwen3-1.7B 的平均得分提升了 **14.8%**，Qwen3-4B-Instruct 提升了 **7.3%**。
*   **模型规模影响**：较大规模的模型（4B）在 OOD 测试集上表现出更强的鲁棒性和策略迁移能力，而较小模型（1.7B）在 OOD 场景下性能略有下降。
*   **可行性验证**：证明了在有限的计算资源（单卡）下，通过统一框架进行 Memory-RL 优化是切实可行的。

#### 7. 优点
*   **高度模块化**：通过原子化组件实现了“乐高式”构建，极大方便了研究人员交换或组合不同的记忆策略。
*   **显存友好**：集成 GRPO 算法，避免了 PPO 算法中 Critic 模型带来的双倍显存开销，适合长上下文任务。
*   **开箱即用**：预置了多种前沿 SOTA 范式（Memory-R1, RMM, MemAgent），具有很强的实用价值。

#### 8. 不足与局限
*   **实验覆盖面**：虽然框架支持多种范式，但论文中的实证部分主要集中在 MemAgent 架构上，对 Memory-R1 和 RMM 的实际训练增益缺乏详细的对比数据。
*   **训练效率**：作者提到当前版本在训练效率上仍有提升空间。
*   **小模型泛化风险**：实验显示小参数模型在经过 RL 优化后，在分布外（OOD）任务上可能存在性能回退的风险。

（完）
