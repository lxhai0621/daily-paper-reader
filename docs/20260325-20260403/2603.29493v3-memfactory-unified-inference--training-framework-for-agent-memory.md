---
title: "MemFactory: Unified Inference & Training Framework for Agent Memory"
title_zh: MemFactory：智能体记忆的统一推理与训练框架
authors: "Ziliang Guo, Ziheng Li, Bo Tang, Feiyu Xiong, Zhiyu Li"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29493v3"
tags: ["query:agent"]
score: 10.0
evidence: 用于智能体强化学习优化记忆操作的统一框架
tldr: 针对长短期记忆增强型智能体开发中内存管理实现碎片化的问题，本文提出了MemFactory，这是首个统一且高度模块化的智能体内存训练与推理框架。它借鉴LLaMA-Factory的成功经验，将内存生命周期抽象为原子化组件，支持通过“乐高式”架构构建自定义内存智能体。该框架集成了GRPO算法以优化内存策略，并在MemAgent等前沿范式上验证了其有效性，显著降低了内存驱动型AI智能体的开发门槛。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29493v3/fig-001.webp\", \"caption\": \"Figure 1 The overall architecture of MemFactory, illustrating the interdependencies among the Modules, Agent, Environment, and Trainer layers.\", \"page\": 4, \"index\": 1, \"width\": 929, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2603.29493v3/fig-002.webp\", \"caption\": \"Table 1 Standard interfaces supported by modules in the Module Layer.\", \"page\": 5, \"index\": 2, \"width\": 968, \"height\": 219}]"
motivation: 现有的内存增强型大模型实现高度碎片化且缺乏统一的基础设施，阻碍了内存管理策略的集成、训练和评估。
method: 提出一种模块化的“乐高式”架构，将内存操作抽象为可插拔组件，并原生集成GRPO算法进行多维度环境奖励驱动的策略微调。
result: "在MemAgent架构上的实验表明，MemFactory相比基础模型平均性能有所提升，最高相对增益达到14.8%。"
conclusion: MemFactory为内存驱动的AI智能体提供了标准化、可扩展且易用的基础设施，将推动该领域的未来创新。
---

## 摘要
记忆增强的大语言模型（LLMs）对于开发具备长期能力的 AI 智能体至关重要。近期，应用强化学习（RL）来优化记忆操作（如提取、更新和检索）已成为一个极具前景的研究方向。然而，现有的实现仍然高度碎片化且针对特定任务，缺乏统一的基础设施来简化这些复杂流水线的集成、训练和评估。为了填补这一空白，我们提出了 MemFactory，这是首个专为记忆增强智能体设计的统一、高度模块化的训练与推理框架。受 LLaMA-Factory 等统一微调框架成功的启发，MemFactory 将记忆生命周期抽象为原子化的、即插即用的组件，使研究人员能够通过“乐高式”架构无缝构建自定义记忆智能体。此外，该框架原生集成了群体相对策略优化（GRPO），以在多维环境奖励的驱动下微调内部记忆管理策略。MemFactory 为近期前沿范式（包括 Memory-R1、RMM 和 MemAgent）提供开箱即用的支持。我们在开源的 MemAgent 架构上，利用其公开的训练和评估数据对 MemFactory 进行了实证验证。在各个评估集上，MemFactory 相比对应的基础模型平均提升了性能，相对增益最高达 14.8%。通过提供标准化、可扩展且易于使用的基础设施，MemFactory 显著降低了准入门槛，为未来记忆驱动型 AI 智能体的创新铺平了道路。

## Abstract
Memory-augmented Large Language Models (LLMs) are essential for developing capable, long-term AI agents. Recently, applying Reinforcement Learning (RL) to optimize memory operations, such as extraction, updating, and retrieval, has emerged as a highly promising research direction. However, existing implementations remain highly fragmented and task-specific, lacking a unified infrastructure to streamline the integration, training, and evaluation of these complex pipelines. To address this gap, we present MemFactory, the first unified, highly modular training and inference framework specifically designed for memory-augmented agents. Inspired by the success of unified fine-tuning frameworks like LLaMA-Factory, MemFactory abstracts the memory lifecycle into atomic, plug-and-play components, enabling researchers to seamlessly construct custom memory agents via a "Lego-like" architecture. Furthermore, the framework natively integrates Group Relative Policy Optimization (GRPO) to fine-tune internal memory management policies driven by multi-dimensional environmental rewards. MemFactory provides out-of-the-box support for recent cutting-edge paradigms, including Memory-R1, RMM, and MemAgent. We empirically validate MemFactory on the open-source MemAgent architecture using its publicly available training and evaluation data. Across the evaluation sets, MemFactory improves performance over the corresponding base models on average, with relative gains of up to 14.8%. By providing a standardized, extensible, and easy-to-use infrastructure, MemFactory significantly lowers the barrier to entry, paving the way for future innovations in memory-driven AI agents.

---

## 论文详细总结（自动生成）

### MemFactory：智能体记忆的统一推理与训练框架论文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）向自主智能体演进，长期记忆管理（提取、更新、检索）变得至关重要。目前，利用强化学习（RL）优化记忆策略（如 Memory-R1, MemAgent）已成为前沿趋势。然而，该领域存在严重的**碎片化问题**：现有的记忆强化学习实现通常与特定任务深度耦合，缺乏统一的基础设施。研究人员在复现或组合不同记忆模块时，面临巨大的工程负担。
**MemFactory 的核心目标**是填补这一空白，借鉴 LLaMA-Factory 的成功经验，为记忆增强型智能体提供首个统一、模块化且易用的训练与推理框架。

#### 2. 论文提出的方法论
MemFactory 采用高度解耦的四层架构，支持“乐高式”构建自定义记忆智能体：
*   **模块层 (Module Layer)：** 将记忆生命周期抽象为原子操作，包括：
    *   **Extractor（提取器）：** 从原始上下文中解析事实。
    *   **Updater（更新器）：** 执行 ADD/DEL/UPDATE/NONE 等操作维护记忆库。
    *   **Retriever（检索器）：** 负责语义检索及基于大推理模型（LRM）的重排序。
    *   **Agent Module（智能体模块）：** 支持端到端的循环记忆策略（如 MemAgent）。
*   **智能体层 (Agent Layer)：** 作为策略执行器，组合上述模块生成交互轨迹（Rollout）。
*   **环境层 (Environment Layer)：** 充当数据加载器和奖励管理器，提供格式奖励（Format Reward）和 LLM-as-a-Judge 等多维信号。
*   **训练层 (Trainer Layer)：** 核心优化引擎，原生集成 **GRPO（群体相对策略优化）** 算法。GRPO 通过组内奖励归一化估计基准，无需额外的 Critic 模型，显著降低了长上下文训练时的显存开销。

#### 3. 实验设计
*   **数据集/场景：** 使用了 MemAgent 公开的训练和评估数据集，涵盖长文本处理和对话任务。
*   **Benchmark：** 包含两个主任务测试集（`eval_50`, `eval_100`）和一个分布外（OOD）测试集（`eval_fwe_16384`）。
*   **对比方法：** 以 Qwen3-1.7B 和 Qwen3-4B-Instruct 作为基础模型（Base Checkpoint），对比经过 MemFactory 强化学习微调后的性能提升。
*   **内置范式：** 框架预置了对 Memory-R1、MemAgent 和 RMM 三种 SOTA 范式的支持。

#### 4. 资源与算力
*   **硬件：** 整个训练和评估流水线可以在**单张 NVIDIA A800 80GB GPU** 上执行。
*   **训练细节：** 每个模型训练 250 个有效步（steps）。
*   **效率：** 论文强调了该框架对计算资源的友好性，即使在有限的算力下也能复现复杂的记忆策略。

#### 5. 实验数量与充分性
*   **实验规模：** 针对两种不同规模的基础模型进行了实验，每组评分均基于 4 次独立试验的平均值（avg@4）以减少生成随机性的影响。
*   **充分性：** 实验涵盖了主任务和 OOD 任务，验证了策略的泛化能力。
*   **客观性：** 使用了公开数据集和标准化的 GRPO 流程，实验设计较为公平，但目前主要针对 MemAgent 架构进行了深入验证，对其他内置范式（如 Memory-R1）的横向对比实验在文中体现较少。

#### 6. 主要结论与发现
*   **性能提升：** MemFactory 显著提升了基础模型的记忆管理能力。Qwen3-1.7B 平均性能提升 **14.8%**，Qwen3-4B-Instruct 提升 **7.3%**。
*   **模型规模效应：** 较大的模型（4B）在 OOD 任务上表现更稳健，能够更好地将学习到的循环记忆策略迁移到未见场景。
*   **框架有效性：** 证明了通过统一框架和强化学习，可以使模型自主演化出复杂的记忆策略（如压缩历史、保留关键事实），优于传统的启发式规则。

#### 7. 优点
*   **高度模块化：** “乐高式”设计极大降低了开发门槛，方便研究者快速实验不同的模块组合。
*   **显存高效：** 原生支持 GRPO，解决了长上下文记忆训练中显存溢出的痛点。
*   **开箱即用：** 集成了多种前沿算法（Memory-R1 等），提供了标准化的基准实现。
*   **统一接口：** 规范了 `generate`、`rollout` 和 `inference` 接口，实现了训练与推理的无缝衔接。

#### 8. 不足与局限
*   **范式覆盖有限：** 虽然支持三大主流范式，但对于更复杂的分布式记忆或多智能体协同记忆的覆盖尚在完善中。
*   **训练效率：** 论文提到在训练效率方面仍有进一步优化的空间。
*   **小模型泛化风险：** 实验显示 1.7B 模型在 OOD 任务上性能有所下降，说明轻量级模型在记忆策略学习中可能存在过拟合或泛化瓶颈。
*   **任务多样性：** 实验主要集中在 MemAgent 任务上，未来需在更多样化的智能体环境（如 Web 导航、工具调用）中验证。

（完）
