---
title: "Privacy Guard & Token Parsimony by Prompt and Context Handling and LLM Routing"
title_zh: 通过提示词与上下文处理及大语言模型路由实现的隐私保护与 Token 节约
authors: Alessio Langiu
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28972v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 自动提示词优化与上下文处理
tldr: "针对大语言模型应用中运营成本与隐私泄露的权衡难题，本文提出“不可分割范式”，认为上下文管理与隐私保护本质一致。通过本地部署的小模型（SLM）构建“隐私卫士”，利用摘要和自动提示优化（APO）分解任务，并将敏感查询路由至零信任模型。该方案在实现零泄露的同时，通过减少云端Token消耗降低了45%的成本，为高效且安全的LLM部署提供了新路径。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.28972v1/fig-001.webp\", \"caption\": \"Figure 7: Latency overhead simulation (Time-To-FirstToken). The Dual-Vault architecture becomes faster than direct Cloud routing for contexts larger than 10k tokens due to massive payload reduction.\", \"page\": 11, \"index\": 1, \"width\": 458, \"height\": 337}, {\"url\": \"assets/figures/arxiv/2603.28972v1/fig-002.webp\", \"caption\": \"Figure 5: LLM-as-a-Judge comparison of final response quality between raw Context (Baseline) and compressed/sanitised Context (Dual-Vault APO).\", \"page\": 10, \"index\": 2, \"width\": 388, \"height\": 325}, {\"url\": \"assets/figures/arxiv/2603.28972v1/fig-003.webp\", \"caption\": \"Figure 2: Token Parsimony vs Data Leakage by user profile and secret typology. The model achieves high Token Parsimony across the board but suffers from severe Leakage strictly in the Lazy/Institutional quadrant.\", \"page\": 8, \"index\": 3, \"width\": 463, \"height\": 269}, {\"url\": \"assets/figures/arxiv/2603.28972v1/fig-004.webp\", \"caption\": \"Figure 4: Projected vs actual OpEx reduction when comparing a standard monolithic Cloud API call against active local prompt decomposition and selective routing.\", \"page\": 9, \"index\": 4, \"width\": 459, \"height\": 337}, {\"url\": \"assets/figures/arxiv/2603.28972v1/fig-005.webp\", \"caption\": \"Figure 3: Exploratory space analysis of Leakage Rate across parameter scales (8B to 104B). Results are based on limited sample sizes designed to identify structural vulnerabilities rather than provide exhaustive statistical baselines.\", \"page\": 9, \"index\": 5, \"width\": 462, \"height\": 269}, {\"url\": \"assets/figures/arxiv/2603.28972v1/fig-006.webp\", \"caption\": \"Figure 1: The Dual-Vault Architecture. A local SLM acts as an Orchestrator and Privacy Guard, decoupling intents from secrets and routing the sanitised sub-tasks to the appropriate Cloud Tier based on trust and risk levels.\", \"page\": 5, \"index\": 6, \"width\": 879, \"height\": 483}]"
motivation: 现有的LLM路由框架在降低成本时忽视了提示词敏感性，导致用户面临向第三方云供应商泄露隐私的风险。
method: 提出一种由本地小模型驱动的“隐私卫士”，通过自动提示优化和上下文压缩技术对任务进行分解与脱敏路由。
result: "实验显示该框架实现了100%的个人隐私脱敏成功率，降低了45%的运营成本，且压缩后的响应偏好率达85%。"
conclusion: 研究证明了Token节约与零泄露是上下文压缩算子的两个对偶投影，实现了隐私保护与成本优化的双赢。
---

## 摘要
大语言模型（LLM）的大规模采用迫使人们在运营成本（OpEx）与数据隐私之间进行权衡。当前的路由框架虽然降低了成本，但忽略了提示词的敏感性，使个人用户和机构面临向第三方云提供商泄露数据的风险。我们正式提出了“不可分范式”（Inseparability Paradigm）：先进的上下文管理在本质上与隐私管理相一致。我们提出了一种本地“隐私卫士”（Privacy Guard）——这是一个由本地小语言模型（SLM）驱动的全方位上下文观察器——它通过执行抽象式摘要和自动提示词优化（APO）将提示词分解为聚焦的子任务，并将高风险查询重定向至零信任或受保密协议（NDA）覆盖的模型。这种双重机制在消除敏感推理向量（零泄露）的同时，减少了云端 Token 负载（降低 OpEx）。一种基于后进先出（LIFO）的上下文压缩机制进一步限制了工作内存，从而限制了潜在的泄露面。我们通过一个包含 1,000 个样本的数据集，在 2x2 基准测试（懒惰型 vs. 专家型用户；个人 vs. 机构秘密）上验证了该框架，实现了 45% 的综合运营成本降低，个人秘密的脱敏成功率达到 100%，并且通过 LLM-as-a-Judge 评估，APO 压缩后的响应相比原始基准获得了 85% 的偏好率。我们的结果表明，Token 节约与零泄露在数学上是同一种上下文压缩算子的对偶投影。

## Abstract
The large-scale adoption of Large Language Models (LLMs) forces a trade-off between operational cost (OpEx) and data privacy. Current routing frameworks reduce costs but ignore prompt sensitivity, exposing users and institutions to leakage risks towards third-party cloud providers. We formalise the "Inseparability Paradigm": advanced context management intrinsically coincides with privacy management. We propose a local "Privacy Guard" -- a holistic contextual observer powered by an on-premise Small Language Model (SLM) -- that performs abstractive summarisation and Automatic Prompt Optimisation (APO) to decompose prompts into focused sub-tasks, re-routing high-risk queries to Zero-Trust or NDA-covered models. This dual mechanism simultaneously eliminates sensitive inference vectors (Zero Leakage) and reduces cloud token payloads (OpEx Reduction). A LIFO-based context compacting mechanism further bounds working memory, limiting the emergent leakage surface. We validate the framework through a 2x2 benchmark (Lazy vs. Expert users; Personal vs. Institutional secrets) on a 1,000-sample dataset, achieving a 45% blended OpEx reduction, 100% redaction success on personal secrets, and -- via LLM-as-a-Judge evaluation -- an 85% preference rate for APO-compressed responses over raw baselines. Our results demonstrate that Token Parsimony and Zero Leakage are mathematically dual projections of the same contextual compression operator.

---

## 论文详细总结（自动生成）

这篇论文提出了一种结合隐私保护与成本优化的新型大语言模型（LLM）编排框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心矛盾**：企业和个人在采用大模型时，面临**运营成本（OpEx）**与**数据隐私**之间的权衡。
*   **现有局限**：目前的模型路由框架（如 FrugalGPT）仅关注成本和性能，忽略了提示词（Prompt）中的敏感信息；而传统的脱敏工具（如正则匹配）难以应对“涌现性泄露”（即通过上下文推理出敏感属性）。
*   **研究背景**：用户普遍存在“懒惰行为”（直接粘贴大量未脱敏文档），导致 Token 浪费和隐私风险激增。论文提出了“不可分割范式”，认为减少 Token 消耗（上下文压缩）与保护隐私（减少敏感信息暴露）在数学上是同一操作的两个侧面。

### 2. 方法论
*   **核心思想**：在本地部署一个小语言模型（SLM）作为“隐私卫士”和“全方位上下文观察器”，在数据离开本地前进行语义压缩、任务分解和脱敏。
*   **关键技术细节**：
    *   **双金库架构（Dual-Vault）**：逻辑上隔离“个人金库”（保护个人隐私）和“机构金库”（保护企业机密）。
    *   **自动提示词优化（APO）与任务分解**：SLM 将复杂的长提示词重写并分解为多个聚焦的子任务。通过只发送任务相关的核心信息，天然地过滤掉无关的敏感背景。
    *   **分层信任路由**：根据脱敏后的残余风险，将任务路由至不同等级的云端（从免费/不可信层到受 NDA 保护的合规层，再到本地零信任层）。
    *   **LIFO 上下文压缩**：采用后进先出（LIFO）机制管理对话记忆，将长对话压缩为摘要，使云端 Token 消耗保持在常数级 $O(1)$，同时消除历史对话中的隐私累积风险。

### 3. 实验设计
*   **测试场景（2x2 矩阵）**：
    *   **用户维度**：专家型（精简指令）vs. 懒惰型（大量文档堆砌）。
    *   **秘密维度**：个人秘密（PII、医疗数据等）vs. 机构秘密（代码、API 密钥、服务器日志）。
*   **数据集**：使用 Faker 库动态生成的 1,000 个样本，包含 3,250 个注入的秘密。
*   **对比基准（Benchmark）**：
    *   **Token 节约率**：对比原始提示词与 APO 压缩后的 Token 数量。
    *   **脱敏成功率**：测量本地 SLM 拦截秘密的准确度。
    *   **回答质量**：采用 **LLM-as-a-Judge** 机制，对比压缩前后云端模型生成答案的质量。

### 4. 资源与算力
*   **本地模型**：主要使用 7B 到 14B 参数规模的模型（如 Qwen 2.5 7B, Llama 3.1 8B）。
*   **硬件要求**：实验指出该框架可在**消费级硬件**上运行，如配备 16GB VRAM 的 NVIDIA T4 GPU 或 8-12GB VRAM 的普通游戏显卡。
*   **量化技术**：使用了 4-bit (Q4_0) 和 6-bit 量化以降低显存占用。
*   **延迟**：本地 SLM 处理延迟约为 0.67 秒。

### 5. 实验数量与充分性
*   **实验规模**：
    *   初步测试 40 个样本（140 个秘密）。
    *   大规模验证扩展至 1,000 个样本。
    *   针对任务分解做了 118 组系统日志处理实验。
    *   针对模型规模做了从 8B 到 104B 的探索性实验。
*   **充分性评价**：实验设计较为全面，涵盖了不同用户行为和秘密类型。通过大规模样本量和多维度指标（成本、安全、质量、延迟）的交叉验证，结论具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **成本大幅降低**：平均降低了 **45%** 的云端运营成本；在处理大量日志的任务分解实验中，成本降幅高达 **97.5%**。
*   **隐私保护有效性**：对个人秘密实现了 **100%** 的脱敏成功率。
*   **质量反而提升**：在 85% 的案例中，经过 APO 压缩后的回答质量优于原始长提示词，因为压缩去除了干扰噪声，缓解了模型的“迷失在中间（Lost in the Middle）”现象。
*   **延迟补偿**：当提示词超过 10k Token 时，本地压缩节省的云端读取时间超过了本地处理的开销，系统反而变得更快。
*   **模型规模局限**：7B 规模的模型在处理复杂的机构秘密（如埋藏在日志中的密钥）时存在 12.9% 的漏报率，需升级至 70B 模型或结合确定性过滤工具。

### 7. 亮点
*   **理论创新**：提出了“不可分割定理”，将隐私保护与成本优化这两个看似冲突的目标统一在“上下文压缩”这一技术路径下。
*   **实用性强**：不依赖昂贵的企业级算力，普通消费级显卡即可部署，具有极高的落地价值。
*   **多重收益**：不仅省钱、保隐私，还能通过减少噪声提升大模型的逻辑推理质量。

### 8. 不足与局限
*   **小模型局限性**：7B 级别的 SLM 在处理非结构化、高复杂度的工业日志时，脱敏可靠性不足，存在“漏网之鱼”。
*   **混合架构依赖**：为了达到 100% 的企业级安全，仍需额外引入正则扫描等确定性过滤层，增加了系统复杂性。
*   **应用限制**：对于极度依赖长上下文细节（如文学创作、长文本校对）的任务，激进的语义压缩可能会导致关键细节丢失。

（完）
