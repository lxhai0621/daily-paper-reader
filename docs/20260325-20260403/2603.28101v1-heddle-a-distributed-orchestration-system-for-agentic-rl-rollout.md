---
title: "Heddle: A Distributed Orchestration System for Agentic RL Rollout"
title_zh: Heddle：一种用于智能体强化学习 Rollout 的分布式编排系统
authors: "Zili Zhang, Yinmin Zhong, Chengxu Yang, Chao Jin, Bingyang Wu, Xinming Wei, Yuliang Liu, Xin Jin"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28101v1"
tags: ["query:agent"]
score: 9.0
evidence: 智能体强化学习展开的分布式编排系统
tldr: 针对 Agentic RL 中 LLM 与工具交互导致的轨迹生成长尾瓶颈问题，本文提出了分布式编排系统 Heddle。Heddle 采用以轨迹为中心的设计，通过轨迹级调度、感知轨迹的放置策略以及自适应资源管理，优化了任务执行的时机、位置和方式。实验表明，Heddle 显著减少了排队延迟和干扰，将端到端 Rollout 吞吐量提升了高达 2.5 倍。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.28101v1/fig-001.webp\", \"caption\": \"Figure 10: Illustration of the placement algorithm. complexity is 𝑂 (𝑛2𝑚), a drastic reduction from the combinatorial baseline𝑂 ( (𝑛−1\", \"page\": 7, \"index\": 1, \"width\": 498, \"height\": 293}, {\"url\": \"assets/figures/arxiv/2603.28101v1/fig-002.webp\", \"caption\": \"Figure 12: Rollout throughput of agentic RL systems under different workloads and models.\", \"page\": 10, \"index\": 2, \"width\": 1042, \"height\": 219}, {\"url\": \"assets/figures/arxiv/2603.28101v1/fig-003.webp\", \"caption\": \"Figure 13: Precision of progressive trajectory prediction under different workloads and models.\", \"page\": 10, \"index\": 3, \"width\": 1042, \"height\": 220}, {\"url\": \"assets/figures/arxiv/2603.28101v1/fig-004.webp\", \"caption\": \"Figure 8: Overview of Heddle.\", \"page\": 5, \"index\": 4, \"width\": 494, \"height\": 250}]"
motivation: 现有的以步骤为中心的设计忽略了轨迹上下文，导致 Agentic RL 在频繁调用工具时面临严重的排队延迟、干扰和长尾轨迹生成效率低下的问题。
method: Heddle 引入了基于运行时预测的轨迹级调度、利用动态规划和机会性迁移的轨迹感知放置，以及动态调整模型并行度的自适应资源管理。
result: 在多种 Agentic RL 工作负载下，Heddle 成功消除了长尾瓶颈，相比现有基准系统实现了高达 2.5 倍的端到端 Rollout 吞吐量提升。
conclusion: Heddle 证明了以轨迹为中心的编排系统能有效解决 Agentic RL 中的长尾分布问题，是提升大规模强化学习训练效率的关键。
---

## 摘要
智能体强化学习（Agentic RL）通过在数据收集的 rollout 阶段和策略训练阶段之间交替，使大语言模型（LLM）能够解决复杂任务。在 rollout 过程中，智能体生成轨迹（trajectories），即 LLM 与外部工具之间的多步交互。然而，频繁的工具调用会导致长尾轨迹生成，从而成为 rollout 的瓶颈。这源于忽略轨迹上下文的以步骤为中心（step-centric）的设计，为长尾轨迹生成引发了三个系统问题：排队延迟、干扰开销和单 token 时间膨胀。我们提出了 Heddle，这是一个以轨迹为中心（trajectory-centric）的系统，旨在优化智能体 rollout 执行的时间、位置和方式。Heddle 集成了三个核心机制：利用运行时间预测和渐进优先级进行轨迹级调度，以最小化累积排队；通过预排序动态规划和在工具调用空闲间隔期间进行机会性迁移，实现轨迹感知放置，以最小化干扰；以及轨迹自适应资源管理器，动态调整模型并行度，以加速长尾轨迹的单 token 时间，同时保持短轨迹的高吞吐量。在多种智能体强化学习工作负载上的评估表明，Heddle 有效地消除了长尾瓶颈，与最先进的基准方案相比，端到端 rollout 吞吐量提高了多达 2.5 倍。

## Abstract
Agentic Reinforcement Learning (RL) enables LLMs to solve complex tasks by alternating between a data-collection rollout phase and a policy training phase. During rollout, the agent generates trajectories, i.e., multi-step interactions between LLMs and external tools. Yet, frequent tool calls induce long-tailed trajectory generation that bottlenecks rollouts. This stems from step-centric designs that ignore trajectory context, triggering three system problems for long-tail trajectory generation: queueing delays, interference overhead, and inflated per-token time. We propose Heddle, a trajectory-centric system to optimize the when, where, and how of agentic rollout execution. Heddle integrates three core mechanisms: trajectory-level scheduling using runtime prediction and progressive priority to minimize cumulative queueing; trajectory-aware placement via presorted dynamic programming and opportunistic migration during idle tool call intervals to minimize interference; and trajectory-adaptive resource manager that dynamically tunes model parallelism to accelerate the per-token time of long-tail trajectories while maintaining high throughput for short trajectories. Evaluations across diverse agentic RL workloads demonstrate that Heddle effectively neutralizes the long-tail bottleneck, achieving up to 2.5$\times$ higher end-to-end rollout throughput compared to state-of-the-art baselines.

---

## 论文详细总结（自动生成）

### 论文总结：Heddle - 智能体强化学习 Rollout 的分布式编排系统

#### 1. 核心问题与研究动机
*   **研究背景**：智能体强化学习（Agentic RL）通过 LLM 与外部工具的多步交互（轨迹生成）来解决复杂任务。Rollout（数据收集）阶段占据了整个训练时间的 80% 以上。
*   **核心问题**：现有的 RL 框架（如 Verl, Slime）采用“以步骤为中心”（Step-centric）的设计，将每一步交互视为独立请求。这种设计忽略了轨迹的整体上下文，导致**长尾轨迹（Stragglers）**成为瓶颈。
*   **瓶颈表现**：长尾轨迹引发了严重的排队延迟（Queueing delay）、计算干扰（Interference）以及单 token 生成时间过长，导致 GPU 资源在等待少数复杂轨迹完成时大量闲置。

#### 2. 核心方法论：以轨迹为中心的设计
Heddle 通过优化执行的**时机（When）**、**位置（Where）**和**方式（How）**来消除瓶颈：
*   **轨迹级调度（When）**：
    *   **渐进式预测**：使用轻量级模型（Qwen-0.6B）在每个步骤后动态预测剩余轨迹长度。
    *   **渐进优先级调度（PPS）**：根据预测长度动态提升长尾轨迹的优先级，并引入抢占机制，确保长尾任务优先执行，减少累积排队时间。
*   **轨迹感知放置（Where）**：
    *   **预排序动态规划**：基于“连续分区最优”引理，通过 DP 算法将长尾轨迹与短轨迹空间隔离，降低长尾任务的干扰系数。
    *   **机会性迁移**：在工具调用（Tool Call）的空闲间隔内，利用 GPU-Direct RDMA 异步迁移 KV Cache，修正预测偏差带来的负载不均，且不阻塞执行主路径。
*   **轨迹自适应资源管理（How）**：
    *   **异构并行配置**：打破同构限制，为长尾轨迹分配高模型并行度（MP）以降低延迟，为短轨迹分配低 MP 以维持高吞吐。
    *   **模拟退火算法**：快速搜索近乎最优的 GPU 资源分配方案。

#### 3. 实验设计
*   **应用场景**：
    1.  **代码智能体**：CodeForces 数据集，涉及代码执行和测试。
    2.  **搜索智能体**：HotpotQA 数据集，涉及多跳网络搜索。
    3.  **数学智能体**：DAPO-Math 数据集，涉及计算器和求解器调用。
*   **模型**：Qwen3-8B、14B、32B。
*   **基准方法（Benchmarks）**：
    *   **Slime**：基于最少负载的动态路由。
    *   **Verl**：基于缓存亲和性的静态放置。
    *   **Verl***：结合缓存亲和与负载均衡的混合策略。

#### 4. 资源与算力
*   **硬件环境**：8 台服务器，共 **64 块 NVIDIA Hopper GPU**（每台 8 块）。
*   **互联技术**：机内 900 GB/s NVLink，机间 400 Gb/s InfiniBand，支持 GPUDirect RDMA。
*   **内存**：每台服务器 1.8 TB 主存。
*   **软件栈**：PyTorch 2.8.0, CUDA 12.2, Ray 2.49.0。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种不同类型的任务负载、3 种模型参数规模。
*   **消融实验**：分别验证了调度器（PPS）、放置策略（DP + 迁移）和资源管理器（异构并行）的有效性。
*   **客观性**：对比了当前最先进的开源框架，并分析了系统开销（如预测延迟、迁移耗时），证明了优化带来的收益远大于系统开销。实验设计较为全面，覆盖了吞吐量、排队延迟、预测准确率等多个维度。

#### 6. 主要结论与发现
*   **性能提升**：Heddle 在不同工作负载下比 Verl 提升 1.4x–2.3x 吞吐量，比 Slime 提升 1.2x–2.5x。
*   **规模效应**：模型越大，计算干扰越严重，Heddle 的优化效果越显著。
*   **长尾消除**：通过 PPS 和异构资源分配，长尾轨迹的完成时间显著缩短，从而降低了整个 Batch 的 Makespan。

#### 7. 优点与亮点
*   **视角创新**：从传统的“步骤级”优化转向“轨迹级”编排，抓住了 Agentic RL 的本质特征。
*   **开销掩盖**：巧妙利用工具调用的“等待时间”进行预测和状态迁移，实现了近乎零成本的动态调整。
*   **数学支撑**：对放置问题进行了形式化建模，并证明了连续分区策略的最优性，使启发式算法更具理论依据。

#### 8. 不足与局限
*   **预测依赖**：系统的效率高度依赖于轨迹长度预测的准确性，对于极度随机或环境反馈不可预测的任务，收益可能受限。
*   **工具调用时长要求**：如果外部工具执行时间极短（如微秒级），则可能无法完全掩盖 KV Cache 迁移的开销。
*   **异构硬件假设**：实验主要在同构 GPU 集群上模拟异构并行配置，未深入探讨在实际物理异构（如 H100 与 L40 混部）环境下的表现。

（完）
