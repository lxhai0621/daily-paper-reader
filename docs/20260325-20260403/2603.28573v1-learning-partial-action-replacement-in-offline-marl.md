---
title: Learning Partial Action Replacement in Offline MARL
title_zh: 离线多智能体强化学习中的学习部分动作替换
authors: "Yue Jin, Giovanni Montana"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28573v1"
tags: ["query:agent"]
score: 9.0
evidence: 使用 PPO 和自适应策略的离线多智能体强化学习
tldr: 针对离线多智能体强化学习中联合动作空间爆炸导致的分布外（OOD）问题，本文提出了PLCQL框架。该框架将部分动作替换（PAR）子集选择建模为上下文老虎机问题，利用PPO算法学习状态相关的自适应策略，动态决定替换智能体的数量。相比现有方法，PLCQL显著降低了计算开销，并在多个基准测试中取得了领先的性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.28573v1/fig-001.webp\", \"caption\": \"Fig. 1. Illustration of the PLCQL algorithm.\", \"page\": 8, \"index\": 1, \"width\": 581, \"height\": 544}]"
motivation: 现有的部分动作替换方法在处理大规模智能体时计算成本极高，且无法根据环境状态动态调整替换策略。
method: 提出PLCQL框架，通过上下文老虎机模型和不确定性加权奖励学习状态相关的自适应PAR策略，将Q函数评估次数从n次降至1次。
result: "在MPE、MaMuJoCo和SMAC等基准测试中，PLCQL在66%的任务中获得最高分，并在84%的任务中优于之前的SPaCQL方法。"
conclusion: PLCQL通过自适应的动作替换机制，在保证离线学习稳定性的同时，大幅提升了多智能体强化学习的计算效率和性能表现。
---

## 摘要
离线多智能体强化学习 (MARL) 面临一个关键挑战：联合动作空间随智能体数量呈指数级增长，导致数据集覆盖范围呈指数级稀疏，且不可避免地会出现分布外 (OOD) 的联合动作。部分动作替换 (PAR) 通过将一部分智能体锚定在数据集动作上来缓解这一问题，但现有方法依赖于枚举多个子集配置，计算成本高昂，且无法适应不同的状态。我们提出了 PLCQL 框架，该框架将 PAR 子集选择建模为上下文老虎机 (contextual bandit) 问题，并使用带有不确定性加权奖励的近端策略优化 (PPO) 来学习状态相关的 PAR 策略。这种自适应策略在每个更新步骤中动态确定要替换的智能体数量，从而在策略改进与保守价值估计之间取得平衡。我们证明了一个价值误差界限，表明估计误差与偏离智能体的预期数量呈线性关系。与之前的基于 PAR 的方法 SPaCQL 相比，PLCQL 将每次迭代的 Q 函数评估次数从 n 减少到 1，显著提高了计算效率。实验表明，PLCQL 在 MPE、MaMuJoCo 和 SMAC 基准测试的 66% 任务中获得了最高的归一化分数，在 84% 的任务中优于 SPaCQL，同时大幅降低了计算成本。

## Abstract
Offline multi-agent reinforcement learning (MARL) faces a critical challenge: the joint action space grows exponentially with the number of agents, making dataset coverage exponentially sparse and out-of-distribution (OOD) joint actions unavoidable. Partial Action Replacement (PAR) mitigates this by anchoring a subset of agents to dataset actions, but existing approach relies on enumerating multiple subset configurations at high computational cost and cannot adapt to varying states. We introduce PLCQL, a framework that formulates PAR subset selection as a contextual bandit problem and learns a state-dependent PAR policy using Proximal Policy Optimisation with an uncertainty-weighted reward. This adaptive policy dynamically determines how many agents to replace at each update step, balancing policy improvement against conservative value estimation. We prove a value-error bound showing that the estimation error scales linearly with the expected number of deviating agents. Compared with the previous PAR-based method SPaCQL, PLCQL reduces the number of per-iteration Q-function evaluations from n to 1, significantly improving computational efficiency. Empirically, PLCQL achieves the highest normalised scores on 66% of tasks across MPE, MaMuJoCo, and SMAC benchmarks, outperforming SPaCQL on 84% of tasks while substantially reducing computational cost.

---

## 论文详细总结（自动生成）

### 离线多智能体强化学习中的学习部分动作替换 (PLCQL) 论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
离线多智能体强化学习（Offline MARL）面临的核心挑战是**联合动作空间的组合爆炸**。随着智能体数量增加，联合动作空间呈指数级增长，导致离线数据集的覆盖极其稀疏。即使每个智能体的个体动作都在数据集中，它们的特定组合也极有可能是“分布外”（OOD）的。这会导致价值函数产生严重的过估计，使学习到的策略失效。
现有的“部分动作替换”（PAR）方法（如 SPaCQL）通过仅替换一部分智能体的动作、而将其余智能体锚定在数据集动作上来缓解这一问题。然而，SPaCQL 依赖于枚举所有可能的子集大小，计算开销巨大，且无法根据当前环境状态动态调整替换策略。

#### 2. 方法论：核心思想与关键技术
论文提出了 **PLCQL (PAR Learning-based Conservative Q-Learning)** 框架，其核心思想是将 PAR 子集选择建模为一个**上下文老虎机（Contextual Bandit）**问题：
*   **核心思想**：学习一个状态相关的自适应策略 $\pi_{par}$，动态决定在当前状态下应该替换多少个智能体的动作（子集大小 $k$）。
*   **上下文老虎机模型**：
    *   **上下文**：当前的引导状态（Bootstrap State）$s'$。
    *   **动作**：要替换的智能体数量 $k \in \{1, \dots, n\}$。
    *   **奖励设计**：采用**不确定性加权奖励**。奖励由估计的联合 Q 值和基于 Q 网络集成（Ensemble）方差的不确定性惩罚项组成。这鼓励策略选择既能带来高价值又能保证估计可靠的 $k$ 值。
*   **算法流程**：
    1.  使用 **PPO (Proximal Policy Optimization)** 算法训练 PAR 策略 $\pi_{par}$。
    2.  在 Q 函数更新时，根据 $\pi_{par}$ 采样一个 $k$，并随机选择 $k$ 个智能体进行动作替换。
    3.  结合保守 Q 学习（CQL）的惩罚项，利用自适应的 Bellman 算子更新价值函数。
*   **理论保证**：论文证明了一个价值误差界限，显示估计误差与 PAR 策略诱导的预期偏离智能体数量呈线性相关。

#### 3. 实验设计
*   **基准测试 (Benchmarks)**：
    *   **MPE**：包括协作导航 (CN)、捕食者-猎物 (PP) 和 World 任务。
    *   **MaMuJoCo**：Half-Cheetah 任务（连续动作）。
    *   **SMAC (StarCraft II)**：包括 2s3z, 3s_vs_5z, 5m_vs_6m, 6h_vs_8z 等不同难度的地图（离散动作）。
*   **数据集质量**：涵盖 Expert（专家）、Medium（中等）、Medium-Replay（中等回放）、Random（随机）和 Mixed（混合）等多种类型。
*   **对比方法**：
    *   策略约束类：OMAR, IQL, MA-TD3+BC, AWAC, MAICQ。
    *   价值约束类：MACQL, CFCQL, SPaCQL。
    *   扩散模型类：DoF。

#### 4. 资源与算力
*   **硬件**：实验使用了 **NVIDIA Tesla V100 GPU**。
*   **模型细节**：使用 10 个 Q 网络组成的集成模型；PAR 策略网络为 2 层隐藏层（每层 64 单元）的 MLP。
*   **计算效率提升**：论文明确指出，PLCQL 将每次迭代的 Q 函数评估次数从 $n$ 次（SPaCQL 的做法）减少到 **1 次**，显著降低了墙钟训练时间。

#### 5. 实验数量与充分性
*   **实验规模**：在 3 个基准测试的共 32 个任务/数据集组合上进行了测试。
*   **重复实验**：所有实验均使用 **5 个随机种子**运行，并报告了均值和标准差。
*   **消融实验**：对比了自适应学习与固定 $k$ 值（$k=1$ 或 $k=n$）的效果，以及不确定性加权奖励的必要性。
*   **充分性评价**：实验覆盖了连续和离散动作空间、不同规模的智能体群体以及极具代表性的离线数据集，对比基线涵盖了近年来的主流算法，实验设计客观且充分。

#### 6. 主要结论与发现
*   **性能领先**：PLCQL 在 66% 的任务中达到了 SOTA 性能，并在 84% 的任务中优于其前身 SPaCQL。
*   **效率卓越**：在保持或提升性能的同时，大幅降低了 PAR 类方法的计算开销。
*   **自适应性**：实验证明，没有一个固定的 $k$ 值能在所有任务中表现最好，而 PLCQL 学习到的状态相关策略能有效平衡保守性与协调性。

#### 7. 优点
*   **高效性**：通过上下文老虎机简化了复杂的子集枚举问题，极大地提升了训练速度。
*   **自适应性**：首次在离线 MARL 中实现了 PAR 策略的动态调整，能够根据状态不确定性自动调节保守程度。
*   **理论与实践结合**：提供了严谨的价值误差界限证明，并在大规模基准测试中验证了有效性。

#### 8. 不足与局限
*   **子集采样方式**：虽然 $k$ 是自适应学习的，但在确定 $k$ 后，具体的智能体子集仍是**均匀随机采样**的，未考虑智能体之间的特定依赖关系。
*   **集成模型开销**：虽然减少了 Q 评估次数，但算法仍依赖于包含 10 个 Q 网络的集成模型，在智能体数量极多或内存受限的场景下可能存在扩展性挑战。
*   **上下文定义**：目前仅使用状态作为上下文，未来或可引入更多历史信息或智能体间的通信特征。

（完）
