---
title: Optimizing RAG Rerankers with LLM Feedback via Reinforcement Learning
title_zh: 通过强化学习利用大语言模型反馈优化 RAG 重排序器
authors: "Yuhang Wu, Xiangqing Shen, Fanfan Wang, Cangqi Zhou, Zhen Wu, Xinyu Dai, Rui Xia"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02091v1"
tags: ["query:ma-kf"]
score: 10.0
evidence: 使用强化学习优化 RAG 重排序器以提高相关性
tldr: 针对检索增强生成（RAG）中重排序器与下游生成任务失配的问题，本文提出 RRPO 框架。该框架将重排序视为序列决策过程，利用大模型的生成质量作为反馈信号，通过强化学习直接优化文档的实际效用，无需昂贵的人工标注。实验表明，RRPO 在多个基准测试中显著超越了 RankZephyr 等强基准，并展现出极佳的泛化性与鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.02091v1/fig-001.webp\", \"caption\": \"Table 1: Main experimental results on the HotpotQA and AmbigNQ datasets. * adapts from previous work DeepRAG (Guan et al., 2025).\", \"page\": 6, \"index\": 1, \"width\": 469, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2604.02091v1/fig-002.webp\", \"caption\": \"Table 9: Performance of RRPO with smaller LLM Readers.\", \"page\": 15, \"index\": 2, \"width\": 461, \"height\": 292}, {\"url\": \"assets/figures/arxiv/2604.02091v1/fig-003.webp\", \"caption\": \"Figure 1: Comparison of previous methods with our proposed RRPO framework.\", \"page\": 2, \"index\": 3, \"width\": 946, \"height\": 438}, {\"url\": \"assets/figures/arxiv/2604.02091v1/fig-004.webp\", \"caption\": \"Table 6: Generalization results on different prompt templates on HotpotQA.\", \"page\": 14, \"index\": 4, \"width\": 461, \"height\": 293}]"
motivation: 传统的重排序模型依赖静态人工标注，导致检索出的相关文档往往无法满足下游大模型生成准确答案的实际需求。
method: 提出 RRPO 强化学习框架，通过大模型反馈将重排序与生成质量对齐，并引入参考锚定确定性基准以确保训练稳定性。
result: 在知识密集型任务中显著优于 RankZephyr，且能有效适配 GPT-4o 等不同模型并与查询扩展模块正交集成。
conclusion: RRPO 证明了通过强化学习将检索环节与生成环节直接对齐，可以显著提升 RAG 系统的整体效能和鲁棒性。
---

## 摘要
重排序器在优化检索增强生成（RAG）的检索结果中起着至关重要的作用。然而，目前的重排序模型通常是在静态的人工标注相关性标签上独立优化的，与下游的生成过程脱节。这种孤立导致了根本性的不匹配：被信息检索指标识别为主题相关的文档，往往无法提供大语言模型（LLM）生成精确答案所需的实际效用。为了弥补这一差距，我们引入了重排序偏好优化（RRPO），这是一种通过强化学习直接将重排序与 LLM 的生成质量对齐的框架。通过将重排序建模为一个序列决策过程，RRPO 利用 LLM 反馈来优化上下文效用，从而消除了对昂贵人工标注的需求。为了确保训练稳定性，我们进一步引入了一种参考锚定的确定性基准。在知识密集型基准测试上的广泛实验表明，RRPO 显著优于强基线模型，包括强大的列表式重排序器 RankZephyr。进一步的分析强调了我们框架的通用性：它可以无缝泛化到不同的阅读器（如 GPT-4o），与 Query2Doc 等查询扩展模块正交集成，并且在面对噪声监督信号训练时仍保持鲁棒性。

## Abstract
Rerankers play a pivotal role in refining retrieval results for Retrieval-Augmented Generation. However, current reranking models are typically optimized on static human annotated relevance labels in isolation, decoupled from the downstream generation process. This isolation leads to a fundamental misalignment: documents identified as topically relevant by information retrieval metrics often fail to provide the actual utility required by the LLM for precise answer generation. To bridge this gap, we introduce ReRanking Preference Optimization (RRPO), a reinforcement learning framework that directly aligns reranking with the LLM's generation quality. By formulating reranking as a sequential decision-making process, RRPO optimizes for context utility using LLM feedback, thereby eliminating the need for expensive human annotations. To ensure training stability, we further introduce a reference-anchored deterministic baseline. Extensive experiments on knowledge-intensive benchmarks demonstrate that RRPO significantly outperforms strong baselines, including the powerful list-wise reranker RankZephyr. Further analysis highlights the versatility of our framework: it generalizes seamlessly to diverse readers (e.g., GPT-4o), integrates orthogonally with query expansion modules like Query2Doc, and remains robust even when trained with noisy supervisors.

---

## 论文详细总结（自动生成）

以下是对论文《Optimizing RAG Rerankers with LLM Feedback via Reinforcement Learning》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：检索增强生成（RAG）中重排序器（Reranker）与下游生成器（LLM Reader）之间的**目标失配（Misalignment）**。
*   **研究背景**：传统的重排序器通常基于静态的人工标注（如 NDCG 等信息检索指标）进行独立优化。然而，检索指标认为“相关”的文档，未必是 LLM 生成准确答案所需的“高价值”文档。
*   **研究动机**：为了消除这种隔阂，作者提出直接利用下游 LLM 的生成质量作为反馈信号，通过强化学习（RL）优化重排序器，使其从关注“主题相关性”转向关注“模型效用性”。

### 2. 方法论：RRPO 框架
*   **核心思想**：将重排序过程建模为一个**序列决策过程（MDP）**，利用冻结的下游 LLM 作为奖励模型，通过强化学习直接优化重排序策略。
*   **关键技术细节**：
    *   **MDP 建模**：
        *   **状态 (State)**：当前尚未被选中的候选文档集合。
        *   **动作 (Action)**：从剩余集合中选择一个文档。
        *   **奖励 (Reward)**：将选定的文档序列输入 LLM Reader，根据生成答案的质量（EM、F1、Hit 等指标）计算奖励。
    *   **策略网络**：基于点对点（Pointwise）重排序模型（如 GTE），通过 Softmax 转化为概率分布进行采样。
    *   **参考锚定确定性基准 (Reference-anchored Deterministic Baseline)**：这是本文解决 RL 训练不稳定的核心创新。由于 LLM 奖励高度离散且方差大，训练传统的 Critic 网络（判别器）效果不佳。RRPO 使用参考模型（未优化的原始模型）的贪婪搜索结果作为基准值 $V(s)$，从而稳定优势函数（Advantage Function）的计算。
    *   **优化目标**：采用类似 PPO 的剪切（Clip）机制和 KL 散度惩罚，确保策略更新平滑。

### 3. 实验设计
*   **数据集**：
    *   **知识密集型 QA**：HotpotQA（多跳推理）、AmbigNQ（歧义消除）。
    *   **泛化性测试**：2WikiMultiHopQA、MusiQue（更复杂的多跳任务）。
*   **Benchmark 与对比方法**：
    *   **基础重排序器**：GTE、BGE、Jina-v2、Qwen3-reranker。
    *   **强力基准**：RankZephyr（强大的列表式 LLM 重排序器）。
    *   **RAG 策略**：FLARE、DRAGIN（动态检索方法）。
*   **实验设置**：使用 BM25 检索前 100 个文档，重排序后取 Top-k（k=3 或 5）输入 Qwen2.5-7B 进行生成。

### 4. 资源与算力
*   **算力设备**：
    *   **训练**：使用 4 张 **NVIDIA RTX 3090** GPU 训练重排序模型。
    *   **推理**：使用 2 张 **NVIDIA A6000** GPU 部署 LLM Reader（利用 vLLM 加速）。
*   **训练细节**：在 HotpotQA 上训练 2 个 Epoch，在 AmbigNQ 上训练 4 个 Epoch。采用了混合精度训练（AMP）和 Flash Attention。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的实验，包括主实验（多个数据集）、消融实验（不同训练步数 $k$）、泛化性实验（不同 Reader、不同 Prompt 模板）、鲁棒性实验（噪声监督）。
*   **充分性与公平性**：
    *   **充分性**：涵盖了从开源模型到闭源模型（GPT-4o, Claude-3.5）的广泛测试，证明了方法的普适性。
    *   **公平性**：在对比实验中严格控制了变量（如相同的候选文档池、相同的 Reader 配置），并进行了统计显著性检验（p < 0.01），确保结果客观可靠。

### 6. 主要结论与发现
*   **性能提升**：RRPO 显著优于所有监督学习的重排序基准，甚至超过了参数量更大的列表式重排序器 RankZephyr。
*   **效用优先**：实验证明，优化“上下文效用”比单纯优化“语义相关性”对 RAG 系统更有利。
*   **跨模型泛化**：在一个 Reader（如 Qwen）上训练的重排序器，可以直接提升其他 Reader（如 GPT-4o）的性能，说明它学到了通用的文档价值判断准则。
*   **正交性**：RRPO 可以与 Query2Doc 等查询扩展技术结合，产生叠加增益。

### 7. 优点与亮点
*   **无需人工标注**：完全依赖 LLM 反馈，解决了 RAG 场景下高质量排序标签缺失的问题。
*   **训练稳定性**：通过“参考锚定基准”巧妙避开了训练 Critic 网络的难题，降低了 RL 的门槛。
*   **即插即用**：作为一个独立的重排序模块，它可以轻松集成到现有的任何 RAG 工作流中。
*   **高效性**：通过响应缓存和确定性生成回放，减少了 50% 的 LLM 推理开销。

### 8. 不足与局限
*   **检索上限限制**：RRPO 属于重排序阶段，其性能上限受限于初始检索器（Retriever）的召回率。如果前 100 个文档中没有答案，RRPO 也无法发挥作用。
*   **计算开销**：尽管有优化，但 RL 训练过程仍涉及大量的 LLM 推理调用，相比传统的监督学习，训练成本依然较高。
*   **任务依赖**：奖励函数（EM/F1）主要针对 QA 任务设计，对于长文本生成或摘要等任务，可能需要更复杂的奖励设计。

（完）
