---
title: "Visual Attention Drifts,but Anchors Hold:Mitigating Hallucination in Multimodal Large Language Models via Cross-Layer Visual Anchors"
title_zh: 视觉注意力会漂移，但锚点依然稳固：通过跨层视觉锚点缓解多模态大语言模型中的幻觉
authors: "Chengxu Yang, Jingling Yuan, Chuang Hu, Jiawei Jiang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25088v1"
tags: ["query:mmkqa"]
score: 9.0
evidence: 缓解多模态大语言模型中的幻觉
tldr: 针对多模态大语言模型中的物体幻觉问题，本文研究发现幻觉源于深层注意力向早期视觉噪声的回归。为此提出了CLVA（跨层视觉锚点），这是一种无需训练的方法，通过强化中间层的关键视觉锚点并抑制深层回归噪声，有效纠正注意力偏移。实验证明，该方法在多种架构和基准测试中均能显著减少幻觉，且不增加计算负担。
selection_source: fresh_fetch
motivation: 现有研究缺乏对模型后期注意力偏移的解释，且深层注意力往往会退化回初始视觉噪声导致幻觉产生。
method: 提出无需训练的CLVA方法，利用中间层捕获的视觉锚点来强化关键特征并抑制深层的回归噪声。
result: 在多种模型架构和基准测试上均取得了优异表现，且几乎不增加额外的计算时间和显存消耗。
conclusion: 通过跨层视觉锚点纠正深层注意力偏移是缓解多模态模型幻觉的一种高效、通用且具有可解释性的方案。
---

## 摘要
多模态大语言模型（MLLMs）经常受到物体幻觉的困扰。虽然现有研究利用注意力增强和视觉回溯，但我们发现这些工作在模型最终阶段的注意力漂移方面缺乏足够的解释性。在本文中，我们研究了视觉特征的逐层演变，并发现幻觉源于深层注意力向早期层的初始视觉噪声回归。我们观察到，输出的可靠性取决于在中间层而非最终层获取视觉锚点。基于这些见解，我们提出了 CLVA（Cross-Layer Visual Anchors，跨层视觉锚点），这是一种无需训练的方法，它在抑制回归噪声的同时增强了关键的中间层特征。该方法通过利用从注意力动态中捕获的关键锚点，有效地将深层注意力拉回到正确的视觉区域。我们在多种架构和基准测试上评估了我们的方法，证明了其在不显著增加计算时间和 GPU 显存的情况下具有出色的性能。

## Abstract
Multimodal Large Language Models often suffer from object hallucination. While existing research utilizes attention enhancement and visual retracing, we find these works lack sufficient interpretability regarding attention drift in final model stages. In this paper, we investigate the layer wise evolution of visual features and discover that hallucination stems from deep layer attention regressing toward initial visual noise from early layers. We observe that output reliability depends on acquiring visual anchors at intermediate layers rather than final layers. Based on these insights, we propose CLVA, which stands for Cross-Layer Visual Anchors, a training free method that reinforces critical mid layer features while suppressing regressive noise. This approach effectively pulls deep layer attention back to correct visual regions by utilizing essential anchors captured from attention dynamics. We evaluate our method across diverse architectures and benchmarks, demonstrating outstanding performance without significant increase in computational time and GPU memory.

---

## 论文详细总结（自动生成）

以下是对论文《Visual Attention Drifts, but Anchors Hold: Mitigating Hallucination in Multimodal Large Language Models via Cross-Layer Visual Anchors》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：多模态大语言模型（MLLM）在生成过程中经常出现“物体幻觉”（即生成与图像内容不符的描述）。
*   **研究动机**：
    *   现有缓解幻觉的方法（如对比解码、注意力修改）虽然有效，但缺乏对模型深层注意力演变的解释性。
    *   作者通过可视化发现，模型在中间层其实能精准定位目标（视觉锚点），但在进入深层后，注意力会发生“漂移”，退化回早期层的初始视觉噪声或语言偏见。
    *   研究旨在利用模型内部已有的“正确知识”（中间层锚点）来纠正深层的注意力偏差。

### 2. 方法论：CLVA（跨层视觉锚点）
*   **核心思想**：这是一种**无需训练（Training-free）**的即插即用方法。通过提取中间层的“正向锚点”和早期层的“负向锚点”，在深层推理时对注意力矩阵进行动态修正。
*   **关键技术细节**：
    1.  **多头分类**：根据对视觉标记（Tokens）的注意力强度，将注意力头分为“视觉敏感头”和“视觉不敏感头”。
    2.  **双锚点提取**：
        *   **正向视觉锚点 ($M_{pos}$)**：从中间层的敏感头中提取，代表模型识别出的关键语义区域。
        *   **负向视觉锚点 ($M_{neg}$)**：从早期层的不敏感头中提取，代表初始的背景噪声或空间偏见。
    3.  **显著性掩码**：利用 Z-score 统计方法识别出锚点中的显著区域，生成二值掩码 $Z_{pos}$ 和 $Z_{neg}$。
    4.  **注意力重锚定（公式）**：在深层的解码过程中，修改文本对图像的注意力权重：
        $$\tilde{A}(i, j) = A(i, j) \cdot (1 + \alpha Z_{pos}(j) - \beta Z_{neg}(j))$$
        其中 $\alpha$ 为增强因子，$\beta$ 为抑制因子。最后进行归一化以保持概率分布。

### 3. 实验设计
*   **评估模型**：LLaVA-1.5-7B、Qwen-VL-7B、InstructBLIP-7B。
*   **数据集与基准测试（Benchmarks）**：
    *   **POPE**：评估物体存在性的二分类任务（包含随机、热门、对抗三种采样策略）。
    *   **CHAIR**：评估图像描述任务中的物体幻觉比例（指标为 $CHAIR_i$ 和 $CHAIR_s$）。
    *   **MME**：综合评估模型的感知和认知能力。
    *   **LLaVA-Bench**：评估复杂开放式场景下的表现。
*   **对比方法**：IMCCD（对比解码）、MemVR（视觉回溯）、ClearSight（视觉增强）。

### 4. 资源与算力
*   **硬件环境**：实验在一台 **NVIDIA H20 GPU** 上完成。
*   **算力开销**：
    *   由于是无需训练的方法，CLVA 仅在推理阶段增加极小的计算量。
    *   **效率优势**：相比于 IMCCD 等需要双倍前向传播的方法，CLVA 的推理速度快了近一倍，显存占用仅为对方的一半左右（例如在 LLaVA-1.5 上，CLVA 耗时 7分54秒，而 IMCCD 需 16分38秒）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了三种主流的 MLLM 架构（线性投影、Q-Former 等）。
    *   在 POPE 的 9 个子集、CHAIR 的 500 张图像以及 MME 的 14 个子任务上进行了全面测试。
    *   **消融实验**：验证了正向锚点和负向锚点各自的必要性，并探讨了参数 $\alpha, \beta$ 的敏感度。
    *   **多样性测试**：包括了 Greedy Search、Beam Search 和 Nucleus Sampling 等不同解码策略。
*   **客观性**：实验对比了多种 SOTA 方法，并提供了详细的推理速度和显存对比数据，结果较为客观、公平。

### 6. 主要结论与发现
*   **注意力漂移规律**：模型在中间层（如第 14-16 层）的视觉定位最准，随后注意力会向第 1 层的噪声分布回归。
*   **CLVA 的有效性**：通过强化中间层特征并抑制早期噪声，CLVA 在 POPE 上的 F1 分数提升了 0.28%~6.33%，在 CHAIR 上显著降低了幻觉率。
*   **通用性**：该方法不仅适用于简单的线性对齐模型，对带有 Q-Former 结构的复杂模型同样有效。

### 7. 优点
*   **高效且轻量**：无需重新训练或微调，推理延迟增加极低（约 1%~2%），显存友好。
*   **强解释性**：基于对模型内部层级特征演变的深入观察，而非黑盒式的启发式修改。
*   **即插即用**：能够广泛适配不同架构的 MLLM，且在长文本生成中表现稳健。

### 8. 不足与局限
*   **超参数依赖**：增强因子 $\alpha$ 和抑制因子 $\beta$ 需要针对不同模型进行微调，若 $\alpha$ 过大会破坏语义理解。
*   **特定任务退化**：在 MME 的数值计算和文本翻译子任务中，性能略有下降，这可能是因为这些任务更依赖语言解码而非视觉锚定。
*   **锚点层选择**：虽然论文给出了经验值（如 $L/2 - 2$），但对于不同深度的模型，最优锚点层的位置可能仍需手动探索。

（完）
