---
title: "Environment Maps: Structured Environmental Representations for Long-Horizon Agents"
title_zh: 环境地图：面向长程智能体的结构化环境表示
authors: "Yenchia Feng, Chirag Sharma, Karime Maamari"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23610v3"
tags: ["query:ma-kf"]
score: 9.0
evidence: 将异构证据整合到结构化图中
tldr: 针对大语言模型在处理长程复杂软件工作流时易出错且缺乏持久环境认知的问题，本文提出了“环境地图（Environment Maps）”。这是一种结构化的图表示方法，通过整合屏幕录制和执行轨迹，将环境抽象为上下文、动作、工作流和默会知识。在 WebArena 基准测试中，该方法显著提升了智能体的成功率，为长程规划提供了可解释且可编辑的持久化基础。
selection_source: fresh_fetch
motivation: 现有的智能体在动态软件界面中常因缺乏持久的环境表征而导致级联错误和任务失败。
method: 提出一种由上下文、动作、工作流和默会知识组成的结构化图表示法，将异构环境证据转化为持久的智能体无关表征。
result: "在 WebArena 测试中，使用环境地图的智能体成功率达到 28.2%，远高于仅限会话上下文的基准（14.2%）和原始轨迹数据（23.3%）。"
conclusion: 环境地图为长程规划提供了结构化、可解释且可增量改进的接口，有效增强了智能体在复杂环境中的鲁棒性。
---

## 摘要
尽管大语言模型（LLMs）发展迅速，但复杂软件工作流的鲁棒自动化仍是一个悬而未决的问题。在长程（long-horizon）场景中，智能体经常受到级联错误和环境随机性的困扰；动态界面中的一次失误就可能导致任务失败，进而引发幻觉或陷入反复试错。本文介绍了“环境地图”（Environment Maps）：一种持久的、与智能体无关的表示方式，通过将屏幕录像和执行轨迹等异构证据整合到结构化图中，来缓解这些失败。该表示由四个核心组件组成：(1) 上下文（抽象位置），(2) 动作（参数化可供性），(3) 工作流（观测到的轨迹），以及 (4) 隐性知识（领域定义和可重用程序）。我们在 WebArena 基准测试的五个领域中评估了该框架。配备环境地图的智能体实现了 28.2% 的成功率，几乎是受限于会话上下文的基线模型（14.2%）的两倍，且优于能够访问用于生成环境地图的原始轨迹数据的智能体（23.3%）。通过在模型和环境之间提供结构化接口，环境地图为长程规划奠定了持久的基础，且具有可解释性、可编辑性和增量完善性。

## Abstract
Although large language models (LLMs) have advanced rapidly, robust automation of complex software workflows remains an open problem. In long-horizon settings, agents frequently suffer from cascading errors and environmental stochasticity; a single misstep in a dynamic interface can lead to task failure, resulting in hallucinations or trial-and-error. This paper introduces $\textit{Environment Maps}$: a persistent, agent-agnostic representation that mitigates these failures by consolidating heterogeneous evidence, such as screen recordings and execution traces, into a structured graph. The representation consists of four core components: (1) Contexts (abstracted locations), (2) Actions (parameterized affordances), (3) Workflows (observed trajectories), and (4) Tacit Knowledge (domain definitions and reusable procedures). We evaluate this framework on the WebArena benchmark across five domains. Agents equipped with environment maps achieve a 28.2% success rate, nearly doubling the performance of baselines limited to session-bound context (14.2%) and outperforming agents that have access to the raw trajectory data used to generate the environment maps (23.3%). By providing a structured interface between the model and the environment, Environment Maps establish a persistent foundation for long-horizon planning that is human-interpretable, editable, and incrementally refinable.

---

## 论文详细总结（自动生成）

这是一份关于论文《Environment Maps: Structured Environmental Representations for Long-Horizon Agents》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在长程（Long-horizon）任务中，自动驾驶智能体（Agents）在复杂软件界面中常面临**级联错误**：一旦在动态 UI 中走错一步，往往会导致任务彻底失败、产生幻觉或陷入无意义的试错。
*   **核心痛点**：现有的智能体缺乏对环境的持久化、结构化认知。它们通常只依赖当前页面的观察，不清楚“我在哪”、“哪些动作有效”以及“动作的长期后果是什么”。
*   **研究目标**：提出一种名为“环境地图（Environment Maps）”的持久化表示法，将零散的屏幕录像、执行轨迹和文档整合为结构化的图，为智能体提供一个可查询、可解释且与具体模型无关的知识库。

### 2. 方法论：核心思想与技术细节
论文定义环境地图为一个四元组 $M = (C, A, W, K)$，其构建流程分为五个阶段：
*   **核心组件**：
    1.  **Contexts (C)**：抽象上下文。通过 URL 正则化（如将 `/users/123` 抽象为 `/users/{id}`）识别不同的页面或视图。
    2.  **Actions (A)**：参数化动作。将具体操作（如“点击 John”）泛化为模板（如“点击 {customer_name}”），并记录观察到的参数值。
    3.  **Workflows (W)**：观测到的工作流。记录从起始到目标的步骤序列。
    4.  **Tacit Knowledge (K)**：隐性知识。包括领域定义（如“MR”代表“Merge Request”）和操作规程（如“如何过滤问题”）。
*   **构建流水线**：
    *   **阶段 1-2**：解析原始轨迹（DOM 快照、事件流），提取动作名称、描述和结果。
    *   **阶段 3**：利用 LLM 进行模式检测，将原始动作抽象为参数化模板。
    *   **阶段 4**：基于 URL 模式聚类生成上下文，并从轨迹中提取领域知识。
    *   **阶段 5**：确定性合并，去重并保留溯源链接（Provenance）。

### 3. 实验设计
*   **数据集与场景**：使用 **WebArena** 基准测试，涵盖 5 个真实 Web 环境：E-Commerce（电商）、CMS（后台管理）、GitLab（代码托管）、Reddit（论坛）和 Map（地图）。
*   **评估标准**：采用 **WebArena Verified** 框架，通过 HAR 追踪检查和类型感知分类进行更严格、可重复的评估。
*   **对比方法**：
    1.  **Baseline (No Map)**：仅提供当前页面观察（可访问性树）和任务意图。
    2.  **Trajectory Access**：允许智能体通过 CLI 工具（如 Read, Grep）访问原始的人类操作轨迹文件（JSONL 格式）。
    3.  **Environment Map Access (本文方法)**：智能体可访问结构化的环境地图文件。
*   **智能体配置**：统一使用 `claude-3.5-sonnet` 作为底层模型。

### 4. 资源与算力
*   **算力说明**：论文未明确提及训练所需的 GPU 型号或数量，因为该方法主要基于预训练的 LLM（Claude API）进行推理和地图构建，而非从头训练模型。
*   **构建成本**：论文给出了构建地图的经济成本。为 5 个环境构建地图的成本约为 **$1 到 $4**，每个环境耗时约 **13 到 31 分钟**。这相比于动辄数百 GPU 小时的微调（SFT）具有极高的成本优势。

### 5. 实验数量与充分性
*   **实验规模**：在 812 个自然语言任务上进行了测试，涵盖了所有难度级别。
*   **充分性分析**：
    *   实验对比了“原始数据”与“结构化数据”的差异，证明了**表示形式（Representation）**本身的重要性。
    *   进行了**泛化性分析**：将任务分为“有轨迹覆盖”和“无轨迹覆盖”两组，证明了即使在没有直接演示的任务中，环境地图依然能通过提供 UI 结构知识提升性能。
    *   实验设计客观，通过固定底层模型栈，排除了模型能力差异对结果的影响。

### 6. 主要结论与发现
*   **性能翻倍**：配备环境地图的智能体成功率达到 **28.2%**，远高于基线（14.2%）和原始轨迹访问（23.3%）。
*   **复杂环境增益更大**：在 UI 密集、分支复杂的环境（如 GitLab 和 CMS）中，环境地图的优势最明显。
*   **行为改变**：使用地图的智能体表现出更精准的工具调用行为（平均每任务 12.1 次工具调用，多为查询地图），且回溯（Backtracking）率显著降低。
*   **知识优于数据**：将原始轨迹转化为结构化地图，比直接给智能体看原始轨迹更有助于任务解决。

### 7. 优点与亮点
*   **智能体无关性**：地图是持久化的 JSON 格式，不绑定特定模型，可随环境变化增量更新。
*   **人类可读与可编辑**：地图结构清晰，人类专家可以轻松修正其中的错误或添加缺失的领域知识。
*   **低成本高效率**：无需昂贵的模型微调，通过结构化现有数据即可显著提升长程规划能力。
*   **鲁棒性**：通过参数化动作和 URL 抽象，缓解了 UI 微小变动带来的影响。

### 8. 不足与局限
*   **跨站点任务瓶颈**：目前的方法在处理跨多个不同站点的复杂工作流时表现不佳（成功率接近 0）。
*   **依赖 URL 模式**：上下文识别高度依赖 URL 结构，对于单页应用（SPA）或 URL 不随状态变化的复杂应用可能失效。
*   **简单环境冗余**：在导航深度较浅的简单环境（如 Reddit）中，结构化地图带来的增益与原始轨迹相比并不显著。
*   **时效性维护**：虽然支持增量更新，但当 Web 环境发生重大重构时，如何自动检测并修复地图中的陈旧节点仍是挑战。

（完）
