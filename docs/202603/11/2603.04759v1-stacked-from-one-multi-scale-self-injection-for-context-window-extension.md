---
title: "Stacked from One: Multi-Scale Self-Injection for Context Window Extension"
title_zh: 始于一的堆叠：用于上下文窗口扩展的多尺度自注入
authors: "Wei Han, Pan Zhou, Shuicheng Yan"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04759v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 用于上下文窗口扩展和压缩的多尺度自注入技术
tldr: 针对大语言模型长文本处理成本高的问题，本文提出 SharedLLM 框架。该方法通过“自注入”机制，将同一个短文本 LLM 堆叠为压缩器和解码器，利用多尺度上下文压缩和树状数据结构实现高效的信息检索。在仅使用 8K 长度数据训练的情况下，模型能扩展至 128K 以上，在保持高性能的同时显著降低了内存占用并提升了推理速度。
selection_source: fresh_fetch
motivation: 现有的长文本扩展方法依赖昂贵的持续预训练，且推理效率和内存开销巨大。
method: 提出 SharedLLM 框架，通过自注入机制将同源模型层堆叠为压缩器和解码器，并结合树状结构进行多尺度上下文压缩与查询感知检索。
result: 在仅 8K 训练长度下实现了 128K 以上的上下文扩展，性能优于强基线，且推理速度提升 2 至 3 倍。
conclusion: 该研究证明了通过多尺度自注入和高效压缩机制，可以在极低成本下实现大模型长文本处理能力的显著提升。
---

## 摘要
当代大语言模型（LLMs）有限的上下文窗口仍然是其在不同领域广泛应用的主要瓶颈。虽然在长上下文数据上进行持续预训练提供了一种直接的解决方案，但它会产生高昂的数据获取和计算成本。为了应对这一挑战，我们提出了 SharedLLM，这是一个基于多粒度上下文压缩和查询感知信息获取的新型框架。SharedLLM 由两个堆叠的短上下文 LLM 组成：一个作为压缩器的底层模型和一个作为解码器的顶层模型。底层模型将长输入压缩为紧凑的多粒度表示，然后将其转发给顶层模型进行上下文感知处理。为了最大化效率，这种信息传递仅发生在最底层，从而绕过了冗长的前向传递和冗余的交叉注意力操作。整个过程（其中顶层和底层模型源自相同的底层 LLM 层）被称为“自注入”（self-injection）。为了支持这种架构，一种专门的基于树的数据结构实现了上下文信息的有效编码和查询感知检索。尽管仅在 8K token 的序列上进行训练，SharedLLM 仍能有效地泛化到超过 128K token 的输入。在一系列全面的长上下文建模和理解基准测试中，SharedLLM 取得了优于或相当强基准模型的性能，在效率和准确性之间达到了最佳平衡。此外，这些设计选择使 SharedLLM 能够显著减少内存占用，并带来显著的推理加速（比流式架构快 2 倍，比编码器-解码器架构快 3 倍）。

## Abstract
The limited context window of contemporary large language models (LLMs) remains a primary bottleneck for their broader application across diverse domains. Although continual pre-training on long-context data offers a straightforward solution, it incurs prohibitive data acquisition and computational costs. To address this challenge, we propose~\modelname, a novel framework based on multi-grained context compression and query-aware information acquisition. SharedLLM comprises two stacked short-context LLMs: a lower model serving as a compressor and an upper model acting as a decoder. The lower model compresses long inputs into compact, multi-grained representations, which are then forwarded to the upper model for context-aware processing. To maximize efficiency, this information transfer occurs exclusively at the lowest layers, bypassing lengthy forward passes and redundant cross-attention operations. This entire process, wherein the upper and lower models are derived from the same underlying LLM layers, is termed~\textit{self-injection}. To support this architecture, a specialized tree-based data structure enables the efficient encoding and query-aware retrieval of contextual information. Despite being trained on sequences of only 8K tokens, \modelname~effectively generalizes to inputs exceeding 128K tokens. Across a comprehensive suite of long-context modeling and understanding benchmarks, \modelname~achieves performance superior or comparable to strong baselines, striking an optimal balance between efficiency and accuracy. Furthermore, these design choices allow \modelname~to substantially reduce the memory footprint and yield notable inference speedups ($2\times$ over streaming and $3\times$ over encoder-decoder architectures).

---

## 论文详细总结（自动生成）

### 论文总结：Stacked from One (SharedLLM) —— 多尺度自注入实现上下文窗口扩展

#### 1. 核心问题与整体含义（研究动机和背景）
当代大语言模型（LLMs）在处理超长文本时面临“计算瓶颈”与“训练成本”的双重挑战。虽然通过长文本持续预训练可以扩展上下文，但数据获取难且算力开销巨大；而现有的插值法或流式处理方法往往在推理速度、内存占用或长程依赖保持上存在短板。
本文提出了 **SharedLLM** 框架，旨在通过**自注入（Self-Injection）**机制，利用同一个短上下文模型的权重构建层次化架构，在极低训练成本（仅需 8K 长度数据）下，实现模型对 128K 以上超长上下文的高效处理。

#### 2. 方法论：核心思想与关键技术
SharedLLM 的核心是将一个预训练好的短上下文 LLM 拆分为“底层压缩器”和“顶层解码器”：
*   **自注入架构（Self-Injection）**：底层模型（Lower Model）由目标 LLM 的前 $M$ 层组成，负责将长文本块压缩为 KV 状态；顶层模型（Upper Model）则是完整的 LLM，通过交叉注意力（Cross-Attention）融合这些压缩后的信息。由于两者源自同一模型，隐藏空间天然对齐，无需复杂的预热训练。
*   **动态上下文树（Context Tree）**：
    *   将长文本划分为多个 Chunk，每个 Chunk 构建一个二叉树结构。
    *   **多尺度压缩**：树的高层存储粗粒度信息（高压缩率），底层存储细粒度信息（低压缩率）。
    *   **查询感知检索（Query-Aware Search）**：在推理时，根据当前 Query 与树节点的语义相似度，动态选择需要展开的分支。相关的部分保留细粒度细节，不相关的部分仅保留粗粒度摘要。
*   **位置感知交叉注意力**：在顶层模型的交叉注意力层中引入块级（Chunk-level）旋转位置编码（RoPE），确保模型能够感知压缩块之间的全局时序关系。

#### 3. 实验设计
*   **数据集与场景**：
    *   **持续预训练**：从 RedPajama 采样 20B tokens（1%），训练长度截断为 8K。
    *   **指令微调（SFT）**：使用 LongAlpaca 和部分筛选的指令数据。
*   **Benchmark**：
    *   **语言建模**：在 Arxiv、PG19、ProofPile 等数据集上测试不同长度（4K 到 128K）的困惑度（Perplexity）。
    *   **长文本理解**：使用 **LongBench**（14个任务，涵盖 QA、摘要、代码等）和 **InfiniBench**（处理 100K+ 级别的超长任务）。
    *   **检索能力**：Passkey Retrieval（大海捞针测试）。
*   **对比方法**：包括位置插值（PI）、YaRN、CEPE（编码器-解码器架构）、StreamingLLM（流式）、Activation Beacon（激活标灯）以及 SnapKV/OmniKV 等推理优化方法。

#### 4. 资源与算力
*   **硬件**：使用了 **8 × NVIDIA A800 (80GB)** GPU 集群。
*   **训练细节**：
    *   语言建模阶段：全局 Batch Size 为 128，训练 1 个 epoch。
    *   SFT 阶段：全局 Batch Size 为 8，训练 2 个 epoch。
    *   **参数效率**：仅微调交叉注意力层和顶层的部分自注意力层，无需全参数微调。

#### 5. 实验数量与充分性
论文进行了多维度的实验验证：
*   **主实验**：覆盖了 Llama-2-7B、Llama-3-8B 和 Mistral-7B 三种主流开源模型。
*   **消融实验**：验证了注入层位置（底部 vs 顶部 vs 交错）、查询感知机制的必要性、节点切分噪声的影响以及块级位置编码的作用。
*   **超参数敏感性**：测试了树高度（Tree Height）和压缩率（$\beta$）对性能的影响。
*   **效率评估**：对比了不同长度下的内存占用和推理延迟。
实验设计较为全面，对比了从训练到推理的各类强基线，结果具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **外推能力极强**：仅在 8K 序列上训练，SharedLLM 即可在 128K 长度下保持极低的困惑度，且不发生性能崩溃。
*   **性能领先**：在 LongBench 和 InfiniBench 上，SharedLLM 在多个子项（尤其是多文档 QA 和代码补全）中优于现有的长文本扩展方法。
*   **高效推理**：相比 CEPE 提速约 3 倍，相比流式架构提速约 2 倍，且内存增长随长度增加非常缓慢。
*   **查询感知的价值**：实验证明，动态检索相关细粒度信息对于解决复杂的长文本 QA 任务至关重要。

#### 7. 优点（亮点）
*   **架构优雅**：提出的“自注入”概念极大地降低了异构模型对齐的难度，实现了“以一变二”的堆叠效果。
*   **多尺度平衡**：通过树状结构同时兼顾了全局粗粒度语义和局部细粒度细节，解决了传统压缩方法容易丢失关键信息的问题。
*   **推理友好**：兼容 FlashAttention，且通过浅层压缩避开了深层前向计算的冗余，具有极高的工业应用潜力。

#### 8. 不足与局限
*   **检索开销**：虽然整体推理变快，但查询感知检索过程需要额外的一次浅层前向传递来计算相似度，这带来了约 15% 的额外计算开销。
*   **模型规模限制**：实验主要集中在 7B/8B 规模的模型上，尚未验证该方法在 70B 或更大规模模型上的扩展性表现。
*   **任务依赖性**：树的高度和压缩率对不同任务的敏感度不同，可能需要针对特定场景（如纯摘要 vs 纯检索）进行超参数微调。

（完）
