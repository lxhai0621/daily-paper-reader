---
title: Gradually Excavating External Knowledge for Implicit Complex Question Answering
title_zh: 渐进式挖掘外部知识以解决隐式复杂问答
authors: "Chang Liu, Xiaoguang Li, Lifeng Shang, Xin Jiang, Qun Liu, Edmund Y. Lam, Ngai Wong"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08148v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: RAG的迭代外部知识获取
tldr: "针对大语言模型在处理隐式复杂问答时存在的知识陈旧和单次生成局限性，本文提出一种渐进式知识挖掘框架。该框架允许模型通过迭代方式主动获取外部信息并进行逻辑推理，动态调整解题策略。在StrategyQA数据集上，该方法以不到竞争对手6%的参数量达到了78.17%的准确率，刷新了10B规模模型的SOTA记录。"
selection_source: fresh_fetch
motivation: 解决大语言模型在开放域隐式问答中面临的知识覆盖不足、时效性差以及单次生成缺乏全面性的问题。
method: 提出一种渐进式挖掘框架，使模型能迭代选择查询外部知识或执行逻辑推理步骤，动态构建解题路径。
result: "在StrategyQA测试中取得78.17%的准确率，在参数量远小于同类模型的情况下成为10B量级的新SOTA。"
conclusion: 结合外部知识库的迭代推理机制能显著提升中等规模语言模型处理复杂隐式问题的能力。
---

## 摘要
近年来，大语言模型（LLMs）因其展现出的类人能力和巨大潜力而备受关注。然而，对于开放域隐式问答问题，LLMs 可能并非最终解决方案，原因在于：1）领域知识未覆盖或已过时；2）单次生成模式限制了其全面性。为此，本研究提出了一种用于开放域复杂问答的渐进式知识挖掘框架，使 LLMs 能够迭代且主动地获取外部信息，并基于已获取的历史知识进行推理。具体而言，在求解过程的每一步中，模型会选择一个动作来执行，例如查询外部知识或执行单个逻辑推理步骤，从而逐步逼近最终答案。我们的方法能够有效利用即插即用的外部知识，并动态调整解决复杂问题的策略。在 StrategyQA 数据集上的评估结果显示，我们的方法以不到竞争对手 6% 的参数量实现了 78.17% 的准确率，为 10B 规模左右的 LLMs 树立了新的 SOTA（最先进水平）。

## Abstract
Recently, large language models (LLMs) have gained much attention for the emergence of human-comparable capabilities and huge potential. However, for open-domain implicit question-answering problems, LLMs may not be the ultimate solution due to the reasons of: 1) uncovered or out-of-date domain knowledge, 2) one-shot generation and hence restricted comprehensiveness. To this end, this work proposes a gradual knowledge excavation framework for open-domain complex question answering, where LLMs iteratively and actively acquire external information, and then reason based on acquired historical knowledge. Specifically, during each step of the solving process, the model selects an action to execute, such as querying external knowledge or performing a single logical reasoning step, to gradually progress toward a final answer. Our method can effectively leverage plug-and-play external knowledge and dynamically adjust the strategy for solving complex questions. Evaluated on the StrategyQA dataset, our method achieves 78.17% accuracy with less than 6% parameters of its competitors, setting new SOTA for ~10B-scale LLMs.

---

## 论文详细总结（自动生成）

这是一份关于论文《Gradually Excavating External Knowledge for Implicit Complex Question Answering》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：大语言模型（LLMs）虽然在通用对话中表现出色，但在处理**开放域隐式复杂问答**（Open-domain Implicit Complex QA）时面临挑战。这类问题通常需要特定的外部知识，且解题逻辑隐藏在问题背后，无法通过简单的单次生成（One-shot）直接解决。
*   **核心问题**：
    1.  **知识局限性**：LLM 的预训练知识可能过时或未覆盖特定领域。
    2.  **策略隐蔽性**：复杂问题需要多步拆解，且后续步骤往往依赖于前一步获取的知识。
*   **整体含义**：本文提出了 **GEEK**（Gradually Excavating External Knowledge）框架，旨在通过迭代、主动地挖掘外部知识并动态调整推理策略，提升中等规模模型处理复杂问题的能力。

### 2. 方法论
*   **核心思想**：将问答过程建模为一个迭代的“动作选择-执行-状态更新”循环。模型不是一次性给出答案，而是根据当前状态决定是“拆解问题”、“检索知识”还是“直接回答”。
*   **关键技术模块**：
    *   **核心模型 (Core Model)**：基于 Flan-T5-11B，作为控制器负责动作选择（Action Selection）和逻辑推理。
    *   **检索器 (Retriever)**：采用双层 DPR（Dense Passage Retrieval）架构，先进行文档级检索，再进行段落级精选，从维基百科语料库中获取背景。
    *   **提取器 (Extractor)**：基于 FiD（Fusion-in-Decoder）架构，将检索到的多个长段落压缩为与子问题相关的简洁事实句子。
*   **算法流程**：
    1.  **动作选择**：核心模型根据当前问题状态 $Q_t$ 从动作空间（FinalAnswer, AddDecomp, Retrieve & Extract, SelfAnswer）中选择最优动作。
    2.  **子问题生成（预回答技巧）**：在生成子问题 $d_t$ 时，模型会同时预测剩余的所有子问题及伪答案，以确保推理路径的连贯性。
    3.  **知识获取**：若需外部知识，调用检索器和提取器更新事实库。
    4.  **策略探索 (SE)**：引入类似束搜索（Beam Search）的机制，在每一步探索多个可能的推理分支，最后通过多数投票确定答案。

### 3. 实验设计
*   **数据集**：主要使用 **StrategyQA**。这是一个极具挑战性的隐式推理数据集，要求模型具备多步推理和外部知识获取能力。
*   **Benchmark**：StrategyQA 官方排行榜及相关 SOTA 模型。
*   **对比方法**：
    *   **超大规模模型**：ChatGPT (175B), PaLM (540B), PaLM2 (340B)。
    *   **检索增强/推理增强方法**：RR (175B), Visconde (175B), FaithfulCoT, ReAct 等。
    *   **同规模模型**：UL2 (20B), LLaMA (7B/13B), KARD (3B)。

### 4. 资源与算力
*   **算力投入**：论文明确指出使用了 **8 张 NVIDIA V100 GPU** 进行训练。
*   **软件框架**：采用了 **DeepSpeed** 平台处理显存溢出问题，并使用 HuggingFace 的 **Accelerate** 库进行推理加速和模型卸载（Offloading）。
*   **训练时长**：文中未明确给出具体的训练总时长，但提到了在 8 卡 V100 上进行多任务监督微调（SFT）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **主实验**：在 StrategyQA 测试集上与十余种主流模型进行了对比。
    *   **消融实验**：分别验证了子问题拆解（Decomp）、检索提取（RE）和策略探索（SE）对准确率的贡献。
    *   **底座对比**：测试了 Flan-T5、LLaMA-7B、LLaMA-13B 等不同底座的效果。
    *   **人工评估模拟**：利用 GPT-4 对生成策略的质量进行了人类偏好评估。
*   **充分性与公平性**：实验设计较为客观。作者不仅对比了准确率，还强调了参数效率（以 6% 的参数量达到同等甚至更高性能）。通过 GPT-4 对原始数据集标注进行精细化清洗，确保了训练数据的质量。

### 6. 主要结论与发现
*   **性能突破**：GEEK (11B) 在 StrategyQA 上达到了 **78.17%** 的准确率，刷新了 10B 规模模型的 SOTA，甚至超越了许多 175B 规模的检索增强模型（如 RR）。
*   **外部知识的关键性**：消融实验证明，引入检索和提取模块使准确率提升了约 4.5 个百分点。
*   **策略探索的价值**：通过 SE 机制探索多个解题路径，准确率从 75.98% 进一步提升至 78.17%。
*   **模型规模并非唯一因素**：通过精细的迭代设计和外部工具协作，较小的模型也能在复杂任务上战胜“巨型”模型。

### 7. 优点
*   **参数高效性**：用极小的计算代价实现了顶尖的推理性能。
*   **动态调整能力**：不同于预先固定拆解路径的方法，GEEK 能根据新获取的知识动态修正后续的子问题。
*   **可解释性强**：提供了完整的推理链条，包括子问题、检索到的事实及最终逻辑推导过程。
*   **预回答辅助设计**：生成子问题时考虑后续步骤的“预回答”技巧显著增强了推理的全局性。

### 8. 不足与局限
*   **幻觉风险**：尽管引入了外部事实，但神经提取器和核心模型在处理复杂逻辑时仍可能产生事实性错误或逻辑幻觉。
*   **数据依赖**：模型性能高度依赖于 StrategyQA 这种高质量的拆解标注数据，在缺乏此类标注的领域迁移难度较大。
*   **检索瓶颈**：目前仅依赖维基百科语料库，对于需要实时新闻或极冷门知识的问题，检索覆盖率可能受限。
*   **逻辑一致性**：存在“中间步骤错误但最终答案凑巧正确”或“步骤正确但最后一步推导失误”的情况（错误分析显示逻辑错误占 20%）。

（完）
