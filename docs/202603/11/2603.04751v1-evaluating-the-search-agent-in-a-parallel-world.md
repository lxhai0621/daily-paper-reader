---
title: Evaluating the Search Agent in a Parallel World
title_zh: 在平行世界中评估搜索智能体
authors: "Jiawei Chen, Xintian Shen, Lihao Zheng, Lifu Mu, Haoyi Sun, Ning Mao, Hao Ma, Tao Wei, Pan Zhou, Kun Zhan"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04751v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 评估开放世界问题中的搜索智能体
tldr: 针对搜索智能体评估中存在的基准过时、参数记忆干扰及不可复现等问题，本文提出Mind-ParaWorld（MPW）框架。该框架通过合成超越模型知识截止期的“平行世界”场景，利用法律模型构建原子事实，并由引擎模型动态生成搜索结果。通过包含19个领域、1608个实例的MPW-Bench，研究揭示了智能体在证据收集、信息充足性判断及停止决策方面的性能瓶颈，为评估搜索能力提供了新范式。
selection_source: fresh_fetch
motivation: 传统的搜索智能体评估受限于静态基准的动态失效、模型固有记忆的干扰以及商业搜索引擎的不确定性。
method: 提出Mind-ParaWorld框架，通过构建基于原子事实的虚拟平行世界和动态搜索引擎，模拟未知环境下的搜索任务。
result: 实验表明，尽管智能体擅长综合信息，但在证据收集、覆盖范围以及判断何时停止搜索等关键环节表现受限。
conclusion: 该研究通过隔离参数记忆的影响，为客观评估搜索智能体的核心检索与推理能力提供了可复现的新途径。
---

## 摘要
集成网络搜索工具显著扩展了大型语言模型（LLMs）处理开放世界、实时和长尾问题的能力。然而，评估这些搜索智能体（Search Agents）面临着巨大的挑战。首先，构建高质量的深度搜索基准测试成本极高，而未经核实的合成数据往往面临来源不可靠的问题。其次，静态基准测试面临动态过时的问题：随着互联网信息的演变，由于流行度增加，原本需要深度研究的复杂查询往往退化为简单的检索任务，且标准答案（ground truths）会因时间推移而过时。第三，归因歧义困扰着评估工作，因为智能体的表现往往由其参数化记忆主导，而非其实际的搜索和推理能力。最后，对特定商业搜索引擎的依赖引入了变数，阻碍了可复现性。为了解决这些问题，我们提出了一个名为 Mind-ParaWorld 的新颖框架，用于在平行世界中评估搜索智能体。具体而言，MPW 采样现实世界的实体名称，以合成处于模型知识截止日期之后的未来场景和问题。随后，一个平行世界法则模型（ParaWorld Law Model）为每个问题构建一组不可分割的原子事实（Atomic Facts）和唯一的标准答案。在评估过程中，智能体不再检索现实世界的结果，而是与一个平行世界引擎模型（ParaWorld Engine Model）交互，该模型基于这些不可违背的原子事实动态生成搜索引擎结果页面（SERPs）。我们发布了 MPW-Bench，这是一个涵盖 19 个领域、包含 1,608 个实例的交互式基准测试。在三种评估设置下的实验表明，虽然搜索智能体在信息完整的情况下擅长证据合成，但其性能不仅受限于陌生搜索环境中的证据收集和覆盖范围，还受限于不可靠的证据充分性判断以及“何时停止”决策的瓶颈。

## Abstract
Integrating web search tools has significantly extended the capability of LLMs to address open-world, real-time, and long-tail problems. However, evaluating these Search Agents presents formidable challenges. First, constructing high-quality deep search benchmarks is prohibitively expensive, while unverified synthetic data often suffers from unreliable sources. Second, static benchmarks face dynamic obsolescence: as internet information evolves, complex queries requiring deep research often degrade into simple retrieval tasks due to increased popularity, and ground truths become outdated due to temporal shifts. Third, attribution ambiguity confounds evaluation, as an agent's performance is often dominated by its parametric memory rather than its actual search and reasoning capabilities. Finally, reliance on specific commercial search engines introduces variability that hampers reproducibility. To address these issues, we propose a novel framework, Mind-ParaWorld, for evaluating Search Agents in a Parallel World. Specifically, MPW samples real-world entity names to synthesize future scenarios and questions situated beyond the model's knowledge cutoff. A ParaWorld Law Model then constructs a set of indivisible Atomic Facts and a unique ground-truth for each question. During evaluation, instead of retrieving real-world results, the agent interacts with a ParaWorld Engine Model that dynamically generates SERPs grounded in these inviolable Atomic Facts. We release MPW-Bench, an interactive benchmark spanning 19 domains with 1,608 instances. Experiments across three evaluation settings show that, while search agents are strong at evidence synthesis given complete information, their performance is limited not only by evidence collection and coverage in unfamiliar search environments, but also by unreliable evidence sufficiency judgment and when-to-stop decisions-bottlenecks.

---

## 论文详细总结（自动生成）

这是一份关于论文《Evaluating the Search Agent in a Parallel World》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于**搜索智能体（Search Agents）**的评估难题。随着 LLM 集成搜索工具处理实时和长尾问题的能力增强，传统评估方法面临三大瓶颈：
*   **动态失效（Dynamic Obsolescence）**：互联网信息不断演变，静态基准中的复杂问题会因信息普及而退化为简单检索（难度坍缩），或因时间推移导致标准答案失效（事实漂移）。
*   **归因歧义（Attribution Ambiguity）**：难以区分智能体是靠“参数化记忆”背出了答案，还是真正通过“搜索和推理”解决了问题。
*   **不可复现与高成本**：依赖商业搜索引擎会导致评估环境不可控（排名算法黑盒），且高质量深度搜索数据的标注成本极高。

### 2. 方法论：Mind-ParaWorld (MPW) 框架
核心思想是构建一个**认知隔离且逻辑闭环的“平行世界”**，将搜索任务置于模型知识截止日期之后的“未来场景”中。
*   **平行世界问题构建**：采样真实实体（如球员、品牌），利用“平行世界模型”外推生成未来时间段的虚构场景和复杂问题。
*   **平行世界法则（ParaWorld Laws）**：由法则模型将每个问题分解为一组不可违背的**原子事实（Atomic Facts）**。这些事实作为该世界的“物理定律”，确保答案唯一且逻辑自洽。
*   **平行世界引擎模型（PEM）**：替代真实搜索引擎。当智能体发起查询时，PEM 根据原子事实动态生成类似新闻摘要的搜索结果（SERP）。
*   **抗捷径机制（Anti-shortcut）**：PEM 会对查询进行分类。只有针对单一实体和属性的“原子查询”才能命中事实；模糊或复杂的“复合查询”仅返回无关噪音，迫使智能体必须进行任务分解。

### 3. 实验设计
*   **数据集（MPW-Bench）**：涵盖体育、娱乐、消费品、科技等 **19 个领域**，共 **1,608 个实例**。根据原子事实的数量分为易、中、难三个等级。
*   **评估设置**：
    *   **Setting A (Oracle-Facts)**：直接提供所有事实，测试模型推理和综合能力的上限。
    *   **Setting B (Guided Search)**：提供查询引导，测试模型在受控分解下的搜索表现。
    *   **Setting C (End-to-End)**：完全自主搜索，模拟真实应用场景。
*   **对比方法**：评估了包括 MindWatcher-32B、Qwen3 系列、MiniMax-m2、通义实验室 DeepResearch、Youtu Agent 以及商业模型 GPT-5、Gemini 2.5/3、Kimi-K2 等。

### 4. 资源与算力
*   **模型使用**：实验中使用了 **Qwen3-235B-A22B-Thinking** 作为平行世界引擎模型（PEM）和最终的评判模型（LLM-as-Judge）。
*   **算力细节**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或训练/推理的总时长。但考虑到使用了 235B 参数规模的模型进行大规模交互式推理，其算力消耗显著。

### 5. 实验数量与充分性
*   **实验规模**：在 1,608 个场景下对十余种主流模型进行了跨设置（A/B/C）的对比，总计产生了数万条交互轨迹。
*   **充分性与公平性**：
    *   实验涵盖了从开源到闭源、从 7B 到数百 B 规模的模型，具有广泛代表性。
    *   引入了**事实覆盖率（FCR）**和**命中率（Hit Rate）**等过程指标，而非仅看最终准确率，评估维度全面。
    *   通过 Setting A 验证了任务的可解性，确保了评估的客观性。

### 6. 主要结论与发现
*   **推理不是瓶颈，搜索才是**：在 Setting A（信息完整）下，多数模型准确率极高（>85%），但在 Setting C（自主搜索）下表现大幅下滑，说明瓶颈在于证据获取而非逻辑推理。
*   **搜索策略缺失**：多数模型存在“过早停止”的问题，在未收集齐必要证据时就尝试回答。
*   **边际收益递减**：随着搜索轮数增加，智能体获取新事实的效率迅速下降，且查询质量（命中率）随时间恶化，说明模型缺乏根据反馈调整搜索策略的能力。
*   **复杂度敏感**：随着任务难度增加，事实覆盖率（FCR）显著下降，导致最终成功率呈指数级降低。

### 7. 优点（亮点）
*   **创新范式**：通过“平行世界”彻底解决了数据污染和知识泄露问题，实现了对搜索能力的“纯净”测量。
*   **环境可控**：摆脱了对商业搜索引擎的依赖，使实验具备高度的可复现性和确定性。
*   **细粒度诊断**：通过原子事实的命中记录，能够精确定位智能体是在哪一步（分解、检索、还是综合）出现了失败。

### 8. 不足与局限
*   **合成数据偏差**：平行世界的逻辑一致性高度依赖于法则模型的生成质量，可能存在潜在的逻辑漏洞。
*   **环境简化**：PEM 生成的搜索结果虽然受控，但相比真实的互联网环境（包含广告、SEO 干扰、多模态信息等）仍显简化，可能无法完全模拟极端复杂的现实搜索。
*   **应用限制**：目前主要针对文本搜索，对于多模态搜索（如图片、视频检索）的覆盖尚在起步阶段。

（完）
