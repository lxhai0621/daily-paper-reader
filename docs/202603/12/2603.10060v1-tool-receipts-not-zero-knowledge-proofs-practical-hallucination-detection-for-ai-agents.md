---
title: "Tool Receipts, Not Zero-Knowledge Proofs: Practical Hallucination Detection for AI Agents"
title_zh: 工具收据而非零知识证明：面向 AI 智能体的实用幻觉检测
authors: Abhinaba Basu
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.10060v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 通过验证工具执行来源为AI智能体提供实用的幻觉检测
tldr: "AI智能体在调用工具时常产生幻觉。本文提出NabaOS，一个受印度认识论启发的轻量级验证框架。它通过HMAC签名的工具执行收据来验证LLM响应中的每一项主张，将其分类为工具输出、推理或外部证词等。相比于耗时数分钟的零知识证明（ZKP），NabaOS在保持高检测率（如94.2%的工具伪造检测）的同时，将验证延迟降低至15毫秒以内，为交互式智能体提供了实用的信任机制。"
selection_source: fresh_fetch
motivation: 现有的零知识证明验证方法因计算开销巨大而无法满足交互式AI智能体实时检测工具调用幻觉的需求。
method: 提出NabaOS框架，利用HMAC签名生成不可伪造的工具执行收据，并根据认识论来源对LLM的每一项主张进行实时交叉验证。
result: "在NyayaVerifyBench基准测试中，该方法以低于15ms的开销实现了94.2%的工具伪造检测率和87.6%的数量错误检测率。"
conclusion: 相比于昂贵的加密证明，基于收据的验证和认识论分类为交互式智能体提供了更具成本效益且实用的幻觉检测方案。
---

## 摘要
通过工具调用执行任务的 AI 智能体经常会产生幻觉——伪造工具执行、误报输出数量或将推论当作事实。最近的可验证 AI 推理方法依赖于零知识证明，虽然提供了密码学保证，但每次查询需要数分钟的证明时间，这使得它们对于交互式智能体而言并不实用。我们提出了 NabaOS，这是一个受印度认识论（正理派，Nyaya Shastra）启发的轻量级验证框架，它根据认识来源（量，pramana）对大语言模型（LLM）响应中的每个主张进行分类：直接工具输出（现量，pratyaksha）、推论（比量，anumana）、外部证言（圣教量，shabda）、不存在（缺缘量，abhava）或无根据的观点。我们的运行时系统生成 LLM 无法伪造的 HMAC 签名工具执行收据，随后将主张与这些收据进行交叉引用，以实时检测幻觉。我们在 NyayaVerifyBench 上进行了评估，这是一个包含 1,800 个智能体响应场景的新基准，涵盖四种语言并注入了六种类型的幻觉。NabaOS 检测到了 94.2% 的伪造工具引用、87.6% 的数量误报和 91.3% 的虚假不存在主张，且每个响应的验证开销低于 15 毫秒。对于深度委托（执行多步 Web 任务的智能体），我们的交叉检查协议通过独立重新抓取捕获了 78.4% 的 URL 伪造。我们将其与五种方法进行了对比：zkLLM（密码学证明，180秒/查询）、TOPLOC（局部敏感哈希）、SPEX（基于采样的执行证明）、张量承诺和自一致性检查。NabaOS 在交互式智能体中实现了最佳的成本-延迟-覆盖率权衡：在小于 15 毫秒的时间内实现了 94.2% 的覆盖率，而 zkLLM 在 180,000 毫秒内实现了近乎完美的覆盖率。对于交互式智能体，实用的基于收据的验证比密码学证明具有更好的成本效益，且认识论分类为用户提供了可操作的信任信号，而非简单的二元判断。

## Abstract
AI agents that execute tasks via tool calls frequently hallucinate results - fabricating tool executions, misstating output counts, or presenting inferences as facts. Recent approaches to verifiable AI inference rely on zero-knowledge proofs, which provide cryptographic guarantees but impose minutes of proving time per query, making them impractical for interactive agents. We propose NabaOS, a lightweight verification framework inspired by Indian epistemology (Nyaya Shastra), which classifies every claim in an LLM response by its epistemic source (pramana): direct tool output (pratyaksha), inference (anumana), external testimony (shabda), absence (abhava), or ungrounded opinion. Our runtime generates HMAC-signed tool execution receipts that the LLM cannot forge, then cross-references claims against these receipts to detect hallucinations in real time. We evaluate on NyayaVerifyBench, a new benchmark of 1,800 agent response scenarios across four languages with injected hallucinations of six types. NabaOS detects 94.2% of fabricated tool references, 87.6% of count misstatements, and 91.3% of false absence claims, with <15ms verification overhead per response. For deep delegation (agents performing multi-step web tasks), our cross-checking protocol catches 78.4% of URL fabrications via independent re-fetching. We compare against five approaches: zkLLM (cryptographic proofs, 180s/query), TOPLOC (locality-sensitive hashing), SPEX (sampling-based proof of execution), tensor commitments, and self-consistency checking. NabaOS achieves the best cost-latency-coverage trade-off for interactive agents: 94.2% coverage at <15ms versus zkLLM's near-perfect coverage at 180,000ms. For interactive agents, practical receipt-based verification provides better cost-benefit than cryptographic proofs, and epistemic classification gives users actionable trust signals rather than binary judgments.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **NabaOS** 的轻量级验证框架，旨在解决 AI 智能体（AI Agents）在调用工具时产生的“幻觉”问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：随着 AI 智能体开始处理财务、医疗和法律等高风险任务，其产生的“工具调用幻觉”（如伪造工具执行记录、误报搜索结果数量、将推论伪装成事实）带来了严重的信任危机。
*   **核心矛盾**：现有的可验证推理方案（如零知识证明 ZKP）虽然在密码学上是安全的，但计算开销巨大（每次查询需数分钟），且只能证明“计算过程正确”，无法证明“输出内容真实”。
*   **研究目标**：开发一种低延迟、实用且能区分证据来源的验证机制，使交互式智能体能够实时检测并标记幻觉。

### 2. 方法论
NabaOS 的核心思想是将**印度正理派（Nyaya Shastra）认识论**与**密码学收据**相结合：
*   **认识论分类（Pramana）**：将 LLM 的每一项主张按来源分为五类：
    *   **现量 (Pratyaksha)**：直接来自工具输出。
    *   **比量 (Anumana)**：基于工具数据的逻辑推理。
    *   **圣教量 (Shabda)**：外部来源（如 URL）的证言。
    *   **缺缘量 (Abhava)**：关于“不存在”的主张（如“未找到邮件”）。
    *   **无根据 (Ungrounded)**：纯属模型臆造。
*   **工具执行收据（Tool Receipts）**：
    *   由智能体运行时（而非 LLM 本身）生成 HMAC 签名的收据。
    *   收据包含：工具名、输入/输出哈希、结果计数、关键事实字段及时间戳。
    *   由于 LLM 不掌握密钥，无法伪造收据。
*   **验证流程**：
    1.  运行时执行工具并生成签名收据。
    2.  LLM 生成响应，并根据提示词对主张进行“自标记”（Self-tagging），关联收据 ID。
    3.  验证引擎实时交叉比对 LLM 主张与收据内容，检测数量不符、事实错误或伪造调用。
*   **深度交叉检查**：对于自主多步任务，通过独立重新抓取 URL、重演计算逻辑等方式进行二次验证。

### 3. 实验设计
*   **基准测试**：引入了 **NyayaVerifyBench**，包含 1,800 个智能体响应场景。
*   **数据构成**：涵盖 4 种语言（英语、印地语、中文、西班牙语）；包含 1,200 个注入幻觉的场景和 600 个干净对照场景。
*   **幻觉类型**：伪造工具调用、数量不符、事实不符、推论冒充事实、虚假不存在、来源伪造。
*   **对比方法**：
    *   无验证（Baseline）。
    *   自一致性检查（Self-Consistency）。
    *   RAG 语义对齐（RAG-Grounding）。
    *   输出正则匹配（Output Regex）。
    *   SVIP-style（基于学习的检测器）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或训练时长。
*   **性能指标**：论文强调了 NabaOS 的极低开销，验证过程在普通机器上即可运行，每个响应的额外延迟**小于 15 毫秒**，相比 ZKP 方案（数分钟）具有压倒性优势。

### 5. 实验数量与充分性
*   **实验规模**：共 1,800 组实验，涵盖 6 种幻觉类型和 4 种语言，实验设计较为全面。
*   **评估维度**：不仅测试了检测率（Detection Rate），还评估了误报率（FPR）、延迟、成本以及信任等级的校准度（Calibration）。
*   **客观性**：通过三名标注者进行人工标注，并计算了 Cohen’s Kappa 系数（0.87），证明了基准测试标注的一致性和客观性。

### 6. 主要结论与发现
*   **高效检测**：NabaOS 实现了 **91%** 的综合幻觉检测率，其中对伪造工具调用的检测率高达 **94.2%**。
*   **多语言稳定性**：由于基于结构化收据而非纯文本，NabaOS 在非英语环境下的表现非常稳定（波动仅 4.1%），远优于基于语义的方法。
*   **信任校准**：被标记为“完全验证”的响应准确率达 **98.7%**，证明了该框架提供的信任信号具有高度参考价值。
*   **实用性**：验证开销极低（<15ms），完全满足实时交互需求。

### 7. 优点
*   **实用主义取向**：避开了昂贵的 ZKP 路径，通过简单的 HMAC 签名解决了最迫切的工具调用验证问题。
*   **细粒度反馈**：不再是简单的“对/错”二元判断，而是通过认识论分类告诉用户“AI 是如何知道这些信息的”，增强了透明度。
*   **架构解耦**：验证逻辑与 LLM 推理分离，不依赖特定模型，具有良好的通用性。

### 8. 不足与局限
*   **依赖模型配合**：自标记机制依赖于 LLM 的指令遵循能力，若模型不配合或恶意伪造标签，验证粒度会下降。
*   **工具本身的可信度**：NabaOS 只能验证 LLM 是否忠实于工具输出，如果工具本身（如 API 返回错误数据）有问题，该框架无法检测。
*   **深度验证延迟**：对于需要重新抓取网页的“深度验证”，延迟会增加到 200-500ms，不适用于所有场景。
*   **对抗风险**：虽然 LLM 无法伪造签名，但可能通过微妙的改写来规避收据中的事实比对。

（完）
