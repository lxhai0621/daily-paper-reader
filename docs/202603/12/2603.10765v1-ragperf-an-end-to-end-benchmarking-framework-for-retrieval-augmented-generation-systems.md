---
title: "RAGPerf: An End-to-End Benchmarking Framework for Retrieval-Augmented Generation Systems"
title_zh: RAGPerf：检索增强生成系统的端到端基准测试框架
authors: "Shaobo Li, Yirui Zhou, Yuan Xu, Kevin Chen, Daniel Waddington, Swaminathan Sundararaman, Hubertus Franke, Jian Huang"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10765v1"
tags: ["query:ma-kf"]
score: 10.0
evidence: RAG 流水线和组件的基准测试框架
tldr: RAGPerf是一个针对检索增强生成（RAG）系统的端到端基准测试框架。它将RAG工作流解构为嵌入、索引、检索、重排序和生成等模块，支持多种数据类型、向量数据库和LLM。该框架能自动化收集性能（如吞吐量、资源占用）与质量指标，帮助开发者深入分析各组件对系统整体表现的影响，且运行开销极低。
selection_source: fresh_fetch
motivation: 现有的RAG系统缺乏一个能够对各模块进行细粒度性能分析和端到端质量评估的统一基准测试框架。
method: 设计并实现了一个模块化的RAGPerf框架，通过解耦工作流、支持多样化负载生成及集成主流向量数据库和LLM来进行全面评测。
result: 实验证明RAGPerf能够有效捕获不同配置下的系统行为，自动化收集多维度的性能与准确性指标，且自身性能开销微乎其微。
conclusion: RAGPerf为RAG系统的优化和选型提供了强有力的工具支持，其开源特性有助于推动RAG技术的标准化评估。
---

## 摘要
我们介绍了基于 RAG 的 AI 系统基准测试框架 (RAGPerf) 的设计与实现，用于表征 RAG 流水线的系统行为。为了便于详细的性能分析和细粒度的性能剖析，RAGPerf 将 RAG 工作流解耦为几个模块化组件：嵌入 (embedding)、索引 (indexing)、检索 (retrieval)、重排序 (reranking) 和生成 (generation)。RAGPerf 为用户提供了配置每个组件核心参数的灵活性，并能检查这些参数对端到端查询性能和质量的影响。RAGPerf 拥有一个工作负载生成器，通过支持多样化的数据集（如文本、PDF、代码和音频）、不同的检索与更新比例以及查询分布，来模拟真实场景。RAGPerf 还支持不同的嵌入模型、主流向量数据库（如 LanceDB、Milvus、Qdrant、Chroma 和 Elasticsearch）以及用于内容生成的不同大语言模型 (LLM)。它实现了性能指标（即端到端查询吞吐量、主机/GPU 内存占用和 CPU/GPU 利用率）和准确性指标（即上下文召回率、查询准确性和事实一致性）的自动化收集。我们通过一系列综合实验展示了 RAGPerf 的功能，并在 GitHub 上开源了其代码库。我们的评估表明，RAGPerf 产生的性能开销可以忽略不计。

## Abstract
We present the design and implementation of a RAG-based AI system benchmarking (RAGPerf) framework for characterizing the system behaviors of RAG pipelines. To facilitate detailed profiling and fine-grained performance analysis, RAGPerf decouples the RAG workflow into several modular components - embedding, indexing, retrieval, reranking, and generation. RAGPerf offers the flexibility for users to configure the core parameters of each component and examine their impact on the end-to-end query performance and quality. RAGPerf has a workload generator to model real-world scenarios by supporting diverse datasets (e.g., text, pdf, code, and audio), different retrieval and update ratios, and query distributions. RAGPerf also supports different embedding models, major vector databases such as LanceDB, Milvus, Qdrant, Chroma, and Elasticsearch, as well as different LLMs for content generation. It automates the collection of performance metrics (i.e., end-to-end query throughput, host/GPU memory footprint, and CPU/GPU utilization) and accuracy metrics (i.e., context recall, query accuracy, and factual consistency). We demonstrate the capabilities of RAGPerf through a comprehensive set of experiments and open source its codebase at GitHub. Our evaluation shows that RAGPerf incurs negligible performance overhead.

---

## 论文详细总结（自动生成）

这是一份关于论文《RAGPerf: An End-to-End Benchmarking Framework for Retrieval-Augmented Generation Systems》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：随着检索增强生成（RAG）系统的广泛应用，开发者面临着复杂的系统优化难题。现有的基准测试工具存在局限性：要么仅关注语义准确性（如召回率、幻觉率），要么仅测试单一组件（如仅测试向量数据库或LLM推理），缺乏能够同时表征**端到端系统性能**（吞吐量、延迟、资源利用率）与**生成质量**的统一框架。
*   **研究动机**：RAG流水线涉及嵌入、索引、检索、重排序和生成等多个阶段，各组件间的交互和配置（如Batch Size、索引算法、硬件分配）对最终表现影响巨大。因此，需要一个可复现、模块化且能模拟真实动态场景（如数据频繁更新）的端到端基准测试框架。

### 2. 方法论与关键技术
RAGPerf 的核心思想是将 RAG 流程解耦为模块化组件，并通过自动化工具进行全方位剖析：
*   **模块化解耦**：将流水线分为 Embedding、Indexing、Retrieval、Reranking 和 Generation 五个独立模块，通过标准接口（如 `BaseLLM`, `DBInstance`）支持用户自定义替换。
*   **动态工作负载生成器**：
    *   支持 **CRUD 操作**：模拟真实世界中知识库的持续增长、修订和删除。
    *   **访问分布模拟**：支持均匀分布和齐夫分布（Zipfian），后者用于模拟“热点数据”查询。
    *   **动态真值生成**：针对数据更新场景，利用 LLM（如 DistilBERT 和 T5）自动生成修改后的事实及其对应的问答对，以验证系统是否检索到了最新信息。
*   **低开销系统剖析器**：作为一个独立的后台守护进程运行，通过 Linux `procfs` 和 NVIDIA `NVML` 接口实时采集 CPU、GPU、内存、I/O 和 PCIe 带宽数据。
*   **配置化管理**：通过 YAML 文件定义整个流水线的参数，实现实验的高度可重复性。

### 3. 实验设计
*   **数据集与场景**：
    *   **文本**：Wikipedia (19.3GB) + Natural Questions。
    *   **PDF/图像**：ArXiv (48GB) + 视觉问答。
    *   **代码**：GitHub Code (32GB)。
    *   **音频**：The People's Speech (35.5GB) + 语音转录检索。
*   **对比对象**：
    *   **向量数据库**：LanceDB, Milvus, Qdrant, Chroma, Elasticsearch。
    *   **模型**：Qwen2.5 系列 (3B 到 72B), Llama-3.3, DeepSeek-R1, GPT-20B 等。
    *   **索引算法**：HNSW, IVF, FLAT, DiskANN, GPU-accelerated indices (CAGRA)。
*   **评估指标**：
    *   **性能**：端到端延迟、吞吐量 (QPS)、TTFT（首字延迟）、TPOT（每 Token 延迟）、硬件利用率。
    *   **质量**：上下文召回率 (Context Recall)、事实一致性 (Factual Consistency)、查询准确性 (Query Accuracy)。

### 4. 资源与算力
*   **硬件配置**：
    *   **CPU**：2 颗 32 核 AMD EPYC 9334 处理器。
    *   **内存**：1.4TB DDR5 主机内存。
    *   **GPU**：2 块 NVIDIA H100NVL (每块 94GB 显存)。
    *   **存储**：2TB Samsung 990 Pro SSD。
*   **软件环境**：Ubuntu 22.04, PyTorch 2.8.0, vLLM 推理引擎。
*   **算力消耗**：文中未明确给出总训练/测试时长，但详细记录了不同配置下的执行时间（如文本索引约需 2500-8000 秒不等）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括：
    1.  不同模态（文本、PDF、音频）的端到端延迟分解。
    2.  五种向量数据库在索引和查询阶段的性能对比。
    3.  不同规模 LLM 对生成质量的影响。
    4.  数据更新频率对延迟和准确性的动态影响分析。
    5.  资源受限场景（限制 CPU 核数、内存、显存）下的敏感性测试。
*   **充分性评价**：实验覆盖了从硬件底层到应用高层的多个维度，对比了主流的开源工具，且包含了消融实验（如 Batch Size、嵌入维度、索引类型），实验设计客观、公平且具有很强的工程参考价值。

### 6. 主要结论与发现
*   **性能瓶颈**：在文本 RAG 中，**LLM 生成阶段**是主要瓶颈（占比 75%-91%）；而在多模态（如 PDF）场景中，**格式转换（OCR）和重排序**往往成为核心开销。
*   **资源敏感性**：CPU 核数对吞吐量影响较小，但**主机内存不足**会强制使用磁盘索引，导致性能下降 80% 以上；**GPU 显存**是核心限制因素，直接决定了并发 Batch Size 和模型规模。
*   **更新代价**：为了保证数据新鲜度，启用临时平坦索引（Flat Index）会导致查询延迟随更新量增加而线性上升，直到索引重建。
*   **质量与规模**：高召回率并不等同于高准确性，小型模型往往无法有效利用检索到的长上下文。

### 7. 优点与亮点
*   **端到端完整性**：不仅看准确率，还看硬件“账单”，为生产环境部署提供了直接依据。
*   **动态性模拟**：引入了 CRUD 操作和动态真值生成，比静态数据集更贴近真实业务。
*   **极低开销**：剖析器对系统性能的影响仅为 0.11%，保证了测试结果的真实性。
*   **多模态支持**：涵盖了 PDF、音频等复杂场景，具有前瞻性。

### 8. 不足与局限
*   **硬件平台局限**：目前的性能监控高度依赖 NVIDIA 的 NVML 接口，对 AMD 或国产算力芯片的支持未在文中体现。
*   **评估器偏差**：质量评估依赖于 Ragas（LLM-as-a-judge），可能存在 LLM 固有的偏见或评分不一致问题。
*   **网络开销未深究**：实验主要在单机多卡环境，对于分布式 RAG 系统中的网络通信开销分析较少。

（完）
