---
title: Retrieval or Representation? Reassessing Benchmark Gaps in Multilingual and Visually Rich RAG
title_zh: 检索还是表示？重新评估多语言及视觉丰富 RAG 中的基准差距
authors: "Martin Asenov, Kenza Benkirane, Dan Goldwater, Aneiss Ghodsi"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04238v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 提高RAG准确性和相关性的技术
tldr: 本研究重新评估了多语言及视觉丰富文档在检索增强生成（RAG）中的性能差异。虽然端到端多模态检索器声称优于传统的BM25，但作者发现文档表示（如转录和预处理）才是性能提升的核心驱动力。通过在固定检索机制下优化预处理，BM25在多个基准测试中缩小了与先进模型的差距。研究呼吁建立分解式评估基准，以准确区分转录与检索能力的贡献。
selection_source: fresh_fetch
motivation: 探究多模态检索器在处理复杂视觉和多语言文档时，其性能优势究竟源于检索算法的改进还是文档表示的优化。
method: 在保持检索机制不变的前提下，通过系统性地改变文档的转录和预处理方法，重新评估传统BM25与现代检索器的性能差距。
result: 实验证明，仅通过改进文档表示，传统的BM25就能在多语言和视觉基准测试中恢复巨大的性能差距。
conclusion: 文档表示是驱动RAG性能提升的主要因素，评估基准应将转录与检索能力分开衡量，以便更准确地归因技术进步。
---

## 摘要
检索增强生成 (RAG) 是将语言模型锚定到外部文档和最新信息的一种常用方法。经典的检索系统依赖于诸如 BM25 之类的词法方法，这些方法通过词项重叠和语料库级权重对文档进行排序。在大型查询-文档数据集上训练的端到端多模态检索器声称，相比这些方法有显著改进，特别是在具有复杂视觉布局的多语言文档方面。我们证明了更好的文档表示是基准性能提升的主要驱动因素。通过在保持检索机制不变的情况下系统地改变转录和预处理方法，我们证明了 BM25 可以在多语言和视觉基准上弥补巨大的差距。我们的研究结果呼吁建立分解的评估基准，分别衡量转录和检索能力，从而使该领域能够正确归因进展，并将精力集中在关键之处。

## Abstract
Retrieval-augmented generation (RAG) is a common way to ground language models in external documents and up-to-date information. Classical retrieval systems relied on lexical methods such as BM25, which rank documents by term overlap with corpus-level weighting. End-to-end multimodal retrievers trained on large query-document datasets claim substantial improvements over these approaches, especially for multilingual documents with complex visual layouts. We demonstrate that better document representation is the primary driver of benchmark improvements. By systematically varying transcription and preprocessing methods while holding the retrieval mechanism fixed, we demonstrate that BM25 can recover large gaps on multilingual and visual benchmarks. Our findings call for decomposed evaluation benchmarks that separately measure transcription and retrieval capabilities, enabling the field to correctly attribute progress and focus effort where it matters.

---

## 论文详细总结（自动生成）

这篇论文对当前多语言及视觉丰富文档检索（RAG）领域的流行观点提出了挑战。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：近年来，端到端多模态检索器（如 ColPali, ColQwen2）在处理复杂布局和多语言文档时表现出远超传统词法检索（如 BM25）的性能。
*   **核心问题**：这种性能差距究竟源于**检索机制**（算法本身）的进步，还是仅仅因为现代模型拥有更好的**文档表示**（更优的 OCR 转录和文本预处理）？
*   **整体含义**：论文旨在通过受控实验，重新评估传统检索方法在优化表示后的潜力，并呼吁学术界将“转录能力”与“检索能力”解耦评估。

### 2. 论文提出的方法论
*   **核心思想**：采用“控制变量法”，保持检索算法（BM25）固定，系统性地改变上游的文档转录（OCR）和语言预处理流程。
*   **关键技术细节**：
    *   **多层次 OCR 转录**：对比了四种方案：Adobe PDF 提取（默认）、EasyOCR（开源）、Mistral OCR 3（现代专用 OCR）、以及 Ministral 3B（利用视觉语言模型 VLM 进行 Markdown 转录）。
    *   **语言特定预处理**：针对不同语系实施定制化处理，包括罗曼语系的词干提取（Stemming）、斯拉夫语系的词形还原（Lemmatization）、阿拉伯语的形态分析（Morphological analysis）以及日语/越南语的分词（Segmentation）。
    *   **视觉内容语义化**：利用 VLM 为图片、图表生成文本描述，将原本不可检索的视觉信息转化为 BM25 可处理的词项锚点。

### 3. 实验设计
*   **数据集**：**VisR-Bench**，这是一个包含 15 种语言、涉及大量长文档及复杂视觉布局（图表、表格）的 RAG 基准测试。
*   **对比方法**：
    *   **词法检索**：BM25（不同配置）。
    *   **稠密文本检索**：SBERT, BGE-large, BGE-M3, NV-Embed-v2。
    *   **多模态检索**：CLIP, SigLip, VisRAG, VLM2Vec, GME, 以及 Col 系列模型（ColPali, ColQwen2 等）。
*   **评估指标**：Top-1 和 Top-5 检索准确率。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **原因分析**：由于该研究主要侧重于推理阶段的评估和现有模型的应用（如调用 OCR API 或运行小型 VLM 进行转录），而非从头训练大型检索模型，因此对大规模算力的依赖相对较小。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 15 种语言的完整消融实验，针对每种 OCR 模型和多种预处理组合进行了详尽测试。
*   **充分性与公平性**：
    *   实验设计非常**客观且公平**，因为它直接在相同的基准测试上对比了“优化后的 BM25”与“SOTA 多模态模型”。
    *   通过对 15 种语言的逐一分析，证明了结论的普适性，而非仅在单一语言上生效。

### 6. 主要结论与发现
*   **表示胜过检索**：通过改进 OCR 质量和语言预处理，BM25 在多语言基准上的 Top-5 准确率提升了 8.9%，在视觉丰富文档上提升了高达 31.1%。
*   **弥合差距**：优化后的 BM25（1994 年的技术）性能竟然超过了许多 2024 年发布的先进稠密向量检索器和多模态模型。
*   **瓶颈识别**：
    *   在阿拉伯语、日语中，**OCR 质量**是主要瓶颈。
    *   在捷克语、芬兰语等高度屈折语中，**词形还原**等预处理是关键。
*   **视觉转录的威力**：即使是简单的 VLM 描述也能让 BM25 找回丢失的图表信息，这表明许多多模态模型的优势在于其内置的视觉理解而非检索逻辑。

### 7. 优点
*   **视角独特**：在盲目追求复杂模型的趋势下，回归基础，揭示了被忽视的预处理环节的重要性。
*   **实用性强**：为工业界提供了一个低成本提升 RAG 性能的路径——优化 OCR 和预处理，而非盲目更换昂贵的多模态检索架构。
*   **分析深入**：对不同语言的失败模式进行了细致分类（OCR 主导型 vs. 预处理主导型）。

### 8. 不足与局限
*   **视觉推理上限**：尽管 BM25 进步巨大，但在涉及**纯视觉推理**（如空间关系、非文本编码的图形趋势）时，仍然无法触及顶级多模态检索器的上限。
*   **依赖转录质量**：该方法的有效性高度依赖于所选 VLM 转录模型的质量，若转录模型产生幻觉，检索也会失效。
*   **延迟权衡**：虽然检索阶段 BM25 很快，但前期的 VLM 转录过程可能会引入额外的计算开销和延迟。

（完）
