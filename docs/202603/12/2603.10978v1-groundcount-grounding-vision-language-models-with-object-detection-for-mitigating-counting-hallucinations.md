---
title: "GroundCount: Grounding Vision-Language Models with Object Detection for Mitigating Counting Hallucinations"
title_zh: GroundCount：结合目标检测的视觉语言模型对齐以缓解计数幻觉
authors: "Boyuan Chen, Minghao Shao, Siddharth Garg, Ramesh Karri, Muhammad Shafique"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10978v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 通过外部接地缓解计数幻觉
tldr: 针对视觉语言模型（VLM）在计数任务中普遍存在的幻觉问题，本文提出 GroundCount 框架。该框架利用目标检测模型（如 YOLO）提供的显式空间定位信息来增强 VLM。实验表明，该方法在多个 VLM 架构上实现了显著的准确率提升，并缩短了推理时间。研究揭示了显式符号化引导优于隐式特征融合，为解决 VLM 的空间语义集成限制提供了新思路。
selection_source: fresh_fetch
motivation: 视觉语言模型在计数任务中存在严重的幻觉问题，而传统的目标检测模型在空间定位和实例计数方面表现卓越且计算开销低。
method: 提出 GroundCount 框架，通过将目标检测模型生成的显式空间坐标和计数信息以结构化提示词的形式输入 VLM，实现空间接地增强。
result: "该方法在 Ovis2.5-2B 模型上实现了 81.3% 的计数准确率（提升 6.6%），并使强模型的推理时间减少了 22%。"
conclusion: 计数失败源于空间语义集成的根本性限制，采用显式符号化引导的增强策略比隐式特征融合更能有效缓解 VLM 的计数幻觉。
---

## 摘要
视觉语言模型 (VLM) 在计数任务中表现出持续的幻觉，其准确率显著低于其他视觉推理任务（情感分析除外）。这种现象甚至在具备推理能力的最先进 VLM 中依然存在。相反，基于 CNN 的目标检测模型 (ODM)（如 YOLO）在空间定位和实例计数方面表现出色，且计算开销极低。我们提出了 GroundCount，这是一个通过 ODM 的显式空间对齐（grounding）来增强 VLM，从而缓解计数幻觉的框架。在最佳情况下，我们的基于提示的增强策略在性能最好的模型 (Ovis2.5-2B) 上实现了 81.3% 的计数准确率（提升了 6.6 个百分点），同时通过消除强模型中由幻觉驱动的推理循环，将推理时间缩短了 22%。我们进行了全面的消融研究，证明位置编码是一个关键组件，它对较强的模型有益，但对较弱的模型有害。相比之下，置信度分数对大多数架构都会引入噪声，移除它们可以提高五个评估模型中四个模型的性能。我们进一步评估了特征级融合架构，发现尽管存在复杂的交叉注意力机制，通过结构化提示进行的显式符号对齐仍优于隐式特征融合。我们的方法在五个评估的 VLM 架构中的四个上取得了一致的改进（6.2-7.5 个百分点），其中一个架构由于其迭代反射机制与结构化提示不兼容而导致性能下降。这些结果表明，计数失败源于根本的空间-语义整合限制，而非特定架构的缺陷，同时也强调了增强策略中架构兼容性的重要性。

## Abstract
Vision Language Models (VLMs) exhibit persistent hallucinations in counting tasks, with accuracy substantially lower than other visual reasoning tasks (excluding sentiment). This phenomenon persists even in state-of-the-art reasoning-capable VLMs. Conversely, CNN-based object detection models (ODMs) such as YOLO excel at spatial localization and instance counting with minimal computational overhead. We propose GroundCount, a framework that augments VLMs with explicit spatial grounding from ODMs to mitigate counting hallucinations. In the best case, our prompt-based augmentation strategy achieves 81.3% counting accuracy on the best-performing model (Ovis2.5-2B) - a 6.6pp improvement - while reducing inference time by 22% through elimination of hallucination-driven reasoning loops for stronger models. We conduct comprehensive ablation studies demonstrating that positional encoding is a critical component, being beneficial for stronger models but detrimental for weaker ones. Confidence scores, by contrast, introduce noise for most architectures and their removal improves performance in four of five evaluated models. We further evaluate feature-level fusion architectures, finding that explicit symbolic grounding via structured prompts outperforms implicit feature fusion despite sophisticated cross-attention mechanisms. Our approach yields consistent improvements across four of five evaluated VLM architectures (6.2--7.5pp), with one architecture exhibiting degraded performance due to incompatibility between its iterative reflection mechanisms and structured prompts. These results suggest that counting failures stem from fundamental spatial-semantic integration limitations rather than architecture-specific deficiencies, while highlighting the importance of architectural compatibility in augmentation strategies.

---

## 论文详细总结（自动生成）

这是一份关于论文《GroundCount: Grounding Vision-Language Models with Object Detection for Mitigating Counting Hallucinations》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：视觉语言模型（VLM）在计数任务中存在严重的“幻觉”问题。即使是具备强大推理能力的最新模型，其计数准确率也远低于物体识别或属性识别任务。
*   **研究背景**：
    *   VLM 的视觉编码器（如 ViT）倾向于全局注意力，难以捕捉细粒度的空间实例细节。
    *   自回归解码过程缺乏显式的空间约束，容易受文本先验干扰。
    *   **研究动机**：利用在空间定位和实例计数方面表现卓越且计算开销极低的 CNN 目标检测模型（ODM，如 YOLO），作为 VLM 的“外部插件”来提供显式的空间接地（Grounding）信息。

### 2. 方法论
论文提出了 **GroundCount** 框架，包含三种递进的实施策略：
*   **方案 A：基于提示词的 ODM 增强（Prompt-Based Augmentation）**
    *   **核心思想**：将 ODM 的检测结果转化为结构化文本，直接插入 VLM 的 Prompt 中。
    *   **技术细节**：
        1.  **空间编码**：将图像划分为 3×3 栅格（如“左上”、“正中”等）。
        2.  **物体排序**：按“从左到右、从下到上”的优先级对检测到的物体排序。
        3.  **结构化描述**：生成格式如 `[类别] [索引] [位置]: [置信度]` 的字符串。
*   **方案 B：特征级融合架构（Feature-Level Fusion）**
    *   **核心思想**：在 VLM 的视觉编码器和语言解码器之间插入一个轻量化融合网络。
    *   **关键技术**：采用双路融合。**支路 A** 使用 FiLM（特征线性调制）让 CNN 特征调制 ViT 的 Patch Token；**支路 B** 使用交叉注意力机制让 ViT Token 查询 CNN 的局部和全局特征。
*   **方案 C：混合增强（Combined Strategy）**
    *   结合了方案 A 的显式文本提示和方案 B 的隐式特征融合，并进行联合微调。

### 3. 实验设计
*   **数据集与 Benchmark**：
    *   **PhD Benchmark**：包含 33,688 个 VQA 对，涵盖物体、属性、计数、位置和情感五大类任务。
    *   **挑战变体**：包括 specious context (sec, 误导性描述)、incorrect context (icc, 错误描述) 和 contradictory common sense (ccs, 违背常识的 AI 生成图)。
*   **对比模型**：
    *   **VLM**：Molmo2-4B, Ovis2.5-2B, R-4B, Qwen3-VL-2B, InternVL3.5-1B。
    *   **ODM**：YOLOv13x。
*   **评估指标**：准确率（Accuracy）和推理延迟（Inference Time）。

### 4. 资源与算力
*   **硬件环境**：使用 **NVIDIA A100 (80GB)** GPU 进行实验。
*   **训练细节**：
    *   融合网络训练使用 AdamW 优化器，学习率为 $2 \times 10^{-5}$。
    *   最大训练步数为 **40k steps**。
    *   推理阶段使用 float32 精度和贪婪解码（Greedy Decoding）。
*   **推理开销**：YOLOv13x 的推理时间约为 0.1s，相对于 VLM 的数秒级解码几乎可以忽略。

### 5. 实验数量与充分性
*   **实验规模**：
    *   横跨 5 种主流 VLM 架构，验证了方法的普适性。
    *   针对方案 B 进行了 4 种不同的微调配置（仅训练融合层、全量微调等）对比。
    *   **消融实验**：系统性地移除了位置编码、置信度分数，并测试了不同检测阈值（0.5 vs 0.3）的影响。
*   **充分性评价**：实验设计非常充分且客观。通过引入对抗性上下文（sec/icc）测试了模型的鲁棒性，并对比了“仅使用 ODM”的基准线，证明了 VLM 在获得接地信息后确实产生了增益。

### 6. 主要结论与发现
*   **显式胜过隐式**：方案 A（纯 Prompt 增强）效果最好，在 Ovis2.5-2B 上达到 81.3% 的准确率（提升 6.6pp）。这表明 VLM 目前处理结构化符号信息的能力强于学习隐式的跨模态特征对齐。
*   **效率逆增长**：对于强模型，提供 ODM 信息反而使推理时间缩短了约 **22%**。这是因为显式信息减少了模型因幻觉而产生的无效“链式思考（CoT）”循环。
*   **位置编码的双刃剑效应**：位置信息对强模型（如 Molmo2, Ovis）有益，但会干扰弱模型（如 R-4B, InternVL），导致弱模型在无位置信息时表现更好。
*   **架构兼容性**：InternVL3.5 是唯一表现下降的模型，研究者认为其内部的迭代反射机制与外部结构化提示存在冲突。

### 7. 优点
*   **即插即用**：方案 A 无需重新训练模型，具有极高的实用价值。
*   **视角独特**：不仅关注准确率，还深入分析了推理步数和 Token 消耗，揭示了缓解幻觉对提升效率的贡献。
*   **分析深入**：通过消融实验界定了位置信息和置信度在不同能力等级模型中的不同作用。

### 8. 不足与局限
*   **架构依赖性**：实验发现某些特定架构（如 InternVL）对该增强策略不耐受，说明方法并非万能。
*   **ODM 瓶颈**：系统的上限受限于 ODM 的性能。如果 YOLO 漏检或误检，VLM 很难自行纠正。
*   **任务局限**：目前主要集中在二元分类（Yes/No）的计数问题上，对于开放式计数或极小目标的处理能力尚待进一步验证。
*   **训练规模**：特征融合实验（方案 B）的训练步数相对较短（40k），可能未完全挖掘出特征级融合的潜力。

（完）
