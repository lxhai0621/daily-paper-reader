---
title: "DBAutoDoc: Automated Discovery and Documentation of Undocumented Database Schemas via Statistical Analysis and Iterative LLM Refinement"
title_zh: DBAutoDoc：通过统计分析与迭代式大语言模型精炼实现未记录数据库模式的自动发现与文档化
authors: "Amith Nagarajan, Thomas Altman"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23050v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 自动发现并记录未归档的数据库模式
tldr: "DBAutoDoc是一个自动发现和记录未记录数据库模式的系统。针对许多关键数据库缺乏主外键约束和清晰命名的现状，该系统结合了统计数据分析与大语言模型（LLM）的迭代优化。其核心思想是将模式理解视为图结构问题，通过类似神经网络反向传播的机制，在模式依赖图中迭代传播语义修正，从而生成高质量的实体关系图和文档。实验显示其在多个模型上达到96.1%的综合得分，显著优于纯LLM方法。"
selection_source: fresh_fetch
motivation: 许多关键数据库系统缺乏必要的文档、主外键约束和清晰的命名，导致模式理解极其困难。
method: 结合统计分析与LLM迭代优化，通过在模式依赖图中传播语义修正来逐步完善数据库描述。
result: "在基准测试中获得96.1%的综合评分，且在主外键检测上比纯LLM方法提升了23个F1分值。"
conclusion: DBAutoDoc证明了结合确定性统计分析与迭代LLM细化能有效解决复杂数据库的自动化文档化难题。
---

## 摘要
大量关键数据库系统缺乏足够的文档。声明的主键缺失，外键约束因性能原因被移除，列名是晦涩的缩写，且不存在实体关系图。我们提出了 DBAutoDoc，这是一个通过结合统计数据分析与迭代式大语言模型（LLM）精炼，自动发现并记录未记录关系数据库模式的系统。DBAutoDoc 的核心见解是，模式理解本质上是一个迭代的、图结构化的问题。DBAutoDoc 从神经网络的反向传播中汲取结构灵感，在多次精炼迭代中通过模式依赖图传播语义修正，直到描述趋于收敛。这种传播是离散且语义化的，而非数学化的，但其结构类比是精确的：早期迭代产生类似于随机初始化的粗略描述，而随着上下文在图中流动，后续的传递会使全局图景变得清晰。该系统做出了论文中详述的四项具体贡献。在一套基准数据库上，DBAutoDoc 使用综合指标在两个模型系列（Google 的 Gemini 和 Anthropic 的 Claude）中实现了 96.1% 的总加权得分。消融分析表明，确定性流水线相比仅使用 LLM 的外键（FK）检测带来了 23 个百分点的 F1 值提升，证实了该系统的贡献是实质性的，且独立于 LLM 的预训练知识。DBAutoDoc 已作为开源软件发布，包含所有评估配置和提示词模板，以实现完全的可复现性。

## Abstract
A tremendous number of critical database systems lack adequate documentation. Declared primary keys are absent, foreign key constraints have been dropped for performance, column names are cryptic abbreviations, and no entity-relationship diagrams exist. We present DBAutoDoc, a system that automates the discovery and documentation of undocumented relational database schemas by combining statistical data analysis with iterative large language model (LLM) refinement.   DBAutoDoc's central insight is that schema understanding is fundamentally an iterative, graph-structured problem. Drawing structural inspiration from backpropagation in neural networks, DBAutoDoc propagates semantic corrections through schema dependency graphs across multiple refinement iterations until descriptions converge. This propagation is discrete and semantic rather than mathematical, but the structural analogy is precise: early iterations produce rough descriptions akin to random initialization, and successive passes sharpen the global picture as context flows through the graph.   The system makes four concrete contributions detailed in the paper. On a suite of benchmark databases, DBAutoDoc achieved overall weighted scores of 96.1% across two model families (Google's Gemini and Anthropic's Claude) using a composite metric. Ablation analysis demonstrates that the deterministic pipeline contributes a 23-point F1 improvement over LLM-only FK detection, confirming that the system's contribution is substantial and independent of LLM pre-training knowledge. DBAutoDoc is released as open-source software with all evaluation configurations and prompt templates included for full reproducibility.

---

## 论文详细总结（自动生成）

### DBAutoDoc：通过统计分析与迭代式 LLM 精炼实现未记录数据库模式的自动发现与文档化

#### 1. 核心问题与整体含义（研究动机和背景）
在企业环境中，大量生产数据库被称为“黑暗数据库（Dark Databases）”：它们缺乏主键（PK）和外键（FK）声明（常因性能优化被移除），列名使用晦涩的缩写（如 `cust_cd`），且没有任何实体关系图（ERD）或数据字典。手动对这些拥有数百张表的复杂模式进行文档化需要专家耗费数周时间，成本极高（约 1.2 万至 4.8 万美元）。现有的模式提取工具或单次 LLM 分析无法结合实际数据统计，且难以处理表与表之间的复杂依赖关系。

#### 2. 方法论：核心思想与关键技术
DBAutoDoc 的核心见解是将模式理解视为一个**迭代的图结构问题**。其架构分为六个阶段，核心技术包括：
*   **分层统计发现流水线**：结合命名模式匹配、唯一性分析（UCC 检测）和包含依赖（IND 检测）来识别 PK 和 FK 候选者。
*   **双向 LLM 反馈循环**：统计引擎生成候选者，LLM 进行语义验证；同时 LLM 在分析过程中提出的新关联会交由统计引擎进行数据一致性校验。
*   **迭代式上下文传播算法（类反向传播）**：
    *   **前向传递**：按依赖层级（Topological Sort）分析表，利用父表描述作为子表的上下文。
    *   **反向传播**：子表的分析结果（如发现的业务逻辑）作为“语义梯度”回传给父表，触发父表描述的修正。
*   **多准则收敛检测**：通过稳定性窗口、置信度阈值和语义变化幅度判断何时停止迭代（通常 2 次迭代即可收敛）。

#### 3. 实验设计
*   **数据集**：
    *   **公共基准**：AdventureWorks2022（复杂模式）、Chinook、Northwind。
    *   **合成基准**：LousyDB（专门设计，含晦涩缩写和数据质量问题，确保 LLM 未在训练中见过）。
    *   **私有企业数据库**：OrgA（专业协会 CRM）、OrgB（贸易协会系统），用于验证泛化能力。
*   **对比方法（Benchmark）**：
    *   **纯统计方法**：仅依赖数据特征发现键。
    *   **纯 LLM 方法**：不使用确定性过滤和统计门控。
    *   **跨模型对比**：对比了 Google Gemini、Anthropic Claude 和 OpenAI GPT 系列模型。
*   **评估指标**：PK/FK 的 F1 分数、表/列描述覆盖率、综合加权得分。

#### 4. 资源与算力
论文未提及具体的 GPU 训练算力，因为该系统是基于现有的 LLM API 构建的应用层框架。
*   **成本分析**：在 Gemini Flash 上，每 100 张表的文档化成本约为 **0.70 美元**。
*   **Token 消耗**：Gemini 模型消耗约 3.2M tokens，而 Claude 模型通过更高效的上下文处理，消耗量仅为 Gemini 的 1/7。
*   **耗时**：对于 36 张表的数据库，全自动处理约需 1.5 小时。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 6 个不同领域、不同规模（11 到 125 张表）的数据库。
*   **消融实验**：详细分析了确定性门控（Deterministic Gates）的作用，证明其为 FK 检测带来了 23 个百分点的 F1 提升。
*   **充分性**：实验设计较为充分，通过私有数据库和合成数据库有效排除了 LLM “记忆”公共基准测试数据的风险（Data Contamination），结果具有客观性。

#### 6. 主要结论与发现
*   **性能卓越**：在 AdventureWorks2022 上实现了 95.0% 的 PK F1 和 94.2% 的 FK F1，综合得分 96.1%。
*   **协同效应**：LLM 擅长发现语义关联（高召回率），而确定性统计流水线擅长剔除错误关联（高精确度），两者结合远优于单一方法。
*   **成本骤降**：相比人工专家文档化，DBAutoDoc 将成本降低了 **99.5% 以上**。
*   **迭代必要性**：通过图结构传播上下文，能显著修正初始分析中的语义偏差。

#### 7. 优点
*   **开源贡献**：提供了完整的提示词模板、评估脚本和基准配置，具有极高的可复现性。
*   **结构创新**：将神经网络的反向传播概念引入语义文档化，解决了“冷启动”时的上下文缺失问题。
*   **实用性强**：支持 SQL Server、PostgreSQL 和 MySQL，直接生成可执行的 SQL 注释脚本和 Mermaid ERD 图。

#### 8. 不足与局限
*   **幻觉风险**：LLM 仍可能产生语义幻觉，生成的文档在关键任务中仍需人工审核。
*   **语言限制**：对于非英语列名的数据库，LLM 的语义推理能力可能会下降。
*   **数据依赖**：对于空表或数据量极小的表，统计引擎无法提供有效的支持，导致质量下降。
*   **评估主观性**：虽然键的发现有客观标准，但“描述质量”的评估目前仍缺乏大规模的独立人类专家双盲测试。

（完）
