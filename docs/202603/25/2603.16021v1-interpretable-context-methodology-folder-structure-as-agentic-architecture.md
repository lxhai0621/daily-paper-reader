---
title: "Interpretable Context Methodology: Folder Structure as Agentic Architecture"
title_zh: 可解释上下文方法论：作为智能体架构的文件夹结构
authors: "Jake Van Clief, David McDermott"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16021v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 使用文件夹结构和Markdown提示词进行上下文传递的智能体架构
tldr: 针对当前AI智能体编排框架过于复杂、工程开销大的问题，本文提出了一种名为“模型工作区协议（MWP）”的方法。该方法摒弃了复杂的代码级编排，转而利用文件系统结构（如编号文件夹和Markdown文件）来管理上下文和任务流。通过将Unix管道设计和模块化分解思想应用于AI上下文构建，MWP允许单个智能体在无需多智能体框架的情况下完成复杂的顺序工作流，显著提高了系统的可解释性和人类干预的便捷性。
selection_source: fresh_fetch
motivation: 现有的多智能体框架在处理需要人工审核的顺序工作流时存在过度工程化和维护成本高的问题。
method: 提出MWP协议，利用编号文件夹表示阶段，通过Markdown文件传递提示词和上下文，并结合本地脚本处理非AI任务。
result: 实现了一个无需复杂编排框架即可让单个智能体高效完成多步骤任务的系统，简化了上下文管理并降低了工程开销。
conclusion: 该方法证明了通过合理的文件系统结构设计可以替代复杂的智能体编排代码，为AI工作流提供了一种更简洁、可解释的架构。
---

## 摘要
当前的 AI 智能体编排方法通常涉及构建多智能体框架，通过代码管理上下文传递、记忆、错误处理和步骤协调。这些框架在复杂的并发系统中表现良好，但对于每一步都需要人工审查输出的顺序工作流，它们引入了不必要的工程开销。本文提出了模型工作区协议（Model Workspace Protocol, MWP），这是一种用文件系统结构取代框架级编排的方法。编号文件夹代表不同阶段，纯 Markdown 文件承载提示词和上下文，告知单个 AI 智能体在每个步骤中扮演的角色。本地脚本处理无需 AI 参与的机械性工作。其结果是，单个智能体通过在正确的时间读取正确的文件，即可完成原本需要多智能体框架才能完成的工作。该方法将 Unix 管道设计、模块化分解、多轮编译和文学编程的思想应用于为 AI 智能体构建上下文这一特定问题。该协议已在 MIT 许可下开源。

## Abstract
Current approaches to AI agent orchestration typically involve building multi-agent frameworks that manage context passing, memory, error handling, and step coordination through code. These frameworks work well for complex, concurrent systems. But for sequential workflows where a human reviews output at each step, they introduce engineering overhead that the problem does not require. This paper presents Model Workspace Protocol (MWP), a method that replaces framework-level orchestration with filesystem structure. Numbered folders represent stages. Plain markdown files carry the prompts and context that tell a single AI agent what role to play at each step. Local scripts handle the mechanical work that does not need AI at all. The result is a system where one agent, reading the right files at the right moment, does the work that would otherwise require a multi-agent framework. This approach applies ideas from Unix pipeline design, modular decomposition, multi-pass compilation, and literate programming to the specific problem of structuring context for AI agents. The protocol is open source under the MIT license.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为**模型工作区协议（Model Workspace Protocol, MWP）**的新型 AI 智能体架构方法论。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前的 AI 智能体编排框架（如 LangChain, CrewAI, AutoGen）对于需要人工逐步审核的**顺序工作流**来说过于复杂。这些框架依赖大量的代码抽象来管理上下文传递和任务协调，导致非开发人员难以调整工作流（如修改提示词、增删步骤），且中间状态难以直观检查。
*   **整体含义**：论文提出“回归原始”，借鉴 Unix 管道设计和多轮编译思想，利用**文件系统结构**（文件夹和 Markdown 文件）直接作为智能体的编排架构。其核心逻辑是：通过合理的文件夹组织，让单个智能体在正确的时间读取正确的文件，从而替代复杂的多智能体代码框架。

### 2. 方法论：核心思想与技术细节
MWP 的核心是将工作区划分为一个**五层上下文层次结构**，通过文件夹编号强制执行顺序：
*   **核心思想**：
    *   **一个阶段，一个任务**：每个文件夹代表一个独立步骤，输出作为下一个步骤的输入。
    *   **纯文本接口**：使用 Markdown 和 JSON，确保人类和 AI 都能直接读写。
    *   **分层上下文加载**：智能体仅加载当前阶段所需的上下文，避免长文本导致的性能下降（“迷失在中间”现象）。
*   **五层结构细节**：
    *   **Layer 0 (全局身份)**：`CLAUDE.md`，定义工作区身份和结构。
    *   **Layer 1 (任务路由)**：`CONTEXT.md`，定义如何根据用户需求分配阶段。
    *   **Layer 2 (阶段契约)**：阶段内的 `CONTEXT.md`，规定该步骤的输入、处理流程和输出。
    *   **Layer 3 (参考资料/工厂)**：稳定的规则、风格指南、领域知识（不随运行改变）。
    *   **Layer 4 (工作产物/产品)**：当前运行生成的中间件，如研究报告、脚本草稿（随运行改变）。
*   **工作流程**：人类在阶段文件夹的 `output/` 目录进行审核和编辑，智能体读取编辑后的文件进入下一阶段，实现“文件系统级”的提示词链。

### 3. 实验设计
论文通过实际生产环境中的工作流验证了 MWP：
*   **应用场景**：
    1.  **脚本到动画流水线**：从研究、脚本编写到生成 Remotion 动画代码。
    2.  **课程幻灯片制作**：从非结构化资料到生成完整的演示文稿。
    3.  **工作区生成器**：一个能够自动构建其他 MWP 工作区的元工作区。
*   **对比基准 (Benchmark)**：
    *   **上下文效率对比**：对比了 MWP 阶段性加载与“单体提示词（Monolithic Prompting）”的 Token 占用。实验显示，MWP 将上下文维持在 2k-8k Token，而单体方法会超过 40k Token 且包含大量无关信息。
    *   **用户干预模式**：观察了 52 名从业者在不同阶段的编辑频率。

### 4. 资源与算力
*   **模型使用**：主要使用 **Claude Code** 配合 **Claude Opus 4.6** 作为主编排智能体，并调用 **Claude Sonnet 4.6** 处理子任务。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。由于 MWP 是一种编排协议而非模型训练方法，其资源消耗主要体现在推理阶段的 Token 成本。

### 5. 实验数量与充分性
*   **实验规模**：建立了一个由 **52 名成员**组成的从业者社区进行测试，涵盖 AI 工程师、内容创作者和学术研究人员。
*   **数据收集**：报告了 33 名用户在动画流水线中的干预模式。
*   **充分性评价**：实验在实际生产流中具有一定的广度，但**缺乏严谨的受控实验**（如对比同一任务下 MWP 与 LangChain 的开发效率定量数据）。数据多源于用户自述和观察，属于定性研究向定量研究的过渡阶段。

### 6. 主要结论与发现
*   **U 型干预模式**：人类在第一阶段（设定方向）和最后阶段（对齐最终产物）干预最频繁，中间阶段由于受到 Layer 3 规则的严格约束，干预较少。
*   **可解释性即副作用**：由于所有中间产物都是文件，系统天然具有可观察性，无需额外的日志或仪表盘。
*   **非技术友好性**：无编程经验的用户可以通过编辑 Markdown 文件成功调整复杂的 AI 工作流。
*   **性能提升**：通过物理隔离无关上下文，显著降低了模型因长文本干扰而产生的错误。

### 7. 优点
*   **极简主义**：无需学习复杂的框架 API，只要会操作文件夹和写 Markdown 即可。
*   **高度可移植**：整个工作区就是一个文件夹，支持 Git 版本控制，易于分享和复现。
*   **人类中心化**：将“人工审核”无缝集成到文件系统的读写循环中，符合混合主动系统设计原则。
*   **解耦性**：模型无关（Model-agnostic），可以轻松更换底层 LLM。

### 8. 不足与局限
*   **应用范围限制**：不适用于需要高并发、实时动态交互或复杂分支逻辑的系统。
*   **手动开销**：对于完全自动化的任务，频繁的文件读写和人工审核点可能显得多余。
*   **实验偏差**：目前的反馈主要来自受邀的早期采用者社区，可能存在选择性偏差。
*   **缺乏自动化调试工具**：目前溯源错误（如最终输出的错误源于哪一层参考资料）仍依赖人工检查，论文提出的“语义调试器”尚处于设想阶段。

（完）
