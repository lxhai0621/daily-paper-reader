---
title: "PCR: A Prefetch-Enhanced Cache Reuse System for Low-Latency RAG Serving"
title_zh: PCR：一种用于低延迟 RAG 服务的预取增强型缓存复用系统
authors: "Wenfeng Wang, Xiaofeng Hou, Peng Tang, Hengyi Zhou, Jing Wang, Xinkai Wang, Chao Li, Minyi Guo"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23049v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 用于低延迟RAG服务的预取增强缓存复用
tldr: 针对RAG系统因引入外部文档导致输入序列过长、Prefill阶段延迟高的问题，本文提出PCR系统以优化KV缓存复用效率。PCR通过前缀树结构结合Look-ahead LRU策略提升缓存命中率，利用层级重叠流水线掩盖CPU-GPU传输延迟，并引入基于队列的预取机制提前从SSD加载数据。该系统显著降低了长文本推理的计算与I/O开销，在首字生成时间（TTFT）上实现了最高2.47倍的加速。
selection_source: fresh_fetch
motivation: RAG系统长序列输入带来的KV缓存计算压力巨大，且现有缓存复用技术面临命中率低及数据传输延迟高等挑战。
method: PCR引入了基于待处理请求的Look-ahead LRU策略、层级计算与传输重叠技术以及从SSD到内存的异步预取机制。
result: 实验表明，PCR在多种高吞吐场景下均优于现有KV缓存复用方法，平均首字生成时间（TTFT）提升高达2.47倍。
conclusion: 通过智能预取和流水线化数据管理，PCR有效解决了RAG服务中的KV缓存瓶颈，是实现低延迟长文本推理的有效方案。
---

## 摘要
检索增强生成（RAG）系统通过引入补充检索文档来增强大语言模型（LLM）的性能，从而实现更准确且具有上下文感知能力的响应。然而，集成这些外部文档通常会导致极长的输入序列，这显著增加了预填充（prefill）阶段的计算成本，在该阶段会为所有输入 token 生成键值（KV）表示。这种延迟瓶颈在高吞吐量服务场景下尤为突出。KV 缓存复用（KV-cache reuse）通过存储共享输入前缀的先前计算 KV 状态，从而避免在包含重叠上下文的请求之间进行冗余计算，提供了一种极具前景的解决方案。然而，缓存复用的有效性通常受到三个实际挑战的限制：由于朴素的淘汰策略导致的低缓存命中率、高昂的 CPU-GPU 数据传输开销，以及当缓存溢出到存储时缓慢的 SSD I/O。为了解决这些问题，我们提出了 PCR，这是一个旨在通过智能预取和流水线数据移动来最大化 KV 缓存复用效率的系统。具体而言，PCR 引入了三项关键技术：(1) 一种带有前瞻性 LRU（look-ahead LRU）替换策略的前缀树缓存结构，该策略利用调度器队列中的待处理请求来提高缓存命中率；(2) 层级重叠（layer-wise overlapping），通过 CUDA 流在 KV 缓存加载和 GPU 计算之间建立流水线，以隐藏通信延迟；(3) 基于队列的预取，在需要之前主动将相关的 KV 缓存从 SSD 加载到 DRAM 中。广泛的实验表明，PCR 优于现有的 KV 缓存复用方法，在平均首字延迟（TTFT）方面实现了高达 2.47 倍的加速。

## Abstract
Retrieval-Augmented Generation (RAG) systems enhance the performance of large language models (LLMs) by incorporating supplementary retrieved documents, enabling more accurate and context-aware responses. However, integrating these external documents often results in very long input sequences, which significantly increases computation costs during the prefill stage, where key-value (KV) representations for all input tokens are generated. This latency bottleneck becomes especially pronounced under high-throughput serving scenarios. KV-cache reuse offers a promising solution by storing previously computed KV states for shared input prefixes, thereby avoiding redundant computation across requests that contain overlapping context. Yet, the effectiveness of cache reuse is often limited by three practical challenges: low cache hit rates due to naive eviction policies, high CPU-GPU data transfer overhead, and slow SSD I/O when caches spill to storage. To address these issues, we propose PCR, a system designed to maximize KV-cache reuse efficiency through intelligent prefetching and pipelined data movement. Specifically, PCR introduces three key techniques: (1) a prefix-tree caching structure with a look-ahead LRU replacement policy that uses pending requests in the scheduler queue to improve cache hit ratios; (2) layer-wise overlapping that pipelines KV-cache loading and GPU computation across CUDA streams to hide communication latency; and (3) queue-based prefetching that proactively loads relevant KV caches from SSD into DRAM before they are needed. Extensive experiments show that PCR outperforms existing KV-cache reuse methods, achieving up to a 2.47x speedup in terms of average TTFT.

---

## 论文详细总结（自动生成）

### 论文总结：PCR - 用于低延迟 RAG 服务的预取增强型缓存复用系统

#### 1. 核心问题与研究动机
*   **核心问题**：检索增强生成（RAG）系统通过引入大量外部文档提升 LLM 准确性，但这导致输入序列极长，使得**预填充（Prefill）阶段**的计算开销巨大，成为首字生成延迟（TTFT）的主要瓶颈。
*   **研究动机**：虽然 KV 缓存复用（KV-cache reuse）能减少重复计算，但现有方案面临三大挑战：
    1.  **缓存命中率低**：传统的淘汰策略（如 LRU）未考虑 RAG 特有的请求模式。
    2.  **传输开销高**：CPU 与 GPU 之间频繁的 KV 缓存换入换出导致严重的通信延迟。
    3.  **存储瓶颈**：当缓存溢出到 SSD 时，同步读取速度远慢于计算速度，导致 GPU 空转。

#### 2. 方法论
PCR 系统通过以下三项核心技术优化 KV 缓存的管理与移动：
*   **前缀树缓存与前瞻性 LRU（Look-ahead LRU）**：
    *   使用前缀树组织固定大小的 KV 缓存块，确保位置相关的 KV 状态能被精确匹配。
    *   **核心创新**：通过观察调度器队列中“即将到来”的请求，提前提升这些请求所需缓存块的优先级，防止其被提前淘汰，从而提高缓存命中率。
*   **层级重叠技术（Layer-wise Overlapping）**：
    *   利用 LLM 的逐层计算特性，将 KV 缓存的加载（CPU→GPU）、当前层计算、以及新生成缓存的写回（GPU→CPU）分配到三个独立的 CUDA 流中。
    *   通过流水线并行，将数据传输延迟隐藏在计算延迟之后，使有效开销缩减至仅一层级的传输时间。
*   **基于队列的异步预取（Queue-based Prefetching）**：
    *   系统监控等待队列，利用请求排队的时间窗口，由后台线程提前将 SSD 中的 KV 缓存加载至 DRAM。
    *   配合异步写回机制，确保 SSD 的慢速 I/O 不会阻塞推理主流程。

#### 3. 实验设计
*   **数据集与场景**：使用 Wikipedia 作为检索语料库，SQuAD 作为查询集。模拟真实的 RAG 工作流，每个请求检索 2 个相关文档，平均输入长度约 6.8k tokens。
*   **对比方法（Baselines）**：
    1.  **vLLM**：工业级推理框架（含基础前缀缓存）。
    2.  **LMCache**：最先进的跨层级 KV 缓存复用系统。
    3.  **CCache/SCCache**：PCR 的简化版，分别仅支持 CPU 缓存和 SSD+CPU 缓存（无优化）。
*   **评估指标**：平均 TTFT、端到端延迟（E2EL）、P95/P99 尾部延迟。

#### 4. 资源与算力
*   **硬件平台**：
    *   **平台 A**：2 × NVIDIA A6000 (48GB), 256GB RAM, 4TB NVMe SSD。
    *   **平台 B**：2 × NVIDIA RTX 4090 (24GB), 128GB RAM, 4TB NVMe SSD。
*   **模型**：涵盖 Llama 3.1 (8B/3B)、Qwen 2.5 (8B/14B) 及 Llama 2 (7B/13B)。
*   **训练/推理时长**：论文未提及模型训练，主要关注推理服务性能；实验通过 2000 次采样迭代进行压力测试。

#### 5. 实验数量与充分性
*   **实验规模**：在两种硬件架构、六种不同规模的模型、多种请求到达率（0.5 到 1.0 req/s）下进行了广泛测试。
*   **消融实验**：专门针对“层级重叠”和“预取窗口大小”进行了独立评估，验证了各组件的贡献。
*   **充分性评价**：实验设计较为充分，涵盖了从平均性能到尾部延迟的全面对比，且考虑了不同 KV 架构（GQA vs MHA）对缓存大小的影响，结果具有较强的说服力。

#### 6. 主要结论与发现
*   **性能提升**：PCR 在 TTFT 上相比 vLLM 最高实现 **2.47 倍**加速，相比 LMCache 也有显著提升。
*   **延迟隐藏**：层级重叠技术平均减少了 15% 的延迟，尤其在 KV 缓存较大的模型（如 Llama 2）上效果更明显。
*   **预取价值**：在高负载场景下，基于队列的预取能有效消除 SSD 加载延迟，TTFT 随请求率增加的增长曲线比基准线更平缓。
*   **稳定性**：PCR 在 P99 尾部延迟上表现优异，证明其调度策略能有效减少系统抖动。

#### 7. 优点
*   **系统级协同**：不改变模型架构或牺牲精度（精确前缀匹配），通过纯系统工程手段优化 I/O 瓶颈。
*   **前瞻性调度**：Look-ahead LRU 巧妙地利用了推理引擎的队列信息，将“被动缓存”变为“主动管理”。
*   **高效实现**：基于 vLLM 开发，利用 `cudaMemcpyBatchAsync` 等现代 CUDA API 最小化内核启动开销。

#### 8. 不足与局限
*   **硬件依赖**：层级重叠的效果依赖于计算延迟大于传输延迟，在极高带宽（如 NVLink）或极小模型上，优化收益可能收窄。
*   **预取窗口敏感性**：预取窗口的大小需要根据模型和负载进行调优，缺乏一种完全自动化的动态调整机制。
*   **应用限制**：仅适用于具有精确前缀重叠的场景，对于文档顺序变化或微小改动的输入，缓存复用率会迅速下降。

（完）
