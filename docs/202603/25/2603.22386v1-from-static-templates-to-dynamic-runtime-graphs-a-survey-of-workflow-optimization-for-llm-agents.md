---
title: "From Static Templates to Dynamic Runtime Graphs: A Survey of Workflow Optimization for LLM Agents"
title_zh: 从静态模板到动态运行时图：LLM 智能体工作流优化综述
authors: "Ling Yue, Kushal Raj Bhandari, Ching-Yun Ko, Dhaval Patel, Shuxin Lin, Nianjun Zhou, Jianxi Gao, Pin-Yu Chen, Shaowu Pan"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22386v1"
tags: ["query:ma-kf"]
score: 10.0
evidence: 大模型智能体工作流优化与代理计算图综述
tldr: 本综述探讨了基于大语言模型（LLM）智能体的计算图（ACG）工作流优化。文章将现有方法分为静态模板和动态运行时图两大类，并从结构确定时机、优化对象及评估信号三个维度对文献进行系统梳理。通过区分工作流模板、实例图与执行轨迹，本文提出了一个统一的框架和结构感知评估标准，旨在为LLM智能体工作流的设计与优化提供清晰的指导和可复现的评价体系。
selection_source: fresh_fetch
motivation: 随着LLM智能体系统日益复杂，亟需一套统一的框架和术语来分类、优化和评估这些由LLM调用、工具使用等构成的复杂工作流。
method: 本文提出了智能体计算图（ACG）的概念，并从结构确定时间、优化目标和反馈信号三个核心维度对现有工作流优化方法进行了系统性分类。
result: 明确区分了静态与动态工作流设计，并定义了模板、实例图与执行轨迹之间的差异，构建了一个涵盖成本、鲁棒性和结构变异性的多维度评估体系。
conclusion: 该综述为LLM智能体工作流优化提供了标准化的词汇表和评估框架，为未来开发更高效、可复现的智能体系统奠定了理论基础。
---

## 摘要
基于大语言模型（LLM）的系统正变得日益流行，它们通过构建可执行的工作流来解决任务，这些工作流交织了 LLM 调用、信息检索、工具使用、代码执行、内存更新和验证。本综述回顾了设计和优化此类工作流的最新方法，我们将这些工作流视为智能体计算图（Agentic Computation Graphs, ACGs）。我们根据工作流结构确定的时间点对文献进行组织，其中“结构”是指存在哪些组件或智能体、它们如何相互依赖以及信息如何在它们之间流动。这一视角区分了静态方法和动态方法：前者在部署前固定一个可重用的工作流脚手架，而后者在执行前或执行期间为特定运行选择、生成或修订工作流。我们进一步从三个维度对先前的工作进行组织：结构确定的时间、工作流的哪一部分被优化，以及哪些评估信号引导优化（例如，任务指标、验证器信号、偏好或基于轨迹的反馈）。我们还区分了可重用的工作流模板、特定运行的实例化图以及执行轨迹，将可重用的设计选择与特定运行中实际部署的结构以及实现的运行时行为分离开来。最后，我们概述了一种结构感知的评估视角，通过图级属性、执行成本、鲁棒性以及跨输入的结构变化来补充下游任务指标。我们的目标是为 LLM 智能体工作流优化领域的未来工作提供清晰的词汇表、定位新方法的统一框架、对现有文献更具可比性的视角以及更具可重复性的评估标准。

## Abstract
Large language model (LLM)-based systems are becoming increasingly popular for solving tasks by constructing executable workflows that interleave LLM calls, information retrieval, tool use, code execution, memory updates, and verification. This survey reviews recent methods for designing and optimizing such workflows, which we treat as agentic computation graphs (ACGs). We organize the literature based on when workflow structure is determined, where structure refers to which components or agents are present, how they depend on each other, and how information flows between them. This lens distinguishes static methods, which fix a reusable workflow scaffold before deployment, from dynamic methods, which select, generate, or revise the workflow for a particular run before or during execution. We further organize prior work along three dimensions: when structure is determined, what part of the workflow is optimized, and which evaluation signals guide optimization (e.g., task metrics, verifier signals, preferences, or trace-derived feedback). We also distinguish reusable workflow templates, run-specific realized graphs, and execution traces, separating reusable design choices from the structures actually deployed in a given run and from realized runtime behavior. Finally, we outline a structure-aware evaluation perspective that complements downstream task metrics with graph-level properties, execution cost, robustness, and structural variation across inputs. Our goal is to provide a clear vocabulary, a unified framework for positioning new methods, a more comparable view of existing body of literature, and a more reproducible evaluation standard for future work in workflow optimizations for LLM agents.

---

## 论文详细总结（自动生成）

### 论文总结：从静态模板到动态运行时图：LLM 智能体工作流优化综述

#### 1. 核心问题与研究动机
*   **核心问题**：随着大语言模型（LLM）从简单的聊天机器人演变为复杂的智能体系统，如何设计和优化由多个步骤（如工具调用、检索、代码执行、验证等）构成的“工作流结构”成为了提升系统性能的关键。
*   **研究动机**：目前智能体工作流的设计大多依赖手动编写或固定模板，缺乏统一的术语、分类框架和优化理论。论文旨在将工作流视为“智能体计算图”（Agentic Computation Graphs, ACG），并系统性地梳理如何通过自动化手段优化这些图的拓扑结构、节点参数及执行逻辑。

#### 2. 方法论：核心思想与技术细节
论文提出了一个统一的框架，从以下几个核心维度对工作流优化进行定义和分类：
*   **核心抽象（ACG）**：将工作流定义为由节点（执行动作）和边（数据/控制流）组成的图。区分了三个层次：
    *   **可重用模板 ($\bar{G}$)**：部署前定义的通用结构。
    *   **实例化图 ($G_{run}$)**：针对特定输入在运行时生成的具体结构。
    *   **执行轨迹 ($\tau$)**：运行过程中产生的实际动作、观测和成本序列。
*   **分类体系**：
    *   **静态优化（Static）**：在部署前固定工作流脚手架，通过离线搜索（如 MCTS、进化算法）优化节点提示词或固定拓扑。
    *   **动态优化（Dynamic）**：在推理时根据输入动态调整。包括：
        *   **选择/剪枝**：从超图中激活子图。
        *   **预执行生成**：在运行前为任务生成专属 DAG。
        *   **执行中编辑**：根据中间反馈实时修改图结构（如增加验证节点、重试逻辑）。
*   **优化目标函数**：通常表示为质量与成本的权衡：$\max E[R(\tau; x) - \lambda C(\tau)]$，其中 $R$ 是任务质量，$C$ 是执行成本（Token、时长、金钱），$\lambda$ 是平衡因子。
*   **反馈信号**：包括标量指标（成功率）、验证器信号（单元测试）、人类偏好以及基于轨迹的文本反馈。

#### 3. 实验设计与 Benchmark
作为一篇综述论文，其“实验设计”体现在对现有文献的系统性筛选和分类：
*   **文献范围**：共调研了 77 篇相关工作，包括 39 篇核心优化论文、7 篇相邻领域论文及 31 篇背景资源。
*   **评估资产**：整理了 27 个工作流相关的评估资源，分为：
    *   **工作流生成基准**：如 WorFBench, FlowBench。
    *   **交互式工具使用基准**：如 GAIA, $\tau$-Bench, MCP-Bench。
    *   **软件/操作环境**：如 SWE-bench, Terminal-Bench。
*   **对比维度**：通过“比较卡片”（Comparison Card）对比了各方法的结构确定时间（GDT）、塑性模式（GPM）、表示形式及更新机制。

#### 4. 资源与算力
*   **综述本身**：未涉及具体的模型训练，因此未消耗大规模算力。
*   **被调研文献**：论文指出，不同的优化方法算力需求差异巨大。例如，AFlow 和 ADAS 等离线搜索方法需要大量的评估运行（Search Budget）；而动态生成方法（如 WorkflowLLM）则侧重于微调（Fine-tuning）成本。论文强调，未来的研究应明确报告“离线优化成本”与“在线推理成本”。

#### 5. 实验数量与充分性
*   **调研充分性**：论文覆盖了从 2023 年到 2026 年（预印本时间）的最前沿工作，涵盖了学术界和工业界（如 IBM, RPI 等机构）的主流方法。
*   **客观性**：论文不仅总结了成功经验，还专门讨论了“单智能体基线”的强力竞争（如 OneFlow），客观地指出许多多智能体工作流的增益可能来自于更多的计算量而非结构本身。
*   **评估协议**：论文提出了一个“最小报告协议”，要求未来实验必须包含 Token 消耗、成功成本比、结构变异性等指标，以确保实验的公平性和可复现性。

#### 6. 主要结论与发现
*   **结构胜过提示词**：当任务失败源于逻辑缺失（如缺少验证）时，优化图结构比单纯调优提示词（Prompt Tuning）更有效。
*   **静态 vs 动态的权衡**：静态模板适用于任务重复性高、验证信号强的场景；动态编辑适用于环境不确定性大、需要实时纠错的复杂任务。
*   **联合优化的必要性**：节点参数（提示词）与全局拓扑具有耦合性，交替优化两者通常能获得最佳效果。
*   **验证器的价值**：在工作流中合理放置廉价的验证器（如格式检查、单元测试）是提升性价比的核心手段。

#### 7. 优点
*   **术语统一**：首次提出了“智能体计算图（ACG）”和“图塑性模式（GPM）”等概念，解决了领域内术语混乱的问题。
*   **分类严谨**：通过 GDT（确定时间）和 GPM（塑性）两个维度，清晰地界定了静态与动态方法的边界。
*   **实战指导**：提供了“实践配方”，指导开发者根据任务异质性选择合适的优化路径（先静态，再剪枝，最后考虑动态生成）。

#### 8. 不足与局限
*   **结构归因困难**：目前仍难以精确量化工作流中某一个特定节点或边对最终成功的贡献度（Credit Assignment）。
*   **验证器依赖**：许多优化方法高度依赖高质量的自动验证器，在缺乏客观评价标准的开放域任务中难以应用。
*   **环境漂移**：现有研究较少关注工具 API 变更或环境漂移对固定工作流模板的长期影响。
*   **理论缺失**：目前该领域仍缺乏关于“结构复杂性”与“样本效率”之间关系的严谨理论支撑。

（完）
