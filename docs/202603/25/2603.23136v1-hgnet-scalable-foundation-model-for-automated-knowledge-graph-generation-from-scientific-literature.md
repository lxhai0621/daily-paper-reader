---
title: "HGNet: Scalable Foundation Model for Automated Knowledge Graph Generation from Scientific Literature"
title_zh: HGNet：用于从科学文献中自动化生成知识图谱的可扩展基础模型
authors: "Devvrat Joshi, Islem Rekik"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23136v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 从科学文献中自动生成知识图谱
tldr: 针对科学文献知识图谱构建中长实体识别难、跨领域泛化差及忽视层级结构的问题，本文提出两阶段框架。第一阶段Z-NERD通过正交语义分解和多尺度注意力机制实现零样本实体识别；第二阶段HGNet利用层级感知消息传递和连续抽象场损失进行关系抽取。该方法在多个基准上达到SOTA，显著提升了零样本性能。
selection_source: fresh_fetch
motivation: 现有科学知识图谱构建方法难以处理长实体和复杂的层级关系，且在跨领域应用时泛化能力不足。
method: 提出包含Z-NERD实体识别和HGNet层级关系抽取的两阶段框架，并引入连续抽象场（CAF）损失来建模层级深度。
result: "在SciERC和SPHERE等数据集上刷新纪录，尤其在零样本场景下，实体识别和关系抽取性能分别提升了10.76%和26.2%。"
conclusion: 该研究通过形式化层级抽象并结合正交语义分解，为可扩展且一致的科学知识图谱自动化构建提供了高效的解决方案。
---

## 摘要
自动化知识图谱（KG）构建对于在快速增长的科学文献库中进行导航至关重要。然而，现有方法在识别长多词实体方面存在困难，往往难以在不同领域间泛化，且通常忽略了科学知识的层级特性。虽然通用大语言模型（LLM）具有适应性，但其计算成本高昂，且在专业任务上的准确性表现不一。结果导致当前的知识图谱较为浅显且不一致，限制了其在探索和综合方面的效用。我们提出了一个用于可扩展、零样本科学知识图谱构建的两阶段框架。第一阶段 Z-NERD 引入了：(i) 正交语义分解（OSD），通过隔离文本中的语义“转折”来促进领域无关的实体识别；(ii) 多尺度 TCQK 注意力机制，通过感知 n-gram 的注意力头来捕捉连贯的多词实体。第二阶段 HGNet 通过层级感知的消息传递进行关系抽取，显式地对父、子和同级关系进行建模。为了增强全局一致性，我们引入了两个互补的目标：可微层级损失（Differentiable Hierarchy Loss），用于抑制环路和快捷边；以及连续抽象场（CAF）损失，将抽象级别嵌入到欧几里得空间中一条可学习的轴上。这是首个将层级抽象形式化为标准欧几里得嵌入中连续属性的方法，为双曲几何方法提供了一个更简单的替代方案。我们发布了 SPHERE，这是一个用于层级关系抽取的跨领域基准测试集。我们的框架在 SciERC、SciER 和 SPHERE 上达到了新的 SOTA 水平，在分布外测试中将命名实体识别（NER）提升了 8.08%，关系抽取（RE）提升了 5.99%。在零样本设置下，NER 的增益达到 10.76%，RE 的增益达到 26.2%。

## Abstract
Automated knowledge graph (KG) construction is essential for navigating the rapidly expanding body of scientific literature. However, existing approaches struggle to recognize long multi-word entities, often fail to generalize across domains, and typically overlook the hierarchical nature of scientific knowledge. While general-purpose large language models (LLMs) offer adaptability, they are computationally expensive and yield inconsistent accuracy on specialized tasks. As a result, current KGs are shallow and inconsistent, limiting their utility for exploration and synthesis. We propose a two-stage framework for scalable, zero-shot scientific KG construction. The first stage, Z-NERD, introduces (i) Orthogonal Semantic Decomposition (OSD), which promotes domain-agnostic entity recognition by isolating semantic "turns" in text, and (ii) a Multi-Scale TCQK attention mechanism that captures coherent multi-word entities through n-gram-aware attention heads. The second stage, HGNet, performs relation extraction with hierarchy-aware message passing, explicitly modeling parent, child, and peer relations. To enforce global consistency, we introduce two complementary objectives: a Differentiable Hierarchy Loss to discourage cycles and shortcut edges, and a Continuum Abstraction Field (CAF) Loss that embeds abstraction levels along a learnable axis in Euclidean space. This is the first approach to formalize hierarchical abstraction as a continuous property within standard Euclidean embeddings, offering a simpler alternative to hyperbolic methods. We release SPHERE (https://github.com/basiralab/SPHERE), a multi-domain benchmark for hierarchical relation extraction. Our framework establishes a new state of the art on SciERC, SciER, and SPHERE, improving NER by 8.08% and RE by 5.99% on out-of-distribution tests. In zero-shot settings, gains reach 10.76% for NER and 26.2% for RE.

---

## 论文详细总结（自动生成）

### 论文总结：HGNet —— 用于科学文献自动化知识图谱生成的扩展基础模型

#### 1. 核心问题与研究动机
科学文献的指数级增长使得人工综述变得不可能。自动化构建知识图谱（KG）是解决之道，但现有方法面临四大挑战：
*   **长多词实体识别难**：如“in situ transmission electron microscopy”等复杂术语难以完整识别。
*   **领域泛化能力差**：在某一学科训练的模型难以直接应用于新领域（零样本能力弱）。
*   **忽视层级结构**：现有模型多为“层级盲”，无法区分科学概念间的父子或同级关系。
*   **逻辑不一致性**：生成的图谱往往包含环路（A包含B，B又包含A）或错误的快捷边。
*   **LLM 局限性**：通用大模型（如 GPT-4）虽然具备泛化性，但计算成本极高且在专业领域表现不稳定。

#### 2. 方法论：两阶段框架
论文提出了一个轻量级（约 3 亿参数）的框架，分为两个阶段：

**第一阶段：Z-NERD（零样本实体识别）**
*   **正交语义分解 (OSD)**：将词嵌入的变化向量分解为“持续成分”（解释性）和“发散成分”（新概念引入）。通过捕捉这种领域无关的“语义转折”信号，增强模型的跨领域泛化能力。
*   **多尺度 TCQK 注意力**：在计算注意力前，对 Query (Q) 和 Key (K) 进行不同尺度的 1D 卷积（如 1, 3, 5, 7），使不同的注意力头专门负责捕捉不同长度的 n-gram 模式，从而识别长实体。

**第二阶段：HGNet（层级图网络）**
*   **概率层级消息传递**：构建一个潜在的概率图，并设计三个并行通道（父到子、子到父、同级到同级）进行消息传递，以保留层级信息的流向。
*   **可微层级损失 (DHL)**：利用矩阵指数的迹（Trace）来确保图结构是一个有向无环图（DAG），并惩罚跳过中间层级的“快捷边”。
*   **连续抽象场 (CAF) 损失**：在欧几里得空间中学习一个“抽象场向量”，将所有实体投影到该轴上得到连续的抽象分值。这为层级深度提供了几何解释，是双曲嵌入的一种更简单、高效的替代方案。

#### 3. 实验设计
*   **数据集**：
    *   **标准基准**：SciERC、SciER、BioRED、SemEval-2017 Task 10。
    *   **新发布数据集 SPHERE**：包含 10,000 篇文档、111,000 条关系，涵盖计算机科学、物理、生物和材料科学四个领域。
*   **评估指标**：NER 使用微观 F1，关系抽取使用严格的 Rel+ F1（要求实体边界、类型及关系类型全部正确）。
*   **对比方法**：
    *   **监督模型**：SciBERT、PL-Marker、HGERE、PURE。
    *   **零样本/LLM 模型**：UniversalNER-7b、Llama-3.3-70b、Qwen2.5-32b、GPT-3.5 等。
    *   **图网络基准**：GCN、GAT。

#### 4. 资源与算力
*   **硬件**：实验在配备 **NVIDIA A30 (24GB)** GPU 的高性能计算集群上运行。
*   **软件/优化**：使用 PyTorch 2.1 和 Hugging Face Transformers。采用 AdamW 优化器和线性学习率调度。
*   **效率**：模型参数量约为 2.93 亿（~300M），远小于 7B 以上的 LLM，推理速度在 A30 上可达 14.6 doc/s。

#### 5. 实验数量与充分性
*   **实验规模**：在 5 个不同领域的数据集上进行了测试，涵盖了监督学习和零样本学习两种设置。
*   **消融实验**：针对 Z-NERD 的 TCQK 和 OSD 模块，以及 HGNet 的 DHL 和 CAF 损失分别进行了消融，验证了每个组件的必要性。
*   **客观性**：对比了从轻量级监督模型到超大规模 LLM 的多种基准，并使用了官方的分布外（OOD）测试集，实验设计较为公平、全面。

#### 6. 主要结论与发现
*   **性能突破**：在 NER 上平均提升 8.08%，在 RE 上平均提升 5.99%。
*   **零样本优势**：在零样本设置下，NER 提升 10.76%，RE 提升高达 **26.2%**，证明了 OSD 和层级建模在跨领域迁移中的强大作用。
*   **几何有效性**：证明了在欧几里得空间中通过“抽象轴”建模层级结构是可行的，且比复杂的双曲几何方法更易训练且效果更好。

#### 7. 优点与亮点
*   **创新几何视角**：首次将层级抽象形式化为欧几里得空间中的连续几何属性（CAF）。
*   **轻量且高效**：以 300M 的参数量在特定任务上击败了参数量大几十倍的 LLM。
*   **贡献数据集**：发布的 SPHERE 数据集填补了大规模、多领域层级关系抽取数据的空白。
*   **逻辑严密**：通过 DHL 损失从数学上保证了知识图谱的逻辑一致性（无环性）。

#### 8. 不足与局限
*   **数据生成偏差**：SPHERE 数据集是利用 LLM 生成并自标注的，虽然经过了人工审计，但可能仍带有生成模型的固有偏差。
*   **计算复杂度**：虽然引入了 Krylov 子空间方法加速，但 DHL 损失中的矩阵指数计算在极大规模节点（数万个）时仍可能面临挑战。
*   **信息源限制**：目前主要处理文本信息，尚未整合科学文献中常见的图表、公式等模态数据。
*   **环路处理**：在处理某些罕见的“循环引用”概念时，强制 DAG 约束可能会导致部分语义关联的丢失。

（完）
