---
title: "When Models Judge Themselves: Unsupervised Self-Evolution for Multimodal Reasoning"
title_zh: 当模型自我评判时：多模态推理的无监督自我进化
authors: "Zhengxian Wu, Kai Shi, Chuanrui Zhang, Zirui Liao, Jun Yang, Ni Yang, Qiuying Peng, Luyuan Zhang, Hangrui Xu, Tianhuang Su, Zhenyu Yang, Haonan Lu, Haoqian Wang"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21289v2"
tags: ["query:mmkqa"]
score: 9.0
evidence: 多模态大语言模型推理的无监督自我演化
tldr: 针对多模态大模型推理任务对高质量标注数据和教师模型蒸馏的依赖问题，本文提出一种无监督自进化训练框架。该框架无需人工标注或外部奖励模型，通过对同一输入的多个推理路径进行采样，结合自一致性信号和模型自身的判断力进行动态加权，并利用组相对策略优化（GRPO）在无标签数据上实现性能提升。实验证明，该方法在五个数学推理基准测试中显著增强了模型的推理能力和泛化性，为多模态模型的规模化自进化提供了新路径。
selection_source: fresh_fetch
motivation: 现有多模态推理模型高度依赖昂贵的人工标注或教师模型蒸馏，限制了其规模化扩展。
method: 提出一种基于自一致性信号和模型自我判断调制的无监督框架，利用组相对策略优化（GRPO）在无标签数据上进行策略更新。
result: 在五个数学推理基准测试中，该方法在不使用外部监督的情况下持续提升了模型的推理性能和泛化能力。
conclusion: 该研究证明了多模态模型可以通过无监督的自我进化实现性能突破，为构建可扩展的自进化智能体提供了有效方案。
---

## 摘要
多模态大语言模型的最新进展在推理任务上取得了强劲表现，但这些提升很大程度上依赖于高质量的标注数据或教师模型蒸馏，这两者都成本高昂且难以扩展。为了解决这一问题，我们提出了一种用于多模态推理的无监督自我进化训练框架，在不使用人工标注答案或外部奖励模型的情况下实现了稳定的性能提升。对于每个输入，我们采样多条推理轨迹，并对它们的组内结构进行联合建模。我们利用 Actor 的自一致性信号作为训练先验，并引入一种基于有界评判器（Judge）的调制机制，以持续对不同质量的轨迹进行重新加权。我们进一步将调制后的分数建模为组级分布，并将绝对分数转换为每组内的相对优势，从而实现更稳健的策略更新。通过在无标签数据上使用组相对策略优化（GRPO）进行训练，我们的方法在五个数学推理基准测试中持续提升了推理性能和泛化能力，为实现可扩展的自我进化多模态模型提供了一条路径。代码可在 https://github.com/OPPO-Mente-Lab/LLM-Self-Judge 获取。

## Abstract
Recent progress in multimodal large language models has led to strong performance on reasoning tasks, but these improvements largely rely on high-quality annotated data or teacher-model distillation, both of which are costly and difficult to scale. To address this, we propose an unsupervised self-evolution training framework for multimodal reasoning that achieves stable performance improvements without using human-annotated answers or external reward models. For each input, we sample multiple reasoning trajectories and jointly model their within group structure. We use the Actor's self-consistency signal as a training prior, and introduce a bounded Judge based modulation to continuously reweight trajectories of different quality. We further model the modulated scores as a group level distribution and convert absolute scores into relative advantages within each group, enabling more robust policy updates. Trained with Group Relative Policy Optimization (GRPO) on unlabeled data, our method consistently improves reasoning performance and generalization on five mathematical reasoning benchmarks, offering a scalable path toward self-evolving multimodal models. The code are available at https://github.com/OPPO-Mente-Lab/LLM-Self-Judge.

---

## 论文详细总结（自动生成）

以下是对论文《When Models Judge Themselves: Unsupervised Self-Evolution for Multimodal Reasoning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：多模态大语言模型（MLLM）在推理任务上的进步高度依赖高质量标注数据或强力教师模型的蒸馏。然而，人工标注成本极高且难以规模化，而现有评估器的能力也逐渐达到瓶颈。
*   **核心问题**：如何在**完全无监督**（无人工标注、无外部奖励模型、无教师模型）的情况下，利用模型自身生成的信号实现持续的自我进化？
*   **挑战**：现有的无监督方法（如“多数投票”机制）容易强化模型的系统性偏差（即模型可能一致地给出错误答案），导致训练不稳定、策略过早收敛或输出分布坍缩。

### 2. 方法论：核心思想与关键技术
该论文提出了一种基于 **Actor-Judge（演员-评判者）** 联合建模的无监督自我进化框架：
*   **角色分工**：从同一个多模态模型中实例化出两个角色。**Actor** 负责针对输入生成多个推理轨迹；**Judge**（Actor 的冻结副本）负责评估这些轨迹的质量。
*   **自一致性初始奖励（SC Reward）**：Actor 对同一问题进行 $n$ 次采样，计算每个答案出现的频率。与传统的二值化“多数投票”不同，该方法保留了答案分布的软信息，作为训练先验。
*   **有界评判器调制（Judge Modulation）**：Judge 从答案正确性、推理质量和视觉依据三个维度打分。为了防止 Judge 的噪声干扰训练，引入了一个**有界且连续可微的校准函数 $g(s)$**（基于 Sigmoid 构造），对初始奖励进行微调。
*   **组相对分布建模（Group-wise Distributional Modeling）**：
    *   采用 **GRPO（组相对策略优化）** 算法。
    *   将调制后的分数转化为组内的**相对优势（Relative Advantage）**。
    *   通过能量分布归一化，将优化目标从“追求绝对高分”转变为“在组内重新分配概率质量”，有效防止了模型向单一错误模式坍缩。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：Geometry3k、GeoQA、MMR1（均去除标签，仅使用图像和问题）。
    *   **评估基准（Benchmarks）**：MathVision、MathVerse、WeMath、LogicVista、DynaMath（涵盖几何、代数、逻辑推理等）。
*   **对比方法**：
    *   **基座模型**：Qwen2.5-VL-7B-Instruct。
    *   **无监督方法**：VisionZero、EvoLMM、MM-UPT（基于多数投票）。
    *   **监督/蒸馏方法**：Vision-R1、R1-Onevision 等。
*   **泛化性测试**：在非数学任务（ChartQA、MMVP）上验证通用视觉推理能力。

### 4. 资源与算力
*   **硬件环境**：使用了 1 个节点，配备 **8 张 NVIDIA A800 GPU (80GB)**。
*   **训练细节**：训练周期为 20 个 epoch。
*   **训练时长**：文中提到其训练成本约为监督 GRPO 的 **1.4 倍**（监督 GRPO 耗时约 10.5 小时，推算本项目约需 **14.7 小时**）。这种额外的开销主要源于在线采样和 Judge 评分过程。

### 5. 实验数量与充分性
*   **实验规模**：论文在 5 个主流数学推理基准上进行了全面测试，并针对不同规模的基座模型（从 2B 到 32B，包括 InternVL3、GLM-4.1V 等）验证了方法的普适性。
*   **消融实验**：详细对比了“仅自一致性”、“仅 Judge 评分”、“多数投票”以及“分布建模”对性能的影响。
*   **充分性评价**：实验设计非常充分且客观。作者不仅展示了准确率的提升，还分析了训练过程中的熵变化、响应长度演变以及 Pass@10 指标，证明了训练的稳定性和有效性。

### 6. 主要结论与发现
*   **性能突破**：在无监督条件下，该方法在 MathVision 上实现了 **+5.9%** 的绝对准确率提升（从 25.0% 提升至 30.9%），表现接近甚至超过了某些依赖教师蒸馏的方法。
*   **稳定性**：相比多数投票机制，该框架能维持更健康的策略熵和响应长度，避免了模型过快陷入低熵的确定性错误。
*   **协同效应**：自一致性提供了稳定的分布先验，而 Judge 调制在模糊情况下引导模型向高质量轨迹靠拢，两者结合形成了正向反馈。

### 7. 优点与亮点
*   **完全脱离外部监督**：实现了真正的“闭环自我进化”，不依赖昂贵的外部奖励模型或人工标注。
*   **有界调制设计**：巧妙地解决了 Judge 自身可能存在的偏差问题，通过有界函数限制了错误信号的放大。
*   **分布匹配视角**：将强化学习优化解释为分布匹配过程，为理解无监督自进化提供了理论支撑。

### 8. 不足与局限
*   **“错误共识”风险**：如果 Actor 的采样分布严重偏向错误答案，且 Judge 也给该错误答案打高分，模型仍会强化错误学习（论文在 Case Study 中承认了这一点）。
*   **Judge 能力上限**：系统的进化上限受限于初始 Judge 的潜在能力。如何让 Judge 随着 Actor 的进步而同步进化（即“评判者的自我进化”）仍是未来待解决的难题。
*   **采样多样性下降**：虽然准确率提升，但在某些情况下 Pass@10 指标有所下降，表明模型输出分布变得更加集中，可能牺牲了一定的探索多样性。

（完）
