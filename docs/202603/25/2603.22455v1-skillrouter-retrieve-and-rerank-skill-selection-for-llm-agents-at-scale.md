---
title: "SkillRouter: Retrieve-and-Rerank Skill Selection for LLM Agents at Scale"
title_zh: SkillRouter：面向大规模 LLM 智能体的检索-重排序技能选择
authors: "YanZhao Zheng, ZhenTao Zhang, Chao Ma, YuanQiang Yu, JiHuan Zhu, Baohua Dong, Hangcheng Zhu"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22455v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 从大规模技能库中为大模型智能体检索相关技能
tldr: "随着LLM智能体技能库规模激增，如何从海量功能重叠的技能中精准选择成为挑战。本文通过实证研究发现，技能的完整实现代码（Body）是路由选择的关键信号，仅依赖元数据会导致性能大幅下降。据此，研究者提出了SkillRouter，一个仅1.2B参数的轻量级检索-重排序流水线，在8万规模的技能库上实现了74%的Top-1准确率，且支持消费级硬件部署。"
selection_source: fresh_fetch
motivation: 现有智能体架构通常只向模型暴露技能名称和描述，难以在海量且功能重叠的技能库中实现精准路由。
method: 提出SkillRouter两阶段流水线，结合0.6B编码器和0.6B重排序器，重点利用技能的完整实现代码进行特征提取与匹配。
result: "在包含8万个技能的基准测试中，SkillRouter达到了74.0%的Top-1路由准确率，在轻量级模型中表现最优。"
conclusion: 技能实现细节是路由决策的核心信号，通过轻量化的检索-重排序架构即可在大规模场景下实现高效且精准的技能选择。
---

## 摘要
随着 LLM 智能体生态系统的发展，可用技能（工具、插件）的数量已达到数万个，这使得将所有技能注入智能体的上下文变得不可行。这产生了一种对技能路由（skill routing）的需求，即根据用户任务从庞大的资源池中检索最相关的技能。社区技能库中普遍存在的功能重叠使这一问题更加复杂，许多技能具有相似的名称和用途，但在实现细节上有所不同。尽管具有实际重要性，但技能路由仍未得到充分探索。目前的智能体架构采用渐进式披露设计——仅向智能体公开技能名称和描述，而隐藏完整的实现主体（implementation body）——这隐含地认为元数据足以进行选择。我们通过对包含约 8 万个技能和 75 个专家验证查询的基准测试进行系统实证研究，挑战了这一假设。我们的关键发现是，技能主体（完整实现文本）是决定性的信号：移除它会导致所有检索方法的性能下降 29-44 个百分点，且交叉编码器注意力分析显示 91.7% 的注意力集中在主体字段。受此发现启发，我们提出了 SkillRouter，这是一个总参数量仅为 12 亿（0.6B 编码器 + 0.6B 重排序器）的两阶段检索-重排序流水线。SkillRouter 实现了 74.0% 的 Top-1 路由准确率，并在我们评估的紧凑型和零样本基准模型中取得了最强的平均结果，同时仍可部署在消费级硬件上。

## Abstract
As LLM agent ecosystems grow, the number of available skills (tools, plugins) has reached tens of thousands, making it infeasible to inject all skills into an agent's context. This creates a need for skill routing -- retrieving the most relevant skills from a large pool given a user task. The problem is compounded by pervasive functional overlap in community skill repositories, where many skills share similar names and purposes yet differ in implementation details. Despite its practical importance, skill routing remains under-explored. Current agent architectures adopt a progressive disclosure design -- exposing only skill names and descriptions to the agent while keeping the full implementation body hidden -- implicitly treating metadata as sufficient for selection. We challenge this assumption through a systematic empirical study on a benchmark of ~$80K skills and 75 expert-verified queries. Our key finding is that the skill body (full implementation text) is the decisive signal: removing it causes 29--44 percentage point degradation across all retrieval methods, and cross-encoder attention analysis reveals 91.7% of attention concentrating on the body field. Motivated by this finding, we propose SkillRouter, a two-stage retrieve-and-rerank pipeline totaling only 1.2B parameters (0.6B encoder + 0.6B reranker). SkillRouter achieves 74.0% top-1 routing accuracy and delivers the strongest average result among the compact and zero-shot baselines we evaluate, while remaining deployable on consumer hardware.

---

## 论文详细总结（自动生成）

这是一份关于论文《SkillRouter: Retrieve-and-Rerank Skill Selection for LLM Agents at Scale》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）智能体生态的扩张，可用的技能（工具、插件）已达数万个。由于上下文窗口限制和推理成本，无法将所有技能注入模型。因此，**技能路由（Skill Routing）**——即根据用户任务从海量池中检索最相关技能——成为关键。
*   **研究动机**：现有架构通常只向智能体展示技能的名称和描述（元数据），而隐藏具体的实现代码（Body）。论文挑战了“仅靠元数据足以进行技能选择”的假设，旨在解决大规模、高功能重叠技能库中的精准检索问题。

### 2. 方法论：核心思想与技术细节
论文提出了 **SkillRouter**，一个两阶段的“检索-重排序”流水线：
*   **核心思想**：强调**技能主体（Skill Body，即实现代码）**在选择中的决定性作用，并在检索和重排序阶段均引入完整文本。
*   **第一阶段：双编码器检索（SR-Emb-0.6B）**
    *   使用 Qwen3-Emb-0.6B 作为基础，通过对比学习训练。
    *   **负采样策略**：结合语义负样本、词法负样本（BM25）、分类负样本和随机负样本。
    *   **假负例过滤**：通过名称去重、三元组 Jaccard 相似度和嵌入相似度三层过滤，剔除功能重复的技能。
*   **第二阶段：交叉编码器重排序（SR-Rank-0.6B）**
    *   输入格式：将查询与候选技能的名称+描述+主体拼接。
    *   **关键技术：列表损失函数（Listwise Cross-Entropy）**。相比点对点（Pointwise）评分，列表损失强制模型在同质化候选集中进行对比，显著提升了区分度。

### 3. 实验设计
*   **数据集/基准测试**：构建了一个包含 **~80,000 个技能** 的大规模池，结合了 SkillBench 的专家验证数据。
*   **场景设置**：
    *   **Easy 级别**：标准技能池。
    *   **Hard 级别**：额外加入 780 个由 LLM 生成的“干扰项”技能（语义相关但功能不同）。
*   **对比方法**：
    *   **稀疏检索**：BM25。
    *   **稠密编码器**：E5-Large、BGE-Large、GTE-Large、NV-Embed-v2、OpenAI text-embedding-3-large 等。
    *   **重排序器**：Qwen3-Rank (0.6B/8B) 零样本、GPT-4o-mini/GPT-5.4-mini 作为评判者。

### 4. 资源与算力
*   **模型规模**：主推版本为 **1.2B 参数**（0.6B 编码器 + 0.6B 重排序器），另有 8B 扩展版本。
*   **训练细节**：在**单张 GPU** 上训练（未指明具体型号，如 A100 或 H100）。
*   **配置**：Batch Size 为 8，梯度累积 4（有效 BS=32），使用 BF16 混合精度和梯度检查点技术，训练 1 个 Epoch。
*   **部署目标**：设计初衷是支持在消费级硬件（如笔记本 CPU）上本地运行。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 75 个核心查询（24 个单技能，51 个多技能），在 8 万规模的技能池上进行测试。
*   **实验维度**：包括检索准确率对比、重排序增益分析、消融实验（针对 Body 字段、过滤机制、损失函数）、注意力权重分析以及案例研究。
*   **充分性与公平性**：对比了从 300M 到 8B 的多种开源模型及闭源 API，实验设计较为严谨。通过 Easy/Hard 两级难度测试，验证了模型在面对极具迷惑性干扰项时的鲁棒性。

### 6. 主要结论与发现
*   **Body 是核心信号**：移除技能实现代码（Body）会导致性能下降 **29-44%**。交叉编码器的注意力权重显示，**91.7% 的注意力集中在 Body 字段**。
*   **小模型胜过大模型**：经过微调的 1.2B SkillRouter 在 Top-1 准确率（74.0%）上超过了 8B 规模的零样本基准模型。
*   **列表损失至关重要**：在重排序阶段，使用 Listwise 损失比 Pointwise 损失在 Hit@1 指标上高出 **30.7%**。
*   **假负例过滤的必要性**：在同质化严重的社区技能库中，过滤功能重复的假负例可提升 4.0% 的准确率。

### 7. 优点与亮点
*   **揭示了关键偏见**：有力地反驳了“技能元数据足以进行路由”的行业惯例，为智能体架构设计提供了新准则。
*   **高效且实用**：1.2B 的参数量极具竞争力，兼顾了高性能与低部署门槛（支持端侧部署）。
*   **训练配方扎实**：详细探讨了负采样、过滤和损失函数对技能路由这一特殊任务的影响，具有很强的工程参考价值。

### 8. 不足与局限
*   **查询规模较小**：虽然技能池很大，但评估用的专家验证查询仅 75 个，统计效力可能受限。
*   **推理能力天花板**：约 22% 的失败案例源于需要多步推理的复杂任务（如从发票图像到欺诈检测的间接关联），单纯依靠检索难以解决。
*   **基准单一**：主要基于 SkillBench 构建，尚未在其他多样化的智能体技能生态（如 ToolBench）中广泛验证泛化性。

（完）
