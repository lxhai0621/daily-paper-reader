---
title: "Towards Intelligent Geospatial Data Discovery: a knowledge graph-driven multi-agent framework powered by large language models"
title_zh: 迈向智能地理空间数据发现：一种由大语言模型驱动的知识图谱驱动多智能体框架
authors: "Ruixiang Liu, Zhenlong Li, Ali Khosravi Kazazi"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20670v2"
tags: ["query:ma-kf"]
score: 9.0
evidence: 知识图谱驱动的多智能体数据发现框架
tldr: 针对地理空间数据发现中存在的语义不一致和关键词搜索局限性，本研究提出了一种由大语言模型驱动、知识图谱引导的多智能体框架。该框架通过统一本体对异构元数据进行语义对齐，并构建知识图谱建模数据集关系，利用多智能体协作实现意图解析与检索。实验证明该方法显著提升了检索准确率和透明度，为下一代智能空间数据基础设施和自主GIS奠定了基础。
selection_source: fresh_fetch
motivation: 传统的地理空间数据目录依赖关键词搜索，难以处理海量异构数据中的语义一致性问题及用户真实意图。
method: 提出一种结合统一元数据本体、知识图谱与大语言模型多智能体协作的框架，实现从意图解析到结果合成的闭环检索。
result: 实验结果显示，该框架在检索意图匹配准确度、排序质量、召回率以及过程透明度方面均优于传统系统。
conclusion: 该研究推动了地理空间数据发现向语义化和智能化范式转变，为实现自主GIS提供了关键的技术支撑。
---

## 摘要
地理空间数据在规模、多样性和增长速度上的快速增长，形成了高度分布式、异构且语义不一致的数据生态系统。现有的数据目录、门户和基础设施仍主要依赖于语义支持有限的关键词搜索，这往往无法捕捉用户意图，导致检索性能较弱。为了应对这些挑战，本研究提出了一种由大语言模型驱动的知识图谱驱动多智能体框架，用于智能地理空间数据发现。该框架引入了统一的地理空间元数据本体作为语义中介层，以对齐跨平台的异构元数据标准，并构建了地理空间元数据知识图谱，对数据集及其多维关系进行显式建模。基于结构化表示，该框架采用多智能体协作架构来执行意图解析、知识图谱检索和答案合成，形成了从用户查询到结果的可解释且闭环的发现过程。代表性用例和性能评估的结果表明，与传统系统相比，该框架显著提高了意图匹配准确度、排序质量、召回率和发现透明度。本研究将地理空间数据发现推向了更加语义化、意图感知和智能化的范式，为下一代智能自主空间数据基础设施提供了实践基础，并为自主 GIS（Autonomous GIS）的宏伟愿景做出了贡献。

## Abstract
The rapid growth in the volume, variety, and velocity of geospatial data has created data ecosystems that are highly distributed, heterogeneous, and semantically inconsistent. Existing data catalogs, portals, and infrastructures still rely largely on keyword-based search with limited semantic support, which often fails to capture user intent and leads to weak retrieval performance. To address these challenges, this study proposes a knowledge graph-driven multi-agent framework for intelligent geospatial data discovery, powered by large language models. The framework introduces a unified geospatial metadata ontology as a semantic mediation layer to align heterogeneous metadata standards across platforms and constructs a geospatial metadata knowledge graph to explicitly model datasets and their multidimensional relationships. Building on the structured representation, the framework adopts a multi-agent collaborative architecture to perform intent parsing, knowledge graph retrieval, and answer synthesis, forming an interpretable and closed-loop discovery process from user queries to results. Results from representative use cases and performance evaluation show that the framework substantially improves intent matching accuracy, ranking quality, recall, and discovery transparency compared with traditional systems. This study advances geospatial data discovery toward a more semantic, intent-aware, and intelligent paradigm, providing a practical foundation for next-generation intelligent and autonomous spatial data infrastructures and contributing to the broader vision of Autonomous GIS.

---

## 论文详细总结（自动生成）

### 论文结构化深度总结：迈向智能地理空间数据发现

#### 1. 核心问题与整体含义
*   **研究动机**：在地理大数据时代，地理空间数据呈现出高增长、高异构性和语义不一致的特征。现有的数据门户（如 Data.gov）主要依赖关键词匹配，难以理解用户的复杂语义意图（如隐含的空间范围、时间约束或特定主题），导致检索效率低下，用户往往需要反复尝试关键词才能找到所需数据。
*   **核心目标**：构建一个能够理解自然语言意图、实现跨平台语义对齐、并提供可解释检索结果的智能地理空间数据发现框架，作为“自主 GIS”（Autonomous GIS）的关键组成部分。

#### 2. 方法论
该框架由三个核心组件构成：**地理空间元数据知识图谱**、**多智能体协作流水线**以及**编排与存储模块**。
*   **统一本体设计**：设计了一个语义中介层，将 STAC、FGDC CSDGM 和 CKAN 等异构标准映射到统一本体（包含数据集、主题、空间、时间、组织、格式、许可等实体及相互关系）。
*   **知识图谱构建**：通过元数据摄取、文本嵌入（Embedding）、LLM 辅助的主题生成与治理、以及时空信息增强，构建了大规模结构化图谱。
*   **多智能体协作流程**：
    *   **意图解析智能体 (Intent Parsing Agent)**：将自然语言转换为结构化意图（GeoIntent）。引入置信度评估公式 $Conf(I) = \alpha \cdot A(I) + \beta \cdot P(I)$，若置信度低则触发“人机回环”澄清。
    *   **图检索智能体 (Graph Retrieval Agent)**：执行向量相似度匹配、硬性约束过滤（空间相交、时间重叠）和多维相关性评分。评分采用加权线性聚合模型，并结合 F1 分数衡量时空覆盖度。
    *   **答案合成智能体 (Answer Synthesis Agent)**：利用 LLM 对 Top-K 结果进行重排序（Reranking），生成匹配理由，并提取知识图谱子图以增强透明度。

#### 3. 实验设计
*   **数据集/场景**：整合了 6 个主流数据目录（包括 Data.gov、PASDA 及 4 个 STAC 节点），共包含 **264,177 个数据集**，构建了拥有约 280 万实体和 1223 万关系的知识图谱。
*   **Benchmark**：构建了包含 **100 个测试查询** 的基准集，按复杂度分为四级（从单一主题到包含时空、组织、格式等多重约束的复杂查询）。
*   **对比方法**：
    1.  **Data.gov 原生搜索**（基准）。
    2.  **IGDD-G**（仅包含意图解析和图检索的消融版本）。
    3.  **IGDD-G&A**（完整框架，包含答案合成重排序）。
*   **评估指标**：意图匹配率 (EIMR)、归一化折损累计增益 (NDCG@10) 和召回率 (Recall@20)。

#### 4. 资源与算力
*   **模型使用**：底层 LLM 采用了 OpenAI 的 **GPT-5.2 API**（注：原文如此，可能指代其内部测试版本或未来版本），文本嵌入使用了 `text-embedding-3-large`。
*   **算力环境**：知识图谱部署在高性能服务器上的 **Neo4j** 数据库中。
*   **消耗统计**：单次完整查询平均耗时 **30-40 秒**，消耗约 **11,000 个 token**。文中未明确提及具体的 GPU 型号或训练时长，因为主要基于现成的 API 进行推理和图数据库操作。

#### 5. 实验数量与充分性
*   **实验规模**：针对 100 个查询进行了相关性池化（Pooling），共人工标注了 **3,646 个数据集** 的相关性得分（0-3 级）。
*   **充分性**：实验设计较为充分，涵盖了消融实验以验证“答案合成智能体”的有效性，并针对不同复杂度的查询进行了分层评估。
*   **客观性**：采用双人盲审标注机制，并制定了标准化的评分准则，确保了评估过程的客观与公平。

#### 6. 主要结论与发现
*   **性能飞跃**：完整框架 (IGDD-G&A) 的 NDCG@10 达到 **85.22%**，远高于 Data.gov 的 **43.65%**。
*   **意图理解精准**：意图解析智能体的准确率 (EIMR) 高达 **99.27%**，能够极好地处理复杂约束。
*   **重排序的重要性**：消融实验显示，答案合成智能体通过 LLM 的语义推理，使 NDCG@10 提升了约 **15-17%**，有效过滤了向量检索中的语义噪声。
*   **复杂度适应性**：随着查询复杂度增加，传统系统的性能剧烈下降，而本框架表现出极强的鲁棒性。

#### 7. 优点
*   **语义对齐能力**：通过统一本体解决了跨平台元数据标准不一的顽疾。
*   **可解释性与透明度**：不仅给出结果，还提供匹配理由和知识子图可视化，增强了用户信任。
*   **人机协作**：引入置信度阈值和人机回环机制，有效处理了自然语言的歧义性。
*   **多源协同**：实现了从局部（如 PASDA）到全球（如 Google Earth Engine）数据的无缝集成。

#### 8. 不足与局限
*   **阶段局限性**：目前主要侧重于“发现”阶段，尚未实现从发现到“分析就绪数据”（Analysis-Ready Data）自动预处理的全流程转化。
*   **分辨率匹配缺失**：尚未深入考虑地理数据的空间/时间分辨率与用户需求的精细匹配（如比例尺敏感性）。
*   **成本与延迟**：依赖商业大模型 API 导致单次查询成本较高且存在数十秒的延迟，在大规模并发场景下的适用性有待优化。
*   **偏差风险**：检索结果仍受限于底层 LLM 的知识边界及知识图谱构建时主题生成的质量。

（完）
