---
title: "Reasoner-Executor-Synthesizer: Scalable Agentic Architecture with Static O(1) Context Window"
title_zh: 推理器-执行器-合成器：具有静态 O(1) 上下文窗口的可扩展智能体架构
authors: Ivan Dobrovolskyi
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22367v1"
tags: ["query:ma-kf"]
score: 10.0
evidence: 通过分离RAG组件以减少上下文窗口的可扩展智能体架构
tldr: 针对传统RAG架构在处理大规模数据时存在的Token成本高昂及幻觉风险，本文提出RES（推理器-执行器-合成器）架构。该架构通过将意图解析、确定性数据检索与叙述生成严格分离，使执行器在不消耗Token的情况下仅向合成器传递固定大小的统计摘要。实验证明，该架构在处理千万级文献时能保持恒定的Token开销，实现了O(1)复杂度并从根本上消除了数据幻觉。
selection_source: fresh_fetch
motivation: 传统RAG架构随数据集增大导致Token成本线性增长且易产生幻觉。
method: 提出RES三层架构，将意图解析、确定性数据处理与叙述生成分离，仅向生成器传递固定大小的统计摘要。
result: 在ScholarSearch测试中，无论数据集规模如何，平均Token消耗稳定在1574个，实现了O(1)复杂度。
conclusion: 该架构通过结构化分离彻底消除了数据幻觉，并显著提升了大规模数据集下智能体的扩展性与经济性。
---

## 摘要
部署为自主智能体的大语言模型（LLMs）通常采用检索增强生成（RAG），将检索到的文档输入上下文窗口，这引发了两个问题：幻觉风险随上下文长度增加而增长，且 Token 成本随数据集规模线性扩展。我们提出了推理器-执行器-合成器（RES）架构，这是一种三层设计，严格分离了意图解析（推理器）、确定性数据检索与聚合（执行器）以及叙述生成（合成器）。执行器不消耗 LLM Token，仅向合成器传递固定大小的统计摘要。我们从形式上证明了 RES 在数据集规模上实现了 O(1) 的 Token 复杂度，并在基于 Crossref API（包含 1.3 亿多篇文献）的学术研究助手 ScholarSearch 上验证了该架构。在 100 次基准测试运行中，无论数据集包含 42,000 篇还是 1,630 万篇文献，RES 的平均 Token 消耗均为 1,574 个。该架构通过设计消除了数据幻觉：LLM 永远不会接触原始记录。

## Abstract
Large Language Models (LLMs) deployed as autonomous agents commonly use Retrieval-Augmented Generation (RAG), feeding retrieved documents into the context window, which creates two problems: the risk of hallucination grows with context length, and token cost scales linearly with dataset size. We propose the Reasoner-Executor-Synthesizer (RES) architecture, a three-layer design that strictly separates intent parsing (Reasoner), deterministic data retrieval and aggregation (Executor), and narrative generation (Synthesizer). The Executor uses zero LLM tokens and passes only fixed-size statistical summaries to the Synthesizer. We formally prove that RES achieves O(1) token complexity with respect to dataset size, and validate this on ScholarSearch, a scholarly research assistant backed by the Crossref API (130M+ articles). Across 100 benchmark runs, RES achieves a mean token cost of 1,574 tokens regardless of whether the dataset contains 42,000 or 16.3 million articles. The architecture eliminates data hallucination by construction: the LLM never sees raw records. KEYWORDS LLM agents; agentic architecture; hallucination elimination; token optimization; context window; retrieval-augmented generation; deterministic execution; scholarly metadata; Crossref API; O(1) complexity.

---

## 论文详细总结（自动生成）

这是一份关于论文《Reasoner-Executor-Synthesizer: Scalable Agentic Architecture with Static O(1) Context Window》的结构化分析报告：

### 1. 核心问题与整体含义
*   **研究动机**：传统的检索增强生成（RAG）架构在处理大规模数据集时面临两大瓶颈：
    1.  **幻觉风险**：随着上下文窗口中注入的文档增多，模型更容易产生幻觉（如“迷失在中间”现象）。
    2.  **成本线性增长**：Token 消耗随检索到的文档数量线性增加，导致大规模数据分析在经济上不可行。
*   **核心目标**：提出一种新型智能体架构（RES），旨在实现**零数据幻觉**和**O(1)（常数级）Token 复杂度**，使 AI 智能体能够以固定成本分析任意规模的数据集。

### 2. 方法论
RES 架构将处理流程严格划分为三个互不重叠的层：
*   **推理器 (Reasoner - LLM层)**：
    *   **功能**：解析用户的自然语言查询，将其转化为结构化的查询计划（JSON 格式）。
    *   **约束**：不接触原始数据，仅负责意图识别（如趋势分析、对比、排名）。
*   **执行器 (Executor - 确定性代码层)**：
    *   **功能**：接收查询计划，通过传统编程代码或 API 调用（如 SQL、REST API）在数据库上执行聚合操作（COUNT, SUM, AVG 等）。
    *   **关键点**：**消耗零 LLM Token**。它不下载原始记录，而是生成固定大小的统计摘要（Statistical Summary）。
*   **合成器 (Synthesizer - LLM层)**：
    *   **功能**：接收执行器生成的固定大小统计摘要，生成人类可读的叙述性结论和可视化配置。
*   **数学证明**：论文证明了总 Token 成本 $T_{RES} = T_{Reasoner} + 0 + T_{Synthesizer}$。由于推理器和合成器的输入/输出大小与数据集规模 $n$ 无关，因此复杂度为 $O(1)$。

### 3. 实验设计
*   **实现系统**：**ScholarSearch**，一个基于 Crossref API（涵盖 1.3 亿+ 学术文章）的科研助手。
*   **数据集/场景**：涵盖从 4.2 万（网络安全领域）到 1630 万（医学研究领域）不同规模的文章集合。
*   **Benchmark**：设计了 20 个多样化查询，涵盖趋势分析、对比、排名和通用统计四类意图。
*   **对比方法**：**Naive RAG Baseline**（将 50 条原始记录直接喂给 LLM 的标准模式）。
*   **模型使用**：Anthropic Claude 3.5 Sonnet。

### 4. 资源与算力
*   **算力说明**：文中**未明确提及**具体的 GPU 型号或训练时长。
*   **原因**：该研究侧重于**智能体架构设计（Inference-only）**而非模型训练。实验主要通过 API 调用（Claude API 和 Crossref API）完成，前端使用 Streamlit 部署。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 100 次基准测试运行（20 个查询 $\times$ 5 次重复）。
*   **充分性**：
    *   实验跨越了多个数量级的数据规模（从 $10^4$ 到 $10^7$），有力地证明了 $O(1)$ 复杂度的稳定性。
    *   通过 Log-Log 缩放图直观展示了 RES 与传统方法在成本上的巨大差异。
    *   **客观性**：实验在同一天完成，使用相同的 LLM 模型进行对比，具有较好的公平性。

### 6. 主要结论与发现
*   **成本恒定**：无论数据集规模如何，RES 的平均 Token 消耗稳定在 **1,574 个**（标准差仅 259）。
*   **极端节省**：在处理 1600 万条记录时，RES 比传统 RAG 节省了 **99.9996%** 的成本（成本降低了 120 万倍）。
*   **消除幻觉**：由于 LLM 永远接触不到原始数据记录，该架构从构造上（by construction）消除了数据层面的幻觉。

### 7. 优点
*   **极高的经济性**：解决了企业级大数据分析中 LLM 成本过高的痛点。
*   **架构透明度**：通过分离层级，用户可以清晰看到推理计划和执行结果，增强了系统的可解释性。
*   **领域无关性**：该模式可推广至金融、医疗、物联网等任何拥有结构化/半结构化数据的领域。

### 8. 不足与局限
*   **查询类型限制**：RES 擅长统计和趋势分析，但不适用于需要“深度阅读”单篇长文档细节的查询（如“总结某篇特定论文的实验方法”）。
*   **依赖底层能力**：执行器的效能高度依赖于数据源（如 API 或数据库）是否支持高效的聚合/过滤功能。
*   **推理风险**：系统的准确性仍受限于推理器（Reasoner）解析复杂指令的能力，如果第一层解析错误，后续结果将失效。

（完）
