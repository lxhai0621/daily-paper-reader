---
title: "ReBOL: Retrieval via Bayesian Optimization with Batched LLM Relevance Observations and Query Reformulation"
title_zh: ReBOL：结合批量 LLM 相关性观测与查询重构的贝叶斯优化检索
authors: "Anton Korikov, Scott Sanner"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20513v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 通过贝叶斯优化和查询重构提高检索召回率
tldr: 针对传统向量检索在LLM重排序阶段因缺乏上下文交互和多模态相关性分布导致的召回受限问题，本文提出ReBOL。该方法结合LLM查询重构初始化贝叶斯优化（BO）后验分布，通过迭代批量获取文档并利用LLM进行相关性评分来动态更新后验，从而优化检索过程。在BEIR数据集上的实验表明，ReBOL在保持与重排序器相当延迟的同时，显著提升了召回率和排名性能。
selection_source: fresh_fetch
motivation: 传统的向量检索无法捕捉复杂的查询-文档交互，限制了后续LLM重排序的性能上限。
method: 提出ReBOL框架，利用LLM查询重构初始化贝叶斯优化后验，并通过批量LLM评分迭代更新模型以优化文档检索。
result: 在多个BEIR数据集上，ReBOL的召回率和NDCG指标均优于主流LLM重排序基线，且延迟表现相当。
conclusion: 将贝叶斯优化与LLM评分相结合是一种提升检索召回率和精度的有效策略，克服了传统Top-k检索的局限性。
---

## 摘要
LLM 重排序受限于通过向量相似度检索到的前 k 个文档，这既无法实现上下文查询-文档 Token 交互，也无法捕捉多模态相关性分布。虽然 LLM 查询重构尝试通过生成改进的或额外的查询来提高召回率，但其后仍需进行向量相似度检索。因此，我们提出通过引入 ReBOL 来解决这些前 k 个检索阶段的失效问题。ReBOL：1) 利用 LLM 查询重构来初始化关于文档相关性的多模态贝叶斯优化（BO）后验分布；2) 迭代地获取文档批次进行 LLM 查询-文档相关性评分，随后更新后验分布以优化相关性。在探索了查询重构和文档批次多样化技术后，我们在五个 BEIR 数据集上，使用两种 LLM（Gemini-2.5-Flash-Lite, GPT-5.2）将 ReBOL 与 LLM 重排序基线进行了评估。ReBOL 始终能获得更高的召回率和具有竞争力的排名，例如在 Robust04 数据集上，与最佳 LLM 重排序器相比，其 recall@100 为 46.5% 对 35.0%，NDCG@10 为 63.6% 对 61.2%。我们还展示了 ReBOL 可以实现与 LLM 重排序器相当的延迟。

## Abstract
LLM-reranking is limited by the top-k documents retrieved by vector similarity, which neither enables contextual query-document token interactions nor captures multimodal relevance distributions. While LLM query reformulation attempts to improve recall by generating improved or additional queries, it is still followed by vector similarity retrieval. We thus propose to address these top-k retrieval stage failures by introducing ReBOL, which 1) uses LLM query reformulations to initialize a multimodal Bayesian Optimization (BO) posterior over document relevance, and 2) iteratively acquires document batches for LLM query-document relevance scoring followed by posterior updates to optimize relevance. After exploring query reformulation and document batch diversification techniques, we evaluate ReBOL against LLM reranker baselines on five BEIR datasets and using two LLMs (Gemini-2.5-Flash-Lite, GPT-5.2). ReBOL consistently achieves higher recall and competitive rankings, for example compared to the best LLM reranker on the Robust04 dataset with 46.5% vs. 35.0% recall@100 and 63.6% vs. 61.2% NDCG@10. We also show that ReBOL can achieve comparable latency to LLM rerankers.

---

## 论文详细总结（自动生成）

这是一份关于论文《ReBOL: Retrieval via Bayesian Optimization with Batched LLM Relevance Observations and Query Reformulation》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对当前“检索-重排序”（Retrieve-and-Rerank）架构中的**召回瓶颈**问题提出了挑战。
*   **动机**：传统的向量检索（Dense Retrieval）依赖于查询和文档的独立编码，无法捕捉 Token 级别的深度交互。此外，向量检索通常使用点积等线性评分函数，这会诱导单模态的相关性分布，而实际的相关文档往往分布在多个不同的语义簇中（多模态分布）。
*   **核心问题**：如果第一阶段的向量检索未能召回相关文档，后续性能再强的 LLM 重排序器也无法挽回损失。现有的 LLM 查询重构技术虽然能改善召回，但最终仍受限于向量相似度检索的固有缺陷。

### 2. 方法论：ReBOL 框架
论文提出了 **ReBOL**（基于贝叶斯优化的批量 LLM 相关性观测检索），将检索过程重新建模为一个主动学习的优化问题。
*   **核心思想**：利用贝叶斯优化（BO）在文档嵌入空间中寻找高相关性区域，通过 LLM 的高精度评分作为“观测值”来迭代更新对全局相关性分布的认知。
*   **关键技术细节**：
    1.  **多模态 GP 先验初始化**：使用高斯过程（GP）作为先验。通过初始查询 $q_0$ 和 LLM 生成的多个重构查询 $\{q_1, ..., q_Q\}$ 在嵌入空间中标记为“最大相关点”，从而初始化一个具有多个峰值的后验分布。
    2.  **批量文档获取**：使用采集函数（Acquisition Function）从海量文档库中挑选最有价值的文档批次。
    3.  **LLM 绝对评分**：调用 LLM 对选定文档进行 0-3 分的绝对相关性评分（Pointwise Scoring），这些评分作为 BO 的新观测值。
    4.  **后验更新**：根据 LLM 的反馈更新 GP 的均值和方差，引导下一轮搜索。
*   **算法创新**：引入了受 **MMR（最大边际相关性）** 启发的多样化批量采集函数，在选择高预测相关性文档的同时，确保文档在嵌入空间中的覆盖范围，以增强探索能力。

### 3. 实验设计
*   **数据集**：选取了 BEIR 基准中的 5 个代表性数据集：TREC-NEWS、Robust04、TREC-COVID、SciFact 和 NFCorpus。
*   **Benchmark 与对比方法**：
    *   **传统检索**：BM25（Flat/MF）、SPLADE、密集向量检索。
    *   **LLM 重排序基线**：Pointwise (PW) 评分、Listwise (LW) 排序（包括单次调用和滑动窗口模式）。
    *   **变体对比**：ReBOL-IQ（仅初始查询）与 ReBOL-QR（包含查询重构）。
*   **LLM 模型**：主要使用 Gemini-2.5-Flash-Lite 和 GPT-5.2，并测试了 Cross-Encoder 作为评分器的效果。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **成本分析**：作者强调了 LLM 的推理延迟是主要开销。通过批量处理（Batch size = 10），ReBOL 将单次查询的 LLM 调用次数控制在与标准重排序器相当的水平（约 10 次调用处理 100 个文档），从而在延迟上具有可比性。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个不同领域的检索任务，每项任务均对比了 10 种以上的基线配置。
*   **消融实验**：
    *   测试了不同的观测数量（k=50, 100, 200）。
    *   对比了多种采集函数（Greedy, UCB, Random, KB, MMR）。
    *   验证了不同 LLM 作为底层评分器的鲁棒性。
*   **评价**：实验设计较为充分且客观。通过引入“ReBOL-Top-k”消融项（即不使用 BO 迭代，仅在向量检索前 k 个中观测），证明了**主动采集过程**对提升召回率的显著贡献。

### 6. 主要结论与发现
*   **召回率大幅提升**：在所有数据集上，ReBOL 的 Recall@100 显著优于所有 LLM 重排序基线。例如在 Robust04 上，召回率从 35.0% 提升至 46.5%。
*   **排名性能领先**：在保持高召回的同时，NDCG@10 指标也达到了竞争水平，甚至在多个任务中超越了复杂的滑动窗口 Listwise 重排序器。
*   **查询重构的协同效应**：查询重构为 BO 提供了极佳的“热启动”点，帮助模型快速定位多个潜在的相关簇。
*   **多样性的重要性**：MMR 风格的批量采集函数在处理大规模语料库时表现最佳，因为它能有效防止搜索陷入局部最优。

### 7. 优点
*   **打破 Top-k 限制**：ReBOL 不再受限于初始向量检索给出的固定候选集，能够通过 BO 发现向量检索漏掉的深层相关文档。
*   **多模态建模**：通过非线性核函数（RBF）成功捕捉了相关性的多模态分布，比传统的单向量匹配更符合实际检索场景。
*   **效率平衡**：通过批量观测和 GP 闭式解推理，在提升性能的同时维持了可接受的推理延迟。

### 8. 不足与局限
*   **数据集覆盖面**：虽然涵盖了 5 个数据集，但对于超大规模工业级索引的扩展性验证尚不充分。
*   **依赖嵌入质量**：BO 的搜索空间建立在预训练嵌入模型之上，如果嵌入空间本身存在严重的语义断裂，BO 的效率可能会受限。
*   **LLM 成本**：尽管进行了批量优化，但相比纯向量检索，其成本仍然高出数个数量级，主要适用于对精度要求极高的重型检索场景。
*   **偏差风险**：LLM 作为评分员可能存在位置偏差或对特定内容的偏好，这些偏差会被 BO 过程放大。

（完）
