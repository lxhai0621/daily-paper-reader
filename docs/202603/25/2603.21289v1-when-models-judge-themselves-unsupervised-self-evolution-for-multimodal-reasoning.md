---
title: "When Models Judge Themselves: Unsupervised Self-Evolution for Multimodal Reasoning"
title_zh: 当模型自我评判：多模态推理的无监督自我进化
authors: "Zhengxian Wu, Kai Shi, Chuanrui Zhang, Zirui Liao, Jun Yang, Ni Yang, Qiuying Peng, Luyuan Zhang, Hangrui Xu, Tianhuang Su, Zhenyu Yang, Haonan Lu, Haoqian Wang"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21289v1"
tags: ["query:mmkqa"]
score: 9.0
evidence: 多模态大语言模型推理的无监督自我演化
tldr: 针对多模态大模型推理任务中高质量标注数据获取成本高的问题，本文提出一种无监督自进化训练框架。该框架无需人工标注或外部奖励模型，通过采样多个推理路径并利用模型自身的“一致性”作为先验，结合受限的“评判者”调制机制对不同质量的路径进行重加权。采用组相对策略优化（GRPO）在无标签数据上进行训练，显著提升了模型在数学推理任务上的性能和泛化能力，为多模态模型的规模化自进化提供了新路径。
selection_source: fresh_fetch
motivation: 现有多模态推理模型的提升高度依赖昂贵的人工标注或教师模型蒸馏，难以实现低成本的规模化扩展。
method: 提出一种基于组相对策略优化（GRPO）的框架，利用自一致性信号和受限的评判者调制机制，在无监督条件下对推理路径进行重加权和策略更新。
result: 在五个数学推理基准测试中，该方法在不使用标注数据的情况下持续提升了模型的推理性能和泛化能力。
conclusion: 该研究证明了多模态模型可以通过无监督自进化实现性能突破，为构建可扩展的自进化智能体提供了有效方案。
---

## 摘要
多模态大语言模型的最新进展在推理任务上取得了强劲表现，但这些改进在很大程度上依赖于高质量的标注数据或教师模型蒸馏，这两者的成本都很高且难以扩展。为解决这一问题，我们提出了一种用于多模态推理的无监督自我进化训练框架，在不使用人工标注答案或外部奖励模型的情况下实现了稳定的性能提升。对于每个输入，我们采样多条推理轨迹，并联合建模它们的组内结构。我们利用 Actor 的自一致性信号作为训练先验，并引入一种基于有界 Judge 的调制机制，以持续对不同质量的轨迹进行重新加权。我们进一步将调制后的分数建模为组级分布，并将绝对分数转换为每组内的相对优势，从而实现更稳健的策略更新。通过在无标签数据上使用组相对策略优化 (GRPO) 进行训练，我们的方法在五个数学推理基准测试中持续提升了推理性能和泛化能力，为迈向自我进化的多模态模型提供了一条可扩展的路径。代码可在 https://dingwu1021.github.io/SelfJudge/ 获取。

## Abstract
Recent progress in multimodal large language models has led to strong performance on reasoning tasks, but these improvements largely rely on high-quality annotated data or teacher-model distillation, both of which are costly and difficult to scale.To address this, we propose an unsupervised self-evolution training framework for multimodal reasoning that achieves stable performance improvements without using human-annotated answers or external reward models. For each input, we sample multiple reasoning trajectories and jointly model their within group structure.We use the Actor's self-consistency signal as a training prior, and introduce a bounded Judge based modulation to continuously reweight trajectories of different quality.We further model the modulated scores as a group level distribution and convert absolute scores into relative advantages within each group, enabling more robust policy updates. Trained with Group Relative Policy Optimization (GRPO) on unlabeled data, our method consistently improves reasoning performance and generalization on five mathematical reasoning benchmarks, offering a scalable path toward self-evolving multimodal models.The code are available at https://dingwu1021.github.io/SelfJudge/.

---

## 论文详细总结（自动生成）

以下是对论文《When Models Judge Themselves: Unsupervised Self-Evolution for Multimodal Reasoning》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：多模态大语言模型（MLLM）在推理任务上的进步高度依赖高质量的人工标注数据或强力教师模型的蒸馏，这些资源成本高昂且难以规模化扩展。
*   **研究动机**：现有的无监督自进化方法（如“多数投票”机制）存在局限性：
    *   **错误强化**：高一致性不代表高正确性，模型可能在错误的路径上达成共识并不断强化偏见。
    *   **模式坍缩**：训练容易陷入少数几种模式，导致策略熵迅速下降，抑制了有效的探索。
    *   **缺乏细粒度区分**：无法捕捉候选答案之间细微的质量差异。

### 2. 方法论：无监督自进化框架
该论文提出了一种无需外部监督信号的自进化框架，核心思想是让模型同时扮演 **Actor（执行者）** 和 **Judge（评判者）** 两个角色。

*   **核心流程**：
    1.  **轨迹采样**：对于每个输入，Actor 采样 $n$ 条推理轨迹。
    2.  **自一致性初始奖励 ($r_{SC}$)**：计算每个答案在采样组中的出现频率，作为初始奖励先验。
    3.  **有界 Judge 调制 ($g(s)$)**：使用一个冻结的相同模型作为 Judge，对每条轨迹的答案正确性、推理质量和视觉对齐进行评分。通过一个**有界且连续可微的校准函数**（基于 Sigmoid）将评分转化为调制信号，对初始奖励进行修正，防止 Judge 的噪声被无限放大。
    4.  **组内分布建模**：将调制后的分数视为组级分布，利用 **Log-Sum-Exp** 机制将绝对分数转换为组内的**相对优势（Relative Advantage）**。
    5.  **策略优化**：采用 **GRPO（组相对策略优化）** 算法更新 Actor 策略，使模型概率分布向高质量轨迹偏移，而非简单地向最高频答案坍缩。

### 3. 实验设计
*   **训练数据集**：Geometry3k、GeoQA、MMR1（均为无标签版本）。
*   **评估基准（Benchmark）**：
    *   数学推理：MathVision, MathVerse, WeMath, LogicVista, DynaMath。
    *   泛化性测试：ChartQA（图表理解）, MMVP（通用视觉推理）。
*   **对比方法**：
    *   基础模型：Qwen2.5-VL-7B-Instruct。
    *   无监督自进化方法：VisionZero, EvoLMM, MM-UPT（基于多数投票）。
    *   有监督/蒸馏方法：Vision-R1, OpenVLThinker 等。

### 4. 资源与算力
*   **硬件环境**：单节点 8 × NVIDIA A800 GPU (80GB)。
*   **训练细节**：
    *   使用 Verl 框架进行分布式训练。
    *   训练周期为 20 Epochs，学习率 $1 \times 10^{-6}$。
    *   **算力开销**：由于需要在线采样和 Judge 评分，其训练时长约为有监督 GRPO 的 **1.4 倍**（有监督 GRPO 在该配置下约需 10.5 小时，推算本项目约需 14.7 小时）。

### 5. 实验数量与充分性
*   **实验规模**：论文在 5 个主流数学推理基准上进行了全面测试，并跨越了不同规模的底座模型（从 2B 到 32B，包括 InternVL3, GLM-4.1V 等）。
*   **消融实验**：详细对比了“仅自一致性”、“仅 Judge 评分”、“多数投票”以及“有/无分布建模”的效果，验证了各组件的必要性。
*   **充分性评价**：实验设计较为充分且客观。通过对比不同训练数据源（Geo3K vs GeoQA vs MMR1）和不同规模的底座，证明了方法的稳健性和可扩展性。

### 6. 主要结论与发现
*   **性能突破**：在不使用任何人工标注的情况下，该方法在 MathVision 上取得了 +5.9% 的绝对精度提升（从 25.0% 提升至 30.9%）。
*   **超越多数投票**：相比传统的多数投票（MM-UPT），该方法能有效纠正模型内部的系统性偏见，避免模型在错误答案上“达成共识”。
*   **训练稳定性**：组内分布建模和有界调制机制使模型在长期训练中保持了更健康的策略熵和响应长度，缓解了模式坍缩。
*   **泛化能力**：在数学推理上训练的模型，在图表理解和通用视觉任务上也表现出了一定的性能增益。

### 7. 优点与亮点
*   **完全脱离标注**：实现了一种闭环的自进化，不依赖外部奖励模型或昂贵的教师模型。
*   **创新的奖励建模**：将“自一致性”与“Judge 评分”结合，并引入组内相对优势的概念，解决了无监督训练中信号噪声大的痛点。
*   **高兼容性**：实验证明该方法对不同架构和参数规模的 MLLM 均有效。

### 8. 不足与局限
*   **Judge 能力瓶颈**：自进化的上限受限于 Judge（即模型自身）的初始评估能力。如果模型对某个问题存在根深蒂固的错误认知（错误共识），Judge 可能会给出错误的正面反馈。
*   **Pass@10 下降风险**：实验观察到 Pass@10 指标在某些情况下有所下降，这表明模型在追求更高准确率的同时，输出的采样多样性受到了压缩。
*   **计算开销**：在线采样 8 条轨迹并进行 Judge 评分增加了训练的计算负担。

（完）
