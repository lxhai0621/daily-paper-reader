---
title: "RECOVER: Robust Entity Correction via agentic Orchestration of hypothesis Variants for Evidence-based Recovery"
title_zh: RECOVER：通过假设变体的智能体编排实现基于证据的鲁棒实体纠错
authors: "Abhishek Kumar, Aashraya Sachdeva"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16411v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 使用工具调用智能体进行实体纠错的智能体编排
tldr: 针对自动语音识别（ASR）中稀有和特定领域实体识别困难的问题，本文提出RECOVER框架。该框架作为一个具备工具调用能力的智能体，通过整合多种ASR假设（如1-Best、ROVER集成等）作为证据，结合外部实体检索和大型语言模型（LLM）的约束性修正，显著提升了实体识别的准确性。在五个数据集上的实验表明，该方法在降低实体词错误率（E-WER）和提高召回率方面表现优异。
selection_source: fresh_fetch
motivation: 传统的ASR系统在处理稀有或专业领域实体时容易出错且难以在后期纠正，导致在金融、医疗等关键领域应用受限。
method: 提出一种基于智能体的纠错框架，利用多种ASR假设策略并结合实体检索与LLM约束修正来恢复错误实体。
result: "在五个多样化数据集上，该方法使实体词错误率（E-WER）相对降低了8-46%，并将召回率最高提升了22个百分点。"
conclusion: RECOVER框架通过多假设协同和LLM纠错，有效解决了ASR中的实体识别难题，其中LLM-Select策略在保持整体性能的同时表现最为出色。
---

## 摘要
自动语音识别（ASR）中的实体识别对于稀有和特定领域的术语具有挑战性。在金融、医学和空中交通管制等领域，这些错误的代价十分高昂。如果实体在 ASR 输出中完全缺失，ASR 后纠错将变得非常困难。为了解决这一问题，我们引入了 RECOVER，这是一个作为工具使用智能体的智能体纠错框架。它利用来自 ASR 的多个假设作为证据，检索相关实体，并在约束条件下应用大语言模型（LLM）进行纠错。这些假设通过不同的策略进行利用，即 1-Best、实体感知选择（Entity-Aware Select）、识别器输出投票误差减少（ROVER）集成以及 LLM-Select。在五个不同数据集上的评估结果显示，该框架在实体短语词错误率（E-WER）上实现了 8-46% 的相对降低，并将召回率提高了多达 22 个百分点。LLM-Select 在保持整体词错误率（WER）的同时，在实体纠错方面取得了最佳的综合性能。

## Abstract
Entity recognition in Automatic Speech Recognition (ASR) is challenging for rare and domain-specific terms. In domains such as finance, medicine, and air traffic control, these errors are costly. If the entities are entirely absent from the ASR output, post-ASR correction becomes difficult. To address this, we introduce RECOVER, an agentic correction framework that serves as a tool-using agent. It leverages multiple hypotheses as evidence from ASR, retrieves relevant entities, and applies Large Language Model (LLM) correction under constraints. The hypotheses are used using different strategies, namely, 1-Best, Entity-Aware Select, Recognizer Output Voting Error Reduction (ROVER) Ensemble, and LLM-Select. Evaluated across five diverse datasets, it achieves 8-46% relative reductions in entity-phrase word error rate (E-WER) and increases recall by up to 22 percentage points. The LLM-Select achieves the best overall performance in entity correction while maintaining overall WER.

---

## 论文详细总结（自动生成）

以下是对论文《RECOVER: Robust Entity Correction via agentic Orchestration of hypothesis Variants for Evidence-based Recovery》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现代自动语音识别（ASR）系统在处理稀有词、专有名词及特定领域术语（如医疗、金融、空管）时表现不佳。如果这些实体在 ASR 的“最优路径（1-best）”输出中被删除或严重损坏，传统的后期文本纠错往往因缺乏原始证据而难以恢复。
*   **研究动机**：
    *   **高昂代价**：在关键领域，实体的微小识别错误（如药名、呼号）可能导致严重后果。
    *   **证据缺失**：单次解码可能完全丢失实体信息，需要从多个候选假设（N-best）中寻找线索。
    *   **黑盒限制**：许多生产级 ASR 系统是黑盒，无法获取内部解码概率，因此需要一种基于文本的、灵活的后期处理方案。
    *   **幻觉风险**：直接使用大语言模型（LLM）纠错容易产生过度纠错或幻觉，需要严格的约束机制。

### 2. 方法论
RECOVER 框架将纠错过程建模为一个**具备工具调用能力的智能体（Agent）**，其核心流程如下：
*   **多假设生成**：使用同一 ASR 模型（如 Whisper-small）通过温度采样（T={0.0, 0.2, 0.4, 0.6, 0.8}）生成 5 个不同的转录假设，以捕获互补的错误。
*   **动态实体检索**：针对每个音频片段，从庞大的实体库中检索 Top-200 相关候选。打分公式结合了：
    1.  **精确匹配**（Exact token hits）；
    2.  **模糊相似度**（Levenshtein similarity）；
    3.  **语音前缀匹配**（Phonetic prefix）。
*   **智能体编排（三个核心工具）**：
    1.  **Tool 1: 假设融合（Fuse）**：提供四种策略（1-Best、实体感知选择、ROVER 集成投票、LLM-Select）将多条假设合并或筛选为基准文本。
    2.  **Tool 2: 提议纠错（Propose）**：LLM 根据检索到的实体候选，提出“查找-替换”式的编辑建议，严格限制替换词必须来自实体库。
    3.  **Tool 3: 验证与应用（Verify & Apply）**：通过确定性守卫（Guardrails）检查编辑建议，包括相似度阈值校验、位置重计算和非重叠应用，确保纠错的鲁棒性。

### 3. 实验设计
*   **数据集**：涵盖五个多样化领域：
    *   **Earnings-21**（金融）：财报电话会议，包含公司名、人名。
    *   **ATCO2**（空管）：高噪声环境下的航空呼号。
    *   **Eka-Medical**（医疗）：疾病、症状、药物名称。
    *   **Common Voice**（通用）：使用 NER 模型提取的命名实体。
    *   **ContextASR-Bench**（对话）：电影名称识别。
*   **Benchmark**：以 Whisper-small 的贪婪解码（T=0）作为 Baseline。
*   **对比方法**：对比了 RECOVER 框架下的四种融合策略（1-Best, Entity-Aware Select, ROVER Ensemble, LLM-Select），并进行了 LLM 模型（GPT-4o vs GPT-4o-mini）的消融实验。

### 4. 资源与算力
*   **ASR 推理**：使用了 `faster-whisper` 库进行高效解码。
*   **LLM 算力**：主要实验基于 **GPT-4o** API，消融实验使用了 **GPT-4o-mini**。
*   **说明**：文中未明确提及具体的 GPU 硬件型号、数量或总训练/推理时长，这在基于 API 调用和预训练模型推理的论文中较为常见。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个不同领域的数据集上进行了全面测试，样本量从数百到上万个片段不等（如 Common Voice 包含 16,401 个片段）。
*   **充分性**：
    *   涵盖了从低错误率（ContextASR）到极高错误率（ATCO2）的场景。
    *   提供了详细的实体级对齐统计（替换、删除、插入错误的变化）。
    *   进行了模型规模（GPT-4o vs mini）的对比。
*   **客观性**：使用了标准的 SCLITE 工具和 E-WER（实体词错误率）指标，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **显著提升**：RECOVER 使实体词错误率（E-WER）相对降低了 **8-46%**。
*   **召回率是关键**：召回率最高提升了 **22 个百分点**，证明该框架能有效找回 ASR 丢失的实体。
*   **策略优劣**：
    *   **LLM-Select** 表现最稳健，在纠错的同时能保持或改善整体 WER。
    *   **ROVER 集成**在某些场景下 E-WER 降低最多，但在噪声环境下会引入额外的插入错误，导致整体 WER 恶化。
*   **纠错机制**：主要的改进来自于**替换错误（Substitution）**的减少，即修正了实体的近音或拼写错误。

### 7. 优点
*   **模块化智能体设计**：将 LLM 的推理能力与确定性的验证工具结合，有效解决了 LLM 的幻觉问题。
*   **多假设证据链**：通过温度采样挖掘单一 ASR 模型的潜力，无需访问模型内部参数，通用性强。
*   **受限编辑**：通过严格的实体库约束，确保了纠错的专业性和准确性。

### 8. 不足与局限
*   **推理延迟与成本**：生成 5 个假设并调用 LLM 智能体会显著增加处理每个音频片段的时间和 API 费用，不适合极高实时性要求的场景。
*   **依赖实体库**：系统性能高度依赖于外部实体列表的质量和覆盖范围，对于库外（OOV）实体的纠错能力有限。
*   **策略自适应性**：目前需要人工选择融合策略，未来若能根据音频质量自动切换策略会更理想。

（完）
