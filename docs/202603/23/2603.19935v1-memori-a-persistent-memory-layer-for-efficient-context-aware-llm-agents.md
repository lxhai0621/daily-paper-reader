---
title: "Memori: A Persistent Memory Layer for Efficient, Context-Aware LLM Agents"
title_zh: Memori：用于高效、上下文感知大语言模型智能体的持久化记忆层
authors: "Luiz C. Borro, Luiz A. B. Macarini, Gordon Tindall, Michael Montero, Adam B. Struck"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19935v1"
tags: ["query:agent"]
score: 10.0
evidence: 为上下文感知LLM智能体提供基于语义三元组的持久记忆层
tldr: "Memori 是一个与模型无关的持久化记忆层，旨在解决 LLM 智能体在多轮对话中因注入原始对话导致的成本高和性能下降问题。它通过将非结构化对话转化为语义三元组和摘要，实现了高效的上下文感知。在 LoCoMo 基准测试中，Memori 以极低的 Token 消耗实现了 81.95% 的准确率，显著降低了部署成本并提升了推理效率。"
selection_source: fresh_fetch
motivation: 现有的 LLM 记忆方案通常依赖于注入大量原始对话，导致 Token 成本高昂、性能退化且存在供应商锁定问题。
method: 开发了一种高级增强流水线，将非结构化对话转换为紧凑的语义三元组和对话摘要，从而实现精确的记忆检索和推理。
result: "在 LoCoMo 基准测试中达到 81.95% 的准确率，Token 消耗仅为完整上下文的 5%，比竞争方案节省了 67% 的成本。"
conclusion: 研究证明有效的 LLM 记忆应依赖于结构化的数据表示而非单纯扩大上下文窗口，这为低成本部署智能体提供了新路径。
---

## 摘要
随着大语言模型（LLM）演变为自主智能体，API 层的持久化记忆对于实现跨 LLM 和多会话交互的上下文感知行为至关重要。现有方法强制绑定特定供应商，并依赖于将大量原始对话注入提示词，导致高昂的 token 成本和性能下降。我们推出了 Memori，这是一个与 LLM 无关的持久化记忆层，它将记忆视为一个数据结构化问题。其高级增强流水线将非结构化对话转换为紧凑的语义三元组和对话摘要，从而实现精确检索和连贯推理。在 LoCoMo 基准测试中，Memori 达到了 81.95% 的准确率，在每次查询仅使用 1,294 个 token（约占全上下文的 5%）的情况下，性能优于现有的记忆系统。这显著降低了成本，包括比竞争方法减少了 67% 的 token 使用量，且与全上下文方法相比节省了 20 倍以上的成本。这些结果表明，LLM 智能体中有效的记忆取决于结构化表示而非更大的上下文窗口，从而实现了可扩展且具有成本效益的部署。

## Abstract
As large language models (LLMs) evolve into autonomous agents, persistent memory at the API layer is essential for enabling context-aware behavior across LLMs and multi-session interactions. Existing approaches force vendor lock-in and rely on injecting large volumes of raw conversation into prompts, leading to high token costs and degraded performance.   We introduce Memori, an LLM-agnostic persistent memory layer that treats memory as a data structuring problem. Its Advanced Augmentation pipeline converts unstructured dialogue into compact semantic triples and conversation summaries, enabling precise retrieval and coherent reasoning.   Evaluated on the LoCoMo benchmark, Memori achieves 81.95% accuracy, outperforming existing memory systems while using only 1,294 tokens per query (~5% of full context). This results in substantial cost reductions, including 67% fewer tokens than competing approaches and over 20x savings compared to full-context methods.   These results show that effective memory in LLM agents depends on structured representations instead of larger context windows, enabling scalable and cost-efficient deployment.

---

## 论文详细总结（自动生成）

以下是对论文《Memori: A Persistent Memory Layer for Efficient, Context-Aware LLM Agents》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：随着 LLM 演变为自主智能体，如何在多轮、跨会话交互中实现高效且持久的记忆。
*   **研究动机**：
    *   **现有方案缺陷**：目前的记忆系统通常直接将大量原始对话记录注入 Prompt（提示词），导致 Token 成本极高，且容易引发“上下文腐烂”（Context Rot），即模型在冗长信息中迷失，性能下降。
    *   **供应商锁定**：许多记忆方案与特定模型绑定，缺乏灵活性。
    *   **本质认知**：作者认为记忆不应只是一个“存储问题”，而是一个“数据结构化问题”。

### 2. 方法论与核心技术
Memori 被设计为一个与模型无关的解耦记忆层，其核心是 **高级增强（Advanced Augmentation）** 流水线：
*   **语义三元组生成（Semantic Triples）**：将非结构化对话拆解为原子知识单元（主语-谓语-宾语）。这种结构化表示能过滤噪音（如寒暄、纠错），提供高信号的索引，显著压缩数据。
*   **对话摘要（Conversation Summarization）**：三元组虽然精确但缺乏叙事流。系统同步生成高层级的对话摘要，捕捉用户的意图演变和任务背景。
*   **双层关联架构**：每个三元组都与其来源对话的摘要相链接。检索时，系统不仅能提取孤立的事实，还能还原该事实背后的“故事”。
*   **混合检索机制**：结合了基于嵌入（Embedding）的余弦相似度搜索和基于关键词的 BM25 匹配，确保检索的全面性。

### 3. 实验设计
*   **数据集**：使用 **LoCoMo (Long Conversation Memory)** 基准测试，该测试专门用于评估智能体在长对话中跟踪状态、时间推理和提取细微偏好的能力。
*   **评估维度**：涵盖单跳推理（Single-Hop）、多跳推理（Multi-Hop）、时间推理（Temporal）和开放域推理（Open-Domain）。
*   **对比方法**：
    *   **主流记忆框架**：Zep、LangMem、Mem0。
    *   **性能上限**：全上下文（Full-Context）方法（即将所有历史记录全部输入）。
*   **评估方式**：采用 **LLM-as-a-Judge** 模式，使用 GPT-4.1-mini 作为裁判，根据 Ground-truth 答案对生成结果进行评分。

### 4. 资源与算力
*   **模型使用**：生成阶段使用 GPT-4.1-mini；嵌入模型使用 Gemma-300。
*   **存储与检索**：使用 FAISS 进行本地向量索引和相似度搜索。
*   **算力说明**：文中**未明确说明**具体的硬件配置（如 GPU 型号、数量）或训练时长。由于该系统主要作为 API 层运行，其核心开销在于调用 LLM 进行记忆处理的推理成本。

### 5. 实验数量与充分性
*   **实验规模**：针对 LoCoMo 数据集中的 1529 个问题进行了测试（不含对抗性类别）。
*   **充分性**：Memori 的结果取自 3 轮运行的平均值，并提供了标准差，显示了结果的稳定性。
*   **客观性**：对比了目前市面上最流行的开源记忆框架，并使用了公认的第三方基准测试，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **性能领先**：Memori 实现了 **81.95%** 的综合准确率，在检索类系统中排名第一，优于 Zep (79.09%) 和 LangMem (78.05%)。
*   **极致的 Token 效率**：Memori 每次查询平均仅使用 **1,294** 个 Token，仅占全上下文方案的 **5%** 左右。
*   **成本优势**：相比 Zep 节省了 **67%** 的 Token 消耗；相比全上下文方案，成本降低了 **20 倍**以上。
*   **结构胜过容量**：实验证明，记忆的质量取决于其结构化程度，而非上下文窗口的大小。

### 7. 优点与亮点
*   **高效压缩**：通过三元组和摘要的结合，成功将“噪音”对话转化为“高信号”知识，极大地降低了推理成本。
*   **解耦设计**：作为独立的 SDK 层，可以轻松集成到现有的 LLM 工作流中，不依赖特定模型。
*   **推理增强**：在单跳和多跳推理上表现优异，证明了结构化记忆对逻辑连接的帮助。

### 8. 不足与局限
*   **时间推理瓶颈**：虽然表现不错，但在时间推理（Temporal Reasoning）上略逊于 LangMem 和 Zep，说明单纯的三元组可能在处理复杂的时间状态演变时仍有提升空间。
*   **开放域挑战**：在开放域推理中得分较低（63.54%），这是所有检索类系统的通病，因为这类问题往往缺乏明确的检索锚点。
*   **预处理开销**：虽然推理时节省了 Token，但“高级增强”流水线在处理原始对话生成三元组和摘要时，会产生额外的 LLM 调用成本和延迟，文中未详细量化这一部分的预处理开销。

（完）
