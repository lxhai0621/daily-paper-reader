---
title: "RouterKGQA: Specialized--General Model Routing for Constraint-Aware Knowledge Graph Question Answering"
title_zh: RouterKGQA：面向约束感知知识图谱问答的专用-通用模型路由
authors: "Bo Yuan, Hexuan Deng, Xuebo Liu, Min Zhang"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20017v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 通过在结构化知识图谱中进行推理来减轻大模型幻觉
tldr: 本研究针对知识图谱问答（KGQA）中专用小模型效率高但易出错、通用大模型推理强但成本高的问题，提出了RouterKGQA框架。该框架通过路由机制实现专用与通用模型的协作：专用模型负责生成推理路径并进行约束感知过滤，仅在必要时由通用模型进行知识图谱引导的修复。实验表明，该方法在提升准确率的同时显著降低了推理成本，实现了性能与效率的平衡。
selection_source: fresh_fetch
motivation: 现有的KGQA方法在检索效率与复杂约束处理能力之间难以兼得，导致小模型易产生不可达路径而大模型调用成本过高。
method: 提出RouterKGQA框架，利用专用模型生成路径并结合约束感知过滤，仅在需要时路由至通用模型进行修复，并优化了代理工作流。
result: 在多个基准测试中，F1值平均提升3.57个百分点，且每题平均仅需1.15次大模型调用。
conclusion: RouterKGQA通过专用与通用模型的协同路由，实现了高性能、低成本且具备约束感知能力的知识图谱问答。
---

## 摘要
知识图谱问答（KGQA）是一种通过将推理建立在结构化且可验证的知识图谱之上，从而缓解大语言模型（LLM）幻觉的有前景的方法。现有方法分为两种范式：基于检索的方法利用小型专用模型，虽然效率高，但经常产生不可达路径并遗漏隐式约束；而基于智能体的方法利用大型通用模型，能实现更强的结构化落地，但成本显著更高。我们提出了 RouterKGQA，这是一个专用-通用模型协作框架，其中专用模型生成推理路径，而通用模型仅在需要时执行知识图谱引导的修复，从而以极低的成本提升性能。我们进一步为专用模型配备了约束感知答案过滤，从而减少了冗余答案。此外，我们设计了一个更高效的通用智能体工作流，进一步降低了推理成本。实验结果表明，RouterKGQA 在各基准测试中的平均 F1 值和 Hits@1 分别比之前的最优方法高出 3.57 个百分点和 0.49 个百分点，而每个问题平均仅需 1.15 次 LLM 调用。代码和模型可在 https://github.com/Oldcircle/RouterKGQA 获取。

## Abstract
Knowledge graph question answering (KGQA) is a promising approach for mitigating LLM hallucination by grounding reasoning in structured and verifiable knowledge graphs. Existing approaches fall into two paradigms: retrieval-based methods utilize small specialized models, which are efficient but often produce unreachable paths and miss implicit constraints, while agent-based methods utilize large general models, which achieve stronger structural grounding at substantially higher cost. We propose RouterKGQA, a framework for specialized--general model collaboration, in which a specialized model generates reasoning paths and a general model performs KG-guided repair only when needed, improving performance at minimal cost. We further equip the specialized with constraint-aware answer filtering, which reduces redundant answers. In addition, we design a more efficient general agent workflow, further lowering inference cost. Experimental results show that RouterKGQA outperforms the previous best by 3.57 points in F1 and 0.49 points in Hits@1 on average across benchmarks, while requiring only 1.15 average LLM calls per question. Codes and models are available at https://github.com/Oldcircle/RouterKGQA.

---

## 论文详细总结（自动生成）

### 论文总结：RouterKGQA —— 面向约束感知知识图谱问答的专用-通用模型路由框架

#### 1. 论文的核心问题与整体含义
论文针对**知识图谱问答（KGQA）**中现有两类范式的局限性提出了改进方案。
*   **研究动机**：大语言模型（LLM）存在事实幻觉。虽然可以通过知识图谱（KG）进行约束，但现有方法面临两难：
    *   **基于检索的专用小模型**：推理效率高，但容易生成 KG 中不存在的虚假关系（关系幻觉），且难以处理问题中的隐式约束（如时间、数值限制）。
    *   **基于智能体的通用大模型**：通过多轮交互探索 KG，准确性高但计算开销巨大，推理延迟高且成本昂贵。
*   **核心目标**：在保证高准确率和约束感知能力的同时，大幅降低推理成本，实现专用模型与通用模型的优势互补。

#### 2. 论文提出的方法论
RouterKGQA 框架包含三个阶段，核心思想是“**专用生成 + 必要时通用修复**”：

*   **阶段 1：约束感知路径生成（专用模型）**
    *   训练一个专用小模型（如 Llama-3-8B），将自然语言问题映射为**约束感知推理路径（CRP）**。
    *   **CRP 结构**：包含一条主路径（关系序列）和一组约束集（实体、数值或字符串约束）。
    *   **可达性验证（路由决策）**：在 KG 上执行主路径，若结果为空（不可达），则判定专用模型失败，将请求路由至阶段 2。
*   **阶段 2：高效路径修复（通用模型智能体）**
    *   **推理蓝图生成**：通用模型（如 GPT-4o-mini）一次性生成高层语义子任务序列。
    *   **KG 引导的束搜索（Beam Search）**：在关系空间而非实体空间进行搜索。利用 SBERT 进行语义粗筛选，再由通用模型进行精选。这种方式比传统的实体级探索更高效。
    *   **停止准则**：利用专用模型预测的跳数作为搜索深度，实验证明该预测非常鲁棒。
*   **阶段 3：渐进式查询执行**
    *   将修复后的 CRP 转换为 SPARQL 查询。
    *   **约束松弛策略**：若带约束执行失败，按“字符串 -> 数值 -> 实体”的顺序逐步放宽约束，以提高系统的鲁棒性。

#### 3. 实验设计
*   **数据集**：使用了两个主流 Freebase 基准测试：
    *   **WebQuestionsSP (WebQSP)**：相对简单的多跳推理。
    *   **Complex WebQuestions (CWQ)**：包含大量复杂组合逻辑和约束。
*   **Benchmark 指标**：Hits@1（首选答案准确率）和 F1 值（答案集完整性）。
*   **对比方法**：
    *   **LLM-only**：直接使用 Llama 或 GPT 模型。
    *   **基于检索/表示的方法**：如 EmbedKGQA、NSM+h。
    *   **基于 LLM 的先进方法**：包括智能体类（ToG, PoG, EffiQA）和生成类（RoG, ChatKBQA, GCR, MemQ）。

#### 4. 资源与算力
*   **模型使用**：专用模型采用 Llama-2-7B 和 Llama-3.1-8B；通用模型采用 GPT-4o-mini。
*   **训练细节**：使用 **LoRA** 进行微调。学习率 5e-5，Batch Size 16（4x4 梯度累积），使用 bf16 精度。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量，也未提及具体的训练总时长。但论文详细列出了推理成本的估算（基于 API 价格和 Token 消耗），强调了其低成本特性。

#### 5. 实验数量与充分性
*   **实验规模**：在两个大型数据集上进行了全量测试，涵盖了从 1 跳到多跳、从无约束到多约束的各种情况。
*   **消融实验**：针对路由阶段（Stage 1/2/3）、停止准则、约束类型（实体/数值/字符串）分别做了消融，验证了各模块的贡献。
*   **模型鲁棒性分析**：更换了不同的专用模型（Qwen, Llama）和通用模型（ChatGPT, GPT-4o-mini）进行对比，证明框架具有通用性。
*   **复杂性分析**：专门针对 CWQ 的不同组合类型（比较、最高级等）进行了细分性能分析。
*   **客观性**：通过统一 Hits@1 的定义，解决了以往论文中指标计算不一致的问题，确保了对比的公平性。

#### 6. 论文的主要结论与发现
*   **性能领先**：RouterKGQA 在 F1 值上平均提升了 **3.57** 个百分点，Hits@1 提升了 **0.49**。
*   **极高效率**：平均每个问题仅需 **1.15 次** LLM 调用，远低于传统智能体方法（如 ToG 的 10.91 次），成本与纯检索方法相当，但准确率更高。
*   **约束的重要性**：约束感知过滤主要提升了 F1 值（精度），而对 Hits@1 影响较小，说明其有效过滤了冗余的错误答案。
*   **CRP 的优势**：相比复杂的 S-expression（逻辑表达式），扁平化的 CRP 序列更易于小模型学习和生成。

#### 7. 优点
*   **路由机制巧妙**：利用“路径可达性”作为低成本的路由信号，实现了效率与能力的动态平衡。
*   **约束处理精细**：显式建模数值和时间约束，填补了以往路径生成类方法在处理复杂逻辑上的空白。
*   **搜索效率高**：在关系空间进行束搜索，并结合 SBERT 过滤，极大地减少了对昂贵大模型的调用次数。

#### 8. 不足与局限
*   **约束表达限制**：目前的 CRP 仅支持扁平化的约束，对于嵌套极深的复杂逻辑表达能力可能受限。
*   **路由信号单一**：仅依赖路径是否可达来决定是否路由。如果路径可达但语义错误（语义幻觉），系统可能无法触发修复。
*   **外部工具依赖**：系统性能仍受限于实体链接（ELQ）的准确性和知识图谱本身的完备性。
*   **语言与领域覆盖**：实验仅限于英文 Freebase 数据集，在多语言或垂直领域 KG 上的泛化能力有待验证。

（完）
