---
title: "DIAL-KG: Schema-Free Incremental Knowledge Graph Construction via Dynamic Schema Induction and Evolution-Intent Assessment"
title_zh: DIAL-KG：通过动态模式归纳与演化意图评估实现的无模式增量知识图谱构建
authors: "Weidong Bao, Yilin Wang, Ruyu Gao, Fangling Leng, Yubin Bao, Ge Yu"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20059v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 增量知识图谱构建框架
tldr: 针对传统知识图谱构建方法依赖预定义模式且难以处理动态增量数据的局限，本文提出DIAL-KG框架。该框架由元知识库驱动，通过双轨提取确保知识完整性，利用治理裁决保证事实保真度，并结合模式演化实现新模式的自动诱导与增量更新。实验表明，该框架在图谱质量和模式诱导方面均达到了SOTA水平，显著提升了知识图谱构建的灵活性与效率。
selection_source: fresh_fetch
motivation: 传统知识图谱构建方法多为静态且依赖预定义模式，无法高效应对现实场景中动态增长的数据及模式演变需求。
method: 提出一种由元知识库协调的闭环框架，包含双轨提取、治理裁决和模式演化三个循环阶段，实现增量式构建。
result: 实验结果显示，DIAL-KG在构建图谱的质量以及诱导模式的准确性上均优于现有主流方法，达到了最先进水平。
conclusion: DIAL-KG通过动态模式诱导和增量更新机制，为构建灵活、高保真的动态知识图谱提供了一种有效的闭环解决方案。
---

## 摘要
知识图谱（KGs）是搜索、问答和推荐等应用的基础。传统的知识图谱构建方法主要是静态的，依赖于基于预定义模式从固定语料库进行单步构建。然而，此类方法对于数据动态到达的现实场景并非最优，因为整合新信息需要进行完整且计算成本高昂的图谱重建。此外，预定义的模式阻碍了知识图谱构建的灵活性。为了解决这些局限性，我们引入了 DIAL-KG，这是一个由元知识库（MKB）编排的增量知识图谱构建闭环框架。该框架在一个三阶段循环中运行：(i) 双轨抽取（Dual-Track Extraction），通过默认生成三元组并在处理复杂知识时切换到事件抽取来确保知识的完整性；(ii) 治理裁决（Governance Adjudication），确保所提取事实的忠实度和时效性，以防止幻觉和知识陈旧；以及 (iii) 模式演化（Schema Evolution），从经过验证的知识中归纳出新模式以指导后续构建周期，并将当前轮次的知识增量应用于现有知识图谱。大量实验表明，我们的框架在构建的图谱质量和归纳的模式质量方面均达到了最先进（SOTA）的性能。

## Abstract
Knowledge Graphs (KGs) are foundational to applications such as search, question answering, and recommendation. Conventional knowledge graph construction methods are predominantly static, rely ing on a single-step construction from a fixed corpus with a prede f ined schema. However, such methods are suboptimal for real-world sce narios where data arrives dynamically, as incorporating new informa tion requires complete and computationally expensive graph reconstruc tions. Furthermore, predefined schemas hinder the flexibility of knowl edge graph construction. To address these limitations, we introduce DIAL KG, a closed-loop framework for incremental KG construction orches trated by a Meta-Knowledge Base (MKB). The framework oper ates in a three-stage cycle: (i) Dual-Track Extraction, which ensures knowledge completeness by defaulting to triple generation and switching to event extraction for complex knowledge; (ii) Governance Adjudica tion, which ensures the fidelity and currency of extracted facts to prevent hallucinations and knowledge staleness; and (iii) Schema Evolution, in which new schemas are induced from validated knowledge to guide subsequent construction cycles, and knowledge from the current round is incrementally applied to the existing KG. Extensive experiments demon strate that our framework achieves state-of-the-art (SOTA) performance in the quality of both the constructed graph and the induced schemas.

---

## 论文详细总结（自动生成）

### DIAL-KG：通过动态模式归纳与演化意图评估实现的无模式增量知识图谱构建

#### 1. 核心问题与整体含义（研究动机和背景）
传统的知识图谱构建（KGC）方法通常是**静态且闭塞**的：它们依赖于预先定义的模式（Schema），并假设数据是一次性给定的。在现实场景中，信息以流式方式动态到达，且知识会随时间发生演变（如产品停产、职位变动）。
*   **局限性**：现有方法若要整合新信息，往往需要昂贵的全局重构；预定义模式限制了对新兴领域知识的捕捉；缺乏对知识“生命周期”（新增、修改、废弃）的有效管理。
*   **研究目标**：提出一种无需预定义模式、支持增量更新、能够自动感知知识演化并进行自我修正的闭环构建框架。

#### 2. 方法论：核心思想与关键技术
DIAL-KG 提出了一个由**元知识库（Meta-Knowledge Base, MKB）**驱动的闭环框架，通过三个阶段的循环实现增量构建：

*   **核心思想**：将 KGC 从“静态流水线”转变为“基于治理的闭环系统”，引入类似人类记忆的“版本化”机制，保留知识的来源与修订历史。
*   **关键技术细节**：
    1.  **双轨提取（Dual-Track Extraction）**：根据复杂度分流。简单事实（如属性）进入“静态轨道”生成三元组；复杂事实（含时间、状态变迁）进入“事件轨道”生成事件结构，避免语义扭曲。
    2.  **治理裁决（Governance Adjudication）**：包含证据验证（防幻觉）、逻辑验证（一致性检查）和**演化意图评估**。后者通过识别触发词（如“deprecated”、“replaced”）来判断知识是否失效。
    3.  **模式演化（Schema Evolution）**：从验证后的事实中聚类并诱导新模式，更新 MKB。MKB 随后作为检索约束，指导下一轮提取。
    4.  **事务性集成（Transactional Integration）**：通过“软废弃（Soft Deprecation）”机制更新图谱，即不物理删除旧事实，而是标记为“已废弃”并记录证据，确保可追溯性。

#### 3. 实验设计
*   **数据集**：
    *   **WebNLG & Wiki-NRE**：标准静态基准数据集，通过切片模拟流式到达。
    *   **SoftRel-Δ**：论文自建的数据集，基于 Kubernetes 发布日志，专门用于测试系统处理知识演化（废弃、移除）的能力。
*   **对比方法（Baselines）**：
    *   **EDC**：基于 LLM 的提取-定义-规范化框架。
    *   **AutoKG**：基于多智能体协作的自动化构建框架。
*   **评估指标**：除了传统的 Precision/Recall/F1，还引入了 **Δ-Precision**（新增事实准确率）和 **D-HP**（废弃处理精度）。

#### 4. 资源与算力
*   **模型使用**：生成与推理任务使用了 **Qwen-Max**；独立评测（LLM-as-a-Judge）使用了 **DeepSeek-V3**（温度设定为 0.1）。语义相似度计算使用了 **BGE-M3** 嵌入模型。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。由于该框架主要依赖现成的 LLM API 进行推理和上下文学习（In-context Learning），而非从头训练大模型，其算力消耗主要集中在 API 调用和向量检索上。

#### 5. 实验数量与充分性
*   **实验规模**：在三个不同性质的数据集（通用、百科、特定领域日志）上进行了测试。
*   **消融实验**：针对“意图评估”、“事件表示”和“共指对齐”模块分别做了消融，验证了各组件对增量可靠性的贡献。
*   **客观性**：引入了独立的大模型作为裁判，并结合人工抽检，实验设计较为严谨。通过对比模式的冗余度（Redundancy）和紧凑性，客观证明了其模式诱导的优越性。

#### 6. 主要结论与发现
*   **性能领先**：在静态提取任务上，F1 值比强基线（EDC）高出约 1.5%~4.7%。
*   **增量可靠性**：在处理知识演化时，废弃处理精度（D-HP）超过 **98%**，证明了其在动态环境下的极高保真度。
*   **模式质量**：生成的模式比 EDC 更精简（关系类型减少达 15%），且冗余度降低了 1.6~2.8 个点，有效解决了同义关系重复定义的问题。

#### 7. 优点
*   **闭环自进化**：通过 MKB 实现了模式的自动发现与反馈，无需人工干预预定义本体。
*   **生命周期管理**：创新的“软废弃”机制解决了知识过时问题，这在以往的 Schema-free 研究中较少涉及。
*   **表达力平衡**：双轨机制既保留了三元组的简洁性，又通过事件结构捕捉了复杂动态语义。

#### 8. 不足与局限
*   **推理延迟**：由于治理阶段涉及多步 LLM 推理（证据、逻辑、意图验证），在处理极高速度的数据流时可能存在延迟瓶颈。
*   **模型依赖**：系统高度依赖底层 LLM 的推理能力，若模型在特定领域的逻辑判断出错，可能导致错误的知识废弃。
*   **多模态缺失**：目前仅限于文本数据，尚未扩展到图像、视频等跨模态信号的知识演化。

（完）
