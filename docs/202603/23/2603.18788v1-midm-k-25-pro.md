---
title: "Mi:dm K 2.5 Pro"
title_zh: "Mi:dm K 2.5 Pro"
authors: KT Tech innovation Group
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18788v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 长上下文理解与128K令牌支持
tldr: dm K 2.5 Pro 是一款拥有 32B 参数的企业级大语言模型，旨在解决复杂推理、长文本理解和智能体工作流需求。通过深度扩展（DuS）技术、128K 上下文支持以及包含推理 SFT 和异步强化学习的多阶段后训练，该模型在韩语特定基准测试中达到领先水平，并在保持安全性的同时，展现了卓越的逻辑推理与工具调用能力。
selection_source: fresh_fetch
motivation: 针对企业环境对多步推理、长文本处理及韩语特定领域的高复杂度需求，现有模型在扩展性和专业性上仍显不足。
method: 采用基于层预测的深度扩展（DuS）进行预训练，并结合推理专项微调、模型合并及异步强化学习的“融合训练”优化模型性能。
result: 模型在韩语特定基准测试中取得 SOTA 结果，且在长文本处理、复杂问题解决和安全性评估方面表现优异。
conclusion: dm K 2.5 Pro 证明了通过高质量数据策应与针对性推理优化，可以在特定语言和企业级应用中实现超越通用大模型的性能。
---

## 摘要
随着大语言模型（LLM）领域的不断演进，仅靠简单的文本生成已无法满足需求，多步推理、长上下文理解和智能体工作流（agentic workflows）变得至关重要。这一转变对企业环境中的现有模型构成了挑战，尤其是在单纯依靠规模扩展（scaling）已不足以应对的韩语及特定领域场景中。我们推出了 Mi:dm K 2.5 Pro，这是一款拥有 32B 参数的旗舰级 LLM，旨在通过以推理为核心的优化来解决企业级复杂问题。我们的方法论通过以质量为中心的策划流水线构建了坚实的数据基础，该流水线利用抽象语法树（AST）分析处理代码，利用填空合成（gap-filling synthesis）处理数学，并采用基于 LLM 的质量评估器。在预训练阶段，我们通过基于层预测器的深度扩展（Depth Upscaling, DuS）和支持 128K token 上下文窗口的渐进式策略来扩展模型。后训练阶段则引入了专门的多阶段流水线，包括推理有监督微调（Reasoning SFT）、模型合并和异步强化学习（RL），以培养解决复杂问题的能力。随后，通过“融合训练”（Fusion Training）将这些能力与对话流畅度、一致的回答风格以及可靠的工具使用进行重新平衡。评估结果显示，Mi:dm K 2.5 Pro 的性能可与全球及韩国国内领先模型相媲美。此外，它在韩语特定基准测试中取得了最先进（SOTA）的成绩，展现了深厚的语言与文化理解力。最后，负责任 AI 评估验证了其抵御攻击的安全性，确保了部署时的安全特性，并在无害性与响应性之间达成了平衡。

## Abstract
The evolving LLM landscape requires capabilities beyond simple text generation, prioritizing multi-step reasoning, long-context understanding, and agentic workflows. This shift challenges existing models in enterprise environments, especially in Korean-language and domain-specific scenarios where scaling is insufficient. We introduce Mi:dm K 2.5 Pro, a 32B parameter flagship LLM designed to address enterprise-grade complexity through reasoning-focused optimization.   Our methodology builds a robust data foundation via a quality-centric curation pipeline utilizing abstract syntax tree (AST) analysis for code, gap-filling synthesis for mathematics, and an LLM-based quality evaluator. Pre-training scales the model via layer-predictor-based Depth Upscaling (DuS) and a progressive strategy supporting a 128K token context window. Post-training introduces a specialized multi-stage pipeline, including Reasoning SFT, model merging, and asynchronous reinforcement learning (RL), to develop complex problem-solving skills. "Fusion Training" then rebalances these capabilities with conversational fluency, consistent response styling, and reliable tool-use.   The evaluations show that Mi:dm K 2.5 Pro achieves competitive performance against leading global and domestic models. In addition, it sets state-of-the-art results on Korean-specific benchmarks, showcasing deep linguistic and cultural understanding. Finally, Responsible AI evaluations validate safety against attacks, ensuring a secure profile for deployment with a balance of harmlessness and responsiveness.

---

## 论文详细总结（自动生成）

以下是对论文《Mi:dm K 2.5 Pro》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文的核心在于解决企业级环境对大语言模型（LLM）提出的更高要求：**多步推理、长上下文理解和智能体（Agentic）工作流**。
*   **研究动机**：现有的中小规模模型在处理复杂推理和长文档分析时存在局限，且单纯通过增加参数量（Scaling）在韩语及特定专业领域（如法律、STEM）的效果并不理想。
*   **背景**：KT（韩国电信）旨在开发一款 32B 参数的旗舰模型，通过“推理导向”的优化策略，在保持模型规模适中的前提下，提升其在韩国文化背景下的专业解决问题能力。

### 2. 核心方法论
模型构建分为三个关键阶段：
*   **高质量数据基础**：
    *   **代码数据**：利用**抽象语法树（AST）**进行结构化分析和可执行性过滤，并根据算法复杂度进行难度分级。
    *   **数学数据**：采用“填空合成”策略，针对特定领域（如几何、拓扑）的结构性缺失进行针对性合成。
    *   **质量评估**：开发基于 LLM 的自动化评估框架，通过人类专家迭代优化 Prompt，实现 84.2% 的评估准确率。
*   **预训练优化**：
    *   **深度扩展（DuS）**：基于**层预测器（Layer-predictor）**将现有模型参数重用并扩展深度，比传统的余弦相似度扩展更稳定。
    *   **长上下文扩展**：采用渐进式策略（4K $\to$ 32K $\to$ 64K $\to$ 128K），结合 **YaRN 机制**和 **RegMix** 自动数据配比，确保长文本能力提升的同时不损失通用性能。
*   **后训练流水线（Post-training）**：
    *   **推理 SFT**：使用“通道令牌”（如 `analysis`, `final`）区分内部推理链与最终输出。
    *   **模型合并（Model Merging）**：训练多个领域专家模型（数学、代码、通用），通过线性合并集成能力。
    *   **异步强化学习（RL）**：采用 **GSPO 算法**和 **verl 框架**，实现生成（Rollout）与更新（Update）的完全异步化，解决长推理链导致的 GPU 空转问题。
    *   **融合训练（Fusion Training）**：在强化学习后期重新平衡推理能力与对话流畅度、工具调用能力。

### 3. 实验设计
*   **数据集与场景**：
    *   **通用英语基准**：GPQA-Diamond（推理）、MMLU-Pro（知识）、MATH-Hard（数学）、IFEval（指令遵循）、LiveCodeBench（代码）、Terminal-Bench（智能体）。
    *   **韩语特定基准**：Ko-Sovereign（文化/社会）、KoBALT（语言任务）、CLIcK（文化智能）、KMMLU（综合知识）。
*   **对比方法（Baselines）**：
    *   全球/国内领先模型：Qwen-3-30B、Solar-Open-100B、K-EXAONE-236B、HyperCLOVA X-SEED-Think-32B。
*   **评估维度**：定量基准测试、人类偏好评估（Human Evaluation）、负责任 AI（RAI）安全性评估。

### 4. 资源与算力
*   **算力提及**：论文在讨论“异步 RL 执行”效率时，提到了使用 **8 节点（共 64 张 GPU）** 的实验环境进行同步与异步模式的对比测试。
*   **未明确说明**：论文未披露整个模型从预训练到后训练所消耗的总算力资源（如总 GPU 小时数或具体芯片型号，推测为 H100 或 A100 集群）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大量的阶段性实验，包括：
    *   DuS 扩展策略的消融实验。
    *   长上下文扩展各阶段的性能监控。
    *   模型合并比例（如 8:2, 5:5, 2:8）对性能影响的详细对比。
    *   RL 阶段不同难度 Prompt 筛选的效果分析。
*   **充分性与客观性**：实验覆盖了从基础能力到高级推理、从安全性到系统效率的多个维度。通过引入第三方基准（Artificial Analysis）和细粒度的子领域分析（如法律、物理、化学），实验结果显得较为客观和透明。

### 6. 主要结论与发现
*   **性能领先**：Mi:dm K 2.5 Pro 在韩语特定基准（如 Ko-Sovereign）上创下了 SOTA 记录，显著优于参数量更大的模型（如 236B 的 EXAONE）。
*   **推理与规模的平衡**：证明了通过高质量数据策应和多阶段 RL，32B 规模的模型可以在复杂推理任务上与 100B+ 模型竞争。
*   **系统效率**：完全异步的 RL 架构将 Token 吞吐量提升了约 30%，有效缓解了长推理任务中的“长尾瓶颈”。
*   **安全性**：在红队测试中表现出极低的攻击成功率（36.3%），在无害性与响应性之间取得了良好平衡。

### 7. 优点（亮点）
*   **工程化落地强**：详细描述了如何通过 AST 分析和层预测器等技术手段解决实际训练中的稳定性与效率问题。
*   **韩语深度定制**：不仅是翻译，还针对韩语的敬语体系、行政区划、社会常识进行了专门的数据增强和评估。
*   **异步 RL 架构**：提出的 GSPO 异步训练方案对于长链推理模型的工业化生产具有很高的参考价值。

### 8. 不足与局限
*   **模型规模限制**：尽管在 30B 级别表现优异，但在处理极高难度的数学（如 AIME）或极长链推理时，与 400B+ 的超大规模模型相比仍有天花板。
*   **合成数据依赖**：模型在数学和代码领域大量依赖合成数据，虽然经过过滤，但仍可能存在潜在的分布偏差或“模型崩溃”风险。
*   **多语言覆盖**：虽然支持中日韩英四语，但实验重点几乎完全集中在韩语和英语，对中日文能力的详细评估相对较少。

（完）
