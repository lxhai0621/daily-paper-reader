---
title: "SYMDIREC: A Neuro-Symbolic Divide-Retrieve-Conquer Framework for Enhanced RTL Synthesis and Summarization"
title_zh: SYMDIREC：一种用于增强 RTL 综合与摘要生成的神经符号分治-检索-征服框架
authors: "Prashanth Vijayaraghavan, Apoorva Nitsure, Luyao Shi, Charles Mackin, Ashutosh Jadhav, David Beymer, Ehsan Degan, Vandana Mukherjee"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.17208v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 用于RTL综合的神经符号RAG框架
tldr: 针对大语言模型在处理寄存器传输级（RTL）综合与总结任务时面临的语法严苛、监督数据有限及结构精度不足等挑战，本文提出了SYMDIREC框架。该框架采用神经符号化的“分解-检索-征服”策略，将复杂任务分解为符号化子目标，结合微调的检索器获取相关代码，并通过LLM推理进行组装。在无需微调LLM的情况下，显著提升了Verilog和VHDL任务的性能。
selection_source: fresh_fetch
motivation: 现有大语言模型在RTL设计中缺乏符号化规划，导致生成的硬件描述语言结构精度较低且难以对齐自然语言。
method: 提出一种神经符号框架，通过将RTL任务分解为符号子目标、利用微调检索器获取代码并由LLM推理组装来实现。
result: "在RTL综合任务中Pass@1提升约20%，在总结任务中ROUGE-L提升15-20%，显著优于现有提示词和RAG基准。"
conclusion: 符号化引导能有效增强大语言模型在硬件设计自动化任务中的结构化处理能力和生成质量。
---

## 摘要
寄存器传输级 (RTL) 综合与摘要生成是硬件设计自动化的核心，但由于严格的 HDL 语法、有限的监督以及与自然语言的弱对齐，对于大语言模型 (LLMs) 而言仍具挑战性。现有的提示词和检索增强生成 (RAG) 方法尚未结合符号规划，限制了它们的结构精度。我们提出了 SYMDIREC，这是一种神经符号框架，它将 RTL 任务分解为符号子目标，通过微调后的检索器检索相关代码，并利用 LLM 推理组装经过验证的输出。SYMDIREC 在无需对 LLM 进行微调的情况下支持 Verilog 和 VHDL，在综合任务上的 Pass@1 准确率比提示词和 RAG 基准高出约 20%，在摘要生成任务上的 ROUGE-L 提升了 15-20%，证明了符号引导在 RTL 任务中的优势。

## Abstract
Register-Transfer Level (RTL) synthesis and summarization are central to hardware design automation but remain challenging for Large Language Models (LLMs) due to rigid HDL syntax, limited supervision, and weak alignment with natural language. Existing prompting and retrieval-augmented generation (RAG) methods have not incorporated symbolic planning, limiting their structural precision. We introduce SYMDIREC, a neuro-symbolic framework that decomposes RTL tasks into symbolic subgoals, retrieves relevant code via a fine-tuned retriever, and assembles verified outputs through LLM reasoning. Supporting both Verilog and VHDL without LLM fine-tuning, SYMDIREC achieves ~20% higher Pass@1 rates for synthesis and 15-20% ROUGE-L improvements for summarization over prompting and RAG baselines, demonstrating the benefits of symbolic guidance in RTL tasks.

---

## 论文详细总结（自动生成）

### 论文总结：SYMDIREC 神经符号 RTL 综合与摘要生成框架

#### 1. 核心问题与研究动机
硬件设计中的寄存器传输级（RTL）任务（如将自然语言描述转化为 Verilog/VHDL 代码，或反之进行功能总结）对大语言模型（LLM）极具挑战。主要难点在于：
*   **语法严苛性**：硬件描述语言（HDL）对逻辑结构和时序要求极高，微小错误会导致综合失败。
*   **数据稀缺**：相比通用编程语言，高质量且带有自然语言对齐的 RTL 数据较少。
*   **结构对齐弱**：现有的检索增强生成（RAG）或提示词工程缺乏对硬件功能逻辑（如布尔表达式、数据流）的显式建模，导致生成的模块结构松散。

#### 2. 方法论：Divide-Retrieve-Conquer (分治-检索-征服)
SYMDIREC 提出了一种神经符号框架，通过三个阶段将符号逻辑集成到 LLM 工作流中：
*   **Divide（符号化分解）**：利用 LLM 将复杂的 RTL 任务分解为多个功能子组件（如计数器、多路复用器）。每个组件不仅有自然语言描述，还附带**符号逻辑表示**（如布尔逻辑或状态转移方程），作为结构化支架。
*   **Retrieve（符号化检索）**：使用在专门构建的 **RTL-IR 数据集**上微调的双编码器检索器。检索时同时考虑文本描述和符号逻辑，从知识库中匹配最相关的 RTL 代码片段或摘要。
*   **Conquer（验证与组装）**：LLM 充当“验证者”，根据符号意图对检索到的候选片段进行评分和筛选，最后将验证通过的子模块组装成完整的 RTL 设计或连贯的自然语言总结。

#### 3. 实验设计
*   **数据集与 Benchmark**：
    *   **Verilog-Eval**：包含 156 个功能任务及自检测试平台（Testbench）。
    *   **VHDL-Eval**：包含 202 个 VHDL 任务，用于评估跨语言泛化能力。
    *   **RTL-IR**：作者构建的包含约 5 万条记录的训练集，涵盖文本-代码对、功能等效代码（FEC）等。
*   **对比方法**：
    *   基础提示词（Vanilla Prompting）、CoDes（描述链）、ReAct（迭代推理）。
    *   通用 RAG（基于 CodeBERT）和微调后的 RAG（VRAG-FT）。
    *   领域专用模型：RTLCoder、CodeV。
*   **评估指标**：综合任务使用 **Pass@1**（通过测试平台的比例）；摘要任务使用 **ROUGE-L**。

#### 4. 资源与算力
*   **硬件**：检索器的微调在 **2 台 NVIDIA V100 GPU** 上完成。
*   **软件/工具**：使用 Milvus 向量数据库进行高效检索，LangChain 进行 LLM 调用编排。
*   **效率**：单个任务的平均处理时间约为 5-10 秒，具有较好的实用性。

#### 5. 实验数量与充分性
*   **实验规模**：在两个主流硬件语言基准上进行了全面测试。
*   **统计严谨性**：所有实验均独立运行 5 次，报告均值和标准差，并进行了配对 t 检验（p < 0.01），确保结果的统计显著性。
*   **消融实验**：针对子组件数量（N）和检索深度（k）进行了详细的敏感性分析，验证了符号逻辑在提升检索精度方面的贡献。实验设计客观、公平，设置了“Oracle”（SYMDIREC-GT）作为性能上限参考。

#### 6. 主要结论与发现
*   **性能飞跃**：SYMDIREC 在 RTL 综合任务中 Pass@1 提升了约 20%，在摘要生成中 ROUGE-L 提升了 15-20%。
*   **符号逻辑的价值**：相比纯文本检索，引入符号化支架能显著减少幻觉，提高模块间的逻辑一致性。
*   **跨语言有效性**：该框架在 Verilog 和 VHDL 上表现均衡，证明了其作为通用 RTL 辅助工具的潜力。

#### 7. 优点
*   **神经符号结合**：巧妙地将 LLM 的推理能力与硬件设计的结构化逻辑（符号化）结合，解决了 RTL 设计的模块化难题。
*   **无需微调大模型**：仅需微调轻量级的检索器，降低了适配新领域或新语言的成本。
*   **跨语言支持**：同时支持 Verilog 和 VHDL，这在现有研究中较为少见。

#### 8. 不足与局限
*   **符号生成依赖性**：框架的效果高度依赖于 LLM 生成初始符号表达式的准确性，若 LLM 无法正确理解复杂逻辑，后续检索将受限。
*   **单文件限制**：目前仅支持单文件 RTL 设计，无法处理具有复杂跨文件依赖的大型层次化项目。
*   **分治粒度敏感**：子组件分解过细会导致逻辑碎片化，过粗则失去检索精度，最优粒度（N 值）需根据任务复杂度手动或启发式调整。

（完）
