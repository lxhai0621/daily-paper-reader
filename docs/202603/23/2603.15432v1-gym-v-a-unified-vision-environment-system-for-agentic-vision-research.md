---
title: "Gym-V: A Unified Vision Environment System for Agentic Vision Research"
title_zh: Gym-V：一个用于智能体视觉研究的统一视觉环境系统
authors: Fanqing Meng Lingxiao Du Jiawei Gu Jiaqi Liao Linjie Li Zijian Wu Xiangyan Liu Ziqi Zhao Mengkang Hu Yue Zhang Zichen Liu Jiaheng Zhang Michael Qizhe Shieh
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15432v1"
tags: ["query:agent"]
score: 9.0
evidence: 视觉智能体的强化学习基础设施
tldr: 针对视觉智能体缺乏标准化强化学习基础设施的问题，本文推出了Gym-V。这是一个包含10个领域、179个程序化生成的视觉环境的统一平台，支持难度可控的实验。研究发现观察支架（如描述和规则）对训练成功至关重要，且跨领域多样化训练能显著提升泛化能力。该平台为视觉语言模型（VLM）智能体的训练与评估提供了重要基石。
selection_source: fresh_fetch
motivation: 视觉智能体研究缺乏标准化的“Gym”式基础设施，导致难以进行系统性的学习驱动因素分析和公平比较。
method: 开发了Gym-V平台，提供10个领域共179个难度可控的程序化生成视觉环境，用于开展受控实验。
result: 实验表明观察支架比强化学习算法的选择更关键，且多样化的跨领域训练能有效提升智能体的泛化性能。
conclusion: Gym-V为视觉智能体的训练和评估提供了统一的基础设施，有助于加速未来视觉语言模型智能体的研究。
---

## 摘要
随着智能体系统越来越多地依赖于来自可验证奖励的强化学习，标准化的“gym”基础设施已成为快速迭代、可复现性和公平比较的关键。视觉智能体缺乏此类基础设施，限制了对驱动其学习的因素以及当前模型不足之处的系统性研究。我们推出了 Gym-V，这是一个统一的平台，包含跨越 10 个领域、难度可控的 179 个程序化生成的视觉环境，能够实现以前在碎片化工具包中无法进行的受控实验。通过使用该平台，我们发现观察支架（observation scaffolding）对训练成功的决定性作用超过了强化学习算法的选择，其中字幕和游戏规则决定了学习是否能够成功。跨领域迁移实验进一步表明，在多样化的任务类别上进行训练具有广泛的泛化能力，而狭窄的训练可能导致负迁移，且多轮交互会放大所有这些效应。Gym-V 作为一个便捷的训练环境和评估工具包基础发布，旨在加速未来对智能体视觉语言模型（agentic VLMs）的研究。

## Abstract
As agentic systems increasingly rely on reinforcement learning from verifiable rewards, standardized ``gym'' infrastructure has become essential for rapid iteration, reproducibility, and fair comparison. Vision agents lack such infrastructure, limiting systematic study of what drives their learning and where current models fall short. We introduce \textbf{Gym-V}, a unified platform of 179 procedurally generated visual environments across 10 domains with controllable difficulty, enabling controlled experiments that were previously infeasible across fragmented toolkits. Using it, we find that observation scaffolding is more decisive for training success than the choice of RL algorithm, with captions and game rules determining whether learning succeeds at all. Cross-domain transfer experiments further show that training on diverse task categories generalizes broadly while narrow training can cause negative transfer, with multi-turn interaction amplifying all of these effects. Gym-V is released as a convenient foundation for training environments and evaluation toolkits, aiming to accelerate future research on agentic VLMs.

---

## 论文详细总结（自动生成）

以下是对论文《Gym-V: A Unified Vision Environment System for Agentic Vision Research》的结构化深入总结：

### 1. 核心问题与研究动机
*   **背景**：随着视觉语言模型（VLM）向智能体（Agent）方向发展，基于可验证奖励的强化学习（RL）成为主流。然而，视觉智能体领域缺乏像文本领域（如 Reasoning Gym）那样标准化的“Gym”基础设施。
*   **核心问题**：现有的视觉环境工具包碎片化严重，任务覆盖窄，且缺乏统一的接口，导致难以系统性地研究视觉感知、推理支架（Scaffolding）以及 RL 算法对模型学习的影响。
*   **研究意义**：Gym-V 旨在提供一个统一、可扩展且难度可控的平台，支持视觉智能体的规模化训练与公平比较。

### 2. 方法论
*   **核心思想**：构建一个兼容 OpenAI Gym 和 Ray RLlib 接口的统一视觉环境系统，将交互式训练、离线监督和基准评估整合在同一个 `reset/step` 循环中。
*   **关键技术细节**：
    *   **程序化生成与自动验证**：包含 179 个环境，通过参数化控制难度（如网格大小、物体数量），并提供确定性的规则校验器。
    *   **可组合包装器（Wrappers）**：将“观察设计”作为实验变量。通过 Wrapper 可以动态修改任务描述、添加状态摘要（字幕）、控制历史窗口长度或集成外部工具（如 Python 解释器），而无需改动底层环境逻辑。
    *   **分布式奖励服务（EaaS）**：针对需要昂贵模型评分的生成式任务（如文生图），采用“评估即服务”架构，通过 Ray Serve 实现跨 GPU 的自动批处理和水平扩展。
    *   **统一分类法**：将环境分为单轮推理（算法、几何、图论等 7 类）和多轮交互（游戏、空间导航、时间控制 3 类）。

### 3. 实验设计
*   **场景与数据集**：涵盖 10 个领域，共 179 个环境（105 个单轮，74 个多轮）。
*   **Benchmark 对象**：评估了 9 种主流模型，包括 Gemini-3-Pro、Claude 4.5、GPT-5.2 以及 Qwen3-VL/Qwen2.5-VL 系列。
*   **对比实验**：
    *   **RL 算法对比**：在相同计算预算下对比了 GRPO、GSPO 和 SAPO。
    *   **消融实验**：研究了历史长度（0/3/5 轮）、规则提示（有/无）、观察支架（仅图像 vs. 图像+字幕）的影响。
    *   **迁移学习**：进行了 6×6（单轮）和 4×4（多轮）的跨领域迁移矩阵实验。

### 4. 资源与算力
*   **训练框架**：使用 `verl` 框架对 Qwen2.5-VL-7B-Instruct 进行微调。
*   **超参数**：学习率 $1 \times 10^{-6}$，单轮批次大小 256（每实例 8 个 rollout），多轮批次 256 条完整轨迹。
*   **缺失信息**：论文**未明确说明**具体的 GPU 型号（如 H100 或 A100）数量、总训练时长或总算力消耗（TFLOPS）。

### 5. 实验充分性与客观性
*   **实验规模**：做了大量实验，包括 9 个模型的零射评估、16 个环境的 RL 训练曲线对比、多维度的消融研究以及复杂的跨领域迁移分析。
*   **客观性**：通过程序化生成避免了数据污染；通过统一的 Wrapper 机制确保了不同干预措施（如是否有字幕）在相同环境动力学下的公平对比。
*   **充分性**：实验覆盖了从感知到推理、从单轮到多轮、从训练到泛化的全生命周期，论证较为充分。

### 6. 主要结论与发现
*   **观察支架至关重要**：观察支架（如添加字幕、提供规则）对训练成功的影响**超过了 RL 算法的选择**。在某些任务中，没有规则或字幕会导致学习完全失败。
*   **算法无绝对优劣**：没有单一 RL 算法在所有场景下胜出，GSPO 在长时程多轮任务中表现出更好的稳定性。
*   **难度悬崖**：模型在面对难度增加时表现出非线性的性能崩溃（难度悬崖），这为 RL 提供了持续的学习信号。
*   **泛化规律**：在多样化任务（如认知类）上训练能产生广泛的正迁移，而狭窄任务（如几何类）可能导致负迁移。
*   **多轮放大效应**：多轮交互会显著放大感知错误和策略漂移，使支架设计和课程选择变得更加关键。

### 7. 优点
*   **高度统一**：成功将视觉 Agent 的训练、评估和工具使用整合进一个标准 API。
*   **科研友好**：Wrapper 机制允许研究者将注意力集中在“如何呈现信息”这一核心变量上，极大地便利了受控实验。
*   **解决数据污染**：程序化生成的环境提供了无限的、不可预见的测试用例。

### 8. 不足与局限
*   **时间控制瓶颈**：在“时间（Temporal）”类环境（如街机游戏）中，当前 VLM 几乎无法取得进展，说明模型在精细时间序列控制上仍有巨大缺陷。
*   **算力报告不详**：缺乏具体的硬件资源和训练成本数据，不利于其他团队评估复现成本。
*   **模型规模限制**：RL 训练实验主要集中在 7B 规模模型，尚未验证结论在超大规模模型（如 70B+）上的扩展性。

（完）
