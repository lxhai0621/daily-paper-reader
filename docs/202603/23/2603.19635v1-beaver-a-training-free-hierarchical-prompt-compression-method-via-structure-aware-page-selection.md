---
title: "BEAVER: A Training-Free Hierarchical Prompt Compression Method via Structure-Aware Page Selection"
title_zh: BEAVER：一种通过结构感知页面选择实现的无需训练的分层提示词压缩方法
authors: "Zhengpei Hu, Kai Li, Dapeng Fu, Chang Zeng, Yue Li, Yuanhao Tang, Jianqiang Huang"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19635v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 用于长文档理解的分层提示词压缩
tldr: 针对大语言模型长上下文推理延迟高和语义碎片化的问题，本文提出BEAVER，一种无需训练的层级化提示词压缩方法。该方法通过双路径池化将变长上下文映射为稠密页级张量，并结合语义与词法双分支选择及句子平滑技术，在保持篇章完整性的同时极大提升了推理效率。实验表明，BEAVER在保持高保真度的同时，将128k上下文的延迟降低了26.4倍，为长文档理解提供了高效方案。
selection_source: fresh_fetch
motivation: 现有压缩方法面临高昂的训练成本或因过度Token剪枝导致的语义碎片化挑战，限制了长上下文LLM的推理效率。
method: 提出一种基于结构感知页面选择的层级化框架，通过双路径池化实现硬件并行，并结合语义与词法双分支选择及句子平滑来保持篇章完整性。
result: 在多个基准测试中表现优异，特别是在RULER多针检索中保持了高保真度，且在128k上下文下实现了26.4倍的延迟缩减。
conclusion: BEAVER证明了通过结构感知的层级化选择可以在无需训练的情况下，显著提升长上下文LLM的推理吞吐量并保持语义理解能力。
---

## 摘要
大语言模型（LLM）上下文窗口的指数级扩张解锁了长文档理解的能力，但也引入了推理延迟和信息利用方面的严重瓶颈。现有的压缩方法通常由于激进的 token 剪枝而面临高昂的训练成本或语义碎片化问题。在本文中，我们提出了 BEAVER，这是一个新颖的无需训练的框架，它将压缩从线性的 token 移除转变为结构感知的分层选择。BEAVER 通过双路径池化将变长上下文映射为稠密的页面级张量，从而最大化硬件并行性，并通过结合语义和词汇双分支选择与句子平滑的混合规划器来保持话语完整性。在四个长上下文基准测试上的广泛评估表明，BEAVER 达到了与 LongLLMLingua 等最先进（SOTA）方法相当的性能。值得注意的是，在 RULER 基准测试中，BEAVER 在基线方法性能下降的多针检索任务中保持了高保真度。在效率方面，BEAVER 在 128k 上下文中将延迟降低了 26.4 倍，为高吞吐量应用提供了一个可扩展的解决方案。我们的代码可在 https://cslikai.cn/BEAVER/ 获取。

## Abstract
The exponential expansion of context windows in LLMs has unlocked capabilities for long-document understanding but introduced severe bottlenecks in inference latency and information utilization. Existing compression methods often suffer from high training costs or semantic fragmentation due to aggressive token pruning. In this paper, we propose BEAVER, a novel training-free framework that shifts compression from linear token removal to structure-aware hierarchical selection. BEAVER maximizes hardware parallelism by mapping variable-length contexts into dense page-level tensors via dual-path pooling, and preserves discourse integrity through a hybrid planner combining semantic and lexical dual-branch selection with sentence smoothing. Extensive evaluations on four long-context benchmarks demonstrate that BEAVER achieves comparable performance to state-of-the-art (SOTA) methods like LongLLMLingua. Notably, on the RULER benchmark, BEAVER maintains high fidelity in multi-needle retrieval where baselines deteriorate. Regarding efficiency, BEAVER reduces latency by 26.4x on 128k contexts, offering a scalable solution for high-throughput applications. Our code is available at https://cslikai.cn/BEAVER/.

---

## 论文详细总结（自动生成）

### 论文总结：BEAVER — 无需训练的结构感知分层提示词压缩方法

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）上下文窗口扩展至百万级，长文档理解能力显著增强，但也带来了两大瓶颈：
*   **计算墙（Computation Wall）：** 自注意力机制的 $O(L^2)$ 复杂度导致推理延迟（尤其是首字延迟）随长度激增。
*   **信息利用率下降：** 模型常出现“迷失在中间”（Lost in the Middle）现象，无法有效利用长文本中的关键信息。
现有的提示词压缩方法要么依赖高昂的微调成本（如 LLMLingua-2），要么因激进的 Token 级剪枝导致语义碎片化，破坏了文本的连贯性。**BEAVER** 的提出旨在通过一种**无需训练、硬件友好且感知结构**的分层选择框架，在降低延迟的同时保持语义完整性。

#### 2. 方法论：核心思想与技术细节
BEAVER 将压缩范式从“线性 Token 移除”转变为“结构感知的分层页面选择”，包含三个核心组件：
*   **分段器（Segmenter）：** 利用自然分隔符（如换行符）将长文本切分为逻辑段落，并采用贪婪分页策略将其映射为固定大小的二维页面张量（Page Tensor），便于 GPU 并行处理。
*   **页面编码器（PageEncoder）：** 
    *   **双路径池化：** 结合加权平均池化（捕获全局语义）和最大池化（捕获局部显著特征）。
    *   **上下文 ITF 权重：** 引入“逆词频”（In-Context ITF）机制，自动降低高频无意义 Token 的权重，突出稀有实体和关键词。
*   **查询规划器（QueryPlanner）：**
    *   **混合匹配：** 结合语义相似度（稠密向量）和词法重叠（精确匹配）进行双分支评分。
    *   **结构化先验：** 模拟人类认知，保留**锚点页**（开头元数据）、**流动页**（查询前的上下文窗口）和**闪速页**（评分最高的关键证据）。
    *   **句子平滑：** 将选中的页面扩展至最近的句子边界，确保输出文本的语法完整。

#### 3. 实验设计
*   **数据集/场景：** 涵盖了四大长上下文基准测试：
    1.  **LongBench：** 综合性长文本理解（QA、摘要、代码等）。
    2.  **Zero-SCROLLS：** 零样本长文本推理。
    3.  **RULER：** 针对长上下文检索（如“大海捞针”）的高难度合成测试。
    4.  **L-Eval：** 跨领域（法律、金融、小说）的泛化性评估。
*   **对比方法：** 
    *   **无监督统计方法：** Selective-Context, LongLLMLingua。
    *   **有监督/专门学习方法：** LLMLingua, LLMLingua-2, SBERT, OpenAI Embeddings。

#### 4. 资源与算力
*   **硬件：** 实验在 **NVIDIA A100 (80GB) GPU** 上进行。
*   **训练时长：** 该方法为 **Training-Free（无需训练）**，因此无训练算力消耗，仅涉及推理阶段的编码开销。
*   **推理后端：** 统一采用 `gpt-3.5-turbo-instruct` 作为下游评估模型，PageEncoder 使用 `Qwen3-8B` 的嵌入层。

#### 5. 实验数量与充分性
*   **实验规模：** 论文在 4 个基准测试、数十个子任务上进行了详尽对比，涵盖了从 16k 到 128k 的不同上下文长度。
*   **消融实验：** 针对页面大小、池化方式、ITF 权重、评分分支、选择策略等进行了 10 余组消融实验，验证了各组件的必要性。
*   **公平性与客观性：** 实验严格遵守各基准测试的 Token 预算限制（如 2k/3k），并对比了不同参数规模（0.6B 到 32B）的模型，证明了方法的鲁棒性和普适性。

#### 6. 主要结论与发现
*   **性能卓越：** 在 LongBench 和 L-Eval 上达到或超过了 SOTA 学习型方法（如 LLMLingua-2）。
*   **检索能力极强：** 在 RULER 的多针检索任务中，BEAVER 保持了近乎 100% 的准确率，而基线方法在相同压缩比下性能大幅崩溃。
*   **效率惊人：** 在 128k 上下文下，BEAVER 比 LongLLMLingua 快 **26.4 倍**，且延迟随长度增长的曲线非常平缓。
*   **高可扩展性：** 在不同规模的模型（如 Qwen3-0.6B）上表现稳定，性能保留率高达 84%-98%。

#### 7. 优点
*   **无需训练：** 避免了昂贵的数据标注和模型微调，具有极强的即插即用性。
*   **硬件并行优化：** 通过二维张量化处理，充分利用 GPU 并行能力，显著降低预填充延迟。
*   **语义保真度高：** 结构化选择和句子平滑机制有效避免了 Token 级剪枝带来的“语义乱码”问题。
*   **鲁棒性强：** 结合了词法和语义匹配，对实体、数字等关键细节的保留优于纯概率模型。

#### 8. 不足与局限
*   **粒度权衡：** 页面级选择虽然保护了结构，但相比 Token 级剪枝，在极小预算下可能保留少量冗余信息。
*   **多跳推理挑战：** 检索机制仍基于相似度，对于证据与查询之间无直接重叠、需深度演绎推理的任务，可能存在局限。
*   **超参数依赖：** 尽管无需训练，但混合评分的权重（$\lambda$）和页面大小（$M$）等超参数在极端不同的领域间可能需要手动微调。

（完）
