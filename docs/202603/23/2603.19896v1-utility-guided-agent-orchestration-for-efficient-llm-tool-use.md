---
title: Utility-Guided Agent Orchestration for Efficient LLM Tool Use
title_zh: 效用引导的智能体编排，旨在实现高效的大语言模型工具使用
authors: "Boyan Liu, Gongming Zhao, Hongli Xu"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19896v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 效用引导的智能体编排以实现高效工具使用
tldr: 本研究针对LLM智能体在工具调用时面临的质量与成本权衡问题，提出了一种效用引导的编排策略。传统方法如固定工作流或ReAct往往在灵活性或效率上存在缺陷。该方法通过显式建模预期收益、执行成本、不确定性和冗余度，动态选择响应、检索或工具调用等动作。实验证明，该框架能有效控制智能体行为，在保证任务性能的同时显著降低资源消耗，为高效智能体编排提供了可分析的决策机制。
selection_source: fresh_fetch
motivation: 解决LLM智能体在工具调用过程中，答案质量与执行成本（如延迟和Token消耗）之间难以平衡的矛盾。
method: 提出一种效用引导的编排策略，通过综合评估预期收益、步骤成本、不确定性和冗余度来动态决策最优动作。
result: 实验显示，显式的编排信号能显著优化智能体行为，在多种任务场景下实现了比ReAct和固定工作流更高的效率。
conclusion: 轻量级的效用设计为工具调用型智能体提供了一个可控且实用的决策框架，有效实现了质量与成本的权衡。
---

## 摘要
使用工具的大语言模型（LLM）智能体通常在回答质量与执行成本之间面临根本性的权衡。固定的工作流虽然稳定但缺乏灵活性，而像 ReAct 这样自由形式的多步推理方法虽然可能提高任务性能，但代价是过多的工具调用、更长的轨迹、更高的 Token 消耗以及增加的延迟。在本文中，我们将智能体编排视为一个显式的决策问题，而不是完全将其留给提示词层面的行为。我们提出了一种效用引导的编排策略，通过平衡预估收益、步骤成本、不确定性和冗余，在响应、检索、工具调用、验证和停止等动作中进行选择。我们的目标并非追求普适的最佳任务性能，而是提供一个可控且可分析的策略框架，用于研究使用工具的 LLM 智能体在质量与成本之间的权衡。在直接回答、阈值控制、固定工作流、ReAct 以及几种策略变体上的实验表明，显式的编排信号会显著影响智能体的行为。对成本定义、工作流公平性和冗余控制的进一步分析表明，轻量级的效用设计可以为智能体控制提供一种合理且实用的机制。

## Abstract
Tool-using large language model (LLM) agents often face a fundamental tension between answer quality and execution cost. Fixed workflows are stable but inflexible, while free-form multi-step reasoning methods such as ReAct may improve task performance at the expense of excessive tool calls, longer trajectories, higher token consumption, and increased latency. In this paper, we study agent orchestration as an explicit decision problem rather than leaving it entirely to prompt-level behavior. We propose a utility-guided orchestration policy that selects among actions such as respond, retrieve, tool call, verify, and stop by balancing estimated gain, step cost, uncertainty, and redundancy. Our goal is not to claim universally best task performance, but to provide a controllable and analyzable policy framework for studying quality-cost trade-offs in tool-using LLM agents. Experiments across direct answering, threshold control, fixed workflows, ReAct, and several policy variants show that explicit orchestration signals substantially affect agent behavior. Additional analyses on cost definitions, workflow fairness, and redundancy control further demonstrate that lightweight utility design can provide a defensible and practical mechanism for agent control.

---

## 论文详细总结（自动生成）

### 效用引导的智能体编排：实现高效 LLM 工具使用

#### 1. 论文的核心问题与整体含义
论文探讨了使用工具的大语言模型（LLM）智能体在**回答质量**与**执行成本**（如 Token 消耗、延迟、轨迹长度）之间的根本矛盾。
*   **研究动机**：现有的智能体控制方式主要分为两类：一是**固定工作流**，虽然成本可控但过于僵化，无法应对复杂多变的中间证据；二是**自由推理（如 ReAct）**，虽然灵活且质量高，但容易产生冗余的工具调用和不必要的推理步骤。
*   **核心目标**：将智能体编排（Orchestration）从隐含的提示词行为转变为一个**显式的决策问题**，通过轻量级的效用框架实现对智能体行为的可控、可分析调节。

#### 2. 论文提出的方法论
论文提出了一种**效用引导的编排框架**，其核心思想是在每一步执行前，显式评估不同动作的“价值”。
*   **动作空间**：定义了五个核心动作：`respond`（直接回答）、`retrieve`（检索）、`tool_call`（调用工具）、`verify`（验证证据）、`stop`（停止）。
*   **效用函数**：对于状态 $s_t$ 下的动作 $a$，其效用评分公式为：
    $$U(a | s_t) = \text{Gain}(a | s_t) - \lambda_1 \text{StepCost}(a | s_t) - \lambda_2 \text{Uncertainty}(a | s_t) - \lambda_3 \text{Redundancy}(a | s_t)$$
*   **关键技术细节**：
    *   **预估收益 (Gain)**：LLM 自行评估采取该动作对最终回答质量的边际贡献。
    *   **步骤成本 (StepCost)**：标准化的步骤惩罚，用于抑制轨迹无限扩张。
    *   **不确定性 (Uncertainty)**：评估当前证据是否足以回答问题，高不确定性会促使更多检索或验证。
    *   **冗余度 (Redundancy)**：惩罚与历史动作语义相似或重复的行为。
*   **执行流程**：智能体构建状态表示 -> 计算各动作效用 -> 选择最高效用动作执行 -> 更新状态并循环，直到选择 `stop` 或达到预算上限。

#### 3. 实验设计
*   **数据集与场景**：使用了 **HotpotQA** 发展集的 200 个随机样本，专注于知识密集型问答任务。
*   **Benchmark 与对比方法**：
    *   **Direct**：直接回答，无工具使用。
    *   **Fixed Workflows**：包括 minimal（最小化）、search-twice（双重搜索）、search-verify（搜索并验证）等固定流程。
    *   **Threshold Control**：基于简单阈值的控制。
    *   **ReAct**：经典的推理-动作循环。
    *   **Policy Variants**：本文提出的效用策略及其不同成本定义（Token 成本、延迟成本）的变体。
*   **评估指标**：F1 分数（质量）、Token 消耗、墙钟时间（Wall Time）、效率得分（F1/Tokens）。

#### 4. 资源与算力
*   **模型使用**：实验主要基于 **Llama-3.1-8B** 本地部署模型进行。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量以及训练/推理的总时长。由于该方法侧重于推理侧的编排逻辑而非模型训练，其算力消耗主要体现在 LLM 的推理调用上。

#### 5. 实验数量与充分性
*   **实验规模**：在 200 个 HotpotQA 样本上进行了多轮对比实验。
*   **实验维度**：
    *   **主实验**：对比了 7 种以上的方法。
    *   **消融实验**：分别移除收益、不确定性、冗余和停止控制，验证各组件必要性。
    *   **深度分析**：探讨了推理步数与 F1 的边际效应、不同成本定义（Token vs 延迟）的影响、语义冗余 vs 精确匹配冗余。
*   **充分性评价**：实验设计较为全面，涵盖了质量、成本、公平性等多个维度。通过帕累托前沿（Pareto Frontier）图表展示了不同方法在质量-成本权衡中的位置，结果具有较强的说服力和客观性。

#### 6. 论文的主要结论与发现
*   **显式编排有效性**：显式的效用信号能显著改变智能体行为，使其在保持竞争力的 F1 分数时，比无约束的 ReAct 更具成本效益。
*   **边际收益递减**：增加推理步数最初能提升质量，但随后 Token 和延迟成本会迅速上升，而 F1 提升趋于平缓。
*   **启发式信号的价值**：虽然 Gain 和 Uncertainty 是 LLM 的自评启发式信号（非精确概率），但它们与智能体的持续执行率（Continue Rate）呈现出明显的正相关，证明了其作为决策依据的实用性。
*   **冗余控制**：语义冗余控制能有效压缩轨迹和 Token 消耗，虽然在当前实现下由于增加了语义检查步骤导致延迟略有上升。

#### 7. 优点
*   **解耦控制与推理**：将“如何做”与“是否值得做”分开，使智能体行为更具可解释性。
*   **轻量级且易迁移**：不需要复杂的强化学习训练，通过简单的效用公式即可在现有 LLM 上实现。
*   **多维度权衡分析**：论文不仅关注性能，还深入分析了成本代理（Proxy）的设计，为实际部署提供了参考。

#### 8. 不足与局限
*   **信号校准问题**：依赖 LLM 的自评（Self-estimation），这些信号未经校准，可能在不同模型或任务中表现不稳定。
*   **实验覆盖范围**：主要集中在 HotpotQA 这一类任务上，未在更复杂的长程规划或多工具协作场景（如 ToolBench）中广泛验证。
*   **性能上限**：在绝对性能（F1）上，该方法尚未能全面超越不受限的 ReAct，其优势更多体现在“效率”和“可控性”上。
*   **延迟开销**：引入额外的效用计算步骤（尤其是语义冗余检查）会带来一定的计算延迟。

（完）
