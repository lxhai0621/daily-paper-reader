---
title: "All-Mem: Agentic Lifelong Memory via Dynamic Topology Evolution"
title_zh: All-Mem：通过动态拓扑演化的智能体终身记忆
authors: "Can Lv, Heng Chang, Yuchen Guo, Shengyu Tao, Shiji Zhou"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19595v1"
tags: ["query:agent"]
score: 10.0
evidence: 通过动态拓扑演化的智能体终身记忆
tldr: 针对长期交互智能体在处理海量历史记忆时面临的冗余和噪声问题，本文提出 All-Mem 框架。该框架通过动态拓扑演化维护记忆库，利用 LLM 在离线状态下执行分裂、合并和更新操作，在保持证据不可变性的同时优化检索效率。实验证明其在长程记忆检索和问答任务上优于现有基准。
selection_source: fresh_fetch
motivation: 现有的记忆系统在处理长期历史时容易产生冗余和信息丢失，难以在固定上下文预算内提供精准检索。
method: 提出一种基于动态拓扑演化的记忆框架，通过 LLM 诊断器执行非破坏性的拓扑编辑（分裂、合并、更新）来整合记忆。
result: 在 LOCOMO 和 LONGMEMEVAL 数据集上的实验表明，All-Mem 在检索精度和问答性能上均显著优于代表性基准模型。
conclusion: All-Mem 通过显式的拓扑管理实现了高效且可追溯的终身记忆维护，为长期交互智能体提供了更可靠的记忆方案。
---

## 摘要
终身交互式智能体有望在数月或数年内为用户提供协助，这要求在固定的上下文和延迟预算下，持续写入长期记忆并为每个新查询检索正确的证据。现有的记忆系统往往随着历史记录的增长而退化，产生冗余、过时或噪声较多的检索上下文。我们提出了 All-Mem，这是一个在线/离线终身记忆框架，它通过显式的、非破坏性的整合来维护拓扑结构的记忆库，避免了基于摘要压缩所特有的不可逆信息损失。在在线运行中，它将检索锚定在有界的可见表面上，以保持粗略搜索成本的有界性。在定期离线状态下，大语言模型（LLM）诊断器会提出带有置信度评分的拓扑编辑建议，并使用 SPLIT（拆分）、MERGE（合并）和 UPDATE（更新）三个算子通过门控机制执行，同时保留不可变的证据以实现可追溯性。在查询时，类型化链接支持在需要时从活动锚点到归档证据进行跳数受限且有预算限制的扩展。在 LOCOMO 和 LONGMEMEVAL 上的实验表明，与代表性基准模型相比，该方法在检索和问答（QA）方面均有提升。

## Abstract
Lifelong interactive agents are expected to assist users over months or years, which requires continually writing long term memories while retrieving the right evidence for each new query under fixed context and latency budgets. Existing memory systems often degrade as histories grow, yielding redundant, outdated, or noisy retrieved contexts. We present All-Mem, an online/offline lifelong memory framework that maintains a topology structured memory bank via explicit, non destructive consolidation, avoiding the irreversible information loss typical of summarization based compression. In online operation, it anchors retrieval on a bounded visible surface to keep coarse search cost bounded. Periodically offline, an LLM diagnoser proposes confidence scored topology edits executed with gating using three operators: SPLIT, MERGE, and UPDATE, while preserving immutable evidence for traceability. At query time, typed links enable hop bounded, budgeted expansion from active anchors to archived evidence when needed. Experiments on LOCOMO and LONGMEMEVAL show improved retrieval and QA over representative baselines.

---

## 论文详细总结（自动生成）

### All-Mem: 基于动态拓扑演化的智能体终身记忆框架总结

#### 1. 论文的核心问题与整体含义
论文针对长期交互智能体（Lifelong Agents）在处理跨越数月甚至数年的海量历史记忆时面临的三个核心挑战：
*   **实时性约束**：复杂的记忆整理需要大量计算，难以在低延迟的在线交互循环中完成。
*   **结构漂移**：随着历史增长，记忆库会出现冗余、信息缠绕和陈旧状态，导致检索质量下降。
*   **安全维护**：传统的摘要压缩或直接覆盖会导致原始证据不可逆丢失，缺乏可追溯性。
**研究动机**：提出一种既能保持检索高效性，又能实现非破坏性记忆维护的终身记忆系统。

#### 2. 论文提出的方法论
All-Mem 借鉴了互补学习系统（CLS）理论，核心思想是**在线获取与离线整合解耦**：
*   **在线/离线解耦**：在线阶段仅进行轻量级写入和初步链接；繁重的记忆维护（如去重、拆分）移至离线阶段。
*   **可见表面（Visible Surface）**：为了控制搜索成本，向量检索仅在“可见”单元子集上进行，而归档单元通过拓扑链接保持可达性。
*   **智能体拓扑整合（ATC）**：离线时，LLM 诊断器根据置信度提出拓扑编辑建议，通过三个非破坏性算子执行：
    *   **SPLIT（拆分）**：将包含多个主题的混合单元拆分为多个兄弟单元。
    *   **MERGE（合并）**：将冗余的重复信息合并为一个代表性单元。
    *   **UPDATE（更新）**：识别并标记被替代的陈旧信息，建立版本链接。
*   **拓扑感知检索**：分为三阶段：1. 在可见表面锚定初始节点；2. 沿类型化链接（时间、语义、版本、兄弟）进行有预算的跳数扩展；3. 最终重排序与选择。

#### 3. 实验设计
*   **数据集/场景**：使用了两个长程对话记忆基准：**LOCOMO**（侧重长程对话理解）和 **LONG MEM EVAL-S**（侧重长程交互记忆）。
*   **对比方法（Baselines）**：
    *   基础方法：Full History（全历史）、Naive RAG（朴素检索）。
    *   先进记忆系统：**MemGPT**、**A-Mem**、**HippoRAG2**、**Mem0**、**LightMem**。
*   **评估指标**：检索质量（R@5, N@5）和问答质量（GPT-4o-Judge 评分、F1、BLEU、ROUGE）。

#### 4. 资源与算力
*   **模型使用**：生成器和诊断器均使用 **GPT-4o-mini**，嵌入模型使用 **All-MiniLM-L6-v2**。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。由于该方法主要依赖 API 调用（OpenAI）和向量数据库操作，其核心开销在于 LLM 的 Token 消耗。论文详细对比了 Token 成本，显示 All-Mem 在查询时的 Token 注入量显著低于 A-Mem 和 Mem0。

#### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流基准上进行了详尽测试，涵盖了检索和生成两个维度。
*   **消融实验**：非常充分。分别验证了可见表面门控、各个拓扑算子（SPLIT/MERGE/UPDATE）、检索扩展机制以及链接类型的有效性。
*   **客观性与公平性**：通过固定生成器、嵌入模型和 Token 预算（2048 tokens）来确保对比的公平性。此外，还进行了“准确率-Token 成本”的 Pareto 曲线扫描，证明了其在不同预算下的优越性。

#### 6. 论文的主要结论与发现
*   **性能领先**：All-Mem 在所有基准测试中均取得了最佳的检索和问答性能，显著优于 Mem0 等强基准。
*   **扩展性强**：随着历史长度增加，All-Mem 的检索延迟增长远低于全量搜索，证明了“可见表面”设计的有效性。
*   **可恢复性**：通过拓扑链接，95% 的归档证据可以在 4 跳之内被重新找回，实现了“归档而不丢失”。
*   **成本效益**：在达到相同准确率的情况下，All-Mem 消耗的推理 Token 更少。

#### 7. 优点
*   **非破坏性维护**：通过版本链接而非直接删除或重写来处理陈旧信息，保证了证据的完整性和可追溯性。
*   **解耦架构**：巧妙解决了在线交互延迟与离线深度整理之间的矛盾。
*   **结构化演化**：引入了类似 Zettelkasten（卡片盒）笔记法的动态拓扑，使记忆库具有自我进化的能力。
*   **预算可控**：通过跳数和候选数量限制，严格控制了检索过程的计算开销。

#### 8. 不足与局限
*   **离线维护开销**：虽然减轻了在线负担，但离线整合仍需消耗大量 LLM API 调用成本。
*   **阈值敏感性**：拓扑编辑依赖预设的置信度阈值（如 0.9），在不同领域可能需要重新调优。
*   **同步延迟**：离线整合是周期性的，这意味着最新的记忆可能需要一段时间才能被完美地整合进拓扑结构中。
*   **隐私风险**：长期存储用户详细历史并进行深度分析，可能带来更高的数据隐私和治理挑战。

（完）
