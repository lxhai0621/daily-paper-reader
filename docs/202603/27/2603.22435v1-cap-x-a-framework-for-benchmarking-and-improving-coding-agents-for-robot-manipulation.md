---
title: "CaP-X: A Framework for Benchmarking and Improving Coding Agents for Robot Manipulation"
title_zh: CaP-X：一个用于基准测试和改进机器人操纵编码智能体的框架
authors: "Max Fu, Justin Yu, Karim El-Refai, Ethan Kou, Haoru Xue, Huang Huang, Wenli Xiao, Guanzhi Wang, Fei-Fei Li, Guanya Shi, Jiajun Wu, Shankar Sastry, Yuke Zhu, Ken Goldberg, Linxi \"Jim\" Fan"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22435v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 具身操纵的自主控制器
tldr: 本研究针对机器人操作中的“代码即策略”（CaP）方法，提出了 CaP-X 开源框架。该框架包含交互式环境 CaP-Gym 和评估基准 CaP-Bench，用于系统研究代码代理。研究发现，虽然模型性能依赖人工抽象，但通过多轮交互、执行反馈和自动技能合成等测试时计算手段，可显著提升鲁棒性。基于此，作者开发了免训练的 CaP-Agent0 和结合强化学习的 CaP-RL，在仿真和真实机器人上均实现了高可靠性。
selection_source: fresh_fetch
motivation: 旨在解决“代码即策略”方法在具身机器人操作中缺乏系统评估以及过度依赖人工预设抽象的问题。
method: 构建了包含 CaP-Gym 环境、CaP-Bench 基准、CaP-Agent0 代理及 CaP-RL 强化学习算法的综合性框架 CaP-X。
result: 评估 12 种模型后发现，增加测试时计算能有效弥补低级原语带来的性能损失，使代理在复杂任务中达到人类水平。
conclusion: CaP-X 为推进具身智能代码代理的研究提供了原则性的开源平台，证明了无需大规模训练也能实现可靠的机器人控制。
---

## 摘要
“代码即策略”（Code-as-Policy）探讨了可执行代码如何补充数据密集型的视觉-语言-动作（VLA）方法，但其作为具身操纵自主控制器的有效性仍未得到充分探索。我们提出了 CaP-X，这是一个用于系统研究机器人操纵中“代码即策略”智能体的开源框架。其核心是 CaP-Gym，这是一个交互式环境，智能体通过合成和执行组合了感知与控制原语的程序来控制机器人。在此基础上，CaP-Bench 在不同的抽象级别、交互程度和感知落地方面评估了前沿的语言和视觉-语言模型。通过对 12 个模型的评估，CaP-Bench 揭示了一个一致的趋势：性能随人工设计的抽象而提高，但随这些先验知识的移除而下降，暴露了对设计者脚手架（scaffolding）的依赖。同时，我们观察到这种差距可以通过扩展智能体测试时计算（agentic test-time computation）来弥补——通过多轮交互、结构化执行反馈、视觉差异分析、自动技能合成和集成推理——即使智能体在底层原语上运行，也能显著提高鲁棒性。这些发现使我们推导出 CaP-Agent0，这是一个无需训练的框架，在模拟和真实机器人上的多项操纵任务中实现了人类水平的可靠性。我们进一步引入了 CaP-RL，表明具有可验证奖励的强化学习可以提高成功率，并以极小的差距实现从模拟到现实（sim2real）的迁移。总之，CaP-X 为推进具身编码智能体提供了一个原则性的、开源的平台。

## Abstract
"Code-as-Policy" considers how executable code can complement data-intensive Vision-Language-Action (VLA) methods, yet their effectiveness as autonomous controllers for embodied manipulation remains underexplored. We present CaP-X, an open-access framework for systematically studying Code-as-Policy agents in robot manipulation. At its core is CaP-Gym, an interactive environment in which agents control robots by synthesizing and executing programs that compose perception and control primitives. Building on this foundation, CaP-Bench evaluates frontier language and vision-language models across varying levels of abstraction, interaction, and perceptual grounding. Across 12 models, CaP-Bench reveals a consistent trend: performance improves with human-crafted abstractions but degrades as these priors are removed, exposing a dependence on designer scaffolding. At the same time, we observe that this gap can be mitigated through scaling agentic test-time computation--through multi-turn interaction, structured execution feedback, visual differencing, automatic skill synthesis, and ensembled reasoning--substantially improves robustness even when agents operate over low-level primitives. These findings allow us to derive CaP-Agent0, a training-free framework that recovers human-level reliability on several manipulation tasks in simulation and on real embodiments. We further introduce CaP-RL, showing reinforcement learning with verifiable rewards improves success rates and transfers from sim2real with minimal gap. Together, CaP-X provides a principled, open-access platform for advancing embodied coding agents.

---

## 论文详细总结（自动生成）

### 论文总结：CaP-X —— 机器人操纵编码智能体的基准测试与改进框架

#### 1. 核心问题与整体含义（研究动机和背景）
传统的机器人控制在“手动编写程序（精确但脆弱）”与“端到端视觉-语言-动作模型（VLA，泛化强但缺乏解释性且依赖大量数据）”之间存在鸿沟。近年来，“代码即策略（Code-as-Policy, CaP）”利用大语言模型（LLM）生成控制代码，试图结合两者的优点。
然而，现有 CaP 研究存在两个核心问题：
*   **过度依赖人工脚手架**：性能往往源于人类预先设计的高级 API（如 `stack_objects()`），而非智能体本身的推理能力。
*   **缺乏系统性评估**：在不同抽象级别、交互模式和感知反馈下，编码智能体的表现究竟如何尚不明确。
**CaP-X** 旨在通过提供一个统一的开源框架，系统性地研究、评估并提升编码智能体在机器人操纵中的表现。

#### 2. 论文提出的方法论
CaP-X 框架由四个核心组件构成：
*   **CaP-Gym**：一个分层控制环境，将物理模拟器（如 RoboSuite, BEHAVIOR）与 Python 代码执行器（REPL）绑定。它提供底层感知（如 SAM3 分割、Molmo 点选）和控制（如 IK 求解器）原语。
*   **CaP-Bench**：一个多维度的基准测试，涵盖 8 个评估等级（S1-S4 单轮，M1-M4 多轮），从抽象级别（高级 vs 底级）、交互（单轮 vs 多轮）和感知落地（状态、图像、视觉差异描述）三个维度进行评估。
*   **CaP-Agent0（免训练框架）**：
    *   **视觉差异模块 (VDM)**：将视觉观察转化为结构化自然语言描述，对比动作前后的变化。
    *   **自动技能库合成**：从成功的执行轨迹中自动提取并推广通用的底级逻辑，形成可复用的技能函数。
    *   **并行推理与集成**：同时采样多个模型的生成结果，并由中心智能体进行合成。
*   **CaP-RL（强化学习）**：利用可验证奖励（RLVR）和 GRPO 算法对 LLM 进行在线后训练，优化其生成控制代码的逻辑。

#### 3. 实验设计
*   **模型范围**：评估了 12 种前沿模型，包括闭源（GPT-5.2, Gemini-3-Pro, Claude 4.5 等）和开源（Qwen3-235B, DeepSeek-V3.1 等）。
*   **任务集**：
    *   **核心任务**：7 个典型操纵任务（方块提升、堆叠、擦拭、插拔等）。
    *   **扩展任务**：LIBERO-PRO（30 个长程任务）和 BEHAVIOR（移动操纵任务）。
*   **Benchmark 设定**：以人类专家编写的代码作为性能上限（Oracle），对比不同模型在 S1-M4 各个等级下的成功率。
*   **对比方法**：除了不同 LLM 之间的对比，还与 OpenVLA、$\pi_0$、$\pi_{0.5}$ 等最先进的端到端 VLA 模型进行了对比。

#### 4. 资源与算力
*   **训练细节**：在 CaP-RL 部分，对 Qwen2.5-Coder-7B 进行了 50 次迭代的后训练。
*   **算力说明**：文中**未明确列出**具体的 GPU 型号、数量或总训练时长。但提到感知和控制原语被实现为无状态服务，支持高吞吐量的并行评估。

#### 5. 实验数量与充分性
*   **实验规模**：每个任务/等级组合均进行了 **100 次试验**，总计评估了数千次模型调用，数据量足以支撑统计显著性分析。
*   **充分性**：实验涵盖了从仿真到真实机器人（Franka Panda, AgiBot G1）的迁移，并进行了详尽的消融实验（如 CaP-Agent0 的各组件贡献），验证了多轮交互、技能库和并行推理的有效性。
*   **客观性**：通过引入 S1（特权状态）等级，成功解耦了“推理失败”与“感知噪声”，使得评估更加公平。

#### 6. 主要结论与发现
*   **抽象依赖性**：模型性能随 API 抽象程度降低而显著下降，证明当前 CaP 方法高度依赖人类设计的脚手架。
*   **测试时计算的价值**：通过多轮交互（纠错）和集成推理，即使在底级原语上，智能体也能达到甚至超过人类专家的水平。
*   **感知落地的形式**：直接输入原始 RGB 图像（M2）往往会导致性能下降，而将视觉转化为**文本描述（VDM/M3）**能显著提升代码生成的准确性。
*   **RL 的有效性**：CaP-RL 能显著提升小模型的代码策略能力，且由于操作的是抽象 API，Sim-to-Real 的差距极小。

#### 7. 优点
*   **系统性强**：首次深入探讨了 API 抽象级别对编码智能体的影响。
*   **免训练高性能**：CaP-Agent0 证明了通过合理的智能体架构设计，无需微调即可在复杂任务上超越经过训练的 VLA 模型。
*   **开源贡献**：提供了统一的接口和 187 个任务，极大降低了具身智能代码代理的研究门槛。
*   **闭环能力**：强调了多轮交互和执行反馈在处理物理不确定性中的关键作用。

#### 8. 不足与局限
*   **接触密集型任务挑战**：对于需要极高频率视觉伺服或复杂接触动力学的任务（如精细插入、倾倒），纯代码生成仍显笨拙，可能需要与底层策略混合。
*   **运动规划限制**：目前依赖逆运动学（IK）插值，缺乏避障等高级运动规划原语的深度集成。
*   **推理成本**：CaP-Agent0 采用的并行采样和多轮交互虽然提升了成功率，但也显著增加了推理延迟和 API 调用成本。
*   **实验覆盖**：虽然涵盖了移动操纵，但大部分核心分析仍集中在桌面操纵场景。

（完）
