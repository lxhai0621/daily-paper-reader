---
title: "SEVerA: Verified Synthesis of Self-Evolving Agents"
title_zh: SEVerA：自我演化智能体的形式化验证合成
authors: "Debangshu Banerjee, Changming Xu, Gagandeep Singh"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25111v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 自我进化智能体的验证合成
tldr: 针对自演化LLM智能体在自主执行中缺乏安全与正确性保证的问题，本文提出SEVerA框架。该框架引入形式化保护生成模型（FGGM），通过一阶逻辑定义输出契约并结合拒绝采样与验证回退机制。SEVerA通过搜索、验证和学习三个阶段，在保证硬性形式化约束的同时，利用梯度优化提升任务效用。实验证明，SEVerA在实现零约束违规的同时，性能优于现有基准。
selection_source: fresh_fetch
motivation: 现有的自演化智能体框架在处理未知输入时缺乏形式化的安全和正确性保障，存在可靠性与安全风险。
method: 提出SEVerA框架，利用FGGM为LLM调用添加形式化契约，并通过搜索、形式化验证和基于梯度的学习优化智能体。
result: 在Dafny验证、符号数学和工具使用等任务中，SEVerA实现了零约束违规，且性能超越了无约束及最先进的基准模型。
conclusion: 形式化行为约束不仅能确保智能体的正确性，还能有效引导合成过程，从而产生更高质量的自演化智能体。
---

## 摘要
最近的进展展示了自我演化大语言模型（LLM）智能体在程序修复和科学发现等任务中的有效性。在这种范式中，规划器 LLM 合成一个调用参数化模型（包括 LLM）的智能体程序，然后针对每个任务对这些模型进行微调以提高性能。然而，现有的自我演化智能体框架无法提供安全或正确性的形式化保证。由于此类程序通常在未见过的输入上自主执行，缺乏保证引发了可靠性和安全性方面的担忧。我们将智能体代码生成建模为一个受约束的学习问题，将硬性形式化规范与捕捉任务效用的软性目标相结合。我们引入了形式化保护生成模型（FGGM），它允许规划器 LLM 使用一阶逻辑为每个生成模型调用指定形式化的输出契约。每个 FGGM 调用都将底层模型封装在一个带有经过验证的回退机制的拒绝采样器中，确保对于任何输入和参数设置，每个返回的输出都满足契约。基于 FGGM，我们提出了 SEVerA（自我演化验证智能体），这是一个三阶段框架：搜索（Search）合成包含 FGGM 调用的候选参数化程序；验证（Verification）证明所有参数值在硬约束下的正确性，将问题简化为无约束学习；学习（Learning）应用可扩展的基于梯度的优化（包括 GRPO 风格的微调），在保持正确性的同时改进软性目标。我们在 Dafny 程序验证、符号数学合成和符合策略的智能体工具使用（$τ^2$-bench）上评估了 SEVerA。在各项任务中，SEVerA 实现了零约束违规，同时性能优于无约束和 SOTA 基准模型，这表明形式化行为约束不仅能保证正确性，还能引导合成更高质量的智能体。

## Abstract
Recent advances have shown the effectiveness of self-evolving LLM agents on tasks such as program repair and scientific discovery. In this paradigm, a planner LLM synthesizes an agent program that invokes parametric models, including LLMs, which are then tuned per task to improve performance. However, existing self-evolving agent frameworks provide no formal guarantees of safety or correctness. Because such programs are often executed autonomously on unseen inputs, this lack of guarantees raises reliability and security concerns. We formulate agentic code generation as a constrained learning problem, combining hard formal specifications with soft objectives capturing task utility. We introduce Formally Guarded Generative Models (FGGM), which allow the planner LLM to specify a formal output contract for each generative model call using first-order logic. Each FGGM call wraps the underlying model in a rejection sampler with a verified fallback, ensuring every returned output satisfies the contract for any input and parameter setting. Building on FGGM, we present SEVerA (Self-Evolving Verified Agents), a three-stage framework: Search synthesizes candidate parametric programs containing FGGM calls; Verification proves correctness with respect to hard constraints for all parameter values, reducing the problem to unconstrained learning; and Learning applies scalable gradient-based optimization, including GRPO-style fine-tuning, to improve the soft objective while preserving correctness. We evaluate SEVerA on Dafny program verification, symbolic math synthesis, and policy-compliant agentic tool use ($τ^2$-bench). Across tasks, SEVerA achieves zero constraint violations while improving performance over unconstrained and SOTA baselines, showing that formal behavioral constraints not only guarantee correctness but also steer synthesis toward higher-quality agents.

---

## 论文详细总结（自动生成）

### 论文总结：SEVerA——自我演化智能体的形式化验证合成

#### 1. 论文的核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）在程序修复、科学发现等任务中的广泛应用，**自我演化智能体（Self-Evolving Agents）**成为一种新范式。在这种模式下，规划器LLM合成一个调用参数化模型（如LLM、神经网络）和外部工具（如SMT求解器）的程序，并针对特定任务进行微调。
*   **核心问题**：现有的自我演化框架缺乏形式化的安全或正确性保证。由于这些合成程序在未见过的输入上自主运行，一旦发生“幻觉”或逻辑错误，可能导致严重的可靠性和安全性风险（例如：在代码修复中删除测试用例而非修复Bug，或在工具调用中违反安全策略）。
*   **研究动机**：如何在保留自我演化框架的灵活性和性能优势的同时，为智能体提供类似传统约束引导程序合成的硬性形式化保证。

#### 2. 论文提出的方法论
论文提出了 **SEVerA** 框架，将智能体合成建模为一个**受约束的学习问题**，结合了硬性的形式化规范（Hard Constraints）和软性的任务效用目标（Soft Objectives）。

*   **核心思想：形式化保护生成模型 (FGGM)**
    *   允许规划器LLM为每个生成模型调用指定一个基于一阶逻辑（FOL）的输出契约（Contract）。
    *   **拒绝采样与回退机制**：每个FGGM调用都被封装在一个拒绝采样器中。如果模型输出不满足契约，系统会自动触发一个经过验证的非参数化“回退程序”（Fallback），确保无论模型参数如何变化，输出始终满足约束。
*   **SEVerA 的三个阶段**：
    1.  **搜索 (Search)**：规划器LLM在受限的Dafny语言空间内搜索候选程序，并定义FGGM及其局部契约。
    2.  **验证 (Verification)**：利用Dafny内置的验证器证明候选程序在所有可能的参数值下均满足全局行为规范。
    3.  **学习 (Learning)**：在保证正确性的前提下，利用基于梯度的优化（如GRPO强化学习算法）微调模型参数，以提升任务效用并减少对回退机制的依赖。

#### 3. 实验设计
论文在四个具有代表性的领域进行了评估：
*   **任务与数据集**：
    *   **受限符号回归 (SymReg)**：从噪声数据中恢复数学公式，需满足已知的符号边界约束。
    *   **Dafny程序验证 (DafnyBench)**：合成循环不变式等注释，约束是输出必须可解析且不改变原程序逻辑。
    *   **符号数学表达式合成 (GSM-Symbolic)**：生成符合特定形式语法的数学表达式。
    *   **策略合规的工具使用 ($\tau^2$-bench)**：在零售和航空场景中调用API，必须遵守退款、预订修改等领域策略。
*   **对比方法**：包括原始LLM（Qwen3-8B, Claude 4.5）、无约束的自我演化框架、SOTA约束解码方法（如CRANE）、以及专门的工具调用智能体（Agent-C）。

#### 4. 资源与算力
*   **硬件**：所有实验均在 **NVIDIA A100 GPUs (40 GB)** 上运行。
*   **训练细节**：
    *   对于开源模型（Qwen3-8B），使用了 **GRPO** 算法配合 **LoRA** 适配器进行微调。
    *   设置了明确的验证器超时（如Dafny验证每进程240秒，Z3求解器5-10秒）。
    *   搜索预算（$\Delta$）设为10次迭代，FGGM拒绝采样预算（$K$）设为5次。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从连续空间的符号回归到离散逻辑的程序验证等多种任务，数据集规模从几十个到几百个样本不等（如SymReg包含35个任务，DafnyBench包含760个测试用例）。
*   **充分性与公平性**：
    *   进行了**消融实验**（如表7），分别验证了局部契约微调、全局损失微调以及两者结合对性能的影响。
    *   对比实验考虑了闭源模型（无法微调参数）和开源模型（可微调参数）两种情况，证明了框架的通用性。
    *   实验设计较为客观，通过“约束违规率”和“任务准确率”双重指标衡量，确保了对比的公平性。

#### 6. 论文的主要结论与发现
*   **零违规保证**：SEVerA 在所有任务中均实现了 **0% 的约束违规率**，而基准方法在某些任务中违规率高达76%。
*   **性能提升**：形式化约束不仅保证了安全，还通过缩小搜索空间**引导**规划器生成更高质量的程序。例如，在Dafny验证任务上，SEVerA的成功率比最强基准高出约10%。
*   **学习有效性**：通过GRPO微调，模型能够学习直接生成满足契约的输出，从而减少拒绝采样的次数，提升了推理效率和最终准确率。

#### 7. 优点
*   **理论完备性**：提供了健全性（Soundness）证明，确保验证过的智能体在任何输入下都安全。
*   **架构灵活性**：FGGM机制兼容闭源模型（通过提示词工程）和开源模型（通过参数微调）。
*   **端到端自动化**：从自然语言描述到经过验证的Python/Dafny代码，实现了高度自动化的合成流程。
*   **回退机制**：巧妙地解决了生成模型不可预测性的问题，提供了确定性的安全底线。

#### 8. 不足与局限
*   **资源感知缺失**：目前的规范仅约束功能正确性，未考虑计算资源（如Token消耗、运行时间预算）的硬性约束。
*   **验证器依赖**：系统的有效性高度依赖于底层验证器（如Dafny）和SMT求解器的表达能力及效率，对于极其复杂的非线性约束可能面临超时问题。
*   **回退程序编写**：虽然论文展示了LLM可以合成简单的回退程序，但对于某些极其复杂的任务，寻找一个既简单又满足契约的非参数化回退程序可能具有挑战性。
*   **应用限制**：目前主要集中在代码和逻辑任务，对于缺乏明确形式化定义的开放域对话任务适用性有限。

（完）
