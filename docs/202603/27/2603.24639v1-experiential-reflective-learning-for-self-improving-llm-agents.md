---
title: Experiential Reflective Learning for Self-Improving LLM Agents
title_zh: 面向自我提升型大语言模型智能体的经验反思学习
authors: "Marc-Antoine Allard, Arnaud Teinturier, Victor Xing, Gautier Viaud"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24639v1"
tags: ["query:agent"]
score: 9.0
evidence: 从过去的交互中检索相关的启发式方法以指导智能体执行
tldr: 针对LLM智能体在特定环境中缺乏适应性且无法利用过往经验的问题，本文提出经验反思学习（ERL）框架。该框架通过反思任务执行轨迹和结果来生成可迁移的启发式规则，并在测试阶段根据当前任务检索并注入相关规则以指导执行。实验表明，ERL在Gaia2基准测试中显著提升了任务成功率和可靠性，证明了从单次经验中提取抽象规则对智能体自我改进的有效性。
selection_source: fresh_fetch
motivation: 当前的LLM智能体难以适应专业环境，且无法从过去的交互中积累经验，导致处理新任务时效率低下。
method: 提出ERL框架，通过反思任务轨迹生成可操作的启发式规则，并在推理时动态检索相关规则以增强智能体上下文。
result: "在Gaia2基准测试中，ERL比ReAct基线成功率提升了7.8%，且在任务可靠性上优于先前的经验学习方法。"
conclusion: 通过反思单次尝试提取可迁移的启发式规则，是实现智能体在复杂环境中自我改进和快速适应的有效手段。
---

## 摘要
大语言模型（LLMs）的最新进展使得开发能够进行复杂推理和多步问题解决的自主智能体成为可能。然而，这些智能体难以适应特定环境，且未能利用过去的交互经验，无论积累了多少经验，在面对新任务时往往都从零开始。我们提出了经验反思学习（Experiential Reflective Learning, ERL），这是一个简单的自我提升框架，通过经验学习实现快速的环境适应。ERL 通过对任务轨迹和结果进行反思来生成启发式策略，捕捉可在不同任务间迁移的可操作教训。在测试阶段，系统根据当前任务检索相关的启发式策略，并将其注入智能体的上下文以指导执行。在 Gaia2 基准测试中，ERL 的成功率比 ReAct 基准提高了 7.8%，在任务完成可靠性方面有显著提升，并优于先前的经验学习方法。通过系统的消融实验，我们发现选择性检索至关重要，且启发式策略比少样本轨迹提示提供了更具迁移性的抽象。这些结果表明，通过反思单次尝试的经验来提取可迁移的启发式策略，能够实现有效的智能体自我提升。

## Abstract
Recent advances in large language models (LLMs) have enabled the development of autonomous agents capable of complex reasoning and multi-step problem solving. However, these agents struggle to adapt to specialized environments and do not leverage past interactions, approaching each new task from scratch regardless of their accumulated experience. We introduce Experiential Reflective Learning (ERL), a simple self-improvement framework that enables rapid environment adaptation through experiential learning. ERL reflects on task trajectories and outcomes to generate heuristics, capturing actionable lessons that transfer across tasks. At test time, relevant heuristics are retrieved based on the current task and injected into the agent's context to guide execution. On the Gaia2 benchmark, ERL improves success rate by 7.8% over a ReAct baseline, with large gains in task completion reliability, and outperforms prior experiential learning methods. Through systematic ablations, we find that selective retrieval is essential and that heuristics provide more transferable abstractions than few-shot trajectory prompting. These results demonstrate that reflecting on single-attempt experiences to extract transferable heuristics enables effective agent self-improvement.

---

## 论文详细总结（自动生成）

以下是对论文《Experiential Reflective Learning for Self-Improving LLM Agents》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管 LLM 智能体在复杂推理上表现出色，但在面对特定环境（如新工具、领域规范）时缺乏适应能力。
*   **核心痛点**：现有智能体通常是“无记忆”的，每次处理新任务都从零开始，无法利用过往的成功或失败经验。
*   **研究目标**：开发一种简单、无需参数微调（Parameter-free）的自我提升框架，使智能体能从单次任务尝试中提取可迁移的知识，实现持续学习。

### 2. 方法论：经验反思学习 (ERL)
ERL 框架由两个核心阶段组成：
*   **启发式生成 (Heuristic Generation)**：
    *   智能体在完成任务后（无论成功或失败），会对任务描述、执行轨迹（推理步骤、工具调用）和结果反馈进行“事后剖析”。
    *   生成结构化的**启发式规则 (Heuristic)**，包含：1) **分析**（定位逻辑故障点或获胜关键）；2) **学习指南**（明确触发条件和建议动作，例如：“当发送邮件给联系人时，先通过通讯录工具解析姓名”）。
*   **检索增强执行 (Retrieval-augmented Execution)**：
    *   面对新任务时，系统使用 LLM 作为评分器，从启发式池中检索最相关的 Top-k 条规则。
    *   检索标准结合了任务相似性、经验多样性和指南的信息量。
    *   将选中的启发式规则注入智能体的系统提示词（System Prompt）中，指导其执行。

### 3. 实验设计
*   **主要数据集**：**Gaia2**（Search 和 Execution 分片），这是一个模拟移动环境的基准，包含 12 个应用和 101 个工具。
*   **辅助数据集**：**$\tau^2$-bench**（涵盖航空、零售、电信三个客户服务领域），用于评估智能体与用户协作的能力。
*   **基准 (Benchmark)**：默认的 **ReAct** 智能体框架。
*   **对比方法**：
    *   **ExpeL**：通过对比成功/失败轨迹提取见解。
    *   **AutoGuide**：生成上下文感知的指南。
    *   **Few-shot Prompting**：直接在上下文中提供原始的任务执行轨迹作为示例。

### 4. 资源与算力
*   **模型使用**：实验采用了 **GPT-5-mini** 作为智能体骨干网络、判断器和启发式生成模型；使用 **GPT-5.2** 进行启发式检索。
*   **算力细节**：文中**未明确提及**具体的 GPU 型号、数量或训练时长。由于该方法是无参数的（Parameter-free），其开销主要集中在 API 调用和 Token 消耗上。
*   **成本分析**：ERL 相比基线增加了约 40% 的 API 成本，主要源于检索到的启发式规则增加了输入 Token 数（输入 Token 增加了约 85%）。

### 5. 实验数量与充分性
*   **实验规模**：在 Gaia2 的 8 个“宇宙”（训练集）上积累经验，在 2 个独立“宇宙”（测试集）上评估。每个任务运行 3 次以计算平均成功率、Pass@3 和 Pass^3。
*   **消融实验**：涵盖了检索方法（LLM vs 嵌入向量 vs 随机）、启发式数量 $k$ 的影响、仅学习失败 vs 仅学习成功经验的对比、以及无奖励信号时的表现。
*   **充分性评价**：实验设计较为全面，通过跨领域（Gaia2 和 $\tau^2$-bench）验证了泛化性，并深入探讨了启发式规则优于原始轨迹的原因，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能提升**：ERL 在 Gaia2 上比 ReAct 基线成功率提升了 **7.8%**，且显著优于 ExpeL 和 AutoGuide。
*   **可靠性增强**：ERL 在 **Pass^3**（连续三次成功的概率）指标上提升显著，表明学习到的规则使智能体表现更稳定。
*   **抽象胜于原始**：经过反思提取的“启发式规则”比原始的“少样本轨迹”更具迁移性，且在相同 Token 预算下效果更好。
*   **经验来源差异**：**失败经验**对“搜索类”任务帮助更大（提供负面约束）；**成功经验**对“执行类”任务更有利（强化证明有效的序列）。
*   **检索至关重要**：LLM 检索优于向量检索，且盲目增加启发式数量（超过 60 条）反而会导致性能下降。

### 7. 优点
*   **高效简洁**：无需昂贵的模型微调，仅通过 Prompt 工程即可实现持续改进。
*   **单次学习能力**：能够从单次任务尝试中提取教训，不依赖大规模对比数据集。
*   **结构化知识**：生成的启发式规则具有可解释性，易于人类理解和调试。
*   **环境适应快**：特别适合在工具繁多、规则复杂的专业环境中快速部署。

### 8. 不足与局限
*   **Token 开销**：检索到的规则显著增加了上下文长度，导致推理成本上升。
*   **依赖反思质量**：如果底层 LLM 无法准确分析失败原因，生成的启发式规则可能会产生误导。
*   **复杂交互限制**：在涉及复杂用户协作（如 $\tau^2$-bench 的电信领域）时，由于用户行为的不可预测性，固定规则的效果会打折扣。
*   **冲突处理**：随着经验池扩大，如何处理相互冲突或过时的启发式规则仍是未来挑战。

（完）
