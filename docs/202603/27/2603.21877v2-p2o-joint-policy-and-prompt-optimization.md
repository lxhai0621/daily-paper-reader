---
title: "P^2O: Joint Policy and Prompt Optimization"
title_zh: P^2O：策略与提示词联合优化
authors: "Xinyu Lu, Kaiqi Zhang, Jinglin Yang, Boxi Cao, Yaojie Lu, Hongyu Lin, Min He, Xianpei Han, Le Sun"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21877v2"
tags: ["query:ma-kf"]
score: 9.0
evidence: 协同提示词优化与策略优化
tldr: 针对大语言模型在强化学习（RLVR）中面临的“困难样本”探索效率低、奖励稀疏问题，本文提出P^2O框架。该框架结合了提示词优化与策略优化，利用GEPA算法为困难样本生成引导性提示词，从而产生更密集的监督信号。通过将提示词带来的推理增益蒸馏至模型参数中，P^2O显著提升了模型的推理能力和泛化性能。
selection_source: fresh_fetch
motivation: 传统的RLVR在面对成功率接近零的困难样本时，由于奖励稀疏导致模型缺乏有效的监督信号，难以进行有效学习。
method: 提出P^2O框架，利用遗传帕累托（GEPA）算法优化提示词以引导模型探索，并将优化后的推理路径蒸馏到模型参数中。
result: "实验表明P^2O在分布内数据集上表现优异，且在分布外基准测试中平均提升了4.7%。"
conclusion: 结合提示词优化与策略优化能有效解决强化学习中的探索难题，显著增强大语言模型的推理与泛化能力。
---

## 摘要
具有可验证奖励的强化学习（RLVR）已成为增强大语言模型（LLM）推理能力的强大范式。然而，原生 RLVR 面临探索效率低下的问题，尤其是在面对成功率接近于零的“困难样本”时。在此类场景下，对稀疏结果奖励的依赖通常会导致零优势估计，尽管这些实例具有很高的信息价值，但模型实际上却缺乏监督信号。为解决这一问题，我们提出了 P^2O，这是一个协同提示词优化（Prompt Optimization）与策略优化（Policy Optimization）的新型框架。P^2O 在训练迭代过程中识别困难样本，并利用 GeneticPareto (GEPA) 提示词优化算法来演化提示词模板，从而引导模型发现成功的轨迹。至关重要的是，与依赖输入增强的传统提示词工程方法不同，P^2O 将这些优化提示词所带来的推理增益直接蒸馏到模型参数中。该机制为困难样本提供了更密集的正向监督信号，并加速了收敛。大量实验表明，P^2O 不仅在分布内数据集上取得了卓越性能，还表现出强大的泛化能力，在分布外基准测试中实现了显著提升（平均提升 4.7%）。

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) has emerged as a powerful paradigm for enhancing the reasoning capabilities of Large Language Models (LLMs). However, vanilla RLVR suffers from inefficient exploration, particularly when confronting "hard samples" that yield nearzero success rates. In such scenarios, the reliance on sparse outcome rewards typically results in zero-advantage estimates, effectively starving the model of supervision signals despite the high informational value of these instances. To address this, we propose P^2O, a novel framework that synergizes Prompt Optimization with Policy Optimization. P^2O identifies hard samples during training iterations and leverages the GeneticPareto (GEPA) prompt optimization algorithm to evolve prompt templates that guide the model toward discovering successful trajectories. Crucially, unlike traditional prompt engineering methods that rely on input augmentation, P^2O distills the reasoning gains induced by these optimized prompts directly into the model parameters. This mechanism provides denser positive supervision signals for hard samples and accelerates convergence. Extensive experiments demonstrate that P^2O not only achieves superior performance on in-distribution datasets but also exhibits strong generalization, yielding substantial improvements on out-of-distribution benchmarks (+4.7% avg.).

---

## 论文详细总结（自动生成）

以下是对论文《P^2O: Joint Policy and Prompt Optimization》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在“具有可验证奖励的强化学习”（RLVR）中，模型常面临**探索瓶颈**。当训练集中包含“困难样本”（即模型在多次采样中成功率为零的题目）时，由于奖励极其稀疏，模型无法获得有效的梯度信号（零优势估计），导致模型在简单样本上过拟合，而在复杂推理任务上停滞不前。
*   **研究背景**：传统的课程学习或奖励塑造方法需要昂贵的人工启发式规则。本文受到提示词工程（Prompt Engineering）的启发，认为模型其实具备解决困难问题的潜能，只是在标准策略下难以被激活。

### 2. 方法论：核心思想与关键技术
P^2O 提出了**策略优化与提示词优化联合迭代**的框架，核心思想是将提示词视为动态的潜在变量，通过“跳出局部最优”和“内化推理能力”两个步骤循环：
*   **阶段一：策略优化与上下文蒸馏（Context Distillation）**
    *   **困难样本挖掘**：在训练中识别成功率低于阈值 $\tau$ 的样本。
    *   **蒸馏机制**：对于困难样本，使用阶段二优化出的提示词 $z$ 增强输入 $\tilde{x}$，引导模型生成正确轨迹。关键在于：**生成时使用增强输入，但计算策略梯度时使用原始输入 $x$**。这样模型就能将提示词诱导出的推理模式“内化”到参数中，脱离对提示词的依赖。
*   **阶段二：进化提示词优化（GEPA 算法）**
    *   **反射进化**：利用一个“反射 LLM”（可以是自身或更强的老师模型）分析失败案例，在语义空间进行“梯度下降”，生成改进的提示词变体。
    *   **帕累托选择（Pareto Selection）**：维护一个多样化的提示词种群，通过帕累托前沿筛选出在不同错误模式上表现优异的提示词集合。
    *   **贪婪分配**：通过贪婪集合覆盖算法，为每个困难样本分配最合适的提示词模板。

### 3. 实验设计
*   **数据集**：从 DeepScaler 和 DeepMath 中各抽取 5,000 个样本（DeepScaler-5K, DeepMath-5K）。
*   **Benchmark（基准测试）**：涵盖 6 个极具挑战性的数学竞赛数据集：AIME24、AIME25、AMC、MATH500、MINERVA、OLYMPIAD。
*   **对比方法**：
    *   基座模型：Qwen3-0.6B/1.7B/4B。
    *   主流算法：GRPO（Group Relative Policy Optimization）。
    *   变体对比：P^2O Self-Ref（自反射）和 P^2O Teacher-Ref（老师模型反射，使用 Kimi-K2）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量。
*   **训练细节**：模型为 Qwen3-4B，训练 5 个 epoch，全局 Batch Size 为 128，学习率 $1 \times 10^{-6}$，最大生成长度 12k tokens。在提示词优化阶段，GEPA 的候选池大小（Beam Size）设为 16。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同的 5K 训练集上进行了完整训练，并在 6 个分布外（OOD）基准上验证。
*   **消融实验**：
    1.  **上下文蒸馏的必要性**：对比了“直接在带提示词的数据上训练”与“蒸馏到原始输入”，证明了直接训练会导致模型产生提示词依赖，泛化性变差。
    2.  **提示词多样性**：证明了在同一组（Group）中使用多种帕累托最优提示词比使用单一提示词效果更好。
*   **充分性评价**：实验设计较为充分，不仅涵盖了准确率对比，还深入分析了训练曲线、困难样本数量下降趋势以及定性的案例分析，客观地展示了方法在极难题目（如 AIME）上的突破。

### 6. 主要结论与发现
*   **性能飞跃**：P^2O 在所有基准上均优于 GRPO。在 DeepScaler-5K 上，平均准确率提升 4.7%；在极难的 AIME24/25 上，提升幅度分别达到 12.9% 和 11.7%。
*   **打破瓶颈**：实验证明，通过提示词优化发现的成功轨迹可以作为高质量的监督信号，有效解决了 RLVR 中的探索死锁问题。
*   **反射源的影响**：在某些任务中，模型自反射（Self-Ref）的效果甚至优于外部老师模型，说明模型有时更易于内化符合自身推理逻辑的改进建议。

### 7. 优点（亮点）
*   **内化能力**：不同于传统的提示词工程（只在推理时有用），P^2O 通过蒸馏将提示词带来的增益永久固化在模型参数中。
*   **自动化探索**：利用 GEPA 实现了提示词的自动演化，无需人工干预即可针对特定困难样本生成引导信息。
*   **泛化性强**：在分布外数据集上的显著提升证明了该方法学到的是通用的推理能力，而非简单的模板匹配。

### 8. 不足与局限
*   **领域局限**：目前仅在数学推理任务上验证，对于缺乏客观验证机制（Verifiable Rewards）的通用对话或创意写作任务，其适用性尚待研究。
*   **计算开销**：虽然内化后的推理不增加开销，但在训练过程中，GEPA 阶段需要额外的 LLM 推理来生成反射和变异提示词，增加了训练阶段的算力负担。
*   **超参数敏感性**：困难样本的定义（阈值 $\tau$ 和采样数 $K$）以及提示词进化的频率可能对最终效果有较大影响，论文未深入讨论这些超参数的鲁棒性。

（完）
