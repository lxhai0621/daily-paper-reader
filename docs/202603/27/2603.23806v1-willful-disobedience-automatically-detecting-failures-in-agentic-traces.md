---
title: "Willful Disobedience: Automatically Detecting Failures in Agentic Traces"
title_zh: 蓄意违抗：自动检测智能体轨迹中的故障
authors: "Reshabh K Sharma, Shraddha Barke, Benjamin Zorn"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.23806v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 检测智能体轨迹中的失败和提示合规性
tldr: 随着AI代理在复杂工作流中的应用，仅关注结果的评估方法难以捕捉过程中的程序性失败。本文提出AgentPex，这是一种利用AI自动检测代理执行轨迹中违规行为的工具。它从系统指令中提取行为规则，并据此评估代理的合规性。在电信、零售等领域的实验表明，AgentPex能发现结果评分无法察觉的规范违规，为开发者提供细粒度的行为分析，助力提升代理的可靠性。
selection_source: fresh_fetch
motivation: 传统的基于结果的基准测试无法检测到AI代理在执行过程中出现的错误路由、不安全工具调用或违反提示词规则等程序性失败。
method: 提出AgentPex工具，通过从代理提示词和系统指令中自动提取行为规范，并据此对执行轨迹进行合规性评估。
result: 在τ2-bench的424个轨迹上的评估显示，AgentPex能有效区分不同模型的行为，并揭示出结果评分无法捕捉的规范违规。
conclusion: AgentPex实现了对代理行为的大规模细粒度分析，帮助开发者深入理解代理在不同领域和指标下的优劣势。
---

## 摘要
AI 智能体正越来越多地嵌入到真实的软件系统中，它们通过多轮对话、工具调用和中间决策来执行多步工作流。这些被称为“智能体轨迹”（agentic traces）的长执行历史使得验证变得困难。仅关注结果的基准测试可能会遗漏关键的过程性故障，例如错误的工作流路由、不安全的工具使用或违反提示词指定的规则。本文介绍了 AgentPex，这是一款旨在系统评估智能体轨迹的 AI 驱动工具。AgentPex 从智能体提示词和系统指令中提取行为规则，然后利用这些规范自动评估轨迹的合规性。我们在来自 τ2-bench 的 424 条轨迹上评估了 AgentPex，涵盖了电信、零售和航空公司客户服务领域的模型。我们的结果表明，AgentPex 能够区分不同模型的智能体行为，并发现仅靠结果评分无法捕获的规范违规行为。它还提供了按领域和指标划分的细粒度分析，使开发人员能够大规模地了解智能体的优缺点。

## Abstract
AI agents are increasingly embedded in real software systems, where they execute multi-step workflows through multi-turn dialogue, tool invocations, and intermediate decisions. These long execution histories, called agentic traces, make validation difficult. Outcome-only benchmarks can miss critical procedural failures, such as incorrect workflow routing, unsafe tool usage, or violations of prompt-specified rules. This paper presents AgentPex, an AI-powered tool designed to systematically evaluate agentic traces. AgentPex extracts behavioral rules from agent prompts and system instructions, then uses these specifications to automatically evaluate traces for compliance. We evaluate AgentPex on 424 traces from τ2-bench across models in telecom, retail, and airline customer service. Our results show that AgentPex distinguishes agent behavior across models and surfaces specification violations that are not captured by outcome-only scoring. It also provides fine-grained analysis by domain and metric, enabling developers to understand agent strengths and weaknesses at scale.

---

## 论文详细总结（自动生成）

### 论文分析报告：AgentPex —— 自动检测智能体执行轨迹中的“蓄意违抗”

#### 1. 论文的核心问题与整体含义（研究动机和背景）
随着 AI 智能体（Agents）在复杂软件系统（如 GitHub Copilot、客服系统）中的广泛应用，它们不再是简单的单轮对话，而是涉及多步推理、工具调用和中间决策的长序列执行，即**智能体轨迹（Agentic Traces）**。
目前的评估方法存在三大痛点：
*   **多步执行的不透明性**：长轨迹使得评估意图和正确性变得极其困难。
*   **“蓄意违抗”（Willful Disobedience）**：智能体虽然最终完成了任务（结果正确），但在过程中可能违反了系统提示词中的关键规则（如跳过必要的计算工具、泄露内部推理、违反安全策略等）。
*   **部署规模下的评估难题**：手动评估每日成千上万条轨迹是不现实的。
本文提出的 **AgentPex** 旨在通过 AI 驱动的自动化手段，从提示词中提取规范并系统性地审计智能体执行过程的合规性。

#### 2. 论文提出的方法论
AgentPex 的核心思想是将“提示词视为程序规范”，通过三个阶段实现自动化评估：
*   **阶段 1：轨迹标准化（Trace Normalization）**：将来自不同来源（如 OpenAI 格式、VS Code 日志）的异构轨迹统一转化为包含系统提示词、工具 Schema 和消息日志的标准格式。
*   **阶段 2：规范提取（Specification Extraction）**：利用 LLM 从系统提示词和任务描述中提取两类规范：
    *   **策略规范（Policy Specs）**：包括输出规范（格式、风格）、转换规范（步骤顺序）、禁止边缘（禁止的工具调用序列）和参数规范。
    *   **任务特定规范（Task-Specific Specs）**：基于用户请求预测执行计划（Predicted Plan）和预期的最终状态（Final State）。
*   **阶段 3：轨迹评估与聚合（Trace Evaluation & Aggregation）**：
    *   使用 **LLM-as-a-judge** 模式，根据提取的规范对轨迹进行逐项打分（0-100分）。
    *   **关键技术细节**：采用**门控最小聚合策略（Gated-minimum aggregation）**。最终得分是所有指标的加权平均值，但受限于“关键指标”（如预测计划、输出规范）中的最低分。这确保了智能体即使在次要指标上表现完美，若犯了原则性错误也无法获得高分。

#### 3. 实验设计
*   **数据集/场景**：使用了 **$\tau^2$-bench**，这是一个模拟真实双向互动的客服基准测试，涵盖**航空、零售、电信**三个领域。
*   **Benchmark**：对比了 $\tau^2$-bench 的原生评估指标（基于最终数据库状态和通信内容的二元奖励 Reward）。
*   **对比模型**：在相同任务下生成并评估了三个模型的轨迹：**Claude 3.5 Sonnet**、**GPT-4.1**（注：文中指代特定版本）和 **o4-mini**。
*   **评估机**：使用 `gpt-5-mini-2025-08-07` 作为规范提取和评估的底层模型。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件资源（如 GPU 型号或数量）以及训练时长。
*   **原因分析**：由于 AgentPex 是一个基于推理的评估框架，而非训练框架，其核心开销在于调用 LLM API 进行规范提取和打分。论文重点讨论了评估的逻辑流程而非底层算力消耗。

#### 5. 实验数量与充分性
*   **实验规模**：共分析了 **424 条** 有效智能体轨迹（初始 450 条，过滤掉不完整响应后）。
*   **充分性评价**：
    *   **多维度覆盖**：实验涵盖了 3 个不同能力的模型和 3 个垂直行业领域，具有较好的代表性。
    *   **客观性**：通过 ROC-AUC 曲线分析了 AgentPex 评分与人工标注（$\tau^2$ Ground Truth）的相关性，证明了其作为无需人工标注的代理指标的有效性。
    *   **细粒度分析**：不仅给出了总分，还深入分析了不同模型在特定违规类型（如“同时输出文本与工具调用”）上的差异，实验设计较为客观、公平。

#### 6. 论文的主要结论与发现
*   **结果与过程的脱节**：在 Claude 3.5 Sonnet 获得满分（1.0 Reward）的轨迹中，**83% 实际上包含至少一种程序性违规**（如跳过验证步骤、违反输出格式）。
*   **模型行为差异**：
    *   **Claude 3.5**：倾向于在调用工具时附带多余的解释文本，且容易产生主观评论。
    *   **GPT-4.1**：更容易出现语义幻觉（提供不存在的信息）或单轮批量调用多个工具。
    *   **o4-mini**：工作流路由能力较强，但容易跳过关键的身份验证步骤。
*   **互补性**：AgentPex 虽然不能完全取代人工定义的最终状态检查，但它能提供关键的过程审计信号，这对于生产环境的安全和可追溯性至关重要。

#### 7. 优点
*   **无需人工标注**：完全从现有的提示词和工具定义中自动生成评估准则，极大地降低了评估成本。
*   **揭示“隐性失败”**：成功识别了那些“结果正确但过程危险”的智能体行为，填补了现有基准测试的空白。
*   **可解释性**：每个评分都附带详细的推理过程（Reasoning），方便开发者进行针对性的提示词优化。

#### 8. 不足与局限
*   **评估成本**：每条轨迹需要多次 LLM 调用（提取+多次评估），在大规模实时监控中可能存在成本和延迟压力。
*   **规范重叠与双重扣分**：由于规范是从自然语言提取的，不同类别（如输出规范与转换规范）可能存在重叠，导致同一错误被多次扣分。
*   **领域局限性**：目前主要在客服领域验证，对于代码生成、自主科研等更开放、长程的智能体任务，其预测最终状态的能力可能受限。
*   **幻觉风险**：评估机本身（LLM-as-a-judge）可能存在幻觉，尤其是在处理极其复杂的逻辑依赖时。

（完）
