---
title: "Adaptive Chunking: Optimizing Chunking-Method Selection for RAG"
title_zh: 自适应分块：优化 RAG 的分块方法选择
authors: "Paulo Roberto de Moura Júnior, Jean Lelong, Annabelle Blangero"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25333v1"
tags: ["query:ma-kf"]
score: 10.0
evidence: 优化分块方法选择以提升RAG系统的有效性
tldr: 本研究针对检索增强生成（RAG）中分块策略缺乏灵活性和评估标准的问题，提出了自适应分块（Adaptive Chunking）框架。该框架通过五项新颖的内在指标（如引用完整性、块内凝聚力等）为不同文档动态选择最优分块策略，并引入了LLM正则分割器和递归合并分割器。实验表明，该方法在不改变模型的情况下显著提升了RAG的回答准确率和成功率，为构建鲁棒的RAG系统提供了新路径。
selection_source: fresh_fetch
motivation: 现有的分块方法多为通用型，难以捕捉多样化文本的复杂结构，且缺乏专门的评估框架来独立衡量分块质量。
method: 提出一种自适应框架，基于五种新颖的内在指标评估分块质量，并结合新型LLM正则分割器和递归合并分割器为每个文档选择最佳策略。
result: "在法律、技术等多个领域的测试中，该方法将RAG回答准确率从约64%提升至72%，并使成功回答的问题数量增加了30%以上。"
conclusion: 通过内在指标引导的自适应分块策略能显著增强RAG系统的性能，证明了文档感知型分块在信息检索中的关键作用。
---

## 摘要
检索增强生成 (RAG) 的有效性高度依赖于文档的分块方式，即为了索引和检索而将文档分割成较小单元的过程。然而，常用的“一刀切”方法往往无法捕捉到不同文本的细微结构和语义。尽管分块起着核心作用，但它缺乏专门的评估框架，导致难以独立于下游性能来评估和比较各种策略。我们通过引入“自适应分块”（Adaptive Chunking）来挑战这一范式，该框架根据五项新颖的、基于文档的内在指标为每个文档选择最合适的分块策略：引用完整性 (RC)、块内凝聚力 (ICC)、文档上下文连贯性 (DCC)、块完整性 (BI) 和尺寸合规性 (SC)，这些指标直接从关键维度评估分块质量。为了支持该框架，我们还引入了两种新的分块器：LLM-regex 分割器和“先分割后合并”的递归分割器，以及针对性的后处理技术。在涵盖法律、技术和社会科学领域的多元语料库上，我们的指标引导自适应方法显著提高了下游 RAG 的性能。在不改变模型或提示词的情况下，我们的框架提升了 RAG 的结果，将答案正确率从 62-64% 提高到 72%，并将成功回答的问题数量增加了 30% 以上（从 49 个增加到 65 个）。这些结果表明，在互补的内在指标套件引导下，这种具有文档感知能力的自适应分块为构建更稳健的 RAG 系统提供了一条实用且有效的途径。代码可在 https://github.com/ekimetrics/adaptive-chunking 获取。

## Abstract
The effectiveness of Retrieval-Augmented Generation (RAG) is highly dependent on how documents are chunked, that is, segmented into smaller units for indexing and retrieval. Yet, commonly used "one-size-fits-all" approaches often fail to capture the nuanced structure and semantics of diverse texts. Despite its central role, chunking lacks a dedicated evaluation framework, making it difficult to assess and compare strategies independently of downstream performance. We challenge this paradigm by introducing Adaptive Chunking, a framework that selects the most suitable chunking strategy for each document based on a set of five novel intrinsic, document-based metrics: References Completeness (RC), Intrachunk Cohesion (ICC), Document Contextual Coherence (DCC), Block Integrity (BI), and Size Compliance (SC), which directly assess chunking quality across key dimensions. To support this framework, we also introduce two new chunkers, an LLM-regex splitter and a split-then-merge recursive splitter, alongside targeted post-processing techniques. On a diverse corpus spanning legal, technical, and social science domains, our metric-guided adaptive method significantly improves downstream RAG performance. Without changing models or prompts, our framework increases RAG outcomes, raising answers correctness to 72% (from 62-64%) and increasing the number of successfully answered questions by over 30% (65 vs. 49). These results demonstrate that adaptive, document-aware chunking, guided by a complementary suite of intrinsic metrics, offers a practical and effective path to more robust RAG systems. Code available at https://github.com/ekimetrics/adaptive-chunking.

---

## 论文详细总结（自动生成）

这是一份关于论文《Adaptive Chunking: Optimizing Chunking-Method Selection for RAG》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在检索增强生成（RAG）系统中，文档分块（Chunking）是影响检索质量的关键，但目前普遍采用“一刀切”的固定策略，无法适应不同结构和语义特征的文档。
*   **研究动机**：
    *   **缺乏评估标准**：目前分块质量通常只能通过下游任务（如回答准确率）间接衡量，缺乏独立的内在评估指标。
    *   **上下文断裂**：固定长度或简单的递归分割容易切断语义联系（如代词与其指代对象分离）或破坏结构完整性（如切断表格）。
    *   **性能瓶颈**：不合理的分块会导致检索到的上下文包含过多噪音或信息缺失，限制了 LLM 的发挥。

### 2. 方法论：自适应分块框架
该论文提出了 **Adaptive Chunking** 框架，核心思想是：**针对每份文档，从多种分块策略中动态选择内在指标得分最高的一种。**

*   **五项内在评估指标**：
    1.  **引用完整性 (RC)**：利用共指消解模型，衡量实体及其代词是否被保留在同一个块内。
    2.  **块内凝聚力 (ICC)**：计算块内各句子嵌入向量与块整体向量的余弦相似度，衡量语义一致性。
    3.  **文档上下文连贯性 (DCC)**：衡量分块与其周围局部上下文（滑动窗口）的语义对齐程度。
    4.  **块完整性 (BI)**：衡量段落、表格、标题等结构单元是否被完整保留。
    5.  **尺寸合规性 (SC)**：衡量分块长度是否落在预设的理想区间（如 100-1100 token）。
*   **新增分块技术**：
    *   **LLM-Regex 分割器**：利用 LLM 分析文档前几千个 token，生成针对该文档结构的正则表达式（如识别特定的法律条款编号），再进行确定性分割。
    *   **先分割后合并递归分割器 (Split-then-Merge)**：先按优先级（标题 > 段落 > 句子）细分，再贪婪地合并相邻小块，以减少碎片化。
*   **后处理优化**：引入“超大块再分割”和“极小块合并”机制，确保所有分块在尺寸上符合嵌入模型的最优处理范围。

### 3. 实验设计
*   **数据集**：包含 33 份来自法律、技术和社会科学领域的真实 PDF 文档，总计约 118 万 token。
*   **对比基准 (Baselines)**：
    *   LangChain 默认递归分割器 (RecursiveCharacterTextSplitter)。
    *   基于页面的分割 (Page-based)。
    *   语义分割器 (Semantic Chunking)。
    *   基于固定句数的分割 (Sentence-based)。
*   **RAG 流程**：采用混合搜索（BM25 + 向量检索）+ 重排序（Reranking）+ GPT-4.1 生成答案。
*   **评估指标**：除了内在指标，还使用 GPT-4 作为裁判评估“检索完整性”和“答案正确率 (G-Eval)”。

### 4. 资源与算力
*   **模型使用**：使用了 `GPT-4.1` (用于生成和评估)、`Qwen3-Embedding-4B` (用于检索嵌入)、`jina-embeddings-v3` (用于计算内在指标) 以及 `snowflake-arctic-embed-l-v2.0` (用于重排序)。
*   **算力说明**：文中未明确提及具体的 GPU 型号和数量。但提供了运行耗时统计：LLM-Regex 分割平均耗时 146.85 秒（含异步 API 调用），整个自适应评估流程平均每份文档耗时约 210 秒。

### 5. 实验数量与充分性
*   **实验规模**：针对 33 份文档生成了 99 组高质量问答对进行端到端测试。
*   **充分性**：实验覆盖了多个领域，并对每种分块方法的内在指标进行了详尽的消解分析。
*   **客观性**：使用了 Wilcoxon 符号秩检验来验证结果的统计显著性，确保性能提升并非偶然。对比实验中，基准方法均保持默认参数以模拟实际生产环境。

### 6. 主要结论与发现
*   **无“银弹”策略**：实验显示没有一种分块方法在所有文档上都表现最好。自适应策略在 48% 的情况下选择了页面分割，42% 选择了改进的递归分割。
*   **性能显著提升**：自适应分块将 RAG 的答案正确率从 62-64% 提升至 **72%**，成功回答的问题数量增加了 **32.7%**。
*   **内在指标的有效性**：内在指标的微小提升（0.4-2.4%）能在下游 RAG 任务中产生放大效应（8-10% 的性能增益），证明了优化分块质量的杠杆作用。

### 7. 优点与亮点
*   **解耦评估**：首次提出了不依赖下游 RAG 结果的五维内在评估体系，为分块研究提供了量化工具。
*   **结构感知**：通过 Markdown 解析和结构保护技术，解决了表格和标题被切断的痛点。
*   **实用性强**：该框架不需要重新训练模型或修改提示词，仅通过优化索引阶段的处理即可获得显著收益。

### 8. 不足与局限
*   **计算开销**：在索引阶段需要运行多种分块方法并计算指标，且涉及 LLM 调用和共指消解，计算成本高于传统方法。
*   **语言限制**：引用完整性 (RC) 指标依赖的共指消解模型目前仅支持英文。
*   **静态选择**：分块策略在索引时确定，无法根据用户查询的类型（如总结类 vs. 事实检索类）进行动态调整。
*   **超参数依赖**：指标的权重分配（目前是等权重）和滑动窗口大小等参数仍带有启发式色彩，可能需要针对特定领域微调。

（完）
