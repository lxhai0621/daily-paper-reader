---
title: Natural-Language Agent Harnesses
title_zh: 自然语言智能体测试框架
authors: "Linyue Pan, Lexiao Zou, Shuo Guo, Jingchen Ni, Hai-Tao Zheng"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25723v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 用于可移植和可编辑智能体控制逻辑的自然语言智能体装具
tldr: 针对当前智能体测试框架（harness）设计复杂且难以迁移的问题，本文提出了自然语言智能体框架（NLAH）。该方法将高层控制逻辑从代码中剥离，转为可编辑的自然语言描述，并通过智能框架运行时（IHR）实现跨平台的执行。通过在编程和计算机使用基准上的评估，证明了该方法在提高框架可移植性、可研究性及开发效率方面的显著优势。
selection_source: fresh_fetch
motivation: 传统的智能体框架逻辑通常深埋于代码中，导致其难以迁移、比较和作为独立的科学对象进行研究。
method: 提出了 NLAH 架构，利用自然语言表达框架行为，并结合 IHR 运行时通过显式契约和轻量级适配器执行逻辑。
result: 在编码和计算机使用基准测试中，验证了该方案在操作可行性、模块消融和代码到文本迁移方面的有效性。
conclusion: 将智能体框架逻辑外部化为自然语言构件，为构建更具可移植性、透明且易于优化的智能体系统提供了新路径。
---

## 摘要
智能体的性能日益取决于测试框架工程（harness engineering），但测试框架的设计通常埋没在控制器代码和特定运行时的约定中，导致其难以作为科学对象进行迁移、比较和研究。我们探究了智能体测试框架的高层控制逻辑是否可以外化为一种可移植的可执行构件。为此，我们引入了自然语言智能体测试框架（NLAHs），它使用可编辑的自然语言来表达测试框架行为；以及智能测试框架运行时（IHR），这是一个通过显式契约、持久化构件和轻量级适配器来执行这些测试框架的共享运行时。通过编程和计算机使用基准测试，我们对运行可行性、模块消融以及从代码到文本的测试框架迁移进行了受控评估。

## Abstract
Agent performance increasingly depends on \emph{harness engineering}, yet harness design is usually buried in controller code and runtime-specific conventions, making it hard to transfer, compare, and study as a scientific object. We ask whether the high-level control logic of an agent harness can instead be externalized as a portable executable artifact. We introduce \textbf{Natural-Language Agent Harnesses} (NLAHs), which express harness behavior in editable natural language, and \textbf{Intelligent Harness Runtime} (IHR), a shared runtime that executes these harnesses through explicit contracts, durable artifacts, and lightweight adapters. Across coding and computer-use benchmarks, we conduct controlled evaluations of operational viability, module ablation, and code-to-text harness migration.

---

## 论文详细总结（自动生成）

### 论文总结：自然语言智能体测试框架 (Natural-Language Agent Harnesses)

#### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：现代 AI 智能体的成功越来越依赖于其外围的“测试框架”（Harness），即负责多步推理、工具调用、内存管理、任务委派和停止准则的控制层。然而，目前的框架逻辑通常硬编码在控制器代码或特定运行时的约定中，导致这些逻辑难以迁移、难以公平比较，也难以作为独立的科学对象进行消融研究。
*   **核心问题**：能否将智能体框架的高层控制逻辑从底层代码中剥离，转化为一种可移植、可编辑且可执行的“自然语言构件”？
*   **整体含义**：论文提出了将“提示工程”扩展为“上下文工程”和“框架工程”的理念，旨在通过标准化和外部化控制模式，提升智能体系统的透明度和可研究性。

#### 2. 方法论：核心思想与技术细节
论文提出了两个核心组件：
*   **自然语言智能体测试框架 (NLAH)**：
    *   **核心思想**：用结构化的自然语言描述框架逻辑。
    *   **关键要素**：包括**契约**（输入输出要求、权限、停止规则）、**角色**（如策划者、执行者、验证者）、**阶段结构**（任务拓扑）、**适配器与脚本**（确定性钩子）、**状态语义**（持久化路径）以及**失败分类学**（驱动恢复的错误类型）。
*   **智能框架运行时 (IHR)**：
    *   **核心思想**：在运行循环中引入一个 LLM 作为“解释器”，直接读取 NLAH 逻辑并执行。
    *   **技术实现**：包含一个解释逻辑的循环内 LLM、一个提供工具和多智能体接口的后端，以及一个定义契约和状态语义的“运行时宪章”（Runtime Charter）。
    *   **文件支持状态模块**：将持久状态外部化为路径可寻址的构件（如 `task_state.json`），确保状态在上下文截断或分支切换时依然稳定。

#### 3. 实验设计
*   **数据集/场景**：
    *   **编程领域**：使用 **SWE-bench Verified**，评估智能体解决真实 GitHub Issue 的能力。
    *   **计算机使用领域**：使用 **OSWorld**，评估智能体在真实桌面环境中的操作能力。
*   **对比方法与 Benchmark**：
    *   **RQ1（行为效应）**：对比完整 IHR、无运行时技能（w/o RTS）和无框架技能（w/o HS）的表现。
    *   **RQ2（模块化消融）**：从基础版（Basic）开始，逐一添加文件状态、证据支持、验证器、自我进化、多候选搜索和动态编排等模块。
    *   **RQ3（迁移研究）**：对比原生代码实现的框架（如 OS-Symphony）与重构后的 NLAH 版本。

#### 4. 资源与算力
*   **模型**：使用了 **GPT-5.4**（推理努力程度设为 xhigh）。
*   **硬件环境**：Ubuntu 24.04 服务器，配置为 **64 核 CPU** 和 **251 GiB 内存**。
*   **容器化**：所有任务在 Docker 中运行，单任务上限为 32 vCPU、84 GiB 内存和 40 GiB 存储。
*   **算力说明**：文中未明确提及具体的 GPU 型号和数量，主要强调了 CPU 和内存配置，这可能反映了该研究更侧重于逻辑编排而非底层模型训练。

#### 5. 实验数量与充分性
*   **实验规模**：由于预算限制，论文在基准测试的子集上运行（125 个 SWE-bench 样本和 36 个 OSWorld 样本）。
*   **消融实验**：针对 7 种不同的框架设计模式进行了详细的模块化消融（RQ2），并进行了代码到文本的配对迁移实验（RQ3）。
*   **充分性评价**：实验设计较为严谨，涵盖了从行为改变到模块贡献再到迁移保真度的多维度评估。虽然样本量受限，但通过“配对翻转”（Paired Flips）和轨迹分析，提供了深入的定性与定量证据。

#### 6. 主要结论与发现
*   **框架逻辑具有实质影响**：NLAH 不仅仅是提示词包装，它显著改变了智能体的工具调用频率、运行时间和任务处理拓扑。
*   **结构并非越多越好**：增加复杂的验证和搜索模块并不总是提升性能。如果框架内部的“成功标准”与基准测试的“最终评测标准”不一致，过多的结构反而会引入摩擦或导致路径偏离。
*   **迁移优势**：将代码框架迁移为 NLAH 后，在 OSWorld 上表现更好。原因在于 NLAH 促使智能体从脆弱的 GUI 局部修复转向更稳健的基于文件和构件的验证。
*   **自我进化模块最有效**：在所有消融模块中，“自我进化”在不显著增加成本的情况下，对性能提升最明显。

#### 7. 优点
*   **科学性**：将模糊的智能体编排逻辑转化为可观察、可测试的科学对象。
*   **可移植性**：NLAH 允许开发者以自然语言快速调整和迁移复杂的智能体工作流，无需重写底层控制器代码。
*   **解耦设计**：成功实现了“运行时策略”与“任务族逻辑”的分离，为未来的自动化框架搜索（Harness Synthesis）奠定了基础。

#### 8. 不足与局限
*   **精度损失**：自然语言在表达某些确定性逻辑时不如代码精确，可能导致执行歧义。
*   **运行时污染**：强大的运行时宪章可能会吸收部分本应属于框架文本的行为，导致难以完全区分两者的贡献。
*   **成本与延迟**：由于在循环中引入了额外的 LLM 解释步骤，系统的 Token 消耗和响应延迟显著增加。
*   **样本覆盖**：受限于预算，全量基准测试的评估尚待完善。

（完）
