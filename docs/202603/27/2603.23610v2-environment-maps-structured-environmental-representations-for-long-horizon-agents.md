---
title: "Environment Maps: Structured Environmental Representations for Long-Horizon Agents"
title_zh: 环境地图：面向长程智能体的结构化环境表示
authors: "Yenchia Feng, Chirag Sharma, Karime Maamari"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23610v2"
tags: ["query:ma-kf"]
score: 9.0
evidence: 将异构证据整合为智能体的结构化图表示
tldr: 针对大语言模型在复杂软件工作流中面临的长程规划失败和环境随机性问题，本文提出了“环境地图（Environment Maps）”。这是一种结构化的、与代理无关的环境表示方法，通过将屏幕录像和执行轨迹整合为包含上下文、动作、工作流和隐性知识的图结构。在 WebArena 基准测试中，该方法显著提升了任务成功率，为长程规划提供了可解释且可编辑的持久化基础。
selection_source: fresh_fetch
motivation: 现有的智能体在处理长程复杂任务时，容易因环境动态变化和单步错误导致级联失败。
method: 提出一种将异构证据（如执行轨迹）整合为包含上下文、动作、工作流和知识的结构化图表示方法。
result: "在 WebArena 测试中，使用环境地图的智能体成功率达到 28.2%，远高于仅依赖会话上下文的基线（14.2%）。"
conclusion: 环境地图为长程智能体提供了一个可解释、可编辑且持续优化的结构化接口，有效增强了其规划能力。
---

## 摘要
尽管大语言模型（LLMs）发展迅速，但复杂软件工作流的鲁棒自动化仍是一个悬而未决的问题。在长程（long-horizon）场景中，智能体经常受到级联错误和环境随机性的困扰；动态界面中的一次失误就可能导致任务失败，进而引发幻觉或陷入反复试错。本文介绍了“环境地图”（Environment Maps）：一种持久的、与智能体无关的表示方式，通过将屏幕录像和执行轨迹等异构证据整合到结构化图中，来缓解这些失败。该表示由四个核心组件组成：(1) 上下文（抽象位置），(2) 动作（参数化可供性），(3) 工作流（观测到的轨迹），以及 (4) 隐性知识（领域定义和可重用程序）。我们在 WebArena 基准测试的五个领域中评估了该框架。配备环境地图的智能体实现了 28.2% 的成功率，几乎是受限于会话上下文的基线模型（14.2%）的两倍，且优于能够访问用于生成环境地图的原始轨迹数据的智能体（23.3%）。通过在模型和环境之间提供结构化接口，环境地图为长程规划奠定了持久的基础，且具有可解释性、可编辑性和增量完善性。

## Abstract
Although large language models (LLMs) have advanced rapidly, robust automation of complex software workflows remains an open problem. In long-horizon settings, agents frequently suffer from cascading errors and environmental stochasticity; a single misstep in a dynamic interface can lead to task failure, resulting in hallucinations or trial-and-error. This paper introduces $\textit{Environment Maps}$: a persistent, agent-agnostic representation that mitigates these failures by consolidating heterogeneous evidence, such as screen recordings and execution traces, into a structured graph. The representation consists of four core components: (1) Contexts (abstracted locations), (2) Actions (parameterized affordances), (3) Workflows (observed trajectories), and (4) Tacit Knowledge (domain definitions and reusable procedures). We evaluate this framework on the WebArena benchmark across five domains. Agents equipped with environment maps achieve a 28.2% success rate, nearly doubling the performance of baselines limited to session-bound context (14.2%) and outperforming agents that have access to the raw trajectory data used to generate the environment maps (23.3%). By providing a structured interface between the model and the environment, Environment Maps establish a persistent foundation for long-horizon planning that is human-interpretable, editable, and incrementally refinable.

---

## 论文详细总结（自动生成）

这篇论文由 Distyl AI 的研究团队提交至 ICLR 2026 World Models Workshop，提出了一种名为“环境地图”（Environment Maps）的结构化环境表示方法，旨在解决大语言模型（LLM）智能体在处理长程（Long-horizon）复杂软件工作流时的鲁棒性问题。

以下是对该论文的深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：在长程任务中，智能体常因“级联错误”而失败。即在动态界面中，一步失误会导致智能体失去对当前位置、有效动作及其后果的持久理解，从而引发幻觉或无效的试错。
*   **研究动机**：现有的方法（如 UI 转换图、预测模型或技能库）往往彼此孤立，且缺乏持久性或与 UI 变化对齐的能力。作者认为，智能体需要一个统一、可维护、且与具体模型无关的“底座”，将状态、动作、动态观测和任务语义整合在一起。

### 2. 方法论：环境地图框架
环境地图被定义为一个四元组 $M = (C, A, W, K)$，通过以下五个阶段的流水线从原始观测（如屏幕录像、执行轨迹）中构建：
1.  **统一步骤序列（Unified Step Sequence）**：将原始事件流解析为包含动作名称、描述、结果和 URL 的步骤序列。
2.  **动作提取（Action Extraction）**：从轨迹中提取已执行动作，并从 DOM 中识别未执行但潜在的动作。
3.  **动作泛化（Action Generalization）**：利用 LLM 识别模式，将具体动作参数化（例如，将“点击 John”泛化为“点击 {用户名}”）。
4.  **上下文与知识提取（Context & Knowledge Extraction）**：
    *   **上下文（Contexts）**：通过 URL 正则化将页面聚类，定义抽象位置。
    *   **隐性知识（Tacit Knowledge）**：提取领域定义（如缩写含义）和操作程序（如过滤问题的具体步骤）。
5.  **整合与合并（Integration & Merging）**：将上述信息合并为结构化的 JSON 图，支持智能体通过文件工具（如 Read, Grep）进行查询。

### 3. 实验设计
*   **基准测试**：使用 **WebArena**，涵盖 812 个自然语言任务。
*   **实验场景**：包括电子商务（Magento）、内容管理系统（CMS）、代码托管（GitLab）、论坛（Reddit）和地图（OpenStreetMap）五个真实 Web 环境。
*   **对比方法**：
    *   **Baseline (No Map)**：仅提供当前页面观测（可访问性树）和任务意图。
    *   **Trajectory Access**：允许智能体访问原始的人类执行轨迹文件（JSONL 格式），但未经过结构化处理。
    *   **Environment Map Access (本文方法)**：智能体可以访问基于相同轨迹数据生成的结构化环境地图。
*   **智能体配置**：采用 `claude-sonnet-4-5` 作为核心模型，配备文件操作和 Bash 工具。

### 4. 资源与算力
*   **构建成本**：论文明确指出，构建一个环境地图的成本极低。针对 WebArena 的五个环境，每个环境的构建成本约为 **1 到 4 美元**，耗时 **13 到 31 分钟**。
*   **训练算力**：该方法**不需要对模型进行微调（SFT）**，因此不涉及大规模 GPU 集群的训练时长。它主要依赖于 LLM API 的推理能力来生成和查询地图。

### 5. 实验数量与充分性
*   **实验规模**：在 812 个任务上进行了测试，样本量充足。
*   **消融/对比实验**：通过对比“原始轨迹”和“结构化地图”，有效地证明了**表示形式（Representation）**本身的重要性，而不仅仅是数据量的多寡。
*   **分析维度**：实验不仅关注成功率，还分析了步骤数、工具调用次数、回溯率（Backtracking）以及在未见任务上的泛化能力，实验设计客观且全面。

### 6. 主要结论与发现
*   **性能提升**：配备环境地图的智能体成功率达到 **28.2%**，远高于基线（14.2%）和原始轨迹访问组（23.3%）。
*   **结构化的价值**：即使数据源相同，将数据组织成结构化的地图比直接提供原始轨迹能带来更高的成功率（+4.9%）。
*   **复杂环境受益最大**：在 UI 密集、分支复杂的环境（如 GitLab 和 CMS）中，环境地图带来的提升最为显著。
*   **效率提升**：使用地图的智能体回溯行为更少，网络请求总量降低，且能进行更精准的知识检索。

### 7. 优点
*   **智能体无关性**：地图是持久且通用的，可以跨不同模型（如从 GPT 切换到 Claude）重用，无需重新训练。
*   **可解释性与可编辑性**：地图以 JSON 格式存储，人类可以直接检查、诊断错误并手动编辑修正。
*   **增量更新**：支持随着环境演变或新轨迹的加入而不断完善，维护成本低。

### 8. 不足与局限
*   **跨站点限制**：目前的地图主要针对单一环境，对于跨多个不同网站的复杂工作流支持有限。
*   **时效性风险**：如果 Web 界面发生重大重构，地图可能会过时，虽然作者提到了增量更新，但自动检测和修复 UI 漂移的能力仍有待加强。
*   **简单场景边际效应**：在导航路径非常浅的简单环境（如 Reddit）中，结构化地图相比原始轨迹的优势并不明显。

（完）
