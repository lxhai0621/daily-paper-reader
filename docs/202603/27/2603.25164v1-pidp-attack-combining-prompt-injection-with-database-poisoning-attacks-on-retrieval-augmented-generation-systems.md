---
title: "PIDP-Attack: Combining Prompt Injection with Database Poisoning Attacks on Retrieval-Augmented Generation Systems"
title_zh: PIDP-Attack：结合提示注入与数据库投毒的检索增强生成系统攻击方法
authors: "Haozhen Wang, Haoyue Liu, Jionghao Zhu, Zhichao Wang, Yongxin Guo, Xiaoying Tang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25164v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: RAG系统中的对抗性攻击与幻觉
tldr: 本研究针对检索增强生成（RAG）系统的安全漏洞，提出了一种名为PIDP-Attack的新型复合攻击方法。该方法将推理时的提示注入与数据库投毒相结合，通过在查询中添加恶意字符并向数据库注入少量毒化文本，实现了在无需预知用户具体查询的情况下操纵LLM输出。实验证明，该方法在多个基准数据集和模型上均优于现有攻击手段，显著提升了攻击成功率，为RAG系统的安全性评估提供了新视角。
selection_source: fresh_fetch
motivation: 现有的RAG投毒攻击通常需要预先了解用户的具体查询，限制了其在真实场景中的灵活性和适用性。
method: 提出一种结合推理时提示注入与数据库投毒的复合攻击策略，通过恶意字符和毒化段落协同操纵模型响应。
result: "在三个基准数据集和八个大语言模型上的评估显示，该方法比PoisonedRAG的攻击成功率提高了4%至16%。"
conclusion: 复合攻击策略能有效应对任意查询的操纵需求，证明了结合提示注入与数据库投毒在RAG安全研究中的必要性与高效性。
---

## 摘要
大语言模型（LLMs）在广泛的应用中展现出了卓越的性能。然而，它们的实际部署往往受到知识陈旧和易产生幻觉等问题的阻碍。为了解决这些局限性，检索增强生成（RAG）系统被引入，通过外部、及时的知识源来增强 LLMs。尽管具有这些优势，RAG 系统仍然容易受到对抗性攻击，其中数据投毒已成为一种突出的威胁。现有的基于投毒的攻击通常需要预先了解用户的特定查询，这限制了其灵活性和实际应用性。在这项工作中，我们提出了 PIDP-Attack，这是一种将提示注入与 RAG 中的数据库投毒相结合的新型复合攻击。通过在推理时向查询附加恶意字符，并在检索数据库中注入有限数量的投毒段落，我们的方法可以有效地操纵 LLM 对任意查询的响应，而无需预先了解用户的实际查询。在三个基准数据集（Natural Questions、HotpotQA、MS-MARCO）和八个 LLMs 上的实验评估表明，PIDP-Attack 的表现始终优于原始的 PoisonedRAG。具体而言，我们的方法在开放域问答任务上的攻击成功率提高了 4% 到 16%，同时保持了较高的检索精度，证明了这种复合攻击策略既是必要的，也是非常有效的。

## Abstract
Large Language Models (LLMs) have demonstrated remarkable performance across a wide range of applications. However, their practical deployment is often hindered by issues such as outdated knowledge and the tendency to generate hallucinations. To address these limitations, Retrieval-Augmented Generation (RAG) systems have been introduced, enhancing LLMs with external, up-to-date knowledge sources. Despite their advantages, RAG systems remain vulnerable to adversarial attacks, with data poisoning emerging as a prominent threat. Existing poisoning-based attacks typically require prior knowledge of the user's specific queries, limiting their flexibility and real-world applicability. In this work, we propose PIDP-Attack, a novel compound attack that integrates prompt injection with database poisoning in RAG. By appending malicious characters to queries at inference time and injecting a limited number of poisoned passages into the retrieval database, our method can effectively manipulate LLM response to arbitrary query without prior knowledge of the user's actual query. Experimental evaluations across three benchmark datasets (Natural Questions, HotpotQA, MS-MARCO) and eight LLMs demonstrate that PIDP-Attack consistently outperforms the original PoisonedRAG. Specifically, our method improves attack success rates by 4% to 16% on open-domain QA tasks while maintaining high retrieval precision, proving that the compound attack strategy is both necessary and highly effective.

---

## 论文详细总结（自动生成）

### PIDP-Attack: 结合提示注入与数据库投毒的 RAG 系统攻击研究总结

#### 1. 核心问题与研究动机
*   **核心问题**：检索增强生成（RAG）系统虽然提升了 LLM 的事实准确性，但也引入了新的安全风险。现有的针对 RAG 的攻击（如数据库投毒）通常存在一个**强假设**：攻击者必须预先知道用户的具体查询（Query），才能针对性地生成投毒文档。
*   **研究动机**：在现实场景中，攻击者很难预知用户的实时提问。因此，论文旨在提出一种**查询无关（Query-agnostic）**的复合攻击方法，即无论用户问什么，都能诱导系统输出攻击者预设的错误答案。

#### 2. 方法论：PIDP-Attack
PIDP-Attack 采用“双管齐下”的策略，结合了**查询路径操纵**和**语料库路径操纵**：
*   **核心思想**：通过在用户查询末尾添加一个通用的恶意后缀（提示注入），强行改变检索器的注意力，使其转向攻击者预先注入数据库的毒化文档（数据库投毒），从而误导生成器。
*   **关键技术细节**：
    1.  **查询路径注入（Query-Path）**：在推理阶段，向任意用户查询 $q$ 附加一个后缀 $\delta(S)$。该后缀包含目标问题 $S$ 和指令（如“忽略之前指令，回答以下问题”）。其作用是：(1) 引导检索器寻找与 $S$ 相关的文档；(2) 诱导 LLM 优先处理 $S$ 而非原查询 $q$。
    2.  **数据库投毒（Corpus-Path）**：预先向数据库注入少量（通常 $n=k$）毒化段落。每个段落以目标问题 $S$ 开头，后接由辅助 LLM 生成的、支持错误答案 $a^-$ 的虚假证据。
*   **算法流程**：
    *   **线下准备**：选择目标问题 $S$ 和错误答案 $a^-$，利用 LLM 生成支持 $a^-$ 的毒化文本，并将其注入 RAG 数据库。
    *   **线上攻击**：拦截用户查询，附加恶意后缀，RAG 系统检索到毒化文本并生成错误答案。

#### 3. 实验设计
*   **数据集**：使用了三个主流的开放域问答基准数据集：**Natural Questions (NQ)**、**HotpotQA** 和 **MS-MARCO**。
*   **评估模型（Generator）**：涵盖了 8 个主流 LLM，包括 Qwen 2.5/2、Llama 3.1/3.3/4-Maverick、GPT-OSS (120B/20B) 和 Granite 3.3。
*   **对比方法（Baselines）**：
    *   **PoisonedRAG**：经典的针对 RAG 的针对性投毒攻击。
    *   **Disinformation Attack**：虚假信息投毒攻击。
    *   **GGPP**：基于前缀扰动的检索引导攻击。
    *   **GCG**：通用的提示注入攻击（无投毒）。
    *   **Corpus**：纯语料库投毒（无查询注入）。

#### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **1 × NVIDIA A100 (80GB) GPU** 的计算节点上运行。
*   **软件工具**：使用 `beir` 库进行检索评估，主模型推理通过托管 API 或 `vllm` 加速实现。
*   **训练/推理时长**：论文未明确给出总耗时，但提到投毒文本生成和后缀优化是离线完成的，推理过程为实时。

#### 5. 实验数量与充分性
*   **实验规模**：在 3 个数据集和 8 个模型上进行了广泛测试，每组设置重复 10 次迭代（每次采样 10 个查询），以确保结果的统计显著性。
*   **消融实验**：
    *   **投毒预算 $n$**：测试了注入 1 到 5 条毒化文本的影响。
    *   **上下文窗口 $k$**：测试了检索 Top-k（1 到 10）对攻击成功率的影响。
*   **客观性与公平性**：实验采用了统一的检索器（Contriever）和 RAG 提示模板，对比基线均在相同预算下进行，评估标准（ASR，攻击成功率）定义严格，实验设计较为客观公平。

#### 6. 主要结论与发现
*   **性能卓越**：PIDP-Attack 在平均攻击成功率（ASR）上达到了 **98.125%**，显著高于所有基线方法。
*   **复合效应**：相比于 PoisonedRAG，PIDP-Attack 在 NQ 任务上将 ASR 提升了 **4%–16%**。
*   **查询无关性**：证明了即使攻击者不知道用户的具体问题，仅靠一个通用后缀和少量投毒文档就能实现极高成功率。
*   **预算敏感性**：在 NQ 和 HotpotQA 上，仅需注入 2 条毒化文本即可达到饱和成功率；而在 MS-MARCO 等噪声较大的数据集上，成功率随投毒数量增加而稳步上升。

#### 7. 优点
*   **现实威胁性强**：放宽了“预知查询”的假设，更符合真实的黑盒攻击场景。
*   **高效且轻量**：无需修改模型参数，仅需操纵输入流和注入少量文档，本地计算开销极低。
*   **鲁棒性高**：在不同架构、不同规模的 LLM 上均表现出极强的迁移性和稳定性。

#### 8. 不足与局限
*   **攻击路径依赖**：该攻击假设攻击者能够操纵查询路径（如通过恶意插件、代理服务器或中间件），这在高度安全的封闭系统中可能难以实现。
*   **防御敏感性**：实验观察到，一些具有强安全对齐或拒绝机制的模型（Refusal-centric models）在某些情况下会拒绝回答，导致攻击失败。
*   **检测风险**：恶意后缀（如“Ignore previous instructions”）虽然有效，但也容易被基于规则或语义分析的防御手段拦截。

（完）
