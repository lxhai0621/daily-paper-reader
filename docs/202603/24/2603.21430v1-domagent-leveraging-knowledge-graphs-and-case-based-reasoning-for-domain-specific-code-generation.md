---
title: "DomAgent: Leveraging Knowledge Graphs and Case-Based Reasoning for Domain-Specific Code Generation"
title_zh: DomAgent：利用知识图谱与基于案例的推理进行特定领域代码生成
authors: "Shuai Wang, Dhasarathy Parthasarathy, Robert Feldt, Yinan Yu"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21430v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 使用定向检索和知识图谱的自主编程智能体
tldr: 针对通用大模型在特定领域代码生成中因缺乏专业知识而表现不佳的问题，本文提出DomAgent。该智能体通过DomRetriever模块，模仿人类学习方式，将知识图谱的结构化推理与基于案例的经验推理相结合，实现领域知识的精准检索与合成。实验表明，DomAgent在数据科学及工业软件开发任务中表现优异，能显著提升开源模型在复杂场景下的代码生成能力。
selection_source: fresh_fetch
motivation: 通用大模型在处理特定领域代码生成时，因缺乏专业知识和经验案例，导致在实际软件开发中成功率较低。
method: 提出DomAgent及其核心模块DomRetriever，通过结合知识图谱的自上而下推理与基于案例的自下而上推理，实现结构化知识与经验案例的动态检索与合成。
result: 在DS-1000基准测试和真实卡车软件开发任务中，DomAgent显著提升了代码生成质量，缩小了开源小模型与闭源大模型之间的差距。
conclusion: 该研究证明了结合结构化知识与实例推理能有效增强大模型的领域适应能力，为特定领域自动化编程提供了高效解决方案。
---

## 摘要
大语言模型（LLMs）在代码生成方面展现出了卓越的能力。然而，由于大多数 LLM 是在公共领域语料库上训练的，直接将其应用于现实世界的软件开发往往成功率较低，因为这些场景通常需要特定领域的知识。特别是，特定领域任务通常需要高度专业化的解决方案，而这些方案在通用 LLM 的训练数据中往往代表性不足或完全缺失。为了应对这一挑战，我们提出了 DomAgent，这是一种自主编程智能体，通过结构化推理和针对性检索，使 LLM 能够生成领域自适应代码，从而弥补这一差距。DomAgent 的核心组件是 DomRetriever，这是一个新颖的检索模块，它通过结合概念理解与经验案例，模拟人类学习特定领域知识的方式。它动态地将自顶向下的知识图谱推理与自底向上的基于案例的推理相结合，实现了结构化知识和代表性案例的迭代检索与综合，以确保上下文相关性和广泛的任务覆盖面。DomRetriever 既可以作为 DomAgent 的一部分运行，也可以独立与任何 LLM 配合使用，以实现灵活的领域自适应。我们在数据科学领域的公开基准数据集（DS-1000）上评估了 DomAgent，并进一步将其应用于现实世界的卡车软件开发任务。实验结果表明，DomAgent 显著增强了特定领域的代码生成能力，使小型开源模型在复杂的现实应用中能够缩小与大型闭源 LLM 之间的大部分性能差距。代码可在以下网址获取：https://github.com/Wangshuaiia/DomAgent。

## Abstract
Large language models (LLMs) have shown impressive capabilities in code generation. However, because most LLMs are trained on public domain corpora, directly applying them to real-world software development often yields low success rates, as these scenarios frequently require domain-specific knowledge. In particular, domain-specific tasks usually demand highly specialized solutions, which are often underrepresented or entirely absent in the training data of generic LLMs. To address this challenge, we propose DomAgent, an autonomous coding agent that bridges this gap by enabling LLMs to generate domain-adapted code through structured reasoning and targeted retrieval. A core component of DomAgent is DomRetriever, a novel retrieval module that emulates how humans learn domain-specific knowledge, by combining conceptual understanding with experiential examples. It dynamically integrates top-down knowledge-graph reasoning with bottom-up case-based reasoning, enabling iterative retrieval and synthesis of structured knowledge and representative cases to ensure contextual relevance and broad task coverage. DomRetriever can operate as part of DomAgent or independently with any LLM for flexible domain adaptation. We evaluate DomAgent on an open benchmark dataset in the data science domain (DS-1000) and further apply it to real-world truck software development tasks. Experimental results show that DomAgent significantly enhances domain-specific code generation, enabling small open-source models to close much of the performance gap with large proprietary LLMs in complex, real-world applications. The code is available at: https://github.com/Wangshuaiia/DomAgent.

---

## 论文详细总结（自动生成）

以下是对论文《DomAgent: Leveraging Knowledge Graphs and Case-Based Reasoning for Domain-Specific Code Generation》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：通用大语言模型（LLMs）虽然在通用编程任务上表现出色，但在面对特定领域（如卡车软件开发、专业数据科学库）时，由于训练数据中缺乏领域专有知识（如第三方私有库、特定业务逻辑），其生成代码的成功率显著下降。
*   **核心问题**：如何有效地为 LLM 注入特定领域的结构化知识和经验案例，使其在不进行大规模微调的情况下，能够生成符合特定领域规范和逻辑的高质量代码。
*   **整体含义**：论文提出了 **DomAgent**，这是一个模仿人类学习过程（结合概念理解与经验积累）的自主编程智能体。它通过整合知识图谱（KG）和基于案例的推理（CBR），实现了领域知识的精准检索与代码生成。

### 2. 方法论
DomAgent 的核心架构包含三个关键模块：
*   **基于知识图谱引导的分层案例选择**：
    *   为了构建高效的案例库，利用 KG 的层级结构进行采样。
    *   **锚定与聚类**：在包（Package）级别设置锚点，对函数（Function）进行语义聚类，确保案例库覆盖了多样的功能使用模式。
    *   **覆盖驱动选择**：通过迭代遍历，仅保留能增加包覆盖率或功能聚类覆盖率的案例，从而用极小（约 30%）的案例量达到高覆盖效果。
*   **DomRetriever 检索模块**：
    *   **自上而下（Top-down）**：从 KG 中检索相关的包、函数定义及描述。
    *   **自下而上（Bottom-up）**：基于语义相似度和“包重叠度”对案例进行重排序，检索最相关的代码示例。
    *   **智能体精炼**：推理 LLM 充当智能体，通过调用 `SearchKG` 和 `SearchCase` 工具，在 `<think>` 阶段自主审查并过滤无关信息。
*   **智能体训练**：
    *   **监督微调（SFT）**：教模型如何使用检索工具标签。
    *   **强化学习（RL）**：采用 **GRPO 算法**，利用外部强大模型作为奖励模型，优化智能体过滤无关知识和选择精准案例的能力。

### 3. 实验设计
*   **数据集**：
    1.  **DS-1000**：涵盖 NumPy、Pandas、PyTorch 等 7 个主流数据科学库的 1000 个编程任务。
    2.  **Truck CAN Signal**：来自沃尔沃（Volvo）真实场景的卡车 CAN 信号读写任务，包含 776 个信号，涉及 6 个专业功能域。
*   **对比方法**：
    *   **基础模型**：GPT-4o, Qwen-2.5-7B, LLaMA-3.1-8B, LLaMA-3.3-70B。
    *   **专业编程智能体**：StarCoder, WizardCoder, Magicoder 等。
*   **评估指标**：采用 **pass@1**（一次生成通过测试用例的比例）。

### 4. 资源与算力
*   **硬件设施**：使用了 **8 台 NVIDIA A100 80G GPU**。
*   **训练细节**：
    *   训练时长：2 个 Epoch。
    *   超参数：Batch size 为 16，学习率为 1e-6。
    *   推理：每个查询生成 16 个候选结果（rollouts）用于 RL 优化。

### 5. 实验数量与充分性
*   **实验规模**：在两个完全不同的领域（学术基准 vs. 工业实际应用）进行了大规模测试。
*   **消融实验**：详细对比了“仅 KG”、“仅 CBR”、“标准 RAG 流程”与“DomAgent 流程”的差异，验证了每个组件的增益。
*   **充分性评价**：实验设计非常充分。不仅对比了不同规模的模型，还验证了 DomRetriever 作为独立模块增强其他生成器（如 GPT-4o）的效果。通过 KG 引导选择与随机采样的对比，证明了其案例库构建的高效性。

### 6. 主要结论与发现
*   **性能飞跃**：DomAgent 显著提升了开源小模型（7B/8B）的性能，使其在特定领域任务中能够媲美甚至超越未增强的 GPT-4o。
*   **检索精度**：通过智能体的“推理-审查”机制，有效解决了传统 RAG 中长上下文干扰和无关信息误导的问题。
*   **工业价值**：在卡车 CAN 信号任务中，DomAgent 将 Qwen-2.5-7B 的 pass@1 从 39.62% 提升至 **96.64%**，证明了其在严苛工业环境下的部署潜力。
*   **案例效率**：KG 引导的案例选择仅需 30% 的数据量即可达到随机采样 80% 数据量的效果。

### 7. 优点
*   **双向推理结合**：巧妙地结合了结构化知识（自上而下）与实例经验（自下而上），符合人类认知逻辑。
*   **即插即用**：DomRetriever 模块具有通用性，可以适配任何 LLM 后端。
*   **低成本高效率**：通过智能检索和小型案例库，规避了频繁微调模型的高昂成本。
*   **实战验证**：论文不仅停留在学术数据集，还深入工厂一线进行了真实业务验证。

### 8. 不足与局限
*   **KG 依赖性**：系统性能高度依赖于高质量领域知识图谱的构建，而 KG 的初期挖掘和建模仍需一定的人工参与。
*   **结构化深度不足**：目前主要依赖语义相似度，尚未充分利用抽象语法树（AST）等更深层的代码结构特征进行检索（作者将其列为未来工作）。
*   **训练门槛**：虽然推理成本低，但 RL 训练阶段仍需要高性能 GPU 集群和强大的教师模型（如 GPT-4o/5）提供奖励信号。

（完）
