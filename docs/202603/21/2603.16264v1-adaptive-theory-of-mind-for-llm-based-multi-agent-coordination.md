---
title: Adaptive Theory of Mind for LLM-based Multi-Agent Coordination
title_zh: 基于大语言模型多智能体协作的自适应心理理论
authors: "Chunjiang Mu, Ya Zeng, Qiaosheng Zhang, Kun Shao, Chen Chu, Hao Guo, Danyang Jia, Zhen Wang, Shuyue Hu"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16264v1"
tags: ["query:agent"]
score: 9.0
evidence: 基于大语言模型的多智能体协作
tldr: 本研究探讨了大语言模型（LLM）智能体在多智能体协作中的心理理论（ToM）应用。研究发现，智能体间ToM阶数的不匹配（推理深度过深或过浅）会损害协作效率。为此，作者提出了自适应心理理论（A-ToM）智能体，它能根据历史交互推断队友的ToM阶数并预测其行为，从而实现动态对齐。实验证明，A-ToM在多种协作任务中均表现优异，强调了ToM对齐在提升多智能体系统性能中的核心作用。
selection_source: fresh_fetch
motivation: 智能体之间心理理论（ToM）推理深度的不匹配会导致推理不足或过度，从而损害多智能体协作的效率。
method: 提出自适应心理理论（A-ToM）框架，通过历史交互估计队友的推理阶数，并利用该估计预测其行为以实现对齐。
result: 在重复矩阵博弈、网格导航和Overcooked等四项任务中的实验结果验证了ToM对齐的重要性及A-ToM的有效性。
conclusion: 实现智能体间心理理论阶数的对齐是优化多智能体协作的关键，且该方法对非LLM智能体也具有泛化潜力。
---

## 摘要
心理理论（Theory of Mind, ToM）是指推断他人心理状态的能力，而高阶心理理论则涉及考虑到他人也拥有其自身的心理理论。长期以来，为大语言模型（LLM）驱动的智能体配备心理理论被认为能够提高它们在多智能体协作任务中的协调能力。然而，我们发现心理理论阶数失配（即智能体之间心理理论推理深度的不匹配）会导致对他人推理不足或过度推理，从而损害其协调性。为了解决这一问题，我们设计了一种自适应心理理论（A-ToM）智能体，它能够与合作伙伴的心理理论阶数保持一致。基于先前的交互，该智能体估计合作伙伴可能的心理理论阶数，并利用该估计来预测合作伙伴的行为，从而促进行为协调。我们在四个多智能体协调任务上进行了实证评估：重复矩阵博弈、两个网格导航任务以及一个 Overcooked 任务。结果验证了我们关于心理理论对齐的发现，并证明了 A-ToM 智能体的有效性。此外，我们还讨论了 A-ToM 对非大语言模型智能体的泛化能力，以及哪些因素会削弱心理理论对齐的重要性。

## Abstract
Theory of Mind (ToM) refers to the ability to reason about others' mental states, and higher-order ToM involves considering that others also possess their own ToM. Equipping large language model (LLM)-driven agents with ToM has long been considered to improve their coordination in multiagent collaborative tasks. However, we find that misaligned ToM orders-mismatches in the depth of ToM reasoning between agents-can lead to insufficient or excessive reasoning about others, thereby impairing their coordination. To address this issue, we design an adaptive ToM (A-ToM) agent, which can align in ToM orders with its partner. Based on prior interactions, the agent estimates the partner's likely ToM order and leverages this estimation to predict the partner's action, thereby facilitating behavioral coordination. We conduct empirical evaluations on four multi-agent coordination tasks: a repeated matrix game, two grid navigation tasks and an Overcooked task. The results validate our findings on ToM alignment and demonstrate the effectiveness of our A-ToM agent. Furthermore, we discuss the generalizability of our A-ToM to non-LLM-based agents, as well as what would diminish the importance of ToM alignment.

---

## 论文详细总结（自动生成）

这篇论文针对基于大语言模型（LLM）的多智能体协作问题，提出了“自适应心理理论”（Adaptive Theory of Mind, A-ToM）框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在多智能体协作中，虽然赋予智能体“心理理论”（ToM，即推断他人意图的能力）能提升性能，但研究发现高阶 ToM 并不总是有效。
*   **研究动机**：作者发现性能下降的深层原因是**ToM 阶数失配（Misalignment）**。例如，如果两个智能体都采用 1 阶 ToM（即都认为对方是简单的 0 阶智能体），会导致过度推理或推理不足，从而在需要打破对称性的任务中失败（如窄路会车时两人同时向同一侧避让）。
*   **整体含义**：论文旨在证明“对齐”比“深度”更重要，并提出一种让智能体能动态识别并匹配队友推理深度的机制。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将队友可能的 ToM 阶数（0阶、1阶、2阶）视为不同的“专家建议”，通过在线学习算法动态估计队友当前的推理阶数，并据此选择最优的协作行为。
*   **关键技术细节**：
    *   **递归 ToM 建模**：定义 ToM-0 为直接决策；ToM-1 假设对方是 ToM-0；ToM-2 假设对方是 ToM-1。
    *   **A-ToM 架构**：包含状态编码、ToM 模块（维护多个假设智能体）、决策模块和动作控制器。
    *   **在线对齐算法**：引入了两种算法来更新“专家”权重：
        1.  **Follow-the-Leader (FTL)**：选择历史上预测队友动作最准的那个 ToM 阶数。
        2.  **Hedge (加权平均)**：维护一个概率分布，通过指数损失函数更新权重，具有更好的探索性和对非平稳环境的适应性。
*   **流程**：观察状态 -> 各阶专家预测队友动作 -> 根据历史准确率选定预测值 -> 做出最优响应 -> 观察队友实际动作并更新专家权重。

### 3. 实验设计
*   **实验场景**：
    1.  **重复矩阵博弈**：测试对称性破碎能力（Memory-1 和 Memory-N 两种记忆模式）。
    2.  **网格导航 (Game 1 & 2)**：测试路径规划中的冲突避免。
    3.  **Overcooked (煮汤任务)**：测试复杂环境下的分工协作（采用全对称布局增加难度）。
*   **Benchmark 与对比方法**：
    *   **固定阶数智能体**：ToM-0, ToM-1, ToM-2 之间的两两组合（验证失配效应）。
    *   **非 LLM 智能体**：Greedy（基于规划）和 PBT（基于强化学习训练的智能体），用于测试泛化性。
    *   **自对比**：A-ToM (FTL) vs A-ToM (Hedge)。

### 4. 资源与算力
*   **模型使用**：所有实验均使用 **LLaMA-3.3-70B-Instruct** 作为底层大模型。
*   **参数设置**：推理温度（Temperature）设为 0.1 以保证稳定性（泛化性分析中设为 0.9 以测试鲁棒性）。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量及总训练/推理时长。由于是基于 Prompting 的零样本（Zero-shot）方法，不涉及大规模微调训练，但多轮交互的 API 调用或本地推理开销较大。

### 5. 实验数量与充分性
*   **实验规模**：每个配置独立重复运行 **30 次**，并报告平均值和方差。
*   **充分性**：实验覆盖了从简单博弈到复杂协作任务（Overcooked），并进行了跨智能体类型（LLM 与非 LLM）的泛化性测试。
*   **客观性**：通过矩阵博弈清晰地展示了失配导致的死循环现象，实验设计较为公平，对比了不同阶数对齐与失配的完整矩阵。

### 6. 主要结论与发现
*   **对齐至关重要**：实验证实，只有当 ToM 阶数对齐（如 ToM-1 配合 ToM-0，或 ToM-2 配合 ToM-1）时，协作效率最高。
*   **A-ToM 的有效性**：A-ToM 智能体能迅速识别队友类型并实现对齐，性能接近或达到理想对齐状态下的水平。
*   **自适应优势**：在自博弈（Self-play）中，采用 Hedge 算法的 A-ToM 表现优于 FTL，因为它能更好地处理两个都在进化的智能体之间的动态平衡。
*   **理性与空间影响**：任务的最优动作空间越清晰、智能体越理性，ToM 失配带来的负面影响就越严重，此时 A-ToM 的价值越大。

### 7. 优点
*   **理论结合实际**：将心理学中的 ToM 阶数理论与计算机科学中的“专家建议”在线学习算法结合，解决了 LLM 协作中的过度推理问题。
*   **零样本协作**：无需针对特定任务训练，利用 LLM 的推理能力实现通用的零样本协调。
*   **可解释性强**：通过专家权重的演化过程，可以直观地看到智能体是如何“理解”队友的。

### 8. 不足与局限
*   **阶数限制**：实验仅探讨了 0-2 阶 ToM，虽然符合人类认知习惯，但在极高复杂度的博弈中可能需要更高阶建模。
*   **计算开销**：A-ToM 需要同时运行多个假设（专家）的推理过程，这会显著增加 LLM 的 Token 消耗和推理延迟。
*   **规模限制**：实验主要集中在双智能体协作，尚未验证其在更大规模（如 5 人以上）多智能体系统中的扩展性。
*   **环境假设**：实验环境多为全观测或结构化状态，对于完全非结构化的开放世界环境，状态编码的准确性可能成为瓶颈。

（完）
