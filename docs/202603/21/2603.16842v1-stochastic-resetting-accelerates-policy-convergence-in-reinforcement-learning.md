---
title: Stochastic Resetting Accelerates Policy Convergence in Reinforcement Learning
title_zh: 随机重置加速强化学习中的策略收敛
authors: "Jello Zhou, Vudtiwat Ngampruetikorn, David J. Schwab"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16842v1"
tags: ["query:agent"]
score: 9.0
evidence: 随机重置以加速强化学习中的策略收敛
tldr: 本研究将统计力学中的随机重置机制引入强化学习，探讨其对策略收敛的影响。通过在学习过程中间歇性地将智能体返回初始状态，该方法在表格环境和深度强化学习任务中均显著加速了收敛速度。研究表明，随机重置通过截断冗长且无信息的轨迹来增强价值传播效率，且与时间折扣不同，它能在保持最优策略的同时提升学习性能，尤其适用于稀疏奖励和探索困难的场景。
selection_source: fresh_fetch
motivation: 探索统计力学中用于优化首通时间的随机重置机制，是否能作为一种优化原则来加速强化学习中的策略收敛过程。
method: 在表格化网格世界和基于神经网络的连续控制任务中，引入间歇性将智能体返回固定参考状态的随机重置机制。
result: 随机重置通过截断无信息轨迹并增强价值传播，在稀疏奖励和探索困难的环境中显著提升了深度强化学习的收敛效率。
conclusion: 随机重置是一种简单且有效的加速学习机制，它在不改变最优策略的前提下，成功将物理现象转化为强化学习的优化手段。
---

## 摘要
随机重置（即动力学过程间歇性地返回到固定的参考状态）已成为优化首通属性的一种强大机制。现有理论主要处理静态、非学习过程。在本文中，我们探讨了随机重置如何与强化学习相互作用，在强化学习中，底层动力学通过经验进行调整。在表格型网格环境中，我们发现即使重置没有减少纯扩散智能体的搜索时间，它也能加速策略收敛，这表明存在一种超越经典首通优化的新机制。在具有基于神经网络的值函数逼近的连续控制任务中，我们展示了当探索困难且奖励稀疏时，随机重置可以改进深度强化学习。与时间折扣不同，重置在保留最优策略的同时，通过截断冗长且无信息的轨迹来增强价值传播，从而加速收敛。我们的结果确立了随机重置作为一种简单、可调的加速学习机制，将统计力学中的一个经典现象转化为强化学习的一种优化原则。

## Abstract
Stochastic resetting, where a dynamical process is intermittently returned to a fixed reference state, has emerged as a powerful mechanism for optimizing first-passage properties. Existing theory largely treats static, non-learning processes. Here we ask how stochastic resetting interacts with reinforcement learning, where the underlying dynamics adapt through experience. In tabular grid environments, we find that resetting accelerates policy convergence even when it does not reduce the search time of a purely diffusive agent, indicating a novel mechanism beyond classical first-passage optimization. In a continuous control task with neural-network-based value approximation, we show that random resetting improves deep reinforcement learning when exploration is difficult and rewards are sparse. Unlike temporal discounting, resetting preserves the optimal policy while accelerating convergence by truncating long, uninformative trajectories to enhance value propagation. Our results establish stochastic resetting as a simple, tunable mechanism for accelerating learning, translating a canonical phenomenon of statistical mechanics into an optimization principle for reinforcement learning.

---

## 论文详细总结（自动生成）

这篇论文探讨了统计力学中的“随机重置”（Stochastic Resetting）机制如何加速强化学习（RL）中的策略收敛。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在强化学习中，智能体往往会陷入冗长且无信息的探索轨迹中，导致价值信息传播缓慢。论文探讨了物理学中用于优化“首通时间”（First-Passage Time, FPT）的随机重置机制，是否能作为一种通用的优化原则来加速学习过程。
*   **研究动机**：
    *   **跨学科借鉴**：随机重置在非平衡统计物理中已被证明能优化搜索过程，但在具有自适应学习能力的智能体（非平稳过程）中的应用尚不明确。
    *   **现有手段的局限**：常用的时间折扣因子（$\gamma$）虽然能影响收敛，但会改变最优策略的景观；而随机重置有望在不改变最优策略的前提下提升效率。

### 2. 核心方法论
*   **核心思想**：在智能体与环境交互的每一步，以固定概率 $r$ 强制将智能体送回初始状态（Start State），而不考虑其当前动作或状态。
*   **关键技术细节**：
    *   **重置协议**：重置是外部干预，不触发价值函数的更新（即不产生基于重置动作的奖励或状态转移学习），仅改变训练轨迹的分布。
    *   **价值传播机制**：在 Q-learning 等时序差分（TD）算法中，价值信息从目标点反向传播。随机重置通过截断偏离目标的“漫长路径”，使得成功的轨迹更趋向于直接，从而加快了 Bellman 更新向初始状态的渗透。
    *   **与折扣因子的区别**：折扣因子 $\gamma$ 修改了 Bellman 方程的权重，从而改变了最优策略（例如使智能体更避险）；随机重置则保持最优策略不变，仅优化到达该策略的训练路径。

### 3. 实验设计
论文在三种复杂程度递增的环境中进行了验证：
*   **表格型网格世界 (GridWorld)**：
    *   **场景**：$60 \times 60$ 和 $120 \times 120$ 的网格。
    *   **对比**：对比了重置概率 $r$ 对搜索效率（随机游走到达目标的时间）与学习效率（策略收敛所需步数）的影响。
*   **随机悬崖环境 (WindyCliff)**：
    *   **场景**：带有随机风力干扰的悬崖行走任务。
    *   **对比**：重点对比了重置率 $r$ 与折扣因子 $\gamma$ 对最终学习到的策略路径长度的影响。
*   **连续控制任务 (MountainCar)**：
    *   **算法**：使用深度 Q 网络（DQN）。
    *   **场景**：标准环境与“扩展陷阱”环境（增加探索难度），以及稀疏奖励与步长惩罚两种奖励机制。
    *   **Benchmark**：以无重置（$r=0$）的传统 RL 算法为基准。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **实验规模推测**：由于涉及大量的独立重复实验（每组条件 250 到 512 次重复），且包含深度强化学习（DQN），可以推断该研究需要一定的计算集群支持以完成统计学意义上的显著性验证。

### 5. 实验数量与充分性
*   **实验数量**：非常充分。每个实验条件都进行了 **250 至 512 次独立随机种子运行**，并报告了中位数和四分位距（IQR），这在 RL 论文中属于极高标准，有效排除了随机性干扰。
*   **充分性与公平性**：
    *   涵盖了从简单表格法到复杂深度神经网络的跨度。
    *   进行了详尽的参数扫掠（Parameter Sweeps），包括不同的探索率 $\epsilon$、重置率 $r$ 和折扣因子 $\gamma$。
    *   通过对比 $N=60$（重置不优化搜索）和 $N=120$（重置优化搜索）的情况，客观地分离了“搜索优化”与“学习加速”两个不同的贡献维度。

### 6. 主要结论与发现
*   **加速收敛**：在所有测试环境中，适当的随机重置率都能显著加快策略收敛速度。
*   **超越搜索优化**：即使在重置反而会增加随机游走搜索时间的情况下（如较小的网格），重置依然能通过优化价值传播路径来加速学习。
*   **策略保持**：随机重置不会像折扣因子那样改变最优策略的景观，它是一种纯粹的训练加速手段。
*   **适用场景**：重置在**探索困难**且**奖励稀疏**的环境中效果最为显著；在奖励信号密集（如每步都有惩罚）的环境中，重置的收益较小甚至可能有害。

### 7. 优点与亮点
*   **物理与 AI 的结合**：成功将统计物理中的非平衡态理论转化为 RL 的优化原则，具有很强的理论美感。
*   **机制清晰**：通过实验清晰地阐明了重置是如何通过截断“无信息轨迹”来增强价值传播的。
*   **简单易用**：该方法仅需一个超参数 $r$，且不需要修改现有的 RL 算法架构，具有极高的实用性。

### 8. 不足与局限
*   **环境局限性**：实验主要集中在具有明确起始点和终点的任务。对于多目标、无固定起点或部分可观测（POMDP）的复杂环境，重置的效果尚未验证。
*   **欺骗性奖励风险**：在存在“陷阱奖励”（Deceptive Rewards）的环境中，简单地返回起点可能导致智能体无法跳出局部最优，论文对此提及较少。
*   **超参数敏感性**：虽然 $r$ 是可调的，但过高的重置率会阻止智能体到达远端目标，如何自动寻找最优重置率仍是一个挑战。

（完）
