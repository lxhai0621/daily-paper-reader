---
title: "When Only the Final Text Survives: Implicit Execution Tracing for Multi-Agent Attribution"
title_zh: 当仅有最终文本留存时：面向多智能体归因的隐式执行追踪
authors: "Yi Nian, Haosen Cao, Shenzhe Zhu, Henry Peng Zou, Qingqing Luan, Yue Zhao"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17445v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 多智能体系统归因与交互拓扑
tldr: 针对多智能体系统在缺失日志时难以追溯责任的问题，本文提出隐式执行追踪（IET）框架。该方法利用密钥将智能体特征嵌入令牌分布，使最终文本具备自描述性。通过感知转换的评分机制，IET能从纯文本中精确重建智能体交互拓扑并实现令牌级归因，在不损害生成质量的前提下为系统审计提供了新途径。
selection_source: fresh_fetch
motivation: 在缺乏执行日志和智能体标识符的情况下，难以对多智能体系统生成的错误或有害输出进行责任归属和交互拓扑重建。
method: 提出隐式执行追踪（IET）框架，通过在生成过程中将特定智能体的密钥信号嵌入令牌分布，使文本成为可检测的自描述执行轨迹。
result: 实验证明 IET 能够以高准确率恢复智能体片段和协调结构，同时有效保持了文本的生成质量。
conclusion: IET 为多智能体语言系统提供了一种无需元数据、保护隐私且支持令牌级归因的有效审计机制。
---

## 摘要
当多智能体系统产生错误或有害的回答时，如果执行日志和智能体标识符不可用，谁该负责？多智能体语言系统日益依赖于委派和迭代细化等结构化交互，然而最终输出往往掩盖了底层的交互拓扑和智能体贡献。我们引入了 IET（隐式执行追踪），这是一个独立于元数据的框架，能够直接从生成的文本中实现令牌级归因，并提供了一种用于交互拓扑重建的简单机制。在生成过程中，特定于智能体的密钥信号被嵌入到令牌分布中，将文本转化为仅能通过密钥检测的自描述执行轨迹。在检测阶段，一种感知转换的评分方法会识别智能体交接点并重建交互图。实验表明，IET 在保持生成质量的同时，能以高准确度恢复智能体分段和协作结构，从而为多智能体语言系统实现隐私保护审计。

## Abstract
When a multi-agent system produces an incorrect or harmful answer, who is accountable if execution logs and agent identifiers are unavailable? Multi-agent language systems increasingly rely on structured interactions such as delegation and iterative refinement, yet the final output often obscures the underlying interaction topology and agent contributions. We introduce IET (Implicit Execution Tracing), a metadata-independent framework that enables token-level attribution directly from generated text and a simple mechanism for interaction topology reconstruction. During generation, agent-specific keyed signals are embedded into the token distribution, transforming the text into a self-describing execution trace detectable only with a secret key. At detection time, a transition-aware scoring method identifies agent handover points and reconstructs the interaction graph. Experiments show that IET recovers agent segments and coordination structure with high accuracy while preserving generation quality, enabling privacy-preserving auditing for multi-agent language systems.

---

## 论文详细总结（自动生成）

### 论文总结：当仅有最终文本留存时：面向多智能体归因的隐式执行追踪（IET）

#### 1. 核心问题与整体含义
随着多智能体系统（MAS）在复杂任务中的应用日益广泛，其高故障率（41%-87%）引发了严重的问责问题。目前主流的诊断框架（如 Who&When）高度依赖于完整的执行日志和智能体标识符（即“白盒”环境）。然而，在现实生产中，由于隐私保护、合规限制或简单的“复制粘贴”操作，这些元数据往往会丢失，导致最终输出的文本与生成它的系统轨迹完全脱节。
**论文的核心问题是：** 在仅有最终文本、缺失任何外部日志或元数据的情况下，如何准确识别每个文本片段是由哪个智能体生成的（归因），并重建它们之间的交互结构（拓扑重建）？

#### 2. 方法论
论文提出了 **IET（Implicit Execution Tracing，隐式执行追踪）** 框架，其核心思想是将执行身份直接绑定到生成过程中的令牌分布中。
*   **信号嵌入（生成阶段）：** 在解码时，利用特定智能体的密钥对逻辑值（Logits）进行调制。通过统计信号框架，将智能体身份转化为两种密钥：固定扰动方向密钥 $k_p$ 和上下文相关的词汇排列密钥 $k_\pi$。这种调制在统计上是可检测的，但对于没有密钥的用户来说，文本看起来与自然语言无异。
*   **令牌级归因（检测阶段）：** 使用滑动窗口统计评分函数 $f(t, a)$ 评估观察到的令牌与特定智能体信号的对齐程度。
*   **竞争性变点检测：** 引入一种检测算子，通过计算领先候选智能体与其他智能体之间的“竞争余量”信号，并结合累积和（CUSUM）算法，精确识别智能体交接的边界。
*   **拓扑编码：** 将交互路径（如 A 委派给 B）表示为在线更新的邻接矩阵，并将其编码为位掩码（Bitmask）嵌入水印信号中。检测时通过逆映射即可恢复完整的交互图（如链式、星型、树型结构）。

#### 3. 实验设计
*   **数据集：**
    *   **MAMA 拓扑数据集：** 包含星型、链型、树型等多种协调结构的结构化对话。
    *   **Who & When 基准：** 专注于多智能体转录中的说话人识别和错误归因。
    *   **其他：** TruthfulQA（用于辩论场景评估）、HotpotQA（用于滑动窗口实验）。
*   **对比方法：**
    *   **LLM 基准：** 使用 ChatGPT-4o 和 DeepSeek-v3 直接根据文本推断说话人。
    *   **传统分割基准：** Random、Recursive、Semantic-BoW、TextTiling（词汇凝聚力方法）。
*   **评估指标：** 令牌级准确率（TokenAcc）、交并比（IoU）、拓扑准确率（TopoAcc）、智能体/步骤归因准确率（AgentAcc/StepAcc）。

#### 4. 资源与算力
*   **模型：** 主要实验基于 `Llama-3.1-8B-Instruct`。
*   **算力说明：** 论文**未明确说明**具体的 GPU 数量或总训练时长。由于该方法主要涉及推理时的解码调制和后处理检测，不涉及大规模模型重训练，因此对算力的需求主要集中在推理和统计验证上。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 4、5、6 个智能体规模的多种拓扑结构（星型、链型、树型），每种配置包含 100 个样本。
*   **鲁棒性测试：** 进行了身份移除、边界破坏、PII（个人隐私信息）脱敏过滤等多种对抗性场景下的实验。
*   **充分性评价：** 实验设计较为全面，不仅验证了基础的归因能力，还通过下游任务（故障归因）验证了恢复轨迹的实用价值。对比了当前最强的闭源和开源模型，证明了在无元数据情况下，仅靠模型推理无法完成该任务，体现了 IET 的必要性。

#### 6. 主要结论与发现
*   **高精度恢复：** IET 在所有拓扑设置下均实现了超过 94% 的 TokenAcc 和约 0.93 的 IoU，且拓扑恢复准确率接近 100%。
*   **基准失效：** 即使是 GPT-4o 等强模型，在缺失元数据时，其 IoU 仅在 0.10-0.18 之间，证明了纯文本推理的局限性。
*   **隐私与合规友好：** IET 对 PII 脱敏（如将姓名替换为 [NAME]）具有极强的鲁棒性，因为信号嵌入在分布层级而非词汇层级。
*   **下游支持：** 在元数据完全丢失的情况下，利用 IET 恢复的轨迹仍能支持有效的故障归因，显著优于所有基线。

#### 7. 优点
*   **创新性：** 首次将水印技术从简单的“AI 检测”提升为系统级的“执行追踪”工具。
*   **解耦性：** 实现了归因能力与系统日志的解耦，解决了“复制粘贴”导致的溯源断裂问题。
*   **细粒度：** 支持令牌级的精确归因，而非仅限于句子或段落级。
*   **实用性：** 能够处理复杂的交互拓扑，而不仅仅是简单的轮流对话。

#### 8. 不足与局限
*   **系统控制权：** 该方法要求必须在生成端（服务端）部署 IET 调制逻辑，无法对完全不受控的第三方黑盒 API 进行追溯。
*   **生成质量风险：** 虽然实验称保持了质量，但任何对 Logits 的扰动在极端敏感的任务中仍可能存在潜在影响（尽管论文中使用了低强度扰动）。
*   **扩展性限制：** 实验最高测试到 6 个智能体，在超大规模智能体集群（如数百个智能体）中的密钥冲突和检测精度尚未验证。
*   **后期编辑敏感性：** 如果用户对生成的文本进行了大幅度的手动修改或重写，嵌入的统计信号可能会被破坏，导致归因失效。

（完）
