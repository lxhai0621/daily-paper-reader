---
title: "GradMem: Learning to Write Context into Memory with Test-Time Gradient Descent"
title_zh: GradMem：通过测试时梯度下降学习将上下文写入内存
authors: "Yuri Kuratov, Matvey Kairov, Aydar Bulatov, Ivan Rodkin, Mikhail Burtsev"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13875v1"
tags: ["query:agent"]
score: 9.0
evidence: 用于长上下文管理的压缩记忆
tldr: 针对大语言模型处理长上下文时KV缓存内存开销大的问题，本文提出GradMem。该方法通过在推理阶段对少量前缀记忆令牌进行梯度下降优化，将上下文信息压缩进固定大小的记忆状态中。GradMem利用自监督重构损失进行迭代式写入，相比传统的单向前向写入方法，在键值检索和自然语言任务上表现出更强的记忆容量和泛化能力。
selection_source: fresh_fetch
motivation: 传统的KV缓存机制在处理长上下文时内存开销巨大，需要一种更高效的压缩记忆机制来替代。
method: 提出GradMem方法，在模型权重冻结的情况下，通过测试时梯度下降优化前缀记忆令牌，以最小化上下文重构损失。
result: 在关联键值检索任务中优于前向写入方法，并在bAbI和SQuAD等自然语言任务中展现出竞争力的性能。
conclusion: GradMem证明了通过测试时优化进行损失驱动的记忆写入，能比传统前向方法更有效地扩展模型处理长上下文的能力。
---

## 摘要
许多大语言模型应用需要基于长上下文进行条件生成。Transformer 通常通过存储每层过去激活的大型 KV 缓存来支持这一点，这会产生巨大的内存开销。一个理想的替代方案是压缩内存：一次性读取上下文，将其存储在紧凑状态中，并根据该状态回答多个查询。我们在上下文移除设置下对此进行研究，即模型在推理时必须在无法访问原始上下文的情况下生成答案。我们引入了 GradMem，它通过逐样本的测试时优化将上下文写入内存。给定一个上下文，GradMem 在保持模型权重冻结的情况下，对一小组前缀内存标记（prefix memory tokens）执行几步梯度下降。GradMem 显式地优化模型级的自监督上下文重建损失，从而实现具有迭代误差校正的损失驱动写入操作，这与仅前向（forward-only）的方法不同。在关联键值检索任务上，GradMem 在相同内存大小的情况下优于仅前向的内存写入器，且额外的梯度步骤比重复的前向写入更有效地扩展了容量。我们进一步证明 GradMem 可以迁移到合成基准之外：配合预训练语言模型，它在包括 bAbI 和 SQuAD 变体在内的自然语言任务上取得了具有竞争力的结果，且仅依赖于编码在内存中的信息。

## Abstract
Many large language model applications require conditioning on long contexts. Transformers typically support this by storing a large per-layer KV-cache of past activations, which incurs substantial memory overhead. A desirable alternative is ompressive memory: read a context once, store it in a compact state, and answer many queries from that state. We study this in a context removal setting, where the model must generate an answer without access to the original context at inference time. We introduce GradMem, which writes context into memory via per-sample test-time optimization. Given a context, GradMem performs a few steps of gradient descent on a small set of prefix memory tokens while keeping model weights frozen. GradMem explicitly optimizes a model-level self-supervised context reconstruction loss, resulting in a loss-driven write operation with iterative error correction, unlike forward-only methods. On associative key--value retrieval, GradMem outperforms forward-only memory writers with the same memory size, and additional gradient steps scale capacity much more effectively than repeated forward writes. We further show that GradMem transfers beyond synthetic benchmarks: with pretrained language models, it attains competitive results on natural language tasks including bAbI and SQuAD variants, relying only on information encoded in memory.

---

## 论文详细总结（自动生成）

以下是对论文《GradMem: Learning to Write Context into Memory with Test-Time Gradient Descent》的结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：大语言模型（LLM）在处理长上下文时，传统的 KV-cache 机制会导致内存开销随长度线性增长，且难以实现高效的上下文复用。
*   **研究动机**：现有的“压缩记忆”方法（如 RMT）多采用单向前向传播（Forward-only）来编码信息，缺乏反馈机制，导致压缩效率低且无法纠错。
*   **研究目标**：提出一种基于“测试时优化”的记忆机制，通过梯度下降将上下文信息迭代式地写入极小规模的记忆令牌（Memory Tokens）中，实现高压缩比下的高保真信息检索。

### 2. 方法论
*   **核心思想**：将记忆令牌视为可训练的参数，在推理阶段（Test-time）针对每个样本执行少量的梯度下降步骤，以最小化自监督的重构损失。
*   **关键技术细节**：
    *   **WRITE 阶段（写入）**：冻结模型权重 $\theta$，仅针对内存令牌 $M$ 进行 $K$ 步梯度更新。目标函数是**上下文重构损失**（即模型在给定 $M$ 的情况下，预测原始上下文 $C$ 的交叉熵）。
    *   **READ 阶段（读取）**：在“上下文移除”设定下，模型仅利用优化后的内存令牌 $\hat{M}$ 和查询 $Q$ 来生成答案 $Y$。
    *   **元学习（Meta-learning）**：使用 MAML 框架预训练模型参数和内存初始值 $M_0$，使得模型在推理时仅需极少数（$K \le 5$）梯度步就能捕获关键信息。
*   **算法流程**：初始化内存 $\rightarrow$ 计算重构损失 $\rightarrow$ 梯度更新内存令牌 $\rightarrow$ 移除上下文 $\rightarrow$ 基于内存回答问题。

### 3. 实验设计
*   **数据集/场景**：
    *   **关联 KV 检索**：合成任务，用于精确测量固定大小内存能存储多少对键值。
    *   **bAbI 任务**：测试多步推理能力。
    *   **Short SQuAD**：测试自然语言问答的压缩效果。
    *   **WikiText-103**：测试语言建模中的长程依赖。
*   **对比方法（Baselines）**：
    *   **上限**：全注意力 Transformer（保留完整 KV-cache）。
    *   **层级记忆模型**：Mamba (SSM)、ARMT（关联递归记忆）。
    *   **同类压缩模型**：RMT（递归记忆 Transformer，采用前向写入）。

### 4. 资源与算力
*   **硬件支持**：实验中明确提到了使用 **NVIDIA A100 GPU**。
*   **算力优化**：由于 GradMem 涉及二阶梯度（Double-backward），作者开发了高效的注意力算子（如 **Flash HVP**），将 1024 长度下的显存占用从 60GB 优化至 30GB。
*   **训练时长**：文中未明确给出总训练小时数，但强调了 WRITE 阶段的延迟可以通过多次 READ 查询来摊销。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从合成任务到真实自然语言任务的 4 大类数据集。
*   **消融实验**：
    *   对比了不同梯度步数 $K$（1 到 30 步）对性能的影响。
    *   验证了元学习（Meta-learning）对收敛速度的决定性作用。
    *   分析了重构损失与下游任务准确率的相关性。
*   **客观性**：在相同内存容量（如 8 个令牌）下与 RMT 进行对比，证明了梯度驱动写入的优越性，实验设计较为公平。

### 6. 主要结论与发现
*   **梯度写入更高效**：在相同内存大小下，GradMem 的记忆容量远超前向写入方法。例如，8 个令牌在 5 步梯度下可存储 96 对 KV，而前向方法在 16 对时就已失效。
*   **计算可扩展性**：在推理时增加梯度步数 $K$ 能持续提升准确率，表现出明显的“计算换精度”特征。
*   **选择性记忆**：模型在优化过程中会自动优先保留对回答问题更有用的“值（Value）”信息，而非机械地背诵全文。
*   **泛化能力**：该方法成功迁移至预训练模型（如 GPT-2, Pythia），在自然语言 QA 任务上达到竞争水平。

### 7. 优点
*   **迭代纠错**：相比前向模型，梯度下降提供了显式的误差信号，允许模型反复修正记忆。
*   **高压缩比**：能够将数千个令牌的信息压缩进极少数的向量中。
*   **灵活性**：无需重新微调整个模型，仅通过改变推理时的优化步数即可调整模型表现。

### 8. 不足与局限
*   **训练开销**：二阶梯度的计算极其昂贵，对显存要求高，且难以利用现有的 FlashAttention 等一阶优化内核。
*   **推理延迟**：WRITE 阶段（写入记忆）比普通前向传播慢得多，仅适用于“一次写入、多次查询”的场景。
*   **重构损失的偏差**：简单的 Token 重构可能无法完全对齐复杂的下游推理需求，未来可能需要更高级的自监督目标。

（完）
