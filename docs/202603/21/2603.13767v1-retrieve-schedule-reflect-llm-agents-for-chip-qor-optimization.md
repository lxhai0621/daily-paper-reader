---
title: "Retrieve, Schedule, Reflect: LLM Agents for Chip QoR Optimization"
title_zh: 检索、调度、反思：用于芯片 QoR 优化的 LLM 智能体
authors: "Yikang ouyang, Yang Luo, Dongsheng Zuo, Yuzhe Ma"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13767v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 基于RAG的智能体LLM框架用于芯片优化
tldr: 针对现代芯片设计中高质量结果（QoR）优化高度依赖人工经验的问题，本文提出了一种基于大语言模型（LLM）的智能体框架。该框架通过检索增强生成（RAG）整合自然语言专家知识，并利用帕累托驱动的反馈进行自我反思，实现了与EDA工具的直接交互和长程优化调度。实验表明，该方法在性能、功耗和面积优化上优于强化学习等黑盒搜索方法，且效率提升4倍以上，达到了专家级水平。
selection_source: fresh_fetch
motivation: 现代芯片设计中的QoR优化依赖复杂的人工调度，缺乏高效的自动化长程优化手段。
method: 提出一种结合RAG专家知识检索、搜索树调度以及帕累托驱动反思机制的LLM智能体框架。
result: "相比强化学习方法，该框架在提升10%时序性能的同时降低了功耗和面积，且运行速度快4倍以上。"
conclusion: 该研究证明了LLM智能体在自动化芯片设计优化中的巨大潜力，能够实现高效且可定制的QoR权衡。
---

## 摘要
现代芯片设计要求在严格的上市时间限制下，对时序、功耗和面积进行多目标优化。尽管 EDA 工具中集成了强大的优化算法，但实现高 QoR（设计质量）取决于有效的长周期调度，而这在很大程度上依赖于人工专家的干预。为了解决这一问题并实现芯片设计自动化，我们提出了一种基于 LLM 的智能体框架，通过与 EDA 工具直接交互来调度芯片优化。该智能体以自然语言形式的专业知识为基础，通过检索增强生成 (RAG) 将其表示为搜索树。我们通过语言反思，利用帕累托驱动的 QoR 反馈进一步提高了调度质量。实验结果表明，与强化学习等黑盒搜索方法相比，我们的框架在功耗和面积更低的情况下，时序提升了 10%，且速度提高了 4 倍以上。优化后的 QoR 也与人类专家达到的水平相当。最后，该智能体支持以自然语言表达的定制化任务，从而实现偏好性的 QoR 权衡。代码和芯片设计数据将在 https://github.com/YiKangOY/Open-LLM-ECO 公开。

## Abstract
Modern chip design requires multi-objective optimization of timing, power, and area under stringent time-to-market constraints. Although powerful optimization algorithms are integrated into EDA tools, achieving high QoR hinges on effective long-horizon scheduling, which relies heavily on manual expert intervention. To address this issue and automate chip design, we propose an agentic LLM framework that schedules chip optimizations through direct interaction with EDA tools. The agent is grounded in natural language expertise expressed as a search tree through retrieval-augmented generation (RAG). We further improve scheduling quality with Pareto-driven QoR feedback through language reflection. Experimental results show that, compared with black-box search methods such as reinforcement learning, our framework achieves 10% greater timing improvement while consuming less power and area, with more than 4x speedup. The post-optimization QoR is also comparable to that achieved by human experts. Finally, the agent supports customized tasks expressed in natural language, enabling preferential QoR trade-offs. The code and chip design data will be publicly available at https://github.com/YiKangOY/Open-LLM-ECO.

---

## 论文详细总结（自动生成）

以下是对论文《Retrieve, Schedule, Reflect: LLM Agents for Chip QoR Optimization》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现代芯片设计中的 QoR（结果质量，包括时序、功耗、面积 PPA）优化是一个 NP 难问题。虽然 EDA 工具集成了多种优化算法，但如何针对特定设计**长程调度**这些算法（即决定在什么阶段、以什么参数、按什么顺序执行哪些算法）高度依赖资深工程师的经验。
*   **研究动机**：
    *   **人工成本高**：手动调度迭代耗时且需要持续监控。
    *   **现有自动化方法局限**：如强化学习（RL）等黑盒方法仅依赖数值反馈，缺乏领域专家知识的引导，导致搜索效率低且难以处理多目标权衡。
    *   **定制化困难**：传统方法难以直接理解自然语言描述的特定设计偏好。

### 2. 方法论
该论文提出了一个名为 **Retrieve-Schedule-Reflect** 的多智能体 LLM 框架，模拟工程师的“调度-执行-反思”循环：
*   **核心思想**：将芯片优化建模为由 LLM 驱动的迭代过程，通过检索专家知识库引导决策，并利用历史尝试的帕累托最优结果进行自我进化。
*   **关键技术细节**：
    1.  **基于搜索树的 RAG（检索增强生成）**：构建了一个包含专家调度策略和 EDA 手册的搜索树（Search Tree）。节点存储自然语言描述的设计状态、优化策略和工具手册片段。智能体根据当前设计状态检索最相关的“路径模板”作为参考。
    2.  **多智能体协作**：
        *   **调度器（Scheduler）**：分析当前 QoR 报告，结合检索到的专家路径，决定下一轮的优化目标（如先修时序还是先降功耗）和高层策略。
        *   **执行器（Executor）**：将高层策略转化为具体的 EDA 工具命令（如 `timing_opt` 及其复杂参数），并参考工具手册确保语法合法。
        *   **反思器（Reflector）**：在多轮尝试后，识别帕累托最优（Pareto-optimal）的优化轨迹，总结成功经验和失败教训，生成反馈供下一轮调度使用。
    3.  **帕累托驱动的反思机制**：通过对比不同轮次的 PPA 权衡结果，学习何时该“激进优化”，何时该“保守恢复”。

### 3. 实验设计
*   **数据集与场景**：采用 ICCAD'24 竞赛数据集中的 8 个开源芯片设计（基于 ASAP7 7nm 工艺库），涵盖了从小型模块到复杂的 NVDLA 分区和 Ariane 处理器核心。
*   **Benchmark（基准方法）**：
    *   **RL Baseline**：使用 A3C 强化学习算法的黑盒搜索方法。
    *   **Human Baseline**：由经验丰富的工程师手动进行的 10 轮优化调度。
*   **对比指标**：总负时序裕量（Total Slack）、最差时序裕量（Worst Slack）、功耗（Power）和面积（Area）。

### 4. 资源与算力
*   **硬件环境**：2 颗 AMD EPYC 7543 32 核 CPU，1 块 NVIDIA RTX 3090 GPU。
*   **LLM 模型**：默认使用 **GPT-5-mini**（2025-08-07 版本），消融实验中对比了 GPT-4o-mini 和 DeepSeek-V3.2。
*   **运行时间**：框架平均完成一个设计的优化约需 3.54 小时（包含 EDA 工具运行时间），比 RL 方法快 4 倍以上。

### 5. 实验数量与充分性
*   **实验规模**：在 8 个不同规模和特征的设计上进行了测试，结果具有代表性。
*   **消融实验**：分别验证了“无 RAG”、“无反思”以及“不同 LLM 底座”对结果的影响，证明了 RAG 和反思机制的互补性。
*   **定制化测试**：额外进行了“允许 5% 时序损失以换取功耗优化”的自然语言指令实验，验证了框架处理开放式目标的能力。
*   **评价**：实验设计较为全面，对比了人类专家和主流 AI 方法，客观展示了 LLM 在复杂工程调度中的优势。

### 6. 主要结论与发现
*   **性能卓越**：该框架在时序改进上比 RL 基准高出 **10%**，同时实现了更低的功耗和面积，整体 QoR 达到或超过了人类专家水平。
*   **效率极高**：相比需要大量训练样本的 RL，该框架通过专家知识引导，搜索步数更少，速度提升 **4 倍**以上。
*   **灵活可定制**：能够直接理解“在保证时序的前提下尽量减小面积”等自然语言偏好，并据此调整调度策略。
*   **反思的重要性**：跨轮次的反思机制是避免陷入局部最优、实现长程优化的关键。

### 7. 优点与亮点
*   **知识驱动而非纯数据驱动**：通过 RAG 将人类工程师的直觉和手册知识结构化，解决了 LLM 在垂直领域“幻觉”和“不专业”的问题。
*   **闭环自进化**：引入帕累托前沿概念进行反思，使智能体能从多目标冲突中学习复杂的权衡逻辑。
*   **端到端自动化**：实现了从自然语言任务到 EDA 工具执行的完整闭环，显著降低了芯片设计的门槛。

### 8. 不足与局限
*   **工具依赖性**：实验主要基于 Synopsys PrimeECO 等商业工具，虽然提供了 Mock 代码，但实际部署仍需昂贵的 EDA 许可。
*   **LLM 成本**：对于小型设计，LLM 的 API 调用成本和交互延迟可能占比较高（如图 5 所示）。
*   **搜索空间限制**：目前的搜索树基于已有的专家经验，对于全新的、突破性的优化路径探索能力可能受限于知识库的覆盖范围。
*   **实时性**：虽然比 RL 快，但对于需要秒级反馈的交互式设计场景，数小时的运行时间仍显略长。

（完）
