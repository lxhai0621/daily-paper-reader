---
title: AI Scientist via Synthetic Task Scaling
title_zh: 通过合成任务扩展实现 AI 科学家
authors: "Ziyang Cai, Harkirat Behl"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.17216v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 面向智能体的自动化科学发现与合成任务生成
tldr: 本研究针对自动科学发现中AI智能体缺乏有效训练方法的问题，提出了一种合成任务扩展流水线。该流水线能自动生成基于真实数据集的机器学习挑战，并通过自我调试确保质量。通过利用GPT-5生成的轨迹训练Qwen3系列模型，显著提升了智能体在MLGym基准测试中的表现，为培养具备科研能力的AI提供了新路径。
selection_source: fresh_fetch
motivation: 现有的LLM在机器学习研究中常产生无效想法，且缺乏系统化的训练环境来提升智能体的科研能力。
method: 开发了一套自动合成机器学习任务的流水线，涵盖主题采样、数据集验证及代码生成，并引入自我调试循环确保任务质量。
result: "使用合成任务轨迹训练的Qwen3-4B和8B模型在MLGym基准上的AUP指标分别提升了9%和12%。"
conclusion: 通过合成任务扩展训练数据是提升AI智能体科学发现能力的有效途径，为实现自动化AI科学家奠定了基础。
---

## 摘要
随着 AI 智能体（AI agents）的出现，自动化科学发现已成为一个可实现的目标。许多近期工作构建了能够进行机器学习研究的智能体系统，但并未提供训练此类智能体的原则性方法——且当前的 LLM 往往会生成看似合理但无效的想法。为了在训练能够“从做中学”的智能体方面取得进展，我们提供了一种针对机器学习智能体的新型合成环境生成流水线。我们的流水线能够自动合成与 SWE-agent 框架兼容的机器学习挑战，涵盖主题采样、数据集提议和代码生成。生成的合成任务具有以下特点：1) 基于真实的机器学习数据集，因为提议的数据集已通过 Huggingface API 验证；2) 通过自调试循环验证了更高的质量。为了验证合成任务的有效性，我们应对了机器学习任务基准测试 MLGym。我们从合成任务中采样教师模型（GPT-5）的轨迹，然后利用这些轨迹训练学生模型（Qwen3-4B 和 Qwen3-8B）。使用我们的合成任务训练的学生模型在 MLGym 上实现了性能提升，使 Qwen3-4B 的 AUP 指标提高了 9%，Qwen3-8B 提高了 12%。

## Abstract
With the advent of AI agents, automatic scientific discovery has become a tenable goal. Many recent works scaffold agentic systems that can perform machine learning research, but don't offer a principled way to train such agents -- and current LLMs often generate plausible-looking but ineffective ideas. To make progress on training agents that can learn from doing, we provide a novel synthetic environment generation pipeline targeting machine learning agents. Our pipeline automatically synthesizes machine learning challenges compatible with the SWE-agent framework, covering topic sampling, dataset proposal, and code generation. The resulting synthetic tasks are 1) grounded in real machine learning datasets, because the proposed datasets are verified against the Huggingface API and are 2) verified for higher quality with a self-debugging loop. To validate the effectiveness of our synthetic tasks, we tackle MLGym, a benchmark for machine learning tasks. From the synthetic tasks, we sample trajectories from a teacher model (GPT-5), then use the trajectories to train a student model (Qwen3-4B and Qwen3-8B). The student models trained with our synthetic tasks achieve improved performance on MLGym, raising the AUP metric by 9% for Qwen3-4B and 12% for Qwen3-8B.

---

## 论文详细总结（自动生成）

### 论文总结：通过合成任务扩展实现 AI 科学家 (AI Scientist via Synthetic Task Scaling)

#### 1. 核心问题与整体含义
论文探讨了如何训练 AI 智能体（Agents）以实现**自主科学发现**（特别是机器学习研究）。虽然现有的 LLM 拥有丰富的理论知识，但在执行多步骤、目标导向的科研任务（如调试、实验迭代、结果分析）时表现不佳，且往往生成“看似合理但实际无效”的想法。论文的核心动机是：**缺乏一种原则性的方法来训练智能体“从做中学”**。为此，作者提出了一种可扩展的流水线，通过自动合成大量机器学习任务并生成专家轨迹，来提升智能体的科研实战能力。

#### 2. 方法论
论文提出了一个分为三个阶段的自动合成流水线，旨在生成高质量、多样化的训练数据：
*   **阶段 1：环境合成 (Environment Synthesis)**
    *   **主题采样**：从模型中采样 1000 个不同的机器学习研究主题。
    *   **任务与数据集提议**：为每个主题生成任务描述，并通过 HuggingFace API 搜索并验证真实的匹配数据集。
    *   **代码生成**：生成与 SWE-agent 框架兼容的配置文件、基准实现（Baseline）和评估脚本。
*   **阶段 2：环境验证 (Environment Verification)**
    *   引入**自我调试循环 (Self-debugging Loop)**：使用 GPT-5 智能体试运行新生成的任务。如果运行出错，将错误反馈给模型进行修复（最多迭代 $k$ 次），确保任务在技术上是可运行且有效的。
*   **阶段 3：轨迹生成与过滤 (Trajectory Generation & Filtering)**
    *   在高性能计算（HPC）集群上并行运行验证通过的任务，收集教师模型（GPT-5）解决任务的完整操作轨迹。
    *   **过滤机制**：仅保留至少有一次成功提交且长度适中（<48k tokens）的轨迹，最终形成约 3.4 万条 SFT（监督微调）训练数据。

#### 3. 实验设计
*   **基准测试 (Benchmark)**：主要使用 **MLGym**，包含 13 个涵盖计算机视觉、强化学习、语言建模等领域的机器学习挑战。
*   **对比方法**：
    *   基座模型：GPT-4o, GPT-5, Qwen3-4B, Qwen3-8B。
    *   实验组：经过合成任务轨迹微调后的 **SFT-Qwen3-4B** 和 **SFT-Qwen3-8B**。
*   **评估指标**：AUP (Area Under the Performance curve)，用于综合衡量智能体在多次尝试中提升分数的效率和最终水平。

#### 4. 资源与算力
*   **硬件环境**：任务生成和验证在 HPC 集群上完成，每个任务占用 1 个 GPU。文中提到评估环境模拟的是 **NVIDIA RTX A6000 GPU**（8 核 CPU）。
*   **数据规模**：从 1000 个主题中验证出 500 个任务，最终生成了约 3.4 万条有效轨迹。
*   **训练细节**：使用了 SFT 微调。虽然未明确给出训练 Qwen3 模型的总 GPU 小时数，但提到单个任务的训练超时限制为 30 分钟至 5 小时不等。

#### 5. 实验数量与充分性
*   **实验规模**：生成了 500 个合成任务，这比 MLGym 自带的 13 个任务规模大得多，体现了良好的扩展性。
*   **重复性**：在 MLGym 的每个子任务上进行了 **64 次独立运行**，并以小提琴图形式展示分布，结果具有统计学意义。
*   **充分性评价**：实验在提升幅度上表现显著，但在**消融实验**方面略显不足（未分别验证数据集验证、自调试循环等单个组件的贡献度）。

#### 6. 主要结论与发现
*   **性能显著提升**：经过合成任务训练后，Qwen3-4B 的 AUP 提升了 **9%**，Qwen3-8B 提升了 **12%**。
*   **跨任务泛化**：尽管训练数据是合成的，但模型在未见过的 MLGym 任务上表现出更强的迭代优化和调试能力。
*   **规模效应**：通过自动化的流水线，可以不依赖人类标注而持续扩展智能体的“科研经验值”。

#### 7. 优点
*   **完全自动化**：无需人工干预即可生成多样化的科研挑战，解决了科研智能体训练数据匮乏的问题。
*   **真实性保障**：通过 HuggingFace API 挂钩真实数据集，避免了纯合成数据可能导致的“幻觉”任务。
*   **鲁棒性高**：自调试循环显著提高了生成任务的成功率和质量。
*   **框架兼容**：生成的任务直接兼容 SWE-agent，易于集成到现有的智能体生态中。

#### 8. 不足与局限
*   **基准单一**：目前仅在 MLGym 上进行了深入评估，对其他基准（如 MLE-Bench）的泛化效果尚待验证。
*   **教师模型依赖**：训练质量受限于教师模型（GPT-5）的能力上限，无法超越教师未曾解决的难题。
*   **格式对齐风险**：性能提升可能部分归功于模型更熟悉 SWE-agent 的交互格式，而非纯粹的科研逻辑提升。
*   **缺乏创新优化**：目前的 SFT 范式主要是在模仿，尚未引入强化学习（RL）来显式鼓励智能体发现“全新的科研思路”。

（完）
