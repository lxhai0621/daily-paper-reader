---
title: "Outcome-Aware Tool Selection for Semantic Routers: Latency-Constrained Learning Without LLM Inference"
title_zh: 语义路由的结果感知工具选择：无需 LLM 推理的延迟受限学习
authors: "Huamin Chen, Xunzhuo Liu, Junchen Jiang, Bowei He, Xue Liu"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.13426v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: LLM网关中语义路由器的结果感知工具选择
tldr: 针对大模型推理网关中语义路由器的延迟挑战，本文提出了一种名为OATS的工具选择方法。该方法通过将工具向量向历史上成功查询的质心插值，在不增加推理延迟、参数或GPU成本的前提下，显著提升了工具检索的准确性。实验表明，在MetaTool和ToolBench数据集上，OATS均优于基准模型，且所有计算均可在毫秒级CPU时间内完成，为低延迟场景提供了高效的工具选择方案。
selection_source: fresh_fetch
motivation: 语义路由器在LLM请求路径中至关重要，现有的工具选择方法往往会增加推理延迟和计算成本。
method: 提出OATS方法，通过离线将工具嵌入向量向其历史上执行成功的查询向量质心进行插值优化。
result: "在MetaTool数据集上将NDCG@5从0.869提升至0.940，且在ToolBench上也取得了显著改进。"
conclusion: 在低延迟约束下，应优先采用零成本的向量微调方法，仅在数据密度足够时才考虑引入学习型组件。
---

## 摘要
LLM 推理网关中的语义路由在关键请求路径中选择工具，每增加一毫秒的延迟都会在数百万次请求中产生累积效应。我们提出了结果感知工具选择（OATS），它将工具嵌入向其历史上成功执行的查询质心进行插值——这是一个离线过程，在推理服务时不会增加参数、延迟或 GPU 成本。在 MetaTool（199 个工具，4,287 个查询）上，该方法将 NDCG@5 从 0.869 提升至 0.940；在 ToolBench（2,413 个 API）上，从 0.834 提升至 0.848。我们还评估了两种学习型扩展：一个包含 2,625 个参数的 MLP 重排序器和一个包含 197K 参数的对比适配器。当结果数据相对于工具集较为稀疏时，MLP 重排序器的效果会变差或仅与基准持平；而对比适配器在 MetaTool 上提供了相当的增益（NDCG@5: 0.931）。所有方法均在相同的 30% 留出测试集上进行了评估。实践结论是：应优先采用零成本的优化方案，仅在数据密度足够时才引入学习组件。所有机制均能在个位数毫秒级的 CPU 预算内运行。

## Abstract
Semantic routers in LLM inference gateways select tools in the critical request path, where every millisecond of added latency compounds across millions of requests. We propose Outcome-Aware Tool Selection (OATS), which interpolates tool embeddings toward the centroid of queries where they historically succeed -- an offline process that adds no parameters, latency, or GPU cost at serving time. On MetaTool (199~tools, 4,287~queries), this improves NDCG@5 from 0.869 to 0.940; on ToolBench (2,413~APIs), from 0.834 to 0.848. We also evaluate two learned extensions: a 2,625-parameter MLP re-ranker and a 197K-parameter contrastive adapter. The MLP re-ranker hurts or matches baseline when outcome data is sparse relative to the tool set; the contrastive adapter provides comparable gains on MetaTool (NDCG@5: 0.931). All methods are evaluated on the same held-out 30\% test split. The practical takeaway is to start with the zero-cost refinement and add learned components only when data density warrants it. All mechanisms run within single-digit millisecond CPU budgets.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **OATS (Outcome-Aware Tool Selection)** 的方法，旨在解决大语言模型（LLM）推理网关中，语义路由器在极低延迟约束下进行准确工具选择的问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机）
在 LLM 推理架构中，语义路由器（Semantic Router）位于请求的关键路径上，负责在请求发送给后端模型前选择合适的工具。
*   **核心矛盾**：工具选择必须在**个位数毫秒级**内完成，且通常在 **CPU** 上运行以节省 GPU 资源。
*   **现有局限**：
    1.  **LLM 驱动的选择**：虽然准确但延迟极高（数百至数千毫秒），且消耗昂贵的 GPU。
    2.  **静态向量匹配**：虽然快（<5ms），但依赖于开发者编写的工具描述。如果描述模糊或存在词汇偏差，检索准确率会很低，且无法从历史成功/失败案例中学习。
*   **研究目标**：在不增加推理延迟和 GPU 成本的前提下，利用历史调用结果（Outcome）来优化工具选择的准确性。

### 2. 方法论：核心思想与技术细节
OATS 的核心思想是**“离线学习，在线检索”**。它通过将工具的向量表示向历史上执行成功的查询向量质心靠拢，实现对向量空间的微调。

*   **核心机制 (Stage 1: 向量微调)**：
    *   **收集反馈**：从日志中提取（查询 $q$, 工具 $t$, 结果 $o$）三元组。
    *   **质心插值**：计算工具 $t$ 对应的正向查询质心（成功案例）和负向查询质心（失败案例）。
    *   **更新公式**：$\hat{e}_i = (1 - \alpha) \cdot e(d_i) + \alpha \cdot \bar{e}(Q^+_i) - \beta \cdot \bar{e}(Q^-_i)$。其中 $\alpha$ 是吸引强度，$\beta$ 是排斥强度。
    *   **迭代与动量**：通过多次迭代更新向量，并使用动量机制防止震荡。
*   **扩展机制 (Stage 2 & 3)**：
    *   **Stage 2 (MLP 重排序)**：训练一个极小的 MLP（2,625 参数），利用相似度间隙、历史成功率等特征进行二次评分。
    *   **Stage 3 (对比适配器)**：引入一个轻量级适配器（197K 参数），通过对比学习（InfoNCE Loss）重新塑造整个向量空间。

### 3. 实验设计
*   **数据集**：
    1.  **MetaTool**：包含 199 个工具和 4,287 个查询，涵盖相似选择、特定场景、可靠性问题等子任务。
    2.  **ToolBench**：规模更大，包含 2,413 个 API，测试环境更复杂。
*   **Benchmark 与对比方法**：
    *   **基准**：BM25（词法检索）、静态向量检索（Static Embedding, SE）、SE + 词法加权。
    *   **对比**：将 OATS 的三个阶段（S1, S2, S3）与上述基准进行对比，并参考了部分 LLM（如 GPT-3.5, Vicuna）在相同任务上的表现。
*   **评价指标**：Recall@K, Precision@K, NDCG@K, MRR。

### 4. 资源与算力
*   **推理算力**：所有 OATS 方法均在 **单核 CPU** 上运行。
*   **延迟表现**：p50 延迟保持在 **3-7 毫秒** 之间。
*   **训练算力**：文中未详细列出具体的 GPU 型号和训练时长，但强调了 Stage 1 是**零成本**的离线批处理任务，Stage 2/3 的参数量极小（KB 级别），训练开销极低，可在普通硬件上快速完成。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流工具选择基准上进行了全面测试，使用了固定的 70/30 训练/测试拆分。
*   **消融实验**：详细对比了仅使用向量微调（S1）、加入 MLP（S2）和加入适配器（S3）的效果。
*   **客观性**：实验涵盖了从 200 个工具到 2,400 个工具的不同规模，并分析了数据密度（每个工具对应的样本数）对学习效果的影响，结论较为客观。

### 6. 主要结论与发现
1.  **零成本增益**：Stage 1（向量微调）在不增加任何推理开销的情况下，在 MetaTool 上将 NDCG@5 从 0.869 提升至 0.940。
2.  **学习组件的局限性**：当每个工具的样本量较少时（如 ToolBench），复杂的 MLP 重排序器反而会降低性能。
3.  **超越 LLM**：在处理“相似工具选择”等困难任务时，经过微调的 OATS 检索器表现优于许多 7B-13B 规模的 LLM，且速度快 1000 倍。
4.  **实践建议**：优先使用向量微调，仅在数据密度足够高（如每个工具 >10 条记录）时才考虑引入学习型组件。

### 7. 优点（亮点）
*   **极高的实用性**：直接针对生产环境中的延迟痛点，无需修改现有的检索代码逻辑，只需更新向量数据库中的向量。
*   **冷启动友好**：对于没有历史数据的工具，自动退回到静态描述匹配。
*   **成本极低**：摆脱了对 GPU 的依赖，适合高并发的网关层部署。

### 8. 不足与局限
*   **结果信号质量依赖**：实验使用的是人工标注的 ground-truth 作为结果信号，实际生产中获取高质量的“成功/失败”反馈可能存在噪声。
*   **基础模型天花板**：微调效果受限于原始嵌入模型（如 MiniLM）的表达能力，如果原始模型完全无法区分两个语义领域，微调的提升空间有限。
*   **动态性挑战**：如果工具的功能频繁更新，离线微调的频率和时效性将面临挑战。

（完）
