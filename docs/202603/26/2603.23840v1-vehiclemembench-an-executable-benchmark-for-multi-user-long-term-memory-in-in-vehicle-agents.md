---
title: "VehicleMemBench: An Executable Benchmark for Multi-User Long-Term Memory in In-Vehicle Agents"
title_zh: VehicleMemBench：车载智能体多用户长期记忆的可执行基准测试
authors: "Yuhao Chen, Yi Xu, Xinyun Ding, Xiang Fang, Shuochen Liu, Luxi Lin, Qingyu Zhang, Ya Li, Quan Liu, Tong Xu"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.23840v1"
tags: ["query:agent"]
score: 9.0
evidence: 车载智能体的多用户长期记忆基准测试
tldr: 随着车载智能体向长期伴侣演进，多用户偏好建模和动态习惯适应变得至关重要。本文提出VehicleMemBench，这是一个基于可执行仿真环境的多用户长短期记忆基准。它包含23个工具模块，每条样本涵盖80多个历史事件，通过对比动作后的环境状态与目标状态进行客观评估。该基准填补了现有单用户、静态问答评估的空白，为车载智能体的长期自适应决策提供了评估工具。
selection_source: fresh_fetch
motivation: 现有基准多局限于单用户和静态问答，无法捕捉车载环境下多用户偏好的冲突及随时间演变的动态特性。
method: 构建了一个包含23个工具模块的可执行仿真基准，通过长达80个历史事件的上下文评估智能体的工具调用和记忆能力。
result: 实验显示，即便顶尖模型在处理动态偏好演变和领域特定记忆需求时仍面临巨大挑战。
conclusion: 车载系统需要更强大且专门化的记忆管理机制，以支持真实场景下的长期自适应决策。
---

## 摘要
随着对智能车载体验需求的日益增长，车载智能体正从简单的助手演变为长期伴侣。这种演变要求智能体能够持续建模多用户偏好，并在面对用户间偏好冲突和随时间变化的习惯时做出可靠决策。然而，现有的基准测试很大程度上局限于单用户、静态的问答设置，未能捕捉到偏好的时间演变以及真实车载环境中多用户、工具交互的特性。为了填补这一空白，我们推出了 VehicleMemBench，这是一个基于可执行车载模拟环境构建的多用户长上下文记忆基准测试。该基准通过比较动作后的环境状态与预定义的评估目标状态来评估工具使用和记忆能力，从而实现无需基于大语言模型（LLM）或人工评分的客观且可重复的评估。VehicleMemBench 包含 23 个工具模块，每个样本包含超过 80 个历史记忆事件。实验表明，强大的模型在直接指令任务中表现良好，但在涉及记忆演变的场景中（特别是当用户偏好动态变化时）表现挣扎。即使是先进的记忆系统也难以处理该环境中的特定领域记忆需求。这些发现强调了需要更稳健且专门的记忆管理机制，以支持现实世界车载系统中的长期自适应决策。为了促进未来的研究，我们发布了数据和代码。

## Abstract
With the growing demand for intelligent in-vehicle experiences, vehicle-based agents are evolving from simple assistants to long-term companions. This evolution requires agents to continuously model multi-user preferences and make reliable decisions in the face of inter-user preference conflicts and changing habits over time. However, existing benchmarks are largely limited to single-user, static question-answer settings, failing to capture the temporal evolution of preferences and the multi-user, tool-interactive nature of real vehicle environments. To address this gap, we introduce VehicleMemBench, a multi-user long-context memory benchmark built on an executable in-vehicle simulation environment. The benchmark evaluates tool use and memory by comparing the post-action environment state with a predefined target state, enabling objective and reproducible evaluation without LLM-based or human scoring. VehicleMemBench includes 23 tool modules, and each sample contains over 80 historical memory events. Experiments show that powerful models perform well on direct instruction tasks but struggle in scenarios involving memory evolution, particularly when user preferences change dynamically. Even advanced memory systems struggle to handle domain-specific memory requirements in this environment. These findings highlight the need for more robust and specialized memory management mechanisms to support long-term adaptive decision-making in real-world in-vehicle systems. To facilitate future research, we release the data and code.

---

## 论文详细总结（自动生成）

以下是对论文《VehicleMemBench: An Executable Benchmark for Multi-User Long-Term Memory in In-Vehicle Agents》的深度结构化总结：

### 1. 核心问题与整体含义
随着车载智能体（In-Vehicle Agents）从简单的语音助手向“长期伴侣”演进，它们需要具备处理**多用户复杂偏好**、**长期记忆**以及**动态习惯演变**的能力。
*   **研究动机**：现有的记忆基准测试多为单用户、静态问答（QA）形式，缺乏对多用户冲突、偏好随时间演变（如习惯改变、错误纠正）以及与车载工具（API）交互的真实模拟。
*   **核心目标**：建立一个可执行的、基于状态变化的基准测试，客观评估车载智能体在长上下文、多用户干扰及动态偏好下的决策能力。

### 2. 方法论：核心思想与技术细节
论文提出了 **VehicleMemBench**，其核心由“数据生成流水线”和“可执行仿真环境”两部分组成：
*   **数据生成流水线（5阶段）**：
    1.  **人格群体生成**：基于 Persona-Hub 生成包含 3 名用户的群体，涵盖基本信息、文化兴趣、生活习惯及车载偏好。
    2.  **事件链构建**：设计了 5 种挑战类型的事件链：偏好冲突、指代消解、条件约束、状态偏移、错误纠正。
    3.  **时间交织策略**：将不同用户的多个偏好线程按时间戳合并，模拟真实世界中交错发生的事件。
    4.  **对话生成**：利用 LLM 将结构化事件转化为自然的多轮对话，包含乘客间的互动和隐晦的偏好表达。
    5.  **QA 与目标状态生成**：生成需要检索记忆才能完成的查询，并预定义执行后的“目标环境状态”。
*   **可执行仿真环境**：
    *   基于 VehicleWorld 扩展，包含 **23 个设备模块**（如空调、座椅、导航）和 **111 个可执行 API**。
    *   **状态评估协议**：不依赖 LLM 评分，而是通过比较智能体操作后的“预测状态”与“参考状态”是否一致（Exact State Match, ESM）来衡量成功率。

### 3. 实验设计
*   **数据集规模**：包含 50 个场景，500 个查询，每个样本平均包含 80+ 个历史记忆事件，历史记录平均长度达 9.2 万个 Token。
*   **对比方法**：
    *   **模型层**：评估了 7 个家族的 12 种主流模型，包括 Gemini-3-Pro、GPT-5、Qwen3-Max、GLM-5、Kimi-K2.5、Doubao-Seed-1.6、MiniMax-M2.5 等。
    *   **记忆系统层**：对比了 5 种通用记忆系统（MemOS, Mem0, LightMem, Memobase, Supermemory）以及 2 种基准方法（递归摘要、KV 存储）。
*   **评估维度**：整体任务性能、工具使用能力（提供金标记忆）、记忆检索能力（Memory Score）。

### 4. 资源与算力
*   **算力说明**：论文**未提及**本地 GPU 训练时长或型号。
*   **实现方式**：所有评估均通过 **API 调用**完成（如 OpenAI、Google、各国产大模型接口）。
*   **成本分析**：论文提到评估过程具有较高的计算成本，单模型无记忆设置需 1000-1500 次 API 调用，引入记忆系统后因涉及记忆构建和更新，调用量常超过 5000 次。

### 5. 实验数量与充分性
*   **实验规模**：针对 12 种模型和 7 种记忆系统进行了系统性交叉评估，涵盖了 5 种不同的推理类型（冲突、约束等）。
*   **充分性**：实验设计较为充分。除了主实验外，还进行了“无记忆设置”的对照实验（证明了任务必须依赖记忆才能完成）以及针对错误类型的细粒度分析（Memory Error, Execution Error 等）。
*   **客观性**：采用基于环境状态的硬性指标（ESM、F1），排除了 LLM 裁判的主观偏差，评估过程具有高度可重复性。

### 6. 主要结论与发现
*   **记忆是瓶颈**：顶尖模型在拥有“金标记忆”时工具调用成功率极高（如 Gemini 达 90.6%），但在自主检索记忆时性能大幅下降（降至 64.8%），说明记忆管理比工具执行更难。
*   **通用记忆系统表现平平**：现有的通用记忆系统在车载特定领域表现甚至不如简单的“递归摘要”或“KV 存储”，说明通用机制难以处理高度动态且与状态绑定的车载偏好。
*   **挑战类型差异**：**“条件约束”**（如：只有下雨时才开启某种模式）是最难的推理类型，而“偏好冲突”相对容易处理。
*   **模型迭代收益递减**：同系列模型的版本升级（如 GLM-4.7 到 GLM-5）在长期记忆任务上的提升非常有限。

### 7. 优点与亮点
*   **可执行性**：通过 API 调用和状态比对进行评估，比传统的文本生成评估更严谨、更贴近工业应用。
*   **多用户视角**：填补了车载场景下多用户偏好冲突建模的空白。
*   **高质量数据**：采用了严格的人工校验流程（包括人格一致性、事件-对话对齐、答案正确性），确保了 Benchmark 的可靠性。
*   **动态性**：涵盖了偏好随时间演变的多种模式（偏移、纠正），反映了真实的人机交互特性。

### 8. 不足与局限
*   **模型覆盖**：虽然涵盖了主流模型，但受限于 API 成本，未能覆盖所有开源模型或更小参数规模的模型。
*   **场景复杂度**：目前的交互虽然长，但主要集中在功能控制，未来可扩展到更复杂的长程规划任务。
*   **实时性考量**：基准测试主要关注准确性，对车载环境极其重要的“推理延迟”和“响应速度”关注较少。

（完）
