---
title: "From AI Assistant to AI Scientist: Autonomous Discovery of LLM-RL Algorithms with LLM Agents"
title_zh: 从 AI 助手到 AI 科学家：利用 LLM 智能体自主发现 LLM-RL 算法
authors: "Sirui Xia, Yikai Zhang, Aili Chen, Siye Wu, Siyu Yuan, Yanghua Xiao"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.23951v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 在闭环框架中使用LLM智能体自主发现算法
tldr: 本研究针对大语言模型强化学习算法手动设计成本高、迭代难的问题，提出了POISE框架。该框架通过LLM智能体实现算法发现的闭环自动化，利用结构化归档管理提案、代码、评估及反思，支持证据驱动的迭代。在数学推理任务中，POISE从GRPO出发自动发现了分析方差缩放等改进机制，显著提升了模型性能，证明了自动化算法发现的可行性与可解释性。
selection_source: fresh_fetch
motivation: 传统的大语言模型策略优化算法设计依赖高成本的人工迭代，且难以有效复用历史经验。
method: 提出POISE闭环框架，通过维护包含提案、实现、评估和反思的结构化谱系档案，实现算法的自动化搜索与迭代。
result: "在数学推理实验中，POISE发现的算法将AIME25 pass@32准确率从26.7%提升至43.3%，并显著改善了整体性能指标。"
conclusion: 自动化智能体能够有效发现并优化复杂的强化学习算法机制，为AI驱动的科学发现提供了有力证据。
---

## 摘要
为语言模型发现改进的策略优化算法仍然是一个昂贵的人工过程，需要反复进行机制层面的修改和验证。与简单的组合代码搜索不同，该问题需要在与训练动态紧密耦合的算法机制空间中进行搜索，同时在迭代过程中复用经验证据。我们提出了 POISE，这是一个用于自动发现语言模型策略优化算法的闭环框架。POISE 维护一个结构化的、具有谱系关联的归档库，将提议、可执行实现、标准化评估和自然语言反思联系起来，以支持证据驱动的迭代。在以 GRPO 为起点的数学推理实验中，POISE 评估了 64 个候选算法，并发现了改进的机制，包括解析方差缩放（analytic-variance scaling）和有效性掩码（validity masking）。最佳变体将加权综合得分（weighted Overall）从 47.8 提升至 52.5（+4.6），并将 AIME25 pass@32 从 26.7% 提高到 43.3%，证明了自动策略优化发现的可行性，同时支持可解释的设计原则。

## Abstract
Discovering improved policy optimization algorithms for language models remains a costly manual process requiring repeated mechanism-level modification and validation. Unlike simple combinatorial code search, this problem requires searching over algorithmic mechanisms tightly coupled with training dynamics while reusing empirical evidence across iterations. We propose POISE, a closed-loop framework for automated discovery of policy optimization algorithms for language models. POISE maintains a structured, genealogically linked archive linking proposals, executable implementations, standardized evaluations, and natural-language reflections to support evidence-driven iteration. In mathematical reasoning experiments starting from GRPO, POISE evaluates 64 candidate algorithms and discovers improved mechanisms, including analytic-variance scaling and validity masking. The best variant improves weighted Overall from 47.8 to 52.5 (+4.6) and increases AIME25 pass@32 from 26.7% to 43.3%, demonstrating the feasibility of automated policy optimization discovery while supporting interpretable design principles.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **POISE**（Policy Optimization through Iterative Search and Evidence）的闭环框架，旨在利用大语言模型（LLM）智能体自主发现并优化用于 LLM 训练的强化学习（RL）算法。

以下是对该论文的深度结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：当前 LLM 的策略优化算法（如 PPO、GRPO）设计高度依赖人工迭代。研究人员需要反复手动修改损失函数、优势估计和正则化机制，并进行高成本的验证。
*   **研究动机**：算法机制与训练动态紧密耦合，微小的设计差异（如剪切范围、归一化方式）会导致性能剧变。现有的自动化研究多集中在简单代码搜索，缺乏对实验证据的系统性复用和深度反思。
*   **整体含义**：论文试图将 LLM 从“编程助手”提升为“AI 科学家”，通过闭环系统实现算法提案、实现、评估及反思的自动化循环。

### 2. 方法论：POISE 框架
*   **核心思想**：将算法发现建模为在算法空间中的**认识论进化搜索（Epistemic Evolutionary Search）**。系统维护一个“谱系关联归档库”，记录每个算法的提案、代码、训练轨迹、评估指标和自然语言反思。
*   **关键技术细节**：
    *   **阶段 I：提案生成**。利用高斯过程（GP）作为采集函数，平衡“当前表现强”和“具有进化潜力”的父节点，结合历史证据和文献先验生成新提案。
    *   **阶段 II：实现与验证**。在统一的分布式训练接口（基于 verL 框架）下自动生成代码，并通过验证循环确保实现逻辑与设计意图一致。
    *   **阶段 III：反思分析与归档**。对比父子算法的训练动态（如熵值、奖励曲线），由 LLM 智能体生成机制层面的诊断报告，将定量结果转化为可复用的科学知识。

### 3. 实验设计
*   **任务场景**：LLM 的数学推理能力优化。
*   **基准（Benchmark）**：以 **GRPO**（Group Relative Policy Optimization）作为初始基准。
*   **数据集**：
    *   **训练集**：MATH 数据集（Levels 3-5）中抽取的 5000 个样本。
    *   **评估集**：AIME24、AIME25、AMC、MATH-500、Minerva、OlympiadBench。
*   **对比方法**：系统自主生成的 63 个进化变体，以及针对“长度压缩”约束的特定分支实验。

### 4. 资源与算力
*   **硬件资源**：使用了 **8 张 80GB A100 GPU**。
*   **模型配置**：
    *   **Agent 引擎**：Gemini-3-pro-preview（负责推理、代码生成和反思）。
    *   **策略模型（Policy Model）**：Qwen2.5-Math-1.5B。
*   **训练细节**：每个算法运行 8 个 Epoch，Global Batch Size 为 256，Group Size 为 8。

### 5. 实验数量与充分性
*   **实验规模**：共评估了 **64 个候选算法**（1 个基准 + 63 个变体）。
*   **充分性**：实验不仅关注最终分数，还深入分析了训练轨迹（熵、奖励、响应长度）。
*   **客观性**：所有算法在完全相同的硬件、后端框架、训练预算和评估协议下运行，确保了对比的公平性。此外，还设置了“长度压缩”约束实验，验证了系统在多目标优化下的可控性。

### 6. 主要结论与发现
*   **性能显著提升**：发现的最佳变体 **VM-AV-GRPO** 将综合加权得分从 47.8 提升至 52.5。在极难的 AIME25 任务上，pass@32 从 26.7% 飙升至 **43.3%**。
*   **新机制的发现**：
    *   **解析方差缩放（Analytic-Variance Scaling）**：用理论方差代替经验方差，增强稀疏成功信号的放大效果。
    *   **有效性掩码（Validity Masking）**：通过掩码机制确保“格式正确但答案错误”的样本不会获得错误的奖励激励。
*   **设计原则提取**：总结出“信号解耦”、“条件归一化”和“正确性优先的效率塑形”等可解释的算法设计准则。

### 7. 优点与亮点
*   **闭环证据链**：不同于盲目搜索，POISE 强调“反思”，能从失败的实验中提取教训（如发现固定惩罚项会导致格式不稳定），并指导后续迭代。
*   **谱系化管理**：通过谱系树追踪算法演化，使得研究过程具有极高的透明度和可追溯性。
*   **自然语言引导**：证明了可以通过自然语言指令（如要求压缩长度）有效引导进化方向，展现了极强的交互性。

### 8. 不足与局限
*   **计算成本**：尽管使用了小模型（1.5B）进行迭代，但 64 次完整的强化学习训练仍消耗了巨大的计算资源。
*   **任务覆盖面**：目前仅局限于数学推理任务，尚未在通用对话、代码生成或多模态任务上验证其普适性。
*   **因果识别风险**：虽然 LLM 提供了反思报告，但这些解释仍属于“基于证据的推测”，而非严格受控的因果分析，可能存在归因偏差。

（完）
