---
title: "ALARA for Agents: Least-Privilege Context Engineering Through Portable Composable Multi-Agent Teams"
title_zh: 智能体的 ALARA 原则：通过可移植、可组合的多智能体团队实现最小特权上下文工程
authors: "Christopher J. Agostino, Nayan D'Souza"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20380v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 管理多智能体团队与上下文工程
tldr: 针对多智能体系统配置碎片化、难以协作和权限管理混乱的问题，本文借鉴辐射安全中的ALARA原则，提出了一种声明式的上下文-智能体-工具（CAT）数据层。通过npcsh命令行工具，实现了对智能体权限和上下文的最小化、结构化管理。该方法确保了智能体行为的确定性，并通过对22个本地模型的广泛评估，验证了其在文件操作、网页搜索和多智能体协作等任务中的有效性。
selection_source: fresh_fetch
motivation: 现有多智能体框架在行为规范和权限管理上过于碎片化，导致智能体基础设施难以共享、版本化和协作维护。
method: 提出CAT数据层和npcsh工具，通过相互关联的声明式文件严格限制智能体的工具访问和上下文范围，实现最小特权原则。
result: 在22个本地模型和115项实际任务上的2500次执行测试中，系统成功刻画了不同规模模型在复杂任务链和多智能体委派中的表现。
conclusion: 结构化的上下文工程和最小特权原则能显著提升多智能体系统的可控性、可移植性及协作效率。
---

## 摘要
工业界从业者和学术界研究人员经常使用多智能体系统来加速工作，然而，这些系统运行所依赖的框架并未提供一种简单、统一的机制来可扩展地管理智能体控制架构（harness）的关键方面，这既影响了单个人机交互的质量，也影响了从业者通过共享智能体基础设施协作达成共同目标的能力。虽然智能体框架已经实现了日益复杂的多智能体系统，但定义这些智能体行为的行为规范仍然分散在散文式指令文件、框架内部配置以及像 MCP 服务器这样独立于单个智能体定义的机制中，使得这些规范难以在团队和项目之间共享、进行版本控制或协同维护。我们将辐射安全中的 ALARA 原则（暴露量保持在合理可达到的最低水平）应用于智能体上下文，引入了一个声明式的上下文-智能体-工具（CAT）数据层，该层通过相互关联的文件表示，将每个智能体的工具访问权限和上下文限制在其角色所需的最小值，并开发了用于执行该系统的命令行 shell \texttt{npcsh}。由于系统以结构化方式解析并强制执行这些文件，修改智能体的工具列表会产生有保证的行为变化，而非模型可能遵循也可能不遵循的建议。我们在涵盖文件操作、网络搜索、多步脚本编写、工具链调用和多智能体委派的 115 个实际任务中，评估了 22 个参数量从 6 亿到 350 亿的本地托管模型，通过约 2500 次总执行量，刻画了哪些模型系列在哪些任务类别中取得成功，以及它们在何处失效。

## Abstract
Industry practitioners and academic researchers regularly use multi-agent systems to accelerate their work, yet the frameworks through which these systems operate do not provide a simple, unified mechanism for scalably managing the critical aspects of the agent harness, impacting both the quality of individual human-agent interactions and the capacity for practitioners to coordinate toward common goals through shared agent infrastructure. Agent frameworks have enabled increasingly sophisticated multi-agent systems, but the behavioral specifications that define what these agents can do remain fragmented across prose instruction files, framework-internal configuration, and mechanisms like MCP servers that operate separately from individual agent definitions, making these specifications difficult to share, version, or collaboratively maintain across teams and projects. Applying the ALARA principle from radiation safety (exposures kept as low as reasonably achievable) to agent context, we introduce a declarative context-agent-tool (CAT) data layer expressed through interrelated files that scope each agent's tool access and context to the minimum its role requires, and \texttt{npcsh}, a command-line shell for executing it. Because the system parses and enforces these files structurally, modifying an agent's tool list produces a guaranteed behavioral change rather than a suggestion the model may or may not follow. We evaluate 22 locally-hosted models from 0.6B to 35B parameters across 115 practical tasks spanning file operations, web search, multi-step scripting, tool chaining, and multi-agent delegation, characterizing which model families succeed at which task categories and where they break down across $\sim$2500 total executions.

---

## 论文详细总结（自动生成）

以下是对论文《ALARA for Agents: Least-Privilege Context Engineering Through Portable Composable Multi-Agent Teams》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前多智能体系统（MAS）的配置高度碎片化，行为规范散落在自然语言指令（如 `.cursorrules`）、框架代码（如 LangGraph）和工具协议（如 MCP）中。这种碎片化导致了三个核心问题：
    *   **可靠性下降**：长上下文导致模型忽略指令（“迷失在中间”）。
    *   **安全风险**：过度授权（Over-privilege）使得智能体容易受到提示注入攻击。
    *   **协作困难**：缺乏统一、可版本化、可移植的声明式标准。
*   **核心思想**：借鉴核辐射防护中的 **ALARA 原则**（As Low As Reasonably Achievable，保持在合理可达到的最低水平），提出将智能体的工具访问和上下文范围限制在完成任务所需的“最小特权”范围内。

### 2. 方法论
论文引入了声明式的 **CAT（Context-Agent-Tool）数据层**，通过 `npcsh` 命令行工具执行：
*   **Context（上下文文件）**：在团队层面定义，指定编排者（Orchestrator）和子团队层级，实现上下文隔离。
*   **NPC（智能体定义）**：通过 YAML 文件定义单个智能体，包括模型配置和 **Jinx 列表**（即该智能体的工具权限集）。
*   **Jinxes（Jinja 执行模板）**：
    *   **核心技术**：将工具定义为 YAML 模板，包含输入类型、描述和执行步骤（支持 Python、Bash、LLM 或嵌套其他 Jinx）。
    *   **结构化强制执行**：工具权限不是通过提示词“建议”给模型的，而是直接从其 API Schema 中增删。不在列表中的工具对模型不可见，从根本上杜绝了过度授权。
    *   **组合性**：Jinxes 形成有向无环图（DAG），允许复杂工作流的模块化构建。

### 3. 实验设计
*   **数据集/场景**：设计了一个包含 **115 个实际任务** 的基准测试，涵盖 13 个类别（文件操作、网页搜索、多步脚本、工具链调用、多智能体委派等）。
*   **对比对象**：评估了 **22 个本地托管模型**，涵盖 9 个模型系列（Qwen 3/3.5, GLM, Gemma 3, Mistral, Llama 3.1/3.2, Phi, OLMo2），参数量从 0.6B 到 35B。
*   **验证机制**：每个任务配有自然语言指令和自动化的文件系统状态检查命令，确保评估的客观性。

### 4. 资源与算力
*   **算力说明**：实验在“消费级硬件”上运行，使用 **Ollama** 作为推理后端。
*   **具体参数**：每个任务设置 360 秒超时，最多允许 5 次重试（带错误反馈）。
*   **缺失信息**：论文未明确给出具体的 GPU 型号（如 RTX 4090 等）或总训练/推理时长。

### 5. 实验数量与充分性
*   **实验规模**：共进行了约 **2,530 次任务执行**。
*   **充分性**：实验覆盖了从极小规模（0.6B）到中等规模（35B）的广泛模型，并进行了跨家族对比、参数规模缩放分析以及任务类别的消融分析。
*   **客观性**：通过 trace 级别的数据分析（尝试次数、工具调用量、任务时长）来刻画模型行为，而非仅看最终得分，实验设计较为公正。

### 6. 主要结论与发现
*   **家族差异胜过规模**：模型家族间的性能差异大于同一家族内的参数缩放效应。例如，某些针对工具优化的 4B 模型表现优于未优化的 27B 模型。
*   **能力相关性**：智能体性能与 MMLU 得分强相关（r≈0.8），但工具调用量（r≈0.7）比任务时长或重试次数更能预测成功。
*   **重试的边际效应**：约 80% 的成功发生在第一次尝试。重试在网页搜索中增益最大（+21%），但在多智能体委派中几乎无效（+3%），甚至可能因上下文累积导致性能下降。
*   **委派难题**：多智能体委派是所有模型表现最差的类别，反映了当前本地模型在复杂角色协调上的局限。

### 7. 优点
*   **安全性与确定性**：通过结构化 Schema 强制执行最小特权，比单纯依靠提示词约束更安全、更可靠。
*   **高可移植性**：采用类似“基础设施即代码（IaC）”的声明式文件，使得智能体团队可以在不同环境间轻松迁移。
*   **人机协同设计**：Jinx 既是智能体的工具，也是人类的斜杠命令，实现了统一的交互界面。

### 8. 不足与局限
*   **模型范围限制**：主要关注 35B 以下的本地模型，虽然提到闭源大模型（如 GPT-4o）得分超过 90%，但缺乏详细的对比数据。
*   **任务复杂度上限**：虽然涵盖了 115 个任务，但对于极大规模的企业级复杂工作流，声明式文件的维护成本和编排压力尚待验证。
*   **硬件细节模糊**：缺乏具体的推理延迟和显存占用数据，对于希望复现性能的开发者参考价值有限。

（完）
