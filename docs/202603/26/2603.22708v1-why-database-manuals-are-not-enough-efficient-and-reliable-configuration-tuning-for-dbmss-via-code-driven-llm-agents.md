---
title: "Why Database Manuals Are Not Enough: Efficient and Reliable Configuration Tuning for DBMSs via Code-Driven LLM Agents"
title_zh: 为什么数据库手册还不够：通过代码驱动的 LLM 智能体实现高效可靠的 DBMS 配置调优
authors: "Xinyi Zhang, Tiantian Chen, Zhentao Han, Zhaoyan Hong, Wei Lu, Sheng Wang, Mo Sha, Anni Wang, Shuang Liu, Yakun Zhang, Feifei Li, Xiaoyong Du"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22708v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 用于自主数据库配置调优的代码驱动 LLM 智能体
tldr: 针对现代数据库参数调优中数据驱动方法开销大、文档驱动方法不精确的问题，本文提出SysInsight。该系统通过静态代码分析与大语言模型（LLM）结合，直接从DBMS源代码中挖掘细粒度的调优知识，并将其转化为可验证的定量规则。实验表明，SysInsight在调优速度和性能提升上均显著优于现有SOTA方法，实现了更高效、可靠的自动化调优。
selection_source: fresh_fetch
motivation: 现有调优方法依赖高开销的数据采集或过于笼统的文档手册，忽视了源代码中蕴含的精确控制逻辑。
method: 结合静态代码分析与LLM推理从源码中提取调优见解，并通过关联规则挖掘将其转化为定量规则以引导参数调整。
result: "与最先进的基准方法相比，SysInsight的收敛速度平均提高了7.11倍，且性能提升了19.9%。"
conclusion: 证明了从源代码中挖掘底层逻辑是实现高效、可靠数据库自动调优的有效途径。
---

## 摘要
现代数据库管理系统 (DBMS) 开放了数百个配置参数（knobs），这些参数对性能有着至关重要的影响。现有的自动调优方法要么采用数据驱动范式，这会产生巨大的开销；要么依赖于从数据库文档中提取的人工驱动启发式规则，而这些规则通常具有局限性且过于通用。考虑到配置参数的控制逻辑本质上编码在 DBMS 源代码中，我们认为可以直接从代码中挖掘出极具潜力的调优策略，从而揭示基于系统内部机制的细粒度见解。为此，我们提出了 SysInsight，这是一个代码驱动的数据库调优系统，它能自动从 DBMS 源代码中提取细粒度的调优知识，以加速并稳定调优过程。SysInsight 将静态代码分析与基于 LLM 的推理相结合，以识别受参数控制的执行路径并提取语义调优见解。随后，通过基于调优观测结果的关联规则挖掘，这些见解被转化为定量且可验证的调优规则。在在线调优过程中，系统诊断被用于识别关键参数，并在规则指导下进行调整。评估结果表明，与 SOTA 基准相比，SysInsight 收敛到最佳配置的速度平均快 7.11 倍，同时实现了 19.9% 的性能提升。

## Abstract
Modern database management systems (DBMSs) expose hundreds of configuration knobs that critically influence performance. Existing automated tuning methods either adopt a data-driven paradigm, which incurs substantial overhead, or rely on manual-driven heuristics extracted from database documentation, which are often limited and overly generic. Motivated by the fact that the control logic of configuration knobs is inherently encoded in the DBMS source code, we argue that promising tuning strategies can be mined directly from the code, uncovering fine-grained insights grounded in system internals. To this end, we propose SysInsight, a code-driven database tuning system that automatically extracts fine-grained tuning knowledge from DBMS source code to accelerate and stabilize the tuning process. SysInsight combines static code analysis with LLM-based reasoning to identify knob-controlled execution paths and extract semantic tuning insights. These insights are then transformed into quantitative and verifiable tuning rules via association rule mining grounded in tuning observations. During online tuning, system diagnosis is applied to identify critical knobs, which are adjusted under the rule guidance. Evaluations demonstrate that compared to the SOTA baseline, SysInsight converges to the best configuration on average 7.11X faster while achieving a 19.9% performance improvement.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **SysInsight** 的系统，旨在通过结合大语言模型（LLM）和源代码分析，解决数据库管理系统（DBMS）配置参数（Knobs）自动调优中的效率与可靠性问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现代 DBMS 拥有数百个影响性能的配置参数，寻找最优配置是一个 NP-hard 问题。
*   **研究动机**：
    *   **数据驱动方法（如 RL、BO）**：依赖大量迭代实验，采样开销巨大，且缺乏对系统内部逻辑的理解。
    *   **手册驱动方法（如 GPTuner）**：依赖数据库文档。然而，手册内容往往过于笼统（如“建议设为内存的 70%”），且覆盖率低（许多参数在手册中并无详细说明）。
*   **核心假设**：配置参数的控制逻辑本质上编码在 DBMS 的**源代码**中。通过分析代码，可以提取出比手册更精确、更具细粒度的调优策略。

### 2. 方法论：SysInsight 系统架构
SysInsight 采用“代码驱动”的范式，分为离线知识提取和在线调优两个阶段：

*   **离线阶段：知识挖掘与规则生成**
    1.  **函数-参数映射（Static Analysis）**：利用 LLVM IR 进行静态污点分析，追踪参数如何通过数据流和控制流影响具体的系统函数（如 `log_buffer_flush_to_disk`）。
    2.  **代码驱动假设生成（LLM Agents）**：
        *   **检索智能体**：通过 AST 搜索获取受控函数及其上下文代码。
        *   **总结智能体**：利用 LLM 推理参数对执行路径的影响，生成语义化的“调优假设”（例如：若某函数 CPU 占用高，则应减小某参数）。
    3.  **调优规则挖掘（ARM）**：通过实验收集观测数据，利用改进的**关联规则挖掘（Association Rule Mining, FP-Growth）**，将模糊的语义假设转化为定量的规则（例如：若函数 A 占比 > 1%，则将参数 X 减小 10%）。
*   **在线阶段：诊断与调优**
    1.  **基于诊断的参数选择**：利用 `perf` 采样和 **SHAP 值分析**识别性能瓶颈函数，并根据映射关系锁定需要调整的关键参数。
    2.  **规则增强的配置推荐**：检索匹配当前运行上下文的规则，结合 LLM 生成最终的配置建议。

### 3. 实验设计
*   **数据集/场景**：
    *   **TPC-C**（OLTP 场景，模拟交易处理）。
    *   **TPC-H**（OLAP 场景，复杂查询分析，Scale Factor 1, 10, 50）。
    *   **SYSBENCH**（通用读写负载）。
*   **目标系统**：MySQL 8.0.36（技术报告中补充了 PostgreSQL 的实验）。
*   **对比方法（Baselines）**：
    *   **手册驱动**：GPTuner, DB-Bert。
    *   **数据驱动**：SMAC, DDPG++, ResTune, OtterTune。
*   **评估指标**：收敛速度、性能提升幅度、可靠性（错误配置的数量）。

### 4. 资源与算力
*   **LLM 模型**：默认使用 **GPT-4o-mini** 作为后端（兼顾成本与性能），同时也对比了 GPT-4o 和 GPT-3.5-turbo。
*   **离线开销**：
    *   函数-参数提取耗时约 **45 小时**。
    *   LLM 生成假设耗时约 **4 小时**（消耗约 1.5M input tokens，成本约 $5）。
*   **在线开销**：生成单次推荐平均耗时 **26 秒**（其中 LLM 推理占绝大部分时间）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三种主流 Benchmark，并在 TPC-H 上测试了不同数据规模（SF 1 到 50）的扩展性。
*   **消融实验**：分别验证了“调优假设”和“定量规则”的贡献，以及不同参数选择方法（代码驱动 vs LLM 驱动 vs ML 驱动）的效果。
*   **客观性**：实验采用了多次运行取中位数及四分位距的方式，并严格隔离了目标负载的训练数据，确保了跨负载迁移的公平性。

### 6. 主要结论与发现
*   **性能卓越**：SysInsight 的收敛速度比 SOTA（GPTuner）快 **7.11 倍**，最终性能平均提升 **19.9%**。
*   **高可靠性**：在调优过程中，SysInsight 推荐的“坏配置”（导致性能下降的配置）显著少于其他方法，证明了规则引导的安全性。
*   **代码胜过手册**：实验证明，从代码中提取的知识比从手册中提取的知识更细致、更贴合实际运行状态。

### 7. 优点与亮点
*   **白盒化调优**：首次实现了自动化的代码驱动调优，将调优逻辑与系统内部实现（函数调用、控制流）直接挂钩，具有极强的可解释性。
*   **知识闭环**：通过关联规则挖掘，将 LLM 的感性推理（假设）转化为理性的定量指导（规则），有效缓解了 LLM 的幻觉问题。
*   **诊断精准**：引入 SHAP 值进行瓶颈函数定位，使参数选择不再是“盲目搜索”，而是“对症下药”。

### 8. 不足与局限
*   **静态分析局限**：对于高度动态或通过复杂指针传递的控制逻辑，静态分析可能存在漏报或误报。
*   **版本依赖性**：DBMS 源码版本更新后，可能需要重新运行离线提取流程（虽然文中提到该过程是自动化的）。
*   **LLM 依赖**：系统的核心推理仍依赖于商业 LLM 的 API，对于完全离线或私有化部署的环境，需要高性能的本地大模型支持。
*   **实验覆盖**：虽然测试了 MySQL 和 PostgreSQL，但对于分布式数据库或非关系型数据库的适用性尚待验证。

（完）
