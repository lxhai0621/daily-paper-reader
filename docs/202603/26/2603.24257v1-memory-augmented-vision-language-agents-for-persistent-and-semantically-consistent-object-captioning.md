---
title: Memory-Augmented Vision-Language Agents for Persistent and Semantically Consistent Object Captioning
title_zh: 记忆增强型视觉语言智能体：实现持久且语义一致的物体描述
authors: "Tommaso Galliena, Stefano Rosa, Tommaso Apicella, Pietro Morerio, Alessio Del Bue, Lorenzo Natale"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24257v1"
tags: ["query:agent"]
score: 9.0
evidence: 用于持久语义表示的记忆增强视觉语言智能体
tldr: 针对视觉语言模型在不同视角下对同一物体描述不一致的问题，本文提出了一种增强记忆的视觉语言智能体。该模型将数据关联、物体描述和探索策略集成在统一的自回归框架中，通过物体级情节记忆确保跨序列的语义一致性。在光影真实的3D环境中，利用自监督学习和伪标签技术进行训练，显著提升了物体描述的准确性和一致性。
selection_source: fresh_fetch
motivation: 现有的视觉语言模型在多视角下对同一物体的描述缺乏一致性，且现有方法多为离线或多阶段流水线，难以实现实时的语义推理。
method: 提出一种统一的自回归框架，结合当前RGB观测、俯视探索地图和序列化的物体级情节记忆，实现数据关联、描述生成与探索策略的同步处理。
result: "在手动标注的测试集上，该模型在标准描述指标上提升了11.86%，在描述自相似性上提升了7.39%，并实现了可扩展的紧凑场景表示。"
conclusion: 该研究证明了通过集成物体级记忆和统一框架，可以有效解决具身智能体在复杂环境中的语义一致性问题。
---

## 摘要
视觉语言模型（VLM）在不同视角下对同一物体的描述往往不一致，这阻碍了具身智能体随时间构建一致语义表示的能力。以往的方法通过离线多视图聚合或多阶段流水线来解决不一致性，这些流水线将探索、数据关联和描述学习解耦，对先前观察到的物体的推理能力有限。在本文中，我们介绍了一种统一的记忆增强型视觉语言智能体，它在单个自回归框架内同时处理数据关联、物体描述和探索策略。该模型处理当前的 RGB 观测、自顶向下的探索地图以及序列化为物体级标记（tokens）的物体级情景记忆，从而确保在长序列中保持持久的物体身份和语义一致性。为了以自监督方式训练模型，我们利用基于分歧的策略和伪描述模型在逼真的 3D 环境中收集了一个数据集，该模型强制要求多视图描述历史之间的一致性。在手动标注的物体级测试集上的广泛评估表明，与基准模型相比，标准描述评分提高了高达 11.86%，描述自相似性提高了 7.39%，同时通过紧凑的场景表示实现了可扩展的性能。代码、模型权重和数据可在 https://github.com/hsp-iit/epos-vlm 获取。

## Abstract
Vision-Language Models (VLMs) often yield inconsistent descriptions of the same object across viewpoints, hindering the ability of embodied agents to construct consistent semantic representations over time. Previous methods resolved inconsistencies using offline multi-view aggregation or multi-stage pipelines that decouple exploration, data association, and caption learning, with limited capacity to reason over previously observed objects. In this paper, we introduce a unified, memory-augmented Vision-Language agent that simultaneously handles data association, object captioning, and exploration policy within a single autoregressive framework. The model processes the current RGB observation, a top-down explored map, and an object-level episodic memory serialized into object-level tokens, ensuring persistent object identity and semantic consistency across extended sequences. To train the model in a self-supervised manner, we collect a dataset in photorealistic 3D environments using a disagreement-based policy and a pseudo-captioning model that enforces consistency across multi-view caption histories. Extensive evaluation on a manually annotated object-level test set, demonstrate improvements of up to +11.86% in standard captioning scores and +7.39% in caption self-similarity over baseline models, while enabling scalable performance through a compact scene representation. Code, model weights, and data are available at https://github.com/hsp-iit/epos-vlm

---

## 论文详细总结（自动生成）

### 论文总结：EPOS-VLM 记忆增强型视觉语言智能体

#### 1. 核心问题与整体含义（研究动机和背景）
在具身智能（Embodied AI）领域，视觉语言模型（VLM）面临一个关键挑战：**语义不一致性**。当智能体在 3D 环境中移动时，由于视角、距离或遮挡的变化，现有的 VLM 往往会对同一个物理物体给出截然不同甚至矛盾的描述（例如，从正面看是“沙发”，从侧面看变成“扶手椅”）。
*   **研究动机**：人类通过动态探索和记忆构建稳定的物体表征，而现有 VLM 多基于静态图像训练，缺乏物体持久性（Object Permanence）和跨时间整合信息的能力。
*   **背景**：以往方法多采用离线聚合或多阶段流水线（将探索、关联、描述解耦），导致智能体无法根据当前的语义不确定性主动调整观察视角。

#### 2. 方法论：核心思想与技术细节
论文提出了 **EPOS-VLM**（Embodied Persistent Object Semantics），这是一个统一的、记忆增强的视觉语言动作模型。
*   **核心思想**：将数据关联、物体描述和导航动作集成在单个自回归 Transformer 框架中，利用“物体级情节记忆”作为上下文。
*   **关键技术细节**：
    *   **结构化情节记忆**：记忆存储了物体的持久 ID、3D 位置及历史描述频率。这些信息被序列化为特定的文本 Token（如 `[OBJ-ID]`, `[POS]`, `[CAP-HISTORY]`），直接注入 VLM 的语言上下文。
    *   **统一输出序列**：模型在每个时间步输出一个结构化序列：`[MATCH]`（将当前检测到的物体关联到记忆 ID 或标记为新物体）、`[CAPTION]`（生成一致的描述）、`[ACTION]`（预测下一步动作）。
    *   **3D-CPS 伪描述生成器**：为了实现自监督训练，提出了一种 3D 感知的一致性伪描述方法。它通过聚合多视角观察，利用大模型（如 Qwen3-VL-30B）过滤掉视角相关的噪声，生成物体固有的属性描述作为训练标签。
    *   **主动探索策略**：模型学习根据描述的“分歧度”来选择动作，倾向于重新访问那些语义描述不稳定的物体，以消除歧义。

#### 3. 实验设计
*   **数据集与场景**：在 **Habitat** 仿真平台中进行，使用了 **HM3D**（128 个训练场景，28 个测试场景）和 **Gibson**（32 个场景，用于跨域泛化测试）。
*   **Benchmark**：建立了一个手动标注的物体级描述测试集，包含准确的物体属性标签。
*   **对比方法**：
    *   **基础 VLM**：Qwen3-VL-2B, BLIP-2, InternVL-3B, CoCa, Florence。
    *   **多阶段/聚合方法**：IC3, ECO-score, LD-CPS。
    *   **消融实验**：对比了去掉记忆模块的变体（w/o memory）。
    *   **策略对比**：随机目标探索、前沿探索（Frontier Exploration）与 EPOS-VLM 学习到的策略。

#### 4. 资源与算力
*   **硬件**：使用了 **2 张 NVIDIA A100 GPU**。
*   **训练细节**：
    *   采用 **LoRA**（低秩自适应）进行高效微调。
    *   训练 3 个 Epoch，使用 AdamW 优化器，学习率为 $2 \times 10^{-4}$。
    *   Batch Size 为 16，梯度累积 4 步。
*   **推理效率**：模型在测试时保持约 0.7s/步的恒定推理时间，内存占用低于 10KB（远优于基于点云的 1GB 占用）。

#### 5. 实验数量与充分性
论文进行了多维度的广泛实验：
*   **伪标签质量评估**：证明了 3D-CPS 生成的标签优于现有聚合方法。
*   **描述准确性与一致性**：在 HM3D 和 Gibson 两个数据集上验证了性能提升。
*   **数据关联实验**：验证了模型在长序列中追踪物体身份的准确性。
*   **消融实验**：明确了情节记忆对提升语义一致性的核心贡献。
*   **效率实验**：对比了推理时间和内存随步数增加的变化曲线。
**评价**：实验设计非常充分，涵盖了从感知到动作的全链路评估，且跨数据集的泛化测试增强了结果的说服力。

#### 6. 主要结论与发现
*   **性能提升**：EPOS-VLM 在标准描述指标（如 SPICE）上比最强基准提升了 **11.86%**，在描述自相似性（一致性）上提升了 **7.39%**。
*   **记忆的重要性**：去掉记忆模块后，模型的一致性大幅下降，证明了持久化表示是具身推理的关键。
*   **动作与语义的耦合**：学习到的探索策略能主动减少语义歧义，比单纯的几何覆盖（前沿探索）更有效。
*   **高效性**：通过 Token 化记忆，模型在处理长序列时比传统的 3D 点云方法更节省算力和内存。

#### 7. 优点与亮点
*   **端到端统一性**：首次将数据关联（Tracking）、描述（Captioning）和动作（Action）整合进同一个自回归序列中，避免了多阶段误差累积。
*   **紧凑的记忆表示**：将复杂的 3D 空间和历史信息转化为 VLM 可理解的 Token，既保留了语义深度又实现了计算的高效性。
*   **自监督闭环**：通过 3D-CPS 解决了具身环境下缺乏大规模一致性描述标注的问题。

#### 8. 不足与局限
*   **依赖外部检测器**：模型仍需依赖预训练的实例分割模型（如 Mask2Former），若分割出错，后续的关联和描述也会受影响。
*   **环境假设**：实验主要在静态、光影真实的模拟环境中进行，尚未充分讨论动态场景（物体移动）或极端传感器噪声下的表现。
*   **实时性挑战**：虽然内存占用低，但 0.7s 的推理延迟对于某些需要极高实时响应的机器人任务可能仍显略慢。

（完）
