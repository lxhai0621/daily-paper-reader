---
title: "Code-MIE: A Code-style Model for Multimodal Information Extraction with Scene Graph and Entity Attribute Knowledge Enhancement"
title_zh: Code-MIE：一种基于场景图和实体属性知识增强的代码风格多模态信息抽取模型
authors: "Jiang Liu, Ge Qiu, Hao Fei, Dongdong Xie, Jinbo Li, Fei Li, Chong Teng, Donghong Ji"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20781v1"
tags: ["query:mmkqa"]
score: 9.0
evidence: 基于场景图的多模态信息抽取
tldr: 针对多模态信息提取（MIE）中自然语言模板与结构化任务不匹配的问题，本文提出Code-MIE框架。该框架将MIE任务统一建模为代码理解与生成，通过提取文本中的实体属性并结合图像生成的场景图来增强知识表示。输入采用Python函数形式，输出为结构化字典。实验证明，Code-MIE在M3D、Twitter等多个数据集上均达到了SOTA水平，显著提升了多模态信息的提取精度。
selection_source: fresh_fetch
motivation: 现有多模态信息提取方法多采用自然语言模板，难以有效处理结构化信息，且缺乏对视觉场景和实体属性的深度整合。
method: 提出Code-MIE框架，将MIE任务转化为Python代码生成，利用实体属性和图像场景图增强输入，并以字典形式输出提取结果。
result: 在M3D、Twitter-15/17及MNRE等多个中英文数据集上，Code-MIE均取得了领先的性能指标，超越了现有基准模型。
conclusion: 将多模态信息提取任务代码化并结合场景图与属性增强，是提升模型处理复杂结构化数据能力的有效途径。
---

## 摘要
随着大语言模型（LLMs）的快速发展，越来越多的研究者开始关注基于 LLMs 的信息抽取。然而，现有的相关方法仍有改进空间。首先，现有的多模态信息抽取（MIE）方法通常采用自然语言模板作为 LLMs 的输入和输出，这与主要包含实体和关系等结构化信息的信息抽取任务特征不匹配。其次，虽然少数方法采用了结构化且对 IE 更友好的代码风格模板，但它们仅在纯文本 IE 上探索了其方法，而非多模态 IE。此外，这些方法的设计较为复杂，需要为每个任务设计单独的模板。在本文中，我们提出了一种代码风格多模态信息抽取框架（Code-MIE），将 MIE 形式化为统一的代码理解与生成。Code-MIE 具有以下新颖设计：(1) 从文本中提取性别、所属机构等实体属性，以引导模型理解实体的上下文和角色。(2) 将图像转换为场景图和视觉特征，从而将丰富的视觉信息融入模型。(3) 输入模板被构建为一个 Python 函数，其中实体属性、场景图和原始文本构成函数参数。相比之下，输出模板被形式化为包含所有抽取结果（如实体、关系等）的 Python 字典。为了评估 Code-MIE，我们在 M$^3$D、Twitter-15、Twitter-17 和 MNRE 数据集上进行了广泛的实验。结果表明，与六个竞争基线模型相比，我们的方法实现了最先进的性能，在 M$^3$D 的英文和中文数据集上分别达到 61.03% 和 60.49%，在其他三个数据集上分别达到 76.04%、88.07% 和 73.94%。

## Abstract
With the rapid development of large language models (LLMs), more and more researchers have paid attention to information extraction based on LLMs. However, there are still some spaces to improve in the existing related methods. First, existing multimodal information extraction (MIE) methods usually employ natural language templates as the input and output of LLMs, which mismatch with the characteristics of information tasks that mostly include structured information such as entities and relations. Second, although a few methods have adopted structured and more IE-friendly code-style templates, they just explored their methods on text-only IE rather than multimodal IE. Moreover, their methods are more complex in design, requiring separate templates to be designed for each task. In this paper, we propose a Code-style Multimodal Information Extraction framework (Code-MIE) which formalizes MIE as unified code understanding and generation. Code-MIE has the following novel designs: (1) Entity attributes such as gender, affiliation are extracted from the text to guide the model to understand the context and role of entities. (2) Images are converted into scene graphs and visual features to incorporate rich visual information into the model. (3) The input template is constructed as a Python function, where entity attributes, scene graphs and raw text compose of the function parameters. In contrast, the output template is formalized as Python dictionaries containing all extraction results such as entities, relations, etc. To evaluate Code-MIE, we conducted extensive experiments on the M$^3$D, Twitter-15, Twitter-17, and MNRE datasets. The results show that our method achieves state-of-the-art performance compared to six competing baseline models, with 61.03\% and 60.49\% on the English and Chinese datasets of M$^3$D, and 76.04\%, 88.07\%, and 73.94\% on the other three datasets.

---

## 论文详细总结（自动生成）

### Code-MIE 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**多模态信息抽取（Multimodal Information Extraction, MIE）**任务。
*   **研究动机**：尽管大语言模型（LLM）在IE领域表现出色，但现有方法存在两个主要缺陷：
    1.  **模板不匹配**：大多数MIE方法使用自然语言模板，而IE任务本质上是结构化的（实体、关系等），自然语言描述容易导致模型产生“幻觉”或格式错误。
    2.  **多模态融合不足**：现有的代码风格（Code-style）IE研究主要集中在纯文本领域，且设计复杂，通常需要为每个子任务定制模板。
*   **核心目标**：提出一个统一的、任务无关（Task-agnostic）的代码风格框架，通过引入显式的知识增强（实体属性和场景图）来提升多模态结构化信息的抽取能力。

#### 2. 方法论：核心思想与关键技术
Code-MIE 将 MIE 任务形式化为**代码理解与生成**任务，主要包含以下四个关键步骤：
*   **实体属性生成（Entity Attribute Generation）**：利用 Qwen3-Max 提取文本中实体的细粒度属性（如职业、性别、国籍、所属机构等），为关系推理提供深层语义背景。
*   **场景图生成（Scene Graph Generation）**：利用 Qwen3-VL 将图像转化为显式的“主语-谓语-宾语”三元组，捕捉视觉对象及其空间/动作关系。
*   **视觉特征提取**：使用预训练的 ViT 提取图像的隐式特征，并通过线性投影层与文本特征对齐。
*   **代码风格模板构建**：
    *   **输入模板**：定义为一个 Python 函数 `information_extraction`，将原始文本、实体属性字典和场景图列表作为函数参数。
    *   **输出模板**：定义为 Python 字典格式，包含 `entity_dic`（实体）、`chain_dic`（实体链）、`relation_dic`（关系）和 `grounding_dic`（视觉定位坐标）。
*   **微调策略**：采用 **LoRA** 技术对底座模型（Qwen3-8B）进行高效微调，冻结 ViT，仅训练投影层和 LLM 的适配器参数。

#### 3. 实验设计
*   **数据集**：
    *   **M3D**：多模态、多语言（中英）、多任务（NER、实体链、RE、VG）的大规模数据集。
    *   **Twitter-15 & Twitter-17**：多模态命名实体识别（MNER）基准。
    *   **MNRE**：多模态关系抽取（MRE）基准。
*   **对比方法（Baselines）**：
    *   **自然语言模板类**：Qwen3-VL-8B, REAMO。
    *   **代码风格类**：CodeIE, KnowCoder, CodeKGC, Code4UIE（这些模型被适配到了多模态场景进行对比）。
*   **评价指标**：Precision, Recall, F1-score；针对不同任务使用 MUC/B3/CEAF（实体链）和 IoU（视觉定位）。

#### 4. 资源与算力
*   **硬件**：使用了一台 **NVIDIA A100-SXM4-80GB GPU**。
*   **软件/模型**：底座模型为 Qwen3-8B，视觉编码器为 vit-base。
*   **训练细节**：MNRE 数据集训练 3 个 Epoch，其余数据集 5 个 Epoch；学习率在 2e-4 到 8e-4 之间；Batch size 为 4。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个主流数据集（涵盖中英双语）上进行了测试，实验设计较为全面。
*   **消融实验**：分别验证了实体属性（EA）、场景图（SG）、视觉特征（VF）以及代码风格模板（对比自然语言模板）的有效性。
*   **分析深度**：包含了**幻觉错误率分析**、**案例研究**（Case Study）以及针对四个子任务的**错误类型分布分析**。
*   **客观性**：通过在相同底座模型上复现多种 SOTA 代码风格方法并进行多模态适配，确保了对比的公平性。

#### 6. 主要结论与发现
*   **SOTA 性能**：Code-MIE 在所有数据集上均显著优于基线模型，M3D 平均 F1 提升约 3%，MNRE 提升约 3.28%。
*   **代码风格的优越性**：实验证明代码风格模板比自然语言模板更适合结构化抽取，能显著降低格式错误和逻辑幻觉（尤其在长文本和复杂任务中）。
*   **知识增强的作用**：显式的场景图有助于识别视觉相关的实体关系，而实体属性则显著增强了需要深层推理的关系抽取任务。
*   **任务无关的优势**：单一模板即可处理多种 IE 任务，利用了任务间的关联性（如实体识别对关系抽取的支撑）。

#### 7. 优点（亮点）
*   **统一建模**：将复杂的多模态抽取任务简化为单一的代码生成问题，设计简洁且通用。
*   **显隐结合**：同时利用了 ViT 的隐式视觉特征和场景图的显式语义知识，弥补了纯特征融合的解释性不足。
*   **幻觉抑制**：通过代码语法的严格约束，有效缓解了 LLM 在处理结构化输出时的幻觉问题。
*   **多语言支持**：在中文和英文数据集上均表现稳健。

#### 8. 不足与局限
*   **级联误差风险**：实体属性和场景图依赖于外部高性能模型（如 Qwen3-Max/VL）预先生成，如果前端模型生成错误，会直接影响后续抽取的准确性。
*   **计算开销**：虽然使用了 LoRA，但生成场景图和属性的过程增加了预处理的计算成本和时间。
*   **边界识别难题**：错误分析显示，模型在实体边界识别和视觉定位框边界确定上仍存在挑战，这是生成式模型普遍面临的共性问题。
*   **实时性限制**：由于涉及多步生成和复杂的代码格式输出，该方法在对实时性要求极高的场景下可能受限。

（完）
