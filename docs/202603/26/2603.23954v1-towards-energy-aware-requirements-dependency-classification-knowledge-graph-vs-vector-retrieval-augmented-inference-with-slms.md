---
title: "Towards Energy-aware Requirements Dependency Classification: Knowledge-Graph vs. Vector-Retrieval Augmented Inference with SLMs"
title_zh: 迈向能耗感知的需求依赖分类：基于小语言模型的知识图谱与向量检索增强推理对比研究
authors: "Shreyas Patil, Pragati Kumari, Novarun Deb, Gouri Ginde"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.23954v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 比较了基于知识图谱的检索与基于向量的语义检索在RAG中的应用
tldr: 本研究针对系统需求冲突检测中大语言模型能耗过高的问题，提出了一种能耗感知的框架。该框架对比了基于知识图谱检索（KGR）与向量语义检索（VSR）在增强小语言模型（SLM，7B-8B规模）推理方面的表现。通过综合评估能耗、延迟、碳排放及F1分数，研究揭示了结构化检索与语义检索在需求冲突检测中的性能权衡，为可持续需求工程提供了高效且环保的架构方案。
selection_source: fresh_fetch
motivation: 旨在解决大语言模型在自动化需求冲突检测中因计算需求巨大而导致的能源浪费问题，推动向节能的小语言模型转型。
method: 对比研究了知识图谱检索（KGR）与向量语义检索（VSR）在增强SLM推理中的效果，并结合零样本、少样本及思维链等提示策略进行评估。
result: 通过对能耗、延迟、碳排放和F1分数的系统测量，揭示了结构化检索与语义检索在预测性能与环境影响之间的复杂权衡。
conclusion: 该研究为能源高效的需求工程提供了可复现的可持续架构，证明了通过优化检索策略可以在保持检测准确性的同时显著降低环境成本。
---

## 摘要
系统规范的持续演进需要频繁评估冲突需求，这一过程传统上是劳动密集型的。尽管大语言模型（LLMs）在自动化此类检测方面展现出巨大潜力，但其庞大的计算需求往往导致过度的能源浪费。因此，为了实现可持续的需求工程，迫切需要向小语言模型（SLMs）和能耗感知架构转型。本研究提出并实证评估了一个能耗感知框架，该框架对比了基于知识图谱的检索（KGR）与基于向量的语义检索（VSR），旨在增强 7B 至 8B 参数规模下基于 SLM 的推理。通过利用结构化图遍历和高维语义映射，我们提取候选需求，随后由推理引擎将其分类为冲突或中性。我们在零样本（Zero-Shot）、少样本（Few-Shot）和思维链（Chain of Thoughts）提示方法下评估了这些检索增强策略。本研究采用衡量能耗（Wh）、延迟（s）和碳排放（gCO2eq）的三支柱可持续性框架，结合标准准确度指标（F1 分数），首次对预测性能与环境影响之间的权衡进行了系统性的实证评估和分析。我们的研究结果强调了结构化检索与语义检索在检测需求冲突方面的有效性，为节能需求工程提供了一种可复现的、具有可持续性意识的架构。

## Abstract
The continuous evolution of system specifications necessitates frequent evaluation of conflicting requirements, a process that is traditionally labour intensive. Although large language models (LLMs) have demonstrated significant potential for automating this detection, their massive computational requirements often result in excessive energy waste. Consequently, there is a growing need to transition toward Small Language Models (SLMs) and energy aware architectures for sustainable Requirements Engineering. This study proposes and empirically evaluates an energy aware framework that compares Knowledge Graph-based Retrieval (KGR) with Vector-based Semantic Retrieval (VSR) to enhance SLM-based inference at the 7B to 8B parameter scale. By leveraging structured graph traversal and high dimensional semantic mapping, we extract candidate requirements, which are then classified as conflicting or neutral by an inference engine. We evaluate these retrieval enhanced strategies across Zero-Shot, Few-Shot, and Chain of Thoughts prompting methods. Using a three-pillar sustainability framework measuring energy consumption (Wh), latency (s), and carbon emissions (gCO2eq) alongside standard accuracy metrics (F1 Score), this research provides a first systematic empirical evaluation and trade off analysis between predictive performance and environmental impact. Our findings highlight the effectiveness of structured versus semantic retrieval in detecting requirement conflicts, offering a reproducible, sustainability aware architecture for energy efficient requirement engineering.

---

## 论文详细总结（自动生成）

以下是对论文《Towards Energy-aware Requirements Dependency Classification: Knowledge-Graph vs. Vector-Retrieval Augmented Inference with SLMs》的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：在需求工程（RE）中，识别需求间的冲突（Conflict）至关重要，但传统人工识别耗时耗力。虽然大语言模型（LLM）在自动化检测方面表现出色，但其巨大的计算需求导致了严重的能源浪费和碳排放。
*   **核心问题**：如何在保证需求冲突检测准确性的同时，降低计算能耗？研究聚焦于利用小语言模型（SLM，7B-8B规模）结合检索增强生成（RAG）技术，探索可持续、低碳的自动化需求分类方案。

### 2. 方法论
论文提出了一个能耗感知的框架，对比了两种检索增强策略：
*   **知识图谱检索（KGR）**：
    *   **核心思想**：利用结构化信息克服语义检索的“结构盲区”。
    *   **技术细节**：使用 spaCy 提取需求中的实体（参与者、动作、对象、属性、条件），构建 Neo4j 知识图谱。
    *   **算法逻辑**：通过实体重叠、关系类型一致性和图邻近度（最短路径）计算综合得分：$Score = \alpha \cdot \text{实体重叠} + \beta \cdot \text{角色一致性} + \gamma \cdot \text{图距离倒数}$。
*   **向量语义检索（VSR）**：
    *   使用 `all-mpnet-base-v2` 模型将需求转化为 768 维向量，存储于 Milvus 数据库，通过余弦相似度检索近邻。
*   **推理引擎**：采用三种 SLM（DeepSeek-Coder-7B, Llama-3-8B, Mistral-7B），配合零样本（Zero-Shot）、少样本（Few-Shot）和思维链（CoT）提示策略进行冲突分类。

### 3. 实验设计
*   **数据集**：涵盖 5 个不同领域的软件需求规格说明书（SRS）：OpenCOSS（安全关键系统）、WorldVista（医疗）、UAV（无人机控制）、PURE（通用软件）、IBM-UAV（工业级航空航天）。
*   **Benchmark 与对比方法**：
    *   **检索对比**：KGR vs. VSR。
    *   **推理对比**：三种 SLM 在不同提示策略下的表现。
    *   **基准线**：全量推理（Vanilla Inference，即不经过检索过滤，对所有可能的需求对进行分类）。
*   **评估指标**：Recall@K（检索效率）、Macro F1（分类准确度）、能耗（Wh）、延迟（s）、碳排放（gCO2eq）以及综合指标 EcoScore。

### 4. 资源与算力
*   **检索阶段**：在本地 Mac Mini M4（16GB RAM）上运行，Milvus 和 Neo4j 部署在 Docker 容器中。
*   **推理阶段**：在 Google Colab 上使用 **NVIDIA A100 GPU**（FP16 精度执行）。
*   **监控工具**：使用 `CodeCarbon` 实时追踪推理过程中的能耗和碳足迹。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个数据集、3 种主流 SLM、3 种提示策略以及 2 种检索架构。
*   **充分性评价**：实验设计较为充分。研究不仅关注准确率，还通过消融实验分析了提示策略对能耗的影响，并针对不同规模的数据集（如 IBM-UAV 的大规模检索）进行了压力测试。实验过程遵循了 Green AI 的透明度标准，排除了模型加载等非执行阶段的干扰，确保了对比的公平性。

### 6. 主要结论与发现
*   **检索效率**：在中小规模数据集中，**KGR 比 VSR 节能 71%-75%**，且碳排放降低高达 80%，而检索召回率（Recall）损失极小（<5%）。但在大规模数据集（IBM-UAV）中，VSR 的能效扩展性更好。
*   **模型表现**：**Mistral-7B 配合零样本提示**展现了最佳的性能与可持续性平衡（EcoScore 最高）。
*   **整体收益**：相比于全量推理，采用 KGR/VSR 增强的流水线将**总计算能耗和碳排放降低了约 51.8%**，显著提升了“性能-能耗比”。
*   **提示策略影响**：少样本提示（Few-Shot）会显著增加 Token 消耗和能耗（约 5-6%），且并不总能提升准确率；CoT 在 Llama 模型上提升明显但伴随能耗代价。

### 7. 优点
*   **首创性**：首次系统性地对比了知识图谱与向量检索在需求工程中的能耗表现。
*   **多维评估**：引入了三支柱可持续性框架，将环境影响（碳排放）作为核心评价指标。
*   **实用性**：证明了 7B-8B 规模的 SLM 在经过检索增强后，完全有能力处理复杂的需求依赖分类任务，为企业提供了低成本的替代方案。

### 8. 不足与局限
*   **模型范围**：仅测试了 7B-8B 规模的模型，未探讨更小（如 1B-3B）或稍大（13B）模型的表现。
*   **语言限制**：实验仅限于英语需求文档，未验证在多语言环境下的泛化能力。
*   **超参数固定**：KGR 中的权重系数（$\alpha, \beta, \gamma$）和检索深度 $K$ 是基于启发式实验固定的，可能并非全局最优。
*   **人工验证缺失**：虽然依赖标准数据集的标签，但未引入人类专家对检索出的候选对进行定性分析。

（完）
