---
title: "Environment Maps: Structured Environmental Representations for Long-Horizon Agents"
title_zh: 环境地图：面向长程智能体的结构化环境表示
authors: "Yenchia Feng, Chirag Sharma, Karime Maamari"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23610v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 长程智能体的结构化环境表示
tldr: 针对大语言模型在复杂软件工作流中面临的长程规划失败和环境随机性问题，本文提出了“环境地图（Environment Maps）”。这是一种结构化的、与代理无关的环境表示方法，通过将屏幕录像和执行轨迹整合为包含上下文、动作、工作流和隐性知识的图结构。在 WebArena 基准测试中，该方法显著提升了任务成功率，为长程规划提供了可解释且可编辑的持久化基础。
selection_source: fresh_fetch
motivation: 现有的智能体在处理长程复杂任务时，容易因环境动态变化和单步错误导致级联失败。
method: 提出一种将异构证据（如执行轨迹）整合为包含上下文、动作、工作流和知识的结构化图表示方法。
result: "在 WebArena 测试中，使用环境地图的智能体成功率达到 28.2%，远高于仅依赖会话上下文的基线（14.2%）。"
conclusion: 环境地图为长程智能体提供了一个可解释、可编辑且持续优化的结构化接口，有效增强了其规划能力。
---

## 摘要
尽管大语言模型（LLMs）发展迅速，但复杂软件工作流的鲁棒自动化仍是一个悬而未决的问题。在长程（long-horizon）场景中，智能体经常受到级联错误和环境随机性的困扰；在动态界面中的单次失误就可能导致任务失败，进而引发幻觉或陷入反复试错。本文介绍了“环境地图”（Environment Maps）：一种持久的、与智能体无关的表示方式，通过将屏幕录像和执行轨迹等异构证据整合到结构化图中，来缓解这些失败。该表示由四个核心组件组成：(1) 上下文（抽象位置），(2) 动作（参数化可操作性），(3) 工作流（观测到的轨迹），以及 (4) 隐性知识（领域定义和可重用程序）。我们在 WebArena 基准测试的五个领域中评估了该框架。配备环境地图的智能体实现了 28.2% 的成功率，几乎是受限于会话绑定上下文的基线模型（14.2%）性能的两倍，并且优于能够访问用于生成环境地图的原始轨迹数据的智能体（23.3%）。通过在模型和环境之间提供结构化接口，环境地图为长程规划建立了持久的基础，且具有人类可解释性、可编辑性和增量完善性。

## Abstract
Although large language models (LLMs) have advanced rapidly, robust automation of complex software workflows remains an open problem. In long-horizon settings, agents frequently suffer from cascading errors and environmental stochasticity; a single misstep in a dynamic interface can lead to task failure, resulting in hallucinations or trial-and-error. This paper introduces $\textit{Environment Maps}$: a persistent, agent-agnostic representation that mitigates these failures by consolidating heterogeneous evidence, such as screen recordings and execution traces, into a structured graph. The representation consists of four core components: (1) Contexts (abstracted locations), (2) Actions (parameterized affordances), (3) Workflows (observed trajectories), and (4) Tacit Knowledge (domain definitions and reusable procedures). We evaluate this framework on the WebArena benchmark across five domains. Agents equipped with environment maps achieve a 28.2% success rate, nearly doubling the performance of baselines limited to session-bound context (14.2%) and outperforming agents that have access to the raw trajectory data used to generate the environment maps (23.3%). By providing a structured interface between the model and the environment, Environment Maps establish a persistent foundation for long-horizon planning that is human-interpretable, editable, and incrementally refinable.

---

## 论文详细总结（自动生成）

以下是对论文《Environment Maps: Structured Environmental Representations for Long-Horizon Agents》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）驱动的智能体在处理复杂软件工作流（长程任务）时，容易因环境的动态性、UI 漂移和缺乏持久的环境理解而产生“级联错误”。
*   **研究动机**：
    *   **长程规划失败**：智能体在多步交互中难以维持对“我在哪”、“哪些动作有效”以及“动作后果是什么”的持续认知。
    *   **现有方法局限**：现有的 UI 转换图、预测模型或技能库通常是孤立的、非持久的，或者直接操作原始观测数据，缺乏统一且可维护的抽象表示。
    *   **数据利用率低**：原始的执行轨迹（Traces）难以被智能体直接高效利用。

### 2. 方法论：环境地图（Environment Maps）
论文提出了一种持久、与智能体无关的结构化表示方法，将异构经验整合为图结构 $M = (C, A, W, K)$：
*   **核心组件**：
    1.  **上下文 (Contexts, $C$)**：抽象的页面位置，通过 URL 模式归一化识别。
    2.  **动作 (Actions, $A$)**：参数化的可操作性（如将“点击 John”抽象为“点击 {用户名}”）。
    3.  **工作流 (Workflows, $W$)**：观测到的成功执行轨迹序列。
    4.  **隐性知识 (Tacit Knowledge, $K$)**：领域术语定义和特定上下文的操作规程。
*   **构建流程（5阶段）**：
    1.  **统一步骤序列**：解析原始观测，提取动作名称、描述和结果。
    2.  **动作提取**：从轨迹中提取已执行动作，并从 DOM 中识别潜在动作。
    3.  **动作泛化**：利用 LLM 识别模式，将具体动作抽象为带占位符的参数化模板。
    4.  **上下文与知识提取**：基于 URL 模式聚类页面，并提取领域定义和操作逻辑。
    5.  **整合与合并**：去重并建立实体间的可追溯链接，生成最终的 JSON 格式地图。

### 3. 实验设计
*   **基准测试**：使用 **WebArena**，涵盖 5 个真实 Web 环境（电子商务、CMS 管理后台、GitLab、Reddit 论坛、地图）。
*   **任务规模**：共 812 个自然语言指令任务。
*   **对比方法**：
    1.  **Baseline (No Map)**：仅提供当前页面观测（可访问性树）和任务意图。
    2.  **Trajectory Access**：允许智能体访问原始的人类执行轨迹文件（JSONL 格式）。
    3.  **Environment Map Access**：允许智能体访问基于相同轨迹数据构建的结构化环境地图。
*   **智能体配置**：基于 `claude-3.5-sonnet`，使用 Claude Agent SDK，具备文件读写和 Bash 工具调用能力。

### 4. 资源与算力
*   **训练算力**：论文方法**无需对模型进行微调（Fine-tuning）**，属于零样本/RAG 范式的增强。
*   **构建成本**：构建一个环境地图的成本极低，每个环境约需 **1-4 美元** 的 API 费用，耗时 **13-31 分钟**。
*   **推理算力**：实验运行在云端基础设施的 Docker 容器中，未详细说明具体的 GPU 型号和数量，但强调了其成本远低于 GPU 训练。

### 5. 实验数量与充分性
*   **实验规模**：在 812 个任务上进行了完整测试，涵盖了所有难度级别。
*   **充分性分析**：
    *   **泛化性测试**：专门对比了有轨迹覆盖的任务（179个）和无轨迹覆盖的任务（633个），证明了地图的泛化能力。
    *   **统计严谨性**：提供了 Wilson 95% 置信区间，并进行了 HAR 轨迹分析以评估回溯（Backtracking）率。
    *   **消融/对比**：通过对比“原始轨迹”和“结构化地图”，清晰地隔离了“表示形式”对性能的影响。实验设计客观且公平。

### 6. 主要结论与发现
*   **性能翻倍**：配备环境地图的智能体成功率达到 **28.2%**，远超基线（14.2%）。
*   **表示形式至关重要**：即使使用相同的数据源，结构化地图（28.2%）也显著优于原始轨迹数据（23.3%）。
*   **复杂环境增益更大**：在分支多、UI 密集的 GitLab 和 CMS 环境中，地图带来的提升最为显著。
*   **效率提升**：地图显著减少了智能体的回溯行为和网络请求数量，使动作更加精准。

### 7. 优点
*   **解耦性**：环境表示与特定智能体策略解耦，地图可跨模型重用。
*   **可解释性与可编辑性**：地图以人类可读的 JSON 格式存储，方便人工检查、修正和增量更新。
*   **低成本高收益**：相比昂贵的模型微调，通过构建结构化知识库实现性能大幅提升，极具工程实用价值。

### 8. 不足与局限
*   **跨站点限制**：目前的地图主要针对单一环境，对于跨站点的复杂工作流（如从 GitLab 跳转到 CMS）支持不足。
*   **时效性挑战**：Web 环境频繁更新时，地图可能过时，自动检测和修复 UI 漂移的功能尚待完善。
*   **依赖初始数据**：地图的质量高度依赖于初始输入的人类轨迹或观测数据的覆盖范围。
*   **状态验证不足**：目前的动作后果描述主要是文本性的，缺乏基于实时状态观测的硬性验证。

（完）
