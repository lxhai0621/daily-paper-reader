---
title: "CoCR-RAG: Enhancing Retrieval-Augmented Generation in Web Q&A via Concept-oriented Context Reconstruction"
title_zh: CoCR-RAG：通过面向概念的上下文重构增强网络问答中的检索增强生成
authors: "Kaize Shi, Xueyao Sun, Qika Lin, Firoj Alam, Qing Li, Xiaohui Tao, Guandong Xu"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.23989v1"
tags: ["query:ma-kf"]
score: 10.0
evidence: 面向概念的RAG上下文重构
tldr: 针对Web问答中多源文档风格迥异、冗余信息多导致RAG效果受限的问题，本文提出CoCR-RAG框架。该框架利用抽象语义表示（AMR）提取核心概念，并通过大模型将多源信息重构为统一、高信息密度的上下文。实验证明该方法在PopQA等数据集上显著优于现有重构方法，且具有良好的通用性和鲁棒性。
selection_source: fresh_fetch
motivation: 解决Web检索文档中存在的格式不一、信息冗余及跨源融合困难等问题，以提升RAG的生成质量。
method: 引入基于AMR的概念蒸馏算法提取核心语义，并利用LLM将这些概念重构为精炼且逻辑统一的上下文。
result: 在PopQA和EntityQuestions基准测试中，CoCR-RAG的表现显著超过了现有的上下文重构方法。
conclusion: CoCR-RAG是一个灵活、即插即用的组件，能有效增强不同RAG框架在处理复杂Web信息时的性能。
---

## 摘要
检索增强生成（RAG）通过整合来自网络和其他外部来源的信息，在增强问答系统方面展现出良好的前景。然而，从异构网络中检索到的支持文档通常源自多个渠道，具有多样的写作风格、不同的格式以及不一致的粒度。将这些多源文档融合为连贯且知识密集型的上下文仍然是一个重大挑战，因为无关和冗余信息的出现可能会损害推断答案的事实一致性。本文提出了面向概念的上下文重构 RAG（CoCR-RAG），该框架通过基于语言学的概念级集成来解决 RAG 中的多源信息融合问题。具体而言，我们引入了一种概念蒸馏算法，从抽象语义表示（AMR）中提取核心概念；AMR 是一种稳定的语义表示方式，将文本含义结构化为逻辑图。随后，大语言模型将从多个检索文档中蒸馏出的概念进行融合，并重构为统一的、信息密集的上下文，仅补充必要的句子成分以突出核心知识。在 PopQA 和 EntityQuestions 数据集上的实验表明，CoCR-RAG 在这些网络问答基准测试中显著优于现有的上下文重构方法。此外，CoCR-RAG 在各种骨干大语言模型上均表现出鲁棒性，证明其作为一个灵活、即插即用的组件，能够适配不同的 RAG 框架。

## Abstract
Retrieval-augmented generation (RAG) has shown promising results in enhancing Q&A by incorporating information from the web and other external sources. However, the supporting documents retrieved from the heterogeneous web often originate from multiple sources with diverse writing styles, varying formats, and inconsistent granularity. Fusing such multi-source documents into a coherent and knowledge-intensive context remains a significant challenge, as the presence of irrelevant and redundant information can compromise the factual consistency of the inferred answers. This paper proposes the Concept-oriented Context Reconstruction RAG (CoCR-RAG), a framework that addresses the multi-source information fusion problem in RAG through linguistically grounded concept-level integration. Specifically, we introduce a concept distillation algorithm that extracts essential concepts from Abstract Meaning Representation (AMR), a stable semantic representation that structures the meaning of texts as logical graphs. The distilled concepts from multiple retrieved documents are then fused and reconstructed into a unified, information-intensive context by Large Language Models, which supplement only the necessary sentence elements to highlight the core knowledge. Experiments on the PopQA and EntityQuestions datasets demonstrate that CoCR-RAG significantly outperforms existing context-reconstruction methods across these Web Q&A benchmarks. Furthermore, CoCR-RAG shows robustness across various backbone LLMs, establishing itself as a flexible, plug-and-play component adaptable to different RAG frameworks.

---

## 论文详细总结（自动生成）

### 论文结构化总结：CoCR-RAG

#### 1. 核心问题与整体含义（研究动机和背景）
大语言模型（LLMs）在处理长尾知识或特定领域查询时容易产生幻觉。检索增强生成（RAG）虽能引入外部知识，但在 Web 问答场景下面临**多源信息融合**的挑战：
*   **异构性**：来自不同网页的文档写作风格、格式和粒度差异巨大。
*   **噪声干扰**：检索到的文档包含大量冗余、无关甚至矛盾的信息，干扰 LLM 的事实一致性判断。
*   **推理负担**：直接将原始多源文档拼接作为上下文，会增加模型的计算负担并稀释核心知识。

本文旨在通过**语言学引导的概念级集成**，将零散、多噪声的原始文档重构为统一、高信息密度的上下文。

#### 2. 方法论：核心思想与关键技术
CoCR-RAG 框架主要由三个阶段组成：
*   **信息检索**：使用检索器（如 Contriever 或 BM25）获取前 $K$ 个相关文档。
*   **概念蒸馏（Concept Distillation）**：
    *   利用 **AMR（抽象语义表示）** 将文本转化为逻辑图（Rooted, Directed, Acyclic Graph）。AMR 能忽略表层词汇差异，捕捉深层语义。
    *   **算法流程**：通过深度优先搜索（DFS）遍历 AMR 图节点，提取核心概念。
    *   **特殊处理**：针对实体名（`:name`）、维基百科链接（`:wiki`）和日期（`date-entity`）进行标准化处理，消除歧义并对齐外部知识库。
    *   **回溯（Backtrace）**：将抽象的 AMR 节点映射回原始文档中的具体词汇，保留时态等上下文变体。
*   **面向概念的上下文重构（Context Reconstruction）**：
    *   利用 LLM（如 LLaMA-2-13B）将提取的离散概念重新组织成简洁的句子。
    *   **核心思想**：仅补充必要的语法元素，剔除所有与核心概念无关的修饰词，形成“事实清单”式的精炼文本。
*   **推理生成**：将重构后的上下文作为“事实（Facts）”输入给骨干 LLM，引导其基于此进行问答。

#### 3. 实验设计
*   **数据集**：PopQA（侧重长尾知识）和 EntityQuestions（实体中心问答）。
*   **Benchmark 与对比方法**：
    *   **Vanilla**：直接使用原始检索文档。
    *   **词法过滤类**：Keywords（关键词提取）、Selective Context（SelCon，基于信息熵压缩）。
    *   **语法/摘要类**：Summary（大模型摘要）、LLMLingua（精细化提示词压缩）。
*   **骨干模型（Backbones）**：涵盖了 12 种开源 LLM，包括 GPT-Neo (1.3B/2.7B)、GPT-j (6B)、OPT (1.3B/2.7B/6.7B)、Bloom (560m 到 3B) 以及 LLaMA-2-chat (7B/13B)。

#### 4. 资源与算力
*   **模型使用**：文中明确提到使用 LLaMA-2-13B-chat-hf 执行上下文重构任务，并使用了基于 mBART 训练的 AMR 解析器。
*   **算力细节**：**论文未明确说明**具体的 GPU 型号、数量、训练时长或推理总耗时。这在学术论文中较为常见，尤其是当研究重点在于推理框架而非模型训练时。

#### 5. 实验数量与充分性
*   **实验规模**：研究涵盖了 2 个主流数据集、12 个不同家族和规模的骨干模型，并针对检索文档数量 $K$（从 1 到 10）进行了详细的敏感度分析。
*   **充分性与公平性**：
    *   实验对比了 Acc（准确率）和 AUC（曲线下面积），全面衡量了模型在不同上下文长度下的表现。
    *   对比方法涵盖了当前最先进的提示词压缩技术（如 LLMLingua），实验设计较为客观、公平。
    *   通过对 12 个模型的测试，验证了该方法作为“即插即用”组件的普适性。

#### 6. 主要结论与发现
*   **性能提升**：CoCR-RAG 在所有骨干模型和数据集上均显著优于 Vanilla 和其他重构基准。
*   **长上下文优势**：随着检索文档数量 $K$ 的增加，原始文档的噪声干扰加剧，而 CoCR-RAG 的领先优势更加扩大，证明了其在处理复杂多源信息时的有效性。
*   **鲁棒性**：CoCR-RAG 结合了词法过滤（去噪快）和语法重构（易理解）的优点，在不同规模的模型上表现稳定（标准差 $\sigma$ 较低）。
*   **模型敏感度**：具有较长上下文窗口的模型（如 LLaMA-2）在应用 CoCR-RAG 后提升最明显，说明这些模型虽能处理长文本，但仍极易受噪声干扰。

#### 7. 优点
*   **语言学根基**：利用 AMR 这种稳定的语义结构，比纯提示词工程（Prompt Engineering）更具解释性和事实一致性。
*   **即插即用**：无需对骨干模型进行微调，可直接适配现有的各种 RAG 流程。
*   **无需调参**：相比 SelCon 或 LLMLingua 需要调节压缩率等超参数，CoCR-RAG 基于语义节点提取，应用更加简便。

#### 8. 不足与局限
*   **解析器依赖**：系统的效果高度依赖于 AMR 解析器的准确性，如果解析阶段丢失关键语义，后续重构将无法挽回。
*   **计算开销**：重构过程需要额外调用一次 LLM，虽然生成的文本更短能节省推理成本，但增加了预处理的延迟。
*   **实验覆盖**：未在商业闭源模型（如 GPT-4）上进行大规模测试（作者解释是为了避免模型内部参数化知识的干扰）。
*   **语言限制**：虽然使用了多语言 AMR 解析器，但实验主要集中在英文数据集，在其他语种上的表现尚待验证。

（完）
