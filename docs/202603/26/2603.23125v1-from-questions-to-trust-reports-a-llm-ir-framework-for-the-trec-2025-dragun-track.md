---
title: "From Questions to Trust Reports: A LLM-IR Framework for the TREC 2025 DRAGUN Track"
title_zh: 从问题到信任报告：面向 TREC 2025 DRAGUN 赛道的 LLM-IR 框架
authors: "Ignacy Alwasiak, Kene Nnolim, Jaclyn Thi, Samy Ateia, Markus Bink, Gregor Donabauer, David Elsweiler, Udo Kruschwitz"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23125v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 检索增强的可信度报告和LLM-IR框架
tldr: 该研究针对TREC 2025 DRAGUN任务开发了UR_Trecking系统，旨在帮助用户评估在线新闻的可信度。系统结合了大语言模型（LLM）与信息检索（IR）技术，涵盖关键问题生成和增强检索的可信度报告生成。通过语义过滤、思维链（CoT）查询扩展、monoT5重排序及LLM证据合成，系统显著提升了检索相关性与领域信任度，为自动化新闻评估提供了有效框架。
selection_source: fresh_fetch
motivation: 针对用户评估在线新闻可信度的迫切需求，开发能够生成批判性问题并提供证据支持的可信度报告工具。
method: 结合LLM问题生成、CoT查询扩展、monoT5重排序以及基于LLM的证据合成，构建了一个端到端的检索增强生成框架。
result: 官方评估显示，思维链查询扩展和重排序技术显著提高了检索结果的相关性和领域可信度。
conclusion: 该系统证明了LLM与IR结合在提升新闻可信度评估方面的潜力，但也指出问题生成的质量仍有改进空间。
---

## 摘要
TREC 2025 的 DRAGUN 赛道旨在满足日益增长的需求，即开发有效的支持工具以帮助用户评估在线新闻的可信度。我们描述了为任务 1（关键问题生成）和任务 2（检索增强的可信度报告）提交的 UR_Trecking 系统。我们的方法结合了基于大语言模型（LLM）的问题生成、语义过滤、利用聚类实现的多样性增强，以及多种查询扩展策略（包括基于推理的思维链扩展），旨在从 MS MARCO V2.1 分段语料库中检索相关证据。检索到的文档使用 monoT5 模型进行重排序，并结合领域级可信度数据集通过 LLM 相关性评判器进行过滤。针对任务 2，所选证据由 LLM 综合为带有引用的简洁可信度报告。官方评估结果表明，与基准检索相比，思维链查询扩展和重排序显著提高了相关性和领域信任度，而问题生成的性能表现出中等质量，仍有改进空间。最后，我们总结了遇到的主要挑战，并为在系统的未来迭代中增强鲁棒性和可信度评估提出了建议方向。

## Abstract
The DRAGUN Track at TREC 2025 targets the growing need for effective support tools that help users evaluate the trustworthiness of online news. We describe the UR_Trecking system submitted for both Task 1 (critical question generation) and Task 2 (retrieval-augmented trustworthiness reporting). Our approach combines LLM-based question generation with semantic filtering, diversity enforcement using clustering, and several query expansion strategies (including reasoning-based Chain-of-Thought expansion) to retrieve relevant evidence from the MS MARCO V2.1 segmented corpus. Retrieved documents are re-ranked using a monoT5 model and filtered using an LLM relevance judge together with a domain-level trustworthiness dataset. For Task 2, selected evidence is synthesized by an LLM into concise trustworthiness reports with citations. Results from the official evaluation indicate that Chain-of-Thought query expansion and re-ranking substantially improve both relevance and domain trust compared to baseline retrieval, while question-generation performance shows moderate quality with room for improvement. We conclude by outlining key challenges encountered and suggesting directions for enhancing robustness and trustworthiness assessment in future iterations of the system.

---

## 论文详细总结（自动生成）

这是一份关于论文《From Questions to Trust Reports: A LLM-IR Framework for the TREC 2025 DRAGUN Track》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在虚假信息泛滥的背景下，普通用户往往缺乏独立评估在线新闻可信度的能力，且搜索行为有时会强化错误信念。
*   **研究动机**：针对 TREC 2025 DRAGUN 赛道，开发一套自动化工具，通过生成“批判性问题”引导用户思考（任务 1），并提供基于证据的“可信度报告”（任务 2），从而增强用户辨别新闻真伪的能力。
*   **整体含义**：该研究探索了如何将大语言模型（LLM）的推理能力与传统信息检索（IR）技术结合，构建一个端到端的“检索增强生成”（RAG）框架，用于复杂的新闻可信度评估任务。

### 2. 方法论：核心思想与技术流程
该系统（UR_Trecking）由两个主要阶段组成：
*   **任务 1：问题生成与过滤**
    1.  **生成**：使用 GPT-4o-Nano 根据预设原则（如调查来源背景、所有权、偏见等）为每篇新闻生成约 30 个批判性问题。
    2.  **过滤**：通过语义过滤剔除复合问题和过长问题。
    3.  **多样性增强**：利用 Sentence Transformer 生成嵌入，通过 **K-Means 聚类**将问题分组，并选取最接近每个类簇质心的 10 个问题，确保覆盖不同的可信度维度。
*   **任务 2：证据检索与报告生成**
    1.  **查询扩展**：对比了四种策略，其中 **思维链（CoT）扩展** 效果最佳，即引导 LLM 逐步推理回答问题所需的具体信息。
    2.  **检索与重排序**：从 MS MARCO V2.1 语料库检索候选文档，使用 **monoT5 模型**进行重排序。
    3.  **可信度过滤**：结合 LLM 相关性评判和第三方领域信任数据集（Lin et al. 整合的 NewsGuard 等数据），筛选出既相关又来自高信用域名的证据。
    4.  **合成**：LLM 综合筛选出的证据，生成带有引用的简明报告。

### 3. 实验设计
*   **数据集**：使用 **MS MARCO V2.1 (Segmented)** 作为检索语料库。
*   **Benchmark**：TREC 2025 DRAGUN 官方评估标准。由 NIST 评审员制定评分量规（Rubric），包含“必须知道”、“值得知道”等权重。
*   **对比方法**：
    *   **检索策略对比**：基准（无扩展）、布尔扩展、思维链（CoT）扩展、结构化 DSL 扩展。
    *   **重排序对比**：对比了 monoT5 重排序前后的相关性得分和域名信任分。

### 4. 资源与算力
*   **模型使用**：主要依赖 OpenAI 的 **GPT-4o-Nano**（考虑到成本与效率的平衡）。
*   **重排序模型**：使用了 HuggingFace 上的 `monot5-base-msmarco`。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。由于主要采用现成模型进行推理和微调后的重排序器，其算力消耗主要集中在 API 调用和本地推理上。

### 5. 实验数量与充分性
*   **实验规模**：针对 TREC 提供的多个主题（Topics）进行了测试，并对前 20 个示例问题进行了详细的消融分析（如重排序前后的对比）。
*   **充分性**：实验对比了多种查询扩展策略，并引入了外部可信度数据集作为过滤信号，逻辑较为严密。
*   **局限性**：作者承认由于时间限制，仅提交了一个运行版本（Run），且未能集成原计划的“迭代修正循环”，这在一定程度上影响了实验的深度。

### 6. 主要结论与发现
*   **检索优化**：**思维链（CoT）查询扩展** 结合 **monoT5 重排序** 表现最稳健，显著提升了检索结果的相关性和来源的可信度。
*   **重排序作用**：重排序不仅提升了语义相关性，还间接促进了高质量域名（高信任分）的排名。
*   **生成质量**：问题生成（任务 1）表现中等。虽然成功避免了生成矛盾或有害的问题，但在与人类评审员关注点的“对齐”上仍有提升空间。
*   **报告合成**：生成的报告在支持性得分上较低，反映出检索到的证据往往只能部分覆盖新闻声明。

### 7. 优点
*   **多样性处理**：通过 K-Means 聚类解决 LLM 生成问题同质化的问题，设计巧妙。
*   **信任信号集成**：不仅关注语义相关性，还显式引入了领域级信任分（Domain Trust Scores），增强了 RAG 系统的权威性。
*   **推理驱动**：利用 CoT 扩展挖掘新闻背后隐藏的信息需求，比简单的关键词扩展更符合事实核查逻辑。

### 8. 不足与局限
*   **对齐偏差**：生成的问题和报告与人类专家制定的评估标准（Rubric）一致性不够高。
*   **评估依赖**：系统内部评估高度依赖 LLM Judge，可能存在模型偏见或校准不准的问题。
*   **系统完整性**：由于时间压力，部分关键组件（如迭代纠错循环）未能在最终提交版本中实现，导致系统仍处于原型阶段。
*   **覆盖范围**：对于未包含在 Lin et al. 数据集中的域名，系统默认赋予 0 分，这可能导致一些新兴或小众但可靠的媒体被误杀。

（完）
