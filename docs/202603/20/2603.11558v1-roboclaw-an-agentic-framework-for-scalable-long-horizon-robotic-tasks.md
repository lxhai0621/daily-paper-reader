---
title: "RoboClaw: An Agentic Framework for Scalable Long-Horizon Robotic Tasks"
title_zh: RoboClaw：一种用于可扩展长程机器人任务的智能体框架
authors: "Ruiying Li, Yunlang Zhou, YuYao Zhu, Kylin Chen, Jingyuan Wang, Sukai Wang, Kongtao Hu, Minhui Yu, Bowen Jiang, Zhan Su, Jiayao Ma, Xin He, Yongjian Shen, Yangyang, Guanghui Ren, Maoqing Yao, Wenhao Wang, Yao Mu"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11558v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 用于自主数据采集和任务执行的智能体机器人框架
tldr: RoboClaw是一个统一的数据采集、策略学习与任务执行的智能体框架，旨在解决长程机器人任务中数据采集难和多策略执行脆弱的问题。它引入了“纠缠动作对（EAP）”机制，通过前向操作与逆向恢复动作的耦合实现自主复位和持续数据采集。该框架利用视觉语言模型（VLM）进行高层推理和策略调度，显著降低了人工干预，提升了长程任务的成功率和系统的可扩展性。
selection_source: fresh_fetch
motivation: 现有的机器人学习流程在数据采集、策略学习和部署上相互分离，导致对人工环境重置的高度依赖以及长程任务执行的脆弱性。
method: 提出RoboClaw框架，通过纠缠动作对（EAP）实现自主复位的数据采集，并利用VLM驱动的控制器进行高层推理与动态策略编排。
result: "在真实世界任务中，该方法比基准方法在长程任务成功率上提升了25%，同时减少了53.7%的人工投入时间。"
conclusion: RoboClaw通过统一采集与执行阶段的上下文语义，有效提升了机器人系统的自主性、稳定性和长程任务处理能力。
---

## 摘要
视觉-语言-动作（VLA）系统在语言驱动的机器人操控方面展现出强大潜力。然而，将其扩展到长程任务仍然具有挑战性。现有的流程通常将数据采集、策略学习和部署分开，导致对人工环境重置的严重依赖以及多策略执行的脆弱性。我们提出了 RoboClaw，这是一个将数据采集、策略学习和任务执行统一在单个 VLM 驱动控制器下的智能体机器人框架。在策略层面，RoboClaw 引入了纠缠动作对（Entangled Action Pairs, EAP），将正向操控行为与反向恢复动作耦合，形成用于自主数据采集的自重置循环。该机制实现了在极少人工干预下的持续同策略数据获取和迭代策略优化。在部署期间，同一个智能体执行高层推理，并动态编排已学习的策略原语以完成长程任务。通过在采集和执行过程中保持一致的上下文语义，RoboClaw 减少了两个阶段之间的不匹配，并提高了多策略的鲁棒性。在真实世界操控任务中的实验表明，与传统的开环流程相比，该框架提高了稳定性和可扩展性，同时显著减少了整个机器人生命周期中的人力投入，在长程任务上的成功率比基准方法提高了 25%，并减少了 53.7% 的人力时间投入。

## Abstract
Vision-Language-Action (VLA) systems have shown strong potential for language-driven robotic manipulation. However, scaling them to long-horizon tasks remains challenging. Existing pipelines typically separate data collection, policy learning, and deployment, resulting in heavy reliance on manual environment resets and brittle multi-policy execution. We present RoboClaw, an agentic robotics framework that unifies data collection, policy learning, and task execution under a single VLM-driven controller. At the policy level, RoboClaw introduces Entangled Action Pairs (EAP), which couple forward manipulation behaviors with inverse recovery actions to form self-resetting loops for autonomous data collection. This mechanism enables continuous on-policy data acquisition and iterative policy refinement with minimal human intervention. During deployment, the same agent performs high-level reasoning and dynamically orchestrates learned policy primitives to accomplish long-horizon tasks. By maintaining consistent contextual semantics across collection and execution, RoboClaw reduces mismatch between the two phases and improves multi-policy robustness. Experiments in real-world manipulation tasks demonstrate improved stability and scalability compared to conventional open-loop pipelines, while significantly reducing human effort throughout the robot lifecycle, achieving a 25% improvement in success rate over baseline methods on long-horizon tasks and reducing human time investment by 53.7%.

---

## 论文详细总结（自动生成）

这是一份关于论文《RoboClaw: An Agentic Framework for Scalable Long-Horizon Robotic Tasks》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视觉-语言-动作（VLA）系统在处理**长程（Long-Horizon）机器人任务**时面临扩展性瓶颈。
*   **研究背景**：传统的机器人学习流程通常将数据采集、策略训练和任务部署视为相互独立的阶段。这导致了三个主要问题：
    1.  **高度依赖人工**：需要人工频繁重置环境和提供演示。
    2.  **语义不一致**：不同阶段由不同逻辑驱动，导致任务状态和成功标准的理解存在偏差。
    3.  **分布偏移**：训练数据覆盖的状态分布与部署时的实际情况不匹配，导致长程任务中的错误累积。
*   **整体含义**：RoboClaw 旨在通过一个统一的智能体框架，将生命周期的各个阶段整合在 VLM（视觉语言模型）驱动的闭环中，实现自主的数据采集与鲁棒的任务执行。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：利用 VLM 作为“元控制器”，通过上下文学习（ICL）和思维链（CoT）推理，动态编排底层策略原语。
*   **关键技术细节**：
    *   **纠缠动作对（Entangled Action Pairs, EAP）**：这是该框架的数据引擎。它将“正向操控策略”与“逆向恢复策略”配对（例如：把东西放进抽屉 vs. 从抽屉拿出来）。通过这种配对，机器人可以形成自重置循环，在无需人工干预的情况下持续进行同策略（On-policy）数据采集。
    *   **统一智能体架构**：采用分层抽象，分为**策略（Policies）**、**工具（Tools）**和**技能（Skills）**。
        *   **底层策略**：基于 $\pi_{0.5}$ VLA 模型，使用条件流匹配（Conditional Flow Matching）生成动作。
        *   **中间工具**：通过模型上下文协议（MCP）接口，允许 VLM 调用启动/停止策略、查询环境摘要等。
        *   **高层推理**：VLM 维护结构化记忆（角色身份、任务记忆、工作记忆），进行状态监控和异常恢复。
    *   **算法流程**：智能体观察环境 $\rightarrow$ 结合记忆进行 CoT 推理 $\rightarrow$ 通过 MCP 调用工具执行子任务 $\rightarrow$ 监控结果并更新记忆 $\rightarrow$ 循环直至任务完成。

### 3. 实验设计
*   **实验场景**：四个真实的物理环境，包括卧室梳妆台、厨房架子、书桌和便利店货架。
*   **具体任务**：涉及身体乳放置（长距离移动）、妆前乳放置（含关抽屉动作）、口红插入（高精度对准）和纸巾擦拭（持续接触操控）。
*   **Benchmark 与对比方法**：
    *   **数据采集效率对比**：对比纯人工演示及重置的基准。
    *   **长程任务成功率对比**：
        1.  **Baseline 1**：仅使用在相同数据集上训练的端到端 VLA 模型（无 RoboClaw 框架调度）。
        2.  **Baseline 2**：计算四个独立子任务成功率的乘积（理论预期值）。
    *   **迭代优化对比**：对比从第 1 轮到第 5 轮自主采集迭代后的策略表现。

### 4. 资源与算力
*   **模型微调**：论文提到了对 $\pi_{0.5}$ 进行 LoRA 微调的超参数。
    *   **设置**：bfloat16 精度，Rank=16，Batch size=16。
    *   **训练量**：10k 训练步数，100 步预热，学习率为 $2.5 \times 10^{-5}$。
*   **算力细节**：文中**未明确指出**具体的 GPU 型号（如 A100 或 H100）及具体的训练总时长。
*   **硬件平台**：使用 Agibot G01 双臂移动操作机器人（20 自由度）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   每个子任务进行了 50 次测试以评估成功率。
    *   长程任务评估基于 20 次完整试验的平均值。
    *   数据采集迭代进行了 5 轮，每轮增加 50 个样本。
*   **充分性评价**：实验设计较为全面，涵盖了从单一技能鲁棒性到复杂长程协作的评估。通过对比人工投入时间（减少 53.7%）和干预频率（减少 8.04 倍），客观地量化了框架的效率提升。然而，实验场景虽然多样，但仍局限于室内桌面类任务，未涉及更复杂的户外或非结构化环境。

### 6. 主要结论与发现
*   **效率提升**：RoboClaw 显著降低了人工负担，采集相同数据量所需的人工时间仅为传统方法的约 46%。
*   **性能增强**：在长程任务中，RoboClaw 的成功率比基准方法高出 **25%**。
*   **闭环学习**：通过 EAP 机制进行的自主迭代能显著提升子策略的成功率（例如口红插入任务从 4% 提升至 46%）。
*   **故障恢复**：智能体能够识别“非降级故障”（直接重试）和“降级故障”（调用恢复策略），证明了高层推理在处理现实世界不确定性中的价值。

### 7. 优点与亮点
*   **生命周期统一**：打破了数据采集与部署的壁垒，使部署过程中的失败轨迹也能反哺训练。
*   **EAP 机制**：巧妙地解决了机器人学习中最头疼的“环境重置”问题，实现了真正的自主在线学习。
*   **语义一致性**：通过 VLM 统一了任务理解，减少了多策略切换时的衔接错误。
*   **MCP 接口设计**：提供了一种标准化的方式来连接大模型推理与底层硬件控制。

### 8. 不足与局限
*   **延迟问题**：依赖云端大模型进行推理可能引入较高的延迟，影响实时反应能力。
*   **逆向动作假设**：EAP 机制依赖于能够设计出有效的逆向恢复动作，对于某些不可逆的任务（如切菜、焊接）可能不适用。
*   **场景复杂度**：虽然在真实世界测试，但环境相对静态，对于动态障碍物或极端光照变化的鲁棒性尚未深入探讨。
*   **安全约束**：虽然提到了人工干预接口，但在高度自主的采集过程中，如何确保机器人不会因错误尝试而损坏自身或环境仍是一个挑战。

（完）
