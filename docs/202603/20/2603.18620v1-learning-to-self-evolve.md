---
title: Learning to Self-Evolve
title_zh: 学习自我演化
authors: "Xiaoyin Chen, Canwen Xu, Yite Wang, Boyi Liu, Zhewei Yao, Yuxiong He"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18620v1"
tags: ["query:agent"]
score: 9.0
evidence: 用于测试时上下文优化的强化学习
tldr: 本研究提出LSE（Learning to Self-Evolve）框架，通过强化学习训练大语言模型在测试时自主优化上下文。不同于以往依赖模型固有推理能力的方法，LSE将多步演化简化为单步RL目标，并结合树引导演化循环。实验表明，经过训练的4B参数模型在Text-to-SQL和MMLU任务上超越了GPT-5等强模型驱动的策略及现有提示优化方法，证明了自演化作为可学习技能的有效性。
selection_source: fresh_fetch
motivation: 现有模型在测试时自我演化主要依赖其固有的推理能力，而缺乏针对该演化过程的显式训练。
method: 将多步演化问题简化为单步强化学习目标，以模型下游性能的提升作为奖励，并结合树引导的演化循环。
result: 4B参数的LSE模型在Text-to-SQL和MMLU任务上表现优于GPT-5、Claude Sonnet 4.5驱动的策略以及TextGrad等优化方法。
conclusion: 研究证明了将自演化视为一种可学习的技能是有效的，且训练出的策略具有良好的跨模型迁移能力。
---

## 摘要
我们介绍了“学习自我演化”（Learning to Self-Evolve, LSE），这是一个强化学习框架，旨在训练大语言模型（LLM）在测试时改进自身的上下文。我们将 LSE 置于测试时自我演化的场景中，模型通过对已见问题的反馈迭代地优化其上下文，从而在处理新问题时表现得更好。现有方法完全依赖于模型的固有推理能力，从未针对该任务进行显式训练。LSE 将多步演化问题简化为单步强化学习目标，其中每次上下文编辑都根据下游性能的提升获得奖励。我们将该目标与树引导的演化循环相结合。在 Text-to-SQL 生成（BIRD）和通用问答（MMLU-Redux）任务上，使用 LSE 训练的 4B 参数模型优于由 GPT-5 和 Claude Sonnet 4.5 驱动的自我演化策略，也优于包括 GEPA 和 TextGrad 在内的提示优化方法，并且无需额外训练即可迁移以指导其他模型。我们的结果强调了将自我演化视为一种可学习技能的有效性。

## Abstract
We introduce Learning to Self-Evolve (LSE), a reinforcement learning framework that trains large language models (LLMs) to improve their own contexts at test time. We situate LSE in the setting of test-time self-evolution, where a model iteratively refines its context from feedback on seen problems to perform better on new ones. Existing approaches rely entirely on the inherent reasoning ability of the model and never explicitly train it for this task. LSE reduces the multi-step evolution problem to a single-step RL objective, where each context edit is rewarded by the improvement in downstream performance. We pair this objective with a tree-guided evolution loop. On Text-to-SQL generation (BIRD) and general question answering (MMLU-Redux), a 4B-parameter model trained with LSE outperforms self-evolving policies powered by GPT-5 and Claude Sonnet 4.5, as well as prompt optimization methods including GEPA and TextGrad, and transfers to guide other models without additional training. Our results highlight the effectiveness of treating self-evolution as a learnable skill.

---

## 论文详细总结（自动生成）

以下是对论文《Learning to Self-Evolve》（学习自我演化）的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 LLM 在部署后通常是静态的，无法根据测试时的经验进行动态调整。虽然已有研究探索了“测试时自我演化”（通过提示词优化或自我反思），但这些方法完全依赖模型的**固有推理能力**，从未针对“如何改进自身上下文”这一特定任务进行显式训练。
*   **研究动机**：作者认为自我演化本质上是一个强化学习（RL）问题，模型需要具备信用分配、梯度估计和平衡探索与利用的能力。因此，提出通过显式强化学习训练一个专门负责“自我演化”的策略模型，使其能够根据反馈更有效地重写指令。

### 2. 方法论
*   **核心思想**：将复杂的“多步演化”轨迹简化为“单步强化学习”目标。训练一个自我演化策略 $f_\psi$，使其在接收到当前上下文和性能摘要后，生成一个能显著提升下游任务表现的新上下文。
*   **关键技术细节**：
    *   **改进型奖励（Improvement-based Reward）**：不直接使用编辑后的绝对分数作为奖励，而是使用“性能增量” $r_{LSE} = \bar{R}(c_{post}) - \bar{R}(c_{pre})$。这消除了初始提示词质量带来的偏差，迫使模型专注于“改进”动作本身。
    *   **树引导演化（Tree-guided Evolution）**：在测试时，不使用简单的线性链式演化（容易陷入局部最优），而是维护一个演化树。利用 **UCB（置信上限）算法**选择最有潜力的节点进行扩展，允许模型在发现效果不佳时进行回溯。
    *   **训练流程**：使用 Qwen3-4B-Instruct 作为基础模型，通过在训练集上生成的演化轨迹进行离线强化学习（使用 PPO/GRPO 变体）。

### 3. 实验设计
*   **数据集/场景**：
    *   **Text-to-SQL**：使用 **BIRD** 数据集，涵盖金融、毒理学、代码库等 5 个领域。
    *   **通用问答**：使用 **MMLU-Redux**，涵盖商业伦理、哲学、解剖学等 10 个学科。
*   **Benchmark 与对比方法**：
    *   **强模型基准**：使用 GPT-5 和 Claude Sonnet 4.5 作为自我演化策略（驱动相同的 4B 动作模型）。
    *   **提示优化方法**：对比了 **GEPA**（基于反射的进化搜索）和 **TextGrad**（基于文本梯度的优化）。
    *   **动作模型**：统一使用 Qwen3-4B-Instruct 执行具体任务。

### 4. 资源与算力
*   **算力说明**：文中**未明确列出**具体的 GPU 型号和数量。
*   **训练细节**：提到使用了 `verl` 强化学习框架。训练配置为：学习率 $1 \times 10^{-5}$，每批次采样 32 个节点，每个节点生成 4 个 rollout，训练 4 个 epoch。数据生成阶段每个领域运行 200 次，每次 20 轮演化。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 2 大类任务、15 个细分领域。
*   **充分性**：
    *   **消融实验**：对比了改进型奖励（ALSE）与标准 GRPO 奖励的效果，以及树搜索与线性链的差异。
    *   **迁移性测试**：验证了 LSE 训练的模型能否指导未见过的动作模型（如 Arctic-Text2SQL-R1-7B）。
    *   **客观性**：所有方法使用相同的初始提示词和问题序列，确保了对比的公平性。

### 6. 主要结论与发现
*   **小模型逆袭**：经过 LSE 训练的 **4B 参数模型**在自我演化任务上的表现超过了未经过针对性训练的 **GPT-5** 和 **Claude 4.5**。
*   **奖励设计至关重要**：基于“性能提升”的奖励比基于“绝对分数”的奖励能提供更清晰的训练信号，显著提升模型编辑提示词的能力。
*   **树搜索的必要性**：UCB 树搜索能有效防止演化过程中的性能崩溃，比线性演化更稳健。
*   **通用性**：LSE 学习到的是一种通用的“优化策略”，可以跨模型迁移，提升专门针对特定任务微调过的模型的性能。

### 7. 优点
*   **创新性**：首次将自我演化能力定义为一种“可学习的技能”，并通过 RL 显式优化。
*   **高效性**：证明了小模型通过特定任务的 RL 训练，可以在元任务（Meta-task）上超越闭源巨型模型。
*   **实用性**：树搜索机制解决了提示词优化中常见的“越改越烂”且无法回溯的问题。

### 8. 不足与局限
*   **单步假设**：为了降低训练难度，将多步演化简化为单步 RL，可能忽略了长期演化轨迹中的复杂依赖关系。
*   **领域限制**：目前仍需针对特定任务领域（如 SQL 或 QA）分别训练演化策略，尚未实现完全跨任务的通用演化模型。
*   **搜索开销**：树搜索虽然效果好，但在测试时需要多次调用模型和评估性能，增加了推理成本。
*   **编辑范围**：目前仅限于修改指令（Instruction）字段，未涉及工具调用、内存检索等更复杂的上下文组件。

（完）
