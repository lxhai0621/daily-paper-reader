---
title: Efficient and Interpretable Multi-Agent LLM Routing via Ant Colony Optimization
title_zh: 基于蚁群算法的高效且可解释的多智能体大语言模型路由
authors: "Xudong Wang, Chaoning Zhang, Jiaquan Zhang, Chenghao Li, Qigan Sun, Sung-Ho Bae, Peng Wang, Ning Xie, Jie Zou, Yang Yang, Hengtao Shen"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12933v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 多智能体LLM路由与工具使用优化
tldr: 针对多智能体系统（MAS）在推理成本、延迟和透明度方面的挑战，本文提出AMRO-S框架。该框架借鉴蚁群优化思想，利用微调的小模型进行意图推理，并引入任务特定的信息素专家机制来优化路径选择。AMRO-S通过质量门控异步更新机制实现了高效、可解释且低延迟的路由，在多个基准测试中显著提升了质量与成本的平衡。
selection_source: fresh_fetch
motivation: 现有的多智能体路由策略通常依赖昂贵的大模型选择器或静态策略，导致推理成本高、延迟大且缺乏透明度。
method: 提出AMRO-S框架，结合小模型意图推理、任务特定信息素专家机制以及质量门控异步更新，实现语义感知的路径选择。
result: 在五个公开基准测试和高并发压力测试中，AMRO-S在质量与成本的权衡上持续优于强基准模型，并提供了可追溯的路由证据。
conclusion: AMRO-S通过模拟蚁群优化机制，为多智能体系统提供了一种高效、可解释且具备良好扩展性的路由解决方案。
---

## 摘要
大语言模型（LLM）驱动的多智能体系统（MAS）在复杂推理和工具使用方面展现出强大的能力，而异构智能体池进一步拓宽了质量与成本之间的权衡空间。尽管取得了这些进展，但实际部署往往受到高推理成本、延迟和有限透明度的限制，这阻碍了可扩展且高效的路由。现有的路由策略通常依赖于昂贵的基于 LLM 的选择器或静态策略，并且在动态负载和混合意图下对语义感知路由的可控性有限，往往导致性能不稳定和资源利用效率低下。为了解决这些局限性，我们提出了 AMRO-S，一种用于多智能体系统（MAS）的高效且可解释的路由框架。AMRO-S 将 MAS 路由建模为语义条件的路径选择问题，通过三个关键机制增强路由性能：首先，它利用经过有监督微调（SFT）的小语言模型进行意图推理，为每个查询提供低开销的语义接口；其次，它将路由记忆分解为特定任务的信息素专家，减少跨任务干扰并优化混合工作负载下的路径选择；最后，它采用质量门控异步更新机制，将推理与学习解耦，在不增加延迟的情况下优化路由。在五个公共基准测试和高并发压力测试上的广泛实验表明，AMRO-S 在强路由基准之上持续改进了质量与成本的权衡，同时通过结构化的信息素模式提供了可追溯的路由证据。

## Abstract
Large Language Model (LLM)-driven Multi-Agent Systems (MAS) have demonstrated strong capability in complex reasoning and tool use, and heterogeneous agent pools further broaden the quality--cost trade-off space. Despite these advances, real-world deployment is often constrained by high inference cost, latency, and limited transparency, which hinders scalable and efficient routing. Existing routing strategies typically rely on expensive LLM-based selectors or static policies, and offer limited controllability for semantic-aware routing under dynamic loads and mixed intents, often resulting in unstable performance and inefficient resource utilization. To address these limitations, we propose AMRO-S, an efficient and interpretable routing framework for Multi-Agent Systems (MAS). AMRO-S models MAS routing as a semantic-conditioned path selection problem, enhancing routing performance through three key mechanisms: First, it leverages a supervised fine-tuned (SFT) small language model for intent inference, providing a low-overhead semantic interface for each query; second, it decomposes routing memory into task-specific pheromone specialists, reducing cross-task interference and optimizing path selection under mixed workloads; finally, it employs a quality-gated asynchronous update mechanism to decouple inference from learning, optimizing routing without increasing latency. Extensive experiments on five public benchmarks and high-concurrency stress tests demonstrate that AMRO-S consistently improves the quality--cost trade-off over strong routing baselines, while providing traceable routing evidence through structured pheromone patterns.

---

## 论文详细总结（自动生成）

这是一份关于论文《Efficient and Interpretable Multi-Agent LLM Routing via Ant Colony Optimization》（基于蚁群算法的高效且可解释的多智能体大语言模型路由）的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：多智能体系统（MAS）虽然能处理复杂任务，但在实际部署中面临**高推理成本、高延迟、低透明度**以及在动态负载下**路由效率低下**的问题。
*   **研究动机**：现有的路由策略要么依赖昂贵的 LLM 作为选择器（增加开销），要么使用静态规则（缺乏灵活性）。作者旨在寻找一种既能感知语义意图，又能根据系统反馈动态优化，且具备可解释性的轻量级路由方案。

### 2. 方法论：AMRO-S 框架
AMRO-S 将多智能体路由建模为分层有向图上的**语义条件路径选择问题**，核心机制包括：
*   **SLM 语义路由（意图推理）**：使用经过有监督微调（SFT）的小语言模型（如 Llama-3.2-1B）作为前端，将用户查询映射为任务意图分布（如数学、代码、通用），提供低开销的语义接口。
*   **任务特定信息素专家（ACO 启发）**：借鉴蚁群算法（ACO），为每类任务维护独立的信息素矩阵（Pheromone Specialists）。通过“查询条件融合”技术，根据 SLM 输出的意图权重动态合成当前查询的路由偏好，减少跨任务干扰。
*   **质量门控异步更新**：
    *   **推理阶段**：仅执行路径采样和智能体调用，不产生额外延迟。
    *   **学习阶段**：在后台异步进行。引入 LLM-Judge 作为质量门控，仅对高质量的执行轨迹进行信息素强化，实现持续进化。
*   **启发式引导**：结合智能体能力先验、实时负载和响应时间，构建启发式函数，辅助信息素进行路径决策。

### 3. 实验设计
*   **数据集**：涵盖五大公开基准测试：**GSM8K**（小学数学）、**MMLU**（综合知识）、**MATH**（竞赛数学）、**HumanEval**（代码生成）、**MBPP**（Python 编程）。
*   **对比方法（Baselines）**：
    *   **单模型**：GPT-4o-mini, Gemini-1.5-flash 等。
    *   **推理策略**：CoT, ToT, GoT, AoT。
    *   **多智能体框架**：GPTSwarm, AFlow, LLM-Debate。
    *   **路由方法**：RouteLLM, RouterDC, MasRouter。
*   **集成测试**：将 AMRO-S 插入现有的 MAS 框架（如 MacNet, GPTSwarm, HEnRY）验证其通用性。

### 4. 资源与算力
*   **硬件**：实验在一台 **NVIDIA A100 (80GB) GPU** 上运行。
*   **训练时长**：语义路由器的微调（SFT）过程非常高效，通常在 **30 分钟内** 即可完成。
*   **模型规模**：路由器使用 1B-1.5B 规模的小模型，显著降低了运行时的显存和计算占用。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度测试，包括 5 个基准数据集的性能对比、针对 3 个现有框架的适配性实验、详细的消融实验（验证 SFT 和不同路由器背后的影响）。
*   **压力测试**：进行了从 20 到 1000 个并发进程的高并发压力测试，验证系统在极端负载下的稳定性。
*   **客观性**：通过统一推理预算（限制交互轮数和调用次数）和标准化的成本核算规则，确保了对比的公平性。

### 6. 主要结论与发现
*   **性能提升**：AMRO-S 在平均分上比最强的多智能体路由基准（MasRouter）高出 **1.90 分**，在 MATH 和 MBPP 等难题上进步更明显。
*   **效率优势**：在高并发（1000 进程）下实现了 **4.7 倍的加速**，且准确率保持稳定，而传统负载均衡方法（WRR）在此时准确率大幅下降。
*   **成本优化**：在保持或提升准确率的同时，显著降低了 Token 使用成本。
*   **可解释性**：通过可视化信息素热图，清晰展示了系统如何针对不同任务（如代码 vs 数学）自动发现不同的协作拓扑结构。

### 7. 优点与亮点
*   **生物启发式创新**：成功将蚁群算法的群体智能引入 LLM 路由，解决了离散路径优化问题。
*   **解耦设计**：异步更新机制解决了“在线学习导致延迟增加”的痛点。
*   **高可解释性**：信息素矩阵为黑盒 LLM 系统提供了可追踪、结构化的路由证据。
*   **轻量化**：使用 SLM 代替大模型做路由，极具工程落地价值。

### 8. 不足与局限
*   **拓扑限制**：目前模型基于预定义的分层有向图，对于完全动态、无固定阶段的智能体协作模式支持有限。
*   **冷启动依赖**：虽然有异步进化，但初始阶段仍依赖一定量的标注数据或高质量先验进行“离线预热”。
*   **Judge 风险**：质量门控依赖 LLM-Judge 的判断，若 Judge 模型本身存在偏见或错误，可能导致信息素的错误强化。

（完）
