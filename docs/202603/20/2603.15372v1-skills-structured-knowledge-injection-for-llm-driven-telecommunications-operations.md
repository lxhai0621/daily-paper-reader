---
title: "SKILLS: Structured Knowledge Injection for LLM-Driven Telecommunications Operations"
title_zh: SKILLS：面向大语言模型驱动电信运营的结构化知识注入
authors: Ivo Brett
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15372v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: LLM智能体通过真实API接口执行工作流
tldr: 本研究探讨了通用大模型在电信运营自动化中的应用，提出了SKILLS基准框架。该框架包含37个涵盖8个TM Forum API领域的场景，通过模拟API服务器和确定性评估指标，对比了通用代理与注入结构化领域知识（SKILL.md）后的代理表现。实验证明，结构化知识注入显著提升了MiniMax、Nemotron等开源模型的任务执行成功率，为电信领域LLM应用提供了实践指导。
selection_source: fresh_fetch
motivation: 旨在验证通用大模型能否直接通过API可靠执行电信运营流程，还是需要特定的结构化领域知识引导。
method: 提出了SKILLS框架，通过注入包含工作流逻辑和业务规则的SKILL.md文档，在模拟生产环境下对LLM代理进行评估。
result: "实验显示所有测试模型在注入知识后均有显著提升，其中MiniMax M2.5达到了81.1%的最高成功率。"
conclusion: 结构化知识注入是提升LLM代理在复杂电信业务场景中可靠性和执行能力的有效途径。
---

## 摘要
随着电信运营商加速采用人工智能驱动的自动化，一个实际问题仍未解决：通用大语言模型（LLM）智能体能否通过真实的 API 接口可靠地执行电信运营工作流，还是需要结构化的领域指导？我们推出了 SKILLS（面向大语言模型驱动服务生命周期运营的结构化知识注入），这是一个基准测试框架，包含跨越 8 个 TM Forum 开放 API 领域（TMF620、TMF621、TMF622、TMF628、TMF629、TMF637、TMF639、TMF724）的 37 个电信运营场景。每个场景都基于实时模拟 API 服务器，其中包含预置的具有生产代表性的数据、MCP 工具接口，以及结合了响应内容检查、工具调用验证和数据库状态断言的确定性评估准则。我们在两种条件下评估了开源权重模型：基准（具有工具访问权限但无领域指导的通用智能体）和“带技能”（通过编码了工作流逻辑、API 模式和业务规则的可移植 SKILL.md 文档增强的智能体）。在 5 种开源权重模型条件和 185 次场景运行中的结果显示，所有模型的技能都有持续提升。MiniMax M2.5 领先（带技能 81.1%，提升 13.5 个百分点），其次是 Nemotron 120B（78.4%，提升 18.9 个百分点）、GLM-5 Turbo（78.4%，提升 5.4 个百分点）和 Seed 2.0 Lite（75.7%，提升 18.9 个百分点）。

## Abstract
As telecommunications operators accelerate adoption of AI-enabled automation, a practical question remains unresolved: can general-purpose large language model (LLM) agents reliably execute telecom operations workflows through real API interfaces, or do they require structured domain guidance? We introduce SKILLS (Structured Knowledge Injection for LLM-driven Service Lifecycle operations), a benchmark framework comprising 37 telecom operations scenarios spanning 8 TM Forum Open API domains (TMF620, TMF621, TMF622, TMF628, TMF629, TMF637, TMF639, TMF724). Each scenario is grounded in live mock API servers with seeded production-representative data, MCP tool interfaces, and deterministic evaluation rubrics combining response content checks, tool-call verification, and database state assertions. We evaluate open-weight models under two conditions: baseline (generic agent with tool access but no domain guidance) and with-skill (agent augmented with a portable SKILL.md document encoding workflow logic, API patterns, and business rules). Results across 5 open-weight model conditions and 185 scenario-runs show consistent skill lift across all models. MiniMax M2.5 leads (81.1% with-skill, +13.5pp), followed by Nemotron 120B (78.4%, +18.9pp), GLM-5 Turbo (78.4%, +5.4pp), and Seed 2.0 Lite (75.7%, +18.9pp).

---

## 论文详细总结（自动生成）

以下是对论文《SKILLS: Structured Knowledge Injection for LLM-Driven Telecommunications Operations》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：电信运营涉及复杂的标准 API（如 TM Forum Open API）和特定的业务逻辑。通用大语言模型（LLM）虽然具备工具调用能力，但在实际电信工作流中常因 API 选择错误、忽略隐藏依赖或缺乏领域特定决策规则而失败。
*   **核心问题**：通用 LLM 智能体能否仅靠 API 接口可靠执行电信任务？如果不能，结构化的领域知识注入（SKILL）能在多大程度上提升其性能？

### 2. 论文提出的方法论
*   **核心思想**：引入 **SKILLS 框架**，通过向 LLM 智能体注入一个可移植的结构化文档（`SKILL.md`），来指导其执行特定的电信运营任务。
*   **关键技术细节**：
    *   **SKILL.md 设计**：包含先决条件（所需工具）、有序工作流步骤、API 调用模式、业务规则（如 SLA 权重、阈值表）、错误处理指南及输出格式要求。
    *   **三层评估体系**：
        1.  **工具调用验证**：程序化检查是否调用了正确/禁止的工具。
        2.  **响应内容检查**：利用 LLM 裁判（Gemini 2.0 Flash）验证输出信息的准确性。
        3.  **数据库状态断言**：直接检查后端 MongoDB 的状态变化，确保操作产生了正确的副作用（如记录创建或修改）。
    *   **技术栈**：使用模型上下文协议（MCP）作为工具接口，连接到 8 个 TMF 模拟服务器。

### 3. 实验设计
*   **数据集/场景**：构建了 **37 个电信运营场景**，涵盖 8 个 TM Forum 领域（如产品目录、故障票据、性能管理、拓扑分析等）。
*   **复杂度分级**：将场景分为 4 个等级（简单、中等、困难、复杂）。其中“复杂”级任务必须依赖 `SKILL.md` 中的特定逻辑才能完成。
*   **对比方法**：
    *   **Baseline (B)**：通用智能体，仅有工具访问权，无领域指导。
    *   **With-Skill (S)**：在 Baseline 基础上注入 `SKILL.md` 作为系统上下文。
*   **测试模型**：包括 MiniMax M2.5、Nemotron 120B、GLM-5 Turbo、Seed 2.0 Lite、Healer Alpha 等开源/开放权重模型。

### 4. 资源与算力
*   **算力说明**：文中**未明确提及**具体的 GPU 型号、数量或训练时长。
*   **实验环境**：模型通过 OpenRouter API 访问；工具执行环境使用 Vercel 沙箱；TMF 参考实现运行在 Docker 容器中。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 **185 次场景运行**（37 个场景 × 5 种主要模型条件）。
*   **充分性与客观性**：
    *   实验覆盖了电信运营的多个核心领域，具有较好的代表性。
    *   引入了数据库状态断言，比单纯的文本评估更客观、严谨。
    *   **局限性**：每个场景仅运行了一次，缺乏多次运行的方差分析；部分 TMF 领域（如 TMF629 和 TMF724）的场景占比过高，分布不均。

### 6. 论文的主要结论与发现
*   **性能提升**：所有模型在注入技能后均表现出显著提升（整体提升 5-19 个百分点，复杂场景提升达 33-44 个百分点）。
*   **模型表现**：**MiniMax M2.5** 在“带技能”条件下表现最佳（81.1% 成功率），打破了其他模型约 78% 的瓶颈。
*   **沙箱歧视失败（Sandbox Discrimination Failure）**：发现重推理模型（如 Nemotron）倾向于为简单的 API 检索任务创建复杂的临时沙箱环境，导致 97% 的工具调用消耗在基础设施搭建上，而非实际业务逻辑，最终因超时失败。
*   **基准提升压缩效应**：原生能力越强的模型，整体技能提升幅度越小，但在复杂任务上依然高度依赖技能注入。

### 7. 优点
*   **实战导向**：不仅评估文本生成，还通过真实的 API 调用和数据库状态验证操作的正确性。
*   **发现新模式**：识别并命名了“沙箱歧视失败”这一推理模型在智能体场景下的典型反模式。
*   **可移植性**：提出的 `SKILL.md` 采用自然语言编写，不依赖特定平台代码，易于在不同模型间迁移。

### 8. 不足与局限
*   **样本偏差**：场景在不同 TMF 领域的分布不平衡。
*   **评估风险**：响应内容检查依赖单一的 LLM 裁判，可能存在偏见。
*   **基础设施干扰**：推理速度慢的模型受沙箱超时等基础设施限制的影响较大，难以完全区分是“模型能力不足”还是“环境限制”。
*   **迭代偏差**：技能文档是根据场景结果迭代优化的，可能存在过拟合特定测试用例的风险。

（完）
