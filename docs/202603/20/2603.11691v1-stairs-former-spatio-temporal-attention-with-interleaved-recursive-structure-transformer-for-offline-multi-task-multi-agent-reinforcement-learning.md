---
title: "STAIRS-Former: Spatio-Temporal Attention with Interleaved Recursive Structure Transformer for Offline Multi-task Multi-agent Reinforcement Learning"
title_zh: STAIRS-Former：用于离线多任务多智能体强化学习的交错递归结构时空注意力 Transformer
authors: "Jiwon Jeon, Myungsik Cho, Youngchul Sung"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11691v1"
tags: ["query:agent"]
score: 9.0
evidence: 具有时空注意力的离线多智能体强化学习
tldr: 针对离线多任务多智能体强化学习中智能体数量多变及长时序依赖建模难的问题，本文提出STAIRS-Former。该模型通过交错递归结构增强了时空注意力机制，能有效捕捉智能体间的空间协作与长程时间依赖。此外，引入令牌丢弃技术提升了对不同智能体规模的泛化性。在SMAC-v2等多个基准测试中，该方法显著超越了现有技术，达到了新的SOTA水平。
selection_source: fresh_fetch
motivation: 现有的离线多智能体强化学习方法在处理变动智能体数量和长时序依赖时，存在注意力机制利用不足及历史信息捕捉受限的问题。
method: 提出一种具有时空层级和交错递归结构的Transformer架构，并结合令牌丢弃技术以增强模型的鲁棒性与泛化能力。
result: 在SMAC、SMAC-v2、MPE和MaMuJoCo等多个多智能体基准测试中，STAIRS-Former均取得了优于现有方法的性能表现。
conclusion: STAIRS-Former通过优化的时空注意力结构，为解决复杂离线多任务多智能体强化学习问题提供了一个高效且泛化性强的解决方案。
---

## 摘要
由于不同任务中智能体数量的变化以及对未知场景泛化的需求，利用多任务数据集进行离线多智能体强化学习（MARL）具有挑战性。先前的研究采用具有观测标记化（observation tokenization）和分层技能学习的 Transformer 来解决这些问题。然而，它们未能充分利用 Transformer 的注意力机制进行智能体间的协作，且依赖于单一的历史标记（history token），这限制了它们在部分可观测 MARL 环境中捕捉长时程时间依赖关系的能力。在本文中，我们提出了 STAIRS-Former，这是一种增强了空间和时间层级的 Transformer 架构，能够在捕捉长交互历史的同时，实现对关键标记的有效关注。我们进一步引入了标记丢弃（token dropout），以增强在不同智能体群体规模下的鲁棒性和泛化能力。在包括 SMAC、SMAC-v2、MPE 和 MaMuJoCo 在内的多种多智能体基准测试及多任务数据集上的广泛实验表明，STAIRS-Former 一贯优于先前的方法，并达到了新的最先进性能。

## Abstract
Offline multi-agent reinforcement learning (MARL) with multi-task datasets is challenging due to varying numbers of agents across tasks and the need to generalize to unseen scenarios. Prior works employ transformers with observation tokenization and hierarchical skill learning to address these issues. However, they underutilize the transformer attention mechanism for inter-agent coordination and rely on a single history token, which limits their ability to capture long-horizon temporal dependencies in partially observable MARL settings. In this paper, we propose STAIRS-Former, a transformer architecture augmented with spatial and temporal hierarchies that enables effective attention over critical tokens while capturing long interaction histories. We further introduce token dropout to enhance robustness and generalization across varying agent populations. Extensive experiments on diverse multi-agent benchmarks, including SMAC, SMAC-v2, MPE, and MaMuJoCo, with multi-task datasets demonstrate that STAIRS-Former consistently outperforms prior methods and achieves new state-of-the-art performance.

---

## 论文详细总结（自动生成）

以下是对论文《STAIRS-Former: Spatio-Temporal Attention with Interleaved Recursive Structure Transformer for Offline Multi-task Multi-agent Reinforcement Learning》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：在离线多任务多智能体强化学习（MT-MARL）中，如何处理**变动的智能体数量**、**部分可观测性下的长程依赖**以及**对未知任务的泛化能力**。
*   **研究背景**：现有的基于 Transformer 的方法（如 UPDeT、ODIS、HiSSD）虽然能处理变动的观测维度，但存在两个主要缺陷：
    1.  **空间维度利用不足**：通常使用浅层 Transformer，导致注意力分布趋于均匀，无法有效捕捉智能体间的复杂协作关系。
    2.  **时间维度建模薄弱**：仅依赖单一的历史标记（History Token），本质上是简单的 RNN 处理，难以捕捉长时序依赖。

### 2. 方法论：STAIRS-Former
该模型通过交错递归结构增强了 Transformer 的时空建模能力，核心组件包括：
*   **空间递归模块 (Spatial Recursive Module)**：采用递归深度 Transformer 结构。通过在每一层进行多次递归更新（Recursive Steps），在不显著增加参数量的前提下，深化模型对实体间关系的推理能力。
*   **层级时间模块 (Temporal Module)**：引入双时间尺度历史状态：
    *   **低级历史 ($h_L$)**：每步更新，捕捉瞬时动态。
    *   **高级历史 ($h_H$)**：通过 GRU 每隔 $T_H$ 个时间步更新一次，捕捉长程抽象信息。
    *   **时间焦点层 (TFL)**：在 Transformer 内部为空间实体标记和时间历史标记分别设置独立的 FFN 路径，防止特征干扰。
*   **令牌丢弃机制 (Token-Dropout)**：在训练期间随机丢弃部分实体令牌（如队友或敌人信息），强制模型不依赖特定数量的输入，从而提升对不同规模智能体群体的泛化鲁棒性。
*   **训练目标**：采用适配离散动作空间的 TD3+BC 损失函数，结合 Qatten 混合网络进行全局价值分解。

### 3. 实验设计
*   **实验场景**：
    *   **SMAC (StarCraft II)**：包括 Marine-Easy, Marine-Hard, Stalker-Zealot 三个任务集。
    *   **SMAC-v2**：更具随机性和挑战性的版本（Terran, Protoss, Zerg 种族）。
    *   **MPE**：协作导航任务。
    *   **MaMuJoCo**：多智能体连续控制任务（HalfCheetah）。
*   **数据集质量**：涵盖 Expert, Medium, Medium-Expert, Medium-Replay 四种质量。
*   **对比方法 (Baselines)**：
    *   **UPDeT-m**：基础 Transformer 离线变体。
    *   **ODIS**：学习任务无关协作技能的方法。
    *   **HiSSD**：当前最先进的（SOTA）层级技能发现方法。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX 4090 GPU** (24GB 显存)。
*   **训练时长**：在相同设置下，STAIRS-Former 训练 50K 步约需 **4 小时**；相比之下，SOTA 方法 HiSSD 需 7 小时 20 分钟。
*   **参数量**：STAIRS-Former 约为 **22 万**参数，远低于 HiSSD 的 68 万参数，但在性能上更优。

### 5. 实验数量与充分性
*   **实验规模**：非常充分。论文在 4 个大类基准测试、数十个子任务（包括已见任务 Seen 和未见任务 Unseen）上进行了测试。
*   **消融实验**：针对空间递归、时间层级、令牌丢弃、TFL 模块分别做了消融，验证了各组件的贡献。
*   **客观性**：所有实验均采用 5 个随机种子取平均值，并提供了详细的注意力图可视化、休眠神经元分析（Dormant Neuron Analysis）以及针对 Transformer 深度和注意力平滑度的对比实验，论证严谨。

### 6. 主要结论
*   **性能领先**：STAIRS-Former 在几乎所有 SMAC 和 SMAC-v2 任务中均刷新了 SOTA 记录，尤其在**未见任务**上的泛化表现显著优于 HiSSD。
*   **泛化性强**：在 Stalker-Zealot 等复杂异构任务中，平均胜率比 HiSSD 提升了约 48.6%。
*   **时空协同**：实验证明，空间层级对已见任务至关重要，而时间层级和令牌丢弃对未见任务的泛化起到了决定性作用。

### 7. 优点与亮点
*   **架构创新**：通过递归结构和双尺度时间建模，解决了 Transformer 在 MARL 中“注意力均匀化”和“长程记忆缺失”的顽疾。
*   **高效性**：以更少的参数量和更短的训练时间，实现了更高的性能，具有极高的实用价值。
*   **可解释性**：通过动态注意力图展示了模型在战斗中如何自动学习到“集火（Focus Fire）”和“拉扯（Kiting）”等高级战术。

### 8. 不足与局限
*   **离线数据依赖**：尽管引入了 BC 正则化，但模型性能仍受限于离线数据集的覆盖范围和质量（如在 Marine-Easy 的 Medium-Replay 数据集上表现略逊）。
*   **超参数敏感性**：递归步数 $\nu$ 和时间间隔 $T_H$ 需要根据任务特性进行微调，虽然论文给出了通用配置，但在极端不同的环境（如极长时程任务）中可能需要重新搜索。
*   **应用限制**：目前主要在离散动作空间验证，虽然在 MaMuJoCo 进行了连续空间尝试，但其在超大规模智能体（如数百个）场景下的计算开销仍有待进一步评估。

（完）
