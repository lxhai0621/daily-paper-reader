---
title: "Scribe Verification in Chinese manuscripts using Siamese, Triplet, and Vision Transformer Neural Networks"
title_zh: 基于孪生网络、三元组网络和视觉 Transformer 神经网络的中文手稿抄写员验证
authors: "Dimitrios-Chrysovalantis Liakopoulos, Yanbo Zhang, Chongsheng Zhang, Constantine Kotropoulos"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13877v1"
tags: ["query:ancient-text"]
score: 9.0
evidence: 中国古代手稿的数字化处理与抄写员验证
tldr: 本研究探讨了利用深度学习模型对中国古代手稿进行书手鉴定，即自动判断两个手稿片段是否出自同一人之手。研究采用了清华简数据集和多属性中国书法数据集，实现了基于卷积神经网络和Transformer的孪生及三元组架构。实验证明，采用对比损失训练的MobileNetV3+自定义孪生模型在准确率和AUC指标上表现最优，为古籍数字化和真伪鉴定提供了有效技术手段。
selection_source: fresh_fetch
motivation: 旨在通过深度度量学习方法自动识别中国古代手稿的抄写者身份，以辅助文物鉴定和历史研究。
method: 比较了基于CNN和Vision Transformer的孪生及三元组神经网络架构，并结合对比损失进行训练。
result: 在清华简和书法数据集上，MobileNetV3+自定义孪生模型在准确率和ROC曲线下面积上均取得了领先结果。
conclusion: 深度度量学习模型，特别是轻量级卷积网络结合孪生架构，能有效实现中国手稿的书手自动验证。
---

## 摘要
本文研究了用于中文手稿抄写员验证的深度学习模型。具体而言，即利用深度度量学习方法自动判定两个手稿片段是否出自同一抄写员之手。研究采用了两个数据集：清华简数据集以及多属性中国书法数据集的精选子集（侧重于样本量较多的书法家）。文中实现了孪生（Siamese）和三元组（Triplet）神经网络架构，涵盖了基于卷积和 Transformer 的模型。实验结果表明，采用对比损失训练的 MobileNetV3+ 自定义孪生模型在两个数据集上均取得了最佳或次佳的总体准确率和受试者工作特征曲线（ROC）下面积。

## Abstract
The paper examines deep learning models for scribe verification in Chinese manuscripts. That is, to automatically determine whether two manuscript fragments were written by the same scribe using deep metric learning methods. Two datasets were used: the Tsinghua Bamboo Slips Dataset and a selected subset of the Multi-Attribute Chinese Calligraphy Dataset, focusing on the calligraphers with a large number of samples. Siamese and Triplet neural network architectures are implemented, including convolutional and Transformer-based models. The experimental results show that the MobileNetV3+ Custom Siamese model trained with contrastive loss achieves either the best or the second-best overall accuracy and area under the Receiver Operating Characteristic Curve on both datasets.

---

## 论文详细总结（自动生成）

这篇论文针对中国古代及现代手稿的“书手鉴定”（Scribe Verification）任务，提出并对比了多种深度度量学习模型。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何自动判定两段中文手稿片段是否由同一位抄写员（或书法家）所书写。
*   **研究动机**：古代手稿（如竹简）由于年代久远，存在图像噪声大、样本稀缺、墨迹褪色及材质受损等挑战。传统的识别方法难以处理这些复杂情况。
*   **研究意义**：在数字人文领域，书手鉴定对于古籍的保护、分类、真伪鉴别以及手稿片段的归组具有重要意义。

### 2. 核心方法论
论文的核心思想是将手稿图像映射到一个低维嵌入空间（Embedding Space），使得同一抄写员的作品在空间中距离较近，不同抄写员的作品距离较远。
*   **关键技术细节**：
    *   **架构选择**：实现了三种架构：**孪生网络（Siamese）**、**三元组网络（Triplet）**和**视觉 Transformer（ViT）**。
    *   **特征提取器（Backbones）**：对比了 MobileNetV3、VGG19、ResNet（18/34/50）以及一种改进的 **MobileNetV3+ Custom**（将末端替换为160通道的瓶颈层和10维嵌入层）。
    *   **损失函数**：
        *   **对比损失（Contrastive Loss）**：用于孪生网络，通过欧氏距离衡量成对样本的相似度。
        *   **三元组损失（Triplet Loss）**：用于三元组网络，同时处理锚点、正样本和负样本，优化相对距离。
    *   **嵌入维度**：所有模型最终均输出一个 **10维** 的特征向量。

### 3. 实验设计
*   **数据集**：
    1.  **清华简数据集（Tsinghua Bamboo Slips）**：代表古代手稿，包含11位抄写员，训练集12,595张，测试集3,050张。
    2.  **多属性中国书法数据集（MCCD）子集**：代表现代书法，选取了8位样本量最大的书法家，训练集9,852张，测试集4,187张。
*   **对比方法**：在统一的预处理（224x224, RGB, 归一化）和动态采样策略下，对比了上述提到的所有 CNN 变体和 ViT 模型。
*   **评估指标**：ROC 曲线、AUC（曲线下面积）、准确率（ACC）、误报率（FAR）和漏报率（FRR）。

### 4. 资源与算力
*   **硬件环境**：使用单台笔记本电脑进行训练，配置为 **NVIDIA GeForce RTX 4060 Laptop GPU (8GB VRAM)**，处理器为 Intel Core i7-13620H，内存 32GB。
*   **训练细节**：使用 PyTorch 框架，Adam 优化器，学习率 $1 \times 10^{-3}$，Batch Size 为 32，共训练 30 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个不同性质的数据集（古籍 vs 现代书法）上测试了约 8 种不同的模型架构组合。
*   **充分性与客观性**：
    *   实验涵盖了从轻量级（MobileNet）到深层（ResNet50）再到注意力机制（ViT）的多种主流架构。
    *   采用了固定随机种子（42）和统一的测试对（test_pairs.csv），确保了不同模型间对比的公平性。
    *   引入了动态采样机制，增加了训练样本的多样性。

### 6. 主要结论与发现
*   **最佳模型**：**MobileNetV3+ Custom 孪生网络**在清华简数据集上表现最好（AUC 0.958，ACC 89.19%）；**ResNet34 孪生网络**在 MCCD 数据集上表现最佳（AUC 0.952，ACC 89.06%）。
*   **架构权衡**：中等深度的卷积网络（如改进的 MobileNetV3 和 ResNet34）在处理带有噪声和纹理特征的手稿时，比极深的网络（ResNet50）或 ViT 更具鲁棒性。
*   **损失函数**：在当前任务下，对比损失（Contrastive Loss）比三元组损失（Triplet Loss）表现更稳定且收敛更快。
*   **ViT 表现**：尽管 ViT 具有全局注意力，但在样本量相对有限且依赖局部笔画特征的手稿鉴定任务中，其表现略逊于 CNN。

### 7. 优点
*   **统一框架**：建立了一个可复现的、基于 PyTorch 的书手鉴定基准框架。
*   **针对性改进**：对 MobileNetV3 进行了结构微调（Custom 瓶颈层），使其更适合度量学习任务。
*   **跨时代验证**：同时在古代竹简和现代书法数据集上验证，证明了方法的通用性。

### 8. 不足与局限
*   **嵌入维度较低**：仅使用了 10 维嵌入，虽然符合对比基准，但可能限制了模型捕捉更复杂书法风格的能力。
*   **数据集局限**：所用数据集并非完全公开（需申请），且主要集中在中文手稿，未测试其他语种。
*   **超参数敏感性**：度量学习对 Margin（边界值）等超参数较为敏感，文中未深入探讨不同超参数对结果的影响。
*   **算力限制**：实验在移动端 GPU 上完成，未在大规模集群上进行更深层次的超参搜索。

（完）
