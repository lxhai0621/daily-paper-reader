---
title: Prototype-Based Knowledge Guidance for Fine-Grained Structured Radiology Reporting
title_zh: 基于原型知识引导的细粒度结构化放射学报告生成
authors: "Chantal Pellegrini, Adrian Delchev, Ege Özsoy, Nassir Navab, Matthias Keicher"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11938v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 将自由文本信息注入结构化报告生成
tldr: 针对结构化放射报告自动化中细粒度决策难和标注数据稀缺的问题，本文提出 ProtoSR 方法。该方法利用指令微调的大模型从 8 万余份自由文本报告中挖掘知识，构建与结构化模板对齐的多模态原型知识库。在推理时，模型通过检索相关视觉原型并利用残差机制修正预测。实验表明，ProtoSR 在 Rad-ReStruct 基准上取得 SOTA 性能，显著提升了对罕见发现和详细属性的理解能力。
selection_source: fresh_fetch
motivation: 结构化放射报告自动化面临细粒度决策难和结构化标注数据稀缺的挑战，而海量自由文本报告蕴含丰富的图像关联信息。
method: 提出 ProtoSR 框架，利用大语言模型从自由文本中挖掘知识构建多模态原型库，并通过原型检索和残差学习增强模型预测。
result: 在 Rad-ReStruct 基准测试中达到最先进水平，特别是在详细属性问题的预测上提升显著。
conclusion: 证明了将自由文本中提取的信号整合到结构化报告生成中，能有效提升模型对细粒度医学图像的理解能力。
---

## 摘要
结构化放射学报告相比自由文本有望实现更快、更一致的沟通，但其自动化仍然具有挑战性，因为模型必须在有限的结构化监督下，针对罕见发现和属性做出许多细粒度的离散决策。相比之下，自由文本报告在常规诊疗中大规模生成，并通过详细描述隐式地编码了与图像关联的细粒度信息。为了利用这种非结构化知识，我们提出了 ProtoSR，一种将自由文本信息注入结构化报告填充的方法。首先，我们引入了一个自动提取流水线，利用经过指令微调的大语言模型（LLM）挖掘超过 8 万份 MIMIC-CXR 研究，并构建了一个与结构化报告模板对齐的多模态知识库，其中每个备选答案都由一个视觉原型表示。利用该知识库，ProtoSR 经过训练可以检索与当前“图像-问题”对相关的原型，并通过原型条件残差增强模型预测，从而提供一种能够选择性纠正预测的数据驱动型“第二意见”。在 Rad-ReStruct 基准测试中，ProtoSR 取得了当前最先进的结果，在详细属性问题上的提升最为显著，证明了整合源自自由文本的信号对于细粒度图像理解的价值。

## Abstract
Structured radiology reporting promises faster, more consistent communication than free text, but automation remains difficult as models must make many fine-grained, discrete decisions about rare findings and attributes from limited structured supervision. In contrast, free-text reports are produced at scale in routine care and implicitly encode fine-grained, image-linked information through detailed descriptions. To leverage this unstructured knowledge, we propose ProtoSR, an approach for injecting free-text information into structured report population. First, we introduce an automatic extraction pipeline that uses an instruction-tuned LLM to mine 80k+ MIMIC-CXR studies and build a multimodal knowledge base aligned with a structured reporting template, representing each answer option with a visual prototype. Using this knowledge base, ProtoSR is trained to retrieve prototypes relevant for the current image-question pair and augment the model predictions through a prototype-conditioned residual, providing a data-driven second opinion that selectively corrects predictions. On the Rad-ReStruct benchmark, ProtoSR achieves state-of-the-art results, with the largest improvements on detailed attribute questions, demonstrating the value of integrating free-text derived signal for fine-grained image understanding.

---

## 论文详细总结（自动生成）

### 论文总结：基于原型知识引导的细粒度结构化放射学报告生成 (ProtoSR)

#### 1. 核心问题与整体含义（研究动机和背景）
放射学报告的**结构化（Structured Reporting, SR）**相比传统的自由文本具有更高的一致性和临床价值，但其自动化面临两大挑战：
*   **决策粒度极细**：模型需要针对罕见病变、位置、严重程度等属性做出大量离散选择。
*   **监督数据稀缺**：高质量的结构化标注数据集规模较小，难以覆盖长尾分布的罕见属性。
与此同时，临床中存在海量的**自由文本报告**，其中蕴含了丰富的图像关联信息。本文的研究动机在于：**如何系统性地挖掘大规模自由文本报告中的非结构化知识，并将其转化为能够引导模型进行细粒度结构化预测的信号？**

#### 2. 论文提出的方法论
论文提出了 **ProtoSR** 框架，其核心思想是构建一个与结构化模板对齐的“原型知识库”，并在推理时通过检索机制为模型提供“第二意见”。

*   **知识库构建（LLM 驱动的挖掘）**：
    1.  **术语扩展**：利用 LLM 生成标签的同义词和缩写，增强对自由文本多样性的覆盖。
    2.  **模板约束提取**：使用指令微调的 LLM（如 Qwen2.5-7B）从 MIMIC-CXR 报告中提取符合 Rad-ReStruct 模板的标签。
    3.  **原型生成**：将具有相同标签的图像特征通过元素级最大池化（Max Pooling）聚合，形成代表该选项的“视觉原型”。
*   **知识增强的后期融合架构**：
    1.  **基础模型**：采用分层 VQA 架构，结合图像编码器（EfficientNet）和文本编码器（RadBERT）。
    2.  **原型检索分支**：计算当前图像特征与知识库中原型的余弦相似度，检索相关证据。
    3.  **残差修正**：将检索到的视觉证据和标签倾向性输入 MLP，生成一个**残差 Logit 修正值**。最终预测结果为：`基础模型输出 + 缩放后的残差修正`。这种设计允许模型在保持基础判断的同时，利用外部知识对细粒度属性进行针对性纠偏。

#### 3. 实验设计
*   **数据集**：
    *   **Rad-ReStruct**：目标基准数据集（3,597 份研究），包含三层结构的细粒度标签（L1 存在性、L2 具体发现、L3 详细属性）。
    *   **MIMIC-CXR**：外部知识源（80,000+ 份研究），用于挖掘自由文本并构建原型库。
*   **Benchmark 与对比方法**：
    *   **通用医疗 VLLM**：MedGemma, CheXagent, RaDialog。
    *   **专用结构化报告模型**：hi-VQA, Context-VQA。
*   **评估指标**：分层 F1 分数（L1, L2, L3）、整体 F1 分数、报告级准确率（Report Acc）。

#### 4. 资源与算力
*   **硬件**：使用了一块 **Nvidia RTX 3090 GPU (24 GB)**。
*   **训练细节**：训练 34 个 epoch，Adam 优化器，学习率 1e-5，Batch Size 为 8，梯度累加为 4。
*   **知识库更新**：每 10k 步更新一次原型库，以保持与微调中的图像编码器同步。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的评估，包括不同 LLM 后端（Llama 3.1, Mistral, Qwen2.5）的提取效果对比、知识库覆盖率分析、以及在 Rad-ReStruct 上的性能测试。
*   **消融实验**：
    *   验证了**术语扩展**的必要性（显著提升提取质量）。
    *   对比了**早期融合 vs. 后期融合**（证明后期融合残差修正更有效）。
    *   进行了**随机原型测试**（证明提升源于知识内容而非模型容量增加）。
*   **公平性**：遵循了 Rad-ReStruct 的官方划分和评估协议，对比了当前最先进的通用和专用模型，实验设计较为客观、公平。

#### 6. 论文的主要结论与发现
*   **SOTA 性能**：ProtoSR 在所有层级上均优于现有方法，尤其在 **L3（细粒度属性）** 上提升最为显著（相对提升达 72.1%）。
*   **知识迁移的有效性**：证明了通过 LLM 挖掘的自由文本知识可以有效弥补结构化标注数据的不足，特别是对于长尾分布的罕见属性。
*   **架构优势**：后期融合的残差设计能够有效整合外部证据，而不会干扰基础模型的学习稳定性。

#### 7. 优点
*   **创新的知识注入方式**：不直接生成文本，而是将自由文本转化为视觉原型，通过 Logit 空间的残差修正来引导离散决策。
*   **轻量化增强**：知识分支非常轻量（仅 MLP 和缩放向量），易于集成到现有 VQA 架构中。
*   **临床实用性**：通过术语扩展解决了放射学描述中严重的“同义异名”问题，提高了挖掘的鲁棒性。

#### 8. 不足与局限
*   **对 LLM 提取质量的依赖**：知识库的质量高度依赖于 LLM 提取标签的准确性，虽然有过滤机制，但仍可能引入噪声。
*   **模态局限性**：目前仅在胸部 X 光（CXR）上验证，尚未在 CT、MRI 等更复杂的 3D 模态或多部位检查中测试。
*   **原型更新开销**：虽然每 10k 步更新一次，但在超大规模数据集上，频繁重新计算原型特征可能会带来额外的计算负担。

（完）
