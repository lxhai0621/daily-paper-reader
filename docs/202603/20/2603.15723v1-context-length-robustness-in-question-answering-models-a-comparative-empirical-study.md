---
title: "Context-Length Robustness in Question Answering Models: A Comparative Empirical Study"
title_zh: 问答模型中的上下文长度鲁棒性：一项比较实证研究
authors: "Trishita Dhara, Siddhesh Sheth"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15723v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 问答模型中上下文长度鲁棒性的实证研究
tldr: 本研究针对大语言模型在长且多噪声上下文中的鲁棒性进行了实证研究。通过在SQuAD和HotpotQA基准上系统性增加无关上下文，研究发现模型性能随长度增加而下降，且多跳推理任务（HotpotQA）的准确率下降幅度几乎是单跨度提取任务（SQuAD）的两倍。该研究揭示了任务类型对上下文鲁棒性的显著影响，强调了在长文档应用中评估此类鲁棒性的重要性。
selection_source: fresh_fetch
motivation: 旨在探究大语言模型在处理包含大量无关信息的长上下文时，其问答性能的鲁棒性及任务依赖性。
method: 通过在保持答案相关信号不变的情况下，系统性地增加无关上下文长度，对比评估模型在SQuAD和HotpotQA上的表现。
result: 随着上下文长度增加，模型性能普遍下降，且多跳推理任务的准确率降幅约为单跨度提取任务的两倍。
conclusion: 多跳推理对上下文稀释更为敏感，建议在评估长文档或检索增强生成应用的模型可靠性时，应明确考察上下文长度鲁棒性。
---

## 摘要
大语言模型正越来越多地应用于相关信息嵌入在长且多噪声上下文的环境中。尽管如此，在不同的问答任务中，模型对不断增长的上下文长度的鲁棒性仍未得到充分理解。在这项工作中，我们利用两个广泛使用的基准测试集（SQuAD 和 HotpotQA），对大语言模型的上下文长度鲁棒性进行了一项受控实证研究。我们通过系统地增加无关上下文的数量，同时保留包含答案的信号，评估了模型准确率随总上下文长度变化的情况。这使我们能够将上下文长度的影响与任务难度的变化隔离开来。我们的结果显示，随着上下文长度的增加，性能会出现一致的下降，且与单跨度抽取任务相比，多跳推理任务的下降幅度明显更大。特别是，在同等的上下文扩展下，HotpotQA 的准确率下降几乎是 SQuAD 的两倍。这些发现突显了鲁棒性在不同任务间的差异，并表明多跳推理特别容易受到上下文稀释的影响。我们认为，在评估模型可靠性时，应明确评估上下文长度的鲁棒性，特别是对于涉及长文档或检索增强生成的应用。

## Abstract
Large language models are increasingly deployed in settings where relevant information is embedded within long and noisy contexts. Despite this, robustness to growing context length remains poorly understood across different question answering tasks. In this work, we present a controlled empirical study of context-length robustness in large language models using two widely used benchmarks: SQuAD and HotpotQA.   We evaluate model accuracy as a function of total context length by systematically increasing the amount of irrelevant context while preserving the answer-bearing signal. This allows us to isolate the effect of context length from changes in task difficulty. Our results show a consistent degradation in performance as context length increases, with substantially larger drops observed on multi-hop reasoning tasks compared to single-span extraction tasks. In particular, HotpotQA exhibits nearly twice the accuracy degradation of SQuAD under equivalent context expansions.   These findings highlight task-dependent differences in robustness and suggest that multi-hop reasoning is especially vulnerable to context dilution. We argue that context-length robustness should be evaluated explicitly when assessing model reliability, especially for applications involving long documents or retrieval-augmented generation.

---

## 论文详细总结（自动生成）

以下是对论文《Context-Length Robustness in Question Answering Models: A Comparative Empirical Study》的结构化总结：

### 1. 研究动机与核心问题
*   **研究背景**：大语言模型（LLM）常被部署在检索增强生成（RAG）或长文档分析等场景中，这些场景要求模型从包含大量无关信息的长上下文中提取关键信息。
*   **核心问题**：尽管模型支持的上下文窗口在扩大，但其对“上下文稀释”（即在保持答案信号不变的情况下增加无关干扰信息）的鲁棒性仍不明确。
*   **研究目的**：通过受控实验，量化分析上下文长度增加对不同类型问答任务（单步提取 vs. 多步推理）性能的具体影响。

### 2. 方法论
*   **核心思想**：采用“信号+干扰”的构造方式，隔离上下文长度这一变量，确保任务本身的难度（答案所需证据）在不同长度下保持恒定。
*   **关键技术细节**：
    *   **信号段（Signal Segment）**：从原始上下文中提取包含答案的 256 标记（tokens）窗口。
    *   **干扰段（Distractor Segments）**：从同数据集的其他实例中采样 128 标记的无关文本块。
    *   **构造流程**：将信号段固定，通过在前面拼接不同数量的干扰段，将总上下文长度分别扩展至 256、512、1024 和 2048 标记。
*   **数学建模**：
    *   定义准确率 $Acc_M(L)$ 为长度 $L$ 的函数。
    *   提出**信噪比视角**：假设 Transformer 的注意力分配与标记占比成正比，信号质量随 $1/L$ 衰减。对于需要 $k$ 步推理的任务，成功概率按 $(1/L)^k$ 速度更大幅度地下降。

### 3. 实验设计
*   **数据集**：
    *   **SQuAD**：代表单跨度答案提取任务（局部证据）。
    *   **HotpotQA**：代表多跳推理任务（跨文档证据整合）。
*   **评估模型**：GPT-4.1 和 GPT-4.1-mini（代表不同参数规模的模型）。
*   **对比基准**：以 256 标记（纯信号或极少干扰）作为基准性能，对比在 512、1024、2048 标记下的性能跌幅。
*   **评估指标**：精确匹配（Exact Match, EM）。

### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 型号或训练时长。
*   **实现细节**：实验基于 API 调用进行推理评估。作者提到为了应对速率限制，实施了请求延迟和指数退避策略，并对所有查询结果进行了缓存以确保一致性。

### 5. 实验充分性
*   **实验规模**：每个数据集随机抽取 200 个验证实例，总计进行了 16 组核心配置实验（2 模型 × 2 任务 × 4 长度）。
*   **客观性与公平性**：
    *   使用了 95% 自举置信区间（Bootstrap Confidence Intervals）来验证结果的统计显著性。
    *   所有模型使用相同的提示词（Prompt）和确定性解码设置（Temperature=0）。
    *   **局限性**：200 个样本的规模相对较小，且仅覆盖了 GPT-4 系列模型，实验的广度有待进一步加强。

### 6. 主要结论与发现
*   **性能普遍下降**：随着无关上下文增加，所有模型的准确率均呈下降趋势，即使答案信息完全保留在文中。
*   **任务依赖性显著**：
    *   **多跳推理（HotpotQA）极其脆弱**：在 2048 长度下，准确率下降高达 24.5% - 27.0%。
    *   **单步提取（SQuAD）相对鲁棒**：同等条件下准确率仅下降 2.5% - 6.5%。
*   **规模效应有限**：虽然大模型（GPT-4.1）的绝对准确率更高，但其性能随长度增加而退化的模式与小模型（GPT-4.1-mini）高度相似，说明增加参数量并不能消除上下文长度带来的鲁棒性问题。

### 7. 优点与亮点
*   **变量控制严谨**：通过固定“信号窗口”的方法，成功排除了任务难度变化对实验结果的干扰。
*   **理论解释简洁**：提出的信噪比衰减公式为“为什么多跳推理更容易受长上下文干扰”提供了一个直观的机械论解释。
*   **实用性强**：研究结果直接警示了 RAG 系统开发者，单纯增加检索上下文长度可能会严重损害复杂推理任务的可靠性。

### 8. 不足与局限
*   **长度覆盖有限**：实验最高仅测试到 2048 标记，而现代模型已支持 128k 甚至更长的上下文，未探索极长上下文下的表现。
*   **模型多样性不足**：仅测试了闭源的 GPT 系列模型，未包含 Llama 或 Mistral 等开源模型。
*   **噪声类型单一**：干扰信息仅为随机无关文本，未考虑具有误导性或矛盾信息的对抗性噪声。
*   **指标单一**：仅使用 EM 指标，可能无法捕捉到模型在长文本下产生的“幻觉”或部分正确的推理过程。

（完）
