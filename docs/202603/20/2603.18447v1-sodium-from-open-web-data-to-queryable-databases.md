---
title: "SODIUM: From Open Web Data to Queryable Databases"
title_zh: SODIUM：从开放网络数据到可查询数据库
authors: "Chuxuan Hu, Philip Li, Maxwell Yang, Daniel Kang"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18447v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 从网络数据中自动发现知识并实例化数据库
tldr: 本研究针对领域专家在从开放网络获取结构化数据进行分析时面临的繁琐过程，提出了SODIUM任务，旨在将网络视为潜在数据库并进行系统实例化。为此，作者构建了包含105个任务的SODIUM-Bench基准测试。针对现有AI代理表现不佳的问题，开发了SODIUM-Agent，通过ATP-BFS算法和多代理协作实现深度网络探索与结构化信息提取，显著提升了自动化构建查询数据库的能力。
selection_source: fresh_fetch
motivation: 领域专家在进行数据分析前，往往需要耗费大量精力从海量且分散的网页中手动搜索、提取并整合结构化信息。
method: 开发了由网络探索器和缓存管理器组成的SODIUM-Agent多代理系统，并采用ATP-BFS算法优化深度探索与信息提取过程。
result: "SODIUM-Agent在SODIUM-Bench基准测试中取得了91.1%的准确率，远超现有最强AI代理46.5%的表现。"
conclusion: 该研究通过形式化SODIUM任务并提供高效的代理系统，为自动化将开放网络数据转化为可查询数据库奠定了基础。
---

## 摘要
在研究过程中，领域专家经常提出分析性问题，其答案需要整合来自广泛网络源的数据。因此，在开始分析之前，他们必须投入大量精力搜索、提取和组织原始数据。我们将这一过程形式化为 SODIUM 任务，其中我们将网络等开放领域概念化为必须进行系统实例化以支持下游查询的潜在数据库。解决 SODIUM 任务需要：(1) 对开放网络进行深入且专业的探索；(2) 利用结构相关性进行系统的信息提取；(3) 将收集到的信息整合为连贯、可查询的数据库实例。为了量化自动化 SODIUM 的挑战，我们构建了 SODIUM-Bench，这是一个包含 105 个任务的基准测试，这些任务源自 6 个领域的已发表学术论文，要求系统探索开放网络，从多种来源收集数据并汇总到结构化表中。现有系统在处理 SODIUM 任务时面临困难：我们在 SODIUM-Bench 上评估了 6 个先进的 AI 智能体，其中最强的基准模型仅达到 46.5% 的准确率。为了弥补这一差距，我们开发了 SODIUM-Agent，这是一个由网络探索器和缓存管理器组成的多智能体系统。在所提出的 ATP-BFS 算法驱动下，并通过对缓存源和导航路径的原则性管理进行优化，SODIUM-Agent 能够进行深入且全面的网络探索，并执行结构连贯的信息提取。SODIUM-Agent 在 SODIUM-Bench 上实现了 91.1% 的准确率，表现优于最强基准模型约 2 倍，优于最弱基准模型高达 73 倍。

## Abstract
During research, domain experts often ask analytical questions whose answers require integrating data from a wide range of web sources. Thus, they must spend substantial effort searching, extracting, and organizing raw data before analysis can begin. We formalize this process as the SODIUM task, where we conceptualize open domains such as the web as latent databases that must be systematically instantiated to support downstream querying. Solving SODIUM requires (1) conducting in-depth and specialized exploration of the open web, which is further strengthened by (2) exploiting structural correlations for systematic information extraction and (3) integrating collected information into coherent, queryable database instances.   To quantify the challenges in automating SODIUM, we construct SODIUM-Bench, a benchmark of 105 tasks derived from published academic papers across 6 domains, where systems are tasked with exploring the open web to collect and aggregate data from diverse sources into structured tables. Existing systems struggle with SODIUM tasks: we evaluate 6 advanced AI agents on SODIUM-Bench, with the strongest baseline achieving only 46.5% accuracy. To bridge this gap, we develop SODIUM-Agent, a multi-agent system composed of a web explorer and a cache manager. Powered by our proposed ATP-BFS algorithm and optimized through principled management of cached sources and navigation paths, SODIUM-Agent conducts deep and comprehensive web exploration and performs structurally coherent information extraction. SODIUM-Agent achieves 91.1% accuracy on SODIUM-Bench, outperforming the strongest baseline by approximately 2 times and the weakest by up to 73 times.

---

## 论文详细总结（自动生成）

### 论文总结：SODIUM —— 从开放网络数据到可查询数据库

#### 1. 核心问题与整体含义（研究动机和背景）
在科学研究中，领域专家经常需要从互联网上分散、深层嵌套且格式不一的网页中提取数据，以构建结构化的数据库进行下游分析。这一过程目前高度依赖人工搜索、提取和整合，效率极低。
论文将这一挑战形式化为 **SODIUM 任务**（Structuring Open DomaIn Unstructured Data into Materialized Databases），其核心思想是将整个开放网络视为一个“潜在数据库”，目标是通过系统化的手段将其“实例化”为用户指定的结构化表格。

#### 2. 方法论：核心思想与关键技术
论文提出了 **SODIUM-Agent**，一个由“网络探索器（Web Explorer）”和“缓存管理器（Cache Manager）”构成的多代理系统。

*   **核心算法：ATP-BFS（增强-剪枝广度优先搜索）**：
    *   **增强（Augment）**：通过推理已发现链接的模式（如年份、ID变化），主动合成可能存在但未直接显示的 URL。
    *   **选择与剪枝（Select & Prune）**：在预设预算（如 K=10）内，根据文本相关性过滤掉无关链接，防止搜索空间爆炸。
    *   **排序（Rank）**：基于查询目标对候选链接进行优先级排序。
*   **页面探索器（Page Explorer）**：能够识别并处理静态网页、动态交互式网页（通过模拟点击、展开标签等）以及在线文档（PDF、图像）。
*   **缓存管理器（Cache Manager）**：利用表格的结构相关性（行/列依赖）。如果相邻单元格已填充，系统会优先复用其来源页面或导航路径，从而减少重复搜索并确保数据的一致性。

#### 3. 实验设计
*   **基准测试（SODIUM-Bench）**：作者构建了一个包含 **105 个任务** 的基准，数据源自 6 个领域（人口统计、体育、金融、经济、食品、气候）的 48 篇已发表学术论文。
*   **评估规模**：涉及 458 个主键值、506 个属性，共计 **2149 个单元格** 的数据检索。
*   **对比方法**：评估了 6 种先进的 AI 代理系统：**AG2、AutoGPT、AutoGen、OpenAI ResearchBot、WebVoyager、Open Deep Research**。
*   **评估指标**：采用单元格准确率（Cell Acc）、行/列准确率（Row/Col Acc）、表格准确率（Table Acc）以及任务级总准确率。

#### 4. 资源与算力
*   **模型使用**：所有代理均基于 `gpt-5-2025-08-07`（注：论文设定背景为 2026 年）进行驱动。
*   **算力说明**：文中**未明确说明**具体的物理算力（如 GPU 型号或训练时长），因为该系统主要基于大语言模型（LLM）的 API 调用和代理逻辑构建，而非从头训练基础模型。

#### 5. 实验数量与充分性
*   **实验规模**：进行了超过 2000 次独立的检索操作，涵盖了从浅层到深层（深度达 6 层以上）的网页探索。
*   **充分性**：实验设计非常充分。除了主实验外，还包括：
    *   **消融实验**：验证了“独立 Web Explorer”与“完整 SODIUM-Agent”的性能差异。
    *   **深度分析**：分析了不同搜索深度对准确率的影响。
    *   **成本分析**：量化了缓存机制在降低 API 调用成本方面的贡献。
*   **客观性**：通过“精确匹配”和“LLM 语义评判”双重标准进行评估，确保了结果的公正性。

#### 6. 主要结论与发现
*   **性能飞跃**：SODIUM-Agent 达到了 **91.1%** 的任务准确率，而最强的基准模型（AG2）仅为 46.5%，提升了约 2 倍。
*   **结构化优势**：在要求极高的“表格级准确率”（整表完全正确）上，SODIUM-Agent 达到 52.4%，而现有基准模型几乎全部低于 3%。
*   **缓存价值**：缓存机制处理了约 72% 的单元格填充任务，不仅提高了准确率，还将平均每个单元格的成本降低了约 **70%**。
*   **深度探索能力**：ATP-BFS 算法使代理能够有效处理深层嵌套网页，打破了现有代理在长程导航中的脆弱性。

#### 7. 优点
*   **任务形式化创新**：首次将网络数据提取定义为“潜在数据库实例化”过程，非常符合实际科研需求。
*   **ATP-BFS 算法**：巧妙地结合了模式推理（URL 合成）与启发式剪枝，解决了 Web 搜索中分支因子过大的问题。
*   **结构感知**：通过缓存管理器模拟了人类专家的复用逻辑，显著提升了数据的全局一致性。

#### 8. 不足与局限
*   **模型依赖**：系统高度依赖于底层 LLM（如 GPT-5）的推理能力，若模型能力下降，系统表现可能受限。
*   **反爬与动态限制**：虽然能处理动态网页，但对于极其复杂的验证码、严格的频率限制或需要身份验证的数据库，文中未深入讨论应对方案。
*   **实时性风险**：由于涉及多步导航和多次 API 调用，对于超大规模表格的实时构建可能存在延迟。

（完）
