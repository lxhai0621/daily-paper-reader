---
title: "TRUST-SQL: Tool-Integrated Multi-Turn Reinforcement Learning for Text-to-SQL over Unknown Schemas"
title_zh: TRUST-SQL：面向未知模式的工具集成多轮强化学习 Text-to-SQL 框架
authors: "Ai Jian, Xiaoyun Zhang, Wanrou Du, Jingqing Ruan, Jiangbo Pei, Weipeng Zhang, Ke Zeng, Xunliang Cai"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16448v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 自主智能体从未知数据库模式中识别相关子集
tldr: 针对现实中数据库模式庞大且未知的挑战，本文提出TRUST-SQL框架。该框架将Text-to-SQL建模为部分可观测马尔可夫决策过程，通过四阶段协议引导智能体主动探索元数据。结合创新的双轨GRPO强化学习策略，利用令牌级掩码优势解决信用分配问题。实验表明，该方法在无需预加载模式的情况下，性能显著优于基础模型，并能媲美甚至超越依赖模式预填的强基准模型。
selection_source: fresh_fetch
motivation: 传统的Text-to-SQL研究假设已知完整模式，但在处理包含海量噪声元数据的真实企业级数据库时，这种假设并不成立。
method: 提出一种集成工具的四阶段协议，并采用双轨GRPO策略，通过令牌级掩码优势将探索奖励与执行结果分离以优化强化学习。
result: "在五个基准测试中，4B和8B模型变体分别实现了30.6%和16.6%的平均绝对性能提升，且表现优于依赖模式预填的基准。"
conclusion: TRUST-SQL证明了通过结构化协议和改进的强化学习，智能体可以在不预加载元数据的情况下高效、准确地完成复杂数据库查询任务。
---

## 摘要
在全模式假设（Full Schema Assumption）下，Text-to-SQL 解析已取得了显著进展。然而，在现实世界的企业环境中，这一前提往往失效，因为数据库包含数百张表以及海量的噪声元数据。智能体必须主动识别并验证相关的子集，而不是预先注入完整的模式，这便引出了本研究探讨的“未知模式”（Unknown Schema）场景。为此，我们提出了 TRUST-SQL（通过工具进行未知模式下的真实推理）。我们将该任务建模为部分可观测马尔可夫决策过程（POMDP），其中自主智能体采用结构化的四阶段协议，将推理建立在经过验证的元数据之上。至关重要的是，该协议为我们新颖的双轨 GRPO（Dual-Track GRPO）策略提供了结构化边界。通过应用 Token 级掩码优势（token-level masked advantages），该策略将探索奖励与执行结果隔离，以解决信用分配（credit assignment）问题，相比标准 GRPO 实现了 9.9% 的相对提升。在五个基准测试上的广泛实验表明，TRUST-SQL 的 4B 和 8B 变体相较于其基础模型，平均绝对提升分别达到了 30.6% 和 16.6%。值得注意的是，尽管在完全没有预加载元数据的情况下运行，我们的框架依然能够持续匹配或超越依赖模式预填充（schema prefilling）的强基准模型。

## Abstract
Text-to-SQL parsing has achieved remarkable progress under the Full Schema Assumption. However, this premise fails in real-world enterprise environments where databases contain hundreds of tables with massive noisy metadata. Rather than injecting the full schema upfront, an agent must actively identify and verify only the relevant subset, giving rise to the Unknown Schema scenario we study in this work. To address this, we propose TRUST-SQL (Truthful Reasoning with Unknown Schema via Tools). We formulate the task as a Partially Observable Markov Decision Process where our autonomous agent employs a structured four-phase protocol to ground reasoning in verified metadata. Crucially, this protocol provides a structural boundary for our novel Dual-Track GRPO strategy. By applying token-level masked advantages, this strategy isolates exploration rewards from execution outcomes to resolve credit assignment, yielding a 9.9% relative improvement over standard GRPO. Extensive experiments across five benchmarks demonstrate that TRUST-SQL achieves an average absolute improvement of 30.6% and 16.6% for the 4B and 8B variants respectively over their base models. Remarkably, despite operating entirely without pre-loaded metadata, our framework consistently matches or surpasses strong baselines that rely on schema prefilling.

---

## 论文详细总结（自动生成）

### 论文总结：TRUST-SQL 框架

#### 1. 核心问题与整体含义（研究动机和背景）
传统的 Text-to-SQL 研究大多基于**全模式假设（Full Schema Assumption）**，即假设数据库的所有表结构（Schema）都能预先加载到模型的上下文窗口中。然而，在真实的工业场景中，数据库往往包含数百张表和海量的噪声元数据，直接注入完整模式会导致上下文溢出或引入干扰信息。
论文提出了**未知模式（Unknown Schema）**场景，要求智能体像人类专家一样，通过主动探索、查询元数据来识别相关的表和列。核心挑战在于：如何防止模型在长路径交互中产生幻觉，以及如何解决多轮交互中的**信用分配（Credit Assignment）**问题（即最终 SQL 的错误究竟是因为找错了表，还是写错了逻辑）。

#### 2. 方法论：核心思想与技术细节
TRUST-SQL 将任务建模为**部分可观测马尔可夫决策过程（POMDP）**，核心包含两个部分：
*   **四阶段交互协议（EPGC）**：
    1.  **Explore（探索）**：调用工具查询数据库元数据。
    2.  **Propose（提议）**：强制性的认知检查点，要求模型提交已验证的模式子集，以此抑制幻觉。
    3.  **Generate（生成）**：基于提议的模式生成候选 SQL。
    4.  **Confirm（确认）**：执行并提交最终答案。
*   **双轨 GRPO 策略（Dual-Track GRPO）**：
    *   基于 DeepSeek 的 GRPO 算法，引入**双轨优化**。
    *   **Schema 轨道**：专门评估探索阶段的质量（与标准答案模式的重合度）。
    *   **Full 轨道**：评估最终 SQL 的执行正确性和格式规范性。
    *   **Token 级掩码优势（Token-level Masked Advantages）**：通过掩码技术，确保探索阶段生成的 Token 只接收模式奖励，而生成阶段的 Token 只接收执行奖励，从而实现精准的信用分配。

#### 3. 实验设计
*   **数据集**：
    *   **BIRD-Dev**：大规模、跨领域的复杂数据库基准。
    *   **Spider-Test**：标准 Text-to-SQL 基准。
    *   **鲁棒性测试集**：Spider-Syn（同义词替换）、Spider-DK（隐性领域知识）、Spider-Realistic（消除歧义）。
    *   **Spider 2.0 (SQLite)**：更具挑战性的企业级工作流测试。
*   **对比方法**：包括单轮模型（OmniSQL, SQL-R1）和多轮强化学习模型（MTIR-SQL, SQL-Trail）。
*   **评估指标**：执行准确率（Execution Accuracy, EX%），分为 Greedy 解码和 Majority Voting（多数投票）。

#### 4. 资源与算力
论文详细列出了训练配置：
*   **硬件**：使用 NVIDIA A100 GPU。
*   **4B 模型**：SFT 阶段使用 16 张 A100（耗时 6.5 小时）；RL 阶段使用 8 张 A100（耗时 60 小时）。
*   **8B 模型**：SFT 阶段使用 16 张 A100（耗时 12 小时）；RL 阶段使用 32 张 A100（耗时 40 小时）。
*   **训练框架**：采用 SLIME 框架进行 RL 缩放。

#### 5. 实验数量与充分性
实验设计非常充分且具有说服力：
*   **多维度对比**：在 5 个主流基准和 1 个极难基准（Spider 2.0）上进行了测试。
*   **消融实验**：针对奖励权重 $\lambda$、模式奖励的耦合方式、SFT 预热的必要性、以及训练/推理轮数预算进行了深入分析。
*   **测试时缩放（Test-time Scaling）**：分析了 Pass@K 随采样次数增加的性能提升趋势。
*   **客观性**：对比了在“有模式预填”和“无模式预填”两种情况下的表现，证明了该框架的独立性。

#### 6. 主要结论与发现
*   **性能飞跃**：TRUST-SQL 在未知模式下，4B 变体平均提升 30.6%，8B 变体提升 16.6%。
*   **超越基准**：尽管 TRUST-SQL 完全不预加载模式，其表现依然能匹配甚至超越那些拥有“全模式特权”的强基准模型。
*   **双轨优化的有效性**：相比标准 GRPO，双轨策略带来了 9.9% 的相对性能提升，证明了分离探索与执行信号的必要性。
*   **抑制幻觉**：引入 Propose 阶段后，幻觉错误率从 26.4% 降至 2.8%。

#### 7. 优点
*   **场景真实**：直击企业级数据库中“模式过大无法全量输入”的痛点。
*   **算法创新**：双轨 GRPO 巧妙地解决了长路径交互中的信用分配难题，是强化学习在工具集成任务中的一次成功应用。
*   **高效性**：相比于 CHESS 等依赖复杂 Prompt 工程的方法，TRUST-SQL 在推理延迟和 Token 消耗上降低了百倍以上。

#### 8. 不足与局限
*   **推理开销**：多轮交互自然比单轮翻译带来更高的延迟和计算成本。
*   **方言限制**：目前主要针对 SQLite 进行优化和评估，尚未广泛测试 PostgreSQL 或 MySQL 等其他 SQL 方言。
*   **轮数限制**：训练时设定了固定的最大轮数（如 10 轮），对于极度复杂的数据库，可能需要更动态的轮数分配策略。

（完）
