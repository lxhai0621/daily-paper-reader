---
title: "RoboClaw: An Agentic Framework for Scalable Long-Horizon Robotic Tasks"
title_zh: RoboClaw：一种用于可扩展长程机器人任务的智能体框架
authors: "Ruiying Li, Yunlang Zhou, YuYao Zhu, Kylin Chen, Jingyuan Wang, Sukai Wang, Kongtao Hu, Minhui Yu, Bowen Jiang, Zhan Su, Jiayao Ma, Xin He, Yongjian Shen, Yang Yang, Guanghui Ren, Maoqing Yao, Wenhao Wang, Yao Mu"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11558v2"
tags: ["query:ma-kf"]
score: 9.0
evidence: 用于自主机器人任务执行的智能体框架
tldr: 针对长程机器人任务中数据采集依赖人工重置和多策略执行脆弱的问题，本文提出RoboClaw框架。该框架利用大视觉语言模型（VLM）统一了数据采集、策略学习和任务执行。核心创新是“纠缠动作对（EAP）”，通过将操作行为与恢复动作耦合实现自主重置，显著降低了人工干预。实验表明，该方法在提升长程任务成功率的同时，大幅减少了人力投入，为可扩展的机器人学习提供了新思路。
selection_source: fresh_fetch
motivation: 现有的机器人学习流程过度依赖人工环境重置，且在执行长程任务时多策略衔接不够稳健。
method: 提出一种由VLM驱动的代理框架，通过“纠缠动作对”实现自主数据采集，并动态编排策略原语以完成复杂任务。
result: "在真实世界任务中，该方法将长程任务成功率提升了25%，并减少了53.7%的人力时间投入。"
conclusion: RoboClaw通过统一采集与执行流程，为实现可扩展且低人工干预的长程机器人操作提供了有效方案。
---

## 摘要
视觉-语言-动作（VLA）系统在语言驱动的机器人操控方面展现出强大潜力。然而，将其扩展到长程任务仍然具有挑战性。现有的流程通常将数据采集、策略学习和部署分开，导致对人工环境重置的严重依赖以及多策略执行的脆弱性。我们提出了 RoboClaw，这是一个将数据采集、策略学习和任务执行统一在单个 VLM 驱动控制器下的智能体机器人框架。在策略层面，RoboClaw 引入了纠缠动作对（Entangled Action Pairs, EAP），将正向操控行为与反向恢复动作耦合，形成用于自主数据采集的自重置循环。该机制实现了在极少人工干预下的持续同策略数据获取和迭代策略优化。在部署期间，同一个智能体执行高层推理，并动态编排已学习的策略原语以完成长程任务。通过在采集和执行过程中保持一致的上下文语义，RoboClaw 减少了两个阶段之间的不匹配，并提高了多策略的鲁棒性。在真实世界操控任务中的实验表明，与传统的开环流程相比，该框架提高了稳定性和可扩展性，同时显著减少了整个机器人生命周期中的人力投入，在长程任务上的成功率比基准方法提高了 25%，并减少了 53.7% 的人力时间投入。

## Abstract
Vision-Language-Action (VLA) systems have shown strong potential for language-driven robotic manipulation. However, scaling them to long-horizon tasks remains challenging. Existing pipelines typically separate data collection, policy learning, and deployment, resulting in heavy reliance on manual environment resets and brittle multi-policy execution. We present RoboClaw, an agentic robotics framework that unifies data collection, policy learning, and task execution under a single VLM-driven controller. At the policy level, RoboClaw introduces Entangled Action Pairs (EAP), which couple forward manipulation behaviors with inverse recovery actions to form self-resetting loops for autonomous data collection. This mechanism enables continuous on-policy data acquisition and iterative policy refinement with minimal human intervention. During deployment, the same agent performs high-level reasoning and dynamically orchestrates learned policy primitives to accomplish long-horizon tasks. By maintaining consistent contextual semantics across collection and execution, RoboClaw reduces mismatch between the two phases and improves multi-policy robustness. Experiments in real-world manipulation tasks demonstrate improved stability and scalability compared to conventional open-loop pipelines, while significantly reducing human effort throughout the robot lifecycle, achieving a 25% improvement in success rate over baseline methods on long-horizon tasks and reducing human time investment by 53.7%.

---

## 论文详细总结（自动生成）

以下是对论文《RoboClaw: An Agentic Framework for Scalable Long-Horizon Robotic Tasks》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对视觉-语言-动作（VLA）系统在处理**长程（Long-horizon）机器人任务**时的三大瓶颈：
*   **数据采集成本高**：传统流程极度依赖人工进行环境重置（Reset）和示教，难以大规模扩展。
*   **执行脆弱性**：多策略顺序执行时，微小误差会随步骤累积，且缺乏有效的故障恢复机制。
*   **阶段性脱节**：数据采集、策略训练与实际部署三个阶段往往相互独立，导致语义理解和状态分布不一致。
**RoboClaw 的核心目标**是构建一个统一的智能体框架，将 VLM（视觉语言模型）作为“大脑”来驱动数据采集、策略编排与任务执行的全生命周期。

### 2. 方法论：核心思想与关键技术
RoboClaw 采用分层架构，将机器人操作抽象为**策略（Policies）**、**工具（Tools）**和**技能（Skills）**：
*   **核心思想**：利用 VLM 作为元控制器（Meta-controller），通过**上下文学习（ICL）**和**思维链（CoT）**推理，动态决定调用哪些底层策略。
*   **纠缠动作对（Entangled Action Pairs, EAP）**：这是本文的关键创新。为每个正向操作策略（如“把瓶子放进抽屉”）配对一个反向恢复策略（如“把瓶子从抽屉拿出来”）。通过这种正反向耦合，机器人可以形成“自重置循环”，在无需人工干预的情况下自主、连续地采集在线数据。
*   **模型上下文协议（MCP）**：建立了一套统一的接口，使 VLM 能够通过调用工具（如启动策略、查询环境摘要、请求人工干预）来控制机器人。
*   **底层 VLA 模型**：采用 $\pi_{0.5}$ 模型，利用**条件流匹配（Conditional Flow Matching）**目标函数进行训练，将视觉、语言指令和关节状态映射为动作序列。

### 3. 实验设计
*   **实验场景**：在四个真实世界场景中进行验证：卧室梳妆台、厨房架子、书房书桌、便利店货架。
*   **具体任务**：包括身体乳放置（长距离移动）、妆前乳放置（含关闭抽屉的约束操作）、口红插入（高精度对齐）、纸巾擦拭（持续表面接触）。
*   **Benchmark 与对比方法**：
    *   **数据采集基准**：对比纯人工示教与重置的传统流程。
    *   **执行基准**：对比端到端 VLA 模型（无智能体编排）以及基于子任务成功率乘积的理论预期值。
    *   **评估指标**：成功率、人工投入时间、人工干预频率。

### 4. 资源与算力
*   **模型微调**：论文提供了 $\pi_{0.5}$ 微调的超参数（见表1）。使用了 LoRA 技术，秩（Rank）为 16，Alpha 为 16，Dropout 为 0.1。
*   **训练细节**：Batch size 为 16，训练步数为 10k 步，学习率为 $2.5 \times 10^{-5}$，采用 bfloat16 精度。
*   **硬件说明**：**文中未明确指出具体的 GPU 型号和数量**，也未提及具体的总训练时长，仅说明了推理步数为 3 步。

### 5. 实验数量与充分性
*   **实验规模**：针对 4 个子任务各进行了 50 次独立测试；长程任务（梳妆台整理）进行了 20 次完整试验；数据采集经历了 5 轮迭代。
*   **充分性评价**：实验设计较为全面，涵盖了从单技能优化到多技能长程协作的完整链路。通过消融实验展示了迭代采集对成功率的提升。
*   **客观性**：对比了理论预期成功率，并量化了人工投入的减少（53.7%），数据支撑较为扎实。但实验主要集中在特定的桌面操控任务，环境多样性仍有提升空间。

### 6. 主要结论与发现
*   **效率提升**：RoboClaw 显著降低了人力成本，采集相同数据量所需的人工时间仅为传统方法的 1/2.16。
*   **性能增强**：在长程任务中，RoboClaw 的成功率比基准方法高出 **25%**。
*   **闭环进化**：通过 EAP 机制，策略成功率随迭代次数稳步上升（例如口红插入从 4% 提升至 46%）。
*   **故障自愈**：智能体能够识别“非破坏性失败”（直接重试）和“破坏性失败”（调用恢复策略），显著增强了系统的鲁棒性。

### 7. 优点（亮点）
*   **全生命周期统一**：打破了数据、训练、部署的壁垒，确保了全流程语义的一致性。
*   **自重置机制（EAP）**：巧妙解决了机器人学习中最头疼的“环境重置”问题，使大规模在线数据采集成为可能。
*   **动态编排**：相比静态的规划器，基于 VLM 的动态调度能更好地处理执行过程中的意外情况。

### 8. 不足与局限
*   **延迟问题**：依赖云端大模型（VLM）进行推理会引入显著的延迟，可能影响实时性要求极高的任务。
*   **可逆性假设**：EAP 机制假设任务是可逆的。对于某些不可逆任务（如切菜、焊接或打破物体），该框架难以直接应用。
*   **安全边界**：虽然设有“呼叫人工”工具，但在复杂动态环境下，如何界定 VLM 的安全操作边界仍需进一步研究。
*   **硬件依赖**：实验主要在 Agibot G01 平台上完成，框架在不同动力学特性的机器人上的泛化性有待验证。

（完）
