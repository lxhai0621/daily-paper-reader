---
title: "OxyGen: Unified KV Cache Management for Vision-Language-Action Models under Multi-Task Parallelism"
title_zh: OxyGen：多任务并行下的视觉-语言-动作模型统一 KV 缓存管理
authors: "Xiangyu Li, Huaizhi Tang, Xin Ding, Weijun Wang, Ting Cao, Yunxin Liu"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14371v1"
tags: ["query:agent"]
score: 9.0
evidence: 智能体记忆构建中的统一 KV 缓存管理
tldr: 针对具身智能体在多任务并行（如操作、对话）下的推理效率问题，本文提出 OxyGen 系统。该系统通过统一 KV 缓存管理，将 KV 缓存视为跨任务和跨时间的共享资源。核心贡献包括跨任务 KV 共享以消除冗余预填充，以及跨帧连续批处理以解耦不同速率的任务生成。在 π0.5 模型上的实验表明，OxyGen 显著提升了推理速度和任务频率，且不损失动作质量。
selection_source: fresh_fetch
motivation: 现有的具身智能模型推理系统因孤立的 KV 缓存管理导致冗余计算和资源竞争，难以支持高效的多任务并行执行。
method: 提出统一 KV 缓存管理范式，通过跨任务 KV 共享和跨帧连续批处理优化共享观测值的预填充与异构任务的解码。
result: 在 π0.5 模型上实现了高达 3.7 倍的加速，同时达到 200 tokens/s 的语言吞吐量和 70 Hz 的动作频率。
conclusion: 统一 KV 缓存管理是实现具身智能模型高效、实时多任务并行推理的关键技术路径。
---

## 摘要
具身智能体日益需要根据共享的观测结果，在不同的时间约束下并行执行多项任务，例如操作、对话和记忆构建。最近的混合 Transformer（MoT）视觉-语言-动作模型（VLAs）在架构上支持此类异构输出，但现有的推理系统由于冗余计算和资源竞争，无法在设备端部署中实现高效的多任务并行。我们发现孤立的 KV 缓存管理是其根本原因。为了解决这一问题，我们提出了统一 KV 缓存管理，这是一种将 KV 缓存视为跨任务和跨时间的共享资源的推理范式。这种抽象实现了两项关键优化：跨任务 KV 共享消除了共享观测值的冗余预填充，而跨帧连续批处理则在控制周期内将变长语言解码与固定速率的动作生成解耦。我们针对最流行的 MoT VLA 模型 $π_{0.5}$ 实现了该范式，并在代表性的机器人配置下进行了评估。OxyGen 相比于孤立执行实现了高达 3.7 倍的加速，在不降低动作质量的情况下，同时实现了超过 200 tokens/s 的语言吞吐量和 70 Hz 的动作频率。

## Abstract
Embodied AI agents increasingly require parallel execution of multiple tasks, such as manipulation, conversation, and memory construction, from shared observations under distinct time constraints. Recent Mixture-of-Transformers (MoT) Vision-Language-Action Models (VLAs) architecturally support such heterogeneous outputs, yet existing inference systems fail to achieve efficient multi-task parallelism for on-device deployment due to redundant computation and resource contention. We identify isolated KV cache management as the root cause. To address this, we propose unified KV cache management, an inference paradigm that treats KV cache as a first-class shared resource across tasks and over time. This abstraction enables two key optimizations: cross-task KV sharing eliminates redundant prefill of shared observations, while cross-frame continuous batching decouples variable-length language decoding from fixed-rate action generation across control cycles. We implement this paradigm for $π_{0.5}$, the most popular MoT VLA, and evaluate under representative robotic configurations. OxyGen achieves up to 3.7$\times$ speedup over isolated execution, delivering over 200 tokens/s language throughput and 70 Hz action frequency simultaneously without action quality degradation.

---

## 论文详细总结（自动生成）

这篇论文提出了 **OxyGen**，一个针对混合 Transformer（MoT）架构的视觉-语言-动作（VLA）模型的高效推理系统。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：具身智能体（如家用机器人）需要同时处理多种任务，例如在操作物体的同时进行对话或构建环境记忆。
*   **核心问题**：虽然最新的 MoT-VLA 模型（如 $\pi_{0.5}$）在架构上支持多模态输出，但现有的推理系统采用“**孤立执行**”范式。这导致了两个主要效率瓶颈：
    1.  **冗余计算**：同一帧的视觉观测被多次重复编码，为不同任务生成相同的 KV 缓存。
    2.  **资源竞争**：不同任务（如实时动作生成与长文本语言解码）竞争单一 GPU 资源，导致动作频率下降或语言吞吐量低。
*   **研究目标**：通过优化 KV 缓存管理，实现高频动作控制与高吞吐语言生成的并行。

### 2. 方法论：统一 KV 缓存管理
OxyGen 的核心思想是将 KV 缓存视为跨任务和跨时间的“第一类共享资源”。
*   **跨任务 KV 共享 (Cross-task KV Sharing)**：
    *   在每一帧中，VLM 主干网络仅对观测值进行一次预填充（Prefill），生成的 KV 缓存被动作专家（用于扩散/流匹配采样）和语言专家（用于自回归解码）共同复用，消除了重复编码开销。
*   **跨帧连续批处理 (Cross-frame Continuous Batching)**：
    *   **解耦调度**：将动作生成的硬实时需求（需在当前帧完成）与语言生成的软实时需求（可跨多帧完成）解耦。
    *   **动态批处理**：将当前帧新生成的语言请求与前几帧尚未完成的在位请求进行批处理。通过增加 Batch Size 提高 GPU 利用率，从而在不影响动作频率的前提下大幅提升语言吞吐量。
*   **可恢复状态管理**：引入统一 KV 管理器，维护每个请求的生成状态（包括 KV 缓存、已生成 Token 和终止标志），支持跨帧的中断与恢复。

### 3. 实验设计
*   **模型与基准**：
    *   **模型**：最流行的 MoT-VLA 模型 $\pi_{0.5}$。
    *   **数据集/场景**：LIBERO（机器人操作）、DROID（真实世界操作数据）、ALOHA（双臂精细操作）。
*   **对比方法 (Baselines)**：
    *   **Sequential Isolated (Baseline)**：官方 openpi 框架，任务按顺序独立执行。
    *   **Parallel Isolated (Parallel)**：通过 CUDA MPS 实现的多进程并行执行，但 KV 缓存不共享。
*   **评估指标**：动作频率 (Hz)、语言吞吐量 (tokens/s)、任务成功率、内存占用及能耗。

### 4. 资源与算力
*   **硬件平台**：单张 **NVIDIA RTX 4090 GPU**。这是移动机器人端侧部署的代表性高性能硬件。
*   **训练/推理细节**：论文侧重于推理优化，未详细说明模型训练时长，但强调了在单 GPU 上实现了实时多任务推理。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 3 个主流机器人基准上进行了详尽的性能测试。
    *   **消融实验**：分别验证了“跨任务共享”和“跨帧批处理”的增益。
    *   **负载泛化性**：测试了不同请求到达模式（均匀到达、泊松到达、随机长度请求）。
    *   **质量验证**：在 LIBERO 测试集上验证了加速后的模型成功率与原版一致。
*   **评价**：实验设计全面，涵盖了性能、功耗、内存和实际任务成功率，对比基线合理，实验结果具有说服力。

### 6. 主要结论与发现
*   **大幅加速**：相比孤立执行，OxyGen 实现了高达 **3.7 倍** 的端到端加速。
*   **高性能并行**：能够同时维持 **70 Hz** 的动作频率和超过 **200 tokens/s** 的语言吞吐量。
*   **能效提升**：由于减少了内存访问和冗余计算，单次请求能耗降低了高达 **78%**，且内存增加仅约 15%。
*   **无损质量**：推理优化完全不影响机器人的操作成功率。

### 7. 优点与亮点
*   **范式创新**：首次将 LLM 服务中的连续批处理思想引入到具身智能 VLA 模型中，并针对动作/语言的异构时延需求进行了定制。
*   **实用性强**：直接针对当前最流行的开源 VLA 模型和推理框架进行改进，代码已开源，具有很高的工程参考价值。
*   **多维优化**：不仅关注速度，还兼顾了端侧部署关心的功耗和内存指标。

### 8. 不足与局限
*   **模型依赖性**：该方法主要针对 MoT（混合 Transformer）架构的 VLA 模型，对于非共享主干网络的多模型组合方案适用性有限。
*   **硬件局限**：实验仅在 RTX 4090 上完成，未在算力更受限的嵌入式平台（如 Jetson Orin）上验证其性能表现。
*   **任务类型**：目前仅验证了动作和语言任务，对于视频生成（世界模型）等更复杂的异构任务并行尚待探索。

（完）
