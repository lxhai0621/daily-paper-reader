---
title: "Before the Tool Call: Deterministic Pre-Action Authorization for Autonomous AI Agents"
title_zh: 工具调用之前：自主 AI 智能体的确定性动作前授权
authors: Uchi Uchibeke
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20953v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 自主 AI 智能体调用工具时的确定性预操作授权
tldr: "针对当前AI智能体在执行工具调用（如转账、数据库查询）时缺乏确定性授权机制的问题，本文提出了Open Agent Passport (OAP) 规范。OAP通过在执行前同步拦截工具调用，并根据声明式策略进行评估，实现了确定性的预操作授权。实验表明，OAP能以极低的延迟（中位数53ms）有效阻止攻击，将社交工程攻击的成功率从74.6%降低至0%，为智能体安全提供了强有力的保障。"
selection_source: fresh_fetch
motivation: 现有的AI安全架构依赖于概率性的模型对齐或事后评估，缺乏在工具调用层面进行确定性、基于策略的预先授权机制。
method: 提出了Open Agent Passport (OAP) 规范，通过同步拦截工具调用并依据声明式策略进行评估，生成加密签名的审计记录。
result: "OAP在53毫秒的中位延迟内完成授权决策，并在对抗性测试中将针对模型的社交工程攻击成功率从74.6%降至0%。"
conclusion: 预操作授权是独立于沙箱执行和模型筛选的关键安全层，能有效强制执行安全约束、质量门禁和合规控制。
---

## 摘要
如今的 AI 智能体拥有密码，但没有权限凭证。它们执行工具调用（如资金转账、数据库查询、Shell 命令、子智能体委派），但在动作执行前缺乏强制授权的标准机制。当前的安全架构依赖于模型对齐（概率性的、训练时的）和事后评估（回顾性的、批量的），两者都无法在单个工具调用层面提供基于策略的确定性强制执行。我们将这一差距定义为“动作前授权”问题，并提出了开放智能体护照（Open Agent Passport, OAP）。这是一种开放规范和参考实现，可在执行前同步拦截工具调用，根据声明式策略对其进行评估，并生成经加密签名的审计记录。实验表明，OAP 强制执行授权决策的中位耗时为 53 毫秒（N=1,000）。在一个实战对抗测试平台中（涵盖 1,151 个会话中的 4,437 次授权决策，设有 5,000 美元悬赏），在宽松策略下，针对模型的社会工程攻击成功率为 74.6%；而在严格的 OAP 策略下，同等规模的攻击者在 879 次尝试中实现了 0% 的成功率。我们将动作前授权与沙箱执行（限制影响范围但不能防止未经授权的动作）以及基于模型的筛选（概率性的）进行了区分，并证明它们是互补的。用于强制执行安全约束（如支出限制、能力范围）的同一基础设施，也可用于强制执行质量关卡、操作合约和合规控制。该规范已按 Apache 2.0 协议发布（DOI: 10.5281/zenodo.18901596）。

## Abstract
AI agents today have passwords but no permission slips. They execute tool calls (fund transfers, database queries, shell commands, sub-agent delegation) with no standard mechanism to enforce authorization before the action executes. Current safety architectures rely on model alignment (probabilistic, training-time) and post-hoc evaluation (retrospective, batch). Neither provides deterministic, policy-based enforcement at the individual tool call level.   We characterize this gap as the pre-action authorization problem and present the Open Agent Passport (OAP), an open specification and reference implementation that intercepts tool calls synchronously before execution, evaluates them against a declarative policy, and produces a cryptographically signed audit record. OAP enforces authorization decisions in a measured median of 53 ms (N=1,000). In a live adversarial testbed (4,437 authorization decisions across 1,151 sessions, $5,000 bounty), social engineering succeeded against the model 74.6% of the time under a permissive policy; under a restrictive OAP policy, a comparable population of attackers achieved a 0% success rate across 879 attempts.   We distinguish pre-action authorization from sandboxed execution (contains blast radius but does not prevent unauthorized actions) and model-based screening (probabilistic), and show they are complementary. The same infrastructure that enforces security constraints (spending limits, capability scoping) also enforces quality gates, operational contracts, and compliance controls. The specification is released under Apache 2.0 (DOI: 10.5281/zenodo.18901596).

---

## 论文详细总结（自动生成）

这是一份关于论文《Before the Tool Call: Deterministic Pre-Action Authorization for Autonomous AI Agents》的结构化技术总结：

### 1. 论文的核心问题与整体含义
*   **核心问题**：自主 AI 智能体在执行具有现实影响的“工具调用”（如转账、删除数据库、执行 Shell 命令）时，缺乏一种在动作执行前进行强制授权的标准机制。
*   **研究背景**：目前的 AI 安全主要依赖于**模型对齐**（概率性的，容易被提示词注入绕过）或**事后评估**（滞后性的）。作者指出，这本质上是一个“授权缺位”问题，而非单纯的“对齐”问题。智能体目前拥有访问密码，但没有“准行证”（Permission Slips）。
*   **整体含义**：本文提出了 **Open Agent Passport (OAP)** 规范，旨在工具调用边界建立一个确定性的、基于策略的、可审计的授权层。

### 2. 论文提出的方法论
*   **核心思想**：在智能体框架层（而非模型层）引入一个同步拦截钩子（Hook），在工具执行前根据预定义的声明式策略进行强制校验。
*   **关键技术组件**：
    *   **智能体护照 (Agent Passport)**：一种经 Ed25519 加密签名的凭证，绑定智能体身份、授权能力范围（Capabilities）和限制条件（Limits）。
    *   **策略包 (Policy Packs)**：使用 JSON/YAML 编写的声明式规则，定义了特定操作（如金融转账）必须满足的约束（如单笔限额、收款人白名单）。
    *   **动作前钩子 (before_tool_call Hook)**：集成在智能体框架中，拦截工具调用请求，将其发送至授权引擎。
*   **算法流程**：
    1.  **拦截**：捕获工具名称、参数及上下文。
    2.  **校验凭证**：验证护照签名是否有效、是否被吊销。
    3.  **能力匹配**：检查请求的工具是否在护照授权范围内。
    4.  **策略评估**：将工具参数与策略包中的规则进行确定性比对（如 `amount > max_per_tx`）。
    5.  **决策输出**：返回 ALLOW（允许）、DENY（拒绝）或 ESCALATE（提权/人工审批），并生成加密签名的审计记录。

### 3. 实验设计
*   **对抗性测试平台 (APort Vault CTF)**：
    *   **场景**：一个在线银行智能体挑战赛，参与者尝试通过社交工程诱导智能体进行未经授权的转账。
    *   **对比基准**：
        *   **T1 (宽松策略)**：仅依赖模型对齐，策略限制极少。
        *   **T5 (严格策略)**：强制执行 OAP 确定的能力范围和零额度限制。
*   **性能基准测试**：在生产环境下进行 N=1,000 次请求测试，测量授权决策的延迟（p50, p95, p99）。
*   **覆盖率分析**：对比 OAP 机制对 OWASP Agentic Top 10 安全风险的覆盖程度。

### 4. 资源与算力
*   **算力说明**：论文**未提及**大规模 GPU 训练算力，因为 OAP 是一个系统架构和协议规范，而非深度学习模型。
*   **部署环境**：参考实现部署在 Cloudflare Workers（边缘计算），利用其全球分布特性降低延迟。
*   **开发工具**：提供了针对 LangChain、CrewAI、Claude Code 等主流框架的适配器。

### 5. 实验数量与充分性
*   **实验规模**：
    *   分析了 **1,151 个攻击会话**中的 **4,437 次授权决策**。
    *   性能测试样本量为 **1,000 次**。
*   **充分性评价**：实验设计较为充分且具有极高的实战价值。通过“实战对抗（CTF）+ 5,000 美元悬赏”的方式，获取了真实的攻击数据，比单纯的静态数据集测试更具说服力。对比实验（T1 vs T5）清晰地展示了策略层在防御社交工程攻击中的决定性作用。

### 6. 论文的主要结论与发现
*   **安全性提升**：在宽松策略下，社交工程攻击成功率高达 **74.6%**；而在引入严格的 OAP 策略后，成功率降至 **0%**（879 次尝试无一成功）。
*   **确定性优势**：即使模型被提示词注入“洗脑”并生成了违规工具调用，OAP 也能根据静态规则将其拦截。
*   **低延迟**：云端 API 授权决策的中位延迟仅为 **53 毫秒**，对于通常耗时数秒的智能体工具执行过程来说，开销几乎可以忽略不计。
*   **互补性**：证明了动作前授权与沙箱执行、模型筛选是互补关系，共同构成了智能体安全防御纵深。

### 7. 优点
*   **确定性与非绕过性**：将安全逻辑从概率性的 LLM 中剥离，实现在框架层的强制执行。
*   **标准化与互操作性**：提出了开放规范，不绑定特定模型或框架，具有良好的通用性。
*   **可审计性**：每一条决策都有加密签名，为合规和责任追溯提供了技术支撑。
*   **实战验证**：通过高额悬赏的 CTF 验证了方案在面对真实人类攻击者时的鲁棒性。

### 8. 不足与局限
*   **委派链处理**：目前的 v1.0 版本尚未形式化处理多智能体协作中的复杂委派链问题（计划在 v1.1 解决）。
*   **策略表达力**：采用 JSON/YAML 声明式语言，虽然易读且计算复杂度低，但在处理极其复杂的逻辑（如基于历史状态的动态判断）时可能不如编程语言灵活。
*   **平台信任假设**：系统安全性前提是智能体框架本身未被篡改且会诚实地调用 Hook。如果运行环境被攻破，Hook 可能被绕过。
*   **静态性**：策略目前是静态定义的，缺乏像某些研究中提到的根据执行轨迹自动学习/调整策略的能力。

（完）
