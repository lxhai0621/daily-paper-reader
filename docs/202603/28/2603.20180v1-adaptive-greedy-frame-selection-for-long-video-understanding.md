---
title: Adaptive Greedy Frame Selection for Long Video Understanding
title_zh: 面向长视频理解的自适应贪婪帧选择
authors: "Yuning Huang, Fengqing Zhu"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20180v1"
tags: ["query:mmkqa"]
score: 9.0
evidence: 针对长视频问答的问题自适应帧选择
tldr: 针对长视频理解中视觉语言模型受限于输入帧数的问题，本文提出一种自适应贪婪帧选择方法。该方法通过联合优化查询相关性和语义代表性，在固定帧预算下平衡关键瞬间捕捉与时间覆盖。通过引入子模函数最大化和基于问题类型的分类器，该方法在MLVU基准测试中显著优于均匀采样，尤其在低预算场景下表现出色。
selection_source: fresh_fetch
motivation: 传统的均匀采样易丢失关键信息，而纯相关性驱动的选择常导致帧冗余且缺乏时间覆盖。
method: 提出一种贪婪选择算法，结合SigLIP计算相关性与DINOv2计算覆盖度，并利用文本分类器动态调整选择策略。
result: 在MLVU长视频问答基准上，该方法在各种帧预算下均优于均匀采样和现有基线，低预算下提升尤为显著。
conclusion: 自适应地平衡相关性与覆盖度是提升长视频理解效率和准确性的有效途径。
---

## 摘要
大视觉语言模型 (VLMs) 越来越多地应用于长视频问答，但推理过程往往受限于输入帧数及由此产生的视觉 token 数量。朴素的稀疏采样可能会遗漏关键时刻，而纯粹由相关性驱动的选择往往会坍缩到近乎重复的帧上，并牺牲了对时间跨度较大的证据的覆盖。我们提出了一种问题自适应的贪婪帧选择方法，在固定帧预算下共同优化查询相关性和语义代表性。我们的方法构建了一个 1 FPS 的候选池（上限为 1000 帧），具有精确的时间戳对齐，并在两个互补空间（用于问题相关性的 SigLIP 和用于语义相似性的 DINOv2）中嵌入候选帧，通过贪婪地最大化模块化相关性项和设施选址覆盖项的加权和来选择帧。该目标函数是归一化的、单调的且子模的，从而产生标准的 (1-1/e) 贪婪近似保证。为了考虑相关性和覆盖范围之间依赖于问题的权衡，我们引入了四种预设策略和一个轻量级的纯文本问题类型分类器，将每个查询路由到其表现最佳的预设。在 MLVU 上的实验表明，在各种帧预算下，该方法相比均匀采样和近期强大的基准模型在准确率上均有持续提升，且在预算紧张的情况下提升最为显著。

## Abstract
Large vision--language models (VLMs) are increasingly applied to long-video question answering, yet inference is often bottlenecked by the number of input frames and resulting visual tokens. Naive sparse sampling can miss decisive moments, while purely relevance-driven selection frequently collapses onto near-duplicate frames and sacrifices coverage of temporally distant evidence. We propose a question-adaptive greedy frame selection method that jointly optimizes query relevance and semantic representativeness under a fixed frame budget. Our approach constructs a 1~FPS candidate pool (capped at 1000) with exact timestamp alignment, embeds candidates in two complementary spaces (SigLIP for question relevance and DINOv2 for semantic similarity), and selects frames by greedily maximizing a weighted sum of a modular relevance term and a facility-location coverage term. This objective is normalized, monotone, and submodular, yielding a standard (1-1/e) greedy approximation guarantee. To account for question-dependent trade-offs between relevance and coverage, we introduce four preset strategies and a lightweight text-only question-type classifier that routes each query to its best-performing preset. Experiments on MLVU show consistent accuracy gains over uniform sampling and a strong recent baseline across frame budgets, with the largest improvements under tight budgets.

---

## 论文详细总结（自动生成）

以下是对论文《Adaptive Greedy Frame Selection for Long Video Understanding》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：长视频理解中，视觉语言模型（VLM）受限于计算预算和上下文窗口，无法处理过多视频帧。
*   **研究动机**：
    *   **均匀采样**：容易遗漏稀少但关键的瞬间。
    *   **相关性驱动选择**：容易导致“冗余坍缩”（选出的帧高度相似）或“覆盖坍缩”（忽略了视频中其他时间段的重要证据）。
    *   **需求**：需要在有限的帧预算下，找到一种既能匹配问题（相关性），又能代表视频全局（覆盖度）的帧选择策略。

### 2. 方法论
*   **核心思想**：将帧选择建模为一个**子模函数最大化（Submodular Maximization）**问题，通过贪婪算法在固定预算下平衡查询相关性和语义覆盖度。
*   **关键技术细节**：
    *   **候选池构建**：以 1 FPS 采样构建候选池（上限 1000 帧），确保时间戳精确对齐。
    *   **双嵌入空间**：
        *   **相关性空间**：利用 **SigLIP** 提取视觉和文本特征，计算帧与问题的余弦相似度。
        *   **语义代表性空间**：利用 **DINOv2** 提取视觉特征，衡量帧与帧之间的语义相似度。
    *   **目标函数**：$F(S) = \alpha R(S) + \beta C(S)$。其中 $R(S)$ 是相关性项（模块化函数），$C(S)$ 是设施选址覆盖项（Facility-Location Coverage，子模函数）。
    *   **自适应路由机制**：训练一个轻量级的纯文本分类器，根据问题类型（如剧情、计数、总结等）将查询路由至四种预设策略（相关性优先、覆盖优先等）。
*   **算法流程**：采用贪婪算法逐一选择边际增益最大的帧，理论上可保证获得 $(1-1/e)$ 的近似最优解。

### 3. 实验设计
*   **数据集**：主要使用 **MLVU**（多任务长视频理解基准），该数据集包含 9 个官方问题类别，便于进行细粒度的分类分析。
*   **对比基准（Benchmark）**：
    *   **Uniform Sampling**（均匀采样）。
    *   **AKS**（Adaptive Keyframe Sampling, CVPR 2025）：一种近期强力的自适应关键帧采样基线。
*   **实验设置**：在不同的帧预算 $K$（如 5, 10, 20, 32, 64, 100 帧）下评估模型准确率。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或具体的训练/推理总时长。
*   **训练细节**：提到问题类型分类器仅需训练 10 个 epoch 即可达到约 97.7% 的准确率，表明该组件非常轻量。

### 5. 实验数量与充分性
*   **实验规模**：在 MLVU 的 9 个子类别上进行了详尽的每类评估。
*   **消融实验**：对比了四种不同的策略预设（相关性 vs. 覆盖度的权衡），并验证了“预测路由”与“真值路由（Oracle）”的性能差异。
*   **充分性与客观性**：实验覆盖了从极低到中等规模的帧预算区间，对比了最新的 SOTA 方法，实验设计逻辑严密，能够客观反映自适应策略的有效性。

### 6. 主要结论与发现
*   **性能领先**：在所有帧预算下，该方法均显著优于均匀采样和 AKS 基线。
*   **低预算优势**：在预算极度受限（如 5-20 帧）时，提升幅度最大，证明了智能选择在资源紧张时的关键作用。
*   **自适应的必要性**：实验证明没有一种固定的权衡策略能适用于所有问题类型，通过分类器进行策略路由能进一步提升性能。

### 7. 优点
*   **理论严谨**：利用子模性理论保证了贪婪选择的近似最优性。
*   **双重平衡**：显式地解决了相关性与冗余之间的矛盾，避免了采样坍缩。
*   **即插即用**：作为一种训练无关（Training-free）的采样器（除轻量分类器外），可直接适配各种预训练 VLM。
*   **高效性**：算法复杂度低，在候选池规模为 1000 时具有良好的实时性。

### 8. 不足与局限
*   **基准覆盖有限**：目前仅在 MLVU 上进行了深入实验，缺乏在 Video-MME 或 EgoSchema 等其他长视频基准上的广泛验证。
*   **分类器依赖**：虽然分类器准确率高，但在面对开放域或分类模糊的问题时，路由错误可能会影响最终表现。
*   **静态选择**：该方法属于单次选择，未探索多轮迭代或基于模型反馈的动态证据获取。

（完）
