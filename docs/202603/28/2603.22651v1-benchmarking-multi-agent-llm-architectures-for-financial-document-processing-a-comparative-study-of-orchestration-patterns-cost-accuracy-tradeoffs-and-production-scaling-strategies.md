---
title: "Benchmarking Multi-Agent LLM Architectures for Financial Document Processing: A Comparative Study of Orchestration Patterns, Cost-Accuracy Tradeoffs and Production Scaling Strategies"
title_zh: 金融文档处理的多智能体大语言模型架构基准测试：编排模式、成本-准确率权衡及生产扩展策略的比较研究
authors: "Siddhant Kulkarni, Yukta Kulkarni"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22651v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 多智能体LLM架构与编排模式基准测试
tldr: 本研究针对金融文档处理，系统性地评估了顺序、并行、层级和反思四种多智能体LLM架构。通过对1万份SEC文件的基准测试，对比了不同架构在准确率、成本和延迟等维度的表现。研究发现反思型架构准确率最高但成本昂贵，而层级架构在成本效益上表现最佳。此外，通过混合配置和扩展性分析，为金融监管环境下的生产部署提供了实用的架构选择和优化策略。
selection_source: fresh_fetch
motivation: 在金融文档结构化信息提取中，缺乏关于多智能体LLM架构选择及其在成本、准确率和扩展性之间权衡的实证指导。
method: 通过1万份SEC文件，对比评估了顺序流水线、并行分发合并、层级主管-工人及反思自纠正四种多智能体编排架构在五种主流LLM上的表现。
result: 反思型架构实现了最高的F1分数（0.943），但成本是基准的2.3倍，而层级架构在成本与准确率的帕累托前沿上占据最优位置。
conclusion: 通过采用语义缓存、模型路由和自适应重试等混合策略，可以在仅增加少量成本的情况下显著提升系统性能，为大规模金融生产环境提供了落地指南。
---

## 摘要
利用大语言模型（LLM）从金融文档中提取结构化信息的应用正迅速普及，然而生产部署在面临基础架构决策时仍缺乏足够的实证指导。我们提出了一个系统的基准测试，比较了四种多智能体编排架构：顺序流水线（sequential pipeline）、并行分发合并（parallel fan-out with merge）、分层主管-工人（hierarchical supervisor-worker）以及反思性自纠错循环（reflexive self-correcting loop）。这些架构在 10,000 份美国证券交易委员会（SEC）申报文件（10-K、10-Q 和 8-K 表格）语料库上，针对五种前沿及开源 LLM 进行了评估。我们的评估涵盖了 25 种提取字段类型，包括治理结构、高管薪酬和财务指标，并从五个维度进行衡量：字段级 F1 分数、文档级准确率、端到端延迟、单文档成本和 Token 效率。研究发现，反思性架构实现了最高的字段级 F1 分数（0.943），但其成本是顺序基准架构的 2.3 倍；而分层架构在成本-准确率帕累托前沿（Pareto frontier）上占据了最有利的位置（F1 为 0.921，成本为 1.4 倍）。我们进一步对语义缓存、模型路由和自适应重试策略进行了消融研究，证明混合配置仅需 1.15 倍的基准成本，即可获得反思性架构 89% 的准确率提升。我们对每日 1,000 到 100,000 份文档的扩展性分析揭示了非显性的吞吐量-准确率下降曲线，为容量规划提供了参考。这些发现为在受监管的金融环境中部署多智能体 LLM 系统的从业者提供了可操作的指导。

## Abstract
The adoption of large language models (LLMs) for structured information extraction from financial documents has accelerated rapidly, yet production deployments face fundamental architectural decisions with limited empirical guidance. We present a systematic benchmark comparing four multi-agent orchestration architectures: sequential pipeline, parallel fan-out with merge, hierarchical supervisor-worker and reflexive self-correcting loop. These are evaluated across five frontier and open-weight LLMs on a corpus of 10,000 SEC filings (10-K, 10-Q and 8-K forms). Our evaluation spans 25 extraction field types covering governance structures, executive compensation and financial metrics, measured along five axes: field-level F1, document-level accuracy, end-to-end latency, cost per document and token efficiency. We find that reflexive architectures achieve the highest field-level F1 (0.943) but at 2.3x the cost of sequential baselines, while hierarchical architectures occupy the most favorable position on the cost-accuracy Pareto frontier (F1 0.921 at 1.4x cost). We further present ablation studies on semantic caching, model routing and adaptive retry strategies, demonstrating that hybrid configurations can recover 89\% of the reflexive architecture's accuracy gains at only 1.15x baseline cost. Our scaling analysis from 1K to 100K documents per day reveals non-obvious throughput-accuracy degradation curves that inform capacity planning. These findings provide actionable guidance for practitioners deploying multi-agent LLM systems in regulated financial environments.

---

## 论文详细总结（自动生成）

### 论文总结：金融文档处理的多智能体 LLM 架构基准测试

#### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于金融服务行业中大规模监管文件（如 SEC 的 10-K、10-Q 表格）的结构化信息提取。虽然 LLM 已被广泛应用，但单次提示词（Single-prompt）提取面临上下文窗口限制、幻觉率高及缺乏验证机制等挑战。多智能体（Multi-Agent）架构虽能解决这些问题，但在生产部署中，开发者缺乏关于**编排模式（Orchestration Patterns）**、**成本-准确率权衡**以及**大规模扩展策略**的实证指导。本研究旨在填补这一空白，为受监管金融环境下的系统设计提供决策依据。

#### 2. 论文提出的方法论
研究对比了四种核心的多智能体编排架构，均由专门的原子智能体（解析器、提取器、分析器、校验器等）组成：
*   **顺序流水线 (Sequential Pipeline)**：固定链式处理，上下文累积传递。特点是确定性强，但错误会向下游传播。
*   **并行分发合并 (Parallel Fan-Out with Merge)**：按领域（如财务、治理）拆分任务并行执行，最后由协调智能体解决冲突。特点是延迟低，吞吐量高。
*   **层级主管-工人 (Hierarchical Supervisor-Worker)**：由主管智能体动态分配任务，并根据置信度评分决定是否重新提取。特点是资源分配灵活，支持异构模型路由。
*   **反思自纠正循环 (Reflexive Self-Correcting Loop)**：引入显式的验证-反馈-修正循环，通过领域规则（如资产负债表平衡）对输出进行批判性修正。特点是准确率上限最高。

#### 3. 实验设计
*   **数据集**：构建了一个包含 **10,000 份 SEC 申报文件**（2021-2024财年）的基准数据集，涵盖 10-K、10-Q 和 8-K 三种类型。
*   **提取字段**：共 25 个字段，分为财务指标（10个）、公司治理（8个）和高管薪酬（7个）。
*   **对比方法 (Benchmark)**：
    *   **模型**：对比了 GPT-4o、Claude 3.5 Sonnet、Gemini 1.5 Pro、Llama 3 70B 和 Mixtral 8x22B。
    *   **架构**：上述四种编排模式。
*   **评估维度**：字段级 F1 分数、文档级准确率、端到端延迟、单文档成本、Token 效率。

#### 4. 资源与算力
*   **硬件环境**：使用了 8 台机器组成的集群，每台配置为 96 核 CPU、384 GB RAM 和 **4× NVIDIA A100 80GB GPU**。
*   **软件框架**：开源模型通过 vLLM 部署，支持张量并行；编排使用 LangGraph v0.2。
*   **实验时长**：整个实验运行周期为 **21 天**。

#### 5. 实验数量与充分性
*   **实验规模**：总计进行了 **200,000 次文档级评估**（4 架构 × 5 模型 × 10,000 文档），样本量极大，具有统计学意义。
*   **消融实验**：针对层级架构进行了三类深入消融研究：语义缓存（降低成本）、模型路由（异构模型组合）和自适应重试策略。
*   **扩展性分析**：模拟了每日 1K 到 100K 文档的处理压力，分析性能衰减。
*   **客观性**：通过 12 名持有 CFA/CPA 证书的专业人员进行人工标注和裁决，确保了 Ground Truth 的权威性。

#### 6. 论文的主要结论与发现
*   **性能之王**：反思型架构（Reflexive）准确率最高（F1 0.943），但成本是顺序架构的 2.3 倍。
*   **性价比之王**：**层级架构（Hierarchical）**位于帕累托前沿最优位置，以 60% 的成本实现了反思型 98.5% 的准确率。
*   **优化潜力**：通过“层级-优化”配置（缓存+路由+重试），仅需 1.15 倍基准成本即可获得显著的准确率提升。
*   **扩展性悖论**：反思型架构在超大规模（>50K 文档/日）下性能崩溃最快，因为迭代循环会导致严重的排队延迟和超时截断；顺序架构在极端规模下最稳健。
*   **失败模式**：顺序架构易受跨表引用失败影响；反思型架构则可能陷入“过度思考”，在处理模糊披露时产生震荡。

#### 7. 优点
*   **工程落地导向**：不仅关注准确率，还将成本、延迟和扩展性作为核心指标，对工业界极具参考价值。
*   **细粒度分析**：提供了详细的失败分类学（12 种失败模式），揭示了不同架构的内在缺陷。
*   **优化策略实用**：提出的模型路由（2-tier/3-tier）和语义缓存方案能直接应用于生产系统。

#### 8. 不足与局限
*   **地域局限**：数据集仅限于英文 SEC 文件，未测试其他语言或会计准则（如 IFRS）下的表现。
*   **时效性风险**：成本分析基于 2025 年 1 月的 API 定价，随着模型厂商调价，具体的成本比例可能发生变化。
*   **模型漂移**：未考虑 API 模型随时间更新可能带来的性能波动（Model Degradation）。
*   **复杂度估计**：虽然提出了动态架构切换的设想，但如何准确预判文档提取难度仍是一个未解决的挑战。

（完）
