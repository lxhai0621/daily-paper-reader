---
title: Solver-Aided Verification of Policy Compliance in Tool-Augmented LLM Agents
title_zh: 工具增强型大语言模型智能体中策略合规性的求解器辅助验证
authors: "Cailin Winston, Claris Winston, René Just"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20449v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 用于LLM智能体工具使用策略合规性的SMT求解器辅助框架
tldr: 针对工具增强型大语言模型（TaLLM）在敏感应用中难以严格遵守操作策略的问题，本文提出一种基于SMT求解器的验证框架。该方法通过LLM辅助和人工引导将自然语言策略转化为SMT-LIB-2.0形式逻辑约束，并在运行时利用Z3求解器拦截并验证工具调用。实验表明，该框架在不损失任务准确率的前提下，显著降低了策略违规率，为提升TaLLM的可靠性提供了形式化保障。
selection_source: fresh_fetch
motivation: 现有的TaLLM主要依靠上下文提示来遵循策略，无法为工具调用的合规性提供可靠的硬性保障。
method: 将自然语言策略转化为形式逻辑约束，并在运行时使用Z3求解器对拦截的工具调用进行前置条件检查。
result: 在TauBench基准测试中，该框架在保持任务准确率的同时，有效减少了违反策略的工具调用行为。
conclusion: 将形式化推理集成到TaLLM执行流程中，能显著提升工具调用的合规性与系统的整体可靠性。
---

## 摘要
工具增强型大语言模型（TaLLMs）通过调用外部工具的能力扩展了 LLM，使其能够与现实世界环境进行交互。然而，在客户服务和业务流程自动化等敏感应用中部署 TaLLMs 的一个主要限制是，在工具使用和智能体行为方面缺乏对特定领域运营策略的可靠合规性。目前的方法仅通过在 LLM 上下文中包含策略描述来引导 LLM 遵守策略，但这无法保证能够防止违规行为的发生。在本文中，我们引入了一个 SMT 求解器辅助框架，用于强制执行 TaLLM 智能体中的工具使用策略合规性。具体而言，我们采用一种 LLM 辅助、人工引导的方法，将以自然语言指定的工具使用策略转换为针对智能体可观测状态和工具参数的形式逻辑（SMT-LIB-2.0）约束。在运行时，计划的工具调用会被拦截，并使用 Z3 求解器根据约束条件进行检查，作为工具调用的前置条件。违反策略的工具调用将被拦截。我们在 TauBench 基准测试上进行了评估，结果表明，求解器辅助的策略检查在保持整体任务准确性的同时，减少了策略违规行为。这些结果表明，将形式化推理集成到 TaLLM 的执行中可以提高工具调用策略的合规性和整体可靠性。

## Abstract
Tool-augmented Large Language Models (TaLLMs) extend LLMs with the ability to invoke external tools, enabling them to interact with real-world environments. However, a major limitation in deploying TaLLMs in sensitive applications such as customer service and business process automation is a lack of reliable compliance with domain-specific operational policies regarding tool-use and agent behavior. Current approaches merely steer LLMs to adhere to policies by including policy descriptions in the LLM context, but these provide no guarantees that policy violations will be prevented. In this paper, we introduce an SMT solver-aided framework to enforce tool-use policy compliance in TaLLM agents. Specifically, we use an LLM-assisted, human-guided approach to translate natural-language-specified tool-use policies into formal logic (SMT-LIB-2.0) constraints over agent-observable state and tool arguments. At runtime, planned tool calls are intercepted and checked against the constraints using the Z3 solver as a pre-condition to the tool call. Tool invocations that violate the policy are blocked. We evaluated on the TauBench benchmark and demonstrate that solver-aided policy checking reduces policy violations while maintaining overall task accuracy. These results suggest that integrating formal reasoning into TaLLM execution can improve tool-call policy compliance and overall reliability.

---

## 论文详细总结（自动生成）

### 论文总结：工具增强型大语言模型智能体中策略合规性的求解器辅助验证

#### 1. 论文的核心问题与整体含义（研究动机和背景）
随着工具增强型大语言模型（TaLLM）在客户服务、业务自动化等敏感领域的广泛应用，如何确保智能体严格遵守复杂的业务策略（如退改签规则、权限控制）成为关键挑战。
*   **核心问题**：现有的策略执行方法主要依赖于在系统提示词（System Prompt）中加入自然语言描述，或者使用基于启发式的护栏（Guardrails）。这些方法缺乏形式化语义，无法提供“不违规”的硬性逻辑保证。
*   **研究动机**：将工具调用合规性建模为一个**约束满足问题（CSP）**，通过引入形式化验证工具（SMT 求解器），在工具执行前进行拦截和验证，从而为智能体行为提供确定性的安全保障。

#### 2. 论文提出的方法论
论文提出了一种集成 SMT 求解器的策略检查框架，其核心流程如下：
*   **策略形式化（离线阶段）**：采用“LLM 辅助 + 人工引导”的方法，将自然语言策略文档翻译为 **SMT-LIB-2.0** 形式逻辑约束。定义了未解释常量（状态变量）、排序（枚举类型）和谓词（逻辑条件）。
*   **运行时拦截与验证（在线阶段）**：
    1.  **拦截**：当智能体计划调用工具时，框架拦截该调用。
    2.  **事实提取**：利用 LLM 作为“事实提取器”，从对话历史和工具参数中提取具体的变量值（如当前时间、用户会员等级等）。
    3.  **约束构建**：将提取的事实转化为逻辑断言，并与预定义的策略编码结合，形成完整的 SMT 公式。
    4.  **求解验证**：使用 **Z3 求解器** 检查公式的可满足性。
        *   **SAT（可满足）**：工具调用符合策略，允许执行。
        *   **UNSAT（不可满足）**：违反策略，拦截调用。
*   **反馈循环**：如果被拦截，框架会将“最小不可满足核心”（Min Unsat Core）作为解释反馈给智能体，引导其重新规划（最多重试 3 次）。

#### 3. 实验设计
*   **数据集/场景**：使用了 **$\tau$-bench (Tau-bench)** 基准测试中的**航空领域（Airline Domain）**。该场景包含 50 个任务、13 个工具以及一份长达 1242 词的复杂策略文档（涉及退款、补偿、改签等规则）。
*   **对比方法**：
    *   **Baseline**：仅通过 Prompt 引导的 GPT-4 智能体。
    *   **Solver-Aided Agent**：集成了 SMT 求解器辅助检查的 GPT-4 智能体。
*   **评估指标**：使用 $pass \wedge k$ 指标（衡量在 $k$ 次独立试验中任务全部成功的概率），以及工具调用的精确率（Precision）和召回率（Recall）。

#### 4. 资源与算力
*   **模型使用**：智能体和环境模拟使用 **GPT-4**，事实提取器使用 **GPT-4o**。
*   **求解器**：使用 **Z3 Solver**。
*   **算力说明**：论文未明确提及具体的 GPU 型号或训练时长。由于该方法属于推理侧（Inference-time）的框架，不涉及模型训练，主要开销在于 LLM API 调用和 Z3 求解器的计算（通常在毫秒级）。

#### 5. 实验数量与充分性
*   **实验规模**：针对航空领域的 50 个任务，每个任务进行了 $k=4$ 次试验，总计数百次对话交互。
*   **消融/探索实验**：论文详细讨论了四种不同的策略翻译设计方案（从纯 LLM 生成到人工微调），分析了自动化翻译的局限性。
*   **充分性评价**：实验设计较为客观，使用了标准化的第三方基准测试。虽然仅聚焦于航空领域，但该领域的策略复杂度足以验证形式化方法的有效性。

#### 6. 论文的主要结论与发现
*   **违规率显著降低**：求解器辅助框架将无效工具调用的比例从基准测试的高位降低至 29%。
*   **一致性提升**：随着试验次数 $k$ 的增加，求解器辅助智能体的 $pass \wedge k$ 分数下降更慢（下降 26% vs 基准的 40%），表明其行为更加稳定可靠。
*   **性能权衡**：虽然提高了精确率（减少错误调用），但也导致了召回率的轻微下降（某些正确的调用可能因提取事实错误而被误拦截）。
*   **翻译挑战**：完全自动化的自然语言到 SMT 的翻译目前仍不可靠，存在语法错误、语义不全和约束过松等问题，仍需人工介入。

#### 7. 优点
*   **硬性保证**：不同于概率性的 Prompt 引导，SMT 求解器提供了基于逻辑的确定性合规检查。
*   **可解释性**：通过“最小不可满足核心”提供违规原因，帮助智能体进行更有针对性的重规划。
*   **模块化**：策略检查器作为一个外部网关，无需重新训练或微调底层的 LLM。

#### 8. 不足与局限
*   **人工依赖**：策略的逻辑编码过程仍高度依赖人工审查和微调，难以实现完全自动化的端到端部署。
*   **事实提取风险**：如果 LLM 事实提取器从对话中提取了错误信息，会导致求解器得出错误的验证结果（Garbage in, Garbage out）。
*   **应用限制**：目前仅在结构化程度较高的工具调用场景下有效，对于模糊的、非工具性的行为策略（如语气要求）难以建模。
*   **延迟开销**：每次工具调用前增加了一次 LLM 提取和一次 SMT 求解，可能会增加系统的响应延迟。

（完）
