---
title: "UI-Voyager: A Self-Evolving GUI Agent Learning via Failed Experience"
title_zh: UI-Voyager：一种通过失败经验学习的自进化 GUI 智能体
authors: "Zichuan Lin, Feiyu Liu, Yijun Yang, Jiafei Lyu, Yiming Gao, Yicheng Liu, Zhicong Lu, Yangbin Yu, Mingyu Yang, Junyou Li, Deheng Ye, Jie Jiang"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24533v1"
tags: ["query:agent"]
score: 9.0
evidence: 使用多模态LLM的自我进化移动GUI智能体
tldr: UI-Voyager 是一种新型自进化移动 GUI 智能体，旨在解决长程任务中学习效率低和奖励稀疏的问题。它采用两阶段学习框架：首先通过拒绝微调（RFT）实现数据与模型的自主协同进化；随后引入群体相对自蒸馏（GRSD），利用成功路径纠正失败路径中的关键决策点。该方法无需人工标注，在 AndroidWorld 测试中表现优异，超越了人类水平。
selection_source: fresh_fetch
motivation: 现有的 GUI 智能体在处理长程任务时，难以从失败轨迹中高效学习，且面临稀疏奖励下的信用分配难题。
method: 提出两阶段框架，结合拒绝微调（RFT）进行自主进化，并利用群体相对自蒸馏（GRSD）通过成功经验修正失败决策。
result: "在 AndroidWorld 基准测试中，4B 规模的模型达到了 81.0% 的成功率，显著超越了现有基线模型及人类表现。"
conclusion: UI-Voyager 证明了通过自主学习失败经验和自蒸馏技术，可以在无需人工标注的情况下实现高效、高性能的移动 GUI 自动化。
---

## 摘要
随着多模态大语言模型（MLLMs）的发展，自主移动 GUI 智能体引起了越来越多的关注。然而，现有方法在长程 GUI 任务中仍面临从失败轨迹中学习效率低下以及在稀疏奖励下信用分配模糊的问题。为此，我们提出了 UI-Voyager，一种新型的两阶段自进化移动 GUI 智能体。在第一阶段，我们采用了拒绝微调（RFT），实现了数据和模型在完全自主循环中的持续协同进化。第二阶段引入了群体相对自蒸馏（GRSD），该方法识别群体展开（rollouts）中的关键分叉点，并从成功轨迹中构建密集的步骤级监督，以纠正失败的轨迹。在 AndroidWorld 上的广泛实验表明，我们的 4B 模型实现了 81.0% 的 Pass@1 成功率，优于众多近期的基准模型，并超过了人类水平的表现。消融实验和案例研究进一步验证了 GRSD 的有效性。我们的方法在无需昂贵的人工数据标注的情况下，向高效、自进化且高性能的移动 GUI 自动化迈出了重要一步。

## Abstract
Autonomous mobile GUI agents have attracted increasing attention along with the advancement of Multimodal Large Language Models (MLLMs). However, existing methods still suffer from inefficient learning from failed trajectories and ambiguous credit assignment under sparse rewards for long-horizon GUI tasks. To that end, we propose UI-Voyager, a novel two-stage self-evolving mobile GUI agent. In the first stage, we employ Rejection Fine-Tuning (RFT), which enables the continuous co-evolution of data and models in a fully autonomous loop. The second stage introduces Group Relative Self-Distillation (GRSD), which identifies critical fork points in group rollouts and constructs dense step-level supervision from successful trajectories to correct failed ones. Extensive experiments on AndroidWorld show that our 4B model achieves an 81.0% Pass@1 success rate, outperforming numerous recent baselines and exceeding human-level performance. Ablation and case studies further verify the effectiveness of GRSD. Our method represents a significant leap toward efficient, self-evolving, and high-performance mobile GUI automation without expensive manual data annotation.

---

## 论文详细总结（自动生成）

以下是对论文《UI-Voyager: A Self-Evolving GUI Agent Learning via Failed Experience》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：移动端 GUI 智能体在处理长程（Long-horizon）任务时面临两大挑战：一是**学习效率低下**，大量的失败尝试轨迹被直接丢弃，未能转化为学习经验；二是**信用分配模糊**，在稀疏奖励（仅任务结束时告知成功或失败）的情况下，智能体难以判断是哪一步操作导致了最终的失败。
*   **核心目标**：开发一种能够从失败经验中自主学习、无需人工标注、且能高效解决复杂移动端任务的自进化智能体。

### 2. 方法论
UI-Voyager 采用了两阶段的自进化优化管线：

*   **第一阶段：拒绝微调（Rejection Fine-Tuning, RFT）**
    *   **核心思想**：通过“生成-过滤-训练”的闭环实现模型能力的初步提升。
    *   **流程**：利用种子任务生成器合成新任务；智能体自主执行并产生多条轨迹；利用基于规则的验证器（Verifier）筛选出成功的轨迹；对模型进行有监督微调（SFT）。此过程迭代进行，使数据和模型协同进化。
*   **第二阶段：群体相对自蒸馏（Group Relative Self-Distillation, GRSD）**
    *   **核心思想**：利用同一任务中“成功轨迹”作为“失败轨迹”的导师，进行步骤级的精准纠错。
    *   **关键技术：分叉点检测（Fork Point Detection）**：
        *   使用 **SSIM（结构相似性指数）** 比较成功轨迹和失败轨迹的屏幕截图。
        *   识别出“分叉点”：即两段轨迹在某一时刻处于相同的 UI 状态（SSIM 高），但随后采取了不同的动作导致结果分化。
    *   **自蒸馏训练**：将失败轨迹在分叉点处的错误动作替换为成功轨迹中的正确动作，构建“自纠错样本”，通过标准的交叉熵损失进行训练。这为模型提供了密集的步骤级监督信号，解决了信用分配问题。

### 3. 实验设计
*   **基准测试（Benchmark）**：**AndroidWorld**，包含 116 个跨越真实移动应用的复杂任务。
*   **对比方法**：
    *   **通用多模态模型**：Qwen3-VL 系列（2B 到 235B）、Gemini-2.5-Pro。
    *   **专用 GUI 智能体**：UI-Tars、MAI-UI、Step-GUI、UI-Venus、Mobile-Agent 等。
    *   **强化学习基线**：GRPO（群体相对策略优化）和 PPO（近端策略优化）。
*   **评估指标**：Pass@1 成功率。

### 4. 资源与算力
*   **模型骨干**：以 **Qwen3-VL-4B-Instruct** 为基础模型。
*   **训练数据**：使用了 AndroidWorld 提供的超过 7000 个训练任务。
*   **算力说明**：论文中**未明确列出**具体的 GPU 型号、数量或总训练时长。但提到在评估阶段为了确保可靠性，对 UI-Voyager 进行了 64 次独立运行取平均值。

### 5. 实验数量与充分性
*   **实验规模**：对比了约 25 种不同规模和类型的基线模型。
*   **消融实验**：详细对比了 RFT 的迭代轮数（4 轮）对性能的影响，以及 GRSD 与传统强化学习方法（PPO、GRPO）在相同起点的表现差异。
*   **案例研究**：针对 BrowserMaze（迷宫导航）和系统设置等具体任务展示了分叉点检测的可视化过程。
*   **充分性评价**：实验设计较为全面，通过 64 个随机种子的平均分证明了结果的稳健性，消融实验有力地支撑了 GRSD 优于传统 RL 方法的结论。

### 6. 主要结论与发现
*   **性能突破**：仅有 **4B 参数**的 UI-Voyager 达到了 **81.0%** 的成功率，不仅刷新了 SOTA，还超过了人类专家水平（80.0%）。
*   **以小博大**：4B 模型在 GUI 特定任务上的表现优于参数量大几十倍的模型（如 235B 的 MAI-UI 或 Qwen3-VL）。
*   **GRSD 的优越性**：在稀疏奖励环境下，GRSD 比 PPO 和 GRPO 更有效，因为它能直接利用失败轨迹中的有用上下文，并提供精确的纠错信号。

### 7. 优点与亮点
*   **失败经验的再利用**：不同于以往只看重成功数据的做法，该方法通过分叉点检测挖掘了失败轨迹的价值。
*   **高效的信用分配**：通过 SSIM 这种轻量级的图像匹配技术定位关键决策点，无需调用昂贵的外部大模型进行状态标注。
*   **完全自主**：整个进化过程不需要人工干预或手动标注数据，具有极强的可扩展性。

### 8. 不足与局限
*   **动作空间限制**：实验基于 AndroidWorld 预定义的离散动作空间（点击、滑动等），可能无法完全模拟真实世界中连续、异步的触控动态。
*   **实时性挑战**：SSIM 匹配对 UI 动画、光标闪烁或通知弹窗较为敏感，在实时流式环境下可能会产生误判。
*   **环境单一**：目前主要在 AndroidWorld 闭环环境中验证，尚未展示在其他操作系统（如 iOS 或 Windows）上的泛化能力。

（完）
