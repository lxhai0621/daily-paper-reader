---
title: "AgenticRec: End-to-End Tool-Integrated Policy Optimization for Ranking-Oriented Recommender Agents"
title_zh: AgenticRec：面向排序推荐智能体的端到端工具集成策略优化
authors: "Tianyi Li, Zixuan Wang, Guidong Lei, Xiaodong Li, Hui Li"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21613v1"
tags: ["query:agent"]
score: 9.0
evidence: 具有工具调用能力的排序导向智能体推荐框架
tldr: AgenticRec是一个面向排序的智能体推荐框架，旨在解决现有LLM推荐智能体在推理与排序反馈脱节及细粒度偏好捕捉不足的问题。该框架通过集成推荐专用工具的ReAct循环、列表级组相对策略优化（list-wise GRPO）以及渐进式偏好细化（PPR）机制，实现了从推理、工具调用到最终排序生成的全链路端到端优化，显著提升了推荐性能。
selection_source: fresh_fetch
motivation: 现有推荐智能体在中间推理过程与最终排序反馈之间存在脱节，且难以捕捉用户的细粒度偏好。
method: 提出了集成专用工具的ReAct循环，并结合列表级GRPO算法和渐进式偏好细化（PPR）来优化决策轨迹。
result: 实验结果表明，AgenticRec在多个基准测试中显著优于现有基准模型。
conclusion: 统一推理、工具使用和排序优化对于提升推荐智能体的性能至关重要。
---

## 摘要
基于大语言模型的推荐智能体为推荐系统提供了一种极具前景的范式。然而，现有的推荐智能体通常面临中间推理与最终排序反馈脱节的问题，且无法捕捉细粒度的偏好。为了解决这一问题，我们提出了 AgenticRec，这是一个面向排序的智能体推荐框架，能够在稀疏隐式反馈下优化整个决策轨迹（包括中间推理、工具调用和最终排序列表生成）。本研究具有三个核心贡献：首先，我们设计了一套集成在 ReAct 循环中的推荐专用工具，以支持基于证据的推理；其次，我们提出了理论上无偏的列表级组相对策略优化（list-wise GRPO），以最大化排序效用，确保复杂工具使用轨迹中的准确信用分配；最后，我们引入了渐进式偏好细化（PPR）来解决细粒度的偏好歧义。通过从排序违规中挖掘难负样本并应用双向偏好对齐，PPR 最小化了成对排序误差的凸上界。在基准数据集上的实验证实，AgenticRec 显著优于现有基准模型，验证了统一推理、工具使用和排序优化的必要性。

## Abstract
Recommender agents built on Large Language Models offer a promising paradigm for recommendation. However, existing recommender agents typically suffer from a disconnect between intermediate reasoning and final ranking feedback, and are unable to capture fine-grained preferences. To address this, we present AgenticRec, a ranking-oriented agentic recommendation framework that optimizes the entire decision-making trajectory (including intermediate reasoning, tool invocation, and final ranking list generation) under sparse implicit feedback. Our approach makes three key contributions. First, we design a suite of recommendation-specific tools integrated into a ReAct loop to support evidence-grounded reasoning. Second, we propose theoretically unbiased List-Wise Group Relative Policy Optimization (list-wise GRPO) to maximize ranking utility, ensuring accurate credit assignment for complex tool-use trajectories. Third, we introduce Progressive Preference Refinement (PPR) to resolve fine-grained preference ambiguities. By mining hard negatives from ranking violations and applying bidirectional preference alignment, PPR minimizes the convex upper bound of pairwise ranking errors. Experiments on benchmarks confirm that AgenticRec significantly outperforms baselines, validating the necessity of unifying reasoning, tool use, and ranking optimization.

---

## 论文详细总结（自动生成）

以下是对论文《AgenticRec: End-to-End Tool-Integrated Policy Optimization for Ranking-Oriented Recommender Agents》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的基于大语言模型（LLM）的推荐智能体存在两个主要缺陷：
    1.  **推理与反馈脱节**：中间推理过程和工具调用通常依赖通用的语言先验或手工提示词，而非直接由推荐系统的排序反馈驱动。
    2.  **细粒度偏好捕捉不足**：在处理高度相似的候选项目时，仅靠粗粒度的列表级信号难以区分细微的偏好差异。
*   **研究背景**：推荐本质上是一个基于大规模隐式反馈的排序问题。虽然 LLM 具有强大的语义理解能力，但需要通过多步推理和外部证据（如协同过滤信号）来弥补文本信息的不足。

### 2. 方法论
AgenticRec 提出了一个两阶段的端到端训练框架：
*   **工具集成的排序推理（TIRR）**：
    *   采用 **ReAct** 范式（Think-Act-Observation），将工具调用嵌入推理链。
    *   **推荐专用工具箱**：包括用户画像工具、项目信息查询、行为统计工具以及基于协同过滤（SASRec）的 U2U/I2I 工具。
    *   **决策预算**：限制最大工具调用次数（$T_{max}=10$），鼓励高效决策。
*   **列表级组相对策略优化（List-Wise GRPO）**：
    *   **核心思想**：利用 GRPO 算法，通过一组采样轨迹的相对得分来优化策略，降低方差。
    *   **奖励设计**：以 NDCG@K 作为主要奖励，辅以格式约束惩罚和成功的工具使用奖励。
    *   **端到端优化**：将排序结果作为最终 Action，使奖励能回传至中间的推理和工具调用步骤。
*   **渐进式偏好细化（PPR）**：
    *   **难负样本挖掘**：从智能体预测错误的排序中提取排在正样本之前的负样本。
    *   **双向偏好推理**：同时训练智能体识别“哪个更可能被买”和“哪个更不可能被买”，通过双向对齐强化偏好边界。

### 3. 实验设计
*   **数据集**：Amazon 2023 平台的四个子集：CDs、Instruments、Office、Games。
*   **任务设置**：从 20 个候选项目中选出 Top-10 进行排序。
*   **对比基准（Baselines）**：
    1.  **传统序列推荐**：Caser, GRU4Rec, SASRec, ReaRec。
    2.  **免训练 LLM 方法**：LLMRank (GPT-4), InteRecAgent (GPT-4)。
    3.  **可训练 LLM 方法**：TALLRec, LLaRA, S-DPO, ReRe。
*   **评估指标**：Hit Ratio@K (H@K) 和 NDCG@K (N@K)，K 取 1, 5, 10。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 台 NVIDIA A800 GPU (80GB)**。
*   **模型骨干**：主要使用 **Qwen3-4B-Instruct**（实验中也测试了 1.7B 和 8B 版本以验证扩展性）。
*   **训练细节**：第一阶段训练 3 个 epoch，第二阶段（PPR）训练 1 个 epoch。学习率设为 $1 \times 10^{-6}$，Batch Size 为 64。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同领域的数据集上进行了全量对比，涵盖了从传统模型到最先进 LLM 推荐模型的 10 种基准方法。
*   **消融实验**：详细对比了“纯推理”、“工具集成”、“端到端训练”以及“PPR 细化”的效果，验证了各模块的必要性。
*   **深度分析**：包括工具调用频率分析、GRPO 组大小（Group Size）的影响分析、模型参数规模的扩展性分析（Scaling Ability）以及案例研究。
*   **评价**：实验设计全面且客观，通过跨领域数据集和多种基准的对比，充分证明了方法的有效性。

### 6. 主要结论与发现
*   **性能领先**：AgenticRec 在所有数据集和指标上均显著优于现有最强基准（如 LLaRA 和 ReRe）。
*   **工具调用的必要性**：在端到端训练下，工具集成（TIRR）能显著提升性能，而免训练模式下工具调用有时反而引入噪声。
*   **PPR 的作用**：通过挖掘排序违规进行双向训练，能有效提升模型在 Top-1 和 Top-5 上的精确度。
*   **扩展性**：模型性能随参数规模（1.7B -> 8B）增加而稳步提升，显示出良好的泛化潜力。

### 7. 优点与亮点
*   **端到端闭环**：首次实现了将“推理-工具调用-排序生成”全链路通过列表级奖励进行统一优化。
*   **理论支撑**：论文提供了 List-wise GRPO 梯度估计的无偏性证明，以及 PPR 最小化排序误差概率上界的理论推导。
*   **工具感知**：将协同过滤等传统推荐信号转化为智能体可调用的工具，实现了神经符号化的结合。

### 8. 不足与局限
*   **推理延迟**：由于采用了 ReAct 循环和多次工具调用，推理过程的 Token 消耗和时间延迟会高于单次生成的模型。
*   **候选集依赖**：实验设定在已有的候选集（20个）上进行重排序，尚未探讨在大规模全量库检索场景下的表现。
*   **长序列挑战**：虽然论文提到未来将引入记忆机制，但目前对极长用户历史记录的处理能力仍受限于 LLM 的上下文窗口。

（完）
