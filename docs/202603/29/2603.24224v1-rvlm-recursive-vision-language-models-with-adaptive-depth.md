---
title: "RVLM: Recursive Vision-Language Models with Adaptive Depth"
title_zh: RVLM：具有自适应深度的递归视觉语言模型
authors: "Nicanor Mayumu, Zeenath Khan, Melodena Stephens, Patrick Mukala, Farhad Oroumchian"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24224v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 带有视觉子智能体的迭代生成-执行循环
tldr: 针对医疗AI系统推理过程不透明及固定迭代步数效率低的问题，本文提出RVLM框架。该框架通过“生成-执行”循环，利用Python代码驱动视觉子代理进行迭代推理，使诊断过程可审计。同时引入RRouter实现自适应深度控制。在脑部MRI和胸部X射线数据集上的实验表明，RVLM在保持高一致性的同时，能有效识别复杂临床特征并生成结构化报告。
selection_source: fresh_fetch
motivation: 传统医疗视觉语言模型存在推理过程不可审计以及固定迭代次数导致计算资源分配不均的问题。
method: 提出RVLM框架，通过Python代码驱动的迭代推理循环实现诊断证据的可追溯性，并利用RRouter根据任务复杂度自适应调整迭代深度。
result: 在BraTS 2023和MIMIC-CXR数据集上，该模型在病灶检测、跨模态差异识别及结构化报告生成方面表现出高一致性和准确性。
conclusion: RVLM通过结合可解释的迭代推理与自适应计算，为构建符合临床监管要求的透明、高效医疗AI系统提供了新方案。
---

## 摘要
医疗人工智能系统面临两个根本性的局限。首先，传统的视觉语言模型（VLM）执行单次推理，产生黑盒预测，无法在临床术语层面进行审计或解释。其次，展示中间步骤的迭代推理系统依赖于固定的迭代预算，在简单案例上浪费计算资源，而在复杂案例上深度不足。我们通过一个统一的框架解决了这两个局限。RVLM 用迭代的“生成-执行”循环取代了单次推理：在每一步中，模型编写 Python 代码，调用视觉子代理，处理图像并积累证据。每一个诊断结论都基于可执行代码，满足了临床 AI 治理框架的可审计性要求。RRouter 使迭代深度具备自适应性：一个轻量级控制器根据任务复杂度特征预测最佳预算，随后监控进度并在推理停滞时提前终止。我们在 BraTS 2023 脑膜瘤（脑部 MRI）和 MIMIC-CXR（胸部 X 光）数据集上，使用未经微调的 Gemini 2.5 Flash 进行了评估。在多次重复运行中，RVLM 在显著发现（如肿块存在和增强）上表现出高度一致性，并能检测出流体衰减反转恢复（FLAIR）信号特征与分割边界之间的跨模态差异。在 MIMIC-CXR 上，它能生成结构化报告并正确识别特定视角的伪影。代码：https://github.com/nican2018/rvlm。

## Abstract
Medical AI systems face two fundamental limitations. First, conventional vision-language models (VLMs) perform single-pass inference, yielding black-box predictions that cannot be audited or explained in clinical terms. Second, iterative reasoning systems that expose intermediate steps rely on fixed iteration budgets wasting compute on simple cases while providing insufficient depth for complex ones. We address both limitations with a unified framework. RVLM replaces single-pass inference with an iterative generate-execute loop: at each step, the model writes Python code, invokes vision sub-agents, manipulates images, and accumulates evidence. Every diagnostic claim is grounded in executable code, satisfying auditability requirements of clinical AI governance frameworks. RRouter makes iteration depth adaptive: a lightweight controller predicts the optimal budget from task-complexity features, then monitors progress and terminates early when reasoning stalls. We evaluate on BraTS 2023 Meningioma (brain MRI) and MIMIC-CXR (chest X-ray) using Gemini 2.5 Flash without fine-tuning. Across repeated runs, RVLM shows high consistency on salient findings (e.g., mass presence and enhancement) and can detect cross-modal discrepancies between Fluid-Attenuated Inversion Recovery (FLAIR) signal characteristics and segmentation boundaries. On MIMIC-CXR, it generates structured reports and correctly recognises view-specific artefacts. Code: https://github.com/nican2018/rvlm.

---

## 论文详细总结（自动生成）

### RVLM：具有自适应深度的递归视觉语言模型论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
医疗 AI 系统在临床应用中面临两大核心瓶颈：
*   **解释性缺失（黑盒问题）：** 传统的视觉语言模型（VLM）通常采用“单次推理”（Single-pass），直接输出诊断结果，缺乏可供临床医生审计或验证的中间推理逻辑。
*   **计算资源分配僵化（固定预算问题）：** 现有的迭代推理系统通常设定固定的最大迭代次数。这导致简单病例浪费计算资源和增加延迟，而复杂病例则可能因迭代不足而无法深入分析。
**研究目标：** 提出一个既能提供可审计推理轨迹，又能根据任务复杂度动态调整计算深度的统一框架。

#### 2. 方法论：核心思想与技术细节
该论文提出了由两个核心组件构成的框架：
*   **RVLM (Recursive Vision-Language Model)：**
    *   **核心思想：** 将单次推理转变为“生成-执行”的递归循环。模型在持久化的 Python REPL 环境中运行。
    *   **技术细节：** 模型不直接输出文本，而是编写 Python 代码来调用视觉原语（如 `describe_image` 描述特定模态、`llm_query_with_images` 进行跨模态对比）。
    *   **图像操作：** 支持程序化图像处理（裁剪 ROI、对比度增强、计算差异图），将图像视为第一类对象存储在 `context_images` 中，减少了重复传输原始像素的 Token 开销。
*   **RRouter (Recursion Router)：**
    *   **自适应深度：** 引入预飞行（Pre-flight）复杂度评估器。通过计算**标签熵（Label Entropy）**、**肿瘤体积**、**子区域数量**和**微小区域指标**来预测最佳迭代预算 $n^*$。
    *   **停滞检测（Stall Detection）：** 在推理过程中监控进度，若连续两次迭代未产生有效输出或新变量，则提前终止。

#### 3. 实验设计
*   **数据集：** 
    1.  **BraTS 2023 Meningioma：** 脑膜瘤 MRI 数据，包含四种模态（T1n, T1ce, T2w, FLAIR）及分割掩码。
    2.  **MIMIC-CXR：** 胸部 X 光片，包含 PA/Lateral/AP 等多个视角。
*   **Benchmark 与对比方法：** 
    *   主要对比对象是 **Single-pass VLM（单次推理基准）**。
    *   评估维度包括诊断一致性、跨模态差异检测能力以及推理轨迹的可审计性。
*   **后端模型：** 使用 **Gemini 2.5 Flash**，采取 **Zero-shot（无微调）** 策略，验证框架的通用性。

#### 4. 资源与算力
*   **算力说明：** 论文未提及具体的本地 GPU 训练资源，因为该方法基于现有的闭源大模型 API（Gemini 2.5 Flash）进行提示词工程和环境构建。
*   **推理开销：** 论文详细记录了推理统计数据。例如，一个复杂的脑部 MRI 案例耗时约 67-95 秒，消耗约 1.3k - 1.5k 个输入 Token。通过 REPL 引用图像索引，比传统多轮对话节省了约 70-80% 的图像传输成本。

#### 5. 实验数量与充分性
*   **实验规模：** 进行了 16 次独立重复运行以测试可靠性；针对典型病例（如 WHO 1 级脑膜瘤、纯增强肿瘤、胸部 X 光伪影）进行了深入的案例研究。
*   **充分性评价：** 
    *   **优点：** 实验涵盖了两种截然不同的临床场景（3D 脑部 MRI 和 2D 胸部 X 光），证明了框架的泛化能力。
    *   **不足：** 样本量较小，主要侧重于定性分析和一致性验证，缺乏大规模的定量指标（如在数千个样本上的准确率、灵敏度或 F1 分数对比）。

#### 6. 主要结论与发现
*   **可审计性：** RVLM 能够生成完整的 Python 执行日志，每一项诊断声明都对应具体的可执行代码，符合欧盟 AI 法案等监管要求。
*   **发现能力：** RVLM 能够自主检测出单次推理模型无法识别的“跨模态差异”（例如 FLAIR 信号异常但分割掩码未覆盖的区域）。
*   **效率提升：** RRouter 成功将简单病例的迭代次数从固定的 12 次降至 3 次，显著降低了延迟和成本，同时保证了复杂病例的分析深度。
*   **一致性：** 在显著临床发现上表现出 90% 以上的一致性，其变异性模式与人类放射科医生的跨读者差异相似。

#### 7. 优点
*   **透明度高：** 将“推理”具象化为“代码”，使 AI 的思考过程可回溯、可验证。
*   **自适应计算：** 解决了迭代推理中“一刀切”的预算问题，优化了精度与成本的权衡。
*   **无需微调：** 仅通过提示词协议即可适配不同医疗任务，降低了部署门槛。
*   **临床友好：** 自动将代码轨迹转换为结构化的 PDF 报告，兼顾了技术审计与临床阅读需求。

#### 8. 不足与局限
*   **定量评估不足：** 缺乏大规模临床金标准数据集的统计学性能评估（如与资深医生对比的 ROC 曲线）。
*   **维度限制：** 目前主要处理 2D 轴向切片，尚未完全实现全容积（3D）的自动切片导航推理。
*   **依赖性：** 高度依赖底层 VLM 的代码生成能力，若基础模型发生幻觉，生成的代码可能无法执行。
*   **实时性：** 由于涉及多次 API 调用和代码执行，其推理速度（分钟级）尚不适用于急诊筛查等高实时性场景。

（完）
