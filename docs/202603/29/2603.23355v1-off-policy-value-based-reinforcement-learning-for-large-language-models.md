---
title: Off-Policy Value-Based Reinforcement Learning for Large Language Models
title_zh: 大语言模型的离策基于价值的强化学习
authors: "Peng-Yuan Wang, Ziniu Li, Tian Xu, Bohan Yang, Tian-Shuo Liu, ChenYang Wang, Xiong-Hui Chen, Yi-Chen Li, Tianyun Yang, Congliang Chen, Yang Yu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23355v1"
tags: ["query:agent"]
score: 9.0
evidence: 大语言模型的离策价值强化学习
tldr: 针对大语言模型强化学习中在线策略方法采样效率低的问题，本文提出了一种基于价值的离线策略强化学习框架 ReVal。该方法通过贝尔曼更新结合逐步一致性信号与轨迹级结果验证，并利用经验回放机制实现数据的多次复用。实验证明，ReVal 在数学推理任务上的收敛速度和最终性能均优于 GRPO，显著提升了长程任务的训练效率。
selection_source: fresh_fetch
motivation: 当前主流的 LLM 强化学习方法多为在线策略，导致昂贵的采样数据仅能使用一次，数据利用效率极低。
method: 提出 ReVal 框架，通过贝尔曼更新整合逐步一致性与轨迹级验证信号，并支持基于经验回放的离线策略学习。
result: "在 DeepSeek-R1-Distill-1.5B 模型上，ReVal 在 AIME24 和 GPQA 任务中分别比 GRPO 提升了 2.7% 和 4.5%。"
conclusion: 基于价值的离线策略强化学习是提升大语言模型训练效率和推理能力的有效替代方案。
---

## 摘要
提高数据利用效率对于在生成轨迹成本高昂的长程任务中扩展强化学习（RL）至关重要。然而，目前主流的大语言模型（LLM）强化学习方法大多是同策（on-policy）的：它们对每批数据仅更新一次后便将其丢弃，随后收集新样本，导致样本效率低下。在这项工作中，我们探索了一种替代的基于价值的 LLM 强化学习框架，该框架天然支持离策（off-policy）学习。我们提出了 ReVal，这是一种基于贝尔曼更新（Bellman-update）的方法，它结合了捕捉内部一致性的步进式信号和源自结果验证的轨迹级信号。ReVal 天然支持基于经验回放池（replay-buffer）的训练，从而实现了对过去轨迹的高效重用。在标准数学推理基准测试上的实验表明，ReVal 不仅收敛更快，而且在最终性能上也优于 GRPO。在 DeepSeek-R1-Distill-1.5B 上，ReVal 提高了训练效率，并在 AIME24 上比 GRPO 提升了 2.7%，在域外基准测试 GPQA 上提升了 4.5%。这些结果表明，基于价值的强化学习是 LLM 训练中基于策略方法的实用替代方案。

## Abstract
Improving data utilization efficiency is critical for scaling reinforcement learning (RL) for long-horizon tasks where generating trajectories is expensive. However, the dominant RL methods for LLMs are largely on-policy: they update each batch of data only once, discard it, and then collect fresh samples, resulting in poor sample efficiency. In this work, we explore an alternative value-based RL framework for LLMs that naturally enables off-policy learning. We propose ReVal, a Bellman-update-based method that combines stepwise signals capturing internal consistency with trajectory-level signals derived from outcome verification. ReVal naturally supports replay-buffer-based training, allowing efficient reuse of past trajectories. Experiments on standard mathematical reasoning benchmarks show that ReVal not only converges faster but also outperforms GRPO in final performance. On DeepSeek-R1-Distill-1.5B, ReVal improves training efficiency and achieves improvement of 2.7% in AIME24 and 4.5% in out-of-domain benchmark GPQA over GRPO. These results suggest that value-based RL is a practical alternative to policy-based methods for LLM training.

---

## 论文详细总结（自动生成）

以下是对论文《Off-Policy Value-Based Reinforcement Learning for Large Language Models》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前大语言模型（LLM）的主流强化学习（RL）方法（如 PPO、GRPO）大多是**同策（On-policy）**的。这意味着模型每更新一次参数，之前采集的昂贵轨迹数据就必须丢弃，必须重新采样。
*   **研究背景**：随着 LLM 转向长程推理（Long-horizon reasoning）和智能体（Agentic）任务，生成一条完整轨迹的时间成本极高。同策方法的数据利用率极低，成为了扩展 RL 训练的主要瓶颈。
*   **研究目标**：探索一种**离策（Off-policy）**的基于价值（Value-based）的 RL 框架，通过复用历史经验（Replay Buffer）来提高采样效率并降低训练成本。

### 2. 方法论：ReVal 框架
*   **核心思想**：将 LLM 的 Logits 直接解释为 **Q 值（Action-Value）**，从而在不增加额外价值网络（Value Model）的前提下，实现单模型的价值学习。
*   **关键技术细节**：
    *   **Logit-as-Q 参数化**：基于前人研究，预训练 LLM 的输出 Logits 隐含了软 Q 值信息，ReVal 直接利用这一特性进行贝尔曼更新。
    *   **校准初始化（Calibrated Initialization）**：针对前人工作（如 TBRM）在奖励为 0 时会导致策略漂移的问题，ReVal 引入了改进的奖励塑造（Reward Shaping）项，确保在无奖励信号时模型能稳定在参考策略（Reference Policy）附近。
    *   **ReVal 损失函数**：通过最小化轨迹级的贝尔曼残差（Bellman Residual）进行训练。该损失函数整合了步进式的一致性信号和轨迹级的结果验证信号。
    *   **经验回放机制（Replay Buffer）**：引入 FIFO（先进先出）缓冲区存储历史轨迹。在每次训练迭代中，从缓冲区随机采样旧数据进行多次梯度更新，实现离策学习。

### 3. 实验设计
*   **模型与数据集**：
    *   **基础模型**：DeepSeek-R1-Distill-1.5B 和 Qwen2.5-Math-7B。
    *   **训练数据**：DeepScaleR 数据集。
    *   **评测基准（Benchmarks）**：涵盖 AIME24、AIME25、AMC、MATH、MINERVA、Olympiad Bench 和 GPQA（域外泛化测试）。
*   **对比方法**：
    *   **GRPO**：目前主流的同策策略梯度方法。
    *   **TBRM**：早期的基于价值但通常用于同策场景的方法。
*   **特殊场景**：设置了 **N=1（有限采样）** 的极端实验，模拟采样成本极高的情况。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量（如 A100 或 H100 的具体规模）。
*   **训练时长与效率**：
    *   在 1.5B 模型实验中，ReVal 达到 SOTA 性能的总耗时为 **6.3 小时**，而 GRPO 为 **7.5 小时**（节省约 18% 的总时间）。
    *   单次参数更新耗时约 2.8s，而单次轨迹生成耗时约 36.8s，证明了“多更新、少生成”的离策策略在时间上的优越性。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个不同架构、不同规模（1.5B 和 7B）的模型上进行了验证。
    *   针对 7 个主流数学与推理基准进行了全面评测。
    *   **消融实验**：深入探讨了 KL 正则化强度（$\beta$）、参考策略更新频率、奖励设计（0/1 奖励 vs 归一化优势）对稳定性的影响。
*   **充分性评价**：实验设计较为充分，不仅关注最终精度，还通过“单样本学习曲线”展示了收敛速度的提升（最高达 4.3 倍加速），客观地证明了离策复用的价值。

### 6. 主要结论与发现
*   **性能超越**：在 DeepSeek-R1-Distill-1.5B 上，ReVal 在 AIME24 上比 GRPO 高出 **2.7%**，在 GPQA 上高出 **4.5%**。
*   **收敛加速**：通过增加数据复用频率，ReVal 在达到相同性能水平时，比 GRPO 快 **4.3 倍**。
*   **低采样优势**：在每轮仅采样 1 条轨迹（N=1）的严苛条件下，ReVal 的领先优势扩大，证明其在数据稀缺场景下的鲁棒性。
*   **泛化能力**：在域外基准 GPQA 上的显著提升表明，基于价值的学习可能比单纯的策略梯度更能捕捉到推理任务的底层逻辑。

### 7. 优点：亮点与创新
*   **单模型高效性**：无需额外的 Critic 网络，保持了与 GRPO 相当的低显存占用，同时获得了离策学习的能力。
*   **理论完备性**：解决了价值学习中的初始化校准问题，通过奖励塑造确保了训练初期的稳定性。
*   **实用性强**：直接兼容现有的分布式训练框架（如 Verl），为长程推理任务提供了一个更经济的训练方案。

### 8. 不足与局限
*   **任务局限性**：实验主要集中在数学推理任务（具有明确的可验证奖励），在更通用的对话或无明确答案的对齐任务中表现尚待验证。
*   **回放策略简单**：目前仅使用了简单的 FIFO 缓冲区，尚未探索**优先经验回放（Prioritized Experience Replay）**等更高级的采样策略。
*   **超参数敏感性**：实验显示 $\beta$ 参数和参考策略更新频率对稳定性影响较大，不同规模模型可能需要精细调优。

（完）
