---
title: "The Cognitive Firewall:Securing Browser Based AI Agents Against Indirect Prompt Injection Via Hybrid Edge Cloud Defense"
title_zh: 认知防火墙：通过边缘-云混合防御保护基于浏览器的 AI 智能体免受间接提示注入攻击
authors: "Qianlong Lan, Anuj Kaul"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23791v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 保护基于浏览器的自主AI智能体
tldr: 针对浏览器AI智能体面临的间接提示注入（IPI）威胁，本文提出“认知防火墙”架构。该架构采用端云协同的分布式计算模式，结合本地视觉哨兵、云端深度规划器和确定性执行守卫，在显著降低攻击成功率的同时，通过本地过滤大幅提升了响应速度并保护了隐私，为构建安全的交互式LLM智能体提供了实践基础。
selection_source: fresh_fetch
motivation: 浏览器AI智能体易受间接提示注入攻击，而现有的纯云端或纯端侧防御方案在性能、隐私和安全性之间难以平衡。
method: 提出一种三阶段拆分计算架构，通过本地视觉哨兵、云端深度规划器和确定性执行守卫共同拦截攻击。
result: "该混合架构将攻击成功率降低至1%以下，并比纯云端基准方案在延迟上优化了约17,000倍。"
conclusion: 端云协同的分布式防御与确定性执行约束相结合，能有效兼顾AI智能体的安全性和运行效率。
---

## 摘要
将大语言模型（LLM）部署为自主浏览器智能体，会以间接提示注入（IPI）的形式暴露巨大的攻击面。基于云的防御可以提供强大的语义分析，但会引入延迟并引发隐私担忧。我们提出了“认知防火墙”（Cognitive Firewall），这是一种三阶段的分离计算架构，将安全检查分布在客户端和云端。该系统由本地视觉哨兵（Sentinel）、基于云的深度规划器（Deep Planner）以及在执行时强制执行策略的确定性守卫（Guard）组成。在 1,000 个对抗性样本中，仅靠边缘防御无法检测到 86.9% 的语义攻击。相比之下，完整的混合架构将总攻击成功率（ASR）降低到 1% 以下（静态评估下为 0.88%，自适应评估下为 0.67%），同时保持对副作用操作的确定性约束。通过在本地过滤表现层攻击，该系统避免了不必要的云端推理，并比纯云端基准实现了约 17,000 倍的延迟优势。这些结果表明，执行边界处的确定性强制执行可以补充概率性语言模型，且分离计算为保护交互式 LLM 智能体提供了实用的基础。

## Abstract
Deploying large language models (LLMs) as autonomous browser agents exposes a significant attack surface in the form of Indirect Prompt Injection (IPI). Cloud-based defenses can provide strong semantic analysis, but they introduce latency and raise privacy concerns. We present the Cognitive Firewall, a three-stage split-compute architecture that distributes security checks across the client and the cloud. The system consists of a local visual Sentinel, a cloud-based Deep Planner, and a deterministic Guard that enforces execution-time policies. Across 1,000 adversarial samples, edge-only defenses fail to detect 86.9% of semantic attacks. In contrast, the full hybrid architecture reduces the overall attack success rate (ASR) to below 1% (0.88% under static evaluation and 0.67% under adaptive evaluation), while maintaining deterministic constraints on side-effecting actions. By filtering presentation-layer attacks locally, the system avoids unnecessary cloud inference and achieves an approximately 17,000x latency advantage over cloud-only baselines. These results indicate that deterministic enforcement at the execution boundary can complement probabilistic language models, and that split-compute provides a practical foundation for securing interactive LLM agents.

---

## 论文详细总结（自动生成）

### 论文总结：认知防火墙（The Cognitive Firewall）

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）被部署为能够自主操作浏览器的智能体（如自动订票、管理工单），它们面临着严重的**间接提示注入（IPI）**威胁。由于智能体在同一个上下文窗口中处理用户指令（控制平面）和外部网页内容（数据平面），攻击者可以通过在网页中嵌入不可见或欺骗性的恶意指令（如零透明度文本），诱导智能体执行未授权操作（如泄露隐私、删除数据）。
现有的防御手段面临权衡：云端防御语义分析强但延迟高且有隐私风险；边缘端防御速度快但难以识别复杂的认知操纵。本文旨在通过一种**端云协同的拆分计算架构**，在保证实时性的同时提供深度安全防护。

#### 2. 论文提出的方法论
核心思想是构建一个**“防御漏斗”（Defense Funnel）**，将安全检查按成本和复杂度分层分布。
*   **第一层：边缘哨兵（Edge Sentinel - 视觉感知层）**
    *   运行在浏览器本地，利用 Chrome 内置的 Gemini Nano（小语言模型）和 DOM/CSS 启发式算法。
    *   **功能**：检测表现层攻击。通过对比 DOM 源码与计算后的 CSS 样式，识别用户不可见但模型可见的文本（如 `opacity: 0`、极小字体、视口外定位）。
*   **第二层：云端深度规划器（Cloud Planner - 语义推理层）**
    *   部署在云端安全环境，使用高性能 LLM（如 Llama 3 8B 或 GPT-4）。
    *   **功能**：对清洗后的文本进行深度语义分析，识别角色扮演（Jailbreak）、指令覆盖或逻辑陷阱，并生成执行计划。
*   **第三层：执行守卫（Origin Guard - 执行对齐层）**
    *   本地确定性监控组件，拦截浏览器 API 调用（如 `fetch`）。
    *   **功能**：强制执行确定性策略。检查目标域名是否在白名单内，并验证 HTTP 动作（如 POST/DELETE）是否与用户初始意图（如“仅读取”）一致。

#### 3. 实验设计
*   **数据集与场景**：构建了包含 **1,000 个样本**的测试集，涵盖四类：正常任务（32.2%）、视觉攻击（24%）、语义攻击（25%）和劫持攻击（18.8%）。
*   **基准对比（Benchmark）**：
    *   无防御基准（Baseline）。
    *   仅边缘防御（Edge Only）。
    *   边缘 + 云端混合（Edge + Cloud）。
    *   全功能混合架构（Full Hybrid）。
*   **评估维度**：攻击成功率（ASR）、端到端延迟、Token 节省量、针对自适应攻击者的鲁棒性。

#### 4. 资源与算力
*   **客户端**：MacBook Pro (M3 Pro 芯片, 18GB RAM)，运行 Chrome Canary 128。使用内置的 **Gemini Nano (3.25B 参数)**。
*   **服务器端**：使用 FastAPI 部署的 **Meta Llama 3 (8B Instruct)**，通过 Ollama 托管，模拟 A100 级别的企业云环境。
*   **训练/推理**：未涉及模型训练，重点在于推理阶段的分布式部署。

#### 5. 实验数量与充分性
*   **实验规模**：进行了 1,000 组静态样本实验和 300 组针对性自适应攻击实验（包括 DOM 碎片化、良性包装攻击、白名单域名外泄）。
*   **充分性**：实验设计较为全面，包含了消融实验以验证各层贡献，并考虑了“白盒”攻击者视角下的自适应策略。
*   **客观性**：通过 Sankey 图直观展示了攻击流在各层的拦截情况，对比了延迟分布的对数尺度，数据支撑较为扎实。

#### 6. 主要结论与发现
*   **防御效能**：完整架构将攻击成功率（ASR）从 100% 降低至 **0.88%**（自适应攻击下为 0.67%）。
*   **延迟优势**：边缘哨兵的平均处理时间仅为 **0.06ms**，比云端推理快约 **17,000 倍**。
*   **资源优化**：本地层拦截了 13.1% 的高频视觉攻击，直接减少了 13.1% 的云端 Token 消耗和 GPU 负载。
*   **确定性重要性**：实验证明，即使语义分析（概率性）被绕过，第三层的确定性守卫仍能拦截大量劫持尝试。

#### 7. 优点
*   **架构创新**：首次将拆分计算（Split-computing）应用于 LLM 智能体的安全防御，平衡了性能与安全性。
*   **防御深度**：结合了启发式过滤、概率性语义分析和确定性策略强制执行，形成了多重屏障。
*   **实用性强**：利用浏览器内置 AI API，减少了模型加载的冷启动开销，具备实际部署的可行性。

#### 8. 不足与局限
*   **多模态覆盖不足**：目前主要针对文本和 DOM 结构，无法处理嵌入在图像中的视觉提示注入（如 QR 码或图片隐写）。
*   **可用性权衡**：严格的执行策略导致了 **1.7% 的误报率**（False Positive），可能会拦截复杂的合法交互。
*   **语义残余风险**：在“良性包装”攻击下，仍有 2% 的成功率，表明仅靠自动化意图分类难以完全消除语义歧义。
*   **延迟累积**：虽然边缘层极快，但若进入云端环节，总延迟仍接近 1 秒，对极高实时性要求的场景仍有挑战。

（完）
