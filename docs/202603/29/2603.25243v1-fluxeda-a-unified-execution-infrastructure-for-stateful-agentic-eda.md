---
title: "FluxEDA: A Unified Execution Infrastructure for Stateful Agentic EDA"
title_zh: FluxEDA：一种用于有状态智能体驱动 EDA 的统一执行基础设施
authors: "Zhengrui Chen, Zixuan Song, Yu Li, Qi Sun, Cheng Zhuo"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25243v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: EDA自动化中自主智能体的有状态基础设施
tldr: 本研究针对现有EDA自动化中LLM智能体难以保持工具状态的问题，提出了FluxEDA——一个统一且有状态的执行基础设施。FluxEDA通过托管网关接口和持久化后端实例，允许智能体在保留运行时状态的情况下与异构EDA工具交互，克服了传统脚本级调用的局限。该框架支持状态重用、回滚和协调迭代，为生产环境下的智能体辅助EDA流程提供了坚实的基础设施支撑。
selection_source: fresh_fetch
motivation: 现有的EDA自动化集成多依赖于孤立的脚本或请求级交互，难以在复杂的生产流程中保持工具状态并支持迭代优化。
method: 引入了基于托管网关的执行接口和持久化后端实例，通过结构化请求处理实现与异构EDA工具的有状态交互。
result: 在后布线时序ECO和标准单元子库优化案例中，FluxEDA成功支持了多步分析、状态重用、回滚及协调迭代执行。
conclusion: 有状态且受控的基础设施层是实现智能体辅助EDA自动化的实用且必要的基础。
---

## 摘要
大语言模型和自主智能体正越来越多地被探索用于 EDA 自动化，但许多现有的集成仍依赖于脚本级或请求级的交互，这使得在真实的生产导向环境中难以保留工具状态并支持迭代优化。在这项工作中，我们提出了 FluxEDA，这是一个用于智能体驱动 EDA 的统一且有状态的基础设施底层。FluxEDA 引入了一个基于托管网关的执行接口，具有结构化的请求和响应处理能力。它还维护持久的后端实例。这些特性共同允许上层智能体和可编程客户端通过保留的运行时状态与异构 EDA 工具进行交互，而不是通过孤立的 shell 调用。我们使用两个具有代表性的商业后端案例研究对该框架进行了评估：自动化的布线后时序 ECO 和标准单元子库优化。结果表明，FluxEDA 能够支持在真实工具上下文中的多步分析和优化，包括状态重用、回滚和协调的迭代执行。这些发现表明，一个有状态且受控的基础设施层是智能体辅助 EDA 自动化的实用基础。

## Abstract
Large language models and autonomous agents are increasingly explored for EDA automation, but many existing integrations still rely on script-level or request-level interactions, which makes it difficult to preserve tool state and support iterative optimization in real production-oriented environments. In this work, we present FluxEDA, a unified and stateful infrastructure substrate for agentic EDA. FluxEDA introduces a managed gateway-based execution interface with structured request and response handling. It also maintains persistent backend instances. Together, these features allow upper-layer agents and programmable clients to interact with heterogeneous EDA tools through preserved runtime state, rather than through isolated shell invocations. We evaluate the framework using two representative commercial backend case studies: automated post-route timing ECO and standard-cell sub-library optimization. The results show that FluxEDA can support multi-step analysis and optimization over real tool contexts, including state reuse, rollback, and coordinated iterative execution. These findings suggest that a stateful and governed infrastructure layer is a practical foundation for agent-assisted EDA automation.

---

## 论文详细总结（自动生成）

这是一份关于论文《FluxEDA: A Unified Execution Infrastructure for Stateful Agentic EDA》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 LLM 驱动的 EDA 自动化框架（如 ChatEDA 等）大多依赖于“脚本级”或“请求级”的交互模式。这种模式是**无状态（Stateless）**的，即每次操作都是孤立的 shell 调用，难以在复杂的生产环境中保留工具的运行时状态（如内存中的设计数据），导致无法进行高效的迭代优化、状态重用和错误回滚。
*   **研究背景**：现代芯片设计规模巨大，EDA 流程极其复杂且工具链高度碎片化（接口、语言、数据格式各异）。直接让 AI 智能体对接这些异构工具会产生巨大的集成瓶颈，且 LLM 生成的脚本若无运行时监管，极易导致工具崩溃或状态损坏。

### 2. 方法论
FluxEDA 提出了一个五层架构的统一执行基础设施，核心思想是将“工具能力暴露”与“工作流逻辑”解耦：
*   **托管网关（Managed Gateway）**：通过结构化的 Socket-RPC 接口封装工具原生功能，定义明确的执行边界（仅允许调用注册的 `api_*` 方法），防止 LLM 产生幻觉指令。
*   **持久化后端实例（Persistent Instances）**：引入 `instance_id` 机制。工具进程在启动后保持存活，智能体通过 ID 路由多次请求到同一实例，从而在内存中保留设计上下文，支持“分析-执行-精炼”的闭环。
*   **渐进式能力暴露**：不一次性向 LLM 输入所有 API 文档，而是先提供简单的发现接口（如 `api_list_method`），按需查询详细参数，节省 Token 消耗。
*   **领域技能层（Skills）**：将任务分解、输入验证、执行顺序等显性知识封装为“技能”，指导智能体如何组合原子 API 来完成复杂的 EDA 任务。
*   **运行时管理**：负责进程启动、心跳监测、超时控制和异步长任务处理，确保工业级生产的稳定性。

### 3. 实验设计
论文通过两个具有代表性的商业 EDA 后端案例验证了 FluxEDA 的有效性：
*   **案例一：自动化布线后时序 ECO（Post-route Timing ECO）**
    *   **场景**：在已布线的设计上修复 Setup 和 Hold 时序违例。
    *   **关键操作**：加载设计、生成报告、单元尺寸调整（Sizing）、状态回滚（当发现优化效果变差时）。
*   **案例二：Pareto 驱动的标准单元子库优化**
    *   **场景**：在工艺开发早期，从全库中筛选最小的单元子集以降低维护成本，同时保持性能。
    *   **流程**：分为面积驱动探索（A-Runs）和时序恢复（T-Runs）两个阶段，通过与 STA 工具的持续交互进行路径诊断。
*   **Benchmark**：使用真实的商业 EDA 工具环境和工业级设计数据作为基准。

### 4. 资源与算力
*   **LLM 模型**：论文提到使用了 **GPT-5.4**（注：根据论文标注日期 2026 年，这可能是作者设定的未来版本或特定实验版本）作为核心推理引擎。
*   **框架**：基于 OpenAI Codex 框架实现智能体编排。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量或训练/推理的总时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 ECO 案例中，展示了 3 次关键迭代的详细轨迹，包括一次成功的状态回滚。
    *   在子库优化案例中，提供了从 A-Run1 到 T-Run13 共 **20 余组** 实验决策的完整轨迹（见附录）。
*   **充分性评价**：实验设计较为充分，不仅展示了最终的 QoR（设计质量）提升，还详细记录了智能体的决策逻辑和状态切换过程。通过对比 Baseline 和不同迭代阶段的数据，客观证明了“有状态”架构在处理复杂依赖任务时的优越性。

### 6. 主要结论与发现
*   **状态保留是关键**：FluxEDA 允许智能体在不重新加载设计的情况下进行连续优化，显著提升了迭代效率。
*   **回滚机制的价值**：在 ECO 案例中，当 Iteration 2 导致结果变差时，系统能迅速回滚至 Iteration 1 状态并尝试新路径，这是传统无状态框架难以实现的。
*   **大幅精简库规模**：在子库优化中，FluxEDA 引导智能体在仅使用 24.2% 的单元参考和 45.1% 的单元族的情况下，恢复了 88.4% 的时序差距，证明了其在广阔搜索空间中的导航能力。

### 7. 优点
*   **架构先进性**：首次为 Agentic EDA 引入了系统级的状态管理和运行时治理层，解决了“AI 玩火（无约束执行）”的问题。
*   **工业实用性**：兼容商业工具，支持长时程交互和异步任务，贴近真实芯片设计流程。
*   **Token 友好**：通过渐进式暴露和结构化 RPC，降低了 LLM 处理长上下文的压力。

### 8. 不足与局限
*   **覆盖范围**：实验主要集中在后端物理设计（ECO、库优化），尚未验证其在前端逻辑综合或大规模验证流程中的表现。
*   **模型依赖**：系统效能高度依赖于底层 LLM 的推理能力，对于较弱的模型，其“技能”层的引导效果可能打折扣。
*   **实时性开销**：虽然 RPC 通信延迟相对于 EDA 运行时间较小，但在极大规模并发调用下的系统吞吐量尚未深入讨论。

（完）
