---
title: "EVA: Efficient Reinforcement Learning for End-to-End Video Agent"
title_zh: EVA：面向端到端视频智能体的高效强化学习
authors: "Yaolun Zhang, Ruohui Wang, Jiahao Wang, Yepeng Tang, Xuanyu Zheng, Haonan Duan, Hao Lu, Hanming Deng, Lewei Lu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22918v1"
tags: ["query:agent"]
score: 9.0
evidence: 具有规划和反思功能的端到端视频智能体强化学习
tldr: 针对多模态大模型在长视频理解中面临的序列过长和信息冗余挑战，本文提出了EVA框架。EVA是一个基于强化学习的端到端视频智能体，通过“先规划后感知”的迭代推理机制，自主决定观察视频的时机和内容。研究团队设计了结合SFT、KTO和GRPO的三阶段训练流水线，并构建了高质量数据集。实验表明，EVA在多个基准测试中显著优于现有模型，实现了高效且精准的视频理解。
selection_source: fresh_fetch
motivation: 现有视频理解模型多为被动识别或依赖人工预设流程，难以高效处理长视频中的冗余信息和复杂时空依赖。
method: 提出一种基于总结-规划-行动-反思机制的端到端智能体，并采用SFT、KTO和GRPO三阶段强化学习策略进行训练。
result: "在六个视频理解基准测试中，EVA相比通用多模态大模型提升了6-12%，相比现有自适应智能体提升了1-3%。"
conclusion: EVA通过强化学习实现了自主、高效的查询驱动视频理解，为开发高性能端到端视频智能体提供了有效路径。
---

## 摘要
由于视频包含广泛的时间依赖性和冗余帧，导致其 Token 序列过长，使用多模态大语言模型（MLLM）进行视频理解仍然具有挑战性。现有方法通常将 MLLM 视为被动识别器，在没有自适应推理的情况下处理整个视频或均匀采样的帧。最近基于智能体（agent-based）的方法引入了外部工具，但仍依赖于人工设计的工作流和感知优先策略，导致在长视频上的效率低下。我们提出了 EVA，一个面向端到端视频智能体的高效强化学习框架，它通过迭代的“总结-计划-行动-反思”推理实现了“先计划后感知”。EVA 自主决定看什么、何时看以及如何看，从而实现查询驱动且高效的视频理解。为了训练此类智能体，我们设计了一个简单而有效的三阶段学习流水线——包括监督微调（SFT）、Kahneman-Tversky 优化（KTO）和广义奖励策略优化（GRPO）——从而桥接了监督模仿学习与强化学习。我们进一步为每个阶段构建了高质量数据集，支持稳定且可复现的训练。我们在六个视频理解基准测试上评估了 EVA，展示了其全面的能力。与现有基准相比，EVA 在通用 MLLM 基准上实现了 6-12% 的显著提升，并比之前的自适应智能体方法进一步提高了 1-3%。我们的代码和模型可在 https://github.com/wangruohui/EfficientVideoAgent 获取。

## Abstract
Video understanding with multimodal large language models (MLLMs) remains challenging due to the long token sequences of videos, which contain extensive temporal dependencies and redundant frames. Existing approaches typically treat MLLMs as passive recognizers, processing entire videos or uniformly sampled frames without adaptive reasoning. Recent agent-based methods introduce external tools, yet still depend on manually designed workflows and perception-first strategies, resulting in inefficiency on long videos. We present EVA, an Efficient Reinforcement Learning framework for End-to-End Video Agent, which enables planning-before-perception through iterative summary-plan-action-reflection reasoning. EVA autonomously decides what to watch, when to watch, and how to watch, achieving query-driven and efficient video understanding. To train such agents, we design a simple yet effective three-stage learning pipeline - comprising supervised fine-tuning (SFT), Kahneman-Tversky Optimization (KTO), and Generalized Reward Policy Optimization (GRPO) - that bridges supervised imitation and reinforcement learning. We further construct high-quality datasets for each stage, supporting stable and reproducible training. We evaluate EVA on six video understanding benchmarks, demonstrating its comprehensive capabilities. Compared with existing baselines, EVA achieves a substantial improvement of 6-12% over general MLLM baselines and a further 1-3% gain over prior adaptive agent methods. Our code and model are available at https://github.com/wangruohui/EfficientVideoAgent.

---

## 论文详细总结（自动生成）

以下是对论文 **《EVA: Efficient Reinforcement Learning for End-to-End Video Agent》** 的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的多模态大语言模型（MLLM）在处理视频时通常是“被动识别器”，即一次性处理整段视频或均匀采样帧。这种“感知优先”的模式在面对长视频时，会产生极高的计算开销（Token 冗余）且难以捕捉关键瞬时信息。
*   **研究背景**：虽然已有研究引入外部工具进行帧选择，但这些方法大多依赖人工设计的固定工作流，缺乏自主推理能力。
*   **整体含义**：本文提出了 **EVA** 框架，旨在将 MLLM 转化为“主动观察者”。通过**先规划后感知（Planning-before-perception）**的范式，让模型自主决定“看什么、什么时候看、怎么看”，从而在提升理解准确度的同时大幅降低计算成本。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：构建一个迭代的推理闭环：**总结（Summary）→ 规划（Plan）→ 行动（Action）→ 反思（Reflection）**。
*   **关键技术细节**：
    *   **灵活工具调用**：设计了 `frame_select` 工具，允许智能体控制四个参数：起始时间、结束时间、采样帧数（nframes）和空间缩放比例（resize）。这使得模型能实现“先低清全局预览，后高清局部定位”的策略。
    *   **三阶段训练流水线**：
        1.  **SFT（监督微调）冷启动**：使用教师模型（Qwen2.5-VL-72B）生成的 10k 高质量轨迹，教会模型基本的工具格式和推理模式。
        2.  **KTO（Kahneman-Tversky 优化）纠偏**：利用 11k 成功与失败的策略轨迹，通过人类偏好对齐算法让模型学会避开“信息不足即盲目回答”等错误模式，提升策略稳定性。
        3.  **GRPO（广义奖励策略优化）在线强化学习**：在 10k+ 数据上进行大规模强化学习。
*   **奖励函数设计**：
    *   **准确性奖励**：多选题使用 CSV（自我验证）奖励，简答题使用 ROUGE 分数。
    *   **格式奖励**：对正确调用工具但回答错误的尝试给予微小奖励，以鼓励探索而非随机猜测。

### 3. 实验设计
*   **数据集与 Benchmark**：
    *   **采样效率评估**：LSDBench（采样困境基准）。
    *   **长视频理解**：LongVideoBench, MLVU, VideoMME, LVBench。
    *   **视频推理与幻觉**：Video-Holmes（零样本推理）, ELV-Halluc（语义聚合幻觉）。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, Gemini-2.0-Flash, Gemini-1.5-Pro。
    *   **开源静态采样模型**：Qwen2.5-VL, InternVL2.5, Video-R1, LongVA。
    *   **自适应智能体**：VideoAgent, FrameThinker, VideoMTR。

### 4. 资源与算力
*   **算力配置**：实验使用了 **32 台 H100 GPU**。
*   **训练细节**：
    *   **SFT 阶段**：训练 2 个 epoch，学习率 2e-6，Batch Size 为 8。
    *   **KTO 阶段**：学习率保持不变，beta 设置为 0.1。
    *   **GRPO 阶段**：训练 1 个 epoch，学习率 1e-6，Batch Size 为 64，每个样本生成 8 个 rollout。
*   **推理框架**：使用 vLLM 进行模型部署与推理。

### 5. 实验数量与充分性
*   **实验规模**：论文在 **6 个主流视频理解基准**上进行了全面测试，并额外针对**幻觉检测**（ELV-Halluc）进行了专项评估。
*   **消融实验**：
    *   验证了 SFT → KTO → GRPO 三阶段的演进效果（证明了 KTO 能显著减少交互轮数，GRPO 能进一步优化 Token 分配）。
    *   对比了纯简答题、纯多选题及混合数据对强化学习效果的影响。
*   **充分性评价**：实验设计较为充分，涵盖了从短视频到超长视频（6600秒+）、从客观选择到开放式推理的多种场景。通过与 SOTA 闭源模型对比 Token 使用量，客观地证明了其高效性。

### 6. 主要结论与发现
*   **性能领先**：EVA 在多个长视频基准上超越了现有的开源模型和自适应智能体，准确率提升了 1-12% 不等。
*   **极高的 Token 效率**：在 LSDBench 上，EVA 仅使用约 **6.2k** 个视觉 Token 就达到了 51.8% 的准确率，而性能相近的 Qwen2.5-VL 基准模型需要约 **166k** 个 Token。
*   **策略演化**：经过强化学习，智能体学会了“先全局低清扫描，再局部高清缩放”的复杂策略，而非简单的均匀采样。
*   **缓解幻觉**：在 ELV-Halluc 测试中，EVA 显著降低了语义聚合幻觉率（从 8.8% 降至 5.0%）。

### 7. 优点与亮点
*   **范式创新**：提出的“先规划后感知”有效解决了长视频处理中的“采样困境”。
*   **端到端优化**：不同于以往依赖硬编码规则的智能体，EVA 的工具调用和推理逻辑是通过强化学习端到端训练出来的。
*   **训练稳定性**：引入 KTO 阶段作为 SFT 和 GRPO 之间的桥梁，有效解决了多轮对话中强化学习难以收敛的问题。
*   **高灵活性**：工具支持空间（分辨率）和时间（帧率）的双重调节。

### 8. 不足与局限
*   **工具依赖性**：目前的推理闭环高度依赖预定义的 `frame_select` 接口，如果面对需要其他类型工具（如音频分析、OCR 专项工具）的任务，泛化能力受限。
*   **分布外风险**：对于极其嘈杂或与训练分布差异巨大的查询，智能体可能会陷入无效的迭代循环。
*   **记忆机制缺失**：目前主要依靠上下文窗口存储历史，缺乏长期的跨模态记忆机制来处理超大规模视频库。
*   **计算延迟**：虽然 Token 数量减少了，但多轮迭代推理会增加首字响应延迟（TTFT），在实时交互场景下可能存在挑战。

（完）
