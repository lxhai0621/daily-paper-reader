---
title: "Seeing to Ground: Visual Attention for Hallucination-Resilient MDLLMs"
title_zh: 视以锚定：用于抗幻觉多模态扩散大语言模型的视觉注意力机制
authors: "Vishal Narnaware, Animesh Gupta, Kevin Zhai, Zhenyi Wang, Mubarak Shah"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25711v1"
tags: ["query:mmkqa"]
score: 9.0
evidence: 通过视觉注意力对齐解决多模态大模型中的幻觉问题
tldr: 多模态扩散大语言模型（MDLLM）在并行解码时常因仅依赖文本概率而忽略视觉支持，导致多模态幻觉。本文提出VISAGE，一种无需训练的解码框架，通过量化交叉注意力的空间熵来评估视觉接地程度。该方法在推理阶段校准解码目标，强制注意力头达成定位共识，从而抑制幻觉。实验表明，VISAGE在MMMU和HallusionBench等基准上显著提升了模型的鲁棒性和准确性。
selection_source: fresh_fetch
motivation: MDLLM解码器过度依赖文本似然性而缺乏局部视觉验证，导致语言概率与多模态任务目标不匹配，进而引发幻觉。
method: 提出VISAGE框架，利用交叉注意力分布的空间熵来衡量视觉支持度，并在推理时通过定位共识机制对候选Token进行重新排序。
result: "在MMMU-val和HallusionBench上分别实现了8.59%和7.75%的相对性能提升，并证明了该方法具有分析稳定性。"
conclusion: 通过在解码阶段引入视觉注意力约束，VISAGE有效缓解了MDLLM的幻觉问题，证明了视觉接地对提升多模态生成质量的关键作用。
---

## 摘要
多模态扩散大语言模型（MDLLMs）通过并行掩码解码实现了高并发生成，但其架构仍容易产生多模态幻觉。这种结构性脆弱性源于一个算法缺陷：解码器根据文本似然度对候选词元进行排序，而未验证局部化的视觉支持。我们证实，这种仅依赖语言的排序会导致目标不匹配，即语言概率质量充当了预期多模态任务的错误代理。因此，我们将幻觉重新解释为一种局部优化误差，即解码器利用语言捷径来最大化代理分数，而牺牲了视觉锚定。为解决这一目标不匹配问题，我们引入了 VISAGE，这是一种在推理阶段校准目标的免训练解码框架。VISAGE 通过量化交叉注意力分布的空间熵来估计代理偏差。通过在注意力头之间强制执行局部化共识，该方法惩罚了空间均匀分布，并重新排列词元承诺，以倾向于具有视觉锚定的结果。我们提供了一个分析稳定性保证，证明 VISAGE 在估计误差下能保持有界的目标损失。在幻觉敏感型和通用型基准测试上的评估证明了该框架的鲁棒性，在 MMMU-val 上获得了 8.59% 的相对提升，在 HallusionBench 上获得了 7.75% 的相对提升。

## Abstract
Multimodal Diffusion Large Language Models (MDLLMs) achieve high-concurrency generation through parallel masked decoding, yet the architectures remain prone to multimodal hallucinations. This structural vulnerability stems from an algorithmic flaw: the decoder ranks candidate tokens based on textual likelihood without verifying localized visual support. We establish that this language-only ranking induces an objective mismatch, where language probability mass acts as a misspecified proxy for the intended multimodal task. Consequently, we reinterpret hallucination as a localized optimization error, a phenomenon where the decoder exploits language shortcuts to maximize a proxy score at the expense of visual grounding. To address this objective mismatch, we introduce VISAGE, a training-free decoding framework that calibrates the objective at inference time. VISAGE estimates the proxy discrepancy by quantifying the spatial entropy of cross-attention distributions. By enforcing a localization consensus across attention heads, the method penalizes spatially uniform distributions and re-ranks token commitments to favor visually grounded outcomes. We provide an analytical stability guarantee establishing that VISAGE maintains a bounded objective loss under estimation error. Evaluations across hallucination-sensitive and general-purpose benchmarks demonstrate the robustness of the framework, yielding relative gains of 8.59% on MMMU-val and 7.75% on HallusionBench.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **VISAGE**（Visual Attention Grounding Entropy）的框架，旨在解决多模态扩散大语言模型（MDLLMs）在并行解码过程中产生的幻觉问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：多模态扩散大语言模型（如 MMaDA）虽然通过并行掩码解码提高了生成效率，但极易产生“多模态幻觉”。
*   **研究动机**：作者发现幻觉源于一种**算法缺陷**——解码器在并行取消掩码时，仅根据文本似然度（语言概率）对候选词元（Token）进行排序和确认，而没有验证这些词元是否具有局部化的视觉支持。
*   **理论解释**：论文将幻觉定义为一种“局部优化误差”。解码器为了最大化语言概率这一“代理目标”，往往会利用语言捷径（Language Shortcuts），在缺乏视觉证据的情况下提前确认词元，导致错误信息在后续解码步骤中传播。

### 2. 方法论：核心思想与关键技术
VISAGE 是一种**无需训练（Training-free）**的推理阶段重排序框架，其核心思想是通过交叉注意力机制来校准解码目标。
*   **空间熵评估（Spatial Entropy）**：利用交叉注意力图中图像词元的空间分布来衡量视觉支持度。如果注意力分布均匀（高熵），说明该词元缺乏明确的视觉对应点；如果分布集中（低熵），则说明具有良好的视觉锚定。
*   **$\beta$-分位数共识（$\beta$-quantile Consensus）**：为了防止单个注意力头的异常干扰，VISAGE 聚合了多个头的空间熵。它要求一定比例（$\beta$ 比例）的注意力头达成“定位共识”，只有当多数头都表现出低熵时，才认为该词元是视觉锚定的。
*   **重排序公式**：
    *   计算鲁棒接地熵 $H_i$。
    *   计算线性排序分数：$u_i = c_i \cdot (1 + H_i)^{-\alpha}$。
    *   其中 $c_i$ 是原始语言置信度，$\alpha$ 是惩罚因子。通过该公式，缺乏视觉支持的词元会被降权，从而延迟其确认时间，给模型留出修正机会。
*   **稳定性保证**：论文从数学上证明了 VISAGE 在估计误差下具有有界的目标损失，确保了解码过程的稳定性。

### 3. 实验设计
*   **数据集/场景**：
    *   **POPE**：评估基础物体的存在性幻觉。
    *   **HallusionBench**：评估语言驱动的幻觉和视觉错觉。
    *   **MMMU-val**：评估大学水平的多学科复杂推理能力。
    *   **MME**：综合评估感知和认知任务。
*   **基准模型（Benchmark）**：以 **MMaDA** 作为基础模型。
*   **对比方法**：
    *   **MMaDA (Base)**：标准的置信度排序解码。
    *   **VCD (Visual Contrastive Decoding)**：一种通过对比失真图像来减少语言偏见的强基线方法。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **效率备注**：由于 VISAGE 是一种**免训练**的方法，它直接利用模型推理过程中已有的交叉注意力图进行计算，因此引入的计算开销极小，不需要额外的模型微调或大规模算力支持。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个主流的多模态基准测试，并针对 HallusionBench 进行了细分领域（如地图、数学、OCR 等）的雷达图分析。
*   **消融实验**：
    *   测试了惩罚因子 $\alpha$ 对性能的影响。
    *   对比了不同的头聚合策略（最小值、平均值、$\beta$-分位数共识）。
*   **充分性评价**：实验设计较为充分，不仅验证了幻觉抑制能力，还通过 MMMU 等任务确保了通用生成能力的稳定性。对比了最新的 VCD 方法，证明了其在扩散模型架构下的优越性。

### 6. 主要结论与发现
*   **性能提升**：VISAGE 在 MMMU-val 上实现了 **8.59%** 的相对增益，在 HallusionBench 上实现了 **7.75%** 的提升。
*   **抑制捷径**：实验证明，通过空间熵惩罚，模型能够有效克服由提示词（Prompt）诱导的语言捷径，强制模型“观察”图像后再做决定。
*   **多步推理稳定性**：在复杂推理任务中，VISAGE 防止了早期错误词元的确认，从而避免了幻觉在长序列生成中的级联效应。

### 7. 优点
*   **免训练且高效**：不需要更新模型参数，易于集成到现有的 MDLLM 架构中。
*   **理论支撑强**：将幻觉建模为目标不匹配问题，并提供了数学上的稳定性证明。
*   **鲁棒性高**：通过分位数共识机制，有效过滤了注意力机制中的噪声和伪影。

### 8. 不足与局限
*   **时间维度缺失**：目前的框架主要针对静态图像，未显式建模视频任务中的跨帧时间一致性，在处理视频序列时可能存在局限。
*   **标签噪声敏感**：在 POPE 实验中发现，由于数据集本身存在标注错误（Label Noise），VISAGE 即使做出了正确的视觉判断，也可能因与错误标签不符而被判定为失败。
*   **超参数依赖**：惩罚因子 $\alpha$ 在不同任务（如感知 vs 认知）中需要微调以达到最佳平衡。

（完）
