---
title: "Where Matters More Than What: Decoding-aligned KV Cache Compression via Position-aware Pseudo Queries"
title_zh: 位置比内容更重要：通过位置感知伪查询实现解码对齐的 KV 缓存压缩
authors: "Zhenxu Tian, Yi Su, Juntao Li, Min Zhang"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11564v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 用于长上下文管理的KV缓存压缩
tldr: 针对大语言模型推理中KV缓存占用内存过大的问题，本文提出DapQ框架。现有方法多依赖预填充阶段的注意力模式，无法准确预测解码阶段的Token重要性。DapQ通过位置感知伪查询模拟解码过程，建立更有效的观察窗口进行KV缓存剔除。实验证明，在极低缓存预算下，DapQ仍能保持极高性能，实现了高效且精准的KV缓存压缩。
selection_source: fresh_fetch
motivation: 现有的KV缓存压缩方法主要依赖预填充阶段的注意力权重，难以准确预测解码阶段对Token的实际需求。
method: 提出DapQ框架，利用位置信息在模拟解码查询中的关键作用，通过构造位置感知伪查询来评估并剔除不重要的Token。
result: "在多个基准测试中表现优异，在仅保留3% KV缓存的情况下，在NIAH测试中实现了近乎无损的99.5%性能。"
conclusion: 通过对齐解码阶段的注意力模式，位置感知伪查询能显著提升KV缓存压缩的精确度和模型推理效率。
---

## 摘要
键值（KV）缓存对于大语言模型（LLM）的高效推理至关重要，但过长的上下文会显著增加 KV 缓存的内存占用。现有的 KV 缓存压缩方法通常依赖于预填充阶段 Prompt 观察窗口内的输入端注意力模式来评估 Token 的重要性。由于这些评估并非源自解码过程，它们无法为未来的生成保留关键 Token。直观上，一个有效的观察窗口应该镜像解码阶段的查询，以准确反映生成过程将关注哪些 Token。然而，在推理过程中，真实的解码查询本质上是不可用的。在构建伪查询以逼近真实查询时，我们发现位置信息比语义内容起着更关键的作用。基于这一见解，我们提出了通过位置感知伪查询实现的解码对齐 KV 缓存压缩（DapQ），这是一种新颖且轻量级的逐出框架，它利用位置感知伪查询来模拟输出 Token，从而为重要性评估建立有效的观察窗口。它与实际的生成上下文紧密对齐，并实现了精确的 Token 逐出。在多个基准测试和大语言模型上的广泛评估表明，DapQ 取得了卓越的性能，特别是在严格的内存限制下（例如，在仅保留 3% KV 缓存预算的情况下，在 NIAH 上实现了高达 99.5% 的近乎无损性能）。

## Abstract
The Key-Value (KV) cache is crucial for efficient Large Language Models (LLMs) inference, but excessively long contexts drastically increase KV cache memory footprint. Existing KV cache compression methods typically rely on input-side attention patterns within a prompt observation window to estimate token importance during the prefill stage. They fail to preserve critical tokens for future generation since these assessments are not derived from the decoding process. Intuitively, an effective observation window should mirror the decoding-stage queries to accurately reflect which tokens the generation process will attend to. However, ground-truth decoding queries are inherently unavailable during inference. For constructing pseudo queries to approximate them, we find that positional information plays a more critical role than semantic content. Motivated by this insight, we propose decoding-aligned KV cache compression via position-aware pseudo queries (DapQ), a novel and lightweight eviction framework that leverages position-aware pseudo queries to simulate the output tokens, thereby establishing an effective observation window for importance assessment. It aligns closely with the actual generation context and enables precise token eviction. Extensive evaluations across multiple benchmarks and LLMs demonstrate that DapQ achieves superior performance, particularly under strict memory constraints (e.g., up to nearly lossless performance 99.5% on NIAH with 3% KV cache budgets).

---

## 论文详细总结（自动生成）

这是一份关于论文《Where Matters More Than What: Decoding-aligned KV Cache Compression via Position-aware Pseudo Queries》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：大语言模型（LLM）在处理长上下文时，KV Cache（键值缓存）的内存占用随序列长度线性增长，成为推理效率的瓶颈。
*   **研究动机**：现有的 KV Cache 压缩方法（如 SnapKV、H2O）主要在**预填充（Prefill）阶段**基于 Prompt 的注意力模式来评估 Token 的重要性。然而，这种“输入中心”的方法与**解码（Decoding）阶段**的实际查询需求存在错位。
*   **核心发现**：作者发现，在构建查询向量（Query）时，**位置信息（Position）的影响远大于语义内容（Content）**。即使使用随机或无意义的内容，只要赋予正确的前瞻性位置编码，生成的“伪查询”也能高度模拟真实的解码查询。

### 2. 方法论：DapQ 框架
*   **核心思想**：通过构造“位置感知伪查询”（Position-aware Pseudo Queries）来模拟未来的解码过程，从而在预填充阶段精准识别并保留对未来生成至关重要的 Token。
*   **关键技术细节**：
    1.  **构造伪 Token**：在原始 Prompt（长度为 $L_p$）后拼接 $N$ 个伪 Token（通常取前 4 个和后 28 个 Token 的组合）。
    2.  **位置对齐**：关键步骤是给这 $N$ 个伪 Token 分配**解码阶段前 $N$ 步的位置 ID**（即 $L_p$ 到 $L_p + N - 1$），而非紧随其后的顺序 ID。
    3.  **重要性评估**：模型处理扩展序列，提取伪 Token 产生的查询向量 $Q_{pseudo}$。计算 $Q_{pseudo}$ 与 Prompt 中所有 Key 的注意力分数之和，作为 Token 的重要性得分。
    4.  **精准逐出**：根据得分保留 Top-K 个 KV 对，随后立即丢弃所有伪 Token 及其缓存，从位置 $L_p$ 开始正式解码。
*   **算法流程**：该方法无需微调模型，属于即插即用的推理侧优化。

### 3. 实验设计
*   **数据集/场景**：
    *   **LongBench & LongBenchV2**：多任务长文本理解。
    *   **Ruler**：测试模型在不同上下文长度下的检索和推理能力。
    *   **HELMET**：长上下文评估基准。
    *   **Needle-in-a-Haystack (NIAH)**：大海捞针压力测试。
*   **对比方法**：
    *   **FullKV**（全量缓存基准）。
    *   **SnapKV、PyramidKV**（基于观察窗口的压缩）。
    *   **H2O、StreamingLLM**（基于重击者或注意力池的压缩）。
    *   **LaCache**（阶梯状缓存策略）。
*   **测试模型**：LLaMA-3-8B-Instruct, LLaMA-3.1-8B-Instruct, Qwen2.5-7B-Instruct, Qwen3-8B。

### 4. 资源与算力
*   **硬件环境**：所有效率实验均在单张 **NVIDIA H20 (96G) GPU** 上完成。
*   **软件框架**：基于 PyTorch 2.6.0 和 Transformers 4.53.0，使用了 Flash-Attention 2 优化。
*   **算力开销**：论文强调 DapQ 的额外开销极低。在 8K 到 128K 的上下文测试中，其首字时间（TTFT）与 SnapKV 几乎持平，证明了伪查询构造过程的轻量性。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 4 个主流模型上进行了跨 5 个大型基准测试的对比。
    *   设置了多种 KV Cache 预算（如 256, 128, 64 甚至更低）。
    *   **消融实验**：详细探讨了伪查询的语义内容（前缀、后缀、随机、固定文本）、窗口长度 $N$、以及插入位置对性能的影响。
*   **充分性评价**：实验设计非常充分且客观。作者不仅验证了准确率，还通过数学定理推导了查询相似度与注意力分布误差的界限，并提供了吞吐量和内存占用的实测数据。

### 6. 主要结论与发现
*   **位置主导性**：实验证明位置编码（RoPE）是决定查询向量表示的核心因素，语义内容的改变对注意力模式的影响相对较小。
*   **性能卓越**：DapQ 在极低缓存预算下表现优异。例如，在 LLaMA-3-8B 上，仅保留 256 个 KV 缓存（约 3% 预算），在 NIAH 测试中达到了 **99.5%** 的准确率，远超其他压缩方法。
*   **鲁棒性**：在 LongBench 等复杂任务中，DapQ 展现了更强的抗噪声能力和关键信息保留能力。

### 7. 优点（亮点）
*   **解码对齐**：首次系统性地利用位置信息模拟解码查询，解决了预填充与解码阶段的分布偏移问题。
*   **极度轻量**：不需要预生成响应（对比 LAQ++），不增加内存峰值，不改变模型架构。
*   **理论支撑**：提供了严谨的数学证明，解释了为什么高相似度的伪查询能带来准确的 Token 逐出。

### 8. 不足与局限
*   **语义内容仍有微弱影响**：虽然位置是主导，但实验显示语义内容完全无关时（如乱码），性能虽稳定但仍有提升空间。
*   **层间差异**：目前的方法在所有层使用统一的伪查询策略，未深入探讨不同 Transformer 层对位置/语义敏感度的差异。
*   **极端压缩下的挑战**：在极端的压缩比下，如何智能选择伪查询的语义内容以进一步逼近“无损压缩”仍是未来课题。

（完）
