---
title: "One Supervisor, Many Modalities: Adaptive Tool Orchestration for Autonomous Queries"
title_zh: 一个主管，多种模态：面向自主查询的自适应工具编排
authors: Mayank Saini Arit Kumar Bishwas
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11545v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 用于自主多模态工具编排的智能体AI框架
tldr: 本研究提出一种用于自主多模态查询处理的代理 AI 框架，通过中央主管（Supervisor）协调文本、图像、音频和视频等多种工具。该框架摒弃了预设决策树，采用 RouteLLM 和 SLM 辅助的自适应路由策略进行任务分解与调度。实验表明，该方法在保持准确率的同时，显著降低了响应时间、对话返工率和运营成本，为多模态 AI 的高效部署提供了新方案。
selection_source: fresh_fetch
motivation: 旨在解决多模态 AI 系统在处理跨模态复杂查询时，因缺乏灵活调度机制而导致的效率低、成本高和交互冗余问题。
method: 开发了一个基于中央主管的框架，利用自适应路由动态分解查询并调用特定模态工具（如 OCR、语音转录等）进行处理。
result: "实验显示，该框架在 15 个任务类别中实现了 72% 的耗时缩减、85% 的返工减少以及 67% 的成本降低。"
conclusion: 智能化的集中编排是优化多模态 AI 部署经济性并提升自主查询处理性能的关键。
---

## 摘要
我们提出了一种用于自主多模态查询处理的智能体 AI 框架，该框架协调了涵盖文本、图像、音频、视频和文档模态的专业工具。一个核心主管（Supervisor）动态分解用户查询，将子任务委派给相应的模态工具（例如目标检测、OCR、语音转录），并通过自适应路由策略而非预定义的决策树来综合结果。对于纯文本查询，该框架通过 RouteLLM 使用学习型路由；而对于非文本路径，则使用 SLM 辅助的模态分解。在涵盖 15 个任务类别的 2,847 个查询上的评估结果显示，与匹配的分层基准相比，我们的框架在保持准确率相当的情况下，获得准确答案的时间缩短了 72%，对话返工减少了 85%，成本降低了 67%。这些结果表明，智能化的集中编排从根本上改善了多模态 AI 部署的经济性。

## Abstract
We present an agentic AI framework for autonomous multimodal query processing that coordinates specialized tools across text, image, audio, video, and document modalities. A central Supervisor dynamically decomposes user queries, delegates subtasks to modality-appropriate tools (e.g., object detection, OCR, speech transcription), and synthesizes results through adaptive routing strategies rather than predetermined decision trees. For text-only queries, the framework uses learned routing via RouteLLM, while non-text paths use SLM-assisted modality decomposition. Evaluated on 2,847 queries across 15 task categories, our framework achieves 72% reduction in time-to-accurate-answer, 85% reduction in conversational rework, and 67% cost reduction compared to the matched hierarchical baseline while maintaining accuracy parity. These results demonstrate that intelligent centralized orchestration fundamentally improves multimodal AI deployment economics.

---

## 论文详细总结（自动生成）

这是一份关于论文《One Supervisor, Many Modalities: Adaptive Tool Orchestration for Autonomous Queries》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现代 AI 部署面临“全能处理能力”与“运营成本/响应速度”之间的矛盾。
*   **研究动机**：
    *   **单体模型局限**：将所有查询（无论简单还是复杂）都路由到 GPT-4 等顶级模型会导致极高的成本和不必要的计算浪费。
    *   **分层路由局限**：传统的基于预设决策树的路由系统非常脆弱，一旦查询偏离预期模式，系统就会崩溃或需要完全重启，导致用户体验差。
    *   **多模态挑战**：处理包含文本、图像、音频、视频和文档的复杂查询时，缺乏一种既能自主分解任务又能兼顾经济性的灵活调度机制。

### 2. 方法论
该论文提出了一种基于**中央主管（Supervisor）**的智能体 AI 框架，核心思想是将编排逻辑从硬编码的决策树转变为基于上下文的自主推理。
*   **核心架构**：
    *   **中央主管（Supervisor）**：读取工具的类型化接口规范（前提条件、后置条件、延迟先验），动态分解任务并委派给特定模态工具。
    *   **Couplet Framework（耦合框架）**：将领域优化的传统模型（如 YOLO 用于检测、CLIP 用于检索、Tesseract 用于 OCR）与轻量级小语言模型（SLM）配对。SLM 负责将自然语言指令转化为模型输入，并将原始输出语境化。
    *   **自适应路由**：
        *   **文本路径**：利用 RouteLLM 在强模型（GPT-4o）和弱模型（如 Phi-3.5）之间进行学习型路由。
        *   **非文本路径**：通过 SLM 辅助的模态分解，直接调用感知工具，避免昂贵的多模态 LLM 推理。
*   **关键技术细节**：
    *   **状态管理**：基于 LangGraph 的 `StateGraph` 模型，使用结构化状态对象 `Squery` 确保跨智能体转换时信息零丢失。
    *   **五层存储架构**：包括短期记忆、全量历史、模态特定记忆、相关上下文检索（向量数据库）和压缩记忆。
    *   **三级成本控制**：提供 `trad_couplet`（传统耦合）、`open_src`（开源）和 `closed_src`（闭源）三个成本层级供用户或系统选择。

### 3. 实验设计
*   **数据集/场景**：评估涵盖了 2,847 个查询，分为 15 个任务类别（包括 MMLU 文本推理、VQA-v2 视觉问答、文档 QA、音频转录、视频分析等）。
*   **对比基准（Benchmarks）**：
    *   **Monolithic LLM**：所有查询均路由至 GPT-4。
    *   **Hierarchical Routing**：基于预设决策树的匹配分层基准。
    *   **主流框架**：如 AutoGen 和 LangGraph（在相同工具集下对比编排策略）。
*   **核心指标**：获得准确答案的时间（TTA）、返工率（Rework Rate）、每查询成本、吞吐量和准确率。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或训练时长。
*   **模型使用**：文中提到了使用的模型族，包括 Phi-3.5-mini (3.8B)、LLaMA-3-70B、Mixtral-8x7B、GPT-4o 以及传统模型 YOLO、CLIP 等。

### 5. 实验数量与充分性
*   **实验规模**：进行了 2,847 组实验，样本量较大，覆盖了从简单文本到复杂多模态组合的广泛场景。
*   **消融实验**：论文进行了详细的消融研究，分别验证了记忆层、验证机制、并行执行和 Couplet Framework 对系统性能的影响。
*   **充分性评价**：实验设计较为充分且客观。通过引入 95% 置信区间和显著性检验（p < 0.001），证明了结果在统计学上的稳健性。对比实验在相同工具集下进行，确保了公平性。

### 6. 主要结论与发现
*   **效率提升**：相比分层基准，该框架将获得准确答案的中位时间缩短了 **72%**。
*   **质量优化**：对话返工率（需要用户澄清或修正的比例）降低了 **85%**，这得益于系统的局部修复机制和自主澄清能力。
*   **成本降低**：昂贵模型的调用减少，整体运营成本降低了 **67%**。
*   **性能平衡**：在大幅提升效率的同时，准确率与基准模型保持持平（差异在 ±1% 以内）。
*   **感知加速**：在视觉任务中，使用 YOLO（180ms/帧）远快于端到端 LLM 视觉方案（2.4s/帧）。

### 7. 优点
*   **局部修复能力**：当某个工具失败时，主管能自主尝试替代方案或请求澄清，而不是像传统系统那样导致整个流水线崩溃。
*   **高度可组合性**：主管本身可以作为工具被其他主管调用，支持递归编排。
*   **经济性与性能兼顾**：通过“成本旋钮”和自适应路由，实现了对异构工作负载的最优资源分配。
*   **并行化处理**：能够识别任务依赖关系并并行执行独立子任务，显著降低延迟。

### 8. 不足与局限
*   **编排延迟**：虽然减少了执行延迟，但 Supervisor 本身的推理和分解过程在极高吞吐量场景下可能成为新的瓶颈。
*   **工具集成门槛**：目前 Couplet Framework 支持的模型集是预设的，扩展新工具仍需要一定的接口定义工作。
*   **记忆开销**：对于极长会话，复杂的五层记忆检索和压缩可能会带来额外的计算负担。
*   **过度升级风险**：在某些边缘情况下，路由算法可能会为了保险而过度调用昂贵的闭源模型，导致成本优化未达极致。

（完）
