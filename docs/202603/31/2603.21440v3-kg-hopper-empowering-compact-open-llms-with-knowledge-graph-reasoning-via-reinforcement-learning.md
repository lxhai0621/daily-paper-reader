---
title: "KG-Hopper: Empowering Compact Open LLMs with Knowledge Graph Reasoning via Reinforcement Learning"
title_zh: KG-Hopper：通过强化学习赋予紧凑型开源大语言模型知识图谱推理能力
authors: "Shuai Wang, Yinan Yu"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21440v3"
tags: ["query:ma-kf"]
score: 9.0
evidence: 通过强化学习进行知识图谱推理
tldr: 针对大语言模型在知识图谱多跳推理中存在的灵活性差和错误累积问题，本文提出 KG-Hopper 强化学习框架。该框架通过将整个 KG 遍历和决策过程整合进单一推理周期的“思考”阶段，使 7B 规模的小型开源模型具备全局推理和动态回溯能力。实验证明，KG-Hopper 在多个基准测试中超越了 70B 规模的大模型，并能与 GPT-4o-mini 等闭源模型媲美。
selection_source: fresh_fetch
motivation: 现有的多步推理方法因步骤孤立且缺乏灵活性，容易导致错误累积，难以处理复杂的知识图谱问答任务。
method: 提出一种强化学习框架，将多跳推理的路径探索与回溯过程统一嵌入到模型的“思考”阶段，实现单次推理完成全局决策。
result: 基于 7B 参数的模型在 8 个基准测试上一致优于 70B 的多步推理系统，并达到了与 GPT-3.5-Turbo 和 GPT-4o-mini 相当的水平。
conclusion: KG-Hopper 证明了通过强化学习优化推理过程，小型开源模型也能在复杂的知识密集型任务中展现出卓越的性能和效率。
---

## 摘要
大语言模型（LLMs）展现出令人印象深刻的自然语言能力，但在处理知识密集型推理任务时往往面临困难。利用结构化知识图谱（KGs）的知识库问答（KBQA）因需要准确的多跳推理而体现了这一挑战。现有方法通常在预定义流水线的指导下执行顺序推理步骤，这限制了灵活性，并由于每一步的孤立推理而导致错误级联。为了解决这些局限性，我们提出了 KG-Hopper，这是一个新颖的强化学习（RL）框架，旨在赋予紧凑型开源 LLM 在单轮推理中执行集成多跳 KG 推理的能力。我们不再采用逐步推理的方式，而是训练一个推理 LLM，将整个 KG 遍历和决策过程嵌入到一个统一的“思考”阶段，从而实现对跨步依赖关系的全局推理以及带有回溯的动态路径探索。在八个 KG 推理基准测试上的实验结果表明，基于 7B 参数 LLM 的 KG-Hopper 始终优于更大规模的多步系统（高达 70B），并达到了与 GPT-3.5-Turbo 和 GPT-4o-mini 等私有模型相当的性能，同时保持了紧凑、开源和数据高效的特点。代码已公开：https://github.com/Wangshuaiia/KG-Hopper。

## Abstract
Large Language Models (LLMs) demonstrate impressive natural language capabilities but often struggle with knowledge-intensive reasoning tasks. Knowledge Base Question Answering (KBQA), which leverages structured Knowledge Graphs (KGs) exemplifies this challenge due to the need for accurate multi-hop reasoning. Existing approaches typically perform sequential reasoning steps guided by predefined pipelines, restricting flexibility and causing error cascades due to isolated reasoning at each step. To address these limitations, we propose KG-Hopper, a novel Reinforcement Learning (RL) framework that empowers compact open LLMs with the ability to perform integrated multi-hop KG reasoning within a single inference round. Rather than reasoning step-by-step, we train a Reasoning LLM that embeds the entire KG traversal and decision process into a unified ``thinking'' stage, enabling global reasoning over cross-step dependencies and dynamic path exploration with backtracking. Experimental results on eight KG reasoning benchmarks show that KG-Hopper, based on a 7B-parameter LLM, consistently outperforms larger multi-step systems (up to 70B) and achieves competitive performance with proprietary models such as GPT-3.5-Turbo and GPT-4o-mini, while remaining compact, open, and data-efficient. The code is publicly available at: https://github.com/Wangshuaiia/KG-Hopper.

---

## 论文详细总结（自动生成）

### 论文总结：KG-Hopper

#### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）在处理知识密集型任务（如知识库问答 KBQA）时，常面临幻觉和多跳推理能力不足的问题。
*   **研究背景**：现有的方法通常采用多步顺序推理（Sequential Reasoning），即通过预定义的流水线多次调用 LLM。这种模式存在两个主要缺陷：
    1.  **灵活性差**：难以处理不完整或误导性的知识图谱（KG）数据，缺乏动态调整和回溯能力。
    2.  **错误级联**：早期步骤的错误会传播并放大，且每一步推理相互孤立，忽略了跨步骤的依赖关系。
*   **研究目标**：开发一种能够通过单次推理（Single-round）完成复杂多跳 KG 遍历和决策的紧凑型开源模型。

#### 2. 方法论
*   **核心思想**：借鉴类似 DeepSeek-R1 的“思考（Thinking）”机制，将 KG 检索、路径探索、错误纠正和最终回答整合进 LLM 的单次生成过程中。
*   **关键技术细节**：
    *   **KG 检索工具**：采用两阶段策略，先检索谓词（关系），再根据语义相关性选择并检索目标实体，通过 SPARQL 语句与图数据库交互。
    *   **冷启动（Cold Start）**：使用 500 条高质量的思维链（CoT）数据对基础模型进行有监督微调（SFT），教会模型如何调用 `<search>` 标签。
    *   **强化学习（RL）框架**：采用 **GRPO（组相对策略优化）** 算法。
    *   **复合奖励函数**：
        1.  **检索奖励**：鼓励模型使用工具，但设有上限以防过度检索。
        2.  **格式奖励**：强制模型遵循 `<think>`、`<search>`、`<answer>` 的结构。
        3.  **推理奖励**：利用外部大模型（如 Llama-3.3-70B）评估推理轨迹的逻辑合理性。
        4.  **答案奖励**：通过语义相似度判断最终答案是否正确。
    *   **优化技巧**：
        *   **掩码处理**：在训练中掩盖检索到的三元组 Token，防止模型简单重复外部知识。
        *   **历史重采样（History Resampling）**：在训练后期移除简单的一跳问题，强制模型专注于复杂的多跳推理（课程学习思想）。

#### 3. 实验设计
*   **数据集**：涵盖 8 个主流基准测试，涉及 Freebase（CWQ, WebQSP, WebQuestions, GrailQA）和 WikiData（QALD10-en, T-REx, Zero-Shot RE, Creak）两大知识图谱。
*   **对比方法**：
    *   **Prompt-only**：Qwen-2.5-7B, Llama-3.1-8B/70B, GPT-4o/mini。
    *   **KG 增强（无微调）**：上述模型结合 KG 检索工具。
    *   **SFT 增强**：Interactive-KBQA, KG-CoT 等基于 SFT 的方法。
*   **评估指标**：主要使用 Hit@1（问答任务）和 Accuracy（事实验证任务）。

#### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA A100 80G GPU**。
*   **训练参数**：训练 2 个 Epoch，Batch Size 为 16，学习率为 1e-6，每个输入生成 16 个 Rollout（采样轨迹）。
*   **模型规模**：主要基于 Qwen-2.5-7B 和 Llama-3.1-8B 进行实验。

#### 5. 实验数量与充分性
*   **实验规模**：在 8 个不同性质的数据集上进行了测试，涵盖了复杂问答、槽位填充和事实核查。
*   **消融实验**：详细对比了 RL 与 SFT 的效果、各项奖励组件的贡献、有无历史重采样的影响，以及 LLM 作为裁判（Judge）的可靠性。
*   **充分性评价**：实验设计较为全面，通过跨模型（Qwen/Llama）和跨规模（7B vs 70B）的对比，客观地证明了 RL 在提升逻辑推理深度方面的优势。

#### 6. 主要结论与发现
*   **小模型逆袭**：经过 RL 训练的 7B 模型在多跳推理任务上一致优于未经针对性训练的 70B 模型，并能与 GPT-4o-mini 等顶级闭源模型竞争。
*   **RL 优于 SFT**：RL 能够学习到更具鲁棒性的推理策略，而 SFT 容易陷入模式模仿（Pattern-based），在处理长程依赖时表现较差。
*   **思考过程的重要性**：将 KG 检索嵌入“思考”阶段，使模型具备了自主回溯和自我纠错的能力，有效缓解了错误级联。

#### 7. 优点
*   **高效集成**：首个将 RL 应用于端到端 KG 推理的工作，实现了单轮推理完成多跳任务，降低了系统复杂性。
*   **数据效率**：仅需少量冷启动数据和标量奖励信号即可实现显著性能提升。
*   **开源透明**：提供了完整的训练框架和代码，具有较强的可复现性和实用价值。

#### 8. 不足与局限
*   **外部模型依赖**：推理奖励（Reasoning Reward）依赖于更强大的外部模型（如 70B 模型）进行打分，这在一定程度上限制了训练的完全独立性。
*   **检索延迟**：虽然是单轮推理，但“思考”阶段产生的长 Token 序列和多次工具调用仍会带来一定的推理延迟。
*   **知识图谱覆盖**：实验主要集中在通用 KG，对于垂直领域（如医疗、法律）的专业 KG 推理效果尚待验证。

（完）
