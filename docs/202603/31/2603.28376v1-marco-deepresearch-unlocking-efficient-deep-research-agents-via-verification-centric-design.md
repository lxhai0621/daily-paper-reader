---
title: "Marco DeepResearch: Unlocking Efficient Deep Research Agents via Verification-Centric Design"
title_zh: Marco DeepResearch：通过以验证为中心的设计解锁高效深度研究智能体
authors: "Bin Zhu, Qianghuai Jia, Tian Lan, Junyang Ren, Feng Gu, Feihu Jiang, Longyue Wang, Zhao Xu, Weihua Luo"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28376v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 具有多步推理能力的自主深度研究智能体
tldr: 针对深度研究智能体在长程任务中因缺乏验证机制导致错误累积的问题，本文提出 Marco DeepResearch。该框架采用以验证为中心的设计，涵盖 QA 数据合成、轨迹构建和推理时缩放三个层面。通过引入显式验证模式和自验证机制，Marco DeepResearch 在 8B 参数规模下显著超越同类模型，甚至在复杂基准测试中比肩 30B 规模的领先智能体。
selection_source: fresh_fetch
motivation: 现有深度研究智能体在数据合成和推理过程中缺乏显式验证机制，导致错误在长程任务中不断累积并降低性能。
method: 提出一种三层验证框架，包括在 QA 合成中确保答案唯一性、在训练轨迹中注入验证模式，以及在推理阶段利用智能体进行自验证。
result: 实验表明 Marco DeepResearch 在 BrowseComp 等基准测试上显著优于 8B 规模智能体，且在受限工具调用次数下接近 30B 规模模型的表现。
conclusion: 以验证为中心的设计能有效提升轻量级研究智能体的效率与可靠性，使其在复杂开放式调查任务中展现出极强的竞争力。
---

## 摘要
深度研究智能体能够自主进行开放式调查，整合复杂的信息检索与跨多源的多步推理，以解决现实世界的问题。为了在长程任务中维持这一能力，训练和推理过程中的可靠验证至关重要。现有范式的一个主要瓶颈在于问答（QA）数据合成、轨迹构建和测试时扩展中缺乏显式的验证机制。在每个阶段引入的错误都会向下游传播，并降低智能体的整体性能。为解决这一问题，我们提出了 Marco DeepResearch，这是一种在三个层面上通过以验证为中心的框架设计进行优化的深度研究智能体：(1) QA 数据合成：我们在基于图和基于智能体的 QA 合成中引入了验证机制，以控制问题难度的同时确保答案的唯一性和正确性；(2) 轨迹构建：我们设计了一种验证驱动的轨迹合成方法，将显式的验证模式注入到训练轨迹中；(3) 测试时扩展：我们在推理时将 Marco DeepResearch 自身作为验证器，有效提升了在挑战性问题上的性能。广泛的实验结果表明，我们提出的 Marco DeepResearch 智能体在大多数具有挑战性的基准测试（如 BrowseComp 和 BrowseComp-ZH）上显著优于 8B 规模的深度研究智能体。至关重要的是，在最大 600 次工具调用的预算下，Marco DeepResearch 甚至超越或接近了若干 30B 规模的智能体，例如通义 DeepResearch-30B。

## Abstract
Deep research agents autonomously conduct open-ended investigations, integrating complex information retrieval with multi-step reasoning across diverse sources to solve real-world problems. To sustain this capability on long-horizon tasks, reliable verification is critical during both training and inference. A major bottleneck in existing paradigms stems from the lack of explicit verification mechanisms in QA data synthesis, trajectory construction, and test-time scaling. Errors introduced at each stage propagate downstream and degrade the overall agent performance. To address this, we present Marco DeepResearch, a deep research agent optimized with a verification-centric framework design at three levels: \textbf{(1)~QA Data Synthesis:} We introduce verification mechanisms to graph-based and agent-based QA synthesis to control question difficulty while ensuring answers are unique and correct; \textbf{(2)~Trajectory Construction:} We design a verification-driven trajectory synthesis method that injects explicit verification patterns into training trajectories; and \textbf{(3)~Test-time scaling:} We use Marco DeepResearch itself as a verifier at inference time and effectively improve performance on challenging questions. Extensive experimental results demonstrate that our proposed Marco DeepResearch agent significantly outperforms 8B-scale deep research agents on most challenging benchmarks, such as BrowseComp and BrowseComp-ZH. Crucially, under a maximum budget of 600 tool calls, Marco DeepResearch even surpasses or approaches several 30B-scale agents, like Tongyi DeepResearch-30B.

---

## 论文详细总结（自动生成）

以下是对论文《Marco DeepResearch: Unlocking Efficient Deep Research Agents via Verification-Centric Design》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
深度研究智能体（Deep Research Agents）旨在通过自主搜索和多步推理解决复杂的开放式问题。然而，在长程（Long-horizon）任务中，智能体面临一个核心瓶颈：**错误累积与传播**。
*   **数据层面**：现有的合成 QA 数据往往存在答案不唯一或错误的问题，导致训练信号模糊。
*   **轨迹层面**：传统的 ReAct 模式缺乏中间步骤的校验，智能体容易接受早期的错误结果。
*   **推理层面**：在增加计算预算（Test-time scaling）时，简单的增加步数往往会引入更多噪声而非更准的答案。
**研究动机**：通过在数据合成、模型训练和推理缩放三个阶段引入“以验证为中心”的设计，提升轻量级（8B）智能体的可靠性与效率。

### 2. 方法论：核心思想与关键技术
Marco DeepResearch 提出了一个三层验证框架：

*   **验证驱动的 QA 数据合成**：
    *   **基于图的合成（对抗验证）**：采用“答案优先”的逆向构建。通过“生成器-攻击者-分析器”循环，不断添加约束条件，直到攻击者无法找到除目标实体外的其他候选答案，确保答案的唯一性。
    *   **基于智能体的合成（证据优先）**：智能体先在网页中构建证据图，再生成问题。通过独立的验证智能体进行事实一致性检查和闭卷过滤（确保必须通过搜索才能回答）。
*   **验证驱动的轨迹构建**：
    *   **多智能体协作模式**：设计“主智能体+搜索子智能体+验证子智能体”的架构。验证子智能体对中间结果和最终答案进行独立校验，若失败则触发修正逻辑。这些复杂的交互轨迹随后被转换为单智能体 ReAct 格式用于 SFT。
    *   **失败轨迹重滚（Re-rollout）**：对回答错误的轨迹进行诊断，在反馈指导下重新生成正确轨迹。
*   **验证器引导的测试时缩放（Test-time Scaling）**：
    *   **Discard All 策略**：当触发退化信号（如达到最大步数或无法解决）时，清空所有中间推理历史，仅保留原始查询重新开始，防止错误上下文干扰。
    *   **自验证机制**：将 Marco DeepResearch 自身作为验证器，对多个候选答案进行独立和联合校验。

### 3. 实验设计
*   **数据集/基准测试**：涵盖了 6 个深度搜索基准，包括 **BrowseComp**（网页浏览）、**BrowseComp-ZH**（中文浏览）、**GAIA**（现实世界助手任务）、**xBench-DeepSearch**、**WebWalkerQA** 和 **DeepSearchQA**。
*   **对比方法**：
    *   **基础模型**：GPT-5 High, OpenAI-o3, Claude-4.5-Opus, DeepSeek-V3.2 等。
    *   **≥30B 规模智能体**：通义 DeepResearch-30B, MiroThinker-v1.5-30B/72B, WebSailor-v2 等。
    *   **≤8B 规模智能体**：MiroThinker-v1.0-8B, AgentCPM-Explore-4B, RE-TRAC-4B 等。

### 4. 资源与算力
*   **硬件环境**：使用了 **64 张 NVIDIA A100 GPU**。
*   **训练框架**：基于 Megatron-LM。
*   **技术细节**：使用 YaRN 将上下文窗口扩展至 **128K**。
*   **训练时长**：文中未明确给出具体的训练总时长，但提到了 SFT 和基于 GRPO（Group Relative Policy Optimization）的强化学习阶段。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个主流基准上进行了全面测试，并针对 8B 规模模型进行了详尽的对比。
*   **消融实验**：非常充分。分别验证了：(1) 对抗验证对 QA 质量的提升；(2) 多智能体验证轨迹对性能的贡献；(3) RL 阶段的增益；(4) 测试时缩放策略（Discard All + Verify）的效果；(5) 上下文窗口（64K vs 128K）的影响。
*   **客观性**：通过手动评估 100 个合成样本（错误率 < 10%）来验证数据质量，并使用了统一的工具调用预算（600 次）进行公平对比。

### 6. 主要结论与发现
*   **以小博大**：Marco DeepResearch (8B) 在 BrowseComp 和 BrowseComp-ZH 等核心基准上显著超越了所有同尺寸模型。
*   **跨级竞争**：在受限的工具调用预算下，该 8B 模型在中文基准上超越了通义 DeepResearch-30B 和 MiroThinker-30B，证明了架构设计比单纯堆砌参数更有效。
*   **验证的力量**：测试时缩放策略带来了巨大的性能提升（在 BrowseComp-ZH 上提升了 17.8 个百分点），证明了“重试+验证”是解决长程任务的关键。

### 7. 优点（亮点）
*   **全生命周期验证**：不仅在推理时验证，更将验证逻辑内化到训练数据（QA 对和轨迹）中。
*   **高效的数据合成**：对抗验证机制解决了合成数据中常见的“答案不唯一”痛点，提高了监督信号的质量。
*   **推理策略创新**：结合了 Discard All 和自验证，有效解决了长程推理中的上下文污染问题。

### 8. 不足与局限
*   **依赖强老师模型**：高质量轨迹的合成仍需依赖 Qwen3.5-Plus、GPT-4 等更强大的模型作为“老师”。
*   **推理成本**：虽然模型参数量小，但“重滚”和“多次验证”策略会显著增加推理时的 Token 消耗和时间延迟。
*   **极难任务差距**：在 GAIA 等极具挑战性的任务上，虽然领先 8B 模型，但距离 OpenAI-o3 或 GPT-5 等顶级闭源模型仍有一定差距。

（完）
