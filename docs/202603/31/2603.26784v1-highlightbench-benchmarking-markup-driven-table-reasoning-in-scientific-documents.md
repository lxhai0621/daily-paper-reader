---
title: "HighlightBench: Benchmarking Markup-Driven Table Reasoning in Scientific Documents"
title_zh: HighlightBench：科学文献中标记驱动表格推理的基准测试
authors: "Lexin Wang, Shenghua Liu, Yiwei Wang, Yujun Cai, Yuyao Ge, Jiayu Yao, Jiafeng Guo, Xueqi Cheng"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.26784v1"
tags: ["query:mmkqa"]
score: 9.0
evidence: 多模态模型中标记驱动的表格推理基准测试
tldr: 针对科学文档中表格视觉标记（如高亮、加粗）的理解与推理，本文提出了 HighlightBench 评测基准。该基准将评估分解为标记定位、约束检索、局部关系、聚合比较及一致性缺失五个维度，旨在区分模型在感知与推理阶段的失效原因。通过引入显式中间决策的参考流水线，研究发现即使是强大的多模态大模型在处理结构化输出约束下的视觉标记推理时仍表现不稳定。
selection_source: fresh_fetch
motivation: 现有评估无法区分多模态大模型是未能识别视觉标记还是未能根据标记进行逻辑推理，导致在评估标记驱动的表格理解时存在盲点。
method: 引入了包含五大任务家族的诊断性基准 HighlightBench，并提供了一个使中间决策显式化的参考流水线以实现细粒度的错误归因。
result: 实验显示，即使是顶尖模型在需要将视觉线索与符号推理一致对齐并遵循结构化输出约束时，依然表现出不稳定性。
conclusion: HighlightBench 为评估模型在处理带标记表格时的感知到执行链条提供了有效的诊断工具，揭示了当前模型在视觉标记驱动推理方面的局限性。
---

## 摘要
高亮、下划线和加粗文本等视觉标记在以表格为中心的文档中非常常见。尽管多模态大语言模型（MLLMs）在文档理解方面取得了实质性进展，但其将此类线索视为显式逻辑指令的能力仍有待探索。更重要的是，现有的评估无法区分模型是未能识别标记，还是未能利用标记进行推理。这在评估基于标记的表格行为时产生了一个关键盲点。为了填补这一空白，我们推出了 HighlightBench，这是一个用于标记驱动表格理解的诊断性基准测试，它将评估分解为五个任务族：标记定位（Markup Grounding）、受限检索（Constrained Retrieval）、局部关系（Local Relations）、聚合与比较（Aggregation & Comparison）以及一致性与缺失性（Consistency & Missingness）。我们还提供了一个参考流水线，使中间决策显式化，从而实现可重复的基准测试，并在从感知到执行的链条上进行更细粒度的错误归因。实验表明，当视觉线索必须在结构化输出约束下与符号推理保持一致时，即使是强大的模型也表现得不稳定。

## Abstract
Visual markups such as highlights, underlines, and bold text are common in table-centric documents. Although multimodal large language models (MLLMs) have made substantial progress in document understanding, their ability to treat such cues as explicit logical directives remains under-explored. More importantly, existing evaluations cannot distinguish whether a model fails to see the markup or fails to reason with it. This creates a key blind spot in assessing markup-conditioned behavior over tables. To address this gap, we introduce HighlightBench, a diagnostic benchmark for markup-driven table understanding that decomposes evaluation into five task families: Markup Grounding, Constrained Retrieval, Local Relations, Aggregation \& Comparison, and Consistency \& Missingness. We further provide a reference pipeline that makes intermediate decisions explicit, enabling reproducible baselines and finer-grained attribution of errors along the perception-to-execution chain. Experiments show that even strong models remain unstable when visual cues must be consistently aligned with symbolic reasoning under structured output constraints.

---

## 论文详细总结（自动生成）

### 论文总结：HighlightBench - 科学文献中标记驱动表格推理的基准测试

#### 1. 核心问题与研究动机
在科学论文和技术报告中，表格常含有高亮、加粗、下划线或箭头等**视觉标记**，用以强调关键信息或指示特定的逻辑操作（如“比较高亮行”）。
*   **研究动机**：现有的多模态大模型（MLLM）虽然在文档理解上进步显著，但在处理这些标记时存在“盲点”。
*   **核心问题**：当前的评估方法通常只看最终答案，无法区分模型失败是因为**没看见标记**（感知失败），还是**不知道如何根据标记进行推理**（逻辑失败）。这种模糊性导致无法准确衡量模型在标记驱动任务下的真实能力。

#### 2. 方法论
论文提出了 **HighlightBench** 诊断基准和一套**参考流水线（Reference Pipeline）**：
*   **五大任务家族（T1-T5）**：
    1.  **标记定位 (T1)**：测试模型能否将视觉标记准确绑定到单元格、行或列。
    2.  **受限检索 (T2)**：在标记约束下提取特定值。
    3.  **局部关系 (T3)**：测试标记周围的表格拓扑结构（如邻居单元格）是否被正确识别。
    4.  **聚合与比较 (T4)**：对标记区域进行排序、求最大值或求和等操作。
    5.  **一致性与缺失性 (T5)**：处理含有缺失值（N/A）或冲突标记的复杂情况。
*   **参考流水线设计**：
    *   **文档图生成 (Docgraph)**：将图像转换为包含文本、结构和标记属性的结构化表示。
    *   **两阶段路由 (Two-stage Routing)**：先确定任务路径，再解析具体约束，减少错误传播。
    *   **DSL 执行**：将推理计划转化为确定性的领域特定语言（DSL）脚本执行，确保计算过程透明可追溯。

#### 3. 实验设计
*   **数据集构成**：共包含 446 张表格图像和 3,283 个问题。
    *   **真实世界子集**：从 CV 和 NLP 论文中收集，保留了复杂的表头和不规则布局。
    *   **合成子集**：通过模板生成，用于控制难度和进行反事实分析。
*   **对比方法**：评估了 8 个主流模型，包括闭源模型（Claude 4 Sonnet, Gemini 2.5 Flash）和开源模型（Qwen3-VL, InternVL3.5, MiniCPM-V 等）。
*   **反事实分析 (Counterfactual Analysis)**：保持表格内容和问题不变，仅改变标记的位置（如将高亮移至错误答案上），观察模型决策的变化。

#### 4. 资源与算力
*   论文主要关注**评估基准的构建与模型推理表现**，并未详细说明模型训练所需的具体算力（如 GPU 型号、数量或训练时长）。
*   实验部分主要涉及模型推理（Inference），使用了 VLMEvalKit 工具包进行标准化调用。

#### 5. 实验数量与充分性
*   **实验规模**：针对 8 个模型在 5 大任务、21 个细分任务上进行了全面测试，总计超过 3000 个样本。
*   **充分性**：实验设计较为充分。除了端到端的准确率对比，还专门设计了反事实实验来探测模型的“捷径行为”。
*   **客观性**：通过引入结构化输出（JSON）和自动评分器，减少了人工评价的主观性；通过合成数据确保了答案的唯一性和确定性。

#### 6. 主要结论与发现
*   **模型不稳定性**：即使是顶尖模型（如 Gemini 2.5 Flash），在需要同时满足标记定位、逻辑计算和格式约束时也会表现出明显波动。
*   **标记“劫持”现象**：反事实实验证明，视觉标记会显著误导模型。当标记出现在错误选项上时，模型往往会忽略正确的数值逻辑，转而选择被标记的错误项（Marker Hijacked）。
*   **感知差异**：模型对加粗和下划线的识别相对稳定，但对**高亮（Highlight）**的识别和定位能力较弱，容易产生混淆。
*   **结构化优势**：论文提出的参考流水线在处理复杂结构和执行任务（T3-T4）时优于大多数开源端到端模型，证明了显式中间表示的重要性。

#### 7. 优点与亮点
*   **诊断性强**：不仅给出分数，还能通过任务分解定位模型是在感知、绑定还是执行阶段出错。
*   **反事实设计**：巧妙地隔离了“视觉干扰”与“逻辑推理”，揭示了模型对视觉显著性的过度依赖。
*   **可解释性**：提供的参考流水线为未来的表格理解系统提供了一个可检查、可调试的框架。

#### 8. 不足与局限
*   **样本规模**：虽然问题数量较多，但表格图像总量（446张）相对较小，可能无法覆盖所有极端的科学图表布局。
*   **领域局限**：主要集中在计算机视觉（CV）和自然语言处理（NLP）领域的论文表格，对生物、化学等其他学科的特殊表格格式覆盖有限。
*   **合成数据偏差**：合成子集虽然易于控制，但其布局多样性可能仍逊于真实的复杂扫描文档。

（完）
