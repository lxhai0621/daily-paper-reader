---
title: "GEMS: Agent-Native Multimodal Generation with Memory and Skills"
title_zh: GEMS：具有记忆与技能的智能体原生多模态生成
authors: "Zefeng He, Siyuan Huang, Xiaoye Qu, Yafu Li, Tong Zhu, Yu Cheng, Yang Yang"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28088v1"
tags: ["query:agent"]
score: 9.0
evidence: 具有持久轨迹级记忆的智能体原生多模态生成
tldr: GEMS是一个旨在提升多模态生成模型处理复杂指令和专业任务能力的框架。它通过Agent Loop实现闭环迭代优化，利用Agent Memory进行分层记忆管理，并借助Agent Skill提供领域专家知识。实验证明，该框架能显著增强不同规模模型的性能，甚至使轻量级模型在特定基准上超越SOTA模型，展示了Agent架构在扩展模型能力方面的巨大潜力。
selection_source: fresh_fetch
motivation: 现有多模态生成模型在处理复杂指令和特定领域任务时存在局限性，难以通过单一模型直接解决。
method: 提出了包含Agent Loop（闭环优化）、Agent Memory（分层记忆）和Agent Skill（领域技能）的三位一体Agent原生框架。
result: 在多项主流及下游任务中取得显著提升，使6B规模的轻量级模型在GenEval2上超越了最先进的Nano Banana 2。
conclusion: GEMS证明了通过Agent化架构可以有效突破基础模型的固有能力边界，提升多模态生成的质量和专业性。
---

## 摘要
近期的多模态生成模型在通用生成任务上取得了显著进展，但在处理复杂指令和专业下游任务时仍面临挑战。受 Claude Code 等先进智能体框架成功的启发，我们提出了 GEMS（具有记忆与技能的智能体原生多模态生成），这是一个旨在突破基础模型在通用和下游任务中固有局限性的框架。GEMS 构建于三个核心组件之上：Agent Loop（智能体循环）引入了一个结构化的多智能体框架，通过闭环优化迭代提升生成质量；Agent Memory（智能体记忆）提供持久的轨迹级记忆，分层存储事实状态和压缩的经验总结，在减少冗余的同时实现对优化过程的全局视图；Agent Skill（智能体技能）提供可扩展的领域特定专业知识库，支持按需加载，使系统能够有效处理多样化的下游应用。在五个主流任务和四个下游任务中，通过在多个生成后端上的评估，GEMS 一致地实现了显著的性能提升。最值得注意的是，它使轻量级的 6B 模型 Z-Image-Turbo 在 GenEval2 上超越了当前最先进的 Nano Banana 2，证明了智能体治理在扩展模型能力、使其超越原始极限方面的有效性。

## Abstract
Recent multimodal generation models have achieved remarkable progress on general-purpose generation tasks, yet continue to struggle with complex instructions and specialized downstream tasks. Inspired by the success of advanced agent frameworks such as Claude Code, we propose \textbf{GEMS} (Agent-Native Multimodal \textbf{GE}neration with \textbf{M}emory and \textbf{S}kills), a framework that pushes beyond the inherent limitations of foundational models on both general and downstream tasks. GEMS is built upon three core components. Agent Loop introduces a structured multi-agent framework that iteratively improves generation quality through closed-loop optimization. Agent Memory provides a persistent, trajectory-level memory that hierarchically stores both factual states and compressed experiential summaries, enabling a global view of the optimization process while reducing redundancy. Agent Skill offers an extensible collection of domain-specific expertise with on-demand loading, allowing the system to effectively handle diverse downstream applications. Across five mainstream tasks and four downstream tasks, evaluated on multiple generative backends, GEMS consistently achieves significant performance gains. Most notably, it enables the lightweight 6B model Z-Image-Turbo to surpass the state-of-the-art Nano Banana 2 on GenEval2, demonstrating the effectiveness of agent harness in extending model capabilities beyond their original limits.

---

## 论文详细总结（自动生成）

以下是对论文《GEMS: Agent-Native Multimodal Generation with Memory and Skills》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：尽管现有的多模态生成模型（如 T2I 模型）在通用任务上表现优异，但在处理**复杂指令**（多维度约束）和**专业下游任务**（如空间布局、长文本渲染、创意绘画）时仍面临“长尾挑战”，即基础模型的能力边界难以覆盖这些高难度或特定领域的任务。
*   **研究动机**：
    *   现有的推理时缩放（Inference-time scaling）方法（如简单的提示词重写或迭代优化）往往缺乏全局视角，导致信息冗余或引导不足。
    *   特定任务的专家系统难以集成到通用的生成流水线中，架构碎片化严重。
    *   受 Claude Code 等先进智能体框架启发，探索如何通过“智能体原生（Agent-Native）”的架构来突破基础模型的固有性能限制。

### 2. 方法论：GEMS 框架
GEMS 提出了由三个核心支柱组成的智能体架构：

*   **Agent Loop（智能体循环）**：
    *   **Planner（规划器）**：识别用户意图并触发相关技能，生成初始增强提示词。
    *   **Decomposer（分解器）**：将复杂指令拆解为多个可验证的原子视觉标准（如“是否有猫？”、“猫是否是黑色的？”）。
    *   **Generator（生成器）**：模型无关的组件，负责图像合成。
    *   **Verifier（验证器）**：利用多模态大模型（MLLM）根据原子标准对生成的图像进行二进制反馈（Yes/No）。
    *   **Refiner（细化器）**：结合当前状态、历史记忆和验证反馈，迭代优化提示词。
*   **Agent Memory（智能体记忆）**：
    *   **分层压缩策略**：为了平衡信息密度与 Token 效率，将记忆分为两层：
        *   **事实工件（Factual Artifacts）**：原始存储提示词、图像和反馈。
        *   **经验总结（Experiential Summaries）**：通过压缩器将冗长的思维链（CoT）提炼为高层次的经验，消除冗余。
*   **Agent Skill（智能体技能）**：
    *   **按需加载机制**：建立了一个可扩展的领域专家库（如空间智能、文本渲染、创意绘画）。
    *   **渐进式暴露**：平时仅加载轻量级的技能清单，只有当特定技能被触发时才加载详细的指令，降低了系统的认知负荷和计算开销。

### 3. 实验设计
*   **数据集与场景**：
    *   **主流基准（5个）**：GenEval、GenEval2（最具挑战性）、DPG-Bench、OneIG（中英双语）、WISE。
    *   **下游任务（4个）**：LongText-Bench（文本渲染）、SpatialGenEval（空间智能）、CREA（创意绘画）、ArtiMuse（审美评价）。
*   **对比方法**：
    *   **闭源模型**：Nano Banana (1 & 2)、GPT-Image、Seedream 4。
    *   **开源模型**：Bagel、Z-Image、Qwen-Image。
    *   **推理时缩放方法**：Rewrite（重写）、Promptist、Random Search、Maestro、CRAFT。
*   **生成后端**：验证了轻量级模型 **Z-Image-Turbo (6B)** 和中等规模模型 **Qwen-Image-2512 (20B)**。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或训练时长。
*   **推理配置**：
    *   使用了 **Kimi K2.5** 作为 MLLM 后端（用于验证和细化）。
    *   最大迭代次数默认设置为 5 次。
    *   Z-Image-Turbo 采用 8 步推理，Qwen-Image 采用 50 步推理。
*   **效率分析**：论文通过实验展示了 GEMS 在平均生成约 3 张图像时即可达到极高分数，强调了其在推理成本与性能之间的平衡。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 9 个不同的任务维度，并在两个不同架构、不同参数规模（6B 和 20B）的开源模型上进行了验证。
*   **消融实验**：针对 Agent Loop、Agent Memory（不同记忆组件的影响）和 Agent Skill（技能分布与贡献）分别做了详细的消融研究。
*   **客观性与公平性**：
    *   在对比 Search、Maestro 和 CRAFT 等方法时，严格限制了它们的并行度或迭代次数，以确保在相似的计算预算下进行比较。
    *   GenEval2 的结果取了三次独立运行的平均值，增强了结果的可信度。

### 6. 主要结论与发现
*   **性能飞跃**：GEMS 使 6B 规模的 Z-Image-Turbo 在 GenEval2 上超越了最先进的闭源模型 Nano Banana 2。
*   **平均增益**：在主流任务上平均提升 14.22 分，在下游任务上平均提升 14.03 分。
*   **记忆的重要性**：分层压缩记忆比简单的上下文堆叠更有效，能显著提升细化器的优化方向感。
*   **技能的价值**：在特定领域（如空间关系和创意绘画），Agent Skill 提供的专家知识是突破基础模型上限的关键。

### 7. 优点
*   **架构创新**：将生成任务重构为“智能体原生”的迭代优化问题，而非单一的生成过程。
*   **高效记忆管理**：通过压缩经验而非原始思维链，解决了长上下文带来的噪声和 Token 浪费问题。
*   **高度可扩展**：Agent Skill 模块允许用户通过简单的 Markdown 文件添加新技能，具有极强的灵活性。
*   **模型无关性**：该框架可以作为任何 T2I 模型的“外挂”增强系统。

### 8. 不足与局限
*   **推理延迟**：由于采用了迭代循环架构，尽管单次生成很快，但多次迭代累加后的端到端延迟仍然较高。
*   **自主性限制**：目前的系统仍依赖预定义的流水线协调，尚未实现完全自主的工具调用和记忆管理。
*   **功能覆盖**：目前的后端模型（Z-Image-Turbo 等）不支持图像编辑功能，限制了 GEMS 在图像局部修改任务上的应用。
*   **依赖强 MLLM**：系统的验证和细化质量高度依赖于后端 MLLM（如 Kimi K2.5）的判断能力。

（完）
