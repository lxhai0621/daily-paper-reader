---
title: Resolving the Robustness-Precision Trade-off in Financial RAG through Hybrid Document-Routed Retrieval
title_zh: 通过混合文档路由检索解决金融 RAG 中的鲁棒性与精确度权衡
authors: "Zhiyuan Cheng, Longying Lai, Yue Liu"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.26815v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 混合文档路由检索以提高RAG精度
tldr: 针对金融文档RAG系统中分块检索（CBR）易产生跨文档混淆及语义文件路由（SFR）精度不足的问题，本文提出了混合文档路由检索（HDRR）。HDRR采用两阶段架构，先利用SFR定位目标文档，再在文档内进行分块检索。实验表明，HDRR在FinDER基准测试中显著降低了失败率并提升了回答精度，有效解决了鲁棒性与精确度之间的权衡。
selection_source: fresh_fetch
motivation: 传统的金融RAG系统在处理结构相似的文档时，面临分块检索导致的跨文档混淆与语义路由导致的精度损失之间的权衡。
method: 提出HDRR两阶段架构，首先通过LLM结构化输出将查询路由至特定文档，随后在限定范围内执行分块检索以获取精确上下文。
result: "HDRR在FinDER基准上取得了7.54的最高平均分，相比CBR提升了25.2%，同时将失败率降至6.4%并显著提高了完美回答率。"
conclusion: HDRR通过结合文档级过滤与细粒度分块检索，成功消除了跨文档干扰并保持了高精度，是金融领域RAG系统的更优方案。
---

## 摘要
用于金融文档问答的检索增强生成 (RAG) 系统通常遵循基于分块 (chunk-based) 的范式：将文档拆分为片段，嵌入到向量空间中，并通过相似度搜索进行检索。虽然在通用场景下有效，但这种方法在结构同质的语料库（如监管文件）中会遇到跨文档分块混淆的问题。语义文件路由 (SFR) 利用大语言模型 (LLM) 的结构化输出将查询路由到整个文档，减少了灾难性失败，但牺牲了针对性分块检索的精确度。我们通过在 FinDER 基准测试（涵盖五个组别的 1,500 个查询）上的受控评估识别出了这种鲁棒性与精确度的权衡：SFR 获得了更高的平均分（6.45 对比 6.02）和更少的失败（10.3% 对比 22.5%），而基于分块的检索 (CBR) 则产生了更多完美答案（13.8% 对比 8.5%）。为了解决这一权衡，我们提出了混合文档路由检索 (HDRR)，这是一种两阶段架构，它将 SFR 用作文档过滤器，随后在识别出的文档范围内进行基于分块的检索。HDRR 在消除跨文档混淆的同时，保留了针对性分块的精确度。实验结果表明，HDRR 在各项指标上均达到了最佳性能：平均分为 7.54（比 CBR 高 25.2%，比 SFR 高 16.9%），失败率仅为 6.4%，正确率为 67.7%（比 CBR 高 18.7 个百分点），完美答案率为 20.1%（比 CBR 高 6.3 个百分点，比 SFR 高 11.6 个百分点）。HDRR 通过在所有五个实验组中同时实现最低的失败率和最高的精确度，解决了这一权衡问题。

## Abstract
Retrieval-Augmented Generation (RAG) systems for financial document question answering typically follow a chunk-based paradigm: documents are split into fragments, embedded into vector space, and retrieved via similarity search. While effective in general settings, this approach suffers from cross-document chunk confusion in structurally homogeneous corpora such as regulatory filings. Semantic File Routing (SFR), which uses LLM structured output to route queries to whole documents, reduces catastrophic failures but sacrifices the precision of targeted chunk retrieval. We identify this robustness-precision trade-off through controlled evaluation on the FinDER benchmark (1,500 queries across five groups): SFR achieves higher average scores (6.45 vs. 6.02) and fewer failures (10.3% vs. 22.5%), while chunk-based retrieval (CBR) yields more perfect answers (13.8% vs. 8.5%). To resolve this trade-off, we propose Hybrid Document-Routed Retrieval (HDRR), a two-stage architecture that uses SFR as a document filter followed by chunk-based retrieval scoped to the identified document(s). HDRR eliminates cross-document confusion while preserving targeted chunk precision. Experimental results demonstrate that HDRR achieves the best performance on every metric: an average score of 7.54 (25.2% above CBR, 16.9% above SFR), a failure rate of only 6.4%, a correctness rate of 67.7% (+18.7 pp over CBR), and a perfect-answer rate of 20.1% (+6.3 pp over CBR, +11.6 pp over SFR). HDRR resolves the trade-off by simultaneously achieving the lowest failure rate and the highest precision across all five experimental groups.

---

## 论文详细总结（自动生成）

这篇论文针对金融领域检索增强生成（RAG）系统中的性能瓶颈，提出了一种创新的混合架构。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：金融监管文件（如 SEC 的 10-K 报告）具有高度的**结构同质性**。不同公司的报告往往包含相同的章节标题（如“风险因素”）和类似的法律术语。
*   **核心问题**：
    *   **分块检索（CBR）的困境**：传统的 RAG 将文档切分为块并存入向量数据库。在处理金融文档时，不同公司的相似章节在向量空间中距离极近，导致系统经常检索到错误公司的信息（**跨文档分块混淆**）。
    *   **语义文件路由（SFR）的缺陷**：直接将查询路由到整个文档虽能避免公司混淆，但由于文档极长（10万+ token），会导致 LLM 难以精准定位答案（**上下文稀释**）。
*   **研究动机**：解决 RAG 在处理结构化同质语料库时，在“检索鲁棒性”与“回答精确度”之间的权衡问题。

### 2. 论文提出的方法论：HDRR
论文提出了**混合文档路由检索（HDRR）**，这是一种三阶段的检索架构：
*   **第一阶段：文档路由（Routing）**：利用 LLM 的“结构化输出”功能，从自然语言查询中提取元数据（如股票代码 Ticker 和财年 Year），并将其映射到特定的文件路径。
*   **第二阶段：限定范围的分块检索（Scoped Retrieval）**：
    *   如果路由成功，系统仅在识别出的特定文档内部进行分块检索（结合全文搜索 FTS 和向量搜索）。
    *   通过这种方式，完全消除了来自其他公司文档的干扰噪声。
    *   **回退机制**：如果路由无法识别文档，系统会自动降级为全局分块检索（同 CBR），确保系统可用性。
*   **第三阶段：答案生成**：将重排序（Reranking）后的精准分块提交给生成模型。

### 3. 实验设计
*   **数据集**：使用 **FinDER** 基准测试集，这是专门为金融 RAG 评估设计的，包含 5,703 个真实的金融查询-证据-答案对。
*   **实验场景**：从 FinDER 中随机抽取 1,500 个查询，分为 5 个独立组（每组 300 个），以评估性能的稳定性。
*   **对比方法**：
    1.  **CBR (Baseline)**：标准的分块检索 RAG，包含混合搜索和重排序。
    2.  **SFR**：基于 LLM 路由的全文检索，不进行分块。
    3.  **HDRR (Proposed)**：本文提出的混合路由检索方法。
*   **评估指标**：采用 LLM-as-a-judge（GPT-4.1）进行评分（1-10分），统计平均分、失败率（1分）、正确率（≥8分）和完美率（10分）。

### 4. 资源与算力
*   **模型使用**：生成与路由模型使用 **GPT-4.1**，重排序模型使用 **Jina Reranker v2**，嵌入模型使用 OpenAI 的 **text-embedding-3-small**。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。由于主要基于 API 调用和现有的向量检索库（FAISS、SQLite FTS5），其核心开销在于 API 的 Token 消耗和推理延迟，而非模型训练。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 1,500 次查询测试，覆盖了定性分析、定量计算和多步推理等多种金融问答场景。
*   **充分性与公平性**：
    *   实验采用了**配对设计**，即三种方法在完全相同的查询组上运行，确保了对比的公平性。
    *   通过 5 个独立组的重复实验，计算了标准差，证明了结果的统计稳定性。
    *   实验设计较为客观，充分展示了不同方法在不同得分区间（低、中、高）的分布变化。

### 6. 主要结论与发现
*   **HDRR 全面领先**：HDRR 在所有指标上均显著优于 CBR 和 SFR。平均分达到 7.54（比 CBR 提升 25.2%）。
*   **解决权衡问题**：HDRR 成功实现了最低的失败率（6.4%）和最高的完美回答率（20.1%），证明了“先路由、后分块”策略的有效性。
*   **失败原因分析**：CBR 的失败主要源于跨文档混淆（22.5% 的高失败率）；SFR 虽然失败率较低，但由于上下文过长，完美率仅为 8.5%。

### 7. 优点
*   **架构简洁高效**：通过简单的两阶段过滤逻辑，解决了复杂的向量空间混淆问题。
*   **成本效益高**：相比于 SFR 动辄输入 10万+ Token，HDRR 仅输入精选分块，大幅降低了推理成本。
*   **鲁棒性强**：内置的回退机制确保了在元数据提取失败时，系统性能不会低于标准 RAG。
*   **定义明确**：论文形式化定义了“命名规整性”和“结构同质性”两个语料库属性，为该方法的适用范围提供了理论指导。

### 8. 不足与局限
*   **元数据依赖**：该方法高度依赖于文档的命名规范（如必须有 Ticker 和 Year）。对于命名混乱或缺乏元数据的非结构化语料库，路由阶段可能失效。
*   **实体识别风险**：如果公司更名或 Ticker 变更（如文中提到的 PEAK 变为 DOC），路由可能会指向错误文件或触发回退。
*   **多文档比较限制**：目前的评估主要集中在单公司查询，对于涉及数十家公司横向对比的复杂查询，路由逻辑可能变得非常复杂。

（完）
