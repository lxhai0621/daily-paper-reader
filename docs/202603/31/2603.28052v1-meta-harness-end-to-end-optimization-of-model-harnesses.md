---
title: "Meta-Harness: End-to-End Optimization of Model Harnesses"
title_zh: Meta-Harness：模型 Harness 的端到端优化
authors: "Yoonho Lee, Roshen Nair, Qizheng Zhang, Kangwook Lee, Omar Khattab, Chelsea Finn"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28052v1"
tags: ["query:agent"]
score: 9.0
evidence: 优化大模型应用的上下文管理和检索代码
tldr: LLM系统的性能不仅取决于模型权重，还取决于处理信息的“harness”代码。目前harness多为人工设计，且现有优化器效果有限。本文提出Meta-Harness，一个通过代理提议者在代码空间搜索最优harness的系统。它利用文件系统访问历史代码、得分和执行轨迹，在文本分类、数学推理和代理编码等任务中显著提升了性能并降低了成本，实现了harness工程的自动化。
selection_source: fresh_fetch
motivation: 现有的LLM应用harness主要依赖人工设计，且传统文本优化器难以有效处理复杂的代码逻辑优化。
method: 提出Meta-Harness系统，利用代理提议者通过访问历史代码、评分和执行轨迹，在外部循环中自动搜索和优化harness代码。
result: 在文本分类、数学推理和代理编码任务中，Meta-Harness均显著优于现有最先进的上下文管理系统和人工设计的基准。
conclusion: 通过赋予优化系统更丰富的历史经验访问权限，可以实现LLM应用harness工程的高效自动化。
---

## 摘要
大语言模型（LLM）系统的性能不仅取决于模型权重，还取决于其 Harness：即决定存储、检索以及向模型呈现哪些信息的代码。然而，Harness 目前仍主要由人工设计，且现有的文本优化器与该场景匹配度较差，因为它们对反馈的压缩过于激进。我们推出了 Meta-Harness，这是一个为 LLM 应用搜索 Harness 代码的外环系统。它使用一个代理式提案器（agentic proposer），通过文件系统访问所有先前候选方案的源代码、评分和执行轨迹。在在线文本分类任务中，Meta-Harness 相比最先进的上下文管理系统提升了 7.7 个百分点，同时使用的上下文 Token 减少了 4 倍。在检索增强的数学推理任务中，单个发现的 Harness 在五个留出模型上的 200 道 IMO 级别题目上的平均准确率提升了 4.7 个百分点。在代理式编程任务中，发现的 Harness 在 TerminalBench-2 上超过了最佳的人工设计基准。综上所述，这些结果表明，更丰富地利用先验经验可以实现自动化的 Harness 工程。

## Abstract
The performance of large language model (LLM) systems depends not only on model weights, but also on their harness: the code that determines what information to store, retrieve, and present to the model. Yet harnesses are still designed largely by hand, and existing text optimizers are poorly matched to this setting because they compress feedback too aggressively. We introduce Meta-Harness, an outer-loop system that searches over harness code for LLM applications. It uses an agentic proposer that accesses the source code, scores, and execution traces of all prior candidates through a filesystem. On online text classification, Meta-Harness improves over a state-of-the-art context management system by 7.7 points while using 4x fewer context tokens. On retrieval-augmented math reasoning, a single discovered harness improves accuracy on 200 IMO-level problems by 4.7 points on average across five held-out models. On agentic coding, discovered harnesses surpass the best hand-engineered baselines on TerminalBench-2. Together, these results show that richer access to prior experience can enable automated harness engineering.

---

## 论文详细总结（自动生成）

### Meta-Harness：模型 Harness 的端到端优化——论文总结

#### 1. 论文的核心问题与整体含义
*   **研究动机**：大语言模型（LLM）系统的性能不仅取决于模型权重，还高度依赖于其 **Harness（外壳/支架代码）**。Harness 负责决定如何存储、检索以及向模型呈现信息（如 Prompt 构建、检索策略、内存管理）。
*   **核心问题**：目前 Harness 主要依靠人工手动设计和迭代。现有的文本优化器（如 OPRO, TextGrad）在处理 Harness 优化时效果不佳，因为它们通常将反馈压缩为标量分数或简短摘要，丢失了诊断复杂代码逻辑失败所需的详细执行轨迹（Execution Traces）。
*   **整体含义**：本文旨在通过一种自动化的“外环”搜索系统，利用具备代码能力的 Agent 来自动编写、测试和改进 Harness 代码，实现 Harness 工程的端到端优化。

#### 2. 方法论：Meta-Harness
*   **核心思想**：将 Harness 优化视为在代码空间（Code Space）中的搜索问题。Meta-Harness 充当一个“优化 Harness 的 Harness”，它不压缩反馈，而是让优化器直接访问完整的历史经验。
*   **关键技术细节**：
    *   **代理提议者（Agentic Proposer）**：使用一个强大的编程 Agent（如 Claude Code 驱动的 Opus-4.6），它不仅能生成代码，还能调用工具（如 `grep`, `cat`）来分析历史记录。
    *   **文件系统经验库**：系统维护一个文件系统，存储所有先前候选 Harness 的源代码、评估得分和**原始执行轨迹**（包括 Prompt、模型输出、工具调用等）。
    *   **搜索循环**：
        1.  提议者阅读文件系统中的历史经验，进行因果推理。
        2.  提议者编写并提交新的 Harness 代码。
        3.  系统在搜索集上评估该代码，并将结果（代码、分数、轨迹）存回文件系统。
        4.  重复上述过程，最终输出帕累托前沿（Pareto Frontier）上的最优 Harness。
*   **算法流程**：该方法不依赖预定义的变异算子，而是依靠 Agent 的推理能力来识别失败模式并提出针对性修改（如从修改 Prompt 转向修改检索逻辑）。

#### 3. 实验设计
论文在三个具有挑战性的领域进行了评估：
*   **在线文本分类**：在 LawBench（法律）、Symptom2Disease（医疗）、USPTO-50k（化学）数据集上运行。对比了 ACE、MCE 等人工设计系统，以及 OpenEvolve、TTT-Discover 等自动优化器。
*   **检索增强数学推理**：在包含 50 万道题目的语料库中搜索检索策略，并在 200 道 IMO（国际数学奥林匹克）级别的难题上测试。对比了 BM25、稠密检索（Dense Retrieval）和无检索基准。
*   **代理式编程（Agentic Coding）**：在 TerminalBench-2 基准上进行，目标是优化在命令行环境中执行长程任务的 Agent Harness。对比了 Terminus-KIRA、Claude Code 等顶尖 Agent。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号或数量。
*   **API 使用**：实验主要依赖闭源模型的 API。提议者使用了 Claude Opus-4.6；被优化的基础模型包括 GPT-OSS-120B、GPT-5.4 系列、Gemini-3 系列和 Claude Haiku 4.5。
*   **耗时**：文中提到一个典型的搜索运行在几小时的挂墙时间（Wall-clock time）内即可完成。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 3 个截然不同的领域。
    *   在文本分类中进行了 9 个分布外（OOD）数据集的泛化性测试。
    *   在数学任务中进行了跨 5 个不同基础模型的迁移性测试。
    *   包含了消融实验（对比仅分数、分数+摘要、完整轨迹三种反馈模式）。
*   **充分性与公平性**：实验设计较为充分且客观。通过固定评估预算（如 60 次评估）来公平对比不同的优化器，并使用了留出测试集（Held-out test sets）来验证搜索结果的泛化性，避免了对搜索集的过拟合。

#### 6. 主要结论与发现
*   **性能超越人工**：Meta-Harness 发现的 Harness 在所有任务中均优于最先进的人工设计基准。例如，在文本分类中准确率提升 7.7%，且 Token 使用量减少 4 倍。
*   **反馈质量至关重要**：消融实验证明，**访问原始执行轨迹**是性能提升的关键。仅靠分数或 LLM 生成的摘要无法提供足够的诊断信息。
*   **泛化性强**：在特定模型和数据集上搜索得到的 Harness，可以直接迁移到其他更强或更弱的模型上，并保持性能增益。
*   **搜索效率高**：在文本分类中，Meta-Harness 仅需 4 次评估即可达到其他优化器 40 次评估的效果。

#### 7. 优点
*   **端到端自动化**：将原本高度依赖经验的 Harness 工程转化为可自动搜索的优化问题。
*   **非压缩反馈机制**：通过文件系统让 Agent 具备“长程记忆”和“深度诊断”能力，能够进行复杂的因果推理。
*   **代码空间搜索**：相比于仅优化 Prompt 文本，直接优化 Python 代码允许更复杂的逻辑变更（如条件分支、多步检索）。
*   **结果可解释**：生成的 Harness 是可读的 Python 代码，便于人类检查是否存在硬编码或过拟合。

#### 8. 不足与局限
*   **对提议者能力依赖严重**：该方法的效果高度依赖于底层编程 Agent（如 Opus-4.6）的推理能力。如果 Agent 编程能力不足，搜索效率会大幅下降。
*   **评估成本**：虽然搜索次数不多，但每次评估可能涉及在整个数据集上运行 Agent，对于大规模任务，API 调用成本可能较高。
*   **搜索空间限制**：目前的实验主要集中在单文件 Python 程序的优化，对于涉及复杂多模块架构的超大规模系统，搜索难度会呈指数级增加。
*   **潜在的过拟合风险**：虽然论文做了 OOD 测试，但在极小规模的搜索集上仍可能出现针对特定任务模式的“代码级过拟合”。

（完）
