---
title: "RAGent: Physics-Aware Agentic Reasoning for Training-Free mmWave Human Activity Recognition"
title_zh: RAGent：用于免训练毫米波人体活动识别的物理感知智能体推理
authors: "Mingda Han, Huanqi Yang, Zehua Sun, Wenhao Li, Yanni Yang, Guoming Zhang, Yetong Cao, Weitao Xu, Pengfei Hu"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27571v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 基于可重用雷达知识的证据落地推理
tldr: "针对毫米波雷达人体活动识别中数据标注成本高和跨域迁移难的问题，本文提出了RAGent框架。该框架通过视觉语言模型离线构建可复用的雷达知识库，无需人工标注。在部署阶段，它利用物理感知的运动空间检索和多角色智能体推理实现免训练识别。实验表明，RAGent在无需针对特定领域重训的情况下，实现了93.39%的识别准确率，具有极强的泛化能力。"
selection_source: fresh_fetch
motivation: 传统的毫米波雷达识别方法依赖大量人工标注且难以适应新环境，导致部署成本高昂且缺乏通用性。
method: 结合视觉语言模型跨模态监督构建知识库，并利用物理感知的运动空间检索与多角色智能体推理进行免训练识别。
result: "在自建数据集上达到了93.39%的准确率，且在无需目标域适配的情况下展现出卓越的跨域泛化性能。"
conclusion: RAGent证明了通过物理感知的智能体推理和知识检索，可以实现高效、免训练且具备强鲁棒性的毫米波雷达活动识别。
---

## 摘要
毫米波（mmWave）雷达能够实现保护隐私的人体活动识别（HAR），但由于标注成本高昂以及在领域偏移下迁移性差，其实际部署仍受到阻碍。尽管先前的研究部分缓解了这些挑战，但大多数方法在每个新的部署环境下仍需要重新训练或适配。这使得毫米波 HAR 陷入“收集-微调-重新部署”的重复循环中，导致大规模实际部署变得困难。在本文中，我们提出了 RAGent，这是一个用于毫米波 HAR 的部署时免训练框架，它将识别重新定义为基于可重用雷达知识的有据推理，而非针对特定部署的模型优化。在离线阶段，RAGent 通过受约束的跨模态监督构建可重用的雷达知识库，其中视觉语言模型（VLM）在无需人工雷达标注的情况下，将活动语义从同步视频迁移到配对的雷达片段中。在部署时，RAGent 仅通过雷达识别活动，方法是在显式运动学空间中检索物理上可比的先例，并通过结构化的多角色推理确定最终标签。该推理协议通过零梯度自我进化在离线阶段进一步优化。在自收集数据集上的广泛实验表明，RAGent 在无需针对每个领域重新训练或目标域适配的情况下，实现了 93.39% 的准确率，同时在跨领域场景下表现出鲁棒的泛化能力。

## Abstract
Millimeter-wave (mmWave) radar enables privacy-preserving human activity recognition (HAR), yet real-world deployment remains hindered by costly annotation and poor transferability under domain shift. Although prior efforts partially alleviate these challenges, most still require retraining or adaptation for each new deployment setting. This keeps mmWave HAR in a repeated collect-tune-redeploy cycle, making scalable real-world deployment difficult. In this paper, we present RAGent, a deployment-time training-free framework for mmWave HAR that reformulates recognition as evidence-grounded inference over reusable radar knowledge rather than deployment-specific model optimization. Offline, RAGent constructs a reusable radar knowledge base through constrained cross-modal supervision, where a Vision-Language Model (VLM) transfers activity semantics from synchronized videos to paired radar segments without manual radar annotation. At deployment time, RAGent recognizes activities from radar alone by retrieving physically comparable precedents in an explicit kinematic space and resolving the final label through structured multi-role reasoning. The reasoning protocol is further refined offline through zero-gradient self-evolution. Extensive experiments on a self-collected dataset show that RAGent achieves 93.39% accuracy without per-domain retraining or target-domain adaptation, while generalizing robustly across domains.

---

## 论文详细总结（自动生成）

以下是对论文《RAGent: Physics-Aware Agentic Reasoning for Training-Free mmWave Human Activity Recognition》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：毫米波（mmWave）雷达人体活动识别（HAR）在实际部署中面临两大瓶颈：
    1.  **标注成本高**：雷达谱图非直观，人工标注需要专家经验且耗时耗力。
    2.  **跨域迁移难**：环境变化或个体差异会导致信号分布偏移，传统模型需要针对新场景进行“采集-标注-重训-验证”的繁琐循环。
*   **整体含义**：论文提出了 **RAGent** 框架，旨在实现**部署时免训练（Training-Free）**的识别。它将识别任务从“特定领域的模型优化”转变为“基于可重用知识的证据推理”，通过结合物理规律和大型语言模型（LLM）的推理能力，打破了毫米波识别对目标域数据的依赖。

### 2. 方法论：核心思想与技术细节
RAGent 采用“离线构建知识库 + 在线证据推理”的架构：
*   **离线知识库构建（无需人工雷达标注）**：
    *   **跨模态监督**：利用同步的视频和雷达数据，通过视觉语言模型（VLM）自动提取视频语义并迁移至雷达片段。
    *   **双通道校验**：视频通道生成伪标签，雷达通道进行语义一致性检查，确保进入知识库的样本标签准确。
*   **物理感知数值检索**：
    *   **特征提取**：从多普勒-时间图（DTM）和距离-时间图（RTM）中提取25维显式物理特征（如带宽、步频、位移、轨迹线性度等）。
    *   **ANOVA 子空间优化**：利用方差分析（ANOVA）筛选最具辨识度的特征子空间，提高检索的鲁棒性。
*   **多角色智能体推理（Council of Experts）**：
    *   **历史学家（Historian）**：总结检索到的相似先例，提供类别先验。
    *   **物理学家（Physicist）**：基于物理约束（如位移量、运动强度）对候选标签进行“一票否决”。
    *   **观察员（Observer）**：直接观察查询样本的谱图语义特征。
    *   **法官（Judge）**：综合各方证据，在物理一致性的前提下做出最终裁决。
*   **零梯度自我进化**：在离线阶段，通过 LLM 自动分析识别失败的案例，迭代优化推理协议（Prompt），无需更新模型参数。

### 3. 实验设计
*   **数据集与场景**：自建数据集，包含 8 名参与者、3 种典型室内环境（走廊、会议室、卧室）、12 种人体活动（跳跃、拳击、踢腿、深蹲起、行走等），共计 2,568 个雷达片段。
*   **对比方法（Benchmarks）**：
    *   **推理类**：仅检索（Retrieval-Only）、LLM 直接识别（LLM-Direct）。
    *   **监督学习类**：CNN、ViT、CNN+BiLSTM。
    *   **适配类（参考实现）**：mmCLIP-light、SynMotion-light、EI-adapt。

### 4. 资源与算力
*   **硬件设备**：使用 TI AWR1843BOOST 毫米波雷达和 DCA1000EVM 数据采集卡；视频采集使用海康威视 DS-E12 摄像头。
*   **算力说明**：论文**未明确说明**具体的 GPU 训练时长（因为核心推理过程是免训练的）。
*   **推理开销**：详细记录了推理成本。单次推理平均耗时 **7.243 秒**（主要消耗在 LLM/VLM 调用上），每 1000 次样本推理成本约为 **0.838 美元**。

### 5. 实验数量与充分性
*   **实验规模**：进行了端到端准确率测试、混淆矩阵分析、消融实验（针对四个核心角色）、微基准测试（知识库大小、类别数量、检索深度 $M$、特征维度 $K$）。
*   **泛化性验证**：专门设计了**跨环境（Unseen Env）**、**跨受试者（Unseen Subj）**和**跨日期（长达30天）**的实验。
*   **充分性评价**：实验设计较为全面，涵盖了从系统性能到鲁棒性的多个维度，对比了多种主流技术路线，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **高准确率**：在完全免训练的情况下，RAGent 达到了 **93.39%** 的准确率，接近最强的监督学习模型（ViT, 94.12%）。
*   **强泛化性**：在未见过的环境和受试者中，准确率保持在 91% 以上，显著优于需要适配的传统方法。
*   **物理约束的重要性**：消融实验显示，移除“物理学家”角色会导致准确率下降约 10%，证明了物理规律在纠正 LLM 语义幻觉中的关键作用。
*   **检索驱动**：检索到的先例是推理的基础，移除“历史学家”会导致性能大幅下滑（下降约 20%）。

### 7. 优点（亮点）
*   **范式创新**：将 HAR 从“黑盒分类”转向“证据驱动的智能体推理”，实现了真正的部署时免训练。
*   **物理与语义结合**：巧妙地利用显式物理特征（Kinematic Descriptors）来约束 LLM 的推理，增强了决策的可解释性和可靠性。
*   **标注自动化**：通过 VLM 跨模态迁移解决了雷达数据标注难的问题，降低了系统构建门槛。

### 8. 不足与局限
*   **推理延迟高**：单次识别需要 7 秒以上，无法满足实时性要求极高的场景（如跌倒瞬间触发报警）。
*   **成本问题**：依赖商业 LLM API，大规模部署可能产生持续的调用费用。
*   **场景限制**：目前仅针对单人、粗粒度的全身活动，尚未验证在多人复杂场景或极细微动作识别中的表现。
*   **硬件依赖**：物理特征的提取与特定雷达参数相关，跨硬件平台的通用性有待进一步验证。

（完）
