---
title: "Codebase-Memory: Tree-Sitter-Based Knowledge Graphs for LLM Code Exploration via MCP"
title_zh: Codebase-Memory：通过 MCP 为大语言模型代码探索构建基于 Tree-Sitter 的知识图谱
authors: "Martin Vogel, Falk Meyer-Eschenbach, Severin Kohler, Elias Grünewald, Felix Balzer"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27277v1"
tags: ["query:agent"]
score: 9.0
evidence: 用于LLM代码探索智能体的持久化知识图谱
tldr: 针对LLM代码智能体在探索代码库时存在的高Token消耗和缺乏结构化理解的问题，本文提出了Codebase-Memory。这是一个基于Model Context Protocol (MCP) 的开源系统，利用Tree-Sitter构建持久化知识图谱。它支持66种语言，通过多阶段流水线实现调用图遍历和影响分析。实验表明，该系统在保证较高回答质量的同时，显著降低了Token消耗和工具调用次数，在图原生查询任务中表现优异。
selection_source: fresh_fetch
motivation: 传统的LLM代码探索依赖重复的文件读取和搜索，导致Token消耗巨大且缺乏对代码结构的深度理解。
method: 开发了一个基于Tree-Sitter和MCP的系统，通过并行流水线构建包含调用图和社区发现功能的跨语言知识图谱。
result: "在31个真实仓库中，该系统以十分之一的Token消耗和更少的工具调用实现了83%的回答质量，并在图原生查询中表现出色。"
conclusion: Codebase-Memory证明了利用结构化知识图谱可以显著提升LLM代码探索的效率，是处理大规模代码库的有效方案。
---

## 摘要
大语言模型（LLM）编程智能体通常通过重复的文件读取和 grep 搜索来探索代码库，每次查询消耗数千个 token，且缺乏结构化理解。我们提出了 Codebase-Memory，这是一个开源系统，通过模型上下文协议（MCP）构建持久的、基于 Tree-Sitter 的知识图谱。该系统通过包含并行工作池、调用图遍历、影响分析和社区发现的多阶段流水线解析 66 种语言。在 31 个真实代码库的评估中，Codebase-Memory 实现了 83% 的回答质量（相比之下，文件探索智能体为 92%），而 token 消耗减少了 10 倍，工具调用次数减少了 2.1 倍。对于中心节点检测和调用者排名等图原生查询，它在 31 种语言中的 19 种上达到或超过了探索智能体的表现。

## Abstract
Large Language Model (LLM) coding agents typically explore codebases through repeated file-reading and grep-searching, consuming thousands of tokens per query without structural understanding. We present Codebase-Memory, an open-source system that constructs a persistent, Tree-Sitter-based knowledge graph via the Model Context Protocol (MCP), parsing 66 languages through a multi-phase pipeline with parallel worker pools, call-graph traversal, impact analysis, and community discovery. Evaluated across 31 real-world repositories, Codebase-Memory achieves 83% answer quality versus 92% for a file-exploration agent, at ten times fewer tokens and 2.1 times fewer tool calls. For graph-native queries such as hub detection and caller ranking, it matches or exceeds the explorer on 19 of 31 languages.

---

## 论文详细总结（自动生成）

### Codebase-Memory 论文结构化深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
当前 LLM 编程智能体（如 Claude Code、Cursor、Aider）在处理大规模代码库时，主要依赖**基于文本的探索方式**（如重复读取文件、grep 搜索）。这种方式存在三个核心痛点：
*   **Token 消耗巨大**：每次查询可能消耗数万 Token，且随着代码库规模线性增长。
*   **缺乏结构化理解**：LLM 难以直接感知函数调用图、依赖链和模块边界，必须通过多次工具调用手动构建上下文。
*   **效率低下**：对于“修改此函数会破坏什么？”等结构化问题，文本搜索难以捕捉传递关系。
**Codebase-Memory** 旨在通过构建一个持久化的、基于知识图谱的检索层，将代码结构作为“一等公民”暴露给 LLM，从而提升效率并降低成本。

#### 2. 论文提出的方法论
该系统采用 **Tree-Sitter** 作为解析引擎，通过 **Model Context Protocol (MCP)** 协议为 LLM 提供 14 种结构化查询工具。
*   **核心思想**：将代码库抽象为存储在 SQLite 中的属性图（Property Graph），包含节点（函数、类、路由等）和边（调用、实现、导入等）。
*   **关键技术细节**：
    *   **多阶段流水线**：分为解析（Parse）、构建（Build）和服务（Serve）三个阶段。支持并行提取、6 策略调用解析（从精确导入匹配到模糊匹配）以及 LSP 风格的类型解析（针对 Go/C/C++）。
    *   **社区发现算法**：利用 **Louvain 模块化优化算法** 对调用图进行分区，自动识别功能社区，辅助架构分析。
    *   **增量同步**：使用 **XXH3 哈希** 监控文件变化，仅对受影响文件进行增量索引，确保图谱实时性。
    *   **安全硬化**：设计了 8 层 CI 审计套件，包括二进制字符串审计、网络出口监控及多引擎病毒扫描（VirusTotal），应对 MCP 服务的供应链安全风险。

#### 3. 实验设计
*   **数据集/场景**：涵盖 **31 种编程语言** 的真实开源仓库，规模从小型项目到 **Linux 内核**（210 万节点）。
*   **Benchmark**：设计了 12 类标准化问题，包括中心节点检测、调用者排名、依赖清单和全调用链追踪。
*   **对比方法**：
    *   **MCP Agent**：使用 Codebase-Memory 提供的 14 种工具。
    *   **Explorer Agent**：传统的基于文件读取和 grep 搜索的智能体。
    *   **后端模型**：统一使用 Claude 3.5 Opus。

#### 4. 资源与算力
*   **硬件环境**：实验主要在 **Apple M3 Pro (macOS)** 上运行。
*   **性能表现**：
    *   **索引速度**：Django 仓库（4.9 万节点）约 6 秒；Linux 内核（210 万节点）约 3 分钟。
    *   **查询延迟**：图遍历和调用路径追踪延迟均 **< 1ms**。
*   **训练算力**：该系统属于静态分析工具，不涉及大规模模型训练，因此未报告 GPU 训练时长。

#### 5. 实验数量与充分性
*   **实验规模**：在 31 个不同语言的仓库上进行了头对头对比，每种语言均覆盖了 12 类典型查询。
*   **充分性评价**：实验覆盖了极广的语言范围（66 种支持中的 31 种），并对比了 Token 消耗、工具调用次数和回答质量等关键维度。
*   **客观性风险**：回答质量由论文第一作者根据参考答案手动评分（0-1 刻度），虽然设定了 PASS/FAIL 阈值，但仍存在一定的主观性偏差。

#### 6. 论文的主要结论与发现
*   **极高的 Token 效率**：MCP Agent 的 Token 消耗比传统探索方式**降低了 10 倍**，工具调用次数减少了 **2.1 倍**。
*   **质量竞争力**：在回答质量上，MCP Agent 达到了 Explorer Agent 的 **90%**（83% vs 92%）。
*   **图原生优势**：在中心节点检测、调用者排名等任务中，MCP Agent 在 19/31 种语言中表现优于或持平于传统方式。
*   **可扩展性**：系统以单二进制文件形式运行，能够处理数百万节点的大型代码库。

#### 7. 优点
*   **零依赖部署**：采用静态链接的 C 语言编写，集成 SQLite，无需复杂的数据库基础设施。
*   **跨语言能力强**：原生支持 66 种语言，远超同类研究。
*   **标准化接口**：通过 MCP 协议实现，可无缝接入 Claude Code、Zed 等主流 AI 开发工具。
*   **安全意识高**：在 MCP 领域率先提出了详尽的二进制验证和供应链安全流程。

#### 8. 不足与局限
*   **抽象损失**：由于图谱只存储结构而不存储完整源码行，在需要详尽代码上下文的任务中表现不如文件读取。
*   **宏处理缺陷**：在 C 语言等宏重度使用的场景下表现较差（质量仅 0.58），因为 Tree-Sitter AST 难以处理预处理指令。
*   **静态分析限制**：无法处理运行时行为、反射或复杂的动态分发。
*   **评估偏差**：缺乏自动化的 Benchmark 评分机制（如 SWE-bench 的自动运行测试），依赖人工评分。

（完）
