---
title: "GraphWalker: Agentic Knowledge Graph Question Answering via Synthetic Trajectory Curriculum"
title_zh: GraphWalker：通过合成轨迹课程实现的智能体知识图谱问答
authors: "Shuwen Xu, Yao Xu, Jiaxiang Liu, Chenhao Yuan, Wenshuo Peng, Jun Zhao, Kang Liu"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28533v1"
tags: ["query:mmkqa"]
score: 10.0
evidence: 智能体知识图谱问答
tldr: 针对知识图谱问答（KGQA）中训练数据稀缺和推理泛化性差的问题，本文提出 GraphWalker 框架。该框架通过自动化轨迹合成和阶段式微调（SFT），先利用随机游走路径建立探索先验，再通过专家轨迹强化反思与纠错能力。实验证明其在 CWQ 和 WebQSP 等数据集上达到 SOTA，并显著提升了对分布外推理路径的泛化性能。
selection_source: fresh_fetch
motivation: 现有的智能体 KGQA 方法受限于缺乏自主导航训练或推理路径过于固定，导致在复杂图结构上的泛化能力不足。
method: 提出一种两阶段微调范式，结合基于约束随机游走的自动化轨迹合成与专家轨迹微调，以增强智能体的探索与纠错能力。
result: GraphWalker 在 CWQ 和 WebQSP 数据集上取得了最先进的性能，并在 GrailQA 等测试中展现出卓越的分布外泛化能力。
conclusion: 通过合成轨迹课程学习，GraphWalker 有效解决了 KGQA 智能体训练数据匮乏的问题，并为轻量级强化学习阶段奠定了高性能基础。
---

## 摘要
智能体知识图谱问答（KGQA）要求智能体与知识图谱（KGs）进行迭代交互，这在训练数据稀缺性和推理泛化性方面都带来了挑战。具体而言，现有方法通常限制了智能体的探索：基于提示（prompting）的方法缺乏自主导航训练，而目前的训练流程通常将推理局限于预定义的轨迹。为此，本文提出了 GraphWalker，这是一个新颖的智能体 KGQA 框架，通过自动化轨迹合成（Automated Trajectory Synthesis）和分阶段微调（Stage-wise Fine-tuning）来解决这些挑战。GraphWalker 采用两阶段有监督微调（SFT）训练范式：首先，在从受限随机游走路径合成的结构多样化轨迹上训练智能体，从而在知识图谱上建立广泛的探索先验；其次，在少量专家轨迹上进一步微调智能体，以培养其反思和错误恢复能力。大量实验表明，我们的分阶段 SFT 范式为轻量级强化学习（RL）阶段解锁了更高的性能上限，使 GraphWalker 在 CWQ 和 WebQSP 数据集上达到了最先进的性能。在 GrailQA 以及我们构建的 GraphWalkerBench 上的额外结果证实，GraphWalker 增强了对分布外（out-of-distribution）推理路径的泛化能力。代码已在 https://github.com/XuShuwenn/GraphWalker 公开。

## Abstract
Agentic knowledge graph question answering (KGQA) requires an agent to iteratively interact with knowledge graphs (KGs), posing challenges in both training data scarcity and reasoning generalization. Specifically, existing approaches often restrict agent exploration: prompting-based methods lack autonomous navigation training, while current training pipelines usually confine reasoning to predefined trajectories. To this end, this paper proposes \textit{GraphWalker}, a novel agentic KGQA framework that addresses these challenges through \textit{Automated Trajectory Synthesis} and \textit{Stage-wise Fine-tuning}. GraphWalker adopts a two-stage SFT training paradigm: First, the agent is trained on structurally diverse trajectories synthesized from constrained random-walk paths, establishing a broad exploration prior over the KG; Second, the agent is further fine-tuned on a small set of expert trajectories to develop reflection and error recovery capabilities. Extensive experiments demonstrate that our stage-wise SFT paradigm unlocks a higher performance ceiling for a lightweight reinforcement learning (RL) stage, enabling GraphWalker to achieve state-of-the-art performance on CWQ and WebQSP. Additional results on GrailQA and our constructed GraphWalkerBench confirm that GraphWalker enhances generalization to out-of-distribution reasoning paths. The code is publicly available at https://github.com/XuShuwenn/GraphWalker

---

## 论文详细总结（自动生成）

### 论文总结：GraphWalker - 通过合成轨迹课程实现的智能体知识图谱问答

#### 1. 核心问题与研究动机
*   **核心问题**：现有的智能体知识图谱问答（KGQA）面临**训练数据稀缺**和**推理泛化性差**的双重挑战。
*   **研究动机**：
    *   **传统方法局限**：信息检索（IR）和语义解析（SP）方法通常局限于预定义的子图或静态逻辑形式，难以应对大规模、多噪声的真实知识图谱。
    *   **现有智能体不足**：基于提示（Prompting）的方法缺乏针对性训练；而现有的训练方法（如 RoG）往往限制了智能体的自主交互能力，或仅在预抽取的子图上运行，无法处理全局图结构。
    *   **强化学习瓶颈**：缺乏强大的 SFT（有监督微调）先验，导致强化学习（RL）难以在复杂的图空间中发现有效路径。

#### 2. 方法论
GraphWalker 提出了一个**两阶段 SFT + 轻量级 RL** 的训练范式，核心在于构建高质量的合成轨迹课程：
*   **数据构建流水线**：
    *   **GraphSynth-15k**：通过**受限随机游走（CRW）**在全局 KG 上生成多样化的推理路径，利用 LLM 合成自然语言问题，并经过语义过滤和环境反馈模拟，生成包含“思考-行动-观察”的 1.5 万条探索轨迹。
    *   **GraphRoll-6k**：利用专家模型（如 GPT-4o-mini）在真实数据集上生成 6000 条高质量专家轨迹，通过拒绝采样确保答案准确且证据充分，重点培养智能体的**反思与错误恢复**能力。
*   **训练阶段**：
    *   **阶段 1 (SFT)**：在 GraphSynth 上训练，建立广泛的图探索先验。
    *   **阶段 2 (SFT)**：在 GraphRoll 上微调，强化自我修正和复杂逻辑处理。
    *   **阶段 3 (RL)**：采用 **GRPO（群体相对策略优化）**算法，利用稀疏的精确匹配（EM）奖励进一步提升性能上限。

#### 3. 实验设计
*   **数据集**：
    *   **标准评测**：CWQ (Complex WebQuestions) 和 WebQSP。
    *   **零样本泛化**：GrailQA（零样本切片）。
    *   **结构泛化**：自建 **GraphWalkerBench**，包含训练集中未见的推理结构（如 IP、PI 结构）。
*   **基准方法（Benchmarks）**：
    *   **提示类**：ToG, GoG, ToG-2.0。
    *   **训练类**：RoG, KG-Agent, KBQA-o1, KG-R1。
    *   **闭源模型直接对比**：GPT-4o-mini, DeepSeek-V3.2。
*   **设置**：所有实验均在**全局 KG（Global KG）**环境下进行，而非预先抽取的子图，增加了任务难度。

#### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA A100 (80GB) GPU**。
*   **软件框架**：基于 LlamaFactory 进行微调，使用 slime 框架进行 RL 训练。
*   **训练细节**：
    *   SFT 阶段：学习率 1e-5，Batch Size 32，训练 2 个 Epoch。
    *   RL 阶段：学习率 1e-6，训练 100 个 Step，Global Batch Size 为 512。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的实验，包括主实验对比、跨任务迁移实验、零样本泛化实验。
*   **消融实验**：针对 SFT 阶段 1、阶段 2、RL 阶段、数据规模、质量过滤、关系重排序等多个变量进行了详尽的消融分析。
*   **客观性**：通过 Pass@k 分析展示了探索空间的扩大，并使用了多个不同规模的开源底座（Qwen2.5-3B/7B, Llama-3.1-8B）验证了方法的普适性。实验设计严谨，对比基准涵盖了当前最先进的开源和闭源方案。

#### 6. 主要结论与发现
*   **性能领先**：GraphWalker 在 CWQ 和 WebQSP 上均取得了 SOTA 结果，显著超过了包括 GPT-4 驱动的 GoG 在内的现有方法。
*   **泛化能力强**：即使仅在 CWQ 上训练，模型在 WebQSP 和 GrailQA 上的零样本表现依然优异，证明了合成轨迹课程对提升结构泛化性的有效性。
*   **课程学习的必要性**：实验证明“先学探索（Stage 1），后学反思（Stage 2）”的顺序至关重要，混合训练的效果明显弱于阶段式训练。
*   **RL 的催化作用**：强大的 SFT 先验是 RL 成功的关键，RL 能够进一步挖掘 SFT 模型的潜力，提升推理效率。

#### 7. 优点
*   **创新的数据合成方案**：通过受限随机游走解决了 KGQA 领域专家标注数据昂贵且覆盖面窄的问题。
*   **全局图交互**：不依赖子图抽取，使智能体具备在真实大规模 KG 环境中生存和导航的能力。
*   **反思机制**：通过 GraphRoll 数据集显式引入了错误恢复逻辑，增强了智能体在面对噪声或死胡同时的鲁棒性。

#### 8. 不足与局限
*   **对专家模型的依赖**：数据合成和轨迹生成高度依赖于高性能 LLM（如 GPT-4o-mini），这可能引入专家模型的偏见。
*   **KG 模式限制**：虽然在 Freebase 上表现优异，但对于模式（Schema）完全不同的其他知识图谱，可能需要重新生成合成数据。
*   **计算成本**：虽然 RL 阶段是轻量级的，但两阶段 SFT 加上数据合成的整体流水线对算力和 API 调用仍有一定要求。

（完）
