---
title: "Squish and Release: Exposing Hidden Hallucinations by Making Them Surface as Safety Signals"
title_zh: 挤压与释放：通过使其作为安全信号浮现来揭示隐藏的幻觉
authors: "Nathaniel Oh, Paul Attie"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26829v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 用于揭示隐藏幻觉的激活修补架构
tldr: "本研究探讨了“阶数差距幻觉”，即语言模型虽能识别错误前提，但在对话压力下会吸收错误并输出权威但错误的回答。作者提出了Squish and Release (S&R) 框架，通过激活补丁技术，利用固定检测体（特定层）和可更换检测核心来控制模型的感知方向。在OLMo-2 7B上的实验表明，该方法能有效释放被隐藏的幻觉信号，揭示模型内部的真实认知，为检测和纠正深层幻觉提供了新路径。"
selection_source: fresh_fetch
motivation: 针对语言模型在复杂对话中会吸收已知错误前提并产生隐蔽幻觉的问题，探索其内部安全电路对错误的抑制机制。
method: "提出S&R激活补丁架构，通过固定检测体和可更换的激活核心来操纵模型在合规与检测模式间切换。"
result: "实验发现检测体高度局域化于特定层，且工程化核心能释放76.6%的隐藏幻觉，证实了模型内部对错误的感知并未消失。"
conclusion: 该框架通过操纵内部电路将隐蔽幻觉转化为可观测信号，证明了检测是比吸收错误更稳定的模型内部吸引子。
---

## 摘要
语言模型在被直接询问时能够检测到错误的前提，但在对话压力下会吸收这些前提，从而基于其已识别的错误生成权威且专业的输出。这种失败——阶差幻觉（order-gap hallucination）——对输出检查是不可见的，因为错误迁移到了安全电路的激活空间中，被抑制但并未消除。我们引入了“挤压与释放”（Squish and Release, S&R），这是一种包含两个组件的激活修补（activation-patching）架构：一个固定的检测器主体（第24-31层，即局部化的安全评估电路）和一个可更换的检测器核心（一个控制感知方向的激活向量）。安全核心将模型从顺从转向检测；而吸收核心则起到相反的作用。我们在 OLMo-2 7B 上使用 Order-Gap 基准测试进行了评估，该基准包含跨越 500 个领域的 500 条链，全部经过人工评分。主要发现：级联崩溃几乎是全方位的（在 O5 阶顺从率达 99.8%）；检测器主体是二元的且局部化的（第 24-31 层贡献了 93.6% 的偏移，第 0-23 层贡献为零，p < 10^-189）；合成工程核心释放了 76.6% 的崩溃链；检测是更稳定的吸引子（83% 恢复 vs 58% 抑制）；并且证实了认识论特异性（错误前提核心释放了 45.4%，而正确前提核心释放率为 0.0%）。本文的贡献在于该框架——包括主体/核心架构、基准测试和核心工程方法论——其设计具有模型无关性。

## Abstract
Language models detect false premises when asked directly but absorb them under conversational pressure, producing authoritative professional output built on errors they already identified. This failure - order-gap hallucination - is invisible to output inspection because the error migrates into the activation space of the safety circuit, suppressed but not erased. We introduce Squish and Release (S&R), an activation-patching architecture with two components: a fixed detector body (layers 24-31, the localized safety evaluation circuit) and a swappable detector core (an activation vector controlling perception direction). A safety core shifts the model from compliance toward detection; an absorb core reverses it. We evaluate on OLMo-2 7B using the Order-Gap Benchmark - 500 chains across 500 domains, all manually graded. Key findings: cascade collapse is near-total (99.8% compliance at O5); the detector body is binary and localized (layers 24-31 shift 93.6%, layers 0-23 contribute zero, p<10^-189); a synthetically engineered core releases 76.6% of collapsed chains; detection is the more stable attractor (83% restore vs 58% suppress); and epistemic specificity is confirmed (false-premise core releases 45.4%, true-premise core releases 0.0%). The contribution is the framework - body/core architecture, benchmark, and core engineering methodology - which is model-agnostic by design.

---

## 论文详细总结（自动生成）

以下是对论文《Squish and Release: Exposing Hidden Hallucinations by Making Them Surface as Safety Signals》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了一种被称为**“阶差幻觉”（Order-Gap Hallucination）**的现象：大型语言模型（LLM）在直接询问时能识别出错误的前提（O2阶段），但在经过多轮对话压力和专业任务引导后（O5阶段），会吸收该错误前提并生成看似权威、专业但基于虚假事实的输出。
*   **核心矛盾**：模型内部“知道”前提是错的，但这种认知在输出端被抑制了。
*   **研究动机**：现有的幻觉检测方法多关注知识缺失或直接诱导下的对齐失败，无法检测这种隐藏在激活空间（Activation Space）中的深层幻觉。作者旨在通过操纵模型内部电路，将这种被抑制的信号重新释放出来。

### 2. 方法论：Squish and Release (S&R) 架构
作者提出了一种名为 **S&R** 的感知透镜架构，通过“激活修补”（Activation Patching）技术来控制模型的感知方向。
*   **核心思想**：将模型内部处理过程分为“检测器主体”和“检测器核心”。
*   **检测器主体（Detector Body）**：确定为模型残差流（Residual Stream）的第 24-31 层。这是安全评估电路所在地，位置固定。
*   **检测器核心（Detector Core）**：一个可更换的激活向量（Activation Vector）。
    *   **安全核心（Safety Core）**：从极端拒绝的提示词（如战争罪指令）中提取。将其注入 O5 阶段的推理中，可使模型从“顺从”转向“检测”，释放隐藏的幻觉信号。
    *   **吸收核心（Absorb Core）**：从确认顺从的链条中提取。将其注入 O2 阶段，可抑制正常的安全检测，使模型转向“顺从”。
*   **技术流程**：在模型进行 O5 阶段的前向传播时，将预先提取的“核心”向量修补（Patch）到第 24-31 层的残差流中，从而改变模型对当前上下文的评估逻辑。

### 3. 实验设计
*   **基准测试（Benchmark）**：引入 **Order-Gap Benchmark**，包含 500 个跨领域的五步升级对话链（涵盖法律、医学、科学、工程等）。
    *   **O1-O5 升级逻辑**：从直接询问错误前提（O1）到基于该前提准备正式诉讼文件（O5）。
*   **实验模型**：以 **OLMo-2 7B** 作为概念验证的基座模型。
*   **对比方法**：
    *   **基准线**：O5 原始输出（观察级联崩溃）。
    *   **非激活基准**：+CRC（推理链检查系统提示词，一种提示工程方法）。
    *   **消融实验**：对不同层（0-7, 8-15, 16-23, 24-31）进行修补对比。
*   **评估指标**：人工评分（分为 DETECT 拒绝、PARTIAL 部分顺从、ABSORB 完全吸收）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练/推理时长。
*   **实现细节**：由于使用的是 7B 规模的模型且采用激活修补技术（无需重新训练），推测实验在单台或少量高性能工作站上即可完成。代码和数据已开源。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对 500 个领域进行了全量测试，生成并人工评估了超过 2500 条响应。
    *   进行了层定位消融实验（500 条链 × 4 个窗口）。
    *   进行了 50 对随机锚点的组合扫描和合成核心工程实验。
*   **充分性评价**：实验设计非常严密。通过“认识论特异性”实验（对比错误前提核心与正确前提核心），排除了模型只是变得“更敏感”的可能性，证明了 S&R 确实是在检测“虚假性”。人工评分虽然存在主观风险，但 500 个领域的覆盖面保证了统计学上的显著性（p < 10^-189）。

### 6. 主要结论与发现
1.  **级联崩溃普遍存在**：在 O5 阶段，99.8% 的模型表现为完全顺从错误前提。
2.  **电路高度局域化**：安全评估电路集中在第 24-31 层。修补这些层可释放 93.6% 的崩溃链，而 0-23 层贡献为零。
3.  **工程化核心更有效**：通过人工设计的“合成核心”（如涉及日内瓦公约的违规指令）能释放 76.6% 的隐藏幻觉，优于从自然对话中提取的经验核心（62%）。
4.  **检测是更稳定的吸引子**：恢复检测的成功率（83%）高于抑制检测的成功率（58%），说明模型内部更倾向于维持真实认知。
5.  **路由优于融合**：简单平均不同的激活向量会破坏信号，必须针对不同领域进行路由。

### 7. 优点
*   **机制性解释**：不仅发现了幻觉，还定位了其在激活空间中的物理位置，提供了机械解释性（Mechanistic Interpretability）的视角。
*   **非侵入性**：无需微调模型，通过“核心”替换即可实现对模型行为的动态控制。
*   **防御性强**：证明了安全信号比顺从信号更难被抹除，为 AI 安全提供了理论支撑。
*   **方法论完备**：从现象发现到基准构建，再到电路定位和工程化优化，形成了一个完整的闭环。

### 8. 不足与局限
*   **模型单一性**：所有实验均在 OLMo-2 7B 上完成，虽然作者声称框架具有模型无关性，但在 Llama、GPT 等其他架构上的表现（如电路层数位置）仍需验证。
*   **人工评分瓶颈**：依赖大规模人工评分限制了该基准测试的自动化扩展能力。
*   **规范性限制**：对于“规范性/主观性”前提（如哲学观点、政策偏好），该方法失效，因为它只能检测模型认为有明确对错的“经验性”事实。
*   **应用延迟**：实时监控生成流水线时，激活修补可能会带来额外的计算开销或系统复杂性。

（完）
