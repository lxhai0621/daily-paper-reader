---
title: "Agentic Diagrammatica: Towards Autonomous Symbolic Computation in High Energy Physics"
title_zh: Agentic Diagrammatica：迈向高能物理中的自主符号计算
authors: "Tony Menzo, Alexander Roman, George T. Fleming, Sergei Gleyzer, Konstantin T. Matchev, Stephen Mrenna"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26990v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: LLM智能体规划并执行多步理论计算
tldr: 本研究推出了 Diagrammatica，这是 HEPTAPOD 智能体框架的符号计算扩展，旨在让大语言模型（LLM）能够自主规划并执行高能物理中的多步理论计算。针对 LLM 在符号计算中面临的数学约定一致性挑战，该工具通过工具约束计算和知识接地，利用受信任的后端执行精确操作。它提供量纲分析（NDA）和精确图分析（EDA）两条路径，并在衰变宽度目录和μ子衰变多重性研究中验证了其有效性。
selection_source: fresh_fetch
motivation: 解决大语言模型在处理高能物理符号计算时，因隐含数学约定难以验证而导致的可靠性不足问题。
method: 开发了基于工具约束的架构，通过紧凑的图规范引导智能体调用受信任后端进行 NDA 估算或基于 FeynCalc 的 EDA 精确计算。
result: 成功构建了所有树级单顶点 1 到 2 衰变宽度的详尽目录，并完成了μ子衰变多重性在实验中的探测灵敏度分析。
conclusion: 该框架证明了智能体在处理复杂高能物理理论推导方面的潜力，为实现自主符号计算提供了可靠的路径。
---

## 摘要
我们介绍了 Diagrammatica，这是 HEPTAPOD 智能体框架的一个符号计算扩展，它使大语言模型（LLM）智能体能够规划并执行多步理论计算。符号计算对 LLM 智能体提出了独特的可靠性挑战，因为其正确性受隐式数学约定的约束，而这些约定并未以易于在计算后端进行检查的形式进行编码。我们确定了两种互补的补救措施：工具约束计算和针对性知识落地，并将前者作为主要架构。具体而言，我们将智能体的动作分布集中在具有约定固定语义的工具调用上，其中智能体指定一个紧凑且可供人类审计的图规范，并由受信任的后端精确执行符号或数值操作。该工具包提供了两条互补的计算路径，它们共用一个图规范：用于数量级速率估计的朴素量纲分析（NDA），以及通过自动生成 FeynCalc 代码进行树级符号计算的精确图解分析（EDA），两者均辅以自动费曼图枚举和可导航的理论知识库。该架构在两个基准测试上进行了验证：(1) 涵盖标量、费米子和矢量母粒子的所有树级单顶点 $1\to 2$ 部分衰变宽度的详尽目录，包含完整的无质量极限和阈值极限以及标准模型验证；(2) 对缪子衰变多重性 $μ^+ \to ν_μ\barν_e + n(e^+e^-) + e^-$ 的 NDA 灵敏度研究，确定了在当前和计划中的缪子实验中可观测到的最大 $n$ 值。

## Abstract
We present Diagrammatica, a symbolic computation extension to the HEPTAPOD agentic framework, which enables LLM agents to plan and execute multi-step theoretical calculations. Symbolic computation poses a distinctive reliability challenge for LLM agents, as correctness is governed by implicit mathematical conventions that are not encoded in a form that can be easily checked in the computational backend. We identify two complementary remedies, tool-constrained computation and targeted knowledge grounding, and pursue the first as the primary architecture. Concretely, we concentrate the agent's action distribution onto tool calls with convention-fixing semantics, in which the agent specifies a compact, human-auditable diagram specification and a trusted backend performs the symbolic or numerical manipulations exactly. The toolkit provides two complementary calculation paths consuming a shared diagram specification: Naive Dimensional Analysis (NDA) for order-of-magnitude rate estimates and Exact Diagrammatic Analysis (EDA) for tree-level symbolic calculations via automatic FeynCalc code generation, both supplemented by automatic Feynman diagram enumeration and a navigable theory knowledge base. The architecture is validated on two benchmarks: (1) an exhaustive catalog of all tree-level, single-vertex $1\to 2$ partial decay widths across scalar, fermion, and vector parents, with complete massless and threshold limits and Standard Model validation; and (2) an NDA sensitivity study of the muon decay multiplicity $μ^+ \to ν_μ\barν_e + n(e^+e^-) + e^-$, determining the maximum observable $n$ at current and planned muon experiments.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **Diagrammatica** 的符号计算扩展工具包，旨在通过大语言模型（LLM）智能体实现高能物理（HEP）理论计算的自动化。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在高能物理中，符号计算（如推导衰变宽度、截面等）虽然有成熟的计算机代数系统（CAS，如 FeynCalc），但过程繁琐且极度依赖于隐式的数学约定（如度规符号、旋量归一化等）。LLM 虽然具备一定的符号处理能力，但在长链条推理中难以保持约定的一致性，容易产生“看似合理但实际错误”的计算结果。
*   **研究动机**：开发一种可靠的智能体架构，使 LLM 能够自主规划并执行复杂的高能物理理论推导，同时通过结构化约束消除 LLM 的随机性错误。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：采用**工具约束计算（Tool-constrained computation）**。不让 LLM 直接编写自由格式的代码，而是将其动作空间限制在模式验证（Schema-validated）的工具调用上。
*   **关键技术细节**：
    *   **共享图规范（Diagram Specification）**：定义了一种 LLM 友好的 JSON 格式，用于描述费曼图（粒子、自旋、顶点类型、耦合等）。这作为智能体与计算后端之间的“单一事实来源”。
    *   **双路径计算架构**：
        1.  **NDA（朴素量纲分析）**：基于量纲分析和相空间体积提供数量级估计，适用于任意多体末态。
        2.  **EDA（精确图解分析）**：自动生成 FeynCalc 代码，执行完整的迹求和、自旋求和及相空间积分，产生精确的符号表达式。
    *   **知识落地（Knowledge Grounding）**：建立了一个可导航的理论知识库（Skills Graph），在关键决策点为智能体提供针对性的物理约定参考。
    *   **自动化流程**：集成 FeynGraph 进行费曼图枚举，并能将符号结果导出为可调用的 Python 函数。

### 3. 实验设计：场景与基准测试
论文设计了两个具有挑战性的基准任务（Benchmarks）来验证架构：
*   **任务 1：详尽的 $1 \to 2$ 衰变速率目录**
    *   **场景**：遍历所有可能的树级单顶点衰变过程（涵盖标量、费米子、矢量母粒子）。
    *   **Benchmark**：与标准模型（SM）的 PDG 数据对比，并进行手算公式的交叉验证。
    *   **对比方法**：对比了 EDA（精确）、NDA（估计）以及不同耦合基底（如手征基底 vs. 矢量-轴矢量基底）的一致性。
*   **任务 2：缪子衰变多重性灵敏度研究**
    *   **场景**：研究 $\mu^+ \to \nu_\mu \bar{\nu}_e + n(e^+e^-) + e^+$ 随 $n$ 增加的速率变化。
    *   **Benchmark**：使用 MadGraph 5 进行数值交叉验证，并对比 SINDRUM 实验的测量值。

### 4. 资源与算力
*   **智能体模型**：使用了 **Claude 3.5/4.6 (Claude Code)** 作为核心智能体引擎。
*   **算力消耗**：
    *   **任务 1**：耗时 29 分钟，包含 66 次工具调用，处理了约 900 万个输入 token。
    *   **任务 2**：耗时 38 分钟，包含 63 次工具调用，处理了约 790 万个输入 token。
*   **后端环境**：计算在本地沙盒环境中执行，调用了 Wolfram Engine (FeynCalc) 和 MadGraph 5。论文未提及模型训练算力，因为其重点在于推理阶段的智能体编排。

### 5. 实验数量与充分性
*   **实验规模**：
    *   任务 1 生成了 **19 个独立过程**的完整符号公式库，涵盖了 6 个顶点族。
    *   任务 2 枚举了超过 **15 万个费曼图**（针对 $n=0$ 到 $n=3$），并对 $n=4$ 进行了外推分析。
*   **充分性与客观性**：实验设计非常系统，不仅涵盖了标准模型过程，还测试了多种自旋组合和算符（如维度-5 算符）。通过 EDA、NDA 和 MadGraph 三种独立方法的交叉比对，确保了结果的客观性和准确性。

### 6. 主要结论与发现
*   **可靠性提升**：通过工具约束，智能体能够避开符号计算中的常见陷阱，生成的符号公式在极限情况下（如无质量极限）表现符合物理预期。
*   **物理发现**：在任务 2 中，智能体自主确定了缪子衰变多重性的实验前沿：$n=2$ 是精确测量区，$n=3$ 是发现区，而 $n=4$ 是未来高强度缪子束流（HiMB）的探测极限。
*   **误差分析**：树级符号公式与 PDG 实验值的偏差在 2%-4% 以内，这主要归因于未考虑的高阶修正（NLO），证明了计算的精确性。

### 7. 优点
*   **约定固定（Convention-fixing）**：通过 Schema 强制固定物理约定，解决了 LLM 在符号推导中最核心的可靠性问题。
*   **多保真度协同**：NDA 的快速估算与 EDA 的精确推导相结合，模拟了物理学家的工作流。
*   **高透明度**：生成的 JSON 规范和 FeynCalc 脚本是持久化的，人类专家可以随时审计，而非“黑盒”输出。

### 8. 不足与局限
*   **适用范围限制**：目前的 EDA 路径仅限于树级 $1 \to 2$ 和 $2 \to 2$ 过程，尚未支持单圈（One-loop）计算或更复杂的多体相空间积分。
*   **耦合处理简化**：目前不支持非对角线的风味耦合矩阵（Flavor-indexed coupling matrices），无法处理某些复杂的干涉项。
*   **模型依赖**：虽然架构通用，但高度依赖于像 Claude 4.6 这样具备极强规划和代码理解能力的顶级 LLM。

（完）
