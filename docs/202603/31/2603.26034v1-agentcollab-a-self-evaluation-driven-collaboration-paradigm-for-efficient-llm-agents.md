---
title: "AgentCollab: A Self-Evaluation-Driven Collaboration Paradigm for Efficient LLM Agents"
title_zh: AgentCollab：一种用于高效大语言模型智能体的自评估驱动协作范式
authors: "Wenbo Gao, Renxi Liu, Xian Wang, Fang Guo, Shuai Yang, Xi Chen, Hui-Ling Zhen, Hanting Chen, Weizhe Lin, Xiaosong Li, Yaoyuan Wang"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26034v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 自主LLM智能体的自我评估驱动协作
tldr: AgentCollab 是一种针对大语言模型智能体的自驱动协作推理框架，旨在平衡执行效率与推理鲁棒性。该框架利用智能体自身的自我反思信号，动态协调不同能力的模型：在简单任务中由低成本模型快速执行，仅在检测到推理停滞时才升级至更强模型。通过引入难度感知的累积升级策略，AgentCollab 在多个基准测试中显著优化了准确率与效率的权衡，为高效智能体协作提供了新范式。
selection_source: fresh_fetch
motivation: 针对大语言模型智能体在长程推理中存在的执行效率与推理鲁棒性之间的权衡难题。
method: 提出一种基于自我评估信号的动态协作框架，通过累积失败信号触发从低成本模型向高能力模型的推理升级。
result: 在多个多步智能体基准测试中，该方法一致性地提升了智能体的准确率-效率帕累托前沿。
conclusion: AgentCollab 证明了通过模型间自发协作和动态资源分配，可以有效提升复杂任务下智能体的运行效率。
---

## 摘要
由大语言模型（LLM）驱动的自主智能体通过长程推理和工具交互执行复杂任务，其中在执行效率和推理鲁棒性之间存在着根本性的权衡。处于不同能力-成本水平的模型具有互补优势：低成本模型能够实现快速执行，但在困难的推理环节可能表现不佳；而更强大的模型则以更高的计算成本提供更稳健的推理。我们提出了 AgentCollab，这是一种自驱动的协作推理框架，在智能体执行过程中动态协调具有不同推理能力的模型。该框架不依赖于外部路由模块，而是利用智能体自身的自我反思信号来判断当前的推理轨迹是否取得了实质性进展，并仅在必要时将控制权提升至更强的推理层级。为了进一步稳定长程执行，我们引入了一种难度感知的累积提升策略，该策略根据最近的失败信号分配额外的推理预算。在实验中，我们使用“小-大”两级模型设置来实例化该框架。在多种多步智能体基准测试上的实验表明，AgentCollab 持续改进了大语言模型智能体的准确率-效率帕累托前沿。

## Abstract
Autonomous agents powered by large language models (LLMs) perform complex tasks through long-horizon reasoning and tool interaction, where a fundamental trade-off arises between execution efficiency and reasoning robustness. Models at different capability-cost levels offer complementary advantages: lower-cost models enable fast execution but may struggle on difficult reasoning segments, while stronger models provide more robust reasoning at higher computational cost. We present AgentCollab, a self-driven collaborative inference framework that dynamically coordinates models with different reasoning capacities during agent execution. Instead of relying on external routing modules, the framework uses the agent's own self-reflection signal to determine whether the current reasoning trajectory is making meaningful progress, and escalates control to a stronger reasoning tier only when necessary. To further stabilize long-horizon execution, we introduce a difficulty-aware cumulative escalation strategy that allocates additional reasoning budget based on recent failure signals. In our experiments, we instantiate this framework using a two-level small-large model setting. Experiments on diverse multi-step agent benchmarks show that AgentCollab consistently improves the accuracy-efficiency Pareto frontier of LLM agents.

---

## 论文详细总结（自动生成）

以下是对论文《AgentCollab: A Self-Evaluation-Driven Collaboration Paradigm for Efficient LLM Agents》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：自主 LLM 智能体在处理长程（Long-horizon）任务时，面临**执行效率（推理速度/成本）**与**推理鲁棒性（成功率）**之间的根本权衡。
*   **研究背景**：
    *   **单一模型局限性**：小模型推理快但易在复杂环节出错，且一步出错往往导致后续步数激增（轨迹漂移）；大模型鲁棒但全过程使用会导致极高的延迟和成本。
    *   **现有方案不足**：现有的模型路由（Routing）技术（如 RouteLLM）多针对单轮对话的成本优化，忽略了智能体多轮交互中“轨迹层级”的效率。外部路由模块还会引入额外的推理开销和监督信号需求。

### 2. 核心方法论
AgentCollab 提出了一种**自驱动协作推理范式**，其核心思想是让智能体通过“自我反思”来动态协调不同规模的模型。

*   **自驱动升级机制（Self-driven Escalation）**：
    1.  **预热阶段（Warm-up）**：初始阶段由大模型（$M_L$）执行，负责全局规划和任务分解。
    2.  **常规阶段（Routine）**：随后转交给小模型（$M_S$）执行具体的工具调用和推理。
    3.  **自我评估（Self-evaluation）**：在每一步推理中，模型会生成一个结构化的进度评估（Progress Check），包含理由和布尔值（TRUE/FALSE）。
    4.  **动态升级**：若检测到“停滞”（FALSE），控制权临时移交给大模型，由其修复错误轨迹。
*   **难度感知的累积预算分配（Difficulty-aware Cumulative Budget Allocation）**：
    *   为了防止频繁切换导致的计算缓存（KV Cache）失效，引入了累积策略。
    *   **公式逻辑**：升级层级 $l$ 取决于连续失败的次数。大模型的干预步数 $B_l$ 随 $l$ 增加。
    *   **线性增长模型**：$f(B_0, l) = \min \{B_{max}, B_0 + kl\}$，其中 $k$ 是增长率。这确保了在持续困难的环节，大模型能获得更长的连续执行时间以彻底解决问题。

### 3. 实验设计
*   **智能体框架**：集成了 DDV2（华为 openPangu 团队）和 WebSailor 两个开源框架。
*   **模型配置**：使用了 7B/38B（DDV2）和 3B/7B/32B（WebSailor）等不同规模的组合。
*   **基准测试（Benchmarks）**：
    1.  **BrowseComp_zh**：中文网页浏览与深度研究任务。
    2.  **HLE-math**：极具挑战性的数学推理任务。
    3.  **WritingBench**：长文本生成与写作任务。
*   **对比方法**：Small-only（纯小模型）、Large-only（纯大模型）、Random（随机切换）、RouteLLM、FrugalGPT。

### 4. 资源与算力
*   **硬件环境**：实验在 **Ascend 910B3 NPU** 上运行，使用了 4 个 Worker。
*   **推理后端**：使用 **vLLM-Ascend (v0.9.1)** 进行本地推理。
*   **训练时长**：文中未提及模型训练时长，因为该方法主要基于推理侧的动态调度，**无需额外的路由模型训练或策略微调**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三个不同领域的基准测试，样本量分别为 289、866 和 1000 个问题。
*   **消融实验**：
    *   对比了“规划者（Planner）”与“执行者（Executor）”角色分配大模型的效果。
    *   对比了静态预算与动态累积预算的效果。
    *   测试了不同超参数 $k$ 对帕累托前沿的影响。
*   **充分性评价**：实验设计较为全面，不仅对比了准确率，还详细分析了推理步数、加速比（Speedup）和切换频率，能够客观反映系统在效率与质量之间的平衡能力。

### 6. 主要结论与发现
*   **性能提升**：AgentCollab 在所有基准测试中均显著优于小模型基准，且非常接近纯大模型的表现（例如在 BrowseComp_zh 上，DDV2 框架下准确率从 18.3% 提升至 33.9%，接近大模型的 34.6%）。
*   **效率优势**：相比纯大模型，实现了 **1.3x 至 2.4x 的端到端加速**。
*   **轨迹优化**：实验发现，小模型虽然单步快，但出错后会导致推理步数大幅增加；AgentCollab 通过大模型适时介入，缩短了总推理轨迹长度。
*   **模型间隙影响**：协作效果取决于模型间的能力差距，差距越大（如 7B+32B），协作带来的帕累托前沿提升越明显。

### 7. 优点与亮点
*   **无需训练**：完全依赖模型自身的反思能力，具有即插即用的特性，降低了部署门槛。
*   **轨迹感知**：不同于传统的单轮路由，它关注长程任务的整体进度，通过减少无效推理步数来提升效率。
*   **动态预算**：累积升级策略有效缓解了模型频繁切换带来的计算开销，提升了 KV Cache 的复用率。

### 8. 不足与局限
*   **指令遵循依赖**：该方法高度依赖模型准确判断自身进度的能力。实验显示，在指令遵循能力较弱的模型（如早期 Qwen 版本）上，会出现较多“伪进展”判断，导致效果打折。
*   **同构模型限制**：实验主要在同系列不同规模的模型间进行，尚未探索跨架构、异构模型（如一个擅长搜索，一个擅长数学）之间的协作效果。
*   **闭源模型缺失**：由于 API 调用延迟不稳定，实验未包含 GPT-4 或 Gemini 等闭源模型，限制了在云端服务场景下的参考价值。

（完）
