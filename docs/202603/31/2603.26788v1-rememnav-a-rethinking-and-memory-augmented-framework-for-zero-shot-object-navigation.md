---
title: "ReMemNav: A Rethinking and Memory-Augmented Framework for Zero-Shot Object Navigation"
title_zh: ReMemNav：一种用于零样本物体导航的反思与记忆增强框架
authors: "Feng Wu, Wei Zuo, Wenliang Yang, Jun Xiao, Yang Liu, Xinhua Zeng"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.26788v1"
tags: ["query:agent"]
score: 9.0
evidence: 用于零样本智能体导航的记忆增强框架
tldr: ReMemNav是一个针对零样本物体导航的层次化框架，旨在解决视觉语言模型在导航中面临的空间幻觉和探索死锁问题。该框架通过集成全景语义先验与情景记忆，引入RAM模型辅助空间推理，并设计了基于情景语义缓冲区的双模态反思机制。实验表明，该方法在HM3D和MP3D数据集上显著提升了导航的成功率和探索效率。
selection_source: fresh_fetch
motivation: 针对视觉语言模型在零样本物体导航中存在的空间幻觉、局部死锁以及高层语义与底层控制不匹配等挑战。
method: 提出一种结合全景语义先验与情景记忆的层次化框架，利用RAM模型锚定推理，并通过双模态反思机制和深度掩码动作执行来优化决策。
result: 在HM3D和MP3D数据集上，ReMemNav在成功率和SPL指标上均大幅超越了现有的免训练零样本导航基准方法。
conclusion: 通过引入情景记忆和反思机制，ReMemNav有效增强了智能体在未知环境中的常识推理与空间导航能力。
---

## 摘要
零样本物体导航要求智能体在没有先验地图或特定任务训练的情况下，在陌生环境中定位未见过的目标物体，这仍然是一个重大挑战。尽管视觉语言模型（VLMs）的最新进展为该任务提供了极具前景的常识推理能力，但这些模型仍面临空间幻觉、局部探索死锁以及高层语义意图与底层控制之间脱节的问题。为此，我们提出了一种名为 ReMemNav 的新型分层导航框架，该框架将全景语义先验和情节记忆（episodic memory）与 VLMs 无缝集成。我们引入了 Recognize Anything Model 来锚定 VLM 的空间推理过程。我们还设计了一种基于情节语义缓冲队列的自适应双模态反思机制。该机制利用历史记忆主动验证目标可见性并修正决策，以防止死锁。对于底层动作执行，ReMemNav 利用深度掩码提取一系列可行操作，允许 VLM 选择最优动作并映射为实际的空间移动。在 HM3D 和 MP3D 上的广泛评估表明，ReMemNav 在成功率和探索效率方面均优于现有的免训练零样本基准模型。具体而言，我们实现了显著的绝对性能提升，在 HM3D v0.1 上 SR 和 SPL 分别提高了 1.7% 和 7.0%，在 HM3D v0.2 上提高了 18.2% 和 11.1%，在 MP3D 上提高了 8.7% 和 7.9%。

## Abstract
Zero-shot object navigation requires agents to locate unseen target objects in unfamiliar environments without prior maps or task-specific training which remains a significant challenge. Although recent advancements in vision-language models(VLMs) provide promising commonsense reasoning capabilities for this task, these models still suffer from spatial hallucinations, local exploration deadlocks, and a disconnect between high-level semantic intent and low-level control. In this regard, we propose a novel hierarchical navigation framework named ReMemNav, which seamlessly integrates panoramic semantic priors and episodic memory with VLMs. We introduce the Recognize Anything Model to anchor the spatial reasoning process of the VLM. We also design an adaptive dual-modal rethinking mechanism based on an episodic semantic buffer queue. The proposed mechanism actively verifies target visibility and corrects decisions using historical memory to prevent deadlocks. For low-level action execution, ReMemNav extracts a sequence of feasible actions using depth masks, allowing the VLM to select the optimal action for mapping into actual spatial movement. Extensive evaluations on HM3D and MP3D demonstrate that ReMemNav outperforms existing training-free zero-shot baselines in both success rate and exploration efficiency. Specifically, we achieve significant absolute performance improvements, with SR and SPL increasing by 1.7% and 7.0% on HM3D v0.1, 18.2% and 11.1% on HM3D v0.2, and 8.7% and 7.9% on MP3D.

---

## 论文详细总结（自动生成）

### ReMemNav：一种用于零样本物体导航的反思与记忆增强框架论文总结

#### 1. 核心问题与背景（研究动机）
零样本物体导航（Zero-Shot Object Navigation）要求智能体在完全陌生的环境中寻找未见过的目标物体。尽管视觉语言模型（VLM）具备强大的常识推理能力，但在实际导航中面临三大挑战：
*   **空间幻觉**：VLM 容易根据局部纹理误判不存在的物体（如将墙上的阴影看成电视）。
*   **局部探索死锁**：由于缺乏记忆，智能体容易在同一区域循环徘徊（如在客厅反复寻找沙发，即使已经确认没有）。
*   **控制脱节**：高层语义指令与底层物理动作执行之间缺乏有效的映射机制。

#### 2. 方法论：核心思想与关键技术
ReMemNav 提出了一种分层认知框架，核心思想是结合**轻量级语义锚定**、**情景记忆**和**双模态反思机制**。

*   **语义视觉锚定（Semantic Visual Anchoring）**：利用 Recognize Anything Model (RAM) 对智能体全景（6个视角）图像提取标签，生成结构化语义先验字典。这为 VLM 提供了底层的物理锚点，减少了纯视觉推理带来的幻觉。
*   **情景记忆缓冲队列（Episodic Memory Buffer Queue）**：维护一个滑动窗口队列（容量 $K=10$），存储历史节点的坐标和 VLM 生成的场景描述。这避免了构建复杂全局地图的计算开销，同时保留了关键的探索上下文。
*   **自适应双模态反思机制（Adaptive Dual-Modal Rethinking）**：
    *   **模式 A（决策评估）**：当未发现目标时，系统检查预选方向是否与记忆中的已探索区域重叠。若重叠，则触发反思，强制 VLM 结合历史描述重新选择新路径，从而打破死锁。
    *   **模式 B（目标验证）**：当 VLM 声称发现目标时，系统强制其对特定视角进行二次精细核实，过滤视觉伪影，防止误停。
*   **底层动作执行**：利用深度掩码采样可行路径射线，由 VLM 在图像上选择最优射线并映射为物理空间的极坐标移动。

#### 3. 实验设计
*   **数据集/场景**：在 **HM3D** (v0.1 和 v0.2) 以及 **MP3D** 两个主流大规模 3D 室内环境数据集上进行评估。
*   **Benchmark（指标）**：采用成功率（SR）和路径长度加权成功率（SPL）。
*   **对比方法**：对比了包括 ZSON、CoW、ESC、VLFM、SG-Nav、VLMNav 等在内的多种先进的免训练（Training-free）和基于训练的零样本导航方法。

#### 4. 资源与算力
*   **模型使用**：核心认知引擎采用了 **Gemini-3-Flash**（表现最优），并对比了 **Qwen2.5-VL-7B**、**Qwen3-VL-4B/8B** 等开源模型。
*   **算力说明**：论文**未明确提及**具体的 GPU 训练时长或数量。由于该框架属于**免训练（Training-free）**方案，其主要算力消耗在于推理阶段的 VLM API 调用或本地模型部署。

#### 5. 实验数量与充分性
*   **实验规模**：在三个基准数据集上进行了数千个 Episode 的测试（HM3D v0.1 2000个，v0.2 1000个，MP3D 2195个）。
*   **消融实验**：针对 RAM 模块、反思机制（ADRM）、记忆缓冲（EMBQ）分别做了剥离实验，验证了各组件的增益。
*   **敏感性分析**：对记忆容量 $K$ 进行了倒 U 型曲线分析，确定了最优参数。
*   **客观性**：实验覆盖了不同规模和质量的数据集，对比了多种 SOTA 方法，实验设计较为全面、客观。

#### 6. 主要结论与发现
*   **性能领先**：ReMemNav 在所有免训练方法中达到 SOTA。在 HM3D v0.2 上，SR 提升了 18.2%，SPL 提升了 11.1%。
*   **记忆的重要性**：引入情景记忆能显著解决局部死锁问题，但记忆窗口并非越大越好（$K=10$ 最优），过长的记忆会引入噪声干扰 VLM 推理。
*   **反思机制的价值**：双模态反思能有效过滤 RAM 和 VLM 产生的假阳性目标，大幅提升导航的鲁棒性。

#### 7. 优点与亮点
*   **分层架构清晰**：将高层语义推理与底层物理感知解耦，既利用了 VLM 的常识，又通过 RAM 进行了约束。
*   **轻量化记忆**：相比于构建复杂的语义拓扑地图，滑动窗口式的情景记忆更易于实现且计算高效。
*   **双系统启发**：借鉴人类认知的“快思考”与“慢思考”理论，设计的反思机制具有很强的逻辑解释性。

#### 8. 不足与局限
*   **VLM 依赖性**：实验显示性能高度依赖于底层 VLM 的能力（Gemini 显著优于 Qwen），在弱模型上的表现仍有提升空间。
*   **长程记忆缺失**：由于采用滑动窗口，智能体可能会遗忘很久之前的探索信息，在超大规模或多层建筑中的表现可能受限。
*   **实时性挑战**：多次调用 VLM 进行反思和验证可能会增加单步决策的延迟，论文未详细讨论推理耗时对实时导航的影响。

（完）
