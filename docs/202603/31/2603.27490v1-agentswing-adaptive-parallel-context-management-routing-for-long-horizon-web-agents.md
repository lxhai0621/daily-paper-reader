---
title: "AgentSwing: Adaptive Parallel Context Management Routing for Long-Horizon Web Agents"
title_zh: AgentSwing：面向长程 Web 智能体的自适应并行上下文管理路由
authors: "Zhaopeng Feng, Liangcai Su, Zhen Zhang, Xinyu Wang, Xiaotian Zhang, Xiaobin Wang, Runnan Fang, Qi Zhang, Baixuan Li, Shihao Cai, Rui Ye, Hui Chen, Jiang Yong, Joey Tianyi Zhou, Chenxiong Qian, Pengjun Xie, Bryan Hooi, Zuozhu Liu, Jingren Zhou"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27490v1"
tags: ["query:agent"]
score: 10.0
evidence: 长程Web智能体的自适应并行上下文管理
tldr: 针对长程网络智能体在信息搜索中面临的有限上下文容量瓶颈，本文提出了AgentSwing框架。该框架摒弃了传统的静态上下文管理策略，引入了基于搜索效率和终端精度的概率框架。通过在触发点并行扩展多个上下文管理分支，并利用前瞻路由选择最优路径，AgentSwing在多个基准测试中显著提升了任务成功率，并大幅减少了交互轮数。
selection_source: fresh_fetch
motivation: 现有的静态上下文管理策略无法适应长程搜索过程中上下文有用性和可靠性的动态变化。
method: 提出一种状态感知的自适应并行上下文管理路由框架，通过并行扩展分支并结合前瞻路由来动态选择最佳上下文策略。
result: 实验表明AgentSwing在多种基准测试中均优于静态方法，在交互轮数减少高达3倍的同时，提升了长程智能体的性能上限。
conclusion: 该研究为长程智能体的上下文管理提供了原则性的分析视角和高效的自适应解决方案。
---

## 摘要
随着大语言模型（LLMs）演进为用于长程信息检索的自主智能体，管理有限的上下文容量已成为一个关键瓶颈。现有的上下文管理方法通常在整个轨迹中采用单一的固定策略。这种静态设计可能在某些状态下表现良好，但随着长程搜索过程中累积上下文的效用和可靠性不断演变，它们无法进行自适应调整。为了形式化这一挑战，我们引入了一个概率框架，通过两个互补维度来刻画长程任务的成功：搜索效率和终端精度。基于这一视角，我们提出了 AgentSwing，这是一个状态感知的自适应并行上下文管理路由框架。在每个触发点，AgentSwing 并行扩展多个经过上下文管理的决策分支，并使用前瞻路由（lookahead routing）来选择最有前景的后续路径。在不同基准测试和智能体骨干网络上的实验表明，AgentSwing 始终优于强大的静态上下文管理方法，通常在交互轮数减少多达 3 倍的情况下达到或超过其性能，同时也提升了长程 Web 智能体的最终性能上限。除了实证收益外，所提出的概率框架还为分析和设计未来长程智能体的上下文管理策略提供了一个原则性的视角。

## Abstract
As large language models (LLMs) evolve into autonomous agents for long-horizon information-seeking, managing finite context capacity has become a critical bottleneck. Existing context management methods typically commit to a single fixed strategy throughout the entire trajectory. Such static designs may work well in some states, but they cannot adapt as the usefulness and reliability of the accumulated context evolve during long-horizon search. To formalize this challenge, we introduce a probabilistic framework that characterizes long-horizon success through two complementary dimensions: search efficiency and terminal precision. Building on this perspective, we propose AgentSwing, a state-aware adaptive parallel context management routing framework. At each trigger point, AgentSwing expands multiple context-managed branches in parallel and uses lookahead routing to select the most promising continuation. Experiments across diverse benchmarks and agent backbones show that AgentSwing consistently outperforms strong static context management methods, often matching or exceeding their performance with up to $3\times$ fewer interaction turns while also improving the ultimate performance ceiling of long-horizon web agents. Beyond the empirical gains, the proposed probabilistic framework provides a principled lens for analyzing and designing future context management strategies for long-horizon agents.

---

## 论文详细总结（自动生成）

这篇论文由阿里巴巴通义实验室（Tongyi Lab）提出，旨在解决长程（Long-Horizon）Web智能体在处理复杂信息搜索任务时面临的上下文容量瓶颈问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：长程Web智能体在执行任务（如深度搜索、验证、回溯）时，往往需要数十甚至上百步交互。有限的上下文窗口（Context Window）与不断累积的交互历史之间存在矛盾。
*   **研究动机**：
    *   **静态策略的局限性**：现有的上下文管理（CM）方法（如“全部丢弃”Discard-All、“保留最近N轮”Keep-Last-N或“摘要”Summary）通常在整个任务轨迹中固定使用单一策略。
    *   **状态依赖性**：在搜索过程中，上下文的价值是动态变化的。有时需要保留中间结构，有时则充斥着噪声或无效路径，需要果断清理。静态方法无法根据当前轨迹状态自适应调整。

### 2. 方法论：AgentSwing 框架
AgentSwing 提出了一种**状态感知的自适应并行上下文管理路由框架**，其核心包含两个维度和一个机制：

*   **概率评估框架**：将长程任务的成功率（Pass@1）分解为两个互补维度：
    1.  **搜索效率 ($\eta$)**：智能体在耗尽资源（如最大轮数）前到达终点的概率。
    2.  **终端精度 ($\rho$)**：在到达终点的前提下，给出正确答案的概率。
    *   公式表示为：$P(Success) = \eta \times \rho$。
*   **关键技术流程**：
    1.  **触发机制**：当上下文长度达到预设阈值（如模型最大容量的20%或40%）时，触发上下文管理。
    2.  **并行上下文管理（Parallel CM）**：同时应用多种策略（Keep-Last-N, Summary, Discard-All）生成多个候选的“管理后上下文”分支。
    3.  **前瞻路由（Lookahead Routing）**：每个分支独立向前试行 $K$ 步（实验中 $K=3$ 效果最佳）。
    4.  **动态路由选择**：智能体模型根据原始上下文和各分支试行后的表现，评估并选择最有前景的分支作为主轨迹继续执行，丢弃其余分支。

### 3. 实验设计
*   **数据集/基准测试**：
    *   **BrowseComp**：复杂的网页浏览基准测试（200个任务）。
    *   **BrowseComp-ZH**：中文网页浏览测试（289个任务）。
    *   **Humanity's Last Exam (HLE)**：极具挑战性的跨学科难题（500个文本任务）。
*   **对比方法**：
    *   **Baseline**：不进行上下文管理（w/o CM）。
    *   **静态策略**：Discard-All、Keep-Last-N、Summary。
*   **智能体骨干网络**：GPT-OSS-120B、DeepSeek-v3.2、Tongyi-DR-30B-A3B。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的训练算力（如GPU型号、数量或训练时长）。
*   **原因分析**：AgentSwing 主要是一种**推理时（Test-time）**的扩展和路由机制，侧重于算法框架在现有预训练/微调模型上的应用，而非从头训练模型。实验中提到了使用 Code Sandbox 进行安全的 Python 代码执行。

### 5. 实验数量与充分性
*   **实验规模**：在三个不同语言和难度的基准测试上进行了测试，涵盖了从30B到120B参数规模的多种模型。
*   **消融实验**：
    *   测试了不同的前瞻步数 $K$（1, 3, 5）。
    *   测试了不同 CM 策略的组合效果。
    *   分析了策略转换的概率分布。
*   **充分性评价**：实验设计较为充分，通过“对齐终端精度”（Aligned Terminal Precision）等指标公平地比较了不同策略在相同任务子集上的表现，客观地展示了 AgentSwing 在效率与精度之间的平衡能力。

### 6. 主要结论与发现
*   **性能提升**：AgentSwing 在所有基准测试中均显著优于静态 CM 方法。例如，在 BrowseComp-ZH 上将 DeepSeek-v3.2 的表现提升至 71.3%，超过了多个闭源商业模型。
*   **效率增益**：在达到相同性能水平时，AgentSwing 所需的交互轮数比静态方法少 3 倍。
*   **打破权衡**：静态方法通常在效率（$\eta$）和精度（$\rho$）之间做权衡（如 Discard-All 精度高但效率低），而 AgentSwing 通过自适应路由移动到了效率-精度平面的更优区域。
*   **前瞻的重要性**：消融实验证明，仅靠并行 CM 而不进行前瞻试行（Lookahead）无法达到最优效果，短期的未来反馈对路由决策至关重要。

### 7. 优点与亮点
*   **理论创新**：首次提出了针对长程智能体的效率-精度概率分解框架，为后续研究提供了理论工具。
*   **自适应性**：摒弃了“一刀切”的静态策略，利用模型自身的判断力在不同任务阶段选择最合适的上下文处理方式。
*   **通用性强**：不依赖于特定的模型架构，在多种开源骨干网络上均表现出一致的增益。

### 8. 不足与局限
*   **推理开销**：由于需要并行扩展分支并进行前瞻试行，AgentSwing 在触发点会产生额外的 Token 消耗和计算延迟。
*   **模型依赖**：路由的效果高度依赖于骨干模型本身的判断和推理能力，如果模型无法准确评估分支前景，路由可能失效。
*   **未涉及训练优化**：目前仅作为外部控制机制，尚未探讨如何通过训练使模型原生具备这种自适应上下文管理的能力。

（完）
