---
title: Mitigating Hallucination on Hallucination in RAG via Ensemble Voting
title_zh: 通过集成投票缓解 RAG 中的“幻觉之上的幻觉”
authors: "Zequn Xie, Zhengyang Sun"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27253v1"
tags: ["query:ma-kf"]
score: 10.0
evidence: 通过多智能体集成投票减轻RAG中的幻觉
tldr: 针对检索增强生成（RAG）中因检索错误导致生成模型产生“幻觉上的幻觉”这一问题，本文提出了VOTE-RAG框架。该框架无需训练，采用两阶段结构：首先通过多代理并行生成多样化查询并聚合检索文档，随后由多代理基于聚合文档独立生成答案并进行多数投票。实验证明，VOTE-RAG在六个基准数据集上表现优异，且具有架构简单、可并行化等优势，有效提升了RAG系统的可靠性。
selection_source: fresh_fetch
motivation: 旨在解决RAG中因检索到的错误信息误导生成模型，进而引发“幻觉上的幻觉”这一复合错误问题。
method: 提出一种无需训练的VOTE-RAG框架，利用检索投票聚合多源文档，并通过响应投票机制确定最终输出。
result: 在六个基准数据集上的实验表明，该框架的性能媲美或超越了现有的复杂框架，且运行效率更高。
conclusion: 简单且可靠的集成投票机制是缓解RAG幻觉、避免问题漂移的一种更优且更高效的方案。
---

## 摘要
检索增强生成 (RAG) 旨在通过整合外部知识来减少大语言模型 (LLMs) 中的幻觉。然而，RAG 引入了一个关键挑战：“幻觉之上的幻觉”，即有缺陷的检索结果误导生成模型，从而导致复合幻觉。为了解决这一问题，我们提出了 VOTE-RAG，这是一个新颖的、无需训练的框架，具有两阶段结构和高效、可并行的投票机制。VOTE-RAG 包括：(1) 检索投票，由多个智能体并行生成多样化的查询并聚合所有检索到的文档；(2) 响应投票，由多个智能体基于聚合文档独立生成答案，最终输出由多数投票决定。我们在六个基准数据集上进行了对比实验。结果表明，VOTE-RAG 的性能与更复杂的框架相当甚至更优。此外，VOTE-RAG 具有更简单的架构，完全可并行化，并避免了“问题漂移”风险。我们的工作证明，简单、可靠的集成投票是缓解 RAG 幻觉的一种更优且更高效的方法。

## Abstract
Retrieval-Augmented Generation (RAG) aims to reduce hallucinations in Large Language Models (LLMs) by integrating external knowledge. However, RAG introduces a critical challenge: hallucination on hallucination," where flawed retrieval results mislead the generation model, leading to compounded hallucinations. To address this issue, we propose VOTE-RAG, a novel, training-free framework with a two-stage structure and efficient, parallelizable voting mechanisms. VOTE-RAG includes: (1) Retrieval Voting, where multiple agents generate diverse queries in parallel and aggregate all retrieved documents; (2) Response Voting, where multiple agents independently generate answers based on the aggregated documents, with the final output determined by majority vote. We conduct comparative experiments on six benchmark datasets. Our results show that VOTE-RAG achieves performance comparable to or surpassing more complex frameworks. Additionally, VOTE-RAG features a simpler architecture, is fully parallelizable, and avoids the problem drift" risk. Our work demonstrates that simple, reliable ensemble voting is a superior and more efficient method for mitigating RAG hallucinations.

---

## 论文详细总结（自动生成）

### 论文总结：通过集成投票缓解 RAG 中的“幻觉之上的幻觉”

#### 1. 核心问题与整体含义
论文针对检索增强生成（RAG）系统中的一个关键瓶颈——**“幻觉之上的幻觉”（Hallucination on Hallucination）**展开研究。虽然 RAG 通过引入外部知识减少了 LLM 的固有幻觉，但如果检索阶段返回了错误、偏见或无关的信息，这些“检索幻觉”会进一步误导生成模型，导致最终输出产生复合型的幻觉错误。研究动机在于寻找一种比现有的复杂迭代辩论（如 DRAG 框架）更简单、更高效且鲁棒的方法来打破这种错误传导链。

#### 2. 方法论：VOTE-RAG 框架
VOTE-RAG 是一个**无需训练（Training-free）**的框架，核心思想是利用“群体智慧”，通过两阶段的并行集成投票机制来增强系统的可靠性：

*   **第一阶段：检索投票（Retrieval Voting）**
    *   **并行查询生成**：由 $N$ 个独立的智能体针对原始问题并行生成多样化的查询语句（Queries），以克服单次查询的偏见。
    *   **查询池聚合**：将所有生成的查询汇总，从知识库中检索出一组更全面、覆盖面更广的候选文档池。
*   **第二阶段：响应投票（Response Voting）**
    *   **独立答案生成**：$N$ 个响应智能体基于相同的聚合文档池，独立生成各自的答案。
    *   **多数投票决策**：通过多数投票函数（如字符串匹配或语义聚类）从多个候选答案中选出最终结果。
*   **数学原理**：论文引用了霍夫丁不等式（Hoeffding's inequality）证明，只要单个智能体的错误率低于 0.5 且保持一定的独立性，最终集成结果的错误率将随智能体数量 $N$ 的增加而呈指数级下降。

#### 3. 实验设计
*   **数据集**：涵盖了三类任务共 6 个基准数据集：
    *   **开放域问答**：NQ, TriviaQA, PopQA。
    *   **多跳推理问答**：2WikiMultihopQA, HotpotQA。
    *   **常识推理**：StrategyQA。
*   **对比方法（Baselines）**：
    *   基础模型：Naive Gen, Naive RAG。
    *   检索优化类：IRCoT, Iter-RetGen, FLARE。
    *   生成/反思优化类：Self-RAG, SuRe。
    *   **核心对手**：DRAG（基于多轮辩论的复杂 RAG 优化框架）。
*   **实验设置**：使用 Llama-3.1-8B-Instruct 作为基础模型，E5-base-v2 作为检索器，Wikipedia 作为知识库。

#### 4. 资源与算力
论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）以及具体的推理/训练时长。但由于该方法是“无需训练”的，其主要开销集中在推理阶段的 LLM 调用次数上。文中提到 VOTE-RAG 在 $N=5$ 时固定使用 10 次 LLM 调用，并强调其并行化结构能显著降低实际运行的墙钟时间（Wall-clock latency）。

#### 5. 实验数量与充分性
*   **实验规模**：在 6 个主流数据集上进行了全面测试，每类任务均有代表性。
*   **消融实验**：针对智能体数量 $N$（取值 3, 5, 7）进行了对比，验证了性能随 $N$ 增加的鲁棒性。
*   **客观性**：实验对比了多种不同类型的 SOTA 方法，并特别针对“效率”和“延迟”与最先进的辩论模型（DRAG）进行了深入分析。实验设计较为公平，使用了统一的开源框架 FlashRAG 进行复现。

#### 6. 主要结论与发现
*   **性能优异**：VOTE-RAG 在多个数据集上的表现达到或超过了复杂的迭代辩论框架（如 DRAG）。
*   **解决“问题漂移”**：传统的迭代辩论方法在轮数过多时容易偏离原始问题（Problem Drift），而 VOTE-RAG 的并行结构有效避免了这一风险。
*   **效率优势**：虽然总 Token 消耗与辩论方法相当，但由于其完全并行的特性，在实际应用中的响应速度（延迟）远优于串行的迭代方法。
*   **规模效应**：实验证实，增加投票智能体的数量能稳定提升准确率，展现出良好的可扩展性。

#### 7. 优点
*   **架构简洁**：相比于需要复杂 Prompt 工程和多轮交互逻辑的辩论模型，投票机制极易实现和维护。
*   **高并行性**：检索和响应两个阶段均可并行化，适合大规模生产环境部署。
*   **鲁棒性强**：通过集成方法有效过滤了单个智能体因检索噪声产生的随机幻觉。

#### 8. 不足与局限
*   **计算成本**：虽然延迟低，但 $N$ 倍的智能体意味着 $N$ 倍的 Token 消耗，对于 API 调用成本敏感的场景可能不够经济。
*   **独立性假设**：投票机制的有效性高度依赖于智能体之间的“独立性”。如果所有智能体都受到相同的强误导性文档影响，可能会产生一致的错误（系统性偏差），此时投票机制将失效。
*   **任务局限**：目前主要在短答案问答和推理任务上验证，对于长文本生成或极具创造性的任务，简单的多数投票可能难以定义。

（完）
