---
title: "Red-MIRROR: Agentic LLM-based Autonomous Penetration Testing with Reflective Verification and Knowledge-augmented Interaction"
title_zh: Red-MIRROR：基于智能体大语言模型的自主渗透测试系统，结合反思验证与知识增强交互
authors: "Tran Vy Khang, Nguyen Dang Nguyen Khang, Nghi Hoang Khoa, Do Thi Thu Hien, Van-Hau Pham, Phan The Duy"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27127v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 基于智能体LLM的自主渗透测试，具有知识增强交互功能
tldr: "针对Web应用渗透测试中人工成本高及现有LLM智能体知识依赖、记忆碎片化等问题，本文提出Red-MIRROR多智能体系统。该系统通过RAG增强外部知识、SRMM管理持久状态及双阶段反射机制进行自适应验证，构建了紧耦合的记忆-反射骨干。实验显示，Red-MIRROR在XBOW基准测试中达到86%的成功率，显著优于现有SOTA工具，展现了卓越的长程推理与攻击载荷优化能力。"
selection_source: fresh_fetch
motivation: 传统渗透测试高度依赖专家经验且效率低下，而现有LLM智能体在知识获取、记忆管理和结果验证方面存在明显局限。
method: 提出一种结合检索增强生成（RAG）、共享循环记忆机制（SRMM）和双阶段反射验证的多智能体协作框架。
result: "在XBOW基准测试中取得86.0%的成功率，远超PentestAgent（50%）和AutoPT（46%）等现有先进系统。"
conclusion: Red-MIRROR通过紧耦合的记忆与反射机制，有效提升了自动化渗透测试的成功率和处理复杂长程任务的能力。
---

## 摘要
Web 应用程序仍然是网络安全中的主要攻击面，SQL 注入、XSS 和业务逻辑漏洞等漏洞持续导致重大数据泄露。虽然渗透测试在识别这些弱点方面非常有效，但传统的各种手动方法耗时且严重依赖稀缺的专家知识。最近基于大语言模型（LLM）的多智能体系统在自动化渗透测试方面展现了潜力，但仍面临关键局限：过度依赖参数化知识、会话记忆碎片化以及对攻击载荷（payload）和响应的验证不足。本文提出了 Red-MIRROR，这是一种新型多智能体自动化渗透测试系统，它引入了一个紧耦合的记忆-反思骨干网络，以显式管理智能体间的推理。通过综合利用检索增强生成（RAG）进行外部知识增强、共享循环记忆机制（SRMM）进行持久状态管理，以及双阶段反思机制进行自适应验证，Red-MIRROR 为复杂的 Web 漏洞利用提供了稳健的解决方案。在 XBOW 基准测试和 Vulhub CVE 上的实证评估表明，Red-MIRROR 在 Vulhub 场景下的表现与最先进的智能体相当，同时在 XBOW 基准测试上展现出明显优势。在 XBOW 基准测试中，Red-MIRROR 达到了 86.0% 的总成功率，优于 PentestAgent (50.0%)、AutoPT (46.0%) 和 VulnBot 基准 (6.0%)。此外，该系统实现了 93.99% 的子任务完成率，表明其具有强大的长程推理和载荷优化能力。最后，我们讨论了伦理影响并提出了减轻滥用风险的保护措施。

## Abstract
Web applications remain the dominant attack surface in cybersecurity, where vulnerabilities such as SQL injection, XSS, and business logic flaws continue to cause significant data breaches. While penetration testing is effective for identifying these weaknesses, traditional manual approaches are time-consuming and heavily dependent on scarce expert knowledge. Recent Large Language Models (LLM)-based multi-agent systems have shown promise in automating penetration testing, yet they still suffer from critical limitations: over-reliance on parametric knowledge, fragmented session memory, and insufficient validation of attack payloads and responses. This paper proposes Red-MIRROR, a novel multi-agent automated penetration testing system that introduces a tightly coupled memory-reflection backbone to explicitly govern inter-agent reasoning. By synthesizing Retrieval-Augmented Generation (RAG) for external knowledge augmentation, a Shared Recurrent Memory Mechanism (SRMM) for persistent state management, and a Dual-Phase Reflection mechanism for adaptive validation, Red-MIRROR provides a robust solution for complex web exploitation. Empirical evaluation on the XBOW benchmark and Vulhub CVEs shows that Red-MIRROR achieves performance comparable to state-of-the-art agents on Vulhub scenarios, while demonstrating a clear advantage on the XBOW benchmark. On the XBOW benchmark, Red-MIRROR attains an overall success rate of 86.0 percent, outperforming PentestAgent (50.0 percent), AutoPT (46.0 percent), and the VulnBot baseline (6.0 percent). Furthermore, the system achieves a 93.99 percent subtask completion rate, indicating strong long-horizon reasoning and payload refinement capability. Finally, we discuss ethical implications and propose safeguards to mitigate misuse risks.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **Red-MIRROR** 的自主渗透测试系统，旨在利用大语言模型（LLM）多智能体协作来解决 Web 安全测试中的自动化难题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：Web 应用是网络攻击的主要目标（如 SQL 注入、XSS 等）。传统的渗透测试依赖高薪专家且耗时较长。
*   **现有局限**：现有的基于 LLM 的渗透测试智能体存在三大瓶颈：
    1.  **知识依赖**：过度依赖模型内部参数化知识，易产生幻觉。
    2.  **记忆碎片化**：在长程攻击链中，会话上下文容易丢失或被稀释。
    3.  **验证不足**：缺乏对攻击载荷（Payload）和服务器响应的有效验证，导致盲目尝试。
*   **核心目标**：构建一个具备持久化记忆、自适应反思和外部知识增强的稳健自动化渗透测试框架。

### 2. 核心方法论
Red-MIRROR 采用多智能体架构（Planner, Collector, Exploiter, Summarizer），其核心技术包括：
*   **共享循环记忆机制 (SRMM)**：
    *   **分区存储**：按智能体角色划分存储空间，确保信息来源可追溯。
    *   **单向流动与单次写入**：执行智能体（Collector/Exploiter）写入观察结果，规划智能体（Planner）读取聚合后的摘要，防止反馈循环和幻觉。
    *   **有界聚合**：通过 LLM 摘要功能压缩历史信息，解决长上下文导致的性能下降。
*   **双阶段反思机制 (Dual-Phase Reflection)**：
    *   **内部反思 (Intra-reflection)**：执行智能体根据服务器返回的错误或状态码，实时微调 Payload（如绕过 WAF 过滤）。
    *   **外部反思 (Inter-reflection)**：Planner 智能体通过“多数投票制”评估任务是否成功，并根据全局状态调整下一步策略。
*   **知识增强 (RAG & Fine-tuning)**：
    *   集成 RAG 模块，实时检索 CVE、CAPEC 和 MITRE ATT&CK 知识库。
    *   使用 **LoRA** 技术对 Qwen2.5-14B 进行微调，训练数据包含 1,644 个安全相关的指令对。

### 3. 实验设计
*   **数据集/场景**：
    *   **XBOW Benchmark**：包含 50 个挑战（涵盖 Easy, Medium, Hard 三个等级）。
    *   **Vulhub**：选取了 8 个真实的严重级别 CVE 漏洞环境。
*   **对比方法**：
    *   **VulnBot**（基准线）。
    *   **PentestAgent**（SOTA 智能体）。
    *   **AutoPT**（SOTA 智能体）。
*   **评估指标**：成功率 (SR)、子任务完成率 (SCR)、漏洞利用耗时 (TTE)。

### 4. 资源与算力
*   **硬件环境**：
    *   **攻击机**：Windows 主机（Intel i5-11300H, 16GB RAM, GTX 1650），通过 SSH 控制 Kali Linux 虚拟机。
    *   **模型推理**：使用 Kaggle 平台（2 × NVIDIA Tesla T4 GPU, 30GB RAM）。
*   **模型使用**：
    *   **DeepSeek-V3.2**：作为高性能 API 基准。
    *   **Qwen2.5-14B**：作为本地部署的开源模型代表，进行了 4-bit 量化和 LoRA 微调。
*   **训练时长**：文中未明确给出具体的 LoRA 训练总时长，但提到了 Kaggle 每周约 30 小时的配额。

### 5. 实验充分性与客观性
*   **实验规模**：共进行了 58 个场景的测试，涵盖了 OWASP Top 10 的主要类别。
*   **消融实验**：详细对比了“无 SRMM/无反思”、“仅 SRMM”、“仅反思”以及“全功能”四种配置，验证了各组件的增益。
*   **鲁棒性分析**：针对 5 种不同强度的输入过滤（从无过滤到动态替换）进行了分类评估，证明了系统在复杂防御环境下的生存能力。
*   **客观性**：所有对比方法均在相同的 DeepSeek-V3.2 模型下运行，确保了架构层面的公平比较。

### 6. 主要结论
*   **性能领先**：在 XBOW 基准测试中，Red-MIRROR 取得了 **86.0%** 的成功率，远超 PentestAgent (50.0%) 和 AutoPT (46.0%)。
*   **长程推理能力**：子任务完成率高达 **93.99%**，证明其在复杂攻击链中的稳定性。
*   **开源潜力**：经过微调的 14B 模型虽然仍落后于 DeepSeek 等巨型模型，但在特定任务上的表现较基础模型有显著提升（成功率从 2% 升至 12%）。
*   **效率提升**：在存在防御过滤的场景下，双阶段反思机制能将漏洞利用步数（TTE）减少约 40%。

### 7. 优点与亮点
*   **记忆管理创新**：SRMM 解决了 LLM 智能体常见的“上下文膨胀”和“信息稀释”问题。
*   **闭环反馈**：双阶段反思模拟了人类专家的“尝试-观察-调整”逻辑，而非简单的脚本执行。
*   **细粒度评估**：手动标注了子任务级别的 Benchmark，为评估智能体的中间推理过程提供了新工具。
*   **伦理考量**：提出了基于角色的访问控制、审计日志和 RAG 知识门控等安全防护建议。

### 8. 不足与局限
*   **盲注挑战**：在缺乏语义反馈的“盲注”（Blind Injection）场景下，由于信号微弱且受网络抖动影响，系统表现大幅下降。
*   **密码学限制**：缺乏专门的数学分析和脚本编写能力，无法处理复杂的加密算法破解任务。
*   **框架依赖性**：对于某些需要特定 HTTP 标头或非标准参数位置的框架级漏洞，如果 RAG 知识库未覆盖，系统难以自主推断。
*   **成本问题**：由于引入了多次反思和摘要操作，其 Token 消耗成本（约 $0.20/挑战）高于简化版智能体。

（完）
