---
title: "Safety as Computation: Certified Answer Reuse via Capability Closure in Task-Oriented Dialogue"
title_zh: 安全即计算：任务型对话中通过能力闭包实现经过认证的答案重用
authors: Cosimo Spera
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21448v1"
tags: ["query:agent"]
score: 7.0
evidence: 任务导向对话中的认证答案重用
tldr: 针对任务型对话系统在处理可推导查询时存在的冗余计算问题，本文提出将安全认证作为答案复用的计算原语。通过在基于能力的系统中利用安全认证步骤计算出的不动点闭包，系统可以预先生成所有可达答案。研究引入了带有预答块的认证答案库（CAS），通过形式化包含检查实现亚毫秒级的查询响应，在保证安全性的同时显著提升了系统效率。
selection_source: fresh_fetch
motivation: 现有对话系统通常独立处理每一轮对话，导致即使答案可从先前状态推导，仍会进行冗余的检索或生成计算。
method: 提出一种基于能力闭包的安全认证范式，利用认证答案库（CAS）和预答块（PAB）在认证时预先实例化所有可能的后续答案及其来源证明。
result: 该方法通过形式化包含检查，使后续查询能在亚毫秒级时间内得到响应，有效消除了冗余的检索和生成开销。
conclusion: 将安全认证转化为计算原语，为任务型对话系统提供了一种兼顾安全性与高性能的答案复用新范式。
---

## 摘要
我们为任务型对话系统引入了一种新范式：将安全认证作为答案重用的计算原语。当前系统独立处理每一轮对话，即使答案可以从先前的状态推导出来，也会通过检索或生成重新计算。我们展示了在基于能力的系统中，安全认证步骤计算了一个不动点闭包 cl(At)，该闭包已经包含了从当前配置可达的所有答案。我们通过由预答案块（PAB）增强的认证答案库（CAS）来实现这一见解：在每个经过认证的轮次中，系统会物化所有可推导的后续答案以及最小的出处证明。后续查询通过形式化包含检查在亚毫秒级时间内得到回答，消除了冗余的检索和生成。

## Abstract
We introduce a new paradigm for task-oriented dialogue systems: safety certification as a computational primitive for answer reuse. Current systems treat each turn independently, recomputing answers via retrieval or generation even when they are already derivable from prior state. We show that in capability-based systems, the safety certification step computes a fixed-point closure cl(At) that already contains every answer reachable from the current configuration. We operationalize this insight with a Certified Answer Store (CAS) augmented by Pre-Answer Blocks (PAB): at each certified turn, the system materializes all derivable follow-up answers together with minimal provenance witnesses. Subsequent queries are answered in sub-millisecond time via formal containment checks, eliminating redundant retrieval and generation.