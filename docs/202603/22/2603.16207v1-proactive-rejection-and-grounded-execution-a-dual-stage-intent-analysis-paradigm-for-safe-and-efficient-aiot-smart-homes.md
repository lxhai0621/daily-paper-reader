---
title: "Proactive Rejection and Grounded Execution: A Dual-Stage Intent Analysis Paradigm for Safe and Efficient AIoT Smart Homes"
title_zh: 主动拒绝与落地执行：一种用于安全高效 AIoT 智能家居的双阶段意图分析范式
authors: "Xinxin Jin, Zhengwei Ni, Zhengguo Sheng, Victor C. M. Leung"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16207v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 具有双阶段意图分析的物联网具身智能体
tldr: 针对智能家居中大模型代理存在的指令不可靠和交互效率低的问题，本文提出双阶段意图感知（DS-IA）框架。该框架通过语义防火墙过滤无效指令，并利用确定性级联验证器确保物理执行的可行性。实验表明，该方法显著提升了指令匹配准确率和无效指令拒绝率，并在减少用户干扰的同时提高了任务自主完成率，实现了安全高效的AIoT交互。
selection_source: fresh_fetch
motivation: 现有大模型在智能家居场景中面临实体幻觉和交互频率困境，难以在鲁莽执行与过度询问之间取得平衡。
method: 提出DS-IA框架，将高层意图理解与底层物理执行分离，通过语义防火墙过滤指令并利用级联验证器进行物理可行性检查。
result: "在HomeBench上实现58.56%的精确匹配率，无效指令拒绝率达87.04%，并将自主成功率从42.86%提升至71.43%。"
conclusion: 该框架通过准确的环境感知和双阶段验证，有效减少了用户干扰并提升了智能家居系统的安全性和执行效率。
---

## 摘要
随着大语言模型（LLMs）从信息提供者转变为物联网（IoT）中的具身智能体，它们在可靠性和交互效率方面面临着重大挑战。直接执行 LLM 生成的指令通常会导致实体幻觉（例如，尝试控制不存在的设备）。同时，现有的迭代框架（如 SAGE）受困于“交互频率困境”，在鲁莽执行和过度询问用户之间摇摆不定。为了解决这些问题，我们提出了一种双阶段意图感知（DS-IA）框架。该框架将高层用户意图理解与底层物理执行相分离。具体而言，第一阶段充当语义防火墙，通过检查家居当前状态来过滤无效指令并解析模糊命令。第二阶段随后采用确定性级联验证器——一种严格的逐步规则检查器，按顺序验证房间、设备和功能——以确保动作在执行前在物理上确实可行。在 HomeBench 和 SAGE 基准测试上的广泛实验表明，DS-IA 实现了 58.56% 的精确匹配（EM）率（优于基准线 28% 以上），并将无效指令的拒绝率提高到 87.04%。在 SAGE 基准测试上的评估进一步揭示，DS-IA 通过平衡主动查询与基于状态的推理，解决了交互频率困境。具体来说，它将自主成功率（在无需不必要用户干预的情况下解决任务）从 42.86% 提升至 71.43%，同时在识别真正需要人工澄清的不可约模糊性方面保持了高精度。这些结果强调了该框架通过准确的环境落地来最大限度减少用户干扰的能力。

## Abstract
As Large Language Models (LLMs) transition from information providers to embodied agents in the Internet of Things (IoT), they face significant challenges regarding reliability and interaction efficiency. Direct execution of LLM-generated commands often leads to entity hallucinations (e.g., trying to control non-existent devices). Meanwhile, existing iterative frameworks (e.g., SAGE) suffer from the Interaction Frequency Dilemma, oscillating between reckless execution and excessive user questioning. To address these issues, we propose a Dual-Stage Intent-Aware (DS-IA) Framework. This framework separates high-level user intent understanding from low-level physical execution. Specifically, Stage 1 serves as a semantic firewall to filter out invalid instructions and resolve vague commands by checking the current state of the home. Stage 2 then employs a deterministic cascade verifier-a strict, step-by-step rule checker that verifies the room, device, and capability in sequence-to ensure the action is actually physically possible before execution. Extensive experiments on the HomeBench and SAGE benchmarks demonstrate that DS-IA achieves an Exact Match (EM) rate of 58.56% (outperforming baselines by over 28%) and improves the rejection rate of invalid instructions to 87.04%. Evaluations on the SAGE benchmark further reveal that DS-IA resolves the Interaction Frequency Dilemma by balancing proactive querying with state-based inference. Specifically, it boosts the Autonomous Success Rate (resolving tasks without unnecessary user intervention) from 42.86% to 71.43%, while maintaining high precision in identifying irreducible ambiguities that truly necessitate human clarification. These results underscore the framework's ability to minimize user disturbance through accurate environmental grounding.

---

## 论文详细总结（自动生成）

以下是对论文《Proactive Rejection and Grounded Execution: A Dual-Stage Intent Analysis Paradigm for Safe and Efficient AIoT Smart Homes》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：随着大语言模型（LLM）从信息提供者转向物联网（IoT）中的具身智能体（Embodied Agents），智能家居正从“问答”模式转向“指令-执行”模式。
*   **核心痛点**：
    1.  **实体幻觉（Entity Hallucination）**：LLM 容易生成控制不存在设备的指令（如在没有加湿器的房间尝试开启加湿器）。
    2.  **交互频率困境（Interaction Frequency Dilemma）**：现有框架（如 SAGE）在“鲁莽执行”和“过度询问用户”之间摇摆，要么导致安全风险，要么因频繁打扰用户而降低体验。
*   **研究动机**：现有的迭代式框架缺乏对指令模糊性的全局认知，导致在处理复杂或无效指令时出现“任务遗漏”或“强制幻觉”。

### 2. 方法论
论文提出了 **DS-IA（双阶段意图感知）框架**，核心思想是将高层意图理解与底层物理执行解耦：
*   **Stage 1：全局意图分析（语义防火墙）**：
    *   利用 LLM 的语义理解能力，对照当前环境快照（Environment Snapshot）对用户指令进行预检。
    *   将指令分类为：有效（Valid）、无效（Invalid，触发早期拒绝）或混合（Mixed）。
*   **Stage 2：分层落地验证（级联验证器）**：
    *   **三级级联检查**：依次验证房间存在性（VR）、设备关联性（VD）和功能支持度（VC）。
    *   **生成并过滤（Generate-and-Filter）策略**：针对混合意图，生成原始动作序列后，由验证器精确拦截不可执行的动作并替换为错误令牌（$\epsilon_{err}$），同时保留并执行有效动作，确保任务不遗漏。
*   **公式化表达**：引入物理落地约束函数 $\Phi(A, S_t)$，将推理目标从单纯的概率最大化修正为在物理可行性约束下的最优序列生成。

### 3. 实验设计
*   **数据集/场景**：
    1.  **HomeBench**：包含 100 个模拟环境和 2500 条指令，专门用于测试物理落地鲁棒性（含 38.6% 的无效指令）。
    2.  **SAGE Benchmark**：包含 50 个复杂任务，侧重于人机交互效率（如设备解析、个性化偏好、长程任务持久性）。
*   **对比方法**：
    *   **Standard Few-Shot**：原生 LLM 的 4-shot 提示词方法。
    *   **SAGE (Iterative ReAct)**：目前工业界最先进的迭代式“思考-行动”框架。
*   **评价指标**：精确匹配（EM）、F1-Score、自主成功率（无需询问用户）、澄清成功率（正确触发询问）。

### 4. 资源与算力
*   **模型使用**：
    *   在 HomeBench 测试中使用了 **Qwen-2.5-7B-Instruct**。
    *   在 SAGE 基准测试中使用了 **GPT-4o-mini**（为了与 SAGE 原论文环境保持一致，确保公平对比）。
*   **硬件环境**：使用单张 **NVIDIA RTX 5090** GPU，启用 BFloat16 精度和 SDPA 加速。
*   **训练时长**：文中未提及训练时长，因该框架主要基于 In-Context Learning 和推理时验证，无需大规模二次训练。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 2500 条 HomeBench 指令和 50 组 SAGE 复杂交互任务，并进行了 1000 组任务的消融实验。
*   **充分性**：实验覆盖了单设备、多设备、无效指令、混合指令等多种维度，分类细致（VS, VM, IS, IM, MM）。
*   **客观性**：通过在不同基准上切换模型（Qwen 和 GPT），验证了框架的通用性而非依赖特定模型的强大性能。消融实验对比了有无 Stage 1 的情况，量化了安全性和计算成本的权衡。

### 6. 主要结论与发现
*   **安全性大幅提升**：在无效指令拒绝率上，DS-IA 达到 **87.04%**，远高于基准线的 14.07%，有效遏制了实体幻觉。
*   **解决交互困境**：自主成功率从 **42.86% 提升至 71.43%**，意味着系统能通过环境感知自行解决更多模糊性，减少了对用户的干扰。
*   **执行精度**：整体精确匹配（EM）率达到 **58.56%**，优于所有对比基准 28% 以上。
*   **计算效率**：通过 Stage 1 的早期拒绝机制，减少了约 **18.1%** 的昂贵代码生成调用，实现了“以廉价的预填充换取昂贵的生成节省”。

### 7. 优点
*   **架构解耦**：创新的双阶段设计有效解决了 LLM 在具身控制中的“近视”问题。
*   **鲁棒的过滤机制**：Generate-and-Filter 策略解决了混合指令下“全有或全无”的执行失败问题。
*   **落地性强**：级联验证器提供了确定性的物理约束，是连接概率性 LLM 与确定性物理世界的有效桥梁。

### 8. 不足与局限
*   **感知维度单一**：目前主要依赖文本形式的环境元数据，尚未整合视觉等多模态感知（如无法处理“打开那个红色的灯”等视觉指代）。
*   **实时性挑战**：虽然减少了生成调用，但双阶段推理仍存在一定的延迟，对于极高实时性要求的场景可能存在瓶颈。
*   **动态环境同步**：论文假设环境快照是实时的，但在实际 IoT 场景中，传感器延迟可能导致快照与物理现实不同步，框架对此类容错处理讨论较少。

（完）
