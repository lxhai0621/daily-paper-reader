---
title: "AcceRL: A Distributed Asynchronous Reinforcement Learning and World Model Framework for Vision-Language-Action Models"
title_zh: AcceRL：一种用于视觉-语言-动作模型的分布式异步强化学习与世界模型框架
authors: "Chengxuan Lu, Shukuan Wang, Yanjie Li, Wei Liu, Shiji Jin, Fuyuan Qian, Peiming Li, Baigui Sun, Yang Liu"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18464v1"
tags: ["query:agent"]
score: 9.0
evidence: 用于 VLA 模型的分布式异步强化学习框架
tldr: AcceRL是一个针对大规模视觉-语言-动作（VLA）模型设计的分布式异步强化学习框架。它通过物理隔离训练、推理和采样过程，消除了同步障碍，并首次将可训练的世界模型集成到异步流水线中以生成虚拟经验。该框架在LIBERO基准测试中达到了SOTA水平，显著提升了硬件利用率、吞吐量和样本效率，为复杂控制任务提供了高效的训练方案。
selection_source: fresh_fetch
motivation: 旨在解决大规模VLA模型在强化学习过程中面临的计算效率低下、同步延迟以及数据获取困难等核心挑战。
method: 提出了一种完全异步且解耦的框架，通过物理隔离核心环节并集成即插即用的世界模型来生成虚拟经验。
result: 在LIBERO基准测试中取得SOTA性能，实现了吞吐量的超线性扩展，并展现出极高的样本效率和训练稳定性。
conclusion: AcceRL为大规模VLA模型的高效强化学习训练提供了一个高性能、可扩展且具备世界模型增强能力的系统级解决方案。
---

## 摘要
大规模视觉-语言-动作（VLA）模型的强化学习（RL）在计算效率和数据获取方面面临重大挑战。我们提出了 AcceRL，这是一个完全异步且解耦的强化学习框架，旨在通过物理隔离训练、推理和采样过程来消除同步障碍。至关重要的是，AcceRL 首次将即插即用、可训练的世界模型集成到分布式异步强化学习流水线中，以生成虚拟经验。在 LIBERO 基准测试上的实验表明，AcceRL 达到了当前最先进（SOTA）的性能。在系统层面，它在吞吐量上表现出超线性扩展，并具有极高的硬件利用率。在算法层面，世界模型增强变体在复杂控制任务中提供了前所未有的样本效率和稳健的训练稳定性。

## Abstract
Reinforcement learning (RL) for large-scale Vision-Language-Action (VLA) models faces significant challenges in computational efficiency and data acquisition. We propose AcceRL, a fully asynchronous and decoupled RL framework designed to eliminate synchronization barriers by physically isolating training, inference, and rollouts. Crucially, AcceRL is the first to integrate a plug-and-play, trainable world model into a distributed asynchronous RL pipeline to generate virtual experiences. Experiments on the LIBERO benchmark demonstrate that AcceRL achieves state-of-the-art (SOTA) performance. Systematically, it exhibits super-linear scaling in throughput and highly efficient hardware utilization. Algorithmically, the world-model-augmented variant delivers unprecedented sample efficiency and robust training stability in complex control tasks.

---

## 论文详细总结（自动生成）

### AcceRL：用于视觉-语言-动作（VLA）模型的分布式异步强化学习与世界模型框架论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：大规模视觉-语言-动作（VLA）模型（如 7B 参数量级）在应用强化学习（RL）时面临严重的**计算效率低下**和**数据获取困难**。
*   **研究背景**：
    *   **同步瓶颈**：传统 RL 框架采用同步机制（收集数据-更新参数），导致高性能 GPU 在等待缓慢的物理模拟器时处于闲置状态（长尾效应）。
    *   **数据饥渴**：VLA 模型需要海量交互数据，而物理模拟器的运行速度和推理延迟限制了采样频率。
    *   **策略滞后**：在异步架构中，行为策略与学习策略不一致会导致训练不稳定。

#### 2. 方法论
AcceRL 提出了一个完全解耦的异步架构，核心思想是将训练、推理和采样在物理上隔离。
*   **双层异步机制**：
    *   **宏观异步**：训练器（Trainer）与采样工人（Rollout Workers）解耦。采样工人持续将轨迹存入重放池，训练器并行采样更新，无需互相等待。
    *   **微观异步**：环境交互与模型推理解耦。采用“推理即服务”（Inference-as-a-Service）模式，通过动态窗口机制实现集中式批处理推理，最大化 GPU 利用率。
*   **可训练的世界模型（World Model）**：
    *   集成基于扩散模型（Diffusion-based）的环境动力学模型，实现“在想象中学习”。
    *   通过生成高保真虚拟经验，绕过物理模拟器的速度限制，将样本效率提升了 200 倍。
*   **算法优化**：
    *   **GIPO 算法**：使用高斯重要性采样策略优化，通过软阻尼代替 PPO 的硬截断，缓解异步带来的策略滞后问题。
    *   **价值重计算与全局优势归一化**：在更新前重新评估状态价值，确保优势信号的准确性。
    *   **VLA 适配**：包括词表瘦身（仅保留动作 Token）、动作感知注意力池化价值头、以及 Token 级别的策略优化。

#### 3. 实验设计
*   **基准测试（Benchmark）**：使用 **LIBERO** 机器人操作基准，涵盖 LIBERO-Spatial（空间）、Object（物体）、Goal（目标）和 Long（长程任务）四个子集。
*   **对比方法**：
    *   **OpenVLA-OFT**：基于模仿学习（BC）的基准。
    *   **SimpleVLA-RL** 与 **RLinf-VLA**：当前最先进的 VLA 强化学习微调框架。
*   **评估指标**：任务成功率、系统吞吐量（SPS，每秒样本数）、硬件利用率。

#### 4. 资源与算力
*   **硬件型号**：实验使用了 **NVIDIA H200 GPU**。
*   **规模**：
    *   训练器扩展测试中使用了 **1 到 7 块 GPU**。
    *   采样端扩展到了 **64 个 Rollout Workers**。
*   **训练细节**：利用 DeepSpeed ZeRO-2 优化器状态分区，以支持大规模参数模型在有限显存下的高效训练。

#### 5. 实验数量与充分性
*   **实验覆盖面**：
    *   在 LIBERO 的四个核心任务集上进行了性能对比。
    *   进行了详细的**系统扩展性实验**，验证了吞吐量随 GPU 数量增加的超线性增长。
    *   包含多组**消融实验**：验证了价值重计算（Value Recomputation）的必要性、GIPO 算法相对于 PPO 的稳定性优势。
*   **充分性评价**：实验设计较为全面，不仅关注算法指标（成功率），还深入探讨了分布式系统的工程效率，实验结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **性能领先**：AcceRL 在 LIBERO 所有类别中均达到 SOTA，尤其在长程任务（LIBERO-Long）中成功率达 99.1%，远超模仿学习基准（90.7%）。
*   **超线性扩展**：得益于 ZeRO 优化和异步解耦，系统吞吐量随硬件增加呈超线性增长，GPU 利用率保持在 94% 以上。
*   **世界模型威力**：引入世界模型后，仅需 10,000 次真实环境交互即可实现快速收敛，样本效率提升极其显著。
*   **稳定性**：GIPO 算法相比 PPO 在异步环境下表现出更强的鲁棒性，收敛速度提升约 7.5 倍。

#### 7. 优点与亮点
*   **系统级创新**：首次在分布式异步 RL 流水线中无缝集成可训练的世界模型，解决了 VLA 训练的“数据荒”。
*   **极致解耦**：通过物理隔离彻底消除了物理模拟器的速度瓶颈和 GPU 闲置问题。
*   **工程落地性强**：词表瘦身和 Token 级别优化等细节针对 VLA 模型的特性做了深度定制，具有很高的实用价值。

#### 8. 不足与局限
*   **模型支持范围**：目前主要针对 VLA 模型的动作微调，尚未支持大规模语言模型（LLM）的全量对齐训练。
*   **世界模型误差**：虽然采用了短时域（Short Horizon）采样来缓解，但扩散模型生成的虚拟经验仍可能存在累积误差，影响极复杂任务的精度。
*   **硬件门槛**：尽管优化了效率，但训练 7B 级别的 VLA 模型仍需昂贵的 H200 等顶级计算资源。

（完）
