---
title: "DeepStage: Learning Autonomous Defense Policies Against Multi-Stage APT Campaigns"
title_zh: DeepStage：学习针对多阶段 APT 攻击的自主防御策略
authors: "Trung V. Phan, Tri Gia Nguyen, Thomas Bauschert"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16969v1"
tags: ["query:agent"]
score: 9.0
evidence: 用于自主防御智能体的深度强化学习框架
tldr: DeepStage是一个针对高级持续性威胁（APT）的深度强化学习防御框架。它将企业环境建模为部分可观测马尔可夫决策过程，通过融合主机和网络遥测数据构建溯源图，并利用图神经网络与LSTM推断攻击阶段。该框架采用分层PPO算法自动执行监控、访问控制和修复等防御动作，在真实测试床中显著提升了防御的阶段感知能力和成本效益。
selection_source: fresh_fetch
motivation: 针对多阶段APT攻击，现有的防御手段缺乏对攻击阶段的自适应感知和自动化响应能力。
method: 结合图神经网络编码器与LSTM进行攻击阶段估计，并利用分层近端策略优化（PPO）算法在POMDP模型下学习最优防御策略。
result: "在CALDERA驱动的APT场景中，DeepStage达到了0.89的阶段加权F1分数，比风险感知DRL基准提高了21.9%。"
conclusion: DeepStage证明了结合阶段感知与深度强化学习可以实现高效且低成本的自主网络防御。
---

## 摘要
本文提出了 DeepStage，这是一个用于针对高级持续性威胁（APT）进行自适应、阶段感知防御的深度强化学习（DRL）框架。企业环境被建模为部分可观测马尔可夫决策过程（POMDP），其中主机溯源数据和网络遥测数据被融合为统一的溯源图。基于我们之前的研究工作 StageFinder，图神经编码器和基于 LSTM 的阶段估计器推断出与 MITRE ATT&CK 框架对齐的概率性攻击者阶段。这些阶段信念结合图嵌入，引导分层近端策略优化（PPO）智能体在监控、访问控制、遏制和修复等方面选择防御动作。在基于 CALDERA 驱动的 APT 剧本的真实企业测试平台中进行评估，DeepStage 实现了 0.89 的阶段加权 F1 分数，比风险感知 DRL 基准高出 21.9%。结果表明了有效的阶段感知和高成本效益的自主网络防御。

## Abstract
This paper presents DeepStage, a deep reinforcement learning (DRL) framework for adaptive, stage-aware defense against Advanced Persistent Threats (APTs). The enterprise environment is modeled as a partially observable Markov decision process (POMDP), where host provenance and network telemetry are fused into unified provenance graphs. Building on our prior work, StageFinder, a graph neural encoder and an LSTM-based stage estimator infer probabilistic attacker stages aligned with the MITRE ATT&CK framework. These stage beliefs, combined with graph embeddings, guide a hierarchical Proximal Policy Optimization (PPO) agent that selects defense actions across monitoring, access control, containment, and remediation. Evaluated in a realistic enterprise testbed using CALDERA-driven APT playbooks, DeepStage achieves a stage-weighted F1-score of 0.89, outperforming a risk-aware DRL baseline by 21.9%. The results demonstrate effective stage-aware and cost-efficient autonomous cyber defense.

---

## 论文详细总结（自动生成）

### 论文总结：DeepStage —— 针对多阶段 APT 攻击的学习型自主防御策略

#### 1. 核心问题与研究动机
*   **核心问题**：高级持续性威胁（APT）具有隐蔽性强、持续时间长、多阶段演进（如从侦察到数据外泄）等特点。传统的入侵检测系统（IDS）难以识别新型攻击，而现有的深度强化学习（DRL）防御方案通常依赖扁平化的特征表示，忽略了系统实体间的因果关系（溯源关系）以及攻击随时间演进的阶段性特征。
*   **研究背景**：APT 攻击在不同阶段产生的信号微弱且交织在正常活动中。为了实现有效的自主防御，需要一种能够感知攻击阶段、理解系统行为因果逻辑，并能在不确定性环境下做出自适应决策的机制。

#### 2. 方法论
DeepStage 框架将企业防御建模为**部分可观测马尔可夫决策过程（POMDP）**，核心流程如下：
*   **数据融合与图构建**：整合主机级日志（Auditd/Sysmon）和网络级告警（Zeek/Snort），构建“融合溯源图”。节点代表进程、文件、套接字或告警，边代表因果或数据流关系。
*   **状态表示学习**：
    *   **图编码（GNN）**：利用图神经网络将复杂的溯源图转化为低维嵌入向量 $g_t$。
    *   **阶段估计（LSTM）**：将嵌入序列输入 LSTM 模型，推断当前攻击处于 MITRE ATT&CK 框架中哪个阶段的概率分布 $p_t$。
*   **分层强化学习（Hierarchical PPO）**：
    *   **架构**：采用分层近端策略优化（PPO）算法。**元策略（Meta-policy）**根据阶段估计选择子策略；**子策略（Sub-policy）**在具体的动作空间（监控、访问控制、遏制、修复）中执行防御动作。
    *   **奖励函数设计**：引入阶段感知权重 $\alpha_k$ 和 $\beta_k$，公式为 $R = \alpha_k R_{security} - \beta_k \lambda C(a)$。在攻击后期（如数据外泄），提高安全收益权重并降低操作成本惩罚，鼓励更激进的防御。

#### 3. 实验设计
*   **实验场景**：构建了一个包含 6 台 Ubuntu 虚拟机（企业主机）和 1 台 Kali Linux（攻击者）的受控企业测试床，涵盖 LAN、DMZ、服务器区和管理区。
*   **攻击生成**：使用 **MITRE CALDERA** 自动化攻击模拟平台，执行 10 个基础剧本及其随机变体，涵盖从初始侵入到横向移动的全流程。
*   **基准方法（Benchmarks）**：
    1.  **Risk-Aware DRL**：基于静态攻击图（MulVAL）和风险最小化的传统 DRL 方法。
    2.  **DeepStage-Unaware**：DeepStage 的变体，取消了阶段感知权重（即所有阶段权重统一），用于消融实验。
*   **评估指标**：阶段加权 F1 分数、成本效益曲线、收敛速度、阶段转换响应速度。

#### 4. 资源与算力
*   **硬件设备**：使用了一台专用 GPU 工作站，配置为 **NVIDIA RTX 3060 GPU** 和 32 GB RAM，用于 DRL 训练和重度计算。
*   **虚拟机配置**：每台虚拟机分配 4 vCPU 和 8 GB RAM。
*   **训练规模**：在企业测试床中进行了 **2,000 个剧本周期（Episodes）** 的训练和微调。

#### 5. 实验数量与充分性
*   **实验规模**：通过 CALDERA 生成了大量标注样本，每个快照周期为 300 秒。实验涵盖了 APT 的 6 个主要阶段。
*   **充分性评价**：实验设计较为充分，包含了与现有先进方法的对比以及针对“阶段感知”模块的消融实验。通过随机化攻击剧本变体，增强了模型的泛化能力验证。
*   **客观性**：在受控的真实虚拟化环境中运行，数据采集自标准内核工具（CamFlow/Auditd），具有较高的客观性。

#### 6. 主要结论与发现
*   **防御效能显著提升**：DeepStage 实现了 **0.89 的阶段加权 F1 分数**，比风险感知 DRL 基准提高了 **21.9%**，比阶段不敏感版本提高了 11.3%。
*   **成本效益优化**：在相同的操作成本下，DeepStage 获得的安全收益最高。它能在早期阶段保持低成本监控，在检测到高置信度的后期威胁时迅速升级防御。
*   **收敛与响应更快**：DeepStage 的收敛速度比基准方法快约 **1.7 倍**（约 900 个周期即可稳定），且在攻击阶段转换时的响应延迟更低。

#### 7. 优点
*   **因果感知**：通过溯源图捕获了攻击的上下文因果关系，而非简单的统计特征。
*   **分层决策结构**：有效处理了复杂的防御动作空间，提高了策略的可解释性和学习效率。
*   **阶段感知奖励塑造**：巧妙地将网络安全领域的先验知识（不同阶段风险不同）融入强化学习的奖励机制中。

#### 8. 不足与局限
*   **网络规模受限**：实验环境仅包含 6 台主机，对于拥有成千上万节点的大型企业网络，其图处理的计算开销和策略的扩展性仍待验证。
*   **攻击多样性风险**：虽然使用了 CALDERA 变体，但仍属于“已知剧本”范畴，对于完全未知的“零日”攻击路径，其阶段估计的准确性可能下降。
*   **Sim-to-Real 差距**：实验在受控虚拟环境中进行，真实生产环境中的背景噪音（如复杂的业务流量）可能会对溯源图的纯净度和 GNN 编码产生干扰。

（完）
