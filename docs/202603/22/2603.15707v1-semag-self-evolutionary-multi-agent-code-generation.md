---
title: "SEMAG: Self-Evolutionary Multi-Agent Code Generation"
title_zh: SEMAG：自演化多智能体代码生成
authors: "Yulin Peng, Haowen Hou, Xinxin Zhu, Ying Tiffany He, F. Richard Yu"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15707v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 用于复杂任务的自我进化多智能体框架
tldr: SEMAG是一个自进化的多智能体代码生成框架，旨在解决现有方法依赖手动模型选择和固定工作流的局限性。它模仿人类编程实践，将任务分解为规划、编码、调试和讨论阶段，并能根据任务难度动态调整工作流。该框架支持智能体实时访问和升级最新模型，在CodeContests等基准测试中刷新了SOTA记录，展现了极强的适应性和性能。
selection_source: fresh_fetch
motivation: 现有的LLM代码生成方法受限于固定的工作流和手动模型选择，难以适应不断变化的任务复杂度和快速演进的模型能力。
method: 提出SEMAG框架，通过模拟人类编程阶段并引入自进化机制，实现工作流的动态调整和模型自动升级。
result: "SEMAG在CodeContests上比同类方法提升了3.3%，结合自进化模型选择后准确率达到52.6%，创下新的SOTA。"
conclusion: SEMAG证明了通过多智能体协作和自进化模型选择，可以显著提升LLM在复杂编程任务中的表现和适应性。
---

## 摘要
大语言模型（LLMs）在处理复杂编程任务方面取得了显著进展。然而，当前的方法依赖于手动模型选择和固定的工作流，这限制了它们适应不断变化的任务复杂性的能力。为了解决这一问题，我们提出了 SEMAG，一个模仿人类编码实践的自演化多智能体代码生成框架。它将编程任务分解为包括规划、编码、调试和讨论在内的多个阶段，同时根据任务难度调整工作流。其自演化智能体可以实时访问最新模型，并自动升级骨干模型。SEMAG 在各项基准测试中刷新了 Pass@1 准确率的最先进水平。在使用相同骨干模型的情况下，SEMAG 在 CodeContests 上的表现优于先前方法 3.3%。当结合能够自动识别最优骨干模型的自演化模型选择功能时，SEMAG 的准确率达到 52.6%，展示了该框架的有效性以及对不断演进的 LLM 能力的适应性。

## Abstract
Large Language Models (LLMs) have made significant progress in handling complex programming tasks. However, current methods rely on manual model selection and fixed workflows, which limit their ability to adapt to changing task complexities. To address this, we propose SEMAG, a Self-Evolutionary Multi-Agent code Generation framework that mimics human coding practices. It decomposes programming tasks into stages, including planning, coding, debugging, and discussion, while adapting workflows to task difficulty. Its self-evolutionary agents can access the latest models in real time and automatically upgrade the backbone model. SEMAG sets new state-of-the-art Pass@1 accuracy across benchmarks. Using identical backbone models, SEMAG outperforms prior methods by 3.3% on CodeContests. When augmented with self-evolutionary model selection that automatically identifies optimal backbones, SEMAG reaches 52.6%, showcasing both framework effectiveness and adaptability to evolving LLM capabilities.

---

## 论文详细总结（自动生成）

这是一份关于论文《SEMAG: Self-Evolutionary Multi-Agent Code Generation》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的代码生成框架存在三大局限：
    1.  **固定推理深度**：无论任务难易，工作流往往固定，导致简单任务浪费算力，复杂任务推理不足。
    2.  **调试陷入局部最优**：单次调试或缺乏协作的迭代容易让模型在错误思路上“打转”。
    3.  **模型选择滞后**：框架通常绑定特定模型（如 GPT-4），无法自动利用快速迭代的新模型（如 Claude 3.7 或 DeepSeek-v3）。
*   **研究背景**：随着 LLM 能力的演进，如何模仿人类“先规划、后编码、再调试、难点集体讨论”的动态过程，并实现骨干模型的自动升级，成为提升自动化软件开发效率的关键。

### 2. 方法论
SEMAG 提出了一个**分层多智能体框架**，核心包含以下关键技术：
*   **四层分级合成架构**：
    *   **Level 1 (直接生成)**：简单任务直接输出代码。
    *   **Level 2 (规划与验证)**：引入规划智能体生成方案，并由验证智能体通过 I/O 模拟进行校验。
    *   **Level 3 (追踪引导调试)**：利用 `EMBEDTRACE` 捕获运行时变量状态，结合解释和建议智能体进行精准修复。
    *   **Level 4 (多智能体协作讨论)**：当调试停滞时，启动多个辩论智能体提出不同方案，由判别智能体进行加权共识决策。
*   **自适应层级转换机制**：利用**执行追踪相似度（Normalized Edit Distance）**来判断当前层级是否陷入瓶颈。如果连续迭代的代码行为高度相似且未通过测试，则自动升级到更高难度的处理层级。
*   **自演化模型选择**：智能体通过搜索工具实时获取最新的模型评测、新闻和社区讨论，自动筛选并测试 3 个候选模型，通过加权投票选出当前任务的最优骨干模型。

### 3. 实验设计
*   **数据集**：涵盖了 7 个主流基准测试，分为两类：
    *   **基础类**：HumanEval, MBPP, 以及它们的扩展版 HumanEval-ET, MBPP-ET（含更多边缘测试用例）。
    *   **竞赛类**：APPS, LiveCode, CodeContests（难度最高）。
*   **对比方法**：包括 Direct Prompting、Self-Planning、MapCoder、LDB 以及之前的 SOTA 方法 **LPW**。
*   **骨干模型**：主要使用 GPT-3.5 和 GPT-4o 进行公平对比，并在自演化实验中引入了 Claude-3.7-Sonnet、GPT-4.1 和 DeepSeek-v3。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 训练资源。由于 SEMAG 是一个基于 API 的多智能体框架，其核心开销在于推理过程中的 Token 消耗。
*   **效率表现**：实验提到，通过分层策略，SEMAG 在简单任务上比 LPW 减少了约 15-20% 的 Token 消耗，在复杂任务上减少了约 5-9%。

### 5. 实验数量与充分性
*   **实验规模**：在 7 个数据集上进行了详尽测试，每个实验运行 3 次并计算标准差，确保了结果的统计显著性。
*   **消融实验**：针对规划验证、修复建议、讨论决策等组件进行了组合测试，验证了各模块的协同效应。
*   **参数分析**：对爬虫深度（Nlinks）、尝试次数（Mtry）、调试迭代（Mdebug）以及温度系数（Temperature）进行了网格搜索或敏感度分析。
*   **客观性**：通过使用相同的骨干模型（GPT-4o）与 SOTA 方法对比，证明了框架本身的优越性；通过自演化实验证明了其适应新模型的能力。

### 6. 主要结论与发现
*   **性能突破**：SEMAG 在所有基准测试中均达到 SOTA。在 HumanEval 上达到 **98.8%** 的 Pass@1，在 LiveCode 上达到 **65.0%**。
*   **复杂任务优势**：在最难的 CodeContests 上，使用相同模型时比 LPW 提升 3.3%；开启自演化选择（切换到 Claude 3.7）后，准确率从 38.0% 飙升至 **52.6%**。
*   **效率优化**：分层机制成功实现了“按需分配算力”，在提升准确率的同时降低了平均 Token 成本。

### 7. 优点与亮点
*   **动态性**：不再是死板的线性工作流，而是根据代码执行的“追踪指纹”动态决定是否加大投入。
*   **前瞻性**：自演化机制解决了“论文发布即过时”的问题，使框架具备了自动集成未来更强模型的能力。
*   **鲁棒性**：通过多智能体辩论和加权共识，有效克服了单模型在复杂逻辑下的思维定式。

### 8. 不足与局限
*   **实时性挑战**：自演化模块依赖网页搜索和实时总结，这会显著增加首个任务处理的延迟（实验显示约需 6 分钟进行模型筛选）。
*   **搜索偏差风险**：模型选择高度依赖搜索引擎的结果，如果网络信息存在偏差或新模型缺乏公开数据，可能导致选择次优模型。
*   **成本控制**：虽然分层机制节省了 Token，但在 Level 4 的多轮辩论中，Token 消耗依然较高，且超参数（如 Mtry）的设置对成本影响较大。

（完）
