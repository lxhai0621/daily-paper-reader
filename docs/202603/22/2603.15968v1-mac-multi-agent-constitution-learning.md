---
title: "MAC: Multi-Agent Constitution Learning"
title_zh: MAC：多智能体宪法学习
authors: "Rushil Thareja, Gautam Gupta, Francesco Pinto, Nils Lukas"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15968v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 使用智能体网络优化结构化提示词
tldr: "本研究针对宪法AI中规则编写依赖人工且现有提示优化器效率低下的问题，提出了多智能体宪法学习（MAC）框架。MAC通过专门的智能体网络对结构化规则集进行迭代优化，并引入MAC+通过成功轨迹训练进一步提升性能。实验表明，MAC在PII识别和工具调用任务中表现优异，不仅比现有提示优化方法提升超过50%，且在无需参数更新的情况下达到了与有监督微调和GRPO相当的性能，同时保持了高度的可解释性。"
selection_source: fresh_fetch
motivation: 现有的提示优化方法在自动学习宪法规则时面临标注数据需求大且缺乏结构化约束的问题，导致优化效果受限。
method: 提出MAC框架，利用多智能体协作对结构化规则进行编辑与审核，并结合MAC+通过强化学习机制优化更新轨迹。
result: "在PII标记等任务中，MAC的性能比主流提示优化方法提升了50%以上，且效果媲美SFT和GRPO等参数微调方法。"
conclusion: MAC证明了通过多智能体协作自动学习结构化宪法规则是实现高性能、可解释且无需微调的AI对齐的有效途径。
---

## 摘要
宪法式 AI (Constitutional AI) 是一种基于自然语言编写的规则集来监督和控制大语言模型 (LLM) 的方法。这些规则通常由人类专家编写，但原则上，只要有足够的针对目标行为的训练数据，就可以自动学习。现有的基于 LLM 的提示词优化器尝试实现这一点，但在学习“宪法”方面效果不佳，原因在于：(i) 它们需要大量的标注样本；(ii) 优化后的提示词缺乏结构，导致随着提示词长度增加，改进效果逐渐减弱。为了解决这些局限性，我们提出了多智能体宪法学习 (MAC)，它通过一个由具有专门任务（接受、编辑或拒绝规则更新）的智能体组成的网络，对以规则集形式表示的结构化提示词进行优化。我们还提出了 MAC+，它通过在成功轨迹上训练智能体来强化能够带来更高奖励的更新，从而提升性能。我们在个人身份信息 (PII) 标记任务上评估了 MAC，这是一个标签有限且可解释性至关重要的分类任务，并证明了其可以泛化到诸如工具调用等其他智能体任务。MAC 的表现优于近期的提示词优化方法 50% 以上，生成了人类可读且可审计的规则集，并在无需参数更新的情况下，实现了与监督微调和 GRPO 相当的性能。

## Abstract
Constitutional AI is a method to oversee and control LLMs based on a set of rules written in natural language. These rules are typically written by human experts, but could in principle be learned automatically given sufficient training data for the desired behavior. Existing LLM-based prompt optimizers attempt this but are ineffective at learning constitutions since (i) they require many labeled examples and (ii) lack structure in the optimized prompts, leading to diminishing improvements as prompt size grows. To address these limitations, we propose Multi-Agent Constitutional Learning (MAC), which optimizes over structured prompts represented as sets of rules using a network of agents with specialized tasks to accept, edit, or reject rule updates. We also present MAC+, which improves performance by training agents on successful trajectories to reinforce updates leading to higher reward. We evaluate MAC on tagging Personally Identifiable Information (PII), a classification task with limited labels where interpretability is critical, and demonstrate that it generalizes to other agentic tasks such as tool calling. MAC outperforms recent prompt optimization methods by over 50%, produces human-readable and auditable rule sets, and achieves performance comparable to supervised fine-tuning and GRPO without requiring parameter updates.

---

## 论文详细总结（自动生成）

以下是对论文《MAC: Multi-Agent Constitution Learning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：宪法式 AI（Constitutional AI）通过自然语言规则引导模型行为，具有高透明度和可审计性，适用于医疗、金融等严监管领域。然而，手动编写这些规则（即“宪法”）既昂贵又耗时。
*   **现有局限**：现有的自动提示词优化（APO）方法（如 GEPA、MIPRO）在学习宪法规则时存在两个主要缺陷：
    1.  **数据依赖性强**：通常需要大量标注数据。
    2.  **缺乏结构化约束**：生成的提示词往往是无结构的文本块，随着长度增加效果边际递减，且难以被人类审计。
*   **核心目标**：提出一种能够从少量示例中自动学习、优化并生成**结构化、可读性强**的自然语言规则集的方法。

### 2. 方法论：核心思想与技术细节
MAC 框架将宪法视为一组模块化的规则，通过一个专门的智能体网络进行迭代优化：
*   **核心架构（四个专门智能体）**：
    1.  **ANNOTATOR (标注者)**：根据当前宪法规则执行下游任务（如 PII 标记）。
    2.  **DECISION (决策者)**：分析错误模式（假阳性/假阴性），决定更新策略（添加、编辑或删除规则）。
    3.  **CREATOR (创建者)**：当决策为“添加”时，起草新的候选规则。
    4.  **EDITOR (编辑器)**：当决策为“编辑”时，针对性修改现有的特定规则。
*   **算法流程**：
    *   **迭代优化**：在训练批次上运行 ANNOTATOR，计算 F1 分数并提取错误样本。
    *   **提议与验证**：DECISION 引导 CREATOR/EDITOR 生成候选宪法。如果候选宪法在当前批次上的表现有所提升，则接受更新；否则重试或保留原版本。
    *   **验证集筛选**：定期在验证集上评估，保留性能最佳的宪法版本。
*   **扩展变体**：
    *   **MAC+**：利用大模型（14B）生成的优化轨迹（Trace），通过有监督微调（SFT）来增强小模型（3B）智能体的专门化能力。
    *   **reMAC**：在推理时引入检索增强，将训练集中的相似示例作为 Few-shot 加入提示词，补充抽象规则的不足。

### 3. 实验设计
*   **数据集与场景**：
    *   **PII 标记（核心任务）**：涵盖法律（ECHR）、医疗（MACCROBAT）和金融（PUPA）三个领域，侧重于小样本下的敏感信息识别。
    *   **工具调用（泛化任务）**：使用 BFCL 基准测试，验证 MAC 在智能体任务中的通用性。
*   **对比基准 (Benchmarks)**：
    *   **预训练标记器**：Microsoft Presidio (基于 SpaCy/BERT/Stanza)、Nvidia GLiNER-PII。
    *   **提示优化方法**：GEPA、MIPRO。
    *   **训练方法（上限参考）**：有监督微调 (SFT)、GRPO（强化学习）。
*   **实验模型**：Qwen 2.5 系列（3B, 7B, 14B）。

### 4. 资源与算力
*   **算力说明**：论文提到使用 **vLLM** 进行推理加速。在 MAC+ 阶段，使用了 **LoRA**（rank=64, alpha=128）对 3B 模型进行微调。
*   **具体细节**：文中**未明确说明**具体的 GPU 型号、数量或总训练时长。但给出了优化预算：所有方法统一限制为 480 次前向传播（3 个 Epoch，每个 Epoch 20 个 Batch，Batch Size 为 8）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   跨越了 3 个不同领域的数据集。
    *   测试了 3 种不同参数规模的模型（3B, 7B, 14B）。
    *   进行了消融实验（对比 MAC、reMAC 和 MAC+）。
    *   包含了一项跨任务的泛化实验（工具调用）。
*   **公平性与客观性**：实验通过严格限制“计算预算”（前向传播次数一致）来确保提示优化器之间的公平对比。同时，MAC+ 的训练数据采用了“跨领域”策略（在医疗/金融上训练，在法律上测试），有效证明了其学习的是“任务结构”而非单纯的“数据记忆”。

### 6. 主要结论与发现
*   **性能卓越**：MAC 在 PII 标记任务上比现有的提示优化方法（GEPA/MIPRO）平均提升超过 **50%**。
*   **媲美微调**：在 3B 模型上，MAC+ 的性能（59.02 F1）甚至超过了直接进行有监督微调（56.59 F1）和 GRPO（39.68 F1）。
*   **规模效应**：MAC 对小模型的提升尤为显著。随着模型规模增大，虽然差距缩小，但 MAC 依然保持领先。
*   **可解释性**：MAC 生成的规则集平均仅约 9 条，长度适中且人类完全可读，远优于 GEPA 生成的冗长提示词。

### 7. 优点
*   **模块化与结构化**：将提示词优化分解为独立的规则操作，使得优化过程更稳定，结果更易审计。
*   **隐私友好**：无需对模型权重进行针对敏感数据的微调，通过推理侧的规则控制行为，降低了数据泄露风险。
*   **智能体协作**：通过任务分解，使低算力的小模型也能通过专门化分工完成复杂的优化任务。

### 8. 不足与局限
*   **规则冲突风险**：随着规则数量增加，不同规则之间可能产生冲突或副作用，论文未深入探讨规则间的层级管理。
*   **指标依赖性**：MAC 的优化高度依赖于验证指标（如 F1）的准确性。如果指标本身存在偏差或噪声，学习到的规则可能会过拟合于错误的指标。
*   **审计成本**：虽然规则是可读的，但当规则集变得非常庞大时，人类审计的负担依然存在（尽管比黑盒提示词好得多）。
*   **上下文限制**：如果规则过多，可能会超出小模型的上下文窗口限制，影响推理效率。

（完）
