---
title: Internalizing Agency from Reflective Experience
title_zh: 从反思性经验中内化代理能力
authors: "Rui Ge, Yichao Fu, Yuyang Qian, Junda Su, Yiming Zhao, Peng Zhao, Hao Zhang"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16843v1"
tags: ["query:agent"]
score: 9.0
evidence: 在长程设置中从反思经验中学习基于反馈的代理能力
tldr: 针对大语言模型智能体在长程交互中难以有效利用环境反馈的问题，本文提出LEAFE框架。该方法通过将环境反馈转化为可操作的经验，引导模型在失败点回溯并探索修正路径，最后通过监督微调将这些反思性经验内化。实验表明，LEAFE在交互式编程和智能体任务中显著提升了模型的恢复能力和成功率，优于传统的强化学习基线。
selection_source: fresh_fetch
motivation: 现有的结果驱动型训练方法过度关注最终成功信号，忽视了丰富的环境反馈，导致模型难以在复杂长程任务中实现有效的错误恢复。
method: 提出LEAFE框架，通过总结反馈为经验、回溯决策点并探索修正分支，最后将这些经验引导的修正行为蒸馏到模型中。
result: "在多项交互式编程和智能体任务中，LEAFE不仅提升了Pass@1，还在Pass@k指标上显著超越了GRPO等强化学习基线。"
conclusion: 通过内化反思性经验，模型能够学习到更强的反馈驱动型代理能力，从而在有限的交互预算下更有效地解决复杂问题。
---

## 摘要
大语言模型越来越多地被部署为自主代理，必须通过与提供丰富反馈的环境进行长程交互，来执行规划、行动并从错误中恢复。然而，主流的结果驱动型后训练方法（例如带有可验证奖励的强化学习）主要优化最终的成功信号，导致丰富的环境反馈未被充分利用。因此，这些方法通常会导致分布收缩：策略变得更擅长复现一小部分已经成功的行为，却未能提升在长程场景中扩展问题解决能力（如 Pass@k）所需的基于反馈的代理能力。为了解决这一问题，我们提出了 LEAFE（从反思性经验中学习基于反馈的代理能力），这是一个从反思性经验中内化恢复代理能力的框架。具体而言，在探索过程中，代理将环境反馈总结为可操作的经验，回溯到早期的决策点，并使用修正后的动作探索替代分支。随后，我们通过监督微调将这些经验引导的修正蒸馏到模型中，使策略能够在未来的交互中更有效地恢复。在固定交互预算下的一系列多样化交互式编程和代理任务中，LEAFE 在 Pass@1 上始终优于基础模型，并且比结果驱动的基准方法（如 GRPO）以及基于经验的方法（如 Early Experience）实现了更高的 Pass@k，在 Pass@128 上提升高达 14%。

## Abstract
Large language models are increasingly deployed as autonomous agents that must plan, act, and recover from mistakes through long-horizon interaction with environments that provide rich feedback. However, prevailing outcome-driven post-training methods (e.g., RL with verifiable rewards) primarily optimize final success signals, leaving rich environment feedback underutilized. Consequently, they often lead to distribution sharpening: the policy becomes better at reproducing a narrow set of already-successful behaviors, while failing to improve the feedback-grounded agency needed to expand problem-solving capacity (e.g., Pass@k) in long-horizon settings.   To address this, we propose LEAFE (Learning Feedback-Grounded Agency from Reflective Experience), a framework that internalizes recovery agency from reflective experience. Specifically, during exploration, the agent summarizes environment feedback into actionable experience, backtracks to earlier decision points, and explores alternative branches with revised actions. We then distill these experience-guided corrections into the model through supervised fine-tuning, enabling the policy to recover more effectively in future interactions. Across a diverse set of interactive coding and agentic tasks under fixed interaction budgets, LEAFE consistently improves Pass@1 over the base model and achieves higher Pass@k than outcome-driven baselines (GRPO) and experience-based methods such as Early Experience, with gains of up to 14% on Pass@128.

---

## 论文详细总结（自动生成）

这是一份关于论文《Internalizing Agency from Reflective Experience》的结构化深度分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的核心问题是：**如何让大语言模型（LLM）智能体在长程交互任务中具备真正的“代理能力（Agency）”，即利用环境反馈进行自我纠错和恢复的能力。**

*   **研究背景**：目前的后训练主流方法（如带有可验证奖励的强化学习 RLVR，代表算法为 GRPO）主要依赖最终的成功/失败信号（Outcome-driven）。
*   **研究动机**：作者发现，仅靠结果驱动的训练会导致“分布收缩（Distribution Sharpening）”——模型只是提高了复现已有成功路径的概率，但在面对未曾见过的错误时，依然缺乏利用反馈进行修复的能力。这导致模型在 Pass@1 上有所提升，但在衡量能力上限的 Pass@k（大样本下的成功率）上表现乏力。
*   **核心目标**：将“反思-回溯-修正”这一过程内化（Internalize）到模型参数中，使其在不依赖外部搜索算法的情况下，提升在复杂环境中的生存和纠错能力。

### 2. 方法论：LEAFE 框架
LEAFE（Learning Feedback-Grounded Agency from Reflective Experience）分为两个阶段：

*   **阶段 1：带回溯的树状经验生成（Tree-Based Experience Generation with Rollback）**
    *   **定期反思**：智能体在交互过程中，每隔 $K$ 步或遇到失败时进行反思。
    *   **识别与总结**：模型识别出导致失败的关键决策点 $\tau$，并生成一段自然语言形式的“经验总结 $e$”（包含诊断和改进建议）。
    *   **回溯分支**：环境重置到 $\tau$ 点，模型在经验 $e$ 的引导下尝试新的动作分支，从而生成“失败 $\rightarrow$ 反思 $\rightarrow$ 回溯 $\rightarrow$ 修正 $\rightarrow$ 成功”的轨迹。
*   **阶段 2：经验蒸馏（Experience Distillation）**
    *   **行为复述（Behavior Rehearsal）**：对所有成功的轨迹进行标准的监督微调（SFT），以保持基础任务能力。
    *   **经验到策略的蒸馏（Experience-to-Policy Distillation）**：这是核心创新。将阶段 1 中“在经验 $e$ 引导下做出的正确动作”作为目标，但在输入端**去掉**显式的经验 $e$。
    *   **目标函数**：通过这种反事实训练，模型学会了在原始上下文（无外部提示）下直接做出原本需要反思后才能做出的修正动作，从而将纠错能力内化。

### 3. 实验设计
*   **数据集/场景**：涵盖了五个具有挑战性的交互式基准：
    1.  **WebShop**：在线购物导航。
    2.  **ALFWorld**：具身智能家庭任务。
    3.  **ScienceWorld**：科学实验模拟。
    4.  **Sokoban**：推箱子逻辑规划。
    5.  **CodeContests**：竞赛级编程（带执行反馈）。
*   **对比方法（Baselines）**：
    *   **Base**：未经特定任务微调的指令模型。
    *   **GRPO-RLVR**：目前最先进的结果驱动强化学习方法。
    *   **EarlyExp**：一种通过隐式世界建模学习早期经验的方法。
    *   **ACE**：一种基于 Prompt 和外部“剧本”存储经验的非训练方法。
*   **评估指标**：重点对比 Pass@1（单次成功率）和 Pass@128（探索能力上限）。

### 4. 资源与算力
*   **模型规模**：使用了 Qwen2.5 (7B, 14B, 32B, 72B) 和 Llama-3/3.1 (8B, 70B) 系列。
*   **训练细节**：
    *   Batch size 为 128，学习率为 $1 \times 10^{-6}$。
    *   训练轮数为 2-3 个 Epoch。
    *   **算力说明**：论文**未明确列出**具体的 GPU 型号（如 H100/A100）数量及总训练时长。但从其使用的模型规模（最高 72B）和 VRL 框架来看，属于中大规模算力消耗。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个不同领域的任务上进行了验证，每项任务都对比了多种模型架构。
*   **消融实验**：
    *   对比了不同采样策略（独立采样 vs 迭代细化 vs LEAFE 回溯）。
    *   验证了蒸馏损失函数（$L_{cf}$）的有效性。
    *   进行了模型规模缩放（Scaling）分析。
    *   **OOD 泛化测试**：在 CodeContests 上训练，在 MBPP 上测试，验证了模型学习的是通用代理能力而非过拟合。
*   **客观性**：实验设计较为公平，对比了同等交互预算下的表现，且 Pass@k 曲线能直观反映模型能力边界的扩展。

### 6. 主要结论与发现
*   **内化优于外挂**：LEAFE 成功将复杂的纠错逻辑转化为了模型的内在直觉，显著提升了 Pass@128。
*   **突破能力瓶颈**：相比 GRPO，LEAFE 在长程任务中的 Pass@k 提升更明显（如 CodeContests 提升 14%），证明了结果驱动 RL 在扩展模型能力边界方面的局限性。
*   **样本效率**：LEAFE 在达到相同准确率时所需的采样次数更少，具有更高的推理效率。
*   **协同效应**：LEAFE 可以与 GRPO 结合，进一步提升 Pass@1 的表现。

### 7. 优点与亮点
*   **思路新颖**：区分了“分布收缩”与“代理能力内化”，为智能体训练提供了新视角。
*   **结构化探索**：通过回溯机制（Rollback）解决了长程任务中正向奖励稀疏的问题，使模型能从失败中精准学习。
*   **实用性强**：训练后的模型在推理时不需要额外的反思步骤或复杂的 Prompt，降低了部署延迟。

### 8. 不足与局限
*   **反馈依赖**：LEAFE 的效果高度依赖于环境能否提供清晰、可诊断的反馈。如果环境反馈模糊（如仅有失败/成功），反思阶段将难以生成有效的经验。
*   **环境可重置性**：该方法假设环境可以随时回溯到任意历史状态，这在非确定性或无法重置的现实世界场景中难以实现。
*   **计算开销**：阶段 1 的树状探索需要大量的环境交互，虽然提升了最终模型能力，但训练阶段的交互成本较高。

（完）
