---
title: Financial Transaction Retrieval and Contextual Evidence for Knowledge-Grounded Reasoning
title_zh: 金融交易检索与基于知识推理的上下文证据
authors: "Artem Sakhno, Daniil Tomilov, Yuliana Shakhvalieva, Inessa Fedorova, Daria Ruzanova, Omar Zoloev, Andrey Savchenko, Maksim Makarenko"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15459v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 检索优先架构与行为知识库
tldr: 针对通用大模型在处理时间分布的金融交易数据时表现不佳的问题，本文提出 FinTRACE 架构。该架构采用检索优先策略，将原始交易转换为可复用的特征表示，并结合规则检测器构建行为知识库。通过指令微调，FinTRACE 显著提升了低监督环境下的交易分析性能，在流失预测等任务上实现了零样本性能翻倍，为金融领域的大模型推理提供了有效支撑。
selection_source: fresh_fetch
motivation: 通用大语言模型难以有效处理时间分布的表格化金融交易数据，且传统专用模型对标注数据依赖性强且迁移性差。
method: 提出 FinTRACE 架构，通过将交易转化为特征并利用规则检测器构建行为知识库，结合检索增强和指令微调来增强大模型的推理能力。
result: 在流失预测任务中，FinTRACE 将零样本 MCC 从 0.19 提升至 0.38，并在 16 样本设置下达到 0.40，刷新了交易分析任务的 SOTA 记录。
conclusion: FinTRACE 证明了通过检索增强的行为模式和知识库支撑，可以显著提升大模型在金融交易分析中的推理精度和少样本学习能力。
---

## 摘要
如今，金融机构的成功在很大程度上取决于其处理客户生成的数字足迹（例如从各种来源收集的交易历史）以改进用户建模流水线的能力。由于通用大语言模型（LLMs）在处理时间分布的表格数据方面存在困难，生产技术栈仍依赖于迁移能力有限且需要标注数据的专用表格和序列模型。为解决这一问题，我们推出了 FinTRACE，这是一种检索优先的架构，它将原始交易转换为可重用的特征表示，应用基于规则的检测器，并将生成的信号存储在行为知识库中，该知识库与下游任务的目标具有分级关联。在公开和工业基准测试中，FinTRACE 显著提升了低监督交易分析能力，将流失预测任务的零样本（zero-shot）马修斯相关系数（MCC）从 0.19 翻倍至 0.38，并将 16 样本（16-shot）MCC 从 0.25 提升至 0.40。我们进一步利用 FinTRACE，通过对检索到的行为模式进行指令微调来增强 LLMs 的知识对齐，在交易分析问题上取得了最先进的 LLM 结果。

## Abstract
Nowadays, success of financial organizations heavily depends on their ability to process digital traces generated by their clients, e.g., transaction histories, gathered from various sources to improve user modeling pipelines. As general-purpose LLMs struggle with time-distributed tabular data, production stacks still depend on specialized tabular and sequence models with limited transferability and need for labeled data. To address this, we introduce FinTRACE, a retrieval-first architecture that converts raw transactions into reusable feature representations, applies rule-based detectors, and stores the resulting signals in a behavioral knowledge base with graded associations to the objectives of downstream tasks. Across public and industrial benchmarks, FinTRACE substantially improves low-supervision transaction analytics, doubling zero-shot MCC on churn prediction performance from 0.19 to 0.38 and improving 16-shot MCC from 0.25 to 0.40. We further use FinTRACE to ground LLMs via instruction tuning on retrieved behavioral patterns, achieving state-of-the-art LLM results on transaction analytics problems.

---

## 论文详细总结（自动生成）

这是一份关于论文《Financial Transaction Retrieval and Contextual Evidence for Knowledge-Grounded Reasoning》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：通用大语言模型（LLMs）在处理金融交易数据时表现不佳。这类数据具有长序列、时间分布不均、属性异构（商户ID、金额、时间戳）且缺乏自然语言语义等特点。
*   **研究动机**：
    *   **泛化鸿沟**：现有的 LLM 在零样本或少样本设置下难以捕捉交易行为；而专用的表格模型（如 CatBoost）或序列模型（如 CoLES）虽然在全监督下表现优异，但依赖大量标注数据且迁移性差。
    *   **证据碎片化**：生产环境中的金融分析往往被拆分为多个独立的流水线，导致行为证据无法跨任务复用。
*   **整体含义**：本文旨在通过构建一个“检索优先”的架构，将原始交易日志转化为结构化的行为知识库，从而为 LLM 提供可解释的推理证据，弥合通用模型与专用模型之间的性能差距。

### 2. 方法论：FinTRACE 架构
FinTRACE（金融交易检索与上下文引擎）的核心思想是不直接将原始交易行序列化为文本，而是构建一个三层语义结构的**行为知识库（KB）**：
*   **特征精华层 (Feature Essences)**：利用 LATTE 框架提取任务无关的数值描述符（如交易频率、金额波动等）。
*   **行为模式层 (Behavioral Patterns)**：通过 LLM 引导，将特征归类为时间动态、货币行为和商户分布三大类，并提炼出高层概念（如“财务稳定性”）。
*   **白盒规则提取**：使用 AutoWoE 工具建立特征与行为/目标之间的显式映射，生成人类可读的模糊规则（例如：“若活跃天数 ≤ 70.5 → 强流失信号”）。
*   **推理与微调流程**：
    *   **检索推理**：在推理时，从 KB 中检索与当前用户最相关的行为事实和规则，构建结构化 Prompt。
    *   **指令微调**：将 KB 中的三元组（指令、上下文、响应）转化为合成数据集，对 LLM 进行指令微调，使其内化金融推理模式。
    *   **自反思机制 (Reflection Tuning)**：在少样本场景下，通过存储预测失败/成功的案例进行推理修正。

### 3. 实验设计
*   **数据集**：
    1.  **Gender Prediction**：人口统计分类基准（8.4K 用户）。
    2.  **Rosbank**：流失预测任务（10K 用户）。
    3.  **DataFusion**：大规模流失预测基准（96K 用户，取样 19.6K）。
    4.  **Proprietary Dataset**：某大型银行的私有数据集（10K 用户），用于个性化决策任务。
*   **Benchmark 与对比方法**：
    *   **LLM 基准**：GPT-OSS (120B), Llama-3-8B-Instruct。
    *   **专用模型**：TabPFN, CatBoost, CoLES, LLM4ES。
    *   **对比策略**：零样本/少样本推理、TabLLM（表格序列化）、KNN 检索等。

### 4. 资源与算力
*   **硬件环境**：实验运行在一个包含 **4 张 NVIDIA A100 GPU** 的集群上。
*   **模型规模**：使用了 gpt-oss-120b 进行零样本/少样本评估，使用 Llama-3-8B-Instruct 进行指令微调。
*   **训练时长**：文中未明确给出具体的训练总时长，但提到了微调过程遵循标准的合成指令微调流水线。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个公开数据集和 1 个工业私有数据集，测试了从 0-shot、16-shot 到全监督微调的多个维度。
*   **消融实验**：
    *   对比了不同的行为模式选择策略（随机 vs LLM 引导）。
    *   对比了不同的上下文构建策略（简单统计量 vs 白盒规则）。
    *   分析了反思机制（Reflection）在不同样本量下的增益。
*   **充分性评价**：实验设计较为全面，通过跨数据集对比和消融分析证明了 FinTRACE 各组件的有效性。引入私有工业数据增加了结果的现实说服力。

### 6. 主要结论与发现
*   **性能翻倍**：在 Rosbank 流失预测中，FinTRACE 将零样本 MCC 从 0.19 提升至 0.38。
*   **缩小差距**：经过 FinTRACE 指令微调的 LLM 在 Rosbank 上达到 0.48 MCC，追平了最先进的专用序列模型（CoLES），同时保留了通用的 MMLU 语言能力。
*   **规则的重要性**：消融实验显示，显式的“白盒规则”是性能提升的核心，单纯提供统计摘要（如分位数）对 LLM 帮助有限。

### 7. 优点
*   **可解释性**：通过白盒规则提供证据链，使 LLM 的金融决策过程可追踪、可审计。
*   **低监督友好**：在标注数据极度匮乏（零样本/少样本）的情况下表现极强。
*   **知识复用**：构建的知识库是任务无关的，可以轻松迁移到新的金融分析目标中。

### 8. 不足与局限
*   **依赖预处理**：FinTRACE 的效果高度依赖于底层特征工程（如 LATTE 框架）的质量。
*   **计算开销**：虽然推理时比原始交易序列更高效，但构建知识库和提取白盒规则需要额外的前期计算。
*   **领域局限**：目前仅在金融交易数据上验证，尚未证明其在其他类型的结构化事件序列（如医疗记录或日志分析）中的通用性。

（完）
