---
title: Who Tests the Testers? Systematic Enumeration and Coverage Audit of LLM Agent Tool Call Safety
title_zh: 谁来测试测试者？大语言模型智能体工具调用安全性的系统化枚举与覆盖率审计
authors: "Xuan Chen, Lu Yan, Ruqi Zhang, Xiangyu Zhang"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18245v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: LLM智能体工具调用安全与交互模式审计
tldr: "本研究针对 LLM 智能体工具调用安全评估的完整性问题，提出了 SafeAudit 元审计框架。该框架利用 LLM 枚举器系统生成多样化的工具调用工作流和用户场景，并引入“规则抗性”指标来量化现有基准的覆盖缺陷。实验表明，SafeAudit 在多个基准中发现了超过 20% 的隐藏不安全行为，揭示了现有安全测试的局限性，强调了元审计在智能体安全保障中的必要性。"
selection_source: fresh_fetch
motivation: 现有的 LLM 智能体安全基准可能存在覆盖不全的问题，无法识别所有潜在的危险工具调用交互模式。
method: 提出 SafeAudit 框架，通过 LLM 自动枚举测试用例并利用规则抗性指标识别现有安全规则下的未覆盖漏洞。
result: "在 3 个主流基准和 12 个环境中检测到超过 20% 的残余不安全行为，且覆盖率随测试预算增加而提升。"
conclusion: 现有安全评估体系存在显著漏洞，元审计应作为基准测试的必要补充，以提升智能体工具调用的安全性。
---

## 摘要
大语言模型（LLM）智能体越来越多地通过外部工具执行操作，这使得它们的安全性取决于工具调用工作流，而不仅仅是文本生成。尽管最近的基准测试在不同环境和风险类别中对智能体进行了评估，但一个基本问题仍未得到解答：现有的测试套件有多完整？在智能体通过基准测试后，仍有哪些不安全的交互模式存在？我们提出了 SafeAudit，这是一个通过两项贡献填补这一空白的元审计（meta-audit）框架。首先，一个基于 LLM 的枚举器，通过枚举有效的工具调用工作流和多样的用户场景来系统地生成测试用例。其次，我们引入了“规则抗性”（rule-resistance），这是一种非语义的定量指标，它从现有基准测试中提取紧凑的安全规则，并识别在这些规则下仍未被覆盖的不安全交互模式。在 3 个基准测试和 12 个环境中，SafeAudit 发现了现有基准测试未能暴露的超过 20% 的残留不安全行为，且覆盖率随着测试预算的增加而单调增长。我们的结果突显了当前安全性评估中存在的显著完整性差距，并证明了元审计作为基于基准测试的智能体安全性测试的必要补充。

## Abstract
Large Language Model (LLM) agents increasingly act through external tools, making their safety contingent on tool-call workflows rather than text generation alone. While recent benchmarks evaluate agents across diverse environments and risk categories, a fundamental question remains unanswered: how complete are existing test suites, and what unsafe interaction patterns persist even after an agent passes the benchmark? We propose SafeAudit, a meta-audit framework that addresses this gap through two contributions. First, an LLM-based enumerator that systematically generates test cases by enumerating valid tool-call workflows and diverse user scenarios. Second, we introduce rule-resistance, a non-semantic, quantitative metric that distills compact safety rules from existing benchmarks and identifies unsafe interaction patterns that remain uncovered under those rules. Across 3 benchmarks and 12 environments, SafeAudit uncovers more than 20% residual unsafe behaviors that existing benchmarks fail to expose, with coverage growing monotonically as the testing budget increases. Our results highlight significant completeness gaps in current safety evaluation and motivate meta-auditing as a necessary complement to benchmark-based agent safety testing.

---

## 论文详细总结（自动生成）

### 论文总结：Who Tests the Testers? LLM 智能体工具调用安全性的系统化枚举与覆盖率审计

#### 1. 核心问题与整体含义（研究动机和背景）
随着 LLM 智能体（Agents）在电子邮件、日程管理和网络操作等现实场景中的广泛应用，其安全性评估变得至关重要。现有的安全基准测试（Benchmarks）虽然涵盖了多种环境和风险类别，但存在一个核心缺陷：**缺乏完整性（Completeness）评估**。
*   **研究动机**：即使智能体通过了现有基准测试，在稍微改变的用户场景或复杂的工具调用链中，仍可能表现出不安全行为。
*   **核心问题**：现有的测试套件是否足够全面？在智能体通过基准测试后，还有哪些不安全的交互模式未被发现？如何量化这种“未覆盖”的安全风险？

#### 2. 方法论：SafeAudit 框架
论文提出了 **SafeAudit**，一个用于发现基准测试盲点并评估其完整性的元审计（Meta-audit）框架。其核心由两个阶段组成：

*   **阶段 1：基于 LLM 的测试用例枚举器（Enumerator）**
    *   **结构化枚举**：不直接生成测试用例，而是先生成高层级的“不安全交互模式”。这包括：(1) **工具调用链**（如搜索->提取->执行），识别在查找、执行或验证步骤中可能出现的结构性故障；(2) **多样化用户场景**（如不同职业背景、不同程度的授权委托）。
    *   **实例化**：将抽象模式转换为具体的、可执行的测试用例，确保其符合环境约束且具有现实意义。

*   **阶段 2：规则抗性（Rule-resistance）评估协议**
    *   **规则提取**：从现有基准测试中提取紧凑的安全规则（如“必须验证最终转账金额”），并将其转化为系统提示词（System Prompt）中的约束。
    *   **量化指标**：如果一个新生成的测试用例在应用了从基准测试中提取的所有安全规则后，依然能诱导智能体产生不安全行为，则该用例具有“规则抗性”。
    *   **未覆盖率（Uncovered Rate, UR）**：定义为在规则约束下仍表现不安全的用例比例，以此衡量基准测试的局限性。

#### 3. 实验设计
*   **数据集/基准测试**：选择了三个具有代表性的基准：
    1.  **Agent-SafetyBench (ASB)**：涵盖日常任务。
    2.  **AgentHarm**：关注有害工具的使用。
    3.  **ToolSafety**：研究外部 API 误用导致的故障。
*   **实验场景**：跨越 12 个环境（如电子邮件、医疗、旅游、金融、电子商务等）。
*   **目标智能体（Backbone LLMs）**：评估了 8 种模型，包括 GPT-4o-mini、Claude-3.5-haiku、Llama-3.1-8b 以及更强的 GPT-5、DeepSeek-R1、Qwen3-32B 等。
*   **对比方法**：
    *   **DirectGen**：直接提示 LLM 生成不安全模式。
    *   **SIRAJ**：基于固定风险类别的重构方法。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件资源（如 GPU 型号、数量或总训练时长）。
*   **模型使用**：实验主要依赖于 API 调用（OpenAI, Anthropic）和开源模型推理。生成测试用例使用了 GPT-5-mini（预估为 GPT-4o 级别的模型），评估使用了微调后的 Qwen2.5-7B 作为安全裁判。

#### 5. 实验数量与充分性
*   **实验规模**：在 12 个环境中，每种方法生成了 500 个测试用例，总计进行了数千次智能体交互实验。
*   **充分性分析**：
    *   **多模型验证**：涵盖了从轻量级到最先进的多种 LLM。
    *   **消融与敏感性测试**：研究了测试预算（用例数量）对覆盖率的影响、规则顺序的影响、以及裁判模型（Judge）的鲁棒性。
    *   **效用权衡**：验证了加入安全规则后，智能体在执行正常任务时的成功率（Utility）是否下降。
*   **客观性**：通过引入“规则抗性”这一非语义指标，减少了人工评估的主观性，实验设计较为公平。

#### 6. 主要结论与发现
*   **基准测试存在显著盲点**：SafeAudit 发现了现有基准测试未能暴露的 **20% 以上** 的残余不安全行为。
*   **覆盖率随预算增长**：随着测试用例生成预算的增加，未覆盖率呈现单调增长趋势，说明系统化枚举能持续发现新漏洞。
*   **新型交互模式**：识别出约 11% 的全新不安全模式（如跨线程引用不一致），这些模式在现有测试套件中完全缺失。
*   **模型差异**：即使是经过强安全对齐的模型（如 GPT-5），在 SafeAudit 生成的复杂工作流场景下仍存在被突破的风险。

#### 7. 优点（亮点）
*   **视角独特**：不只是提出新基准，而是提出了“元审计”概念，反思“测试者本身是否合格”。
*   **方法科学**：引入“规则抗性”作为衡量测试用例新颖性的标准，比单纯的语义相似度比较更具说服力。
*   **自动化程度高**：整个流程（从环境分析到规则提取再到用例生成）高度自动化，易于扩展到新环境。

#### 8. 不足与局限
*   **开环生成**：目前的枚举器是开环的，如果能引入反馈机制（根据已发现的漏洞动态调整生成策略），效率可能更高。
*   **规则表达限制**：将安全规则简单地附加到系统提示词中可能无法完全代表所有防御手段（如外部监控器或硬编码过滤）。
*   **裁判偏见**：虽然使用了多个裁判模型，但 LLM-as-a-judge 固有的偏见（如对长文本的偏好）可能仍会影响安全性判定的准确性。
*   **应用限制**：主要关注工具调用逻辑，对于多模态输入或极长时程（Long-horizon）任务的覆盖尚待深入。

（完）
