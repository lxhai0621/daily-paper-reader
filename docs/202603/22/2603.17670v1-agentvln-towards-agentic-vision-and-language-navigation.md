---
title: "AgentVLN: Towards Agentic Vision-and-Language Navigation"
title_zh: AgentVLN：迈向智能体视觉语言导航
authors: "Zihao Xin, Wentong Li, Yixuan Jiang, Ziyuan Huang, Bin Wang, Piji Li, Jianke Zhu, Jie Qin, Shengjun Huang"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17670v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 智能体视觉语言导航框架
tldr: AgentVLN是一个面向具身导航的高效框架，旨在解决视觉语言导航（VLN）中的空间感知受限和2D-3D表征不匹配问题。该框架采用“VLM作为大脑”的范式，通过解耦语义推理与感知规划，并引入跨空间表征映射、主动探索策略及查询驱动的感知思维链（QD-PCoT），显著提升了长程导航的准确性与鲁棒性，并支持边缘计算部署。
selection_source: fresh_fetch
motivation: 现有的VLN系统在处理复杂指令时面临空间感知不足、2D与3D表征不一致以及单目尺度模糊等挑战。
method: 提出一种基于POSMDP的框架，利用VLM进行高层推理，并通过跨空间映射、自纠错机制及感知思维链实现精准的路径规划与环境探索。
result: 在长程VLN基准测试中，AgentVLN一致优于现有的最先进方法，并证明了其在边缘计算平台上的部署可行性。
conclusion: AgentVLN为轻量化具身导航模型提供了一种实用的范式，通过增强智能体的元认知能力和表征一致性，有效解决了复杂环境下的导航难题。
---

## 摘要
视觉语言导航 (VLN) 要求具身智能体在未知环境中将复杂的自然语言指令转化为长程导航任务。尽管视觉语言模型 (VLM) 提供了强大的 2D 语义理解能力，但当前的 VLN 系统仍受限于有限的空间感知、2D-3D 表示不匹配以及单目尺度歧义。在本文中，我们提出了 AgentVLN，这是一种新颖且高效的具身导航框架，可部署在边缘计算平台上。我们将 VLN 建模为部分可观测半马尔可夫决策过程 (POSMDP)，并引入了“VLM 作为大脑” (VLM-as-Brain) 的范式，通过即插即用的技能库将高层语义推理与感知和规划解耦。为了解决多层级表示的不一致性，我们设计了一种跨空间表示映射，将感知层的 3D 拓扑路标投影到图像平面，为 VLM 生成像素对齐的视觉提示。基于这一桥梁，我们集成了上下文感知的自我修正和主动探索策略，以从遮挡中恢复并抑制长轨迹上的误差累积。为了进一步解决非结构化环境中指令的空间歧义，我们提出了一种查询驱动的感知思维链 (QD-PCoT) 方案，使智能体具备主动寻求几何深度信息的元认知能力。最后，我们构建了 AgentVLN-Instruct，这是一个大规模指令微调数据集，具有基于目标可见性的动态阶段路由。大量实验表明，AgentVLN 在长程 VLN 基准测试中始终优于先前的最先进 (SOTA) 方法，为下一代具身导航模型的轻量化部署提供了一种实用的范式。

## Abstract
Vision-and-Language Navigation (VLN) requires an embodied agent to ground complex natural-language instructions into long-horizon navigation in unseen environments. While Vision-Language Models (VLMs) offer strong 2D semantic understanding, current VLN systems remain constrained by limited spatial perception, 2D-3D representation mismatch, and monocular scale ambiguity. In this paper, we propose AgentVLN, a novel and efficient embodied navigation framework that can be deployed on edge computing platforms. We formulate VLN as a Partially Observable Semi-Markov Decision Process (POSMDP) and introduce a VLM-as-Brain paradigm that decouples high-level semantic reasoning from perception and planning via a plug-and-play skill library. To resolve multi-level representation inconsistency, we design a cross-space representation mapping that projects perception-layer 3D topological waypoints into the image plane, yielding pixel-aligned visual prompts for the VLM. Building on this bridge, we integrate a context-aware self-correction and active exploration strategy to recover from occlusions and suppress error accumulation over long trajectories. To further address the spatial ambiguity of instructions in unstructured environments, we propose a Query-Driven Perceptual Chain-of-Thought (QD-PCoT) scheme, enabling the agent with the metacognitive ability to actively seek geometric depth information. Finally, we construct AgentVLN-Instruct, a large-scale instruction-tuning dataset with dynamic stage routing conditioned on target visibility. Extensive experiments show that AgentVLN consistently outperforms prior state-of-the-art methods (SOTA) on long-horizon VLN benchmarks, offering a practical paradigm for lightweight deployment of next-generation embodied navigation models. Code: https://github.com/Allenxinn/AgentVLN.

---

## 论文详细总结（自动生成）

### AgentVLN：迈向智能体视觉语言导航 论文总结

#### 1. 核心问题与研究动机
视觉语言导航（VLN）要求智能体根据自然语言指令在未知的3D物理环境中导航。尽管现有的视觉语言模型（VLM）在2D语义理解上表现出色，但在具身导航任务中面临三大挑战：
*   **空间感知受限**：VLM难以理解复杂的3D几何结构和度量尺度。
*   **表征不匹配**：2D视觉表征与3D物理轨迹之间存在跨空间断层。
*   **单目尺度歧义**：仅靠RGB图像难以准确判断目标的深度和空间方位。
此外，现有模型参数量巨大（通常≥7B），且依赖云端推理，难以满足边缘设备（如四足机器人）的实时控制需求。

#### 2. 方法论
论文提出了 **AgentVLN** 框架，核心思想是将导航建模为**部分可观测半马尔可夫决策过程（POSMDP）**，并采用“**VLM作为大脑**”的范式：
*   **VLM-as-Brain 架构**：将高层语义推理与底层技能执行解耦。VLM作为中央控制器，通过调用即插即用的技能库（感知技能 $F_{percep}$ 和规划技能 $F_{plan}$）来完成任务。
*   **跨空间表征映射**：为了消除2D-3D不一致，系统将感知层计算出的3D拓扑路标通过透视投影映射回2D图像平面，生成**像素对齐的视觉提示**，使VLM能直接在视觉空间内做决策。
*   **上下文感知自纠错与主动探索**：当遇到遮挡或偏离轨迹时，智能体能生成细粒度的原子动作（如左转、右转、前进一步）进行自主校正。
*   **查询驱动的感知思维链（QD-PCoT）**：针对空间歧义，智能体不再盲目预测坐标，而是主动发起自然语言查询（如“椅子离我多远？”）并调用感知技能获取深度信息，通过多轮交互实现精准定位。
*   **AgentVLN-Instruct 数据集**：构建了大规模指令微调数据集，引入动态阶段路由机制，模拟人类“先粗略导航，后精准定位”的寻路过程。

#### 3. 实验设计
*   **数据集与基准**：在 **R2R-CE**（连续环境下的房间到房间导航）和 **RxR-CE**（多语言、长程导航）两个主流基准上进行评估。
*   **对比方法**：对比了包括 NavGPT、NaVid、StreamVLN、InternVLA-N1、JanusVLN、EfficientVLN 和 DualVLN 在内的多种 SOTA 模型。
*   **评估指标**：成功率（SR）、Oracle成功率（OS）、路径长度加权成功率（SPL）和导航误差（NE）。
*   **真实世界验证**：在 Unitree Go2 四足机器人上部署，测试了室内和室外复杂场景。

#### 4. 资源与算力
*   **模型底座**：采用 **Qwen2.5-VL-3B** 作为核心大脑。
*   **算力资源**：实验在配备 **32 台 NVIDIA A100 GPU** 的计算集群上完成。
*   **训练细节**：使用 AdamW 优化器，Batch Size 为 128，学习率为 $2 \times 10^{-5}$，采用余弦退火策略。
*   **推理部署**：由于模型轻量化（3B），支持在 **Jetson 边缘计算平台**上进行实时本地推理。

#### 5. 实验数量与充分性
*   **实验规模**：论文在两个大型公开数据集上进行了详尽的对比实验，并提供了消融实验。
*   **消融实验**：分别验证了 VLM-as-Brain 范式、自纠错策略（CDFG）和 QD-PCoT 机制对性能的提升。此外，还研究了时间上下文长度（帧数）对导航效果的影响。
*   **客观性**：实验涵盖了从仿真环境到真实物理世界的迁移，对比对象包含了从 3B 到 8B 以上不同规模的模型，结果显示其 3B 模型在多项指标上超越了 7B/8B 模型，证明了设计的有效性和公平性。

#### 6. 主要结论与发现
*   **效率与性能并存**：AgentVLN-3B 在 R2R-CE 上的成功率（SR）达到 67.2%，显著超过了参数量更大的 InternVLA-N1-8.3B（58.2%）。
*   **解耦的优越性**：将高层推理与底层物理执行解耦，能显著降低 VLM 的认知负担，提高长程导航的鲁棒性。
*   **元认知能力的重要性**：通过 QD-PCoT 主动查询缺失的空间信息，比单纯依靠端到端回归坐标更能有效解决深度歧义问题。

#### 7. 优点
*   **轻量化与可部署性**：3B 参数量即可达到 SOTA 性能，且无需额外的深度估计模块常驻内存，适合边缘设备。
*   **像素对齐的交互**：通过投影技术将 3D 几何信息转化为 VLM 易于理解的 2D 视觉提示，巧妙解决了跨空间表征问题。
*   **模块化设计**：技能库即插即用，适应不同传感器（如 LiDAR 或深度相机）时无需重新训练大模型。

#### 8. 不足与局限
*   **依赖感知算法质量**：系统的性能上限仍受限于底层感知技能（如 SLAM 和深度图获取）的准确性。
*   **多轮推理延迟**：QD-PCoT 虽然提高了精度，但在极端实时性要求下，多轮对话推理可能会引入额外的决策延迟。
*   **环境覆盖**：虽然进行了实地测试，但在极端天气或极度动态变化的环境（如拥挤的人群）中的表现仍有待进一步验证。

（完）
