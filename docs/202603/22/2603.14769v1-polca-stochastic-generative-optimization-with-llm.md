---
title: "POLCA: Stochastic Generative Optimization with LLM"
title_zh: POLCA：基于大语言模型的随机生成式优化
authors: "Xuanfei Ren, Allen Nie, Tengyang Xie, Ching-An Cheng"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14769v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 优化从大模型提示到多轮智能体的复杂系统
tldr: 本研究针对LLM提示词和多轮智能体等复杂系统的优化难题，提出了POLCA框架。该框架将优化过程形式化为随机生成优化问题，利用LLM作为优化器，通过数值奖励和文本反馈进行引导。POLCA引入优先级队列平衡探索与利用，结合ε-Net保持多样性及LLM总结器进行元学习，有效应对了反馈噪声和随机性。实验证明其在多个基准测试中均优于现有算法，具有极高的样本和时间效率。
selection_source: fresh_fetch
motivation: 传统复杂系统（如智能体和提示词）的优化依赖繁琐的人工迭代，且面临反馈噪声和随机行为带来的挑战。
method: 提出POLCA框架，通过优先级队列管理候选方案，并结合ε-Net机制保持多样性以及LLM总结器进行跨历史试验的元学习。
result: 在τ-bench、HotpotQA、VeriBench和KernelBench等多个基准测试中，POLCA在确定性和随机性问题上均显著优于现有最先进算法。
conclusion: POLCA为随机环境下的复杂系统优化提供了一种理论上有收敛保证、实践中高效且鲁棒的自动化生成优化方案。
---

## 摘要
优化复杂系统（从大语言模型提示词到多轮智能体）传统上需要耗费大量人力的手动迭代。我们将这一挑战形式化为一个随机生成式优化问题，其中生成式语言模型充当优化器，在数值奖励和文本反馈的引导下寻找最优系统。我们提出了基于局部上下文聚合的优先级优化（POLCA），这是一个可扩展的框架，旨在处理优化过程中的随机性（如噪声反馈、采样小批量和随机系统行为），同时有效管理解空间的无约束扩张。POLCA 维护一个优先级队列来管理探索与利用之间的权衡，系统地追踪候选解及其评估历史。为了提高效率，我们集成了 ε-Net 机制以维持参数多样性，并使用大语言模型总结器在历史试验中进行元学习。我们从理论上证明了 POLCA 在随机性条件下能够收敛至近优候选解。我们在多种基准测试上评估了该框架，包括 τ-bench、HotpotQA（智能体优化）、VeriBench（代码翻译）和 KernelBench（CUDA 内核生成）。实验结果表明，POLCA 实现了鲁棒、样本高效且时间高效的性能，在确定性和随机性问题上均持续优于现有最先进算法。本工作的代码库已在 https://github.com/rlx-lab/POLCA 公开。

## Abstract
Optimizing complex systems, ranging from LLM prompts to multi-turn agents, traditionally requires labor-intensive manual iteration. We formalize this challenge as a stochastic generative optimization problem where a generative language model acts as the optimizer, guided by numerical rewards and text feedback to discover the best system. We introduce Prioritized Optimization with Local Contextual Aggregation (POLCA), a scalable framework designed to handle stochasticity in optimization -- such as noisy feedback, sampling minibatches, and stochastic system behaviors -- while effectively managing the unconstrained expansion of solution space. POLCA maintains a priority queue to manage the exploration-exploitation tradeoff, systematically tracking candidate solutions and their evaluation histories. To enhance efficiency, we integrate an $\varepsilon$-Net mechanism to maintain parameter diversity and an LLM Summarizer to perform meta-learning across historical trials. We theoretically prove that POLCA converges to near-optimal candidate solutions under stochasticity. We evaluate our framework on diverse benchmarks, including $τ$-bench, HotpotQA (agent optimization), VeriBench (code translation) and KernelBench (CUDA kernel generation). Experimental results demonstrate that POLCA achieves robust, sample and time-efficient performance, consistently outperforming state-of-the-art algorithms in both deterministic and stochastic problems. The codebase for this work is publicly available at https://github.com/rlx-lab/POLCA.

---

## 论文详细总结（自动生成）

以下是对论文《POLCA: Stochastic Generative Optimization with LLM》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
论文关注的是**复杂系统（如 LLM 提示词、代码生成器、多轮智能体）的自动化优化**。
*   **研究动机**：传统上，优化这些系统依赖专家的人工迭代。虽然近期出现了基于 LLM 的生成式优化算法，但它们在面对**随机性**（如噪声反馈、小批量采样误差、系统自身的随机行为）时表现脆弱。
*   **核心挑战**：
    1.  **评估随机性**：单次评估结果不可靠，导致优化器可能丢弃优秀的候选方案或在错误方向上浪费资源。
    2.  **解空间扩张**：LLM 优化器会生成大量语义重复的方案，导致评估成本随迭代线性增长，难以扩展。
*   **整体含义**：论文将此问题形式化为“随机生成式优化”，并提出 POLCA 框架，旨在通过系统化的内存管理和语义过滤，在有限的计算预算下实现鲁棒、高效的自动优化。

### 2. 论文提出的方法论
POLCA（Prioritized Optimization with Local Contextual Aggregation）的核心思想是结合**优先级搜索**与**语义多样性控制**。

*   **核心组件与流程**：
    1.  **优先级队列内存 (Priority Queue Memory)**：维护所有候选方案 $\theta$ 及其评估历史。使用经验均值（或理论上的 UCB 分数）作为优先级，动态平衡“探索新方案”与“利用已知好方案”。
    2.  **$\varepsilon$-Net 语义过滤机制**：这是防止内存爆炸的关键。利用 Embedding 模型将程序映射到向量空间，只有当新生成的方案与现有方案的语义距离大于阈值 $\varepsilon$ 时才被接受。这在理论上保证了候选空间是有界的。
    3.  **LLM 总结器 (Summarizer)**：定期对内存中的成功和失败案例进行对比分析，生成“元指令”（Global Context），引导优化器避开历史错误。
    4.  **局部上下文聚合**：优化器在生成新方案时，同时参考当前小批量的详细反馈（局部）和总结器的元指令（全局）。
*   **算法公式**：在理论分析中，使用 UCB 分数 $UCB_\theta(t) = \hat{\mu}_{\theta} + 2\sigma\sqrt{\frac{\log n}{T_\theta(t)}}$ 来指导采样，并证明了在满足“严格改进假设”下，算法能收敛至近优解。

### 3. 实验设计
论文在四个具有代表性的基准测试上验证了 POLCA，涵盖了不同来源的随机性：
*   **$\tau$-bench**：优化工具调用智能体的提示词。随机性来自多轮对话的随机行为和小批量任务采样。
*   **HotpotQA**：优化多跳问答的提示词。
*   **VeriBench**：将 Python 代码翻译为 Lean 4 形式化证明代码。包含“3步评估”（含随机的 LLM 评分）和“纯编译”（确定性）两种设置。
*   **KernelBench**：优化 CUDA 内核代码以提升 GPU 加速比。属于确定性优化场景。
*   **对比方法**：**DSPy**（顺序修订）、**GEPA**（基于帕累托前沿的进化算法）、**OpenEvolve**（AlphaEvolve 的开源实现，基于 MAP-Elites）。

### 4. 资源与算力
*   **模型使用**：使用了 Gemini 2.0 Flash, Gemini 2.5 Flash-lite, Claude 3.5 Sonnet 和 Claude 3.7 Sonnet 作为后端模型。
*   **算力设备**：KernelBench 实验明确提到在 **NVIDIA L40S GPU** 上运行。
*   **Token 消耗**：附录 D.8 提供了详细统计。例如，$\tau$-bench 的一次完整运行（100 次迭代）消耗约 **3120 万个 Token**；HotpotQA 消耗约 654 万个 Token。
*   **时长**：未明确给出总挂钟时间，但通过“评估步骤”（Evaluation Steps）这一指标强调了 POLCA 的并行化优势。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同领域的 Benchmark 上进行了测试，每个实验通常包含多个随机种子（如 $\tau$-bench 用了 6 个种子，VeriBench 用了 3 个）。
*   **消融实验**：非常充分。分别验证了 $\varepsilon$-Net 的必要性、Summarizer 的增益、$\varepsilon$ 阈值的灵敏度，甚至对比了“为什么不直接用回归模型预测奖励”。
*   **公平性**：所有算法在相同的度量调用次数（Metric Calls）或评估步骤下进行对比，确保了计算预算的一致性。

### 6. 主要结论与发现
*   **性能卓越**：POLCA 在所有基准测试中均显著优于 GEPA 和 OpenEvolve，尤其是在随机性较强的 $\tau$-bench 中，性能提升最明显。
*   **收敛性**：理论证明了 POLCA 在随机环境下具有收敛保证，实验结果验证了其收敛速度快且最终得分高。
*   **语义过滤的价值**：$\varepsilon$-Net 机制能有效节省 30%-50% 的评估预算，且对 $\varepsilon$ 的取值在一定范围内具有鲁棒性。
*   **确定性场景同样适用**：即使在没有随机性的 KernelBench 中，POLCA 的并行探索策略也优于传统的顺序搜索。

### 7. 优点（亮点）
*   **理论完备性**：不仅提出了算法，还提供了关于收敛速度的理论证明，这在 LLM 优化论文中较为少见。
*   **处理随机性的鲁棒性**：通过优先级队列持续更新统计量，解决了“一次评估定生死”的弊端。
*   **可扩展性**：$\varepsilon$-Net 解决了生成式优化中常见的“候选方案冗余”问题，使得长程搜索成为可能。
*   **并行化友好**：设计上支持大规模并行评估，显著降低了优化的总时长。

### 8. 不足与局限
*   **Embedding 依赖**：$\varepsilon$-Net 的效果高度依赖于 Embedding 模型捕捉程序语义的能力。如果 Embedding 无法区分两个功能不同但结构相似的代码，过滤机制可能会失效。
*   **超参数敏感性**：虽然 $\varepsilon$ 有一定鲁棒性，但在不同任务间仍需调整；此外，UCB 公式中的 $\sigma$（噪声程度）在现实中往往难以预知。
*   **计算成本**：尽管比基线高效，但对于普通开发者来说，数千万 Token 的消耗依然是一笔不小的开支。
*   **模型偏差**：实验主要基于 Gemini 和 Claude 系列模型，尚未验证在开源模型（如 Llama 3）上的表现。

（完）
