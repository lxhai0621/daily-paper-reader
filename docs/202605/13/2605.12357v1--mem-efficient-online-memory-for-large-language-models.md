---
title: "$δ$-mem: Efficient Online Memory for Large Language Models"
title_zh: $δ$-mem：大语言模型的高效在线记忆
authors: "Jingdi Lei, Di Zhang, Junxian Li, Weida Wang, Kaixuan Fan, Xiang Liu, Qihan Liu, Xiaoteng Ma, Baian Chen, Soujanya Poria"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12357v1"
tags: ["query:agent"]
score: 10.0
evidence: 智能体系统的轻量级在线记忆机制
tldr: 针对大语言模型在长程对话和智能体任务中处理历史信息成本高、效率低的问题，本文提出了 $δ$-mem。这是一种轻量级在线记忆机制，通过在冻结的注意力机制中引入紧凑的关联记忆状态，利用 Delta 规则更新固定大小的状态矩阵，并生成低秩修正。该方法无需扩展上下文窗口或全量微调，在多个记忆密集型基准测试中显著提升了模型性能，实现了高效的信息积累与复用。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.12357v1/fig-001.webp\", \"caption\": \"Figure 4 Trainable parameter comparison across memory-augmented methods.\", \"page\": 15, \"index\": 1, \"width\": 674, \"height\": 276}, {\"url\": \"assets/figures/arxiv/2605.12357v1/fig-002.webp\", \"caption\": \"Figure 3 Inference efficiency under different prompt and decode lengths.\", \"page\": 15, \"index\": 2, \"width\": 970, \"height\": 259}, {\"url\": \"assets/figures/arxiv/2605.12357v1/fig-003.webp\", \"caption\": \"Table 1 Main benchmark results comparing different memory mechanisms on Qwen3-4B-Instruct. All values report the task-specific metrics detailed in Appendix A. For the final average score, HotpotQA is counted using Exact Match (EM).\", \"page\": 7, \"index\": 3, \"width\": 984, \"height\": 565}, {\"url\": \"assets/figures/arxiv/2605.12357v1/fig-004.webp\", \"caption\": \"Table 5 Evaluation categories, datasets, and metrics in MemoryAgentBench.\", \"page\": 14, \"index\": 4, \"width\": 572, \"height\": 479}, {\"url\": \"assets/figures/arxiv/2605.12357v1/fig-005.webp\", \"caption\": \"Table 2 General benchmark and long-context evaluation results across backbone models. All values report the taskspecific metrics detailed in Appendix A. For the final average score, HotpotQA is counted using Exact Match (EM).\", \"page\": 8, \"index\": 5, \"width\": 968, \"height\": 344}, {\"url\": \"assets/figures/arxiv/2605.12357v1/fig-006.webp\", \"caption\": \"Figure 2 Context recovery performance on HotpotQA and LoCoMo with Qwen3-4B-Instruct as the backbone.\", \"page\": 8, \"index\": 6, \"width\": 585, \"height\": 224}, {\"url\": \"assets/figures/arxiv/2605.12357v1/fig-007.webp\", \"caption\": \"Figure 1 The overview of δ-mem. Given the hidden state from a frozen Transformer backbone, δ-mem first projects it into a low-dimensional memory space, and reads context-relevant associative memory signals from the previous online state, and uses the signal to generate low-rank corrections to the attention computation. After the computation, the memory state is updated with the current key-value information via delta-rule learning.\", \"page\": 4, \"index\": 7, \"width\": 926, \"height\": 445}, {\"url\": \"assets/figures/arxiv/2605.12357v1/fig-008.webp\", \"caption\": \"Table 4 Insertion depth results on HotpotQA and LoCoMo with Qwen3-4B-Instruct as the backbone.\", \"page\": 9, \"index\": 8, \"width\": 964, \"height\": 213}, {\"url\": \"assets/figures/arxiv/2605.12357v1/fig-009.webp\", \"caption\": \"Table 3 Head Ablation Results on HotpotQA and LoCoMo with Qwen3-4B-Instruct as the backbone.\", \"page\": 9, \"index\": 9, \"width\": 964, \"height\": 425}]"
motivation: 传统的上下文窗口扩展方法成本高昂且利用率低，难以满足长程助手和智能体系统对历史信息高效复用的需求。
method: 提出一种基于 Delta 规则更新的紧凑在线记忆机制，通过 8x8 的状态矩阵为冻结的注意力计算提供低秩修正。
result: 在 MemoryAgentBench 和 LoCoMo 等基准测试中，性能分别提升至基准模型的 1.31 倍和 1.20 倍，显著优于现有记忆方案。
conclusion: δ-mem 证明了通过紧凑的在线状态与注意力机制直接耦合，可以在不改变模型骨干或扩展上下文的情况下实现高效的记忆能力。
---

## 摘要
大语言模型在长期助手和智能体系统中日益需要积累和重用历史信息。简单地扩展上下文窗口成本高昂，且往往无法确保有效的上下文利用。我们提出了 $δ$-mem，这是一种轻量级的记忆机制，它通过紧凑的关联记忆在线状态增强了冻结的全注意力主干网络。$δ$-mem 将过去的信息压缩进一个由增量规则（delta-rule）学习更新的固定大小状态矩阵中，并在生成过程中利用其读取值对主干网络的注意力计算生成低秩修正。仅使用 $8\times8$ 的在线记忆状态，$δ$-mem 就能将平均得分提高到冻结主干网络的 1.10 倍，以及最强非 $δ$-mem 记忆基线的 1.15 倍。它在记忆密集型基准测试中取得了更大的增益，在 MemoryAgentBench 上达到 1.31 倍，在 LoCoMo 上达到 1.20 倍，同时在很大程度上保留了通用能力。这些结果表明，通过直接与注意力计算耦合的紧凑在线状态，可以实现有效的记忆，而无需全量微调、更换主干网络或显式的上下文扩展。

## Abstract
Large language models increasingly need to accumulate and reuse historical information in long-term assistants and agent systems. Simply expanding the context window is costly and often fails to ensure effective context utilization. We propose $δ$-mem, a lightweight memory mechanism that augments a frozen full-attention backbone with a compact online state of associative memory. $δ$-mem compresses past information into a fixed-size state matrix updated by delta-rule learning, and uses its readout to generate low-rank corrections to the backbone's attention computation during generation. With only an $8\times8$ online memory state, $δ$-mem improves the average score to $1.10\times$ that of the frozen backbone and $1.15\times$ that of the strongest non-$δ$-mem memory baseline. It achieves larger gains on memory-heavy benchmarks, reaching $1.31\times$ on MemoryAgentBench and $1.20\times$ on LoCoMo, while largely preserving general capabilities. These results show that effective memory can be realized through a compact online state directly coupled with attention computation, without full fine-tuning, backbone replacement, or explicit context extension.

---

## 论文详细总结（自动生成）

### $\delta$-mem: 大语言模型的高效在线记忆论文总结

#### 1. 核心问题与研究动机
随着大语言模型（LLM）在长期个性化助手和智能体系统中的广泛应用，模型需要具备积累、更新和复用历史信息的能力。目前主流的解决方案（如扩展上下文窗口）面临两大挑战：
*   **计算成本高昂：** 标准注意力机制的计算量随上下文长度呈平方级增长。
*   **利用效率低下：** 即使拥有百万级窗口，模型仍会遭遇“上下文腐烂”（Context Rot）或性能退化，无法有效提取长程信息。
**研究动机：** 开发一种紧凑、动态更新且能与模型注意力机制深度耦合的记忆机制，在不改变主干网络参数的前提下，实现高效的长程信息管理。

#### 2. 方法论：核心思想与技术细节
$\delta$-mem 的核心思想是在冻结的 Transformer 主干网络旁增加一个**在线关联记忆状态（OSAM）**。其运行流程分为“读取-引导-写入”三个阶段：
*   **核心组件：** 维护一个固定大小的矩阵 $S$（实验中仅为 $8 \times 8$）作为记忆载体。
*   **读取与引导（Read & Steering）：**
    *   将当前隐藏状态投影为查询向量 $q_m$，从旧状态中读取关联信号 $r_t = S_{t-1}q_m$。
    *   利用 $r_t$ 生成低秩修正项 $\Delta q_t$ 和 $\Delta o_t$，分别注入到主干网络的注意力查询（Query）和输出（Output）端。
*   **写入与更新（Write & Update）：**
    *   采用**门控增量规则（Gated Delta-rule）**更新状态矩阵。公式形式类似于在线回归：$S_t = \lambda_t S_{t-1} + \beta_t (v_m - S_{t-1}k_m)k_m^\top$。
    *   通过误差修正机制，仅将预测偏差（新信息）写入记忆，并利用遗忘门控制长程状态演化。
*   **写入粒度：** 提出了三种策略：
    1.  **TSW（逐词写入）：** 捕捉细粒度变化。
    2.  **SSW（段落写入）：** 对消息取平均后更新，减少噪声。
    3.  **MSW（多状态写入）：** 使用多个并行子状态减少信息干扰。

#### 3. 实验设计
*   **主干网络：** Qwen3-4B-Instruct、Qwen3-8B、SmolLM3-3B。
*   **数据集与基准测试：**
    *   **通用能力：** IFEval（指令遵循）、HotpotQA（多跳推理）、GPQA-Diamond（专家级问答）。
    *   **记忆密集型：** LoCoMo（长程对话记忆）、MemoryAgentBench（智能体记忆评估，涵盖检索、学习、理解等）。
*   **对比方法：**
    *   **文本记忆：** BM25 RAG、LLMLingua-2（压缩）、MemoryBank。
    *   **参数记忆：** Context2LoRA、MemGen。
    *   **外部通道记忆：** MLP Memory。

#### 4. 资源与算力
*   **硬件：** 使用了 **8 张 NVIDIA A800 GPU**。
*   **训练细节：** 采用 bfloat16 精度，DeepSpeed ZeRO-2 优化器。
*   **训练数据：** 在 QASPER 数据集的 2,219 个样本上进行了一个 epoch 的微调（训练序列长度 512，记忆写入预算 8,192）。
*   **参数量：** $\delta$-mem 极其轻量，TSW/SSW 变体仅引入 **4.87M** 可训练参数（约占主干的 0.12%），MSW 变体为 19.47M（0.48%）。

#### 5. 实验数量与充分性
*   **实验规模：** 论文在 3 种不同规模的模型上验证了有效性，涵盖了 5 个主流基准测试。
*   **消融实验：** 进行了详尽的消融研究，包括：
    *   **注入位置：** 对比了 q, k, v, o 不同分支组合的效果。
    *   **插入深度：** 对比了前层、中层、后层及全层注入的影响。
    *   **上下文恢复：** 验证了在完全移除显式历史上下文的情况下，仅靠 8x8 矩阵恢复信息的能力。
*   **客观性：** 实验对比了多种不同范式的记忆机制，并报告了推理效率（显存占用与解码速度），评估较为全面、客观。

#### 6. 主要结论与发现
*   **性能卓越：** 在 MemoryAgentBench 上性能提升至 1.31 倍，在 LoCoMo 上提升至 1.20 倍。
*   **紧凑性：** 仅需 $8 \times 8$ 的极小矩阵即可保留关键历史信号。
*   **通用性：** 在大幅提升记忆能力的同时，基本保持了模型的通用推理和指令遵循能力。
*   **上下文替代：** 实验证明 $\delta$-mem 可以在没有显式上下文的情况下恢复部分多跳证据，缓解了对超长窗口的依赖。

#### 7. 优点
*   **极高效率：** 参数量极小，推理时显存开销几乎可忽略不计，且解码速度稳定。
*   **动态演化：** 相比静态的 LoRA 或 Adapter，其记忆状态随输入动态更新，更适合连续交互场景。
*   **深度耦合：** 直接修正注意力计算，比外部 RAG 检索更具鲁棒性，减少了检索噪声。

#### 8. 不足与局限
*   **训练依赖：** 虽然不需要全量微调，但仍需在特定任务（如 QASPER）上进行有监督微调（SFT）以学习如何读写记忆。
*   **容量瓶颈：** 尽管 8x8 矩阵表现惊人，但对于极度复杂的超长历史，固定大小的状态矩阵可能存在信息饱和或覆盖风险。
*   **任务覆盖：** 实验主要集中在问答和智能体任务，对于长文本摘要或极长代码生成等场景的验证相对较少。

（完）
