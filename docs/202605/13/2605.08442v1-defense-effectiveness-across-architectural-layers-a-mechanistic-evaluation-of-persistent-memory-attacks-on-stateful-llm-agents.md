---
title: "Defense effectiveness across architectural layers: a mechanistic evaluation of persistent memory attacks on stateful LLM agents"
title_zh: 跨架构层的防御有效性：对有状态 LLM 智能体持久化内存攻击的机理性评估
authors: Jun Wen Leong
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.08442v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 对使用RAG的有状态LLM智能体持久内存攻击的机制评估
tldr: "本研究针对大语言模型智能体面临的持久性内存攻击（通过RAG注入恶意指令），系统评估了跨越四个架构层面的六种防御措施。通过对9个开源模型进行5040次实验，研究发现输入层和检索层防御因无法识别语义掩蔽而基本失效，而内存层的工具门控机制（Memory Sandbox）表现最为出色，在不损失模型效用的前提下，将绝大多数模型的攻击成功率降至0%。该研究为理解防御失效机制及优化安全投资提供了重要参考。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.08442v1/fig-001.webp\", \"caption\": \"Table 1: Attack success rate (ASR) by defense across 9 models (N=40 per cell, 360 runs per defense). BTCR is measured on the no-attack arm (2,520 runs). Four defenses are statistically indistinguishable from no-defense; Prompt Hardening is distinguishable but provides no protection for 7 of 9 models.\", \"page\": 5, \"index\": 1, \"width\": 614, \"height\": 213}]"
motivation: 持久性内存攻击能通过RAG将恶意指令植入LLM智能体，但目前尚缺乏针对此类攻击防御有效性的系统性评估。
method: 研究者在9个开源模型上对输入过滤、检索过滤、提示词加固和内存层工具门控等六种防御手段进行了大规模实验评估。
result: "实验显示输入和检索层防御几乎无效，而内存层的Memory Sandbox防御机制在八个模型中实现了0%的攻击成功率且无性能损耗。"
conclusion: 内存层防御是对抗持久性内存攻击的关键，而传统的输入和检索层过滤手段在应对此类语义掩蔽攻击时存在明显局限。
---

## 摘要
针对 LLM 智能体的持久化内存攻击在开源模型上实现了极高的攻击成功率。在这些攻击中，通过 RAG 检索文档注入的恶意指令被存储在持久化内存中，并在随后的会话中执行。然而，目前尚不存在针对此类攻击防御有效性的系统性评估。我们针对 9 个开源模型上的延迟触发攻击，评估了跨 4 个架构层的 6 种防御措施（共 5,040 次运行，每种条件下 N=40）。四种防御措施在接近基准攻击成功率（ASR）时失效：输入级过滤（Minimizer、Sanitizer）和检索级过滤（RAG Sanitizer、RAG LLM Judge）达到了 88-89% 的 ASR，在统计学上与 88.6% 的未防御基准无显著差异。提示词加固（Prompt Hardening）部分失效，ASR 为 77.8%，其下降主要由两个 ASR 为 0% 的模型驱动：一个是真实的防御效果，另一个是独立于防御的模型级拒绝。架构层面的解释是成立的：输入级防御无法观察到 RAG 注入的内容，而检索级分类器则被合规框架下的语义掩蔽所击败。一种防御措施，即内存层的工具门控（Memory Sandbox），通过移除攻击所需的召回能力，将 9 个模型中 8 个的 ASR 降低至 0%。例外情况则完全反转了防御效果：一个在无防御状态下通过执行拒绝实现 0% ASR 的推理模型，在 Memory Sandbox 下反转为 100% ASR，因为移除显式召回迫使模型进入 RAG 路径，而其拒绝机制在该路径下并未激活。在没有攻击的情况下，Memory Sandbox 带来的效用成本为零（所有条件下的 BTCR 均为 100%）。这些结果首次系统地刻画了各类防御在持久化内存攻击面前失效的原因，为明智的防御投资决策提供了依据。

## Abstract
Persistent memory attacks against LLM agents achieve high attack success rates against open-source models. In these attacks, malicious instructions injected via RAG-retrieved documents are stored in persistent memory and executed in later sessions. However, no systematic evaluation of defense effectiveness against this attack class exists. We evaluate six defenses across four architectural layers against delayed-trigger attacks on nine open-source models (5,040 runs, N=40 per condition). Four defenses fail at approximately baseline attack success rate: input-level filtering (Minimizer, Sanitizer) and retrieval-level filtering (RAG Sanitizer, RAG LLM Judge) achieve 88-89% ASR, statistically indistinguishable from the undefended baseline of 88.6%. Prompt Hardening partially fails at 77.8% ASR, with the reduction driven by two models at 0%: one genuine defense effect and one model-level refusal independent of the defense. The architectural explanation holds: input-level defenses cannot observe RAG-injected content, and retrieval-level classifiers are defeated by compliance-framed semantic masking. One defense, tool-gating at the memory layer (Memory Sandbox), reduces ASR to 0% for eight of nine models by removing the recall capability the attack requires. The exception inverts the defense entirely: a reasoning model that achieves 0% ASR under no defense via execution refusal inverts to 100% ASR under Memory Sandbox, because removing explicit recall forces the model onto the RAG pathway where its refusal mechanism does not activate. Memory Sandbox imposes zero utility cost in the absence of attack (BTCR = 100% across all conditions). These results provide the first systematic characterization of why each defense class fails against persistent memory attacks, enabling informed defense investment decisions.

---

## 论文详细总结（自动生成）

这是一份关于论文《Defense effectiveness across architectural layers: a mechanistic evaluation of persistent memory attacks on stateful LLM agents》的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：企业级 LLM 智能体（Agents）通常结合持久化内存和检索增强生成（RAG）来跨会话管理任务。这种架构引入了新的攻击面：恶意指令可以通过 RAG 检索到的文档注入，存储在智能体的持久化内存中，并在后续会话中被触发执行。
*   **核心问题**：现有的安全评估主要集中在输入层攻击（单次会话），而缺乏对“持久化内存攻击”（Persistent Memory Attacks）防御有效性的系统性评估。
*   **研究目的**：评估跨越四个架构层面的六种防御措施，对这种延迟触发攻击（Delayed Trigger Attack, DTA）的实际拦截效果及其失效机制。

### 2. 方法论
*   **延迟触发攻击 (DTA) 流程**：
    *   **S1（注入会话）**：智能体检索到伪装成合规备忘录的恶意文档，并将其存入持久化数据库。
    *   **S2/S3（干预会话）**：良性交互，用于清除上下文缓存。
    *   **S4（触发会话）**：用户发出良性指令，智能体召回存储的恶意规则并执行（如将邮件转发给攻击者）。
*   **评估的六种防御措施**：
    1.  **输入层**：`Minimizer`（基于 TF-IDF 过滤用户查询）、`Sanitizer`（正则+分类器过滤用户输入）。
    2.  **检索层**：`RAG Sanitizer`（在检索阶段过滤文档）、`RAG LLM Judge`（使用小模型判断检索文档是否恶意）。
    3.  **指令层**：`Prompt Hardening`（在系统提示词中加入安全规则）。
    4.  **工具层**：`Memory Sandbox`（在触发会话中移除智能体的“显式召回”工具，仅保留列出键名的能力）。

### 3. 实验设计
*   **实验规模**：采用全因子设计，涵盖 9 个开源模型（Qwen 系列、GLM-4、GPT-OSS 等），共 5,040 次运行（每种条件 N=40）。
*   **基准测试 (Benchmark)**：使用统一智能体环境（Unified Agentic Environment），通过 LangGraph 实现严格的会话隔离。
*   **对比指标**：
    *   **ASR (Attack Success Rate)**：攻击成功率。
    *   **BTCR (Benign Task Completion Rate)**：良性任务完成率（用于衡量防御对效用的影响）。
*   **模型筛选**：预先对 18 个模型进行筛选，最终选取 9 个易受攻击的“执行者”模型进行防御测试。

### 4. 资源与算力
*   **算力细节**：论文提到所有模型均通过 **Ollama 在本地运行**，模型参数量涵盖 1.5B 到 122B。
*   **具体配置**：文中**未明确说明**具体的 GPU 型号、数量或总训练/推理时长，但提到使用了 16k 的上下文长度和 0.0 的温度设置。

### 5. 实验数量与充分性
*   **实验量**：总计 5,040 次实验，其中包含 2,520 次无攻击运行作为对照组。
*   **统计严谨性**：使用了 BCa 自举置信区间（10,000 次重采样）和 Holm-Bonferroni 校正（针对 108 个预设比较），确保了结果的统计显著性。
*   **客观性**：通过 0% 的误报率（在无攻击组中）验证了评估框架的客观性。实验设计考虑了注入成功与执行成功的区分，提供了机理性解释而非单纯的数字堆砌。

### 6. 主要结论与发现
*   **防御全面失效**：输入层和检索层防御基本无效（ASR 约 88-89%）。输入层防御根本看不到 RAG 注入的内容；检索层防御则被“语义掩蔽”（伪装成合规文档）欺骗。
*   **工具层防御最有效**：`Memory Sandbox` 将 8/9 个模型的 ASR 降至 0%，且效用损失为零（BTCR 100%）。
*   **防御反转现象**：对于推理模型 QwQ-32B，`Memory Sandbox` 反而使 ASR 从 0% 升至 100%。原因是移除内存工具迫使模型直接从 RAG 路径获取指令，绕过了其在处理“记忆”时的拒绝机制。
*   **前沿模型表现**：Claude 3.5 Sonnet 和 Claude 4.5 Haiku 在无防御下即表现出极强的抗注入或主动检测能力。

### 7. 优点
*   **机理性分类**：不仅报告防御是否失败，还解释了在哪个架构层、为什么失败（如架构错位、能力不足、语义掩蔽）。
*   **统计方法严谨**：在 LLM 安全论文中少见地使用了严格的统计校正和置信区间分析。
*   **发现新现象**：识别了“幻觉绕过”、“多键规范化”和“防御反转”等复杂的智能体行为模式。

### 8. 不足与局限
*   **攻击多样性有限**：主要测试了一种基于合规文档伪装的攻击载荷，尚未验证其他语义掩蔽策略（如法律声明、HR 政策）。
*   **分类器训练集较小**：检索层分类器仅基于 60 个样本训练，可能限制了其泛化能力。
*   **模型范围**：主要实验集中在开源模型，前沿闭源模型（如 GPT-4o）未纳入全因子防御评估（仅做了基准筛选）。
*   **应用限制**：`Memory Sandbox` 的有效性依赖于智能体是否必须通过显式工具召回记忆，对于能自动处理长上下文的架构可能效果受限。

（完）
