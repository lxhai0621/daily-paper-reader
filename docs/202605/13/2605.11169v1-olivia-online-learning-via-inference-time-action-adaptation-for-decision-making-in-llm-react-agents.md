---
title: "OLIVIA: Online Learning via Inference-time Action Adaptation for Decision Making in LLM ReAct Agents"
title_zh: OLIVIA：通过推理时动作自适应实现大语言模型 ReAct 智能体决策的在线学习
authors: "Sheldon Yu, Junda Wu, Xintong Li, Nikki Lijing Kuang, Sizhe Zhou, Tong Yu, Jiawei Han, Jingbo Shang, Julian McAuley"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.11169v1"
tags: ["query:agent"]
score: 9.0
evidence: LLM ReAct 智能体的在线学习与动作适配
tldr: OLIVIA是一个针对ReAct型LLM智能体的推理时动作自适应框架。针对现有方法在部署时难以进行细粒度、不确定性感知更新的问题，OLIVIA将LLM的动作选择层建模为上下文线性多臂老虎机。通过利用冻结的隐藏状态作为上下文并结合UCB算法，它能根据动作级反馈进行轻量化在线更新，显著提升了智能体在重复任务中的决策效率和可靠性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.11169v1/fig-001.webp\", \"caption\": \"Figure 2: Running-average F1 over the episode stream on the four benchmarks. Static baselines (CoT, BM25, ReAct) remain flat; online methods improve over time. OLIVIA converges faster and reaches higher final F1 than CLIN.\", \"page\": 7, \"index\": 1, \"width\": 821, \"height\": 359}, {\"url\": \"assets/figures/arxiv/2605.11169v1/fig-002.webp\", \"caption\": \"Figure 1: Overview of OLIVIA. Left: A frozen LLM backbone processes the task and trajectory prefix through its transformer layers, producing hidden states and a reasoning trace (Thought). Right: At each action-selection step, OLIVIA extracts the last-layer hidden state as the decision context and scores candidate tools using per-action UCB estimates.\", \"page\": 3, \"index\": 2, \"width\": 822, \"height\": 292}, {\"url\": \"assets/figures/arxiv/2605.11169v1/fig-003.webp\", \"caption\": \"Figure 12: Reward Studies.\", \"page\": 18, \"index\": 3, \"width\": 824, \"height\": 409}, {\"url\": \"assets/figures/arxiv/2605.11169v1/fig-004.webp\", \"caption\": \"Table 1: Main results (F1) across four benchmarks and two backbone models. First three baselines with fixed policies; following methods below are online learning methods that adapt from deployment feedback.\", \"page\": 8, \"index\": 4, \"width\": 694, \"height\": 356}, {\"url\": \"assets/figures/arxiv/2605.11169v1/fig-005.webp\", \"caption\": \"Figure 4: Qwen3-4B on ToolBench. 0 100 200 300 400 500 Step\", \"page\": 17, \"index\": 5, \"width\": 809, \"height\": 210}, {\"url\": \"assets/figures/arxiv/2605.11169v1/fig-006.webp\", \"caption\": \"Figure 5: Qwen3-4B on TaskBench.\", \"page\": 17, \"index\": 6, \"width\": 809, \"height\": 210}]"
motivation: 现有的LLM智能体推理时自适应方法主要依赖间接的上下文操作，缺乏能够直接从动作级反馈中学习并处理不确定性的显式决策层。
method: 将LLM的动作选择层建模为基于上下文线性多臂老虎机的决策层，利用隐藏状态作为特征，并通过UCB算法进行在线策略优化。
result: 在四个基准测试上的实验表明，OLIVIA在任务性能上持续优于静态ReAct模型以及基于提示词的推理时自适应基准方法。
conclusion: 显式的在线决策层为LLM智能体在部署期间的自适应提供了一种比纯提示词或检索方法更有效、更具可追踪性的替代方案。
---

## 摘要
大语言模型（LLM）智能体通过交替进行推理、动作选择和观察来解决序列决策任务。在智能体重复处理相关多步任务的部署场景中，微小的动作选择错误可能会累积，导致无效的工具调用、延迟增加以及可靠性降低。尽管存在部署时改进的需求，但现有的 LLM 智能体推理时自适应方法主要依赖于提示（prompting）或检索，这些方法通过上下文操纵间接地影响行为。对于 ReAct 风格的智能体，此类方法并未暴露一个显式的决策层，该层能够对候选动作进行评分、表示不确定性，或根据动作级反馈进行在线更新。因此，它们在部署期间对可追踪、细粒度且感知不确定性的自适应支持有限。我们提出了 OLIVIA，这是一种针对 ReAct 风格智能体的推理时动作自适应框架。OLIVIA 将 LLM 的最终动作选择层建模为候选动作上的上下文线性老虎机（contextual linear bandit），并将冻结的隐藏状态作为决策上下文。这一选择特别适用于部署，因为它直接在动作选择接口处调整行为，保留了底层的推理过程，并提供了显式的不确定性估计以及基于动作级反馈的轻量级在线更新。通过置信上限（UCB）探索，OLIVIA 以极小的计算开销实现了高样本效率的策略改进。我们在四个基准测试上实例化了 OLIVIA，结果表明，与静态 ReAct 和基于提示的推理时基准方法相比，它持续提高了任务性能。我们的结果表明，显式的在线决策层为 LLM 智能体在部署期间提供了一种替代纯提示或检索式自适应的有效方案。

## Abstract
Large language model agents interleave reasoning, action selection, and observation to solve sequential decision-making tasks. In deployed settings where agents repeatedly handle related multi-step tasks, small action-selection errors can accumulate into wasted tool calls, latency, and reduced reliability. Despite this need for deployment-time improvement, existing inference-time adaptation methods for LLM agents mainly rely on prompting or retrieval, which influence behavior indirectly through context manipulation. For ReAct-style agents, such approaches do not expose an explicit decision layer that can score candidate actions, represent uncertainty, or be updated online from action-level feedback. As a result, they provide limited support for trackable, fine-grained, and uncertainty-aware adaptation during deployment. We propose OLIVIA, an inference-time action adaptation framework for ReAct-style agents. OLIVIA models the LLM's final action-selection layer as a contextual linear bandit over candidate actions, with frozen hidden states as decision contexts. This choice is particularly suitable for deployment because it adapts behavior directly at the action-selection interface, preserves the underlying reasoning process, and provides explicit uncertainty estimates and lightweight online updates from action-level feedback. With upper-confidence-bound exploration, OLIVIA improves the policy sample-efficiently with minimal computational overhead. We instantiate OLIVIA on four benchmarks and show that it consistently improves task performance over static ReAct and prompt-based inference-time baselines. Our results suggest that explicit online decision layers provide an effective alternative to purely prompt- or retrieval-based adaptation for LLM agents during deployment.

---

## 论文详细总结（自动生成）

### 论文总结：OLIVIA —— LLM ReAct 智能体的推理时动作自适应在线学习

#### 1. 核心问题与整体含义（研究动机和背景）
在实际部署中，大语言模型（LLM）智能体（如工具调用助手）通常需要处理一系列相关的多步决策任务。现有的 ReAct 框架虽然支持推理与动作的交替，但其策略在部署后通常是**静态**的。
*   **研究动机**：智能体在重复任务中容易犯同样的错误，且错误会随步骤累积。
*   **现有方法局限**：目前的推理时自适应（如提示词工程、反思、记忆增强）多通过操纵上下文间接影响行为，缺乏一个显式的决策层来量化动作评分、表示不确定性或根据动作级反馈进行实时更新。
*   **核心目标**：开发一种轻量级、无需微调、能直接在动作选择接口处进行在线优化的框架。

#### 2. 方法论：核心思想与关键技术
OLIVIA 将 ReAct 智能体的动作选择建模为一个**上下文线性多臂老虎机（Contextual Linear Bandit）**问题。
*   **核心思想**：在冻结的 LLM 之上添加一个轻量级的线性决策层。利用 LLM 内部的隐藏状态作为上下文特征，通过在线学习不断调整动作偏好。
*   **关键技术细节**：
    *   **上下文提取**：提取 LLM 最后一层在动作决策点前的隐藏状态 $u_{e,t}$ 作为上下文向量，它编码了任务语义和推理历史。
    *   **动作评分与探索（LinUCB）**：使用置信上限（UCB）算法计算每个候选动作的得分。得分由“当前奖励估计”和“不确定性奖励（探索项）”组成，公式为：$Score = \hat{\theta}^\top u + \alpha \sqrt{u^\top A^{-1} u}$。
    *   **LLM 对齐初始化**：为了不破坏预训练知识，决策层的初始参数使用 LLM 自身的动作名称嵌入（Embedding）进行热启动。
    *   **在线更新**：每步动作后，根据环境反馈（如工具是否正确）获得二元奖励，利用 **Sherman-Morrison 公式**进行秩一更新（Rank-one update），实现 $O(d^2)$ 复杂度的增量学习，无需反向传播。

#### 3. 实验设计
*   **数据集/场景**：涵盖了四个主流的工具使用基准测试：
    1.  **ToolBench**：复杂的 API 调用场景。
    2.  **TaskBench**：任务自动化基准。
    3.  **TaskBench-MM**：多模态任务自动化。
    4.  **BFCL (Berkeley Function Calling Leaderboard)**：函数调用能力评估。
*   **对比方法（Baselines）**：
    *   **静态方法**：CoT（纯思维链）、BM25（基于检索）、标准 ReAct。
    *   **在线学习方法**：**CLIN**（基于记忆和反思的持续学习方法）、纯 Bandit 策略。
*   **评估指标**：多集匹配的精确率（Precision）、召回率（Recall）和 **F1 分数**。

#### 4. 资源与算力
*   **模型**：使用了开源权重模型 **Qwen3-4B** 和 **Mistral-7B-v0.1**。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。但作者强调该方法是“轻量级”的，仅涉及矩阵向量乘法和增量更新，计算开销极小，适合在推理过程中实时运行。

#### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同领域的基准测试上进行了测试，并使用了 2 种不同的主流底座模型。
*   **消融实验**：
    *   验证了**探索系数 $\alpha$** 的敏感性。
    *   对比了**奖励设计**（步级反馈 vs. 剧终反馈）。
    *   通过**合成数据（Synthetic Validation）**验证了控制器收敛到最优参数的能力。
*   **客观性**：实验采用了 3 个随机种子取平均值，并展示了随时间变化的在线学习曲线（Learning Curves），能够清晰观察到性能的稳步提升，实验设计较为全面、公平。

#### 6. 主要结论与发现
*   **性能提升**：OLIVIA 在所有基准测试中均显著优于静态 ReAct 和基于提示词的在线学习方法（CLIN）。
*   **收敛速度**：相比于基于文本记忆的方法，OLIVIA 的参数化决策层收敛更快，能更有效地利用稀疏的反馈信号。
*   **反馈粒度**：步级（Step-level）反馈对于多步决策任务至关重要，结合局部反馈和全局评估能达到最佳效果。
*   **不确定性意识**：显式的 UCB 探索机制能有效避免智能体陷入局部最优，通过尝试不确定的动作来修正初始偏见。

#### 7. 优点
*   **非侵入性**：无需微调 LLM 参数，保持了底座模型的推理能力。
*   **高效性**：在线更新算法极其轻量，几乎不增加推理延迟。
*   **可解释性**：通过显式的动作评分和不确定性估计，使得智能体的决策过程比纯提示词方法更具可追踪性。
*   **冷启动友好**：通过 LLM 嵌入初始化，继承了预训练模型的语义先验。

#### 8. 不足与局限
*   **反馈依赖**：该方法高度依赖于能够即时提供的准确奖励信号（如 Ground Truth 动作列表），在缺乏明确反馈的开放域任务中应用受限。
*   **动作空间限制**：目前主要针对离散的候选动作集（如工具库），对于动作空间无限或连续的任务（如自由文本生成）尚需进一步扩展。
*   **分布偏移风险**：虽然论文展示了在相关任务流上的提升，但如果部署环境发生剧烈分布偏移，线性模型可能面临灾难性遗忘或适应缓慢的问题。

（完）
