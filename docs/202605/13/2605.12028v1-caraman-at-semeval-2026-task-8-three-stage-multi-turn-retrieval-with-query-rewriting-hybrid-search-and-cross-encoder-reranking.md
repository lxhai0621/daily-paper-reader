---
title: "Caraman at SemEval-2026 Task 8: Three-Stage Multi-Turn Retrieval with Query Rewriting, Hybrid Search, and Cross-Encoder Reranking"
title_zh: Caraman 在 SemEval-2026 任务 8：结合查询重写、混合搜索和交叉编码器重排序的三阶段多轮检索
authors: "David-Maximilian Caraman, Gheorghe Cosmin Silaghi"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12028v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 包含查询重写和混合搜索的三阶段多轮检索系统
tldr: 本文针对SemEval-2026多轮检索任务提出三阶段流水线：利用LoRA微调的Qwen模型重写查询，结合BM25与稠密检索进行混合搜索，并使用BGE模型重排序。该系统在官方测试中排名第8，显著优于基准，并发现领域特定的生成温度调节对性能提升至关重要。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.12028v1/fig-001.webp\", \"caption\": \"Table 6: Complete hyperparameter configuration for the final submission.\", \"page\": 7, \"index\": 1, \"width\": 411, \"height\": 1044}]"
motivation: 解决多轮对话中后续问题依赖上下文、难以直接进行有效检索的挑战。
method: 结合了基于LoRA微调的查询重写、BM25与稠密向量的混合检索以及交叉编码器重排序技术。
result: "系统在官方测试集上达到0.531的nDCG@5，在38支队伍中位列第8，超出基准线10.7%。"
conclusion: 针对不同领域调整查询生成的随机性（温度）能带来稳定收益，而复杂的提示策略可能适得其反。
---

## 摘要
我们介绍了为 SemEval-2026 任务 8 (MTRAGEval) 开发的系统，该系统参与了四个英语领域的任务 A（检索）。我们的方法采用了一个三阶段流水线：(1) 通过 LoRA 微调的 Qwen 2.5 7B 模型进行查询重写，将依赖上下文的后续问题转换为独立查询；(2) 通过倒数排名融合（Reciprocal Rank Fusion）结合 BM25 和稠密检索的混合检索；(3) 使用 BGE-reranker-v2-m3 进行交叉编码器重排序。在官方测试集上，该系统实现了 0.531 的 nDCG@5，在 38 个参赛系统中排名第 8，比组织者基准高出 10.7%。开发阶段的对比表明，针对查询生成的特定领域温度调节（其中技术领域受益于确定性解码，而通用领域受益于受控的随机性）带来了持续的提升，而更复杂的策略（如领域感知提示和多查询扩展）则会降低性能。

## Abstract
We describe our system for SemEval-2026 Task 8 (MTRAGEval), participating in Task A (Retrieval) across four English-language domains. Our approach employs a three-stage pipeline: (1) query rewriting via a LoRA-fine-tuned Qwen 2.5 7B model that transforms context-dependent follow-up questions into standalone queries, (2) hybrid BM25 and dense retrieval combined through Reciprocal Rank Fusion, and (3) cross-encoder reranking with BGE-reranker-v2-m3. On the official test set, the system achieves nDCG@5 of 0.531, ranking 8th out of 38 participating systems and 10.7% above the organizer baseline. Development comparisons reveal that domain-specific temperature tuning for query generation, where technical domains benefit from deterministic decoding and general domains from controlled randomness, provides consistent gains, while more complex strategies such as domain-aware prompting and multi-query expansion degrade performance.

---

## 论文详细总结（自动生成）

这篇论文介绍了 Caraman 团队在 SemEval-2026 任务 8（MTRAGEval）中的参赛系统，重点解决多轮对话检索中的上下文依赖问题。

### 1. 核心问题与整体含义
在多轮对话中，用户后续提出的问题往往包含代词（如“它”）、省略或隐含的主题延续，导致检索系统如果仅处理当前话语会丢失关键上下文。论文的核心目标是构建一个鲁棒的检索流水线，将依赖上下文的对话问题转化为独立的检索查询，并从大规模跨领域语料库中准确检索相关文档。

### 2. 核心方法论
该系统采用了一个**三阶段检索流水线**：
*   **第一阶段：查询重写（Query Rewriting）**
    *   使用 **Qwen 2.5 7B Instruct** 模型作为基础，通过 **LoRA** 技术在 MTRAGEval 标注数据上进行微调。
    *   核心思想是将多轮对话历史与当前问题结合，生成一个自包含、适合搜索的独立查询。
    *   **关键创新：** 引入了**领域特定温度调节**。技术领域（如 Cloud）使用 0.0 的低温度以保证术语准确性；非正式领域（如 FiQA 论坛）使用 0.3 的稍高温度以增加表达多样性。
*   **第二阶段：混合检索（Hybrid Search）**
    *   **词法检索：** 使用 BM25S 算法捕捉精确匹配（不进行词干提取以保留技术术语）。
    *   **稠密检索：** 使用 BGE-base-en-v1.5 模型生成向量，通过 FAISS 进行相似度搜索。
    *   **融合：** 采用**倒数排名融合（RRF）**算法合并两者的结果列表。
*   **第三阶段：交叉编码器重排序（Cross-Encoder Reranking）**
    *   使用 **BGE-reranker-v2-m3** 对混合检索出的前 50 个候选文档进行精细化打分。

### 3. 实验设计
*   **数据集：** MTRAGEval 评测集，包含四个英语领域：**ClapNQ**（维基百科）、**Cloud**（IBM 技术文档）、**FiQA**（金融论坛）和 **Govt**（美国政府政策）。
*   **规模：** 语料库包含约 36.6 万个段落；测试集包含 777 个查询。
*   **Benchmark：** 官方组织者提供的基准线。
*   **对比实验：**
    *   对比了不同重排序模型（BGE, IBM Granite, MiniLM）。
    *   对比了不同候选池大小（k=30 到 500）。
    *   对比了不同的查询策略（简单重写 vs. 领域感知提示 vs. 多查询扩展）。

### 4. 资源与算力
*   **硬件：** 实验完全在 **Apple M4 Max (128 GB 统一内存)** 上完成。
*   **框架：** 使用了 Apple 的 **MLX** 框架进行 LoRA 微调，检索和重排序使用 PyTorch 及 MPS 加速。
*   **训练细节：** LoRA 微调进行了 500 次迭代，有效批大小为 16。

### 5. 实验数量与充分性
作者进行了较为充分的消融实验和参数搜索：
*   **温度扫描：** 对 7 个温度值在 4 个领域分别进行了端到端测试。
*   **模型筛选：** 测试了 4 种不同的重排序模型。
*   **负面结果分析：** 诚实地报告了“领域感知提示”和“多查询扩展”反而导致性能下降的实验结果。
*   **客观性：** 实验在独立的 holdout 开发集上进行调优，并在官方盲测集上验证，具有较高的客观性和公平性。

### 6. 主要结论与发现
*   **系统表现：** 在官方测试集上 nDCG@5 达到 **0.531**，在 38 个参赛系统中排名 **第 8**，超出基准线 10.7%。
*   **重写至关重要：** 查询重写是提升性能最显著的环节（相对提升 13.7%）。
*   **领域差异：** 发现领域正式程度与最佳生成温度之间存在相关性，技术领域需要更确定性的生成。
*   **反直觉发现：** 增加重排序的候选池大小（超过 50 个）反而会引入噪声并降低性能。

### 7. 优点
*   **实用性强：** 采用三阶段架构，逻辑清晰，易于在实际 RAG 系统中落地。
*   **精细化调优：** 针对不同领域定制生成温度，体现了对数据特征的深入理解。
*   **透明度高：** 详细记录了失败的尝试（如复杂提示策略的失效），为后续研究提供了宝贵的避坑指南。

### 8. 不足与局限
*   **语言限制：** 目前仅支持英语，未测试多语言环境。
*   **硬件依赖：** 实验受限于单机消费级硬件，未能探索更大规模（如 14B+）的重写模型。
*   **融合策略简单：** RRF 融合未考虑不同领域下词法与语义检索的权重差异，未来可探索加权融合。
*   **长上下文挑战：** 虽然当前数据集未超出限制，但对于极长对话历史的截断策略研究不足。

（完）
