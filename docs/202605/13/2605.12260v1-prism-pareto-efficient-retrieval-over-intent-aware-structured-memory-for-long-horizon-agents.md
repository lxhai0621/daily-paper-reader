---
title: "PRISM: Pareto-Efficient Retrieval over Intent-Aware Structured Memory for Long-Horizon Agents"
title_zh: PRISM：面向长程智能体的意图感知结构化记忆上的帕累托高效检索
authors: "Jingyi Peng, Zhongwei Wan, Weiting Liu, Qiuzhuang Sun"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12260v1"
tags: ["query:agent"]
score: 10.0
evidence: 长程智能体在图结构存储上的记忆管理
tldr: 针对长程语言智能体对话历史过长导致上下文窗口受限的问题，本文提出PRISM框架。它将长程记忆视为图结构上的联合检索与压缩问题，通过分层捆绑搜索、查询敏感边缘成本计算、证据压缩和自适应意图路由，在无需微调的情况下实现了高精度与低成本的平衡。实验证明其在LoCoMo基准上以极小的上下文预算达到了领先的准确率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.12260v1/fig-001.webp\", \"caption\": \"Figure 1: (a) Existing memory designs cluster in three regions of the accuracy–context-cost plane, leaving the high-accuracy / low-cost corner underfilled. (b) PRISM is the only system that combines all six design dimensions we identify as relevant.\", \"page\": 2, \"index\": 1, \"width\": 814, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2605.12260v1/fig-002.webp\", \"caption\": \"Table 1: Results on LoCoMo (categories 1–4, 1,540 QA pairs). Best same-protocol result per column in bold; the different-protocol group is reported for reference only.\", \"page\": 7, \"index\": 2, \"width\": 818, \"height\": 259}, {\"url\": \"assets/figures/arxiv/2605.12260v1/fig-003.webp\", \"caption\": \"Figure 4: Per-category routing distribution of Adaptive Intent Routing (N4) on LoCoMo cat 1–4. Each bar shows the share of queries dispatched through each routing path. The keyword_gated, prototype, and none paths incur zero LLM calls; only the LLM path incurs one classifier-side LLM call per query. The annotation marks the overall no-LLM rate of 42.3%.\", \"page\": 22, \"index\": 3, \"width\": 686, \"height\": 359}, {\"url\": \"assets/figures/arxiv/2605.12260v1/fig-004.webp\", \"caption\": \"Figure 2: Architectural overview of PRISM. PRISM is composed of a four-layer memory graph and four inference-time modules: (1) N4 routes query intent; (2) N2 adjusts traversal costs over typed edges; (3) N1 searches relation paths and assembles candidate bundles; and (4) N3 compresses retrieved evidence into a compact context for the answer model.\", \"page\": 4, \"index\": 4, \"width\": 802, \"height\": 458}, {\"url\": \"assets/figures/arxiv/2605.12260v1/fig-005.webp\", \"caption\": \"Table 2: Component ablation on LoCoMo. Each row changes one flag relative to PRISM; bold marks large deviations from PRISM.\", \"page\": 9, \"index\": 5, \"width\": 826, \"height\": 192}, {\"url\": \"assets/figures/arxiv/2605.12260v1/fig-006.webp\", \"caption\": \"Table 5: Hyperparameter configuration for PRISM. All values are fixed across LoCoMo categories 1– 4 and held identical across the same-protocol rows of Table 1.\", \"page\": 16, \"index\": 6, \"width\": 794, \"height\": 898}]"
motivation: 现有的长程记忆管理方法在处理快速累积的对话历史时，难以兼顾检索准确性、推理成本和上下文窗口限制。
method: 提出一种无需训练的检索框架，结合图路径搜索、意图感知成本计算、证据压缩及零LLM路由技术，优化记忆提取。
result: 在LoCoMo基准测试中，PRISM在上下文预算减少一个数量级的情况下，准确率显著优于所有同类基准模型。
conclusion: PRISM通过在图结构记忆上进行帕累托高效的检索与压缩，为长程智能体提供了一种兼顾质量与效率的记忆管理方案。
---

## 摘要
长程语言智能体积累对话历史的速度远快于任何固定上下文窗口的承载能力，这使得记忆管理对于回答准确性和推理成本都至关重要。现有方法要么在不解决检索内容的情况下扩展上下文窗口，要么在摄入阶段进行高成本的事实提取，或者依赖于牺牲了准确性和效率的启发式图遍历。我们提出了 PRISM，这是一个无需训练的检索端框架，它将长程记忆视为图结构记忆上的联合检索与压缩问题。PRISM 结合了四个正交的推理阶段组件：基于类型化关系路径的分层束搜索、使遍历与检测到的查询意图保持一致的查询敏感边定价、将候选束压缩为紧凑回答侧上下文的证据压缩，以及将大多数查询路由至零 LLM 层的自适应意图路由。通过将检索建模为类型化路径模板上的最小成本选择，并结合 LLM 端的压缩步骤，PRISM 在严格的上下文预算下提取出正确的证据，且无需对上游摄入流水线进行任何微调或修改。在 LoCoMo 基准测试上的实验表明，在上下文预算缩小一个数量级的情况下，PRISM 的 LLM 评测准确率显著高于所有同协议基线，占据了准确率-上下文-成本前沿中此前空白的区域，并展示了回答质量与检索效率之间的卓越平衡。

## Abstract
Long-horizon language agents accumulate conversation history far faster than any fixed context window can hold, making memory management critical to both answer accuracy and serving cost. Existing approaches either expand the context window without addressing what is retrieved, perform heavy ingestion-time fact extraction at substantial token cost, or rely on heuristic graph traversal that leaves both accuracy and efficiency on the table. We present PRISM, a training-free retrieval-side framework that treats long-horizon memory as a joint retrieval-and-compression problem over a graph-structured memory. PRISM combines four orthogonal inference-time components: Hierarchical Bundle Search over typed relation paths, Query-Sensitive Edge Costing that aligns traversal with detected query intent, Evidence Compression that compresses the candidate bundle into a compact answer-side context, and Adaptive Intent Routing that routes most queries through zero-LLM tiers. By formulating retrieval as min-cost selection over typed path templates and pairing it with an LLM-side compression step, PRISM surfaces the right evidence under a strict context budget without any fine-tuning or modification to the upstream ingestion pipeline. Experiments on the LoCoMo benchmark show that PRISM delivers substantially higher LLM-judge accuracy than every same-protocol baseline at an order-of-magnitude smaller context budget, occupying a previously empty corner of the accuracy-context-cost frontier and demonstrating a superior balance between answer quality and retrieval efficiency.

---

## 论文详细总结（自动生成）

### PRISM: 面向长程智能体的意图感知结构化记忆上的帕累托高效检索

#### 1. 核心问题与整体含义
长程语言智能体（Long-horizon agents）在持续对话中会积累海量的历史信息，远超大模型（LLM）的上下文窗口限制。现有的记忆管理方法存在“准确性”与“成本”的失衡：要么直接输入全量上下文（成本极高且准确率受损），要么在存入时进行重度事实提取（计算开销大），或者依赖简单的启发式图搜索（效率和精度均不理想）。
论文的核心动机是寻找**帕累托最优（Pareto-Efficient）**的平衡点，即在极低的上下文标记（Token）预算下，实现极高的检索准确率。

#### 2. 方法论
PRISM 是一个**无需训练（Training-free）**的检索端框架，它将长程记忆检索建模为图结构上的“最小成本路径选择”与“内容压缩”的联合问题。其核心包含四个推理阶段的模块：
*   **分层记忆图构建（Memory Graph）：** 将记忆分为四层（实体 Entity、事实点 FacetPoint、面 Facet、片段 Episode），并建立类型化边（语义、时间、因果、演化等）。
*   **N1: 分层捆绑搜索（Hierarchical Bundle Search）：** 通过 8 种预定义的路径模板（如因果桥接路径），从查询锚点出发搜索候选 Episode，并根据路径成本进行评分。
*   **N2: 查询敏感边缘定价（Query-Sensitive Edge Costing）：** 根据查询意图动态调整图遍历的边权重。例如，对于“为什么”类问题，降低因果边的成本，使检索更倾向于因果链条。
*   **N3: 证据压缩（Evidence Compression）：** 在获取候选 Episode 束后，利用 LLM 对摘要进行重排序和筛选，仅保留最相关的 $M$ 个片段，大幅压缩进入最终回答模型的 Token 数量。
*   **N4: 自适应意图路由（Adaptive Intent Routing）：** 为了降低成本，采用“关键词匹配 -> 原型向量匹配 -> LLM 分类”的三级级联路由，使约 42% 的查询无需调用 LLM 即可识别意图。

#### 3. 实验设计
*   **数据集：** 使用 **LoCoMo** 基准测试，涵盖单跳、多跳、时间推理和开放域推理四类任务，共 1,540 个问答对。
*   **对比方法：** 
    *   **全上下文（Full Context）：** 直接输入所有历史。
    *   **图结构基线：** MAGMA, Mem0g。
    *   **摄入重度型基线：** Mem0, M-Flow。
*   **评测协议：** 使用 GPT-4o-mini 作为回答模型和评判模型（LLM-judge），确保对比的公平性。

#### 4. 资源与算力
*   **算力需求：** 论文未明确提及具体的 GPU 型号和训练时长，因为 PRISM 是**无需训练**的。
*   **推理开销：** 检索和图遍历部分在 CPU 上即可高效运行（基于 FAISS 向量索引）。主要的计算开销来自于 OpenAI API 调用（用于意图路由的兜底、证据压缩和最终回答生成）。
*   **成本优势：** 相比全上下文方法，PRISM 将每个查询的平均上下文 Token 从约 26K 降低到了 2K 左右。

#### 5. 实验数量与充分性
*   **实验规模：** 针对 1,540 个 QA 对进行了详尽测试，并提供了 95% 置信区间和 McNemar 显著性检验。
*   **消融实验：** 针对 N1（关系路径）、N2（成本调整）、N3（LLM 重排）和 N4（路由）分别做了消融，验证了各模块的贡献。
*   **充分性：** 实验设计较为客观，通过统一回答模型和评测标准，消除了模型本身能力差异带来的干扰。此外，作者还进行了人工标注分析（50 组多跳问题），以解释特定模块在特定数据集上的表现。

#### 6. 主要结论与发现
*   **性能领先：** PRISM 在 LoCoMo 基准上达到了 0.831 的准确率，显著优于所有同协议基线。
*   **帕累托高效：** 在上下文 Token 减少 13 倍的情况下，准确率反而比全上下文基线高出 35 个百分点。
*   **压缩是关键：** 消融实验显示，N3（证据压缩）是降低 Token 成本和提升精度的核心驱动力。
*   **检索质量胜过窗口大小：** 即使模型能处理长文本，精准的检索依然比“喂入全部数据”更有效。

#### 7. 优点
*   **即插即用：** 无需对现有模型进行微调，也无需改变上游的数据存储逻辑。
*   **多维优化：** 同时从结构化检索（图路径）和语义压缩（LLM 重排）两个维度解决问题。
*   **成本意识：** N4 路由机制体现了对实际部署中 API 成本的细致考量。

#### 8. 不足与局限
*   **基准局限性：** 在 LoCoMo 数据集上，N1 和 N2（图路径和边定价）的提升不明显。作者分析发现这是因为该数据集 72% 的问题通过简单的锚点匹配即可解决，缺乏复杂的“实体桥接”需求。
*   **依赖摘要质量：** 检索和压缩高度依赖于摄入阶段生成的 Episode 摘要，如果摘要丢失关键细节，后续模块无法补救。
*   **实时性挑战：** 虽然压缩了 Token，但推理过程中仍需 1-2 次额外的 LLM 调用（路由和重排），可能增加端到端的响应延迟。

（完）
