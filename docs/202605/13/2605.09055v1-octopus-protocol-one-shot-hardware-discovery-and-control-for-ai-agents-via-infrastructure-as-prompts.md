---
title: "Octopus Protocol: One-Shot Hardware Discovery and Control for AI Agents via Infrastructure-as-Prompts"
title_zh: Octopus 协议：通过“基础设施即提示词”实现 AI 智能体的一键式硬件发现与控制
authors: "Quilee Simeon, Justin M. Wei, Yile Fan"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.09055v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 为AI智能体自动发现硬件并生成API
tldr: 针对机器人控制中编写硬件驱动和接口的高昂工程成本，本文提出 Octopus Protocol。该系统仅需原始操作系统访问权限和语言模型 API，即可通过五阶段流水线（探测、识别、接口、服务、部署）自动发现硬件、生成 MCP 服务器并部署为 HTTP 端点。它实现了“协议即提示”和“编码代理即运行时”的理念，在多种平台上验证了其在 10-15 分钟内完成硬件适配并实现闭环视觉运动控制的能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.09055v1/fig-001.webp\", \"caption\": \"Fig. 1: Octopus architecture. Device (left): a bootstrap command runs a five-stage build pipeline (PROBE, IDENTIFY, INTERFACE, SERVE, DEPLOY) against whatever hardware is plugged in, yielding a live MCP server and a self-healing daemon. Client (right): any MCP-compliant agent drives the hardware through the generated tools.\", \"page\": 1, \"index\": 1, \"width\": 527, \"height\": 298}, {\"url\": \"assets/figures/arxiv/2605.09055v1/fig-002.webp\", \"caption\": \"Fig. 2: Hardware. (a, left) Design intent: a circular camera-slider rig for multi-view perception (CAD render, aspirational, not yet built). (b, right) Current benchtop prototype: off-the-shelf SO-ARM101 arm (Seeed Studio; 6-DOF Feetech STS3215 servos) and a USB camera on a single-axis vertical post atop a partial circular track, driven by a Raspberry Pi 4. Tool count is capped at 30 per installation and varies by platform and what the agent identifies as controllable.\", \"page\": 2, \"index\": 2, \"width\": 808, \"height\": 234}]"
motivation: 现有的机器人系统依赖预设驱动，而为新硬件编写这些底层接口是 AI 代理控制中最主要的工程成本。
method: 提出一种五阶段自动化流水线，利用编码代理自动探测硬件、推断功能并生成符合模型上下文协议（MCP）的服务接口。
result: 在 PC、macOS、树莓派及 6 自由度机械臂上成功验证，可在 15 分钟内自动生成多达 30 个工具并实现闭环控制。
conclusion: Octopus Protocol 通过将硬件协议转化为提示词，极大地降低了 AI 代理接入新硬件的门槛，实现了硬件的“一键式”自动发现与控制。
---

## 摘要
从 Code-as-Policies 到现代视觉-语言-动作（VLA）基础模型，近期的智能体机器人系统都预设目标硬件的驱动程序、SDK 或 ROS 风格的原语已经存在。编写这些原语是为智能体控制引入新硬件的主要工程成本。我们提出了 Octopus 协议，该系统将这一成本压缩至单个 shell 命令。仅需原始操作系统访问权限和语言模型 API 密钥，编码智能体即可执行一个包含五个阶段的流水线——探测（PROBE）、识别（IDENTIFY）、接口化（INTERFACE）、服务化（SERVE）、部署（DEPLOY）——以发现连接的设备、推断其功能、生成带有类型化工具的模型上下文协议（MCP）服务器，并将其部署为实时 HTTP 端点。随后，一个持久化守护进程会监控系统、修复损坏的代码，并通过其为自己生成的摄像头工具感知物理状态。两项架构原则使这项工作成为可能：协议即提示词而非代码，且编码智能体即运行时。我们在三个异构平台（PC/WSL、Apple Silicon macOS、树莓派 4）以及带有 USB 摄像头反馈的商用 6 自由度机械臂上验证了该系统。单个命令即可在约 10-15 分钟内完成硬件上线，并暴露多达 30 个 MCP 工具；随后，符合 MCP 标准的客户端通过这些非人类编写的工具执行闭环视觉运动控制。

## Abstract
Recent agentic-robotics systems, from Code-asPolicies to modern vision-language-action (VLA) foundation models, presuppose that drivers, SDKs, or ROS-style primitives for the target hardware already exist. Writing those primitives is the dominant engineering cost of bringing up new hardware for agent control. We present Octopus Protocol, a system that collapses that cost to a single shell command. Given only raw OS access and a language-model API key, a coding agent executes a five-stage pipeline--PROBE, IDENTIFY, INTERFACE, SERVE, DEPLOY--to discover connected devices, infer their capabilities, generate a Model Context Protocol (MCP) server with typed tools, and deploy it as a live HTTP endpoint. A persistent daemon then monitors the system, heals broken code, and perceives physical state through the camera tools it generated for itself. Two architectural principles make this work: protocols are prompts, not code, and the coding agent is the runtime. We validate the system on three heterogeneous platforms (PC/WSL, Apple Silicon macOS, Raspberry Pi 4) and on a commercial 6-DOF robotic arm with USB camera feedback. One command onboards the hardware in ~10-15 minutes and exposes up to 30 MCP tools; an MCP-compliant client then performs closed-loop visual-motor control through tools no human wrote.

---

## 论文详细总结（自动生成）

以下是对论文《Octopus Protocol: One-Shot Hardware Discovery and Control for AI Agents via Infrastructure-as-Prompts》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前的具身智能系统（如 Code-as-Policies 或 VLA 模型）通常假设硬件驱动、SDK 或 ROS 原语已经存在。然而，为新硬件编写这些底层接口是极其耗时的工程负担（被称为“胶水代码税”）。
*   **核心目标**：消除硬件接入的工程成本，实现硬件发现、接口生成和控制的“一键式”自动化。
*   **整体含义**：提出 Octopus 协议，将硬件基础设施从“编译后的二进制文件”转变为“提示词级别的规范（Infrastructure-as-Prompts）”，让 AI 智能体能够自主完成从原始操作系统访问到实现闭环控制的全过程。

### 2. 方法论
*   **核心思想**：利用编码智能体（Coding Agent）作为运行时，根据硬件探测结果和提示词规范，动态生成并部署符合模型上下文协议（MCP）的服务。
*   **五阶段构建流水线**：
    1.  **PROBE（探测）**：运行 OS 命令（如 `lsusb`, `gpiodetect`）获取硬件清单。
    2.  **IDENTIFY（识别）**：通过本地查询和网络搜索将设备 ID 映射为具体功能（如设置舵机角度）。
    3.  **INTERFACE（接口化）**：为每个功能生成带有类型定义的 MCP 工具架构。
    4.  **SERVE（服务化）**：编写完整的 FastMCP 服务器代码，包含错误处理和硬件 I/O。
    5.  **DEPLOY（部署）**：安装依赖并启动实时 HTTP/SSE 端点。
*   **持续运行后端**：包含 **WATCH**（监控日志）、**HEAL**（自动修复损坏代码或重装依赖）和 **PERCEIVE**（利用生成的摄像头工具进行视觉状态总结）。
*   **关键原则**：协议即提示词（添加新硬件只需编辑 Markdown 规范），编码智能体即运行时（代码是在部署时根据平台特性生成的）。

### 3. 实验设计
*   **实验场景**：
    *   **多平台移植性测试**：在 Windows/WSL、Apple Silicon macOS 和树莓派 4 三种异构平台上运行相同的引导命令。
    *   **端到端任务**：使用树莓派驱动一个 6 自由度的商用机械臂（SO-ARM101）和 USB 摄像头。
    *   **自修复测试**：人为制造依赖缺失、硬件热插拔和代码损坏等故障。
*   **Benchmark/对比**：论文未提及标准数据集（因其针对的是底层基础设施），主要对比的是传统人工编写驱动的流程。验证指标包括工具生成数量（18-30 个）和 14 项集成测试的通过率。

### 4. 资源与算力
*   **算力说明**：文中**未明确提及**具体的 GPU 型号或训练时长。
*   **实现细节**：该系统主要依赖大语言模型（LLM）的 API（如 Claude 或 GPT 系列，通过 MCP 协议连接）进行代码生成。编排器本身非常轻量（约 640 行 Python 代码和 560 行 Markdown 规范）。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同操作系统平台上验证了自动化上线能力，并在 1 个真实的机械臂平台上完成了闭环视觉运动控制演示。
*   **充分性评价**：作为一篇短篇论文，其实验涵盖了跨平台兼容性、物理交互和鲁棒性（自修复），足以证明其概念的可行性。但由于仅测试了一种机械臂，其对更复杂或罕见硬件协议的泛化能力仍有待更大规模的验证。

### 6. 主要结论与发现
*   **高效性**：系统可在 10-15 分钟内完成从零到实时硬件 API 的部署。
*   **灵活性**：同一套提示词规范在不同平台上生成了适配该平台的特定实现。
*   **自主性**：AI 智能体能够利用自己生成的工具（如摄像头感知）实现闭环控制，无需人类编写任何针对特定机械臂的代码。
*   **可靠性**：自修复机制能有效处理运行时的环境变化和代码错误。

### 7. 优点
*   **架构创新**：提出了“基础设施即提示词”的概念，极大地降低了机器人开发的门槛。
*   **标准化**：采用 Anthropic 提出的 MCP 协议，使其生成的工具能直接被各种主流 AI 客户端调用。
*   **闭环能力**：不仅能写驱动，还能通过生成的工具进行自我感知和维护。

### 8. 不足与局限
*   **安全风险**：自动生成的硬件控制代码可能存在安全隐患，缺乏严格的物理安全约束证明（作者将其列为未来工作）。
*   **协议深度**：对于需要极高性能或极低延迟的复杂工业协议，LLM 生成的代码可能无法完全替代深度优化的原生驱动。
*   **依赖性**：系统高度依赖 LLM API 的质量和网络连接，且在处理完全未知的专有协议时可能受限。

（完）
