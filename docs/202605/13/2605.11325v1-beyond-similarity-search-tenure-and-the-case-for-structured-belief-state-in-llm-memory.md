---
title: "Beyond Similarity Search: Tenure and the Case for Structured Belief State in LLM Memory"
title_zh: 超越相似度搜索：Tenure 及其在 LLM 记忆中构建结构化信念状态的论据
authors: Jeffrey Flynt
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.11325v1"
tags: ["query:agent"]
score: 9.0
evidence: 用于跨会话大模型记忆管理的结构化信念状态
tldr: 本研究针对LLM在跨会话记忆中语义搜索失效的问题，提出了Tenure系统。Tenure摒弃了传统的向量相似度检索，采用本地优先的结构化信念库，结合硬范围隔离和别名加权的BM25算法。该系统通过特定字段将事实转化为可执行指令，确保在特定上下文中精准注入背景信息。实验表明，Tenure在检索精度上远超余弦相似度，有效解决了多轮对话中的主题漂移问题，为LLM状态管理提供了新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.11325v1/fig-001.webp\", \"caption\": \"Figure 1: BM25 returns exactly one belief (b-redis-code, score 11.71); all others score zero. Vector search returns all twelve beliefs in a 0.132-wide cosine band (0.667–0.799), with the correct belief ranked second behind b-ts-pref at 0.759, recall is identical, the gap is entirely precision. Query: “What are we using Redis for?” against the 30-belief seed corpus spanning two domain scopes and one secondary-user isolation fixture. The shaded region marks the indiscriminable cosine band.\", \"page\": 11, \"index\": 1, \"width\": 949, \"height\": 381}, {\"url\": \"assets/figures/arxiv/2605.11325v1/fig-002.webp\", \"caption\": \"Table 1: The score distribution for a representative query; the mechanism is structural and independent of threshold or model choice.\", \"page\": 10, \"index\": 2, \"width\": 944, \"height\": 309}, {\"url\": \"assets/figures/arxiv/2605.11325v1/fig-003.webp\", \"caption\": \"Figure 2: The turn 10 spike reflects the cross-session formative case: a subsequent session queries allkeys-lru, an alias added to b-redis-code by the extraction worker after turn 9, retrieving the correct belief at score 10.26, but the vector backend simultaneously retrieves every other belief in the corpus, producing a drift score of 0.50.\", \"page\": 14, \"index\": 3, \"width\": 906, \"height\": 462}, {\"url\": \"assets/figures/arxiv/2605.11325v1/fig-004.webp\", \"caption\": \"Table 2: Evaluation suite case distribution by category.\", \"page\": 18, \"index\": 4, \"width\": 518, \"height\": 436}, {\"url\": \"assets/figures/arxiv/2605.11325v1/fig-005.webp\", \"caption\": \"Table 4: Belief field reference.\", \"page\": 23, \"index\": 5, \"width\": 978, \"height\": 777}]"
motivation: 现有的基于语义相似度的检索方法在处理特定术语和实体消歧时精度极低，导致LLM跨会话记忆管理失效。
method: 开发了Tenure系统，利用结构化信念库、硬范围隔离以及别名加权的BM25检索来精确管理和注入LLM会话状态。
result: 在72个测试案例中，Tenure实现了1.0的平均精度，而传统的余弦相似度仅为0.12，且在多轮对话中表现出更强的抗漂移能力。
conclusion: 跨会话记忆本质上是状态管理问题而非搜索问题，通过结构化信念和精确匹配算法能更有效地提升LLM的上下文感知能力。
---

## 摘要
为什么我们需要另一个 AI 来辅助 AI？我们认为并不需要。无状态的大语言模型（LLM）会话在迭代式、重度依赖会话的工作流中会产生重新定位的成本。先前的研究通过检索增强方法解决跨会话记忆问题：存储历史记录、对其进行嵌入，并根据语义相似度进行检索。跨会话记忆本质上是一个状态管理问题，而非搜索问题。在受限词汇上下文中，相似度搜索在命名实体消解方面表现不佳，因为关于共享技术领域的信念在结构上具有语义接近性。单个用户是最简单的受限词汇上下文；工程团队通过共享代码库和术语收敛到相同的属性。我们提出了 Tenure，这是一个本地优先的代理，它维护一个具有认识状态、版本化更替和范围隔离的有类型信念库，通过精准优先的检索将精选上下文注入到每个 LLM 会话中。硬性范围隔离提供了一种结构性保证：正确的信念会浮现，且仅在用户授权的边界内。Tenure 的有类型模式通过“重要性原因”字段将提取的事实转换为命令式指令，使注入的信念直接可操作，而不是作为模型重新推导的原始材料。对 72 个检索案例的受控评估展示了这种差距。基于稠密嵌入的余弦相似度实现了 0.12 的平均精确度。别名加权的 BM25 保持了 1.0 的平均精确度，在同一语料库上通过了 72/72 个案例，而余弦相似度仅为 8/72。混合检索通常解决不同作者之间的词汇不匹配问题；Tenure 从结构上消除了这一点：查询者和信念作者是同一个人，且别名丰富飞轮会持续索引他们的特定词汇。在多轮话题漂移下，情况会进一步恶化：在对噪声敏感的轮次中，向量后端产生的漂移分数为 0.43--0.50，而 BM25 保持为 0。

## Abstract
Why do we need another AI to help the AI? We argue you don't. Stateless LLM sessions impose re-orientation costs on iterative, session-heavy workflows. Prior work addresses cross-session memory through retrieval-augmented approaches: store history, embed it, retrieve by semantic similarity. Cross-session memory is a state management problem, not a search problem. Similarity search fails for named entity resolution within bounded vocabulary contexts because beliefs about a shared technical domain are semantically proximate by construction. A single user is the simplest bounded vocabulary context; engineering teams converge on the same property through shared codebases and terminology.   We present Tenure, a local-first proxy that maintains a typed belief store with epistemic status, versioned supersession, and scope isolation, injecting curated context into every LLM session through precision-first retrieval. Hard scope isolation provides a structural guarantee: the right beliefs surface, and only within the boundaries the user has authorized. Tenure's typed schema converts extracted facts into imperative instructions via a why it matters field, making injected beliefs directly actionable rather than raw material for the model to re-derive.   A controlled evaluation on 72 retrieval cases demonstrates the gap. Cosine similarity over dense embeddings achieves mean precision of 0.12. Alias-weighted BM25 maintains mean precision of 1.0, passing 72/72 cases versus 8/72 for cosine similarity on the same corpus. Hybrid retrieval typically solves vocabulary mismatch between disparate authors; Tenure eliminates this structurally: query and belief authors are the same person, and an alias enrichment flywheel continuously indexes their specific vocabulary. Under multi-turn topic drift this worsens: the vector backend produces drift scores of 0.43--0.50 on noise-critical turns where BM25 maintains 0.

---

## 论文详细总结（自动生成）

这是一份关于论文《Beyond Similarity Search: Tenure and the Case for Structured Belief State in LLM Memory》的结构化深入分析报告：

### 1. 核心问题与整体含义
*   **研究动机**：大语言模型（LLM）的会话是无状态的，导致用户在迭代工作流中面临沉重的“重新定位税”（Re-Orientation Tax），即每次新会话都要重复解释背景。
*   **核心矛盾**：现有的主流方案（如 ChatGPT Memory, Mem0）将跨会话记忆视为**搜索问题**，依赖向量嵌入的语义相似度检索。作者认为这是错误的抽象，因为在特定技术领域或单用户语料中，所有概念在语义上都极其接近（如 Redis 和 MongoDB），导致相似度检索精度极低，引入大量噪声。
*   **整体含义**：论文主张跨会话记忆本质上是一个**状态管理问题**。通过构建结构化的“信念状态”（Belief State）而非存储原始对话片段，可以实现更精准、可控的上下文注入。

### 2. 方法论：Tenure 系统
*   **核心思想**：采用“本地优先”的代理架构，通过结构化提取、硬性范围隔离和精准匹配检索来管理记忆。
*   **关键技术细节**：
    *   **结构化信念模式**：每个信念包含 14 个字段，包括类型（偏好、决策、实体等）、认识状态（活跃、推断、已更替）、范围（全局、领域、项目）以及版本更替链。
    *   **命令式指令（Why it matters 字段）**：这是 Tenure 的核心创新。它不只记录事实（如“使用 TypeScript”），而是记录该事实对未来响应的影响（如“所有代码示例应采用严格模式”），将事实转化为模型可直接执行的指令。
    *   **检索机制**：摒弃向量搜索，采用**别名加权的 BM25 算法**。针对“长查询匹配短文档”的倒排检索问题进行了索引工程优化（如分析器不对称性、Shingle 路径、14倍别名权重提升）。
    *   **硬性范围隔离**：通过作用域（Scope）作为硬过滤器，确保项目 A 的隐私或决策绝不会干扰项目 B。
    *   **别名丰富飞轮（Alias Enrichment Flywheel）**：在对话中自动捕获实体的不同称呼（如 k8s, kube），不断增强检索精度。

### 3. 实验设计
*   **数据集/场景**：构建了一个包含 72 个检索案例的评估套件。
    *   **60 个静态案例**：涵盖别名解析、范围消歧、版本更替链排除、模糊匹配、跨用户隔离等。
    *   **12 个会话级案例**：测试在多轮对话、话题剧烈漂移（从 Redis 到 K8s 再回到 Redis）情况下的检索稳定性。
*   **Benchmark**：作者发布了一个可重复使用的基准测试套件，针对单用户持久记忆的结构化属性进行测试。
*   **对比方法**：将 Tenure 的 **别名加权 BM25** 与基于 **nomic-embed-text (768维) 的余弦相似度向量搜索** 进行直接对比。

### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 训练时长或型号。
*   **系统性质**：Tenure 被设计为一个本地运行的 Docker 容器，其核心检索逻辑（BM25）和状态管理对算力要求极低。
*   **模型依赖**：系统在提取阶段依赖“前沿模型”（Frontier Models，如 GPT-4 或 Claude 3.5），并设有“模型层级门禁”（Tier Gate），只有达到特定结构化输出可靠性的模型才能触发记忆提取。

### 5. 实验数量与充分性
*   **实验规模**：共 72 组受控实验。虽然样本绝对数量级不及大规模预训练数据集，但作为“正确性验证”实验，其覆盖面非常广泛。
*   **充分性与公平性**：实验在相同的种子语料库（30 个信念）和相同的测试框架下运行。通过引入“漂移分数”（Drift Score）这一指标，客观地量化了向量搜索在多轮对话中由于语义堆叠导致的精度崩溃。实验设计具有很强的针对性，证明了在受限词汇表（Bounded Vocabulary）场景下，结构化方法对统计学方法的压倒性优势。

### 6. 主要结论与发现
*   **精度差距巨大**：在 72 个案例中，BM25 实现了 **1.0** 的平均精度（72/72 通过），而余弦相似度仅为 **0.12**（8/72 通过）。
*   **向量搜索的结构性失效**：在特定领域内，向量搜索无法区分语义接近的实体。例如，查询 Redis 时，向量后端会同时召回 K8s、MongoDB 等所有相关技术栈，产生 0.43-0.50 的高漂移噪声。
*   **状态管理优势**：通过“版本更替”机制，系统能有效防止“上下文腐烂”（Context Rot），确保旧的、被覆盖的决策不会干扰模型。

### 7. 优点
*   **精准性与确定性**：通过硬性范围隔离和精准匹配，消除了 RAG 中常见的幻觉和噪声注入。
*   **可观察性与可维护性**：信念库是人类可读、可编辑的 JSON 状态，而非不可解释的向量维度，方便用户审计和纠错。
*   **指令化记忆**：`why it matters` 字段极大地降低了模型在推理时的认知负担，将“推导背景”转变为“执行指令”。

### 8. 不足与局限
*   **提取质量依赖**：系统的有效性高度依赖于 LLM 在会话结束时的提取能力，若模型提取失败或产生错误信念，会影响后续会话。
*   **冷启动问题**：在用户尚未建立足够信念库的初期，系统优势不明显。
*   **适用范围限制**：该方法在单用户或词汇高度收敛的小型团队中表现最佳；在词汇极度发散、无共识的大规模多用户公共知识库场景下，可能仍需结合语义搜索。
*   **手动干预需求**：虽然有自动化压缩机制，但在复杂冲突下可能仍需用户手动介入审核信念冲突。

（完）
