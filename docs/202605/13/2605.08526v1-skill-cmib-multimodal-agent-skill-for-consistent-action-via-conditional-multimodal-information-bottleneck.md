---
title: "Skill-CMIB: Multimodal Agent Skill for Consistent Action via Conditional Multimodal Information Bottleneck"
title_zh: Skill-CMIB：通过条件多模态信息瓶颈实现一致动作的多模态智能体技能
authors: "Zihan Huang, Junda Wu, Tong Yu, Qianqi Yan, Rohan Surana, Uttaran Bhattacharya, Lina Yao, Xin Eric Wang, Julian McAuley"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.08526v1"
tags: ["query:agent"]
score: 9.0
evidence: 用于一致性动作和技能蒸馏的多模态大模型智能体
tldr: 针对LLM智能体在执行任务时表现不一致的问题，本文提出Skill-CMIB方法。该方法利用条件多模态信息瓶颈，将试错轨迹蒸馏为可重用的多模态技能。通过将技能分解为可解释的文本技能卡和互补的感知残差信息，Skill-CMIB在减少冗余的同时保留了关键任务信息，显著提升了智能体在多模态环境下的执行稳定性，且无需高昂的多样本推理开销。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.08526v1/fig-001.webp\", \"caption\": \"Table 3: Model Performance on Multimodal-Mind2Web. Results present as percentages.\", \"page\": 9, \"index\": 1, \"width\": 826, \"height\": 401}, {\"url\": \"assets/figures/arxiv/2605.08526v1/fig-002.webp\", \"caption\": \"Table 1: Results across different settings on Multimodal-Mind2Web (Deng et al., 2023).\", \"page\": 8, \"index\": 2, \"width\": 827, \"height\": 430}, {\"url\": \"assets/figures/arxiv/2605.08526v1/fig-003.webp\", \"caption\": \"Table 2: Action Success Rate (%) compared with baselines on ScreenSpot (Deng et al., 2023).\", \"page\": 8, \"index\": 3, \"width\": 698, \"height\": 212}]"
motivation: 现有的LLM智能体在执行长序列任务时缺乏一致性，且难以在多模态场景下有效提取并平衡文本与视觉中的关键技能信息。
method: 提出条件多模态信息瓶颈（CMIB），通过两阶段分解分别提取可解释的文本技能卡和与其互补的条件感知残差，并利用变分目标进行优化。
result: 该方法成功构建了低冗余、高效率的多模态技能，在不增加推理采样成本的前提下，显著增强了智能体动作执行的稳定性。
conclusion: CMIB为多模态智能体的技能内化提供了一种结构化方案，通过解耦文本与感知信息，实现了更可靠的任务执行。
---

## 摘要
虽然基于大语言模型（LLM）的智能体在规划和执行长动作序列方面表现出色，但其执行在不同尝试中往往保持不一致，限制了可靠性。巩固智能体的一致性需要将试错轨迹提炼为可重用的技能，这些技能在保留任务相关不变性的同时舍弃特定于轨迹的噪声。然而，在多模态设置中，关键挑战不仅在于有用的不变性分布在视觉和语言信息中，还在于不同的模态支持不同类型的可重用技能内容：虽然某些技能是可言传且可解释的，但其他技能则存在于文本之外的感知证据中。仅限文本的技能可能会丢失感知线索，而天真地存储文本和感知则会引入冗余和噪声。现有的推理时方法（如自一致性）通过昂贵的多样本解码来提高可靠性，而内化策略则缺乏一种将可言传的技能内容与残余感知信息分离的方法。为了解决这个问题，我们引入了条件多模态信息瓶颈（CMIB），这是一种多模态技能构建方法。CMIB 从多模态技能的联合瓶颈开始，并推导出一个精确的序列分解：（1）提炼可解释技能卡的文本阶段瓶颈，以及（2）仅压缩在文本之外仍具有预测性的感知中残余信息的条件多模态瓶颈。与天真的双流公式不同，CMIB 显式地将多模态潜变量以文本技能为条件，从而在结构上减少了跨模态冗余，并实现了对文本和感知压缩的独立控制。我们使用变分目标实例化了 CMIB，使其条件分解易于优化，从而产生了可重用的多模态技能，在不产生多样本推理开销的情况下提高了执行稳定性。

## Abstract
While LLM-based agents excel at planning and executing long action sequences, their execution often remains inconsistent across trials, limiting reliability. Consolidating agent consistency requires distilling trial-error trajectories into reusable skills that preserve task-relevant invariants while discarding trajectory-specific noise. However, in multimodal settings, the key challenge is not only that useful invariants are distributed across vision and language information, but that different modalities support different kinds of reusable skill content: while some skills are verbalizable and interpretable, others reside in perceptual evidence beyond text. Text-only skills may lose perceptual cues, whereas storing text and perception naively introduces redundancy and noise. Existing inference-time methods, such as self-consistency, improve reliability through costly multi-sample decoding, while internalization strategies lack a way to separate verbalizable skill content from residual perceptual information. To address this, we introduce Conditional Multimodal Information Bottleneck (CMIB), a method for multimodal skill construction. CMIB begins with a joint bottleneck over multimodal skills and derives an exact sequential decomposition: (1) a text-stage bottleneck distilling interpretable skill cards, and (2) a conditional multimodal bottleneck compressing only residual information in perception that remains predictive beyond text. Unlike naive two-stream formulations, CMIB explicitly conditions the multimodal latent on the text skill, thus structurally reducing cross-modal redundancy and enabling independent control over textual and perceptual compression. We instantiate CMIB with a variational objective that makes its conditional decomposition tractable to optimize, yielding reusable multimodal skills that improve execution stability without incurring multi-sample inference overhead.

---

## 论文详细总结（自动生成）

### 论文总结：Skill-CMIB —— 基于条件多模态信息瓶颈的一致性智能体技能构建

#### 1. 核心问题与整体含义（研究动机和背景）
基于大语言模型（LLM）的智能体在执行长序列任务时，常面临**执行不一致性**的问题，即同一任务在不同尝试中表现差异巨大。为了提高可靠性，通常需要将试错轨迹提炼为可重用的“技能”。
在多模态场景下，技能信息分布在文本（可解释的程序）和视觉（难以言传的感知细节）中。现有方法要么仅依赖文本（丢失视觉线索），要么简单并行存储双模态信息（引入冗余和噪声）。本文旨在解决如何**在减少跨模态冗余的同时，从多模态轨迹中提炼出既包含可解释逻辑又包含互补感知证据的紧凑技能**。

#### 2. 方法论：核心思想与关键技术
论文提出了 **Skill-CMIB** 框架，其核心是**条件多模态信息瓶颈（Conditional Multimodal Information Bottleneck, CMIB）**。
*   **核心思想**：将联合信息瓶颈分解为两个序列阶段：
    1.  **文本阶段瓶颈**：提炼出可解释的“技能卡”（Skill Card），作为符号接口。
    2.  **条件多模态瓶颈**：在已知文本技能的前提下，仅压缩感知信息中对预测任务目标仍有价值的“残余信息”。
*   **关键技术细节**：
    *   **两阶段分解**：通过信息论推导，证明了联合瓶颈可以分解为文本项和以文本为条件的感知项，从而实现对两种模态压缩程度的独立控制。
    *   **变分目标优化**：引入变分后验（Posterior）和先验（Prior）来近似难以直接计算的信息瓶颈目标。
    *   **软控制注入**：文本技能卡作为离散提示词，而条件多模态潜变量（Latent）通过一个轻量级的投影层（Q-former + MLP）转化为“软提示词”（Soft Tokens），注入到冻结的骨干模型中。
    *   **训练流程**：首先通过提示词工程生成文本技能卡，然后固定文本卡，训练多模态编码器和投影层，使潜变量 $z$ 捕获文本无法描述的视觉特征。

#### 3. 实验设计
*   **数据集/场景**：
    *   **Multimodal-Mind2Web**：真实世界的网页导航任务，涉及跨任务、跨网站和跨域评估。
    *   **ScreenSpot**：GUI（图形用户界面）接地（Grounding）基准测试，涵盖移动端、桌面端和网页端的图标及文本定位。
*   **Benchmark 与对比方法**：
    *   **骨干模型**：Qwen2.5-VL-7B-Instruct。
    *   **对比基准**：Vanilla Agent（无技能注入）、Text-Only Skill Card（仅文本技能）、Self-Consistency（推理时多样本采样，K=1到5）、以及 GPT-4V、SeeAct 等 SOTA 模型。
*   **评估指标**：除了标准的成功率（SR）和元素准确率外，特别引入了 **Step Consistency (StepCons)** 来衡量动作序列在多次重复实验中的稳定性。

#### 4. 资源与算力
*   论文中明确提到了使用的模型架构（Qwen2.5-VL-7B、Q-former、MLP）。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量及总训练时长。但提到该方法使用轻量级投影模块且保持骨干模型冻结，暗示其训练成本远低于全参数微调（SFT）。

#### 5. 实验数量与充分性
*   **实验规模**：在两个主流多模态智能体基准上进行了测试，涵盖了多种泛化场景（跨域、跨网站）。
*   **消融实验**：设计了四组对比（无技能、仅文本卡、独立双模态、CMIB 条件模态），验证了条件约束对减少冗余和提升性能的必要性。
*   **一致性分析**：通过多次重复实验对比了 CMIB 与自一致性（Self-Consistency）在不同采样次数下的表现。
*   **评价**：实验设计较为全面，通过引入 StepCons 指标客观地量化了“一致性”这一核心改进点，对比实验涵盖了当前主流的推理增强技术，具有较强的说服力。

#### 6. 主要结论与发现
*   **性能提升**：Skill-CMIB 在网页导航和 GUI 任务上的成功率显著优于骨干模型和仅文本技能的方法。
*   **一致性增强**：CMIB 的动作一致性（StepCons）远高于自一致性方法（即使 K=5 时），证明了技能内化比简单的推理采样更能稳定智能体行为。
*   **高效性**：CMIB 在推理时的延迟极低（仅增加约 1%），而达到同等性能的自一致性方法（K=5）延迟增加了数倍。
*   **互补性**：消融实验证实，条件潜变量 $z$ 确实捕获了文本卡无法覆盖的视觉残余信息。

#### 7. 优点
*   **理论严谨**：基于信息瓶颈理论（IB）进行推导，为多模态信息的解耦和压缩提供了坚实的数学基础。
*   **结构优雅**：通过“文本卡+条件潜变量”的设计，兼顾了技能的可解释性（人类可读）和表达能力（视觉细节）。
*   **推理高效**：避免了昂贵的多样本解码，适合对实时性要求高的智能体应用。

#### 8. 不足与局限
*   **依赖试错质量**：技能的提炼依赖于初始试错轨迹（Rollouts）的质量，如果初始轨迹中缺乏成功范例，可能难以提取有效技能。
*   **骨干模型冻结**：虽然保护了模型的通用能力，但也限制了模型通过参数更新深度适配特定复杂技能的可能性。
*   **应用范围**：目前主要集中在网页和 GUI 任务，在更复杂的具身智能（如机器人操作）中的泛化能力尚待验证。

（完）
