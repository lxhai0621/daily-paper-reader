---
title: Response-Conditioned Parallel-to-Sequential Orchestration for Multi-Agent Systems
title_zh: 多智能体系统中基于响应条件的并行到串行编排
authors: "Nurbek Tastan, Alex Iacob, Lorenzo Sani, Meghdad Kurmanji, Nicholas D. Lane, Samuel Horvath, Karthik Nandakumar"
date: 2026-05-15
pdf: "https://arxiv.org/pdf/2605.15573v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 多智能体系统的混合编排策略
tldr: 本文针对多智能体系统在并行与串行协作模式下的效率与准确性权衡问题，提出了名为Nexa的混合执行框架。Nexa利用轻量级Transformer策略，根据初始并行响应动态预测通信拓扑，仅在必要时执行串行精炼。该方法无需外部评判模型，通过策略梯度优化训练，在降低通信开销的同时提升了系统性能，并展现出跨任务和跨模型的强大泛化性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.15573v1/fig-001.webp\", \"caption\": \"Table 1: Main comparison across AQUA-RAT, HumanEval, and GSM8K. Accuracy is reported as mean ± std over runs. Rating uses average rating only. Token usage is total prompt plus completion tokens. Lower average rating and lower token usage are better.\", \"page\": 6, \"index\": 1, \"width\": 818, \"height\": 210}, {\"url\": \"assets/figures/arxiv/2605.15573v1/fig-002.webp\", \"caption\": \"Figure 1: Accuracy-cost tradeoff for multi-agent system baselines across three tasks. Each point corresponds to one method, with the x-axis showing total token usage including prompt and completion tokens and the y-axis showing mean accuracy.\", \"page\": 7, \"index\": 2, \"width\": 817, \"height\": 235}, {\"url\": \"assets/figures/arxiv/2605.15573v1/fig-003.webp\", \"caption\": \"Figure 3: Task-transfer comparison for NEXA on Qwen2.5-1.5B.\", \"page\": 7, \"index\": 3, \"width\": 817, \"height\": 224}, {\"url\": \"assets/figures/arxiv/2605.15573v1/fig-004.webp\", \"caption\": \"Figure 4: Model-scale transfer for NEXA.\", \"page\": 8, \"index\": 4, \"width\": 825, \"height\": 223}]"
motivation: 现有的多智能体协作模式在最小化通信延迟与最大化响应准确性之间存在难以调和的矛盾。
method: 提出一种基于响应调节的策略模型Nexa，通过预测稀疏有向无环图来动态决定从并行执行转向串行精炼的时机。
result: 实验证明Nexa学习到的通信策略在智能体数量、任务类型或底层模型发生变化时仍具有显著的通用性。
conclusion: Nexa通过灵活的并行到串行编排机制，为构建高效、低延迟且高精度的多智能体协同系统提供了新方案。
---

## 摘要
多智能体系统可以通过多个大语言模型（LLM）智能体之间的协作来解决复杂任务。现有的协作框架通常以并行或串行模式运行。在并行模式下，智能体独立响应查询，随后对响应进行聚合。相比之下，串行系统允许智能体通过有向拓扑进行通信，并逐步相互优化。然而，这两种模式都难以在最小化通信和延迟的同时，最大化最终响应的准确性。在这项工作中，我们引入了一种名为 Nexa 的混合范式，这是一种可训练的基于响应条件的策略，弥合了两种模式之间的差距。Nexa 首先进行并行执行阶段，将生成的响应嵌入到共享语义空间中，然后预测一个稀疏的有向无环通信图。如果图为空，系统保持纯并行；如果不为空，系统则执行一次串行消息传播。该策略是一个轻量级的 Transformer 模型，且该方法避免了对外部 LLM 评判器或奖励模型的需求，也无需手工设计的测试时拓扑搜索。我们对这一混合执行问题进行了形式化，证明了生成的图在构造上是无环的，且该框架严格包含纯并行执行，并提出了一种基于策略梯度优化的训练程序。结果表明，Nexa 在一种设置下学习到的基于响应条件的策略，在智能体数量、任务或底层智能体发生变化时仍可复用，从而强调了所学通信策略的泛化性。

## Abstract
Multi-agent systems can solve complex tasks through collaboration between multiple Large Language Model agents. Existing collaboration frameworks typically operate in either a parallel or a sequential mode. In the parallel mode, agents respond independently to queries followed by aggregation of responses. In contrast, sequential systems allow agents to communicate via a directed topology and refine one another step by step. However, both modes are inadequate for achieving the desired objectives of minimizing communication and latency while simultaneously maximizing the accuracy of the final response. In this work, we introduce a hybrid paradigm called Nexa, a trainable response-conditioned policy that bridges the gap between the two modes. Nexa begins with a parallel execution stage, embeds the resulting responses into a shared semantic space, and then predicts a sparse directed acyclic communication graph. If the graph is empty, the system remains purely parallel; if it is non-empty, the system performs one sequential message propagation. The policy is a lightweight transformer model, and the method avoids the need for external LLM judges or reward models, as well as hand-crafted test-time topology search. We formalize this hybrid execution problem, show that the resulting graph is acyclic by construction, and that the framework strictly subsumes pure parallel execution, and present a training procedure based on policy-gradient optimization. Results demonstrate that the response-conditioned policy learned by Nexa under one setting can be reused when the number of agents, the task, or the underlying agent changes, thus emphasizing the generalizability of the learned communication policy.

---

## 论文详细总结（自动生成）

以下是对论文《Response-Conditioned Parallel-to-Sequential Orchestration for Multi-Agent Systems》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
多智能体系统（MAS）通过多个大语言模型（LLM）协作来解决复杂任务，目前主要有两种模式：
*   **并行模式（Parallel）：** 智能体独立响应，最后聚合结果。优点是延迟低、成本低，但缺乏智能体间的纠错和优化。
*   **串行模式（Sequential）：** 智能体按预设拓扑（如链式或全连接）逐步通信。优点是准确性高，但通信开销大、延迟高，且固定拓扑往往存在冗余。

**核心问题：** 如何在最小化通信成本和延迟的同时，最大化响应的准确性？论文旨在打破“全并行”或“全串行”的二元论，提出一种动态的、基于响应质量的混合编排机制。

### 2. 论文提出的方法论：Nexa
Nexa 提出了一种**基于响应条件的并行到串行（Parallel-to-Sequential）**编排框架：
*   **核心思想：** 先让所有智能体并行生成初步响应，然后由一个轻量级策略模型根据这些响应的质量和一致性，动态决定是否需要进一步的串行精炼，并预测最优的通信拓扑。
*   **关键技术流程：**
    1.  **并行阶段：** $N$ 个智能体对查询 $q$ 独立生成初始响应 $R = \{r_1, \dots, r_n\}$。
    2.  **语义嵌入：** 使用轻量级嵌入模型将响应转化为向量。
    3.  **策略预测（Nexa Policy）：** 一个基于 Transformer 的轻量级策略模型接收这些嵌入向量，预测一个**稀疏有向无环图（DAG）**。该图定义了哪些智能体应该作为“发送者”，哪些作为“接收者”。
    4.  **条件串行阶段：** 如果预测的图非空，则执行一次消息传递。接收者根据发送者的反馈优化自己的响应。
    5.  **训练算法：** 采用**策略梯度（Policy Gradient/REINFORCE）**优化。奖励函数综合考虑了最终答案的准确性（正向奖励）和通信边的数量（负向惩罚），从而鼓励模型在必要时才进行通信。

### 3. 实验设计
*   **数据集：** 涵盖了数学推理（**GSM8K**、**AQUA-RAT**）和代码生成（**HumanEval**）三大基准。
*   **底层模型：** 主要使用 Qwen2.5-1.5B 和 Qwen2.5-7B 作为智能体。
*   **对比基线（Baselines）：**
    *   **Parallel (Majority Vote)：** 纯并行后投票。
    *   **Sequential (Fixed Topologies)：** 如链式（Chain）或全连接（Full Graph）。
    *   **Random DAG：** 随机生成的通信拓扑。
    *   **SPP (Solo Performance Prompting)：** 现有的多智能体协作模式。

### 4. 资源与算力
*   **算力说明：** 论文中未明确给出具体的 GPU 型号（如 A100 数量）或总训练时长。
*   **训练规模：** 提到策略模型是在 1000 个样本的小规模数据集上训练的。由于 Nexa 策略模型本身是一个极轻量级的 Transformer（远小于底层 LLM），其训练和推理开销被描述为“微不足道”（negligible）。

### 5. 实验数量与充分性
*   **实验组数：** 进行了多维度实验，包括：
    *   三大任务的性能对比。
    *   **任务迁移实验：** 在 GSM8K 上训练的策略直接应用于 HumanEval。
    *   **模型规模迁移实验：** 在 1.5B 模型上训练的策略直接应用于 7B 模型。
    *   **智能体数量消融：** 改变智能体数量（如从 3 个增加到 5 个）观察策略的鲁棒性。
*   **充分性评价：** 实验设计较为全面，不仅验证了准确性提升，还重点验证了策略的**泛化性**（跨任务、跨模型、跨规模），这在同类研究中较为少见，增强了结论的说服力。

### 6. 论文的主要结论与发现
*   **帕累托最优：** Nexa 在“准确性-成本”权衡曲线上显著优于纯并行和固定串行方法。它能以极低的额外通信成本达到接近全连接图的准确率。
*   **强泛化性：** 学习到的通信策略具有通用性。例如，针对数学问题学到的“何时需要求助”的逻辑，可以直接迁移到代码编写任务中。
*   **涌现的稀疏性：** 策略模型学会了识别哪些智能体产生了错误响应，并仅针对这些智能体建立通信边，实现了高效的按需通信。

### 7. 优点
*   **动态性：** 避开了手工设计拓扑的局限，实现了响应触发（Response-conditioned）的动态编排。
*   **无需外部评判器：** 不同于许多方法需要一个昂贵的“教练”模型（如 GPT-4）来打分，Nexa 通过轻量级策略自发决策。
*   **即插即用：** 策略模型与底层 LLM 解耦，具有极强的跨模型复用潜力。

### 8. 不足与局限
*   **单次精炼限制：** 目前框架仅支持“并行 -> 一次串行传递”，对于需要多轮深度博弈的极复杂问题，可能需要扩展到多轮迭代。
*   **嵌入依赖：** 策略的效果高度依赖于嵌入模型捕捉响应语义差异的能力，如果响应非常相似但逻辑错误细微，策略可能失效。
*   **任务范围：** 虽然涵盖了数学和代码，但在开放式创意写作或长文本生成等主观性更强的任务上尚未验证。

（完）
