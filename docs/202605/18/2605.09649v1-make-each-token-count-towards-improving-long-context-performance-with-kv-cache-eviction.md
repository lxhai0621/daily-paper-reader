---
title: "Make Each Token Count: Towards Improving Long-Context Performance with KV Cache Eviction"
title_zh: 让每个 Token 都有价值：通过 KV 缓存剔除提升长文本性能
authors: "Ngoc Bui, Hieu Trung Nguyen, Arman Cohan, Rex Ying"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09649v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 通过 KV 缓存剔除管理长上下文
tldr: 针对长文本推理中KV缓存带来的内存和计算瓶颈，本文提出一种基于全局保留机制的KV驱逐方法。研究发现全量缓存并非最优，无关标记会稀释注意力。该方法通过轻量级门控评估标记的未来效用，并在全局范围内统一分配缓存预算，使不同层和头的标记竞争存储空间。实验证明，该方法在大幅减少内存占用的同时，性能达到甚至超过了全量缓存推理，提升了长文本推理能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.09649v1/fig-001.webp\", \"caption\": \"Figure 2: a) Fraction of tokens whose attention persists to horizon L for layer 15, head 24. b) Per-layer, perhead survival at L = 4096 and top-K = 128.\", \"page\": 6, \"index\": 1, \"width\": 533, \"height\": 227}, {\"url\": \"assets/figures/arxiv/2605.09649v1/fig-002.webp\", \"caption\": \"Figure 5: Long-reasoning performance of pure language model (Qwen3-4B).\", \"page\": 11, \"index\": 2, \"width\": 900, \"height\": 277}, {\"url\": \"assets/figures/arxiv/2605.09649v1/fig-003.webp\", \"caption\": \"Figure 9: Token attention-survival under full-cache inference. Top row: Qwen3-4B on 100 OpenR1Math prefills up to T = 16,384 tokens. Bottom row: Qwen3-VL-4B on 98 MMDU multimodal dialogues (text + interleaved images), prefilled to the same length. Left column: survival rate averaged across all layers and query heads. Right column: survival rate for a single representative head (Math: layer 12, head 17; MMDU: layer 15, head 24). For each query position p we identify the head’s selected set of past tokens using either the top-K logits (K ∈ 64, 128, 256, 512) or the smallest set whose softmax mass reaches a threshold τ ∈ 0.90, 0.95, 0.99; a token of birth time i is then alive at horizon L if it appears in the selected set of some query at position p ≥ i + L. The dashed vertical line marks L = 4096. In every panel, every fixed top-K curve drops sharply with L, while the more lenient mass criteria stay substantially higher: the gap between top-K and τ-mass curves is the attention-dilution mass — tokens that still carry meaningful aggregate weight but are pushed out of the head’s hard top-K budget. Heterogeneity at the head level is large: a representative head on either dataset loses the majority of tokens by L = 4096 even under the most lenient criterion (99% mass), in contrast to the across-head average. The same qualitative shape appears in the multimodal setting (rows c, d), confirming that attention dilution is not specific to text-only inputs.\", \"page\": 26, \"index\": 3, \"width\": 700, \"height\": 458}, {\"url\": \"assets/figures/arxiv/2605.09649v1/fig-004.webp\", \"caption\": \"Figure 3: Prefill/compression with our method. A per-layer, per-head retention gate assigns each text/visual token a scalar score; the gate’s final projection is tied across layers to normalize scores. Tokens from all layers, heads, and modalities are then pooled and globally ranked by retention, replacing fixed per-layer/per-head KV budgets.\", \"page\": 7, \"index\": 4, \"width\": 897, \"height\": 293}, {\"url\": \"assets/figures/arxiv/2605.09649v1/fig-005.webp\", \"caption\": \"Table 6: Comparison of different methods across various metrics under varying token budgets. The best eviction methods are shown in bold, and the second-best are underlined. Performances that exceed vanilla inference are highlighted in red. The superscript ] denotes a trainable KV eviction method.\", \"page\": 29, \"index\": 5, \"width\": 991, \"height\": 333}, {\"url\": \"assets/figures/arxiv/2605.09649v1/fig-006.webp\", \"caption\": \"Figure 14: Visualization of retained visual tokens for a sample in the MMMU-Vision-Pro dataset (Layer 3, Head 3). The final generation step (b) demonstrates DBTrimKV isolating the specific textual coordinate data points (e.g., A(0, 0), B(200, 300)) required by the model to formulate the area computation in its reasoning output (c). The correct answer is F.\", \"page\": 34, \"index\": 6, \"width\": 1000, \"height\": 491}, {\"url\": \"assets/figures/arxiv/2605.09649v1/fig-007.webp\", \"caption\": \"Table 2: Results across image and video reasoning benchmarks. The best eviction methods are shown in bold, and the second-best are underlined. Performances that exceed vanilla inference are highlighted in red. The superscript ] denotes a trainable KV eviction method.\", \"page\": 10, \"index\": 7, \"width\": 971, \"height\": 568}, {\"url\": \"assets/figures/arxiv/2605.09649v1/fig-008.webp\", \"caption\": \"Figure 13: Visualization of retained visual tokens for a sample in the MMMU-Vision-Pro dataset (Layer 13, Head 2). While the initial step (a) retains broad context, the final step (b) aggressively prunes background noise to isolate the scythe, the clock, and the “RATHER TIME” banner. This precise visual retention directly grounds the model’s textual deduction (c) that the symbol represents the phrase “Time waits for no man” (Option H).\", \"page\": 33, \"index\": 8, \"width\": 1000, \"height\": 619}, {\"url\": \"assets/figures/arxiv/2605.09649v1/fig-009.webp\", \"caption\": \"Figure 12: Efficiency scaling with KV budget. The figure reports throughput in tokens per second (left) and total decoding time in seconds (right) across different allocated KV memory budgets. Evaluated methods include Vanilla (Full Cache), DBTrimKV, and TrimKV. We fix batch size at 16, number of generated tokens at 8192 and number of generated tokens at 8192. Results are averaged over 5 random seeds.\", \"page\": 32, \"index\": 9, \"width\": 979, \"height\": 325}, {\"url\": \"assets/figures/arxiv/2605.09649v1/fig-010.webp\", \"caption\": \"Table 3: Comparison of different methods across selected metrics under varying token budgets.\", \"page\": 12, \"index\": 10, \"width\": 1019, \"height\": 631}, {\"url\": \"assets/figures/arxiv/2605.09649v1/fig-011.webp\", \"caption\": \"Figure 8: Empirical evidence for Assumption A.1. We extract the per-token query state qt at layer 20, head 0 of Qwen3-8B for 128 rollouts each on 10 AIME-24 prompts, project onto the top-8 PCA directions of the pooled training queries to obtain rt ∈ R8, and fit a per-context VAR(1) model rt+1 = Art + b+ ηt on 75% of rollouts. (a) Stability. Eigenvalues of A across all 10 contexts (each context contributes 8 eigenvalues) lie strictly inside the unit circle; the median spectral radius is ρ̃ = 0.76 and every fitted context is stable, so the chain is mean-reverting. (b) Mean reversion / ergodicity. For one context, the per-rollout running mean 1 t ∑︀ s ≤ trs[0] (grey) and its ensemble average (black) converge to the fitted µ (red dashed) inside a ±σ stationary band; the time average matches the ensemble average, consistent with ergodicity. (c) Sub-exponential innovations. Pooled log-survival of the whitened innovation magnitude |η| (blue) sits strictly between the Gaussian (red dashed) and exponential (green dotted) reference curves, satisfying the sub-exponential tail condition that drives the persistence bound.\", \"page\": 24, \"index\": 11, \"width\": 1000, \"height\": 264}, {\"url\": \"assets/figures/arxiv/2605.09649v1/fig-012.webp\", \"caption\": \"Table 1: Performance of competing method on LLaVA-1.5-7B and short-form question answering tasks. The symbol * indicates that the numbers are copied from [Zhang et al., 2025]. Our result is reproduced by running with the LMMs-Eval framework. In the Avg. column, we report the relative performance compared to the corresponding vanilla inference.\", \"page\": 9, \"index\": 12, \"width\": 987, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2605.09649v1/fig-013.webp\", \"caption\": \"Figure 11: Efficiency scaling with generation length. The figure reports throughput in tokens per second (left) and total decoding time in seconds (right) as the number of generated tokens increases. Evaluated methods include Vanilla (Full Cache), DBTrimKV, and TrimKV. We fix batch size at 16, context length at 16384 and KV budget at 256. We do not report vanilla performance for generation length at 32k due to OOM error. Results are averaged over 5 random seeds.\", \"page\": 31, \"index\": 13, \"width\": 979, \"height\": 325}, {\"url\": \"assets/figures/arxiv/2605.09649v1/fig-014.webp\", \"caption\": \"Figure 10: Efficiency scaling with context length. The figure reports throughput in tokens per second (left) and total decoding time in seconds (right) as the input context length increases. Evaluated methods include Vanilla (Full Cache), DBTrimKV, and TrimKV. We fix batch size at 16, number of generated tokens at 8192 and KV budget at 256. Results are averaged over 5 random seeds.\", \"page\": 31, \"index\": 14, \"width\": 967, \"height\": 321}]"
motivation: 现有的KV缓存驱逐方法通常会导致性能下降，且全量缓存中的无关标记会稀释注意力，影响长文本推理效果。
method: 引入全局保留门控机制，通过学习标记的未来效用评分，并利用共享投影进行跨层跨头的全局校准，实现统一预算下的动态驱逐。
result: 在多项长文本语言、视觉语言推理及多轮对话基准测试中，该方法显著降低了内存消耗，且性能优于或等同于全量缓存。
conclusion: 学习型且全局校准的KV驱逐不仅是有效的压缩技术，更是提升模型长文本推理性能的重要机制。
---

## 摘要
键值（KV）缓存是长文本推理的主要瓶颈，其内存和计算量随序列长度增长。现有的 KV 剔除方法降低了成本，但通常会使性能低于全缓存推理。我们的核心见解是，全缓存注意力并不总是最优的：在长文本中，无关的 token 可能会稀释对有用证据的注意力，因此选择性的、可学习的剔除可以改善生成效果，而不仅仅是逼近全缓存。我们提出了一种基于全局保留（retention）的 KV 剔除方法，在统一的内存预算下学习每个 token 的未来效用。轻量级保留门控为缓存的 KV 条目分配效用分数，共享的最终评分投影在所有层和头之间校准这些分数。这实现了一种单一的全局剔除策略，使来自不同层、头和模态的 token 直接竞争缓存容量。我们进一步提供了理论分析，证明优先保留有用的 token 可以减少注意力稀释，并证明了几何保留作为未来效用的查询无关代理的合理性。在多种长文本语言和视觉语言推理以及多轮对话基准测试中，我们的方法在大幅减少 KV 内存的同时，达到或超过了全缓存推理的水平。这些结果表明，经过学习和全局校准的 KV 剔除不仅是一种压缩技术，也是一种提升长文本推理能力的机制。

## Abstract
The key-value (KV) cache is a major bottleneck in long-context inference, where memory and computation grow with sequence length. Existing KV eviction methods reduce this cost but typically degrade performance relative to full-cache inference. Our key insight is that full-cache attention is not always optimal: in long contexts, irrelevant tokens can dilute attention away from useful evidence, so selective, learnable eviction can improve generation rather than merely approximate the full cache. We introduce a global retention-based KV eviction method that learns each token's future utility under a unified memory budget. Lightweight retention gates assign utility scores to cached KV entries, and a shared final scoring projection calibrates these scores across all layers and heads. This enables a single global eviction policy in which tokens from different layers, heads, and modalities compete directly for cache capacity. We further provide theoretical analysis showing that preferentially retaining useful tokens reduces attention dilution, and we justify geometric retention as a query-agnostic proxy for future utility. Across diverse long-context language and vision-language reasoning, and multi-turn dialogue benchmarks, our method substantially reduces KV memory while matching or surpassing full-cache inference. These results suggest that learned, globally calibrated KV eviction is not only a compression technique, but also a mechanism for improving long-context reasoning.

---

## 论文详细总结（自动生成）

### 论文总结：Make Each Token Count: Towards Improving Long-Context Performance with KV Cache Eviction

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心瓶颈**：在长文本推理中，Transformer 的键值（KV）缓存随序列长度线性增长，导致内存溢出和计算开销巨大。
*   **现有局限**：现有的 KV 驱逐（Eviction）方法多将其视为“压缩问题”，试图逼近全量缓存（Full-cache）的效果，但通常会导致性能下降。
*   **核心见解**：全量缓存并非最优。在长文本中，大量无关 token 会产生“注意力稀释（Attention Dilution）”现象，分散模型对关键信息的关注。因此，选择性地剔除无关 token 不仅能节省内存，还能通过减少干扰来提升推理性能。

#### 2. 论文提出的方法论
*   **核心思想**：引入一种基于**全局保留（Global Retention）**的驱逐策略，学习每个 token 的“未来效用（Future Utility）”，并在全局范围内动态分配缓存预算。
*   **关键技术细节**：
    *   **保留门控（Retention Gates）**：在每个注意力头添加轻量级 MLP，根据 token 嵌入预测保留系数 $\beta$。
    *   **权重绑定（Weight-Tying）**：所有层和头的门控共享最终的评分投影层。这确保了不同层、不同头的 token 产生的效用分数具有可比性，从而实现全局排序。
    *   **全局驱逐策略（DBTrimKV）**：取消每层/每头固定的预算，维护一个全局 KV 预算。根据预测的未来效用分数对所有缓存条目进行全局排名，保留分数最高的条目。
    *   **几何衰减理论**：理论证明了 token 的重要性随时间呈几何级数衰减，并以此作为查询无关（Query-agnostic）的效用代理。
    *   **系统实现**：利用 PagedAttention 布局支持变长的、头特定的 KV 缓存管理。

#### 3. 实验设计
*   **数据集与场景**：
    *   **短文本 VQA**：VQAText, GQA, MME。
    *   **长程推理（图像/视频/文本）**：MMStar, MathVision, MMMUPro, VideoMME, VideoMathQA, AIME24, GSM8K 等。
    *   **多轮对话**：MMDU（使用 Gemini 1.5 作为评测裁判）。
*   **Benchmark 与对比方法**：
    *   **基准**：Vanilla（全量缓存）。
    *   **对比方法**：启发式方法（SnapKV, AdaKV, R-KV, PyramidKV）、视觉剪枝方法（FastV, VisionZip 等）以及作者之前的研究 TrimKV。
*   **模型基础**：主要使用 Qwen3-VL-8B-Thinking, LLaVA-1.5-7B, Phi-3-mini 等。

#### 4. 资源与算力
*   **算力设备**：使用了 **4 台 H100 GPU** 进行训练。
*   **训练细节**：
    *   训练仅针对轻量级保留门控（仅占模型总参数量的 **0.43%**），冻结 LLM/VLM 主干权重。
    *   训练数据集包括 M4Instruct, Academic Video, OpenR1-Math 等的混合子集。
    *   学习率 2e-4，采用余弦调度。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从短文本到超长文本（最高 32k+）、从纯文本到多模态（图像、视频、多轮对话）的广泛场景。
*   **充分性**：
    *   进行了大量的消融实验（如权重绑定、输入特征选择、预测步长 $T-t$ 的影响）。
    *   提供了效率分析（吞吐量、解码延迟随长度的变化曲线）。
    *   实验设计客观，通过与多种 SOTA 启发式和学习型方法在相同预算下对比，证明了其优越性。

#### 6. 主要结论与发现
*   **性能超越全量缓存**：在多个基准测试中，DBTrimKV 在仅使用极小比例（如 128 或 256 预算）KV 缓存的情况下，性能不仅匹配甚至**超过了全量缓存**（例如在 MMDU 上提升了 14%）。
*   **注意力正则化作用**：实验证实了 KV 驱逐可以作为一种注意力正则化机制，通过移除“干扰项”来强化模型对相关上下文的聚焦。
*   **动态分配的必要性**：全局预算分配比固定分配更有效，模型能自动识别哪些层/头需要保留更多视觉或文本信息。

#### 7. 优点
*   **理论支撑强**：提出了“注意力稀释”的数学描述，并为几何保留机制提供了理论证明。
*   **全局优化**：通过权重绑定解决了跨层跨头分数不可比的问题，实现了真正的全局动态预算分配。
*   **通用性高**：同时适用于纯文本 LLM 和多模态 VLM，且对推理架构（如 PagedAttention）友好。
*   **效率与效果兼得**：在大幅降低显存占用的同时提升了推理速度和模型精度。

#### 8. 不足与局限
*   **训练依赖**：虽然门控很轻量，但仍需要针对特定模型进行微调训练，不如启发式方法（如 SnapKV）那样开箱即用。
*   **模型规模覆盖**：实验主要集中在 8B 及以下规模的模型，尚未在 70B 或更大型的“前沿”模型上验证其扩展性。
*   **联合训练缺失**：目前仅冻结主干训练门控，尚未探索主干与门控联合训练可能带来的进一步潜力。

（完）
