---
title: An Agent-Oriented Pluggable Experience-RAG Skill for Experience-Driven Retrieval Strategy Orchestration
title_zh: 面向智能体的可插拔 Experience-RAG 技能：用于经验驱动的检索策略编排
authors: "Dutao Zhang, Tian Liao"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03989v2"
tags: ["query:ma-kf"]
score: 10.0
evidence: 面向智能体的可插拔RAG技能，用于策略编排
tldr: 针对传统RAG系统固定检索流程难以适应异构任务的问题，本文提出Experience-RAG Skill。这是一种面向智能体的可插拔检索编排层，通过分析当前场景并咨询经验记忆，动态选择最优检索策略。在BeIR等多个数据集上的实验表明，该方法能显著提升检索效果，证明了将检索策略选择封装为可复用技能的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.03989v2/fig-001.webp\", \"caption\": \"Figure 1: Overall framework of Experience-RAG Skill. The skill is positioned between the agent and the retriever pool. It transforms the incoming query, history, and metadata into structured scene features, consults reusable experience records, selects a retrieval strategy, invokes the candidate retriever pool, and returns a packaged evidence object to the agent.\", \"page\": 2, \"index\": 1, \"width\": 943, \"height\": 473}, {\"url\": \"assets/figures/arxiv/2605.03989v2/fig-002.webp\", \"caption\": \"Table 2: Comparison with modern baselines.\", \"page\": 3, \"index\": 2, \"width\": 623, \"height\": 196}, {\"url\": \"assets/figures/arxiv/2605.03989v2/fig-003.webp\", \"caption\": \"Table 1: Overall retrieval performance.\", \"page\": 3, \"index\": 3, \"width\": 623, \"height\": 197}]"
motivation: 现有的RAG系统通常采用固定的检索流程，无法满足事实问答、多跳推理等不同任务对检索策略的差异化需求。
method: 提出一种可插拔的检索编排技能，通过分析任务场景并结合经验存储器，在检索器池中动态调度最合适的策略。
result: "在BeIR/nq、hotpotqa和scifact数据集上达到了0.8924的nDCG@10评分，优于固定检索基准并与自适应路由方法相当。"
conclusion: 检索策略的选择可以被有效地封装为一种可复用的智能体技能，而非硬编码在工作流中，从而提高系统的灵活性和性能。
---

## 摘要
检索增强生成系统通常假设单一固定的检索流水线足以应对异构任务，然而事实性问答、多跳推理和科学验证表现出不同的检索偏好。我们提出了 Experience-RAG Skill，这是一种面向智能体的可插拔检索编排层，位于智能体与检索器池之间。该技能分析当前场景，咨询经验记忆，选择合适的检索策略，并向智能体返回结构化证据。在固定候选池的情况下，Experience-RAG Skill 在 BeIR/nq、BeIR/hotpotqa 和 BeIR/scifact 上实现了 0.8924 的总体 nDCG@10，优于固定的单检索器基准，并与 Adaptive-RAG 风格的路由保持竞争力。结果表明，检索策略选择可以有效地封装为可重用的智能体技能，而不是在上位工作流中进行硬编码。

## Abstract
Retrieval-augmented generation systems often assume that one fixed retrieval pipeline is sufficient across heterogeneous tasks, yet factoid question answering, multi-hop reasoning, and scientific verification exhibit different retrieval preferences. We present Experience-RAG Skill, an agent-oriented pluggable retrieval orchestration layer positioned between the agent and the retriever pool. The proposed skill analyzes the current scene, consults an experience memory, selects an appropriate retrieval strategy, and returns structured evidence to the agent. Under a fixed candidate pool, Experience-RAG Skill achieves an overall nDCG@10 of 0.8924 on BeIR/nq, BeIR/hotpotqa, and BeIR/scifact, outperforming fixed single-retriever baselines and remaining competitive with Adaptive-RAG-style routing. The results suggest that retrieval strategy selection can be productively encapsulated as a reusable agent skill rather than being hard-coded in the upper workflow.

---

## 论文详细总结（自动生成）

以下是对论文《An Agent-Oriented Pluggable Experience-RAG Skill for Experience-Driven Retrieval Strategy Orchestration》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的检索增强生成（RAG）系统通常采用固定的检索流水线，但不同类型的任务（如事实问答、多跳推理、科学验证）对检索策略的偏好各异。
*   **研究动机**：在智能体（Agent）场景下，硬编码的检索逻辑缺乏灵活性。论文旨在将检索策略的选择和编排封装为一个“可插拔的技能（Skill）”，使智能体能根据当前任务场景动态选择最优检索路径。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：在智能体与检索器池之间引入一个编排层（Experience-RAG Skill），通过“场景分析-经验咨询-策略路由-结果封装”的流程实现动态检索。
*   **关键模块**：
    *   **场景分析器（Scene Analyzer）**：提取查询（q）、对话历史（h）和元数据（m）的特征，包括任务类型、领域、复杂度、文档结构等。
    *   **经验记忆（Experience Memory）**：存储历史记录，包含场景特征、各策略得分向量、最优策略及其领先幅度（Margin）。
    *   **策略路由器（Strategy Router）**：根据经验记忆决定路由。目前最强配置为**基于规则的路由**（如：直接任务指向稠密检索，多跳/科学任务指向混合检索 RRF）。
    *   **结果封装器（Result Packager）**：将检索到的证据统一打包为结构化对象返回给智能体。
*   **流程公式**：$s = Analyze(q, h, m)$；$r^* = Route(s, M)$；$o = RetrieveAndPackage(q, r^*)$。

### 3. 实验设计
*   **数据集**：选取了 BeIR 基准中的三个代表性任务：**nq**（事实问答）、**hotpotqa**（多跳推理）和 **scifact**（科学声明验证）。
*   **实验设置**：每个数据集抽取 120 个查询，在采样后的候选语料库上进行评估。
*   **对比方法**：
    *   **传统基准**：BM25、查询重写 BM25、稠密检索（Dense）、混合检索（Hybrid RRF）。
    *   **现代/先进基准**：HyDE（查询扩展）、RAPTOR（层次化检索）、LongRAG（长文本检索）、Adaptive-RAG（自适应路由）。
*   **评价指标**：Recall@10、MRR@10、nDCG@10。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的硬件配置（如 GPU 型号、数量）及训练/推理时长。由于实验是在采样数据集上进行的，且核心路由目前以规则为主，推测其对算力的即时需求较低。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了主性能对比实验（表 1）。
    *   针对现代 RAG 变体进行了对比实验（表 2）。
    *   进行了消融实验（表 3），验证了任务感知编排的必要性。
    *   探索了学习型路由（分类器与回归器）与规则路由的对比。
*   **充分性评价**：实验覆盖了异构任务，对比了多种主流 RAG 策略，逻辑闭环。但由于使用了**采样语料库**而非全量数据，且每个数据集仅 120 个样本，实验的统计严密性属于初步验证阶段（Preprint/Short Paper 水平）。

### 6. 主要结论与发现
*   **性能领先**：Experience-RAG Skill 实现了 0.8924 的 nDCG@10，优于所有固定的单检索策略。
*   **编排价值**：该技能的优势在于“异构编排”，即在混合工作负载下能自动匹配最佳工具，而非在单一任务上超越所有专门方法。
*   **路由策略**：在当前经验规模下，基于规则的路由优于基于机器学习的路由（分类/回归）。
*   **竞争性**：其表现与复杂的 Adaptive-RAG 相当，但提供了更清晰的智能体接口边界。

### 7. 优点
*   **模块化与可插拔**：将检索逻辑从智能体主流程中解耦，方便作为独立技能复用。
*   **经验驱动**：引入经验记忆概念，使检索系统具备了从历史表现中学习和优化的潜力。
*   **结构化输出**：统一的结果封装降低了上层智能体处理证据的复杂度。

### 8. 不足与局限
*   **数据规模限制**：实验基于采样语料库，可能无法完全反映在大规模生产环境下的检索挑战。
*   **学习型路由尚不成熟**：目前自动学习路由的效果不如人工规则，限制了系统的自动化进化能力。
*   **缺乏端到端评估**：主要评估的是检索质量（Retrieval-only），缺乏智能体最终生成质量（Generation）和交互效率的完整指标。
*   **候选池固定**：目前假设检索器池是预设好的，尚未解决动态接入新检索工具的问题。

（完）
