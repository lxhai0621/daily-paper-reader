---
title: "PARNESS: A Paper Harness for End-to-End Automated Scientific Research with Dynamic Workflows, Full-Text Indexing, and Cross-Run Knowledge Accumulation"
title_zh: PARNESS：一个用于端到端自动化科学研究的论文框架，支持动态工作流、全文索引和跨运行知识积累
authors: "Yuchen Wang, Zhongzhi Luan"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.05258v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 自主研究系统与动态智能体工作流
tldr: 介绍PARNESS框架，利用动态大模型智能体工作流实现自主科学研究。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.05258v1/fig-001.webp\", \"caption\": \"Figure 2: Four concrete PARNESS pipelines for four disciplines, all expressed in the same YAML DSL on the same DAG kernel. (a) An ML benchmark loop iterates idea generation against a quality gate, looping back to ideation if the gate score is too low. (b) A wet-lab biology pipeline holds an idea-discussion round before any experiment (peer-gate), and a statistical gate after replication that can trigger reruns. (c) A survey pipeline alternates instrument-pilot-revise loops, then sequential IRB / recruit / coding / analyse stages. (d) A theory pipeline fans out parallel proof attempts and parameter sweeps with _routes, then merges into an evidence gate. Orange = LLM-driven agent; green = I/O / persistence; blue = score-gated controller; dashed = back-edge from a routing decision. None of these shapes can be expressed in PaperOrchestra’s five-step recipe [3] or InternAgent’s eleven-state machine [6] without forking the framework.\", \"page\": 6, \"index\": 1, \"width\": 287, \"height\": 521}, {\"url\": \"assets/figures/arxiv/2605.05258v1/fig-002.webp\", \"caption\": \"Figure 3: Cross-domain ideation under finite LLM context. (a) A single ideator with a fixed context window can only accommodate a small subsample of the corpus, and even that subsample is read with the well-documented attention bias of lost-in-the-middle [14]. (b) PARNESS separates retrieval from reasoning: the KG indexer (§5.3) holds the full corpus; each cognitive-role agent (§6.6) is wired to a scenario-typed retrieval (similar / cross-domain / opposite / counter-intuitive) so the same total token budget is spent on maximally different slices.\", \"page\": 6, \"index\": 2, \"width\": 876, \"height\": 239}, {\"url\": \"assets/figures/arxiv/2605.05258v1/fig-003.webp\", \"caption\": \"Figure 1: ConjNorm architecture search framework. Three parallel weight vectors (θagg, θmsg, θpool) are constrained by conjugatenorm pairs and optimised via Bregman divergence minimisation. The GNN evaluator produces validation-loss feedback that drives the gradient update.\", \"page\": 26, \"index\": 3, \"width\": 932, \"height\": 527}, {\"url\": \"assets/figures/arxiv/2605.05258v1/fig-004.webp\", \"caption\": \"Figure 11: (a) PaperOrchestra-style fixed five-step recipe: writing the paper assumes the inputs already exist, the topology is hard-coded, no upstream stages. (b) PARNESS pipeline: composition is data, dynamic fan-out (Connector/Analyst/etc. in parallel), score-gated loops (dashed arrows back to earlier stages), and full life-cycle coverage from crawl to review. Both diagrams represent real shipped pipelines.\", \"page\": 15, \"index\": 4, \"width\": 682, \"height\": 342}, {\"url\": \"assets/figures/arxiv/2605.05258v1/fig-005.webp\", \"caption\": \"Figure 4: Reading a paper once is not the same as having it indexed. (a) AI-Scientist [1, 2] and DeepResearch [5] do parse full PDFs, but only episodically: the parsed text is consumed by the current call and then discarded. (b) PaperOrchestra [3], by contrast, only ever sees abstracts. In both regimes there is no long-lived corpus that the next step or the next run can search. (c) PARNESS indexes every parsed body as typed objects (layout, formulae, OCR, tables) into the KG; abstract-only inputs are resolved against the typed neighbourhood, so the system’s reading capability grows monotonically with use.\", \"page\": 7, \"index\": 5, \"width\": 606, \"height\": 356}, {\"url\": \"assets/figures/arxiv/2605.05258v1/fig-006.webp\", \"caption\": \"Table 2: Mapping from motivation to method pillars to architectural realisation. Each row is a self-contained design move.\", \"page\": 10, \"index\": 6, \"width\": 900, \"height\": 240}, {\"url\": \"assets/figures/arxiv/2605.05258v1/fig-007.webp\", \"caption\": \"Figure 10: Three uses of the four-field Agent contract. The runner makes no domain decisions: it merely follows the routing fields the upstream module emits.\", \"page\": 14, \"index\": 7, \"width\": 998, \"height\": 263}, {\"url\": \"assets/figures/arxiv/2605.05258v1/fig-008.webp\", \"caption\": \"Table 1: Test-set performance of fixed-architecture GNN baselines. MAE is the overall mean absolute error across all three targets; R2 µ is the coefficient of determination for the dipole-moment target.\", \"page\": 28, \"index\": 8, \"width\": 980, \"height\": 251}, {\"url\": \"assets/figures/arxiv/2605.05258v1/fig-009.webp\", \"caption\": \"Figure 9: The four-layer architecture of PARNESS. L0 (DAG kernel) is the foundation: a thin scheduler plus a four-field contract. L1 (Persistence) keeps state durable across nodes and across runs. L2 (Agents) is the population of LLM workers and tools that do the actual research. L3 (Pipeline) is the user surface: YAML pipelines and entry-point scripts. Configuration flows downward at start time; data and persisted knowledge flow upward at runtime.\", \"page\": 13, \"index\": 9, \"width\": 750, \"height\": 487}, {\"url\": \"assets/figures/arxiv/2605.05258v1/fig-010.webp\", \"caption\": \"Figure 5: The PARNESS paper↔code graph. Every parsed paper that ships a repository emits a typed derivation edge to its code-node; repository nodes are linked to each other by similarity edges. A paper without code (paper C) and a freshly-generated idea both reach the closest sibling repository through cross-paper inspiration edges. Several existing systems touch code per-task (AutoSOTA [4], AI-Scientist [1, 2], autoresearch), but none builds a corpus-level typed graph of this shape.\", \"page\": 8, \"index\": 10, \"width\": 699, \"height\": 296}, {\"url\": \"assets/figures/arxiv/2605.05258v1/fig-011.webp\", \"caption\": \"Figure 8: Every PARNESS module is a single Python class behind a single contract, registered with a one-line decorator and described by a YAML node. External coding agents — Claude Code, Cursor, Copilot, OpenCode, Kilo Code — can therefore add a new module, edit an existing module, or re-wire a pipeline by editing one Python file plus one YAML file. The pipeline validator checks the edit before the next run starts. This is the same surface the framework’s authors use; there is no separate “user” API.\", \"page\": 12, \"index\": 11, \"width\": 642, \"height\": 309}, {\"url\": \"assets/figures/arxiv/2605.05258v1/fig-012.webp\", \"caption\": \"Figure 6: Knowledge accumulation across runs. Each PARNESS pipeline appends to long-lived stores (papers, ideas, hypotheses, evidence, KG triples). The next run begins from this corpus, retrieving only the relevant slice for the current step. The challenge is independent of whether the LLM is invoked once or many times: in both cases only a finite-sized slice of accumulated knowledge can fit in the prompt, and the engineering question is which slice.\", \"page\": 9, \"index\": 12, \"width\": 760, \"height\": 349}, {\"url\": \"assets/figures/arxiv/2605.05258v1/fig-013.webp\", \"caption\": \"Figure 7: Scenario-typed retrieval over the PARNESS knowledge graph. The KG holds papers, ideas, experiments and code repositories as typed nodes connected by four edge types (structural / internal / semantic / walk). Retrieval adapters compose those edges into four scenario presets — similar (default RAG), opposite (contradictory results, useful for the Contrarian role), cross-domain (long-range walks, useful for the Connector role) and counter-intuitive (semantic outliers, useful for the Synthesizer role). Each cognitive-role agent (§6.6) is wired to the scenario that fits its cognitive demand, so the same finite context window does maximally different work in each parallel slot.\", \"page\": 9, \"index\": 13, \"width\": 670, \"height\": 308}, {\"url\": \"assets/figures/arxiv/2605.05258v1/fig-014.webp\", \"caption\": \"Figure 12: Six cognitive-role agents used in PARNESS ideation. The KG retrieval step (left) emits a _routes fan-out so the runner schedules each role in parallel; each role’s prompt is engineered for one orthogonal cognitive demand and is wired to a different scenario- typed retrieval slice (right of each role). The aggregator deduplicates and ranks the seeds before passing them to a downstream gate. None of the framework’s domain knowledge sits inside GraphRunner: the roles, the retrieval scenarios, and the aggregator are all ordinary BaseModule instances.\", \"page\": 16, \"index\": 14, \"width\": 882, \"height\": 372}, {\"url\": \"assets/figures/arxiv/2605.05258v1/fig-015.webp\", \"caption\": \"Figure 13: The eight-phase Knowledge-Graph indexing pipeline. LLM phases (orange) handle the open-ended steps — extracting insights, discovering intra-batch relations, semantic edge filtering, and weighted random-walk relation discovery. Deterministic phases (blue) handle dedup, embedding, persistence, and structural-edge replication from SQLite. The pipeline is incremental: a new ingestion batch only re-runs phases 1–4 for new content.\", \"page\": 16, \"index\": 15, \"width\": 747, \"height\": 223}]"
motivation: 自主研究系统与动态智能体工作流。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
最近的自主研究系统——AI-Scientist、PaperOrchestra、AutoSOTA、DeepResearch、InternAgent、ResearchAgent等——表明大语言模型（LLM）智能体可以进行构思、运行实验并撰写论文，但这些系统在框架层面都固定了特定的控制流形态（线性流水线、状态机、单智能体循环或固定配方的技能包）。我们认为这种僵化有五个根源：（1）工作流是动态且具有学科特定性的（实验室工作、综述、模拟、理论推导的循环方式各不相同）；（2）构思受限于LLM上下文，而跨领域构思所需的知识超出了单个上下文的承载能力；（3）仅包含摘要的视图会遗漏论文正文，但全文获取并不均衡，因此必须依靠累积语料库；（4）论文的开源代码库通常是其实验方案的唯一完整规范，但论文与代码之间的联系被忽视了；（5）目前尚无工具能将跨运行的知识以可检索的方式持久化到有限的LLM上下文中。我们提出了PARNESS，这是一个基于四项设计举措构建的开源框架：(i) 一个带有四字段智能体契约的轻量级DAG内核，将调度与领域语义解耦，使得任何学科的循环都可以表示为用户可编辑的YAML；(ii) 一个全文PDF解析和文献库子系统，将论文正文、图表索引为类型化对象，并支持仅摘要的平滑回退；(iii) 一个涵盖论文、想法、实验和代码库的知识图谱索引，具有场景类型化检索功能（相似/矛盾/跨领域/反直觉），为每次LLM调用提供聚焦的切片信息；(iv) 一个小型的扩展接口，允许任何现代编码智能体（Claude Code、Cursor、Copilot、OpenCode）添加或替换任何模块。据我们所知，PARNESS是首个结合了声明式流水线、全PDF与代码库索引以及跨运行知识的开源系统。

## Abstract
Recent autonomous research systems -- AI-Scientist, PaperOrchestra, AutoSOTA, DeepResearch, InternAgent, ResearchAgent and others -- show LLM agents can ideate, run experiments and write papers, but each fixes a particular control-flow shape (linear pipeline, state machine, single-agent loop, or fixed-recipe skill pack) at the framework level. We argue this rigidity has five roots: (1) workflows are dynamic and discipline-specific (lab work, surveys, simulations, theory all loop differently); (2) ideation is bounded by LLM context and cross-domain ideation needs knowledge a single context cannot hold; (3) summary-only views miss the paper body, yet full-text access is uneven, so the cumulative corpus must do the work; (4) a paper's open-source repository is often the only complete specification of its experimental scheme, but the paper-to-code link is neglected; (5) no tool persists cross-run knowledge retrievably into a finite LLM context.   We present PARNESS, an open-source framework built on four design moves. (i) A thin DAG kernel with a four-field Agent contract decouples scheduling from domain semantics, so any discipline's loop is expressible as user-editable YAML. (ii) A full-text PDF-parsing and literature-library subsystem indexes paper bodies, figures and tables as typed objects, with graceful abstract-only fall-back. (iii) A knowledge-graph index over papers, ideas, experiments and code repositories, with scenario-typed retrieval (similar / contradictory / cross-domain / counter-intuitive), surfaces a focused slice into each LLM call. (iv) A small extension surface lets any modern coding agent (Claude Code, Cursor, Copilot, OpenCode) add or replace any module. To our knowledge PARNESS is the first open-source system combining declarative pipelines, full-PDF and code-repository indexing, and cross-run knowledge. Source: https://github.com/gtrhythm/PARNESS

---

## 论文详细总结（自动生成）

### PARNESS 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
论文指出，当前的自主科学研究系统（如 AI-Scientist, PaperOrchestra, InternAgent 等）虽然展示了 LLM 智能体在构思、实验和写作方面的潜力，但普遍存在**架构僵化**的问题。这种僵化体现在五个方面：
*   **控制流固定**：无法适应不同学科（如生物实验、社会调查、理论推导）特有的研究循环。
*   **构思受限**：受限于 LLM 的上下文窗口，难以进行跨领域的深度构思。
*   **信息缺失**：大多仅依赖论文摘要，忽略了正文中的关键细节（如预处理步骤、表格脚注）。
*   **代码脱节**：忽视了论文与开源代码库（GitHub）之间的强关联。
*   **缺乏积累**：每次运行都是“孤岛”，无法利用之前运行中积累的失败教训或成功经验。

**PARNESS** 的提出旨在通过一个“论文马具（Paper Harness）”框架，将调度内核与领域语义解耦，实现跨学科、可积累、全索引的端到端自动化研究。

#### 2. 论文提出的方法论
PARNESS 基于四个核心设计支柱：
*   **轻量级 DAG 内核与声明式 DSL**：
    *   使用 YAML 文件定义研究工作流（DAG），而非硬编码 Python 逻辑。
    *   **四字段智能体契约**：所有模块通过返回 `_route`（路由）、`_routes`（分发）、`_score`（评分）和 `_metadata`（元数据）与内核交互，使内核无需理解领域知识即可完成复杂调度。
*   **全文 PDF 解析与文献库系统**：
    *   集成 PDF-Extract-Kit 和 MinerU，将论文正文、公式、表格解析为类型化对象。
    *   支持“仅摘要”到“全文索引”的平滑过渡，使系统的阅读能力随使用量增加而单调增长。
*   **基于知识图谱（KG）的跨运行积累**：
    *   使用 Neo4j 存储论文、想法、实验和代码库的关联。
    *   **场景类型化检索**：根据智能体角色（如“反思者”、“连接者”）提供相似、矛盾、跨领域或反直觉的知识切片。
*   **面向编码智能体的扩展接口**：
    *   模块采用单文件类设计，配合 JSON-Schema 验证，方便 Claude Code 或 Cursor 等 AI 编程工具直接添加或修改模块。

#### 3. 实验设计
论文通过多种场景验证了系统的灵活性和功能性：
*   **学科场景模拟**：展示了 ML 基准测试循环、湿实验生物学协议、社会科学调查、理论证明与模拟四种不同的 YAML 流水线。
*   **核心案例研究（ConjNorm）**：
    *   **任务**：在 QM9 分子属性预测数据集上进行几何模型架构搜索。
    *   **Benchmark**：对比了随机搜索（Random Search）和固定架构基准（GCN, GAT, GraphSAGE）。
    *   **流程**：涵盖文献获取、构思、实验设计、代码执行、结果聚合、论文撰写及 LaTeX 编译。

#### 4. 资源与算力
*   **算力环境**：参考实现支持单节点（Single-node）和多 GPU（Multi-GPU）配置。
*   **运行耗时**：端到端流水线（如包含 11 个阶段的 arXiv 检索与构思任务）在单 GPU 节点上通常在 **1 小时左右** 完成。
*   **模型使用**：集成了 OpenAI、Anthropic、GLM、MiniMax 等 6 家供应商，实验中具体提到了使用 MiniMax-M2.7 进行适配器验证。

#### 5. 实验数量与充分性
*   **实验规模**：提供了 130 多个注册模块和 50 个预设的 YAML 流水线。
*   **验证维度**：
    *   通过 44 条 Mock 规则进行了离线单元测试。
    *   进行了端到端的流水线运行验证（如 `arxiv_heplat_dag`）。
    *   包含了一个完整的生成论文案例（见附录 B）。
*   **客观性评价**：作者在文中表现得非常**客观且诚实**。他们明确指出，目前主要验证的是系统的**控制流耐用性**和**功能完整性**，尚未进行大规模的人类评估，也未与 AI-Scientist 等系统在相同任务上进行严谨的量化 Benchmark 对比。

#### 6. 论文的主要结论与发现
*   **架构解耦可行性**：证明了复杂的科研逻辑可以被抽象为简单的 DAG 调度和四字段契约。
*   **效率提升**：在 ConjNorm 案例中，PARNESS 搜索架构的速度比随机搜索快 **21.2 倍**（15.45秒 vs 328.08秒）。
*   **性能权衡**：自动生成的架构在 MAE（平均绝对误差）上比随机搜索的最优结果高出 18.36%，发现连续松弛化搜索倾向于选择“均值类”操作，这揭示了自动化搜索空间与离散性能景观之间的对齐挑战。

#### 7. 优点（亮点）
*   **极高的灵活性**：通过 YAML 即可修改研究逻辑，无需重写框架代码，真正实现了“工作流即数据”。
*   **深度的知识持久化**：不仅存论文，还存实验失败记录和代码片段，解决了 LLM 记忆短浅的问题。
*   **全文感知**：相比仅依赖摘要的系统，PARNESS 能触达论文正文和代码库，信息密度更高。
*   **工程完备性**：提供了完善的流水线验证器（Schema、契约、类型、拓扑四层检查），确保了系统的稳定性。

#### 8. 不足与局限
*   **缺乏横向对比**：尚未在公认的基准测试（如 PaperWritingBench）上与其他 SOTA 系统进行量化对比。
*   **成本问题**：多角色（六个认知角色）并行构思虽然增加了多样性，但显著增加了 Token 消耗成本。
*   **模式坍塌风险**：知识图谱的语义边缘提取依赖 LLM 分桶，在合成数据测试中观察到了模式坍塌现象。
*   **质量瓶颈**：生成的论文虽然格式完整，但在科学发现的深度上仍有待提升（如 ConjNorm 案例中性能未达最优）。

（完）
