---
title: "Learning Correct Behavior from Examples: Validating Sequential Execution in Autonomous Agents"
title_zh: 从示例中学习正确行为：验证自主智能体中的顺序执行
authors: "Reshabh K Sharma, Gaurav Mittal, Yu Hu"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.03159v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 使用多模态大模型验证自主智能体的顺序行为
tldr: 针对自主智能体序列行为验证难题，本文提出一种仅需2-10个成功执行轨迹即可自动学习正确行为的模型。该方法结合编译原理中的支配者分析与多模态大模型语义理解，通过前缀树接收器构建广义真值模型，并利用拓扑子序列匹配验证新执行。实验证明该系统在UI测试、代码生成等领域能以极少样本实现高精度缺陷检测，并提供可解释的验证结果。
source: arxiv
selection_source: fresh_fetch
motivation: 传统的自主智能体行为验证方法依赖手动规范或大量训练数据，难以应对复杂的非确定性序列行为。
method: 结合支配者分析与多模态大模型，利用前缀树接收器和多层级等价检测构建广义模型，并通过拓扑子序列匹配进行验证。
result: 系统仅需3条训练轨迹即可在高精度下检测出产品缺陷和虚假成功，并提供覆盖率指标。
conclusion: 该方法为跨领域的自主智能体行为验证提供了一种高效、少样本且具有可解释性的自动化解决方案。
---

## 摘要
随着自主智能体变得日益复杂，验证其顺序行为面临着重大挑战。传统的测试方法通常需要手动规范、精确序列匹配或数千个训练示例。我们提出了一种新颖的算法，仅通过 2-10 个通过的执行轨迹即可自动学习正确行为，并根据该学习模型验证新的执行。我们的方法结合了编译原理中的支配者分析（dominator analysis）与多模态大语言模型驱动的语义理解，以识别关键状态并处理非确定性行为。该系统使用前缀树接收器（Prefix Tree Acceptors）构建广义基准模型，通过多层级等价性检测合并轨迹，并通过拓扑子序列匹配验证新的执行。在受控实验中，我们的系统仅使用 3 个训练轨迹就在检测产品缺陷和虚假成功方面实现了高准确率。该方法提供了具有覆盖率指标的可解释验证结果，并适用于包括 UI 测试、代码生成和机器人流程在内的多个领域。

## Abstract
As autonomous agents become increasingly sophisticated, validating their sequential behavior presents a significant challenge. Traditional testing approaches require manual specification, exact sequence matching, or thousands of training examples. We present a novel algorithm that automatically learns correct behavior from just 2-10 passing execution traces and validates new executions against this learned model. Our approach combines dominator analysis from compiler theory with multimodal large language model-powered semantic understanding to identify essential states and handle non-deterministic behavior. The system constructs a generalized ground truth model using Prefix Tree Acceptors, merges traces through multi-tiered equivalence detection, and validates new executions via topological subsequence matching. In controlled experiments, our system achieved high accuracy in detecting product bugs and false successes using only 3 training traces. This approach provides explainable validation results with coverage metrics and works across diverse domains including UI testing, code generation, and robotic processes.

---

## 论文详细总结（自动生成）

这篇论文由华盛顿大学和微软的研究人员合作完成，提出了一种针对自主智能体（Autonomous Agents）顺序行为验证的新框架。以下是对该论文的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着自主智能体（如UI操作智能体、代码生成智能体）的普及，验证其行为正确性变得极其困难。核心挑战在于**非确定性（Non-determinism）**：
*   **路径多样性**：达到同一目标可能有多种有效路径（如是否出现加载页面、不同的UI操作顺序）。
*   **传统方法局限**：断言测试（Assertion-based）需手动编写且难以覆盖所有路径；录制回放（Record-and-replay）过于脆弱，微小视觉差异即报错；机器学习预言机（ML Oracles）则需要数千个样本且缺乏可解释性。
*   **研究目标**：开发一种仅需极少量（2-10个）成功示例即可自动学习“正确行为标准”，并能区分“可接受的变异”与“真正的失败”的验证系统。

### 2. 方法论
论文提出了一种结合编译原理与多模态大模型（MLLM）的三阶段算法：
*   **阶段1：轨迹捕获与PTA构建**
    *   收集少量成功的执行轨迹（截图+动作），将其转化为**前缀树接收器（Prefix Tree Acceptors, PTA）**，即以状态为节点、动作为边的有向图。
*   **阶段2：合并与泛化（核心创新）**
    *   **多层级状态等价检测**：
        *   **第一层（视觉指标）**：使用感知哈希（pHash）、结构相似性（SSIM）和像素变化率进行快速比对。
        *   **第二层（语义分析）**：若视觉指标模糊，调用多模态LLM（如GPT系列）判断差异是否具有语义意义（例如：时间戳变化不重要，但错误信息弹出很重要）。
    *   **支配者提取（Dominator Extraction）**：借鉴编译器控制流分析技术，计算**支配者树**。如果从起点到状态S的所有路径都必须经过状态D，则D是S的支配者。通过此方法识别出“必须经过”的关键状态（Essential States），过滤掉可选状态（如加载动画）。
*   **阶段3：拓扑子序列匹配验证**
    *   将新执行轨迹与学习到的支配者树进行比对。只要新轨迹按顺序包含了所有关键状态，即使中间插入了额外的可选步骤，也判定为通过（PASS）。计算公式为：`覆盖率 = 匹配的关键状态数 / 总关键状态数`。

### 3. 实验设计
*   **场景**：VS Code 扩展插件缺陷检测。
*   **数据集**：包含28个智能体执行轨迹的受控合成基准测试。
    *   **训练集**：3个成功的执行轨迹。
    *   **测试集**：25个轨迹，涵盖11个产品缺陷（Product Bugs）、3个智能体自身错误（Agent Issues）以及11个正常轨迹。
*   **对比基准（Benchmark）**：智能体自我报告（CUA Self-reporting），即让智能体自己判断是否成功。

### 4. 资源与算力
*   **算力说明**：文中**未明确提及**具体的GPU型号、数量或训练时长。
*   **资源消耗**：由于该方法是“低样本学习”（Low-shot），不需要大规模模型训练。主要的计算开销在于推理阶段调用多模态LLM进行语义等价性检测。

### 5. 实验数量与充分性
*   **实验规模**：总计28组实验。虽然样本量较小，但涵盖了多种失败类型（虚假成功、产品缺陷、智能体故障等）。
*   **充分性评价**：作为一项探索性工作，实验设计具有针对性，能够验证算法在处理非确定性时的有效性。但作者也承认，目前仅在UI测试领域进行了深入验证，缺乏大规模、多领域的工业级数据支持。

### 6. 主要结论与发现
*   **高准确性**：仅用3个示例进行学习，系统在检测产品缺陷和虚假成功方面达到了**100%的准确率**。
*   **优于自我评估**：相比智能体自我报告（准确率82.2%，召回率60%），该方法在所有指标上均有显著提升（F1分数提升30.2%）。
*   **可解释性**：系统不仅给出通过/失败的结果，还能通过支配者树指出缺失了哪个关键步骤，并提供覆盖率指标。
*   **根因分析**：系统能以52.2%的F1分数区分“产品代码坏了”还是“智能体发挥不稳定”，有助于减少CI/CD流水线中的误报。

### 7. 优点
*   **极低样本需求**：2-10个示例即可建模，极大降低了自动化测试的维护成本。
*   **鲁棒性强**：通过语义等价检测和支配者分析，有效解决了UI自动化测试中常见的“脆弱性”问题。
*   **跨领域潜力**：算法逻辑通用，理论上可扩展至机器人、代码验证等多种顺序执行场景。

### 8. 不足与局限
*   **依赖成功案例**：系统无法从失败案例中学习，必须先有成功的“真值”轨迹。
*   **LLM 成本与延迟**：语义等价检测依赖外部大模型，可能产生API费用和推理延迟。
*   **状态表示限制**：目前主要基于视觉截图，对于纯后端服务（无UI）的验证需要开发新的状态表示方法（如API响应或数据库状态）。
*   **时间维度缺失**：目前未考虑操作的时间约束（例如：某个步骤必须在5秒内完成）。

（完）
