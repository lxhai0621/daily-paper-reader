---
title: "GRAIL: A Deep-Granularity Hybrid Resonance Framework for Real-Time Agent Discovery via SLM-Enhanced Indexing"
title_zh: GRAIL：一种通过 SLM 增强索引实现实时智能体发现的深粒度混合共振框架
authors: Jinliang Xu
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02489v2"
tags: ["query:ma-kf"]
score: 9.0
evidence: 通过小语言模型增强索引实现实时智能体发现
tldr: 随着大模型智能体生态的快速扩张，高效准确的智能体发现成为多智能体协作的瓶颈。现有方法在延迟和精度间难以平衡。本文提出GRAIL框架，通过轻量化小模型（SLM）增强索引、伪文档扩展及MaxSim共振匹配机制，实现了亚400毫秒的实时发现。在AgentTaxo-9K数据集上的实验表明，GRAIL在保持高精度的同时，将延迟降低了79倍以上，为大规模智能体发现提供了工业级解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.02489v2/fig-001.webp\", \"caption\": \"Fig. 4: Multi-dimensional distribution analysis of tags (Top: Tag word cloud; Bottom: Log-transformed frequency trend)\", \"page\": 6, \"index\": 1, \"width\": 464, \"height\": 628}, {\"url\": \"assets/figures/arxiv/2605.02489v2/fig-002.webp\", \"caption\": \"Fig. 2: The Overall Architecture of the GRAIL Framework.\", \"page\": 3, \"index\": 2, \"width\": 525, \"height\": 406}, {\"url\": \"assets/figures/arxiv/2605.02489v2/fig-003.webp\", \"caption\": \"Fig. 3: Workflow of the GRAIL Online Discovery Process.\", \"page\": 5, \"index\": 3, \"width\": 516, \"height\": 408}]"
motivation: 针对现有智能体发现方法在LLM解析的高延迟与向量检索的低精度之间存在的权衡难题，寻求一种实时且精准的发现机制。
method: 采用微调的小模型进行意图预测，结合伪文档扩展增强语义密度，并利用MaxSim细粒度匹配机制计算查询与示例间的最大相似度。
result: "在包含9240个智能体的数据集上，GRAIL比LLM解析基准快79倍以上，且在Recall@10指标上显著优于传统向量搜索。"
conclusion: GRAIL框架成功解决了大规模多智能体系统中的实时发现挑战，为构建“智能体互联网”提供了高效可扩展的技术支撑。
---

## 摘要
随着基于大语言模型（LLM）的智能体生态系统迅速扩张，高效且准确的智能体发现（Agent Discovery）已成为大规模多智能体协作的关键瓶颈。现有方法通常面临两难境地：要么依赖重量级 LLM 进行意图解析，导致难以接受的延迟（通常超过 30 秒）；要么使用单一的向量检索，以牺牲语义精度为代价换取速度。为了弥补这一差距，我们提出了 GRAIL（基于粒度共振的智能体/AI 链接），这是一个在不牺牲准确性的情况下实现低于 400 毫秒发现延迟的新型框架。GRAIL 引入了三项关键创新：（1）SLM 增强预测，使用专门微调的小语言模型（SLM）取代通用 LLM 解析器，实现毫秒级的能力标签预测；（2）伪文档扩展（Pseudo-Document Expansion），通过合成查询增强智能体描述，以提升语义密度，实现稳健的稠密检索；（3）MaxSim 共振，一种细粒度匹配机制，通过计算用户查询与离散智能体使用示例之间的最大相似度，有效缓解语义稀释问题。在包含 9,240 个智能体的新型大规模数据集 AgentTaxo-9K 上进行的验证表明，与基于 LLM 解析的基准相比，GRAIL 将端到端发现延迟降低了 79 倍以上，同时在 Recall@10 指标上显著优于传统的向量搜索。该框架为实时的“智能体互联网”（Internet of Agents）提供了一种可扩展的工业级解决方案。

## Abstract
As the ecosystem of Large Language Model (LLM)-based agents expands rapidly, efficient and accurate Agent Discovery becomes a critical bottleneck for large-scale multi-agent collaboration. Existing approaches typically face a dichotomy: either relying on heavy-weight LLMs for intent parsing, leading to prohibitive latency (often exceeding 30 seconds), or using monolithic vector retrieval that sacrifices semantic precision for speed. To bridge this gap, we propose \textbf{GRAIL} (Granular Resonance-based Agent/AI Link), a novel framework achieving sub-400ms discovery latency without compromising accuracy. GRAIL introduces three key innovations: (1) \textbf{SLM-Enhanced Prediction}, replacing the generalized LLM parser with a specialized, fine-tuned Small Language Model (SLM) for millisecond-level capability tag prediction; (2) \textbf{Pseudo-Document Expansion}, augmenting agent descriptions with synthetic queries to enhance semantic density for robust dense retrieval; and (3) \textbf{MaxSim Resonance}, a fine-grained matching mechanism computing maximum similarity between user queries and discrete agent usage examples, effectively mitigating semantic dilution. Validated on \textbf{AgentTaxo-9K}, our new large-scale dataset of 9,240 agents, GRAIL reduces end-to-end discovery latency by over \textbf{79$\times$} compared to LLM-parsing baselines, while significantly outperforming traditional vector search in Recall@10. This framework offers a scalable, industrial-grade solution for the real-time ``Internet of Agents."

---

## 论文详细总结（自动生成）

这是一份关于论文《GRAIL: A Deep-Granularity Hybrid Resonance Framework for Real-Time Agent Discovery via SLM-Enhanced Indexing》的结构化总结：

### 1. 核心问题与整体含义
随着“智能体互联网”（Internet of Agents）概念的兴起，如何在海量候选池中实时、精准地发现匹配用户意图的智能体（Agent Discovery）成为关键挑战。
*   **研究动机**：现有方法存在“两极分化”：
    *   **LLM中心化解析**：精度高但延迟极大（常超过30秒），无法满足实时交互需求。
    *   **单一向量检索**：速度快但精度低，因为将复杂的能力描述压缩为单一向量会导致“语义稀释”，丢失细粒度约束。
*   **核心目标**：在保证亚400ms低延迟的同时，实现高精度的智能体匹配。

### 2. 论文提出的方法论（GRAIL 框架）
GRAIL 引入了“深粒度混合共振”机制，将索引和检索过程解耦为三个维度：
*   **SLM 增强预测（SLM-Enhanced Prediction）**：使用微调后的 3B 参数小语言模型（SLM）取代重量级 LLM，在毫秒内预测查询相关的能力标签（Tags），用于快速候选过滤。
*   **伪文档扩展（Pseudo-Document Expansion, PDE）**：利用 LLM 为智能体描述生成合成查询，扩充语义密度，解决用户查询与技术描述之间的词汇不匹配问题。
*   **MaxSim 共振匹配（MaxSim Resonance）**：
    *   **核心思想**：借鉴 ColBERT 的后期交互思想。不使用平均池化向量，而是将智能体的多个使用示例（Examples）存储为嵌入矩阵。
    *   **算法流程**：计算用户查询向量与智能体示例矩阵中每一行（每个示例）的余弦相似度，取最大值作为“共振得分”。这确保了只要有一个示例匹配，该智能体就能获得高分，有效防止多功能智能体的信号被稀释。
*   **三维索引结构**：构建稀疏索引（标签）、上下文索引（扩展后的描述向量）和意图索引（示例矩阵）。

### 3. 实验设计
*   **数据集**：发布了 **AgentTaxo-9K**，包含 9,240 个智能体和 27,720 条查询。该数据集基于 OASF 框架，涵盖 154 个行业和 924 个子领域，具有严格的层次结构。
*   **Benchmark 与对比方法**：
    *   **MDR (Monolithic Dense Retrieval)**：传统的单一向量检索。
    *   **SLM-Sort**：仅依赖 SLM 预测标签进行检索。
    *   **LLM-Router**：使用重量级 LLM 进行意图解析和路由。
*   **评价指标**：Recall@1、Recall@10、MRR@10 以及端到端延迟。

### 4. 资源与算力
*   **硬件环境**：使用了单张 **NVIDIA T4 GPU (15.0 GB VRAM)**，12.7 GB 系统内存。
*   **模型规格**：SLM 采用 **Qwen2.5-3B-Instruct**（通过 LoRA 微调），向量编码器采用 **BGE-Small-EN-v1.5** (33M 参数)。
*   **训练细节**：文中提到在 27,720 个查询-标签对上进行了微调，但未明确说明具体的训练时长。

### 5. 实验数量与充分性
*   **实验规模**：在近万级规模的智能体池中进行全量检索测试，实验组数包括主性能对比、延迟组成分析和消融实验。
*   **充分性与客观性**：
    *   **消融实验**：分别验证了 MaxSim 和 SLM 模块的贡献，证明了 MaxSim 是提升精度的核心。
    *   **理论证明**：通过数学定理证明了平均池化在多功能智能体表示中的“语义稀释”问题，为方法论提供了理论支撑。
    *   **公平性**：确保了合成查询（用于扩展索引）与测试查询（用于评估）是独立生成的，避免了数据泄露。

### 6. 主要结论与发现
*   **性能飞跃**：GRAIL 的 Recall@10 达到 91.45%，显著优于 MDR (76.77%)。
*   **延迟优势**：端到端延迟仅为 **394.48ms**，比 LLM 路由基准快了 **79 倍以上**。
*   **语义稀释验证**：实验证实，当智能体具备多种功能时，传统向量检索得分会随功能数量增加而线性衰减，而 MaxSim 机制能保持稳定的信号强度。

### 7. 优点
*   **平衡性极佳**：成功解决了工业界在智能体发现中面临的“精度-延迟”权衡难题。
*   **细粒度建模**：通过示例矩阵（Intent Index）保留了智能体的离散能力模式，而非模糊的平均值。
*   **贡献数据集**：AgentTaxo-9K 为该领域提供了一个高质量、分层化的标准化测试床。

### 8. 不足与局限
*   **元数据依赖**：该框架高度依赖智能体提供高质量的使用示例（Examples），若示例缺失或质量差，MaxSim 的优势将受限。
*   **冷启动成本**：引入新领域智能体时，可能需要重新微调 SLM 以保证标签预测的准确性。
*   **存储开销**：相比单一向量，存储示例矩阵（Intent Index）会占用更多的向量数据库空间。

（完）
