---
title: "HAGE: Harnessing Agentic Memory via RL-Driven Weighted Graph Evolution"
title_zh: HAGE：通过强化学习驱动的有权图演化利用智能体记忆
authors: "Dongming Jiang, Yi Li, Guanpeng Li, Qiannan Li, Bingzhe Li"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.09942v1"
tags: ["query:agent"]
score: 10.0
evidence: 强化学习驱动的智能体记忆检索加权图演化
tldr: 针对智能体内存检索中静态结构的局限，本文提出HAGE框架，将内存建模为加权多关系图。通过LLM识别意图并利用强化学习驱动的路由网络动态调节边权重，实现查询驱动的序列化遍历。实验证明，HAGE在长程推理中显著提升了准确性与效率，为动态内存演化提供了新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.09942v1/fig-001.webp\", \"caption\": \"Table 1: LoCoMo comparison with LLM-as-a-judge score under different methods and backbone LLMs. Higher is better. Best results are shown in bold, and second-best results are underlined. HAGE is our proposed method.\", \"page\": 7, \"index\": 1, \"width\": 757, \"height\": 408}, {\"url\": \"assets/figures/arxiv/2605.09942v1/fig-002.webp\", \"caption\": \"Figure 2: Architectural Overview of HAGE.\", \"page\": 4, \"index\": 2, \"width\": 752, \"height\": 231}]"
motivation: 传统的智能体内存检索依赖静态查找或固定图结构，难以捕捉事件间随查询动态变化的复杂关系强度。
method: 提出HAGE框架，利用加权多关系图组织内存，并通过LLM意图识别与强化学习驱动的路由网络实现动态图遍历。
result: 实验结果显示，HAGE在长程推理任务的准确性上优于现有系统，并实现了更优的准确率与效率平衡。
conclusion: HAGE通过引入动态演化的加权图和强化学习优化，有效解决了智能体在复杂任务中的内存检索难题。
---

## 摘要
智能体大语言模型（LLM）系统中的记忆检索通常被视为静态查找问题，依赖于扁平向量搜索或固定的二元关系图。然而，固定的图结构无法捕捉事件之间关系的不同强度、置信度以及依赖于查询的相关性。在本文中，我们提出了 HAGE，这是一个有权多关系记忆框架，它将检索重新概念化为在统一关系记忆图上进行的顺序、查询条件化的遍历。记忆被组织为共享记忆节点上的特定关系图视图，其中每条边都与一个可训练的关系特征向量相关联，该向量编码了多个关系信号。给定一个查询，基于 LLM 的分类器识别关系意图，路由网络动态调节边嵌入的相应维度。遍历分数通过语义相似度与这些查询条件化边表示的习得组合来计算。这使得记忆遍历能够优先考虑高实用性的关系路径，同时软性抑制噪声或弱相关的连接。除了自适应遍历，HAGE 还引入了一个基于强化学习的训练框架，利用下游任务联合优化路由行为和边表示。最后，实证结果表明，与最先进的智能体记忆系统相比，该方法提高了长程推理精度，并实现了良好的精度-效率权衡。我们的代码可在 https://github.com/FredJiang0324/HAGE_MVPReview 获取。

## Abstract
Memory retrieval in agentic large language model (LLM) systems is often treated as a static lookup problem, relying on flat vector search or fixed binary relational graphs. However, fixed graph structures cannot capture the varying strength, confidence, and query-dependent relevance of relationships between events. In this paper, we propose HAGE, a weighted multi-relational memory framework that reconceptualizes retrieval as sequential, query-conditioned traversal over a unified relational memory graph. Memory is organized as relation-specific graph views over shared memory nodes, where each edge is associated with a trainable relation feature vector encoding multiple relational signals. Given a query, an LLM-based classifier identifies the relational intent, and a routing network dynamically modulates the corresponding dimensions of the edge embedding. Traversal scores are computed via a learned combination of semantic similarity and these query-conditioned edge representations. This allows memory traversal to prioritize high-utility relational paths while softly suppressing noisy or weakly relevant connections. Beyond adaptive traversal, HAGE further introduces a reinforcement learning-based training framework that jointly optimizes routing behavior and edge representations using downstream tasks. Finally, empirical results demonstrate improved long-horizon reasoning accuracy and a favorable accuracy-efficiency trade-off compared to state-of-the-art agentic memory systems. Our code is available at https://github.com/FredJiang0324/HAGE_MVPReview.

---

## 论文详细总结（自动生成）

这是一份关于论文《HAGE: Harnessing Agentic Memory via RL-Driven Weighted Graph Evolution》的结构化深度总结：

### 1. 核心问题与研究动机
*   **研究背景**：智能体（Agent）需要长期记忆来处理长程对话和复杂任务。目前的记忆检索主要依赖扁平的向量搜索（RAG）或固定的图结构。
*   **核心问题**：现有的图记忆系统通常使用静态或弱权重的关系（边），无法捕捉不同查询（Query）下关系强度的动态变化。例如，对于时间敏感的查询，时间边至关重要，而对于实体查询则不然。
*   **研究动机**：将记忆检索从“静态查找”转变为“受查询驱动的动态图遍历”，使智能体能够根据当前任务需求，学习如何优先考虑特定的关系路径。

### 2. 方法论
HAGE 框架的核心思想是将记忆检索建模为在加权多关系图上的顺序决策过程。
*   **加权多关系图构建**：记忆被组织为有向多重图，包含四种关系边：时间（Temporal）、语义（Semantic）、因果（Causal）和实体（Entity）。每条边关联一个可训练的特征向量。
*   **查询条件化检索**：
    1.  **查询分析**：利用 LLM 识别查询的关系意图（如“寻找因果关系”）。
    2.  **动态路由**：一个轻量级的 MLP（QueryRouter）接收查询嵌入和边特征，计算结构权重。
    3.  **遍历评分公式**：最终得分 $S$ 是语义相似度与习得结构权重的加权组合：$S = \lambda \cdot \text{cos\_sim} + (1-\lambda) \cdot \text{weight}$。这允许智能体通过语义不相关但结构关键的“桥接节点”。
*   **强化学习联合优化**：
    1.  **MDP 建模**：将图遍历视为马尔可夫决策过程，状态为当前节点和查询，动作为选择邻居。
    2.  **奖励设计**：成功命中证据节点给予正奖励，同时对步数和超时进行惩罚。
    3.  **联合训练**：使用 REINFORCE 算法同时优化路由网络参数和边特征向量。
    4.  **锚点正则化（Anchor Regularization）**：引入 L2 正则化防止边特征在训练中偏离初始语义含义太远，确保泛化性。

### 3. 实验设计
*   **数据集**：
    *   **LoCoMo**：超长对话基准（平均 9K token），评估长程时间、因果检索。
    *   **HotpotQA**：多跳问答基准，测试跨文档证据的连接能力。
*   **对比方法（Baselines）**：
    *   全上下文（Full Context）
    *   SOTA 记忆系统：A-MEM, Nemori, MemoryOS, MAGMA, MemSkill。
*   **评估指标**：LLM-as-a-Judge 评分（主要）、Token 级别 F1 分数、推理延迟和 Token 消耗。
*   **骨干模型**：GPT-4o-mini 和 Qwen2.5-3B。

### 4. 资源与算力
*   **算力说明**：论文提到使用了 PyTorch 实现和 GPU 加速的路由/边优化。
*   **具体细节**：文中**未明确列出**具体的 GPU 型号（如 A100/H100）或具体的训练总时长。
*   **训练效率**：作者强调 Phase 2 的 RL 训练不需要调用 LLM，而是基于预计算的嵌入和图结构，因此训练过程相对高效。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个大型基准上进行了完整测试。
    *   采用了 **5 折交叉验证（5-fold cross-validation）**，确保在对话样本级别没有数据泄露。
    *   进行了详细的**消融实验**，分别验证了静态边、LLM 评分边、可训练边和可训练路由器的贡献。
*   **充分性评价**：实验设计较为客观公平，不仅对比了准确率，还详细分析了准确率与效率（延迟、Token 成本）的权衡，证明了 HAGE 在实际部署中的可行性。

### 6. 主要结论与发现
*   **性能领先**：HAGE 在 LoCoMo 和 HotpotQA 上均取得了最佳的综合评分，尤其在对抗性（Adversarial）和时间推理任务中提升显著。
*   **联合优化的必要性**：消融实验证明，同时训练边特征和路由策略的效果优于仅训练其中之一。
*   **效率平衡**：虽然 HAGE 引入了图遍历，但其 Token 消耗和延迟仍保持在与现有检索系统相当的水平，且准确率更高。

### 7. 优点
*   **动态性**：打破了传统图记忆的静态限制，实现了“因题制宜”的检索。
*   **RL 驱动**：利用下游任务的反馈直接优化检索路径，而非依赖人工设计的启发式规则。
*   **可解释性**：通过关系意图分类和边权重，可以观察到智能体在检索时偏好哪种逻辑联系。

### 8. 不足与局限
*   **LLM 依赖**：查询意图的识别和最终答案的生成仍高度依赖底层 LLM 的指令遵循能力。
*   **场景覆盖**：实验主要集中在对话和多跳问答，对于程序代码分析或大规模文档库的适用性尚待验证。
*   **隐私风险**：作为一种持久化记忆系统，如何安全地处理和遗忘敏感个人信息是此类技术通用的伦理挑战。

（完）
