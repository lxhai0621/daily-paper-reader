---
title: "SARC: A Governance-by-Architecture Framework for Agentic AI Systems"
title_zh: SARC：一种面向代理式 AI 系统的架构化治理框架
authors: Gaston Besanson
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07728v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 工具使用智能体的运行时治理架构
tldr: 针对代理AI系统治理滞后于执行的问题，本文提出SARC架构。该架构将约束视为一等公民，通过在代理循环中集成预操作门控、实时监控、事后审计和升级路由四个执行点，实现运行时治理。实验表明，SARC能有效消除硬约束违规并显著降低软约束超限，为受监管环境下的AI代理提供了可执行且可审计的架构基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.07728v1/fig-001.webp\", \"caption\": \"Table 1: SARC and adjacent frameworks. SARC sits upstream of optimization frameworks (MDP, CMDP, safe RL) and is implementable on top of generic substrates (policy-as-code, runtime enforcement). What SARC contributes that none of the others does is an agent-specific specification of which constraint goes where in the agent loop, with regulatory compilation as a first-class concern.\", \"page\": 6, \"index\": 1, \"width\": 945, \"height\": 428}, {\"url\": \"assets/figures/arxiv/2605.07728v1/fig-002.webp\", \"caption\": \"Table 2: Mapping between classical reference monitor properties and SARC invariants. The framework instantiates the three reference monitor requirements within the agent execution loop, with PAG, ATM, PAA, and ER serving as the concrete enforcement points.\", \"page\": 11, \"index\": 2, \"width\": 881, \"height\": 377}, {\"url\": \"assets/figures/arxiv/2605.07728v1/fig-003.webp\", \"caption\": \"Table 5: SARC compared to practitioner agent-governance approaches in current use. The comparison is not that SARC is universally superior; the leftmost three rows describe legitimate light-touch mechanisms suitable for low-stakes deployments. The point is that the four practitioner approaches each suffer a structural failure mode that SARC’s class-determined enforcement is specifically designed to address.\", \"page\": 21, \"index\": 3, \"width\": 927, \"height\": 292}, {\"url\": \"assets/figures/arxiv/2605.07728v1/fig-004.webp\", \"caption\": \"Table 7: Predicate-noise / enforcement-failure sweep under SARC, H = 1,000 orders per episode, N = 50 seeds per cell, mean ± 95% CI. Under exact predicates and perfect enforcement (top row), SARC executes zero hard violations. Residual violations grow approximately linearly in the enforcement-stack error budget ϵpred + ϵexec rather than in the environmental opportunity rate, which under post-hoc audit yields ∼26.8 hard executions per episode (Table 6). The structural property the framework targets is decoupling residual violations from environmental pressure.\", \"page\": 33, \"index\": 4, \"width\": 1002, \"height\": 314}, {\"url\": \"assets/figures/arxiv/2605.07728v1/fig-005.webp\", \"caption\": \"Figure 1: Reference runtime architecture. The specification Σ compiles into runtime checks attached at four enforcement points. PAG sits between the planner’s intended action and the tool dispatcher; ATM wraps tool execution; PAA inspects the post-action state before the next loop iteration; ER routes any flagged event to a human operator and returns the ruling.\", \"page\": 13, \"index\": 5, \"width\": 974, \"height\": 388}, {\"url\": \"assets/figures/arxiv/2605.07728v1/fig-006.webp\", \"caption\": \"Table 3: Canonical mapping from constraint class to enforcement point. The strongest applicable point is preferred: a hard constraint that can be evaluated at PAG should never be deferred to PAA, where the action has already executed.\", \"page\": 13, \"index\": 6, \"width\": 946, \"height\": 227}, {\"url\": \"assets/figures/arxiv/2605.07728v1/fig-007.webp\", \"caption\": \"Table 4: Annex III categories mapped to dominant SARC constraint patterns. The table identifies typical patterns, not a completeness claim. Real deployments will combine multiple patterns and add domain-specific constraints.\", \"page\": 17, \"index\": 7, \"width\": 946, \"height\": 485}, {\"url\": \"assets/figures/arxiv/2605.07728v1/fig-008.webp\", \"caption\": \"Figure 2: (Left) Hard-violation rate vs. underlying violation probability ϵ. The post-hoc-audit curve scales linearly with ϵ; the SARC curve is flat at zero because PAG blocks before dispatch. Under the modeled assumptions, the structural distinction is preserved as ϵ varies. (Right) Escalation queue mean wait Wq vs. utilization ρ, M/M/c. As ρ→ 1, Wq diverges super-linearly. The reversibility window τrev (dashed) defines the operationally admissible region. The architect’s task is to keep (λe, c, µ) in the region where Wq < τrev; once the system leaves that region, escalation is structurally too slow regardless of operator effort.\", \"page\": 31, \"index\": 8, \"width\": 936, \"height\": 236}, {\"url\": \"assets/figures/arxiv/2605.07728v1/fig-009.webp\", \"caption\": \"Figure 3: Latency-versus-safety trade-off across enforcement strategies. Post-hoc audit incurs zero per-step latency but pays the full violation rate. Output filtering and workflow rules cut violations partially at modest latency cost. SARC at PAG eliminates hard violations at ∼21 ms (deterministic predicate over typed signature). SARC with retrieval-augmented checks costs more but stays in O(ℓtool). SARC implementations that route checks through a separate LLM call are flagged for the architectural reason discussed in §5.1: they cross the O(ℓmodel) boundary and inherit the nondeterminism the check is meant to bound.\", \"page\": 31, \"index\": 9, \"width\": 601, \"height\": 260}]"
motivation: 现有的AI治理手段多为提示词或事后文档，难以在受监管的动态执行过程中实时约束代理行为。
method: 提出SARC架构，将约束规范编译为代理循环中的四个关键执行点，并支持多代理工作流中的约束传播与归因。
result: "在采购任务评估中，SARC实现了硬约束零违规，且相比仅采用策略代码的基准方案，其软窗口超限减少了89.5%。"
conclusion: SARC通过架构层面的约束集成，确保了AI代理行为与合规义务在运行时的一致性与可审计性。
---

## 摘要
代理式 AI 系统越来越多地通过工具、子代理和外部服务采取行动，但治理控制通常仍附着在提示词、仪表板或事后文档中。这在受监管的环境中造成了结构性失配：必须约束执行过程的义务往往仅在执行发生后才被评估。我们引入了 SARC，这是一种针对使用工具的代理的运行时治理架构，它将约束视为与状态、动作空间和奖励并列的一等规范对象。SARC 规范声明了每个约束的来源、类别、谓词、验证点、响应协议和操作点，并将这些编译到代理循环中的四个执行位点：动作前闸门、动作时监控器、动作后审计器和升级路由器。我们形式化了规范-轨迹对应所需的最小不变性，展示了为什么有限的奖励惩罚通常不能替代硬性运行时约束，并将该架构扩展到多代理工作流，涉及约束传播、权限交集和保留归因的轨迹树。我们实现了一个原型审计检查器，并报告了在 50 个种子上的可重复合成评估，在采购任务中将 SARC 与事后审计、输出过滤、工作流规则和仅策略即代码的基准进行了比较。在精确谓词下，SARC 实现了零硬约束违规；其声明的 PAA 节流响应相对于仅策略即代码的方法，将软窗口超限减少了 89.5%。谓词噪声和执行失败扫描结果与以下主张一致：SARC 下的残留硬违规随执行栈误差而非环境违规机会而缩放。SARC 提供了架构基座，通过该基座，义务可以在运行时变得可执行、可检查和可审计。

## Abstract
Agentic AI systems increasingly act through tools, sub-agents, and external services, but governance controls are still commonly attached to prompts, dashboards, or post-hoc documentation. This creates a structural mismatch in regulated settings: obligations that must constrain execution are often evaluated only after execution has occurred. We introduce SARC, a runtime governance architecture for tool-using agents that treats constraints as first-class specification objects alongside state, action space, and reward. A SARC specification declares each constraint's source, class, predicate, verification point, response protocol, and operating point, and compiles these into four enforcement sites in the agent loop: a Pre-Action Gate, an Action-Time Monitor, a Post-Action Auditor, and an Escalation Router. We formalize the minimal invariants required for specification-trace correspondence, show why finite reward penalties do not generally substitute for hard runtime constraints, and extend the architecture to multi-agent workflows through constraint propagation, authority intersection, and attribution-preserving trace trees. We implement a prototype audit checker and report a reproducible synthetic evaluation over 50 seeds comparing SARC against post-hoc audit, output filtering, workflow rules, and policy-as-code-only baselines on a procurement task. SARC executes zero hard-constraint violations under exact predicates; its declared PAA throttling response reduces soft-window overages by 89.5% relative to policy-as-code-only. Predicate-noise and enforcement-failure sweeps are consistent with the claim that residual hard violations under SARC scale with enforcement-stack error rather than environmental violation opportunity. SARC provides the architectural substrate through which obligations can be made executable, inspectable, and auditable at runtime.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **SARC** 的架构化治理框架，旨在解决代理式 AI（Agentic AI）系统在受监管环境中的合规与安全执行问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前的 AI 治理手段（如提示词工程、事后审计、静态文档）与代理式 AI 的动态执行之间存在“结构性失配”。当 AI 代理能够自主调用工具、子代理和外部服务时，传统的治理手段往往“来得太晚”或容易被绕过。
*   **研究背景**：随着欧盟《人工智能法案》（EU AI Act）等法规的出台，受监管行业（如金融、医疗、采购）迫切需要一种能将法律义务转化为运行时（Runtime）约束的技术手段。
*   **整体含义**：SARC 提倡“架构即治理”，将约束（Constraints）提升到与状态（S）、动作空间（A）和奖励（R）同等的一等公民地位，确保治理逻辑嵌入在代理的执行循环中，而非仅仅作为外部说明。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：将监管义务“编译”为运行时约束。SARC 规范由四元组 $\Sigma = \langle S, A, R, C \rangle$ 组成，其中 **C（约束）** 是核心贡献。
*   **关键技术细节**：
    *   **约束五元组**：每个约束定义为 $\langle src, class, pred, verif, resp \rangle$（来源、类别、谓词、验证点、响应协议）。
    *   **约束分类**：
        *   **硬约束 ($C_h$)**：绝对禁止违规，触发则拦截。
        *   **软约束 ($C_s$)**：允许违规但需支付代价（如节流、记录）。
        *   **升级约束 ($C_e$)**：触发时需将控制权移交给人类操作员。
    *   **四个执行位点**：
        1.  **PAG (预操作门控)**：动作执行前拦截。
        2.  **ATM (动作时监控)**：监控流式输出或执行过程。
        3.  **PAA (动作后审计)**：检查动作后的状态变化。
        4.  **ER (升级路由器)**：管理人类介入的队列和响应。
    *   **多代理扩展**：引入了“权限交集”原则（权限随调用链单调不增）和“保留归因的轨迹树”，防止约束在代理间传递时被“洗白”。
    *   **算法流程**：在代理循环的每一步，系统必须通过 PAG 验证，执行受限动作，并在 PAA 阶段更新状态，所有过程生成符合规范的结构化追踪（Trace）。

### 3. 实验设计
*   **场景**：一个合成的**企业采购任务**。代理负责处理采购订单，面临金额限制（硬约束）、滚动支出上限（软约束）和新供应商审核（升级约束）。
*   **Benchmark 与对比方法**：
    *   **事后审计 (Post-hoc Audit)**：仅在执行后记录违规。
    *   **输出过滤 (Output Filtering)**：对 LLM 生成的文本进行过滤。
    *   **工作流规则 (Workflow Rules)**：在编排层设置简单的 if/else 逻辑。
    *   **仅策略即代码 (Policy-as-code-only)**：使用通用策略引擎但缺乏 SARC 的运行时位点和响应机制。
    *   **SARC (本文方法)**：完整实现四个执行位点和类别化响应。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号或训练时长。
*   **原因**：该研究侧重于系统架构和运行时逻辑，其实验部分主要基于合成环境的模拟（Simulation），而非大规模模型训练。评估重点在于约束逻辑的拦截率和延迟，而非模型本身的生成能力。

### 5. 实验数量与充分性
*   **实验规模**：在 **50 个随机种子**下进行了重复实验，并报告了 95% 的置信区间。
*   **充分性**：
    *   进行了**灵敏度分析**：包括谓词噪声（Predicate Noise）和执行失败率（Enforcement Failure）的扫描。
    *   **客观性**：通过对比多种工业界常用的基准方法，展示了 SARC 在处理“低概率、高风险”违规时的结构性优势。
    *   **公平性**：实验控制了相同的环境机会率（$\epsilon$），确保对比是在相同压力测试下进行的。

### 6. 主要结论与发现
*   **硬约束零违规**：在精确谓词下，SARC 实现了硬约束的零违规执行。
*   **软约束优化**：相比于“仅策略即代码”基准，SARC 通过 PAA 节流响应将软窗口超限减少了 **89.5%**。
*   **误差缩放规律**：SARC 下的残留违规率仅随执行栈本身的误差（谓词错误、系统故障）缩放，而与环境中的违规机会无关。
*   **可审计性**：通过原型审计检查器，证明了 SARC 生成的轨迹可以机械化地验证是否符合初始规范。

### 7. 优点
*   **架构化集成**：将治理从“文档”转变为“代码”和“执行逻辑”，解决了代理自主性带来的安全隐患。
*   **合规性映射**：清晰地展示了如何将《欧盟 AI 法案》的具体条款（如第 14 条人类监督）编译为技术约束。
*   **多代理支持**：提出了严密的权限传播和归因模型，填补了复杂代理工作流治理的空白。
*   **经济性考量**：引入了操作点（Operating Point）概念，平衡了误报成本、漏报风险和人力成本。

### 8. 不足与局限
*   **合成环境局限**：实验基于合成采购任务，尚未在真实的大规模生产环境或复杂的对抗性场景（如间接提示词注入攻击）中进行验证。
*   **谓词依赖**：SARC 的有效性高度依赖于谓词（Predicate）的准确性。如果法律义务被错误地翻译为代码谓词，系统将“合规地执行错误规则”。
*   **人力瓶颈**：升级路由器（ER）依赖人类响应，在高并发场景下可能面临队列饱和或操作员疲劳问题。
*   **延迟开销**：虽然单步延迟增加较小（约 21ms），但在极高性能要求的场景下，多点监控仍是不小的开销。

（完）
