---
title: "Why Retrying Fails: Context Contamination in LLM Agent Pipelines"
title_zh: 为什么重试会失败：LLM 智能体流水线中的上下文污染
authors: Zhanfu Yang
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.08563v1"
tags: ["query:agent"]
score: 9.0
evidence: 多步工具增强智能体任务中的上下文污染
tldr: 本研究针对 LLM 智能体在多步任务失败后重试时，因上下文残留导致错误率升高的“上下文污染”现象，提出了上下文污染重启模型（CCRM）。该模型形式化了重试过程中的性能退化，推导出了成功概率闭式解、最优预算分配方案及清理上下文的收益。实验证明，CCRM 能精准预测 SWE-bench 上的表现，揭示了传统独立同分布模型对重试成功率的严重高估，为优化智能体流水线提供了理论指导。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.08563v1/fig-001.webp\", \"caption\": \"Table 1. Comparison with closely related theoretical work. ✓= provided, – = not provided.\", \"page\": 3, \"index\": 1, \"width\": 954, \"height\": 186}, {\"url\": \"assets/figures/arxiv/2605.08563v1/fig-002.webp\", \"caption\": \"Fig. 2. Synthetic validation (n = 30,000 MC trials, diamonds). (a) Formula matches simulation for all cascade strengths. (b) Cascade overhead diverges (phase transition) as ε1/ε0 increases. (c) Optimal T ∗ agrees with simulated optimum.\", \"page\": 7, \"index\": 2, \"width\": 711, \"height\": 196}, {\"url\": \"assets/figures/arxiv/2605.08563v1/fig-003.webp\", \"caption\": \"Fig. 3. Real-data validation. (a) CCRM fit to Verdent [14] SWE-bench Verified: pass@1=0.761, pass@3=0.812 (consecutive attempts, same context). IID overestimates by 0.174 at K = 3. (b) For independent fresh-start retries (USEagent [19], OpenHands), IID overestimates due to correlated task difficulty—a distinct phenomenon.\", \"page\": 8, \"index\": 3, \"width\": 710, \"height\": 226}]"
motivation: 旨在解决 LLM 智能体在重试过程中因历史失败信息污染上下文，导致后续尝试错误率显著高于初始水平的问题。
method: 构建了 CCRM 数学模型，通过建模基础错误率与污染后错误率的关系，推导出关于成功率、预算分配和信息论下界的五项理论成果。
result: "实验显示 CCRM 在 SWE-bench 上的拟合精度极高，揭示了污染后的错误率是基础错误率的 7.1 倍，而 IID 模型高估了 17.4% 的成功率。"
conclusion: 研究强调了上下文清理在智能体重试机制中的重要性，并为多步任务的资源分配提供了最优化的数学准则。
---

## 摘要
当大语言模型（LLM）智能体在执行多步工具增强任务失败并重试时，失败的尝试通常会保留在其上下文窗口中，从而污染下一次尝试，并将每步错误率提高到基础水平之上。这种上下文污染重启现象在实践中被广泛观察到，但完全缺乏正式的理论处理。我们引入了上下文污染重启模型（CCRM）：一个由 T 个工具调用步骤组成的链，每步的基础失败率为 epsilon_0；在任何尝试失败后，后续尝试将在受污染的上下文中运行，其错误率 epsilon_1 > epsilon_0。在该模型下，我们推导出了五个主要结果：(R1) 最多 K 次尝试成功的概率 P 的精确闭式公式；(R2) 级联开销定理，给出了与干净重启基准相比，由污染引起的额外尝试次数 Delta K；(R3) 最优预算分配定理，确定了在固定总预算 B=KT 下使成功概率最大化的流水线深度 T*，并证明了其闭式解；(R4) 通过 Le Cam 方法得出的信息论下界，表明 K_CCRM 在 O(1) 范围内是紧致的；(R5) 干净重启优势定理，量化了重试前清除上下文的确切收益。我们在真实的 SWE-bench Verified 数据上验证了 CCRM：独立同分布（IID）模型将 pass@3 高估了 17.4 个百分点（98.6% 对比 81.2%），而 CCRM 的拟合误差小于 0.001，这意味着级联比 epsilon_1/epsilon_0 = 7.1。蒙特卡洛实验证实了所有理论预测。

## Abstract
When an LLM agent fails a multi-step tool-augmented task and retries, the failed attempt typically remains in its context window -- contaminating the next attempt and elevating the per-step error rate beyond the base level. This context-contaminated restart phenomenon is widely observed in practice yet entirely lacks formal treatment. We introduce the Context-Contaminated Restart Model (CCRM): a chain of T tool-call steps, each failing with base rate epsilon_0; after any failed attempt, the subsequent attempt operates in contaminated context with elevated error rate epsilon_1 > epsilon_0. Under this model we derive five main results. (R1) An exact closed-form formula for P(succeed in at most K attempts). (R2) A cascade-overhead theorem giving the additional attempts Delta K incurred by contamination versus the clean-restart baseline. (R3) An optimal budget-allocation theorem identifying the pipeline depth T* that maximises success probability for a fixed total budget B=KT; we prove the closed form T* = sqrt(B * log(1/(1-epsilon_1)) / log(1/(1-epsilon_0))), with K*=B/T*. (R4) An information-theoretic lower bound via Le Cam's method showing K_CCRM is tight up to O(1). (R5) A clean-restart dominance theorem quantifying the exact benefit of context-clearing before retry. We validate CCRM on real SWE-bench Verified data: the IID model overestimates pass@3 by 17.4 percentage points (98.6% vs. 81.2%), while CCRM fits with error less than 0.001, implying a cascade ratio of epsilon_1/epsilon_0 = 7.1. Monte Carlo experiments confirm all theoretical predictions.

---

## 论文详细总结（自动生成）

这篇论文对大语言模型（LLM）智能体在多步任务中的“重试失败”现象进行了深入的理论分析和实证研究。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当 LLM 智能体在执行多步工具调用任务失败并选择“重试”时，通常会将失败的历史记录保留在上下文窗口中。这种做法会导致后续尝试受到先前错误思路或结果的干扰，即**上下文污染（Context Contamination）**。
*   **研究动机**：尽管开发者普遍观察到重试效果往往不如预期，但现有研究多集中于单次运行的可靠性，缺乏对“跨尝试污染”现象的正规数学建模。论文旨在量化这种污染如何影响成功率、增加计算开销，并寻找最优的资源分配策略。

### 2. 方法论：上下文污染重启模型 (CCRM)
论文提出了 **CCRM 模型**，其核心思想是将重试过程建模为一个非独立的马尔可夫链：
*   **核心假设**：
    *   任务由 $T$ 个步骤组成。
    *   **初始状态**：第一次尝试时，每步错误率为基础错误率 $\epsilon_0$。
    *   **污染状态**：一旦某次尝试失败，后续所有尝试的每步错误率将上升至 $\epsilon_1$（其中 $\epsilon_1 > \epsilon_0$）。
*   **关键公式**：
    *   **成功概率 (R1)**：推导出在 $K$ 次尝试内成功的精确闭式公式：$P = p_0 + (1 - p_0)[1 - (1 - p_1)^{K-1}]$，其中 $p_i = (1 - \epsilon_i)^T$。
    *   **最优流水线深度 (R3)**：在总预算 $B$（总工具调用次数）固定的情况下，推导出最优的任务步数 $T^*$，以平衡单次成功率与重试次数。
    *   **级联开销 (R2)**：量化了由于污染导致的额外尝试次数 $\Delta K$。
    *   **信息论下界 (R4)**：利用 Le Cam 方法证明了该模型在区分污染与非污染状态时的理论极限。

### 3. 实验设计
*   **合成实验**：使用蒙特卡洛（Monte Carlo）模拟进行 30,000 次试验，验证理论公式在不同错误率和任务深度下的准确性。
*   **真实数据验证**：
    *   **数据集**：使用了 **SWE-bench Verified**（软件工程基准测试）的真实数据。
    *   **对比基准**：将 CCRM 模型与传统的 **IID 模型**（假设每次重试相互独立、错误率不变）进行对比。
    *   **具体案例**：分析了 Verdent、USEagent 和 OpenHands 等智能体框架在连续重试与独立重试下的表现差异。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **原因分析**：由于该研究侧重于数学建模、概率推导和基于现有基准测试数据的拟合分析，而非大规模模型训练，因此其核心贡献在于理论框架，对高性能算力的依赖较低。

### 5. 实验数量与充分性
*   **实验规模**：
    *   合成实验进行了 30,000 次随机模拟，覆盖了从轻微污染到严重污染的多种场景。
    *   真实数据涵盖了 SWE-bench 上的数千个任务实例。
*   **充分性与客观性**：实验设计较为充分，通过对比“连续重试（同上下文）”和“独立重试（清空上下文）”两种模式，客观地证明了上下文污染的存在。模型拟合误差小于 0.001，显示了极高的解释力。

### 6. 主要结论与发现
*   **IID 模型的高估**：传统的独立同分布假设会严重高估重试成功率。在 SWE-bench 上，IID 模型预测 pass@3 为 98.6%，而实际仅为 81.2%。
*   **级联效应**：实验发现真实场景下的级联比 $\epsilon_1/\epsilon_0$ 高达 **7.1 倍**，即失败一次后，后续每步出错的概率激增。
*   **干净重启的优越性**：理论和实验均证明，**在重试前清空上下文（Clean-restart）** 总是能显著提高成功率并节省预算。
*   **最优分配**：存在一个最优的任务深度 $T^*$，当污染严重时，应倾向于将任务拆解得更细或减少单次尝试的深度。

### 7. 优点
*   **理论开创性**：首次为 LLM 智能体的上下文污染现象提供了严谨的数学定义和闭式解。
*   **高度实用性**：给出的最优预算分配公式和“必须清空上下文”的建议对工业界优化智能体流水线具有直接指导意义。
*   **拟合度极高**：模型在真实世界数据上的表现与理论预测近乎完美契合。

### 8. 不足与局限
*   **二元状态简化**：模型假设污染状态是二元的（要么没污染，要么污染程度固定），而现实中污染可能随重试次数增加而持续累积或加深。
*   **步骤独立性假设**：模型假设单次尝试内的各步骤错误是独立的，忽略了步骤间的逻辑依赖关系。
*   **任务难度相关性**：论文提到，某些失败是因为任务本身极难（Correlated task difficulty），而 CCRM 目前主要关注上下文导致的失败，未能完全解耦这两者。

（完）
