---
title: "GRC: Unifying Reasoning-Driven Generation, Retrieval and Compression"
title_zh: GRC：统一推理驱动的生成、检索与压缩
authors: "Zhongtao Miao, Qiyu Wu, Yoshimasa Tsuruoka"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.09100v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 统一长上下文智能体任务的生成、检索和上下文压缩
tldr: 本研究针对大语言模型中生成、检索与压缩任务训练分离导致的成本高、部署难问题，提出了GRC统一框架。该框架利用元潜代币（meta latent tokens）在单次前向传播中同时完成推理驱动的生成、增强型文本表示及上下文压缩。GRC不仅降低了RAG部署难度，还实现了O(1)长度的潜存储增强生成。实验表明，该方法在多项任务中表现出色，显著提升了数据利用率和推理效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.09100v1/fig-001.webp\", \"caption\": \"Table 15: Comparison of averaged actual inference time (s) per user query between the naive and HPA-based inference implementation. The batch size is set to 1 for naive implementation. The hyperparameters of HPA-based inference implementation are shown in Table 14.\", \"page\": 21, \"index\": 1, \"width\": 768, \"height\": 613}, {\"url\": \"assets/figures/arxiv/2605.09100v1/fig-002.webp\", \"caption\": \"Figure 1: GRC training framework. One forward pass of a single model can fulfill three objectives: (1) generating output (reasoning-answers); (2) compressing user-assistant chat history into compact representations/KV cache of meta latent tokens through attentions; (3) obtaining reasoning-enhanced text embeddings of context by pooling the token representations of meta latent tokens.\", \"page\": 3, \"index\": 2, \"width\": 806, \"height\": 233}, {\"url\": \"assets/figures/arxiv/2605.09100v1/fig-003.webp\", \"caption\": \"Table 7: Models benchmarked in experiments. This table is from the BRIGHT paper [24].\", \"page\": 18, \"index\": 3, \"width\": 822, \"height\": 423}, {\"url\": \"assets/figures/arxiv/2605.09100v1/fig-004.webp\", \"caption\": \"Table 6: Reconstruction prompt set.\", \"page\": 18, \"index\": 4, \"width\": 773, \"height\": 266}, {\"url\": \"assets/figures/arxiv/2605.09100v1/fig-005.webp\", \"caption\": \"Table 11: GRC QA prompt.\", \"page\": 19, \"index\": 5, \"width\": 773, \"height\": 198}, {\"url\": \"assets/figures/arxiv/2605.09100v1/fig-006.webp\", \"caption\": \"Table 9: GRC query prompt used to extract query embeddings.\", \"page\": 19, \"index\": 6, \"width\": 773, \"height\": 220}, {\"url\": \"assets/figures/arxiv/2605.09100v1/fig-007.webp\", \"caption\": \"Table 5: Hyperparameters and hardware accelerators for training GRC models.\", \"page\": 17, \"index\": 7, \"width\": 652, \"height\": 436}, {\"url\": \"assets/figures/arxiv/2605.09100v1/fig-008.webp\", \"caption\": \"Table 13: GritLM RAG prompt.\", \"page\": 20, \"index\": 8, \"width\": 773, \"height\": 215}, {\"url\": \"assets/figures/arxiv/2605.09100v1/fig-009.webp\", \"caption\": \"Table 12: GRC RAG prompt (regular document).\", \"page\": 20, \"index\": 9, \"width\": 773, \"height\": 285}]"
motivation: 旨在解决LLM中生成、检索和压缩任务独立训练带来的高昂成本及长上下文处理中的效率瓶颈。
method: 引入元潜代币和统一调优方法，使模型能在单次前向传播中协同完成生成、表示与压缩三大任务。
result: 在检索、生成和压缩等多个基准测试中均取得优异成绩，并实现了三倍的数据利用率和更快的推理速度。
conclusion: GRC通过模块化设计和统一架构，为实现生成、嵌入与压缩无缝集成的全能型模型开辟了新路径。
---

## 摘要
如今，文本嵌入和生成任务通常基于大语言模型（LLMs）分别进行训练。这导致了大量的训练成本和部署工作。上下文压缩也是一项具有挑战性且紧迫的任务，对于推理驱动的生成以及需要长上下文和持续学习的智能体任务至关重要。在本文中，我们探索了如何在大语言模型的一次前向传播中统一推理驱动的生成、推理增强的文本表示和上下文压缩任务。通过元潜代币（meta latent tokens）和统一的生成、表示及压缩微调方法，我们提出了一个名为 GRC 的训练框架，将这三项任务连接起来。训练后的模型可以在单次前向传播中完成三个目标，同时在推理过程中保持模块化的、乐高式的灵活性。这种设计大大减少了检索增强生成（RAG）的部署工作，并实现了高效的推理和训练期间三倍的数据利用率。此外，该框架设计实现了一种新的文本嵌入范式：自推理潜嵌入（self-reason-latent embeds），以及一种新的生成范式：潜记忆增强生成（latent memory-augmented generation），其中长度为 O(1) 的压缩且内化的 KV 缓存被用作可更新的记忆。我们还提出了混合分页注意力（hybrid paged attention）来加速模型的推理。在推理密集型检索基准、生成任务、文档压缩、延迟评估和 RAG 设置上的广泛实验证明了我们方法的有效性，并可能为能够无缝处理推理驱动的生成、嵌入和压缩任务的真正统一模型提供启示。

## Abstract
Text embedding and generative tasks are usually trained separately based on large language models (LLMs) nowadays. This causes a large amount of training cost and deployment effort. Context compression is also a challenging and pressing task, which is vital to reasoning-driven generation, and agentic tasks requiring long context and continual learning. In this paper, we explore how to unify reasoning-driven generation, reasoning-enhanced text representation and context compression tasks in one forward pass for LLMs. Through meta latent tokens and a unified generative, representative and compressive tuning approach, we propose a training framework named GRC that bridges the three tasks. The trained models can accomplish three objectives in a single forward pass while maintaining modular, LEGO-style flexibility during inference. This design greatly reduces the deployment effort for retrieval-augmented generation (RAG) and achieves efficient inference and three times data utilization during training. Furthermore, this framework design enables a new paradigm for text embedding: self-reason-latent embeds, and a new generation paradigm, latent memory-augmented generation, where compressed and internalized KV cache with O(1) length is used as the updatable memory. We also propose hybrid paged attention to speed up the inference of our models. Extensive experiments on reasoning-intensive retrieval benchmarks, generative tasks, document compression, latency evaluation, and RAG settings demonstrate the effectiveness of our method and may shed light on the truly unified model that can handle reasoning-driven generation, embedding and compression tasks seamlessly.

---

## 论文详细总结（自动生成）

### 论文总结：GRC - 统一推理驱动的生成、检索与压缩

#### 1. 论文的核心问题与整体含义
*   **研究背景**：当前大语言模型（LLM）在实际应用（如 RAG、智能体任务）中，通常需要三个独立模块：生成模型（产生答案）、嵌入模型（文本检索）和压缩模型（管理长上下文）。
*   **核心痛点**：
    1.  **成本高昂**：三个模块独立训练和部署，导致计算资源浪费。
    2.  **效率低下**：不同模块间隐藏状态或 KV 缓存无法复用，同一文本需多次处理。
    3.  **推理压力**：推理驱动型模型（如 o1）产生的推理链极长，增加了存储和计算负担。
*   **研究目标**：提出 **GRC 框架**，旨在通过单一模型、单次前向传播，同时实现推理驱动的生成、语义检索和上下文压缩。

#### 2. 方法论
*   **核心思想**：引入**元潜代币（Meta Latent Tokens）**作为“潜寄存器”，在不改变模型词表的前提下，通过特定的注意力掩码设计，使其在单次计算中承载生成、压缩和表示三种功能。
*   **关键技术细节**：
    *   **元潜代币 ($r_i$)**：一组可训练的参数，不进入词表。它们在 Transformer 层中学习如何提取和缓存上下文的关键语义。
    *   **统一训练目标**：
        1.  **生成损失 ($L_{Gen}$)**：标准的交叉熵损失，用于推理链和答案的生成。
        2.  **重建损失 ($L_{Recons}$)**：强制模型仅通过元潜代币的 KV 缓存来重建原始上下文，从而实现信息压缩。
        3.  **表示损失 ($L_{Rep}$)**：对元潜代币的顶层隐藏状态进行适配器（Adapter）转换和池化，通过对比学习（InfoNCE）优化检索性能。
    *   **因果掩码设计**：采用统一的因果注意力掩码，但在重建阶段屏蔽原始上下文，迫使模型“回溯”元潜代币中的压缩信息。
    *   **混合分页注意力（HPA）**：一种改进的推理引擎，将常规 KV 缓存与 $O(1)$ 长度的压缩 KV 缓存统一管理，大幅提升吞吐量。

#### 3. 实验设计
*   **数据集与场景**：
    *   **检索**：使用 **BRIGHT**（推理密集型检索基准），涵盖生物、地球科学、编程等 12 个领域。
    *   **生成**：使用 **GSM8K**（数学推理）和 **BBH**（综合推理）。
    *   **压缩**：使用 **PwC** 测试集和新构建的 **Wikipedia Markdown** 文档（2026 年数据，防止污染）。
    *   **RAG 场景**：使用 **Natural Questions (NQ)** 及其对应的 BEIR 语料库。
*   **对比方法**：
    *   检索：对比了 BM25、OpenAI/Google/Cohere 闭源模型、GritLM-7B、ReasonIR-8B 等。
    *   生成：对比了 GPT-4、Llama 2、Mistral、Mixtral 等。
    *   压缩：对比了 AutoCompressor、ICAE-v2 等。

#### 4. 资源与算力
*   **硬件**：使用了 **32 个计算节点**，每个节点配备 **NVIDIA GH200 Grace Hopper Superchip**。
*   **训练细节**：
    *   GRC-1.7B 训练了约 550 步，GRC-4B 训练了约 440 步。
    *   总训练时长约为 32 * 40 GPU 小时。
    *   使用了 PyTorch FSDP 和 GradCache 技术以节省显存。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的广泛实验，包括检索精度、生成能力、重建质量、RAG 端到端表现以及推理延迟测试。
*   **充分性评价**：
    *   **客观性**：通过构建 2026 年的新数据集来验证压缩能力，有效规避了预训练数据污染问题。
    *   **消融研究**：分析了推理链长度、采样温度对检索和压缩性能的影响，实验逻辑严密。
    *   **公平性**：在相同硬件环境下对比了原生实现与 HPA 引擎的延迟，对比基准清晰。

#### 6. 主要结论与发现
*   **性能突破**：GRC-1.7B 在推理密集型检索任务（BRIGHT）上击败了规模更大的 ReasonIR-8B。
*   **推理增强检索**：自生成的推理链能显著提升检索效果，但存在“边际效应”，过长的推理并不总是有效。
*   **压缩优势**：成功将文档 KV 缓存从 $O(N)$ 降低到固定的 $O(1)$（如 128 tokens），且在 RAG 任务中保留了大部分关键信息。
*   **效率飞跃**：提出的 HPA 推理引擎相比原生实现实现了 **10 倍的加速**。

#### 7. 优点
*   **高度集成**：首个在单次前向传播中无缝整合生成、检索和压缩的模型，极大地简化了 RAG 系统的架构。
*   **存储友好**：通过潜记忆增强生成，解决了长文档 KV 缓存占用显存过大的问题。
*   **灵活性强**：支持“乐高式”推理，可根据任务需求灵活切换生成模式或嵌入模式。

#### 8. 不足与局限
*   **训练稳定性风险**：论文提到在最后代币池化（Last Token Pooling）时，隐藏状态同时用于预测下一个 token 和提取向量，可能存在目标冲突。
*   **模型规模局限**：目前实验主要集中在 1.7B 和 4B 规模，尚未验证该框架在超大规模参数（如 70B+）模型上的扩展性。
*   **任务覆盖**：虽然在推理密集型任务表现优异，但在通用短文本检索（如 MTEB 全集）上的表现未做全面展示。

（完）
