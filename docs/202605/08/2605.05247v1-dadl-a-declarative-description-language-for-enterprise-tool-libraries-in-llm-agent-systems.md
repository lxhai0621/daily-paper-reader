---
title: "DADL: A Declarative Description Language for Enterprise Tool Libraries in LLM Agent Systems"
title_zh: DADL：大语言模型智能体系统中企业级工具库的声明式描述语言
authors: Axel Dunkel
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.05247v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 使用DADL和MCP将外部API与LLM智能体集成
tldr: 针对LLM Agent在企业级应用中工具集成繁琐及上下文消耗过大的问题，本文提出DADL描述语言。它采用声明式YAML格式定义API，通过统一运行时替代多服务器部署，并利用“代码模式”接口实现固定大小的上下文占用。该方案支持凭据集中管理，在包含1833个工具的测试中将上下文成本降低了142倍，为构建可扩展、可审计的企业工具库提供了高效路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.05247v1/fig-001.webp\", \"caption\": \"Figure 1. Side-by-side architectural comparison of the conventional one-MCP-server-per-API model (left) with the DADL + Code Mode model (right). The left side shows N independent server processes, each with its own runtime and deployment, exposing NxM individual tool schemas that overflow the LLM context window. The right side shows a single shared runtime that interprets per-API DADL files; the agent reaches the entire\", \"page\": 18, \"index\": 1, \"width\": 1008, \"height\": 463}, {\"url\": \"assets/figures/arxiv/2605.05247v1/fig-002.webp\", \"caption\": \"Table 1. Selected DADL definitions in the public registry.\", \"page\": 19, \"index\": 2, \"width\": 1052, \"height\": 269}, {\"url\": \"assets/figures/arxiv/2605.05247v1/fig-003.webp\", \"caption\": \"Table 2. Comparison with adjacent approaches.\", \"page\": 20, \"index\": 3, \"width\": 1052, \"height\": 427}]"
motivation: 现有的MCP协议在企业规模下存在API集成部署繁琐以及工具目录过大导致上下文窗口消耗线性增长的问题。
method: 提出一种声明式的YAML格式语言DADL，通过统一的运行时解释执行API描述，并采用固定大小的“代码模式”接口进行工具交互。
result: 在包含1833个工具定义的目录上，将工具广告的上下文成本从约14.2万个token降低至约1000个，实现了142倍的缩减。
conclusion: DADL通过声明式描述和统一运行时，有效解决了企业级Agent系统的工具扩展性与成本挑战，实现了高效、安全的工具库管理。
---

## 摘要
模型上下文协议（MCP）是大语言模型（LLM）智能体与外部工具之间的标准接口。然而，在组织规模的应用中，它暴露了两个结构性问题。首先，每个 API 集成都作为一个专用的服务器进程交付，具有独立的部署、依赖树和凭据处理；近期的实证研究表明，绝大多数此类服务器只是 REST API 的薄封装。其次，按工具注册的模式导致上下文窗口消耗随目录规模线性增长，迫使实际部署仅能暴露组织实际使用的 API 中的一小部分。我们提出了 DADL（Dunkel API 描述语言），这是一种 YAML 格式，在单个声明式文件中描述 REST API 的端点、身份验证、分页、响应整形和访问分类。DADL 文件在运行时由执行层解释；无需部署针对每个 API 的服务器进程，也不生成集成代码，尽管运行时本身是一个服务器。由于所有工具共享该运行时，凭据和授权得以集中管理，且目录通过一个与规模无关的固定大小“代码模式”（Code Mode）接口传递给 LLM。其结果是一个企业级工具库：一个版本化、可审计的 API 集成集合，任何团队都可以通过统一的身份验证和授权边界进行扩展、共享和使用。DADL v0.1 规范以 CC BY-SA 4.0 协议发布，公共注册库包含跨 20 个服务的 1,833 个工具定义。在该目录上，代码模式将工具通告的上下文成本从约 142,000 个 token 降低到约 1,000 个，实现了 142 倍的缩减；搜索和执行调用的单次调用成本是额外的，且取决于具体任务。

## Abstract
The Model Context Protocol (MCP) is the standard interface between large language model (LLM) agents and external tools. At organizational scale, however, it exposes two structural problems. First, every API integration is shipped as a dedicated server process with its own deployment, dependency tree, and credential handling; recent empirical work shows the overwhelming majority of these servers are thin wrappers around REST APIs. Second, the per-tool registration model causes context window consumption to grow linearly with catalog size, forcing real deployments to expose only a small fraction of the APIs an organization actually uses.   We present DADL (Dunkel API Description Language), a YAML format describing a REST API's endpoints, authentication, pagination, response shaping, and access classification in a single declarative file. A DADL file is interpreted by an execution layer at runtime; no per-API server process is deployed and no integration code is generated, though the runtime is itself a server. Because all tools share that runtime, credentials and authorization are managed centrally, and the catalog reaches the LLM through a fixed-size Code Mode interface independent of size. The result is an Enterprise Tool Library: a versioned, auditable collection of API integrations any team can extend, share, and consume through one authentication and authorization boundary.   The DADL v0.1 specification is released under CC BY-SA 4.0, and a public registry contains 1,833 tool definitions across 20 services. On this catalog, Code Mode reduces the context cost of tool advertisement from approximately 142,000 tokens to approximately 1,000, a 142x reduction; the per-call cost of search and execute invocations is additional and depends on the task.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **DADL**（Dunkel API Description Language）的声明式描述语言，旨在解决大语言模型（LLM）智能体在企业级应用中面临的工具集成与扩展性难题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：
    1.  **服务器激增（Server Proliferation）**：现有的模型上下文协议（MCP）通常要求每个 API 集成都作为一个独立的服务器运行，导致部署、依赖管理和凭据维护成本极高。
    2.  **上下文窗口消耗（Context Window Consumption）**：MCP 的标准做法是向 LLM 注册所有工具的完整 Schema。随着工具数量增加，Token 消耗呈线性增长，导致 LLM 无法在有限的上下文窗口内处理大规模工具库。
*   **整体含义**：论文提出通过一种声明式的 YAML 格式（DADL）和统一的运行时（Runtime）来替代繁琐的单 API 服务器模式，从而构建一个版本化、可审计且高效的“企业工具库”。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将 API 集成从“编写代码”转变为“编写配置”。通过一个共享的运行时解释 DADL 文件，实现 API 调用、身份验证和数据处理。
*   **关键技术细节**：
    *   **DADL 规范**：使用 YAML 定义 REST API。涵盖 5 种认证方案、4 种分页策略、基于 `jq` 的响应转换（减少 Token 占用）、访问分类（read/write/admin/dangerous）以及“提示（Hints）”机制（嵌入机构知识）。
    *   **代码模式（Code Mode）**：这是解决上下文问题的核心。不再向 LLM 注册成百上千个工具，而是仅暴露 `search`（搜索工具）和 `execute`（执行代码）两个元工具。LLM 通过编写简单的 JavaScript 片段来调用 API，运行时在沙箱中执行。
    *   **复合工具（Composite Tools）**：允许在 DADL 中定义简单的 JS 函数，用于跨端点的数据连接（Joins）或复杂工作流。
    *   **声明式闭环（Declarative Closure）**：确保通过静态检查即可确定 API 可触达的所有 HTTP 端点，增强了安全性。

### 3. 实验设计
*   **数据集/场景**：作者建立了一个公共注册库（Public Registry），包含 **20 个服务**（如 GitHub, Stripe, Cloudflare, GitLab 等）的 **1,833 个工具定义**。
*   **Benchmark（基准）**：
    *   对比了**传统 MCP 模式**与 **DADL 代码模式**在工具通告（Tool Advertisement）时的 Token 消耗。
    *   对比了 DADL 与 OpenAPI、Postman Collections 等现有格式的差异。
*   **对比方法**：主要针对 MCP 官方服务器模式、AutoMCP（自动生成代码模式）以及 MCP Bridge 等方案。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**使用了特定的 GPU 算力或训练时长。
*   **原因**：该研究侧重于系统架构、协议设计和描述语言的定义，而非模型训练。实验部分主要涉及 Token 计数统计和运行时逻辑验证，不涉及大规模深度学习训练。

### 5. 实验数量与充分性
*   **实验规模**：基于 1,833 个真实世界的 API 工具定义进行测试，规模较大且具有代表性。
*   **充分性**：实验重点展示了 Token 缩减比例（142倍），这在量化分析上非常直观。虽然缺乏 LLM 在不同任务下的端到端成功率对比实验，但作为一种基础设施方案的论证，其数据支撑是充分且客观的。

### 6. 主要结论与发现
*   **Token 大幅缩减**：在 1,833 个工具的规模下，上下文成本从 14.2 万 Token 降至约 1000 Token，缩减了 **142 倍**。
*   **库组合效应（Library Composition Effect）**：通过统一的运行时和代码模式，LLM 能够自发地构建跨源查询（例如结合监控告警和云服务元数据进行排障），这种能力是零散服务器模式难以实现的。
*   **运维效率提升**：将集成工作量从“小时级”缩短到“分钟级”，且支持热加载和 GitOps 工作流。

### 7. 优点
*   **架构优雅**：通过声明式语言解决了 MCP 协议在企业级落地时的碎片化问题。
*   **安全性高**：凭据集中管理，访问控制（Authz）统一化，且具备静态可审计性。
*   **易于扩展**：利用 LLM 辅助生成 DADL 文件，降低了工具库的维护门槛。
*   **上下文友好**：固定大小的接口设计让 Agent 能够“知晓”无限量的工具。

### 8. 不足与局限
*   **协议限制**：目前仅支持 REST API，不支持 WebSocket、自定义请求签名（如 AWS SigV4）或需要复杂应用逻辑的集成。
*   **沙箱安全风险**：虽然使用了 JS 沙箱执行复合工具和代码模式，但其对抗恶意代码的鲁棒性仍是一个开放性问题。
*   **Schema 漂移**：DADL 文件描述的是 API 的快照，若上游 API 变更，仍需人工或自动化工具更新 DADL 文件。
*   **调试难度**：代码模式将逻辑移至运行时沙箱，传统的 MCP 调试工具可能需要适配才能查看内部的原始 API 调用。

（完）
