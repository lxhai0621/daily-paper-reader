---
title: "Belief Memory: Agent Memory Under Partial Observability"
title_zh: 信念记忆：部分可观测性下的智能体记忆
authors: "Junfeng Liao, Qizhou Wang, Jianing Zhu, Bo Du, Rui Yan, Xiuying Chen"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05583v1"
tags: ["query:agent"]
score: 9.0
evidence: LLM智能体利用外部记忆积累知识并保留候选结论
tldr: 针对LLM智能体在部分可观测环境下因确定性内存导致的自我强化错误，本文提出BeliefMem。该方法改变了单一结论的存储范式，转而保留多个候选结论及其概率，并利用Noisy-OR规则随新观测动态更新。BeliefMem在检索时呈现所有候选及其概率，使智能体能基于高置信度知识行动，同时保留修正能力。实验证明其在LoCoMo和ALFWorld基准上表现优异，为处理不确定性提供了新方向。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.05583v1/fig-001.webp\", \"caption\": \"Figure 1: Deterministic memory vs. BeliefMem with an API timeout example. After repeated API X timeouts, the deterministic paradigm stores “API X failed” and avoids it in later sessions, reinforcing the error. In contrast, BeliefMem keeps multiple hypotheses (e.g., failure vs. rate limiting) with probabilities, retries the API, and updates beliefs with new evidence, enabling correction over time.\", \"page\": 2, \"index\": 1, \"width\": 825, \"height\": 460}, {\"url\": \"assets/figures/arxiv/2605.05583v1/fig-002.webp\", \"caption\": \"Table 1: LoCoMo results across four categories under GPT-4o-mini and GPT-4o backbones. Each cell reports F1 / BLEU-1. Best and second numbers per column are in bold and underline, respectively.\", \"page\": 7, \"index\": 2, \"width\": 822, \"height\": 389}, {\"url\": \"assets/figures/arxiv/2605.05583v1/fig-003.webp\", \"caption\": \"Figure 6: The prompt used for attribute extraction. It restricts the model to output format, fact-based JSON objects grounded in the provided conversation.\", \"page\": 18, \"index\": 3, \"width\": 829, \"height\": 1268}, {\"url\": \"assets/figures/arxiv/2605.05583v1/fig-004.webp\", \"caption\": \"Table 2: ALFWorld results with the Qwen3-Next-80B-A3B-Instruct, on the in-distribution (Seen) split and the out-of-distribution (Unseen) split. SR(%): Success rate (↑); #Steps: Average steps on solved episodes (↓). ∆ indicates the difference relative to the best result in each column. BeliefMem*: 50% memory corpus used. BeliefMem: full memory corpus used.\", \"page\": 8, \"index\": 4, \"width\": 818, \"height\": 305}, {\"url\": \"assets/figures/arxiv/2605.05583v1/fig-005.webp\", \"caption\": \"Table 7: Sensitivity analysis of hyperparameters (Top-K and λ) on ALFWorld.\", \"page\": 17, \"index\": 5, \"width\": 773, \"height\": 754}, {\"url\": \"assets/figures/arxiv/2605.05583v1/fig-006.webp\", \"caption\": \"Figure 4: (a) BeliefMem maintains competitive performance across varying memory corpus sizes on ALFWorld, outperforming all baselines with only 50% of memory corpus. (b) BeliefMem’s candidate probabilities reliably converge to the true conclusion as evidence accumulates on LoCoMo, whereas naive frequency-based estimation fails to converge under noisy observations.\", \"page\": 9, \"index\": 6, \"width\": 814, \"height\": 262}, {\"url\": \"assets/figures/arxiv/2605.05583v1/fig-007.webp\", \"caption\": \"Table 5: Full results of ablation studies on ALFWorld benchmark (memory corpus size = 1500).\", \"page\": 16, \"index\": 7, \"width\": 682, \"height\": 185}]"
motivation: 现有LLM智能体内存通常将部分观测转化为确定性结论，导致忽略不确定性并产生自我强化的错误。
method: 提出BeliefMem框架，将观测存储为带概率的多个候选结论，并利用Noisy-OR规则随新观测动态更新概率。
result: 在LoCoMo和ALFWorld基准测试中，BeliefMem显著优于现有基线方法，展现了在有限数据下的卓越性能。
conclusion: 概率性内存机制能有效处理部分可观测环境中的不确定性，为智能体长时记忆和决策提供了新的研究范式。
---

## 摘要
在长上下文环境下运行的大语言模型（LLM）智能体依赖外部记忆来随时间积累知识。然而，现有方法通常将每次观测存储为单一的确定性结论（例如，从临时错误中推断出“API X 失败”），尽管这些观测本质上是局部的且具有潜在的歧义。通过确定单一结论并丢弃不确定性，这些方法引入了自我强化的错误：智能体根据存储的结论采取行动，从不重新审视替代方案，并随着时间的推移不断强化该结论。为了解决这一问题，我们提出了 BeliefMem，它将记忆范式从为每次观测确定单一结论转变为保留多个候选结论及其概率。具体而言，BeliefMem 将候选结论存储为独立的记忆条目，每个条目都携带一个概率，该概率随着新观测的到来通过 Noisy-OR 规则进行更新。在检索时，所有候选结论及其概率会一同呈现，使替代方案对智能体保持可见。由于记忆中的每个结论都保留了其概率，BeliefMem 保留了确定性范式所丢弃的不确定性，使智能体能够对证据充分的知识以高置信度采取行动，同时在有新证据出现时保留更新置信度的能力。在 LoCoMo 和 ALFWorld 基准测试上的实证评估表明，即使在数据有限的情况下，BeliefMem 也能获得最佳的平均性能，显著优于知名的基准模型。更广泛地说，这种概率记忆产生了实质性的收益，并为部分可观测环境下的智能体记忆探索了一个新方向。

## Abstract
LLM agents that operate over long context depend on external memory to accumulate knowledge over time. However, existing methods typically store each observation as a single deterministic conclusion (e.g., inferring "API~X failed" from temporary errors), even though such observations are inherently partial and potentially ambiguous. By committing to one conclusion and discarding uncertainty, these methods introduce self-reinforcing error: the agent acts on the stored conclusion, never revisits alternatives, and reinforces the conclusion over time. To address this issue, we propose BeliefMem, which shifts the memory paradigm from committing to a single conclusion per observation to retaining multiple candidate conclusions with their probabilities. Concretely, BeliefMem stores the candidate conclusions as separate memory entries, each carrying a probability that is updated via Noisy-OR rules as new observations arrive. At retrieval, all candidates surface together with their probabilities, keeping alternatives visible to the agent. Since each conclusion in memory retains its probability, BeliefMem preserves the uncertainty that the deterministic paradigm discards, enabling the agent to act with high confidence on well-evidenced knowledge while retaining the capacity to update its confidence when new evidence arrives. Empirical evaluations on LoCoMo and ALFWorld benchmarks show that, even with limited data, BeliefMem achieves the best average performance, remarkably outperforming well-known baselines. More broadly, such probabilistic memory produces substantial gains and explores a new direction for agent memory in partially observable environments.

---

## 论文详细总结（自动生成）

这是一份关于论文《Belief Memory: Agent Memory Under Partial Observability》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是大语言模型（LLM）智能体在长程任务中的**记忆机制**。
*   **核心问题**：现有的记忆方法遵循“确定性范式”，即从局部、模糊的观测中推断出一个单一的确定性结论并存储（例如：API一次超时就记录为“API已失效”）。
*   **自我强化错误（Self-reinforcing Error）**：由于丢弃了不确定性，智能体会基于错误的存储结论行动，从而无法尝试替代方案，导致错误随时间不断强化。
*   **背景**：智能体实际上运行在“部分可观测马尔可夫决策过程”（POMDP）中，观测只是隐藏状态的噪声信号。论文旨在将记忆从“点估计”转变为“信念状态（Belief State）”的近似。

### 2. 方法论：BeliefMem
BeliefMem 的核心思想是**保留多个候选结论及其概率**，并随新证据动态更新。

*   **核心思想**：不再存储单一事实，而是存储一个属性（Attribute）下的多个候选假设（Hypotheses）及其置信度。
*   **关键技术细节**：
    *   **属性级信念表示**：记忆条目表示为 `(属性, 候选结论, 概率)`。
    *   **记忆更新（Add & Merge）**：
        *   **Add**：发现新属性时，初始化候选结论及其概率。
        *   **Merge（Noisy-OR 规则）**：当新观测 $o_{t+1}$ 支持已有结论 $h$ 时，使用 Noisy-OR 公式更新概率：$p_{t+1} = \min(1 - (1 - p_t)(1 - \Delta), 0.99)$，其中 $\Delta$ 是新证据的强度。
        *   **冲突处理**：如果新观测支持矛盾的结论，则降低原有结论的概率（降至 0.25）。
    *   **信念感知检索（Belief-aware Retrieval）**：检索分数结合了**语义相似度**和**时间衰减（Temporal Decay）**。检索结果会向智能体展示该属性下的所有候选结论及其概率，使其在决策时能看到“不确定性”。

### 3. 实验设计
*   **数据集/场景**：
    1.  **LoCoMo**：长程对话记忆基准，包含多达35个会话，测试单跳、多跳、时间推理和开放域问答。
    2.  **ALFWorld**：文本驱动的具身智能体基准，包含6类家庭任务，分为 Seen（分布内）和 Unseen（分布外）环境。
*   **对比方法**：包括 MemGPT、Mem0、A-MEM、ReadAgent、MemoryBank、LangMem、MemoryOS 等知名基准。
*   **评估指标**：F1、BLEU-1（对话）；成功率（SR）、平均步数（Steps）（具身任务）。

### 4. 资源与算力
*   **硬件**：使用 **NVIDIA A800-80GB GPU**。
*   **软件/模型**：
    *   骨干模型：GPT-4o, GPT-4o-mini（用于 LoCoMo）；Qwen3-Next-80B（用于 ALFWorld）。
    *   嵌入模型：text-embedding-3-small。
    *   环境：Python 3.11, PyTorch 2.4.1。
*   **训练/运行时间**：文中未明确给出总训练时长，但提到 BeliefMem 在检索和生成时的 Token 消耗低于部分竞争基线（如 Mem0）。

### 5. 实验数量与充分性
*   **实验规模**：在两个截然不同的领域（对话与具身）进行了全面测试。
*   **消融实验**：详细测试了“无信念记忆”、“无信念检索”、“无 Add 操作”、“无 Merge 操作”等变体，验证了各组件的必要性。
*   **对抗性实验**：专门设计了“注入错误记忆”的场景，测试智能体修正错误的能力。
*   **充分性评价**：实验设计较为客观、公平。通过不同内存语料库大小（500到3000个样本）的对比，展示了方法在低数据量下的鲁棒性。

### 6. 主要结论与发现
*   **性能领先**：BeliefMem 在两个基准上均取得最佳平均性能。在 ALFWorld 的 Unseen 场景中，比第二名高出 12.4%。
*   **数据效率**：仅使用 50% 的记忆语料，BeliefMem 就能超越使用 100% 语料的大多数基线。
*   **错误修正**：在对抗实验中，BeliefMem 的错误修正率是确定性记忆的两倍，且修正速度更快（平均 4.75 步）。
*   **信念收敛**：随着证据积累，正确结论的概率会稳步上升并收敛，有效过滤了观测噪声。

### 7. 优点（亮点）
*   **范式创新**：首次在 LLM 记忆中引入了概率性的信念状态表示，解决了 POMDP 环境下的自我强化错误。
*   **动态更新机制**：利用 Noisy-OR 规则优雅地处理了证据聚合问题，比简单的频率统计更具鲁棒性。
*   **泛化能力强**：在分布外（Unseen）任务中表现尤为突出，说明保留不确定性有助于智能体在陌生环境中进行探索。

### 8. 不足与局限
*   **理论保证缺失**：虽然实验显示了收敛性，但对于基于 Noisy-OR 的近似信念更新，缺乏严格的数学收敛证明。
*   **证据强度依赖**：$\Delta$（证据强度）是由 LLM 直接提取的，如果 LLM 本身的置信度估计存在偏差，可能会影响概率更新的准确性。
*   **计算开销**：虽然 Token 消耗受控，但维护多个候选结论和执行 Merge 操作的计算复杂度高于简单的向量存储。
*   **超参数敏感性**：衰减率 $\lambda$ 和检索数量 $K$ 对性能有显著影响，不同场景下可能需要精细调优。

（完）
