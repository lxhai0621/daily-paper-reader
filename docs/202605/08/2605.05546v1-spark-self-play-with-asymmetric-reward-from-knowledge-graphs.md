---
title: "SPARK: Self-Play with Asymmetric Reward from Knowledge Graphs"
title_zh: SPARK：基于知识图谱非对称奖励的自我博弈
authors: "Hyobin Park, Taeseop Kim, Dong-Geol Choi"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05546v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 从多文档科学文献中自动构建知识图谱
tldr: 本研究针对科学文献中关系推理难以自动生成和验证的问题，提出了SPARK框架。该框架通过从多模态科学文献中构建统一知识图谱（KG），利用KG路径生成推理问题，并以结构化事实作为可验证奖励。通过让小型视觉语言模型在信息不对称下交替扮演提问者和回答者进行自我博弈，SPARK显著提升了模型在跨文档多跳推理任务中的表现，证明了结构化知识对复杂推理的支撑作用。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.05546v1/fig-001.webp\", \"caption\": \"Figure 1. Overview of SPARK. A three-stage pipeline automatically constructs a KG from scientific documents, and Cross-Document Federation connects concept nodes across papers. A single sVLM alternates between Proposer and Solver roles: the Proposer samples reasoning paths from the KG to generate relational questions with gold answers, while the Solver answers without KG access, under information asymmetry. The reward signal decomposes into three KG-verified components, Ranswer, Rpath, and Rconsistency, where Rpath and Rconsistency are terms unique to SPARK that cannot be defined without the KG.\", \"page\": 3, \"index\": 1, \"width\": 1008, \"height\": 567}, {\"url\": \"assets/figures/arxiv/2605.05546v1/fig-002.webp\", \"caption\": \"Figure 2. Overview of SPARK. Qualitative comparison of Qwen3-VL-4B vanilla and SPARK across three cases of increasing relational complexity. Both models receive identical inputs at inference time; differences arise solely from model weights.\", \"page\": 8, \"index\": 2, \"width\": 1010, \"height\": 1048}, {\"url\": \"assets/figures/arxiv/2605.05546v1/fig-003.webp\", \"caption\": \"Table 4. Edge-type sampling weights.\", \"page\": 12, \"index\": 3, \"width\": 1015, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2605.05546v1/fig-004.webp\", \"caption\": \"Table 1. Main performance comparison on public multimodal benchmarks.\", \"page\": 5, \"index\": 4, \"width\": 652, \"height\": 191}]"
motivation: 科学文献中元素间的复杂关系难以显式表达，导致自动生成推理问题和可靠奖励信号面临挑战。
method: 提出SPARK框架，利用从多文档构建的知识图谱路径生成问题，并通过信息不对称下的自我博弈机制训练模型。
result: 实验表明SPARK在多跳问答任务中优于传统语料库基线，且随着推理步数增加，性能优势更加显著。
conclusion: 基于知识图谱结构的引导比非结构化语料库更能有效提升模型在科学领域的多步关系推理能力。
---

## 摘要
自我博弈强化学习在数学和编程等具有形式化可验证结构的领域表现出强大的性能，在这些领域中，问题生成和奖励计算都可以基于显式规则。将这一范式扩展到科学文献更具挑战性：文档内部及文档之间多模态元素的关系在文本中很少被明确表达，这使得自动生成关系推理问题变得困难，并削弱了奖励信号的可靠性。我们提出了 SPARK（基于知识图谱非对称奖励的自我博弈），这是一个从多文档科学文献中自动构建统一知识图谱（KG）并将其作为自我博弈结构基础的框架。跨多模态节点的 KG 路径作为生成关系推理问题的来源，而存储在 KG 中的结构化事实为可验证的奖励计算提供了基础。单个小型视觉语言模型（sVLM）在针对固定 KG 的信息不对称下，交替扮演提议者（Proposer）和求解者（Solver）角色，我们认为这种设计在未来的工作中可以自然地扩展到在线自适应。我们在公开基准测试和自建的跨文档多跳问答数据集上评估了 SPARK。结果表明，SPARK 一致优于基于扁平语料库的自我博弈基线，且随着跳数（hop count）的增加，性能差距进一步扩大，这表明 KG 结构化接地（grounding）对关系多跳推理的贡献超出了非结构化语料库接地所能提供的范围。

## Abstract
Self-play reinforcement learning has shown strong performance in domains with formally verifiable structure, such as mathematics and coding, where both problem generation and reward computation can be grounded in explicit rules. Extending this paradigm to scientific literature is more challenging: the relationships among multi-modal elements within and across documents are rarely made explicit in text, which makes automatic generation of relational reasoning questions difficult and weakens the reliability of reward signals. We propose SPARK (Self-Play with Asymmetric Reward from Knowledge Graphs), a framework that automatically constructs a unified knowledge graph (KG) from multi-document scientific literature and uses it as the structural basis for self-play. KG paths over multimodal nodes serve as a source for generating relational reasoning questions, and structured facts stored in the KG provide a basis for verifiable reward computation. A single small vision-language model (sVLM) alternates between Proposer and Solver roles under information asymmetry against a fixed KG, a design that we believe can be naturally extended toward online adaptation in future work. We evaluate SPARK on public benchmarks and a self-constructed cross-document multi-hop QA dataset. Results show that SPARK consistently outperforms flat-corpus-based self-play baselines, and the performance gap widens as hop count increases, suggesting that KG-structure grounding contributes to relational multi-hop reasoning beyond what unstructured corpus grounding can provide.

---

## 论文详细总结（自动生成）

### SPARK: 基于知识图谱非对称奖励的自我博弈论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
自我博弈（Self-Play）强化学习在数学和编程等具有**形式化可验证结构**的领域取得了巨大成功（如 OpenAI o1、DeepSeek-R1），因为这些领域的问题生成和奖励计算可以基于明确规则。然而，将此范式扩展到**科学文献**面临两大瓶颈：
*   **关系隐晦性**：文档内及文档间多模态元素（图、表、公式、文本）的关系通常是隐含的，难以自动生成高质量的关系推理问题。
*   **奖励不可靠性**：在非结构化文本中，缺乏验证推理路径正确性的标准，导致奖励信号难以区分“看似合理的错误答案”与“结构正确的推理”。

**SPARK** 的核心意义在于：通过自动构建**知识图谱（KG）**，将非结构化的科学文献转化为结构化的推理环境，为自我博弈提供可验证的问题源和奖励依据。

#### 2. 核心方法论
SPARK 框架分为两个阶段：

*   **阶段一：自动 KG 构建（三阶段流水线）**
    1.  **结构图（Structural Graph）**：通过规则解析文档物理布局（如章节、段落、图表位置）。
    2.  **引用图（Reference Graph）**：通过正则匹配解析文中对图表的显式引用（如“如图3所示”）。
    3.  **语义关系图（Semantic Relation Graph）**：利用视觉语言模型（VLM）识别节点间的因果、支持、矛盾等深层语义关系。
    4.  **跨文档联邦（Cross-Document Federation）**：通过嵌入向量相似度连接不同论文中的相同概念，形成跨文档推理路径。

*   **阶段二：基于 KG 的自我博弈循环**
    *   **角色分配**：单个小型视觉语言模型（sVLM）交替扮演 **Proposer（提议者）** 和 **Solver（求解者）**。
    *   **信息不对称机制**：Proposer 访问 KG 路径并生成问题；Solver 在无法访问 KG 的情况下回答问题。
    *   **KG 验证奖励（$R_G$）**：
        *   $R_{answer}$：语义答案准确性。
        *   $R_{path}$：路径忠实度（验证 Proposer 生成的问题是否基于有效的 KG 结构）。
        *   $R_{consistency}$：事实一致性（根据 KG 节点中的结构化事实核查 Solver 的答案）。
    *   **模型更新与 KG 细化**：使用 GRPO 算法更新模型参数，并根据自我博弈结果动态调整 KG 的边置信度（剪枝或新增）。

#### 3. 实验设计
*   **数据集/场景**：
    *   **公开基准**：ScienceQA（科学问答）、DocVQA（文档问答）、ChartQA（图表问答）。
    *   **自建数据集**：Cross-Document Multi-hop QA（包含 50 篇 arXiv 论文，450 个涵盖 1-hop 到 3-hop 的跨文档多跳问题）。
*   **对比方法（Baselines）**：
    *   Qwen3-VL-4B-Instruct（原始模型）。
    *   SFT Only（仅进行监督微调）。
    *   SPIN (No KG)：基于扁平语料库的自我博弈基线，不含 KG 奖励。
    *   InternVL2-4B：其他主流多模态模型。

#### 4. 资源与算力
*   **模型规格**：以 Qwen3-VL-4B 为基础模型。
*   **训练细节**：使用 LoRA（rank=16）进行微调，采用 GRPO 算法。
*   **算力说明**：论文提到了超参数设置（如 30 个 epoch，每个 epoch 100 个问题，batch size 为 2，梯度累积为 4），但**未明确说明具体的 GPU 型号、数量及总训练时长**。

#### 5. 实验数量与充分性
*   **实验规模**：在 3 个公开数据集和 1 个自建多跳数据集上进行了测试。
*   **消融实验**：专门针对奖励组件（$R_a, R_p, R_c$）进行了消融分析，验证了 KG 结构化奖励对提升多跳推理能力的独立贡献。
*   **充分性评价**：实验设计较为充分，特别是通过对比 SPIN（无 KG 的自我博弈），客观地证明了 KG 结构在解决关系推理瓶颈中的关键作用。定性分析部分展示了 1-hop 到 3-hop 的具体案例，增强了结果的可信度。

#### 6. 主要结论与发现
*   **KG 的关键作用**：SPARK 在所有基准测试中均优于基于扁平语料库的基线。
*   **多跳推理优势**：随着推理跳数（Hop Count）增加，SPARK 与基线模型的性能差距显著扩大，证明了其在复杂关系合成方面的卓越能力。
*   **减少幻觉**：通过 $R_{consistency}$ 奖励，模型在生成答案时表现出更低的事实幻觉率。
*   **协同进化**：模型训练与 KG 细化形成了正向循环，提升了知识表示的质量。

#### 7. 优点
*   **结构化创新**：首次将 KG 路径作为自我博弈的结构化基础，解决了科学领域缺乏“编译器”或“判定器”的问题。
*   **信息不对称设计**：巧妙利用 Proposer 和 Solver 的信息差，强制模型内化 KG 中的关系推理能力。
*   **跨文档能力**：通过跨文档联邦技术，使模型能够处理超越单篇论文范围的复杂知识合成任务。

#### 8. 不足与局限
*   **KG 构建依赖**：虽然是自动构建，但阶段三仍依赖于 VLM 的初步判断，若初始 KG 噪声过大，可能会影响后续训练。
*   **算力细节缺失**：未披露具体的硬件资源消耗，难以评估该框架的训练成本和效率。
*   **领域局限性**：目前主要集中在科学文献，对于缺乏明确引用关系或结构化图表的通用领域（如文学、创意写作），其 KG 构建逻辑可能需要大幅调整。
*   **样本规模**：自建的跨文档数据集（450个问题）规模相对较小，未来需更大规模的验证。

（完）
