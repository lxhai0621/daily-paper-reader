---
title: "Knowledge Graphs, the Missing Link in Agentic AI-based Formal Verification"
title_zh: 知识图谱：基于智能体 AI 的形式化验证中缺失的环节
authors: "Vaisakh Naduvodi Viswambharan, Keerthan Kopparam Radhakrishna, Deepak Narayan Gadde, Aman Kumar"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06434v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 从结构化和非结构化数据构建以验证为中心的知识图谱
tldr: 本研究针对大语言模型在生成SystemVerilog断言（SVA）时存在的规格说明模糊及RTL关联性弱等问题，提出了一种基于知识图谱（KG）的多智能体形式验证框架。该方法通过从规格、RTL及工具反馈中提取结构化中间表示构建KG，并驱动语法修复、反例纠错和覆盖率增强三大闭环。实验表明，该方法能显著提升SVA的生成质量和设计关联性，在多个基准测试中达到了极高的形式覆盖率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.06434v1/fig-001.webp\", \"caption\": \"Fig. 1. End-to-end workflow of the proposed approach. NetworkX is used for KG construction, and PyVis provides interactive HTML visualization.\", \"page\": 2, \"index\": 1, \"width\": 1072, \"height\": 684}, {\"url\": \"assets/figures/arxiv/2605.06434v1/fig-002.webp\", \"caption\": \"TABLE I Intermediate Representation artifacts and their roles in the verification workflow\", \"page\": 3, \"index\": 2, \"width\": 1152, \"height\": 390}, {\"url\": \"assets/figures/arxiv/2605.06434v1/fig-003.webp\", \"caption\": \"TABLE II Evaluation of the proposed methodology\", \"page\": 4, \"index\": 3, \"width\": 1100, \"height\": 251}]"
motivation: 现有的LLM生成断言方法因缺乏对规格与RTL底层细节的深度关联，导致频繁出现语义不匹配和语法解析失败。
method: 构建整合设计层次、信号及工具反馈的验证中心知识图谱，并利用多智能体工作流进行迭代式断言生成与精炼。
result: "在七个基准设计中实现了78.5%至99.4%的形式覆盖率，并以较低的修复开销生成了高质量的可编译断言。"
conclusion: 知识图谱是连接自然语言规格与底层硬件设计的关键纽带，能有效增强Agentic AI在形式验证中的自动化能力。
---

## 摘要
大语言模型（LLMs）的最新进展使得从自然语言规范生成 SystemVerilog 断言（SVAs）的工作流成为可能，具有加速形式化验证（FV）的潜力。然而，高质量的断言合成仍然具有挑战性，因为规范往往模糊或不完整，且关键的微架构细节存在于寄存器传输级（RTL）中。许多现有方法将规范和 RTL 视为松散结构的文本，这削弱了规范到 RTL 的关联（grounding），并导致在形式化解析和阐述过程中出现语义不匹配和频繁的语法失败。本研究通过构建一个以验证为中心的知识图谱（KG）来解决这些局限性，该图谱由从规范、RTL 和形式化工具反馈（包括语法诊断、反例（CEXs）和覆盖率报告）中提取的结构化中间表示（IRs）构建而成。该知识图谱将需求、设计层次结构、信号、假设和属性链接起来，为生成提供可追溯的、基于设计的上下文。一个多智能体工作流查询并更新该知识图谱以生成 SVAs，并驱动三个细化循环：由工具诊断引导的语法修复、使用追踪链接的 CEX 引导纠错，以及覆盖率导向的属性增强。对七个基准设计的评估表明，基于知识图谱的上下文检索改善了规范到 RTL 的关联，并能持续产生具有低语法修复开销的可编译 SVAs。该方法实现了 78.5% 到 99.4% 的形式化覆盖率，尽管收敛性表现出设计依赖性，且复杂的时序和算术推理对于当前的 LLM 能力仍具挑战性。

## Abstract
Recent advances in Large Language Models (LLMs) have enabled workflows that generate SystemVerilog Assertions (SVAs) from natural-language specifications, with the potential to accelerate Formal Verification (FV). However, high-quality assertion synthesis remains challenging because specifications are often ambiguous or incomplete and critical micro-architectural details reside in the Register Transfer Level (RTL). Many existing approaches treat the specification and RTL as loosely structured text, which weakens specification-to-RTL grounding and leads to semantic mismatches and frequent syntax failures during formal parsing and elaboration. This work addresses these limitations with a verification-centric Knowledge Graph (KG) constructed from structured Intermediate Representations (IRs) extracted from the specification, RTL, and formal-tool feedback, including syntax diagnostics, Counterexamples (CEXs), and coverage reports. The KG links requirements, design hierarchy, signals, assumptions, and properties to provide traceable, design-grounded context for generation. A multi-agent workflow queries and updates this KG to generate SVAs and to drive three refinement loops: syntax repair guided by tool diagnostics, CEX-guided correction using trace links, and coverage-directed property augmentation. Evaluation across seven benchmark designs indicates that KG-based context retrieval improves specification-to-RTL grounding and consistently produces compilable SVAs with low syntax-repair overhead. The approach achieves formal coverage ranging from 78.5% to 99.4%, though convergence exhibits design dependence with complex temporal and arithmetic reasoning remaining challenging for current LLM capabilities.

---

## 论文详细总结（自动生成）

### 论文总结：知识图谱在基于智能体 AI 的形式化验证中的应用

#### 1. 核心问题与整体含义
*   **研究动机**：形式化验证（FV）是确保硬件设计正确性的关键，但编写 SystemVerilog 断言（SVA）极度耗时且易出错。虽然大语言模型（LLM）能辅助生成断言，但面临两大挑战：一是自然语言规格说明（Spec）往往模糊或不完整；二是 LLM 难以将规格说明与复杂的 RTL（寄存器传输级）底层细节（如信号名、层次结构）准确关联（即“Grounding”问题）。
*   **核心问题**：现有的方法将规格和代码视为松散文本，缺乏持久且可查询的链接，导致生成的断言频繁出现语法错误、语义不匹配以及验证覆盖率不足。

#### 2. 方法论
该论文提出了一种以验证为中心的**知识图谱（KG）驱动的多智能体工作流**，核心思想是将验证过程中的所有要素结构化并互联。
*   **结构化中间表示（IR）**：将规格说明、RTL 元数据、生成的属性、形式化工具反馈（语法错误、反例 CEX、覆盖率报告）转化为 12 种类型的 JSON IR 节点（如 `requirements.json`, `design_model.json`, `formal_results.json`）。
*   **知识图谱构建**：利用 NetworkX 构建运行时 KG，将需求、设计元素、属性和验证结果通过边（如 `derives_from`, `validates`, `proves`）连接，实现双向可追溯性。
*   **多智能体协作**：
    *   **生成阶段**：`spec_analyst` 分解需求，`sva_author` 编写代码，`sva_reviewer` 进行审核。
    *   **闭环精炼阶段**：
        1.  **语法修复循环**：利用工具诊断信息自动修复未定义标识符或类型不匹配。
        2.  **反例（CEX）纠错循环**：解析 VCD 波形，定位属性过强或 RTL 漏洞，调整约束。
        3.  **覆盖率增强循环**：分析未覆盖的代码区域，生成针对性的覆盖属性（Cover properties）。

#### 3. 实验设计
*   **基准测试（Benchmarks）**：使用了 7 个开源硬件设计，包括 FIFO、Lemming（逻辑谜题）、ALU（算术逻辑单元）、CICD（滤波器）、BST（二叉搜索树）、TTC 计数器和 CSR APB 接口。
*   **工具链**：使用 **GPT-5.2**（注：文中提及型号，可能为特定版本或 API 接口）作为底层 LLM，使用 **Cadence JasperGold** 作为形式化验证工具。
*   **对比与评估指标**：评估了属性生成数量、通过/失败比例、语法修复尝试次数、RTL 覆盖率以及最终的形式化覆盖率（Formal Coverage）。

#### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 型号、数量或训练时长。由于该方法基于 Agentic AI 工作流（调用预训练模型 API），其算力消耗主要集中在 LLM 的推理调用和 JasperGold 形式化工具的运行上。
*   **软件环境**：使用了 NetworkX 进行图构建，PyVis 进行可视化。

#### 5. 实验数量与充分性
*   **实验规模**：针对 7 个不同复杂度的设计进行了完整的工作流测试，涵盖了从简单的 FIFO 到复杂的 BST 数据结构。
*   **充分性评价**：实验设计较为全面，不仅关注了断言的生成，还重点测试了语法修复、CEX 纠错和覆盖率提升三个闭环的效果。通过对比纠错前后的覆盖率变化，验证了 KG 在闭环反馈中的有效性。但论文缺乏与“纯 RAG”或“无 KG 辅助的 Agent”的直接消融对比数据。

#### 6. 主要结论与发现
*   **关联性提升**：基于 KG 的上下文检索显著改善了规格到 RTL 的关联，生成的 SVA 具有极高的可编译性。
*   **覆盖率表现**：最终形式化覆盖率在 78.5% 到 99.4% 之间，证明了自动化流程的强大潜力。
*   **瓶颈所在**：复杂的时序推理（如 FSM 状态转换）和全局数据结构不变式（如 BST 的归纳推理）对当前 LLM 仍具挑战，CEX 纠错在这些场景下难以收敛。

#### 7. 优点
*   **全生命周期追溯**：通过 KG 将需求、代码和工具反馈打通，解决了验证过程中的信息孤岛问题。
*   **闭环自动化**：不仅能生成代码，还能根据工具反馈（语法错误、反例、覆盖率缺口）进行自我迭代，减少了人工干预。
*   **确定性增强**：利用结构化 IR 提取 RTL 元数据，减少了 LLM 在信号名和层次结构上的幻觉。

#### 8. 不足与局限
*   **语义推理限制**：对于需要深度时序逻辑或复杂算术推理的场景，自动纠错的成功率较低。
*   **设计依赖性**：实验显示不同设计的收敛性差异较大，某些复杂设计（如 BST）仍需人工介入。
*   **成本与延迟**：多智能体多次迭代和 KG 的频繁更新可能带来较高的 API 调用成本和处理延迟。

（完）
