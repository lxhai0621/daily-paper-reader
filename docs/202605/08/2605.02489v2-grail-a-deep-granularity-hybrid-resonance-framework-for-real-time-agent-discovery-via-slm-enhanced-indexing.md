---
title: "GRAIL: A Deep-Granularity Hybrid Resonance Framework for Real-Time Agent Discovery via SLM-Enhanced Indexing"
title_zh: GRAIL：一种通过 SLM 增强索引实现实时智能体发现的深粒度混合共振框架
authors: Jinliang Xu
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02489v2"
tags: ["query:ma-kf"]
score: 9.0
evidence: 多智能体协作中的实时智能体发现
tldr: 随着大模型智能体生态的快速扩张，高效准确的智能体发现成为多智能体协作的瓶颈。本文提出GRAIL框架，旨在解决现有方法在延迟与精度之间的矛盾。GRAIL通过引入微调的小语言模型（SLM）进行快速意图解析，结合伪文档扩展增强语义密度，并利用MaxSim共振机制实现细粒度匹配。在AgentTaxo-9K数据集上的实验表明，GRAIL在保持高精度的同时，将发现延迟降低至400毫秒以内，比传统LLM方法快79倍以上，为实时“智能体互联网”提供了可扩展的工业级解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.02489v2/fig-001.webp\", \"caption\": \"Fig. 4: Multi-dimensional distribution analysis of tags (Top: Tag word cloud; Bottom: Log-transformed frequency trend)\", \"page\": 6, \"index\": 1, \"width\": 464, \"height\": 628}, {\"url\": \"assets/figures/arxiv/2605.02489v2/fig-002.webp\", \"caption\": \"Fig. 2: The Overall Architecture of the GRAIL Framework.\", \"page\": 3, \"index\": 2, \"width\": 525, \"height\": 406}, {\"url\": \"assets/figures/arxiv/2605.02489v2/fig-003.webp\", \"caption\": \"Fig. 3: Workflow of the GRAIL Online Discovery Process.\", \"page\": 5, \"index\": 3, \"width\": 516, \"height\": 408}]"
motivation: 现有的智能体发现方法在重型LLM解析带来的高延迟与传统向量检索导致的低精度之间面临两难选择。
method: 该框架结合了用于快速标签预测的微调小模型、增强语义密度的伪文档扩展以及基于使用示例的MaxSim细粒度匹配机制。
result: "实验证明GRAIL将端到端发现延迟降低了79倍以上（小于400ms），且在Recall@10指标上显著优于传统向量检索。"
conclusion: GRAIL为大规模、实时的“智能体互联网”提供了一种兼顾高性能与高精度的工业级搜索与发现方案。
---

## 摘要
随着基于大语言模型（LLM）的智能体生态系统迅速扩张，高效且准确的智能体发现（Agent Discovery）已成为大规模多智能体协作的关键瓶颈。现有方法通常面临两难境地：要么依赖重量级 LLM 进行意图解析，导致难以接受的延迟（通常超过 30 秒）；要么使用单一的向量检索，以牺牲语义精度为代价换取速度。为了弥补这一差距，我们提出了 GRAIL（基于粒度共振的智能体/AI 链接），这是一个在不牺牲准确性的情况下实现低于 400 毫秒发现延迟的新型框架。GRAIL 引入了三项关键创新：（1）SLM 增强预测，使用专门微调的小语言模型（SLM）取代通用 LLM 解析器，实现毫秒级的能力标签预测；（2）伪文档扩展（Pseudo-Document Expansion），通过合成查询增强智能体描述，以提升语义密度，实现鲁棒的稠密检索；（3）MaxSim 共振，一种细粒度匹配机制，通过计算用户查询与离散智能体使用示例之间的最大相似度，有效缓解语义稀释。在包含 9,240 个智能体的新型大规模数据集 AgentTaxo-9K 上进行的验证表明，与基于 LLM 解析的基准相比，GRAIL 将端到端发现延迟降低了 79 倍以上，同时在 Recall@10 指标上显著优于传统的向量搜索。该框架为实时的“智能体互联网”（Internet of Agents）提供了一种可扩展的工业级解决方案。

## Abstract
As the ecosystem of Large Language Model (LLM)-based agents expands rapidly, efficient and accurate Agent Discovery becomes a critical bottleneck for large-scale multi-agent collaboration. Existing approaches typically face a dichotomy: either relying on heavy-weight LLMs for intent parsing, leading to prohibitive latency (often exceeding 30 seconds), or using monolithic vector retrieval that sacrifices semantic precision for speed. To bridge this gap, we propose \textbf{GRAIL} (Granular Resonance-based Agent/AI Link), a novel framework achieving sub-400ms discovery latency without compromising accuracy. GRAIL introduces three key innovations: (1) \textbf{SLM-Enhanced Prediction}, replacing the generalized LLM parser with a specialized, fine-tuned Small Language Model (SLM) for millisecond-level capability tag prediction; (2) \textbf{Pseudo-Document Expansion}, augmenting agent descriptions with synthetic queries to enhance semantic density for robust dense retrieval; and (3) \textbf{MaxSim Resonance}, a fine-grained matching mechanism computing maximum similarity between user queries and discrete agent usage examples, effectively mitigating semantic dilution. Validated on \textbf{AgentTaxo-9K}, our new large-scale dataset of 9,240 agents, GRAIL reduces end-to-end discovery latency by over \textbf{79$\times$} compared to LLM-parsing baselines, while significantly outperforming traditional vector search in Recall@10. This framework offers a scalable, industrial-grade solution for the real-time ``Internet of Agents."

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **GRAIL**（Granular Resonance-based Agent/AI Link）的框架，旨在解决大规模多智能体系统中“智能体发现”的效率与精度矛盾。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在“智能体互联网”（Internet of Agents）背景下，如何从成千上万个专业智能体中快速、准确地匹配用户意图。
*   **研究背景**：
    *   **LLM解析法（高延迟）**：依赖重型大模型（如 Gemini 3）解析意图，延迟通常超过 30 秒，无法满足实时协作需求。
    *   **单一向量检索（低精度）**：将智能体描述压缩为单个向量，导致“语义稀释”（Semantic Dilution），难以捕捉细粒度的功能差异。
*   **研究目标**：在保持亚秒级（<400ms）响应的同时，显著提升检索的召回率和准确性。

### 2. 提出的方法论
GRAIL 框架由**离线索引**和**在线发现**两个阶段组成，核心思想是“深粒度混合共振”：
*   **SLM 增强预测（SLM-Enhanced Prediction）**：使用微调后的 3B 参数小语言模型（SLM）取代通用 LLM。它能在毫秒内预测用户查询对应的能力标签（Tags），实现快速的候选初步过滤。
*   **伪文档扩展（Pseudo-Document Expansion, PDE）**：利用 LLM 为每个智能体生成合成查询（Synthetic Queries），并将其与原始描述拼接。这增加了语义密度，解决了用户口语化表达与技术文档之间的“词汇不匹配”问题。
*   **MaxSim 共振机制（MaxSim Resonance）**：
    *   **核心算法**：不再对智能体的使用示例（Examples）进行平均池化，而是保留每个示例的独立嵌入矩阵。
    *   **共振计算**：计算用户查询向量与智能体示例矩阵中每个向量的余弦相似度，并取最大值（MaxSim）。
    *   **理论支撑**：论文通过定理证明，MaxSim 能有效防止多功能智能体在向量空间中被“稀释”，确保特定功能的强信号不被均值化。

### 3. 实验设计
*   **数据集**：发布了 **AgentTaxo-9K**，包含 9,240 个智能体和 27,720 条查询。
    *   **构建原则**：基于 OASF 标准，采用 MECE（相互独立、完全穷尽）原则构建了 154 个行业和 924 个子领域的层级结构。
    *   **查询类型**：涵盖基于能力、基于场景和关键词风格的三类意图。
*   **对比方法（Baselines）**：
    1.  **MDR（单体稠密检索）**：标准的双编码器向量搜索。
    2.  **SLM-Sort**：仅依赖 SLM 预测标签进行稀疏检索。
    3.  **LLM-Router**：使用重型 LLM 直接进行路由选择（作为精度上限参考）。

### 4. 资源与算力
*   **硬件环境**：单台 Linux 服务器，配备 **1 块 NVIDIA T4 GPU**（15GB 显存）、12.7GB 内存。
*   **模型配置**：
    *   **SLM**：使用 Qwen2.5-3B-Instruct，通过 LoRA 技术在 2.7 万条数据上微调。
    *   **编码器**：使用 BGE-Small-EN-v1.5（33M 参数），生成 384 维向量。
*   **训练时长**：文中未明确给出具体的训练总时长，但强调了推理阶段的低延迟。

### 5. 实验数量与充分性
*   **实验规模**：在全量 9,240 个智能体的池子中进行检索（Full-Pool Retrieval），而非简化的小池子，模拟了真实工业环境。
*   **消融实验**：设计了“无 MaxSim”和“无 SLM”两组消融实验，验证了 MaxSim 对精度的决定性作用（Recall@10 提升约 14.7%）。
*   **客观性**：实验涵盖了 Recall@1、Recall@10、MRR 和延迟等多个维度，对比了不同技术路线的优劣，评估较为全面。

### 6. 主要结论与发现
*   **性能卓越**：GRAIL 实现了 **71.54% 的 Recall@1** 和 **91.45% 的 Recall@10**。
*   **极低延迟**：端到端延迟仅为 **394.48ms**，比 LLM 路由方法快了 **79 倍以上**。
*   **语义稀释验证**：实验证实了传统向量检索在处理“多功能智能体”时表现不佳，而 MaxSim 机制能有效保留细粒度信号。
*   **SLM 的价值**：SLM 标签预测虽然对召回率提升有限（约 1%），但它是实现毫秒级意图解析和符号对齐的关键。

### 7. 优点
*   **架构平衡**：巧妙结合了 SLM 的速度、向量检索的泛化性和 MaxSim 的精度。
*   **理论与实践结合**：不仅提出了框架，还给出了语义稀释的数学证明，并发布了高质量的大规模基准数据集。
*   **工业实用性**：在廉价硬件（T4 GPU）上即可实现高性能，具有极强的落地潜力。

### 8. 不足与局限
*   **硬件依赖**：虽然 T4 已经很普及，但 MaxSim 计算涉及矩阵运算，当候选集（C）规模极大时，计算开销可能会增加。
*   **数据集单一**：主要在自建的 AgentTaxo-9K 上验证，缺乏在其他异构智能体协议（如 MCP 或 A2A 真实流量）下的跨域验证。
*   **SLM 维护成本**：当智能体类别或标签体系发生剧烈变动时，SLM 可能需要重新微调以保持预测准确性。

（完）
