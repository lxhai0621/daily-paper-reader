---
title: "Securing the Agent: Vendor-Neutral, Multitenant Enterprise Retrieval and Tool Use"
title_zh: 保护智能体：厂商中立的多租户企业级检索与工具使用
authors: "Francisco Javier Arceo, Varsha Prasad Narsing"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.05287v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 在企业环境中保障RAG架构和智能体工具使用的安全性
tldr: 本研究针对企业级RAG和代理AI系统在多租户环境下的安全挑战，指出传统检索系统因混淆相关性与授权而导致数据泄露。为此，提出一种分层隔离架构，通过服务器端编排、策略感知摄取和检索门控，确保多租户数据的严格隔离。该方案在开源框架OGX中实现，不仅解决了跨租户泄露和工具调用安全问题，还保持了厂商中立性，为企业级AI部署提供了安全可靠的参考架构。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.05287v1/fig-001.webp\", \"caption\": \"Figure 5: Responses API surface area and its relation to other APIs, providers, and tools. The Responses API serves as the central orchestration endpoint, coordinating inference, tool execution, state management, and context compaction.\", \"page\": 11, \"index\": 1, \"width\": 898, \"height\": 639}, {\"url\": \"assets/figures/arxiv/2605.05287v1/fig-002.webp\", \"caption\": \"Figure 4: Empirical evaluation results across five dimensions. (a) Security: ABAC gating eliminates cross-tenant leakage (CTLR and AVR drop to 0%) regardless of orchestration mode. (b) Retrieval quality: chunk-level gating improves Precision@5 by 2.2× by filtering cross-tenant noise. (c) Throughput: gating does not degrade QPS; client-side achieves ∼2× server-side at high concurrency. (d) Predicate pushdown: post-retrieval filtering overhead is small but recall degrades at large corpus sizes; pushdown-capable backends maintain Recall@5=1.0. (e) Adversarial resilience: all 90 prompt injection probes blocked under gated configurations.\", \"page\": 7, \"index\": 2, \"width\": 1026, \"height\": 1114}, {\"url\": \"assets/figures/arxiv/2605.05287v1/fig-003.webp\", \"caption\": \"Figure 1: Layered isolation architecture with server-side orchestration. All three layers execute within the server trust boundary; the client controls what to ask but not how retrieval and tool execution are performed.\", \"page\": 4, \"index\": 3, \"width\": 497, \"height\": 428}, {\"url\": \"assets/figures/arxiv/2605.05287v1/fig-004.webp\", \"caption\": \"Table 1: Mapping from security challenges to architectural enforcement points.\", \"page\": 4, \"index\": 4, \"width\": 508, \"height\": 281}, {\"url\": \"assets/figures/arxiv/2605.05287v1/fig-005.webp\", \"caption\": \"Figure 3: OGX architecture for multitenant enterprise agentic AI on shared Kubernetes infrastructure.\", \"page\": 5, \"index\": 5, \"width\": 504, \"height\": 442}]"
motivation: 现有RAG系统在多租户企业环境中因仅按相关性而非权限检索文档，导致严重的跨租户数据泄露风险。
method: 提出一种分层隔离架构，结合策略感知摄取、检索时门控和服务器端代理编排，将安全关键操作集中化。
result: 通过开源框架OGX的实验验证，该架构在消除跨租户泄露的同时，仅引入了极低的性能开销。
conclusion: 服务器端编排与基于属性的访问控制相结合，是实现安全、多租户企业级AI代理的有效途径。
---

## 摘要
检索增强生成（RAG）和智能体 AI 系统在企业级 AI 部署中日益普及。然而，真实的企业环境引入了学术研究和面向消费者的 API 中基本不存在的挑战：具有异构数据的多租户、严格的访问控制要求、合规性以及对共享基础设施有要求的成本压力。在这些场景下，现有的 RAG 架构存在一个根本性问题：检索系统根据相关性（无论是通过语义相似度、关键词匹配还是混合方法）而非授权对文档进行排序，因此一个租户的查询可能会因为评分最高而泄露另一个租户的机密数据。我们对这一差距进行了形式化定义，并分析了当智能体系统将相关性与授权混为一谈时出现的其他缺陷，包括工具介导的泄露、跨轮次的上下文累积以及客户端编排绕过。为了应对这些挑战，我们引入了一种分层隔离架构，结合了策略感知的数据摄取、检索时门控和共享推理，并通过服务器端智能体编排来强制执行。该方法将安全关键操作（工具执行授权、状态隔离和策略执行）集中在服务器上，为多租户隔离创建了自然的执行点，同时允许客户端框架保留对智能体组合和延迟敏感操作的控制。我们通过在 OGX 中的开源实现验证了所提出的架构。OGX 是一个厂商中立的框架，实现了具有服务器端多轮编排功能的 OpenAI 兼容开源 Responses API。我们对其进行了实证评估，结果表明基于属性的访问控制（ABAC）门控消除了跨租户泄露，同时引入的开销微乎其微。

## Abstract
Retrieval-Augmented Generation (RAG) and agentic AI systems are increasingly prevalent in enterprise AI deployments. However, real enterprise environments introduce challenges largely absent from academic treatments and consumer-facing APIs: multiple tenants with heterogeneous data, strict access-control requirements, regulatory compliance, and cost pressures that demand shared infrastructure.   A fundamental problem underlies existing RAG architectures in these settings: retrieval systems rank documents by relevance--whether through semantic similarity, keyword matching, or hybrid approaches--not by authorization, so a query from one tenant can surface another tenant's confidential data simply because it scores highest. We formalize this gap and analyze additional shortcomings--including tool-mediated disclosure, context accumulation across turns, and client-side orchestration bypass--that arise when agentic systems conflate relevance with authorization. To address these challenges, we introduce a layered isolation architecture combining policy-aware ingestion, retrieval-time gating, and shared inference, enforced through server-side agentic orchestration. This approach centralizes security-critical operations--tool execution authorization, state isolation, and policy enforcement--on the server, creating natural enforcement points for multitenant isolation while allowing client-side frameworks to retain control over agent composition and latency-sensitive operations.   We validate the proposed architecture through an open-source implementation in OGX, a vendor-neutral framework that implements an OpenAI-compatible, open-source Responses API with server-side multi-turn orchestration. We evaluate it empirically and show that ABAC gating eliminates cross-tenant leakage while introducing negligible overhead.

---

## 论文详细总结（自动生成）

这篇论文针对企业级 AI 智能体（Agent）在多租户环境下的安全挑战，提出了一套完整的架构方案。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的检索增强生成（RAG）和智能体架构存在“**相关性与授权的鸿沟**”（Relevance-Authorization Gap）。传统的检索系统（如向量数据库）仅根据语义相似度或关键词匹配进行排序，而不考虑访问控制权限。
*   **研究动机**：在企业多租户场景中，不同部门或客户共享基础设施。如果仅按相关性检索，租户 A 的查询可能会因为评分最高而召回租户 B 的机密数据。此外，客户端编排（Client-side Orchestration）容易被绕过，导致工具调用和上下文累积过程中的数据泄露。

### 2. 方法论：分层隔离架构
论文提出了一种结合“数据路径隔离”与“控制路径编排”的架构，核心思想是将安全关键操作集中在服务器端的信任边界内。

*   **数据路径：三层隔离**
    1.  **策略感知摄取（Layer 1）**：在数据进入系统时，强制附加租户元数据（如所有权、角色属性），确保每个数据块（Chunk）都带有不可篡改的身份标签。
    2.  **检索门控（Layer 2）**：实现基于属性的访问控制（ABAC）。检索公式定义为：只有当“相关性 > 阈值”且“策略(用户, 文档) = 允许”时，文档才会被召回。支持“谓词下推”（Predicate Pushdown）以在数据库底层过滤。
    3.  **共享推理（Layer 3）**：在确保上下文（Context）已过滤的前提下，多个租户共享同一个大模型推理实例，以降低成本。
*   **控制路径：服务器端编排**
    *   将“推理 → 工具调用 → 推理”的循环从客户端移至服务器端。这样可以防止恶意客户端跳过权限检查或篡改工具输入，确保所有状态转换都在监控和策略执行之下。

### 3. 实验设计
*   **场景与数据集**：构建了三个模拟租户（金融、工程、法律），包含 300 个文档（每租户 100 个）。
*   **Benchmark 与对比配置**：采用 2×2 配置矩阵进行对比：
    *   **Config A**：客户端编排 + 无门控（基准）。
    *   **Config B**：客户端编排 + ABAC 门控。
    *   **Config C**：服务器端编排 + 无门控。
    *   **Config D**：服务器端编排 + ABAC 门控（本文方案）。
*   **评估维度**：跨租户泄露率（CTLR）、授权违规率（AVR）、系统延迟、吞吐量以及针对提示词注入（Prompt Injection）的防御能力。

### 4. 资源与算力
*   **硬件**：使用了 **NVIDIA T4 GPU**。
*   **软件栈**：使用 **vLLM** 作为推理引擎，部署了 Llama-3.2-1B-Instruct 模型。
*   **外部 API**：部分实验调用了 OpenAI 的 `gpt-4o-mini` 和 `text-embedding-3-small`。
*   **存储**：使用 `sqlite-vec` 作为本地向量存储提供程序。
*   **训练时长**：文中未提及模型训练时长，因为研究重点在于推理阶段的系统架构和策略执行。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 6 组主要实验，包括 300 次授权查询、300 次跨租户探测和 90 次针对四种攻击类型的提示词注入探测。
*   **充分性评价**：实验设计较为充分且客观。它不仅验证了安全性（泄露率降至 0%），还深入探讨了性能开销（延迟增加）和检索质量（Precision/Recall）的权衡。通过对比不同并发数下的吞吐量，证明了该架构在生产环境中的可行性。

### 6. 主要结论与发现
*   **安全性**：引入 ABAC 门控后，跨租户数据泄露率从近 100% 降至 **0%**。
*   **性能开销极低**：检索门控仅增加了约 **19ms** 的延迟，相对于数秒的大模型推理时间几乎可以忽略不计。
*   **防御鲁棒性**：由于防御发生在检索层而非模型层，该架构能 100% 拦截导致数据泄露的提示词注入攻击。
*   **检索质量提升**：通过过滤掉不属于该租户的“噪声”文档，检索精度（Precision@5）提升了 **2.2 倍**。

### 7. 优点与亮点
*   **厂商中立性**：通过开源框架 **OGX** 实现，兼容 OpenAI API 规范，支持多种推理后端和向量数据库，避免了供应商锁定。
*   **架构严谨**：形式化定义了相关性与授权的冲突，并给出了明确的信任边界定义。
*   **生产就绪**：提供了 Kubernetes Operator，支持在云原生环境下自动化部署和多租户隔离。

### 8. 不足与局限
*   **检索召回率风险**：在不支持“谓词下推”的后端上，先检索后过滤会导致在大规模语料库下召回率（Recall）严重下降（实验显示从 1.0 降至 0.002）。
*   **策略复杂度**：随着租户和权限规则的增加，ABAC 策略的管理复杂度会呈指数级增长。
*   **客户端工具限制**：对于必须在客户端执行的本地工具（如操作用户本地文件），服务器端编排仍无法完全监控其内部行为。
*   **模型固有知识泄露**：该架构仅能防止通过 RAG 泄露的数据，无法阻止模型通过预训练阶段记忆的参数化知识泄露信息。

（完）
