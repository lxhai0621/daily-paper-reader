---
title: Iterative Multimodal Retrieval-Augmented Generation for Medical Question Answering
title_zh: 用于医学问答的迭代式多模态检索增强生成
authors: "Xupeng Chen, Binbin Shi, Chenqian Le, Jiaqi Zhang, Kewen Wang, Ran Gong, Jinhan Zhang, Chihang Wang"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27724v1"
tags: ["query:mmkqa"]
score: 10.0
evidence: 使用页面图像的医疗问答迭代多模态RAG框架
tldr: MED-VRAG通过检索和推理文档页面图像，实现了迭代式多模态RAG。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.27724v1/fig-001.webp\", \"caption\": \"Table 1. Accuracy (%) on medical QA benchmarks. Bold marks full MEDVRAG; not a global best (Med-PaLM 2 wins MedQA/PubMedQA on a matched 3-dataset average 80.2 vs. 75.3). † = published results, taken directly from the original publications. ∗ HuatuoGPT-o1 reports 63.1% on MMLU-Pro (Medical), not the MIRAGE MMLU-Med used here; we omit the column to avoid a non-comparable cell. Our controlled baselines (Qwen3-30B, Qwen3-8B, and the MEDVRAG ablation rows) are run under identical zero-shot conditions, prompt template, and answer-extraction procedure.\", \"page\": 4, \"index\": 1, \"width\": 908, \"height\": 479}, {\"url\": \"assets/figures/arxiv/2604.27724v1/fig-002.webp\", \"caption\": \"Table 2. End-to-end latency per query on 4×A100 (wall-clock, mean of 50 MedQA queries). Stage-1 retrieval and Stage-2 filter both repeat at every iteration when the VLM emits a refined query.\", \"page\": 5, \"index\": 2, \"width\": 497, \"height\": 1117}, {\"url\": \"assets/figures/arxiv/2604.27724v1/fig-003.webp\", \"caption\": \"Figure 1. MEDVRAG pipeline. A medical question is encoded by ColQwen2.5 and matched against a FAISS index over ∼350K PMC pages; an LLM filters candidates by summary (sharded MapReduce); a VLM iteratively reasons over the highest-ranked 10 page images plus top-20 summaries from the N2=100 filtered set, refining queries and accumulating findings in a memory bank across ≤ 3 rounds.\", \"page\": 3, \"index\": 3, \"width\": 935, \"height\": 625}]"
motivation: 使用页面图像的医疗问答迭代多模态RAG框架。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
医学检索增强生成（RAG）系统通常处理从生物医学文献中提取的文本块，从而丢弃了原始文档页面中丰富的视觉内容（表格、插图、结构化布局）。我们提出了 MED-VRAG，这是一个迭代式多模态 RAG 框架，它对 PMC 文档页面图像而非 OCR 文本进行检索和推理。该系统将 ColQwen2.5 补丁级页面嵌入与分片式 MapReduce LLM 过滤器相结合，可扩展至约 35 万页，同时通过离线粗到细索引（每页 C=8 个质心，对质心进行 ANN 检索，并对前 R 个候选名单进行精确双向评分）将第一阶段检索时间保持在 30 毫秒以下。随后，视觉语言模型（VLM）在最多 3 轮推理中迭代优化其查询并在记忆库中积累证据，在 4xA100 上单次迭代耗时约 15.9 秒，完整的三轮流水线耗时约 47.8 秒。在四个医学问答基准测试（MedQA、MedMCQA、PubMedQA、MMLU-Med）中，MED-VRAG 达到了 78.6% 的平均准确率。在使用相同 Qwen2.5-VL-32B 主干网络的受控对比中，检索相比无检索基线带来了 +5.8 个百分点的提升；我们还注意到其比 MedRAG + GPT-4 (76.8%) 高出 +1.8 个百分点，但需注意这是跨论文对比而非直接对比。消融实验表明，页面图像检索相比文本块检索提升了 +1.0，迭代提升了 +1.5，记忆库提升了 +1.0。

## Abstract
Medical retrieval-augmented generation (RAG) systems typically operate on text chunks extracted from biomedical literature, discarding the rich visual content (tables, figures, structured layouts) of original document pages. We propose MED-VRAG, an iterative multimodal RAG framework that retrieves and reasons over PMC document page images instead of OCR'd text. The system pairs ColQwen2.5 patch-level page embeddings with a sharded MapReduce LLM filter, scaling to ~350K pages while keeping Stage-1 retrieval under 30 ms via an offline coarse-to-fine index (C=8 centroids per page, ANN over centroids, exact two-way scoring on the top-R shortlist). A vision-language model (VLM) then iteratively refines its query and accumulates evidence in a memory bank across up to 3 reasoning rounds, with a single iteration costing ~15.9 s and the full three-round pipeline ~47.8 s on 4xA100. Across four medical QA benchmarks (MedQA, MedMCQA, PubMedQA, MMLU-Med), MEDVRAG reaches 78.6% average accuracy. Under controlled comparison with the same Qwen2.5-VL-32B backbone, retrieval contributes a +5.8 point gain over the no-retrieval baseline; we also note a +1.8 point edge over MedRAG + GPT-4 (76.8%), with the caveat that this is a cross-paper rather than head-to-head comparison. Ablations isolate +1.0 from page-image vs text-chunk retrieval, +1.5 from iteration, and +1.0 from the memory bank.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **MED-VRAG**，一个专门为医学问答设计的迭代式多模态检索增强生成（RAG）框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：医学文献（如 PMC 论文）包含大量表格、诊断流程图、解剖图和复杂的结构化布局。
*   **核心问题**：传统的医学 RAG 系统通常将 PDF 转换为纯文本块（OCR），这会丢失关键的视觉信息和空间上下文。此外，复杂的医学问题往往需要跨页面的多步推理，单次检索难以覆盖所有证据。
*   **研究动机**：开发一种能够直接检索“页面图像”并进行多轮迭代推理的系统，以保留医学文献的完整多模态信息。

### 2. 方法论
*   **核心思想**：放弃文本分块，将 35 万个 PMC 文档页面渲染为图像进行索引，利用视觉语言模型（VLM）进行“检索-推理-优化查询”的闭环迭代。
*   **关键技术细节**：
    *   **两阶段检索流水线**：
        *   **Stage 1（粗筛）**：使用 **ColQwen2.5** 补丁级嵌入。为了解决 35 万页图像检索的计算压力，采用“粗到细”策略：离线将每页补丁聚类为 8 个质心，在线先进行质心 ANN 搜索，再对候选集进行精确的双向评分。
        *   **Stage 2（精滤）**：使用 **Qwen3-30B-A3B** 模型，通过分片式 **MapReduce** 架构对前 2000 个页面的摘要进行评分，筛选出前 100 页。
    *   **迭代推理引擎**：使用 **Qwen2.5-VL-32B** 作为主干。VLM 每次观察前 10 张页面图像和前 20 个摘要。
    *   **记忆库（Memory Bank）**：以 JSON 格式存储每轮的发现、推理历史和笔记，防止信息在多轮迭代中丢失，并引导下一轮的查询优化。

### 3. 实验设计
*   **数据集**：使用了四个标准的医学问答基准：**MedQA** (USMLE)、**MedMCQA**、**PubMedQA** 和 **MMLU-Medical**。
*   **对比方法**：
    *   **No-RAG 基线**：包括 GPT-4、Med-PaLM 2、HuatuoGPT-o1、Qwen3 系列。
    *   **文本 RAG 基线**：MedRAG (结合 GPT-3.5/4)、Self-BioRAG。
    *   **消融实验**：对比了“纯文本检索”、“单轮图像检索”、“迭代（无记忆库）”以及“完整系统（带记忆库迭代）”。

### 4. 资源与算力
*   **硬件设备**：使用了 **4 × NVIDIA A100 80GB** GPU。
*   **时间开销**：
    *   第一阶段检索（ANN）：< 30 毫秒。
    *   单次迭代总耗时：约 **15.9 秒**。
    *   完整三轮迭代流水线：约 **47.8 秒**。
*   **数据规模**：处理了约 35 万张 PMC 页面图像。

### 5. 实验数量与充分性
*   **实验规模**：在四个主流数据集上进行了全面评估，样本量充足（如 MedMCQA 包含 4183 个问题）。
*   **充分性**：论文提供了详细的消融实验，逐一验证了页面图像、迭代机制和记忆库的增益。
*   **客观性**：作者进行了受控的内部对比（相同主干网络下的不同配置），但也诚实地指出与外部基线（如 MedRAG）的对比属于跨论文对比，存在提示词和环境差异。此外，还提供了一个四类错误分类分析，增加了实验的深度。

### 6. 主要结论与发现
*   **性能领先**：MED-VRAG 平均准确率达到 **78.6%**，在受控实验中比无检索基线提升了 **5.8%**。
*   **多模态优势**：页面图像检索相比纯文本检索带来了 **1.0%** 的提升，主要体现在对表格和插图的理解上。
*   **迭代价值**：迭代推理贡献了 **1.5%** 的增益，约 38.6% 的问题需要多轮检索才能解决。
*   **效率平衡**：虽然三轮迭代效果最好，但两轮迭代已能捕捉约 75% 的可修复错误，具有更好的计算性价比。

### 7. 优点
*   **保留原始上下文**：直接处理页面图像，避免了 OCR 过程中的信息损失（尤其是表格和布局）。
*   **检索效率高**：通过质心索引技术，实现了在大规模图像库中的毫秒级检索。
*   **可解释性强**：记忆库提供了推理轨迹，且检索结果为原始页面，方便医疗专业人员进行人工核查。

### 8. 不足与局限
*   **统计显著性**：实验结果多基于单次运行，缺乏多次运行的统计显著性检验（如置信区间）。
*   **基线对比单一**：文本 RAG 基线仅使用了 BGE-large 嵌入，未对比更先进的布局感知解析器（如 Nougat）。
*   **应用场景受限**：目前仅针对多项选择题，未在自由文本生成或真实临床决策场景中验证。
*   **延迟挑战**：近 50 秒的端到端延迟对于某些实时性要求极高的临床场景可能仍然过高。

（完）
