---
title: "From Unstructured to Structured: LLM-Guided Attribute Graphs for Entity Search and Ranking"
title_zh: 从非结构化到结构化：大语言模型引导的属性图用于实体搜索与排序
authors: "Yilun Zhu, Nikhita Vedula, Shervin Malmasi"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27410v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 从非结构化文本中提取结构化产品属性
tldr: "针对电商领域实体搜索中传统嵌入方法难以捕捉细粒度属性相关性的问题，本文提出一种结合大模型（LLM）引导的属性图构建与排序的两阶段框架。离线阶段从非结构化文本提取属性并构建类别感知的属性图，在线阶段利用结构化表示进行推理排序。该方法在零样本场景下将平均精度提升了5%以上，同时减少了57%的Token消耗，展现了极高的实用价值。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.27410v1/fig-001.webp\", \"caption\": \"Figure 1: The framework of our Graph Ranker System.\", \"page\": 1, \"index\": 1, \"width\": 506, \"height\": 323}, {\"url\": \"assets/figures/arxiv/2604.27410v1/fig-002.webp\", \"caption\": \"Table 2: Entity similarity search evaluated using Nova Pro LLM. The evaluation threshold represents the minimum similarity score required for a query-candidate pair to be considered relevant. sr and dr denote sparse and dense retrieval respectively.\", \"page\": 4, \"index\": 2, \"width\": 806, \"height\": 309}]"
motivation: 传统嵌入方法在电商搜索中难以捕捉不同类别和语境下细微的属性相关性。
method: 提出一种两阶段方法，通过LLM构建类别感知的结构化属性图，并基于该图进行推理排序。
result: "在零样本场景下平均精度提升超过5%，且单产品Token使用量降低了57%。"
conclusion: 该方法在无需训练数据的情况下实现了鲁棒的跨类别泛化，具有显著的实际部署潜力。
---

## 摘要
实体搜索，即寻找与查询实体最相似的实体，在电子商务领域面临着独特的挑战，因为产品相似性会随类别和上下文而变化。传统的基于嵌入的方法往往难以捕捉细微的、特定于上下文的属性相关性。在本文中，我们提出了一种结合大语言模型（LLM）驱动的属性图构建与图感知 LLM 排序的两阶段方法。在离线阶段，我们从非结构化文本中提取结构化的产品属性，并构建具有类别感知模式的可重用属性图。在在线阶段，我们通过对这种结构化表示而非原始文本进行推理来对检索到的候选对象进行排序，从而在提高排序精度的同时，将每个产品的 token 使用量减少了 57%。实验表明，我们的方法在零样本场景下优于多个基准模型，在不需要训练数据的情况下，平均精度提高了 5% 以上，在不同产品类别中具有鲁棒的泛化能力，并显示出巨大的实际部署潜力。

## Abstract
Entity search, i.e., finding the most similar entities to a query entity, faces unique challenges in e-commerce, where product similarity varies across categories and contexts. Traditional embedding-based approaches often struggle to capture nuanced context-specific attribute relevance. In this paper, we present a two-stage approach combining Large Language Model (LLM)-driven attribute graph construction with graph-aware LLM ranking. In the offline stage, we extract structured product attributes from unstructured text, and construct a reusable attribute graph with category-aware schemas. In the online stage, we rank retrieved candidates by reasoning over this structured representation rather than raw text, reducing per-product token usage by 57% while improving ranking precision. Experiments show that our approach outperforms multiple baselines under zero-shot scenarios, achieving a over 5% improvement in average precision without requiring training data, generalizes robustly across diverse product categories, and shows immense potential for real-world deployment.

---

## 论文详细总结（自动生成）

这篇论文由亚马逊（Amazon）的研究团队撰写，发表于 SIGIR 2026。以下是对该论文的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：在电子商务场景下，如何精准地进行“实体搜索”（即寻找与目标商品最相似的替代品）。
*   **研究动机**：
    *   **传统方法的局限**：基于嵌入（Embedding）的稠密检索难以捕捉细粒度的属性差异（如屏幕尺寸、材质等）；
    *   **LLM 直接排序的痛点**：直接将长篇的非结构化产品描述喂给大模型（LLM）进行排序，会导致 **Token 消耗巨大、推理延迟高、成本昂贵**，且容易受到无关文本信息的干扰，缺乏可解释性。
*   **整体含义**：提出一种“先结构化，后推理”的框架，通过 LLM 构建属性图，将复杂的文本比对转化为清晰的结构化属性比对。

### 2. 方法论
该方法分为两个阶段：
*   **阶段一：离线属性图构建（Offline Stage）**
    1.  **属性定义**：利用 LLM 为 61 个超类和近 5000 个子类定义属性 Schema（如电视对应“屏幕尺寸”，衣服对应“材质”）。
    2.  **属性提取**：使用 Claude 3.5 Sonnet 从非结构化文本（标题、描述、卖点）中提取 JSON 格式的属性-值对。
    3.  **图构建**：构建一个二部图 $G=(V_P, V_A, E)$，其中 $V_P$ 是产品节点，$V_A$ 是属性节点，边 $E$ 上的标签则是具体的属性值。
*   **阶段二：在线图感知排序（Online Stage）**
    1.  **候选生成**：使用 BGE-M3 模型进行稠密检索，从海量库中召回 Top-K 候选。
    2.  **图感知推理排序**：LLM 不再阅读原始长文本，而是接收查询实体与候选实体的**结构化属性列表**。LLM 作为一个零样本（Zero-shot）排序器，通过比对属性值的对齐程度进行打分（0-100分）并重新排序。

### 3. 实验设计
*   **数据集**：使用 Amazon Shopping Queries (ESCI) 大规模真实数据集，包含约 47 万个产品。
*   **Benchmark 与对比方法**：
    *   **Sparse Retrieval**：基于 BM25 的稀疏检索。
    *   **Dense Retrieval**：基于 BGE-M3 的稠密检索。
    *   **dr + raw-ranker**：稠密检索 + LLM 直接对原始非结构化文本进行排序（Baseline）。
    *   **dr + graph-ranker**：本文提出的方法（稠密检索 + 基于属性图的 LLM 排序）。
*   **评估指标**：P@k、MRR、mAP、nDCG。
*   **评估方式**：专家人工标注（200 对样本）+ LLM 自动评估（使用 Nova Pro 模型作为裁判）。

### 4. 资源与算力
*   **模型使用**：主要使用 **Claude 3.5 Sonnet v2** 进行属性提取和在线排序；使用 **Amazon Nova Pro** 进行自动化评估。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。由于该方法基于零样本推理（Zero-shot），不涉及大规模模型微调，算力消耗主要集中在 LLM 的 API 推理调用上。

### 5. 实验数量与充分性
*   **实验规模**：
    *   离线阶段对全库 47 万产品进行了属性提取。
    *   在线评估采样了 8.7k 个查询实体进行自动评估。
    *   人工评估涵盖了 40 个多样化查询和 200 个判定对。
*   **充分性**：实验设置了不同的相似度阈值（≥80 和 ≥50）来验证鲁棒性，并进行了定性错误分析。虽然人工标注的样本量不算巨大，但结合大规模 LLM 自动评估，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **精度提升**：在零样本场景下，相比于直接使用原始文本排序，该方法在 mAP 上提升了 **5% 以上**，P@1 显著优于所有基准。
*   **效率飞跃**：通过将非结构化文本转化为精简的属性 JSON，**单产品 Token 使用量减少了 57%**。
*   **性能优化**：在实际生产环境中，推理延迟比原始文本排序快了约 **250ms**。
*   **泛化能力**：该方法在无需任何训练数据的情况下，能有效处理新出现的商品类别。

### 7. 优点
*   **高效率低成本**：极大地压缩了 LLM 的输入上下文，解决了工业级部署中 LLM 排序太慢、太贵的痛点。
*   **可解释性强**：排序依据是明确的属性比对（如“颜色不符”或“规格一致”），便于系统调试和用户理解。
*   **工业落地性**：论文提到该系统已在亚马逊生产环境部署，证明了其在大规模电商场景下的实用价值。

### 8. 不足与局限
*   **领域依赖性**：对于书籍、电影、视频游戏等缺乏明确物理属性（如尺寸、功率）的类别，提取有意义的属性较为困难，效果受限。
*   **Schema 质量风险**：系统高度依赖 LLM 生成的初始属性 Schema，如果 Schema 定义不全或存在偏差，会直接影响后续的提取和排序质量。
*   **幻觉风险**：尽管有 Schema 约束，但 LLM 在提取属性值时仍可能产生细微的幻觉或错误提取。

（完）
