---
title: "Safe Bilevel Delegation (SBD): A Formal Framework for Runtime Delegation Safety in Multi-Agent Systems"
title_zh: 安全双层委派 (SBD)：多智能体系统中运行时委派安全的形式化框架
authors: Yuan Sun
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27358v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 分层多智能体系统中运行时委派安全性的正式框架
tldr: 针对大语言模型智能体在执行高风险任务时缺乏动态安全保障的问题，本文提出了安全双层委派（SBD）形式化框架。该框架将任务委派建模为双层优化问题，通过元权重网络动态平衡安全与效率，并引入连续委派度实现从人工干预到自主执行的平滑过渡。研究证明了该框架在安全性单调性、收敛性及责任传播方面的理论优势，为多智能体系统的运行时安全提供了严谨的数学基础。
source: arxiv
selection_source: fresh_fetch
motivation: 现有的多智能体架构缺乏在运行时根据任务上下文动态调整安全与效率权衡的机制。
method: 提出一种双层优化框架，外层学习上下文相关的安全权重，内层在概率安全约束下优化委派策略。
result: 理论证明了安全性随权重增加的单调性、内层策略的线性收敛性以及多级委派链中的责任传播界限。
conclusion: SBD框架为高风险环境下多智能体系统的动态安全委派提供了具有理论保障的形式化解决方案。
---

## 摘要
随着大语言模型 (LLM) 智能体被部署在高风险环境中，如何安全地将子任务委派给专门的子智能体变得至关重要。现有工作主要在设计阶段解决多智能体架构选择问题，或提供广泛的经验性指导，但均未能提供一种在执行过程中随任务上下文变化动态调整安全与效率权衡的运行时机制。我们提出了安全双层委派 (SBD)，这是一个用于分层多智能体系统中运行时委派安全的形式化框架。SBD 将任务委派表述为一个双层优化问题：外层元权重网络 phi 学习上下文相关的安全-效率权重 lambda(s) (取值范围 [0,1])；内层循环在满足概率安全约束 P(safe) >= 1-delta 的前提下优化委派策略 pi。连续委派程度 alpha (取值范围 [0, 1]) 控制转移给每个子智能体的决策权限，在完全人工干预 (alpha=0) 与完全自主执行 (alpha=1) 之间实现平滑插值。我们确立了三个理论结果：(1) 安全单调性——更高的外层安全权重会产生弱更安全的内层策略；(2) 内层策略收敛性——在标准平滑性假设下，内层问题的投影梯度下降呈线性收敛；(3) 责任传播界限，该界限在多跳委派链中分配责任，并具有可证明的单智能体上限。我们在医疗 AI (MIMIC-III)、金融风险控制 (S&P 500) 和教育智能体监管 (ASSISTments) 三个高风险领域实例化了 SBD，并指定了数据集、安全约束集、基准和评估协议。本手稿完整展示了形式化框架和理论结果；按照文中所述协议进行的实证验证正在计划中，并将在后续修订版中报告。

## Abstract
As large language model (LLM) agents are deployed in high-stakes environments, the question of how safely to delegate subtasks to specialized sub-agents becomes critical. Existing work addresses multi-agent architecture selection at design time or provides broad empirical guidelines, but neither provides a runtime mechanism that dynamically adjusts the safety-efficiency trade-off as task context changes during execution.   We propose Safe Bilevel Delegation (SBD), a formal framework for runtime delegation safety in hierarchical multi-agent systems. SBD formulates task delegation as a bilevel optimization problem: an outer meta-weight network phi learns context-dependent safety-efficiency weights lambda(s) in [0,1]; an inner loop optimizes the delegation policy pi subject to a probabilistic safety constraint P(safe) >= 1-delta. The continuous delegation degree alpha in [0, 1] controls how much decision authority is transferred to each sub-agent, interpolating smoothly between full human override (alpha=0) and fully autonomous execution (alpha=1).   We establish three theoretical results: (1) Safety Monotonicity--higher outer safety weight produces a weakly safer inner policy; (2) Inner Policy Convergence--projected gradient descent on the inner problem converges linearly under standard smoothness assumptions; (3) an Accountability Propagation bound that distributes responsibility across multi-hop delegation chains with a provable per-agent ceiling. We instantiate SBD in three high-stakes domains--medical AI (MIMIC-III), financial risk control (S and P 500), and educational agent supervision (ASSISTments)--specifying datasets, safety constraint sets, baselines, and evaluation protocols. This manuscript presents the formal framework and theoretical results in full; empirical validation following the protocols described herein is planned and will be reported in a forthcoming revision.

---

## 论文详细总结（自动生成）

这是一份关于论文《Safe Bilevel Delegation (SBD): A Formal Framework for Runtime Delegation Safety in Multi-Agent Systems》的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）智能体在医疗、金融等高风险领域的广泛应用，如何安全地将子任务委派给专门的子智能体成为核心挑战。
*   **研究动机**：现有的多智能体安全机制大多在“设计时”固定（如预设权限、静态架构），无法在“运行时”根据任务的实时风险上下文（如病人的紧急程度、市场的波动性）动态调整。
*   **核心问题**：主智能体应该向子智能体转移多少决策权？这种转移如何随风险环境动态演变并提供形式化的安全保障？

### 2. 方法论：核心思想与技术细节
论文提出了 **Safe Bilevel Delegation (SBD)** 框架，将委派问题建模为**双层优化（Bilevel Optimization）**问题：
*   **核心思想**：引入一个连续的委派程度 $\alpha \in [0, 1]$。$\alpha=0$ 表示完全由人工或主智能体干预，$\alpha=1$ 表示完全自主执行。
*   **外层优化（元学习层）**：学习一个元权重网络 $\phi(s)$，它根据当前状态 $s$ 输出一个权重 $\lambda$。这个 $\lambda$ 决定了系统在当前上下文中对“安全性”与“效率”的重视程度。
*   **内层优化（策略层）**：在给定 $\lambda$ 的情况下，优化委派策略 $\pi$，使其在满足概率安全约束 $P(safe) \ge 1 - \delta$ 的前提下，最小化加权的安全损失和效率损失。
*   **关键技术**：
    *   **超梯度计算**：使用隐式微分（Implicit Differentiation）和 `higher` 库来处理外层对内层最优解的梯度回传。
    *   **责任传播（Accountability Propagation）**：定义了委派链中的责任权重分配公式，确保在多跳委派中责任可追溯。
    *   **投影梯度下降**：在内层执行投影操作，确保策略始终落在安全约束集内。

### 3. 实验设计
论文预注册了三个高风险领域的评估协议（注：V1版本侧重理论和协议，实验结果待修订版更新）：
*   **数据集/场景**：
    1.  **医疗 AI**：使用 MIMIC-III 数据集，根据病人病情严重程度（APACHE-II评分）动态调整委派权限。
    2.  **金融风控**：使用 S&P 500 数据，在 2020 年新冠疫情引发的市场崩溃期间测试系统的风险控制能力。
    3.  **教育监管**：使用 ASSISTments 数据集，针对“风险学生”调整教学智能体的自主权。
*   **对比方法（Baselines）**：
    *   **Fixed-$\alpha$**：固定委派比例。
    *   **Safe-RL (CPO)**：传统的受限策略优化。
    *   **MARL-vanilla**：无安全机制的多智能体强化学习。
    *   **MaAS**：静态架构搜索方法。
    *   **SBD-NoOuter**：消融实验，移除外层动态权重学习。

### 4. 资源与算力
*   **硬件环境**：单台 **NVIDIA A100 (80GB)** GPU。
*   **训练时长**：论文指出，在设置外层迭代 $T_{out}=500$、内层步数 $T_{in}=50$、Batch Size=256 的情况下，每个领域的完整运行时间**少于 7 分钟**。
*   **模型规模**：$\phi$ 和 $\pi$ 均采用 3-4 层的 MLP 网络，宽度为 256。

### 5. 实验数量与充分性
*   **实验规模**：计划在三个完全不同的领域进行验证，每个领域包含 5 个基准对比和 4 组消融实验。
*   **客观性与公平性**：论文提出了明确的**可证伪性标准（Falsifiability Criteria）**。例如，如果 SBD 的安全-效率面积（SEA）不优于所有基准，或者消融外层循环后性能无显著差异，则认为框架失效。这种“预注册”模式增强了实验设计的客观性。
*   **局限性**：由于这是 V1 版本，**目前尚无实际运行结果数据**，其实证充分性需在后续修订版中根据实际曲线和表格进行评估。

### 6. 主要结论与发现（理论部分）
论文通过数学证明确立了三个核心结论：
1.  **安全单调性（Safety Monotonicity）**：证明了外层安全权重 $\lambda$ 的增加会单调地提高内层策略的安全性。
2.  **线性收敛性**：在强凸假设下，内层策略优化能够以线性速率收敛到最优解。
3.  **责任界限**：证明了在有限长度的委派链中，没有任何单一智能体可以承担全部责任，责任随委派程度 $\alpha$ 扩散。

### 7. 优点
*   **动态性**：突破了静态安全边界的限制，实现了运行时根据上下文（Context-aware）调整安全策略。
*   **理论严谨**：不仅有算法，还提供了关于单调性、收敛性和责任分配的正式数学证明。
*   **连续控制**：相比于二元的“委派/不委派”，连续的 $\alpha$ 允许更精细的权限管理。
*   **跨领域适用性**：框架设计通用，能够无缝接入医疗、金融等多种约束环境。

### 8. 不足与局限
*   **实证缺失**：当前版本仅有理论和协议，缺乏实际运行数据支撑。
*   **强凸性假设**：理论证明依赖于内层损失函数的强凸性，但在深度神经网络中，这一假设往往难以严格成立。
*   **安全约束集的来源**：SBD 假设安全约束集 $C$ 是预先给定的（由专家定义），但在复杂现实中，定义一个完备的安全约束集本身就是一个难题。
*   **单层安全模型**：目前只关注“委派决策”的安全，尚未建模子智能体在执行过程中的内部故障。

（完）
