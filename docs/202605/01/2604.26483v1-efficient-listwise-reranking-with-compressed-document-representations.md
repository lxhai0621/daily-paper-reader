---
title: Efficient Listwise Reranking with Compressed Document Representations
title_zh: 基于压缩文档表示的高效列表式重排序
authors: "Hervé Déjean, Stéphane Clinchant"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26483v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 检索增强生成（RAG）中的重排序
tldr: 本研究针对大语言模型在重排序任务中计算开销大的问题，提出了RRK模型。该模型借鉴RAG中的文档压缩技术，将文档转化为固定大小的多标记嵌入表示，并采用列表级重排序策略。通过知识蒸馏训练，RRK在保持高精度的同时，推理速度比参数量更小的模型快3至18倍，尤其在长文档检索场景下展现出卓越的效率和性能优势。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26483v1/fig-001.webp\", \"caption\": \"Table 3: Reranking long documents: RRK performs very well for the MS Marco Document DL 19/20 collections, and its efficiency is even more remarkable (See also Figure 2). nDCG@10, latency ratio relative to RRK.\", \"page\": 6, \"index\": 1, \"width\": 885, \"height\": 308}, {\"url\": \"assets/figures/arxiv/2604.26483v1/fig-002.webp\", \"caption\": \"Figure 2: Efficiency/Effectiveness diagram for the MS-MARCO Document TREC 2020. Captions indicate document max-length\", \"page\": 6, \"index\": 2, \"width\": 454, \"height\": 310}, {\"url\": \"assets/figures/arxiv/2604.26483v1/fig-003.webp\", \"caption\": \"Table 8: RRK compared to E2RANK (trained resp. with MS-Marco and BGE datasets) and PE-RANK.\", \"page\": 11, \"index\": 3, \"width\": 846, \"height\": 409}, {\"url\": \"assets/figures/arxiv/2604.26483v1/fig-004.webp\", \"caption\": \"Table 6: RRK compared to E2RANK (trained resp. with MS-Marco and BGE datasets) and PE-RANK . Latency estimated from [15] and [15]\", \"page\": 7, \"index\": 4, \"width\": 491, \"height\": 410}, {\"url\": \"assets/figures/arxiv/2604.26483v1/fig-005.webp\", \"caption\": \"Figure 3: Efficiency/effectiveness comparison of RRK, PE-Rank, and E2RANK.\", \"page\": 8, \"index\": 5, \"width\": 492, \"height\": 335}, {\"url\": \"assets/figures/arxiv/2604.26483v1/fig-006.webp\", \"caption\": \"Table 1: Evaluation and latency of our models for the BeIR collection (12 datasets). nDCG@10 and latency ratio relative to RRK. While public models perform the best when considering a long input length (2048), their latency compare badly to our RRK models using compressed document representation. †: listwise reranker. See also Figure 1.\", \"page\": 4, \"index\": 6, \"width\": 496, \"height\": 454}, {\"url\": \"assets/figures/arxiv/2604.26483v1/fig-007.webp\", \"caption\": \"Table 2: Detailled evaluation for the TREC DL 19/20 and BeIR collections (𝑛𝐷𝐶𝐺@10 ∗ 100). The top 50 of the SPLADE-v3 retrieved documents is used as input. For the public rerankers we report their best performances with an input length of 2048. Trained models’ performances are reported with an input length of 512 (no improvement with longer input for those datasets). †: listwise rerankers.\", \"page\": 5, \"index\": 7, \"width\": 996, \"height\": 525}]"
motivation: 旨在解决大语言模型在重排序过程中因输入长度和计算复杂度导致的推理效率低下问题。
method: 提出一种将文档压缩为多标记固定尺寸嵌入表示的方法，并结合列表级重排序进行蒸馏训练。
result: 实验表明8B规模的RRK模型在速度上超越了0.6B-4B的小模型，且在长文档任务中优势更为明显。
conclusion: 证明了压缩文档表示与列表级重排序的结合是实现高效且高性能检索系统的有效途径。
---

## 摘要
重排序（Reranking）是对第一阶段检索器输出进行精炼的过程，通常被认为计算成本高昂，尤其是在使用大语言模型（LLMs）时。减轻这一成本的常用方法包括利用较小的 LLM 或控制输入长度。受检索增强生成（RAG）中文档压缩最新进展的启发，我们提出了 RRK，这是一种高效且有效的列表式重排序器，它将文档压缩为多标记（multi-token）固定大小的嵌入表示。我们通过蒸馏进行的简单训练表明，这种丰富的压缩表示与列表式重排序的结合产生了一个高效且有效的系统。特别是，我们的 8B 参数模型比更小的重排序器（0.6-4B 参数）快 3 到 18 倍，同时在效果上与之持平或更优。在长文档基准测试中，效率提升更为显著，RRK 进一步扩大了其优势。

## Abstract
Reranking, the process of refining the output from a first-stage retriever, is often considered computationally expensive, especially when using Large Language Models (LLMs). A common approach to mitigate this cost involves utilizing smaller LLMs or controlling input length. Inspired by recent advances in document compression for retrieval-augmented generation (RAG), we introduce RRK, an efficient and effective listwise reranker compressing documents into multi-token fixed-size embedding representations. Our simple training via distillation shows that this combination of rich compressed representations and listwise reranking yields a highly efficient and effective system. In particular, our 8B-parameter model runs 3x-18x faster than smaller rerankers (0.6-4B parameters) while matching or outperforming them in effectiveness. The efficiency gains are even more striking on long-document benchmarks, where RRK widens its advantage further.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **RRK**（Compressed ReRanKer）的高效列表式重排序框架。以下是对该论文的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）在信息检索的重排序阶段表现优异，但计算开销巨大。传统的列表式重排序需要将查询与多个候选文档拼接输入，导致输入序列极长，注意力机制的二次方复杂度成为严重的推理瓶颈。
*   **研究动机**：受检索增强生成（RAG）中“软压缩（Soft Compression）”技术的启发，作者思考：能否将文档预先压缩为少量的固定长度嵌入（Embedding）标记，在重排序时直接处理这些压缩表示，从而在不显著损失精度的前提下极大提升推理速度？

### 2. 方法论
*   **核心思想**：将文档从原始文本压缩为多标记（Multi-token）的连续向量表示，并在此基础上进行列表式（Listwise）重排序。
*   **关键技术细节**：
    *   **文档压缩器（Compressor）**：基于 PISCO 框架，使用 LoRA 微调的 Qwen-2.5 8B 模型。它将每个文档（无论原始长度）映射为固定数量（如 8 个）的“记忆标记（Memory Tokens）”。
    *   **解码重排序器（Decoder Reranker）**：同样基于 Qwen-2.5 8B。输入序列构造为：`[查询文本] + [文档1压缩向量] + [SEP] + ... + [文档k压缩向量] + [SEP] + [查询文本]`。
    *   **评分机制**：提取查询的最终隐藏状态 $\mathbf{q}$ 和每个文档分隔符 `[SEP]` 处的隐藏状态 $\mathbf{h}_i$，通过余弦相似度计算相关性得分。
    *   **训练流程**：采用**知识蒸馏**。使用 RankNet 列表式损失函数，让 RRK 学习教师模型（如 Jina-v3 或 Qwen-32B）的排序输出。压缩器与重排序器进行联合微调。
*   **算法优势**：复杂度从 $O((|q| + k|d|)^2)$ 降至 $O((2|q| + k(l+1))^2)$，其中 $l$（压缩标记数）远小于 $|d|$（文档长度）。

### 3. 实验设计
*   **数据集**：
    *   **标准检索**：BeIR 基准测试（包含 12 个子数据集，如 TREC-COVID, NFCorpus 等）。
    *   **长文档检索**：MS MARCO Document 版本的 TREC DL 2019/2020。
*   **Benchmark 与对比方法**：
    *   **公共重排序器**：Jina-v3 (0.6B), Qwen3-0.6B/4B。
    *   **微调基准**：ModernBERT-large, 原始 Qwen2.5-8B（文本输入）。
    *   **同类高效模型**：PE-Rank, E2Rank。
*   **评估指标**：nDCG@10 和 延迟比率（Latency Ratio）。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA A100 GPU**。
*   **训练时长**：微调过程耗时约 **48 小时**。
*   **参数设置**：2 个 Epoch，Batch Size 为 2（列表式）或 8（点式），学习率 $1 \times 10^{-4}$。
*   **存储开销**：对于 MS-MARCO（880万文档），8 个记忆标记的 float16 存储约需 **230 GB**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从短文本到长文档的多种场景，并在 12 个 BeIR 数据集上进行了平均性能评估。
*   **消融实验**：
    *   对比了不同训练数据集（MS-MARCO vs E2Rank 数据集）的影响。
    *   对比了压缩器的不同配置（冻结、从头训练、微调预训练压缩器）。
    *   对比了点式（Pointwise）与列表式（Listwise）重排序的效果。
*   **客观性**：实验对比了不同参数规模的模型，并考虑了输入长度对基准模型的影响，评价较为客观公平。

### 6. 主要结论与发现
*   **极高的效率**：8B 参数的 RRK 在推理速度上比 0.6B-4B 的小型重排序器快 **3x 到 18x**。
*   **长文档优势**：在长文档任务中，RRK 的优势进一步扩大，压缩倍率可达 256 倍，且性能优于许多专门针对长文本设计的公开模型。
*   **压缩质量至关重要**：基于 LLM 的“软压缩”生成的表示比基于传统检索嵌入（如 PE-Rank）的表示更精细，能保留更多语义信息，从而维持高重排序精度。
*   **列表式优于点式**：在压缩表示下，列表式训练比点式训练更稳健，能更好地整合来自不同教师模型的知识。

### 7. 优点
*   **打破规模悖论**：证明了通过压缩表示，大参数模型（8B）可以比小参数模型（0.6B）运行得更快。
*   **端到端优化**：压缩器与重排序器联合训练，使压缩过程直接服务于排序目标。
*   **长文本处理能力**：有效解决了长文档重排序中显存溢出和计算缓慢的问题。

### 8. 不足与局限
*   **查询长度限制**：如果查询本身非常长（如 BRIGHT 数据集），RRK 的速度优势会减弱，因为查询部分未被压缩。
*   **存储成本**：预计算并存储文档的压缩向量需要巨大的磁盘空间（数百 GB），这增加了索引阶段的负担。
*   **模型蒸馏依赖**：性能高度依赖于教师模型的质量和训练数据的分布。
*   **小模型适配**：作者提到在 1B-4B 的更小模型上应用此技术尚未取得理想效果，目前仍需依赖 8B 规模的底座。

（完）
