---
title: "ObjectGraph: From Document Injection to Knowledge Traversal -- A Native File Format for the Agentic Era"
title_zh: ObjectGraph：从文档注入到知识遍历——智能体时代的原生文件格式
authors: "Mohit Dubey, Open Gigantic"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27820v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 将文档重新构思为供智能体遍历的有向知识图谱
tldr: 针对现有文档格式为人类线性阅读设计而导致LLM智能体在处理时存在Token浪费和信息冗余的问题，本文提出了一种名为ObjectGraph (.og) 的原生文件格式。该格式将文档重新构想为可遍历的有向知识图谱，是Markdown的超集。通过引入渐进式披露模型和角色范围访问协议，ObjectGraph在保持任务准确性的同时，显著降低了智能体的Token消耗，为智能体时代的文档处理提供了新标准。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.27820v1/fig-001.webp\", \"caption\": \"Figure 2: Mean token consumption per query across document classes and approaches. OBJECTGRAPH Architecture B achieves the greatest reduction on large runbooks.\", \"page\": 8, \"index\": 1, \"width\": 500, \"height\": 281}, {\"url\": \"assets/figures/arxiv/2604.27820v1/fig-002.webp\", \"caption\": \"Figure 3: Cumulative token cost in a 5-turn agentic workflow. Markdown exhibits super-linear compounding. OBJECTGRAPH Architecture B maintains near-linear growth through context isolation.\", \"page\": 8, \"index\": 2, \"width\": 486, \"height\": 252}, {\"url\": \"assets/figures/arxiv/2604.27820v1/fig-003.webp\", \"caption\": \"Table 2: ObjectGraph content-type tags and their semantic contracts.\", \"page\": 4, \"index\": 3, \"width\": 519, \"height\": 359}, {\"url\": \"assets/figures/arxiv/2604.27820v1/fig-004.webp\", \"caption\": \"Figure 4: Ablation: token reduction contribution of individual OBJECTGRAPH features. Index routing and the dense layer account for 82% of total savings.\", \"page\": 9, \"index\": 4, \"width\": 470, \"height\": 280}]"
motivation: 现有的线性文档格式与LLM智能体的检索式阅读需求不匹配，导致Token浪费和信息泄露。
method: 提出ObjectGraph格式，将文档定义为类型化的有向知识图谱，并引入渐进式披露和角色访问协议。
result: "在多类任务中实现了高达95.3%的Token减量，且任务准确率无显著下降，转换器保真度达98.7%。"
conclusion: ObjectGraph通过改变文档底层结构，有效解决了智能体时代的文档消费难题，是高效人机协作的理想格式。
---

## 摘要
现有的每种文档格式都是为线性阅读文本的人类读者设计的。自主大语言模型（LLM）智能体并不阅读，而是检索。这种根本性的不匹配迫使智能体将整个文档注入其上下文窗口，导致在无关内容上浪费 Token，在多轮循环中累积状态，并不加区分地在不同智能体角色间广播信息。我们认为这既不是提示工程问题，也不是检索或压缩问题，而是一个格式问题。我们推出了 OBJECTGRAPH (.og)，这是一种将文档重新构想为可遍历的有向类型知识图谱而非待注入字符串的文件格式。OBJECTGRAPH 是 Markdown 的严格超集——每个 .md 文件都是有效的 .og 文件——除了双原语查询协议外不需要任何基础设施，且无需工具即可供人类和智能体阅读。我们形式化了“文档消耗问题”，刻画了现有格式均无法同时满足的六个结构属性，并证明了 OBJECTGRAPH 满足全部六个属性。我们还引入了渐进式披露模型（Progressive Disclosure Model）、角色范围访问协议（Role-Scoped Access Protocol）和可执行断言节点（Executable Assertion Nodes）作为原生格式原语。对五类文档和八种智能体任务类型的实证评估表明，Token 消耗减少了高达 95.3%，且任务准确率没有统计学上的显著下降（p > 0.05）。在留出文档基准测试中，转译器的保真度达到了 98.7% 的内容保留率。

## Abstract
Every document format in existence was designed for a human reader moving linearly through text. Autonomous LLM agents do not read - they retrieve. This fundamental mismatch forces agents to inject entire documents into their context window, wasting tokens on irrelevant content, compounding state across multi-turn loops, and broadcasting information indiscriminately across agent roles. We argue this is not a prompt engineering problem, not a retrieval problem, and not a compression problem: it is a format problem.   We introduce OBJECTGRAPH (.og), a file format that reconceives the document as a typed, directed knowledge graph to be traversed rather than a string to be injected. OBJECTGRAPH is a strict superset of Markdown - every .md file is a valid .og file - requires no infrastructure beyond a two-primitive query protocol, and is readable by both humans and agents without tooling.   We formalize the Document Consumption Problem, characterise six structural properties no existing format satisfies simultaneously, and prove OBJECTGRAPH satisfies all six. We further introduce the Progressive Disclosure Model, the Role-Scoped Access Protocol, and Executable Assertion Nodes as native format primitives. Empirical evaluation across five document classes and eight agent task types demonstrates up to 95.3 percent token reduction with no statistically significant degradation in task accuracy (p > 0.05). Transpiler fidelity reaches 98.7 percent content preservation on a held-out document benchmark.

---

## 论文详细总结（自动生成）

### ObjectGraph：智能体时代的原生文件格式深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
论文指出，现有的文档格式（如 Markdown, HTML, JSON）均是为人类的**线性阅读**设计的，而自主 LLM 智能体（Agents）的行为模式是**检索式**的。这种“格式与需求的错配”导致了三大失效模式：
*   **Token 通胀（Token Inflation）：** 智能体为了获取一小段信息必须注入整个文档，平均利用率仅为 6.3%。
*   **上下文累积（Context Compounding）：** 在多轮对话中，重复发送长文档导致 Token 消耗呈超线性增长。
*   **角色盲视（Role Blindness）：** 无法针对不同权限或职责的智能体（如编排者 vs 执行者）提供差异化内容。
**核心主张：** 智能体效率问题不是提示工程或检索算法的问题，而是**底层文件格式**的问题。

#### 2. 论文提出的方法论
论文提出了 **ObjectGraph (.og)** 格式，将文档从“字符串流”重新定义为“有向类型知识图谱”。
*   **核心思想：** 文档由多个语义独立的“节点”（Nodes）组成，节点间通过“边”（Edges）建立逻辑依赖（如 `:requires`, `:precedes`）。
*   **关键技术细节：**
    *   **文件级索引（::index）：** 在文件头部提供轻量级路由表，允许智能体在不加载内容的情况下进行 O(1) 节点发现。
    *   **渐进式披露模型（PDM）：** 定义了三层读取深度：Pass 1（索引）、Pass 2（Dense 关键词层，约 12 tokens）、Pass 3（Full 完整内容层）。
    *   **角色范围访问协议（RSAP）：** 节点具有 `scope` 属性，根据调用者的角色（如 `worker`, `admin`）自动过滤内容。
    *   **可执行断言节点（Assertion Nodes）：** 在文档中内置验证逻辑（如检查命令输出），实现任务的自我验证。
    *   **双原语查询协议：** 仅需 `search_index`（搜索索引）和 `resolve_context`（解析上下文并自动追踪依赖边）两个操作。
    *   **Markdown 超集：** 保证向下兼容，任何 `.md` 文件都是合法的 `.og` 文件。

#### 3. 实验设计
*   **数据集/场景：** 构建了一个包含 240 个文档的基准测试，涵盖 5 类文档：技能文件、操作手册（Runbooks）、执行计划、技术文档、知识库。
*   **任务类型：** 定义了 8 种智能体任务，包括信息查找、多步规划、角色条件访问、断言验证等。
*   **对比方法（Baselines）：**
    1.  **B1: Markdown 全量注入**（当前主流做法）。
    2.  **B2: RAG**（基于向量检索的增强生成）。
    3.  **B3: SkillReducer**（现有的针对智能体优化的 Markdown 压缩方案）。
*   **测试模型：** Claude 4.5 系列（Sonnet/Haiku）和 GPT-4o。

#### 4. 资源与算力
论文**未明确说明**具体的 GPU 型号、数量或训练时长。由于 ObjectGraph 主要是关于文件格式定义、解析协议以及基于现有 LLM 的推理实验，而非从头训练大模型，因此其核心贡献在于架构设计和协议优化，对算力的依赖主要体现在推理评估阶段。

#### 5. 实验数量与充分性
*   **实验规模：** 针对 240 个文档和 8 类任务，每个“文档-任务”对重复执行 5 次以获取平均值和 95% 置信区间。
*   **消融实验：** 专门分析了索引路由、Dense 层、角色过滤、增量加载等特征对 Token 减量的贡献度。
*   **客观性与公平性：** 实验涵盖了不同规模的文件（从 200 到 15,000 tokens），并引入了转译器保真度测试（Fidelity Score），确保从 Markdown 转换到 .og 时内容无损。统计学检验（p > 0.05）证明了在大幅减量的情况下准确率没有下降，实验设计较为严谨、客观。

#### 6. 论文的主要结论与发现
*   **极高的 Token 效率：** 相比全量注入，ObjectGraph 实现了 **60% 至 95.3%** 的 Token 减量。
*   **解决累积效应：** 在 5 轮对话实验中，Markdown 消耗了 46,000 tokens，而 ObjectGraph (Arch B) 仅消耗 1,260 tokens，降低了 **36.5 倍**。
*   **准确率提升：** 在角色访问、更新检测和断言验证任务中，准确率大幅领先 Markdown（最高提升 43.5%），这得益于格式提供的结构化信号减少了注意力稀释。
*   **高保真转换：** 自动化转译器在保留原始内容方面的保真度达到 98.7%。

#### 7. 优点
*   **原生性：** 它是首个专门为 AI 智能体设计的文档格式，而非在旧格式上打补丁。
*   **基础设施无关：** 不需要复杂的向量数据库或中间件，仅靠文件结构和简单的查询协议即可运行。
*   **人机共读：** 保持了良好的可读性，人类可以直接编辑，智能体可以高效解析。
*   **安全性：** 通过格式层面的角色控制，降低了多智能体协作中的信息泄露风险。

#### 8. 不足与局限
*   **跨文件联邦：** 目前规范尚不支持跨文件的边引用（Cross-file edges），限制了其在超大规模分布式知识库中的应用。
*   **标准化挑战：** 作为一个新格式，其成功高度依赖于社区采纳和标准化进程（如 RFC 治理）。
*   **对抗性风险：** 尚未深入评估如果文档作者恶意编写错误的 `::index` 或 `::dense` 块，是否会误导智能体。
*   **评估范围：** 虽然涵盖了 240 个文档，但对于企业级数百万文档规模的性能表现仍需进一步验证。

（完）
