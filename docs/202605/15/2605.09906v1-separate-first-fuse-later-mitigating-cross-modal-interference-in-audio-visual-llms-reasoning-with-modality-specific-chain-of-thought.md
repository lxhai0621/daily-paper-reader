---
title: "Separate First, Fuse Later: Mitigating Cross-Modal Interference in Audio-Visual LLMs Reasoning with Modality-Specific Chain-of-Thought"
title_zh: 先分离后融合：通过模态特定思维链缓解视听大语言模型推理中的跨模态干扰
authors: "Xuanchen Li, Yuheng Lu, Chenrui Cui, Tianrui Wang, Zikang Huang, Yu Jiang, Long Zhou, Longbiao Wang, Jianwu Dang"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.09906v1"
tags: ["query:cmv"]
score: 9.0
evidence: 减轻视听大语言模型中的跨模态干扰与幻觉
tldr: 针对音视频大语言模型中存在的跨模态干扰和幻觉问题，本文提出了“先分离后融合”（SFFL）推理框架。该框架通过模态特定的思维链（CoT）生成独立的音频和视频推理轨迹，并利用强化学习及模态偏好标签引导模型在融合前保持模态隔离。实验表明，该方法显著提升了音视频问答的准确性，并有效缓解了跨模态幻觉。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.09906v1/fig-001.webp\", \"caption\": \"Table 1. Comparison of Our Method (SFFL) with Baselines on AVHBench and General AVQA Benchmarks. Results marked with a superscript † are taken from the original papers\", \"page\": 6, \"index\": 1, \"width\": 1019, \"height\": 375}, {\"url\": \"assets/figures/arxiv/2605.09906v1/fig-002.webp\", \"caption\": \"Figure 5. The data pipeline prompts.\", \"page\": 15, \"index\": 2, \"width\": 931, \"height\": 406}, {\"url\": \"assets/figures/arxiv/2605.09906v1/fig-003.webp\", \"caption\": \"Figure 6. The training/inference instruction prompts.\", \"page\": 15, \"index\": 3, \"width\": 931, \"height\": 600}, {\"url\": \"assets/figures/arxiv/2605.09906v1/fig-004.webp\", \"caption\": \"Figure 3. Layer-wise attention allocation from <sum> tokens to audio vs. visual reasoning traces. We report the normalized attention mass across the last 16 layers, grouped by predicted PEM.\", \"page\": 7, \"index\": 4, \"width\": 481, \"height\": 338}, {\"url\": \"assets/figures/arxiv/2605.09906v1/fig-005.webp\", \"caption\": \"Figure 2. Overview of Separate First, Fuse Later (SFFL) reasoning framework.\", \"page\": 3, \"index\": 5, \"width\": 1015, \"height\": 393}, {\"url\": \"assets/figures/arxiv/2605.09906v1/fig-006.webp\", \"caption\": \"Figure 4. Distribution of Preferred Evidence Modality (PEM) labels in AVQA-PEM-14K and the original modality annotations of evaluation benchmarks.\", \"page\": 13, \"index\": 6, \"width\": 783, \"height\": 471}, {\"url\": \"assets/figures/arxiv/2605.09906v1/fig-007.webp\", \"caption\": \"Figure 7. Cases between our method and Qwen3-Omni-Thinking.\", \"page\": 16, \"index\": 7, \"width\": 892, \"height\": 1087}]"
motivation: 现有的音视频大语言模型在推理过程中存在不受控的跨模态干扰，导致一种模态的信息误导另一种模态，从而引发幻觉。
method: 提出SFFL框架，通过模态特定思维链实现推理隔离，并结合模态偏好标签进行强化学习，最后在证据融合阶段整合信息。
result: "在通用AVQA基准上平均提升了5.16%，在跨模态幻觉基准上提升了11.17%，显著增强了模型的准确性和鲁棒性。"
conclusion: 通过在推理初期隔离模态并在后期融合证据，可以有效减轻跨模态干扰，是提升多模态大模型推理可靠性的有效途径。
---

## 摘要
音频和视觉为视听问答提供了互补的证据，然而当前的视听大语言模型可能会受到跨模态干扰的影响：来自一种模态的信息误导了对另一种模态的解释，从而引发幻觉。我们将这一问题归因于中间推理过程中不受控制的跨模态交互。为了缓解这一问题，我们提出了“先分离后融合”（Separate First, Fuse Later, SFFL），这是一种旨在减少跨模态干扰的视听推理框架。SFFL 强制执行模态特定的思维链推理，生成独立的音频和视觉推理轨迹，并整合证据进行回答。我们通过不同模态输入设置下的数据流水线构建了模态偏好标签。我们将这些标签作为强化学习中的辅助奖励，以鼓励在回答时对模态线索产生依赖于实例的偏好。我们进一步引入了一种模态特定推理机制，该机制在分离推理阶段保持模态隔离，同时在证据融合阶段允许完全访问跨模态信息。实验表明，该方法在准确性和鲁棒性方面均有持续提升，在通用 AVQA 基准测试中平均相对增益为 5.16%，在跨模态幻觉基准测试中平均相对增益为 11.17%。

## Abstract
Audio and vision provide complementary evidence for audio-visual question answering, yet current audio-visual large language models may suffer from cross-modal interference: information from one modality misguides the interpretation of another, thereby inducing hallucinations. We attribute this issue to uncontrolled cross-modal interactions during intermediate reasoning. To mitigate this, we propose Separate First, Fuse Later (SFFL), an audio-visual reasoning framework designed to reduce cross-modal interference. SFFL enforces modality-specific chain-of-thought reasoning, producing separate audio and visual reasoning traces and integrating evidence for answering. We construct modality-preference labels via a data pipeline under different modality input settings. We use these labels as an auxiliary reward in reinforcement learning to encourage a instance-dependent preference for modality cues when answering. We further introduce a modality-specific reasoning mechanism that preserves modality isolation during the separated reasoning stage while enabling full access to cross-modal information at the evidence fusion stage. Experiments demonstrate consistent improvements in both accuracy and robustness, yielding an average relative gain of 5.16\% on general AVQA benchmarks and 11.17\% on a cross-modal hallucination benchmark.

---

## 论文详细总结（自动生成）

### 论文总结：Separate First, Fuse Later (SFFL)

#### 1. 论文的核心问题与整体含义
本文针对**视听大语言模型（AVLMs）**在处理音视频问答（AVQA）任务时存在的**跨模态干扰（Cross-Modal Interference）**和**幻觉（Hallucination）**问题展开研究。
*   **研究动机**：人类通过整合视觉和听觉来理解世界，但当前的 AVLM 通常将两种模态的特征直接拼接后输入模型进行联合推理。这种“不受控”的交互会导致模态间的误导（例如：看到画面中有羊，即使音频中只有狗叫，模型也会幻觉出羊叫声），且模型往往表现出“视觉主导”的偏差，忽视了关键的音频证据。
*   **核心目标**：通过改变推理结构，强制模型先对各模态进行独立分析，最后再进行证据融合，从而减少干扰并提升推理的鲁棒性。

#### 2. 论文提出的方法论：SFFL 框架
论文提出了 **Separate First, Fuse Later (SFFL)** 框架，核心思想是“先分离推理，后融合证据”。
*   **模态偏好数据流水线（PEM Data Pipeline）**：
    *   通过在“仅音频”、“仅视频”和“视听结合”三种设置下探测模型的回答一致性和准确性，自动标注出每个问题的**首选证据模态（Preferred Evidence Modality, PEM）**（标签包括：Audio, Visual, Audio-Visual）。
    *   构建了包含 14k 条高置信度 PEM 标签的训练集 AVQA-PEM-14K。
*   **分离后融合推理（SFR）**：
    *   引入结构化控制标签（如 `<mod>`, `<v>`, `<a>`, `<sum>`, `<ans>`），强制模型按照“判定偏好模态 -> 视觉独立推理 -> 音频独立推理 -> 综合总结 -> 给出答案”的顺序生成思维链（CoT）。
*   **模态非对称注意力掩码（MAAM）**：
    *   为了防止推理过程中的信息泄露，在模型生成视觉推理片段时，通过掩码阻断其对音频输入 token 的注意力；在生成音频推理片段时，阻断其对视频输入及视觉推理片段的注意力。
*   **两阶段强化学习（GRPO）**：
    *   使用**群组相对策略优化（GRPO）**算法。
    *   **第一阶段**：仅优化“模态偏好与结构奖励（$R_{mps}$）”，训练模型遵循 SFR 格式并准确识别 PEM。
    *   **第二阶段**：结合“答案准确性奖励（$R_{acc}$）”和结构奖励，进一步提升问答性能。

#### 3. 实验设计
*   **数据集**：
    *   **跨模态幻觉评测**：AVHBench（包含 VAH、AVH、MIS 三个子任务）。
    *   **通用 AVQA 评测**：AVQA（标准基准）、Valor32k-AVQA v2.0（大规模真实场景）、MUSIC-AVQA（音乐特定领域）。
*   **对比方法**：
    *   主流模型：Qwen3-Omni、VideoLLaMA2.1、video-SALMONN-2+、Gemini-3-flash。
    *   推理优化方法：AVCD（对比解码）。
    *   消融基准：纯 GRPO（仅准确率奖励）、SFT（监督微调）版本。
*   **Backbone**：主要基于 Qwen3-Omni-30B 和 Qwen2.5-Omni-7B 进行验证。

#### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA H20 GPU**。
*   **训练细节**：
    *   第一阶段：Batch Size 为 96，学习率 1e-5，4 rollouts，使用 LoRA 微调。
    *   第二阶段：Batch Size 为 48，学习率 1e-6。
*   **时长**：文中未明确给出总训练时长，但提到了基于 ms-swift 框架进行优化。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个主流基准数据集上进行了详尽测试，涵盖了幻觉检测和通用问答。
*   **消融实验**：非常充分。分别验证了 SFR 结构、MAAM 掩码、PEM 标签的有效性，并对比了 SFT 与 RL 训练路径的差异。
*   **客观性**：通过在不同参数规模（7B 和 30B）的 Backbone 上进行实验，证明了方法的通用性；同时利用注意力权重可视化（Attention Mass）解释了 PEM 标签如何引导模型关注特定模态，增强了结果的可信度。

#### 6. 论文的主要结论与发现
*   **性能提升**：SFFL 在通用 AVQA 任务上平均提升 **5.16%**，在幻觉检测任务上显著提升 **11.17%**。
*   **缓解干扰**：通过 MAAM 强制隔离模态，能有效解决“视觉主导”偏差，使模型在音频关键任务（如 VAH）中表现更佳。
*   **RL 的优越性**：相比 SFT，基于奖励驱动的 GRPO 能更好地诱导模型产生结构化的推理行为，且具有更好的跨数据集泛化能力。
*   **PEM 的引导作用**：模型能够学习到不同实例对模态的依赖性，并在融合阶段（`<sum>`）有针对性地分配注意力。

#### 7. 优点
*   **结构创新**：不同于以往简单的特征融合，SFFL 从推理逻辑层面（CoT 结构）入手解决跨模态干扰。
*   **硬隔离机制**：MAAM 掩码的设计从底层注意力机制上保证了模态推理的独立性，比单纯的提示词约束更有效。
*   **可解释性**：通过 PEM 标签和注意力分析，为多模态模型的决策过程提供了直观的解释。

#### 8. 不足与局限
*   **推理开销**：由于采用了更长的结构化 CoT（包含视觉和音频两段独立推理），推理时的 Token 生成量增加，会导致推理延迟（Latency）上升。
*   **数据依赖**：PEM 标签的质量高度依赖于初始模型（如 Qwen3-Omni）在单模态下的探测表现，如果基础模型在单模态下本身极弱，PEM 标注可能失效。
*   **领域局限**：在 MUSIC-AVQA 等特定专业领域，提升幅度相对较小，可能受限于训练数据中专业知识的覆盖范围。

（完）
