---
title: Macro-Action Based Multi-Agent Instruction Following through Value Cancellation
title_zh: 基于宏动作并通过价值抵消实现的多智能体指令遵循
authors: "Wo Wei Lin, Ethan Rathbun, Enrico Marchesini Xiang Zhi Tan"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12655v1"
tags: ["query:agent"]
score: 9.0
evidence: 用于指令遵循和长周期目标的多智能体强化学习
tldr: 本研究针对多智能体强化学习中指令中断宏动作导致价值估计不一致的问题，提出了MAVIC方法。该方法通过在指令边界修正Bellman备份，抵消旧指令目标并恢复当前目标的延续价值，从而实现一致的价值估计。实验证明，MAVIC在复杂协作环境中能显著提高指令遵循度，同时保持基础任务性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.12655v1/fig-001.webp\", \"caption\": \"Table 1: MacDec-POMDP definition where the subscript i denotes individual agent components.\", \"page\": 2, \"index\": 1, \"width\": 520, \"height\": 339}, {\"url\": \"assets/figures/arxiv/2605.12655v1/fig-002.webp\", \"caption\": \"Figure 5: Action distribution frequency for successful delivery is shown by baseline no instruction while action distribution for unachievable instructions.\", \"page\": 8, \"index\": 2, \"width\": 826, \"height\": 235}]"
motivation: 现有的多智能体强化学习在处理中断宏动作的外部指令时，由于Bellman更新耦合了不同指令背景下的价值估计，会导致价值函数不一致。
method: 提出了MAVIC框架，通过在指令切换边界修正Bootstrapping目标，实现价值抵消与恢复，确保单一策略下的价值估计一致性。
result: 在复杂的协作多智能体环境中，MAVIC在保持基础任务性能的同时，实现了极高的指令遵循合规性。
conclusion: MAVIC通过修正Bellman备份而非奖励塑形，有效解决了指令中断带来的价值估计偏差，是实现多智能体指令遵循的稳健方案。
---

## 摘要
在实际应用场景中，多智能体强化学习（MARL）可能需要适应外部自然语言指令，这些指令会中断正在进行的行为并与长时程目标产生冲突。然而，将奖励以指令为条件会引入一种根本性的失效模式，因为贝尔曼更新会将不同指令上下文中的价值估计耦合在一起，导致指令中断宏动作时出现价值不一致。我们提出了用于指令合规的宏动作价值修正（MAVIC），它通过修正传入的指令目标并恢复当前目标下的延续价值，在指令边界处修正贝尔曼回溯。与奖励塑造不同，MAVIC 修改了自举目标本身，从而在统一策略内实现了随机指令切换下的连贯价值估计。我们提供了理论分析和一种 Actor-Critic 实现，并证明了在日益复杂的协作式多智能体环境中，MAVIC 在保持基础任务性能的同时，实现了较高的指令遵循度。

## Abstract
Multi-agent reinforcement learning (MARL) in real-world use cases may need to adapt to external natural language instructions that interrupt ongoing behavior and conflict with long-horizon objectives. However, conditioning rewards on instructions introduces a fundamental failure mode as Bellman updates couple value estimates across instruction contexts, leading to inconsistent values when instructions interrupt macro-actions. We propose Macro-Action Value Correction for Instruction Compliance (MAVIC), which corrects Bellman backups at instruction boundaries by correcting the incoming instruction objective and restoring the continuation value under the current objective. Unlike reward shaping, MAVIC modifies the bootstrapping target itself, enabling consistent value estimation under stochastic instruction switching within a unified policy. We provide theoretical analysis and an actor-critic implementation, and show that MAVIC achieves high instruction compliance while preserving base task performance in increasingly complex cooperative multi-agent environments.

---

## 论文详细总结（自动生成）

这篇论文提出了 **MAVIC**（Macro-Action Value Correction），旨在解决多智能体强化学习（MARL）在遵循动态、随机到达的自然语言指令时出现的价值估计不一致问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在现实世界的协作任务中，多智能体系统需要处理人类随时发出的自然语言指令。这些指令往往会中断正在执行的长周期“宏动作”（Macro-Actions）。
*   **核心矛盾**：传统的贝尔曼更新（Bellman Update）会将不同指令背景下的价值估计耦合在一起。当新指令中断当前动作时，价值函数会从受不同目标驱动的状态进行自举（Bootstrapping），导致“**价值交叉污染**”（Cross-contamination）。
*   **后果**：智能体要么为了维持基础任务性能而忽略指令，要么为了遵循指令而大幅降低基础任务的效率。

### 2. 方法论：核心思想与技术细节
MAVIC 的核心思想是在指令切换的边界处进行**价值抵消与修正**，确保每个指令上下文下的价值估计保持独立且一致。
*   **价值修正机制**：当指令从 $c$ 切换到 $c'$ 时，MAVIC 修改奖励函数，减去新指令背景下的未来价值估计，并补偿当前指令背景下的延续价值。
*   **关键公式**：修正后的动态奖励 $R'$ 定义为：
    *   若指令未变：$R' = R_c$
    *   若指令切换：$R' = R_c - \gamma V(s', c') + \gamma V(s', c)$
    *   这种修正直接作用于自举目标（Bootstrapping target），而非简单的奖励塑形（Reward Shaping）。
*   **架构设计**：
    *   **语言处理**：使用预训练且冻结的 **BERT** 模型作为语言编码器，将指令转化为嵌入向量。
    *   **Actor-Critic 框架**：在宏动作 Dec-POMDP 框架下，Critic 网络以指令嵌入为条件估计价值，Actor 根据修正后的梯度进行更新。
    *   **理论保证**：论文证明了该修正能实现指令间的价值解耦，使得优化全局目标等同于独立优化每个指令下的子任务。

### 3. 实验设计
研究者在三个复杂的协作多智能体环境中验证了 MAVIC：
*   **场景（Environments）**：
    1.  **Box Pushing (BP)**：双智能体协作推箱子，指令涉及改变目标或禁止推箱。
    2.  **Warehouse Tool Delivery (WTD)**：三台异构机器人为工人递送工具，指令涉及更改递送顺序。
    3.  **Overcooked (OC)**：三名智能体协作烹饪，指令包含资源限制（如“不要使用左侧砧板”）或特定请求。
*   **基准方法（Benchmarks）**：
    *   **Naive**：直接将指令奖励加入系统，不进行价值修正。
    *   **Switch**：在不同指令类之间独立切换训练（试图规避交叉污染）。
    *   **Vanilla Mac-IAC**：不带指令的基础宏动作 MARL 方法（作为性能上限参考）。
*   **评估指标**：基础任务性能（Base Task Performance）和指令遵循合规率（Instruction Compliance）。

### 4. 资源与算力
论文**未明确说明**具体的算力资源细节（如 GPU 型号、数量或总训练时长）。但提到使用了基于 [Xiao et al., 2022] 的异步框架实现，并对超参数进行了网格搜索。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同的领域进行了测试，每个实验运行了 **5 个独立随机种子**，并给出了 95% 的置信区间。
*   **消融与泛化**：
    *   进行了**分布外（OOD）指令测试**，验证了模型对未见过的同义指令的泛化能力。
    *   分析了不可行指令下的行为分布。
*   **充分性评价**：实验设计较为充分，涵盖了从简单协作到复杂资源竞争的多种场景，对比基准选择合理，能够客观反映价值修正的必要性。

### 6. 主要结论与发现
*   **消除权衡**：MAVIC 是唯一能在所有环境中同时保持高基础任务得分和高指令遵循率（通常 >90%）的方法。
*   **解决失效模式**：Naive 方法在复杂任务（如 Overcooked）中几乎完全崩溃，因为它无法处理指令带来的负奖励对基础路径的干扰。
*   **稳定性**：相比于 Switch 训练法，MAVIC 在训练过程中表现出更低的方差和更高的稳定性。
*   **泛化性**：通过冻结的语言编码器，MAVIC 能够理解并遵循与训练指令语义相似的新指令。

### 7. 优点
*   **理论严谨**：通过数学证明展示了价值解耦的可能性，为处理中断任务提供了坚实的理论基础。
*   **即插即用**：该价值修正机制可以集成到现有的多种宏动作 Actor-Critic 算法中。
*   **处理中断的能力**：专门针对宏动作被强制提前终止的情况进行了优化，这在人机交互中非常实用。

### 8. 不足与局限
*   **价值估计依赖**：MAVIC 的效果高度依赖于 Critic 对价值函数的准确估计。在部分可观测性极强或奖励极度稀疏的环境下，修正项可能引入较大的方差。
*   **语言理解深度**：虽然使用了 BERT，但本质上仍是将指令作为一种状态特征。对于逻辑极其复杂或需要多步推理的指令，该方法可能仍需结合更高层的逻辑推理模块。
*   **探索问题**：论文提到 MAVIC 并没有显式解决罕见指令下的探索难题，如果某些指令在训练中极少出现，智能体可能无法学会对应的行为。

（完）
