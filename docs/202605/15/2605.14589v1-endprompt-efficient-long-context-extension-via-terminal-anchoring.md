---
title: "EndPrompt: Efficient Long-Context Extension via Terminal Anchoring"
title_zh: EndPrompt：通过终端锚定实现高效的长上下文扩展
authors: "Han Tian, Luxuan Chen, Xinran Chen, Rui Kong, Fang Wang, Jiamin Chen, Jinman Zhao, Yuchen Li, Jiashu Zhao, Shuaiqiang Wang, Haoyi Xiong, Dawei Yin"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.14589v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 通过终端锚定实现高效的长上下文扩展
tldr: 针对大语言模型长文本扩展中训练成本高昂的问题，本文提出EndPrompt方法。该方法通过在短训练序列末尾添加带有目标长度位置索引的终端提示词，在保持语义连贯性的同时引入长程相对距离。实验表明，EndPrompt仅需短序列训练即可实现高效的长文本扩展，在RULER和LongBench等基准测试中优于全长度微调及现有主流方法，显著降低了计算需求。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.14589v1/fig-001.webp\", \"caption\": \"Table 1: Results on the RULER synthetic benchmark (4K-64K).\", \"page\": 6, \"index\": 1, \"width\": 606, \"height\": 380}, {\"url\": \"assets/figures/arxiv/2605.14589v1/fig-002.webp\", \"caption\": \"Table 2: Results on various downstream tasks.\", \"page\": 6, \"index\": 2, \"width\": 757, \"height\": 186}, {\"url\": \"assets/figures/arxiv/2605.14589v1/fig-003.webp\", \"caption\": \"Table 8: Results on the RULER synthetic benchmark (16K).\", \"page\": 15, \"index\": 3, \"width\": 648, \"height\": 379}, {\"url\": \"assets/figures/arxiv/2605.14589v1/fig-004.webp\", \"caption\": \"Table 9: Results on the RULER synthetic benchmark (32K).\", \"page\": 15, \"index\": 4, \"width\": 648, \"height\": 380}, {\"url\": \"assets/figures/arxiv/2605.14589v1/fig-005.webp\", \"caption\": \"Table 7: Results on the RULER synthetic benchmark (8K).\", \"page\": 14, \"index\": 5, \"width\": 648, \"height\": 380}, {\"url\": \"assets/figures/arxiv/2605.14589v1/fig-006.webp\", \"caption\": \"Table 6: Results on the RULER synthetic benchmark (4K).\", \"page\": 14, \"index\": 6, \"width\": 648, \"height\": 381}, {\"url\": \"assets/figures/arxiv/2605.14589v1/fig-007.webp\", \"caption\": \"Figure 4: Comparison of memory footprint and time consumption across different methods.\", \"page\": 13, \"index\": 7, \"width\": 829, \"height\": 341}, {\"url\": \"assets/figures/arxiv/2605.14589v1/fig-008.webp\", \"caption\": \"Figure 1: Overview of the proposed method.\", \"page\": 4, \"index\": 8, \"width\": 820, \"height\": 333}, {\"url\": \"assets/figures/arxiv/2605.14589v1/fig-009.webp\", \"caption\": \"Table 5: Results on short-text evaluation tasks after supervised fine-tuning.\", \"page\": 9, \"index\": 9, \"width\": 739, \"height\": 290}]"
motivation: 旨在解决长文本扩展训练中因序列长度增加导致的计算和内存开销呈二次方增长的难题。
method: 采用两段式构造，将短序列的第二部分分配接近目标长度的位置索引，从而在短物理序列中模拟长程相对距离。
result: 在LLaMA模型8K到64K的扩展任务中，EndPrompt在RULER和LongBench上均取得了优于全长度微调及LongLoRA等方法的表现。
conclusion: 证明了长文本泛化能力可以通过稀疏的位置监督诱导产生，无需昂贵的密集长序列训练。
---

## 摘要
扩展大语言模型的上下文窗口通常需要在目标长度的序列上进行训练，这会产生二次方的内存和计算开销，使得长上下文适配变得昂贵且难以复现。我们提出了 EndPrompt，一种仅使用短训练序列即可实现有效上下文扩展的方法。核心见解是，让模型接触长距离相对位置距离并不需要构建全长度输入：我们将原始短上下文保留为完整的第一段，并附加一个简短的终端提示词作为第二段，为其分配接近目标上下文长度的位置索引。这种两段式结构在短物理序列中同时引入了局部和长距离相对距离，同时保持了训练文本的语义连续性——这是在分割连续上下文的基于块（chunk-based）的模拟方法中所不具备的特性。我们提供了基于旋转位置嵌入（Rotary Position Embedding）和 Bernstein 不等式的理论分析，表明位置插值在注意力函数上引入了严格的光滑性约束，而共享的 Transformer 参数进一步抑制了对未观察到的中间距离的不稳定外推。将 EndPrompt 应用于 LLaMA 系列模型，将上下文窗口从 8K 扩展到 64K，其平均 RULER 得分为 76.03，并在 LongBench 上获得了最高平均分，超过了 LCEG (72.24)、LongLoRA (72.95) 和全长度微调 (69.23)，同时所需的计算量大幅减少。这些结果表明，长上下文泛化可以从稀疏的位置监督中诱导产生，挑战了“可靠的上下文窗口扩展必须进行密集长序列训练”的普遍假设。代码可在 https://github.com/clx1415926/EndPrompt 获取。

## Abstract
Extending the context window of large language models typically requires training on sequences at the target length, incurring quadratic memory and computational costs that make long-context adaptation expensive and difficult to reproduce. We propose EndPrompt, a method that achieves effective context extension using only short training sequences. The core insight is that exposing a model to long-range relative positional distances does not require constructing full-length inputs: we preserve the original short context as an intact first segment and append a brief terminal prompt as a second segment, assigning it positional indices near the target context length. This two-segment construction introduces both local and long-range relative distances within a short physical sequence while maintaining the semantic continuity of the training text--a property absent in chunk-based simulation approaches that split contiguous context. We provide a theoretical analysis grounded in Rotary Position Embedding and the Bernstein inequality, showing that position interpolation induces a rigorous smoothness constraint over the attention function, with shared Transformer parameters further suppressing unstable extrapolation to unobserved intermediate distances. Applied to LLaMA-family models extending the context window from 8K to 64K, EndPrompt achieves an average RULER score of 76.03 and the highest average on LongBench, surpassing LCEG (72.24), LongLoRA (72.95), and full-length fine-tuning (69.23) while requiring substantially less computation. These results demonstrate that long-context generalization can be induced from sparse positional supervision, challenging the prevailing assumption that dense long-sequence training is necessary for reliable context-window extension. The code is available at https://github.com/clx1415926/EndPrompt.

---

## 论文详细总结（自动生成）

这是一份关于论文《EndPrompt: Efficient Long-Context Extension via Terminal Anchoring》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
大语言模型（LLM）的上下文窗口扩展通常面临**计算成本高昂**的问题，因为在目标长度（如 64K 或更长）上进行全长度微调需要消耗随序列长度平方增长的内存和计算资源。
*   **研究动机**：现有的高效扩展方法（如 PoSE）通过将长文本切块并操纵位置索引来模拟长距离，但这会破坏文本的语义连贯性。
*   **核心问题**：模型是否必须观察完整的长序列才能获得长上下文能力？论文探讨了是否可以通过**稀疏的位置监督**（即只观察极短和极远的位置关系）来诱导模型产生长程泛化能力。

### 2. 提出方法论：EndPrompt (ET)
EndPrompt 的核心思想是**“终端锚定” (Terminal Anchoring)**，即在短序列中同时构建局部和长程的位置信号。
*   **核心思想**：将训练数据构造为两个片段。第一段是原始的短上下文（保持语义完整）；第二段是一个简短的“终端提示词”（End Prompt）。
*   **关键技术细节**：
    *   **位置索引操纵**：第一段分配正常的位置索引 $[0, a-1]$；第二段（终端提示词）分配接近目标窗口末端的位置索引 $[L-b, L-1]$（其中 $L$ 是目标扩展长度）。
    *   **位置插值 (PI)**：结合位置插值技术，将分配的索引缩放到预训练范围内，从而降低注意力函数的变化率。
    *   **理论支撑**：基于旋转位置嵌入（RoPE）和 Bernstein 不等式证明，位置插值引入了平滑性约束。由于 Transformer 参数在不同距离间共享，局部和极远距离的监督共同约束了参数空间，使得模型在未观察到的中间距离上也能稳定泛化。

### 3. 实验设计
*   **模型对象**：LLaMA-2-7B、LLaMA-3-8B、Mistral-7B-v0.3。
*   **扩展目标**：从原始的 8K 扩展至 64K（消融实验中进一步扩展至 128K）。
*   **基准测试 (Benchmarks)**：
    *   **RULER**：用于评估长文本检索和复杂信息提取能力（如多针寻回）。
    *   **LongBench**：涵盖单/多文档问答、摘要、代码补全等实际下游任务。
    *   **短文本评估**：MMLU、GSM8K、HumanEval、HellaSwag（评估扩展后是否损害基础能力）。
*   **对比方法**：全长度微调 (Full FT)、LongLoRA、LCEG、PoSE（位置跳跃嵌入）。

### 4. 资源与算力
*   **硬件环境**：单节点配备 **8 张 NVIDIA A800 (80GB) GPU**。
*   **训练配置**：使用 10 亿（1B）个 token 的语料库，进行 1 个 epoch 的全参数微调。
*   **效率提升**：在 64K 长度下，EndPrompt 比全长度微调减少了 **52% 的内存消耗**，并实现了约 **1.41x 至 1.77x 的加速**。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的消融实验，包括：
    *   不同基础模型（LLaMA, Mistral）。
    *   不同扩展长度（32K, 64K, 96K, 128K）。
    *   不同训练数据量（0.5B, 1.0B, 2.0B tokens）。
    *   不同终端提示词的形式（特定短语、特殊 Token、简单词汇）。
*   **充分性评价**：实验设计非常充分且客观。通过在 RULER 这种高难度合成任务和 LongBench 这种实际任务上的双重验证，证明了方法的鲁棒性。同时，论文还对比了短文本能力的恢复情况，确保了评估的全面性。

### 6. 主要结论与发现
*   **稀疏监督有效性**：仅通过短序列和终端位置锚定，模型就能获得极强的长文本处理能力，无需接触中间长度的序列。
*   **性能领先**：在 64K 扩展任务中，EndPrompt 的 RULER 平均分（76.03）显著高于全长度微调（69.23）和 LongLoRA（72.95）。
*   **语义保持**：相比于切块模拟方法（PoSE），EndPrompt 由于保留了前段文本的完整性，在短文本任务和代码补全任务上表现更优。
*   **提示词鲁棒性**：终端提示词的具体内容并不关键，关键在于其在位置编码空间中的“锚定”作用。

### 7. 优点（亮点）
*   **极高效率**：打破了长文本训练必须使用长序列的固有认知，极大地降低了算力门槛。
*   **语义友好**：不破坏原始文本的连续性，这对于需要局部逻辑推理的任务（如代码、数学）至关重要。
*   **理论严密**：不仅有实验，还通过数学分析解释了为什么 PI 和共享参数能支持这种跨距离的泛化。
*   **即插即用**：可以与 PoSE 等现有方法结合，进一步提升性能。

### 8. 不足与局限
*   **结构依赖**：该方法依赖于显式的“终端段”构造，对于某些非线性阅读或流式处理场景的适用性有待进一步研究。
*   **短文本能力回退**：虽然论文提出了通过 SFT 恢复短文本能力，但原始扩展训练后仍存在一定程度的基础能力下降，需要额外的微调步骤。
*   **模型架构限制**：目前主要在基于 RoPE 的 Transformer 模型上验证，对于其他位置编码机制（如相对位置偏差或绝对位置编码）的有效性尚未证实。

（完）
