---
title: Low-Cost Black-Box Detection of LLM Hallucinations via Dynamical System Prediction
title_zh: 基于动力系统预测的低成本黑盒大语言模型幻觉检测
authors: "Dan Wilson, Mohamed Akrout"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.05134v1"
tags: ["query:ma-kf"]
score: 10.0
evidence: 通过动力系统预测检测LLM幻觉
tldr: 针对大语言模型幻觉检测成本高的问题，本文提出一种基于动力系统预测的黑盒检测方法。该方法将LLM响应视为高维流形中的动态序列，利用Koopman算子理论建模事实与幻觉状态的演化规律，通过单次采样即可实现高效检测。实验证明该方法在降低资源开销的同时达到了SOTA性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.05134v1/fig-001.webp\", \"caption\": \"Figure 4: Hallucination detection performance using DS classification on 8K samples of the HaluEval summarization task, showing (a) classification accuracy for multiple black-box LLMs and (b) the corresponding ROC curves for all embedding models.\", \"page\": 6, \"index\": 1, \"width\": 662, \"height\": 232}, {\"url\": \"assets/figures/arxiv/2605.05134v1/fig-002.webp\", \"caption\": \"Figure 5: ROC curves on the HaluEval dataset as a function of the sequence length L for (a) the average classification performance and for (b)–(f) each embedding model showing the variation of the true positive rate against the false positive rate at various prediction error thresholds.\", \"page\": 7, \"index\": 2, \"width\": 582, \"height\": 430}, {\"url\": \"assets/figures/arxiv/2605.05134v1/fig-003.webp\", \"caption\": \"Figure 3: Hallucination Detection Dynamical System (DS): (a) Phase 1: Dynamical system fitting by embedding accurate and hallucinated datasets into token embedding matrices. This data is used to provide two different estimates for the action of the Koopman operator that describes the temporal evolution of the token embedding for accurate and hallucinated responses, (b) Phase 2: Hallucination detection of LLM responses to queries by embedding them and passing them through both predictive models. The classification is determined by comparing the relative accuracy of these estimates.\", \"page\": 4, \"index\": 3, \"width\": 720, \"height\": 231}, {\"url\": \"assets/figures/arxiv/2605.05134v1/fig-004.webp\", \"caption\": \"Figure 7: User-centric threshold calibration: (1) selection of calibration samples based on user preference (tolerant or strict to minor inaccuracies), (2) computation of per-token differential residual scores ∆E , and (3) calibration of the classification threshold η to maximize the target metric.\", \"page\": 9, \"index\": 4, \"width\": 596, \"height\": 360}]"
motivation: 现有的幻觉检测方法依赖昂贵的多次采样或外部检索，亟需一种低成本且无需外部知识的黑盒检测方案。
method: 将LLM响应嵌入向量序列并视为动力系统，利用Koopman算子拟合事实与幻觉的转移算子，并根据预测残差进行分类。
result: 在三个基准数据集上的测试表明，该方法在显著降低计算资源开销的同时，取得了当前最先进的检测性能。
conclusion: 该研究证明了通过动力系统视角分析LLM内部动态是实现低成本、高精度幻觉检测的有效途径。
---

## 摘要
大语言模型（LLMs）经常生成看似合理但非事实的内容，这种现象被称为“幻觉”。虽然现有的检测方法通常依赖于计算成本高昂的基于采样的连贯性检查或外部知识检索，但我们提出了一种将 LLM 视为黑盒动力系统的新方法。通过嵌入模型将 LLM 的响应投影到高维流形中，我们将生成的向量序列描述为模型潜在状态空间动力学的可观测实现。利用库普曼算子理论（Koopman operator theory），我们分别为事实和幻觉状态拟合转移算子，并根据它们各自的预测误差定义差分残差得分。为了适应不同的用户需求和特定领域的敏感性，我们引入了一种偏好感知校准机制，该机制基于少量示例优化分类阈值。这种方法能够在单次采样过程中实现低成本的幻觉检测，避免了二次采样或外部基准的需求。在三个数据基准上的广泛测试表明，我们的方法在减少资源开销的同时，实现了最先进的性能。

## Abstract
Large Language Models (LLMs) frequently generate plausible but non-factual content, a phenomenon known as hallucination. While existing detection methods typically rely on computationally expensive sampling-based consistency checks or external knowledge retrieval, we propose a new method that treats the LLM as a black-box dynamical system. By projecting LLM responses into a high-dimensional manifold via an embedding model, we characterize the resulting vector sequences as observable realizations of the model's latent state-space dynamics. Leveraging Koopman operator theory, we fit the transition operators for both factual and hallucinated regimes and define a differential residual score based on their respective prediction errors. To accommodate varying user requirements and domain-specific sensitivities, we introduce a preference-aware calibration mechanism that optimizes the classification threshold based on a small set of demonstrations. This approach enables low-cost hallucination detection in a single-sample pass, avoiding the need for secondary sampling or external grounding. Extensive testing across three data benchmarks demonstrates that our method achieves state-of-the-art performance with reduced resource overhead.

---

## 论文详细总结（自动生成）

这篇论文提出了一种创新的、低成本的大语言模型（LLM）幻觉检测方法。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：LLM 经常生成虚假但看似合理的“幻觉”内容。现有的检测方法要么需要访问模型内部状态（白盒），要么需要获取 Token 概率（灰盒），或者需要多次采样进行一致性检查或外部知识检索（黑盒）。
*   **核心痛点**：现有的黑盒检测方法计算成本高（多次 API 调用）、延迟大，且高度依赖外部知识库。
*   **整体含义**：本文提出将 LLM 的输出视为一个**动力系统（Dynamical System, DS）**。通过分析 Token 嵌入向量在时间维度上的演化轨迹，无需外部检索或多次采样，仅凭单次生成的文本即可识别幻觉。

### 2. 方法论
*   **核心思想**：认为事实性响应和幻觉响应在嵌入空间中分别演化在不同的“流形（Manifolds）”上。通过学习这两类流形的动力学特征，可以根据预测残差来区分它们。
*   **关键技术细节**：
    *   **库普曼算子理论（Koopman Operator Theory）**：将非线性动力系统投影到高维空间，使其演化过程可以用线性算子表示。
    *   **扩展动态模式分解（EDMD）**：利用该算法从已有的事实和幻觉样本中，分别拟合出两个转移算子 $A_c$（正确）和 $A_h$（幻觉）。
    *   **提升映射（Lifting）**：将 Token 嵌入向量映射到更高维的观测空间（如使用多项式组合），以捕捉复杂的非线性特征。
    *   **差分残差得分（$\Delta E$）**：对于待测文本，同时用 $A_c$ 和 $A_h$ 预测下一个 Token 的位置。如果 $A_h$ 的预测误差显著小于 $A_c$，则判定为幻觉。
    *   **偏好感知校准**：引入一个可调阈值 $\eta$，允许用户根据对幻觉的容忍度（严厉或宽松）进行个性化配置。

### 3. 实验设计
*   **数据集**：
    *   **WikiBio**：句子级标注，包含准确、轻微错误、严重错误三类。
    *   **HaluEval**：摘要任务，包含 10k 对正确和幻觉响应。
    *   **FELM**：涵盖数学、科学、逻辑等专业领域的句子级推理数据集。
*   **对比方法（Benchmarks）**：
    *   **SelfCheckGPT**：基于多次随机采样一致性的黑盒基准。
    *   **LLM 直接检测**：如 GPT-4、Claude、Llama-2 等模型的零样本检测能力。
*   **嵌入模型**：测试了从 30M（Jina-v5-Nano）到 8B（Llama-3）不同规模的 5 种嵌入模型。

### 4. 资源与算力
*   **算力说明**：论文**未明确给出**具体的 GPU 型号、数量或训练时长。
*   **资源消耗特征**：作者强调该方法是“低成本”的，因为它只需要单次推理（Single-pass），且拟合线性动力系统（矩阵运算）的计算开销远小于深度学习训练或大规模检索。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流基准数据集上进行了测试，涵盖了从句子级到篇章级的不同粒度。
*   **充分性分析**：
    *   **模型多样性**：测试了 5 种不同架构和规模的嵌入模型，证明了方法的普适性。
    *   **消融/分析实验**：研究了序列长度对检测准确率的影响，并探讨了跨模型泛化性（用 A 模型的嵌入训练，在 B 模型上测试）。
    *   **客观性**：通过 ROC 曲线和 AUC-PR 等指标进行评估，实验设计较为严谨。

### 6. 主要结论与发现
*   **性能卓越**：该方法在多个数据集上达到或超过了 SOTA（最先进）水平，尤其是在 WikiBio 和 HaluEval 上表现优异。
*   **长度效应**：检测准确率随 Token 序列长度增加而提升，因为更长的轨迹能提供更丰富的动力学信息。
*   **规模无关性**：即使是极小的嵌入模型（如 30M 参数）也能通过该方法实现有效的幻觉检测。
*   **跨模型潜力**：发现不同嵌入模型之间存在一定的动力学共性，暗示幻觉在嵌入空间中可能具有某种“通用流形”特征。

### 7. 优点
*   **极高效率**：单次采样即可检测，显著降低了 API 调用成本和推理延迟。
*   **无需外部依赖**：不依赖维基百科等外部知识库，适用于私有或实时场景。
*   **理论扎实**：将成熟的动力系统理论（Koopman 算子）引入 NLP 领域，提供了新颖的视角。
*   **可定制化**：通过简单的阈值校准即可适应不同用户的偏好。

### 8. 不足与局限
*   **仅限检测**：该方法只能识别出“这里有幻觉”，但无法像检索增强（RAG）那样提供正确的替代信息。
*   **依赖标注数据**：在 Phase 1（拟合阶段）仍需要少量带标注的事实/幻觉样本来训练转移算子。
*   **黑盒机制未完全解明**：虽然实验证明有效，但论文尚未从机理上完全解释为什么事实和幻觉的动力学轨迹会存在如此显著的流形差异。
*   **应用限制**：对于极短的文本（如仅几个 Token 的回答），动力学特征可能不够明显，导致检测效果下降。

（完）
