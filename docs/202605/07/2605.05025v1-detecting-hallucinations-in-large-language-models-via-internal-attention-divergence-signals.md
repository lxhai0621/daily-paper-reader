---
title: Detecting Hallucinations in Large Language Models via Internal Attention Divergence Signals
title_zh: 通过内部注意力散度信号检测大语言模型中的幻觉
authors: Gijs van Dijk
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.05025v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 通过内部注意力信号检测大模型幻觉
tldr: 本研究提出一种轻量级、单次推理的幻觉检测方法，旨在解决大语言模型生成中的不确定性量化问题。该方法通过计算注意力矩阵中各注意力头分布与均匀分布之间的KL散度，提取内部信号并利用逻辑回归探针进行分类。实验证明，该方法在多任务和多模型家族中均能有效预测答案正确性，且计算效率高，无需重复采样或外部模型辅助，为理解模型内部不确定性提供了可解释的白盒视角。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.05025v1/fig-001.webp\", \"caption\": \"Table 8: Experimental setup and hyperparameters. All settings apply to Llama, Mistral, and Qwen unless otherwise stated.\", \"page\": 14, \"index\": 1, \"width\": 952, \"height\": 823}, {\"url\": \"assets/figures/arxiv/2605.05025v1/fig-002.webp\", \"caption\": \"Table 7: Results on validation set after training a lasso regularization probe to predict correctness from attention divergence mean pooled across the whole generation. Within each dataset, the highest AUROC and accuracy and the lowest ECE are shown in bold. Results are reported as mean ± standard deviation over three random seeds and 5 stratified cross-validation folds.\", \"page\": 13, \"index\": 2, \"width\": 950, \"height\": 359}, {\"url\": \"assets/figures/arxiv/2605.05025v1/fig-003.webp\", \"caption\": \"Figure 3: Empirical cumulative distribution functions (ECDFs) of attention divergence values for correct and incorrect generations. Top figure are survival ECDFs P (KL ≥ x) computed at the p ∈ {90, 95, 99} percentiles of the token-level KL divergence between attention distributions and a uniform baseline. Solid lines correspond to correct generations, dashed lines are incorrect generations. Bottom figure is the difference between the survival ECDFs for correct and incorrect generations at p = 99, with shaded areas indicating confidence intervals (CI).\", \"page\": 8, \"index\": 3, \"width\": 415, \"height\": 394}, {\"url\": \"assets/figures/arxiv/2605.05025v1/fig-004.webp\", \"caption\": \"Table 1: Validation results after training a lassoregularized probe to predict correctness from attention divergence mean pooled across the generation. Within each dataset, the highest AUROC is shown in bold. Results are reported as mean ± standard deviation over three random seeds and five stratified cross-validation folds.\", \"page\": 4, \"index\": 4, \"width\": 392, \"height\": 358}, {\"url\": \"assets/figures/arxiv/2605.05025v1/fig-005.webp\", \"caption\": \"Table 2: AUROC on the HotpotQA dataset using Mistral-7B. Baseline results are taken from Bazarova et al. (2025). Our method is evaluated under the same model and dataset.\", \"page\": 5, \"index\": 5, \"width\": 435, \"height\": 290}, {\"url\": \"assets/figures/arxiv/2605.05025v1/fig-006.webp\", \"caption\": \"Table 9: All attention heads selected by the L1 probe across models, datasets, and random seeds. Values denote the number of seeds (out of 10) in which a head was selected. A dash (–) indicates that the head was not selected for that dataset.\", \"page\": 16, \"index\": 6, \"width\": 791, \"height\": 352}]"
motivation: 针对现有幻觉检测方法依赖多次采样或外部模型导致的高计算成本问题，寻求一种高效的内部信号检测方案。
method: 利用注意力头分布与均匀分布间的KL散度作为不确定性特征，并结合逻辑回归探针实现单次推理下的幻觉识别。
result: 实验显示注意力散度信号与答案准确性高度相关，且主要集中在模型的中间层以及命名实体和数字等关键事实词汇上。
conclusion: 内部注意力散度是一种高效、可解释且具有竞争力的白盒信号，能够作为大语言模型幻觉检测的可靠指标。
---

## 摘要
我们提出了一种轻量级且单次推理的不确定性量化方法，用于检测大语言模型中的幻觉。该方法利用注意力矩阵来估计不确定性，无需重复采样或外部模型。具体而言，我们测量了每个注意力头分布与均匀参考分布之间的 Kullback-Leibler 散度，并将这些特征用于逻辑回归探针。在多个数据集、任务类型和模型系列中，注意力散度对答案的正确性具有高度预测性，且其性能与现有的不确定性估计方法相当。我们发现该信号集中在中间层以及命名实体和数字等事实性标记上，这表明注意力动态为模型不确定性提供了一种高效且可解释的白盒信号。

## Abstract
We propose a lightweight and single-pass uncertainty quantification method for detecting hallucinations in Large Language Models. The method uses attention matrices to estimate uncertainty without requiring repeated sampling or external models. Specifically, we measure the Kullback-Leibler divergence between each attention head's distribution and a uniform reference distribution, and use these features in a logistic regression probe. Across multiple datasets, task types, and model families, attention divergence is highly predictive of answer correctness and performs competitively with existing uncertainty estimation methods. We find that this signal is concentrated in middle layers and on factual tokens such as named entities and numbers, suggesting that attention dynamics provides an efficient and interpretable white-box signal of model uncertainty.

---

## 论文详细总结（自动生成）

这篇论文提出了一种利用大语言模型（LLM）内部注意力机制来检测幻觉的新方法。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：LLM 经常生成看似流畅但事实错误的“幻觉”内容。现有的检测方法通常依赖于多次采样（如 Semantic Entropy）或外部模型验证，计算成本高且延迟大。此外，基于输出概率（Logits）的信号往往无法区分“自信的错误”和“可靠的知识”。
*   **核心问题**：能否通过模型单次前向传播中的内部信号（特别是注意力机制），找到一种轻量级、高效且可解释的不确定性量化指标？

### 2. 方法论
*   **核心思想**：利用注意力分布的“集中程度”来衡量模型的不确定性。直觉上，当模型拥有可靠知识时，注意力会集中在关键上下文标记上；而当模型不确定（可能产生幻觉）时，注意力分布会趋于弥散。
*   **关键技术细节**：
    *   **KL 散度计算**：计算每个注意力头产生的分布 $P$ 与均匀分布 $U$（代表最大不确定性）之间的 Kullback–Leibler (KL) 散度。公式简化为：$D_{KL}(P \parallel U) = \log T - H(P)$，其中 $T$ 是上下文窗口大小，$H(P)$ 是注意力分布的熵。
    *   **特征提取**：在生成答案的过程中，提取每一层、每个注意力头的平均 KL 散度，形成特征向量。
    *   **逻辑回归探针**：训练一个带有 **Lasso (L1) 正则化**的逻辑回归模型。L1 正则化用于筛选出对预测答案正确性最关键的少数注意力头，增强了模型的可解释性。
    *   **单次推理**：该方法仅需一次前向传播即可提取特征，无需重复采样。

### 3. 实验设计
*   **数据集**：涵盖了多种任务类型：
    *   **事实问答**：TriviaQA, TruthfulQA。
    *   **多步推理**：HotpotQA。
    *   **数学推理**：GSM8K。
*   **评估模型**：Llama-3.2-3B-Instruct, Qwen3-4B-Instruct, Mistral-7B-Instruct-v0.2。
*   **对比方法（Benchmarks）**：
    *   **基于输出的方法**：Perplexity, Max Entropy, Semantic Entropy, SelfCheckGPT。
    *   **基于内部信号的方法**：Hidden State Probing（隐状态探针）, TOHA（基于拓扑的检测）, Look-back Lens 等。
*   **评价指标**：主要使用 AUROC（衡量区分正确与错误答案的能力），辅助指标包括 ECE（预期校准误差）和准确率。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **效率强调**：作者多次强调该方法是“轻量级”的，因为逻辑回归探针的参数量极小，且特征提取过程与模型推理同步，计算开销几乎可以忽略不计。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 3 个不同家族的模型和 4 个不同性质的数据集上进行了交叉验证。
    *   使用了 3 个随机种子和 5 折分层交叉验证，确保了结果的统计稳定性。
*   **消融实验**：进行了多项消融研究，包括移除特定层/头、改变池化方式（均值 vs 最大值）、以及对比 Prompt 阶段与生成阶段的信号差异。
*   **充分性评价**：实验设计较为全面，不仅验证了有效性，还通过“健全性检查”（Sanity Checks）排除了生成长度、标点符号等干扰因素的影响，实验结论具有较强的说服力。

### 6. 主要结论与发现
*   **高效预测性**：注意力散度信号在所有测试任务中均能有效预测答案正确性，AUROC 在 TruthfulQA 上甚至超过了 0.90。
*   **信号定位**：不确定性信号主要集中在模型的**中间层**。
*   **事实锚点**：注意力散度的异常波动主要发生在生成**命名实体（Named Entities）和数字**等事实性标记时，而停用词或标点符号的信号较弱。
*   **早期预兆**：部分不确定性信号在处理 Prompt 阶段就已经存在，说明幻觉不仅源于生成过程，也源于对输入理解的不确定性。

### 7. 优点
*   **极低开销**：单次前向传播即可完成，适合实时部署。
*   **白盒透明**：相比黑盒检测，该方法揭示了模型内部注意力机制与事实可靠性之间的联系。
*   **泛化性强**：在不同架构（Llama, Qwen, Mistral）和不同任务（问答、数学）上均表现稳健。
*   **无需外部资源**：不需要调用更强的模型（如 GPT-4）作为评判者。

### 8. 不足与局限
*   **依赖探针训练**：虽然特征是通用的，但仍需要少量带标签的数据来训练逻辑回归探针，且不同模型/数据集选中的“关键头”存在差异。
*   **非因果性**：论文证明了注意力散度与幻觉的相关性，但尚未证明其因果关系（即：是注意力弥散导致了幻觉，还是幻觉过程伴随了注意力弥散）。
*   **白盒限制**：该方法要求访问模型的内部权重（注意力矩阵），因此无法应用于闭源 API 模型（如 GPT-4, Claude）。
*   **解释性挑战**：虽然 Lasso 筛选了注意力头，但具体为什么某些特定的头对幻觉敏感，仍缺乏深层的机理解释。

（完）
