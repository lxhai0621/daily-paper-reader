---
title: "ARGUS: Defending LLM Agents Against Context-Aware Prompt Injection"
title_zh: ARGUS：防御针对大语言模型（LLM）智能体的上下文感知提示注入攻击
authors: "Shihao Weng, Yang Feng, Jinrui Zhang, Xiaofei Xie, Jiongchi Yu, Jia Liu"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03378v1"
tags: ["query:agent"]
score: 9.0
evidence: 增强了工具使用、技能和外部知识的LLM智能体
tldr: "本研究针对LLM智能体面临的上下文感知提示注入攻击，指出了现有防御在动态环境下的局限。作者提出了AgentLure基准测试以模拟真实攻击，并开发了ARGUS防御机制。ARGUS通过构建影响溯源图追踪不可信上下文对决策的影响，并在执行前验证决策依据。实验表明，ARGUS在保持87.5%任务效用的同时，将攻击成功率降至3.8%，显著优于现有防御。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.03378v1/fig-001.webp\", \"caption\": \"Table 1: Comparison of prompt-injection benchmarks.\", \"page\": 7, \"index\": 1, \"width\": 830, \"height\": 226}, {\"url\": \"assets/figures/arxiv/2605.03378v1/fig-002.webp\", \"caption\": \"Figure 1: A representative case where an existing defense (Tool Filter) fails on a context-dependent task in AgentLure.\", \"page\": 3, \"index\": 2, \"width\": 498, \"height\": 295}, {\"url\": \"assets/figures/arxiv/2605.03378v1/fig-003.webp\", \"caption\": \"Figure 3: End-to-end overview of ARGUS on the bill-payment trace: agent pipeline (top), the four LLM-backed security tools (middle), and the evolving IPG (bottom). Cross-lane arrows mark how the IPG is written and read by the tools.\", \"page\": 8, \"index\": 3, \"width\": 826, \"height\": 368}, {\"url\": \"assets/figures/arxiv/2605.03378v1/fig-004.webp\", \"caption\": \"Figure 6: Per-vector ASR (%) on AgentLure under standard and adaptive (white-box) attacks.\", \"page\": 17, \"index\": 4, \"width\": 563, \"height\": 327}, {\"url\": \"assets/figures/arxiv/2605.03378v1/fig-005.webp\", \"caption\": \"Figure 2: The pipeline of a tool-augmented LLM agent.\", \"page\": 5, \"index\": 5, \"width\": 784, \"height\": 335}, {\"url\": \"assets/figures/arxiv/2605.03378v1/fig-006.webp\", \"caption\": \"Figure 5: Per-vector ASR (%) for ARGUS and the four ablation variants on AgentLure.\", \"page\": 16, \"index\": 6, \"width\": 574, \"height\": 504}]"
motivation: 现有的提示注入防御和基准测试忽略了真实世界中智能体决策对动态上下文的依赖，导致无法应对上下文感知的攻击。
method: 提出了ARGUS防御机制，通过构建影响溯源图追踪不可信信息的传播，并在执行前对智能体决策进行溯源审计。
result: "ARGUS将攻击成功率降低至3.8%，同时保留了87.5%的任务效用，在性能和鲁棒性上均优于现有防御手段。"
conclusion: ARGUS通过决策溯源审计有效解决了LLM智能体在复杂上下文环境下的安全风险，为构建可靠的智能体系统提供了新思路。
---

## 摘要
大语言模型（LLM）智能体的兴起，伴随着工具使用、技能和外部知识的增强，引入了新的安全风险。其中，提示注入攻击（即攻击者将恶意指令嵌入智能体工作流）已成为主要威胁。然而，现有的基准测试和防御措施存在根本局限性，因为它们假设了上下文不敏感的设置，即智能体在完全指定的效户指令下工作，且攻击是直接且与上下文无关的。因此，它们无法捕捉到现实世界的部署情况，在这些情况下，智能体的行为通常取决于动态上下文，而不仅仅是用户提示，且攻击者可以根据不同的上下文调整其攻击。同样，基于这种狭隘威胁模型构建的现有防御措施忽视了现实世界智能体委托的本质。在本文中，我们提出了 AgentLure，这是一个捕捉上下文相关任务和上下文感知提示注入攻击的基准测试。AgentLure 涵盖了四个智能体领域和跨越不同攻击面的八个攻击向量。我们的评估表明，现有的防御措施在这种设置下往往表现不佳，在智能体系统中对抗此类攻击的效果较差。为了解决这一局限性，我们提出了 ARGUS，这是一种为 LLM 智能体强制执行溯源感知决策审计的防御机制。ARGUS 构建了一个影响溯源图，以追踪不可信上下文如何传播到智能体决策中，并在执行前验证决策是否由可靠证据支撑。我们的评估显示，ARGUS 将攻击成功率降低至 3.8%，同时保留了 87.5% 的任务效用，显著优于现有防御措施，并对自适应白盒攻击者保持鲁棒性。

## Abstract
The rise of Large Language Model (LLM) agents, augmented with tool use, skills, and external knowledge, has introduced new security risks. Among them, prompt injection attacks, where adversaries embed malicious instructions into the agent workflow, have emerged as the primary threat. However, existing benchmarks and defenses are fundamentally limited as they assume context-insensitive settings in which the agent works under a fully specified user instruction, and the attacks are straightforward and context-independent. As a result, they fail to capture real-world deployments where agent behavior usually depends on dynamic context, not just the user prompt, and adversaries can adapt their attacks to different context. Similarly, existing defenses built on this narrow threat model overlook the nature of real-world agent delegation.   In this paper, we present AgentLure, a benchmark that captures context-dependent tasks and context-aware prompt injection attacks. AgentLure spans four agentic domains and eight attack vectors across diverse attack surfaces. Our evaluation shows that existing defenses often struggle in this setting, yielding poor performance against such attacks in agentic systems. To address this limitation, we propose ARGUS, a defense mechanism that enforces provenance-aware decision auditing for LLM agents. ARGUS constructs an influence provenance graph to track how untrusted context propagates into agent decisions and verify whether a decision is justified by trustworthy evidence before execution. Our evaluation shows ARGUS reduces attack success rate to 3.8% while preserving 87.5% task utility, significantly outperforming existing defenses and remaining robust against adaptive white-box adversaries.

---

## 论文详细总结（自动生成）

### 论文总结：ARGUS —— 防御针对大语言模型智能体的上下文感知提示注入攻击

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）智能体在自动化工作流（如财务处理、医疗管理）中的广泛应用，**间接提示注入（Indirect Prompt Injection）**已成为首要安全威胁。
*   **现有局限性：** 现有的基准测试和防御机制大多假设任务是“上下文无关”的（即用户指令已包含所有操作细节）。
*   **现实挑战：** 真实的智能体任务通常是**上下文相关**的（例如“支付我的电费账单”，具体金额和账号需从运行时读取的文件中获取）。攻击者可以实施**上下文感知攻击**，将恶意指令伪装成正常的业务数据（如账单中的备注），诱导智能体执行非法转账。
*   **研究目标：** 填补现有防御在处理动态上下文决策时的空白，确保智能体在依赖外部数据时仍能保持决策的完整性。

#### 2. 论文提出的方法论
作者提出了 **ARGUS**，一种运行时决策审计系统，其核心思想是**“溯源感知决策审计”**。

*   **核心数据结构：影响溯源图 (Influence-Provenance Graph, IPG)**
    *   记录所有运行时上下文条目（如工具返回、检索文档、内存记录）。
    *   为不同来源分配初始信任分，并根据审计结果动态调整。
*   **关键技术组件（四个 LLM 驱动的工具）：**
    1.  **内容分割器 (ContentSegmenter)：** 将观察到的内容细化为“良性（Benign）”和“异常（Anomalous）”片段。
    2.  **参数溯源器 (ArgumentGrounder)：** 追踪工具调用的每一个参数具体源自 IPG 中的哪个片段（拷贝、转换、推导或解析）。
    3.  **不变性检查器 (InvariantChecker)：** 在任务开始前基于用户原始查询生成 2-3 个核心安全约束（如“收款人必须是文档中提到的实体”），并在审计时验证。
    4.  **蕴含验证器 (EntailmentVerifier)：** 验证提议的操作是否仅由良性证据逻辑支撑，而非受异常片段误导。
*   **算法流程：**
    *   拦截所有状态改变的工具调用。
    *   执行**局部审计**（参数溯源）和**全局审计**（任务边界与逻辑验证）。
    *   若审计失败，拦截调用并返回**良性数据提示 (Hints)**，引导智能体使用正确数据重试。

#### 3. 实验设计
*   **数据集/场景：** 构建了 **AgentLure** 基准测试，涵盖银行、旅游、办公空间、Slack 四个领域。
*   **Benchmark 构成：** 包含 320 个样本，涉及 8 个攻击向量（如参数篡改、条件流劫持、技能注入等）和 6 个攻击面。
*   **对比方法：** 对比了 8 种代表性防御手段，包括：
    *   文本级：Delimiters, Sandwich Defense, Instructional Prevention。
    *   执行级：Tool Filter, InjecGuard, MELON, ACE, DRIFT。

#### 4. 资源与算力
*   **模型后端：** 智能体和 ARGUS 的四个审计工具均使用 **GPT-4o-mini**。
*   **算力说明：** 论文未明确提及具体的 GPU 硬件型号或训练时长。由于该研究主要基于 LLM API 进行推理和运行时拦截，不涉及大规模模型预训练，其成本主要体现在 API 调用产生的 Token 开销上（ARGUS 的 Token 开销约为基准线的 1.24 倍）。

#### 5. 实验数量与充分性
*   **实验规模：** 进行了 320 组攻击实验，并针对 8 种防御方法进行了横向对比。
*   **消融实验：** 逐一移除了四个核心组件（分割器、溯源器、蕴含验证、不变性检查），验证了各部分的必要性。
*   **鲁棒性测试：** 引入了**白盒自适应攻击 (AAE)**，模拟攻击者完全了解 ARGUS 架构和 Prompt 的极端情况。
*   **评价：** 实验设计全面且客观。引入了 **EDS (Effective Defense Score)** 指标，综合衡量了安全性（ASR）与任务效用（Utility），避免了通过“过度拦截”来刷低攻击成功率的偏差。

#### 6. 论文的主要结论与发现
*   **防御效果显著：** ARGUS 将攻击成功率 (ASR) 从 28.8% 降低至 **3.8%**，同时保留了 **87.5%** 的任务效用。
*   **现有防御失效：** 传统防御（如 Tool Filter 或 MELON）在上下文相关任务中表现不佳，要么无法识别伪装指令，要么因过度拦截导致任务失败。
*   **自适应鲁棒性：** 在白盒自适应攻击下，ARGUS 的 ASR 仅上升至 5.9%，证明了多层审计机制的协同防御能力。

#### 7. 优点
*   **细粒度审计：** 突破了传统的“观察级”信任，实现了“片段级”和“参数级”的溯源追踪。
*   **动态平衡：** 能够区分同一文档中的合法数据与恶意注入，在保证安全的同时不破坏智能体的正常功能。
*   **恢复机制：** 失败后的“良性数据提示”机制允许智能体自我修正，提高了系统的可用性。

#### 8. 不足与局限
*   **载体完整性依赖：** ARGUS 假设任务载体（如账单文件）本身是合法的。如果攻击者伪造了整个文件（如完全虚假的账单），且其中没有明显的指令性语言，ARGUS 可能无法识别。
*   **延迟与成本：** 每次状态改变的操作都需要额外的 LLM 调用进行审计，虽然使用了轻量级的 GPT-4o-mini，但在极高性能要求的场景下仍存在延迟压力。
*   **应用限制：** 目前主要针对提示注入，对于更广泛的智能体安全问题（如意图漂移、不安全工具组合）的覆盖尚需进一步扩展。

（完）
