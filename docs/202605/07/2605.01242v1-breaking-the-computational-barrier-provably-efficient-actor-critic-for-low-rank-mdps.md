---
title: "Breaking the Computational Barrier: Provably Efficient Actor-Critic for Low-Rank MDPs"
title_zh: 突破计算壁垒：低秩 MDP 的可证明高效 Actor-Critic 算法
authors: "Ruiquan Huang, Donghao Li, Yingbin Liang, Jing Yang"
date: 2026-05-02
pdf: "https://arxiv.org/pdf/2605.01242v1"
tags: ["query:agent"]
score: 9.0
evidence: 智能体学习最优策略的强化学习框架
tldr: 本研究针对强化学习中函数近似算法计算复杂度过高的问题，通过监督学习建立了低秩MDP中常用预言机的层级结构，发现策略评估是最高效的计算代理。基于此，提出了一种仅依赖策略评估的乐观演员-评论家算法。该算法在保证优异样本复杂度的同时，避免了传统方法中昂贵的规划或优化步骤，并成功扩展至近似低秩环境，通过Gym实验验证了其有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.01242v1/fig-001.webp\", \"caption\": \"Table 1. Comparison of algorithms for low-rank MDPs in terms of oracle structure, computational properties, and sample complexity. Oracle Types indicate the highest-level optimization oracle required (policy evaluation PE, policy planning PP, or constrained planning CP). Convexity and Smoothness refer to the regularity of the objective optimized by the corresponding oracle. Computation reports the best-known SL-oracle complexity required to implement the oracle, expressed using the summary SLm(ε) (Theorem 3.6). For clarity, coverage constants are omitted. All results are stated for low-rank MDPs with unknown features. Here d is the feature dimension, |A| is the action space size, and H is the horizon. Θ = Φ×Ψ is the parameter space. The accuracy parameter ε in the computation column corresponds (up to constants and logarithmic factors) to the inverse square root of the sample complexity.\", \"page\": 3, \"index\": 1, \"width\": 1016, \"height\": 354}, {\"url\": \"assets/figures/arxiv/2605.01242v1/fig-002.webp\", \"caption\": \"Figure 1. Evaluations over training. Steps before the dash line is uniform random exploration. y-axis is the value of one episode. Results are averaged across 4 different random seeds.\", \"page\": 29, \"index\": 2, \"width\": 1017, \"height\": 527}]"
motivation: 旨在解决现有低秩MDP强化学习算法在样本效率与计算可行性之间难以兼得的矛盾。
method: 提出一种新型乐观演员-评论家算法，该算法将计算负担简化为仅需执行策略评估预言机。
result: 算法在低秩MDP下实现了更优的样本复杂度，且在计算上比依赖规划预言机的传统方法更高效。
conclusion: 该研究打破了低秩MDP的计算壁垒，为开发可证明高效且计算可行的强化学习算法提供了理论支撑。
---

## 摘要
强化学习（RL）是序列决策的基础框架，其中智能体通过与未知环境的交互来学习最优策略。在具有函数逼近的设置中，许多现有的强化学习算法实现了良好的样本复杂度，但通常依赖于计算上难以处理的预言机（oracles）。在本文中，我们利用监督学习作为计算代理，在低秩马尔可夫决策过程（MDPs）下建立了常用强化学习预言机的清晰层级结构。该层级结构表明，只要监督学习能够被高效解决，策略评估就是计算效率最高的预言机。受此启发，我们提出了一种新型的乐观 Actor-Critic 算法，该算法仅依赖于策略评估预言机。我们证明了该算法在低秩 MDPs 上的表现优于现有的样本复杂度保证，同时避免了先前工作中通常假设的计算昂贵的规划或优化预言机。我们进一步将理论结果扩展到近似低秩 MDPs，并证明该设置涵盖了广泛的现实世界环境。最后，我们在多个标准 Gym 环境中通过实验验证了我们的理论结果。

## Abstract
Reinforcement learning (RL) is a fundamental framework for sequential decision-making, in which an agent learns an optimal policy through interactions with an unknown environment. In settings with function approximation, many existing RL algorithms achieve favorable sample complexity, but often rely on computationally intractable oracles. In this paper, we use supervised learning as a computational proxy to establish a clear hierarchy of commonly adopted RL oracles under low-rank Markov Decision Processes (MDPs). This hierarchy shows that policy evaluation is the most computationally efficient oracle, provided that supervised learning can be efficiently solved. Motivated by this observation, we propose a novel optimistic actor-critic algorithm that relies solely on the policy evaluation oracle. We prove that our algorithm outperforms the existing sample complexity guarantees for low-rank MDPs while avoiding computationally expensive planning or optimization oracles commonly assumed in prior works. We further extend our theoretical results to approximately low-rank MDPs and demonstrate that this setting captures a broad class of real-world environments. Finally, we validate our theoretical results with experiments on several standard Gym environments.

---

## 论文详细总结（自动生成）

### 论文总结：突破计算壁垒——低秩 MDP 的可证明高效 Actor-Critic 算法

#### 1. 核心问题与研究动机
在强化学习（RL）中，函数近似是处理大规模状态空间的关键。然而，现有的针对复杂结构（如低秩 MDP）的算法往往面临“理论与实践的脱节”：虽然它们在理论上具有很高的样本效率，但在计算上通常依赖于难以实现的“预言机”（Oracles），如策略规划（Policy Planning）或受限规划（Constrained Planning）。这些预言机在实际应用中往往是 NP 难的。
**研究动机**：能否设计一种算法，既能在低秩 MDP 中保持优秀的样本效率，又能在计算上仅依赖于易于实现的“监督学习（SL）预言机”？

#### 2. 方法论：Opt-AC 算法
论文提出了一种名为 **Opt-AC（Optimistic Actor-Critic）** 的算法，其核心思想是将复杂的强化学习优化问题简化为一系列监督学习任务。

*   **预言机层级结构**：论文首先证明了计算复杂度的层级：监督学习 (SL) < 策略评估 (PE) < 策略规划 (PP) < 受限规划 (CP)。Opt-AC 成功将需求降至 PE 级别，而 PE 可以高效地还原为 SL。
*   **核心技术细节**：
    1.  **表示学习**：利用最大似然估计（MLE）从交互数据中学习低秩分解的潜在特征 $\phi$ 和 $\mu$。
    2.  **乐观评论家（Optimistic Critic）**：通过经验格拉姆矩阵（Gram Matrix）构建探索奖励（Bonus），并将其加入奖励函数中。利用 PE 预言机估计当前策略在增强奖励下的 $Q$ 值。
    3.  **演员更新（Actor Update）**：采用 KL 正则化的镜像下降（Mirror Descent）更新策略。这种更新方式具有闭式解（Softmax 形式），避免了全局规划。
*   **理论突破**：证明了该算法的样本复杂度为 $\tilde{O}(H^5 d^3 |A|^2 \log |\Theta| / \epsilon^2)$。相比之前最好的结果，在特征维度 $d$ 上实现了 $d$ 倍的提升。

#### 3. 实验设计
*   **实验场景**：使用了标准的连续控制基准测试——**Gym/MuJoCo** 环境，包括 `Ant`、`HalfCheetah`、`Hopper`、`Reacher`、`Pendulum` 和 `InvertedPendulum`。
*   **对比方法（Benchmark）**：
    *   **SAC (Soft Actor-Critic)**：作为强力的基准算法。
    *   **cRFFSAC (Ours)**：基于条件随机傅里叶特征（cRFF）的 Opt-AC 变体，不带显式探索奖励。
    *   **cRFFSAC + Bonus (Ours)**：完整的 Opt-AC 算法，包含基于学习表示的探索奖励。

#### 4. 资源与算力
*   论文中**未明确说明**具体的硬件配置（如 GPU 型号、数量）及总训练时长。
*   文中提到实现是基于开源代码库 `lvrep-rl` 构建的，并使用了多层感知机（MLP）作为网络架构。

#### 5. 实验数量与充分性
*   **实验规模**：在 6 个不同的 MuJoCo 环境中进行了测试。
*   **重复性**：每个实验均使用了 **4 个不同的随机种子**进行平均，并报告了均值和标准差。
*   **充分性评价**：实验覆盖了从简单（Pendulum）到复杂（Ant）的不同难度环境，验证了算法在处理稀疏奖励和高维控制任务时的有效性。虽然 4 个种子在强化学习中属于标准配置，但若能增加更多种子或更多样化的基准（如 DeepMind Control Suite）会更具说服力。

#### 6. 主要结论与发现
*   **计算可行性**：Opt-AC 是首个在低秩 MDP 下既能保证样本效率，又能在计算上完全还原为监督学习的算法。
*   **性能优越性**：在大多数任务中，Opt-AC（尤其是带 Bonus 的版本）的表现优于或等同于 SAC，特别是在需要高效探索的任务（如 `Ant` 和 `HalfCheetah`）中表现突出。
*   **理论扩展**：论文证明了近似低秩结构可以涵盖如高斯转换核等广泛的现实环境，通过 cRFF 技术可以有效地进行特征近似。

#### 7. 优点
*   **理论与实践的桥梁**：通过引入 SL 预言机层级，为强化学习算法的计算效率评估提供了新的标准。
*   **样本效率提升**：在低秩 MDP 这一重要模型下，将样本复杂度对维度 $d$ 的依赖从 $d^4$ 优化到了 $d^3$。
*   **算法简洁性**：Actor 更新采用镜像下降，避免了复杂的受限优化，易于在深度学习框架中实现。

#### 8. 不足与局限
*   **覆盖率假设**：算法依赖于采样预言机 $\rho$ 能够覆盖最优策略的占用度量，在某些极端探索困难的环境中，这个覆盖常数 $C$ 可能会非常大。
*   **模型类假设**：理论分析假设模型空间 $\Theta$ 是有限的（虽然提到可以扩展到无限空间），但在处理极度复杂的非线性动力学时，函数类的选择仍具挑战。
*   **超参数敏感性**：乐观探索奖励的系数 $\alpha$ 和学习率 $\eta$ 等超参数对性能影响较大，实际调参可能需要一定经验。

（完）
