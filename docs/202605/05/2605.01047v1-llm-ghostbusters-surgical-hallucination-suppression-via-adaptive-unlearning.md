---
title: "LLM Ghostbusters: Surgical Hallucination Suppression via Adaptive Unlearning"
title_zh: LLM Ghostbusters：通过自适应遗忘进行精准幻觉抑制
authors: "Joseph Spracklen, Pedram Aghazadeh, Farinaz Koushanfar, Murtuza Jadliwala"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.01047v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 通过自适应遗忘精准抑制大模型幻觉
tldr: "针对大语言模型在代码生成中虚构软件包导致的“包混淆”安全风险，本文提出自适应遗忘（AU）框架。该框架通过混合令牌级目标函数同时抑制幻觉并强化正确输出，并利用自适应发现循环在无需人工干预的情况下持续识别幻觉诱导上下文。实验表明，AU在不损害通用编程能力的前提下，将软件包幻觉率降低了81%，为部署后模型的幻觉治理提供了高效且精准的解决方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.01047v1/fig-001.webp\", \"caption\": \"Figure 2: Token-level tri-masking applies targeted loss functions across every sample for precise suppression and reinforcement.\", \"page\": 6, \"index\": 1, \"width\": 940, \"height\": 198}, {\"url\": \"assets/figures/arxiv/2605.01047v1/fig-002.webp\", \"caption\": \"Figure 5: Token-level probabilities during AU for the prompt \\\"How could I write a high-performance Python web framework that supports bothWSGI and ASGI, optimized for large-scale API workloads\\\". Themodel initially recommends two hallucinated packages (orange) that sound plausible for the prompt, but are actually hallucinated. As epochs increase, these packages are suppressed while valid packages (blue) are reinforced. This is a real sample with actual values from our data.\", \"page\": 11, \"index\": 2, \"width\": 934, \"height\": 266}, {\"url\": \"assets/figures/arxiv/2605.01047v1/fig-003.webp\", \"caption\": \"Figure 6: Effect of inner training epochs on hallucination suppression, coding utility, and distributional drift.\", \"page\": 11, \"index\": 3, \"width\": 518, \"height\": 289}, {\"url\": \"assets/figures/arxiv/2605.01047v1/fig-004.webp\", \"caption\": \"Table 2: Ablation over loss composition. All three evaluation axes reported jointly as detailed in §6.0.1, using the same baseline metrics as Table 1. Best is in bold and second-best is underlined per column.\", \"page\": 11, \"index\": 4, \"width\": 836, \"height\": 269}, {\"url\": \"assets/figures/arxiv/2605.01047v1/fig-005.webp\", \"caption\": \"Figure 3: Nested outer/inner epoch structure. Nested outer/inner epoch structure. Outer epochs regenerate samples and invoke prompt mutation; inner epochs train on cached samples via the tri-masked hybrid objective, stabilizing the unlearning loop. This process is detailed in §3.5.\", \"page\": 7, \"index\": 5, \"width\": 454, \"height\": 802}, {\"url\": \"assets/figures/arxiv/2605.01047v1/fig-006.webp\", \"caption\": \"Table 1: The main results table includes the total hallucination rate as a percentage (lower is better) as well as the absolute difference in hallucination rate compared to baseline (higher is better). The KL-divergence results are shown for each of the coding, instruction-following, and package recommendation queries, measured in nats. The last five columns document code benchmark scores, measured at pass@1 rate. Findings from this table are discussed in detail in §5.1. Best is in bold and second-best is underlined per column.\", \"page\": 10, \"index\": 6, \"width\": 810, \"height\": 350}, {\"url\": \"assets/figures/arxiv/2605.01047v1/fig-007.webp\", \"caption\": \"Figure 1: Adaptive Unlearning pipeline. Adaptive Unlearning pipeline. The four-stage discovery loop, Sampling, Detection, Targeted Unlearning, and Prompt Mutation, iteratively suppresses hallucinated package tokens while preserving general coding utility. The full method is detailed in §3.\", \"page\": 3, \"index\": 7, \"width\": 957, \"height\": 326}, {\"url\": \"assets/figures/arxiv/2605.01047v1/fig-008.webp\", \"caption\": \"Figure 8: Visual depiction of the package detection pipeline to test for package hallucinations, as described in §4.3: Each coding prompt is passed to the model to produce code, then dispatched to two distinct package-elicitation queries: Mode 1 asks for the packages required to run the generated code, andMode 2 asks for packages that would be useful in solving the original task. Each resulting package list is parsed, de-duplicated, and resolved against a snapshot of the PyPI registry; any identifier that fails to resolve is labeled as a hallucination and routed into the tri-mask construction (§4.3).\", \"page\": 17, \"index\": 8, \"width\": 981, \"height\": 215}, {\"url\": \"assets/figures/arxiv/2605.01047v1/fig-009.webp\", \"caption\": \"Figure 7: Effect of adaptive prompt mutation count on hallucination rate, coding utility, and distributional drift.\", \"page\": 12, \"index\": 9, \"width\": 518, \"height\": 283}, {\"url\": \"assets/figures/arxiv/2605.01047v1/fig-010.webp\", \"caption\": \"Figure 4: AU training metrics across 40 epochs. Hallucination rate drops 81% while coding benchmarks remain stable. KL-divergence is concentrated in the package distribution, confirming surgical targeting. Note that the exact numbers in the figure differ slightly from our best result noted in Table 1, as slightly different hyperparameters were used.\", \"page\": 9, \"index\": 10, \"width\": 518, \"height\": 284}, {\"url\": \"assets/figures/arxiv/2605.01047v1/fig-011.webp\", \"caption\": \"Table 3: Per-run hyperparameter configurations. LR = learning rate; Outer / Inner = number of outer and inner epochs (notation 𝑁planned when the run hit the 24-hour wall-clock limit for 7Bmodels and 12-hour wall-clock limit for 16Bmodels the checkpoint that yielded best results is mentioned). 𝜆retain, 𝜆forget, 𝜆reg are the loss-component weights from §3.4; – indicates the term is not used by that method. All runs use full-parameter fine-tuning. Method names follow §4.5.\", \"page\": 16, \"index\": 11, \"width\": 736, \"height\": 409}]"
motivation: 解决大模型在代码生成中虚构软件包导致的“包混淆”安全漏洞，以及现有微调方法成本高或损害模型通用能力的问题。
method: 提出自适应遗忘（AU）框架，结合混合令牌级目标函数和自动发现循环，在无需人工标注的情况下精准抑制幻觉并强化正确输出。
result: "该方法将软件包幻觉率降低了81%，显著缩小了攻击面，同时在标准编程基准测试中保持了模型性能。"
conclusion: AU 证明了通过针对性的分布调整可以有效且低成本地消除特定幻觉，为提升部署后模型的安全性和可靠性提供了通用方案。
---

## 摘要
幻觉（即听起来合理但事实错误的输出）仍然是已部署大语言模型（LLM）面临的一个公开挑战。在代码生成中，模型经常会幻觉出不存在的软件包，推荐虚构库的导入和安装命令。这造成了关键的供应链漏洞：攻击者可以在公共注册中心主动注册此类包含恶意负载的软件包，随后这些包会被开发者或自主智能体安装并执行，这类软件包混淆攻击被称为“slopsquatting”。一旦模型部署，缓解这种故障模式就变得非常困难：全面重新训练成本高昂，而现有方法要么导致模型效用严重下降，要么依赖于预先指定的遗忘集，而这一假设并不适用于无限的幻觉空间。为了解决这个问题，我们提出了自适应遗忘（Adaptive Unlearning, AU），这是一个部署后框架，能够在保留通用模型效用的同时，精准地抑制幻觉。AU 引入了一种混合 Token 级目标，同时强化有效输出并抑制幻觉输出。结合无需人工监督即可持续发现新的诱导幻觉上下文的自适应发现循环，AU 实现了对未见提示词和幻觉的泛化。我们证明了 AU 将软件包幻觉率降低了 81%，对应于 slopsquatting 攻击面的大幅减少，同时保持了在标准编程基准测试上的性能。我们的分析表明，分布变化集中在与软件包相关的生成上，基本不影响通用的编程行为，证实了 AU 的效果被隔离在目标分布中。AU 完全基于模型生成的数据运行，无需人工标注，且可跨领域泛化。

## Abstract
Hallucinations, outputs that sound plausible but are factually incorrect, remain an open challenge for deployed LLMs. In code generation, models frequently hallucinate non-existent software packages, recommending imports and installation commands for fictional libraries. This creates a critical supply-chain vulnerability: an attacker can proactively register such packages on public registries with malicious payloads that are subsequently installed and executed by developers or autonomous agents, a class of package confusion attack known as slopsquatting. Once a model is deployed, mitigating this failure mode is difficult: full retraining is costly, and existing approaches either cause severe degradation of model utility or rely on a pre-specified forget-set, an assumption that does not apply to the unbounded space of hallucinations. To address this problem, we present Adaptive Unlearning (AU), a post-deployment framework that surgically suppresses hallucinations while preserving general model utility. AU introduces a hybrid token-level objective that simultaneously reinforces valid outputs and suppresses hallucinated ones. Combined with an adaptive discovery loop that continuously surfaces new hallucination-inducing contexts without human supervision, AU enables generalization to unseen prompts and hallucinations. We demonstrate that AU reduces package hallucination rates by 81%, corresponding to a substantial reduction in slopsquatting attack surface, while maintaining performance on standard coding benchmarks. Our analysis shows that distributional changes are concentrated on package-related generations, leaving general coding behavior largely unaffected and confirming that AU's effect is isolated to the targeted distribution. AU operates entirely on model-generated data, requires no human annotation, and generalizes across domains.

---

## 论文详细总结（自动生成）

### 论文总结：LLM Ghostbusters - 通过自适应遗忘进行精准幻觉抑制

#### 1. 论文的核心问题与整体含义
*   **研究动机**：大语言模型（LLM）在生成代码时经常虚构不存在的软件包（Package Hallucination）。这不仅是质量问题，更是严重的**供应链安全漏洞**。攻击者可以预测并注册这些虚构的包名（称为 **slopsquatting** 攻击），诱导开发者或自动化代理下载恶意代码。
*   **核心挑战**：现有的缓解方案存在局限性。全面重新训练成本太高；传统的“机器遗忘”（Machine Unlearning）通常需要预先定义的固定遗忘集，且容易导致模型通用能力（如编程逻辑）崩溃；而推理时检测则增加了延迟和系统复杂度。
*   **研究目标**：开发一种在模型部署后，能够精准抑制特定幻觉行为、无需人工标注、且不损害模型原有能力的微调框架。

#### 2. 论文提出的方法论：自适应遗忘（Adaptive Unlearning, AU）
AU 框架采用闭环系统，核心思想是将幻觉抑制视为一种**针对性的分布坍缩（Targeted Distribution Collapse）**。
*   **自适应提示词变异（Adaptive Prompt Mutation）**：为了解决幻觉空间无限的问题，系统会自动变异初始提示词，持续诱导模型产生新的幻觉上下文，确保模型学习的是“抑制模式”而非死记硬背。
*   **三值掩码令牌路由（Tri-mask Token Routing）**：这是 AU 的关键技术。它将生成的 Token 流分为三类并应用不同的损失函数：
    1.  **掩码 0（正则化）**：针对上下文和代码结构 Token，使用交叉熵（CE）损失并锚定到原模型，防止参数漂移。
    2.  **掩码 1（强化）**：针对经 Oracle（如 PyPI 注册表）验证正确的包名，使用 CE 损失进行强化。
    3.  **掩码 2（抑制）**：针对虚构的包名，使用**负偏好优化（NPO）**损失进行抑制。
*   **嵌套训练循环（Nested Training Loops）**：
    *   **外循环**：负责重新采样和提示词变异，捕捉模型演进中的新幻觉。
    *   **内循环**：在缓存的样本上进行多次迭代，帮助模型巩固抑制信号，解决采样不稳定性问题。

#### 3. 实验设计
*   **实验场景**：专注于 Python 编程中的软件包幻觉。
*   **数据集/提示词**：从现有基准中精选 20 个复杂的编程提示词作为种子，通过变异生成大量训练样本。
*   **评估基准（Benchmark）**：
    *   **幻觉率（HR）**：通过 PyPI 官方注册表实时验证。
    *   **编程能力**：使用 **EvalPlus**（包含 HumanEval+ 和 MBPP+），这是比原始基准更严格的测试集。
    *   **分布偏移**：测量模型在代码、指令遵循和包推荐三个维度的 **KL 散度**。
*   **对比方法**：梯度上升（GA）、负偏好优化（NPO）、部分模型坍缩（PMC）以及原始基座模型。
*   **测试模型**：DeepSeek-Coder-7B（稠密模型）和 DeepSeek-Coder-V2-Lite（16B MoE 模型）。

#### 4. 资源与算力
*   **硬件**：使用了 **AMD MI210 和 MI300 GPU**。
*   **训练细节**：采用**全参数微调**（而非 LoRA 等参数高效方法）。
*   **时长**：论文提到 7B 模型运行约 24 小时，16B 模型运行约 12 小时（受限于集群墙钟时间限制）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了两种不同架构的模型（稠密 vs MoE），进行了 40 个 Epoch 以上的训练观察。
*   **消融实验**：非常充分。分别测试了“仅强化（CE-Only）”、“仅抑制（NPO-Only）”、不同内循环次数（1 到 20 次）以及不同变异次数对结果的影响。
*   **客观性**：通过 KL 散度量化了模型在非目标领域的偏移，证明了抑制行为的“精准性”，实验设计考虑了效用与安全性的权衡，较为公平。

#### 6. 论文的主要结论与发现
*   **大幅降低幻觉**：在 DeepSeek-7B 上，幻觉率从 21.2% 降至 **2.5%**（降低了 88%）；在 16B 模型上也有显著提升。
*   **效用保持**：在 HumanEval+ 和 MBPP+ 上的得分基本持平甚至略有上升，证明 AU 不会破坏模型的编程逻辑。
*   **分布隔离**：KL 散度分析显示，模型的改动几乎完全集中在“包名生成”这一特定分布上，代码结构分布保持稳定。
*   **混合目标的必要性**：消融实验显示，单纯抑制幻觉或单纯强化正确答案的效果均远不如两者结合。

#### 7. 优点
*   **无需人工干预**：完全依赖模型自生成数据和自动化 Oracle（PyPI），极大地降低了维护成本。
*   **精准手术式修改**：通过 Tri-mask 实现了 Token 级别的精细控制，避免了传统遗忘方法导致的“模型变傻”现象。
*   **闭环泛化**：自适应变异机制让模型能够应对未见过的提示词，具有更强的鲁棒性。

#### 8. 不足与局限
*   **语义验证缺失**：目前的 Oracle 仅验证包名是否存在，不验证该包在代码逻辑中是否被正确使用。
*   **语言覆盖有限**：实验主要集中在 Python，虽然理论上可扩展到 npm (JS) 或 C++，但尚未在其他生态系统中验证。
*   **MoE 架构挑战**：在 MoE 模型上，由于参数路由的稀疏性，抑制信号的巩固比稠密模型更难，导致效用保持略逊于稠密模型。
*   **算力需求**：全参数微调对算力有一定要求，对于超大规模模型（如 70B+）的适用性有待进一步验证。

（完）
