---
title: "GRAIL: A Deep-Granularity Hybrid Resonance Framework for Real-Time Agent Discovery via SLM-Enhanced Indexing"
title_zh: GRAIL：一种基于 SLM 增强索引的实时智能体发现深粒度混合共振框架
authors: Jinliang Xu
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02489v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 实时智能体发现与多智能体协作框架
tldr: 针对大规模多智能体协作中智能体发现效率与精度难以兼得的问题，本文提出GRAIL框架。该框架通过轻量化小模型（SLM）进行意图解析，结合伪文档扩展增强语义密度，并利用MaxSim共振机制实现细粒度匹配。在AgentTaxo-9K数据集上的实验表明，GRAIL在保持高精度的同时，将发现延迟降至400毫秒以下，比传统LLM方案快79倍以上，为实时智能体发现提供了工业级解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.02489v1/fig-001.webp\", \"caption\": \"Fig. 4: Multi-dimensional distribution analysis of tags (Top: Tag word cloud; Bottom: Log-transformed frequency trend)\", \"page\": 6, \"index\": 1, \"width\": 500, \"height\": 650}, {\"url\": \"assets/figures/arxiv/2605.02489v1/fig-002.webp\", \"caption\": \"Fig. 2: The Overall Architecture of the GRAIL Framework.\", \"page\": 3, \"index\": 2, \"width\": 525, \"height\": 407}, {\"url\": \"assets/figures/arxiv/2605.02489v1/fig-003.webp\", \"caption\": \"Fig. 3: Workflow of the GRAIL Online Discovery Process.\", \"page\": 5, \"index\": 3, \"width\": 516, \"height\": 408}]"
motivation: 现有的智能体发现方法在重型LLM解析带来的高延迟与传统向量检索导致的低语义精度之间存在难以调和的矛盾。
method: 提出GRAIL框架，通过SLM预测能力标签、伪文档扩展增强语义密度以及MaxSim机制进行细粒度匹配。
result: 在AgentTaxo-9K数据集上，GRAIL实现了低于400ms的延迟，比LLM基准快79倍以上，且召回率显著提升。
conclusion: GRAIL为实现大规模、实时的“智能体互联网”提供了一种兼顾高性能与高精度的可扩展工业级解决方案。
---

## 摘要
随着基于大语言模型（LLM）的智能体生态系统迅速扩张，高效且准确的智能体发现已成为大规模多智能体协作的关键瓶颈。现有方法通常面临两难境地：要么依赖重量级 LLM 进行意图解析，导致极高的延迟（通常超过 30 秒）；要么使用单体向量检索，以牺牲语义精度为代价换取速度。为了弥补这一差距，我们提出了 GRAIL（Granular Resonance-based Agent/AI Link），这是一个在不牺牲准确性的情况下实现低于 400 毫秒发现延迟的新型框架。GRAIL 引入了三项关键创新：（1）SLM 增强预测，使用专门微调的小语言模型（SLM）取代通用 LLM 解析器，实现毫秒级的能力标签预测；（2）伪文档扩展，通过合成查询增强智能体描述，以提高稠密检索的语义密度；（3）MaxSim 共振，一种细粒度匹配机制，计算用户查询与离散智能体使用示例之间的最大相似度，有效缓解了语义稀释。在包含 9,240 个智能体的新型大规模数据集 AgentTaxo-9K 上的验证表明，与基于 LLM 解析的基准相比，GRAIL 将端到端发现延迟降低了 79 倍以上，同时在 Recall@10 指标上显著优于传统的向量搜索。该框架为实时“智能体互联网”提供了一种可扩展的工业级解决方案。

## Abstract
As the ecosystem of Large Language Model (LLM)-based agents expands rapidly, efficient and accurate Agent Discovery becomes a critical bottleneck for large-scale multi-agent collaboration. Existing approaches typically face a dichotomy: either relying on heavy-weight LLMs for intent parsing, leading to prohibitive latency (often exceeding 30 seconds), or using monolithic vector retrieval that sacrifices semantic precision for speed. To bridge this gap, we propose \textbf{GRAIL} (Granular Resonance-based Agent/AI Link), a novel framework achieving sub-400ms discovery latency without compromising accuracy. GRAIL introduces three key innovations: (1) \textbf{SLM-Enhanced Prediction}, replacing the generalized LLM parser with a specialized, fine-tuned Small Language Model (SLM) for millisecond-level capability tag prediction; (2) \textbf{Pseudo-Document Expansion}, augmenting agent descriptions with synthetic queries to enhance semantic density for robust dense retrieval; and (3) \textbf{MaxSim Resonance}, a fine-grained matching mechanism computing maximum similarity between user queries and discrete agent usage examples, effectively mitigating semantic dilution. Validated on \textbf{AgentTaxo-9K}, our new large-scale dataset of 9,240 agents, GRAIL reduces end-to-end discovery latency by over \textbf{79$\times$} compared to LLM-parsing baselines, while significantly outperforming traditional vector search in Recall@10. This framework offers a scalable, industrial-grade solution for the real-time ``Internet of Agents."

---

## 论文详细总结（自动生成）

### 论文总结：GRAIL 实时智能体发现框架

#### 1. 核心问题与研究动机
随着“智能体互联网”（Internet of Agents）概念的兴起，如何在海量候选池中实时、准确地发现匹配用户意图的智能体（Agent Discovery）成为关键瓶颈。
*   **现有痛点**：
    *   **LLM 解析方案**：依赖重型大模型解析意图，延迟极高（常超过 30 秒），无法满足实时交互需求。
    *   **单体向量检索**：将智能体描述压缩为单一向量，导致“语义稀释”（Semantic Dilution），难以捕捉细粒度功能差异，精度较低。
*   **研究目标**：在保证高精度的前提下，将发现延迟降低至亚秒级（<400ms）。

#### 2. 核心方法论
GRAIL 框架引入了“深粒度混合共振”机制，将发现过程解耦为三个维度：
*   **离线索引构建（三维索引）**：
    1.  **稀疏索引（Tags）**：提取显式能力标签，构建倒排索引。
    2.  **上下文索引（Context）**：利用 LLM 生成合成查询（Pseudo-Document Expansion），增强智能体描述的语义密度，解决词汇不匹配问题。
    3.  **意图索引（Intent）**：将智能体的多个使用示例（Examples）编码为嵌入矩阵，而非单一向量，保留离散的能力信号。
*   **在线发现流程**：
    1.  **SLM 增强预测**：使用微调的小语言模型（SLM，如 Qwen2.5-3B）在毫秒内预测查询标签。
    2.  **混合召回**：并行执行标签过滤（稀疏路径）和向量搜索（稠密路径），获取候选集。
    3.  **MaxSim 共振重排**：计算用户查询向量与智能体示例矩阵之间的最大余弦相似度（MaxSim）。该机制在数学上证明了即使在多功能智能体中，也能保持强信号，避免语义稀释。
*   **评分公式**：最终得分 $S_{final}$ 是上下文得分与共振得分的加权和。

#### 3. 实验设计
*   **数据集**：构建了 **AgentTaxo-9K**，包含 154 个行业、924 个子领域、9,240 个智能体及 27,720 条用户查询。该数据集遵循 MECE（相互独立，完全穷尽）原则。
*   **基准方法（Baselines）**：
    *   **MDR**：传统的单体稠密检索（基于均值池化向量）。
    *   **SLM-Sort**：仅依赖 SLM 预测标签进行检索。
    *   **LLM-Router**：使用重型 LLM（如 Gemini 3 级别）进行意图解析和路由。
*   **评价指标**：Recall@1、Recall@10、MRR@10 以及端到端延迟。

#### 4. 资源与算力
*   **硬件环境**：单台服务器，配备 1 块 **NVIDIA T4 GPU** (15GB VRAM)，12.7GB 系统内存。
*   **软件环境**：Python 3.12, PyTorch 2.1。
*   **模型细节**：
    *   **SLM**：Qwen2.5-3B-Instruct（使用 LoRA 微调）。
    *   **Embedding**：BGE-Small-EN-v1.5（33M 参数，384 维向量）。
*   **训练时长**：文中未明确给出具体的训练总时长，但提到 SLM 推理时间约为 370ms。

#### 5. 实验数量与充分性
*   **实验规模**：在 9,240 个智能体的大规模池中进行全量检索测试，而非小范围重排，具有较高的实战参考价值。
*   **消融实验**：针对 MaxSim 机制和 SLM 预测分别做了消融测试，验证了各组件的贡献。
*   **客观性**：通过数学定理（Theorem 1）证明了语义稀释现象，并用实验数据支撑了理论推导，实验设计较为严谨、公平。

#### 6. 主要结论与发现
*   **性能卓越**：GRAIL 的 Recall@10 达到 **91.45%**，显著优于 MDR（76.77%）和 LLM 路由方案。
*   **极低延迟**：端到端延迟仅为 **394.48ms**，比 LLM 解析方案快了 **79 倍以上**。
*   **克服稀释**：实验证实，传统的均值池化向量在面对“多功能”智能体时，信号会随功能增加而线性衰减，而 MaxSim 机制能有效保持信号强度。

#### 7. 优点与亮点
*   **理论与实践结合**：不仅提出了框架，还给出了语义稀释问题的数学证明。
*   **工业级可用性**：在廉价硬件（T4 GPU）上实现了高性能，证明了方案的经济性和可扩展性。
*   **新基准贡献**：发布的 AgentTaxo-9K 数据集填补了大规模、层级化智能体发现基准的空白。

#### 8. 不足与局限
*   **语言局限性**：实验主要基于英文模型（BGE-Small-EN），在多语言环境下的表现尚待验证。
*   **标签依赖**：SLM 的预测精度受限于预定义的标签体系，若行业分类发生剧烈变化，可能需要重新微调 SLM。
*   **示例质量敏感**：MaxSim 机制高度依赖智能体元数据中“使用示例”的质量，若示例描述模糊，共振效果可能打折扣。

（完）
