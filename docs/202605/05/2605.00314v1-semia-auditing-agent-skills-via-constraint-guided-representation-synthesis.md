---
title: "Semia: Auditing Agent Skills via Constraint-Guided Representation Synthesis"
title_zh: Semia：通过约束引导的表示合成审计智能体技能
authors: "Hongbo Wen, Ying Li, Hanzhi Liu, Chaofan Shou, Yanju Chen, Yuan Tian, Yu Feng"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00314v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 审计用于执行Shell命令等接口的智能体技能
tldr: Semia是一个针对大模型智能体技能的静态审计工具。针对技能中结构化接口与非结构化描述共存导致的审计难题，Semia通过约束引导的表示合成（CGRS）技术，将技能转化为可审计的Datalog事实库。该工具能有效检测间接注入、隐私泄露等安全风险。在对1.3万余个真实技能的评估中，Semia表现出极高的召回率和准确性，揭示了现有智能体生态中普遍存在的安全隐患。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.00314v1/fig-001.webp\", \"caption\": \"Figure 1: Motivating example: the clawnads skill (a) evades community review, VirusTotal, and direct-LLM analysis; Semia (b) lifts it into SDL facts and proves an unsanitized path from external context to the ungated crypto_sign sink.\", \"page\": 4, \"index\": 1, \"width\": 996, \"height\": 373}, {\"url\": \"assets/figures/arxiv/2605.00314v1/fig-002.webp\", \"caption\": \"Figure 2: High-level workflow of Semia. Phase 1 lifts an agent skill 𝑠 into an SDL fact base 𝑝∗ via a constrained refinement\", \"page\": 4, \"index\": 2, \"width\": 477, \"height\": 272}]"
motivation: 现有的智能体技能审计工具难以同时处理结构化接口和概率性的自然语言描述，导致无法可靠地证明安全漏洞。
method: 提出约束引导的表示合成（CGRS）方法，通过提议-验证-评估循环将技能转化为SDL语言和Datalog事实库，实现可复现的安全查询。
result: "在13,728个真实技能的测试中，Semia发现超过半数存在严重语义风险，并在专家标注样本上达到97.7%的召回率和90.6%的F1值。"
conclusion: Semia证明了结合大模型合成能力与形式化验证方法可以有效审计智能体技能，为构建安全的智能体生态提供了重要工具。
---

## 摘要
智能体技能（agent skill）是一种配置包，它为大语言模型（LLM）驱动的智能体提供具体能力，例如读取电子邮件、执行 shell 命令或签署区块链交易。每个技能都是一个混合产物——结构化部分声明可执行接口，而文本部分规定了这些接口触发的时间和方式——且文本在每次调用时都会被概率性地重新解释。传统的静态分析器解析结构化部分但忽略文本；基于 LLM 的工具虽然阅读文本，但无法可复现地证明受污染的输入是否到达了高影响的汇点（sink）。我们提出了 Semia，一种针对智能体技能的静态审计工具。Semia 将每个技能提升（lift）为技能描述语言（SDL），这是一种 Datalog 事实库，用于捕获 LLM 触发的操作、文本定义的条件以及人机回环（human-in-the-loop）检查点。合成一个既在结构上合理又在语义上忠实于原始文本的事实库是核心挑战；我们通过约束引导的表示合成（CGRS）来解决这一挑战，这是一种“提议-验证-评估”循环，通过不断优化 LLM 候选结果直至收敛。智能体技能的安全属性（例如间接注入、密钥泄露、混淆代理、无保护汇点等）随后可以简化为 Datalog 可达性查询。我们在来自公共市场的 13,728 个真实技能上评估了 Semia。Semia 使所有这些技能都变得可审计，并发现超过一半的技能至少携带一个关键语义风险。在 541 个专家标记技能的分层样本中，Semia 实现了 97.7% 的召回率和 90.6% 的 F1 分数，显著优于基于签名的扫描器和 LLM 基准模型。

## Abstract
An agent skill is a configuration package that equips an LLM-driven agent with a concrete capability, such as reading email, executing shell commands, or signing blockchain transactions. Each skill is a hybrid artifact-a structured half declares executable interfaces, while a prose half dictates when and how those interfaces fire-and the prose is reinterpreted probabilistically on every invocation. Conventional static analyzers parse the structured half but ignore the prose; LLM-based tools read the prose but cannot reproducibly prove that a tainted input reaches a high-impact sink.   We present Semia, a static auditor for agent skills. Semia lifts each skill into the Skill Description Language (SDL), a Datalog fact base that captures LLM-triggered actions, prose-defined conditions, and human-in-the-loop checkpoints. Synthesizing a fact base that is both structurally sound and semantically faithful to the original prose is the central challenge; we address it with Constraint-Guided Representation Synthesis (CGRS), a propose-verify-evaluate loop that refines LLM candidates until convergence. Security properties (e.g., indirect injection, secret leakage, confused deputies, unguarded sinks, etc.) over an agent skill can then be reduced to Datalog reachability queries. We evaluate Semia on 13,728 real-world skills from public marketplaces. Semia renders all of them auditable and finds that more than half carry at least one critical semantic risk. On a stratified sample of 541 expert-labeled skills, Semia achieves 97.7% recall and an F1 of 90.6%, substantially outperforming signature-based scanners and LLM baselines.

---

## 论文详细总结（自动生成）

这是一份关于论文《Semia: Auditing Agent Skills via Constraint-Guided Representation Synthesis》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究背景**：AI 智能体（Agent）通过“技能”（Skills）执行任务。技能是混合产物：一部分是结构化的可执行接口（如 YAML/API 声明），另一部分是描述策略的非结构化文本（如“外部转账需人工审批”）。
*   **核心挑战**：智能体的控制流是概率性的。安全策略往往隐藏在文本描述中，LLM 在运行时可能因受到“间接提示注入”（IPI）攻击而忽略这些策略。
*   **现有局限**：
    *   **传统静态分析（SAST）**：只能解析结构化代码，无法理解文本中的安全约束。
    *   **基于 LLM 的审计**：虽然能读懂文本，但输出不稳定，且无法像传统工具那样给出确定性的可达性证明。
*   **论文目标**：开发一种静态审计工具 Semia，能够将非结构化的技能描述转化为结构化的事实库，从而实现确定性的安全漏洞检测。

### 2. 方法论
Semia 的核心是将技能“提升”（Lift）为一种中间表示，并运行确定性查询。
*   **SDL (Skill Description Language)**：一种关系型事实模式，用于描述技能的骨架（调用、顺序）、数据流、触发器类型、秘密信息（Secrets）以及安全门禁（Barriers）。
*   **CGRS (约束引导的表示合成)**：这是论文的关键技术，采用“提议-验证-评估”循环：
    1.  **提议（Refine）**：LLM 根据原始技能文件生成 SDL 候选。
    2.  **结构验证（Validate）**：检查 SDL 是否符合结构完整性（如变量引用是否闭合、数据流是否连通）。
    3.  **语义评估（Verbalize & Distance）**：将生成的 SDL 重新转译回自然语言，并计算其与原始描述的“语义距离”。
    4.  **反馈循环**：如果验证失败，系统会生成诊断提示（Hints）反馈给 LLM 进行修正，直到收敛或耗尽预算。
*   **Datalog 检测引擎**：一旦获得 SDL 事实库，Semia 使用 Soufflé Datalog 引擎运行 11 个预定义的检测器，涵盖“无保护汇点”、“污染流违规”和“结构异常”三大类风险。

### 3. 实验设计
*   **数据集**：从公共市场（如 OpenClaw）爬取的 **13,728 个真实世界智能体技能**。
*   **Benchmark**：从上述数据中抽取 **541 个技能** 进行分层抽样，并由专家进行人工标注（Cohen's κ = 0.83，表示高度一致性）。
*   **对比方法**：
    *   **VirusTotal (VT)**：代表传统的基于签名的恶意软件扫描。
    *   **ClawScan (C-Scan)**：市场内置的启发式+轻量级 LLM 扫描器。
    *   **直接 LLM 审计**：直接让 LLM 判断技能是否有风险。

### 4. 资源与算力
*   **硬件环境**：Apple M3 Max 笔记本电脑，36 GB 统一内存。
*   **模型使用**：底层 LLM 使用 `claude-3.5-opus`（固定温度为 0 以保证可重复性）。
*   **成本与时长**：
    *   **Phase 1 (合成)**：每个技能的 LLM API 成本通常低于 1 美元，循环次数限制在 5 次以内。
    *   **Phase 2 (检测)**：基于 Datalog 的检测在固定硬件上为亚秒级（sub-second）。

### 5. 实验数量与充分性
*   **实验规模**：进行了大规模（1.3万+技能）的自动化扫描，验证了工具的吞吐能力。
*   **消融实验**：对比了“无 SDL 表示（直接 LLM 审计）”、“有 SDL 但无迭代优化”以及“完整 Semia 流程”的效果。
*   **充分性评价**：实验设计非常充分。不仅有大规模统计，还有针对专家标注集的精确度分析，并包含了 17 个零日漏洞的真实案例分析。分层抽样考虑了技能大小的干扰因素，确保了评估的客观性。

### 6. 主要结论与发现
*   **性能卓越**：Semia 实现了 **97.7% 的召回率** 和 **90.6% 的 F1 分数**，远超 VirusTotal（召回率仅 13.6%）和 ClawScan。
*   **风险普遍性**：在 1.3 万个技能中，超过一半被检测出至少存在一个关键语义风险。
*   **零日漏洞**：Semia 发现了 **17 个此前未知的严重漏洞**（如影子凭证、行为与声明矛盾等），并已获得官方确认和修复。
*   **方法有效性**：消融实验证明，将文本提升为结构化 SDL 是提升 F1 分数的关键（贡献了约 15.9 个百分点）。

### 7. 优点
*   **神经符号结合**：巧妙地利用 LLM 处理模糊语义，利用 Datalog 处理严谨逻辑，解决了 LLM 审计“不可解释、不稳定”的问题。
*   **闭环自纠错**：CGRS 机制通过结构和语义双重约束，显著降低了 LLM 生成中间表示时的幻觉和遗漏。
*   **确定性证明**：能够为每个发现的漏洞提供清晰的“证据路径”（Witness Path），方便开发者复核。

### 8. 不足与局限
*   **API 边界限制**：如果技能调用了深度嵌套的第三方 API 且这些 API 未被包含在分析范围内，可能会导致数据流断裂，产生漏报。
*   **误报风险**：在“缺失人工门禁”（MHG）类别中存在一定误报，因为某些低风险操作（如发推特）在特定语境下可能确实不需要人工干预，但会被工具标记。
*   **依赖 LLM 质量**：虽然有纠错机制，但如果底层 LLM 无法理解极其复杂的逻辑，合成的 SDL 质量仍受限。
*   **动态执行缺失**：作为静态分析工具，它无法捕捉仅在运行时通过复杂交互触发的动态攻击。

（完）
