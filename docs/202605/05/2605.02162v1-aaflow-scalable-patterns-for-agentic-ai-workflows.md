---
title: "AAFLOW: Scalable Patterns for Agentic AI Workflows"
title_zh: AAFLOW：智能体 AI 工作流的可扩展模式
authors: "Arup Kumar Sarker, Mills Staylor, Aymen Alsaadi, Gregor von Laszewski, Shantenu Jha, Geoffrey Fox"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02162v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 集成检索和记忆的智能体AI工作流可扩展模式
tldr: 针对Agentic AI工作流中数据编排碎片化和序列化开销大的问题，本文提出AAFLOW分布式运行时。它将工作流建模为算子抽象，利用Apache Arrow和Cylon构建零拷贝数据平面，并结合确定性调度与异步批处理。实验显示，该系统在不改变LLM推理速度的情况下，通过优化数据流和通信效率，实现了最高4.64倍的流水线加速。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.02162v1/fig-001.webp\", \"caption\": \"Fig. 1. End-to-End code flow of AAFLOW with memory operation. The RAG context constructor uses the memory module through the retriever.\", \"page\": 2, \"index\": 1, \"width\": 524, \"height\": 248}, {\"url\": \"assets/figures/arxiv/2605.02162v1/fig-002.webp\", \"caption\": \"Fig. 5. Framework benchmarking in each vital stage in the RAG pipeline under equal parallelism. AAFLOW beats all frameworks in two vital stages (embed and upsert)\", \"page\": 7, \"index\": 2, \"width\": 496, \"height\": 305}, {\"url\": \"assets/figures/arxiv/2605.02162v1/fig-003.webp\", \"caption\": \"Fig. 6. Strong scaling behavior across configurations for Load, Transform, Embed, Upsert operations. 100 million chunks (a 92 GB synthetic corpus generated from wikitext2_train) are used from 4096 files. Each node has 40 cores, totaling 128–1024 workers are used in multiple runs (e.g., 7 nodes * 40 cores = 280 workers)\", \"page\": 8, \"index\": 3, \"width\": 525, \"height\": 587}, {\"url\": \"assets/figures/arxiv/2605.02162v1/fig-004.webp\", \"caption\": \"Fig. 8. Strong and Weak scaling behavior across configurations for Total execution time. 95000 chunks are used by each worker. Each node has 40 cores, totaling 128–1024 workers are used in multiple runs (e.g. 7 nodes * 40 cores = 280 workers)\", \"page\": 9, \"index\": 4, \"width\": 525, \"height\": 324}, {\"url\": \"assets/figures/arxiv/2605.02162v1/fig-005.webp\", \"caption\": \"Fig. 7. Weak scaling behavior across configurations for Load, Transform, Embed, and Upsert operations. 95000 chunks are used by each worker. Each node has 40 cores, totaling 128–1024 workers are used in multiple runs (e.g. 7 nodes * 40 cores = 280 workers)\", \"page\": 9, \"index\": 5, \"width\": 525, \"height\": 587}, {\"url\": \"assets/figures/arxiv/2605.02162v1/fig-006.webp\", \"caption\": \"Fig. 4. Advanced Async with Parallel Pipeline of AAFLOW. With batching to measure execution time. BE = Size of the batch embeddings; BU = Size of the upsert batching. TTotal = Load T ime+ Transform Time(t1 + t2) + Index T ime\", \"page\": 5, \"index\": 6, \"width\": 474, \"height\": 258}, {\"url\": \"assets/figures/arxiv/2605.02162v1/fig-007.webp\", \"caption\": \"Fig. 3. AAFLOW design incorporating a multilayered architecture with agentic DAG execution supported by a zero-copy data plane, used by high-performance agentic execution, hosted in distributed storage, which is managed by a scalable adaptive workflow.\", \"page\": 5, \"index\": 7, \"width\": 420, \"height\": 344}]"
motivation: 现有Agentic工作流框架在数据编排和序列化方面存在性能瓶颈，且缺乏高性能计算所需的正式执行模型。
method: 引入AAFLOW统一分布式运行时，通过算子抽象、零拷贝数据平面以及资源确定性调度来优化执行计划。
result: 实验证明该方法实现了高达4.64倍的流水线加速，并在嵌入和更新阶段获得了2.8倍的性能提升。
conclusion: AAFLOW通过增强数据流和通信效率，而非仅仅依赖LLM推理加速，显著提升了Agentic AI工作流的可扩展性。
---

## 摘要
大语言模型系统中的智能体工作流集成了检索、推理和记忆，但现有框架由于零散的数据编排、序列化开销和非确定性执行，在可扩展性和可复现性方面存在局限。尽管这些框架增加了灵活性，但它们缺乏遵循高性能计算原则的正式执行模型。我们推出了 AAFLOW，这是一个统一的分布式运行时，通过将智能体工作流建模为算子抽象，创建了通信高效的执行计划。利用 Apache Arrow 和 Cylon，AAFLOW 构建了一个零拷贝数据平面，允许在预处理、嵌入和向量检索之间直接互操作，而无需序列化开销。为了降低协调成本，它采用了资源确定性调度和异步批处理。在保持相当的 LLM 生成吞吐量的同时，实验结果表明流水线速度提升高达 4.64 倍，嵌入和更新插入（upsert）阶段的增益达到 2.8 倍。这些优势并非源于 LLM 推理加速，而是源于增强的数据流、批处理和通信效率。

## Abstract
Agentic workflows in large language model systems integrate retrieval, reasoning, and memory, but existing frameworks suffer from scalability and reproducibility limitations due to fragmented data orchestration, serialization overhead, and non-deterministic execution. Although these frameworks increase flexibility, they don't have a formal execution model that adheres to the principles of high-performance computing. We introduce AAFLOW, a unified distributed runtime that creates communication-efficient execution plans by modeling agentic workflows as an operator abstraction. Using Apache Arrow and Cylon, AAFLOW creates a zero-copy data plane that allows direct interoperability between preprocessing, embedding, and vector retrieval without the need for serialization overhead. To lower coordination costs, it uses resource-deterministic scheduling and asynchronous batching. While retaining comparable LLM generation throughput, experimental results demonstrate up to 4.64 times pipeline speedup and 2.8 times gains in embedding and upsert phases. Rather than LLM inference acceleration, these advantages result from enhanced data flow, batching, and communication efficiency.

---

## 论文详细总结（自动生成）

### 论文总结：AAFLOW：智能体 AI 工作流的可扩展模式

#### 1. 核心问题与研究动机
随着大语言模型（LLM）进入“智能体（Agentic）”时代，系统需要动态集成检索、推理、工具调用和记忆。然而，现有的智能体框架（如 LangChain、LangGraph）在分布式环境下存在严重瓶颈：
*   **数据编排碎片化**：数据在不同组件间移动时产生巨大的序列化/反序列化开销。
*   **执行非确定性**：智能体驱动的控制流导致执行路径难以预测，无法利用高性能计算（HPC）中的调度优化。
*   **缺乏统一抽象**：现有系统将数据处理与智能体逻辑割裂，导致在大规模扩展时效率低下。

#### 2. 方法论
AAFLOW 提出了一个统一的分布式运行时，旨在将智能体工作流转化为通信高效的执行计划。
*   **算子驱动的抽象（Operator Abstraction）**：将工作流形式化为五类算子：嵌入（$Op_{embed}$）、检索（$Op_{retrieve}$）、推理（$Op_{reason}$）、记忆（$Op_{memory}$）和更新插入（$Op_{upsert}$）。每个算子都映射到特定的分布式通信模式（如广播、Shuffle、归约）。
*   **零拷贝数据平面（Zero-copy Data Plane）**：基于 **Apache Arrow** 和 **Cylon** 构建，实现了预处理、嵌入和检索阶段之间的直接内存共享，彻底消除了 Python 对象的序列化开销。
*   **资源确定性调度**：将逻辑上的智能体决策与物理上的资源调度分离。通过异步批处理引擎，将任务分解为 Load、Transform、Embed 和 Upsert 四个阶段，并利用有界队列和持久化工作线程池实现流水线重叠。
*   **分层存储模型**：集成了一个记忆模块，作为智能体与系统状态之间的双向链接，支持跨会话的上下文感知。

#### 3. 实验设计
*   **对比场景**：
    1.  **端到端 RAG 框架对比**：对比 LangChain, LangGraph, CrewAI, AutoGen。
    2.  **分布式摄取流水线对比**：对比 Ray (ScalableRAG), Dask, HigressRAG 等。
    3.  **检索与推理性能**：测试非缓存复杂查询、混合检索和语义缓存查找。
*   **Benchmark 与数据集**：使用合成的系统微基准测试，包括 1 亿个 Chunk（约 92GB 的 Wikitext2 数据集）。
*   **特殊设计**：为了隔离数据编排开销，实验采用了轻量级代理模型（如 DistilGPT-2）来模拟 LLM 推理，从而将系统推向“通信受限”的极端情况，以观察框架本身的性能。

#### 4. 资源与算力
*   **硬件环境**：大规模学术 HPC 集群。
*   **算力细节**：每个节点配备 **40 核 CPU**。实验通过增加节点数，测试了从 **128 到 1024 个并行 Worker** 的扩展性。
*   **GPU 说明**：论文明确指出 LLM 推理本身是 GPU 受限的，而 AAFLOW 的优化重点在于数据流和通信。虽然支持 GPU 集群，但本研究的基准测试主要在 CPU 集群上运行以量化系统层面的开销（$\Omega$）。

#### 5. 实验数量与充分性
*   **实验规模**：进行了多组强扩展性（Strong Scaling）和弱扩展性（Weak Scaling）实验，Worker 数量跨度达 8 倍。
*   **充分性评价**：实验设计较为充分且客观。通过对比多种主流智能体框架和分布式数据框架，证明了性能提升并非来自模型推理，而是来自数据平面的优化。使用轻量级模型作为代理来剥离计算干扰，是系统级论文中常用的公平对比手段。

#### 6. 主要结论与发现
*   **显著加速**：相比 Dask，AAFLOW 实现了 **4.64 倍** 的流水线加速；在嵌入和更新阶段比传统框架快 **2.8 倍**。
*   **端到端提升**：在端到端 RAG 任务中，相比 LangChain 实现了约 **1.88 倍** 的速度提升。
*   **扩展性优异**：AAFLOW 在 1024 个 Worker 规模下仍保持近乎线性的扩展性，而传统框架（如 Ray, Dask）因对象存储和任务调度开销（$\Omega$）出现了明显的性能衰减。
*   **瓶颈定位**：实验证明，大规模智能体系统的主要瓶颈在于数据编排和通信，而非单纯的 LLM 推理速度。

#### 7. 优点
*   **系统级创新**：首次将 HPC 中的零拷贝和确定性调度引入智能体 AI 领域。
*   **高性能底座**：利用 Cylon 和 Arrow 解决了 Python 在分布式环境下处理大规模向量数据的“序列化税”问题。
*   **架构解耦**：算子抽象使得复杂的智能体逻辑可以被编译为可预测的 DAG 执行图。

#### 8. 不足与局限
*   **GPU 深度集成不足**：虽然讨论了 GPU 绑定推理，但实验主要集中在 CPU 侧的数据编排，缺乏在多 GPU 节点间进行大规模张量并行/流水线并行时的协同优化数据。
*   **模型多样性**：主要使用了轻量级代理模型进行测试，在实际应用中，超大规模模型（如 Llama-3-70B）的长推理时间可能会掩盖部分数据平面的优化效果。
*   **应用限制**：目前主要针对 RAG 和摄取密集型场景，对于纯推理、低数据交互的智能体任务提升可能有限。

（完）
