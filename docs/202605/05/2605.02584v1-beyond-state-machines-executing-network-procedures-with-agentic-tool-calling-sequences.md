---
title: "Beyond State Machines: Executing Network Procedures with Agentic Tool-Calling Sequences"
title_zh: 超越状态机：利用代理式工具调用序列执行网络流程
authors: "Purna Sai Garigipati, Onur Ayan, Kishor Chandra Joshi, Xueli An"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02584v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 基于大语言模型的网络AI智能体通过工具调用序列执行程序
tldr: 本研究探讨了基于大语言模型（LLM）的网络AI代理在执行网络程序中的应用。通过对比四种工具调用序列执行方法，分析了其在延迟和准确性方面的表现。研究发现将程序封装在单一工具中能显著降低延迟，并揭示了模型在长序列执行中的可靠性限制。此外，本文还提出了一套系统分析程序执行失败的错误分类法，为未来自治网络的设计提供了重要参考。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.02584v1/fig-001.webp\", \"caption\": \"Fig. 1. Comparison of four procedural execution approaches. (a) A1 embeds the procedure within the agent, (b) A2 retrieves the procedure from an external database, (c) A3 receives the procedure in the input prompt, and (d) A4 encapsulates the procedure within a single tool. The figure highlights the difference between iterative multi-step execution (A1–A3) and single-call execution (A4).\", \"page\": 3, \"index\": 1, \"width\": 1012, \"height\": 561}, {\"url\": \"assets/figures/arxiv/2605.02584v1/fig-002.webp\", \"caption\": \"Fig. 2. Overview of the experimental setups. Scenario A illustrates the UE IP Allocation workflow across two MCP servers: MCP Server 2 provides the IP allocation tools used in approaches A1–A3, while MCP Server 1 hosts encapsulated tools used in approach A4. The highlighted tools in Scenario A indicate the subset of tools executed for the representative request. Scenario B utilizes MCP Server 3, providing a pool of m = 100 network analytics tools to stress-test sequence stability for procedures of length up to k = 50.\", \"page\": 4, \"index\": 2, \"width\": 958, \"height\": 310}, {\"url\": \"assets/figures/arxiv/2605.02584v1/fig-003.webp\", \"caption\": \"Fig. 3. Evaluation of the UE IP Allocation procedure (Scenario A). The top row displays the end-to-end latency cost C(i) categorized by (a) approach and (b) model size. The bottom row illustrates (c) the execution correctness rate, representing the average of the reliability metric R(Pi), alongside the distribution of specific error types across (d) approaches and (e) models. Note that 0.8B, 3B, 9B, and 35B refer to the Qwen-0.8B, Qwen-Coder-3B, Qwen-9B, and Qwen-35B models, respectively.\", \"page\": 5, \"index\": 3, \"width\": 952, \"height\": 592}, {\"url\": \"assets/figures/arxiv/2605.02584v1/fig-004.webp\", \"caption\": \"Fig. 4. Scalability stress test results (Scenario B). Panel (a) illustrates the trade-off between mean latency cost C(i) and execution correctness rate R(Pi) as the target procedure length k increases. Panel (b) details the composition of procedural errors at varying procedure lengths. Note that 3B, 9B, and 35B refer to the Qwen-Coder-3B, Qwen-9B, and Qwen-35B models, respectively.\", \"page\": 6, \"index\": 4, \"width\": 482, \"height\": 528}]"
motivation: 旨在探索利用LLM代理自动化复杂移动通信网络操作的可行性，以克服传统硬编码状态机的灵活性不足。
method: 比较了四种不同的代理执行策略，并通过UE IP分配案例及压力测试评估了不同LLM在多步工具调用中的表现。
result: 结果显示迭代推理增加了延迟和错误风险，而工具封装模式效率更高，且所有模型在长序列任务中均表现出可靠性下降。
conclusion: LLM代理在网络自动化中具有潜力，但必须通过优化工具调用结构和处理长序列执行的可靠性瓶颈来提升性能。
---

## 摘要
代理式人工智能（Agentic AI）将是设计未来移动通信系统的关键赋能技术，能够提供灵活且定制化的服务，实现复杂网络操作的自动化，并驱动全网的自主决策。本研究探讨了如何利用基于大语言模型（LLM）的网络 AI 代理来执行以工具调用序列形式表示的网络流程。我们研究了四种方法，其区别在于代理获取流程的方式以及代理与底层工具之间的执行分配。我们以用户设备（UE）IP 分配流程为案例，评估了这些方法的延迟和执行正确性。此外，我们还进行了压力测试，以考察 LLM 代理在失效前能可靠执行的连续流程步骤数量。结果表明，依赖代理端迭代推理的方法延迟更高且更易出错；而将流程封装在单个工具内（由该工具内部调用其他工具编排步骤）的方法，则通过减少重复推理降低了延迟。压力测试进一步显示，具备高级工具调用能力的模型在长流程执行中比其他模型更可靠；但随着流程长度增加，所有模型的可靠性均有所下降，揭示了多步工具化工作流中明显的执行极限。为系统分析流程执行故障，我们提出了一种流程特定的错误分类法，对多步流程执行中的偏差进行了归类。

## Abstract
Agentic AI will be an essential enabling technology for designing future mobile communication systems, which could provide flexible and customized services, automate complex network operations, and drive autonomous decision-making across the network. This work studies how Large Language Model (LLM)-based network AI agents can be utilized to execute network procedures expressed as sequences of tool invocations. We investigate four approaches, which differ in how the agent obtains the procedure and in how execution is distributed between the agent and the underlying tools. We evaluated the latency and execution correctness across these approaches using a User Equipment (UE) IP allocation procedure as a case study. Furthermore, we conduct a stress test to examine how many sequential procedural steps an LLM agent can reliably execute before failure. Our results show that approaches relying on iterative agent-side reasoning incur higher latency and are more prone to execution errors, while approaches where the procedure is encapsulated within a single tool, which internally orchestrates the required steps by invoking other tools, reduce latency by limiting repeated reasoning. The stress-test results further show that the model with advanced tool-calling capability maintains reliable execution over longer procedures than the other evaluated models; however, all models exhibit reliability degradation as procedure length increases, revealing clear execution limits in multi-step tool-based workflows. To systematically analyze failures in procedure execution, we introduce a procedure-specific error taxonomy that categorizes deviations in multi-step procedural execution.

---

## 论文详细总结（自动生成）

这篇论文探讨了在未来 6G 网络自动化背景下，利用基于大语言模型（LLM）的智能体（Agent）替代传统硬编码状态机来执行网络流程的可行性与可靠性。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：传统的网络流程（如 3GPP 定义的信令流程）通常依赖硬编码的脚本或状态机实现。这种方式虽然稳定，但在面对灵活定制化服务和复杂多变的网络环境时，缺乏扩展性且维护成本高。
*   **核心问题**：LLM 智能体能否可靠地执行电信级的网络流程？具体表现为：智能体在执行多步工具调用序列时，能否保持正确的顺序、处理中间结果，并在流程长度增加时维持稳定性。
*   **整体含义**：该研究为 6G 网络实现“代理式自动化（Agentic Automation）”提供了实证基础，识别了当前 LLM 在处理长序列工具调用时的性能瓶颈。

### 2. 核心方法论
论文将网络流程定义为工具调用序列 $P_i = (\tau_{i,1}, \tau_{i,2}, \dots, \tau_{i,k})$，并提出了四种执行架构：
*   **A1: 代理嵌入式（Agent-Embedded）**：流程逻辑直接写在智能体的系统提示词（System Prompt）中。
*   **A2: 服务器提供式（Server-Provided）**：智能体先从外部数据库检索流程定义，再解析并执行。
*   **A3: 用户/代理提供式（User/Agent-Provided）**：流程逻辑随用户请求（Input Prompt）一起输入。
*   **A4: 工具封装式（Tool-Encapsulated）**：将整个流程逻辑封装在一个“宏工具”内，智能体只需发起一次调用，内部逻辑由确定性代码执行。

**错误分类法（Error Taxonomy）**：
为了系统分析失败原因，论文定义了五类错误：
1.  **错误工具（Wrong Tool）**：包括调用了流程外的工具、幻觉工具名或参数错误。
2.  **重复工具（Duplicate Tool）**：陷入推理循环，重复调用同一工具。
3.  **过早停止（Premature Stop）**：未完成所有步骤就终止。
4.  **顺序错误（Wrong Order）**：工具调用顺序与预设不符。
5.  **无工具调用（No Tool Calls）**：直接给出文本回复而未触发工具。

### 3. 实验设计
研究设计了两个主要场景：
*   **场景 A：UE IP 地址分配**
    *   **背景**：模拟 3GPP 标准中的 IP 分配逻辑（涉及授权、静态 IP 查询、DHCPv4/v6 分配、注册等步骤）。
    *   **对比方法**：对比 A1-A4 四种架构，评估不同规模模型（Qwen-0.8B, 3B-Coder, 9B, 35B）的表现。
*   **场景 B：扩展性压力测试**
    *   **背景**：使用 100 个网络分析工具池，设计长度 $k$ 从 5 到 50 的顺序调用任务。
    *   **目的**：寻找智能体在多步推理中的“崩溃点（Breaking Point）”。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或训练时长。
*   **模型来源**：使用了 Qwen 系列的预训练模型（Qwen 3.5 和 Qwen3-Coder-Next），实验侧重于推理阶段的评估而非模型训练。

### 5. 实验数量与充分性
*   **实验规模**：
    *   场景 A 中，每种模型在每种架构下运行 50 次，共计 800 次独立运行（4 架构 × 4 模型 × 50 次）。
    *   场景 B 中，针对不同长度 $k$，每种模型运行 30 次。
*   **充分性评价**：实验设计较为充分，涵盖了从极小规模（0.8B）到中大规模（35B）的模型对比，且引入了专门针对代码/工具调用优化的 Coder 模型。通过多次重复实验获取平均正确率，结果具有统计学意义。

### 6 主要结论与发现
*   **架构优劣**：**A4（工具封装式）表现最优**，延迟最低且正确率最高，因为它将复杂性从 LLM 推理转移到了确定性代码中。
*   **延迟瓶颈**：迭代推理（A1-A3）的延迟随步骤数线性增长，且 A2（需额外检索）延迟最高。
*   **模型规模悖论**：**模型越大并不一定越稳健**。在压力测试中，Qwen-35B 的性能下降速度甚至快于 Qwen-9B。
*   **可靠性极限**：即使是表现最好的 Qwen-Coder-3B，在步骤数 $k > 30$ 后正确率也大幅下降，主要错误类型是“过早停止”。
*   **特定优化有效性**：经过工具调用专门优化的模型（如 Coder 系列）在长序列任务中显著优于通用模型。

### 7. 优点
*   **实战导向**：将 LLM 智能体与具体的电信标准（3GPP）结合，具有很强的行业应用参考价值。
*   **细粒度分析**：提出的错误分类法比简单的“成功/失败”二元评估更深入，能精准定位 LLM 在流程执行中的弱点。
*   **压力测试**：通过增加序列长度来探索模型极限，揭示了当前 Agent 架构在处理复杂长流程时的局限性。

### 8. 不足与局限
*   **模型多样性不足**：实验仅限于 Qwen 系列模型，未对比 GPT-4、Claude 或 Llama 等其他主流模型，结论的普适性有待验证。
*   **场景复杂度有限**：虽然模拟了 IP 分配，但实际网络环境中的并发处理、异常回滚和异步交互在实验中未充分体现。
*   **缺乏微调对比**：研究主要基于 Zero-shot 推理，未探讨通过微调（Fine-tuning）或少样本学习（Few-shot）是否能显著提升长序列执行的稳定性。

（完）
