---
title: "Retrieval and Multi-Hop Reasoning in 1M-Token Context Windows: Evaluating LLMs on Classical Chinese Text"
title_zh: 100万Token上下文窗口中的检索与多跳推理：在古汉语语料库上评估大语言模型
authors: Eric H. C. Chow
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02173v1"
tags: ["query:ancient-text"]
score: 10.0
evidence: 评估古汉语文本的长上下文检索
tldr: 本研究评估了五种主流大模型在100万token超长上下文下的古文检索与多跳推理能力。通过单针检索和三跳链式推理测试，发现顶尖模型在百万级检索上已近完美，但在多跳推理中表现出稳定、断崖式或平滑下降三种不同的性能衰减模式。研究揭示了标称上下文长度与实际多跳推理能力之间的差距，指出512K至1M区间是区分模型长文本能力的关键。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.02173v1/fig-001.webp\", \"caption\": \"Figure 1: Test 1 schematic. A single biographical fact is planted at one of three depths (25%, 50%, 75%) inside a per-model haystack sized to ∼99% of each model’s 1M-token context window. The model is then asked one question whose answer depends solely on the planted statement.\", \"page\": 6, \"index\": 1, \"width\": 896, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2605.02173v1/fig-002.webp\", \"caption\": \"Figure 2: Test 2 schematic. Each chain consists of three planted statements (N1, N2, N3) inserted at fixed depths of 25%, 50%, and 75% in a per-model haystack at three tiers (256K / 512K / 1M). The single question requires the model to traverse the relations A → B → C and then read C’s attribute X, an implicit graph traversal performed in one forward pass.\", \"page\": 6, \"index\": 2, \"width\": 896, \"height\": 465}, {\"url\": \"assets/figures/arxiv/2605.02173v1/fig-003.webp\", \"caption\": \"Figure 6: Per-chain accuracy across all tiers. Chain C3 (hometown) is the hardest chain; Chain C5 (courtesy name) is notably difficult for DeepSeek V4 Pro. Only Gemini 3.1 Pro achieves 100% on every chain.\", \"page\": 11, \"index\": 3, \"width\": 896, \"height\": 490}, {\"url\": \"assets/figures/arxiv/2605.02173v1/fig-004.webp\", \"caption\": \"Figure 5: Per-model accuracy across the three context tiers.\", \"page\": 10, \"index\": 4, \"width\": 896, \"height\": 540}, {\"url\": \"assets/figures/arxiv/2605.02173v1/fig-005.webp\", \"caption\": \"Figure 3: Single-needle retrieval accuracy at 1M tokens, ordered by score.\", \"page\": 8, \"index\": 5, \"width\": 804, \"height\": 446}, {\"url\": \"assets/figures/arxiv/2605.02173v1/fig-006.webp\", \"caption\": \"Figure 4: Per-needle accuracy split by variant. Altered-variant accuracy provides the cleaner signal of genuine in-context retrieval.\", \"page\": 9, \"index\": 6, \"width\": 943, \"height\": 334}]"
motivation: 旨在探究大模型在百万级超长上下文中的实际检索与复杂推理能力，并区分模型是依赖检索还是训练记忆。
method: 采用古文语料，设计了区分真实与篡改信息的单针检索测试，以及跨越256K至1M长度的三跳推理链测试。
result: "顶尖模型在百万级单针检索中均达到100%准确率，但在多跳推理中表现出稳定、断崖式或平滑下降三种显著的性能衰减特征。"
conclusion: 标称的上下文窗口长度不能代表实际的多跳推理能力，512K到1M的性能表现是衡量长文本模型实力的核心分水岭。
---

## 摘要
我们在古汉语语料库上评估了五种宣称具有100万token上下文窗口的前沿大语言模型的长上下文检索与推理能力。本文报告了两项互补的研究。测试1衡量了100万token输入下的单针检索能力，在三个深度植入了三条传记“针”，并使用真实（符合训练先验）和修改后（违背训练先验）的变体对，以区分真实的上下文检索与对记忆训练数据的依赖。测试2是一项后续研究，旨在探究当检索需要中间推理时，长上下文能力是否会下降，它衡量了跨三个上下文层级（256K、512K和1M token）的三跳链式遍历。我们发现，对于最强大的模型（Gemini 3.1 Pro、Claude Opus 4.7 和 GPT-5.5），100万token下的单针检索已基本解决，各模型均达到100%的准确率；但多跳性能揭示了三种截然不同的衰减特征：稳定型（Gemini Pro、Claude）在512K范围内保持80%以上的准确率，在1M时仅有轻微下降；晚期悬崖型（GPT-5.5、Qwen3.6-plus）在512K到1M之间急剧崩溃；以及平滑下降型（DeepSeek V4 Pro）在整个范围内逐渐衰减。研究结果表明，标称的上下文窗口长度并不能很好地代表可用的长上下文多跳能力，而当前100万上下文旗舰模型之间最显著的分水岭在于512K到1M的过渡阶段。

## Abstract
We evaluate the long-context retrieval and reasoning capabilities of five frontier large language models with advertised 1M-token context windows on a classical Chinese corpus. Two complementary studies are reported. Test 1 measures single-needle retrieval at 1M tokens of input, with three biographical needles planted at three depths and pairs of real (training-prior-consistent) and altered (training-prior-contradicting) variants to separate genuine in-context retrieval from reliance on memorised training data. Test 2, a follow-up designed to probe whether long-context capability degrades when retrieval requires intermediate reasoning, measures three-hop chain traversal across three context tiers (256K, 512K, and 1M tokens). We find that single-needle retrieval at 1M is essentially solved for the strongest models - Gemini 3.1 Pro, Claude Opus 4.7, and GPT-5.5 each achieve 100% - but that multi-hop performance reveals three distinct decay signatures: a stable regime (Gemini Pro, Claude) maintaining greater than 80% accuracy through 512K with modest degradation at 1M; a late-cliff regime (GPT-5.5, Qwen3.6-plus) collapsing sharply between 512K and 1M; and a smooth-decline regime (DeepSeek V4 Pro) decaying gradually across the entire range. The findings suggest that nominal context-window length is a poor proxy for usable long-context multi-hop capability, and that the sharpest discriminator between current 1M-context flagships is the 512K-to-1M transition.

---

## 论文详细总结（自动生成）

这篇论文对2026年主流大语言模型（LLM）在超长上下文（100万Token）下的表现进行了深入的实证研究，特别关注了古汉语（文言文）这一具有挑战性的领域。

### 1. 论文的核心问题与整体含义
随着大模型上下文窗口扩展至100万Token以上，学术界和工业界开始讨论“长上下文是否会让检索增强生成（RAG）变得多余”。
*   **研究动机**：验证模型在处理超长文本时，是真正具备“在上下文内检索和推理”的能力，还是仅仅依赖预训练阶段的记忆（参数化泄露）。
*   **背景**：古汉语具有高信息密度、分词不对称性以及严重的训练数据泄露风险，是评估模型长文本处理能力的理想“压力测试”场景。

### 2. 论文提出的方法论
研究者设计了两项互补的测试任务，旨在剥离模型记忆的影响：
*   **测试 1：单针检索（Single-Needle Retrieval）**
    *   在1M Token的语料中植入特定事实（针）。
    *   **核心创新**：采用“真实事实”与“篡改事实”成对测试。如果模型在真实事实（符合训练先验）上正确但在篡改事实（违背训练先验）上错误，则证明其依赖记忆而非实时检索。
*   **测试 2：多跳链式推理（Multi-Hop Chain Reasoning）**
    *   构建三跳逻辑链（如：A是B的学生 -> B是C的朋友 -> C的籍贯是X -> 问A老师的朋友籍贯在哪？）。
    *   **核心细节**：使用**虚构人名**以彻底消除训练数据泄露。这要求模型必须在一次前向传播中完成隐式的知识图谱遍历。
*   **分词校准**：针对不同模型的分词器（Tokenizer）效率差异（最高差35%），为每个模型定制了填充至其窗口99%容量的专属语料库，确保对比的公平性。

### 3. 实验设计
*   **数据集**：使用清代《宋元學案》，总计约142万字。
*   **评估对象**：Gemini 3.1 Pro, Claude Opus 4.7, GPT-5.5, Qwen3.6-plus, DeepSeek V4 Pro。
*   **Benchmark**：基于NIAH（大海捞针）思想改进的自定义古文测试集。
*   **对比维度**：三个上下文层级（256K、512K、1M Token）的准确率曲线。

### 4. 资源与算力
*   **算力说明**：论文未提及具体的训练算力或GPU型号，因为这是一项基于API的评估研究。
*   **成本考量**：文中讨论了各厂商的“提示词缓存（Prompt Caching）”技术，指出在1M Token规模下，缓存技术使长上下文方案在经济上具有可行性。

### 5. 实验数量与充分性
*   **实验规模**：测试1包含90组实验（5模型 × 18个单元格）；测试2包含75组实验（5模型 × 15个单元格）。
*   **充分性评价**：作为一项“探索性试点研究（Exploratory Pilot）”，其样本量相对较小（每组实验仅运行一次）。虽然揭示了明显的趋势，但在统计严密性上略显不足，不能作为最终的模型排名，更多是方法论的展示。
*   **客观性**：通过虚构人名和篡改事实，极大地提高了评估的客观性，有效识别了模型的“伪检索”行为。

### 6. 论文的主要结论与发现
*   **单针检索已解决**：在1M Token下，顶尖模型（Gemini, Claude, GPT-5.5）的单针检索准确率均达到100%。
*   **多跳推理的三种衰减特征**：
    1.  **稳定型（Stable）**：Gemini 3.1 Pro和Claude Opus 4.7在1M长度下仍保持高水平，衰减极小。
    2.  **晚期悬崖型（Late-cliff）**：GPT-5.5和Qwen3.6-plus在512K之前表现完美，但在接近1M时性能急剧崩溃。
    3.  **平滑下降型（Smooth-decline）**：DeepSeek V4 Pro随长度增加性能线性下降。
*   **分水岭**：512K到1M的区间是区分当前顶级长上下文模型的关键指标。

### 7. 优点
*   **针对性强**：专门针对古汉语特性设计，解决了传统NIAH测试无法区分模型记忆与实时检索的痛点。
*   **任务设计精巧**：多跳链式推理模拟了真实学术研究（如谱牒学研究）中的复杂查询，具有极高的实用参考价值。
*   **揭示了标称值的虚高**：有力地证明了“标称上下文长度”不等于“可用推理长度”。

### 8. 不足与局限
*   **样本量限制**：每种情况仅测试一次，未考虑模型生成的随机性（尤其是强制Temperature=1的模型）。
*   **领域局限**：结论仅限于古汉语，可能无法直接推广到代码或英文长文本。
*   **缺乏中间层级**：在512K和1M之间缺乏更细致的采样，无法精准定位“性能悬崖”发生的具体断点。
*   **评估自动化**：依赖子字符串匹配，可能漏掉部分语义正确但表述不同的答案。

（完）
