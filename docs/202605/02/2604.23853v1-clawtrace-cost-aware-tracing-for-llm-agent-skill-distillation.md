---
title: "ClawTrace: Cost-Aware Tracing for LLM Agent Skill Distillation"
title_zh: ClawTrace：面向大语言模型智能体技能蒸馏的成本感知追踪
authors: "Boqin Yuan, Renchu Song, Yue Su, Sen Yang, Jing Qin"
date: 2026-04-26
pdf: "https://arxiv.org/pdf/2604.23853v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 用于大模型智能体技能蒸馏和优化的成本感知追踪
tldr: "本研究针对LLM智能体技能蒸馏中缺乏成本感知的问题，提出ClawTrace追踪平台。它记录智能体执行细节并生成包含成本和冗余标记的TraceCard。基于此，CostCraft流水线通过生成保留、修剪和修复三类技能补丁优化行为。实验表明，该方法在减少质量退化的同时，将中位数成本降低了32%，并展现出优秀的跨任务迁移能力。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.23853v1/fig-001.webp\", \"caption\": \"Figure 6: Trajectory dashboard. Each row is one agent run, with columns for total cost, token count, step count, and outcome. Daily trend charts and per-agent filtering let operators spot cost spikes before drilling into individual traces.\", \"page\": 11, \"index\": 1, \"width\": 932, \"height\": 527}, {\"url\": \"assets/figures/arxiv/2604.23853v1/fig-002.webp\", \"caption\": \"Figure 1: End-to-end architecture. Capture: ClawTrace instruments an agent session via eight event hooks. Compile: a deterministic compiler produces a TraceCard per session. Distill: CostCraft emits preserve, prune, and repair patches that merge into an evolved SKILL.md.\", \"page\": 2, \"index\": 2, \"width\": 833, \"height\": 559}, {\"url\": \"assets/figures/arxiv/2604.23853v1/fig-003.webp\", \"caption\": \"Figure 4: Quality preservation rate across ablation conditions on 30 held-out tasks. Each point removes one signal (right to left). Full CostCraft preserves quality on 86.7%; No-prune drops to 56.7%.\", \"page\": 7, \"index\": 3, \"width\": 472, \"height\": 286}, {\"url\": \"assets/figures/arxiv/2604.23853v1/fig-004.webp\", \"caption\": \"Figure 2: ClawTrace execution-path view showing per-span cost attribution, tool-call payloads, and sub-agent nesting for a single SpreadsheetBench trajectory.\", \"page\": 3, \"index\": 4, \"width\": 932, \"height\": 526}, {\"url\": \"assets/figures/arxiv/2604.23853v1/fig-005.webp\", \"caption\": \"Figure 5: Per-task cost on 30 SkillsBench tasks. Hollow circles: baseline; filled circles: CostCraft (green = quality win, red = regression, gray = tie). Dashed lines mark medians. Cost decreased on 16 of 27 valid pairs (−44% median decrease); the two quality wins traded higher cost for correct outputs that the baseline failed to produce.\", \"page\": 8, \"index\": 5, \"width\": 979, \"height\": 1050}, {\"url\": \"assets/figures/arxiv/2604.23853v1/fig-006.webp\", \"caption\": \"Table 1: TraceCard schema. Heuristic fields are marked with †.\", \"page\": 4, \"index\": 6, \"width\": 800, \"height\": 199}, {\"url\": \"assets/figures/arxiv/2604.23853v1/fig-007.webp\", \"caption\": \"Figure 7: Step timeline (Gantt view) for a single trajectory. Each bar is one span; bar length is wall-clock duration. Redundant tool calls appear as visually identical adjacent bars, the same pattern the TraceCard redundant_tool_calls heuristic flags programmatically.\", \"page\": 12, \"index\": 7, \"width\": 932, \"height\": 525}]"
motivation: 传统的技能蒸馏方法因缺乏每步成本数据，难以识别并消除影响效率的冗余高成本操作。
method: 开发ClawTrace平台生成带成本标注的TraceCard，并利用CostCraft流水线提取保留、修剪及修复类技能规则。
result: "实验显示成本归因和修剪补丁能有效减少性能回退，并在跨任务场景下将中位数成本降低了32%。"
conclusion: 引入成本感知的追踪机制是优化智能体技能、提升其经济性和泛化能力的关键路径。
---

## 摘要
技能蒸馏流水线从大语言模型（LLM）智能体的轨迹中学习可重用的规则，但它们缺乏一个关键信号：每一步的成本。如果没有每一步的成本信息，流水线就无法区分是为了修复错误而添加缺失步骤，还是为了移除一个从未影响结果的高昂步骤。我们推出了 ClawTrace，这是一个智能体追踪平台，它记录了智能体对话期间的每一次 LLM 调用、工具使用和子智能体生成，并将每个会话编译为 TraceCard：一种包含每步美元成本、Token 计数和冗余标志的紧凑 YAML 摘要。基于 ClawTrace，CostCraft 是一个蒸馏流水线，它读取 TraceCard 并生成三种类型的技能补丁。Preserve（保留）补丁保留导致成功的行为；Prune（剪枝）补丁移除无关紧要的高昂步骤，每个补丁都由针对特定高成本步骤的反事实论据支持；Repair（修复）补丁则基于真值证据修复失败。在 30 个预留的 SpreadsheetBench 任务上的消融实验表明，成本归因和剪枝补丁都能独立减少质量退化。当相同的技能应用于 30 个不相关的 SkillsBench 任务时，出现了一种意想不到的不对称性：剪枝规则可以跨基准测试迁移，并将中位数成本降低了 32%，而基于特定基准惯例训练的保留规则则在新的任务类型上导致了性能退化。我们发布了 ClawTrace 和 TraceCards，作为成本感知智能体研究的开放基础设施。

## Abstract
Skill-distillation pipelines learn reusable rules from LLM agent trajectories, but they lack a key signal: how much each step costs. Without per-step cost, a pipeline cannot distinguish adding a missing step to fix a bug from removing an expensive step that never affected the outcome. We introduce ClawTrace, an agent tracing platform that records every LLM call, tool use, and sub-agent spawn during an agent session and compiles each session into a TraceCard: a compact YAML summary with per-step USD cost, token counts, and redundancy flags. Built on ClawTrace, CostCraft is a distillation pipeline that reads TraceCards and produces three types of skill patches. Preserve patches keep behaviors that led to success. Prune patches remove expensive steps that did not matter, each backed by a counterfactual argument against a named high-cost step. Repair patches fix failures grounded in oracle evidence. Ablations on 30 held-out SpreadsheetBench tasks show that both cost attribution and prune patches independently reduce quality regressions. When the same skill is applied to 30 unrelated SkillsBench tasks, an unexpected asymmetry emerges: prune rules transferred across benchmarks and cut median cost by 32%, while preserve rules, trained on benchmark-specific conventions, caused regressions on new task types. We release ClawTrace and TraceCards as open infrastructure for cost-aware agent research.

---

## 论文详细总结（自动生成）

这是一份关于论文《ClawTrace: Cost-Aware Tracing for LLM Agent Skill Distillation》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
大语言模型（LLM）智能体可以通过从执行轨迹中提取“技能”（结构化的指令包）来提升性能。然而，现有的技能蒸馏流水线存在一个关键缺陷：**缺乏成本感知信号**。
*   **动机**：目前的工具（如 LangSmith, Langfuse）主要为人类开发者提供仪表盘，而非为自动化蒸馏流水线提供可消费的数据。
*   **核心痛点**：如果没有每一步的成本数据，蒸馏系统无法区分“为了修复 Bug 而增加必要步骤”与“为了优化效率而删除昂贵且无用的步骤”。这导致生成的技能往往在提升性能的同时，造成了不必要的成本激增或在处理新任务时出现质量退化。

### 2. 论文提出的方法论
论文提出了一个端到端的框架，包含追踪平台 **ClawTrace** 和蒸馏流水线 **CostCraft**。

#### 核心思想
通过记录带有精确成本标注的执行轨迹，将“正确性”与“效率”分开处理，从而在保证任务成功率的前提下降低运行成本。

#### 关键技术细节
1.  **ClawTrace 追踪平台**：
    *   **事件钩子**：通过 8 个事件钩子（如 `session_start`, `llm_input`, `tool_call` 等）捕获智能体全生命周期。
    *   **成本归因**：精确计算输入、输出、缓存读取（cacheRead）和缓存写入（cacheWrite）的 Token 成本。
    *   **TraceCard 编译**：将原始追踪数据压缩成约 1.5KB 的 YAML 摘要（TraceCard），包含每步美元成本、冗余工具调用标志和子智能体贡献度。
2.  **CostCraft 蒸馏流水线**：
    *   **三类技能补丁**：
        *   **Preserve（保留）**：记录导致成功的关键行为。
        *   **Prune（剪枝）**：针对 TraceCard 中识别的高成本冗余步骤，提出“反事实论据”（即：如果不做这一步，结果是否不变），从而生成删除规则。
        *   **Repair（修复）**：在失败轨迹中，利用 Oracle（真值）证据诊断错误并生成修复规则。
    *   **冲突感知合并**：通过 LLM 层次化合并补丁，优先级顺序为：修复 > 剪枝 > 保留。

### 3. 实验设计
*   **数据集/场景**：
    *   **SpreadsheetBench**：主实验场，涉及 912 个真实的电子表格操作任务，具有确定性的单元格匹配评分。
    *   **SkillsBench**：用于跨基准测试，包含数据处理、科学计算、代码生成等 30 个不相关任务。
*   **Benchmark 与对比方法**：
    *   **Baseline (S0)**：无技能运行。
    *   **消融实验对比**：
        *   **No-cost-attribution**：移除成本信息（模拟 Trace2Skill 的信息环境）。
        *   **No-prune**：移除剪枝补丁。
        *   **No-CF**：移除剪枝时的反事实门控。
*   **评估指标**：质量（Q，0-1 分）、成本（USD）、质量保持率、回归率。

### 4. 资源与算力
*   **模型使用**：所有实验均基于 `openai-codex/gpt-5.4`（注：论文标注日期为 2026 年，此为设定背景下的模型版本）。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号或训练时长。由于该方法属于“训练无关（Training-free）”的适配方法，主要消耗的是 LLM API 的推理成本。
*   **追踪开销**：ClawTrace 的追踪开销极低，仅占智能体运行时间的约 0.30%。

### 5. 实验数量与充分性
*   **实验规模**：
    *   使用了 50 个 SpreadsheetBench 任务（10 个用于训练/进化，30 个用于预留测试，10 个用于开发）。
    *   使用了 30 个 SkillsBench 任务进行跨领域验证。
*   **充分性评价**：
    *   **消融实验**：设计了多组消融实验，证明了成本信号和剪枝补丁的独立价值。
    *   **客观性**：采用了单种子（seed=0）评估，虽然样本量（30个测试任务）在统计学上不算巨大，但通过“机制分区分析”深入探讨了任务失败的分类原因。
    *   **局限性**：实验主要集中在单一模型和特定领域，对于多模型泛化和大规模随机种子的鲁棒性测试尚显不足。

### 6. 论文的主要结论与发现
1.  **成本归因至关重要**：剥离成本信息会导致中位数成本涨幅从 +22% 飙升至 +49%，且灾难性失败（Q=0）显著增加。
2.  **剪枝补丁的保护作用**：令人意外的是，剪枝补丁不仅降低成本，还起到了“护栏”作用。移除剪枝补丁后，质量回归率增加了三倍。
3.  **跨基准迁移的不对称性**：
    *   **剪枝规则**（针对通用浪费模式，如重复读文件）具有极强的通用性，在跨基准测试中使中位数成本降低了 **32%**。
    *   **保留规则**（针对特定任务惯例）在跨基准时容易导致性能退化。

### 7. 优点
*   **创新的中间表示**：TraceCard 提供了一种紧凑、标准且机器可读的格式，解决了 Agent 观测数据难以直接用于蒸馏的问题。
*   **成本与性能的平衡**：首次在技能蒸馏中引入了精确的成本反事实分析，使智能体不仅“聪明”而且“经济”。
*   **开源贡献**：发布了 ClawTrace 平台和 TraceCards 数据集，为成本感知智能体研究提供了基础设施。

### 8. 不足与局限
*   **样本量限制**：训练集仅 10 个任务，导致学习到的剪枝规则种类较少（目前仅覆盖了重复读取和冗余空间文件读取）。
*   **模型单一**：仅在 GPT-5.4 上验证，未测试在较弱模型（如 GPT-4o-mini 或 Llama 系列）上的表现。
*   **启发式依赖**：TraceCard 中的某些字段（如冗余检测、子智能体贡献度）依赖于启发式算法（如 Levenshtein 距离、Jaccard 重叠），在复杂场景下的召回率有待提升。
*   **随机性风险**：由于 LLM 的随机性，单种子实验可能无法完全代表长期运行的稳定性。

（完）
