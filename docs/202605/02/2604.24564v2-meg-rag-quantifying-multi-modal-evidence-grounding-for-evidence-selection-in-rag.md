---
title: "MEG-RAG: Quantifying Multi-modal Evidence Grounding for Evidence Selection in RAG"
title_zh: MEG-RAG：量化多模态证据落地以优化 RAG 中的证据选择
authors: "Xihang Wang, Zihan Wang, Chengkai Huang, Quan Z. Sheng, Lina Yao"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24564v2"
tags: ["query:mmkqa"]
score: 10.0
evidence: 量化多模态证据接地以进行RAG中的证据选择
tldr: 针对多模态检索增强生成（MRAG）中难以区分证据语义支持度与表面相关性的问题，本文提出了MEG指标，通过语义确定性锚点量化证据贡献。基于此构建了MEG-RAG框架，训练多模态重排序器以对齐高价值语义信息。实验证明该方法在M2RAG基准上显著提升了生成准确性与多模态一致性，具有较强的泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.24564v2/fig-001.webp\", \"caption\": \"Table 1: Main comparison of MEG-RAG against baselines on the𝑀2𝑅𝐴𝐺 benchmark tasks across multiple MLLMs. (B-N = Bleu-N, R-N = Rouge-N, R-L = Rouge-L). Bold indicates the best performance, and underline indicates the second-best performance.\", \"page\": 5, \"index\": 1, \"width\": 1056, \"height\": 517}, {\"url\": \"assets/figures/arxiv/2604.24564v2/fig-002.webp\", \"caption\": \"Figure 2: Ablation study on Teacher Model Selection.\", \"page\": 5, \"index\": 2, \"width\": 506, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2604.24564v2/fig-003.webp\", \"caption\": \"Figure 1: Overview of the MEG-RAG framework. (1) Dataset Construction computes MEG scores to quantify the utility of multimodal documents. (2) Reranker Training learns a multimodal reranker using the constructed MEG dataset with a hybrid objective combining document relevance classification and pairwise ranking. (3) MEG-RAG Inference retrieves candidate documents, reranks them to select the top-𝐾 results, and generates the final answer based on the selected evidence.\", \"page\": 3, \"index\": 3, \"width\": 1056, \"height\": 661}]"
motivation: 现有的多模态检索评估指标主要依赖启发式位置置信度，难以准确衡量检索证据对答案语义核心的实际贡献。
method: 提出MEG指标并利用语义确定性锚点关注高IDF信息令牌，进而构建MEG-RAG框架训练重排序器以优化证据选择。
result: 在M2RAG基准测试中，MEG-RAG在多个指标上均优于强基准模型，并展现出跨不同教师模型的鲁棒性。
conclusion: 通过语义接地而非简单的概率分布来优先处理高价值内容，能有效提升多模态大模型生成的准确性和一致性。
---

## 摘要
多模态检索增强生成（MRAG）解决了多模态大语言模型（MLLM）的关键局限性，如幻觉和知识陈旧。然而，当前的 MRAG 系统难以区分检索到的多模态数据是真正支持答案的语义核心，还是仅提供表面相关性。现有指标通常依赖于基于位置的启发式置信度，无法捕捉多模态实体的核心信息密度。为此，我们提出了多模态证据落地（MEG），这是一种量化检索证据贡献的语义感知指标。与标准的置信度度量不同，MEG 利用语义确定性锚定（Semantic Certainty Anchoring），专注于能更好捕捉答案语义核心的高 IDF 信息承载标记。基于 MEG，我们引入了 MEG-RAG 框架，该框架训练一个多模态重排序器，使检索到的证据与真实答案的语义锚点对齐。通过基于语义落地而非标记概率分布来优先处理高价值内容，MEG-RAG 提高了生成输出的准确性和多模态一致性。在 M$^2$RAG 基准测试上的广泛实验表明，MEG-RAG 始终优于强基准模型，并在不同的教师模型中表现出鲁棒的泛化能力。

## Abstract
Multimodal Retrieval-Augmented Generation (MRAG) addresses key limitations of Multimodal Large Language Models (MLLMs), such as hallucination and outdated knowledge. However, current MRAG systems struggle to distinguish whether retrieved multimodal data truly supports the semantic core of an answer or merely provides superficial relevance. Existing metrics often rely on heuristic position-based confidence, which fails to capture the informational density of multimodal entities. To address this, we propose Multi-modal Evidence Grounding (MEG), a semantic-aware metric that quantifies the contribution of retrieved evidence. Unlike standard confidence measures, MEG utilizes Semantic Certainty Anchoring, focusing on high-IDF information-bearing tokens that better capture the semantic core of the answer. Building on MEG, we introduce MEG-RAG, a framework that trains a multimodal reranker to align retrieved evidence with the semantic anchors of the ground truth. By prioritizing high-value content based on semantic grounding rather than token probability distributions, MEG-RAG improves the accuracy and multimodal consistency of generated outputs. Extensive experiments on the M$^2$RAG benchmark show that MEG-RAG consistently outperforms strong baselines and demonstrates robust generalization across different teacher models.

---

## 论文详细总结（自动生成）

这是一份关于论文《MEG-RAG: Quantifying Multi-modal Evidence Grounding for Evidence Selection in RAG》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究背景**：多模态大语言模型（MLLM）虽进展迅速，但仍面临幻觉、知识陈旧等问题。多模态检索增强生成（MRAG）通过引入外部证据来缓解这些问题。
*   **核心问题**：现有的 MRAG 系统在筛选证据时，主要依赖语义相关性或启发式的置信度得分，难以区分检索到的数据是真正提供了“语义支撑”，还是仅仅具有“表面相关性”。
*   **研究动机**：开发一种能够量化检索证据对生成正确答案实际贡献度的指标，并以此训练更精准的重排序器（Reranker）。

### 2. 方法论
论文提出了 **MEG-RAG** 框架，其核心包含以下三个部分：
*   **多模态证据落地指标 (MEG)**：
    *   **核心思想**：通过计算“有证据”和“无证据”两种情况下，教师模型生成正确答案的“语义置信度”之差，来衡量该证据的边际贡献。
    *   **公式逻辑**：$MEG = S(y|x, d) - S(y|x)$。正值表示支持，负值表示干扰，接近零表示冗余。
*   **语义确定性锚定 (Semantic Certainty Anchoring)**：
    *   为了避免被“the”, “is”等高频停用词干扰，利用 **逆文档频率 (IDF)** 动态识别信息承载量大的 Token 作为“锚点”。
    *   仅针对这些锚点计算对数概率的平均值，从而使指标聚焦于实体的语义核心而非语法结构。
*   **多任务重排序器优化**：
    *   **混合损失函数**：结合了**二分类交叉熵损失 (CE Loss)**（学习文档是否相关的绝对判断）和 **RankNet 损失**（学习文档间的相对排序优劣）。
    *   **训练流程**：先利用教师模型生成 MEG 分数构建数据集，再通过混合损失训练一个轻量级的多模态编码器作为重排序器。

### 3. 实验设计
*   **数据集与场景**：使用 **$M^2$RAG** 基准测试，涵盖三个典型任务：
    1.  **多模态问答 (MMQA)**：基于图文证据回答开放域问题。
    2.  **图像描述 (Image Captioning)**：利用检索到的上下文生成图像描述。
    3.  **事实核查 (Fact Verification)**：验证多模态主张的真实性。
*   **对比基准 (Baselines)**：
    *   **None-MRAG**：不使用检索增强。
    *   **Naive-MRAG**：直接使用检索器召回的前 K 个文档。
    *   **Ranking-MRAG**：使用强力基准 **Jina Reranker** 进行重排序。
*   **测试模型**：包括 LLaVA-NeXT、Qwen2.5-VL 以及专门微调过的 MM-RAIT-Qwen2.5-VL。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 台 NVIDIA A100 (80GB)** GPU。
*   **训练细节**：
    *   使用 AdamW 优化器，学习率 2e-5。
    *   Batch Size 为 64（每张 GPU）。
    *   训练时长为 3 个 Epoch，包含前 10% 步数的线性预热。
    *   重排序器架构基于 Jina-m0。

### 5. 实验数量与充分性
*   **实验规模**：构建了包含 **62K 条平衡样本**（正负样本 1:1）的训练集。
*   **实验维度**：
    *   **主实验**：在 3 种不同能力的模型和 3 个任务上进行了全面对比。
    *   **消融实验**：验证了混合损失函数的有效性，并测试了不同教师模型（Qwen vs Llama）对结果的影响。
    *   **敏感性分析**：对损失权重 $\alpha$ 和 MEG 标签阈值 $\tau$ 进行了网格搜索。
*   **客观性**：实验采用了跨任务验证（仅在 MMQA 上训练，在其他任务上测试），证明了方法的通用性和非过拟合性，设计较为严谨。

### 6. 主要结论与发现
*   **性能领先**：MEG-RAG 在所有任务和模型上均显著优于 Jina Reranker。例如在图像描述任务中，CIDEr 得分提升高达 **61.1%**。
*   **泛化能力**：尽管重排序器仅在问答数据上训练，但在事实核查和图像描述任务中依然表现优异，说明 MEG 捕捉的是通用的“信息增益”。
*   **教师模型鲁棒性**：无论使用 Qwen 还是 Llama 作为计算 MEG 分数的教师模型，最终训练出的重排序器性能相近，证明该方法不依赖于特定模型的偏见。

### 7. 优点
*   **指标科学性**：相比于简单的 Token 概率，MEG 结合 IDF 锚定技术，更真实地反映了多模态数据的语义价值。
*   **计算效率**：在推理阶段，重排序器只需输出标量得分，且训练时仅需计算锚点概率，无需处理全词表分布，计算开销可控。
*   **即插即用**：作为重排序模块，可以无缝集成到现有的任何 MRAG 管道中。

### 8. 不足与局限
*   **模态局限**：实验主要集中在“文本+图像”模态，对于视频、音频等时序多模态数据的处理能力尚未在文中得到验证。
*   **数据过滤风险**：为了保证信号纯净，论文丢弃了 80.6% 的中性样本（MEG 在 [-0.2, 0.2] 之间）。虽然提升了训练效率，但也可能导致模型在处理“弱相关”或“部分相关”证据时缺乏细粒度的辨别力。
*   **教师模型依赖**：虽然实验显示对教师模型不敏感，但 MEG 分数的准确性本质上仍受限于教师模型对多模态语义的理解上限。

（完）
