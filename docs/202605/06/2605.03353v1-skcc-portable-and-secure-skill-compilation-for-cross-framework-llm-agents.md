---
title: "SkCC: Portable and Secure Skill Compilation for Cross-Framework LLM Agents"
title_zh: SkCC：面向跨框架 LLM 智能体的可移植且安全的技能编译
authors: "Yipeng Ouyang, Yi Xiao, Yuhao Gu, Xianwei Zhang"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03353v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 优化跨框架LLM智能体的提示词格式
tldr: "SkCC是一个针对大模型智能体技能的编译框架，旨在解决不同框架间提示词格式敏感导致的性能差异及安全漏洞问题。它引入了强类型中间表示SkIR，实现了技能语义与平台格式的解耦，将适配复杂度从O(m*n)降至O(m+n)。通过编译时安全分析和多阶段流水线，SkCC在提升任务成功率的同时，显著降低了运行时的Token消耗，为跨平台智能体技能的开发与部署提供了高效、安全的解决方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.03353v1/fig-001.webp\", \"caption\": \"Figure 4. Cross-model comparison of Original vs. Compiled conditions across four agent frameworks. Compiled consistently outperforms Original, with the largest gains on format-sensitive models (Claude, Kimi).\", \"page\": 6, \"index\": 1, \"width\": 493, \"height\": 280}, {\"url\": \"assets/figures/arxiv/2605.03353v1/fig-002.webp\", \"caption\": \"Figure 2. SkCC’s four-phase compilation pipeline. A unified SKILL.md source is parsed into a raw AST, transformed into a strongly-typed SkIR, validated and hardened by the Analyzer, and emitted into platform-native formats through polymorphic Emitters.\", \"page\": 3, \"index\": 2, \"width\": 1048, \"height\": 259}, {\"url\": \"assets/figures/arxiv/2605.03353v1/fig-003.webp\", \"caption\": \"Figure 6. Ablation study radar chart: the same Kimicompiled format produces divergent effects across threemodels, confirming model-specificity of compilation gains.\", \"page\": 8, \"index\": 3, \"width\": 490, \"height\": 432}, {\"url\": \"assets/figures/arxiv/2605.03353v1/fig-004.webp\", \"caption\": \"Figure 3. Agent workflow with SkCC integration. Skills are authored once as SKILL.md, compiled to platform-native formats, and loaded via progressive routing manifests at agent initialization.\", \"page\": 5, \"index\": 4, \"width\": 499, \"height\": 525}]"
motivation: 不同智能体框架对提示词格式敏感导致性能波动大，且现有技能存在安全漏洞且难以跨平台维护。
method: 提出SkCC编译框架，利用强类型中间表示SkIR解耦语义与格式，并结合编译时分析器进行安全约束检查。
result: "在SkillsBench上，编译后的技能使Claude Code和Kimi CLI的通过率分别提升了约12%和13%，同时节省了10-46%的Token。"
conclusion: SkCC通过引入经典编译器设计思想，成功实现了LLM智能体技能的高效移植、性能优化与安全增强。
---

## 摘要
LLM 智能体已演变为执行复杂任务的自主系统，SKILL.md 规范已成为封装智能体能力的事实标准。然而，一个关键瓶颈仍然存在：不同的智能体框架对提示词格式的敏感度差异巨大，导致性能波动高达 40%，但几乎所有技能都以单一且与格式无关的 Markdown 版本存在。针对每个平台进行手动重写会带来不可持续的维护负担，而之前的审计发现，超过三分之一的社区技能包含安全漏洞。为解决这一问题，我们提出了 SkCC，这是一个将经典编译器设计引入智能体技能开发的编译框架。其核心是 SkIR——一种强类型中间表示，它将技能语义与特定平台的格式解耦，从而实现在异构智能体框架间的可移植部署。围绕该中间表示，编译时分析器在部署前通过反技能注入强制执行安全约束。通过四阶段流水线，SkCC 将适配复杂度从 O(m x n) 降低到 O(m + n)。在 SkillsBench 上的实验表明，编译后的技能表现始终优于原始版本，在 Claude Code 上的通过率从 21.1% 提升至 33.3%，在 Kimi CLI 上从 35.1% 提升至 48.7%，同时实现了低于 10ms 的编译延迟、94.8% 的主动安全触发率，并在不同平台上节省了 10-46% 的运行时 Token。

## Abstract
LLM-Agents have evolved into autonomous systems for complex task execution, with the SKILL.md specification emerging as a de facto standard for encapsulating agent capabilities. However, a critical bottleneck remains: different agent frameworks exhibit starkly different sensitivities to prompt formatting, causing up to 40% performance variation, yet nearly all skills exist as a single, format-agnostic Markdown version. Manual per-platform rewriting creates an unsustainable maintenance burden, while prior audits have found that over one third of community skills contain security vulnerabilities. To address this, we present SkCC, a compilation framework that introduces classical compiler design into agent skill development. At its core, SkIR - a strongly-typed intermediate representation - decouples skill semantics from platform-specific formatting, enabling portable deployment across heterogeneous agent frameworks. Around this IR, a compile-time Analyzer enforces security constraints via Anti-Skill Injection before deployment. Through a four-phase pipeline, SkCC reduces adaptation complexity from $O(m \times n)$ to $O(m + n)$. Experiments on SkillsBench demonstrate that compiled skills consistently outperform their original counterparts, improving pass rates from 21.1% to 33.3% on Claude Code and from 35.1% to 48.7% on Kimi CLI, while achieving sub-10ms compilation latency, a 94.8% proactive security trigger rate, and 10-46% runtime token savings across platforms.

---

## 论文详细总结（自动生成）

### 论文总结：SkCC - 面向跨框架 LLM 智能体的可移植且安全的技能编译

#### 1. 论文的核心问题与整体含义
随着大语言模型（LLM）智能体的发展，**技能（Skills）**已成为封装特定领域知识的标准方式（如 `SKILL.md` 规范）。然而，当前生态面临两大核心挑战：
*   **格式敏感性（Format Sensitivity）**：不同的智能体框架（如 Claude Code, Kimi CLI 等）对提示词格式极其敏感，同一技能在不同框架下的性能差异可达 40%。
*   **维护与安全瓶颈**：为 $M$ 个技能适配 $N$ 个平台需要 $O(M \times N)$ 的手动重写工作量；同时，约 37% 的社区技能存在安全漏洞或恶意负载。
**SkCC** 的提出旨在通过“编译器”思路，将技能语义与平台格式解耦，实现一次编写、多端安全部署，将适配复杂度降低至 $O(M + N)$。

#### 2. 论文提出的方法论
SkCC 引入了经典的四阶段编译器架构：
*   **Frontend（前端）**：解析原始 `SKILL.md`，将 YAML 元数据与 Markdown 正文解耦，生成原始抽象语法树（AST）。
*   **IR Construction（中间表示构建）**：将 AST 转换为强类型的 **SkIR**（中间表示）。SkIR 包含元数据、接口模式（MCP）、安全控制、执行逻辑等六大类信息，并能自动检测嵌套数据深度以优化格式选择。
*   **Analyzer（分析器）**：在编译时进行语义验证和安全加固。核心技术是 **Anti-Skill Injection（反技能注入）**，通过扫描程序文本中的危险模式（如 HTTP 超时缺失、循环无上限、破坏性数据库操作），自动在 IR 中注入防御性约束。
*   **Backend（后端）**：多目标发射器。根据目标模型的“偏好”生成特定格式：
    *   **Claude**：采用 XML 语义分层。
    *   **Codex**：采用带 XML 标签的 Markdown。
    *   **Gemini**：采用 Markdown 结合条件化 YAML（针对深层嵌套数据）。
    *   **Kimi**：采用全量 Markdown 保持。

#### 3. 实验设计
*   **数据集/场景**：使用 **SkillsBench** 基准测试，包含 89 个真实的编程和数据分析任务，具备 Docker 隔离执行环境和自动化测试验证。
*   **对比对象**：
    *   **Original (O)**：目前社区通用的、格式无关的原始技能文件。
    *   **Compiled (C)**：经 SkCC 编译后、针对特定平台优化的技能文件。
*   **对比方法**：除了与原始版本对比，还与 SOTA 的检索增强优化方法（Liu et al.）和 SkVM（基于虚拟机的方案）进行了性能增益对比。

#### 4. 资源与算力
*   **硬件环境**：实验在标准开发机器上运行（Intel i9-13900H CPU, 32GB RAM）。
*   **算力消耗**：由于 SkCC 是一个编译器框架而非模型训练任务，其核心开销在于编译延迟。论文指出单次编译延迟**低于 10ms**，批量处理 225 个技能仅需约 1.8 秒。
*   **模型调用**：实验调用了 Claude-4-6, GPT-5.3-Codex, Gemini-2.5-Pro, Kimi-K2.5 等商业 API 进行推理验证。

#### 5. 实验数量与充分性
*   **实验规模**：对 4 个主流平台进行了完整基准测试，收集并分析了 225 个真实社区技能。
*   **消融实验**：专门设计了跨模型格式交叉验证（如用 Kimi 格式跑 GLM 和 DeepSeek），证明了编译增益的**模型相关性**，而非通用的提示词优化。
*   **客观性**：实验采用了 Pass@1 和 Mean Reward 等硬性指标，并进行了统计显著性检验（p-value, Cohen's d），排除了因网络或 API 崩溃导致的异常数据，实验设计较为严谨、公平。

#### 6. 论文的主要结论与发现
*   **性能显著提升**：编译后的技能在 Claude Code 上通过率从 21.1% 提升至 **33.3%**，在 Kimi CLI 上从 35.1% 提升至 **48.7%**。
*   **Token 效率优化**：尽管编译增加了少量静态结构开销（4-25%），但由于结构更清晰减少了模型的尝试次数，实际运行时节省了 **10-46%** 的 Token。
*   **安全加固有效**：Anti-Skill Injection 覆盖了 94.8% 的评估技能，在编译阶段拦截了多项潜在风险。
*   **格式敏感性确认**：实验证实了不同模型对 XML、YAML、JSON 的解析偏好确实存在，且这种偏好是模型特有的。

#### 7. 优点
*   **架构优雅**：首次将经典编译器设计（Frontend-IR-Backend）完整引入智能体技能开发，解决了 $O(M \times N)$ 的扩展性难题。
*   **安全前置**：不同于运行时的安全检查，SkCC 在编译阶段强制注入安全约束，具有更高的确定性和防御性。
*   **实用性强**：不仅提升了成功率，还通过减少 Token 消耗直接降低了智能体的运行成本。

#### 8. 不足与局限
*   **规则依赖性**：反技能注入（Anti-Skill Injection）目前依赖于预定义的启发式规则，对于新型或复杂的攻击模式可能需要持续更新规则库。
*   **平台覆盖**：虽然覆盖了四大主流框架，但对于更多新兴的开源智能体框架（如 AutoGPT 等）的适配策略尚待进一步研究。
*   **动态性不足**：目前的编译是静态的，尚未实现根据运行时反馈动态调整编译策略的闭环。

（完）
