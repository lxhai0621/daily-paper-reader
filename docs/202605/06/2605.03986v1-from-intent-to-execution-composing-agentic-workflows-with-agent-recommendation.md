---
title: "From Intent to Execution: Composing Agentic Workflows with Agent Recommendation"
title_zh: 从意图到执行：通过智能体推荐构建智能体工作流
authors: "Kishan Athrey, Ramin Pishehvar, Brian Riordan, Mahesh Viswanathan"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03986v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 自动创建多智能体系统并根据用户意图优化智能体工作流
tldr: 本研究针对多智能体系统构建中手动规划和选择智能体的低效问题，提出了一种自动化工作流框架。该框架集成了LLM规划器、动态调用图和基于两阶段检索的智能体推荐系统。通过引入批评智能体进行全局评估，该方法在召回率和可扩展性上均优于现有技术，实现了从用户意图到自动化执行的高效转化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.03986v1/fig-001.webp\", \"caption\": \"Figure 3: Agent description enrichment by appending synthetic queries generated from the agent description.\", \"page\": 6, \"index\": 1, \"width\": 989, \"height\": 368}, {\"url\": \"assets/figures/arxiv/2605.03986v1/fig-002.webp\", \"caption\": \"Figure 5: ReAct Style Planning architecture with integrated critique loop. The agent node generates task decompositions and queries the agent recommender iteratively. Upon completion, the extraction stage produces structured predictions which are evaluated by the critique node. When deficiencies are identified and the iteration budget permits, the feedback node provides targeted corrections that preserve valid aspects while addressing identified issues.\", \"page\": 11, \"index\": 2, \"width\": 850, \"height\": 269}, {\"url\": \"assets/figures/arxiv/2605.03986v1/fig-003.webp\", \"caption\": \"Table 9: Metrics for ReAct Style with Structured Prediction. ↑ higher is better, ↓ lower is better.\", \"page\": 11, \"index\": 3, \"width\": 848, \"height\": 380}, {\"url\": \"assets/figures/arxiv/2605.03986v1/fig-004.webp\", \"caption\": \"Table 5: AR – Agent Description Enrichment experiments (Re-ranker = gpt-4o). The number of synthesized queries is the augmentation described in section 4.4.2\", \"page\": 8, \"index\": 4, \"width\": 758, \"height\": 203}, {\"url\": \"assets/figures/arxiv/2605.03986v1/fig-005.webp\", \"caption\": \"Figure 1: Architecture for an end-to-end MAS with dynamic and redundant workflow\", \"page\": 4, \"index\": 5, \"width\": 943, \"height\": 594}, {\"url\": \"assets/figures/arxiv/2605.03986v1/fig-006.webp\", \"caption\": \"Table 10: Metrics for ReAct Style with critique and Iterative Correction. ↑ higher is better, ↓ lower is better.\", \"page\": 12, \"index\": 6, \"width\": 848, \"height\": 381}, {\"url\": \"assets/figures/arxiv/2605.03986v1/fig-007.webp\", \"caption\": \"Table 6: Our best methods comparison with Re-invoke paper (Hybrid value = 0.9)\", \"page\": 9, \"index\": 7, \"width\": 987, \"height\": 510}, {\"url\": \"assets/figures/arxiv/2605.03986v1/fig-008.webp\", \"caption\": \"Figure 2: Agent Recommender that includes the enrich, retrieve, and re-rank phases.\", \"page\": 5, \"index\": 8, \"width\": 1037, \"height\": 315}]"
motivation: 现有的多智能体系统构建依赖于繁琐的手动规划、智能体选择和执行图创建，限制了开发效率。
method: 提出一个包含LLM规划器、动态调用图和两阶段智能体推荐系统（检索+重排序）的自动化框架，并引入批评智能体进行优化。
result: 实验结果显示，该方法在召回率上超越了现有技术，并表现出更强的鲁棒性和可扩展性。
conclusion: 自动化智能体推荐与批评机制的结合是构建高效端到端多智能体系统的关键步骤。
---

## 摘要
使用 AI 智能体构建的多智能体系统 (MAS) 能够满足各种用户意图，可用于设计和构建一系列相关应用。然而，目前此类 MAS 的创建涉及手动编写计划、手动选择合适的智能体以及手动创建执行图。本文介绍了一种用于自动创建多智能体系统的框架，该框架用自动化流程取代了多个手动步骤。所提出的框架由软件模块和用于编排所需特定任务应用的工作流组成。这些模块包括：一个基于大语言模型 (LLM) 的规划器、一组以自然语言描述的任务、一个动态调用图、一个用于将智能体映射到任务的编排器，以及一个从本地和全局智能体注册表中寻找最合适智能体的智能体推荐器。智能体推荐器使用一个两阶段信息检索 (IR) 系统，包含一个快速检索器和一个基于 LLM 的重排序器。我们进行了一系列实验，探索了嵌入器、重排序器、智能体描述增强以及监督评价智能体 (critique agent) 的选择。我们对该系统进行了端到端基准测试，评估了所提方法在规划、智能体选择和任务完成方面的综合表现。实验结果表明，我们的方法在召回率方面优于现有技术，且与以往方法相比更具鲁棒性和可扩展性。评价智能体根据整体计划全面重新评估智能体和工具的推荐。我们证明，引入评价智能体进一步提高了召回率，证明了对基于任务的智能体选择进行全面审查和修正是构建端到端多智能体系统的关键步骤。

## Abstract
Multi-Agent Systems (MAS) built using AI agents fulfill a variety of user intents that may be used to design and build a family of related applications. However, the creation of such MAS currently involves manual composition of the plan, manual selection of appropriate agents, and manual creation of execution graphs. This paper introduces a framework for the automated creation of multi-agent systems which replaces multiple manual steps with an automated framework. The proposed framework consists of software modules and a workflow to orchestrate the requisite task- specific application. The modules include: an LLM-derived planner, a set of tasks described in natural language, a dynamic call graph, an orchestrator for map agents to tasks, and an agent recommender that finds the most suitable agent(s) from local and global agent registries. The agent recommender uses a two-stage information retrieval (IR) system comprising a fast retriever and an LLM-based re-ranker. We implemented a series of experiments exploring the choice of embedders, re- rankers, agent description enrichment, and supervising critique agent. We benchmarked this system end-to-end, evaluating the combination of planning, agent selection, and task completion, with our proposed approach. Our experimental results show that our approach outperforms the state-of-the- art in terms of the recall rate and is more robust and scalable compared to previous approaches. The critique agent holistically reevaluates both agent and tool recommendations against the overall plan. We show that the inclusion of the critique agent further enhances the recall score, proving that the comprehensive review and revision of task-based agent selection is an essential step in building end-to-end multi-agent systems.

---

## 论文详细总结（自动生成）

这篇论文由思科系统（Cisco Systems）的研究团队撰写，提出了一种名为 **AutoMAS** 的框架，旨在实现从用户自然语言意图到多智能体系统（MAS）执行工作流的自动化构建。

以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：目前构建多智能体系统高度依赖人工，包括手动编写任务计划、手动从海量智能体库中筛选合适的智能体、以及手动构建执行图。
*   **研究动机**：随着专用 AI 智能体数量的爆炸式增长，如何高效、准确地在成千上万个智能体中找到“最合适”的一个（即“智能体堆里的绣花针”问题）成为了瓶颈。传统的关键词搜索或将所有智能体描述放入 LLM 上下文的方法在可扩展性、成本和延迟方面表现不佳。

### 2. 方法论：AutoMAS 框架
AutoMAS 包含以下核心组件和流程：
*   **LLM 规划器 (Planner)**：将用户意图（如“帮我创建一个订餐应用”）分解为自然语言描述的子任务序列，并将其表示为**有限状态机 (FSM)**，以支持非线性逻辑和冗余路径。
*   **变量调用图 (Variable Call Graph, VCG)**：一种动态重路由机制。它根据智能体推荐器返回的多个候选者，结合用户约束（如最低延迟、最低成本）动态选择执行路径。
*   **两阶段智能体推荐器 (Agent Recommender)**：
    *   **第一阶段（检索器）**：使用混合搜索（向量嵌入 + 关键词匹配）从注册表中快速召回高相关性的候选智能体。
    *   **第二阶段（重排序器）**：利用 LLM 对候选列表进行深度语义评估和排序，提升精度。
*   **智能体描述增强 (Enrichment)**：在索引阶段，利用 LLM 为每个智能体生成多个合成查询（Synthetic Queries），并将其与原始描述合并后取嵌入向量的平均值，以增强检索的鲁棒性。
*   **批评机制 (Critique Mechanism)**：引入“批评智能体”对推荐结果进行全局审查，检查智能体之间的输入输出兼容性、是否符合用户约束以及整体计划的逻辑一致性。

### 3. 实验设计
*   **数据集**：
    *   **ToolE (MetaTool)**：包含 199 个工具和 20,550 个查询对，用于优化和评估智能体推荐算法。
    *   **TaskBench**：包含 103 个智能体和 17,000 个查询，涵盖日常生活、Hugging Face 和多媒体 API，用于评估端到端的规划与组合能力。
*   **对比方法 (Baselines)**：对比了 **Re-Invoke**（基于查询重写的检索方法）以及传统的 BM25 和向量检索方法。
*   **评估指标**：
    *   **推荐指标**：nDCG、Recall@K、mAP。
    *   **端到端指标**：Rouge、BERTScore、结构化预测指标（工具选择 F1、序列相似度、编辑距离）以及 **LLM-as-a-Judge**（整体匹配率和步骤匹配率）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **模型使用**：实验主要基于 OpenAI 的模型（如 `gpt-4o`、`o1`、`text-embedding-3-large`）通过 API 调用完成，因此更多关注推理逻辑和系统架构而非底层模型训练。

### 5. 实验数量与充分性
*   **实验规模**：研究团队进行了多维度的消融实验，包括：
    *   不同嵌入模型（small vs. large）的对比。
    *   不同重排序模型（gpt-4o vs. o1）的效果。
    *   合成查询数量（3, 5, 10 个）对检索质量的影响。
    *   引入批评机制前后的性能提升对比。
*   **充分性评价**：实验设计较为充分，涵盖了从单一组件（推荐器）到端到端系统（规划+推荐）的完整链路，且在两个主流基准数据集上进行了验证，结果具有统计意义。

### 6. 主要结论与发现
*   **两阶段检索的优越性**：相比于将所有工具放入上下文，两阶段检索在处理大规模智能体库时更具可扩展性，且通过重排序显著提升了 Top-1 准确率。
*   **增强描述的价值**：通过合成查询增强智能体元数据，能有效弥补用户查询与智能体文档之间的语义鸿沟。
*   **批评机制的关键作用**：批评智能体能显著提升召回率，因为它能从全局视角纠正局部推荐的错误，确保工作流的可执行性。
*   **性能表现**：在 TaskBench 任务中，系统达到了约 59.3% 的整体匹配率和 74.2% 的步骤匹配率，优于现有的 SOTA 方法。

### 7. 优点与亮点
*   **动态性与鲁棒性**：提出的变量调用图（VCG）允许系统在运行时根据环境变化（如某个智能体宕机）或用户约束（成本 vs 速度）动态切换路径。
*   **可扩展架构**：通过解耦检索和推理，AutoMAS 能够处理包含数千个智能体的注册表，解决了 LLM 上下文长度限制的问题。
*   **评估方法创新**：除了传统的文本相似度指标，引入了结构化预测评估和 LLM 评测，更客观地反映了工作流的实际可用性。

### 8. 不足与局限
*   **LLM 指令遵循偏差**：实验发现 LLM 重排序器有时存在固有偏见（例如即便要求优化高成本/高精度，模型仍倾向于选择低成本智能体）。
*   **批评机制的局限**：目前的批评机制只能在检索出的候选池中进行重排或过滤，如果检索阶段（第一阶段）未能召回正确智能体，批评机制无法凭空创造出正确的选择。
*   **成本与延迟**：虽然比全上下文方法快，但多轮 LLM 调用（规划、重排序、批评、反馈循环）在实时应用中仍可能带来较高的延迟和 API 开销。

（完）
