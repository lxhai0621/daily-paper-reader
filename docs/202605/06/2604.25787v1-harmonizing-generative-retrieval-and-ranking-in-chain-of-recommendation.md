---
title: Harmonizing Generative Retrieval and Ranking in Chain-of-Recommendation
title_zh: 在推荐链中协调生成式检索与排序
authors: "Yu Liu, Jiangxia Cao"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25787v1"
tags: ["query:ma-kf"]
score: 6.5
evidence: 使用Transformer骨干的生成式检索与排序框架
tldr: 本研究针对生成式推荐系统中生成候选与精准排序脱节的问题，提出了RecoChain框架。该框架在单一Transformer模型中统一了检索与排序过程：首先通过层次化语义ID预测生成候选项目，随后利用SIM机制对候选进行连续排序。实验证明，RecoChain有效弥合了生成与排序间的性能鸿沟，在大规模数据集上显著提升了Top-K推荐的准确性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.25787v1/fig-001.webp\", \"caption\": \"Figure 2: The training token organization of RecoChain: (1) first predict the next item Semantic ID generation, (2) then conduct the item candidate aware sub-sequence search as following input tokens to measure ranking score.\", \"page\": 2, \"index\": 1, \"width\": 1002, \"height\": 311}]"
motivation: 生成式推荐系统在通过语义ID生成候选后，难以在大量候选项中进行精准排序，存在生成与排序性能不匹配的问题。
method: 提出RecoChain框架，利用单一Transformer骨干网络，结合层次化语义ID生成和SIM排序机制实现端到端推荐。
result: 在大规模真实世界数据集上的实验结果显示，该方法在保持生成能力的同时，显著提高了Top-K推荐的性能。
conclusion: RecoChain通过在链式推荐中协调生成式检索与排序，为构建统一且高效的生成式推荐系统提供了有效方案。
---

## 摘要
生成式推荐系统最近作为一种极具前景的范式出现，它将下一项预测建模为自回归语义 ID 生成，例如 OneRec 系列工作。然而，在这种与下一项无关的预测范式下，它可以通过语义 ID 束搜索出一些潜在的下一项，但难以从中评估哪些项更好（例如，从 256 个候选项中选择前 10 个），从而导致生成与排序性能之间存在差距。为了弥补这一差距，我们提出了 RecoChain，这是一个统一的生成式检索与排序框架，在单个 Transformer 骨干网络中集成了候选生成和排序。具体而言，在推理过程中，模型首先通过分层语义 ID 预测生成候选项，然后执行基于 SIM 的排序过程，以持续评估相应候选项的点击概率。在大规模真实世界数据集上的广泛实验表明，我们的方法有效地弥合了生成式检索与排序之间的差距，在保持强大生成能力的同时，提升了 Top-K 推荐性能。

## Abstract
Generative recommender systems have recently emerged as a promising paradigm by formulating next-item prediction as an auto-regressive semantic IDs generation, such as OneRec series works. However, with the next-item-agnostic prediction paradigm, its could beam out some next potential items via Semantic IDs but hard to estimate which items are better from them, e.g., select the top-10 from beam-256 items, leading to a gap between generation and ranking performance. To fulfill this gap, we propose RecoChain, a unified generative retrieval and ranking framework that integrates candidate generation and ranking within a single Transformer backbone. Specifically, in inference, the model first generates candidate items via hierarchical semantic ID prediction, then performs the SIM-based ranking process to estimate the click possibility of corresponding item candidate continuously. Extensive experiments on large-scale real-world datasets demonstrate that our approach effectively bridges the gap between generative retrieval and ranking, achieving improved Top-K recommendation performance while maintaining strong generative capability.