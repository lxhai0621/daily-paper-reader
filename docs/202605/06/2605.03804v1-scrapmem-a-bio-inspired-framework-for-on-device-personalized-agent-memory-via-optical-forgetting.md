---
title: "ScrapMem: A Bio-inspired Framework for On-device Personalized Agent Memory via Optical Forgetting"
title_zh: ScrapMem：一种受生物启发、通过光学遗忘实现端侧个性化智能体记忆的框架
authors: "Jiale Chang, Yuxiang Ren"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03804v1"
tags: ["query:agent"]
score: 10.0
evidence: 通过光学遗忘机制实现大模型智能体的长期个性化记忆
tldr: "ScrapMem是一个受生物启发的端侧长效个性化记忆框架，旨在解决边缘设备存储受限和多模态数据复杂的问题。它通过“剪贴簿页”整合多模态数据，并引入“光学遗忘”机制，通过降低旧记忆分辨率来压缩存储。同时，利用情节记忆图（EM-Graph）构建因果时序结构，在保持语义一致性的同时显著降低了93%的存储成本，并在ATM-Bench上取得了SOTA性能。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.03804v1/fig-001.webp\", \"caption\": \"Table 1: Overall performance comparison across models on the ATM-Bench dataset. QS is the overall comprehensive metric. N, R, and O denote the accuracy of Number, List Recall, and Open-ended questions. ScrapMem (No-Forget) achieves state-of-the-art results on Joint@10 and the R@10, and surpasses existing baseline methods across key metrics.\", \"page\": 7, \"index\": 1, \"width\": 758, \"height\": 558}, {\"url\": \"assets/figures/arxiv/2605.03804v1/fig-002.webp\", \"caption\": \"Figure 3: Retrieval performance (Recall@K) under varying optical forgetting intensities. The clustering of different forgetting curves demonstrates that ScrapMem is highly robust to specific hyperparameter configurations.\", \"page\": 7, \"index\": 2, \"width\": 445, \"height\": 309}, {\"url\": \"assets/figures/arxiv/2605.03804v1/fig-003.webp\", \"caption\": \"Figure 2: Overview of the ScrapMem. (1) Consolidation and Perception: Unifies heterogeneous records (images, videos, text) into hybrid representations via OCR and vision-to-text extraction. (2) EM-Graph Construction: Organizes nodes into an Episodic Memory Graph with event-centric paths (EM-Paths) for structured retrieval and multi-hop reasoning. (3) Optical Forgetting: Compresses outdated memories through temporal degradation to reduce costs while preserving salient knowledge. The system retrieves relevant EM-Paths to generate personalized, evidence-grounded responses.\", \"page\": 5, \"index\": 3, \"width\": 939, \"height\": 479}, {\"url\": \"assets/figures/arxiv/2605.03804v1/fig-004.webp\", \"caption\": \"Figure 1: Comparison between human memory (CLS theory) and Scrapbook Memory. Top: The hippocampus rapidly encodes multimodal episodic experiences, while the neocortex gradually consolidates them into stable long-term knowledge. Bottom: ScrapMem similarly binds heterogeneous user data into scrapbook pages and progressively compresses old memories via optical forgetting, preserving core semantics for efficient retrieval and personalized reasoning on edge devices.\", \"page\": 2, \"index\": 4, \"width\": 461, \"height\": 688}, {\"url\": \"assets/figures/arxiv/2605.03804v1/fig-005.webp\", \"caption\": \"Figure 4: Storage–performance trade-off on ATMBench (Joint@10). The x-axis uses a logarithmic scale. ScrapMem (Timed-Gentle, orange star) reduces storage by 93.0% relative to the raw-data baseline while retaining over 90% of SOTA performance (46.3% vs. 51.0%). The Pareto frontier indicates strong efficiency and graceful performance degradation, supporting ondevice deployment.\", \"page\": 8, \"index\": 5, \"width\": 445, \"height\": 297}]"
motivation: 针对边缘设备在处理LLM智能体长效多模态记忆时面临的高存储成本和复杂性挑战。
method: 提出“光学遗忘”机制进行渐进式分辨率压缩，并构建情节记忆图（EM-Graph）以组织因果时序结构。
result: "在ATM-Bench上达到51.0%的Joint@10得分，存储空间减少高达93%，召回率提升至70.3%。"
conclusion: ScrapMem为端侧多模态智能体提供了一种高效且节省存储的长效记忆解决方案。
---

## 摘要
由于高昂的存储成本和多模态复杂性，在资源受限的边缘设备上实现大语言模型（LLM）智能体的长期个性化记忆具有挑战性。为解决这一问题，我们提出了 ScrapMem，这是一个将多模态数据整合到“剪贴簿页面”（Scrapbook Page）中的框架。ScrapMem 引入了光学遗忘（Optical Forgetting），这是一种光学压缩机制，通过逐步降低旧记忆的分辨率，在抑制低价值细节的同时降低存储成本。为了保持语义一致性，我们构建了一个情景记忆图（EM-Graph），将关键事件组织成因果-时间结构。在多模态 ATM-Bench 上的广泛实验表明，ScrapMem 具有三个主要优势：(1) 性能强劲，以 51.0% 的 Joint@10 分数达到了新的 SOTA 水平；(2) 存储效率高，通过光学遗忘减少了高达 93% 的内存占用；(3) 召回率提升，通过结构化聚合将 Recall@10 提高到 70.3%。ScrapMem 为多模态 LLM 智能体在端侧实现长期记忆提供了一种有效且节省存储空间的解决方案。

## Abstract
Long-term personalized memory for LLM agents is challenging on resource-limited edge devices due to high storage costs and multimodal complexity. To address this, we propose ScrapMem, a framework that integrates multimodal data into "Scrapbook Page." ScrapMem introduces Optical Forgetting, an optical compression mechanism that progressively reduces the resolution of older memories, lowering storage cost while suppressing low-value details. To maintain semantic consistency, we construct an Episodic Memory Graph (EM-Graph) that organizes key events into a causal-temporal structure. Extensive experiments on the multimodal ATM-Bench showcase that ScrapMem provides three main benefits: (1) strong performance, achieving a new state-of-the-art with a 51.0% Joint@10 score; (2) high storage efficiency, reducing memory usage by up to 93% via optical forgetting; and (3) improved recall, increasing Recall@10 to 70.3% through structured aggregation. ScrapMem offers an effective and storage-efficient solution for on-device long-term memory in multimodal LLM agents.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **ScrapMem** 的创新框架，旨在解决边缘设备上大语言模型（LLM）智能体在处理长期、多模态个性化记忆时面临的存储与计算挑战。

以下是对该论文的深度结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：随着 LLM 智能体部署到边缘设备（如手机、穿戴设备），实现长期个性化交互需要整合海量的多模态数据（照片、邮件、对话等）。
*   **核心挑战**：
    1.  **存储成本高**：多模态数据占用空间巨大，边缘设备难以承受。
    2.  **上下文窗口限制**：现有模型难以处理超长历史。
    3.  **对齐困难**：传统检索方法难以在统一语义空间内有效对齐文本、图像和视频，导致因果和时序推理能力较弱。
*   **整体含义**：受生物学“互补学习系统（CLS）”理论启发，论文提出将记忆模拟为“剪贴簿”，通过视觉压缩和结构化图组织，实现高效的端侧长效记忆。

### 2. 方法论：核心思想与技术细节
ScrapMem 框架包含三个关键阶段：
*   **剪贴簿页面整合与光学感知（Scrapbook Page Consolidation）**：
    *   将一段时间内的异构数据（图、文、视）整合为统一的“剪贴簿页面”。
    *   利用视觉编码器和 OCR 技术将页面转化为视觉 Token 和文本。
    *   通过 LLM 提取语义节点，并保留原始视觉 Token 作为视觉节点，形成混合节点集。
*   **情节记忆图构建（EM-Graph Construction）**：
    *   将节点组织成具有因果和逻辑链条的“情节记忆路径（EM-Paths）”。
    *   引入 **Q-矩阵（关联矩阵）**：通过二进制关联矩阵 $Q \in \{0, 1\}^{|P| \times |V|}$ 记录节点与路径的关系，实现高效的语义重叠匹配和多跳推理。
*   **光学遗忘机制（Optical Forgetting）**：
    *   **核心公式**：$S_t^{(\tau)} = D_\tau(S_t)$，其中 $D_\tau$ 是随时间 $\tau$ 增加的损耗变换算子。
    *   **实现方式**：通过降低 JPEG 质量（Q）和分辨率缩放因子（S）来模拟生物记忆的衰退。
    *   **效果**：逐步过滤掉低价值的视觉细节，仅保留核心语义结构，从而大幅压缩存储空间。

### 3. 实验设计
*   **数据集**：使用 **ATM-Bench**，这是一个包含 4 年真实世界个人数据（邮件、图像、视频）的挑战性多模态个性化记忆基准。
*   **评估指标**：QS（问题类型得分）、Recall@10（检索质量）、Joint@10（端到端性能）。
*   **对比方法**：
    *   **记忆智能体**：A-Mem, Mem0。
    *   **RAG 系统**：HippoRAG2, ATM-RAG, Self-RAG。
    *   **基准线**：Oracle（真值证据）和 No-Evidence（无证据）。

### 4. 资源与算力
*   **模型使用**：
    *   **LLM 评测器**：GPT-5-mini（通过 OpenAI API）。
    *   **记忆处理器**：Qwen3-VL-2B-Instruct。
    *   **检索嵌入模型**：all-MiniLM-L6-v2。
*   **算力说明**：文中**未明确说明**具体的硬件训练环境（如 GPU 型号、数量或训练时长）。由于主要依赖现有的预训练模型和 API，实验可能侧重于推理和系统架构的验证。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了主实验对比（15 组系统配置）。
    *   **灵敏度分析**：针对 5 种不同的遗忘策略（从 Very_Soft 到 Boundary_365）进行了对比。
    *   **存储-性能权衡实验**：分析了不同压缩率下的性能表现。
*   **充分性评价**：实验设计较为充分，涵盖了检索精度、端到端问答准确率以及存储效率等多个维度。通过对比多种 SOTA 方法，证明了其在复杂多模态场景下的优越性。

### 6. 主要结论与发现
*   **性能领先**：ScrapMem 在 Joint@10 上达到 51.0%，刷新了 ATM-Bench 的记录。
*   **极高的存储效率**：通过光学遗忘机制，在仅损失不到 10% 性能的情况下，**存储空间减少了 93%**（从 4.3GB 降至 299.5MB）。
*   **检索鲁棒性**：即使在视觉信息严重降级的情况下，由于 EM-Graph 提供了稳健的语义骨架，Recall@10 仍能保持在 66% 以上。
*   **列表召回能力强**：在处理碎片化证据的 List Recall 任务上，表现显著优于传统 RAG 方法。

### 7. 优点与亮点
*   **生物启发设计**：将“光学遗忘”与“剪贴簿”概念结合，符合人类认知规律，且在工程上极具实用性。
*   **端侧友好**：针对边缘设备量身定制，解决了长效记忆“存不下、算不动”的痛点。
*   **统一表示**：通过视觉渲染将异构数据统一化，规避了复杂的多模态对齐问题。

### 8. 不足与局限
*   **模块依赖性**：性能高度依赖于 OCR 和视觉感知模块的质量，在极端模糊或布局极其复杂的页面上可能会失效。
*   **语义提取噪声**：EM-Graph 的构建依赖 LLM 的语义提取，可能会引入幻觉或错误的事件关联。
*   **应用范围限制**：目前主要针对个性化参考记忆问答，对于需要实时环境反馈、连续工具调用的动态交互场景，其泛化能力尚待验证。

（完）
