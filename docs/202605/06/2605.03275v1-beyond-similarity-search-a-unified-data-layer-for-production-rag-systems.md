---
title: "Beyond Similarity Search: A Unified Data Layer for Production RAG Systems"
title_zh: 超越相似性搜索：面向生产级 RAG 系统的统一数据层
authors: "Venkata Krishna Prasanth Budigi, Siri Chandana Sirigiri"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03275v1"
tags: ["query:ma-kf"]
score: 10.0
evidence: 生产级RAG系统的统一数据层
tldr: 针对生产环境中检索增强生成（RAG）系统面临的数据陈旧、租户泄露和查询复杂性等挑战，本文提出了一种基于PostgreSQL、pgvector和HNSW索引的统一数据层架构。该方案通过消除传统分层系统的同步开销，显著提升了过滤查询性能并确保了多租户安全性。实验表明，该架构在降低延迟和减少代码复杂性方面表现优异，为构建可靠的生产级RAG系统提供了高效的统一数据管理方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.03275v1/fig-001.webp\", \"caption\": \"Table 4: Engineering complexity comparison. Unified architecture eliminates 93% of synchronization code.\", \"page\": 6, \"index\": 1, \"width\": 979, \"height\": 208}, {\"url\": \"assets/figures/arxiv/2605.03275v1/fig-002.webp\", \"caption\": \"Table 1: Query latency benchmarks (200 iterations, 50,000 documents, 128-dim embeddings, HNSW index). Real measured values on PostgreSQL 16 + pgvector 0.6.0.\", \"page\": 6, \"index\": 2, \"width\": 979, \"height\": 208}, {\"url\": \"assets/figures/arxiv/2605.03275v1/fig-003.webp\", \"caption\": \"Figure 1: Conventional 3-Tool Stack vs. Unified Data Layer — architecture, data flow, and measured performance\", \"page\": 3, \"index\": 3, \"width\": 1064, \"height\": 535}]"
motivation: 解决传统RAG系统中由于分层数据架构导致的数据不一致、多租户数据泄露及复杂查询性能低下的问题。
method: 提出一种基于PostgreSQL原生向量搜索（pgvector）和HNSW索引的统一数据层，并结合混合层级架构。
result: "实验显示日期过滤查询延迟降低92%，租户查询延迟降低74%，且完全消除了数据泄露并减少了93%的同步代码。"
conclusion: 统一数据层架构能有效提升生产级RAG系统的可靠性、安全性和查询效率，是替代传统分层系统的优选方案。
---

## 摘要
检索增强生成（RAG）系统已成为将大语言模型植根于组织知识的标准架构。然而，生产部署中原型性能与现实世界可靠性之间始终存在差距。本文确定了造成这一差距的三个根本原因：数据陈旧、租户数据泄露和查询组合爆炸。这三者均可追溯到传统的拆分系统数据层。我们提出并评估了一种基于 PostgreSQL 的统一数据层，该层具有原生向量搜索（pgvector）和 HNSW 索引。对 50,000 份文档的受控基准测试显示，日期过滤查询的延迟降低了 92%，租户范围查询的延迟降低了 74%，实现了零同步不一致，并彻底消除了跨租户数据泄露，同时减少了 93% 的同步代码。此外，我们还讨论了一种推荐的分层混合架构。

## Abstract
Retrieval-Augmented Generation (RAG) systems have become the standard architecture for grounding large language models in organizational knowledge. Yet production deployments consistently expose a gap between clean prototype performance and real-world reliability. This paper identifies three root causes of that gap: data staleness, tenant data leakage, and query composition explosion. All three trace back to the conventional split-system data layer. We propose and evaluate a unified data layer built on PostgreSQL with native vector search (pgvector) and HNSW indexing. Controlled benchmarks on 50,000 documents show 92% latency reduction for date-filtered queries, 74% for tenant-scoped queries, zero synchronization inconsistency, and complete elimination of cross-tenant data leakage with 93% less synchronization code. We additionally discuss a recommended hybrid tier architecture

---

## 论文详细总结（自动生成）

这是一份关于论文《Beyond Similarity Search: A Unified Data Layer for Production RAG Systems》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的检索增强生成（RAG）系统在从原型转向生产环境时，常面临**数据陈旧（Staleness）**、**租户数据泄露（Tenant Leakage）**和**查询组合爆炸（Query Composition Explosion）**三大痛点。
*   **研究背景**：目前主流架构通常采用“三件套”模式（向量数据库 + 关系型元数据数据库 + 缓存层）。这种拆分架构导致了复杂的同步逻辑、安全漏洞以及在处理带有复杂过滤条件（如权限、时间范围、类别）的查询时性能低下。
*   **整体含义**：本文旨在证明，通过将向量搜索回归到成熟的关系型数据库（PostgreSQL）中，构建统一的数据层，可以从根本上解决生产级 RAG 的可靠性与复杂性问题。

### 2. 论文提出的方法论
*   **核心思想**：利用 **PostgreSQL** 及其向量扩展 **pgvector**，将文档内容、向量嵌入、元数据和访问控制策略存储在同一个数据库实例中，通过单一 SQL 语句实现原子化操作。
*   **关键技术细节**：
    *   **统一查询模型**：使用 SQL 结合向量相似度运算符（如 `<=>`）和关系型谓词（WHERE 子句）。
    *   **HNSW 索引**：采用分层导航小世界（HNSW）算法进行高效的近似最近邻（ANN）搜索。
    *   **事务性保证**：利用 PostgreSQL 的 ACID 特性，确保文档更新与向量更新在同一事务中完成，实现“零同步窗口”。
    *   **行级安全性（RLS）**：利用数据库原生的安全机制强制执行租户隔离，而非在应用层过滤。
    *   **混合层级架构（针对大规模场景）**：提出三层架构：热层（PostgreSQL 统一层，处理 80-90% 核心查询）、温层（专用向量数据库，处理长尾纯相似度查询）、冷层（对象存储）。

### 3. 实验设计
*   **数据集/场景**：构建了一个包含 50,000 份文档的受控语料库，包含 128 维向量、20 个租户空间、5 个内容类别，时间跨度为 180 天。
*   **Benchmark（对比基准）**：
    *   **Stack A（拆分系统）**：模拟传统架构，向量搜索与元数据查找分开执行，在应用层合并结果。
    *   **Stack B（统一系统）**：基于 PostgreSQL 16 + pgvector 0.6.0 的统一数据层。
*   **对比维度**：查询延迟（p50/p95/p99）、数据一致性窗口、租户泄露率、代码行数（LOC）。

### 4. 资源与算力
*   **软件环境**：PostgreSQL 16, pgvector 0.6.0。
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 CPU 型号、内存大小或是否使用 GPU 加速）。由于 pgvector 主要运行在 CPU 上，且实验规模为 5 万条，推测使用的是标准云服务器配置。

### 5. 实验数量与充分性
*   **实验量**：
    *   每种查询类型执行了 200 次迭代以获取延迟统计。
    *   进行了 1000 次查询模拟以测试租户泄露。
*   **充分性评价**：实验在功能验证和中等规模性能对比上是**充分且客观**的。它清晰地展示了随着查询约束增加，统一架构的优势。然而，5 万条数据的规模对于验证“超大规模（亿级）”场景略显不足，作者在讨论部分通过提出混合架构弥补了这一局限。

### 6. 主要结论与发现
*   **性能提升**：在带有日期过滤的查询中，统一架构延迟降低了 **92%**；在租户范围查询中降低了 **74%**。
*   **安全性**：统一架构实现了 **0%** 的租户数据泄露，而拆分架构在模拟应用层漏洞时有 0.2% 的泄露率。
*   **一致性**：统一架构实现了 **0ms** 的不一致窗口（原子更新），而拆分架构平均有 3.54ms 的延迟。
*   **工程简化**：消除了 **93%** 的同步代码（从约 1800 行减少到 120 行）。

### 7. 优点
*   **架构优雅**：回归“单一真理来源（Single Source of Truth）”，极大地降低了分布式系统的复杂性。
*   **安全可靠**：将权限控制下沉到数据库引擎层，提供了更强的安全保障。
*   **生产导向**：直接针对企业级 RAG 的痛点（如合规性、新鲜度）提出解决方案，具有极高的实用价值。

### 8. 不足与局限
*   **规模瓶颈**：虽然提出了混合架构，但实验本身仅限于 5 万条数据，未展示在数亿级向量下 PostgreSQL 的原生性能表现。
*   **纯搜索性能**：作者承认，对于不带任何过滤条件的纯向量相似度搜索，专用向量数据库（如 Pinecone, Milvus）在超大规模和 GPU 加速下仍具有吞吐量优势。
*   **硬件细节缺失**：缺乏具体的硬件基准测试环境描述，可能导致其他研究者难以精确复现延迟数值。

（完）
