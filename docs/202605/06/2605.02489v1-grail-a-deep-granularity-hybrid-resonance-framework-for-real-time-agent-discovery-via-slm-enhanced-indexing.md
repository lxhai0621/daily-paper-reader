---
title: "GRAIL: A Deep-Granularity Hybrid Resonance Framework for Real-Time Agent Discovery via SLM-Enhanced Indexing"
title_zh: GRAIL：一种基于 SLM 增强索引的实时智能体发现深度粒度混合共振框架
authors: Jinliang Xu
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02489v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 大规模多智能体协作的实时智能体发现
tldr: 针对大规模多智能体协作中智能体发现效率与精度的权衡难题，本文提出GRAIL框架。该框架通过引入微调的小语言模型（SLM）进行快速意图解析，结合伪文档扩展增强语义表示，并利用MaxSim共振机制实现细粒度匹配。实验表明，GRAIL在保持高精度的同时，将发现延迟缩短至400ms以内，比LLM基准快79倍以上，为实时智能体发现提供了高效的工业级方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.02489v1/fig-001.webp\", \"caption\": \"Fig. 4: Multi-dimensional distribution analysis of tags (Top: Tag word cloud; Bottom: Log-transformed frequency trend)\", \"page\": 6, \"index\": 1, \"width\": 500, \"height\": 650}, {\"url\": \"assets/figures/arxiv/2605.02489v1/fig-002.webp\", \"caption\": \"Fig. 2: The Overall Architecture of the GRAIL Framework.\", \"page\": 3, \"index\": 2, \"width\": 525, \"height\": 407}, {\"url\": \"assets/figures/arxiv/2605.02489v1/fig-003.webp\", \"caption\": \"Fig. 3: Workflow of the GRAIL Online Discovery Process.\", \"page\": 5, \"index\": 3, \"width\": 516, \"height\": 408}]"
motivation: 现有的智能体发现方法在重型LLM的高延迟与传统向量检索的低精度之间存在矛盾，难以满足大规模实时协作需求。
method: 提出GRAIL框架，通过微调小模型预测能力标签、利用伪文档扩展增强语义密度，并结合MaxSim共振机制实现细粒度匹配。
result: "在AgentTaxo-9K数据集上，GRAIL将端到端延迟降低了79倍以上（低于400ms），且在Recall@10指标上显著优于传统向量搜索。"
conclusion: 该框架为实时“智能体互联网”提供了一种兼顾高效率与高精度的工业级可扩展解决方案。
---

## 摘要
随着基于大语言模型（LLM）的智能体生态系统迅速扩张，高效且准确的智能体发现（Agent Discovery）已成为大规模多智能体协作的关键瓶颈。现有方法通常面临两难境地：要么依赖重量级 LLM 进行意图解析，导致极高的延迟（通常超过 30 秒）；要么使用单体向量检索，以牺牲语义精度为代价换取速度。为了弥补这一差距，我们提出了 GRAIL（基于粒度共振的智能体/人工智能链接），这是一个在不牺牲准确性的情况下实现低于 400 毫秒发现延迟的新颖框架。GRAIL 引入了三项关键创新：（1）SLM 增强预测，使用专门微调的小语言模型（SLM）取代通用 LLM 解析器，实现毫秒级的能力标签预测；（2）伪文档扩展，通过合成查询增强智能体描述，以提升语义密度，从而实现鲁棒的稠密检索；（3）MaxSim 共振，一种细粒度匹配机制，计算用户查询与离散智能体使用示例之间的最大相似度，有效缓解了语义稀释。在包含 9,240 个智能体的新型大规模数据集 AgentTaxo-9K 上的验证结果表明，与基于 LLM 解析的基准相比，GRAIL 将端到端发现延迟降低了 79 倍以上，同时在 Recall@10 指标上显著优于传统的向量搜索。该框架为实时“智能体互联网”提供了一种可扩展的工业级解决方案。

## Abstract
As the ecosystem of Large Language Model (LLM)-based agents expands rapidly, efficient and accurate Agent Discovery becomes a critical bottleneck for large-scale multi-agent collaboration. Existing approaches typically face a dichotomy: either relying on heavy-weight LLMs for intent parsing, leading to prohibitive latency (often exceeding 30 seconds), or using monolithic vector retrieval that sacrifices semantic precision for speed. To bridge this gap, we propose \textbf{GRAIL} (Granular Resonance-based Agent/AI Link), a novel framework achieving sub-400ms discovery latency without compromising accuracy. GRAIL introduces three key innovations: (1) \textbf{SLM-Enhanced Prediction}, replacing the generalized LLM parser with a specialized, fine-tuned Small Language Model (SLM) for millisecond-level capability tag prediction; (2) \textbf{Pseudo-Document Expansion}, augmenting agent descriptions with synthetic queries to enhance semantic density for robust dense retrieval; and (3) \textbf{MaxSim Resonance}, a fine-grained matching mechanism computing maximum similarity between user queries and discrete agent usage examples, effectively mitigating semantic dilution. Validated on \textbf{AgentTaxo-9K}, our new large-scale dataset of 9,240 agents, GRAIL reduces end-to-end discovery latency by over \textbf{79$\times$} compared to LLM-parsing baselines, while significantly outperforming traditional vector search in Recall@10. This framework offers a scalable, industrial-grade solution for the real-time ``Internet of Agents."

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **GRAIL**（Granular Resonance-based Agent/AI Link）的框架，旨在解决大规模多智能体系统中“智能体发现”的效率与精度矛盾。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在“智能体互联网”（Internet of Agents）背景下，如何从成千上万个候选智能体中，实时且准确地匹配最符合用户意图的智能体。
*   **研究背景**：
    *   **LLM解析法（高延迟）**：依赖重型模型（如Gemini 3）解析意图，延迟常超过30秒，无法满足实时交互需求。
    *   **单体向量检索（低精度）**：将智能体所有功能压缩为单一向量，导致“语义稀释”（Semantic Dilution），难以捕捉细粒度约束，召回率低。
*   **研究目标**：在保证亚秒级（<400ms）响应的同时，达到甚至超过LLM解析的匹配精度。

### 2. 论文提出的方法论
GRAIL 框架由**离线深度粒度索引**和**在线SLM驱动发现**两部分组成：
*   **核心思想**：解耦智能体元数据，通过多维度索引（标签、上下文、意图示例）和细粒度的“共振”匹配来保留语义细节。
*   **关键技术细节**：
    1.  **SLM增强预测**：使用微调的小语言模型（SLM，如3B参数量）代替通用LLM，在毫秒内预测用户查询的能力标签（Tags），用于快速过滤。
    2.  **伪文档扩展（PDE）**：利用LLM为智能体描述生成合成查询（Synthetic Queries），扩充语义密度，解决用户表达与技术描述之间的词汇鸿沟。
    3.  **MaxSim共振机制**：这是核心算法。不使用平均向量，而是计算用户查询向量与智能体多个离散使用示例（Examples）之间的**最大余弦相似度**。
*   **算法流程**：
    *   **离线**：构建倒排索引（标签）、稠密向量索引（伪文档）和示例矩阵索引（Mi）。
    *   **在线**：SLM预测标签 -> 混合召回（标签过滤+向量搜索） -> **MaxSim重排序**（计算查询与示例矩阵的共振得分）。
*   **理论支撑**：论文通过数学证明（Theorem 1）指出，随着智能体功能增加，传统均值池化（Mean Pooling）的得分会线性衰减，而MaxSim能保持信号强度。

### 3. 实验设计
*   **数据集**：建立了 **AgentTaxo-9K**，包含154个行业、924个子领域、9,240个智能体元数据，以及27,720条用户查询。
*   **Benchmark**：该数据集基于OASF框架构建，具有MECE（相互独立、完全穷尽）的层级结构。
*   **对比方法**：
    *   **MDR**：传统的单体稠密向量检索。
    *   **SLM-Sort**：仅依赖SLM预测标签进行检索。
    *   **LLM-Router**：基于重型LLM的“思考后查找”模式（作为精度上限参考）。

### 4. 资源与算力
*   **硬件环境**：单台 Linux 服务器，配备 **1张 NVIDIA T4 GPU (15GB VRAM)**，12.7GB 内存。
*   **模型配置**：
    *   **SLM**：使用 **Qwen2.5-3B-Instruct**，通过 LoRA 方式在 27,720 条数据上进行微调。
    *   **Embedding**：使用 **BGE-Small-EN-v1.5**（33M参数），向量维度为384。
*   **训练时长**：文中未明确给出具体的训练总时长，但强调了推理延迟。

### 5. 实验数量与充分性
*   **实验规模**：在近万级智能体池中进行全量检索测试，而非小规模子集。
*   **实验维度**：涵盖了检索精度（Recall@1/10, MRR）和系统延迟（Latency）两个核心维度。
*   **消融实验**：分别验证了“无MaxSim（改用均值池化）”和“无SLM（仅靠向量）”的情况。
*   **评价**：实验设计较为充分，通过理论证明与实证结果相互印证，对比基准涵盖了工业界主流方案，具有较高的客观性。

### 6. 主要结论与发现
*   **性能卓越**：GRAIL 实现了 **394.48ms** 的端到端延迟，比 LLM 基准快了 **79倍以上**。
*   **精度提升**：Recall@10 达到 **91.45%**，显著优于传统向量检索（76.77%）和 LLM 路由（78.95%）。
*   **解决稀释**：实验证实了 MaxSim 机制在处理“多功能智能体”时的必要性，MRR 从 0.54 提升至 0.78。
*   **SLM价值**：SLM 标签预测虽然在召回率上贡献略小，但在意图解析速度和符号对齐方面不可或缺。

### 7. 优点
*   **架构创新**：巧妙地结合了 SLM 的速度和 Late Interaction（MaxSim）的精度。
*   **工业级潜力**：亚秒级延迟和低算力需求（T4 GPU即可运行）使其非常适合大规模部署。
*   **理论扎实**：不仅有实验，还提供了关于语义稀释问题的数学证明。
*   **贡献数据集**：开源了 AgentTaxo-9K，填补了大规模智能体发现基准的空白。

### 8. 不足与局限
*   **语言局限性**：实验主要基于英文模型（BGE-Small-EN），在多语言环境下的表现尚未验证。
*   **合成数据偏差**：伪文档扩展依赖 LLM 生成，可能引入生成模型的偏见或幻觉。
*   **扩展性边界**：虽然在 9K 规模表现良好，但在百万级或动态更新频繁的智能体池中，示例矩阵的存储和计算开销可能成为新瓶颈。
*   **超参数敏感性**：最终得分依赖权重参数 $\alpha$，不同场景下可能需要精细调优。

（完）
