---
title: "Autonomous LLM Agent Worms: Cross-Platform Propagation, Automated Discovery and Temporal Re-Entry Defense"
title_zh: 自主 LLM 智能体蠕虫：跨平台传播、自动化发现与时域重入防御
authors: "Mingming Zha, Xiaofeng Wang"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02812v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 具有持久工作空间和内存文件的自主LLM智能体
tldr: 本研究揭示了自主LLM智能体在持久化工作区和消息集成中面临的新型蠕虫传播风险。作者提出了SSCGV分析框架和SRPO载荷优化器，实现了跨平台的零点击自主传播和特权提升。针对此类威胁，研究团队开发了RTW-A防御机制，通过阻断重入路径和限制高风险操作，在不影响正常工作流的前提下有效消除了蠕虫传播链，为智能体安全提供了系统性保障。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.02812v1/fig-001.webp\", \"caption\": \"Table 1: Injectable carriers discovered by SSCGV per framework, classified by LLM context injection position. All carriers are persistent and writable within the agent’s normal permission scope. Platform names and exact carrier paths are withheld during coordinated disclosure.\", \"page\": 8, \"index\": 1, \"width\": 727, \"height\": 180}, {\"url\": \"assets/figures/arxiv/2605.02812v1/fig-002.webp\", \"caption\": \"Figure 1: Framework overview for analyzing and defending against persistent worm propagation in LLM agent ecosystems. The framework operates in three phases: automated vulnerability discovery, attack execution analysis, and formal defense construction.\", \"page\": 5, \"index\": 2, \"width\": 976, \"height\": 540}]"
motivation: 自主LLM智能体的持久化存储和自动加载机制为恶意代码提供了在不同智能体间自动传播和执行高风险操作的新途径。
method: 开发了SSCGV源代码分析器以追踪数据流，并利用SRPO优化器生成能够抵抗LLM摘要和改写的鲁棒性蠕虫载荷。
result: 在三个生产级智能体框架上成功演示了零点击跨平台传播、特权提升和数据窃取，并发现用户提示词载荷比系统提示词更具攻击效力。
conclusion: 提出的RTW-A防御框架通过形式化证明的机制阻断了蠕虫的持久化重入路径，为构建安全的自主智能体生态系统提供了保障。
---

## 摘要
自主 LLM 智能体作为长期运行的进程，具有持久化工作区、内存文件、计划任务状态和消息集成功能。这些特性带来了一种新的传播风险：受攻击者影响的内容可以被写入持久化的智能体状态，通过计划的自动加载重新进入 LLM 决策上下文，并驱动包括配置更改和跨智能体传输在内的高风险操作。我们提出了首个用于自动化分析基于文件的多智能体 LLM 生态系统中持久性蠕虫传播的系统框架。SSCGV 是我们的自动化源代码图分析器，它追踪从文件 I/O 到 LLM 上下文注入点的数据流，并在无需人工分析的情况下根据上下文注入位置对载体进行排名。SRPO 是我们的摘要抗性有效载荷优化器，它生成的蠕虫有效载荷在跨多跳通信的 LLM 介导摘要和改写中具有鲁棒性。在三个生产级智能体框架上的评估表明，我们实现了零点击自主传播、无需平台特定适配的 3 跳跨平台传输、智能体间权限提升以及数据外泄。我们发现了两个经验性见解：用户提示词载体比系统提示词载体具有更高的攻击合规性，且读取操作代表了 LLM 介导系统中的主要完整性威胁。为了防御此类攻击，我们开发了 RTW-A，并在形式化的“无持久性蠕虫传播”定理下得到了证明。RTW 阻断了“暴露读取前写入”的重入；密封配置保护静态文件；类型化内存提升防止不受信任的摘要进入受信任内存；能力衰减限制了外部读取后的高风险操作。这些机制在保留普通工作流的同时，消除了持久化、重入和操作链。受影响的系统已匿名化，等待协调披露。

## Abstract
Autonomous LLM agents operate as long-running processes with persistent workspaces, memory files, scheduled task state, and messaging integrations. These features create a new propagation risk: attacker-influenced content can be written into persistent agent state, re-enter the LLM decision context through scheduled autoloading, and drive high-risk actions including configuration changes and cross-agent transmission. We present the first systematic framework for automated analysis of persistent worm propagation in file-backed multi-agent LLM ecosystems. SSCGV, our automated source-code graph analyzer, traces data flow from file I/O to LLM context injection points and ranks carriers by context injection position without manual analysis. SRPO, our summary-resilient payload optimizer, generates worm payloads robust to LLM-mediated summarization and paraphrasing across multi-hop communication. Evaluated on three production agent frameworks, we demonstrate zero-click autonomous propagation, 3-hop cross-platform transmission without platform-specific adaptation, inter-agent privilege escalation, and data exfiltration. We identify two empirical insights: user prompt carriers achieve higher attack compliance than system prompt carriers, and read operations represent the primary integrity threat in LLM-mediated systems. To defend against this class of attacks, we develop RTW-A, proven under a formal No Persistent Worm Propagation theorem. RTW blocks write-before-exposed-read re-entry; sealed configuration protects static files; typed memory promotion prevents untrusted summaries from entering trusted memory; and capability attenuation limits high-risk actions after external reads. These mechanisms eliminate the persistence, re-entry, action chain while preserving ordinary workflows. Affected systems are anonymized pending coordinated disclosure.

---

## 论文详细总结（自动生成）

### 论文总结：自主 LLM 智能体蠕虫：跨平台传播、自动化发现与时域重入防御

#### 1. 核心问题与整体含义
*   **研究背景**：现代自主 LLM 智能体（Agents）已演变为长期运行的系统，具备持久化工作区、长期记忆、自动任务调度及多渠道消息集成功能。
*   **核心问题**：这些架构特性引入了新型安全风险——**持久性蠕虫传播**。攻击者可将恶意内容写入智能体的持久化状态（如配置文件或记忆），当智能体未来自动加载这些内容时，恶意指令会重新进入决策上下文，驱动智能体自主执行高风险操作（如跨智能体传播、特权提升）。
*   **研究动机**：现有的 LLM 蠕虫研究多依赖手动构建的特定平台向量，且未解决智能体通信中常见的“语义降级”（如摘要、改写导致载荷失效）问题。

#### 2. 方法论
论文提出了一个包含发现、攻击优化和防御的完整框架：
*   **SSCGV (自动化源代码图漏洞分析器)**：
    *   **核心思想**：通过静态分析识别“暴露读取”（Exposed Reads）路径。
    *   **技术细节**：构建代码属性图（CPG），追踪从文件 I/O 到 LLM 上下文注入点（如系统提示词、用户提示词、记忆加载）的数据流。它根据注入位置对载体进行排名，无需平台特定知识。
*   **SRPO (摘要抗性载荷优化器)**：
    *   **核心思想**：利用对抗性优化生成在摘要、改写和压缩下仍能保持语义效力的载荷。
    *   **流程**：采用三 LLM 角色模型：**生成者**提出候选载荷，**模拟器**执行语义转换（摘要/改写），**评判者**评估转换后的载荷是否仍能触发持久化、传播和破坏行为。
*   **RTW-A 防御框架**：
    *   **核心思想**：基于“无持久性蠕虫传播”定理，通过阻断“时域重入”路径来防御。
    *   **关键机制**：
        1.  **RTW 约束**：动态禁止对已受污染载体执行“先写后读”的重入操作。
        2.  **密封配置**：将核心引导文件设为运行时不可写。
        3.  **类型化记忆提升**：仅允许符合预定义 Schema 的结构化内容进入受信任记忆。
        4.  **能力衰减**：在读取外部不可信内容后，自动限制智能体执行高风险操作（如发送消息、执行脚本）。

#### 3. 实验设计
*   **实验场景**：在三个匿名化的生产级开源智能体框架（Framework A, B, C）上进行评估。
*   **Benchmark 与对比**：
    *   **注入位置对比**：对比了用户提示词（User Prompt）载体与系统提示词（System Prompt）载体的攻击合规率。
    *   **跨平台验证**：测试了载荷在异构框架（如从 A 传播到 B）间的 3 跳传输能力。
    *   **模型鲁棒性**：在 GPT-4o-mini 和 Gemini-2.5-Flash 等不同后端上验证攻击的通用性。
    *   **防御对比**：对比了传统基于能力的访问控制与 RTW-A 防御的效果。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。由于该研究侧重于系统架构分析、静态代码审计和基于 API 的载荷优化，其算力需求主要集中在运行 LLM 推理进行对抗优化和实验验证上，而非大规模模型训练。

#### 5. 实验数量与充分性
*   **实验规模**：SSCGV 发现了 31 个可注入的持久化载体；实验涵盖了从漏洞发现到端到端攻击验证的 8 个阶段（E1-E8）。
*   **充分性与客观性**：实验设计较为全面，涵盖了零点击传播、特权提升、数据外泄等多种恶意行为。通过在多个异构框架和不同 LLM 后端上进行测试，证明了威胁的普遍性。实验在受控环境中进行，且对受影响框架进行了负责任的漏洞披露，表现出较高的学术严谨性和客观性。

#### 6. 主要结论与发现
*   **读取比写入更危险**：在 LLM 智能体中，读取受控内容会直接污染决策逻辑，颠覆了传统安全中“写入是主要完整性威胁”的认知。
*   **位置决定效力**：注入到“用户提示词”位置的载体攻击成功率显著高于“系统提示词”，因为模型更倾向于将用户轮次的输入视为即时指令。
*   **零点击跨平台传播**：蠕虫可以在无需人工干预的情况下，利用智能体的自动任务调度（如 Heartbeat 机制）在不同平台的智能体间自主扩散。
*   **防御可行性**：通过控制时域重入（RTW）和能力衰减，可以在不牺牲正常功能的前提下阻断蠕虫传播链。

#### 7. 优点
*   **自动化程度高**：SSCGV 实现了无需人工干预的跨平台漏洞发现。
*   **理论支撑扎实**：防御框架 RTW-A 建立了形式化证明，提供了数学层面的安全保证。
*   **实战导向**：针对生产级框架进行测试，揭示了现有智能体架构在持久化状态管理上的根本性缺陷。

#### 8. 不足与局限
*   **分析深度受限**：SSCGV 依赖于源代码或运行时可见性，对于完全闭源且缺乏透明度的商业智能体系统，其分析能力受限。
*   **可用性权衡**：类型化记忆提升和能力衰减虽然安全，但可能会在一定程度上限制智能体处理复杂、非结构化任务的灵活性。
*   **部署门槛**：RTW-A 需要在智能体运行时底层进行深度集成，对于已经部署且架构僵化的系统，改造难度较大。

（完）
