---
title: Is Grep All You Need? How Agent Harnesses Reshape Agentic Search
title_zh: Grep 是否就是你所需的一切？智能体测试框架如何重塑智能体搜索
authors: "Sahil Sen, Akhil Kasturi, Elias Lumer, Anmol Gulati, Vamse Kumar Subbiah"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.15184v1"
tags: ["query:ma-kf"]
score: 10.0
evidence: RAG中检索策略与智能体架构的相互作用
tldr: 本研究探讨了LLM智能体在搜索任务中检索策略与架构设计的相互作用。通过对比grep（文本匹配）与向量检索，研究分析了工具调用范式及无关干扰信息对性能的影响。实验表明，在多种智能体框架下，grep通常优于向量检索，且智能体外壳（harness）的设计对最终表现具有决定性作用，为优化智能体搜索系统提供了实证依据。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.15184v1/fig-001.webp\", \"caption\": \"Table 3: Experiment 2 (vector-only): accuracy (%) by session-limit configuration.\", \"page\": 7, \"index\": 1, \"width\": 456, \"height\": 269}, {\"url\": \"assets/figures/arxiv/2605.15184v1/fig-002.webp\", \"caption\": \"Table 2: Experiment 2 (grep-only): overall accuracy (%) on the 116-question subset by session-limit configuration.\", \"page\": 7, \"index\": 2, \"width\": 494, \"height\": 269}, {\"url\": \"assets/figures/arxiv/2605.15184v1/fig-003.webp\", \"caption\": \"Table 1: Experiment 1. Overall accuracy (%) on the 116-question LongMemEval-S subset. We report results for both the standard and programmatic tool-calling configurations\", \"page\": 5, \"index\": 3, \"width\": 622, \"height\": 304}, {\"url\": \"assets/figures/arxiv/2605.15184v1/fig-004.webp\", \"caption\": \"Figure 1: Mean performance as noise is added to the retrieval pool. Both methods face minimal degradation, but GREP outperforms vector on average.\", \"page\": 6, \"index\": 4, \"width\": 1056, \"height\": 575}]"
motivation: 现有研究缺乏对检索策略、智能体架构及工具调用范式在复杂搜索任务中相互影响的系统性比较。
method: 通过两个实验对比了grep与向量检索在不同智能体外壳（如Claude Code、Chronos）及不同干扰程度下的表现。
result: 实验发现grep在准确率上普遍优于向量检索，且智能体外壳和工具调用风格对性能有显著影响。
conclusion: 检索策略的选择和智能体外壳的架构设计是提升智能体搜索效能的关键因素。
---

## 摘要
大语言模型（LLM）智能体的最新进展使得复杂的智能体工作流成为可能，模型可以自主检索信息、调用工具并对大规模语料库进行推理，从而代表用户完成任务。尽管检索增强生成（RAG）在智能体搜索系统中的应用日益广泛，但现有文献仍缺乏对检索策略选择如何与智能体架构及工具调用范式相互作用的系统性比较。一些重要的实践维度在智能体循环中仍未得到充分探索，包括工具输出如何呈现给模型，以及当搜索必须应对更多无关上下文文本时性能如何变化。本文报告了一项分为两个实验的实证研究。实验 1 在来自 LongMemEval 的 116 个问题样本上比较了 grep 和向量检索，使用了自定义智能体测试框架（Chronos）和厂商原生的 CLI 测试框架（Claude Code、Codex 和 Gemini CLI），涵盖了内联工具结果和模型单独读取的基于文件的工具结果。实验 2 比较了仅使用 grep 和仅使用向量的检索，同时逐渐混入额外的无关对话历史，使得每个查询都嵌入在更多干扰材料以及相关的段落中。在 Chronos 和厂商 CLI 中，实验 1 的比较结果显示 grep 通常比向量检索具有更高的准确率；同时，即使底层对话数据相同，总体得分仍然强烈取决于所使用的测试框架和工具调用风格。

## Abstract
Recent advances in Large Language Model (LLM) agents have enabled complex agentic workflows where models autonomously retrieve information, call tools, and reason over large corpora to complete tasks on behalf of users. Despite the growing adoption of retrieval-augmented generation (RAG) in agentic search systems, existing literature lacks a systematic comparison of how retrieval strategy choice interacts with agent architecture and tool-calling paradigm. Important practical dimensions, including how tool outputs are presented to the model and how performance changes when searches must cope with more irrelevant surrounding text, remain under-explored in agent loops. This paper reports an empirical study organized into two experiments. Experiment 1 compares grep and vector retrieval on a 116-question sample from LongMemEval, using a custom agent harness (Chronos) and provider-native CLI harnesses (Claude Code, Codex, and Gemini CLI), for both inline tool results and file-based tool results that the model reads separately. Experiment 2 compares grep-only and vector-only retrieval while progressively mixing in additional unrelated conversation history, so that each query is embedded in more distracting material alongside the passages that matter. Across Chronos and the provider CLIs, grep generally yields higher accuracy than vector retrieval in our comparisons in experiment 1; at the same time, overall scores still depend strongly on which harness and tool-calling style is used, even when the underlying conversation data are the same.

---

## 论文详细总结（自动生成）

这是一份关于论文《Is Grep All You Need? How Agent Harnesses Reshape Agentic Search》的结构化深度分析报告：

### 1. 核心问题与研究动机
*   **研究背景**：随着大语言模型（LLM）智能体在复杂任务中自主检索信息的需求增加，检索增强生成（RAG）已成为核心组件。
*   **核心问题**：现有的 RAG 研究多集中在检索算法本身（如向量检索的准确率），而忽略了**检索策略**（词法 vs. 语义）如何与**智能体架构（Harness/外壳）**及**工具调用范式**（结果如何呈现给模型）产生相互作用。
*   **研究动机**：在实际的智能体循环中，检索结果是作为工具输出反馈给模型的。作者试图探究：简单的 `grep`（正则匹配）是否在特定智能体架构下优于复杂的向量检索？当干扰信息（噪声）增加时，不同架构的鲁棒性如何？

### 2. 方法论与核心思想
论文提出了一种多维度的实证研究框架，核心思想在于将“检索”视为智能体工作流的一部分，而非孤立的模块。
*   **检索策略实现**：
    *   **词法搜索 (Grep)**：基于正则表达式和子字符串匹配，无需嵌入模型，计算成本极低。
    *   **语义搜索 (Vector)**：使用预训练嵌入模型进行近似最近邻（ANN）搜索，并辅以重排序（Reranking）。
*   **智能体外壳 (Agent Harnesses)**：
    *   **自定义外壳 (Chronos)**：基于 LangChain 开发，采用“类别条件提示”（根据问题类型调整系统提示词），并能提取结构化时间事件。
    *   **厂商原生 CLI 外壳**：如 Claude Code、Codex、Gemini CLI，这些外壳将工具调用嵌入到 Shell 环境中，模型可以直接执行 Bash 命令。
*   **工具调用架构 (Delivery Method)**：
    *   **标准内联 (Inline)**：检索结果直接作为消息插入对话上下文。
    *   **程序化文件化 (File-Based)**：检索结果写入磁盘，模型仅收到文件路径，需显式调用 `cat` 或 `read` 命令读取。

### 3. 实验设计
*   **数据集**：使用 **LongMemEval** 的 116 个问题子集，涵盖知识更新、多会话聚合、用户偏好、时间推理等 6 类任务。
*   **对比方法**：
    *   **实验 1 (因子实验)**：交叉对比 [Grep vs. Vector] × [Chronos vs. 厂商 CLI] × [Inline vs. Programmatic]。
    *   **实验 2 (噪声缩放)**：通过逐渐增加无关会话（从 5 个会话到全量 60+ 会话），测试检索策略在噪声环境下的性能演变。
*   **评估模型**：包括 Claude Opus 4.6/Haiku 4.5、GPT-5.4、Gemini 3.1 Pro/Flash-Lite（注：论文标注日期为 2026 年，模型版本号反映了该设定）。
*   **Benchmark 评估**：使用 GPT-4o 作为自动评测员（LLM Grader），根据参考答案进行二元判断。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或训练时长。
*   **原因分析**：由于该研究主要关注推理阶段的智能体行为分析，而非模型训练，其资源消耗主要集中在 API 调用和本地脚本执行上。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 5 种主流 LLM 后端。
    *   对比了 4 种不同的智能体外壳架构。
    *   进行了 5 种不同噪声梯度的缩放实验。
*   **充分性评价**：实验设计较为**充分且客观**。它不仅对比了算法，还通过“外壳切换”证明了同样的模型在不同架构下表现迥异。通过配对实验（Paired Comparison）确保了 Grep 和 Vector 在面对相同的干扰数据时具有公平的对比环境。

### 6. 主要结论与发现
*   **Grep 的意外优势**：在内联交付模式下，`grep` 在所有测试的智能体外壳和模型组合中均优于向量检索。这主要是因为 LongMemEval 任务依赖于精确的字面证据（如日期、特定事实）。
*   **外壳决定论**：智能体外壳的设计（如提示词工程、工具接口设计）对性能的影响甚至超过了检索算法本身。同一个模型在不同外壳下的准确率差异可达 15% 以上。
*   **交付方式的倒置效应**：当使用“文件化”交付时，向量检索有时会反超 `grep`。这表明文件化交付对模型的逻辑推理能力（定位、读取、整合）提出了更高要求，而不仅仅是检索质量。
*   **噪声鲁棒性**：向量检索在低噪声（少量会话）时表现较好，但随着干扰信息增加，`grep` 的高精度特性使其在“大海捞针”场景下更具稳定性。

### 7. 优点与亮点
*   **视角新颖**：打破了“RAG 性能 = 检索算法性能”的固有思维，首次系统探讨了智能体外壳（Harness）对搜索效果的重塑作用。
*   **实战导向**：引入了厂商原生的 CLI 智能体（如 Claude Code）进行对比，这比单纯在实验室环境下构建的 Pipeline 更贴近生产实际。
*   **架构深度**：区分了“内联”与“文件化”交付，揭示了上下文压力与工具调用复杂性之间的权衡。

### 8. 不足与局限
*   **任务局限性**：实验主要基于长记忆对话 QA，这类任务通常有明确的字面答案。在需要“概念融合”或“语义抽象”的任务（如跨文档总结）中，`grep` 的表现可能会大幅下降。
*   **模型版本偏差**：论文中使用的模型版本（如 GPT-5.4）在当前现实中尚未发布，这可能暗示该研究是在特定模拟环境或未来预测背景下进行的，其结论在当前主流模型（如 GPT-4o）上的泛化性有待验证。
*   **缺乏混合策略探讨**：论文主要对比了纯 Grep 和纯 Vector，未深入探讨两者结合的混合检索（Hybrid）在智能体循环中的最优配置。

（完）
