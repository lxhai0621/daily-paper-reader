---
title: "Belief Memory: Agent Memory Under Partial Observability"
title_zh: Belief Memory：部分可观测性下的智能体记忆
authors: "Junfeng Liao, Qizhou Wang, Jianing Zhu, Bo Du, Rui Yan, Xiuying Chen"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05583v2"
tags: ["query:agent"]
score: 10.0
evidence: LLM智能体的外部记忆与基于信念的存储
tldr: 针对LLM智能体在部分可观测环境下因确定性内存导致的自我强化错误，本文提出BeliefMem。该方法改变了单一结论的存储范式，转而保留多个候选结论及其概率，并利用Noisy-OR规则随新观测动态更新。BeliefMem在检索时呈现所有候选及其概率，使智能体能基于高置信度知识行动，同时保留修正能力。实验证明其在LoCoMo和ALFWorld基准上表现优异，为处理不确定性提供了新方向。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.05583v2/fig-001.webp\", \"caption\": \"Figure 1: Deterministic memory vs. BeliefMem with an API timeout example. After repeated API X timeouts, the deterministic paradigm stores “API X failed” and avoids it in later sessions, reinforcing the error. In contrast, BeliefMem keeps multiple hypotheses (e.g., failure vs. rate limiting) with probabilities, retries the API, and updates beliefs with new evidence, enabling correction over time.\", \"page\": 2, \"index\": 1, \"width\": 825, \"height\": 460}, {\"url\": \"assets/figures/arxiv/2605.05583v2/fig-002.webp\", \"caption\": \"Table 1: LoCoMo results across four categories under GPT-4o-mini and GPT-4o backbones. Each cell reports F1 / BLEU-1. Best and second numbers per column are in bold and underline, respectively.\", \"page\": 7, \"index\": 2, \"width\": 822, \"height\": 389}, {\"url\": \"assets/figures/arxiv/2605.05583v2/fig-003.webp\", \"caption\": \"Figure 6: The prompt used for attribute extraction. It restricts the model to output format, fact-based JSON objects grounded in the provided conversation.\", \"page\": 18, \"index\": 3, \"width\": 829, \"height\": 1268}, {\"url\": \"assets/figures/arxiv/2605.05583v2/fig-004.webp\", \"caption\": \"Table 2: ALFWorld results with the Qwen3-Next-80B-A3B-Instruct, on the in-distribution (Seen) split and the out-of-distribution (Unseen) split. SR(%): Success rate (↑); #Steps: Average steps on solved episodes (↓). ∆ indicates the difference relative to the best result in each column. BeliefMem*: 50% memory corpus used. BeliefMem: full memory corpus used.\", \"page\": 8, \"index\": 4, \"width\": 818, \"height\": 305}, {\"url\": \"assets/figures/arxiv/2605.05583v2/fig-005.webp\", \"caption\": \"Table 7: Sensitivity analysis of hyperparameters (Top-K and λ) on ALFWorld.\", \"page\": 17, \"index\": 5, \"width\": 773, \"height\": 754}, {\"url\": \"assets/figures/arxiv/2605.05583v2/fig-006.webp\", \"caption\": \"Figure 4: (a) BeliefMem maintains competitive performance across varying memory corpus sizes on ALFWorld, outperforming all baselines with only 50% of memory corpus. (b) BeliefMem’s candidate probabilities reliably converge to the true conclusion as evidence accumulates on LoCoMo, whereas naive frequency-based estimation fails to converge under noisy observations.\", \"page\": 9, \"index\": 6, \"width\": 814, \"height\": 262}, {\"url\": \"assets/figures/arxiv/2605.05583v2/fig-007.webp\", \"caption\": \"Figure 2: Overview of BeliefMem. i) Upon receiving an observation, BeliefMem updates memories via Add (initializing candidates for new attributes) or Merge (incorporating new evidence via noisyOR update). ii) Retrieval scores entries by semantic similarity and temporal decay, returning a full belief rather than a single conclusion. iii) The agent acts conditioned on both the current observation and the retrieved belief, keeping all alternative hypotheses visible at decision time.\", \"page\": 5, \"index\": 7, \"width\": 819, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2605.05583v2/fig-008.webp\", \"caption\": \"Table 5: Full results of ablation studies on ALFWorld benchmark (memory corpus size = 1500).\", \"page\": 16, \"index\": 8, \"width\": 682, \"height\": 185}]"
motivation: 现有LLM智能体内存通常将部分观测转化为确定性结论，导致忽略不确定性并产生自我强化的错误。
method: 提出BeliefMem框架，将观测存储为带概率的多个候选结论，并利用Noisy-OR规则随新观测动态更新概率。
result: 在LoCoMo和ALFWorld基准测试中，BeliefMem显著优于现有基线方法，展现了在有限数据下的卓越性能。
conclusion: 概率性内存机制能有效处理部分可观测环境中的不确定性，为智能体长时记忆和决策提供了新的研究范式。
---

## 摘要
在长上下文环境下运行的 LLM 智能体依赖外部记忆来随时间积累知识。然而，现有方法通常将每次观测存储为单一的确定性结论（例如，从临时错误中推断出“API X 失败”），尽管此类观测本质上是局部的且具有潜在的歧义。通过认定单一结论并丢弃不确定性，这些方法引入了自我强化的错误：智能体根据存储的结论采取行动，从不重新审视替代方案，并随着时间的推移不断强化该结论。为了解决这一问题，我们提出了 BeliefMem，它将记忆范式从对每次观测认定单一结论转变为保留多个候选结论及其概率。具体而言，BeliefMem 将候选结论存储为独立的记忆条目，每个条目携带一个概率，该概率随着新观测的到来通过 Noisy-OR 规则进行更新。在检索时，所有候选结论及其概率会一同呈现，使替代方案对智能体保持可见。由于记忆中的每个结论都保留了其概率，BeliefMem 保留了确定性范式所丢弃的不确定性，使智能体能够对证据充分的知识以高置信度采取行动，同时在有新证据出现时保留更新置信度的能力。在 LoCoMo 和 ALFWorld 基准测试上的实证评估表明，即使在数据有限的情况下，BeliefMem 也能取得最佳的平均性能，显著优于知名的基准方法。更广泛地说，这种概率性记忆产生了实质性的收益，并为部分可观测环境下的智能体记忆探索了一个新方向。

## Abstract
LLM agents that operate over long context depend on external memory to accumulate knowledge over time. However, existing methods typically store each observation as a single deterministic conclusion (e.g., inferring "API~X failed" from temporary errors), even though such observations are inherently partial and potentially ambiguous. By committing to one conclusion and discarding uncertainty, these methods introduce self-reinforcing error: the agent acts on the stored conclusion, never revisits alternatives, and reinforces the conclusion over time. To address this issue, we propose BeliefMem, which shifts the memory paradigm from committing to a single conclusion per observation to retaining multiple candidate conclusions with their probabilities. Concretely, BeliefMem stores the candidate conclusions as separate memory entries, each carrying a probability that is updated via Noisy-OR rules as new observations arrive. At retrieval, all candidates surface together with their probabilities, keeping alternatives visible to the agent. Since each conclusion in memory retains its probability, BeliefMem preserves the uncertainty that the deterministic paradigm discards, enabling the agent to act with high confidence on well-evidenced knowledge while retaining the capacity to update its confidence when new evidence arrives. Empirical evaluations on LoCoMo and ALFWorld benchmarks show that, even with limited data, BeliefMem achieves the best average performance, remarkably outperforming well-known baselines. More broadly, such probabilistic memory produces substantial gains and explores a new direction for agent memory in partially observable environments.

---

## 论文详细总结（自动生成）

这是一份关于论文《Belief Memory: Agent Memory Under Partial Observability》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 LLM 智能体记忆系统通常采用“确定性范式”，即从模糊的观测中推断并存储单一的结论（如：API 报错一次就记录为“API 已失效”）。
*   **研究动机**：在部分可观测环境（POMDP）下，这种做法会导致**自我强化错误（Self-reinforcing error）**。智能体一旦存储了错误结论，就会避免采取可能纠正该结论的行动，从而在后续交互中不断强化错误。
*   **整体含义**：论文提出将记忆从“确定性结论存储”转变为“概率性信念表示”，保留不确定性，使智能体能够根据新证据修正认知。

### 2. 方法论：BeliefMem
*   **核心思想**：模仿 POMDP 中的信念状态（Belief State），为每个记忆属性维护多个候选结论及其对应的概率。
*   **关键技术细节**：
    *   **属性级信念表示**：记忆不再是单一事实，而是属性（Attribute）与候选结论分布的集合。
    *   **记忆更新（Add & Merge）**：
        *   **Add**：发现新属性时初始化候选结论。
        *   **Merge**：当新观测支持已有属性时，使用 **Noisy-OR（噪声或）规则** 更新概率：$p_{t+1} = \min(1 - (1 - p_t)(1 - \Delta), 0.99)$，其中 $\Delta$ 是新证据的强度。
    *   **矛盾处理**：如果新观测支持矛盾的结论，则降低旧结论的概率（降至 0.25）。
    *   **信念感知检索**：检索时不只返回最高概率项，而是返回候选分布。检索分数结合了**语义相似度**和**时间衰减因子**。

### 3. 实验设计
*   **数据集/场景**：
    1.  **LoCoMo**：长文本对话记忆基准，包含单步、多步推理、时间推理和开放域问答。
    2.  **ALFWorld**：文本驱动的具身智能体基准，测试智能体在家庭环境中的任务执行能力（分为 Seen 和 Unseen 场景）。
*   **对比方法**：包括 MemGPT、Mem0、A-MEM、ReadAgent、MemoryBank、LangMem 和 MemoryOS 等知名记忆框架。
*   **评价指标**：F1 分数、BLEU-1（对话）；成功率 SR、完成步数 Steps（具身交互）。

### 4. 资源与算力
*   **硬件**：使用了 **NVIDIA A800-80GB GPU**。
*   **软件/模型**：
    *   LoCoMo 使用 GPT-4o 和 GPT-4o-mini 作为骨干模型。
    *   ALFWorld 使用 Qwen3-Next-80B-A3B-Instruct。
    *   嵌入模型使用 `text-embedding-3-small`。
*   **训练/运行**：论文未提及具体的训练时长（主要为推理和动态更新过程），但对比了 Token 消耗量，证明 BeliefMem 比 A-MEM 等方法更节省 Token。

### 5. 实验数量与充分性
*   **实验规模**：在两个完全不同领域的基准上进行了全面测试。
*   **消融实验**：针对“信念存储”、“信念检索”、“Add 操作”和“Merge 操作”分别做了消融，验证了各模块的必要性。
*   **对抗性实验**：专门设计了注入错误记忆的场景，测试系统的纠错能力。
*   **充分性评价**：实验设计较为全面，涵盖了长短期记忆、泛化能力（Unseen 场景）、数据效率（不同记忆库规模）以及超参数敏感性分析，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能领先**：BeliefMem 在两个基准上均显著优于现有确定性记忆方法。
*   **纠错能力强**：在对抗性实验中，BeliefMem 的纠错率是确定性方法的两倍，且纠错速度更快（平均仅需 4.75 步）。
*   **数据效率高**：仅使用 16.67% 的记忆数据，其表现就能超过大多数基线方法。
*   **信念收敛**：随着证据积累，正确结论的概率会稳步上升并收敛，有效过滤了观测噪声。

### 7. 优点
*   **范式创新**：首次将概率信念状态引入 LLM 智能体的长时记忆管理，解决了确定性记忆的固有缺陷。
*   **鲁棒性**：通过 Noisy-OR 机制，能够优雅地处理传感器噪声或环境的暂时性异常。
*   **透明度**：检索时保留替代方案，增加了智能体决策的透明度和可解释性。

### 8. 不足与局限
*   **理论保证缺失**：虽然实验证明了收敛性，但由于结论空间是开放的，缺乏严格的数学收敛证明。
*   **依赖 LLM 提取强度**：证据强度 $\Delta$ 依赖于 LLM 的主观判断，若 LLM 本身对证据强度的评估存在偏差，可能影响概率更新的准确性。
*   **计算开销**：虽然 Token 消耗优化较好，但维护多个候选结论和执行 Merge 操作的逻辑复杂度高于简单的向量存储。

（完）
