---
title: "Echo: KV-Cache-Free Associative Recall with Spectral Koopman Operators"
title_zh: Echo：基于谱库普曼算子的无 KV 缓存关联召回
authors: "Anupama Sridhar, Alexander Johansen"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06997v1"
tags: ["query:agent"]
score: 9.0
evidence: 用于长上下文智能体工具调用的无KV缓存关联召回
tldr: "针对 Transformer KV 缓存线性增长带来的内存瓶颈及 SSM 长程检索失效问题，本文提出 Echo 架构。它采用光谱库普曼注意力（SKA），利用核岭回归和幂迭代滤波器在恒定内存中存储和检索信息。Echo 在 MQAR 测试中达到 100% 准确率，并在多项长文本任务中优于 SSM 和混合模型，实现了无 KV 缓存的高效关联召回。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.06997v1/fig-001.webp\", \"caption\": \"Figure 1: Zero-shot retrieval accuracy vs. sequence length and KV count for single-hop (a) and two-hop (b) tasks. The pure SSM degrades sharply at longer sequences; SSM+Attn improves but still drops; SSM+SKA (982K params) maintains the highest accuracy and flattest profile across all settings.\", \"page\": 6, \"index\": 1, \"width\": 834, \"height\": 685}, {\"url\": \"assets/figures/arxiv/2605.06997v1/fig-002.webp\", \"caption\": \"Table 4: Zero-shot benchmark comparison of Echo-LM against small language models (180M parameters). All 180M models in the top section use comparable parameter budgets; the key differentiators are architecture and training data.\", \"page\": 7, \"index\": 2, \"width\": 1056, \"height\": 328}, {\"url\": \"assets/figures/arxiv/2605.06997v1/fig-003.webp\", \"caption\": \"Figure 2: Training with action mask set to 1 for all inputs on the Tool Trace Delayed Commit tasks (short). We find that CG+SKA works fine without the action mask. The x-axis is for training steps and y-axis for log loss (left) and accuracy (right).\", \"page\": 14, \"index\": 3, \"width\": 1056, \"height\": 379}, {\"url\": \"assets/figures/arxiv/2605.06997v1/fig-004.webp\", \"caption\": \"Figure 3: A Training run for 400k parameter Mamba-3 variants over the easy and hard System Prompt CoT Retrieval benchmark. The x-axis represents training iterations and y-axis represents training iterations and y-curve evaluation accuracy. Mamba-3 is unable to learn on this task.\", \"page\": 14, \"index\": 4, \"width\": 1056, \"height\": 425}]"
motivation: Transformer 的 KV 缓存存在内存瓶颈，而 SSM 在长距离关联召回任务中准确率会发生骤降。
method: 提出 Echo 架构，利用光谱库普曼注意力（SKA）通过核岭回归和学习的幂迭代滤波器在恒定内存中积累统计量。
result: "在 MQAR 基准测试中达到 100% 准确率，并在大海捞针和工具调用等任务上全面超越纯 SSM 及混合模型。"
conclusion: Echo 证明了利用光谱动力学算子可以在不依赖 KV 缓存的情况下，实现高效且精准的长程关联召回。
---

## 摘要
长链式思维推理和智能体工具调用会产生跨越数万个 token 的轨迹，然而 Transformer 的 KV 缓存随序列长度线性增长，在通用硬件上造成了内存瓶颈。状态空间模型（SSM）提供常数内存递归，但面临“内存悬崖”：一旦存储事实与查询之间的间隔超过递归状态的有效范围，检索准确率就会崩溃。我们推出了 Echo，这是一种围绕谱库普曼注意力（Spectral Koopman Attention, SKA）构建的无 KV 缓存关联召回架构；SKA 是注意力层的即插即用替代方案，它通过闭式动力学算子增强 SSM 块，其充分统计量在无 KV 缓存的情况下以常数内存累积。Echo 通过核岭回归将谱线性系统拟合到键（key）和值（value）的历史记录中，并通过学习到的幂迭代滤波器进行检索，所有这些都基于 O(r^2) 的流式状态，其中 r 是一个较小的投影秩。在多查询关联召回（Multi-Query Associative Recall）基准测试中，纯 Mamba-2 SSM 在所有间隔长度和 KV 对数量下的准确率均未能超过随机水平（约 3%），而在 50M 参数规模下，经 SKA 增强的模型在所有测试配置中均实现了 100% 的检索准确率，包括具有 32 个 KV 对且干扰间隔达 4,096 个 token 的情况。在包括大海捞针、工具轨迹和多跳检索在内的五个额外迁移基准测试中，SKA 在保持常数推理内存的同时，始终优于纯 SSM 和 SSM+Attention 混合模型。消融实验证实，是谱算子而非前缀掩码策略驱动了检索性能的提升。

## Abstract
Long chain-of-thought reasoning and agentic tool-calling produce traces spanning tens of thousands of tokens, yet Transformer KV caches grow linearly with sequence length, creating a memory bottleneck on commodity hardware. State-space models offer constant-memory recurrence but suffer a memory cliff: retrieval accuracy collapses once the gap between a stored fact and its query exceeds the effective horizon of the recurrent state. We introduce Echo, a KV-cache-free associative recall architecture built around Spectral Koopman Attention (SKA); a drop-in replacement for attention layers that augments SSM blocks with a closed-form dynamical operator whose sufficient statistics are accumulated in constant memory with no KV cache. Echo fits a spectral linear system to the key and value history via kernel ridge regression and retrieves through a learned power-iterated filter, all from $O(r^{2})$ streaming state where $r$ is a small projection rank. On the Multi-Query Associative Recall benchmark, a pure Mamba-2 SSM fails to exceed chance accuracy (${\sim}3\%$) across all gap lengths and KV-pair counts, while at the 50M parameter scale SKA-augmented models achieve $100\%$ retrieval accuracy on every configuration tested, including distractor gaps of $4{,}096$ tokens with $32$ KV pairs. Across five additional transfer benchmarks including needle-in-a-haystack, tool-trace, and multi-hop retrieval, SKA consistently outperforms both pure SSM and SSM+Attention hybrids while maintaining constant inference memory. Ablations confirm that the spectral operator, not the prefix masking strategy, drives the retrieval gain.

---

## 论文详细总结（自动生成）

### 论文总结：Echo —— 基于谱库普曼算子的无 KV 缓存关联召回

#### 1. 核心问题与整体含义（研究动机和背景）
论文针对大语言模型在处理长上下文（如长链式思维推理、智能体工具调用）时的两大痛点：
*   **Transformer 的内存瓶颈**：其 KV 缓存随序列长度线性增长，在处理数万 token 时会耗尽硬件内存。
*   **状态空间模型（SSM）的“内存悬崖”**：虽然 SSM（如 Mamba）具有恒定内存递归的优势，但在关联召回任务中表现极差。一旦查询点与事实存储点之间的距离超过其有效视野，检索准确率会发生断崖式下跌。
**核心目标**：设计一种既能保持 SSM 恒定推理内存，又能具备 Transformer 级别长程检索能力的架构。

#### 2. 核心方法论
论文提出了 **Echo** 架构，其核心是 **谱库普曼注意力（Spectral Koopman Attention, SKA）**，旨在替代传统的 Softmax 注意力层。
*   **核心思想**：将内容检索重新表述为**核岭回归（Kernel Ridge Regression）**问题。通过在恒定内存中累积二阶充分统计量，直接求解线性动力学系统的闭式解。
*   **关键技术细节**：
    1.  **统计量累积**：在流式处理中维护三个固定大小的矩阵：正则化 Gram 矩阵 $G$、跨时间协方差 $M$ 和值-键协方差 $C_v$。其存储复杂度为 $O(r^2)$，其中 $r$ 是投影秩（通常很小，如 48 或 64）。
    2.  **算子估计**：利用 Cholesky 分解对 $G$ 进行白化，计算库普曼转移算子 $A_w$ 和值读取算子 $B_v$。
    3.  **幂迭代谱滤波器**：引入 $A^K$（如 $K=2$）作为滤波器。其物理含义是放大动力学系统中的“持久模式”（即存储的事实），同时抑制“瞬态噪声”。
    4.  **Koopman MLP**：用一种基于谱约束动力系统的结构替代传统的 SwiGLU，减少了 1/3 的参数，同时保证了反向传播中的梯度秩（Gradient Rank）不被压缩。

#### 3. 实验设计
论文在多个尺度和场景下进行了验证：
*   **MQAR（多查询关联召回）**：在 50M 参数规模下，测试不同干扰间隔（最高 4096 token）和 KV 对数量。
*   **合成迁移基准（Sub-Million 规模）**：包括“大海捞针”（NIAH）、工具轨迹检索、多跳组合推理、系统提示词召回等 5 项任务。
*   **语言模型（180M 规模）**：在 FineWeb-Edu 数据集上从头训练，并在 HellaSwag、PIQA、ARC、LAMBADA 等 6 个零射（Zero-shot）基准上评估。
*   **对比方法**：纯 Mamba-2、Mamba-2 + Attention 混合模型、Pythia、GPT-2 等。

#### 4. 资源与算力
*   **硬件**：使用了单台 **NVIDIA B200 GPU**。
*   **训练量**：180M 规模的模型在 FineWeb-Edu 上训练了约 **10B tokens**（50,000 步）。
*   **推理内存**：SKA 模块在 50M 模型中仅需约 77 KB 的 FP32 状态，实现了真正的 $O(1)$ 恒定内存推理。

#### 5. 实验数量与充分性
*   **实验覆盖面**：涵盖了从极小规模（<1M）到中等规模（180M）的实验，包含了合成任务、迁移学习任务和真实语言建模任务。
*   **消融实验**：通过消融实验验证了“谱算子”而非“掩码策略”是性能提升的主因。
*   **公平性**：对比模型在参数量、训练步数和优化器配置上保持了高度一致，且测试了长达 64 倍于训练长度的泛化能力。
*   **充分性评价**：实验设计较为严谨，特别是针对“内存悬崖”的对比非常直观，但在更大规模（Billion 级别）和更大数据量（Trillion 级别）上的表现尚待验证。

#### 6. 主要结论与发现
*   **消除内存悬崖**：在 MQAR 任务中，纯 Mamba-2 准确率仅为 3%（随机水平），而 Echo 达到了 **100% 准确率**。
*   **长程泛化**：Echo 在 4096 长度下的检索准确率保持在 65%，而基准模型在超过训练长度后迅速崩溃至 5% 以下。
*   **语言建模能力**：Echo-180M 在 5/6 的零射基准上优于同规模的 Transformer 和 SSM 模型，甚至在某些指标上超过了参数量两倍的 GPT-2 345M。
*   **数据效率**：Echo 表现出极高的数据效率，仅用 10B token 训练的效果即可匹配其他模型用 100B token 训练的水平。

#### 7. 优点
*   **理论优雅**：将库普曼算子理论与核岭回归结合，为注意力机制提供了一个闭式的、具有动力学解释的替代方案。
*   **极低内存**：彻底摆脱了线性增长的 KV 缓存，使长上下文推理在廉价硬件上成为可能。
*   **梯度友好**：SKA 和 Koopman MLP 的 Jacobian 矩阵具有全秩特性，避免了 Softmax 注意力层常见的梯度瓶颈。

#### 8. 不足与局限
*   **算力开销**：虽然内存是 $O(1)$，但每步推理涉及 $O(r^3)$ 的 Cholesky 分解，在 $r$ 较大时可能存在计算延迟（目前通过 Triton 内核融合缓解）。
*   **任务偏差**：合成基准（如 MQAR）具有清晰的“事实-干扰-查询”结构，而自然语言的关联召回更为模糊，Echo 在复杂 Agent 任务（如 RULER）上的表现仍需进一步验证。
*   **规模限制**：目前实验最高仅到 180M 参数，尚未在大规模参数（如 7B+）上证明其扩展性（Scaling Laws）。

（完）
