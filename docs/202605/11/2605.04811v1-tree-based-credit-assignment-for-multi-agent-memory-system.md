---
title: Tree-based Credit Assignment for Multi-Agent Memory System
title_zh: 面向多智能体记忆系统的基于树的信用分配
authors: "Marina Mao, Alexandr Liu, Pengbo Li, Siheng Li, Bo Zhou, Xiang Wang"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.04811v1"
tags: ["query:agent"]
score: 9.5
evidence: 基于强化学习的多智能体记忆系统信用分配
tldr: 针对大语言模型长程任务中的多智能体记忆系统，现有强化学习方法存在奖励信号粗糙或标注成本高的问题。本文提出TreeMem，通过将多智能体流水线扩展为树状结构，利用蒙特卡洛平均估算各智能体对最终奖励的贡献，实现无需特定任务标注的精准信用分配，从而有效优化构建、摘要和检索等异构智能体的协同表现。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.04811v1/fig-001.webp\", \"caption\": \"Table 1: Query-answering accuracy on PersonaMem under 32K, 128K, and 1M history lengths. Bold marks the overall best result, and underlining marks the strongest compared method.\", \"page\": 6, \"index\": 1, \"width\": 798, \"height\": 471}, {\"url\": \"assets/figures/arxiv/2605.04811v1/fig-002.webp\", \"caption\": \"Table 2: More experimental results of TreeMem and baselines on the LongMemEval and LOCOMO benchmarks with Qwen2.5-7B backbone. Bold marks the overall best result, and underlining marks the strongest compared method.\", \"page\": 6, \"index\": 2, \"width\": 810, \"height\": 214}, {\"url\": \"assets/figures/arxiv/2605.04811v1/fig-003.webp\", \"caption\": \"Figure 1: Demonstration and limitations of two training paradigms for existing RL-based methods in multi-agent memory systems.\", \"page\": 2, \"index\": 3, \"width\": 822, \"height\": 362}, {\"url\": \"assets/figures/arxiv/2605.04811v1/fig-004.webp\", \"caption\": \"Table 4: Ablation study on tree-based credit assignment across different agents. w/o tree trains all agents without tree-based credit assignment; Builder w/o tree and Summarizer w/o tree remove it only from the Builder and Summarizer agents, respectively.\", \"page\": 7, \"index\": 4, \"width\": 810, \"height\": 204}, {\"url\": \"assets/figures/arxiv/2605.04811v1/fig-005.webp\", \"caption\": \"Table 3: Performance comparison of multi-agent memory systems trained with different reward signals: final accuracy reward, task-specific rewards, and our agent-specific rewards.\", \"page\": 7, \"index\": 5, \"width\": 810, \"height\": 205}, {\"url\": \"assets/figures/arxiv/2605.04811v1/fig-006.webp\", \"caption\": \"Figure 2: Illustration of our proposed TreeMem: a tree-based multi-agent RL training framework for memory systems, expanding each agent’s sampled actions into multiple branches to estimate agent-specific credit and specialize each agent’s policy.\", \"page\": 3, \"index\": 6, \"width\": 818, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2605.04811v1/fig-007.webp\", \"caption\": \"Figure 3: Reward curve comparison of applying tree-based specific rewards to each agent individually.\", \"page\": 8, \"index\": 7, \"width\": 812, \"height\": 227}, {\"url\": \"assets/figures/arxiv/2605.04811v1/fig-008.webp\", \"caption\": \"Figure 4: Sensitivity analysis of TreeMem with respect to the hyperparameters group size G, branch size J , and branch size K, as defined in Eq. (3).\", \"page\": 9, \"index\": 8, \"width\": 813, \"height\": 207}, {\"url\": \"assets/figures/arxiv/2605.04811v1/fig-009.webp\", \"caption\": \"Figure 5: The efficiency analysis of our TreeMem against two training baselines: “+Rfinal” (final QA accuracy rewards) and “+ Rtask” (task-specific rewards). Across context lengths (32K, 128K, 1M), the left plot compares per-step training time, the middle plot compares steps to convergence, and the right plot evaluates token efficiency as the ratio of memory tokens (from the memory builder/summarizer) to original conversation history tokens, where a lower rate means higher efficiency.\", \"page\": 9, \"index\": 9, \"width\": 813, \"height\": 206}]"
motivation: 现有的多智能体记忆系统优化依赖于粗糙的最终奖励或高成本的人工标注，难以有效指导不同子任务智能体的策略更新。
method: 提出TreeMem方法，将智能体流水线扩展为树结构，通过对后续分支进行蒙特卡洛采样来估算每个智能体的特定信用信号。
result: 在长程任务基准测试中，TreeMem显著优于强基线模型，验证了树状信用分配在多智能体记忆系统中的有效性。
conclusion: TreeMem通过自动化的信用分配机制，成功解决了异构智能体协同优化难题，为提升大模型长程记忆能力提供了新方案。
---

## 摘要
记忆系统被广泛采用以增强大语言模型（LLMs）处理长程任务的能力，通常被组织为包含记忆构建、摘要和检索智能体的多智能体流水线。为了增强该系统，现有的基于强化学习（RL）的方法要么对所有智能体统一应用最终的下游任务奖励（如问答准确率），这种方式粗糙且模糊；要么为不同子任务上的智能体设计特定任务的奖励，这需要昂贵的标注（如关键证据）且难以可靠地定义。为了解决这些局限性，我们提出了面向多智能体记忆系统的基于树的信用分配（TreeMem），它在无需特定任务标注的情况下，从最终奖励中推导出特定于智能体的信用。具体而言，TreeMem 将多智能体流水线（构建者-摘要者-检索者）扩展为树状结构，其中每个智能体的输出被扩展为多个后续分支。每个智能体的贡献通过对其后续分支进行蒙特卡洛平均来估计，从而捕捉中间智能体动作如何影响最终奖励。这将粗糙的最终奖励转化为特定于智能体的优化信号。这些信号随后被用于同时更新所有智能体的策略，帮助异构智能体有效地实现专业化。在长程基准测试上的实验表明，TreeMem 相比强基准模型提升了记忆系统的性能，验证了树状结构信用分配在多智能体记忆系统中的有效性。

## Abstract
Memory systems are widely adopted to enhance LLMs for long-horizon tasks, and are commonly organized as multi-agent pipelines with memory building, summarizing, and retrieval agents. To empower this system, existing RL-based methods either apply final downstream task rewards (e.g., QA accuracy) for all agents uniformly, which are coarse and ambiguous, or design task-specific rewards for agents on different subtasks, which require costly annotations (e.g., key evidence) and are difficult to define reliably. To address these limitations, we propose Tree-based Credit Assignment for Multi-Agent Memory Systems (TreeMem), which derives agent-specific credit from the final reward without task-specific annotations. Specifically, TreeMem extends the multi-agent pipeline (builder--summarizer--retrieval) into a tree structure, where each agent's outputs are expanded into multiple subsequent branches. The contribution of each agent is estimated via Monte Carlo averaging over its subsequent branches, capturing how intermediate agent actions may influence the final reward. This converts the coarse final reward into agent-specific optimization signals. These signals are then used to update all agent policies simultaneously, helping heterogeneous agents specialize effectively. Experiments on long-horizon benchmarks show that TreeMem improves memory system performance over strong baselines, validating the effectiveness of tree-structured credit assignment for the multi-agent memory system.

---

## 论文详细总结（自动生成）

这是一份关于论文《Tree-based Credit Assignment for Multi-Agent Memory System》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对大语言模型（LLM）在处理长程任务（Long-horizon tasks）时的**多智能体记忆系统优化问题**展开研究。
*   **背景**：现有的记忆系统通常由“构建（Builder）”、“摘要（Summarizer）”和“检索（Retrieval）”三个异构智能体组成。
*   **核心痛点**：
    *   **奖励信号粗糙**：传统的强化学习（RL）方法通常将最终任务的奖励（如 QA 准确率）统一分配给所有智能体，导致中间步骤的贡献难以界定（信用分配难题）。
    *   **标注成本高且不可靠**：为每个子任务设计特定奖励（如证据覆盖率）需要大量人工标注，且容易引发“奖励黑客（Reward Hacking）”现象，即智能体优化了中间指标却损害了最终表现。
*   **研究目标**：在无需额外人工标注的情况下，从最终奖励中推导出精准的、针对特定智能体的信用信号。

### 2. 方法论：TreeMem 框架
TreeMem 的核心思想是将线性的多智能体流水线扩展为**树状采样结构**，通过路径聚合来估算中间决策的价值。
*   **树状结构生成**：在训练阶段，对于同一个输入，Builder 采样 $G$ 个动作；对于每个 Builder 动作，Summarizer 采样 $J$ 个分支；对于每个 Summarizer 动作，Retrieval 采样 $K$ 个最终回答。这形成了一个包含 $G \times J \times K$ 条路径的树。
*   **基于树的信用分配（Credit Assignment）**：
    *   **叶子节点**：直接获取最终任务奖励 $R$。
    *   **中间节点**：通过**蒙特卡洛平均（Monte Carlo Averaging）**其所有后续分支的奖励来计算信用。例如，Summarizer 的信用是其后续 $K$ 个回答奖励的平均值；Builder 的信用则是其后续 $J \times K$ 个路径奖励的平均值。
    *   **长度惩罚**：在 Builder 阶段引入长度惩罚项，防止其通过简单复制全文来获取高分，鼓励信息压缩。
*   **策略优化**：采用类似 GRPO 的组相对优化算法。从树中随机抽取一条完整路径作为代表，利用计算出的特定信用（经过组内归一化处理）来同时更新三个智能体的策略。

### 3. 实验设计
*   **数据集与场景**：
    *   **PersonaMem**：核心测试集，包含 32K、128K、1M 三种不同长度的历史对话，涵盖 7 类个性化问答任务。
    *   **LongMemEval & LOCOMO**：用于验证通用长程记忆能力的外部基准测试。
*   **Benchmark 与对比方法**：
    *   **基础方法**：直接输入上下文（Base）、标准 RAG。
    *   **提示词驱动记忆**：CAM、MemoryBank、A-Mem、Mem0。
    *   **RL 优化记忆**：Mem1、Mem-$\alpha$、Memory-R1、CoMAM（目前最强的协同优化基线）。
*   **模型骨干**：采用了 Qwen2.5 和 Llama-3 系列模型（3B 至 8B 规模）。

### 4. 资源与算力
*   **硬件环境**：实验运行在一个包含 **8 张 NVIDIA A100 GPU** 的集群上。
*   **训练细节**：
    *   Batch Size 为 128，Group Size ($G$) 为 8。
    *   最大上下文窗口设置为 32K。
    *   训练周期为 5 个 Epoch。
*   **时长**：文中未明确给出总训练小时数，但通过效率分析图表显示，TreeMem 的收敛速度快于传统任务特定奖励方法，且单步训练耗时增加不明显。

### 5. 实验数量与充分性
实验设计非常全面且具有说服力：
*   **多维度验证**：涵盖了从 32K 到 1M 极长文本的跨度。
*   **消融实验**：分别验证了移除 Builder 或 Summarizer 的树状信号后的性能下降，证明了每个环节信用分配的必要性。
*   **超参数敏感性**：对分支数量 $J, K$ 和组大小 $G$ 进行了详细分析。
*   **客观性**：使用了 GPT-4o 作为裁判（LLM-as-judge）进行评分，并对比了 Token 效率（压缩比），确保了评价的多元化。

### 6. 主要结论与发现
*   **性能领先**：TreeMem 在所有长程任务基准上均显著优于现有 RL 方法，相比最强基线在 PersonaMem 上提升了约 5.5% - 7.5%。
*   **智能体专业化**：通过树状信用分配，Builder 学会了提取关键证据而非冗余信息，Summarizer 学会了更高层级的抽象，实现了真正的异构协作。
*   **效率优势**：尽管训练时有分支采样，但由于收敛更快且生成的记忆更精简，其整体 Token 效率和推理成本优于基线。

### 7. 优点
*   **无需标注**：完全依赖最终结果反馈，解决了长程任务中中间步骤难以标注的难题。
*   **端到端对齐**：信用分配机制确保了中间智能体的优化目标与最终任务目标在数学逻辑上是一致的。
*   **即插即用**：该框架可以适配不同的模型骨干和记忆架构。

### 8. 不足与局限
*   **训练计算开销**：虽然文中提到单步开销可控，但树状采样（$G \times J \times K$）在训练初期依然比单路径采样需要更多的前向计算资源。
*   **超参数依赖**：性能对分支采样数 $J$ 和 $K$ 有一定依赖，采样过少会导致信用估算噪声大，采样过多则计算成本激增。
*   **静态记忆假设**：实验主要集中在问答任务，对于需要频繁动态更新、删除记忆的极复杂交互场景，树状结构的构建可能会更加复杂。

（完）
