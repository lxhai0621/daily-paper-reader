---
title: Decentralized Diffusion Policy Learning for Enhanced Exploration in Cooperative Multi-agent Reinforcement Learning
title_zh: 协作式多智能体强化学习中用于增强探索的分散式扩散策略学习
authors: "Yuyang Zhang, Haldun Balim, Na Li"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07101v1"
tags: ["query:agent"]
score: 9.0
evidence: 多智能体强化学习的去中心化扩散策略
tldr: 针对协作多智能体强化学习（MARL）中高斯策略表达能力不足导致探索受限的问题，本文提出去中心化扩散策略学习（DDPL）。该方法利用扩散概率模型（DDPM）的多模态分布特性增强探索，并引入重要性采样评分匹配（ISSM）实现高效在线训练。实验证明，DDPL在多个连续动作MARL基准测试中均显著优于现有方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.07101v1/fig-001.webp\", \"caption\": \"Table 3: Hyperparameters for Section 5\", \"page\": 25, \"index\": 1, \"width\": 827, \"height\": 185}, {\"url\": \"assets/figures/arxiv/2605.07101v1/fig-002.webp\", \"caption\": \"Figure 3: Average episode return during training for Algorithm 2 and baselines. Curves and shaded areas show the mean and std. over three random seeds, respectively.\", \"page\": 8, \"index\": 2, \"width\": 826, \"height\": 352}, {\"url\": \"assets/figures/arxiv/2605.07101v1/fig-003.webp\", \"caption\": \"Figure 4: LEFT: the average episode return of DDPL with diffusion and Gaussian policies; RIGHT: the action distribution at a representative state for two action dimensions from two agents.\", \"page\": 9, \"index\": 3, \"width\": 826, \"height\": 336}]"
motivation: 传统DecSPG算法中常用的高斯策略表达能力有限，难以处理复杂的智能体交互，严重阻碍了多智能体环境下的有效探索。
method: 提出DDPL框架，将每个智能体的策略参数化为扩散模型，并设计了具有理论保证的重要性采样评分匹配（ISSM）方法进行在线训练。
result: 在MPE、Multi-agent MuJoCo、IsaacLab及JAX版星际争霸等多个连续动作基准测试中，DDPL均取得了持续的性能提升。
conclusion: 通过引入高表达能力的扩散模型和高效的训练机制，DDPL有效解决了协作MARL中的探索难题，提升了多智能体系统的协作效率。
---

## 摘要
协作式多智能体强化学习（MARL）涉及复杂的智能体交互，需要有效的探索策略。一类著名的 MARL 算法，即分散式 Softmax 策略梯度（DecSPG），通过基于能量的策略更新来解决这一问题。然而在实践中，此类基于能量的策略难以维护，通常被投影到高斯策略类中。在这项工作中，我们证明了高斯策略有限的表达能力严重阻碍了 DecSPG 中的探索，且这种局限性随着智能体数量的增加而加剧。为解决这一问题，我们提出了分散式扩散策略学习（DDPL），它使用去噪扩散概率模型对每个智能体的策略进行参数化。这是一种极具表达能力的生成模型，能够捕捉多模态动作分布以增强探索。DDPL 通过重要性采样评分匹配（ISSM）实现了扩散策略的高效在线训练，这是一种具有理论保证的新型训练方法。我们在具有代表性的连续动作 MARL 基准测试中评估了 DDPL，包括多智能体粒子环境、多智能体 MuJoCo、IsaacLab 以及 JAX 重新实现的星际争霸多智能体挑战赛，并观察到了持续的性能提升。

## Abstract
Cooperative multi-agent reinforcement learning (MARL) involves complex agent interactions and requires effective exploration strategies. A prominent class of MARL algorithms, decentralized softmax policy gradient (DecSPG), addresses this through energy-based policy updates. In practice, however, such energy-based policies are intractable to maintain and are commonly projected onto the Gaussian policy class. In this work, we show that the limited expressiveness of Gaussian policies severely hinders exploration in DecSPG, and this limitation worsens as the number of agents grows. To address this issue, we propose decentralized diffusion policy learning (DDPL), which parameterizes each agent's policy with a denoising diffusion probabilistic model, an expressive generative model that captures multi-modal action distributions for enhanced exploration. DDPL enables efficient online training of diffusion policies via importance sampling score matching (ISSM), a novel training method with theoretical guarantee. We evaluate DDPL on representative continuous-action MARL benchmarks, including multi-agent particle environment, multi-agent MuJoCo, IsaacLab, and JAX-reimplemented StarCraft multi-agent challenge, and observe consistently improved performance.

---

## 论文详细总结（自动生成）

这篇论文由哈佛大学的研究团队提出，旨在解决协作式多智能体强化学习（MARL）中由于策略表达能力不足导致的探索受限问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在协作式 MARL 中，智能体需要在高维联合动作空间中进行有效探索以发现最优协调模式。
*   **背景问题**：现有的分散式 Softmax 策略梯度（DecSPG）算法虽然在理论上能收敛到纳什均衡，但在实践中通常将策略投影为**高斯分布（Gaussian Policy）**。高斯策略是单模态的，表达能力有限，无法捕捉复杂的、多模态的动作分布。
*   **核心发现**：论文通过理论证明和实例展示，发现这种“单模态限制”会导致严重的探索不足，且随着智能体数量增加，这种负面影响呈指数级增长，导致算法过早收敛到次优均衡。

### 2. 方法论
*   **核心思想**：提出**分散式扩散策略学习（DDPL）**，利用去噪扩散概率模型（DDPM）的高表达能力来参数化智能体策略，从而捕捉多模态分布，增强探索。
*   **关键技术细节**：
    *   **扩散策略参数化**：每个智能体维护一个基于扩散模型的策略，通过多步去噪过程生成动作。
    *   **重要性采样评分匹配（ISSM）**：这是本文的核心创新。传统的扩散模型训练（评分匹配）需要目标分布的样本，但在在线 RL 中目标分布（基于能量的策略更新）是未知的。ISSM 通过从当前策略采样并利用重要性权重，在不需要目标分布样本的情况下实现扩散策略的在线训练。
    *   **算法流程**：
        1.  **采样**：使用当前扩散策略进行环境交互，存储数据。
        2.  **Q值估计**：估计每个智能体的平均动作价值函数（Q函数）。
        3.  **策略更新**：利用 ISSM 目标函数，将扩散策略向能量函数 $\exp(\eta Q)$ 诱导的目标分布靠拢。
*   **理论保证**：论文证明了 ISSM 的样本复杂度，并给出了算法在纳什均衡间隙（NE-gap）上的收敛界限。

### 3. 实验设计
*   **实验场景**：
    *   **MPE**：Simple-Reference 任务。
    *   **Multi-agent MuJoCo (MaMuJoCo)**：Ant (2x4, 4x2, 8x1) 和 HumanoidStandup。
    *   **IsaacLab**：双影手协作（ShadowHandOver）。
    *   **SMAX (JAX版星际争霸)**：3s_vs_5z, 6h_vs_8z。
*   **对比方法（Benchmarks）**：
    *   **HAPPO / MAPPO**：主流的在策略（On-policy）MARL 算法。
    *   **HASAC**：主流的离策略（Off-policy）最大熵 MARL 算法。
    *   **DDPL-Gaussian**：作为消融实验，将 DDPL 中的扩散策略替换为高斯策略。

### 4. 资源与算力
*   **硬件型号**：使用 **NVIDIA RTX 5090 GPU**。
*   **训练时长**：每个随机种子的训练时间在 **1 到 8 小时**之间（取决于任务复杂度）。
*   **效率表现**：DDPL 的推理时间保持在 1ms 以下；训练开销比同类扩散 RL 算法（如 DACER、QVPO）分别降低了 39% 和 81%。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个不同类型的基准测试，共 8 个具体任务。
*   **充分性**：
    *   每个实验运行了 **3 个随机种子**，并给出了均值和标准差。
    *   进行了**消融实验**，直接对比了扩散策略与高斯策略在相同框架下的表现，验证了多模态的重要性。
    *   进行了**灵敏度分析**，测试了去噪步数（H）和噪声调度（Noise Schedule）对性能的影响。
    *   实验设计较为客观，对比了当前最先进的（SOTA）MARL 算法。

### 6. 主要结论与发现
*   **性能领先**：DDPL 在 8 个任务中的 7 个取得了最高回报，尤其在复杂的 SMAX 任务中，性能提升超过 130%。
*   **探索优势**：扩散策略在训练初期能保持多模态分布，允许智能体尝试不同的协调模式，而高斯策略则迅速收敛到单一模式。
*   **可扩展性**：理论和实验均证明，DDPL 在处理更多智能体参与的任务时，比传统方法更具鲁棒性。

### 7. 优点
*   **高表达能力**：成功将扩散模型引入分散式 MARL，解决了 DecSPG 长期存在的投影误差问题。
*   **训练高效**：ISSM 算法巧妙地解决了在线 RL 中扩散模型难以训练的痛点，且具有坚实的理论支撑。
*   **去中心化**：保持了完全分散的策略执行，适合大规模多智能体系统。

### 8. 不足与局限
*   **通信利用**：目前 DDPL 采用完全分散的策略，未考虑在网络化 MARL 中利用智能体间的低成本通信来进一步提升协作。
*   **适用范围**：研究重点在于“相同利益（Cooperative）”场景，尚未扩展到一般博弈或竞争性场景。
*   **初期学习速度**：在某些任务中，由于探索范围更广，DDPL 的初期得分提升速度可能略慢于单模态算法。

（完）
