---
title: "RAG-Enhanced Kernel-Based Heuristic Synthesis (RKHS): A Structured Methodology Using Large Language Models for Hardware Design"
title_zh: RAG增强的基于内核的启发式合成 (RKHS)：一种利用大语言模型进行硬件设计的结构化方法论
authors: "Shiva Ahir, Alex Doboli"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.26153v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 将检索增强生成（RAG）集成到硬件设计启发式算法中
tldr: 针对EDA启发式算法设计依赖专家经验的问题，本文提出RKHS框架。该方法结合检索增强生成（RAG）、内核模板和LLM迭代优化循环，实现了硬件设计算法的系统化合成。在HLS调度实验中，该方法以极低的运行开销显著降低了调度延迟，证明了其在自动化EDA优化中的有效性和通用性。
source: arxiv
selection_source: fresh_fetch
motivation: 解决EDA工具中启发式算法设计过度依赖人工专家经验且难以系统化生成的问题。
method: 提出RKHS框架，利用RAG技术、内核模板和LLM驱动的自我反馈循环来合成可重用的优化启发式算法。
result: "在HLS列表调度应用中，该方法在仅增加1.3倍运行开销的前提下，将平均调度长度缩短了高达11%。"
conclusion: RKHS为利用大语言模型自动化合成高效、结构化的EDA优化算法提供了一种具有普适性的新方法。
---

## 摘要
启发式设计支撑着现代电子设计自动化（EDA）工具，但制定有效的布局、布线和调度策略需要大量的专业知识。我们研究了大语言模型（LLMs）如何超越单次代码生成，系统地合成可重用的优化启发式算法。我们提出了 RAG 增强的基于内核的启发式合成（RKHS），它集成了检索增强生成（RAG）、紧凑的内核启发式模板以及受迭代自我反馈启发的 LLM 驱动细化循环。在应用于高层综合（HLS）中最小化延迟的列表调度时，原型系统在仅增加 1.3 倍运行开销的情况下，比基准调度器减少了高达 11% 的平均调度长度，且这种结构化的检索-合成循环可推广到其他 EDA 优化问题。

## Abstract
Heuristic design upholds modern electronic design automation (EDA) tools, yet crafting effective placement, routing, and scheduling strategies entails substantial expertise. We study how large language models (LLMs) can systematically synthesize reusable optimization heuristics beyond one-shot code generation. We propose RAG-Enhanced Kernel-Based Heuristic Synthesis (RKHS), which integrates retrieval-augmented generation (RAG), compact kernel heuristic templates, and an LLM-driven refinement loop inspired by iterative self-feedback. Applied to latency-minimizing list scheduling in high-level synthesis (HLS), a prototype reduces average schedule length by up to 11 percent over a baseline scheduler with only 1.3x runtime overhead, and the structured retrieval-synthesis loop generalizes to other EDA optimization problems.

---

## 论文详细总结（自动生成）

以下是对论文《RAG-Enhanced Kernel-Based Heuristic Synthesis (RKHS): A Structured Methodology Using Large Language Models for Hardware Design》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：现代电子设计自动化（EDA）工具高度依赖人工设计的启发式算法来解决布局、布线和调度等 NP-hard 问题。然而，这些算法的设计过程极其耗费人力，且难以在不同基准测试或技术节点间推广。
*   **核心问题**：现有的 LLM 在 EDA 领域多被视为“单次代码生成器”，缺乏系统性的机制来提取结构化特征并合成可重用的优化策略。
*   **整体含义**：本文提出了一种名为 **RKHS** 的框架，旨在通过检索增强生成（RAG）和结构化内核（Kernel）技术，使 LLM 能够自动合成可执行、可解释且可迁移的硬件设计启发式算法。

### 2. 论文提出的方法论
RKHS 将启发式合成视为一个“检索引导的搜索过程”，其核心流程如下：
*   **内核库构建（Kernel Library）**：预先从训练图中提取结构化“内核”。每个内核包含：
    *   **结构签名**：总结重复出现的子图特征（如关键路径摘要、扇出分布、重聚标记）。
    *   **启发式模板**：与该结构相关的调度偏好（如针对重聚区域的优先级公式）。
*   **检索机制**：对于新的输入有向无环图（DAG），计算其结构嵌入向量，利用**余弦相似度**从库中检索最匹配的 $m$ 个内核。
*   **LLM 合成循环**：
    1.  **提示词构建**：将检索到的内核、调度约束和目标规范输入 LLM（如 GPT-4）。
    2.  **代码生成**：LLM 生成确定性的优先级函数 $\pi(v)$。
    3.  **评估与反馈**：在验证集上运行生成的调度器，计算延迟（Makespan）和运行开销。
    4.  **迭代细化**：基于失败案例和性能回归，通过自我反馈循环（Self-Feedback）对启发式代码进行 $N$ 轮迭代优化。
*   **核心公式**：最终合成的优先级函数通常表现为结构特征的加权组合，例如：$\pi(v) = \alpha \cdot crit(v) + \beta \cdot fanout(v) - \gamma \cdot level(v)$。

### 3. 实验设计
*   **应用场景**：高层综合（HLS）中最小化延迟的资源受限列表调度（List Scheduling）。
*   **数据集**：包含 200 个训练图（$G_{train}$）和 50 个验证图（$G_{val}$）。
*   **Benchmark（基准）**：经典的基于层级（Level-based）的列表调度算法。
*   **对比方法与消融实验**：
    *   **Full RKHS**：完整框架。
    *   **No Retrieval**：不使用检索。
    *   **No Motif**：不使用结构化基元抽象。
    *   **Random Kernel**：随机选择内核。

### 4. 资源与算力
*   **LLM 模型**：实验使用了 **GPT-4**。
*   **超参数**：内核库大小 $|K|=50$，迭代次数 $N=3$，检索数量 $m=5$。
*   **算力说明**：文中**未明确说明**具体的硬件算力（如 GPU 型号、数量或具体训练时长）。由于主要依赖 LLM API 和轻量级的列表调度评估，其主要的计算开销集中在 LLM 推理和调度模拟上。

### 5. 实验数量与充分性
*   **实验规模**：使用了 250 个 DAG 图进行训练和验证，并进行了 3 轮迭代合成。
*   **充分性评价**：
    *   **消融实验**：通过移除检索、基元等模块，清晰地证明了各组件对降低延迟和减少方差的贡献。
    *   **客观性**：报告了平均值、标准差和 95% 置信区间，体现了统计严谨性。
    *   **局限性**：实验主要集中在调度问题上，虽然声称具有通用性，但尚未在布局、布线等其他 EDA 任务上展示大规模实验结果。

### 6. 论文的主要结论与发现
*   **性能提升**：RKHS 合成的启发式算法相比基准方法，平均调度延迟降低了 **11%**。
*   **开销可控**：在获得性能提升的同时，运行时间开销仅增加约 **1.3 倍**，在 EDA 流程中属于可接受范围。
*   **结构化优势**：实验证明，结合扇出（Fanout）和重聚（Reconvergence）等结构信号的优先级函数，比单一的层级或关键路径规则更具鲁棒性。
*   **可解释性**：LLM 生成的代码是确定性的 Python 函数，设计者可以直观地理解调度逻辑。

### 7. 优点
*   **方法论创新**：将 RAG 引入 EDA 算法合成，解决了 LLM 在处理复杂图结构时缺乏上下文引导的问题。
*   **闭环优化**：引入了迭代反馈机制，使模型能从失败的调度案例中学习并修正策略。
*   **知识重用**：通过内核库实现了专家经验（结构基元）与模型生成能力的有效结合。

### 8. 不足与局限
*   **数据集规模**：验证集仅 50 个图，对于复杂的工业级芯片设计来说，样本量可能不足以覆盖所有极端情况。
*   **LLM 依赖**：性能高度依赖于底层 LLM（如 GPT-4）的推理能力，且 API 调用可能存在成本和隐私问题。
*   **泛化验证**：目前仅在 HLS 调度任务上验证，尚未证明其在更复杂的物理设计（如布线拥塞预测）中的有效性。
*   **搜索空间**：迭代次数 $N=3$ 较小，可能尚未达到启发式算法的最优解空间。

（完）
