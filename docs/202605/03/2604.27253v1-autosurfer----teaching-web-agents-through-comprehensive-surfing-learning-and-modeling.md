---
title: "AutoSurfer -- Teaching Web Agents through Comprehensive Surfing, Learning, and Modeling"
title_zh: AutoSurfer —— 通过全面的冲浪、学习与建模教学 Web 智能体
authors: "Fazle Elahi Faisal, Qianhui Wu, Baolin Peng, Jianfeng Gao"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.27253v1"
tags: ["query:agent"]
score: 9.5
evidence: 基于多模态大语言模型的网页智能体，用于自动化复杂任务。
tldr: AutoSurfer是一个旨在提升Web智能体性能的综合性轨迹生成框架。针对现有方法数据覆盖不足和任务合成虚假等问题，它通过广度优先探索、基于路径的任务合成及轨迹精炼技术，生成高质量训练数据。在WebArena基准测试中，该方法显著提升了模型性能，超越了现有最先进技术。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.27253v1/fig-001.webp\", \"caption\": \"Figure 1: The full architecture of AutoSurfer. Given a web environment, (a) AutoSurfer comprehensively explores the website to discover complex tasks and their trajectories, (b) AutoSurfer refines the discovered tasks and trajectories to improve correctness and quality while reducing hallucinations, (c) AutoSurfer converts the refined tasks and trajectories into an SFT dataset, and (d) AutoSurfer uses the SFT dataset to train a wLLM.\", \"page\": 3, \"index\": 1, \"width\": 854, \"height\": 231}, {\"url\": \"assets/figures/arxiv/2604.27253v1/fig-002.webp\", \"caption\": \"Table 2: Performance comparison between AutoSurfer and other methods based on WebArena benchmark. Qwen2.5 baseline uses the original Qwen2.5 model, while other methods use the corresponding fine-tuned variant. AutoSurfer has two variants: AutoSurfer wllm and AutoSurfer webarena. The best performing method for each website and overall is highlighted in bold. GPT-4.1 baseline is excluded from the comparison as it is substantially larger than Qwen2.5, but its results show the upper bound performance by a large model.\", \"page\": 9, \"index\": 2, \"width\": 877, \"height\": 277}]"
motivation: 现有的Web智能体训练数据生成方法存在网站覆盖不全、任务合成虚假以及轨迹不可靠等局限性。
method: 提出了一种结合系统性广度优先探索、基于路径的任务合成以及引导式轨迹精炼的综合生成框架。
result: "在WebArena基准测试中，使用AutoSurfer生成的数据微调模型，其任务完成率从19.59%提升至24.23%。"
conclusion: AutoSurfer通过更全面的网站探索和更真实的轨迹生成，为训练高性能、特定网站的Web智能体提供了有效的解决方案。
---

## 摘要
多模态大语言模型（LLM）的最新进展彻底改变了能够自动执行网站复杂任务的 Web 智能体。然而，由于高质量 Web 轨迹训练数据的稀缺，其准确性仍然受限。现有的自动轨迹生成方法由于基于主页的任务建议或随机游走探索，导致网站覆盖不全。这些方法通常会导致幻觉或模糊的任务合成，从而产生不完整且不可靠的轨迹生成。在此，我们提出了 AutoSurfer，这是一个全面的 Web 轨迹生成器，通过三项关键创新解决了这些局限性。首先，AutoSurfer 采用系统的广度优先探索策略，维护已发现页面和动作追踪的队列，跨页面传播知识以避免冗余探索，并递归扩展多级图形用户界面（GUI）元素，这与人类学习新网站的方式非常相似。其次，AutoSurfer 利用探索轨迹来指导任务合成，通过将复杂任务锚定在实际导航路径而非孤立的动作或页面内容中，减少了幻觉。第三，AutoSurfer 使用相同的探索轨迹作为提示，引导 Web 智能体进行更准确、更可靠的轨迹细化。这些创新共同使 AutoSurfer 能够全面覆盖网站的动作空间，并生成适合训练特定网站 LLM 的数据。我们在 WebArena 基准测试上通过微调 Qwen2.5-VL-7B-Instruct 对 AutoSurfer 进行了评估，结果表明其优于最先进的方法（Explorer、OS-Genesis 和 SynthAgent），实现了高达 24.23% 的总任务完成准确率，而此前最佳方法为 19.59%。此外，任务多样性分析表明，AutoSurfer 产生了更多样化的合成任务分布。

## Abstract
Recent advances in multimodal large language models (LLMs) have revolutionized web agents that can automate complex tasks on websites. However, their accuracy remains limited by the scarcity of high-quality web trajectory training data. Existing automatic trajectory generation methods suffer from incomplete website coverage due to homepage-based task proposals or random-walk exploration. Such methods often result in hallucinated or ambiguous task synthesis that lead to incomplete and unreliable trajectory generation. Here, we present AutoSurfer, a comprehensive web trajectory generator that addresses these limitations through three key innovations. First, AutoSurfer employs a systematic breadth-first exploration strategy that maintains a queue of discovered pages and action traces, propagates knowledge across pages to avoid redundant exploration, and recursively expands multi-level graphical user interface elements - closely resembling how a human would learn a new website. Second, AutoSurfer leverages the exploration trajectory to guide task synthesis, reducing hallucinations by grounding complex tasks in actual navigation paths rather than isolated actions or page content alone. Third, AutoSurfer uses the same exploration trajectory as hints to steer a web agent toward more accurate and reliable trajectory refinement. Together, these innovations enable AutoSurfer to comprehensively cover a website's action space and generate data suitable for training website-specific LLMs. We evaluate AutoSurfer on the WebArena benchmark by fine-tuning Qwen2.5-VL-7B-Instruct and demonstrate that it outperforms state-of-the-art methods - Explorer, OS-Genesis, and SynthAgent - achieving up to 24.23% overall task completion accuracy compared to 19.59% for the best prior method. Further, task diversity analysis demonstrates that AutoSurfer yields a more diverse distribution of synthesized tasks.

---

## 论文详细总结（自动生成）

以下是对论文《AutoSurfer -- Teaching Web Agents through Comprehensive Surfing, Learning, and Modeling》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管多模态大语言模型（LLM）推动了 Web 智能体的发展，但其准确性受限于高质量训练数据的匮乏。
*   **现有挑战**：目前的自动轨迹生成方法（如 Explorer, OS-Genesis）存在网站覆盖不全（仅基于主页或随机游走）、任务合成存在幻觉（凭空捏造任务）以及生成的交互轨迹不可靠等问题。
*   **核心目标**：提出一种名为 **AutoSurfer** 的综合生成框架，通过系统性地探索网站、合成基于路径的任务并精炼轨迹，为训练“特定网站大语言模型”（wLLM）提供高质量数据。

### 2. 方法论
AutoSurfer 的工作流程分为四个阶段：
*   **系统性广度优先探索（BFS）**：
    *   维护一个已发现页面和动作追踪的队列，模拟人类学习新网站的过程。
    *   **递归扩展**：专门处理菜单和下拉列表等隐藏元素，递归挖掘多级 GUI 结构。
    *   **区分处理**：将 GUI 元素分为“固定”（模板化）和“动态”（内容相关），对固定元素全面覆盖，对动态元素进行选择性采样，以减少冗余。
*   **基于探索轨迹的任务合成**：
    *   不同于以往仅根据页面内容合成任务，AutoSurfer 将任务锚定在真实的导航路径中。这种“路径接地”的方法显著减少了任务描述中的幻觉，确保任务与实际功能一致。
*   **引导式轨迹精炼**：
    *   利用探索阶段记录的原始轨迹作为“提示”（Hints），引导 Web 智能体重新执行任务。这有助于纠正探索过程中的冗余动作，生成更简洁、准确的训练轨迹。
*   **模型微调**：
    *   将精炼后的任务元组转换为 ShareGPT 格式的监督微调（SFT）数据集，用于训练特定网站的 wLLM。

### 3. 实验设计
*   **数据集/场景**：使用 **WebArena** 基准测试，涵盖四个真实网站环境：CMS（内容管理）、Gitlab（代码托管）、Reddit（论坛）和 Shopping（电商）。
*   **Benchmark**：评估指标为任务完成准确率（Success Rate）。
*   **对比方法**：
    *   **基准模型**：GPT-4.1（作为上限参考）、Qwen2.5-VL-7B 原始模型。
    *   **SOTA 生成器**：Explorer、OS-Genesis、SynthAgent。
*   **评估模型**：主要使用 Qwen2.5-VL-7B-Instruct，并在 Qwen3-VL-8B 上进行了验证。

### 4. 资源与算力
*   **数据生成**：使用 **GPT-4.1** 作为教师模型进行探索、任务合成和轨迹精炼。
*   **微调细节**：论文提到使用了与 SynthAgent 相同的超参数设置进行微调。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或训练总时长，但考虑到微调的是 7B/8B 规模的模型，通常需要多张 A100 或 H100 级别的显卡。

### 5. 实验数量与充分性
*   **实验规模**：在四个不同领域的网站上进行了全面测试，评估了 195 个任务模板。
*   **实验维度**：
    *   **主实验**：对比不同数据生成方法对模型性能的提升。
    *   **跨模型验证**：在 Qwen3 上验证方法的通用性。
    *   **多样性分析**：通过文本嵌入计算任务分布的余弦相似度，证明 AutoSurfer 生成的任务更多样。
    *   **消融实验**：对比了“原始探索数据”与“精炼后数据”的训练效果。
*   **评价**：实验设计较为客观、公平，通过与多种 SOTA 方法在同一基座模型上对比，充分证明了其数据生成的质量优势。

### 6. 主要结论与发现
*   **性能领先**：AutoSurfer 在 WebArena 上的总准确率达到 **24.23%**，显著超过了此前最佳方法 SynthAgent（19.59%）。
*   **wLLM 的有效性**：仅使用单个网站的数据训练出的 wLLM，其表现与使用全量数据训练的模型相当，证明了 AutoSurfer 具备建模完整网站能力的能力。
*   **精炼的重要性**：在 Reddit 上的实验显示，精炼后的数据将模型准确率从 11.54% 提升至 34.62%，证明了去除冗余动作和纠正轨迹的必要性。
*   **任务多样性**：AutoSurfer 实现了最高的任务多样性得分（0.7774），优于 OS-Genesis 等方法。

### 7. 优点
*   **探索策略科学**：BFS 结合多级菜单递归扩展，解决了现有方法“浅尝辄止”的问题，极大提升了功能覆盖率。
*   **减少幻觉**：通过探索轨迹引导任务合成，使生成的任务描述更具真实感和可执行性。
*   **拟人化**：其探索和学习机制非常接近人类熟悉新网站的逻辑。

### 8. 不足与局限
*   **依赖强教师模型**：数据生成高度依赖 GPT-4.1 等昂贵的闭源模型，成本较高。
*   **绝对准确率仍有提升空间**：虽然超越了 SOTA，但 24% 左右的准确率距离人类水平或实际大规模商用仍有较大差距（受限于 7B 规模基座模型的能力）。
*   **场景覆盖限制**：实验排除了 Wikipedia（涉及跨站导航）和 Maps（环境问题），尚未充分验证其在极其复杂的跨站任务中的表现。
*   **静态 SFT 局限**：目前仅限于监督微调，未来可能需要结合强化学习（RL）来处理更长程的交互。

（完）
