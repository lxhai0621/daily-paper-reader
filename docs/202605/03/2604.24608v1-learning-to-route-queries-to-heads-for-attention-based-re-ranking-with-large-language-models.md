---
title: Learning to Route Queries to Heads for Attention-based Re-ranking with Large Language Models
title_zh: 学习将查询路由至注意力头以实现基于大语言模型的注意力重排序
authors: "Yuxing Tian, Fengran Mo, Zhiqi Huang, Weixu Zhang, Jian-Yun Nie"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24608v1"
tags: ["query:ma-kf"]
score: 7.5
evidence: 基于注意力的重排序以评估文档相关性
tldr: 本研究针对大语言模型（LLM）在重排序任务中注意力头利用效率低的问题，提出了RouteHead方法。传统方法通常静态选择或聚合所有注意力头，忽略了不同查询对特定头的依赖。RouteHead通过学习一个轻量级路由器，根据查询特征动态选择最优注意力头子集。通过离线搜索生成伪标签并结合稀疏正则化进行训练，该方法在多个基准测试中显著提升了重排序性能，证明了动态路由在提取LLM排序信号方面的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.24608v1/fig-001.webp\", \"caption\": \"Table 1: Performance comparison (nDCG@10) on BEIR. † represent initial retrieval. Best results are highlighted in Bold.\", \"page\": 3, \"index\": 1, \"width\": 944, \"height\": 552}, {\"url\": \"assets/figures/arxiv/2604.24608v1/fig-002.webp\", \"caption\": \"Table 2: Performance comparison (nDCG@10) on BRIGHT . † represent initial retrieval. Best results are highlighted in Bold.\", \"page\": 4, \"index\": 2, \"width\": 840, \"height\": 500}]"
motivation: 现有的LLM重排序方法采用固定的注意力头聚合策略，无法适应不同查询的差异，且容易受到冗余或冲突信号的影响。
method: 提出RouteHead框架，利用冻结LLM的隐藏状态表示查询，并通过离线搜索获取的伪标签训练路由器，实现查询相关的注意力头动态选择。
result: 实验结果显示，RouteHead在多种LLM骨干网络和多样化基准测试上均一致优于现有的强基准方法。
conclusion: 证明了针对特定查询动态路由注意力头可以更精准地捕捉文档相关性，是提升LLM零样本重排序能力的有效途径。
---

## 摘要
大语言模型（LLMs）最近被探索作为细粒度的零样本重排序器，通过利用注意力信号来估计文档相关性。然而，现有方法要么聚合所有注意力头的信号，要么依赖于通过启发式规则确定的静态选择子集。这种方案可能是次优的，因为提供信息的注意力头可能随查询或领域的不同而变化。此外，由于冗余或冲突的排序信号，简单地组合多个注意力头可能会降低性能。在本文中，我们提出了一种针对 LLM 注意力重排序的查询依赖型注意力头选择方法，称为 RouteHead。具体而言，我们学习了一个轻量级路由器，它可以将每个查询映射到一个最优的注意力头集合，并仅通过聚合这些头的注意力信号来计算相关性得分。由于缺乏查询到注意力头的最优标签，我们首先通过离线搜索构建伪标签。路由器使用可学习的嵌入表示每个注意力头，并使用从冻结的 LLM 隐藏状态中提取的嵌入表示每个查询。然后，它在带有稀疏正则化项的伪标签上进行训练。在不同基准测试和多个 LLM 主干网络上的实验表明，所提方法始终优于强基线方法。

## Abstract
Large Language Models (LLMs) have recently been explored as fine-grained zero-shot re-rankers by leveraging attention signals to estimate document relevance. However, existing methods either aggregate attention signals across all heads or rely on a statically selected subset identified by heuristic rules. This solution can be suboptimal because the informative heads can vary across queries or domains. Moreover, naively combining multiple heads can degrade performance due to redundancy or conflicting ranking signals. In this paper, we propose a query-dependent head selection method, RouteHead, for attention-based re-ranking with LLMs. Specifically, we learn a lightweight router that can map each query to an optimal head set, and relevance scores are computed by aggregating attention signals only from these heads. Since query-to-head optimal labels are unavailable, we first construct pseudo labels via an offline search. The router represents each head with a learnable embedding and represents each query using an embedding extracted from the hidden states of the frozen LLM. Then it is trained on the pseudo labels with a sparsity regularizer. Experiments on diverse benchmarks and multiple LLM backbones show that the proposed method consistently outperforms strong baselines.