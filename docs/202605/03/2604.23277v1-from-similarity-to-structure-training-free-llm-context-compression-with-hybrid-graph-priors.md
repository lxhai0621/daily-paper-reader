---
title: "From Similarity to Structure: Training-free LLM Context Compression with Hybrid Graph Priors"
title_zh: 从相似性到结构：基于混合图先验的免训练大语言模型上下文压缩
authors: "Yitian Zhou, Chaoning Zhang, Jiaquan Zhang, Zhenzhen Huang, Jinyu Guo, Sung-Ho Bae, Lik-Hang Lee, Caiyan Qin, Yang Yang"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23277v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 利用混合图先验进行LLM上下文压缩以处理长输入
tldr: 针对长上下文大模型计算开销大且处理超长输入不稳定的问题，本文提出一种无需训练且模型无关的上下文压缩框架。该方法利用混合图先验（结合语义k-NN与序列边）构建句子图，通过聚类提取主题骨架，并结合任务相关性、代表性及中心度等指标进行句子排序与去重选择。实验证明该方法在长文档基准测试中优于多种强基线，有效平衡了压缩后的相关性、覆盖面与连贯性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.23277v1/fig-001.webp\", \"caption\": \"Table 1: Performance comparison between our proposed method and representative baselines on CNN/DailyMail, GovReport, arXiv, and PubMed. Best scores are in bold.\", \"page\": 7, \"index\": 1, \"width\": 975, \"height\": 290}, {\"url\": \"assets/figures/arxiv/2604.23277v1/fig-002.webp\", \"caption\": \"Table 2: Ablation results of different components of our framework on four benchmarks.\", \"page\": 8, \"index\": 2, \"width\": 975, \"height\": 184}, {\"url\": \"assets/figures/arxiv/2604.23277v1/fig-003.webp\", \"caption\": \"Figure 1: Overview of the proposed structure-aware context compression framework.\", \"page\": 4, \"index\": 3, \"width\": 976, \"height\": 517}, {\"url\": \"assets/figures/arxiv/2604.23277v1/fig-004.webp\", \"caption\": \"Table B.2: Joint scan of scoring weights on GovReport (ROUGE-L).\", \"page\": 17, \"index\": 4, \"width\": 525, \"height\": 409}, {\"url\": \"assets/figures/arxiv/2604.23277v1/fig-005.webp\", \"caption\": \"Figure 2: Performance over token budgets on four datasets. We report the most discriminative metric per dataset (QAFactEval for CNN/DailyMail and GovReport, ROUGE-L for arXiv and PubMed).\", \"page\": 9, \"index\": 5, \"width\": 979, \"height\": 654}]"
motivation: 现有的上下文压缩方法在严格Token预算下，难以同时兼顾任务相关性、主题覆盖范围和跨句连贯性。
method: 构建结合语义相似度与序列关系的混合句子图，利用聚类和图结构指标对句子评分，并通过带预算的贪婪算法进行去重选择。
result: 在四个数据集上的实验结果显示，该方法在长文档任务中表现优异，性能可与强力的抽取式和生成式基线方法相媲美。
conclusion: 引入结构化图先验可以在无需额外训练的情况下，实现高效且具备可解释性的长文本上下文压缩。
---

## 摘要
长上下文大语言模型（LLM）的运行计算成本依然高昂，且往往无法可靠地处理极长输入，这使得上下文压缩成为许多系统的重要组成部分。现有的压缩方法通常依赖于经过训练的压缩器、稠密检索式选择或启发式裁剪，且在严格的 Token 预算下，往往难以同时兼顾任务相关性、主题覆盖范围和跨句连贯性。针对这一问题，我们提出了一种免训练且与模型无关的压缩框架，该框架在结构化图先验的指导下选择紧凑的句子集。我们的方法构建了一个稀疏混合句子图，结合了互 k-NN 语义边与短程序列边，通过聚类提取主题骨架，并使用一种整合了任务相关性、聚类代表性、桥接中心性和环覆盖线索的可解释评分对句子进行排序。随后，采用带有冗余抑制的有预算贪婪选择，按原始顺序生成可读的压缩上下文。在四个数据集上的实验结果表明，我们的方法与强大的抽取式和生成式基准模型相比具有竞争力，在长文档基准测试中表现出更显著的提升。

## Abstract
Long-context large language models remain computationally expensive to run and often fail to reliably process very long inputs, which makes context compression an important component of many systems. Existing compression approaches typically rely on trained compressors, dense retrieval-style selection, or heuristic trimming, and they often struggle to jointly preserve task relevance, topic coverage, and cross-sentence coherence under a strict token budget. To address this, we propose a training-free and model-agnostic compression framework that selects a compact set of sentences guided by structural graph priors. Our method constructs a sparse hybrid sentence graph that combines mutual k-NN semantic edges with short-range sequential edges, extracts a topic skeleton via clustering, and ranks sentences using an interpretable score that integrates task relevance, cluster representativeness, bridge centrality, and a cycle coverage cue. A budgeted greedy selection with redundancy suppression then produces a readable compressed context in original order. Experimental results on four datasets show that our approach is competitive with strong extractive and abstractive baselines, demonstrating larger gains on long-document benchmarks.

---

## 论文详细总结（自动生成）

以下是对论文《From Similarity to Structure: Training-free LLM Context Compression with Hybrid Graph Priors》的结构化深入总结：

### 1. 论文的核心问题与整体含义
随着大语言模型（LLM）支持的上下文窗口不断扩大，处理长文本的计算成本（Quadratic Complexity）和性能退化（如“迷失在中间”现象）成为瓶颈。现有的上下文压缩技术主要分为三类：训练专用压缩器（成本高）、稠密检索（忽略结构）和启发式裁剪（易丢失关键信息）。
**核心问题**：如何在**无需训练（Training-free）**且**模型无关（Model-agnostic）**的前提下，在严格的 Token 预算内，同时保留任务相关性、主题覆盖范围和跨句连贯性。

### 2. 论文提出的方法论
该论文提出了一种基于**混合图先验（Hybrid Graph Priors）**的结构感知压缩框架，核心流程如下：
*   **句子嵌入与图构建**：将文档拆分为句子并提取向量。构建一个**混合句子图**，包含两种边：
    *   **语义边**：通过互 k-最近邻（Mutual k-NN）建立，捕捉语义相似性。
    *   **序列边**：连接物理位置相邻的句子，补偿语义向量无法捕捉的叙事流和连贯性。
*   **主题骨架提取**：利用轻量级的 MiniBatch K-Means 对句子进行聚类，确定文档的主题分布。
*   **结构化评分机制**：为每个句子计算综合得分，包含四个维度：
    1.  **任务相关性**：句子与查询（或文档中心）的余弦相似度。
    2.  **主题代表性**：句子在其所属聚类中的中心程度（保证覆盖面）。
    3.  **桥接中心度（Bridge Centrality）**：利用介数中心性（Betweenness Centrality）识别连接不同主题或章节的关键转折句。
    4.  **环覆盖线索（Cycle Coverage）**：识别图中形成的局部闭环（如论点-证据对），保护局部推理逻辑。
*   **预算化选择与去重**：采用贪婪算法按得分排序，并引入**非极大值抑制（NMS）**过滤语义冗余，最终按原始顺序输出句子以保持可读性。

### 3. 实验设计
*   **数据集**：涵盖了短/中/长不同长度的四个主流基准：
    *   **CNN/DailyMail**（新闻摘要，中短篇）。
    *   **GovReport**（政府报告，长篇）。
    *   **arXiv & PubMed**（科学论文，极长篇且结构化强）。
*   **对比方法（Baselines）**：
    *   **无监督/启发式**：TextRank, LEAD-3, RankSum。
    *   **监督抽取式**：BERTSum, MatchSum, MemSum。
    *   **生成式/长文本模型**：BART, PEGASUS, BIGBIRD, DANCER。
    *   **LLM 提示/压缩专用**：HiGen, SigExt。
*   **评估指标**：ROUGE-1/2/L（字面重合）、BERTScore（语义相似度）、QAFactEval（事实一致性）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总计算时长。
*   **实现细节**：由于该方法是**免训练**的，其主要计算开销在于：
    1.  调用 OpenAI 的 `text-embedding-large` 模型生成嵌入。
    2.  进行图算法计算（如 HNSW 搜索、聚类、介数中心性采样）。
*   **效率提示**：文中提到使用采样技术（Sampled Betweenness）和 MiniBatch 聚类来优化长文档的处理速度。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同领域的数据集上进行了主实验，并针对 5 个核心组件（序列边、代表性、桥接得分、环得分、NMS）进行了详尽的**消融实验**。
*   **深度分析**：进行了超参数敏感性分析（如 k 值、NMS 阈值、序列窗口大小）以及复杂的权重扫描（Weight Scan）。
*   **充分性评价**：实验设计非常**充分且客观**。通过对比不同 Token 预算下的性能曲线（Quality-Budget Curves），证明了该方法在极低预算下依然具有高效性。

### 6. 主要结论与发现
*   **长文档优势显著**：在 GovReport 和 arXiv 等长文档任务上，该方法全面超越了现有的强基线，证明结构化先验在处理复杂长文本时比单纯的语义匹配更有效。
*   **结构先验的价值**：消融实验显示，**序列边**对保持连贯性贡献最大，而**桥接中心度**能有效防止论证链条的断裂。
*   **预算效率**：该方法在 Token 预算受限的情况下，性能下降比基线更缓慢，表现出更高的信息密度。

### 7. 优点
*   **即插即用**：无需针对特定任务或模型进行微调，部署成本极低。
*   **可解释性**：评分标准（如“桥接句”、“主题中心”）具有明确的语言学和逻辑含义，便于审计压缩结果。
*   **结构感知**：通过图论方法弥补了传统向量检索（RAG 常用手段）丢失文档全局结构的缺陷。

### 8. 不足与局限
*   **依赖嵌入模型**：压缩质量高度依赖于底层 Embedding 模型的表达能力。
*   **计算复杂度**：虽然免训练，但对于超大规模文档（如数百万字），构建图和计算介数中心性的开销可能会显著增加。
*   **环枚举的局限**：目前的环覆盖（Cycle Coverage）采用的是启发式枚举，对于极其复杂的逻辑结构可能捕捉不全。
*   **应用限制**：主要针对句子级抽取式压缩，对于需要极高压缩率（如 Token 级裁剪）的场景，其粒度可能不够细。

（完）
