---
title: Structure-Aware Chunking for Tabular Data in Retrieval-Augmented Generation
authors: "Pooja Guttal, Varun Magotra, Vasudeva Mahavishnu, Natasha Chanto, Sidharth Sivaprasad, Manas Gaur"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00318v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 针对RAG的结构感知表格分块技术
tldr: "针对检索增强生成（RAG）中表格数据处理的局限性，本文提出了一种结构感知表格分块（STC）框架。该框架通过构建层次化行树将每行编码为键值对块，并采用受令牌限制的结构对齐拆分与无重叠贪婪合并策略。STC在保留行内语义关系的同时，显著提高了令牌利用率并减少了分块碎片化。实验表明，STC在MAUD数据集上大幅提升了检索指标（如MRR和Recall@1），证明了结构感知在表格RAG中的重要性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.00318v1/fig-001.webp\", \"caption\": \"Fig. 1: Comparison of baseline recursive chunking versus the proposed structure-aware framework. The baseline method (top) operates on linearized text with token-level splitting and overlap, which can fragment rows across multiple chunks and introduce redundant tokens. In contrast, the proposed framework (bottom) represents each row as a key-value (KV) block within a Row Tree hierarchy. By applying token-constrained splitting and greedy merging, the method produces dense, non-overlapping chunks aligned with row-level structure.\", \"page\": 2, \"index\": 1, \"width\": 1074, \"height\": 416}]"
motivation: 现有的RAG分块策略主要针对非结构化文本，忽视了表格数据的结构特征，导致检索效率和准确性受限。
method: 提出STC框架，利用行树表示法将行转化为键值对块，并通过结构对齐的拆分与贪婪合并生成紧凑的分块。
result: "在MAUD数据集上，STC将分块数量减少了多达56%，并将混合检索的MRR从0.3576提升至0.5945。"
conclusion: 保持表格结构完整性的分块方法能显著增强RAG系统的检索性能，是处理表格数据的关键。
---

## Abstract
Tabular documents such as CSV and Excel files are widely used in enterprise data pipelines, yet existing chunking strategies for retrieval-augmented generation (RAG) are primarily designed for unstructured text and do not account for tabular structure. We propose a structure-aware tabular chunking (STC) framework that operates on row-level units by constructing a hierarchical Row Tree representation, where each row is encoded as a key-value block. STC performs token-constrained splitting aligned with structural boundaries and applies overlap-free greedy merging to produce dense, non-overlapping chunks. This design preserves semantic relationships between fields within a row while improving token utilization and reducing fragmentation. Across evaluations on the MAUD dataset, STC reduces chunk count by up to 40% and 56% compared to standard recursive and key-value based baselines, respectively, while improving token utilization and processing efficiency. In retrieval benchmarks, STC improves MRR from 0.3576 to 0.5945 in a hybrid setting and increases Recall@1 from 0.366 to 0.754 in BM25-only retrieval. These results demonstrate that preserving structure during chunking improves retrieval performance, highlighting the importance of structure-aware chunking for RAG over tabular data.

---

## 论文详细总结（自动生成）

这是一份关于论文《Structure-Aware Chunking for Tabular Data in Retrieval-Augmented Generation》的结构化深入总结：

### 1. 研究动机与核心问题
*   **研究背景**：表格数据（如 CSV、Excel）在企业数据流中广泛存在。然而，现有的检索增强生成（RAG）分块策略主要针对非结构化文本（如固定大小、滑动窗口），假设文本是线性且语义连续的。
*   **核心问题**：传统的线性化分块方法在处理表格时，往往会截断上下文、丢失行与列之间的关联，导致分块语义不连贯，进而严重降低检索准确性和生成质量。
*   **研究目标**：提出一种能够感知表格结构的分块框架，以保留行级单元的完整性并提高检索效率。

### 2. 方法论：结构感知表格分块 (STC)
该框架的核心思想是将表格视为层次化结构而非纯文本流，主要包含以下三个阶段：
*   **行树表示 (Row Tree Representation)**：将输入表格转换为层次化树状结构。根节点下是行节点（如果是多工作表 Excel，则中间有工作表节点）。每一行被编码为结构化的**键值对 (KV) 块**（例如 `列名: 单元格值`），从而保留行内字段间的语义关系。
*   **递归与紧急拆分 (Recursive & Emergency Splitting)**：
    *   **递归拆分**：自顶向下遍历行树。如果某个节点（如整个表）超过令牌（Token）预算，则拆分为子节点（行）。
    *   **紧急拆分**：如果单行数据仍超过预算，则在 KV 边界处进行拆分，确保每个片段包含完整的字段而非随机截断的文本。
*   **贪婪合并 (Greedy Merging)**：在相同的父节点（如同一张表）内，将相邻的行级单元按顺序合并，直到达到令牌预算上限。该过程**不设置重叠 (Overlap-free)**，从而生成高密度、无冗余的分块。

### 3. 实验设计
*   **数据集**：使用 **MAUD (Merger Agreement Understanding Dataset)**。这是一个法律阅读理解基准，包含从 SEC EDGAR 系统提取的并购合同，具有高度的半结构化特征（合同条款、问题、答案及相关属性）。
*   **对比方法 (Baselines)**：
    1.  **Recursive**：标准的递归字符拆分（如 LangChain 的 `RecursiveCharacterTextSplitter`），带有 100 令牌的滑动窗口重叠。
    2.  **KV + Recursive (消融实验)**：先将数据转为 KV 格式，再使用标准的递归拆分逻辑。
*   **评估指标**：
    *   **分块效率**：分块总数、平均/最小/最大令牌数、令牌利用率、处理速度。
    *   **检索性能**：Recall@1/3/5、平均倒数排名 (MRR)。分为“混合检索（向量+BM25+重排序）”和“仅 BM25 检索”两种场景。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的硬件配置（如 GPU 型号、数量）或训练/处理的总时长。
*   **模型使用**：实验中提到了使用 `multi-qa-MiniLM-L6-cos-v1` 作为双编码器进行向量检索，以及 `ms-marco-MiniLM-L6-v2` 作为交叉编码器进行重排序。

### 5. 实验数量与充分性
*   **实验规模**：在 MAUD 数据集的训练集（25,827 行）、验证集（6,753 行）和测试集（6,651 行）上分别进行了分块统计实验。检索实验则基于从训练集中随机抽取的 1,000 条记录进行。
*   **充分性评价**：实验设计较为充分。通过消融实验（KV + Recursive）证明了仅改变数据格式是不够的，关键在于“结构感知”的拆分与合并逻辑。同时，涵盖了效率指标和实际检索效果指标，评估维度全面。

### 6. 主要结论与发现
*   **效率提升**：相比标准递归方法，STC 将分块数量减少了约 **40%**；相比 KV+Recursive 减少了 **56%**。同时，STC 的令牌利用率更高（平均约 400 令牌/块，预算为 512），处理速度显著加快。
*   **检索增强**：
    *   在混合检索设置下，MRR 从 0.3576 提升至 **0.5945**。
    *   在仅 BM25 检索设置下，Recall@1 从 0.366 大幅提升至 **0.754**。
*   **核心发现**：在 KV 边界处进行拆分并消除重叠，可以显著增强分块的词法一致性，对稀疏检索（BM25）尤为有利。

### 7. 优点
*   **结构保真度高**：通过行树和 KV 块设计，最大限度地减少了行内信息的碎片化。
*   **资源利用率优**：无重叠的贪婪合并策略消除了冗余令牌，降低了索引存储成本和推理时的计算开销。
*   **通用性**：虽然在法律数据集上测试，但该方法逻辑适用于任何行级结构化数据（日志、数据库导出等）。

### 8. 不足与局限
*   **数据集单一**：主要在 MAUD 法律数据集上进行评估，尚未在更多样化的表格类型（如包含复杂嵌套表或超大规模稀疏表）上验证。
*   **评估局限**：检索评估采用了启发式的字符串匹配来确定相关性，而非完全的人工标注，可能存在一定的评估偏差。
*   **RAG 完整链路缺失**：论文重点在于分块和检索阶段，未深入探讨该方法对最终 LLM 生成质量（如幻觉率、回答准确度）的具体影响。

（完）
