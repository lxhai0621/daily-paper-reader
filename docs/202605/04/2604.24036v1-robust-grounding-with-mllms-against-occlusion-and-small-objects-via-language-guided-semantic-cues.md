---
title: Robust Grounding with MLLMs against Occlusion and Small Objects via Language-guided Semantic Cues
title_zh: 通过语言引导的语义线索增强多模态大语言模型在遮挡和小目标场景下的鲁棒定位能力
authors: "Beomchan Park, Seongho Kim, Hyunjun Kim, Sungjune Park, Yong Man Ro"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24036v1"
tags: ["query:cmv"]
score: 9.0
evidence: 多模态大模型中用于视觉定位的语言引导语义线索
tldr: 针对多模态大模型在拥挤场景中因遮挡和目标微小导致定位性能下降的问题，本文提出了一种利用语言引导语义线索（LGSC）的新方法。该方法通过语义线索提取器（SCE）从视觉管道中提取特征，并结合文本嵌入生成语义先验，重新注入视觉流程以增强目标语义。实验证明，该方法显著提升了模型在复杂拥挤场景下的定位准确性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.24036v1/fig-001.webp\", \"caption\": \"Fig. 2. Overall architecture of our approach (left) and detailed illustration of training Language-Guided Semantic Cues (right).\", \"page\": 3, \"index\": 1, \"width\": 1020, \"height\": 286}]"
motivation: 现有的多模态大模型在处理拥挤场景时，容易受到遮挡和微小目标的干扰，导致视觉语义受损。
method: 提出语义线索提取器（SCE）提取视觉特征，并利用文本嵌入引导生成语言引导语义线索（LGSC）作为语义先验。
result: 实验结果表明，将LGSC集成到MLLM中能有效提高模型在拥挤场景下的目标定位精度。
conclusion: 通过引入语言引导的语义先验，可以有效弥补视觉信息的缺失，增强模型在复杂环境下的鲁棒性。
---

## 摘要
虽然多模态大语言模型（MLLMs）在通用场景中增强了定位能力，但其在拥挤场景中的鲁棒性仍有待探索。拥挤场景带来了视觉挑战（即遮挡和小目标），这会损害物体语义并降低定位性能。相比之下，语言表达不受此类退化的影响，并保留了物体语义。基于这些观察，我们提出了一种新颖的方法，通过利用语言引导的语义线索（LGSCs）来克服这些限制。具体而言，我们的方法引入了一个语义线索提取器（SCE），从 MLLM 的视觉流水线中提取物体的语义线索。然后，我们使用相应的文本嵌入来引导这些线索，生成作为语言语义先验的 LGSCs。随后，它们被重新整合到原始视觉流水线中，以细化物体语义。大量的实验和分析表明，将 LGSCs 整合到 MLLM 中能有效提高拥挤场景下的定位准确性。

## Abstract
While Multimodal Large Language Models (MLLMs) have enhanced grounding capabilities in general scenes, their robustness in crowded scenes remains underexplored. Crowded scenes entail visual challenges (i.e., occlusion and small objects), which impair object semantics and degrade grounding performance. In contrast, language expressions are immune to such degradation and preserve object semantics. In light of these observations, we propose a novel method that overcomes such constraints by leveraging Language-Guided Semantic Cues (LGSCs). Specifically, our approach introduces a Semantic Cue Extractor (SCE) to derive semantic cues of objects from the visual pipeline of an MLLM. We then guide these cues using corresponding text embeddings to produce LGSCs as linguistic semantic priors. Subsequently, they are reintegrated into the original visual pipeline to refine object semantics. Extensive experiments and analyses demonstrate that incorporating LGSCs into an MLLM effectively improves grounding accuracy in crowded scenes.

---

## 论文详细总结（自动生成）

以下是对论文《Robust Grounding with MLLMs against Occlusion and Small Objects via Language-guided Semantic Cues》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：多模态大语言模型（MLLMs）在通用场景下表现优异，但在**拥挤场景**（如交通监控、公共安全）中性能显著下降。
*   **研究动机**：拥挤场景存在严重的**遮挡（Occlusion）**和**小目标（Small Objects）**问题，导致视觉特征受损，语义模糊。
*   **核心观察**：视觉信息易受环境干扰，但对应的**语言表达**（如类别标签、描述）具有天然的抗干扰性，能保留完整的物体语义。受神经科学启发（语言标签能提供自上而下的视觉意识增强），作者提出利用语言作为先验来纠正受损的视觉语义。

### 2. 方法论
*   **核心思想**：在训练阶段利用文本标签引导视觉特征的学习，生成“语言引导语义线索”（LGSCs），并在推理阶段将其作为学到的先验知识，通过交叉注意力机制修复退化的视觉特征。
*   **关键技术细节**：
    *   **语义线索提取器 (SCE)**：一个4层MLP，将物体的视觉特征映射为语义线索空间。
    *   **语言引导机制**：使用预训练的文本编码器（CLIP-ConvNeXt）提取Ground-truth标签的文本嵌入，通过最大化语义线索与文本嵌入之间的余弦相似度（$L_{SCE}$ 损失）来训练SCE。
    *   **语义线索投影器 (SCP)**：将生成的LGSCs映射到LLM的嵌入空间。
    *   **语义细化模块**：采用**低秩交叉注意力（Low-Rank Cross-Attention）**机制。以LGSCs作为Query，原始视觉特征作为Key和Value，使模型能有选择地从视觉背景中聚合相关信息，修复因遮挡或尺寸过小而丢失的语义。
*   **算法流程**：输入图像 -> 区域建议（Region Proposer）提取物体 -> SCE提取语义线索 -> SCP投影 -> 交叉注意力细化特征 -> LLM生成定位索引。

### 3. 实验设计
*   **数据集与场景**：
    *   **监督学习场景**：CrowdHuman（拥挤行人）、VisDrone（无人机视角）、UAVDT（交通监控）、RefDrone（无人机指代理解）。
    *   **零样本（Zero-shot）场景**：CityPersons、WiderPerson、HazyDet（雾天场景）。
    *   **区域描述（Region Captioning）**：RefCOCOg、RefDrone。
*   **Benchmark与对比方法**：
    *   对比了 Ferret-7B、InternVL3-8B、Qwen2.5-VL-7B、Groma-7B 以及基准模型 ChatRex-7B。
    *   评价指标包括 AP50（定位精度）、METEOR 和 CIDEr（描述质量）。

### 4. 资源与算力
*   **硬件设备**：使用了 **8 张 NVIDIA A6000 GPU**。
*   **训练配置**：
    *   全局 Batch Size 为 128。
    *   训练 1 个 epoch，Warmup 比例为 0.03。
    *   学习率：SCE/SCP/注意力模块为 1e-4，其余模块为 2e-5。
    *   冻结了区域建议器（Region Proposer）和文本编码器，仅微调其他部分。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从监督学习到零样本迁移，再到生成式任务（区域描述）的多维度测试。
*   **充分性**：
    *   在 7 个不同的拥挤场景数据集上进行了验证，样本涵盖了行人、车辆、无人机航拍、雾天等多种极端情况。
    *   对比了 2025 年最新的 SOTA 模型（如 Qwen2.5-VL），证明了其在特定挑战场景下的优越性。
    *   **客观性**：通过统一类别合并策略（如将车、总线合并为 vehicle）确保了不同模型间对比的公平性。

### 6. 主要结论与发现
*   **性能提升**：在所有拥挤场景基准测试中，该方法均显著优于现有 SOTA 模型。例如在 CrowdHuman 上比基准提升了 5.4% AP50。
*   **泛化能力**：在未见过的零样本数据集（如 HazyDet）上表现出极强的鲁棒性，性能提升甚至超过 20% AP50。
*   **多任务有效性**：该方法不仅提升了定位（Localization），在区域描述（Captioning）任务上也表现出更好的语义对齐能力。

### 7. 优点
*   **理论结合紧密**：借鉴神经科学中“语言促进视觉意识”的原理，为多模态融合提供了生物学解释。
*   **即插即用**：SCE 和 SCP 模块结构简单，能够较容易地集成到其他基于解耦定位范式的 MLLM 中。
*   **解决痛点**：精准打击了 MLLM 在小目标和遮挡场景下的长期弱点。

### 8. 不足与局限
*   **依赖区域建议器**：该方法属于“解耦定位”范式，其性能上限受限于第一阶段 Region Proposer 的召回率。如果 Proposer 未能检测到物体，LGSC 也无法发挥作用。
*   **训练依赖标签**：虽然推理时不需要文本，但训练时需要高质量的类别或描述标签来引导 SCE，在缺乏标注的数据集上难以训练。
*   **计算开销**：虽然使用了低秩注意力，但相比原始模型，在视觉流水线中增加了额外的分支和计算步骤。

（完）
