---
title: Section-Weighted Hybrid Approach for Legal Case Retrieval
title_zh: 基于部分加权的混合方法用于法律案例检索
authors: "Rajith Arulanandam, Nisansa de Silva"
date: 2026-06-02
pdf: "https://arxiv.org/pdf/2606.03138v1"
tags: ["query:ma-kf"]
score: 8.0
evidence: 利用LLM分段的章节感知混合检索方法，融合BM25和稠密检索并学习权重提高准确性
tldr: 法律案例检索中，仅靠词汇重叠难以捕捉法律推理。本文提出两阶段分段感知框架：先离线用大模型将判决书分割为事实、问题、判决和推理；阶段1用BM25和稠密ANN并行搜索并RRF融合形成候选；阶段2进行细粒度逐段比较，用Z-score归一化解决分数尺度问题，并学习各段权重。在司法基准上，该方法一致优于词法和神经基线，并保持高候选覆盖率。贡献在于引入结构化分段对比，提升检索的可解释性和准确性。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.03138v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1868, \"height\": 759, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03138v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1872, \"height\": 709, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03138v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1504, \"height\": 324, \"label\": \"Table\"}]"
motivation: 法律案例检索需要超越词汇重叠，捕捉法律推理，现有方法缺乏对判决书结构的利用。
method: 先离线用LLM分割判决书，再两阶段检索：阶段1多通道候选生成；阶段2逐段比较，Z-score归一化后加权融合。
result: 在司法规模基准上，该方法一致优于BM25、稠密检索等基线，并保持高候选覆盖率。
conclusion: 分段加权混合方法有效提升法律案例检索性能，结构化的逐段比较增强了可解释性和准确度。
---

## 摘要
寻找真正相似的先例需要捕捉超出表面词汇重叠的法律推理。我们提出了一个两阶段、部分感知的法律案例检索框架，该框架首先使用确定性大语言模型离线将原始判决分割为事实、争议、判决和推理部分。在第一阶段，我们通过倒数排名融合结合并行词汇（BM25）和语义（密集ANN）整篇文档搜索，形成高召回率的候选池。在第二阶段，我们进行细粒度的、同类比较（例如，查询推理与候选推理）。为了解决无界词汇分数和余弦相似度之间的尺度不匹配问题，我们在使用学习到的部分权重聚合信号之前，应用查询级别的Z分数归一化。对于顶级结果，系统返回相关部分文本以及简洁、有根据的理由和当事人立场标签。我们在一个司法规模基准上评估，展示了在强词汇和神经基线上的持续改进，同时保持高候选覆盖率。

## Abstract
Finding truly analogous precedents requires capturing legal reasoning beyond surface word overlap. We present a two-stage, section-aware framework for legal case retrieval that first segments raw judgments into facts, issues, decision, and reasoning using a deterministic large language model (LLM) offline. In Stage 1, we combine parallel lexical (BM25) and semantic (dense ANN) whole-document searches via Reciprocal Rank Fusion (RRF) to form a high-recall candidate pool. In Stage 2, we perform fine-grained, like-for-like comparisons (e.g., query reasoning vs. candidate reasoning). To address the scale mismatch between unbounded lexical scores and cosine similarities, we apply query-wise Z-score normalization before aggregating signals with learned section weights. For the top results, the system returns the relevant section text with a concise, grounded rationale and party-stance labels. We evaluate on a jurisdiction-scale benchmark, demonstrating consistent gains over strong lexical and neural baselines while maintaining high candidate coverage

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：法律案例检索需要找到在先例上推理相似的案件，而不仅仅是词汇层面的重叠。传统方法（如BM25）在关键词表述不同时表现不佳，现代单向量模型（如BERT）则容易混淆案件中的不同法律要素（如事实与判决），导致精度-召回率权衡。
- **背景**：现有工作包括领域特定嵌入（如AnglE-BERT、Italian-Legal-BERT）、图信号增强（Hier-SPCNet）、因果学习（Law-Match）、可解释性（NS-LCR）、基于案例的推理（CBR-RAG）、大模型增强检索（KELLER、PLJP）等，但仍缺乏对判决书内部结构（事实、争议、判决、推理）的显式利用。
- **目标**：提出一个透明、可扩展的两阶段分段感知框架，通过离线LLM将判决书分解为结构化段落，再进行细粒度同类比较，提升检索的准确性和可解释性。

## 2. 方法论：核心思想、关键技术细节与公式
- **核心思想**：先离线将判决书分为四个固定段落（事实facts、争议issues、判决decision、推理reasoning），再用两阶段流水线：第一阶段快速生成高召回候选池，第二阶段基于段落进行细粒度重排序。
- **关键步骤**：
  1. **离线预处理**：使用确定性LLM（temperature=0）将每个判决书分割为四个结构化段落，并计算整篇文档和每个段落的嵌入向量，存储备用。
  2. **第一阶段：候选生成**：对查询的整篇文档，分别使用BM25（词汇）和稠密ANN（语义）检索，各自取top-A和top-B，通过**倒数排名融合（RRF）** 合并，取前M个作为候选池。公式：RRF(d) = 1/(k + r_BM25(d)) + 1/(k + r_Dense(d))，k=60，M=1000。
  3. **第二阶段：分段感知重排序**：对于候选池中每个文档，分别计算查询与文档在每个段落上的词汇分（BM25）和语义分（cosine相似度）。使用学习到的段落权重向量 w_lex[i] 和 w_sem[i] 分别加权聚合：S_lex = Z(∑ w_lex[i]·s_lex[i])，S_sem = Z(∑ w_sem[i]·s_sem[i])，其中Z是查询级别的Z-score归一化（均值为0，标准差为1）。最终得分：Score = α·S_lex + β·S_sem，α和β在开发集上调优。
  4. **解释与立场标注**：对top-K结果，提取匹配的相关段落文本，并用LLM生成1-3句理由和当事人立场（支持/中立/反对）。
- **超参数**：α=0.4, β=0.8；词汇权重[0.313, 0.293, 0.167, 0.227]；语义权重[0.326, 0.035, 0.269, 0.371]；检索池大小A=B=500，k=60，M=1000。
- **算法流程**：如原文Algorithm 1所示，分为四步：候选生成→细粒度评分→归一化与排序→解释生成。

## 3. 实验设计
- **数据集**：
  - **小规模（开发集）**：1500个结构化案例，312个查询-文档对。
  - **全规模（验证集）**：7348个结构化案例，1677个查询-文档对。
  - 所有数据来自**COLIEE 2025**语料库（加拿大联邦法院案例法）。
- **基准测试**：COLIEE 2020、2021、2025（加拿大联邦法院数据集）。
- **对比方法**：
  - 传统检索：BM25、QL。
  - 通用预训练模型：BERT、RoBERTa、LEGAL-BERT。
  - 面向检索的预训练模型：Condenser、coCondenser、SEED、CoT-MAE、RetroMAE、SAILER。
  - 自身变体：BM25基线、稠密基线、仅RRF融合（Two-Stage Hybrid）、未优化的分段感知（Uniform权重）、优化后的分段感知。
- **评估指标**：P@1/3/5/10、R@5/10/100、F1@5/10、MRR@10/50/100、AP、Pool Recall（候选池召回率）。

## 4. 资源与算力
- **论文未明确说明**使用的GPU型号、数量、训练时长等算力细节。仅提到“离线LLM分割”和“预计算嵌入并缓存”，暗示计算成本主要在后处理阶段，但未量化。

## 5. 实验数量与充分性
- **实验数量**：在三个COLIEE年份（2020/2021/2025）上评估，全规模表II列出14种基线方法，小规模表III列出5种系统变体，并包含表I和表III的消融实验。
- **消融实验**：验证了三个关键组件的贡献：
  - 分段感知 vs. 整篇文档（P@1下降3.9点）。
  - 权重优化 vs. 均匀权重（P@1下降2.2点）。
  - Z-score归一化 vs. 无归一化（P@1下降约1.6点）。
  - 混合 vs. 单通道（BM25/稠密单独均低于融合结果）。
- **充分性评价**：实验设计较全面，覆盖了多种基线，且消融实验直接量化每个组件的影响。但未提供在更多不同司法体系（如中国、欧盟）上的结果，可能影响泛化性结论。另外，小规模实验仅312个查询，规模略小。整体上，实验公平且具有说服力。

## 6. 主要结论与发现
- 分段感知混合方法在COLIEE 2025加拿大联邦法院数据集上，在MRR、P@5、F1@5等主要指标上**一致优于所有基线**（包括SAILER、BM25、稠密模型等）。
- 在COLIEE 2021上，全规模优化版本在P、R、F1、MRR、R@100上也大幅超过SAILER（例如P@1从0.1040提升至0.1200，R@100从0.5298提升至0.7230）。
- 候选池召回率维持在94%以上，表明第一阶段高效，第二阶段重排序有效提升精度。
- Z-score归一化对混合分数融合至关重要，防止词汇分主导；段落权重学习表明“推理”和“判决”比“事实”和“争议”在语义通道中更重要。

## 7. 优点
- **结构化增强**：首次将判决书明确分割为四个法律相关段落，实现“同类比较”，更贴合法律推理的实际需求。
- **混合与归一化**：BM25+稠密+RRF结合Z-score归一化+学习权重，解决了分数尺度不匹配问题，且权重可解释。
- **可解释性**：输出段落文本、理由和立场标签，满足法律AI对透明度的要求。
- **效率**：通过离线预计算和两阶段设计（先粗后精），在保持高召回的同时降低计算成本。
- **实验严谨**：进行多维度消融，定量证明每个组件的贡献。

## 8. 不足与局限
- **算力未说明**：未报告LLM分割阶段的成本或训练时间，难以评估实际部署的资源需求。
- **数据集局限**：仅使用COLIEE语料库（加拿大联邦法院），未在多个司法体系（如美国、欧盟、中国）上验证，泛化性存疑。
- **段落固定**：强制将判决书分为四个固定段落，可能不适用于所有判决书结构；某些案例可能包含额外段落（如程序性事项）。
- **LLM分割误差**：LLM分割可能引入错误或遗漏，论文未评估分割质量对检索结果的影响。
- **候选池大小**：M=1000可能在某些大规模语料上不足，导致漏掉相关案例；论文仅报告94%召回，未达100%。
- **立场标注**：立场标签（支持/中立/反对）仅用LLM生成，未进行人工验证或与基准对比，可靠性未知。
- **未与最新LLM嵌入方法对比**：如GPT-4/Claude作为检索编码器或端到端推理方法。

（完）
