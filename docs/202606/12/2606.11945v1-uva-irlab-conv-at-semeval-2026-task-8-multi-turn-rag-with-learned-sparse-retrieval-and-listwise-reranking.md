---
title: "uva-irlab-conv at SemEval-2026 Task 8: Multi-Turn RAG with Learned Sparse Retrieval and Listwise Reranking"
title_zh: uva-irlab-conv 在 SemEval-2026 任务 8 上的研究：基于学习型稀疏检索和列表式重排序的多轮检索增强生成
authors: "Simon Lupart, Kidist Amde Mekonnen, Zahra Abbasiantaeb, Mohammad Aliannejadi"
date: 2026-06-10
pdf: "https://arxiv.org/pdf/2606.11945v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 结合学习稀疏检索和LLM重排序的多轮RAG流水线
tldr: SemEval-2026任务8要求多轮对话系统跨四个领域处理可回答和不可回答查询。我们提出多轮RAG流水线，以学习型稀疏检索为主检索方法，利用LLM的长上下文能力进行对话查询重写、点式和列表式重排序及响应生成。该方法在多个领域上有效集成了对话历史，提升了检索和生成的鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.11945v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 799, \"height\": 737, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11945v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 762, \"height\": 617, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11945v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 756, \"height\": 502, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11945v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 759, \"height\": 497, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.11945v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1217, \"height\": 454, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11945v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 811, \"height\": 253, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11945v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1598, \"height\": 544, \"label\": \"Table\"}]"
motivation: 多轮对话中对话历史利用不足和跨领域泛化能力差，需要一种能有效集成上下文并适应多种领域的检索生成方法。
method: 采用学习型稀疏检索初步检索，结合LLM进行查询重写、点式/列表式重排序，并生成最终答案，所有步骤依赖完整对话历史。
result: 在金融、云文档、政府和维基百科四个领域上，该方法有效处理了可回答和不可回答查询，提高了系统鲁棒性。
conclusion: 多步骤设计通过全程依赖对话历史，显著增强了检索和生成对上下文的集成，提升了跨领域泛化性能。
---

## 摘要
本报告描述了我们在 SemEval-2026 任务 8（多轮检索与问答）中的参与情况。该任务在四个领域（金融、云文档、政府、维基百科）中评估对话系统，并包含无法回答的查询，即现有集合中不包含足够证据来生成完整回复。我们提出了一种多轮检索增强生成流水线，将学习型稀疏检索与基于大语言模型的重排序和生成相结合。使用稀疏检索作为主要检索方法，我们利用了其跨领域的强泛化能力。此外，我们利用大语言模型的长上下文能力进行对话查询重写、点式和列表式重排序，以及生成最终回复，每一步都基于完整的对话历史。这种多步设计使得在整个检索和生成过程中能够有效整合对话上下文，提高了跨领域的鲁棒性。

## Abstract
This report describes our participation in SemEval-2026 Task 8 on multi-turn retrieval and question answering. The task evaluates conversational systems across four domains (finance, cloud documentation, government, Wikipedia), and includes unanswerable queries where the available collection does not contain sufficient evidence to produce a complete response. We propose a multi-turn retrieval-augmented generation pipeline that combines learned sparse retrieval with LLM-based reranking and generation. Using sparse retrieval as the primary retrieval method, we leverage its strong generalization across domains. In addition, we make use of the long-context capabilities of LLMs for conversational query rewriting, pointwise and listwise reranking, and generating the final response, each conditioned on the full conversational history. This multi-step design enables effective integration of conversational context throughout retrieval and generation, improving robustness across domains.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机与背景）

- **问题背景**：SemEval-2026 任务 8（MTRAGEval）要求多轮对话系统在四个领域（金融FiQA、云文档IBM-Cloud、政府GovT、维基百科ClapNQ）中完成检索与问答，并特别包含无法回答查询（unanswerable queries）。现有系统在多轮对话中面临上下文演变、话题转移、歧义和历史噪声等挑战，同时需要具备跨领域泛化能力和对不可回答情况的正确拒绝能力。
- **研究动机**：作者旨在设计一个无需领域特定微调、轻量级且能有效集成完整对话历史的 RAG 流水线，以提升检索精度和生成稳定性，尤其强调跨领域鲁棒性和零样本适用性。
- **整体含义**：该研究展示了一个结合学习型稀疏检索与 LLM 重排序的实用方案，验证了级联式多步处理在复杂多轮对话场景中的有效性，并为后续更高级的 RAG 方法提供了基线。

## 2. 方法论

### 核心思想
采用多阶段级联的 RAG 流水线，每阶段逐步缩小候选集并引入更丰富的上下文：  
**查询重写 → 稀疏检索 → 点式重排序 → 列表式重排序 → 生成**

### 关键技术细节

| 步骤 | 模型/方法 | 细节 |
|------|-----------|------|
| **查询重写** | GPT-4.1（零样本） | 将当前用户话语基于完整对话历史改写为独立查询，消除指代和省略。不引入新信息，输出唯一查询。 |
| **第一阶段检索** | LION-SP-8B（学习型稀疏检索） | 基于 LLM 背架的 LSR 模型，类似 SPLADE，支持倒排索引高效搜索。从全集召回 top-1000 候选。 |
| **点式重排序** | Qwen3-Reranker-8B | 对 top-1000 逐条打分，选出 top-20。 |
| **列表式重排序** | GPT-4.1 | 基于完整对话历史，对 top-20 进行联合比较，排序后输出 top-10。使用特定 prompt 要求按相关性标签排名。 |
| **响应生成** | GPT-4.1（零样本 RAG） | 取 top-5 作为上下文，结合对话历史生成最终答案（≤256 tokens）。 |

- 所有模型均以零样本方式使用，未针对任务领域微调。
- 检索阶段使用 LION-SP-8B 在 MS MARCO 上训练。倒排索引使用 Seismic 库，设置查询/文档阈值 (400,600)。

## 3. 实验设计

### 数据集与场景
- **基准**：MTRAGEval 多轮对话评测集，包含 332 个检索轮次，四个领域各有不同数量：ClapNQ (84), FiQA (59), GovT (106), IBM-Cloud (87)。对话深度从浅到深（1–9+轮）。
- **子任务**：
  - Task A：对话搜索检索（主要评价 nDCG@5）
  - Task B：Oracle RAG 生成（使用黄金段落）
  - Task C：标准 RAG 生成（使用 Task A 检索结果）
- **评价指标**：
  - 检索：nDCG@1, @5, @10
  - 生成：RB_agg（基于 BERT-Recall、BERT-K-Precision、ROUGE-L 的调和平均），RB_llm（LLM 评价），RL_F（RAGAS 忠实度），以及三者的调和平均 H.Avg。分 IDK 条件化（排除不明确轮次）和非条件化两种。

### 对比方法
- **官方基线**：GPT-OSS-20B 查询改写 + ELSER 密集检索，nDCG@5=0.4795。
- **自身消融**：
  - 仅 LSR 检索（nDCG@5=0.4841）
  - +点式重排序（Qwen3-Reranker，nDCG@5=0.5477）
  - +列表式重排序（GPT-4.1，最终提交 uva-1，nDCG@5=0.5475）

## 4. 资源与算力

论文**未明确说明**使用的 GPU 型号、数量、训练时长等硬件资源。仅提及使用的模型：
- LION-SP-8B（8B 参数，推理）
- Qwen3-Reranker-8B（8B 参数，推理）
- GPT-4.1（API 调用，零样本）

所有组件均为零样本推理，无需训练，因此算力需求主要来自推理阶段。作者未进一步披露具体部署环境。

## 5. 实验数量与充分性

### 实验数量
- **检索 (Task A)**：进行了三级消融分析（LSR 基础 → +点式 → +列表式），并给出跨领域结果（4个域）、按对话深度的性能、以及检索-生成相关性分析。
- **生成 (Task B/C)**：报告了 Oracle 和 RAG 两种设置下的三个指标（RB_agg, RB_llm, RL_F），并分析可回答性（高/部分/不可回答）的影响，以及对话深度对生成的影响。
- **相关性分析**：计算了检索 nDCG@10 与生成各指标的 Pearson 相关系数。

### 充分性与公平性
- **优点**：消融实验覆盖了关键组件，领域差异分析、深度分析、相关性分析较为全面。评价使用官方统一指标和验证集，与基线对比公平。
- **局限性**：
  - 生成任务仅有一个最终提交（未做内部消融），且未对不可回答场景进行专门建模（如输出“无法回答”）。
  - 未比较其他同类系统（仅与官方基线对比），排名第 2 但未展示完整 leaderboard 细节。
  - 未评估推理效率或召回率（Recall@1000 未给出）。

## 6. 主要结论与发现

1. **检索性能出色**：最终系统 nDCG@5=0.5475，在 38 支队伍中排名第二，尤其是 top-1 精度通过列表式重排序从 0.4910 提升至 0.5331（+8.6%）。
2. **领域差异显著**：FiQA（金融）是最难领域（nDCG@5=0.330），约为其他领域一半；ClapNQ（维基百科）最易（nDCG@5=0.645）。
3. **生成忠实度与可回答性**：即使在部分可回答/不可回答轮次，系统的忠实度评分 RL_F 仍较高（如不可回答下 0.815），表明模型避免幻觉；但参考答案指标 RB_agg 和 RB_llm 下降。
4. **检索与生成正相关**：两者 Pearson r=0.46（p<0.05），但忠实度与检索几乎无关（r=0.08）。
5. **对话深度影响**：检索性能随深度无明显退化，但生成质量（尤其是 RB 指标）随深度增大而下降。
6. **非条件化评价更好**：在不对 IDK 判断做条件化时，RAG 生成甚至超越 Oracle，说明检索组件非常强。

## 7. 优点

- **零样本 & 跨领域通用**：无需领域内微调，仅用 MS MARCO 训练的 LSR + 通用 LLM 即可取得领先检索性能，实用性高。
- **多级级联设计合理**：从粗到精，先利用稀疏检索高效召回，再通过点式/列表式重排序逐步提升精度，且列表式会完整使用对话历史，弥补重写损失。
- **忠实度保持出色**：生成模型在缺乏证据时倾向于不输出未支撑信息，而非胡乱生成，prompt 设计保守且有效。
- **分析维度全面**：不仅报告总体指标，还深入分析领域、深度、可回答性、检索-生成相关性，提供有价值的洞察。
- **开源提示与实现**：附录提供了完整 prompts，便于复现。

## 8. 不足与局限

- **生成任务排名靠后**：Task B 排名 23/26，Task C 排名 20/29，主要原因是未对不可回答/部分可回答查询进行显式处理（如输出“我不知道”或提供澄清），导致参考指标偏低。
- **未使用更高级 RAG 技术**：如查询分解、自反思、多步推理等，作者也承认是未来工作。
- **实验规模有限**：仅参与单一共享任务，未在更多公共对话数据集上验证泛化性；消融实验仅覆盖检索阶段，生成侧未做变体（如不同 LLM、不同 prompt）。
- **FiQA 域性能明显弱**：可能因金融专业术语和稀疏标注，未做针对性调优。
- **缺少效率分析**：未报告推理延迟、内存占用等，且级联流水线涉及多次 LLM 调用，成本较高。
- **潜在偏差**：所有 LLM 组件均为 GPT-4.1，存在模型偏好风险；评价指标部分基于 LLM 判定（RB_llm），可能引入一致性问题。
- **资源细节缺失**：未说明推理硬件环境，影响可复现性。

（完）
