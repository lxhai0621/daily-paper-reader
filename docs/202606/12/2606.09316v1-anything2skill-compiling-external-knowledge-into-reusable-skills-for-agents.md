---
title: "Anything2Skill: Compiling External Knowledge into Reusable Skills for Agents"
title_zh: "Anything2Skill: 将外部知识编译为智能体可复用技能"
authors: "Qianjun Pan, Yutao Yang, Junsong Li, Jie Zhou, Kai Chen, Xin Li, Qin Chen, Liang He"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.09316v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 利用检索增强生成将外部知识编译为智能体可复用的技能
tldr: "现有RAG仅检索碎片化事实证据，智能体需反复从文本中推断任务步骤。本文提出Anything2Skill，将外部知识（手册、示例、日志等）编译为结构化技能，存入SkillBank并支持版本管理。结合RAG时，智能体同时获取声明事实和可复用技能，在qsv和GitHub-CLI任务上成功率分别达98.85%和94.10%，显著优于纯RAG。结果表明，将潜在程序知识显式编译为技能是从知识访问迈向能力复用的有效途径。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.09316v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1587, \"height\": 893, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.09316v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 778, \"height\": 264, \"label\": \"Table\"}]"
motivation: RAG抽取的零散事实证据无法直接复用为任务程序，智能体需重复推理步骤，降低效率。
method: 基于技能树先验分解知识记录，执行plan-and-expand提取候选技能，转为结构化合约后经分类编译、注册协调、生命周期管理存入SkillBank。
result: "在qsv和GitHub-CLI上分别达到98.85%和94.10%成功率，相比纯RAG提升显着。"
conclusion: 编译潜在程序知识为显式技能是扩展RAG智能体从知识访问到能力复用的有效方法。
---

## 摘要
检索增强生成（RAG）使智能体在推理时能够访问外部知识，但其主要检索的是零散的陈述性证据，导致智能体需要反复从段落、手册、示例、日志或轨迹中推断任务流程。这引出了一个基本问题：能否将外部知识库中提取的技能安装到智能体中，使其快速逼近领域专业知识？在本文中，我们提出Anything2Skill，一种基于分类学指导的框架，将异构外部知识编译为可复用、可检索、可执行的技能。给定一个知识记录语料库，\textsc{Anything2Skill} 首先将每条记录分解为证据窗口，并在技能树先验下执行规划与扩展的技能提取。随后，提取的候选技能被转换为结构化的技能契约，其中指定了调用条件、禁忌、动作序列、工作流步骤、约束、输出规范、支撑证据和置信度分数。为构建可部署的程序性记忆，Anything2Skill 通过分类学感知编译、注册表级协调、生命周期追踪、版本化更新和可视技能树投影，将提取的技能持久化存储在技能库中。在推理时，智能体从原始知识库检索任务相关段落，并从技能库检索相关的程序性技能，使得 RAG 提供陈述性证据，而编译后的技能提供可复用的程序性指导。在 qsv 和 GitHub-CLI 上的实验表明，结合 RAG 的 Anything2Skill 分别达到了 98.85% 和 94.10% 的成功率，显著优于仅使用 RAG 的智能体。这些结果表明，将潜在的程序性知识编译为显式技能是将检索增强智能体从知识访问扩展到能力复用的有效途径。

## Abstract
Retrieval-augmented generation (RAG) enables agents to access external knowledge at inference time, but it primarily retrieves fragmented declarative evidence, leaving agents to repeatedly infer task procedures from passages, manuals, examples, logs, or trajectories. This raises a fundamental question: can skills extracted from external knowledge bases be installed into an agent, enabling it to rapidly approximate domain expertise? In this paper, we propose Anything2Skill, a taxonomy-guided framework that compiles heterogeneous external knowledge into reusable, retrievable, and executable skills for agents. Given a corpus of knowledge records, \textsc{Anything2Skill} first decomposes each record into evidence windows and performs plan-and-expand skill extraction under a skill-tree prior. The extracted candidates are then converted into structured skill contracts that specify invocation conditions, contraindications, action moves, workflow steps, constraints, output specifications, supporting evidence, and confidence scores. To construct a deployable procedural memory, Anything2Skill manages the extracted skills in a persistent SkillBank through taxonomy-aware compilation, registry-level reconciliation, lifecycle tracking, versioned updates, and visible skill-tree projection. At inference time, agents retrieve both task-specific passages from the original knowledge base and relevant procedural skills from the SkillBank, allowing RAG to provide declarative evidence while compiled skills provide reusable procedural guidance. Experiments on qsv and GitHub-CLI show that Anything2Skill combined with RAG achieves 98.85\% and 94.10\% success rates, respectively, substantially outperforming RAG-only agents. These results suggest that compiling latent procedural knowledge into explicit skills is an effective way to extend retrieval-augmented agents from knowledge access toward capability reuse.

---

## 论文详细总结（自动生成）

# 论文总结：Anything2Skill

## 1. 核心问题与整体含义（研究动机和背景）
- **现有 RAG 的局限**：检索增强生成（RAG）虽能为智能体提供外部知识，但检索到的只是零散的陈述性证据（如段落、文档、示例）。智能体必须每次从这些碎片中反复推断出任务步骤、工具调用顺序、约束条件等程序性知识，导致效率低下且容易出错。
- **本文目标**：将外部知识库中隐含的程序性知识（如手册、对话、日志、轨迹）系统性地编译为可复用、可检索、可执行的技能（skill），让智能体在日常推理时直接复用这些“程序性记忆”，从而从“知识访问”迈向“能力复用”。

## 2. 方法论：核心思想、关键技术细节
- **核心思想**：提出 **Anything2Skill** 框架，基于技能分类学（skill taxonomy）指导，将异构外部知识编译为结构化技能合约（skill contract），并存储在持久的 **SkillBank** 中，供智能体在推理时与 RAG 协同使用。
- **关键技术细节**：
  1. **技能分类学（T）**：定义资产类型集合 A = {macro_protocol, session_skill, micro_skill, safety_rule, knowledge_reference}，以及节点、层次结构、映射规则。用于约束提取方向，防止产生随意摘要。
  2. **证据窗口（Evidence Windows）**：将每条知识记录分解为带标题、锚点、元数据的窗口，保证每个提取的技能可回溯到源证据。
  3. **规划-扩展提取（Plan-and-Expand Extraction）**：两步走：  
     - **规划阶段**：LLM 规划器对每个窗口识别技能候选（包含目标、类型、调用线索等）。  
     - **扩展阶段**：独立将每个候选扩展为结构化技能草稿。若窗口不支持则返回空，避免幻觉。  
     - 通过程序性有效性过滤器（Iproc）：保留至少包含工作流步骤、动作序列、约束等程序性内容的候选。
  4. **技能合约结构**：每个技能 s 包含名称、描述、资产类型、分类节点、调用条件、禁忌、动作序列、工作流步骤、约束、输出规格、支撑证据集、置信度分数等。
  5. **SkillBank 管理**：  
     - **文档级编译**：将窗口级草稿按分类学身份键分组，合并为规范的文档级技能（带证据约束）。  
     - **注册表级协调**：新技能与已有技能通过混合检索（密集+稀疏+分类学兼容性）匹配，预测生命周期动作（CREATE、STRENGTHEN、MERGE、SPLIT、DISCARD 等），并维护版本历史和生命周期状态（candidate → retire）。  
     - **可视技能树投影**：将活跃技能按分类层次组织，合并重复项，选择主技能，构建树状结构便于导航与检索。
  6. **推理时**：智能体同时检索原始知识库段落（RAG提供陈述证据）和 SkillBank 中的技能（提供程序性指导），最终输出由基础模型（如gpt5.4）综合生成。

- **公式/算法流程**（文字说明）：  
  - 提取步骤：输入知识记录 → 窗口分解 → 规划候选 → 扩展草稿 → 过滤 → 生成支持证据记录。  
  - 编译步骤：草稿分组（使用身份键γ）→ 约束编译（保持资产类型、分类节点）→ 求解最大化覆盖-最小化冗余目标。  
  - 协调步骤：新技能与库中技能计算混合检索分数 R(s,t) → 管理模型预测生命周期动作 → 应用动作并更新版本。  
  - 树投影：按层次选择父节点，同层重复合并。

## 3. 实验设计：数据集、Benchmark、对比方法
- **数据集与场景**：
  - **qsv**：CSV 命令行操作基准，基于官方 qsv 文档。任务要求选择命令、组合选项、执行表格转换。
  - **GitHub-CLI**：基于 GitHub CLI 仓库操作指南和 gh 命令文档，共 110 篇源文档，编译出 179 个技能（1 全局、6 顶层、62 二级、110 微技能）。任务涉及 issue 处理、PR 工作流、发布管理等。
- **Benchmark**：任务成功率（success rate），即智能体正确完成命令行操作的百分比。
- **对比方法**：
  - Base Agent：无外部上下文。
  - RAG：仅检索原始知识库的 top-3 来源段落。
  - Anything2Skill：仅从 SkillBank 检索技能，无任务时文档检索。
  - Anything2Skill + RAG：同时提供两部分。

## 4. 资源与算力
- **文中未明确说明**：未提及使用的 GPU 型号、数量、训练时长或推理算力。仅提到基础模型为 gpt5.4（可能是一个未来或专有模型），未给出其参数量或部署细节。因此无法评估训练或推理的计算开销。

## 5. 实验数量与充分性
- **实验数量**：共两个基准，每个基准测试四种配置（Base、RAG、Anything2Skill、Anything2Skill+RAG），共 8 个结果。
- **充分性分析**：
  - **优点**：对比了移除技能、移除 RAG、单独使用技能、两者结合的情况，直接验证了技能与 RAG 的互补性。
  - **不足**：
    - 缺乏消融实验：未对技能提取策略（如规划扩展 vs 直接提取）、管理组件（如合并、版本化）进行单独消融。
    - 未测试不同检索 top-k 数量、不同基础模型（如 GPT-4、Llama）的泛化性。
    - 仅覆盖两个命令行领域，未扩展到更通用的智能体任务（如对话、网页操作、科学计算）。
    - 未报告多次运行的标准差或统计显著性检验。
  - **总体**：实验设计直观、对比清晰，但规模有限，客观性尚可，但充分性不足（需更多消融与扩展实验）。

## 6. 主要结论与发现
- 编译的技能（Anything2Skill 单独）显著优于 Base Agent（qsv: 91.95% vs 81.60%；GitHub-CLI: 82.30% vs 64.70%），证明 SkillBank 提供了可复用程序性指导。
- 技能与 RAG 互补：Anything2Skill + RAG 在两个基准上均达到最佳（98.85% 和 94.10%），且优于单独使用 RAG 或技能。
- 技能在复杂 CLI 工作流（GitHub-CLI）上的增益更大，说明当任务需要多条命令组合、约束满足时，程序性技能尤为有效。
- 结论：将潜在程序知识编译为显式技能是扩展 RAG 智能体从知识访问到能力复用的有效途径。

## 7. 优点
- **方法创新**：首次系统性地将异构知识库中的程序性知识通过分类学指导编译为结构化、可版本管理的技能，而非仅做陈述性检索。
- **完备的编译管理 pipeline**：从证据窗口、规划-扩展提取、技能合约，到文档级编译、注册协调、生命周期、版本化、树投影，设计周到。
- **实验结果直观**：清晰展示了技能与 RAG 的互补增益，且与已有基线相比提升显著。
- **可解释性**：每个技能合约包含支撑证据、版本历史，便于审计和回溯。
- **通用性**：框架不限定知识源类型（文档、对话、日志、轨迹），理论上可适配多种智能体场景。

## 8. 不足与局限
- **实验覆盖不足**：仅两个 CLI 基准，未测试在更通用或更复杂的智能体任务（如多轮交互、视觉环境、工具组合）上的效果。技能的可迁移性未知（如跨领域技能是否仍有效）。
- **缺乏消融实验**：未评估各组件（如规划扩展 vs 直接提取、生命周期动作、版本化）的独立贡献。
- **算力与成本未报告**：无法判断编译技能的离线成本以及推理时额外检索技能的开销，也限制了可复现性。
- **依赖特定模型**：所有实验使用 gpt5.4，未验证在不同 LLM 规模或架构下的鲁棒性。
- **技能质量验证**：成功率的提升可能部分归因于更长的上下文（技能和段落叠加），而非技能本身的结构化设计。应做上下文长度控制实验。
- **生命周期管理复杂性**：文中描述的管理模型（Mθ）需要额外的 LLM 调用来预测动作，可能引入延迟和成本，且其准确性未单独评估。

（完）
