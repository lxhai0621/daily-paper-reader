---
title: "QO-Bench: Diagnosing Query-Operator-Preserving Retrieval over Typed Event Tuples"
title_zh: QO-Bench：诊断类型化事件元组上的查询操作符保留检索
authors: "Mengao Zhang, Xiang Yang, Chang Liu, Tianhui Tan, Ke-wei Huang"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.04646v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: RAG系统查询操作符保留检索的诊断基准
tldr: 现有RAG系统优化语义相关性，但检索相关段落未必能正确执行数据库式查询。为此提出QO-Bench基准，基于新闻和公司事件构建类型化元组及确定性答案，支持算子级诊断。评估发现系统检索文本却丢弃算子所需的类型值，算子执行瓶颈比检索更关键，且不同算子下范式排名反转。QO-Bench将目标从段落相关性转向查询算子保持的检索，为诊断和优化提供新框架。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.04646v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1520, \"height\": 287, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.04646v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1518, \"height\": 381, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04646v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1513, \"height\": 572, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04646v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 813, \"height\": 502, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04646v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1336, \"height\": 583, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04646v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 737, \"height\": 390, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04646v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 782, \"height\": 393, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04646v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1495, \"height\": 270, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04646v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1538, \"height\": 531, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04646v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1241, \"height\": 1069, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04646v1/table-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 618, \"height\": 323, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04646v1/table-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 734, \"height\": 507, \"label\": \"Table\"}]"
motivation: 现有RAG系统侧重语义相关性，忽略了检索结果需支持查询算子（如连接、聚合）的正确执行。
method: "构建QO-Bench基准，含22,984篇新闻和614个事件，18个查询模板，通过类型化元组确定性计算答案，实现算子级诊断。"
result: 长上下文oracle远未饱和，算子执行是核心瓶颈；不同算子下范式排名反转，如相似检索在过滤/投影上领先，IE-to-SQL在交集和计数上更优。
conclusion: QO-Bench重新定义检索目标为保持查询算子，揭示了算子执行比检索本身更关键，且强答案模型无法消除此瓶颈。
---

## 摘要
许多关于商业、法律和科学语料库的现实世界问题，都是对文本中潜在记录的数据库式查询的自然语言版本。现有的检索增强生成（RAG）系统主要针对语义相关性进行优化，但检索相关段落并不能保证正确执行查询。我们引入了QO-Bench，这是一个用于类型化事件元组上的查询操作符问答的诊断基准。该基准涵盖来自18个查询模板的22,984篇新闻文章和614个公司事件，评估了785个问题。每个金标准答案通过类型化事件元组确定性计算，并通过召回率评分，答案通过精确匹配而不是大语言模型判断与金标准元组匹配。这种设计支持操作符级别的诊断，如连接和交集。我们在匹配条件下评估了RAG、ReAct RAG、GraphRAG以及信息提取到SQL，并使用长上下文预留上限来隔离检索失败。一个双轴框架——索引时保留与查询时执行——预测了每种范式的失败点，结果证实了这一点：系统检索了相关文本，但丢弃了操作符所需的类型化值，可部署的范式排名在操作符上颠倒，相似性检索在过滤/投影上领先，而提取到SQL在交集和计数上领先。即使提供了金标准证据，长上下文预留也远未饱和，因此操作符执行——不仅仅是检索——是一个核心瓶颈，更强的答案模型也无法消除。QO-Bench将目标从段落相关性重新定义为查询操作符保留检索。

## Abstract
Many real-world questions over business, legal, and scientific corpora are natural-language versions of database-style queries over records latent in text. Existing retrieval-augmented generation (RAG) systems are optimized primarily for semantic relevance, but retrieving plausible passages does not guarantee correct query execution. We introduce QO-Bench, a diagnostic benchmark for query-operator question answering over typed event tuples. The benchmark covers 22,984 news articles and 614 corporate events across 18 query templates, evaluated on 785 questions. Each gold answer is deterministically computed from typed event tuples and scored by recall, with answers matched to the gold tuples by exact match rather than an LLM judge. This design enables operator-level diagnosis such as joins and intersection. We evaluate RAG, ReAct RAG, GraphRAG, and information-extraction-to-SQL under matched conditions, with a long-context oracle ceiling to isolate retrieval failure. A two-axis framework -- index-time preservation versus query-time execution -- predicts where each paradigm fails, and the results bear it out: systems retrieve relevant text but discard the typed values operators need, and the deployable paradigm ranking inverts across operators, with similarity retrieval leading on filter/project and extraction-to-SQL on intersection and counting. Even given the gold evidence, a long-context oracle stays far from saturated, so operator execution -- not retrieval alone -- is a core bottleneck that a stronger answer model does not remove. QO-Bench reframes the goal from passage relevance to query-operator-preserving retrieval.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **动机**：现实世界中许多商业、法律和科学领域的问题本质上是数据库式查询（如“哪些公司在2018年和2022年都是并购买家？”），但所需记录松散地分布在文本语料中，而非预聚合的表格。现有RAG系统优化语义相关性，检索相关段落却不能保证正确执行数据库操作符（如连接、交集、计数）。
- **核心问题**：检索系统是否能够“保持操作符”（operator-preserving），即保留执行自然语言查询中隐含的数据库操作符所需的类型化值和结构。
- **整体含义**：指出仅靠语义相关性检索无法解决复杂查询，需要将检索目标从“段落相关性”重新定义为“查询操作符保留检索”，并提出了一个诊断框架来分解和定位不同范式的失败原因。

### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：将问答形式化为类型化事件元组上的确定性计算（denotational retrieval）。定义查询为 \( q = (k, D_q, \phi, \pi, \alpha) \)，其中 \( k \) 是元组元数，\( D_q \) 是类型域，\( \phi \) 是谓词，\( \pi \) 是投影，\( \alpha \) 是操作符（list, count, group, intersect等）。金标准答案 \( y^*(q) = \alpha(\langle \pi(A(\vec{e})) : \vec{e} \in R_q \rangle) \) 由事件集 \( \hat{E} \) 上的确定性计算得出。
- **两轴分析框架**：
  - **索引时保留（index-time preservation）**：索引是否保留了操作符需要的属性值（如日期、角色、对手方）。
  - **查询时执行（query-time execution）**：运行时能否解析查询协议、检索完整元组并正确执行操作符。
- **关键技术细节**：
  - 使用FNSPID新闻语料（NASDAQ子集，22,984篇文章）和S&P Capital IQ事件（614个单篇文章可证明事件），通过3-of-3 LLM判定（三个独立大语言模型一致同意）和专家验证（94.1%精度）构建金标准。
  - 每个事件以公开标识（股票代码、角色、日期等）组成类型化元组，答案通过精确匹配（而非LLM评判）计算召回率。
  - 包含18个模板（4个能力A：过滤检索；14个能力B：组合操作），785个问题。

### 3. 实验设计：数据集、benchmark、对比方法
- **数据集**：FNSPID新闻语料（NASDAQ子集，2010–2023年文章），S&P Capital IQ Key Developments事件（8种类型：并购宣布/完成/取消/谣言、CEO变更、CFO变更、IPO、股票分割）。
- **Benchmark**：QO-Bench，包含785个标准化问题，金标准为确定性事件元组，评估指标为召回率（±7天日期容忍度）。
- **对比方法**（五种可部署范式 + 一个上限）：
  - RAG（混合检索：dense + BM25 + reranker，取top-30 chunks）。
  - ReAct RAG（LangChain实现，最多5次检索调用，积累证据）。
  - GraphRAG（local模式：实体匹配→邻域；global模式：固定map-reduce社区报告）。
  - IE→SQL（先提取事件到固定schema的SQL数据库，再执行SQL查询）。
  - 长上下文oracle（LC-oracle）：直接提供金标准文章作为上下文，隔离检索失败。
  - 所有范式共享相同答案LLM（Qwen3.6-27B，推理模式）和事件定义文档。

### 4. 资源与算力
- 论文中**未明确说明**使用的GPU型号、数量及训练时长。仅提到使用了vLLM部署Qwen3.6-27B、Gemma-4-31B-IT、gpt-oss-120B等模型用于判定和生成，以及DeepSeek v4系列用于鲁棒性测试，但未给出具体算力配置或训练耗时。

### 5. 实验数量与充分性
- **实验数量**：主要实验包括：
  - 整体对比（表4）：6种操作符族 × 5种范式 + oracle。
  - 每个模板的细粒度结果（表9）：18个模板 × 6个系统。
  - 金文档检索召回率（表10）：展示覆盖能力。
  - 答案模型鲁棒性测试（表11）：使用3种不同LLM（轻量、非推理、重推理）重新评估oracle，以及无上下文基线。
  - 此外还包括消融性质的**oracle**（隔离检索）和**无上下文基线**（隔离参数知识）。
- **充分性与公平性**：
  - 所有范式在匹配条件下比较（相同语料、相同答案模型、相同事件定义）。
  - 使用确定性金标准和精确匹配，避免了LLM评判的偏见。
  - 实验覆盖了多种操作符和范式，结果呈现了排名随操作符变化，说明设计具有诊断价值。
  - 但论文指出每个范式存在多种实现，结果依赖于具体配置（如RAG的top-k、IE→SQL的schema生成），因此应视为架构诊断而非绝对排名。

### 6. 论文的主要结论与发现
- **没有万能范式**：不同操作符下可部署范式排名反转。RAG/ReAct在过滤/投影上领先（~55%），IE→SQL在交集和计数上显著占优（~51%），GraphRAG全局模式整体很低（3.8%）。
- **操作符执行是核心瓶颈**：即使长上下文oracle获得金证据，整体召回率仅52.2%，尤其在交集上仅3.9%（且更强的答案模型（DeepSeek v4-pro Think Max）也不能提升）。这证明失败不仅在于检索，更在于从文本中执行操作符的能力。
- **失败定位**：
  - **相似检索（RAG/ReAct）** 失败于覆盖率（gold文章召回率仅~30%，能力B更低）。
  - **GraphRAG** 失败于值保真度（组织结构但压缩了精确日期、角色、计数）。
  - **IE→SQL** 失败于查询轴（固定schema限制，跨事件连接键无法填充）。
- **LC-oracle远未饱和**：即使提供所有证据，LLM也无法可靠执行组合操作，说明这是架构性不匹配。

### 7. 优点：方法或实验设计上的亮点
- **新颖的问题定义**：明确提出了“查询操作符问答”（QO-QA），并定义“操作符保留检索”这一关键属性，填补了检索与数据库查询之间的空白。
- **确定性金标准**：使用类型化事件元组和精确匹配，避免LLM评判的不稳定性，使得操作符级诊断可靠。
- **双轴诊断框架**：将失败分解为索引时保留和查询时执行，能预测不同范式的失败模式（如相似检索→覆盖率丢失，GraphRAG→值保真度丢失），具有可解释性。
- **实验设计公平**：所有范式共享相同语料、相同答案模型、相同事件定义，且IE→SQL的schema仅从定义生成（无模板泄露），orchle隔离了检索贡献。
- **全面的诊断粒度**：支持六大操作符族（过滤/投影、角色/类型、时间连接、排序、交集、计数/分组）和18个具体模板，能定位具体瓶颈。

### 8. 不足与局限
- **领域特异性**：仅使用公司（财经）新闻，事件类型有限（8种），结论可能不直接迁移到法律、生物医学或政策监控等领域。
- **查询模板化**：问题由模板生成，语言多样性不足，可能低估自然语言变体带来的挑战。
- **金标准误差**：尽管有3-of-3判定和专家验证（94.1%精度），但仍存在来源歧义、日期惯例、实体规范化等残余错误。论文承认这些风险。
- **范式实现依赖**：每个范式只有一种实现（如RAG的top-30、GraphRAG的默认参数），结果可能因配置缩放而改变，不视为模型独立估计。
- **未跨越文档事件**：所有事件被约束为单篇文章可证明，回避了跨文档组合事件；更真实场景下需要跨文档证据合成，难度更高。
- **评估简化**：使用±7天日期容忍度和精确匹配，忽略了时间和模糊匹配的细微差别；召回率未考虑排序或输出格式差异。

（完）
