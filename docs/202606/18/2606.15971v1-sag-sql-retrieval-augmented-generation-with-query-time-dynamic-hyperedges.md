---
title: "SAG: SQL-Retrieval Augmented Generation with Query-Time Dynamic Hyperedges"
title_zh: SAG：基于SQL检索增强生成与查询时动态超边技术
authors: "Yuchao Wu, Junqin Li, XingCheng Liang, Yongjie Chen, Yinghao Liang, Linyuan Mo, Guanxian Li"
date: 2026-06-14
pdf: "https://arxiv.org/pdf/2606.15971v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 使用SQL连接查询动态链接事件块，将结构化约束集成到RAG中
tldr: "现有检索增强生成方法在处理结构化约束和多跳推理时存在局限，知识图谱又面临语义碎片化和维护成本高的问题。本文提出SAG，将每个文本块转换为事件和索引实体，查询时通过SQL连接动态构建局部超边，避免全局图重建。在HotpotQA、2WikiMultiHop和MuSiQue三个基准上，SAG在9个Recall@K指标中取得8个最优，MuSiQue的Recall@5达到80.0%。该系统已支持数亿级别数据生产部署，在线检索延迟控制在秒级，为多跳推理检索提供了高效可扩展的解决方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15971v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1457, \"height\": 941, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15971v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1425, \"height\": 939, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15971v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 637, \"height\": 463, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.15971v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 845, \"height\": 183, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15971v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1457, \"height\": 703, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15971v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 786, \"height\": 241, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15971v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 964, \"height\": 243, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15971v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 779, \"height\": 222, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15971v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 902, \"height\": 242, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15971v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1008, \"height\": 242, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15971v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1154, \"height\": 324, \"label\": \"Table\"}]"
motivation: 现有RAG方法难以处理结构化约束和多跳推理，知识图谱存在语义碎片化、维护成本高和增量更新困难。
method: 将每个文本块转化为完整事件和一组索引实体，查询时用SQL连接动态链接共享实体的事件组成局部超边。
result: "在HotpotQA等三个基准上8/9的Recall@K最佳，MuSiQue Recall@5达80.0%，支持亿级数据秒级延迟。"
conclusion: SAG通过动态超边结构有效提升多跳推理检索性能，且易于增量扩展和并发处理。
---

## 摘要
检索增强生成（RAG）为大语言模型访问外部知识提供了一种有效方法。然而，现有方法依赖密集相似性检索，在处理结构化约束和多跳推理方面存在固有局限性。引入知识图谱可部分缓解这些问题，但会带来语义碎片化、高维护成本以及增量更新困难等代价。本文提出SAG（SQL检索增强生成），一种面向检索与智能体系统的结构化架构。SAG无需预先构建全局静态图，而是将每个文本块转换为一个语义完整的事件和一组索引实体，然后利用SQL连接查询在查询时动态地将共享实体的事件链接为局部超边，从而构建动态实例化的局部索引结构。该设计避免了全局图重建和持续维护的需求；系统通过依赖标准数据库基础设施，自然支持增量写入、并发处理和持续扩展。在HotpotQA、2WikiMultiHop和MuSiQue三个标准多跳基准测试中，SAG在9个Recall@K指标中取得了8项最佳结果，在推理需求最高的MuSiQue上达到80.0%的Recall@5。SAG已在数亿级数据项的生成环境中部署，在线检索延迟保持在秒级以内。项目网站和代码见https://github.com/Zleap-AI/SAG-Benchmark。

## Abstract
Retrieval-Augmented Generation (RAG) offers an effective approach for large language models to access external knowledge. However, existing methods rely on dense similarity retrieval and face inherent limitations in handling structured constraints and multi-hop reasoning. Incorporating knowledge graphs partially alleviates these issues, but at the cost of semantic fragmentation, high maintenance overhead, and difficult incremental updates. This paper introduces SAG (SQLRetrieval Augmented Generation), a structured architecture for retrieval and agent systems. Instead of pre-building a global static graph, SAG converts each chunk into one semantically complete event and a set of indexing entities, then uses SQL join queries to dynamically link events that share entities into local hyperedges,constructing, at query time, a dynamically instantiated local index structure. This design avoids the need for global graph rebuilding and ongoing maintenance; the system naturally supports incremental writes, concurrent processing, and continuous scaling through its reliance on standard database infrastructure. Across HotpotQA, 2WikiMultiHop, and MuSiQue, three standard multi-hop benchmarks,SAG achieves the best results on 8 out of 9 Recall@K metrics, reaching 80.0% Recall@5 on MuSiQue, the benchmark with the highest multi-hop reasoning demands.SAG has also been deployed at a production scale of hundreds of millions of data items, with online retrieval latency kept within seconds. Project site and code are available at https://github.com/Zleap-AI/SAG-Benchmark.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：现有RAG方法依赖密集向量相似性检索，在处理结构化约束（如时间、角色、多步依赖）和多跳推理时存在固有局限；引入知识图谱虽部分缓解，但导致语义碎片化（三元组分解破坏整体语义）、高维护成本和增量更新困难。
- **整体含义**：SAG提出一种结构化检索架构，通过将文档转化为**事件-实体索引**，并在查询时使用**SQL连接动态构建局部超边**，在不预建全局静态图的前提下实现结构化、可扩展的多跳证据组织。其核心设计原则是**重新分配流水线职责**：SQL负责确定性结构约束，向量语义仅做扩展，LLM只做最终精选。

### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：每个文档块被转换为一个语义完整的事件（event）和一组索引实体（entities），共同定义一条**潜在超边**。离线阶段将事件和实体写入SQL、向量和全文索引；在线阶段通过三步完成检索：
  1. **种子检索**：并行执行两条路径——  
     - 路径A（实体引导）：LLM提取查询中的关键实体，经向量相似度扩展（阈值0.9）后，通过SQL join召回所有关联事件（\( E_R^{entity} \)）。  
     - 路径B（直接事件检索）：用查询向量在事件索引中做向量检索（阈值0.4），得\( E_R^{direct} \)。  
     两者合并得到初始候选集\( E_R = E_R^{entity} \cup E_R^{direct} \)。
  2. **查询时扩展**：从\( E_R \)出发，通过SQL反向join获取新实体作为“实体前沿”，再沿共享实体跳转到新事件（默认H=1跳），得到扩展集\( E_E \)。最终候选池\( E_{cand} = E_R \cup E_E \)。**粗排**：保留与查询向量相似度最高的Top-Kcand个事件（默认100）。
  3. **最终选择**：双路径输出——  
     - 路径A（结构路径）：LLM对候选事件精排，选Top-Kevent（默认5）映射回原始块。  
     - 路径B（语义路径）：直接查询块向量索引，选Top-Kdirect（默认5）块。  
     合并去重后输出Top-Kout（默认10）个块。
- **关键技术细节**：
  - 事件保持完整语义，不分解为多个三元组；实体分11种类型，仅做字符串规范化和SQL去重，不引入完整实体消歧。
  - 超边非预先枚举，而是查询时通过SQL join动态激活；多跳扩展本质是数据库关系连接而非图推理。
  - 全流程可审计：`q → U_q → \hat{U}_q → E_R → E_{cand} → \hat{E} → C_{out}`，每步失败可定位。

### 3. 实验设计：数据集、基准、对比方法
- **数据集**：三个多跳推理基准，推理难度递增：  
  - **HotpotQA**（2跳，比较/桥接）  
  - **2WikiMultiHopQA**（多路径，含桥接、比较、推断）  
  - **MuSiQue**（最多4跳，每步不可跳过，反事实过滤，多跳要求最高）  
  各取1000个问题，构建局部语料库（段落数：MuSiQue 11,656，2Wiki 6,119，HotpotQA 9,811）。
- **对比方法**：  
  - **主要基线**：HippoRAG 2（离线构建知识图谱 + 全局PageRank传播），使用相同底层模型统一配置。  
  - **简单检索器**（文献值）：Contriever, BM25, GTR (T5-based)。  
  - **大嵌入模型**（文献值）：BGE-Large-EN-v1.5, GTE-Qwen2-7B, GritLM-7B, NV-Embed-v2 (7B)。
- **评估指标**：Recall@K（K=2,5为主，消融含R@1,10），任意命中标准（至少一个支持证据在Top-K内）。
- **资源与算力**：文中未明确说明任何GPU型号、数量或训练时长。仅提及使用Qwen3.6-Flash（推测为在线推理调用）和BGE-Large-EN-v1.5/NV-Embed-v2作为嵌入模型，但无具体计算资源报告。需指出这一点。

### 4. 实验数量与充分性
- **实验组数**：主实验3个数据集×3个主要对比（SAG、HippoRAG 2、简化基线），共9组Recall@K结果。消融实验6组：  
  1. 超边 vs. 三元组表示（1个数据集）  
  2. 查询时扩展跳数（0 vs. 1）  
  3. 候选事件数量Kcand（50/100/200/500）  
  4. 重排序器类型（轻量Reranker vs. LLM精排）  
  5. 双路径分配比例（Kevent固定为0,2,4,5）  
  6. 嵌入模型鲁棒性（BGE vs. NV-Embed-v2，两个方法对比）  
- **充分性与公平性**：  
  - 所有对比方法在相同底层配置（相同嵌入模型、相同LLM）下重新运行HippoRAG 2，消除模型差异影响。  
  - 消融实验设计系统，覆盖了表示粒度、扩展策略、预算、重排序、路径互补性和嵌入敏感性，能清晰解耦各组件贡献。  
  - 局限：仅覆盖多跳QA任务，未验证其他RAG场景（如开放域问答、摘要、对话）；数据集仅三个（均为英文Wikipedia基），可能不反映其他领域特性。总体实验较为充分，结论可信。

### 5. 论文的主要结论与发现
- **主结果**：SAG在9个Recall@K指标中取得8个最优（除2WikiRecall@5略低）。平均Recall@2/5达79.3%/88.2%，优于HippoRAG 2的68.2%/83.3%。在最长链MuSiQue上差距最大（R@5: 80.0% vs. 65.1%）。  
- **消融关键发现**：  
  - 超边 vs. 三元组：超边贡献额外2.9个百分点（MuSiQue R@5: 80.0% vs. 77.1%）。  
  - 扩展跳数：关闭扩展后R@5从80.0%降至69.4%，说明扩展提供向量无法获得的中间证据。  
  - LLM精排必要：用轻量Reranker（0.6B）后R@5降至62.2%，LLM能联合评估子集完整性。  
  - 双路径互补：纯语义（Kevent=0）仅56.2% R@5，加入2个事件候选即升至73.3%，说明结构路径提供独特跨实体证据。  
  - 嵌入模型鲁棒性：SAG受嵌入模型影响小（NV-Embed-v2下R@5 81.7%，BGE下80.0%），而HippoRAG 2从74.6%降至65.1%（因PageRank放大嵌入误差）。  
- **局限性**：在2Wiki上R@5略低（88.0% vs. 90.4%），因低频繁桥梁实体被修剪策略截断；当前修剪参数为启发式设置。

### 6. 优点：方法或实验设计的亮点
- **方法创新**：  
  - 用SQL join替代全局图传播，避免离线维护，支持增量、并发和持续扩展。  
  - 动态超边在查询时局部实例化，兼具高阶关系表达能力和低构建成本。  
  - 双路径设计（结构+语义）互补，LLM仅用于小范围精排，成本可控。  
  - 全流程可审计，便于调试和归因。  
- **实验设计亮点**：  
  - 统一底层配置对比HippoRAG 2，排除模型差异。  
  - 丰富的消融实验覆盖表示、扩展、预算、重排序、路径等关键维度。  
  - 验证嵌入鲁棒性（BGE vs. NV-Embed），凸显架构优势而非组件优势。  
  - 报告生产级部署（数亿数据，秒级延迟），证明工程可行性。

### 7. 不足与局限
- **修剪策略**：固定启发式修剪（实体前沿预算50）可能丢弃低频繁但关键的桥梁实体，导致2WikiRecall@5落后。  
- **实体合并**：仅做字符串规范和SQL去重，同义实体（如别名、拼写变体）可能被切割，削弱跨文档连接密度。  
- **更新支持**：当前只支持追加更新，无法覆盖/版本化已有事件（对智能体记忆场景不够）。  
- **实验范围**：仅测试三个多跳QA数据集（均为英文Wikipedia），未验证其他任务（如开放域QA、摘要生成、多文档问答）或真实业务场景；未覆盖多语言或长文档。  
- **算力报告缺失**：未提供任何GPU型号、数量、训练时长或离线提取成本，难以复现或评估资源需求。  
- **LLM依赖**：离线事件/实体提取和在线精排仍需LLM调用，虽已压缩但仍有成本；弱嵌入模型下扩展可能受限。  
- **对结构化查询的依赖性**：SAG强依赖SQL join的精确匹配，对实体别名、拼写错误不够鲁棒（需向量扩展补偿）；高度非结构化且缺乏实体连接的场景可能效果不佳。

（完）
