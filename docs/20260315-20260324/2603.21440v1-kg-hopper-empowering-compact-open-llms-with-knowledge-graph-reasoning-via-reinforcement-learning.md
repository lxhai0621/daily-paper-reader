---
title: "KG-Hopper: Empowering Compact Open LLMs with Knowledge Graph Reasoning via Reinforcement Learning"
title_zh: KG-Hopper：通过强化学习赋予紧凑型开源大语言模型知识图谱推理能力
authors: "Shuai Wang, Yinan Yu"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21440v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: LLM 的知识图谱推理与结构化知识库集成
tldr: 针对大语言模型在知识图谱多跳推理中存在的灵活性差和错误累积问题，本文提出 KG-Hopper 强化学习框架。该框架通过将整个 KG 遍历和决策过程整合进单一推理周期的“思考”阶段，使 7B 规模的小型开源模型具备全局推理和动态回溯能力。实验证明，KG-Hopper 在多个基准测试中超越了 70B 规模的大模型，并能与 GPT-4o-mini 等闭源模型媲美。
selection_source: fresh_fetch
motivation: 现有的多步推理方法因步骤孤立且缺乏灵活性，容易导致错误累积，难以处理复杂的知识图谱问答任务。
method: 提出一种强化学习框架，将多跳推理的路径探索与回溯过程统一嵌入到模型的“思考”阶段，实现单次推理完成全局决策。
result: 基于 7B 参数的模型在 8 个基准测试上一致优于 70B 的多步推理系统，并达到了与 GPT-3.5-Turbo 和 GPT-4o-mini 相当的水平。
conclusion: KG-Hopper 证明了通过强化学习优化推理过程，小型开源模型也能在复杂的知识密集型任务中展现出卓越的性能和效率。
---

## 摘要
大语言模型（LLMs）展现出令人印象深刻的自然语言能力，但在处理知识密集型推理任务时往往面临困难。利用结构化知识图谱（KGs）的知识库问答（KBQA）因需要准确的多跳推理而体现了这一挑战。现有方法通常在预定义流水线的指导下执行顺序推理步骤，这限制了灵活性，并由于每一步的孤立推理而导致错误级联。为了解决这些局限性，我们提出了 KG-Hopper，这是一个新颖的强化学习（RL）框架，旨在赋予紧凑型开源 LLM 在单轮推理中执行集成多跳 KG 推理的能力。我们不再采用逐步推理的方式，而是训练一个推理 LLM，将整个 KG 遍历和决策过程嵌入到一个统一的“思考”阶段，从而实现对跨步依赖关系的全局推理以及带有回溯的动态路径探索。在八个 KG 推理基准测试上的实验结果表明，基于 7B 参数 LLM 的 KG-Hopper 一贯优于更大规模的多步系统（高达 70B），并与 GPT-3.5-Turbo 和 GPT-4o-mini 等闭源模型相比具有竞争力的性能，同时保持了紧凑、开源和数据高效的特点。代码已公开：https://github.com/Wangshuaiia/KG-Hopper。

## Abstract
Large Language Models (LLMs) demonstrate impressive natural language capabilities but often struggle with knowledge-intensive reasoning tasks. Knowledge Base Question Answering (KBQA), which leverages structured Knowledge Graphs (KGs) exemplifies this challenge due to the need for accurate multi-hop reasoning. Existing approaches typically perform sequential reasoning steps guided by predefined pipelines, restricting flexibility and causing error cascades due to isolated reasoning at each step. To address these limitations, we propose KG-Hopper, a novel Reinforcement Learning (RL) framework that empowers compact open LLMs with the ability to perform integrated multi-hop KG reasoning within a single inference round. Rather than reasoning step-by-step, we train a Reasoning LLM that embeds the entire KG traversal and decision process into a unified ``thinking'' stage, enabling global reasoning over cross-step dependencies and dynamic path exploration with backtracking. Experimental results on eight KG reasoning benchmarks show that KG-Hopper, based on a 7B-parameter LLM, consistently outperforms larger multi-step systems (up to 70B) and achieves competitive performance with proprietary models such as GPT-3.5-Turbo and GPT-4o-mini, while remaining compact, open, and data-efficient. The code is publicly available at: https://github.com/Wangshuaiia/KG-Hopper.

---

## 论文详细总结（自动生成）

这是一份关于论文《KG-Hopper: Empowering Compact Open LLMs with Knowledge Graph Reasoning via Reinforcement Learning》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLMs）在处理知识密集型任务时容易产生幻觉。虽然知识图谱问答（KBQA）能提供结构化事实，但现有的多步推理方法（Multi-step pipelines）存在**灵活性差**（难以回溯）、**错误级联**（前步错步步错）和**局部最优**（忽略跨步骤依赖）等问题。
*   **研究背景**：受 OpenAI o1 和 DeepSeek-R1 等推理模型的启发，作者希望通过强化学习（RL）让小型开源模型在单次推理的“思考（Thinking）”阶段内，自主完成知识图谱的遍历、决策与回溯，从而实现更稳健的多跳推理。

### 2. 方法论
*   **核心思想**：将多跳 KG 推理过程整合进 LLM 的单一推理周期中。模型在 `<think>` 标签内自主调用搜索工具、分析返回的三元组、修正路径，最后在 `<answer>` 标签中给出答案。
*   **关键技术细节**：
    *   **两阶段训练**：
        1.  **冷启动（SFT）**：使用 500 条高质量的思维链（CoT）数据进行微调，教会模型基本的工具调用格式（如 `<search>` 标签）。
        2.  **推理导向的强化学习（RL）**：采用 **GRPO（Group Relative Policy Optimization）** 算法进行大规模优化。
    *   **奖励函数设计 ($R_{final}$)**：
        *   **检索奖励 ($R_{search}$)**：鼓励模型使用工具，但设有上限以防过度检索。
        *   **格式奖励 ($R_{format}$)**：确保模型遵循特定的标签结构。
        *   **推理奖励 ($R_{reason}$)**：使用外部大模型（如 Llama-3.3-70B）评估 `<think>` 内部逻辑的合理性。
        *   **答案奖励 ($R_{answer}$)**：评估最终答案与真实值的语义一致性。
    *   **优化策略**：
        *   **三元组掩码**：在计算损失时掩盖检索到的 KG 内容，防止模型死记硬背外部知识。
        *   **历史重采样（History Resampling）**：在训练后期移除简单的单跳问题，强迫模型专注于复杂的多跳推理（课程学习思想）。

### 3. 实验设计
*   **数据集**：涵盖 8 个主流基准测试，涉及 Freebase 和 WikiData 两个大型知识图谱。
    *   **Freebase**: CWQ, WebQSP, WebQuestions, GrailQA。
    *   **WikiData**: QALD10-en, T-REx, Zero-Shot RE, Creak（事实核查）。
*   **对比方法**：
    *   **Prompt-only**: Qwen-2.5-7B, Llama-3.1-8B, Llama-3.3-70B, GPT-4o/mini。
    *   **KG 增强型（无微调）**: 上述模型配合检索工具。
    *   **SFT 增强型**: 如 Interactive-KBQA, KG-CoT。
*   **评价指标**：Hits@1 和准确率（Accuracy）。

### 4. 资源与算力
*   **算力设备**：使用了 **8 台 NVIDIA A100 80G GPU**。
*   **训练配置**：训练 2 个 Epoch，Batch Size 为 16，学习率为 1e-6。
*   **采样细节**：每个查询生成 16 个 Rollouts（采样轨迹）用于 GRPO 优化。

### 5. 实验数量与充分性
*   **实验规模**：在 8 个不同性质的数据集上进行了测试，涵盖了问答、槽位填充和事实核查任务。
*   **消融实验**：
    *   对比了 RL 与 SFT 的效果（证明 RL 提升了 4%-10%）。
    *   分析了四种奖励组件的贡献（证明推理奖励和答案奖励最关键）。
    *   验证了在无标准答案（Ground Truth）情况下，使用 LLM 作为裁判的可行性。
    *   验证了历史重采样对提升推理深度的有效性。
*   **评价**：实验设计全面，对比了从 7B 到 70B 的多种开源模型及顶级闭源模型，结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **以小博大**：基于 7B 参数的 KG-Hopper 在多跳推理任务上一致优于 70B 的多步推理系统。
*   **媲美闭源**：其性能达到了与 GPT-3.5-Turbo 和 GPT-4o-mini 相当的水平。
*   **RL 的优越性**：相比 SFT，RL 能显著减少模型对训练模式的机械模仿，增强其在面对缺失知识时的动态调整和回溯能力。
*   **单轮推理优势**：将推理嵌入“思考”阶段能有效捕捉跨步骤的依赖关系，减少错误累积。

### 7. 优点
*   **高效性**：通过单次 LLM 调用完成多跳推理，减少了多次 API 调用带来的延迟和复杂编排。
*   **鲁棒性**：具备自主回溯能力，当 KG 路径缺失时能利用内部知识补全。
*   **数据效率**：仅需少量冷启动数据，主要通过 RL 的标量奖励信号进行能力进化。

### 8. 不足与局限
*   **奖励模型依赖**：推理奖励依赖于更强大的外部模型（如 70B 模型），这在训练阶段增加了成本和潜在的偏见传递。
*   **检索限制**：目前的检索工具基于两阶段过滤，对于极度稀疏或模式极其复杂的 KG 结构，可能仍存在检索瓶颈。
*   **推理深度**：虽然在多跳任务上表现优异，但对于需要极长链条（如 5 跳以上）的极端复杂推理，其性能表现尚未在文中深入探讨。

（完）
