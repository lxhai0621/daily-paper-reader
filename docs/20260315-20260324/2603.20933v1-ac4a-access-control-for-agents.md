---
title: "AC4A: Access Control for Agents"
title_zh: AC4A：智能体的访问控制
authors: "Reshabh K Sharma, Dan Grossman"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20933v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 智能体与外部API交互的访问控制框架
tldr: 针对当前大语言模型智能体在调用API和访问网页时缺乏细粒度权限控制的问题，本文提出了AC4A框架。该框架借鉴Unix文件系统的权限管理理念，允许开发者为智能体定义层级化的资源访问权限，从而避免“全有或全无”的过度授权。AC4A支持API和浏览器两类智能体，通过在运行时动态验证权限，实现了对敏感资源的安全隔离。实验案例表明，该框架能有效限制智能体的操作范围，为构建更安全、可信的自主智能体系统提供了实用方案。
selection_source: fresh_fetch
motivation: 当前智能体系统对API和网页的访问权限过于粗粒度，迫使用户必须过度信任智能体，存在严重的安全隐患。
method: 提出AC4A框架，借鉴Unix文件系统权限模型，通过构建资源层级结构并在运行时动态计算与强制执行细粒度权限。
result: 通过真实API和网页的案例研究，验证了AC4A在限制智能体越权访问和保护敏感数据方面的有效性。
conclusion: AC4A为API和浏览器智能体提供了一种灵活且实用的访问控制方案，显著增强了自主系统的安全性和可控性。
---

## 摘要
大语言模型（LLM）智能体结合了 LLM 的对话交互能力与同外部工具及 API 交互的能力。这使得它们能够执行复杂的任务并自主行动以实现用户目标。然而，当前的智能体系统以“全有或全无”的方式运行：智能体要么拥有对 API 功能和网页内容的完全访问权限，要么完全没有访问权限。这种粗粒度的方法迫使用户信任智能体，赋予其超出特定任务实际所需的能力。在本文中，我们介绍了 AC4A，一个针对智能体的访问控制框架。随着智能体变得更加强大和自主，用户需要一种方法来限制这些智能体可以访问哪些 API 或网页的部分内容，从而消除对 API 或网页所允许的所有内容都必须信任智能体的需求。我们设计 AC4A 的目标是提供一个定义权限的框架，让智能体仅能访问其获授权访问的资源。AC4A 适用于基于 API 和基于浏览器的智能体。它不规定权限的具体内容，而是提供了一种灵活的方式来定义和执行权限，使其在现实系统中具有实用性。AC4A 通过创建授予资源访问权限的许可来工作，其灵感源自成熟的访问控制框架（如 Unix 文件系统）。应用程序将其资源定义为层级结构，并提供一种在运行时计算成功访问资源所需权限的方法。我们通过案例研究展示了 AC4A 在现实 API 和网页上执行权限的有效性。AC4A 的源代码可在 https://github.com/reSHARMA/AC4A 获取。

## Abstract
Large Language Model (LLM) agents combine the chat interaction capabilities of LLMs with the power to interact with external tools and APIs. This enables them to perform complex tasks and act autonomously to achieve user goals. However, current agent systems operate on an all-or-nothing basis: an agent either has full access to an API's capabilities and a web page's content, or it has no access at all. This coarse-grained approach forces users to trust agents with more capabilities than they actually need for a given task.   In this paper, we introduce AC4A, an access control framework for agents. As agents become more capable and autonomous, users need a way to limit what APIs or portions of web pages these agents can access, eliminating the need to trust them with everything an API or web page allows. Our goal with AC4A is to provide a framework for defining permissions that lets agents access only the resources they are authorized to access. AC4A works across both API-based and browser-based agents. It does not prescribe what permissions should be, but offers a flexible way to define and enforce them, making it practical for real-world systems.   AC4A works by creating permissions granting access to resources, drawing inspiration from established access control frameworks like the one for the Unix file system. Applications define their resources as hierarchies and provide a way to compute the necessary permissions at runtime needed for successful resource access. We demonstrate the usefulness of AC4A in enforcing permissions over real-world APIs and web pages through case studies. The source code of AC4A is available at https://github.com/reSHARMA/AC4A

---

## 论文详细总结（自动生成）

### AC4A：智能体访问控制框架论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）智能体在调用外部 API 和操作浏览器方面的能力日益增强，其安全性问题愈发凸显。目前智能体系统普遍采用“全有或全无”（All-or-nothing）的粗粒度授权模式。例如，为了让智能体在日历中创建一个会议，用户必须授予其访问整个日历 API 的权限，这使得智能体有能力删除或篡改其他无关的日程。
**研究动机**：现有的安全研究多集中在防御幻觉、提示词注入等攻击，而缺乏一个基础性的访问控制框架。AC4A 旨在填补这一空白，通过提供细粒度的、基于资源的权限管理，确保智能体“最小特权原则”的实现。

#### 2. 方法论：核心思想与关键技术
AC4A 借鉴了 Unix 文件系统的权限模型，核心思想是将权限定义在“资源”而非“工具调用”上。
*   **资源类型树（Resource Type Trees）**：将应用资源定义为层级结构（如 `年::月::日` 或 `目录::文件`）。父节点的权限通常隐含子节点的权限。
*   **权限表示**：权限由“资源值规范”（Resource Value Specification）和“操作”（Action，如 read, write, create）组成。支持通配符（?）来表示某一层级的所有资源。
*   **权限检查算法（Algorithm 1）**：
    *   当智能体尝试访问资源时，系统计算“所需权限”（Need）与“已有权限”（Have）之间的差异。
    *   通过应用定义的 `resource_difference` 函数，迭代减去已获授权的部分。若剩余需求（Remaining）为空，则允许访问；否则拒绝。
*   **API 与网页的统一强制执行**：
    *   **API 模式**：通过“权限函数”拦截 API 调用，根据参数动态计算所需资源（如根据日期参数确定访问的具体月份）。
    *   **浏览器模式**：将 CSS 选择器映射到资源。AC4A 会在智能体看到网页前，通过 CSS 遮罩（Overlay）隐藏未授权的 DOM 元素。

#### 3. 实验设计
由于智能体访问控制领域缺乏标准化的 Benchmark，论文采用了**案例研究（Case Studies）**的方法：
*   **场景一：井字棋游戏**。用于演示核心组件的端到端运行，包括如何定义资源树、操作以及如何拦截 API 和网页上的删除/查看请求。
*   **场景二：复杂航班预订任务**。涉及 Outlook 日历、Expedia 旅游服务和支付钱包三个真实应用的模拟。
*   **对比与基准**：实验主要对比了“无权限控制”与“AC4A 介入”下的智能体行为，验证其是否能准确拦截越权操作（如尝试读取未授权月份的日历或访问信用卡信息）。

#### 4. 资源与算力
*   **模型使用**：实验使用了 **GPT-4o-mini**（文中称为 GPT-5.4-mini，可能为特定版本或占位符称呼）作为底层 LLM。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。由于 AC4A 是一个推理阶段的框架，不涉及模型重训练，其算力消耗主要集中在 LLM 的推理和 DOM 元素的实时解析上。

#### 5. 实验数量与充分性
*   **实验规模**：论文详细展示了两大类案例，涵盖了 API 调用和浏览器交互两种模式。
*   **充分性评价**：
    *   **定性分析充分**：通过具体的步骤（如 Scenario A/B/C）展示了权限边界的强制执行，逻辑清晰。
    *   **定量分析缺失**：缺乏大规模自动化测试或针对不同 LLM 模型的鲁棒性测试。
    *   **客观性**：实验设计较为客观，展示了在不同权限配置下智能体的失败与成功路径，但由于是案例研究，其覆盖面受限于所选的应用场景。

#### 6. 主要结论与发现
*   **资源中心化优于工具中心化**：将权限绑定在资源（数据）上比绑定在工具（函数）上更有效，因为同一资源可能通过不同工具或 UI 元素被访问。
*   **跨平台一致性**：AC4A 证明了可以使用同一套权限逻辑同时管理 API 智能体和浏览器智能体。
*   **交互模式的灵活性**：提出了 Ask（询问）、Skip（跳过）、Infer（推断）和 YOLO（自动授权）四种处理权限拒绝的模式，平衡了安全与便捷。

#### 7. 优点
*   **模型统一**：首次实现了 API 和浏览器智能体权限强制执行的统一。
*   **细粒度控制**：支持动态参数解析，能够实现“仅允许访问 2026 年 6 月的日历”这种极细粒度的控制。
*   **非侵入性**：对于浏览器智能体，无需修改网站后端，通过前端 DOM 拦截即可实现。
*   **可视化管理**：提供了直观的仪表盘，方便用户查看、授予和撤销权限。

#### 8. 不足与局限
*   **维护成本高**：需要人工编写 API 的权限函数和网页的 CSS 映射文件。如果网页结构发生变化（DOM 更新），映射可能失效。
*   **策略管理缺失**：目前权限是持久性的，缺乏生命周期管理（如单次使用后失效或定时过期）。
*   **复杂逻辑表达受限**：难以处理复杂的条件权限（例如：仅当金额小于 200 美元时允许支付）。
*   **性能开销**：在复杂网页上实时解析 DOM 并应用权限检查可能会引入一定的延迟。

（完）
