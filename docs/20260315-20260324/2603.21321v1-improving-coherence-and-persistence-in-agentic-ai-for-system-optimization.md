---
title: Improving Coherence and Persistence in Agentic AI for System Optimization
title_zh: 提升用于系统优化的代理式 AI 的连贯性与持久性
authors: "Pantea Karimi, Kimia Noorbakhsh, Mohammad Alizadeh, Hari Balakrishnan"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21321v1"
tags: ["query:agent"]
score: 9.0
evidence: 解决上下文退化和知识积累问题的智能体研究架构
tldr: 针对系统优化中LLM面临的演化偏差和连贯性瓶颈，本文提出Engram架构。该架构通过解耦长程探索与上下文窗口限制，利用持久化存档和研究摘要实现知识积累。Engram在多云组播、LLM路由及数据库缓存优化等领域表现优异，显著提升了智能体在复杂系统设计中的持久性和连贯性。
selection_source: fresh_fetch
motivation: 现有的LLM智能体在处理复杂系统优化时，容易陷入局部最优且难以在长周期任务中保持上下文连贯性并积累知识。
method: 提出Engram架构，通过持久化存储代码快照和提炼高层研究摘要，使后续智能体能跨运行周期继承并深化先前的发现。
result: 在多云组播、LLM推理请求路由和数据库KV缓存重用等多样化领域中，Engram均展现出超越现有方法的卓越性能。
conclusion: 通过解耦探索过程与上下文限制并引入持久化知识管理，可以有效提升AI智能体在复杂系统启发式算法设计中的效率和深度。
---

## 摘要
设计高性能系统启发式算法是一个创造性的迭代过程，需要专家提出假设并执行多步概念转变。虽然大语言模型（LLMs）在自动化这一循环方面展现出潜力，但由于两个关键的失效模式，它们在处理复杂系统问题时仍面临困难：进化邻域偏差（evolutionary neighborhood bias）和连贯性上限（coherence ceiling）。进化方法通常依赖标量基准分数，容易陷入局部最优，在需要协调的多步变更时往往会失败。相反，现有的代理（agentic）框架在长周期内会遭遇上下文退化，或者无法在独立的运行之间积累知识。我们提出了 Engram，这是一种代理研究员架构，通过将长周期探索与单一上下文窗口的限制解耦，解决了上述局限性。Engram 将探索组织为一系列代理，这些代理迭代地设计、测试并分析机制。在每次运行结束时，代理会将代码快照、日志和结果存储在持久化存档（Archive）中，并将高层建模见解提炼为简洁、持久的研究摘要（Research Digest）。随后的代理将从全新的上下文窗口开始，通过阅读研究摘要来基于先前的发现进行构建。我们发现 Engram 在多个领域表现出优异的性能，包括多云组播（multi-cloud multicast）、LLM 推理请求路由，以及自然语言查询数据库中的 KV 缓存重用优化。

## Abstract
Designing high-performance system heuristics is a creative, iterative process requiring experts to form hypotheses and execute multi-step conceptual shifts. While Large Language Models (LLMs) show promise in automating this loop, they struggle with complex system problems due to two critical failure modes: evolutionary neighborhood bias and the coherence ceiling. Evolutionary methods often remain trapped in local optima by relying on scalar benchmark scores, failing when coordinated multi-step changes are required. Conversely, existing agentic frameworks suffer from context degradation over long horizons or fail to accumulate knowledge across independent runs.   We present Engram, an agentic researcher architecture that addresses these limitations by decoupling long-horizon exploration from the constraints of a single context window. Engram organizes exploration into a sequence of agents that iteratively design, test, and analyze mechanisms. At the conclusion of each run, an agent stores code snapshots, logs, and results in a persistent Archive and distills high-level modeling insights into a compact, persistent Research Digest. Subsequent agents then begin with a fresh context window, reading the Research Digest to build on prior discoveries.   We find that Engram exhibits superior performance across diverse domains including multi-cloud multicast, LLM inference request routing, and optimizing KV cache reuse in databases with natural language queries.

---

## 论文详细总结（自动生成）

这是一份关于论文《Improving Coherence and Persistence in Agentic AI for System Optimization》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）在自动化设计高性能系统启发式算法时，难以处理复杂的、需要多步概念转变的任务。
*   **研究动机**：作者识别出当前方法的两个关键失效模式：
    1.  **进化邻域偏差（Evolutionary Neighborhood Bias）**：基于代码进化的系统（如 OpenEvolve）依赖标量分数进行筛选，容易陷入局部最优，无法执行需要暂时降低分数但能带来长远突破的架构级重构。
    2.  **连贯性上限（Coherence Ceiling）**：单智能体长程探索会遭遇“上下文腐烂（Context Rot）”，导致注意力不均；而独立的多次运行（Best-of-N）则无法积累知识，导致重复发现。

### 2. 方法论：Engram 架构
*   **核心思想**：将长程探索过程与单一上下文窗口的限制解耦，通过“智能体接力”和“知识持久化”实现跨周期的连贯研究。
*   **关键技术细节**：
    *   **智能体序列（Sequence of Agents）**：将探索任务分解给一系列连续的智能体。每个智能体在全新的上下文窗口中启动，避免了长对话导致的性能下降。
    *   **研究摘要（Research Digest）**：这是 Engram 的核心。每个智能体在结束任务前，会将高层建模见解、失败诊断和后续建议提炼成简洁的摘要。后续智能体通过阅读摘要继承前人的智慧。
    *   **持久化存档（Archive）**：存储所有历史代码快照、执行日志和实验结果。智能体可根据摘要指引，通过工具调用按需检索存档中的详细信息。
    *   **科学探索循环**：每个智能体遵循“假设 → 实现 → 实验 → 分析”的循环，不仅生成代码，还通过分析实验数据来验证假设。

### 3. 实验设计
*   **实验场景**：
    1.  **多云组播（Multi-cloud Multicast）**：优化跨云数据传输的成本和时间。
    2.  **LLM 推理请求路由**：在分布式服务系统中降低平均响应时间（RT）。
    3.  **数据库 KV 缓存重用**：优化自然语言查询下的 SQL 数据处理效率。
    4.  **ADRS 基准测试**：包含 Telemetry、EPLB、Prism 等 6 个标准的系统优化任务。
*   **对比方法**：
    *   **进化算法类**：EoH (Evolution of Heuristics), FunSearch, OpenEvolve。
    *   **迭代设计类**：Glia（基于推理的单智能体框架）。
    *   **人类专家**：各领域的 State-of-the-art (SOTA) 人工设计算法。

### 4. 资源与算力
*   **模型使用**：主要使用了 **OpenAI o3** 和 **gpt-5.2**（注：论文标注日期为 2026 年，文中提到的 gpt-5.2 可能是指该时间点的先进模型或内部版本）。
*   **模拟环境**：LLM 路由实验使用了 Vidur 模拟器，运行在 4 个 NVIDIA A10 GPU 上。
*   **算力消耗**：文中未详细列出总的 GPU 小时数，但提到每个方法运行 10 次，每次运行预算为 100 次模拟评估。

### 5. 实验数量与充分性
*   **实验规模**：在 9 个不同的系统问题上进行了评估。每个设置重复 10 次实验，并报告了 90% 的置信区间。
*   **消融实验**：非常充分。作者分别测试了“无研究摘要”、“无存档”、“单智能体总结”以及“仅代码接力”等变体，证明了研究摘要在引导长程探索中的核心作用。
*   **客观性**：通过对比多种不同范式的 SOTA 方法，并使用标准化的 ADRS 基准，实验设计显得较为客观和公平。

### 6. 主要结论与发现
*   **超越人类专家**：Engram 在 9 个任务中的 8 个表现优于人类 SOTA。例如在多云组播中，Engram 发现了基于动态规划（DP）和混合整数线性规划（MILP）的新算法，成本低于人类设计的 Cloudcast。
*   **突破局部最优**：相比进化算法，Engram 能够忍受中间过程的性能下降（如为了引入更复杂的优化器而导致的暂时性失败），最终实现质的突破。
*   **高效的知识传递**：研究摘要显著提高了探索效率，使后续智能体无需从零开始，能够快速收敛到高性能方案。

### 7. 优点
*   **架构创新**：成功解决了 LLM 智能体在长周期任务中“记不住”和“想不深”的矛盾。
*   **定性分析深入**：论文不仅展示了分数提升，还深入分析了智能体生成的代码逻辑（如从 Steiner 树转向 MILP），证明了 AI 具备执行复杂概念转变的能力。
*   **鲁棒性**：对提示词（Prompt）的敏感度低于进化方法，表现出更强的泛化能力。

### 8. 不足与局限
*   **模型依赖性**：Engram 的成功高度依赖于底层模型（如 o3/gpt-5.2）的推理和总结能力，在较弱的模型上效果可能大打折扣。
*   **成本风险**：虽然通过接力刷新了上下文，但多次调用高性能 LLM 进行长程探索的 API 成本可能较高。
*   **摘要质量瓶颈**：如果前序智能体生成了错误的或具有误导性的研究摘要，可能会导致后续智能体误入歧途（尽管存档机制可以部分缓解此问题）。

（完）
