---
title: "TAMTRL: Teacher-Aligned Reward Reshaping for Multi-Turn Reinforcement Learning in Long-Context Compression"
title_zh: TAMTRL：长上下文压缩中多轮强化学习的教师对齐奖励重塑
authors: "Li Wang, Yandong Wang, Xin Yu, Kui Zhang, Tianhao Peng, Wenjun Wu"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21663v1"
tags: ["query:agent"]
score: 9.0
evidence: 长文本处理中记忆更新的强化学习方法
tldr: 针对大语言模型在处理长文本时面临的上下文窗口限制，本文提出TAMTRL框架。该方法针对多轮内存更新中的信用分配难题，利用相关文档作为教师信号，通过自监督方式将教师信号与每轮输入对齐并重塑奖励。实验证明，TAMTRL在多个长文本基准测试中显著优于现有基准，有效提升了长上下文压缩与处理能力。
selection_source: fresh_fetch
motivation: 现有的长文本分块处理方法仅在最终输出时提供监督，导致多轮内存更新过程中难以准确评估每一步的质量。
method: 提出TAMTRL，通过将相关文档作为教师信号与每轮输入对齐，并利用归一化概率进行自监督奖励重塑。
result: 在七个长文本基准测试和不同规模的模型上，TAMTRL均表现出优于强基准模型的性能。
conclusion: TAMTRL通过细粒度的学习信号解决了多轮强化学习中的信用分配问题，显著增强了模型对长上下文的理解与压缩能力。
---

## 摘要
大语言模型（LLMs）的飞速发展在广泛的任务中带来了显著的性能提升。然而，在处理超出模型上下文窗口限制的长文档时，无法在单次处理中完成整个上下文，因此必须进行分块处理。这需要通过多轮操作来读取不同的分块并更新记忆。然而，监督信号通常仅由最终结果提供，这使得在多轮训练设置中难以评估每一轮记忆更新的质量。这引入了时间信用分配（temporal credit assignment）的挑战。现有方法（如 LLM-as-a-judge 或过程奖励模型）会产生巨大的计算开销，并受到估计噪声的影响。为了更好地解决多轮记忆训练中的信用分配问题，我们提出了面向多轮强化学习的教师对齐奖励重塑（TAMTRL）。TAMTRL 通过将相关文档与每一轮模型输入相对齐，将其作为教师信号，并以自监督的方式通过归一化概率分配奖励。这为每一次记忆更新提供了细粒度的学习信号，并改进了长上下文处理。在七个长上下文基准测试中，对不同规模的多个模型进行的实验表明，TAMTRL 一致优于强基准模型，证明了其有效性。我们的代码可在 https://anonymous.4open.science/r/TAMTRL-F1F8 获取。

## Abstract
The rapid progress of large language models (LLMs) has led to remarkable performance gains across a wide range of tasks. However, when handling long documents that exceed the model's context window limit, the entire context cannot be processed in a single pass, making chunk-wise processing necessary. This requires multiple turns to read different chunks and update memory. However, supervision is typically provided only by the final outcome, which makes it difficult to evaluate the quality of memory updates at each turn in the multi-turn training setting. This introduces a temporal credit assignment challenge. Existing approaches, such as LLM-as-a-judge or process reward models, incur substantial computational overhead and suffer from estimation noise. To better address the credit assignment problem in multi-turn memory training, we propose Teacher-Aligned Reward Reshaping for Multi-Turn Reinforcement Learning (TAMTRL). TAMTRL leverages relevant documents as teacher signals by aligning them with each turn of model input and assigns rewards through normalized probabilities in a self-supervised manner. This provides fine-grained learning signals for each memory update and improves long-context processing. Experiments with multiple models of varying scales across seven long-context benchmarks show that TAMTRL consistently outperforms strong baselines, demonstrating its effectiveness. Our code is available at https://anonymous.4open.science/r/TAMTRL-F1F8.

---

## 论文详细总结（自动生成）

这是一份关于论文《TAMTRL: Teacher-Aligned Reward Reshaping for Multi-Turn Reinforcement Learning in Long-Context Compression》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：大语言模型（LLM）在处理超长文档时受限于上下文窗口，通常需要采用“分块处理+记忆更新”的多轮处理模式。
*   **核心问题**：在多轮强化学习（RL）训练中，监督信号通常仅由最终结果（Outcome Reward）提供。这种“结果导向”的监督难以评估中间每一轮记忆更新的质量，导致**时间信用分配（Temporal Credit Assignment）**难题：即错误的中间更新可能因最终答案正确而被奖励，或者正确的更新因最终答案错误而被惩罚。
*   **整体含义**：论文旨在通过一种无需外部昂贵评估器（如GPT-4或专门的PRM模型）的方法，为多轮记忆更新提供细粒度的中间奖励，从而提升模型处理长文本的能力。

### 2. 论文提出的方法论：TAMTRL
*   **核心思想**：借鉴强化学习中的“中心化训练，去中心化执行（CTDE）”思想，利用一个拥有“全局/干净视角”的**教师模型**来指导只有“局部/噪声视角”的**学生模型**。
*   **关键技术细节**：
    1.  **问题建模**：将长文档处理建模为部分可观测马尔可夫决策过程（POMDP）。
    2.  **教师对齐奖励重塑**：
        *   **教师信号**：在训练阶段，利用文档的地面真值（Ground-truth）提取出与问题相关的“干净分块”。
        *   **概率评分**：教师模型（与学生模型同参数，但输入的是干净分块）计算学生模型生成的记忆序列的平均 Token 概率。
        *   **归一化**：对概率评分进行 Min-Max 归一化，以稳定训练梯度。
    3.  **奖励分配**：最终的每轮奖励 $R = \text{归一化教师评分} \times \text{最终结果奖励（0或1）}$。这确保了只有最终正确的轨迹才会获得中间奖励，且奖励大小取决于与教师视角的对齐程度。
    4.  **优化算法**：采用 DAPO（Decoupled Clip and Dynamic sampling Policy Optimization）算法进行策略优化。

### 3. 实验设计
*   **数据集/场景**：
    *   **训练集**：基于 HotpotQA 构建的合成长文本数据集（包含干扰段落）。
    *   **测试集（7个基准）**：HotpotQA (ID), RULER-QA, NIAH (多键大海捞针), 以及 LongBench-QA 中的四个子集（NarrativeQA, Qasper, 2Wiki-MultihopQA, MuSiQue）。
*   **对比方法（Baselines）**：
    *   **蒸馏类**：SFT, STaR, Vanilla-KD。
    *   **强化学习类**：MemAgent（仅结果奖励）。
    *   **过程监督类**：LLM-judge（使用外部模型评分）, PRM（过程奖励模型）。

### 4. 资源与算力
*   **硬件环境**：使用 8 张 NVIDIA A100 (80GB) GPU。
*   **训练时长**：以 Qwen3-0.6B 为例，TAMTRL 的总训练时间约为 **33.79 小时**（比 MemAgent 略快，因为生成的响应更短且更高效）。
*   **软件框架**：基于 Verl 和 MemAgent 框架实现，推理加速使用 vLLM。

### 5. 实验数量与充分性
*   **实验规模**：在 0.6B 和 1.7B 两种参数规模的 Qwen3 模型上进行了验证。
*   **实验维度**：
    *   **主实验**：跨 7 个长文本基准的性能对比。
    *   **消融实验**：验证了长度归一化和 Min-Max 归一化的必要性。
    *   **敏感性分析**：探讨了不同奖励设计（Global vs Plus）、文档数量（信息密度）、分块大小（Chunk Size）以及训练数据比例对性能的影响。
*   **充分性评价**：实验设计非常全面，涵盖了从 ID 到 OOD 任务的迁移能力，且通过理论分解（定理1）证明了目标函数的合理性，实验结果具有较高的客观性和说服力。

### 6. 论文的主要结论与发现
*   **性能领先**：TAMTRL 在所有基准测试中均优于强基准模型，在 0.6B 和 1.7B 模型上分别实现了 1.87% 和 2.02% 的平均性能提升。
*   **信用分配有效性**：通过教师对齐的概率评分，模型能更准确地识别并保留关键信息，减少了多轮处理中的噪声传播。
*   **泛化能力**：尽管训练时使用了带标注的“干净文档”，但模型在测试时能够很好地泛化到无标注的原始长文档中。
*   **效率优势**：相比于需要外部模型调用的 LLM-judge 或需要额外训练数据的 PRM，TAMTRL 在计算开销更低的情况下取得了更好的效果。

### 7. 优点
*   **自监督特性**：利用模型自身的概率分布作为教师信号，无需依赖昂贵的外部大模型（如 GPT-4）作为裁判。
*   **低开销**：奖励计算仅需一次前向传播，无需自回归生成，显著降低了训练成本。
*   **理论支撑**：提供了信息论视角的公式分解，证明了该方法在鼓励教师对齐的同时保持了策略的正则化。
*   **鲁棒性**：在不同的分块大小和文档密度下均表现稳定。

### 8. 不足与局限
*   **训练依赖标注**：虽然测试不需要，但训练阶段仍依赖于能够区分“相关/不相关”文档的标注，这在某些完全无监督的场景下可能受限。
*   **模型规模限制**：实验主要集中在较小规模的模型（0.6B, 1.7B），尚未在百亿或千亿级参数模型上验证其扩展性（Scaling Law）。
*   **任务类型**：主要侧重于信息提取和多跳问答类长文本任务，对于长文本摘要或创意写作等任务的适用性有待进一步验证。

（完）
