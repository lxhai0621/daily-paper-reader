---
title: "Semantic Tool Discovery for Large Language Models: A Vector-Based Approach to MCP Tool Selection"
title_zh: 大语言模型的语义工具发现：一种基于向量的 MCP 工具选择方法
authors: "Sarat Mudunuri, Jian Wan, Ally Qin, Srinivasan Manoharan"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.20313v1"
tags: ["query:ma-kf"]
score: 10.0
evidence: 用于LLM集成外部API的语义工具发现
tldr: 针对大语言模型在调用MCP协议工具时面临的Token开销大、准确率下降等扩展性挑战，本文提出了一种基于向量检索的语义工具发现架构。该方法通过对工具功能进行稠密向量索引，根据用户意图动态筛选最相关的少量工具（如3-5个），显著降低了上下文负载。实验表明，该方案在保持高准确率的同时，大幅减少了Token消耗并提升了检索效率，为大规模工具集成提供了可行路径。
selection_source: fresh_fetch
motivation: 现有的MCP工具调用方式将所有工具放入上下文，导致Token开销巨大、成本高昂且模型准确率受限。
method: 提出一种基于向量检索的语义索引框架，利用稠密嵌入捕捉工具功能与用户意图的语义关系，实现动态工具筛选。
result: "在包含121个工具的基准测试中，该方法减少了99.6%的Token消耗，K=3时的命中率达97.1%，且检索延迟低于100ms。"
conclusion: 语义工具发现架构有效解决了大规模工具集的扩展性问题，为多智能体和跨组织工具集成提供了高效、精准的解决方案。
---

## 摘要
具备工具调用能力的大语言模型（LLM）通过集成外部工具，在执行复杂任务方面展现出巨大潜力。模型上下文协议（MCP）已成为连接 LLM 与多样化工具集的标准化框架，单个 MCP 服务器可能会提供数十到数百个工具。然而，当前的实现面临着严峻的可扩展性挑战：将所有可用工具提供给 LLM 上下文会导致大量的 Token 开销、成本增加、准确率下降以及上下文窗口限制。我们提出了一种语义工具发现架构，通过基于向量的检索来解决这些挑战。我们的方法使用稠密嵌入对 MCP 工具进行索引，捕捉工具能力与用户意图之间的语义关系，从而动态选择最相关的工具（通常为 3-5 个），而非暴露整个工具目录（50-100+ 个）。实验结果表明，在涵盖 5 个 MCP 服务器、121 个工具和 140 个查询的基准测试中，该方法将工具相关的 Token 消耗降低了 99.6%，在 K=3 时的命中率达到 97.1%，平均倒数排名（MRR）为 0.91，且检索延迟低于 100 毫秒。主要贡献包括：(1) 一种 MCP 工具的语义索引框架；(2) 一种基于查询-工具相似度的动态工具选择算法；(3) 证明了效率和准确性显著提升的全面评估；(4) 对多智能体和跨组织工具发现的可扩展性。

## Abstract
Large Language Models (LLMs) with tool-calling capabilities have demonstrated remarkable potential in executing complex tasks through external tool integration. The Model Context Protocol (MCP) has emerged as a standardized framework for connecting LLMs to diverse toolsets, with individual MCP servers potentially exposing dozens to hundreds of tools. However, current implementations face a critical scalability challenge: providing all available tools to the LLM context results in substantial token overhead, increased costs, reduced accuracy, and context window constraints.   We present a semantic tool discovery architecture that addresses these challenges through vector-based retrieval. Our approach indexes MCP tools using dense embeddings that capture semantic relationships between tool capabilities and user intent, dynamically selecting only the most relevant tools (typically 3-5) rather than exposing the entire tool catalog (50-100+).   Experimental results demonstrate a 99.6% reduction in tool-related token consumption with a hit rate of 97.1% at K=3 and an MRR of 0.91 on a benchmark of 140 queries across 121 tools from 5 MCP servers, with sub-100ms retrieval latency. Contributions include: (1) a semantic indexing framework for MCP tools, (2) a dynamic tool selection algorithm based on query-tool similarity, (3) comprehensive evaluation demonstrating significant efficiency and accuracy improvements, and (4) extensibility to multi-agent and cross-organizational tool discovery.

---

## 论文详细总结（自动生成）

这是一份关于论文《Semantic Tool Discovery for Large Language Models: A Vector-Based Approach to MCP Tool Selection》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：随着模型上下文协议（MCP）的普及，LLM 可以访问的外部工具数量激增（单个服务器可达数百个）。现有的“静态提供”方式（将所有工具 Schema 放入 Prompt）面临四大挑战：
    1.  **Token 开销巨大**：每个工具 Schema 约 200-800 Tokens，百个工具即可耗尽数万 Tokens。
    2.  **成本高昂**：大规模调用时，冗余的工具定义会导致极高的 API 费用。
    3.  **准确率下降**：过多的无关工具会产生“认知负荷”，导致 LLM 在工具选择时出现幻觉或错误。
    4.  **上下文限制**：工具定义挤占了对话历史和 RAG 文档的存储空间。
*   **研究背景**：论文旨在为 MCP 生态系统引入一个“语义发现层”，通过动态筛选最相关的工具来解决可扩展性瓶颈。

### 2. 论文提出的方法论
核心思想是借鉴 **RAG（检索增强生成）** 的逻辑，将工具检索视为一种特殊的语义搜索任务。
*   **工具索引流程**：
    1.  **Schema 提取**：从 MCP 服务器获取工具名称、描述、参数及约束。
    2.  **文档构建**：将结构化 Schema 转换为富文本描述（包含 Purpose, Capabilities, Parameters 等锚点）。
    3.  **向量化与存储**：使用稠密嵌入模型（如 `text-embedding-ada-002`）生成向量，存入 Milvus 向量数据库。
*   **动态筛选流程**：
    1.  **查询嵌入**：将用户自然语言查询转化为向量。
    2.  **相似度搜索**：在向量空间执行点积（Dot Product）计算，检索 Top-K 个最相似的工具。
    3.  **重排序与注入**：可选重排序步骤后，仅将选定的 K 个工具（通常为 3-5 个）注入 LLM 上下文。
*   **反馈循环**：收集工具调用成功率和用户反馈，用于后续微调嵌入或调整检索参数。

### 3. 实验设计
*   **数据集与场景**：构建了一个包含 **121 个工具** 的基准测试，涵盖 5 个不同领域的 MCP 服务器：
    *   Filesystem（文件操作）、MySQL（数据库）、Slack（通讯）、GitHub（代码托管）、Time/Weather（实时信息）。
*   **Benchmark**：设计了 **140 个查询**，分为简单事实类、任务导向类、模糊类及边缘情况（如无需工具或需多工具）。
*   **对比方法**：主要对比了不同 **K 值**（1, 2, 3, 5, 10）下的检索表现，并以“全量工具注入”作为 Token 消耗的基准（Baseline）。
*   **评估指标**：Precision@K、Recall@K、F1-Score、Hit Rate（命中率）、MRR（平均倒数排名）、Token 减少率及延迟。

### 4. 资源与算力
*   **模型使用**：嵌入模型使用了 OpenAI 的 `text-embedding-ada-002`，下游 LLM 使用了 `GPT-4o`。
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号或数量）。由于该方法侧重于推理侧的向量检索而非大规模模型训练，其算力需求主要集中在向量数据库（Milvus）的运行和 API 调用上。文中提到检索延迟在 100ms 以内，显示了该架构对实时系统的友好性。

### 5. 实验数量与充分性
*   **实验规模**：进行了 140 组查询测试，覆盖了 121 个真实工具。
*   **充分性评价**：
    *   **多维度分析**：实验不仅给出了整体平均值，还针对不同服务器（如 GitHub vs Filesystem）进行了细分分析，揭示了领域特征对检索的影响。
    *   **参数敏感性**：通过对比不同 K 值，客观地找到了性能与效率的平衡点（K=3）。
    *   **客观性**：使用了标准的检索评估指标（MRR, F1 等），实验设计较为严谨。但样本量（140 个查询）对于极其复杂的工业场景来说仍显略小。

### 6. 主要结论与发现
*   **性能卓越**：在 **K=3** 时，系统达到了 **97.1% 的命中率** 和 **0.91 的 MRR**，证明语义相似度能极好地匹配工具。
*   **极致降本**：相比全量注入，该方法实现了 **99.6% 的 Token 减少率**，显著降低了运营成本。
*   **低延迟**：端到端检索延迟稳定在 **90ms 左右**，不会成为系统瓶颈。
*   **领域差异**：GitHub 和 MySQL 等具有独特术语的工具检索准确率极高；而 Filesystem（如 read/write/copy）由于语义重叠严重，检索难度相对较大。

### 7. 优点（亮点）
*   **即插即用**：专门针对 MCP 标准设计，易于集成到现有的 Agent 架构中。
*   **解决痛点**：直接击中了大规模工具集成中的“Token 膨胀”和“模型认知过载”问题。
*   **开源贡献**：提供了基准数据集和实现代码，填补了工具检索领域缺乏 Benchmark 的空白。
*   **可扩展性强**：讨论了如何扩展到多智能体路由和跨组织工具发现。

### 8. 不足与局限
*   **模糊查询处理**：对于极度模糊的意图（如“帮我处理项目”），系统难以准确定位工具领域。
*   **语义重叠困境**：在功能相近的工具集（如文件管理）中，仅靠语义嵌入可能无法区分细微的参数差异。
*   **单轮限制**：目前的评估主要基于单轮查询，未充分考虑多轮对话中上下文状态对工具选择的影响。
*   **描述依赖性**：检索质量高度依赖于开发者编写的工具描述（Description），若描述模糊，检索效果会大幅下降。

（完）
