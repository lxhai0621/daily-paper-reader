---
title: "Towards Intelligent Geospatial Data Discovery: a knowledge graph-driven multi-agent framework powered by large language models"
title_zh: 迈向智能地理空间数据发现：一种由大语言模型驱动的知识图谱驱动多智能体框架
authors: "Ruixiang Liu, Zhenlong Li, Ali Khosravi Kazazi"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20670v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 知识图谱驱动的多智能体数据发现框架
tldr: 针对地理空间数据分布零散、语义不一致及传统关键词搜索难以理解用户意图的问题，本文提出了一种由大语言模型驱动、知识图谱引导的多智能体框架。该框架通过统一本体对异构元数据进行语义对齐，构建知识图谱建模数据集关系，并利用多智能体协作实现意图解析与检索。研究显著提升了数据发现的准确性与透明度，为下一代智能空间数据基础设施和自主GIS奠定了基础。
selection_source: fresh_fetch
motivation: 传统的地理空间数据目录依赖关键词搜索，难以处理海量异构数据并准确捕捉用户的复杂语义意图。
method: 提出一种结合统一元数据本体、知识图谱与大语言模型多智能体协作的框架，实现从意图解析到结果合成的闭环检索。
result: 实验表明，该框架在匹配准确率、排序质量、召回率及过程透明度方面均优于传统系统。
conclusion: 该研究推动了地理空间数据发现向语义化和智能化范式转变，是实现自主GIS愿景的重要进展。
---

## 摘要
地理空间数据在数量、种类和速度上的快速增长，形成了高度分布式、异构且语义不一致的数据生态系统。现有的数据目录、门户和基础设施仍主要依赖于语义支持有限的关键词搜索，这往往无法捕捉用户意图，导致检索性能较弱。为应对这些挑战，本研究提出了一种由大语言模型驱动的知识图谱驱动多智能体框架，用于智能地理空间数据发现。该框架引入了统一的地理空间元数据本体作为语义中介层，以对齐跨平台的异构元数据标准，并构建了地理空间元数据知识图谱，对数据集及其多维关系进行显式建模。基于结构化表示，该框架采用多智能体协作架构来执行意图解析、知识图谱检索和答案合成，形成了从用户查询到结果的可解释且闭环的发现过程。代表性用例和性能评估结果表明，与传统系统相比，该框架显著提高了意图匹配准确度、排序质量、召回率和发现透明度。本研究推动地理空间数据发现向更具语义化、意图感知和智能化的范式演进，为下一代智能自主空间数据基础设施提供了实践基础，并为“自主 GIS”（Autonomous GIS）的宏伟愿景做出了贡献。

## Abstract
The rapid growth in the volume, variety, and velocity of geospatial data has created data ecosystems that are highly distributed, heterogeneous, and semantically inconsistent. Existing data catalogs, portals, and infrastructures still rely largely on keyword-based search with limited semantic support, which often fails to capture user intent and leads to weak retrieval performance. To address these challenges, this study proposes a knowledge graph-driven multi-agent framework for intelligent geospatial data discovery, powered by large language models. The framework introduces a unified geospatial metadata ontology as a semantic mediation layer to align heterogeneous metadata standards across platforms and constructs a geospatial metadata knowledge graph to explicitly model datasets and their multidimensional relationships. Building on the structured representation, the framework adopts a multi-agent collaborative architecture to perform intent parsing, knowledge graph retrieval, and answer synthesis, forming an interpretable and closed-loop discovery process from user queries to results. Results from representative use cases and performance evaluation show that the framework substantially improves intent matching accuracy, ranking quality, recall, and discovery transparency compared with traditional systems. This study advances geospatial data discovery toward a more semantic, intent-aware, and intelligent paradigm, providing a practical foundation for next-generation intelligent and autonomous spatial data infrastructures and contributing to the broader vision of Autonomous GIS.

---

## 论文详细总结（自动生成）

### 论文结构化深入总结：迈向智能地理空间数据发现

#### 1. 论文的核心问题与整体含义（研究动机和背景）
随着大数据时代的到来，地理空间数据呈现出海量、异构和分布式的特点。然而，现有的地理空间数据基础设施（如 Data.gov、各种 STAC 门户）主要依赖**关键词匹配**的搜索机制。这种方式存在三大痛点：
*   **语义鸿沟**：无法理解用户复杂的自然语言意图（如隐含的时间范围或空间关系）。
*   **标准不一**：不同平台采用不同的元数据标准（如 ISO 19115, FGDC, STAC），导致跨平台检索困难。
*   **缺乏透明度**：搜索结果的排序逻辑往往是“黑箱”，用户难以判断数据集是否真正符合需求。
**研究动机**：构建一个能够理解意图、跨平台对齐并提供可解释结果的智能发现框架，作为“自主 GIS”（Autonomous GIS）的核心组件。

#### 2. 论文提出的方法论
论文提出了一种**知识图谱驱动的多智能体框架（IGDD）**，核心流程如下：

*   **统一语义建模（Ontology）**：设计了一个统一的地理空间元数据本体，作为语义中介层，将 STAC、FGDC CSDGM 和 CKAN 等异构标准映射到包含 `Dataset`、`Topic`、`Space`、`Time`、`Organization` 等核心实体的结构中。
*   **知识图谱构建**：利用 LLM 提取主题、补全缺失的时空信息，并构建包含数百万实体和关系的 Neo4j 图数据库，支持向量索引、空间索引和时间索引。
*   **多智能体协作工作流**：
    1.  **意图解析智能体（Intent Parsing Agent）**：将自然语言转换为结构化意图（GeoIntent），包含置信度评估公式：$Conf(I) = \alpha \cdot A(I) + \beta \cdot P(I)$（提取准确度与语义合理性的加权）。
    2.  **图检索智能体（Graph Retrieval Agent）**：执行向量相似度匹配，并进行空间/时间“硬过滤”，最后通过多维相关性评分公式 $Score_{raw}(d)$ 进行初步排序。
    3.  **答案合成智能体（Answer Synthesis Agent）**：利用 LLM 对 Top-K 结果进行重排序（Reranking），生成匹配理由，并提取知识图谱子图以增强透明度。

#### 3. 实验设计
*   **数据集/场景**：集成了 6 个主流数据目录（包括 Data.gov、PASDA、多个 STAC 接口），共计 **264,177 个数据集**，构建了拥有约 280 万实体和 1223 万关系的知识图谱。
*   **Benchmark**：预设了 **100 个测试查询**，按复杂度分为四个等级（Level 1 仅主题，到 Level 4 包含主题、空间、时间、组织、格式等多重约束）。
*   **对比方法**：
    *   **Data.gov 原生搜索**（基准）。
    *   **消融实验**：对比了“仅图检索”与“图检索+答案合成（重排序）”的效果。
*   **评估指标**：意图匹配率（EIMR）、排序质量（NDCG@10）、召回率（Recall@20）。

#### 4. 资源与算力
*   **模型使用**：底层 LLM 采用了 OpenAI 的 **GPT-4o**（文中注为 GPT-5.2，推测为特定版本或笔误），嵌入模型使用 `text-embedding-3-large`。
*   **算力/成本**：
    *   **硬件**：部署在高性能服务器上的 Neo4j 数据库。
    *   **效率**：图检索耗时 1-2 秒；端到端完整发现周期约 **30-40 秒**。
    *   **消耗**：平均每个查询消耗约 **11,000 个 tokens**。
*   **训练说明**：文中未提及模型训练时长，因为该框架主要基于现有的预训练大模型进行推理和 Agent 编排。

#### 5. 实验数量与充分性
*   **实验规模**：进行了 100 组系统化查询实验，并对 3,646 个候选数据集进行了人工标注。
*   **充分性**：实验涵盖了气候、人口普查、公共卫生、卫星影像等多个真实用例，展示了系统在不同地理尺度（全球 vs 局部）下的表现。
*   **客观性**：采用了**双人盲审标注**机制，由两名地理信息科学博士根据统一准则对检索结果的相关性进行 0-3 分的评分，确保了 Benchmark 的公正性。

#### 6. 论文的主要结论与发现
*   **性能飞跃**：IGDD 框架的 NDCG@10 达到 **85.22%**，比 Data.gov 原生系统（43.65%）提升了 **41.57%**。
*   **意图理解极准**：意图解析智能体的准确率（EIMR）高达 **99.27%**。
*   **重排序的价值**：答案合成智能体（LLM 重排序）使 NDCG 指标额外提升了约 15%-17%，证明了 LLM 在精细化语义筛选中的作用。
*   **复杂查询优势**：随着查询约束增加，传统系统的性能急剧下降，而本框架表现出极强的鲁棒性。

#### 7. 优点
*   **闭环可解释性**：不仅给出结果，还通过 Agent 给出匹配理由和知识图谱子图，解决了 AI 检索的“幻觉”和不透明问题。
*   **人机协同**：引入了 **Human-in-the-loop** 机制，当用户意图模糊（如“南方”）时，系统会主动询问以澄清需求。
*   **标准兼容性**：通过本体中介层实现了对 STAC、FGDC 等多种异构元数据标准的无缝集成。

#### 8. 不足与局限
*   **功能覆盖有限**：目前仅聚焦于“发现”阶段，尚未涉及数据的自动预处理（如裁剪、重投影）以生成“分析就绪数据”（ARD）。
*   **尺度感知不足**：对于地理空间数据特有的“分辨率”和“比例尺”匹配，目前的建模还不够精细。
*   **延迟问题**：40 秒左右的响应时间虽然对于复杂研究尚可接受，但对于高频交互场景仍显略慢。
*   **成本风险**：依赖商业 LLM API，大规模部署时的 Token 成本较高。

（完）
