---
title: Adaptive Robust Estimator for Multi-Agent Reinforcement Learning
title_zh: 面向多智能体强化学习的自适应鲁棒估计器
authors: "Zhongyi Li, Wan Tian, Jingyu Chen, Kangyao Huang, Huiming Zhang, Hui Yang, Tao Ren, Jinyang Jiang, Yijie Peng, Yikun Ban, Fuzhen Zhuang"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21574v1"
tags: ["query:agent"]
score: 9.0
evidence: 用于协作推理的鲁棒多智能体强化学习框架
tldr: 针对多智能体强化学习在协作推理中存在的信用分配模糊和奖励噪声干扰问题，本文提出了一个鲁棒的框架。该框架包含DACR结构化流水线，将推理分解为回答、评论和重写三个阶段以明确贡献；同时引入ARE自适应鲁棒估计器，通过稳健的批次经验均值估计来应对重尾和噪声奖励。实验证明，该方法在数学推理和具身智能任务中表现优异，显著提升了训练的稳定性和鲁棒性。
selection_source: fresh_fetch
motivation: 多智能体协作推理面临交互模糊导致的信用分配困难，以及噪声奖励引发的训练不稳定和策略偏差问题。
method: 提出了由DACR三阶段推理流水线和ARE自适应鲁棒估计器组成的强化学习框架，旨在实现明确的贡献归因和稳健的优势估计。
result: 在数学推理和具身智能基准测试中，该方法在同构和异构设置下均优于基准，且在奖励噪声环境下表现出更强的鲁棒性。
conclusion: 该研究通过结构化交互和鲁棒估计技术，有效解决了多智能体强化学习中的信用分配与噪声干扰难题，显著提升了协作推理的性能和训练稳定性。
---

## 摘要
多智能体协作已成为增强大语言模型推理能力的强大范式，但它面临着交互层面的模糊性，这种模糊性模糊了生成、批判和修订之间的界限，使得跨智能体的信用分配变得困难。此外，在这种环境下的策略优化容易受到重尾和噪声奖励的影响，这可能会使优势估计产生偏差，并引发不稳定甚至发散的训练。为了解决这两个问题，我们提出了一种用于协作推理的鲁棒多智能体强化学习框架，由两个组件组成：双智能体“回答-批判-重写”（DACR）和自适应鲁棒估计器（ARE）。DACR 将推理分解为一个结构化的三阶段流水线：回答、批判和重写，同时能够显式地将每个智能体的边际贡献归因于其合作伙伴的性能。ARE 在多智能体策略优化过程中提供批次经验均值的鲁棒估计。在数学推理和具身智能基准测试中，即使在存在噪声奖励的情况下，我们的方法在同构和异构设置下均始终优于基准方法。这些结果表明，该方法对奖励噪声具有更强的鲁棒性，且训练动态更稳定，有效防止了由噪声奖励信号引起的优化失败。

## Abstract
Multi-agent collaboration has emerged as a powerful paradigm for enhancing the reasoning capabilities of large language models, yet it suffers from interaction-level ambiguity that blurs generation, critique, and revision, making credit assignment across agents difficult. Moreover, policy optimization in this setting is vulnerable to heavy-tailed and noisy rewards, which can bias advantage estimation and trigger unstable or even divergent training. To address both issues, we propose a robust multi-agent reinforcement learning framework for collaborative reasoning, consisting of two components: Dual-Agent Answer-Critique-Rewrite (DACR) and an Adaptive Robust Estimator (ARE). DACR decomposes reasoning into a structured three-stage pipeline: answer, critique, and rewrite, while enabling explicit attribution of each agent's marginal contribution to its partner's performance. ARE provides robust estimation of batch experience means during multi-agent policy optimization. Across mathematical reasoning and embodied intelligence benchmarks, even under noisy rewards, our method consistently outperforms the baseline in both homogeneous and heterogeneous settings. These results indicate stronger robustness to reward noise and more stable training dynamics, effectively preventing optimization failures caused by noisy reward signals.

---

## 论文详细总结（自动生成）

以下是对论文《Adaptive Robust Estimator for Multi-Agent Reinforcement Learning》的结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：多智能体强化学习（MARL）在提升大语言模型（LLM）推理能力时面临两大挑战：
    1.  **信用分配困难**：智能体间的交互（生成、批判、修订）界限模糊，难以量化单个智能体对最终结果的贡献。
    2.  **奖励信号不稳**：推理任务的奖励（由验证器或奖励模型产生）往往具有重尾分布、噪声大且存在极端离群值。传统的 GRPO 算法依赖批次经验均值进行归一化，极易受离群值干扰，导致训练不稳定甚至发散。
*   **研究背景**：随着 LLM 转向通过强化学习进行后训练，多智能体协作（如“解决者-评估者”配对）成为趋势，但现有的优化算法在处理复杂交互和噪声奖励方面表现脆弱。

### 2. 方法论
论文提出了一个鲁棒的多智能体强化学习框架，包含两个核心组件：
*   **DACR（双智能体回答-批判-重写）协议**：
    *   **三阶段流程**：1. 独立回答；2. 互相交换答案并进行批判；3. 根据对方的批判修订自己的答案。
    *   **交叉评估奖励机制**：除了基础的正确性奖励，引入“边际改进项”，即如果智能体 A 的批判导致智能体 B 的修订版得分提高，则奖励智能体 A。这实现了显式的信用归因。
*   **ARE（自适应鲁棒估计器）**：
    *   **核心思想**：改进了经典的“均值中位数”（Median-of-Means, MoM）估计器。ARE 在每个数据块内部使用自适应损失函数（Barron Loss）进行鲁棒位置估计，再取各块的中位数。
    *   **技术细节**：通过最小化负对数似然来动态调整形状参数 $\alpha$ 和尺度参数 $c$，使其能自动适应不同的噪声分布。
    *   **优化算法**：采用渐进非凸性（GNC）框架和迭代重权最小二乘法（IRLS）来解决非凸优化问题，确保估计的稳定性。

### 3. 实验设计
*   **数据集与场景**：
    *   **数学推理**：在 MATH 7.5k 上训练，在 MATH500（分布内）、AIME24/25、AMC23、Gaokao2023en、MinervaMath（分布外）上测试。
    *   **具身智能**：无人机视觉语言导航（VLN）任务，使用 FlightGPT 框架。
*   **对比基准（Benchmark）**：
    *   **Base Model**：原始指令微调模型。
    *   **Baseline-GRPO**：使用标准 GRPO 算法的多智能体协作模型。
    *   **Single-Agent**：无交互的独立推理模型。
*   **模型规模**：涵盖了 Qwen2.5 (1.5B, 7B)、Llama3.1 (8B) 以及视觉语言模型 Qwen2-VL-2B。

### 4. 资源与算力
*   **硬件环境**：使用了 **1 台拥有 8 张 NVIDIA A800 GPU 的节点**。
*   **软件框架**：使用 Ray 进行分布式训练编排，vLLM 进行高效推理（4 个推理引擎，张量并行度为 2）。
*   **训练细节**：启用了梯度检查点（Gradient Checkpointing）和 Adam 参数卸载（Offloading）以优化显存。具体训练时长未详细说明，但提供了详细的超参数表（如学习率 $1 \times 10^{-6}$，批次大小 32 等）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的实验，包括：
    *   不同规模和家族模型的性能对比（Qwen vs Llama）。
    *   **消融实验**：验证了三阶段交互协议（DACR）相对于独立回答的有效性。
    *   **鲁棒性测试**：通过注入柯西噪声（Cauchy Noise）模拟极端奖励环境，测试了不同污染比例下的模型表现。
    *   **跨领域验证**：从纯文本推理扩展到具身智能导航。
*   **充分性评价**：实验设计较为全面，涵盖了同构/异构智能体配对、分布内/分布外泛化测试，且提供了严谨的理论证明（收敛性与偏差界限分析），实验结果具有较高的说服力和客观性。

### 6. 主要结论与发现
*   **性能提升**：ARE 在所有数学基准测试中均优于标准 GRPO，尤其在 AMC23 任务上，Qwen2.5-1.5B 的准确率从 22.5% 提升至 32.5%。
*   **训练稳定性**：在存在重尾奖励噪声的情况下，ARE 能有效防止梯度尖峰，保持训练曲线平滑，避免优化失败。
*   **协作价值**：DACR 协议显著优于简单的独立尝试，证明了结构化批判和修订在复杂推理中的必要性。
*   **泛化能力**：在具身导航任务中，ARE 显著降低了导航误差（NE），提升了成功率（SR），尤其在未见过的环境中表现更佳。

### 7. 优点
*   **理论与实践结合**：不仅提出了算法，还为 ARE 估计器在有限方差和重尾分布下的渐近性质提供了数学证明。
*   **通用性强**：ARE 作为一个即插即用的估计器，可以轻易集成到现有的 GRPO 或 PPO 框架中。
*   **解决痛点**：直接针对 RLHF/RLAIF 中常见的奖励模型噪声问题，具有很强的实际应用价值。

### 8. 不足与局限
*   **计算开销**：ARE 涉及内部的迭代优化（GNC-IRLS），相比简单的批次均值计算，会增加一定的训练计算耗时。
*   **交互复杂性**：DACR 的三阶段协议增加了推理时的 Token 消耗和延迟，在实时性要求极高的场景下可能受限。
*   **超参数敏感性**：虽然 ARE 具有自适应性，但其初始参数和 GNC 的更新频率仍可能影响最终效果。

（完）
