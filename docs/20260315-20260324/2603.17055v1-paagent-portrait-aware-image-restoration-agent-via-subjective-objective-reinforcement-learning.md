---
title: "PaAgent: Portrait-Aware Image Restoration Agent via Subjective-Objective Reinforcement Learning"
title_zh: PaAgent：通过主客观强化学习实现的画像感知图像修复智能体
authors: "Yijian Wang, Qingsen Yan, Jiantao Zhou, Duwei Dai, Wei Dong"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.17055v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 在图像修复智能体中使用RAG进行工具选择
tldr: 针对现有图像修复智能体在工具选择上缺乏经验总结、搜索效率低的问题，本文提出了PaAgent。该智能体引入了自进化的“画像库”和检索增强生成（RAG）技术，通过总结历史交互经验来精准匹配修复工具。同时，采用主客观强化学习策略提升对复杂退化场景的感知能力。实验证明，PaAgent在多种单项和混合退化任务中表现优异，显著提升了自动化图像修复的效率和质量。
selection_source: fresh_fetch
motivation: 现有的图像修复智能体缺乏对过去交互经验的总结机制，导致在寻找最优修复工具时往往需要进行低效的穷举搜索。
method: 提出了包含自进化画像库和RAG机制的PaAgent，并结合主客观强化学习策略，利用图像质量评分和语义见解共同引导模型感知退化。
result: 在涵盖6种单退化和8种混合退化场景的8个基准测试中，PaAgent均展现出超越现有方法的修复性能。
conclusion: PaAgent通过经验总结和多维度强化学习，有效解决了复杂场景下的自动化图像修复难题，提升了智能体在处理非均匀退化时的准确性。
---

## 摘要
图像修复（IR）智能体利用多模态大语言模型感知退化并调用修复工具，在自动化 IR 任务方面展现出巨大潜力。然而，现有的 IR 智能体通常缺乏对过去交互的见解总结机制，导致在寻找最优 IR 工具时需要进行穷举搜索。为解决这一局限性，我们提出了一种名为 PaAgent 的画像感知 IR 智能体，它结合了用于 IR 工具的自进化画像库和检索增强生成（RAG），以为输入图像选择合适的 IR 工具。具体而言，为了构建和进化画像库，PaAgent 通过结合修复后的图像、选定的 IR 工具和退化图像来总结各种 IR 工具的特征，从而不断丰富该库。此外，利用 RAG 从画像库中检索相关见解，为输入图像选择最优的 IR 工具。此外，为了增强 PaAgent 在复杂场景中感知退化的能力，我们提出了一种主客观强化学习策略，在奖励生成中同时考虑图像质量评分和语义见解，即使在局部和非均匀退化的情况下也能准确提供退化信息。在 8 个 IR 基准测试（涵盖 6 种单一退化和 8 种混合退化场景）上进行的广泛实验验证了 PaAgent 在处理复杂 IR 任务方面的优越性。我们的项目页面见 \href{https://wyjgr.github.io/PaAgent.html}{PaAgent}。

## Abstract
Image Restoration (IR) agents, leveraging multimodal large language models to perceive degradation and invoke restoration tools, have shown promise in automating IR tasks. However, existing IR agents typically lack an insight summarization mechanism for past interactions, which results in an exhaustive search for the optimal IR tool. To address this limitation, we propose a portrait-aware IR agent, dubbed PaAgent, which incorporates a self-evolving portrait bank for IR tools and Retrieval-Augmented Generation (RAG) to select a suitable IR tool for input. Specifically, to construct and evolve the portrait bank, the PaAgent continuously enriches it by summarizing the characteristics of various IR tools with restored images, selected IR tools, and degraded images. In addition, the RAG is employed to select the optimal IR tool for the input image by retrieving relevant insights from the portrait bank. Furthermore, to enhance PaAgent's ability to perceive degradation in complex scenes, we propose a subjective-objective reinforcement learning strategy that considers both image quality scores and semantic insights in reward generation, which accurately provides the degradation information even under partial and non-uniform degradation. Extensive experiments across 8 IR benchmarks, covering six single-degradation and eight mixed-degradation scenarios, validate PaAgent's superiority in addressing complex IR tasks. Our project page is \href{https://wyjgr.github.io/PaAgent.html}{PaAgent}.

---

## 论文详细总结（自动生成）

### PaAgent：通过主客观强化学习实现的画像感知图像修复智能体论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：现有的图像修复（IR）智能体虽然能调用多种工具，但存在两个主要缺陷：
    1.  **缺乏经验总结机制**：智能体在处理新任务时往往孤立地对待每个实例，无法利用历史交互经验，导致在选择最优工具时需要进行低效的穷举搜索或频繁的回滚操作。
    2.  **退化感知模糊性**：现有的方法多依赖单一的无参考图像质量评估（NR-IQA）分数。然而，相似的分数可能对应完全不同的退化模式（如局部严重退化 vs. 全局轻微退化），导致决策失误。
*   **核心目标**：开发一个具备“画像感知”能力的智能体，通过总结历史经验和多维度感知退化，实现高效、精准的复杂图像修复。

#### 2. 方法论：核心思想与关键技术
PaAgent 的核心架构由**工具画像库（Portrait Bank）**和**主客观强化学习（SORL）**策略组成：
*   **自进化工具画像库与 RAG**：
    *   **画像库构建**：存储了超过 10 万条交互见解，每条记录包含“退化信息、所选工具、修复质量的主观评价”三元组。
    *   **检索增强生成（RAG）**：当输入退化图像时，智能体提取退化特征作为查询，通过 RAG 从向量数据库中检索最相关的历史见解，从而直接锁定最合适的修复工具，避免盲目尝试。
    *   **自进化机制**：每次修复任务完成后，系统会自动总结新的交互见解并存入库中，实现能力的持续增长。
*   **主客观强化学习（SORL）策略**：
    *   **核心算法**：采用 **GRPO（组相对策略优化）** 算法，无需复杂的价值网络，通过组内样本的相对优势来优化策略。
    *   **双重奖励机制**：奖励信号由 **客观分数**（集成 CLIP-IQA、NIQE、BRISQUE 等 7 种指标）和 **主观语义见解**（由多模态大模型 Qwen3.5-Plus 生成）共同构成。
    *   **优化目标**：通过 KL 散度约束和剪切代理损失函数，确保模型在提升修复性能的同时保持训练稳定性。

#### 3. 实验设计
*   **数据集与场景**：涵盖了 8 个主流基准测试，包括：
    *   **单一退化**：GoPro（去模糊）、SOTS（去雾）、BSD68（去噪）、Rain13K（去雨）、CSD（去雪）、LOL-V1（低光增强）。
    *   **混合/复杂退化**：LOL Blur（低光+模糊）、CDD-11（包含 11 种单项及复合退化组合）。
*   **对比方法**：对比了 11 种领先方法，包括 10 种全能型（All-in-One）模型（如 PromptIR、AdaIR、RAM 等）和 1 种基于智能体的方法（AgenticIR）。
*   **评价指标**：采用峰值信噪比（PSNR）和结构相似性（SSIM）。

#### 4. 资源与算力
*   **硬件环境**：使用了 **2 台 NVIDIA A100 GPU**。
*   **训练细节**：
    *   使用 LoRA 技术进行参数高效微调（Rank=16）。
    *   训练 10 个 Epoch，Batch Size 为 4。
    *   学习率设为 $10^{-6}$，使用 Adam 优化器。
    *   基础模型采用 Qwen3.5-9B，辅助推理采用 Qwen3.5-Plus。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 8 个数据集上进行了详尽的定量评估，并在多种退化场景下展示了定性视觉对比。
*   **消融实验**：设计了针对工具画像库（随机选择 vs. 手动选择 vs. RAG）、SORL 策略（SFT vs. 纯主观奖励 vs. 主客观结合）以及输入上下文（是否包含 IQA 分数和历史任务）的多组对比实验。
*   **客观性与公平性**：实验覆盖了从简单到极端的各种退化水平（如噪声水平 $\sigma=5$ 到 $50$），并与最新的 ICLR 2025 和 CVPR 2025 论文方法进行对比，显示出较强的客观性和前瞻性。

#### 6. 主要结论与发现
*   **性能领先**：PaAgent 在所有测试场景中均表现优异，尤其在处理复杂的混合退化（如 CDD-11 数据集）时，PSNR 提升显著。
*   **经验价值**：引入画像库和 RAG 机制能有效缩小工具搜索空间，显著减少了多步决策中的回滚次数。
*   **感知增强**：SORL 策略使智能体能够理解退化的空间分布和语义特征，解决了传统方法在非均匀退化下的决策失误问题。
*   **多步决策优势**：相比单步决策，PaAgent 的多步“感知-修复-评估”循环能更彻底地清除残留退化。

#### 7. 优点与亮点
*   **创新性引入 RAG**：首次将检索增强生成应用于图像修复工具的选择，解决了智能体“无记忆”的问题。
*   **主客观融合**：通过强化学习将人类视觉偏好（语义见解）与数学指标（IQA 分数）结合，使修复结果更符合人类审美。
*   **自进化能力**：系统具备持续学习能力，随着使用次数增加，画像库越丰富，决策越精准。

#### 8. 不足与局限
*   **推理延迟**：由于涉及多模态大模型的多次调用（感知、检索、推理、评估），其实时性可能受限，难以应用于超高帧率的视频修复。
*   **模型依赖**：性能高度依赖于底层大模型（如 Qwen3.5）的推理能力，对于计算资源受限的移动端设备部署难度较大。
*   **画像库维护**：随着交互记录增加到百万级以上，向量数据库的检索效率和存储压力可能成为新的挑战。

（完）
