---
title: Are a Thousand Words Better Than a Single Picture? Beyond Images -- A Framework for Multi-Modal Knowledge Graph Dataset Enrichment
title_zh: 千言万语是否优于单张图片？超越图像——一种多模态知识图谱数据集增强框架
authors: "Pengyu Zhang, Klim Zaporojets, Jie Liu, Jia-Hong Huang, Paul Groth"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16974v1"
tags: ["query:mmkqa"]
score: 9.0
evidence: 通过将视觉信息转换为文本描述并利用LLM融合来丰富多模态知识图谱
tldr: 本研究针对多模态知识图谱（MMKG）中图像数据获取难及模糊视觉信息（如Logo、符号）难以利用的问题，提出了Beyond Images框架。该框架通过大规模图像检索、将视觉信息转化为文本描述以及利用大语言模型进行多源描述融合，生成与实体对齐的简洁摘要。实验表明，该方法在多个数据集上显著提升了模型性能，特别是在处理模糊视觉实体时表现优异，为增强MMKG补全提供了一种实用路径。
selection_source: fresh_fetch
motivation: 现有的多模态知识图谱在处理大规模图像采集及模糊视觉信息（如抽象符号）时存在困难，导致视觉语义利用不足。
method: 提出一种包含图像检索、视觉转文本描述以及利用大语言模型进行多源描述融合的自动化数据增强流水线。
result: "在三个公开数据集上均取得性能提升，特别是在处理模糊视觉实体时，MRR和Hits@1分别提升了201.35%和333.33%。"
conclusion: 通过扩大图像覆盖范围并将模糊视觉信息转化为文本，可以在不改变模型架构的情况下显著增强多模态知识图谱的补全能力。
---

## 摘要
多模态知识图谱（MMKG）受益于视觉信息，但大规模图像集的构建十分困难，且往往排除了模糊但相关的视觉内容（如徽标、符号、抽象场景）。我们提出了 Beyond Images，这是一个以数据为中心的自动化增强流水线，并配有可选的人工审核。该流水线分为三个阶段：（1）大规模检索与实体相关的额外图像；（2）将所有视觉输入转换为文本描述，以确保模糊图像贡献的是可用的语义而非噪声；（3）利用大语言模型（LLM）融合多源描述，生成简洁且与实体对齐的摘要。这些摘要在不改变标准 MMKG 模型架构或损失函数的情况下，替换或增强了其中的文本模态。在三个公开的 MMKG 数据集和多个基准模型上，我们观察到了持续的性能提升（整体 Hits@1 最高提升 7%）。此外，在包含视觉模糊的徽标和符号的挑战性实体子集上，将图像转换为文本带来了巨大的提升（MRR 提升 201.35%，Hits@1 提升 333.33%）。此外，我们发布了一个轻量级的文本-图像一致性检查界面，用于可选的有针对性审核，从而提高描述质量和数据集的可靠性。我们的结果表明，扩大图像覆盖范围并将模糊的视觉内容转换为文本，是实现更强 MMKG 补全的一条切实可行的路径。代码、数据集和补充材料可在 https://github.com/pengyu-zhang/Beyond-Images 获取。

## Abstract
Multi-Modal Knowledge Graphs (MMKGs) benefit from visual information, yet large-scale image collection is hard to curate and often excludes ambiguous but relevant visuals (e.g., logos, symbols, abstract scenes). We present Beyond Images, an automatic data-centric enrichment pipeline with optional human auditing. This pipeline operates in three stages: (1) large-scale retrieval of additional entity-related images, (2) conversion of all visual inputs into textual descriptions to ensure that ambiguous images contribute usable semantics rather than noise, and (3) fusion of multi-source descriptions using a large language model (LLM) to generate concise, entity-aligned summaries. These summaries replace or augment the text modality in standard MMKG models without changing their architectures or loss functions. Across three public MMKG datasets and multiple baseline models, we observe consistent gains (up to 7% Hits@1 overall). Furthermore, on a challenging subset of entities with visually ambiguous logos and symbols, converting images into text yields large improvements (201.35% MRR and 333.33% Hits@1). Additionally, we release a lightweight Text-Image Consistency Check Interface for optional targeted audits, improving description quality and dataset reliability. Our results show that scaling image coverage and converting ambiguous visuals into text is a practical path to stronger MMKG completion. Code, datasets, and supplementary materials are available at https://github.com/pengyu-zhang/Beyond-Images.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **Beyond Images** 的自动化框架，旨在通过将视觉信息转化为文本描述来增强多模态知识图谱（MMKG）的性能。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：多模态知识图谱（MMKG）通过整合图像和文本来提升实体表示。然而，现有的 MMKG 构建面临两大挑战：
    1.  **扩展性差**：图像采集往往依赖人工筛选，难以扩展到大规模图谱。
    2.  **模糊视觉信息利用不足**：许多与实体相关的图像（如 Logo、符号、抽象场景）语义模糊，直接提取视觉特征（Embedding）会引入大量噪声，因此常被研究者舍弃。
*   **核心问题**：如何自动化地大规模扩展 MMKG 的视觉覆盖范围，并有效提取那些“模糊但相关”图像中的语义信息？

### 2. 方法论
该框架采用“以数据为中心”的思路，分为三个阶段：
*   **模态扩展模块 (Modality Extension)**：利用实体名称从维基百科等外部资源自动检索并下载更多相关图像，显著提升每个实体的图像覆盖率。
*   **语义生成模块 (Semantic Generation)**：使用预训练的图像转文本模型（如 BLIP-2, GIT, LLaVA）将所有图像（原始的和新检索的）转化为详细的文本描述。这一步将不稳定的视觉信号转化为稳定的文本语义。
*   **LLM 语义融合模块 (Semantic Fusion)**：利用大语言模型（如 Mistral-7B, LLaMA-3.1）将针对同一实体的多条图像描述进行整合、去噪和总结，生成一段简洁且与实体高度对齐的摘要。
*   **应用方式**：生成的文本摘要直接作为增强的文本模态输入到现有的 MMKG 模型中，**无需修改模型架构或损失函数**。

### 3. 实验设计
*   **数据集**：使用了三个主流的公开 MMKG 数据集：**MKG-W**、**MKG-Y** 和 **DB15K**。
*   **基准模型 (Benchmarks)**：对比了四种先进的 MMKG 补全模型：**MMRNS**、**MyGO**、**NativE** 和 **AdaMF**。
*   **对比设置**：
    *   **Baseline**：原始数据集输入。
    *   **G(o)/G(n)**：分别仅使用原始图像或新检索图像生成的文本。
    *   **Fusion**：使用 LLM 融合后的摘要。
*   **特殊评估**：专门针对包含 Logo 和符号的“模糊实体子集”进行了性能测试。

### 4. 资源与算力
*   **算力说明**：论文**未明确给出**具体的 GPU 型号和数量。
*   **训练时长**：文中提到，在生成增强数据后，下游模型的训练时间仅增加约 7 到 30 分钟，表明该方法在推理和训练阶段非常高效。
*   **离线处理**：图像转文本和 LLM 融合属于离线预处理步骤，不占用模型实时训练的算力。

### 5. 实验数量与充分性
*   **实验规模**：实验涵盖了 3 个数据集 × 4 个模型 × 多种输入配置，并测试了 3 种不同的图像转文本模型和 3 种不同的融合 LLM，实验组数非常丰富。
*   **消融实验**：进行了模态贡献分析（如仅文本 vs 文本+图像 vs 文本+图像转文本），验证了各模块的有效性。
*   **客观性**：通过在多个不同架构的模型上取得一致的性能提升，证明了该方法的普适性和公平性。此外，还引入了人工审计界面来验证生成描述的准确性。

### 6. 主要结论与发现
*   **整体性能提升**：在所有数据集和模型上均观察到性能增长，Hits@1 最高提升了 **7%**。
*   **模糊图像的巨大价值**：在处理 Logo 和符号等模糊视觉实体的挑战性子集上，MRR 提升了 **201.35%**，Hits@1 提升了 **333.33%**。这证明了“千言万语（文本化）确实优于单张（模糊）图片”。
*   **融合的重要性**：LLM 融合生成的摘要优于简单的文本拼接，说明 LLM 能有效过滤图像描述中的冗余和噪声。
*   **模态替代性**：在某些模型中，高质量的图像文本描述甚至可以替代原始的视觉 Embedding。

### 7. 优点
*   **模型无关性**：作为一种数据增强方案，它可以无缝集成到任何现有的 MMKG 模型中，具有极高的实用价值。
*   **变废为宝**：成功挖掘了以往被视为噪声而丢弃的模糊图像的语义价值。
*   **自动化与可解释性**：实现了从检索到融合的全自动化，且生成的文本摘要比向量 Embedding 更具可解释性。
*   **配套工具**：发布了人工审核界面，方便研究者对数据质量进行抽样检查。

### 8. 不足与局限
*   **模型依赖性**：增强效果高度依赖于预训练 VLM 和 LLM 的性能，若基础模型产生幻觉，会影响最终质量。
*   **Prompt 敏感性**：文中仅使用了固定的 Prompt，未深入探讨 Prompt Engineering 对结果的影响。
*   **模态单一**：目前仅关注图像到文本的转换，尚未扩展到音频或视频模态。
*   **语言局限**：实验主要基于英语语料，对于多语言 MMKG 的适用性有待验证。

（完）
