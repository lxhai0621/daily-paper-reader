---
title: "KLDrive: Fine-Grained 3D Scene Reasoning for Autonomous Driving based on Knowledge Graph"
title_zh: KLDrive：基于知识图谱的自动驾驶细粒度三维场景推理
authors: "Ye Tian, Jingyi Zhang, Zihao Wang, Xiaoyuan Ren, Xiaofan Yu, Onat Gungor, Tajana Rosing"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21029v1"
tags: ["query:mmkqa"]
score: 10.0
evidence: 知识图谱增强的大模型多模态推理
tldr: 针对自动驾驶中3D场景推理不可靠和幻觉问题，本文提出KLDrive框架。该框架结合了知识图谱（KG）与大语言模型（LLM），通过能量模型构建可靠的场景事实图，并利用LLM代理在结构化约束下进行事实驱动的推理。实验证明，KLDrive在NuScenes-QA等基准测试上显著优于现有方法，尤其在计数等复杂推理任务中大幅减少了幻觉。
selection_source: fresh_fetch
motivation: 现有的自动驾驶大模型方法在处理精细化3D场景事实时存在幻觉、推理不透明且过度依赖特定任务训练的问题。
method: 提出一种由能量驱动的场景事实构建模块和基于结构化约束的LLM推理代理组成的知识图谱增强型框架。
result: 在NuScenes-QA和GVQA基准上均达到SOTA性能，其中在最具挑战性的计数任务上比最强基准提升了46.01个百分点。
conclusion: 将可靠的结构化场景事实构建与显式推理相结合，能有效提升自动驾驶系统的精细化场景理解能力并显著减少模型幻觉。
---

## 摘要
自动驾驶需要对细粒度的三维场景事实进行可靠的推理。基于多模态驾驶观测的细粒度问答提供了一种评估该能力的自然方式，但现有的感知流水线和面向驾驶的大语言模型（LLM）方法仍面临场景事实不可靠、幻觉、推理不透明以及对特定任务训练的严重依赖等问题。我们提出了 KLDrive，这是首个用于自动驾驶细粒度问答的知识图谱增强型 LLM 推理框架。KLDrive 通过设计两个紧密耦合的组件来解决这一问题：一个基于能量的场景事实构建模块，用于将多源证据整合为可靠的场景知识图谱；以及一个 LLM 智能体，在显式结构约束下，在受限的动作空间内执行基于事实的推理。通过将结构化提示与少样本上下文示例相结合，该框架无需繁重的特定任务微调即可适应多种推理任务。在两个大规模自动驾驶问答基准测试上的实验表明，KLDrive 优于先前的最先进方法，在 NuScenes-QA 上实现了 65.04% 的最佳总准确率，在 GVQA 上实现了 42.45 的最佳 SPICE 分数。在最具挑战性的事实推理任务——计数上，它比最强的基准模型提高了 46.01 个百分点，证明了其显著减少了幻觉，并体现了将可靠场景事实构建与显式推理相结合的优势。

## Abstract
Autonomous driving requires reliable reasoning over fine-grained 3D scene facts. Fine-grained question answering over multi-modal driving observations provides a natural way to evaluate this capability, yet existing perception pipelines and driving-oriented large language model (LLM) methods still suffer from unreliable scene facts, hallucinations, opaque reasoning, and heavy reliance on task-specific training. We present KLDrive, the first knowledge-graph-augmented LLM reasoning framework for fine-grained question answering in autonomous driving. KLDrive addresses this problem through designing two tightly coupled components: an energy-based scene fact construction module that consolidates multi-source evidence into a reliable scene knowledge graph, and an LLM agent that performs fact-grounded reasoning over a constrained action space under explicit structural constraints. By combining structured prompting with few-shot in-context exemplars, the framework adapts to diverse reasoning tasks without heavy task-specific fine-tuning. Experiments on two large-scale autonomous-driving QA benchmarks show that KLDrive outperforms prior state-of-the-art methods, achieving the best overall accuracy of 65.04% on NuScenes-QA and the best SPICE score of 42.45 on GVQA. On counting, the most challenging factual reasoning task, it improves over the strongest baseline by 46.01 percentage points, demonstrating substantially reduced hallucinations and the benefit of coupling reliable scene fact construction with explicit reasoning.

---

## 论文详细总结（自动生成）

### KLDrive：基于知识图谱的自动驾驶细粒度三维场景推理论文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
自动驾驶系统不仅需要识别物体，还需要对复杂的3D场景事实（如物体状态、空间关系、数量等）进行可靠推理。现有的端到端大模型（VLM/LLM）方法在处理此类细粒度问答（QA）时存在三大痛点：
*   **事实不可靠与幻觉：** 传感器噪声和遮挡导致感知结果不准确，模型容易产生凭空捏造的“幻觉”。
*   **推理不透明：** 黑盒化的端到端映射使得决策过程无法追溯和验证。
*   **任务适配成本高：** 严重依赖针对特定任务的大规模微调，难以灵活扩展。
**KLDrive** 旨在通过结合**结构化知识图谱（KG）**与**受限动作空间的LLM代理**，实现可解释、高精度且低幻觉的场景推理。

#### 2. 论文提出的方法论
KLDrive 包含两个紧密耦合的核心阶段：
*   **基于能量的场景知识图谱构建（Energy-based KG Generation）：**
    *   **多源证据融合：** 整合相机（RayDN）、激光雷达（FocalFormer3D）和融合模型（IS-Fusion）的检测结果。
    *   **时空恢复：** 利用相邻帧信息通过线性插值恢复因遮挡或运动模糊缺失的物体。
    *   **能量模型（EBM）精炼：** 设计了一个能量函数，综合考虑物体的可保留性（置信度）、成对交互（去重与一致性）、运动状态合理性及支持充分性。通过最小化能量来筛选出最可靠的场景实体。
*   **LLM 驱动的编排与推理（LLM-Driven Reasoning）：**
    *   **Plan-Execute-Observe 循环：** LLM 作为语义规划器，不直接生成答案，而是调用预定义的工具。
    *   **受限动作空间：** 定义了一套场景查询代数（如 `Resolve` 筛选物体、`RelSelect` 空间选择、`Count` 计数等），确保推理过程严格基于感知到的事实。
    *   **上下文学习（ICL）：** 使用冻结权重的 Qwen3-7B 模型，通过 Few-shot 提示词引导其进行结构化推理，无需微调。

#### 3. 实验设计
*   **数据集：** 
    *   **NuScenes-QA：** 包含 39,704 个关于存在性、计数、物体属性、状态和比较的结构化问答。
    *   **GVQA：** 包含 104,030 个更复杂的驾驶场景理解任务，要求生成句子形式的答案。
*   **对比方法（Baselines）：**
    *   **感知基准：** RayDN、FocalFormer3D、IS-Fusion（外接 QA 头部）。
    *   **通用 VLM：** LLaVA-v1.6、InternVL-3.5、Qwen3Vision。
    *   **驾驶专用 LLM：** DriveLM、LiDAR-LLM、MAPLM、CREMA（均经过特定任务微调）。

#### 4. 资源与算力
*   **训练与推理设备：** 使用了配备 AMD Ryzen Threadripper 9985WX CPU、768GB RAM 和 **NVIDIA RTX A6000 GPU** 的工作站。
*   **边缘端评估：** 在 **NVIDIA Jetson Orin NX (16GB)** 上测试了推理延迟。
*   **模型细节：** LLM 后端使用 Qwen3-7B，感知前端在训练集上训练，推理代理部分保持模型冻结。

#### 5. 实验数量与充分性
*   **实验规模：** 在两个主流大规模基准上进行了全面测试，涵盖了数万个测试样本。
*   **消融实验：** 详细对比了“有无 KG 构建模块”和“有无 LLM 代理模块”的效果，验证了 EBM 精炼和工具化推理的必要性。
*   **公平性：** 引入了 KLDrive-GT（使用真值标注构建 KG）作为上限参考，以区分感知误差和推理误差。对比实验涵盖了从感知模型到最新 SOTA 驾驶大模型的全频谱，评估较为客观。

#### 6. 论文的主要结论与发现
*   **性能领先：** KLDrive 在 NuScenes-QA 上达到 65.04% 的总准确率，在 GVQA 上 SPICE 分数达到 42.45，均优于经过微调的 SOTA 模型。
*   **显著抑制幻觉：** 在最难的**计数（Counting）**任务中，KLDrive 准确率达到 64.46%，比最强基准（18.45%）高出 **46.01 个百分点**。
*   **感知质量是瓶颈：** KLDrive-GT 的准确率高达 84.49%，说明如果感知层能提供完美事实，该推理框架的表现将大幅提升。
*   **推理透明度：** 证明了通过显式工具调用，可以将复杂的自然语言查询转化为可审计的符号执行链。

#### 7. 优点
*   **架构创新：** 首次将 3D 场景知识图谱与 LLM 代理结合用于驾驶问答，平衡了感知的结构化与语言模型的灵活性。
*   **高可靠性：** 通过能量模型精炼事实，并利用受限动作空间强制 LLM “据实说话”，极大减少了幻觉。
*   **无需微调：** 仅靠 Few-shot ICL 即可适配复杂任务，降低了部署成本。
*   **可解释性：** 推理过程是显式的符号操作，每一步都可追溯到具体的感知实体。

#### 8. 不足与局限
*   **实时性挑战：** 在 RTX A6000 上平均延迟为 1.26s，在边缘端 Orin NX 上延迟高达 57s，目前仅适用于**离线分析**，无法直接用于实时在线决策。
*   **感知依赖：** 系统的最终表现高度依赖于底层检测器的质量，感知层的漏检或误检会直接传导至推理层。
*   **空间关系简化：** 空间推理依赖于预定义的方位词（如 front, left），对于更复杂的几何拓扑关系处理能力尚待验证。

（完）
