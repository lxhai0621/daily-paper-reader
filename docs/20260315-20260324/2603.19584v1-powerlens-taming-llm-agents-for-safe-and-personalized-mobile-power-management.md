---
title: "PowerLens: Taming LLM Agents for Safe and Personalized Mobile Power Management"
title_zh: PowerLens：面向安全且个性化移动电源管理的 LLM 智能体调控系统
authors: "Xingyu Feng, Chang Sun, Yuzhu Wang, Zhangbing Zhou, Chengwen Luo, Zhuangzhuang Chen, Xiaomin Ouyang, Huanqi Yang"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19584v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 用于个性化移动电源管理的多智能体架构
tldr: PowerLens是一个基于大语言模型（LLM）的Android移动设备电源管理系统。它利用LLM的常识推理能力，通过多智能体架构识别用户UI语义和上下文，生成涵盖18个参数的个性化节能策略。系统引入PDL约束框架确保安全性，并利用双层记忆系统从用户行为中隐式学习偏好。实验显示其节能效果显著且系统开销极低。
selection_source: fresh_fetch
motivation: 现有的移动设备电源管理依赖静态规则，无法适应复杂的用户活动和个性化偏好。
method: 采用多智能体架构结合UI语义识别上下文，并通过PDL约束框架和双层记忆系统实现安全、零配置的个性化策略生成。
result: "在Android设备上实现了81.7%的动作准确率和38.8%的节能提升，且系统自身日耗电量仅为0.5%。"
conclusion: PowerLens证明了利用LLM进行细粒度、安全且个性化的移动电源管理是高效且可行的。
---

## 摘要
电池续航仍是移动设备面临的关键挑战，然而现有的电源管理机制依赖于静态规则或粗粒度的启发式方法，忽略了用户活动和个人偏好。我们提出了 PowerLens，这是一个在 Android 设备上利用大语言模型（LLM）推理能力实现安全且个性化移动电源管理的系统。其核心思想是，LLM 的常识推理可以弥合用户活动与系统参数之间的语义鸿沟，从而实现零样本、上下文感知的策略生成，并通过隐式反馈适应个人偏好。PowerLens 采用多智能体架构，从 UI 语义中识别用户上下文，并针对 18 个设备参数生成全局电源策略。一个基于 PDL 的约束框架在执行前验证每个动作，而一个两层记忆系统通过基于置信度的蒸馏从隐式用户覆盖中学习个性化偏好，无需显式配置，且在 3-5 天内即可收敛。在已获取 root 权限的 Android 设备上进行的广泛实验表明，PowerLens 实现了 81.7% 的动作准确率，且相比原生 Android 系统节能 38.8%，优于基于规则和基于 LLM 的基准方案，同时具有较高的用户满意度、快速的偏好收敛和强大的安全保障，且系统自身仅消耗每日电池容量的 0.5%。

## Abstract
Battery life remains a critical challenge for mobile devices, yet existing power management mechanisms rely on static rules or coarse-grained heuristics that ignore user activities and personal preferences. We present PowerLens, a system that tames the reasoning power of Large Language Models (LLMs) for safe and personalized mobile power management on Android devices. The key idea is that LLMs' commonsense reasoning can bridge the semantic gap between user activities and system parameters, enabling zero-shot, context-aware policy generation that adapts to individual preferences through implicit feedback. PowerLens employs a multi-agent architecture that recognizes user context from UI semantics and generates holistic power policies across 18 device parameters. A PDL-based constraint framework verifies every action before execution, while a two-tier memory system learns individualized preferences from implicit user overrides through confidence-based distillation, requiring no explicit configuration and converging within 3--5 days. Extensive experiments on a rooted Android device show that PowerLens achieves 81.7% action accuracy and 38.8% energy saving over stock Android, outperforming rule-based and LLM-based baselines, with high user satisfaction, fast preference convergence, and strong safety guarantees, with the system itself consuming only 0.5% of daily battery capacity.

---

## 论文详细总结（自动生成）

### PowerLens: 面向安全且个性化移动电源管理的 LLM 智能体调控系统

#### 1. 核心问题与整体含义（研究动机和背景）
移动设备的电池续航一直是用户痛点。现有的电源管理机制存在三大局限：
*   **缺乏语义理解**：硬件级（如 DVFS）和系统级（如 Android Adaptive Battery）优化通常基于粗粒度的启发式规则，无法理解用户当前活动的具体需求（例如：同样是高 CPU 负载，视频会议需要保证流畅，而后台索引则可以降频）。
*   **忽视个性化偏好**：不同用户对屏幕亮度、刷新率的容忍度不同，现有系统难以自动适配。
*   **优化与安全的冲突**：激进的节能策略可能导致关键功能失效（如在导航时关闭 GPS）。
**PowerLens** 的研究动机在于利用大语言模型（LLM）的常识推理能力，弥合用户活动语义与底层系统参数（如 CPU、GPS、显示等）之间的鸿沟，实现零样本、上下文感知的个性化电源管理。

#### 2. 方法论：核心思想与关键技术
PowerLens 采用多智能体架构，通过闭环控制系统管理 18 个关键系统参数：
*   **核心思想**：将电源管理分解为“感知-决策-校验-学习”四个阶段，利用 LLM 作为系统级推理引擎。
*   **多智能体架构**：
    *   **Activity Agent**：通过 Android 辅助功能框架获取 UI 树，识别用户当前的高级活动（如导航、阅读）及细粒度意图。
    *   **Policy Agent**：结合设备状态、用户偏好和安全约束，生成涵盖 18 个参数的全局策略。
    *   **Execution Agent**：负责合法性验证（基于 PDL）并将策略转化为 root shell 命令执行。
    *   **Feedback Agent**：通过“状态差异化”检测用户的手动覆盖行为（如系统调暗亮度后用户又调亮），作为隐式反馈。
*   **PDL 约束框架**：引入命题动态逻辑（Propositional Dynamic Logic）定义安全不变式（如 `App=导航 -> GPS必须开启`），在执行前强制拦截 LLM 的幻觉或错误决策。
*   **双层记忆系统（STM/LPM）**：
    *   **短期记忆 (STM)**：记录当前会话的锁定状态。
    *   **长期个人记忆 (LPM)**：通过异步的“意图分析”和“置信度蒸馏”算法，将重复的用户行为转化为持久的偏好规则，通常在 3-5 天内收敛。

#### 3. 实验设计
*   **实验平台**：已获取 Root 权限的 OnePlus ACE 5（骁龙 8 Gen 3），运行 Android 15。
*   **数据集/场景 (PowerLensBench)**：涵盖 7 大类应用（导航、视频、会议、社交、音乐、信息流、阅读），包含 25 个主流 App 和 48 个典型任务，共 144 个测试实例。
*   **用户画像**：定义了 5 种合成用户画像（如游戏玩家、通勤者、学生等）以评估个性化效果。
*   **对比方法**：
    1.  原生 Android 系统（Stock Android）。
    2.  Android 内置省电模式（Battery Saver）。
    3.  基于静态规则的方法（Rule-Based）。
    4.  单智能体 LLM 方案（Single-Agent LLM）。

#### 4. 资源与算力
*   **LLM 后端**：默认使用 **Gemini-2.5-Flash**。论文对比了 GPT-4o、GLM-4.7、Kimi-K2 等 8 个模型。
*   **算力说明**：文中未提及本地训练 GPU，因为系统主要依赖云端 API 推理。
*   **开销数据**：
    *   **延迟**：完整决策周期平均 12.2 秒。
    *   **成本**：单次决策约 0.005 美元。
    *   **能耗**：系统自身运行仅占每日电量的 **0.5%**，远低于其节省的电量。

#### 5. 实验数量与充分性
*   **实验规模**：进行了 240 组自动化评估实例（48 任务 × 5 画像），以及为期 14 天、涉及 10 名真实用户的实地研究（In-the-wild study）。
*   **消融实验**：详细测试了多智能体架构、记忆系统、PDL 约束、反馈机制对准确率和节能效果的贡献。
*   **安全性测试**：通过对抗性提示（Adversarial Prompts）测试系统在极端情况下的防御能力。
*   **评价**：实验设计全面，结合了自动化 Benchmark 和真实用户反馈，使用了合成画像生成 Ground Truth 以保证评估的客观性，实验充分且公平。

#### 6. 主要结论与发现
*   **节能显著**：相比原生 Android 平均节能 **38.8%**，远超省电模式（4.6%）和规则方法（19.9%）。
*   **准确性高**：动作准确率达到 **81.7%**，用户体验评分（UES）为 4.3/5.0。
*   **安全性强**：PDL 框架消除了 **96.5%** 的原始 LLM 安全违规，最终违规率仅 0.6%。
*   **收敛迅速**：个性化偏好在 3-5 天内即可通过隐式反馈完成学习，无需用户手动配置。

#### 7. 优点
*   **首创性**：首次将 LLM 智能体应用于系统级资源管理，而非简单的 UI 自动化。
*   **隐式学习机制**：通过状态差异检测实现“无感”个性化，极大地提升了用户接受度。
*   **安全保障**：引入形式化的 PDL 约束，解决了 LLM 在关键系统任务中的不可靠问题。
*   **极低开销**：通过异步处理和事件触发机制，将复杂的 LLM 推理开销控制在极低范围内。

#### 8. 不足与局限
*   **权限依赖**：系统需要 Root 权限才能控制底层参数，这限制了其在普通消费者设备上的直接部署。
*   **隐私风险**：虽然有 PII 过滤，但将 UI 语义上传至云端 LLM 仍存在潜在的隐私担忧。
*   **反馈盲区**：状态差异化只能捕获用户“可感知”的参数变化，对于不可见或延迟生效的参数（如后台进程限制），隐式反馈难以奏效。
*   **延迟问题**：12.2 秒的决策延迟对于某些瞬时切换的场景可能略显滞后。

（完）
