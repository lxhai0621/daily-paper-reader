---
title: "AgentHER: Hindsight Experience Replay for LLM Agent Trajectory Relabeling"
title_zh: AgentHER：用于大语言模型智能体轨迹重标记的事后经验重放
authors: Liang Ding
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21357v1"
tags: ["query:agent"]
score: 9.0
evidence: 用于智能体轨迹重标记的后视经验回放
tldr: AgentHER 针对 LLM 智能体在复杂任务中失败率高且失败轨迹被浪费的问题，借鉴强化学习中的事后经验重放（HER）思想，提出了一种将失败轨迹重新标记为成功轨迹的框架。该框架通过失败分类、结果提取、重标记和数据打包四个阶段，将原本丢弃的失败数据转化为高质量的训练数据。实验表明，AgentHER 在 WebArena 和 ToolBench 上显著提升了模型性能，并实现了 2 倍的数据效率提升。
selection_source: fresh_fetch
motivation: 现有的 LLM 智能体在处理复杂任务时失败率极高，而这些失败的轨迹通常被直接丢弃，导致了大量潜在训练数据的浪费。
method: 提出 AgentHER 框架，通过四个阶段将失败轨迹的目标修改为智能体实际达成的结果，从而将失败经验转化为高质量的 SFT 和 DPO 训练数据。
result: "在多个模型系列上，AgentHER 相比仅使用成功案例的 SFT 提升了 7.1-11.7 个百分点，并以 50% 的成功数据量达到了基准性能。"
conclusion: AgentHER 证明了通过事后重标记失败轨迹可以极大地提高 LLM 智能体的训练效率和最终性能，是离线数据增强的有效手段。
---

## 摘要
大语言模型（LLM）智能体在大多数现实任务中都会失败——GPT-4o 在 WebArena 导航任务中的成功率不足 15%，在 ToolBench 上的 pass@1 低于 55%（Zhou 等，2024；Qin 等，2024）——然而，每一条失败的轨迹通常都会被丢弃，浪费了所收集经验的主要来源。我们引入了 AgentHER，这是一个通过将事后经验重放（HER；Andrychowicz 等，2017）原理应用于自然语言智能体轨迹，从而恢复这些丢失的训练信号的框架，用于离线数据增强。其核心见解很简单：一条未能完成目标 A 的轨迹，往往是某个可实现的替代目标 B 的正确演示。AgentHER 通过一个四阶段流水线实现这一想法——失败分类、结果提取、带置信度门控的 LLM 指导提示词重标记以及数据打包——将丢弃的失败案例转化为高质量的 SFT、DPO 和 ShareGPT 训练数据，并提供零成本的基于规则和基于 LLM 裁判的两种实现方式。在 WebArena（Zhou 等，2024）和 ToolBench（Qin 等，2024）上，AgentHER 在四个模型系列（GPT-4o、Qwen2.5-72B/7B、LLaMA-3.1-8B）中，相比仅使用成功案例的 SFT 提升了 7.1-11.7 个百分点（pp），同时实现了 2 倍的数据效率——仅需 50% 的成功演示即可达到基线性能。在 1.5B 到 72B 参数规模下，收益保持一致（+5.8-9.2 pp），并在迭代重新部署下具有复合效应（在额外轮次中提升 +2.1 pp）。人工评估证实，在多裁判验证下，重标记精度达到 97.7%。

## Abstract
LLM agents fail on the majority of real-world tasks -- GPT-4o succeeds on fewer than 15% of WebArena navigation tasks and below 55% pass@1 on ToolBench (Zhou et al., 2024; Qin et al., 2024) -- yet every failed trajectory is routinely discarded, wasting the dominant source of collected experience. We introduce AgentHER, a framework that recovers this lost training signal by adapting the Hindsight Experience Replay (HER; Andrychowicz et al., 2017) principle to natural-language agent trajectories for offline data augmentation. The key insight is simple: a trajectory that fails goal A is often a correct demonstration for some achievable alternative goal B. AgentHER realises this idea through a four-stage pipeline -- failure classification, outcome extraction, LLM-guided prompt relabeling with confidence gating, and data packaging -- that converts discarded failures into high-quality SFT, DPO, and ShareGPT training data, with both zero-cost rule-based and LLM-judge implementations. On WebArena (Zhou et al., 2024) and ToolBench (Qin et al., 2024), AgentHER improves over success-only SFT by +7.1-11.7 pp across four model families (GPT-4o, Qwen2.5-72B/7B, LLaMA-3.1-8B), while achieving 2x data efficiency -- matching baseline performance with only 50% of successful demonstrations. Gains are consistent from 1.5B to 72B parameters (+5.8-9.2 pp) and compound under iterative redeployment (+2.1 pp over additional rounds). Human evaluation confirms 97.7% relabeling precision under multi-judge verification.

---

## 论文详细总结（自动生成）

### AgentHER：用于大语言模型智能体轨迹重标记的事后经验重放 —— 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
当前大语言模型（LLM）智能体在处理现实世界复杂任务（如网页导航、API 调用）时失败率极高（如 GPT-4o 在 WebArena 上的成功率不足 15%）。传统的训练流水线通常直接丢弃这些失败的执行轨迹，导致了巨大的数据浪费。
**研究动机**：论文指出，失败的轨迹并非随机噪声，它们往往包含连贯的推理和正确的中间步骤。借鉴强化学习中的**事后经验重放（Hindsight Experience Replay, HER）**思想，作者提出：一个未能完成目标 A 的轨迹，实际上可能是另一个目标 B 的完美演示。通过重标记（Relabeling）这些失败轨迹，可以变废为宝，大幅提升训练数据的规模和质量。

#### 2. 方法论：核心思想与关键技术
AgentHER 提出了一个自动化的四阶段离线流水线，将失败轨迹转化为高质量的训练数据：
*   **阶段 1：失败检测器（Failure Detector）**：对失败轨迹进行分类（如结果错误、工具错误、幻觉等），并评估其“可恢复性”。引入了基于 MQM 风格的**严重程度加权（Severity Weighting）**，剔除存在灾难性逻辑错误的轨迹，保留仅是未满足约束的轨迹。
*   **阶段 2：结果提取器（Outcome Extractor）**：总结智能体在轨迹中实际达成的成就（事实、数据、调用的工具），作为重标记的锚点。
*   **阶段 3：提示词重标记器（Prompt Relabeler）**：利用 LLM 根据实际结果合成一个新的、自然的、且该轨迹能完美实现的“事后目标”。
    *   **关键技术：多裁判验证（Multi-judge Verification）**：要求两个独立的 LLM 裁判达成一致才接受重标记，将标签噪声从 5.9% 降低到 2.3%。
*   **阶段 4：数据打包器（Data Augmenter）**：将验证后的（新目标, 轨迹）对转化为 SFT（监督微调）或 DPO（直接偏好优化）格式。
    *   **公式细节**：在 DPO 中，作者反转了传统逻辑，保持轨迹固定，对比“事后目标”（Chosen）和“原始失败目标”（Rejected），并使用严重程度权重 $w$ 缩放损失函数。

#### 4. 实验设计
*   **数据集/场景**：
    *   **WebArena**：812 个复杂的网页自动化任务。
    *   **ToolBench**：包含 16,000+ API 调用任务。
*   **Benchmark 与对比方法**：
    *   **主要基准**：SFT-Success（仅使用成功案例进行微调，代表当前主流的最优离线训练方法）。
    *   **对比方法**：Base (Zero-shot)、SFT-Random（等量失败数据但不重标记）、Rejection-Sampling（仅过滤不重标记）、Reflexion（推理时自我反思方法）。
*   **评估模型**：涵盖 GPT-4o、Qwen2.5 (1.5B, 7B, 14B, 72B) 和 LLaMA-3.1-8B。

#### 5. 资源与算力
*   **算力设备**：使用了 **8 台 NVIDIA A100-80G GPU**。
*   **训练细节**：
    *   开源模型使用 LoRA 微调（Rank 16, Alpha 32）。
    *   SFT 训练 3 个 Epoch，DPO 训练 1 个 Epoch。
    *   使用 `ms-swift` 框架进行微调。
    *   GPT-4o 通过 OpenAI 的微调 API 进行训练。

#### 6. 实验数量与充分性
*   **实验规模**：论文在 2 个基准数据集上测试了 4 个模型家族，参数跨度从 1.5B 到 72B。
*   **消融实验**：非常详尽，分别验证了多裁判机制、严重程度加权、置信度过滤、DPO 信号等组件的贡献。
*   **充分性与公平性**：
    *   所有微调结果均取 3 个随机种子的平均值（标准差 < 0.5 pp），确保了数值稳定性。
    *   设置了 SFT-Random 作为等量数据控制组，证明了性能提升源于“重标记”而非单纯的数据量增加。
    *   进行了跨基准迁移实验（WebArena 训练，ToolBench 测试），证明了泛化能力。

#### 7. 论文的主要结论与发现
*   **显著提升**：AgentHER 相比 SFT-Success 在 WebArena 和 ToolBench 上分别提升了 **7.1–11.7 pp**。
*   **高数据效率**：实现了 **2 倍的数据效率**，即仅使用 50% 的成功案例加上重标记的失败案例，即可达到 100% 成功案例的训练效果。
*   **规模效应**：性能提升在不同参数规模（1.5B 到 72B）下保持一致，且在迭代训练（多轮收集失败并重标记）中具有复合增益。
*   **高精度**：人工评估显示，重标记的精度高达 **97.7%**。

#### 8. 优点
*   **变废为宝**：解决了智能体训练中最大的痛点——高失败率导致的数据浪费。
*   **离线增强**：不需要与环境进行额外的实时交互，降低了训练成本。
*   **鲁棒性设计**：多裁判验证和严重程度加权机制极大地提高了合成数据的质量，使其达到生产级标准。
*   **通用性**：方法不依赖特定模型或任务，具有很强的普适性。

#### 9. 不足与局限
*   **任务集重叠风险**：在 WebArena 实验中，训练和评估使用了相同的 812 个页面环境，虽然目标不同，但模型可能学到了特定页面的 HTML 结构（存在一定程度的 Task-set Leakage）。
*   **理论假设**：理论证明部分假设存在“完美裁判”，而现实中 LLM 裁判仍有噪声。
*   **计算开销**：重标记过程需要多次调用 LLM（虽然可以使用较小的模型如 gpt-4o-mini），在极大规模数据下仍有一定推理成本。
*   **失败模式限制**：对于完全崩溃（如 Tool Error）的轨迹，该方法无法提取有效信息，提升有限。

（完）
