---
title: Coding Agents are Effective Long-Context Processors
title_zh: 编程智能体是有效的长上下文处理器
authors: "Weili Cao, Xunjian Yin, Bhuwan Dhingra, Shuyan Zhou"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20432v1"
tags: ["query:ma-kf"]
score: 10.0
evidence: 用于长文本处理和 RAG 的编码智能体
tldr: "针对大语言模型在处理超长上下文时性能下降且注意力机制不可解释的问题，本文提出将长文本处理从隐式的注意力机制转变为显式的编码代理交互。通过让编码代理利用文件系统组织文本并使用原生工具（如代码和终端命令）进行操作，该方法在长文本推理、RAG和大规模问答任务中表现优异，平均超越现有最先进水平17.3%，为长文本处理提供了除扩展上下文窗口之外的新路径。"
selection_source: fresh_fetch
motivation: 现有的LLM在处理长上下文时依赖不可解释的注意力机制，且随着长度增加性能显著下降。
method: 将编码代理作为通用接口，利用文件系统管理文本并调用可执行代码和终端工具进行显式处理。
result: "在多个基准测试中，该方法平均超越现有最先进技术17.3%，并能处理高达三万亿token的大规模语料。"
conclusion: 将长文本处理委托给具备工具使用和文件系统导航能力的编码代理，是替代语义搜索或单纯扩展上下文窗口的有效方案。
---

## 摘要
大语言模型（LLMs）在扩展以访问海量上下文方面取得了显著进展。然而，这种访问是通过潜在且不可解释的注意力机制实现的，且 LLMs 无法有效处理长上下文，随着上下文长度的增加，性能会出现显著下降。在这项工作中，我们研究了是否可以通过允许编程智能体在文件系统中组织文本并使用其原生工具进行操作，将长上下文处理从潜在注意力外部化为显式的、可执行的交互。我们评估了现成的尖端编程智能体作为处理长上下文任务的通用接口，包括长上下文推理、检索增强生成以及包含高达三万亿 token 的大规模语料库的开放域问答。在多个基准测试中，这些智能体的表现平均优于已发表的最先进方法 17.3%。我们将这种功效归因于两个关键因素：原生工具熟练度，使智能体能够利用可执行代码和终端命令而非被动的语义查询；以及文件系统熟悉度，使它们能够像处理目录结构一样导航大规模文本语料库。这些发现表明，将长上下文处理委托给编程智能体为语义搜索或上下文窗口扩展提供了一种有效的替代方案，为 LLMs 的长上下文处理开辟了新方向。

## Abstract
Large Language Models (LLMs) have demonstrated remarkable progress in scaling to access massive contexts. However, the access is via the latent and uninterpretable attention mechanisms, and LLMs fail to effective process long context, exhibiting significant performance degradation as context length increases. In this work, we study whether long-context processing can be externalized from latent attention into explicit, executable interactions, by allowing coding agents to organize text in file systems and manipulate it using its native tools. We evaluate off-the-shelf frontier coding agents as the general interface for tasks that require processing long contexts, including long-context reasoning, retrieval-augmented generation, and open-domain question answering with large-scale corpus contains up to three trillion tokens. Across multiple benchmarks, these agents outperform published state-of-the-art by 17.3% on average. We attribute this efficacy to two key factors: native tool proficiency, which enables agents to leverage executable code and terminal commands rather than passive semantic queries, and file system familiarity, which allows them to navigate massive text corpora as directory structures. These findings suggest that delegating long-context processing to coding agents offers an effective alternative to semantic search or context window scaling, opening new directions for long-context processing in LLMs.

---

## 论文详细总结（自动生成）

### 论文结构化深度总结：《Coding Agents are Effective Long-Context Processors》

#### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：尽管大语言模型（LLM）的上下文窗口不断扩大，但其处理长文本的能力仍受限于**隐式且不可解释的注意力机制**。随着上下文增长，模型会出现“上下文腐烂（Context Rot）”现象，即性能显著下降。
*   **研究动机**：现有的 RAG（检索增强生成）系统依赖固定的检索管道，缺乏处理复杂多步推理的灵活性。作者观察到，**编程智能体（Coding Agents）**在处理大规模代码库时表现出极强的导航和操作能力，因此提出：能否将长文本处理任务转化为编程智能体熟悉的“文件系统操作”任务，从而实现更高效、可解释的长上下文处理？

#### 2. 论文提出的方法论
*   **核心思想**：将长文本语料库重构为**可导航的文件系统结构**，并委派现成的编程智能体利用其原生工具（终端命令、Python 脚本）进行显式处理。
*   **关键技术细节**：
    *   **语料库格式化**：将大规模语料（>100M tokens）拆分为单个 `.txt` 文件并组织在目录层级中；单篇长文档则存为单个文件。
    *   **智能体接口**：使用现成的编程智能体（如 OpenAI Codex、Claude Code），仅提供文件路径和查询指令。
    *   **原生工具调用**：智能体自主决定使用 `grep`、`ripgrep`、`sed`、`head` 等终端命令进行快速搜索，或编写 Python 脚本进行复杂的正则匹配和数据聚合。
    *   **迭代细化**：智能体根据初步搜索结果，自主发现领域特定信息并不断修正查询逻辑（如多步推理中的实体链追踪）。

#### 3. 实验设计
*   **数据集/场景**：
    *   **BrowseComp-Plus (750M tokens)**：复杂的 Web 浏览多步问答。
    *   **LongBench-v2 (188K tokens)**：涵盖单/多文档 QA、代码仓库理解等。
    *   **Oolong (Real/Synthetic, 385K-536K tokens)**：长文档推理与信息聚合。
    *   **Natural Questions (NQ, 3T tokens)**：超大规模语料库的开放域问答。
*   **对比方法（Baselines）**：
    *   **GPT-5 Full Context**：直接将全文（或滑动窗口）输入模型。
    *   **标准 RAG**：基于 Gemini 嵌入或 BM25 的固定检索管道。
    *   **ReAct Agent**：受限于预定义 API 的搜索智能体。
    *   **Recursive Language Model (RLM)**：通过递归调用自身处理文本片段的方法。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **原因**：该研究主要基于**现成的（Off-the-shelf）**闭源模型 API（如 OpenAI 的 GPT-5 驱动的 Codex 和 Anthropic 的 Claude 3.5 Sonnet 驱动的 Claude Code）进行推理实验，而非从头训练模型。
*   **成本分析**：文中提供了详细的推理成本分析，显示编程智能体的平均查询成本高于 RAG，但通常低于或等同于 RLM 等强基线方法。

#### 5. 实验数量与充分性
*   **实验规模**：在 5 个具有代表性的长上下文基准测试上进行了评估，涵盖了从 188K 到 3T token 的极端跨度。
*   **消融实验**：
    *   **文件系统结构影响**：对比了“文件夹结构”与“单 JSON 文件结构”，证明了文件系统导航能力的必要性。
    *   **检索工具的影响**：测试了加入 BM25/向量检索后智能体行为的变化。
    *   **策略分析**：量化分析了智能体在不同任务中的搜索强度、阅读量和代码生成量。
*   **充分性评价**：实验设计较为全面，通过跨模型（Codex 和 Claude Code）验证了结论的普适性，对比基线涵盖了当前主流的长文本处理范式，结果具有较高的客观性和说服力。

#### 6. 论文的主要结论与发现
*   **性能卓越**：编程智能体在 4/5 个基准测试中刷新了 SOTA，平均性能提升 **17.3%**。
*   **核心驱动力**：成功归功于**原生工具熟练度**（精准的可执行交互）和**文件系统熟悉度**（强大的导航先验）。
*   **涌现策略**：智能体能根据任务自主切换策略——在多步问答中进行“迭代查询细化”，在分析任务中进行“程序化聚合”，在阅读理解中进行“混合搜索-阅读”。
*   **反直觉发现**：为编程智能体配备标准的检索工具（如向量检索）并不总能提升性能，有时反而会抑制智能体更深入的自主探索。

#### 7. 优点
*   **范式创新**：将长文本处理从“模型内部注意力”转向“外部环境交互”，提高了处理过程的可解释性和透明度。
*   **通用性强**：无需针对特定任务进行微调或复杂的 Prompt 工程，直接利用编程智能体的通用能力。
*   **可扩展性**：能够处理高达 3 万亿 token 的语料，远超目前主流长上下文模型的处理极限。

#### 8. 不足与局限
*   **推理延迟与成本**：相比于单次检索的 RAG，智能体的多轮交互会导致更高的延迟和 API 调用成本。
*   **环境依赖**：该方法依赖于一个功能完备的执行环境（终端、Python 解释器），在某些受限的应用场景中难以部署。
*   **模型依赖**：目前的效果高度依赖于顶尖的编程模型（如 GPT-5 级别），在较弱的开源模型上可能无法复现同等水平的自主策略涌现。
*   **检索抑制风险**：如何将传统高效检索与智能体自主探索有机结合，仍是一个未解决的挑战。

（完）
