---
title: A Context Engineering Framework for Improving Enterprise AI Agents based on Digital-Twin MDP
title_zh: 一种基于数字孪生马尔可夫决策过程（DT-MDP）的提升企业级 AI 智能体性能的上下文工程框架
authors: "Xi Yang, Aurelie Lozano, Naoki Abe, Bhavya, Saurabh Jha, Noah Zheutlin, Rohan R. Arora, Yu Deng, Daby M. Sow"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22083v1"
tags: ["query:agent"]
score: 9.0
evidence: 离线强化学习用于改进基于大模型的企业智能体
tldr: 针对企业级AI智能体在复杂推理和数据质量方面的挑战，本文提出了一种基于数字孪生马尔可夫决策过程（DT-MDP）的上下文工程框架DT-MDP-CE。该框架通过将智能体推理行为抽象为有限MDP，利用对比逆强化学习从混合质量的离线轨迹中学习奖励函数和策略，并以此指导上下文工程以优化决策。在IT自动化任务中的实验证明，该框架能显著提升智能体性能并具有良好的泛化性。
selection_source: fresh_fetch
motivation: 旨在解决企业级AI智能体在实际部署中面临的数据质量受限、推理逻辑复杂以及缺乏可靠反馈信号等核心难题。
method: 提出DT-MDP-CE框架，通过数字孪生建模推理过程，并利用对比逆强化学习从离线数据中提取策略以指导上下文工程。
result: 在IT自动化领域的广泛实验表明，该框架在多种评估指标上均显著且持续地优于现有的基准智能体。
conclusion: 该框架提供了一种轻量级且模型无关的方案，能有效提升企业环境下智能体的决策能力，并具备推广至类似场景的潜力。
---

## 摘要
尽管用于企业自动化和决策的 AI 智能体取得了快速进展，但由于数据质量和数量有限、现实世界推理需求复杂、自我博弈（self-play）困难以及缺乏可靠的反馈信号，它们的实际部署和进一步的性能提升仍受到限制。为了应对这些挑战，我们提出了一种轻量级、模型无关的框架，通过离线强化学习（RL）来改进基于大语言模型（LLM）的企业级智能体。所提出的基于 DT-MDP 的上下文工程（DT-MDP-CE）框架包含三个关键组件：（1）数字孪生马尔可夫决策过程（DT-MDP），它将智能体的推理行为抽象为有限 MDP；（2）鲁棒的对比逆向强化学习（inverse RL），利用 DT-MDP 高效估计合理的奖励函数，并从质量参差不齐的离线轨迹中推导策略；（3）强化学习引导的上下文工程，利用从（1）和（2）的集成过程中获得的策略来改进智能体的决策行为。作为案例研究，我们将该框架应用于面向企业的 IT 自动化领域中的一个代表性任务。广泛的实验结果表明，在各种评估设置下，该框架相比基准智能体具有持续且显著的改进，这表明该框架可以推广到企业环境中具有类似特征的其他智能体。

## Abstract
Despite rapid progress in AI agents for enterprise automation and decision-making, their real-world deployment and further performance gains remain constrained by limited data quality and quantity, complex real-world reasoning demands, difficulties with self-play, and the lack of reliable feedback signals. To address these challenges, we propose a lightweight, model-agnostic framework for improving LLM-based enterprise agents via offline reinforcement learning (RL). The proposed Context Engineering via DT-MDP (DT-MDP-CE) framework comprises three key components: (1) A Digital-Twin Markov Decision Process (DT-MDP), which abstracts the agent's reasoning behavior as a finite MDP; (2) A robust contrastive inverse RL, which, armed with the DT-MDP, to efficiently estimate a well-founded reward function and induces policies from mixed-quality offline trajectories; and (3) RL-guided context engineering, which uses the policy obtained from the integrated process of (1) and (2), to improve the agent's decision-making behavior. As a case study, we apply the framework to a representative task in the enterprise-oriented domain of IT automation. Extensive experimental results demonstrate consistent and significant improvements over baseline agents across a wide range of evaluation settings, suggesting that the framework can generalize to other agents sharing similar characteristics in enterprise environments.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **DT-MDP-CE** 的框架，旨在通过数字孪生建模和离线强化学习来优化企业级 AI 智能体（Agent）的性能。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：企业级 AI 智能体（如 IT 自动化、SRE 诊断）在实际部署中面临四大挑战：
    1.  **数据瓶颈**：高质量标注数据稀缺且具有私密性。
    2.  **推理复杂**：现实任务涉及多轮、长链条的复杂推理。
    3.  **环境限制**：难以进行大规模在线交互或自我博弈（Self-play）。
    4.  **反馈稀疏**：缺乏可靠、及时的验证信号（Reward）。
*   **研究动机**：现有的微调（SFT）或在线强化学习（RL）方法在企业环境下成本过高且难以实施。作者希望提出一种**轻量级、模型无关**的方案，利用有限的离线轨迹数据来提升智能体的决策质量。

### 2. 方法论：DT-MDP-CE 框架
该框架由三个核心组件构成：
*   **数字孪生 MDP (DT-MDP)**：
    *   **核心思想**：将 LLM 智能体在无限文本空间中的推理行为抽象为有限的马尔可夫决策过程（MDP）。
    *   **技术细节**：通过确定性的抽象函数，将观察（Observations）映射为抽象状态（States），将想法（Thoughts）映射为抽象动作（Actions）。论文探索了基于名称、名称类型及**拓扑结构（Topology-based）**的多种抽象方式。
*   **鲁棒对比逆强化学习 (Robust Contrastive IRL)**：
    *   **核心思想**：利用 T-REX 算法，通过对轨迹进行两两排序（Pairwise Ranking）来学习奖励函数，而非手动设计。
    *   **优势**：能够从质量参差不齐（混合了成功与失败）的离线数据中学习，避免了对“专家数据”的过度依赖。
*   **强化学习引导的上下文工程 (RL-guided Context Engineering)**：
    *   **核心思想**：不改变 LLM 参数，而是利用学习到的 RL 策略在推理时干预上下文。
    *   **干预策略**：
        1.  **提示建议 (Suggesting)**：在 Prompt 中加入 RL 推荐的动作。
        2.  **剪枝过滤 (Pruning)**：过滤掉 RL 认为低概率的无效探索。
        3.  **优先级排序 (Prioritizing)**：重新排列智能体的待处理任务队列。

### 3. 实验设计
*   **场景与数据集**：
    *   **领域**：IT 自动化中的站点可靠性工程（SRE）诊断任务。
    *   **数据集**：使用 **ITBench** 基准，包含 Flagd、Chaos Mesh 及自定义企业故障场景。
    *   **数据规模**：收集了 819 条轨迹（约 1.2 万轮交互），涵盖 GPT-4o、Mistral、Granite 等多种模型的运行记录。
*   **基准测试 (Benchmark)**：
    *   **对比方法**：原始智能体（Baseline）、行为克隆（BC）、基于稀疏奖励的 RL（RL-Sparse）。
    *   **评估指标**：Pass@3 Recall 和 Pass@3 F1 分数。
    *   **智能体架构**：EoG（基于图的解释智能体）和 ReAct 智能体。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练总时长。
*   **模型使用**：实验中使用了 Mistral-Medium-2505 作为底层推理模型，Gemini-2.5-Pro 作为评估裁判（LLM-as-a-judge）。
*   **轻量性**：作者强调该框架是“轻量级”的，因为 RL 策略是在抽象后的有限空间内训练的，而非对大模型进行全参数微调。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 6 个未见过的测试场景上进行了在线评估，每个场景重复 15 次实验。
    *   进行了跨领域（SRE 到 SWE 软件工程）的泛化性实验。
    *   针对不同模型规模（Small, Medium, Large）和不同模型家族（Mistral, Llama）进行了对比。
    *   包含消融实验（不同干预策略组合）和参数敏感性分析（阈值、专家轨迹数量）。
*   **充分性评价**：实验设计较为全面，通过统计显著性检验（Bonferroni 校正、Nemenyi 测试）证明了结果的客观性。

### 6. 主要结论与发现
*   **性能提升**：DT-MDP-CE 在所有测试场景下均显著优于基准智能体，尤其在 Pass@3 指标上有大幅提升。
*   **逆强化学习的优越性**：基于对比 IRL 学习到的中间奖励比单纯的稀疏奖励或行为克隆更有效。
*   **泛化能力**：基于拓扑结构的抽象表现出最强的跨领域迁移能力。
*   **模型规模相关性**：中等规模的模型（如 20B-70B 级别）从该框架中获益最大；极小模型受限于理解力，极大模型则本身基准较高。

### 7. 优点
*   **非侵入性**：无需微调 LLM，通过上下文工程即可实现性能增强，易于在企业现有架构中集成。
*   **数据效率**：能够利用“失败的尝试”进行学习，解决了企业数据稀缺的问题。
*   **可解释性**：通过将推理过程抽象为 MDP，使得智能体的决策逻辑更易于被人类专家理解和审计。

### 8. 不足与局限
*   **领域知识依赖**：DT-MDP 的状态和动作抽象高度依赖领域专家知识（如 SRE 中的拓扑图设计），不同领域的迁移需要重新设计抽象函数。
*   **干预力度有限**：上下文工程属于“软干预”，如果底层 LLM 的基础推理能力极差，RL 策略也难以挽救。
*   **实时性开销**：虽然论文提到开销较小，但在每一轮推理中增加 RL 策略评估和 Prompt 修改，仍会带来一定的延迟和 Token 消耗。

（完）
