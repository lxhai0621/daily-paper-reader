---
title: "P^2O: Joint Policy and Prompt Optimization"
title_zh: P^2O：联合策略与提示优化
authors: "Xinyu Lu, Kaiqi Zhang, Jinglin Yang, Boxi Cao, Yaojie Lu, Hongyu Lin, Min He, Xianpei Han, Le Sun"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21877v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 在强化学习中协同提示词优化与策略优化
tldr: 针对大语言模型在强化学习（RLVR）中面临的“困难样本”探索效率低、缺乏监督信号的问题，本文提出了P^2O框架。该框架将提示词优化与策略优化相结合，利用遗传帕累托算法（GEPA）为困难样本进化出更优的提示模板，引导模型发现成功路径，并将这些推理增益蒸馏至模型参数中。实验证明，P^2O在分布内和分布外基准测试中均表现优异，显著提升了模型的泛化能力。
selection_source: fresh_fetch
motivation: 传统的强化学习在处理成功率接近零的困难样本时，由于奖励稀疏导致模型难以获得有效的监督信号。
method: 提出P^2O框架，通过GEPA算法优化提示词以引导模型探索困难样本，并将优化后的推理能力直接蒸馏到模型参数中。
result: "实验显示P^2O在分布内数据集上表现卓越，并在分布外基准测试中平均提升了4.7%。"
conclusion: P^2O通过协同提示词与策略优化，有效解决了RLVR中的探索难题，增强了LLM的推理性能和泛化性。
---

## 摘要
具有可验证奖励的强化学习（RLVR）已成为增强大语言模型（LLM）推理能力的强大范式。然而，原始的 RLVR 面临探索效率低下的问题，特别是在面对成功率接近于零的“困难样本”时。在此类场景下，对稀疏结果奖励的依赖通常会导致零优势估计，尽管这些实例具有很高的信息价值，但实际上却使模型缺乏监督信号。为解决这一问题，我们提出了 P^2O，这是一个协同提示优化（Prompt Optimization）与策略优化（Policy Optimization）的新型框架。P^2O 在训练迭代过程中识别困难样本，并利用 GeneticPareto (GEPA) 提示优化算法来演化提示模板，引导模型发现成功的轨迹。关键在于，与依赖输入增强的传统提示工程方法不同，P^2O 将这些优化提示所带来的推理增益直接蒸馏到模型参数中。这种机制为困难样本提供了更密集的正向监督信号，并加速了收敛。广泛的实验表明，P^2O 不仅在分布内数据集上取得了卓越的性能，还表现出强大的泛化能力，在分布外基准测试中实现了显著提升（平均提升 4.7%）。

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) has emerged as a powerful paradigm for enhancing the reasoning capabilities of Large Language Models (LLMs). However, vanilla RLVR suffers from inefficient exploration, particularly when confronting "hard samples" that yield nearzero success rates. In such scenarios, the reliance on sparse outcome rewards typically results in zero-advantage estimates, effectively starving the model of supervision signals despite the high informational value of these instances. To address this, we propose P^2O, a novel framework that synergizes Prompt Optimization with Policy Optimization. P^2O identifies hard samples during training iterations and leverages the GeneticPareto (GEPA) prompt optimization algorithm to evolve prompt templates that guide the model toward discovering successful trajectories. Crucially, unlike traditional prompt engineering methods that rely on input augmentation, P^2O distills the reasoning gains induced by these optimized prompts directly into the model parameters. This mechanism provides denser positive supervision signals for hard samples and accelerates convergence. Extensive experiments demonstrate that P^2O not only achieves superior performance on in-distribution datasets but also exhibits strong generalization, yielding substantial improvements on out-of-distribution benchmarks (+4.7% avg.).

---

## 论文详细总结（自动生成）

以下是对论文《P^2O: Joint Policy and Prompt Optimization》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在具有可验证奖励的强化学习（RLVR）中，模型常面临**探索瓶颈**。当训练集中包含“困难样本”（即模型在多次尝试中成功率为零的题目）时，稀疏的奖励信号导致优势估计（Advantage）为零，模型无法获得有效的梯度反馈。
*   **研究背景**：现有的课程学习或奖励塑造方法通常依赖昂贵的人工启发式规则。而提示词优化（Prompt Optimization）虽能激发模型潜能，但通常仅限于推理阶段。
*   **整体含义**：本文提出 **$P^2O$ 框架**，旨在通过动态优化提示词来引导模型探索困难样本的解空间，并将这种通过提示词诱导出的推理能力“蒸馏”到模型参数中，实现策略与提示词的协同进化。

### 2. 方法论：核心思想与算法流程
$P^2O$ 采用**交替最大化（Alternating Maximization）**的策略，分为两个阶段：
*   **阶段 1：带上下文蒸馏的策略优化（Policy Optimization with Context Distillation）**
    *   **困难样本挖掘**：在训练迭代中，识别成功率低于阈值 $\tau$ 的样本。
    *   **上下文蒸馏**：对于困难样本，使用优化后的提示词 $z$ 辅助生成成功轨迹，但在更新模型参数时，计算的是**原始输入 $x$** 下生成该轨迹的概率梯度。这种“脱钩”机制迫使模型将提示词带来的外部引导内化为自身的参数化能力。
*   **阶段 2：进化提示词优化（Evolutionary Prompt Optimization）**
    *   **GEPA 算法**：利用参考模型作为“反思者”，分析失败案例并生成改进的提示词候选。
    *   **帕累托选择（Pareto Selection）**：维护一个多样化的提示词种群，通过帕累托前沿筛选在不同错误模式上表现优异的提示词。
    *   **贪婪分配**：为下一轮训练的困难样本分配最合适的提示词模板。

### 3. 实验设计
*   **数据集与场景**：
    *   **训练集**：从 DeepScaler（5K 样本）和 DeepMath（5K 样本，难度 $\ge 7$）中抽样的子集。
    *   **测试基准（Benchmarks）**：包含 AIME24、AIME25、AMC、MATH500、MINERVA 和 OLYMPIAD 六个高难度数学推理任务。
*   **对比方法**：
    *   基础模型：Qwen3-0.6B/1.7B/4B。
    *   基准算法：标准的 **GRPO**（Group Relative Policy Optimization）。
    *   $P^2O$ 变体：$P^2O_{\text{Self-Ref}}$（自反思）和 $P^2O_{\text{Teacher-Ref}}$（使用 Kimi-K2 作为外部老师进行反思）。

### 4. 资源与算力
*   **模型架构**：以 Qwen3-4B 为骨干网络。
*   **训练参数**：学习率 $1 \times 10^{-6}$，全局 Batch Size 为 128，最大生成长度 12k tokens，训练 5 个 Epoch。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号和数量，但提到 GRPO 算法通过取消 Critic 网络显著降低了内存开销。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同的训练分布（DeepMath 和 DeepScaler）上进行了验证，涵盖了 6 个主流数学基准测试。
*   **消融实验**：
    1.  **上下文蒸馏的必要性**：验证了如果不使用蒸馏（直接在带提示词的输入上训练），模型会产生“提示词依赖”，导致测试性能大幅下降。
    2.  **提示词多样性**：验证了在 Rollout 组内使用多样化的帕累托最优提示词比使用单一提示词效果更好。
*   **充分性评价**：实验设计较为全面，不仅对比了最终精度，还分析了训练过程中的奖励变化曲线和困难样本数量的下降趋势，客观地展示了算法的收敛过程。

### 6. 主要结论与发现
*   **性能提升**：$P^2O$ 在所有基准测试中均优于 GRPO。在极难任务（如 AIME）上提升尤为显著（平均提升约 12.3%）。
*   **泛化能力**：在分布外（OOD）基准测试中，平均性能提升了 4.7%，证明了内化推理模式的有效性。
*   **反思源的影响**：在某些任务中，自反思（Self-Ref）甚至优于外部老师反思（Teacher-Ref），说明模型有时更易于内化符合自身推理逻辑的改进建议。

### 7. 优点（亮点）
*   **解决冷启动问题**：通过提示词优化为 RL 提供了“第一桶金”（初始成功轨迹），解决了困难样本下梯度消失的问题。
*   **内化机制**：通过上下文蒸馏，使模型在推理阶段无需依赖复杂的提示词工程，保持了推理效率。
*   **自适应进化**：提示词随模型能力的提升而动态演化，始终针对模型当前的“薄弱环节”进行干预。

### 8. 不足与局限
*   **计算开销**：虽然 GRPO 节省了内存，但 Phase 2 的提示词进化过程（涉及多次 Rollout 和反思模型调用）会增加额外的计算时间。
*   **任务局限性**：目前仅在数学推理（具有明确可验证奖励）任务上验证，在缺乏客观验证机制的开放域任务（如创意写作）中应用难度较大。
*   **反思模型依赖**：提示词优化的质量高度依赖于“反思模型”的水平，若反思模型无法提供高质量的改进建议，框架效果可能受限。

（完）
