---
title: "HiMu: Hierarchical Multimodal Frame Selection for Long Video Question Answering"
title_zh: HiMu：面向长视频问答的分层多模态帧选择
authors: "Dan Ben-Ami, Gabriele Serussi, Kobi Cohen, Chaim Baskin"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18558v1"
tags: ["query:mmkqa"]
score: 9.0
evidence: 长视频问答中的分层多模态帧选择
tldr: 针对长视频问答中帧选择的效率与准确性权衡问题，本文提出HiMu框架。它通过LLM将复杂查询分解为层次化逻辑树，利用多模态专家（视觉、音频、OCR等）处理原子谓词，并结合模糊逻辑算子进行时序组合。该方法无需训练，在保持高精度的同时显著降低了计算开销，在多个基准测试上超越了现有的相似度匹配和智能体方法。
selection_source: fresh_fetch
motivation: 现有的长视频帧选择方法在快速相似度匹配（丢失结构信息）与高成本智能体推理（计算昂贵）之间存在难以调和的矛盾。
method: 利用LLM将查询分解为逻辑树，通过多模态专家提取信号，并使用模糊逻辑算子在时序上组合这些信号以生成连续的满意度曲线。
result: 在Video-MME等多个榜单上，HiMu以更少的帧数和约1/10的计算量，超越了现有的选择器和复杂的智能体系统。
conclusion: HiMu证明了通过层次化逻辑分解和多模态信号组合，可以在不进行额外训练的情况下实现高效且精准的长视频理解。
---

## 摘要
长视频问答需要对长程时间上下文进行推理，这使得帧选择对于受限于有限上下文窗口的大型视觉语言模型（LVLMs）至关重要。现有方法面临着严峻的权衡：基于相似性的选择器速度快，但会将组合式查询压缩为单个稠密向量，从而丢失了子事件顺序和跨模态绑定；基于智能体的方法通过迭代式 LVLM 推理来恢复这种结构，但成本极高。我们提出了 HiMu，这是一个无需训练的框架，旨在弥合这一差距。通过单次纯文本 LLM 调用，将查询分解为分层逻辑树，其叶节点是原子谓词，每个谓词都被路由到涵盖视觉（CLIP、开放词汇检测、OCR）和音频（ASR、CLAP）的轻量级专家模型。生成的信号经过归一化和时间平滑处理以对齐不同模态，并通过强制执行时间顺序和邻接关系的模糊逻辑算子自底向上进行组合，从而产生连续的满意度曲线。在 Video-MME、LongVideoBench 和 HERBench-Lite 上的评估表明，HiMu 提升了效率-准确率的帕累托前沿：在使用 Qwen3-VL 8B 且仅选取 16 帧的情况下，它的表现优于所有竞争选择器；而在使用 GPT-4o 时，它超越了处理 32-512 帧的智能体系统，同时所需的 FLOPs 减少了约 10 倍。

## Abstract
Long-form video question answering requires reasoning over extended temporal contexts, making frame selection critical for large vision-language models (LVLMs) bound by finite context windows. Existing methods face a sharp trade-off: similarity-based selectors are fast but collapse compositional queries into a single dense vector, losing sub-event ordering and cross-modal bindings; agent-based methods recover this structure through iterative LVLM inference, but at prohibitive cost. We introduce HiMu, a training-free framework that bridges this gap. A single text-only LLM call decomposes the query into a hierarchical logic tree whose leaves are atomic predicates, each routed to a lightweight expert spanning vision (CLIP, open-vocabulary detection, OCR) and audio (ASR, CLAP). The resulting signals are normalized, temporally smoothed to align different modalities, and composed bottom-up through fuzzy-logic operators that enforce temporal sequencing and adjacency, producing a continuous satisfaction curve. Evaluations on Video-MME, LongVideoBench and HERBench-Lite show that HiMu advances the efficiency-accuracy Pareto front: at 16 frames with Qwen3-VL 8B it outperforms all competing selectors, and with GPT-4o it surpasses agentic systems operating at 32-512 frames while requiring roughly 10x fewer FLOPs.

---

## 论文详细总结（自动生成）

### 论文总结：HiMu —— 面向长视频问答的分层多模态帧选择

#### 1. 核心问题与研究动机
长视频问答（VideoQA）的主要瓶颈在于大型视觉语言模型（LVLM）有限的上下文窗口，无法处理原生帧率的整段视频。现有的帧选择方法存在严重的**效率与准确性权衡**：
*   **相似度方法（如 CLIP）：** 速度快但过于简单，将复杂查询压缩为单个向量，丢失了时序逻辑和跨模态关联（如“在某人说话后发生了什么”）。
*   **智能体/迭代方法：** 通过多次调用 LVLM 进行搜索，虽然准确但计算成本极高（FLOPs 高出 10-100 倍）。
**研究动机：** 探索是否能在不进行昂贵的迭代推理的情况下，通过结构化的逻辑分解实现高效且精准的帧选择。

#### 2. 方法论
HiMu 是一个**无需训练（Training-free）**的神经符号框架，其核心流程如下：
*   **查询分解（Query Decomposition）：** 利用单次纯文本 LLM 调用，将自然语言问题解析为**分层逻辑树**。树的叶节点是原子谓词（如“红色的车”），内部节点是逻辑算子。
*   **多模态专家（Multimodal Experts）：** 将叶节点路由至特定的轻量级专家模型：
    *   **视觉：** CLIP（动作/场景）、OVD/YOLO-World（物体）、OCR（屏幕文字）。
    *   **音频：** ASR（语音转文字）、CLAP（环境音/音乐）。
*   **信号处理与组合：** 
    *   **归一化与平滑：** 使用 Median/MAD 归一化不同专家的得分，并通过高斯核进行时间平滑以对齐模态。
    *   **模糊逻辑算子：** 使用 `And`（共存）、`Or`（析取）、`Seq`（时序顺序）和 `RightAfter`（紧邻时序）自底向上组合信号，生成每帧的“满意度曲线”。
*   **PASS 采样策略：** 提出“峰值与扩散选择”（Peak-And-Spread Selection），在识别多个局部得分峰值的同时，提取其邻近帧以捕获短时运动上下文。

#### 3. 实验设计
*   **数据集：** 
    *   **Video-MME：** 涵盖短、中、长视频的全方位基准。
    *   **LongVideoBench：** 侧重于时刻定位和跨模态推理。
    *   **HERBench-Lite：** 侧重于多证据集成的复杂推理（无音频/字幕环境）。
*   **对比方法：** 
    *   **等效对比（16帧）：** Uniform、BOLT、AKS、T*。
    *   **非对称对比（HiMu 16帧 vs. 其他 32-512帧）：** VideoZoomer、VideoChat-A1、VSLS、LVAgent 等。
*   **下游模型：** 测试了 Qwen3-VL、InternVL-3.5、LLaVA-OV、GPT-4o 和 Gemini-2.5-Flash 等多种 LVLM。

#### 4. 资源与算力
*   **硬件：** 实验主要在 **8 × NVIDIA RTX Pro 6000 (48GB)** 或 **8 × A100 (80GB)** 上运行。
*   **延迟表现：** 对于 10 分钟视频（600 帧候选）：
    *   **端到端延迟：** 约 13.3 秒（包含特征提取）。
    *   **摊销延迟（特征缓存后）：** 约 9.0 秒/查询（主要耗时在 LLM 解析和 OVD 检测）。
*   **计算量：** 相比智能体方法，HiMu 所需的 FLOPs 减少了约一个数量级（10倍）。

#### 5. 实验数量与充分性
*   **实验规模：** 论文在三个主流长视频基准上进行了详尽测试，涵盖了从 11 秒到 1 小时的视频。
*   **消融实验：** 验证了逻辑组合（相比简单融合下降 5.49%）、各专家模态（ASR 贡献最大，下降 1.99%）以及帧预算（8 到 64 帧）的影响。
*   **敏感性分析：** 测试了不同 LLM 解析器（Qwen, LLaVA, Gemini 等）和不同专家后端（SigLIP, Grounding DINO 等）的鲁棒性。
*   **评价：** 实验设计客观、公平，通过固定下游模型和帧数进行了严格的受控对比，充分证明了其在帕累托前沿上的领先地位。

#### 6. 主要结论
*   **打破帕累托前沿：** HiMu 在极低的计算成本下达到了接近甚至超过昂贵智能体方法的准确率。
*   **16 帧胜过 64 帧：** HiMu 选出的 16 帧在 Video-MME 上的表现优于均匀采样的 64 帧，证明了“选得准”比“选得多”更重要。
*   **音频的重要性：** 首次证明了将音频（ASR/CLAP）作为帧选择的一等公民能显著提升定位精度。

#### 7. 优点
*   **高效性：** 无需迭代 LVLM 推理，单次解析即可完成复杂逻辑匹配。
*   **可解释性：** 逻辑树结构允许通过热力图查看每一帧被选中的具体原因（哪个谓词触发了哪个专家）。
*   **多模态融合：** 填补了现有选择器忽视音频信息的空白。
*   **即插即用：** 无需针对特定模型训练，可适配任何 LVLM。

#### 8. 不足与局限
*   **初始延迟：** 相比纯相似度方法，特征提取阶段（尤其是 OCR 和 ASR）增加了初始等待时间。
*   **解析器依赖：** 帧选择的质量高度依赖于 LLM 解析逻辑树的准确性，若解析错误则会导致关键帧丢失。
*   **ASR 限制：** 语音识别专家的效果受限于底层 Whisper 模型的语言覆盖范围，在多语言或低资源音频上可能受限。

（完）
