---
title: "Demystifying Reinforcement Learning for Long-Horizon Tool-Using Agents: A Comprehensive Recipe"
title_zh: 揭秘长程工具使用智能体的强化学习：一份全面指南
authors: "Xixi Wu, Qianguo Sun, Ruiyang Zhang, Chao Song, Junlong Wu, Yiyan Qi, Hong Cheng"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21972v1"
tags: ["query:agent"]
score: 10.0
evidence: 为长程工具使用智能体扩展强化学习算法
tldr: 本研究针对大语言模型在长程规划和工具使用中的强化学习（RL）应用，通过在TravelPlanner基准上的系统性实验，探索了奖励设计、模型规模、数据组成、算法选择和环境稳定性五个核心维度。研究总结了7条关键经验，如小模型更依赖阶段性奖励而大模型则能高效处理简单稠密奖励，并确定了约1000个训练样本的最佳平衡点。最终，基于该方案训练的模型在TravelPlanner上达到了SOTA水平，显著优于现有领先模型。
selection_source: fresh_fetch
motivation: 旨在解决大语言模型在复杂多轮环境中使用强化学习进行长程规划时缺乏实用扩展方案的问题。
method: 在TravelPlanner平台上，从奖励塑造、模型缩放、数据构成、算法选择和环境稳定性五个维度进行了系统性的实证研究。
result: 总结出模型规模与奖励/算法选择的依赖关系、1K样本的性能平衡点以及环境稳定性对策略退化的影响等关键结论。
conclusion: 本研究提供的强化学习训练方案使模型在长程工具使用任务中取得了当前最佳性能，为构建自主智能体提供了实践指南。
---

## 摘要
强化学习 (RL) 对于将大语言模型 (LLM) 演进为具备长程规划能力的自主智能体至关重要，然而在复杂的多轮环境中扩展 RL 的实用方案仍然难以捉摸。本文利用 TravelPlanner 开展了一项系统的实证研究，这是一个需要通过工具编排来满足多方面约束的极具挑战性的测试平台。我们从五个维度分解了智能体 RL 的设计空间：奖励塑造、模型缩放、数据组成、算法选择和环境稳定性。我们的受控实验得出了 7 个关键结论，例如：(1) 奖励和算法的选择具有规模依赖性，较小的模型受益于阶段性奖励和增强的探索，而较大的模型则能通过更简单的稠密奖励高效收敛；(2) 约 1000 个具有平衡难度混合的训练样本是域内和域外性能的最佳平衡点；(3) 环境稳定性对于防止策略退化至关重要。基于我们提炼的方案，我们经过 RL 训练的模型在 TravelPlanner 上实现了最先进的性能，显著优于领先的 LLM。

## Abstract
Reinforcement Learning (RL) is essential for evolving Large Language Models (LLMs) into autonomous agents capable of long-horizon planning, yet a practical recipe for scaling RL in complex, multi-turn environments remains elusive. This paper presents a systematic empirical study using TravelPlanner, a challenging testbed requiring tool orchestration to satisfy multifaceted constraints. We decompose the agentic RL design space along 5 axes: reward shaping, model scaling, data composition, algorithm selection, and environmental stability. Our controlled experiments yield 7 key takeaways, e.g., (1) reward and algorithm choices are scale-dependent as smaller models benefit from staged rewards and enhanced exploration, whereas larger models converge efficiently with simpler dense rewards, (2) ~ 1K training samples with a balanced difficulty mixture mark a sweet spot for both in-domain and out-of-domain performance, and (3) environmental stability is critical to prevent policy degradation. Based on our distilled recipe, our RL-trained models achieve state-of-the-art performance on TravelPlanner, significantly outperforming leading LLMs.

---

## 论文详细总结（自动生成）

这是一份关于论文《Demystifying Reinforcement Learning for Long-Horizon Tool-Using Agents: A Comprehensive Recipe》的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：虽然大语言模型（LLM）正演变为能够进行推理和行动的自主智能体，但在涉及数十个步骤、复杂约束的长程（Long-Horizon）规划任务中，如何通过强化学习（RL）有效提升其能力仍缺乏系统性的指导方案（Recipe）。
*   **核心问题**：现有的智能体 RL 研究多集中在短程任务或单一算法改进上，缺乏对奖励设计、模型规模、数据组成、算法选择及环境稳定性等多个维度交织影响的全面理解。
*   **研究目标**：通过在复杂的 TravelPlanner（旅游规划）基准上进行大规模实证研究，提炼出一套可扩展、实用的长程智能体训练“配方”。

### 2. 论文提出的方法论：STAR 框架
论文提出了 **STAR (Synthesis, Training, And Reinforcement)** 统一后训练流水线：
*   **数据合成 (Synthesis)**：
    *   在本地沙盒中验证旅行要素的可行性。
    *   利用强模型（如 GPT-OSS-120B）通过反向翻译生成自然语言查询。
    *   **难度控制**：根据约束数量将查询分为简单、中等、困难三个等级。
*   **监督微调 (SFT)**：
    *   采用拒绝采样（Rejection Sampling），利用 DeepSeek-V3.2-Exp-Thinking 生成高质量轨迹，仅保留成功的样本进行微调，为 RL 提供良好的初始策略。
*   **强化学习 (RL)**：
    *   **核心算法**：以 GRPO（群体相对策略优化）为基础。
    *   **设计空间探索**：从五个轴向进行受控实验：
        1.  **奖励塑造**：对比稠密（SUM）、半稀疏（MACRO）、纯稀疏（SUCCESS）及阶段性课程奖励（CURRICULUM）。
        2.  **模型缩放**：测试 1.5B、3B、7B 不同参数量模型。
        3.  **数据组成**：研究样本数量（100 到 2000）和难度配比的影响。
        4.  **算法选择**：对比标准 GRPO 与增强探索的变体（DAPO, ARPO）。
        5.  **环境稳定性**：模拟工具调用失败（0% 到 10% 故障率）对训练的影响。

### 3. 实验设计
*   **数据集/场景**：
    *   **域内 (In-domain)**：TravelPlanner（包含 1000 个测试实例），要求智能体调用 6 种工具（搜索航班、酒店、餐厅等）并满足预算、偏好等硬约束。
    *   **域外 (OOD)**：7 个知识密集型 QA 基准（如 NQ, TriviaQA, HotpotQA），仅提供维基百科搜索工具，用于评估泛化能力。
*   **Benchmark 与对比方法**：
    *   对比了 GPT-4o, Gemini-1.5-Pro, Kimi-K2.5, DeepSeek-V3 等顶级闭源模型。
    *   对比了 Planner-R1 等针对该任务的最新 RL 方法。

### 4. 资源与算力
*   **算力设备**：
    *   1.5B 和 3B 模型：使用 1 台 8×A100-80G 节点。
    *   7B 模型：使用 2 台 8×A100-80G 节点（共 16 张显卡）。
*   **训练时长**：
    *   RL 阶段通常训练 5 个 Epoch。
    *   根据表 4，单次实验的 GPU 总耗时在 164 到 547 小时之间（取决于模型大小和算法）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的消融实验，涵盖了上述 5 个维度的数十组对比。
*   **充分性与客观性**：
    *   **受控变量**：每次仅改变一个因素（如仅改变奖励函数），确保了因果关系的准确性。
    *   **评估公正**：所有模型（包括对比的闭源模型）均使用相同的 DeepSeek-V3 格式化模型进行解析和自动评分，消除了评估偏差。
    *   **多维度评估**：不仅关注成功率，还分析了训练动力学（收敛速度）、动作熵以及 OOD 泛化表现。

### 6. 主要结论与发现
1.  **规模依赖性**：小模型（1.5B/3B）极度依赖课程奖励和增强探索算法；而大模型（7B）直接使用稠密奖励和标准 GRPO 效果最好。
2.  **泛化税**：过度稠密的任务特定奖励会损害 OOD 性能，半稀疏奖励（MACRO）是平衡域内表现与泛化的最佳选择。
3.  **数据甜点位**：约 **1000 个训练样本**是性能提升的饱和点，盲目增加数据会导致模型过拟合特定格式，降低泛化力。
4.  **难度平衡**：训练集必须包含不同难度的混合，全简单数据学不会复杂约束，全困难数据会导致奖励过稀疏而无法收敛。
5.  **环境鲁棒性**：智能体能容忍 5% 以下的随机工具错误，但 10% 的故障率会导致策略严重退化。

### 7. 优点
*   **实战指南属性**：不仅给出了 SOTA 结果，更重要的是提供了不同规模模型该如何选算法、选奖励的详细“配方”。
*   **开源贡献**：提供了 STAR 框架和训练出的高性能开源模型，性能超越了参数量大得多的闭源模型（如 7B 胜过 Kimi-K2.5）。
*   **系统性强**：将环境稳定性这一常被忽视的因素纳入 RL 训练考量。

### 8. 不足与局限
*   **环境局限**：TravelPlanner 虽然复杂，但仍是模拟沙盒，与具有不可预测动态的真实互联网 API 环境仍有差距。
*   **模型规模上限**：实验最高仅覆盖到 7B 参数，对于 70B 或更大规模模型的行为（如是否会出现更强的零样本探索能力）尚未验证。
*   **泛化评估范围**：OOD 评估仅限于 QA 任务，未测试跨领域智能体任务（如从旅游规划跨到代码调试）的迁移能力。
*   **缺乏多因素交互分析**：主要采用单变量分析，未深入探讨多个因素（如数据量与算法复杂度）同时改变时的非线性交互。

（完）
