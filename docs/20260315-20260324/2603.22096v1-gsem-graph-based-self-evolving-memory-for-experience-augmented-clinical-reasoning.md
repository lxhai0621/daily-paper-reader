---
title: "GSEM: Graph-based Self-Evolving Memory for Experience Augmented Clinical Reasoning"
title_zh: GSEM：基于图的自演化记忆，用于经验增强的临床推理
authors: "Xiao Han, Yuzheng Fan, Sendong Zhao, Haochun Wang, Bing Qin"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22096v1"
tags: ["query:agent"]
score: 9.0
evidence: 基于图的临床推理自进化记忆
tldr: 临床决策智能体通过复用经验可提升性能，但现有方法多将经验视为独立记录，缺乏关联性，导致检索噪声大且复用不可靠。本文提出GSEM（基于图的自进化记忆）框架，通过双层记忆图组织临床经验，捕捉经验内部决策结构及经验间的关联。该框架支持适用性感知检索和基于反馈的在线校准，在MedR-Bench等基准测试中显著提升了LLM的临床推理准确率，为经验增强型临床推理提供了新方案。
selection_source: fresh_fetch
motivation: 现有的临床记忆增强方法将经验视为孤立记录，缺乏显式关系结构，导致检索不准确且复用不可靠。
method: 提出GSEM框架，利用双层记忆图组织经验，并结合适用性感知检索与在线反馈驱动的节点及权重校准机制。
result: "在MedR-Bench和MedAgentsBench上，GSEM配合DeepSeek-V3.2和Qwen3.5-35B均取得了最高的平均准确率（分别为70.90%和69.24%）。"
conclusion: GSEM通过结构化和动态进化的记忆管理，有效解决了临床经验复用中的噪声问题，显著增强了大型语言模型的临床推理能力。
---

## 摘要
临床决策智能体可以从重用先前的决策经验中获益。然而，许多记忆增强方法将经验存储为独立的记录，缺乏显式的关系结构，这可能导致检索噪声、重用不可靠，在某些情况下甚至比直接的大语言模型（LLM）推理性能更差。我们提出了 GSEM（基于图的自演化记忆），这是一个临床记忆框架，它将临床经验组织成双层记忆图，既捕捉了每个经验内部的决策结构，也捕捉了经验之间的关系依赖，并支持适用性感知检索以及基于在线反馈的节点质量和边权重校准。在 MedR-Bench 和 MedAgentsBench 两个基准测试中，结合两种 LLM 主干网络，GSEM 在所有基线方法中均取得了最高的平均准确率，在使用 DeepSeek-V3.2 和 Qwen3.5-35B 时分别达到了 70.90% 和 69.24%。代码可在 https://github.com/xhan1022/gsem 获取。

## Abstract
Clinical decision-making agents can benefit from reusing prior decision experience. However, many memory-augmented methods store experiences as independent records without explicit relational structure, which may introduce noisy retrieval, unreliable reuse, and in some cases even hurt performance compared to direct LLM inference. We propose GSEM (Graph-based Self-Evolving Memory), a clinical memory framework that organizes clinical experiences into a dual-layer memory graph, capturing both the decision structure within each experience and the relational dependencies across experiences, and supporting applicability-aware retrieval and online feedback-driven calibration of node quality and edge weights. Across MedR-Bench and MedAgentsBench with two LLM backbones, GSEM achieves the highest average accuracy among all baselines, reaching 70.90\% and 69.24\% with DeepSeek-V3.2 and Qwen3.5-35B, respectively. Code is available at https://github.com/xhan1022/gsem.

---

## 论文详细总结（自动生成）

以下是对论文《GSEM: Graph-based Self-Evolving Memory for Experience Augmented Clinical Reasoning》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：临床决策具有高风险、信息不完全和强条件依赖的特点。现有的记忆增强型大语言模型（LLM）智能体通常将经验存储为**独立的、扁平的记录**，缺乏显式的关系结构。
*   **研究动机**：作者识别出两种主要的失效模式：
    1.  **边界失效（Boundary Failure）**：检索到的经验仅部分匹配条件，忽略了关键的边界约束（如风险因素）。
    2.  **协作失效（Collaboration Failure）**：同时检索到多个相互冲突或缺乏共同使用价值的经验，导致推理逻辑混乱。
*   **目标**：构建一个既能捕捉经验内部决策逻辑，又能建模经验间关联，且能根据反馈动态进化的临床记忆框架。

### 2. 方法论：GSEM 框架
GSEM（基于图的自演化记忆）由三个核心阶段组成：

*   **双层记忆图构建**：
    *   **实体层（Entity Layer）**：将每个经验分解为五种角色（条件、约束、行动、依据、结果），捕捉经验内部的决策流。
    *   **经验层（Experience Layer）**：将经验视为节点，边权重 $W$ 表示经验间的关联强度（基于实体重叠、结构相似性、协同价值和任务一致性计算）。
    *   **经验定义**：每个经验包含条件、内容、极性（**适应症 $\oplus$** 或 **禁忌症 $\ominus$**）和质量分数 $Q$。
*   **适用性感知检索**：
    *   **混合种子召回**：结合基于实体的稀疏检索和基于嵌入（Embedding）的稠密检索，获取初始种子集。
    *   **多种子图遍历**：从种子节点出发，由 LLM 引导在经验层图中进行“收集、探索、回溯、停止”等操作，寻找满足边界条件且具有协同价值的经验组合。
*   **在线反馈驱动演化**：
    *   任务完成后，根据结果反馈（标量信号 $\Delta t$）动态更新激活节点的质量分数 $Q$ 和边权重 $W$。
    *   采用指数衰减的信用分配机制，强化对成功有贡献的经验及其关联，弱化导致失败的路径。

### 3. 实验设计
*   **数据集/场景**：
    1.  **MedR-Bench**：包含真实的临床病例，涵盖诊断推理和治疗规划（开放式推理）。
    2.  **MedAgentsBench**：具有挑战性的医学问答子集（MedQA, MedMCQA, MedBullets, MedExQA, MedXpertQA），仅保留准确率低于 50% 的难题。
*   **主干模型**：DeepSeek-V3.2 和 Qwen3.5-35B。
*   **对比方法**：
    *   **RAG 类**：Naive RAG, GraphRAG。
    *   **记忆增强类**：Mem0, Mem0-g, A-Mem。
    *   **自演化/经验类**：ReMe, FLEX。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：实验使用了 DeepSeek-V3.2（671B）和 Qwen3.5-35B 作为生成、检索和评测的后端。在构建阶段，每个训练实例采样了 5 条推理轨迹。

### 5. 实验数量与充分性
*   **实验规模**：在两个大型基准测试的多个子集上进行了测试，涵盖了诊断和治疗两大临床核心任务。
*   **充分性**：
    *   进行了**消融实验**，验证了实体召回、嵌入召回和多种子遍历的必要性。
    *   进行了**自演化分析**，展示了随着演化步数增加（50, 150, 250 步），模型性能的持续提升。
    *   进行了**模型规模分析**，探讨了不同大小的检索器与生成器组合对性能的影响。
*   **客观性**：使用了 LLM Judge（DeepSeek-V3.2）进行多维度推理质量评分（效率、事实性、完整性），并提供了 95% 置信区间，实验设计较为严谨。

### 6. 主要结论与发现
*   **性能领先**：GSEM 在所有基准测试中均取得了最高平均准确率，尤其在需要多步推理和边界感知的**治疗规划**任务中提升最为显著。
*   **结构价值**：显式建模经验间的关系（图结构）比存储独立片段（扁平 RAG）更有效，能减少冲突并提高经验复用的连贯性。
*   **演化效应**：在线反馈机制能有效极化经验质量分布，使高价值经验更易被检索，从而实现“越用越聪明”。
*   **模型不对称性**：生成器的能力对最终结果起决定性作用，而检索器可以使用较小的模型（如 Qwen-35B）而不显著损失性能，这为低成本部署提供了可能。

### 7. 亮点
*   **极性建模**：引入“禁忌症”经验，通过学习失败教训来规避错误，这非常符合临床医学的逻辑。
*   **双层图结构**：将微观的决策实体与宏观的经验关联结合，兼顾了检索的精确度和广度。
*   **动态校准**：无需重新训练模型参数，仅通过更新图的权重和节点质量即可实现知识的持续优化。

### 8. 不足与局限
*   **计算开销**：记忆构建阶段需要采样多条轨迹并进行可靠性验证，且图遍历检索比简单的向量检索延迟更高。
*   **评估局限**：虽然使用了高质量基准，但仍属于自动化评估，缺乏真实临床环境下的专家实测或纵向随访数据。
*   **策略敏感性**：LLM 引导的图遍历策略对 Prompt 较为敏感，可能存在路径选择的随机性。
*   **安全风险**：在线演化依赖准确率反馈，若反馈信号存在偏差，可能导致记忆库产生错误的“经验漂移”。

（完）
