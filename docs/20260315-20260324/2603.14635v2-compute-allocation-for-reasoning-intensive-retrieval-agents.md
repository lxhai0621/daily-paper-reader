---
title: Compute Allocation for Reasoning-Intensive Retrieval Agents
title_zh: 推理密集型检索智能体的计算分配
authors: "Sreeja Apparaju, Nilesh Gupta"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14635v2"
tags: ["query:ma-kf"]
score: 9.0
evidence: 智能体的推理密集型检索
tldr: 随着智能体运行时间增长，推理密集型检索成为获取相关信息的关键。本研究探讨了在查询扩展和重排序阶段如何优化计算资源分配。通过在BRIGHT基准测试上使用Gemini系列模型，研究发现重排序阶段从更强的模型和更深的候选池中获益显著，而查询扩展的收益随模型增强而迅速递减，且推理时思考对两阶段提升均有限。研究建议应将计算资源集中在重排序阶段，而非在各阶段间平均分配。
selection_source: fresh_fetch
motivation: 针对长程智能体中推理密集型检索的高昂推理成本，探索如何优化查询扩展与重排序之间的计算资源分配。
method: 利用BRIGHT基准和Gemini模型，通过改变模型容量、推理时思考及重排序深度，系统评估不同阶段的性能表现。
result: 重排序阶段显著受益于更强的模型和更深的候选池，而查询扩展在轻量级模型之外收益微小，且推理时思考提升有限。
conclusion: 检索流水线的计算资源应优先集中于重排序阶段，而非在各阶段间平均分配。
---

## 摘要
随着智能体在长周期内运行，其内存存储持续增长，使得检索对于获取相关信息变得至关重要。许多智能体查询需要推理密集型检索，其中查询与相关文档之间的联系是隐式的，需要通过推理来衔接。大语言模型（LLM）增强的流水线通过查询扩展和候选重排序来解决这一问题，但引入了显著的推理成本。我们利用 BRIGHT 基准测试和 Gemini 2.5 模型系列，研究了推理密集型检索流水线中的计算分配。我们在查询扩展和重排序阶段改变了模型容量、推理时思考以及重排序深度。我们发现，重排序从更强大的模型（NDCG@10 提升 7.5）和更深的候选池（k 从 10 增加到 100 时提升 21%）中获益显著，而查询扩展在超过轻量级模型后表现出收益递减（从弱模型到强模型 NDCG@10 仅提升 1.1）。推理时思考在任何阶段提供的改进都微乎其微。这些结果表明，计算资源应集中在重排序上，而不是在流水线各阶段之间均匀分布。

## Abstract
As agents operate over long horizons, their memory stores grow continuously, making retrieval critical to accessing relevant information. Many agent queries require reasoning-intensive retrieval, where the connection between query and relevant documents is implicit and requires inference to bridge. LLM-augmented pipelines address this through query expansion and candidate re-ranking, but introduce significant inference costs. We study computation allocation in reasoning-intensive retrieval pipelines using the BRIGHT benchmark and Gemini 2.5 model family. We vary model capacity, inference-time thinking, and re-ranking depth across query expansion and re-ranking stages. We find that re-ranking benefits substantially from stronger models (+7.5 NDCG@10) and deeper candidate pools (+21% from $k$=10 to 100), while query expansion shows diminishing returns beyond lightweight models (+1.1 NDCG@10 from weak to strong). Inference-time thinking provides minimal improvement at either stage. These results suggest that compute should be concentrated on re-ranking rather than distributed uniformly across pipeline stages.

---

## 论文详细总结（自动生成）

以下是对论文《Compute Allocation for Reasoning-Intensive Retrieval Agents》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着自主智能体（Agents）运行时间的增加，其存储的记忆（过往决策、任务结果等）不断膨胀。在这些记忆中，许多查询属于**推理密集型检索（Reasoning-Intensive Retrieval, RIIR）**：即查询与目标文档之间没有直接的词汇或语义重叠，必须通过推理（如识别隐式约束）才能建立联系。
虽然目前主流采用“大模型增强流水线”（查询扩展 + 重排序）来解决此问题，但 LLM 的推理成本极高。本研究的核心问题是：**在有限的计算预算下，应该如何在流水线的不同阶段（查询扩展 vs. 重排序）分配算力，以实现性能最大化？**

### 2. 论文提出的方法论
研究将检索过程建模为一个三阶段流水线，并系统性地改变各阶段的计算投入：
*   **查询扩展 (QE)：** 使用 LLM 将原始查询 $q$ 扩展为包含隐式约束和领域术语的 $q_{exp}$。
*   **初始检索：** 使用 BM25 算法从大规模语料库中检索候选列表。选择 BM25 是因为它对 LLM 生成的分布外查询具有较好的鲁棒性。
*   **列表式重排序 (RR)：** 使用 LLM 对初始检索到的前 $k$ 个文档进行推理，评估其相关性并重新排序。
*   **关键变量控制：**
    *   **模型容量：** 使用 Gemini 2.5 系列（Flash-Lite, Flash, Pro）。
    *   **推理时思考（Thinking）：** 开启或关闭模型的动态思考/思维链模式。
    *   **重排序深度：** 改变进入重排序阶段的候选文档数量 $k \in \{10, 20, 50, 100\}$。

### 3. 实验设计
*   **基准测试 (Benchmark)：** 使用 **BRIGHT** 数据集。该基准包含 12 个领域的查询，专门设计用于测试多步推理和隐式约束满足能力。
*   **对比维度：**
    *   不同 QE 模型对检索质量的影响。
    *   不同 RR 模型在固定候选池下的表现。
    *   重排序深度 $k$ 对最终结果的增益。
*   **评估指标：** NDCG@10（排名质量）、Recall@10（检索上限）、每查询成本（$）和延迟（秒）。

### 4. 资源与算力
*   论文主要基于 **Gemini 2.5 API** 进行推理实验。
*   **未明确说明**具体的本地 GPU 型号、数量或训练时长，因为该研究侧重于推理阶段的计算分配策略，而非模型训练。
*   成本分析以 API 调用费用为基准进行量化。

### 5. 实验数量与充分性
*   **实验组数：** 进行了多维度的消融实验，包括 4 种 QE 模型配置、4 种 RR 模型配置以及 4 种不同的重排序深度。
*   **充分性：** 实验覆盖了从极轻量（Flash-Lite）到高性能（Pro）的模型梯度，并对比了“思考模式”的开关。
*   **客观性：** 实验在统一的 BRIGHT 基准下进行，通过成本与性能的曲线对比，清晰地展示了边际收益递减效应，结论较为客观。

### 6. 论文的主要结论与发现
*   **查询扩展 (QE) 收益递减快：** 从无增强到轻量级模型（Flash-Lite）提升巨大，但进一步升级到 Pro 模型，NDCG@10 仅提升 1.1，Recall 提升微乎其微。QE 本质上是词汇覆盖任务，而非深度推理任务。
*   **重排序 (RR) 是性能核心：** RR 对模型容量高度敏感。使用更强的模型可带来显著提升（+7.5 NDCG@10）。
*   **增加重排序深度效果显著：** 将 $k$ 从 10 增加到 100，NDCG@10 提升了约 21%。这表明在更深的候选池中进行验证是高杠杆的算力分配方式。
*   **“思考模式”性价比极低：** 在 QE 或 RR 阶段开启推理思考模式，带来的性能提升非常有限，却显著增加了成本和延迟。

### 7. 优点
*   **实用性强：** 为开发长程智能体的工程师提供了明确的算力分配指南（即：QE 用小模型，RR 用大模型并增加深度）。
*   **揭示了反直觉现象：** 发现推理密集型检索中，额外的“思考”步骤在查询扩展阶段甚至可能因“过度思考”导致性能下降。
*   **成本效益分析：** 不仅仅关注准确率，还深入分析了每一美元投入带来的性能增益。

### 8. 不足与局限
*   **模型单一性：** 实验仅限于 Gemini 2.5 系列，未在 GPT-4、Claude 或开源模型（如 Llama）上验证结论的普适性。
*   **基准局限：** 仅使用了 BRIGHT 一个基准测试，虽然该基准具有代表性，但可能无法完全覆盖所有智能体内存检索的场景。
*   **检索算法单一：** 初始检索仅使用了 BM25，未探讨向量检索（Dense Retrieval）或混合检索在这一框架下的表现。

（完）
