---
title: Communication-Aware Multi-Agent Reinforcement Learning for Decentralized Cooperative UAV Deployment
title_zh: 面向分布式协作无人机部署的通信感知多智能体强化学习
authors: "Enguang Fan, Yifan Chen, Zihan Shan, Matthew Caesar, Jae Kim"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16141v1"
tags: ["query:agent"]
score: 9.0
evidence: 用于自主无人机集群的多智能体强化学习
tldr: 本研究针对无人机集群在部分可观测和通信受限环境下的部署问题，提出了一种基于图的多智能体强化学习（MARL）框架。该框架采用集中式训练分布式执行（CTDE）模式，通过智能体-实体注意力模块和邻居自注意力机制处理局部观测与通信消息。实验表明，该方法在无人机中继部署和对抗任务中表现优异，不仅接近离线优化上限，且具备良好的泛化能力，能有效应对动态通信拓扑。
selection_source: fresh_fetch
motivation: 旨在解决无人机集群在实际部署中面临的部分可观测性及点对点通信链路不稳定的挑战。
method: 提出一种结合智能体-实体注意力与邻居自注意力的图神经网络MARL框架，利用CTDE模式实现去中心化决策。
result: "在中继部署任务中达到74%的覆盖率，接近MILP优化上限，并能直接泛化至未见过的集群规模。"
conclusion: 该通信感知框架有效提升了无人机集群在复杂动态环境下的协作效率与鲁棒性，具有较强的实用价值。
---

## 摘要
自主无人机（UAV）集群越来越多地被用作快速部署的空中中继和传感平台，然而实际部署必须在部分可观测性和间歇性点对点链路下运行。我们提出了一种基于图的多智能体强化学习框架，该框架在集中式训练分布式执行（CTDE）模式下进行训练：集中式评论家（critic）和全局状态仅在训练期间可用，而每架无人机则利用局部观测和来自附近邻居的消息执行共享策略。我们的架构通过智能体-实体注意力模块对局部智能体状态和附近实体进行编码，并在距离受限的通信图上通过邻居自注意力机制聚合无人机间的消息。我们主要在协作中继部署任务（DroneConnect）上进行评估，其次在对抗性交战任务（DroneCombat）上进行评估。在 DroneConnect 中，所提方法在受限通信和部分观测下实现了高覆盖率（例如，在 M = 5 架无人机和 N = 10 个节点的情况下达到 74% 的覆盖率），同时与基于混合整数线性规划（MILP）优化的离线上限相比具有竞争力，并且无需微调即可泛化到未见的团队规模。在对抗性设置中，同一框架在无需更改架构的情况下即可迁移，并比无通信基准提高了胜率。

## Abstract
Autonomous Unmanned Aerial Vehicle (UAV) swarms are increasingly used as rapidly deployable aerial relays and sensing platforms, yet practical deployments must operate under partial observability and intermittent peer-to-peer links. We present a graph-based multi-agent reinforcement learning framework trained under centralized training with decentralized execution (CTDE): a centralized critic and global state are available only during training, while each UAV executes a shared policy using local observations and messages from nearby neighbors. Our architecture encodes local agent state and nearby entities with an agent-entity attention module, and aggregates inter-UAV messages with neighbor self-attention over a distance-limited communication graph. We evaluate primarily on a cooperative relay deployment task (DroneConnect) and secondarily on an adversarial engagement task (DroneCombat). In DroneConnect, the proposed method achieves high coverage under restricted communication and partial observation (e.g. 74% coverage with M = 5 UAVs and N = 10 nodes) while remaining competitive with a mixed-integer linear programming (MILP) optimization-based offline upper bound, and it generalizes to unseen team sizes without fine-tuning. In the adversarial setting, the same framework transfers without architectural changes and improves win rate over non-communicating baselines.

---

## 论文详细总结（自动生成）

### 论文结构化深度总结：面向分布式协作无人机部署的通信感知多智能体强化学习

#### 1. 核心问题与研究动机
*   **研究背景**：无人机（UAV）集群在灾后中继通信、野火监测和战场监视等领域具有重要应用。
*   **核心挑战**：在实际部署中，无人机面临**部分可观测性**（只能感知周围实体）和**通信受限**（只能与近距离邻居通信）的约束。传统的集中式控制存在单点故障风险且难以扩展，而简单的分布式方法往往缺乏有效的协作。
*   **研究目的**：开发一种可扩展的分布式控制系统，使无人机集群能够在动态、受限的环境中自主决定位置，以最大化覆盖范围。

#### 2. 方法论：核心思想与技术细节
该论文提出了一种基于图神经网络（GNN）的**集中式训练分布式执行（CTDE）**框架：
*   **环境建模（智能体-实体图）**：将环境表示为图 $G=(V, E)$，节点包括无人机和地面目标（实体），边表示感知或通信关系。
*   **双重注意力机制（Dual-Attention）**：
    1.  **智能体-实体注意力（Agent-Entity Attention）**：每架无人机利用缩放点积注意力机制，将其自身状态与感知半径 $r_s$ 内的变长实体集进行聚合，生成局部环境嵌入 $h_i^{(0)}$。
    2.  **邻居自注意力（Neighbor Self-Attention）**：在通信半径 $r_c$ 内，无人机通过多轮消息传递（Message Passing）与邻居交换嵌入向量，利用自注意力机制聚合邻居信息，更新自身隐藏状态。
*   **学习范式**：采用 CTDE 模式。训练时，集中式评论家（Critic）获取全局状态以稳定学习；执行时，每架无人机仅依靠局部观测和邻居消息，通过共享策略网络采样动作。
*   **数学表达**：通过公式定义了消息聚合 $m_i^{(k)}$ 和状态更新 $f_{upd}$，确保了对智能体数量和排列的不变性。

#### 3. 实验设计
*   **实验场景**：
    1.  **DroneConnect（主任务）**：全协作任务，无人机作为移动中继，为移动的地面节点提供覆盖。
    2.  **DroneCombat（次要任务）**：混合竞争任务，两组无人机通过激光交战，验证框架的通用性。
*   **评估设置**：组合了四种约束场景：全/部分观测（FO/PO）× 无限制/受限通信（UC/RC）。
*   **基准方法（Benchmarks）**：
    *   **MILP（混合整数线性规划）**：作为离线静态优化的性能上限。
    *   **消融实验**：无通信版本、无实体注意力版本（改用均值池化）。
    *   **其他**：集中式单智能体 RL、独立智能体（无 CTDE）学习。

#### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或具体的训练时长。
*   **训练规模**：提到模型在环境中训练了 $2 \times 10^6$ 个步长（steps），评估基于 50 个随机回合（episodes），并使用了 3 个不同的随机种子以确保结果的统计意义。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 DroneConnect 进行了多组参数组合（$M=3, N=6$ 到 $M=5, N=10$）的对比实验。
    *   包含了**零样本泛化（Zero-shot Generalization）**实验，测试在 $M=5$ 下训练的模型直接应用于 $M=3$ 或 $M=7$ 场景的表现。
    *   进行了覆盖重叠率（Overlap Rate）分析，量化协作效率。
*   **充分性评价**：实验设计较为全面，涵盖了从性能对比到消融分析，再到泛化性测试。通过与数学优化上限（MILP）对比，证明了 RL 方法的有效性。但在随机种子的数量上（3个）略显单薄，大规模集群（如数十架无人机）的扩展性实验尚不充分。

#### 6. 主要结论与发现
*   **高性能**：在最苛刻的 RC+PO（受限通信+部分观测）设置下，该方法在 $M=5, N=10$ 场景中达到了 74% 的覆盖率，接近 MILP 离线上限（80%）。
*   **协作价值**：通信机制显著降低了覆盖重叠率（从 0.19 降至 0.12），表明智能体学会了分工协作而非盲目聚集。
*   **强泛化性**：基于图的架构允许模型在未见过的团队规模下直接运行，且性能保持稳定，无需重新训练。
*   **通用性**：在对抗性任务 DroneCombat 中，该框架在不改变架构的情况下，胜率优于无通信基准和独立 RL 基准。

#### 7. 优点与亮点
*   **架构优雅**：将环境感知（实体）和社交协作（邻居）统一在双重注意力图框架下，处理了变长输入问题。
*   **现实约束考虑周全**：显式建模了通信半径和感知半径，比假设全局通信的 MARL 研究更具实用价值。
*   **零样本能力**：展示了良好的迁移学习潜力，这对于动态增减成员的无人机集群至关重要。

#### 8. 不足与局限
*   **通信模型简化**：实验中假设通信是理想的（只要在半径内即可无损传输），未深入探讨带宽限制、丢包或高延迟对策略的影响。
*   **物理仿真精度**：环境模型相对简化（2D 空间、简单的力学模型），未考虑 3D 环境中的障碍物遮挡或复杂的空气动力学约束。
*   **算力细节缺失**：缺乏训练成本的量化描述，难以评估该方法在边缘计算设备上的训练可行性。
*   **基准对比范围**：主要对比了消融版本和 MILP，若能增加与近年来其他先进通信 MARL 算法（如 TarMAC 或 DGN 的变体）的直接对比会更有说服力。

（完）
