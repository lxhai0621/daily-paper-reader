---
title: "TRUST-SQL: Tool-Integrated Multi-Turn Reinforcement Learning for Text-to-SQL over Unknown Schemas"
title_zh: TRUST-SQL：面向未知模式下 Text-to-SQL 的工具集成多轮强化学习
authors: "Ai Jian, Xiaoyun Zhang, Wanrou Du, Jingqing Ruan, Jiangbo Pei, Weipeng Zhang, Ke Zeng, Xunliang Cai"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16448v2"
tags: ["query:ma-kf"]
score: 9.0
evidence: 自主智能体利用工具将推理建立在经过验证的元数据上
tldr: 针对现实中数据库模式庞大且未知的挑战，本文提出TRUST-SQL框架。该框架将Text-to-SQL建模为部分可观测马尔可夫决策过程，通过四阶段协议引导智能体主动探索元数据。结合创新的双轨GRPO强化学习策略，有效解决了信用分配问题。实验表明，该方法在无需预加载模式的情况下，性能显著优于基础模型，并能媲美甚至超越依赖模式预填的强基准模型。
selection_source: fresh_fetch
motivation: 传统的Text-to-SQL研究假设已知完整模式，但在处理包含海量噪声元数据的真实企业级数据库时，这种假设并不成立。
method: 提出一种集成工具的四阶段协议，并采用双轨GRPO策略，通过令牌级掩码优势将探索奖励与执行结果分离以优化强化学习。
result: "在五个基准测试中，4B和8B模型变体分别实现了30.6%和16.6%的平均绝对性能提升，且表现优于依赖模式预填的基准。"
conclusion: TRUST-SQL证明了通过结构化工具集成和改进的强化学习策略，智能体可以在不预知数据库模式的情况下实现高效且准确的SQL生成。
---

## 摘要
Text-to-SQL 解析在全模式假设（Full Schema Assumption）下取得了显著进展。然而，这一前提在现实世界的企业环境中往往失效，因为这些环境中的数据库包含数百张表以及海量的噪声元数据。智能体必须主动识别并仅验证相关的子集，而不是预先注入完整的模式，这便产生了本研究探讨的“未知模式”（Unknown Schema）场景。为此，我们提出了 TRUST-SQL（通过工具在未知模式下进行真实推理）。我们将该任务建模为一个部分可观测马尔可夫决策过程（POMDP），其中自主智能体采用结构化的四阶段协议，将推理建立在经过验证的元数据之上。至关重要的是，该协议为我们新颖的双轨 GRPO（Dual-Track GRPO）策略提供了结构边界。通过应用 Token 级掩码优势，该策略将探索奖励与执行结果隔离开来，以解决信用分配问题，相比标准 GRPO 实现了 9.9% 的相对提升。在五个基准测试上的广泛实验表明，TRUST-SQL 的 4B 和 8B 变体相较于其基础模型，平均绝对提升分别达到了 30.6% 和 16.6%。值得注意的是，尽管在完全没有预加载元数据的情况下运行，我们的框架始终能达到或超过依赖模式预填充的强基准模型。

## Abstract
Text-to-SQL parsing has achieved remarkable progress under the Full Schema Assumption. However, this premise fails in real-world enterprise environments where databases contain hundreds of tables with massive noisy metadata. Rather than injecting the full schema upfront, an agent must actively identify and verify only the relevant subset, giving rise to the Unknown Schema scenario we study in this work. To address this, we propose TRUST-SQL (Truthful Reasoning with Unknown Schema via Tools). We formulate the task as a Partially Observable Markov Decision Process where our autonomous agent employs a structured four-phase protocol to ground reasoning in verified metadata. Crucially, this protocol provides a structural boundary for our novel Dual-Track GRPO strategy. By applying token-level masked advantages, this strategy isolates exploration rewards from execution outcomes to resolve credit assignment, yielding a 9.9% relative improvement over standard GRPO. Extensive experiments across five benchmarks demonstrate that TRUST-SQL achieves an average absolute improvement of 30.6% and 16.6% for the 4B and 8B variants respectively over their base models. Remarkably, despite operating entirely without pre-loaded metadata, our framework consistently matches or surpasses strong baselines that rely on schema prefilling.

---

## 论文详细总结（自动生成）

以下是对论文《TRUST-SQL: Tool-Integrated Multi-Turn Reinforcement Learning for Text-to-SQL over Unknown Schemas》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的 Text-to-SQL 研究大多基于“全模式假设”（Full Schema Assumption），即假设数据库的所有表结构（Schema）都能预先加载到模型的上下文窗口中。
*   **研究动机**：在真实企业环境中，数据库往往包含数百张表和海量噪声元数据，预加载不仅会导致上下文溢出，还会引入无关干扰。论文提出了**“未知模式”（Unknown Schema）**设定，要求智能体像人类专家一样，通过主动探索、验证元数据来构建 SQL，而非被动翻译。

### 2. 方法论
论文提出了 **TRUST-SQL** 框架，将任务建模为部分可观测马尔可夫决策过程（POMDP）：
*   **四阶段交互协议（EPGC）**：
    1.  **Explore（探索）**：调用工具查询数据库元数据。
    2.  **Propose（提议）**：强制性的认知检查点，要求智能体提交已验证的模式，以防止幻觉。
    3.  **Generate（生成）**：基于提议的模式生成候选 SQL 并观察执行结果。
    4.  **Confirm（确认）**：提交最终 SQL。
*   **双轨 GRPO（Dual-Track GRPO）策略**：
    *   针对多轮交互中的“信用分配”难题（即难以区分失败是因为找错了表还是写错了逻辑），将轨迹分为**模式轨道（Schema Track）**和**全轨道（Full Track）**。
    *   **令牌级掩码优势（Token-level Masked Advantages）**：通过掩码技术，使探索奖励仅作用于探索阶段的 Token，执行奖励作用于生成阶段，实现探索与生成的协同优化。
    *   **执行耦合奖励**：模式奖励仅在 SQL 执行正确时发放，确保探索质量与最终任务成功直接挂钩。

### 3. 实验设计
*   **数据集**：
    *   **BIRD-Dev**：大规模数据库基准，考察模式链接能力。
    *   **Spider-Test**：考察组合泛化能力。
    *   **鲁棒性变体**：Spider-Syn（同义词替换）、Spider-DK（领域知识）、Spider-Realistic（歧义处理）。
*   **对比方法**：
    *   **单轮模型**：OmniSQL-7B, SQL-R1-3B/7B。
    *   **多轮强化学习模型**：MTIR-SQL, SQL-Trail。
*   **评估指标**：执行准确率（Execution Accuracy, EX%），包括贪婪解码（Greedy）和多数投票（Majority Voting）。

### 4. 资源与算力
论文详细列出了训练配置：
*   **硬件**：使用了 **NVIDIA A100 GPU**。
*   **算力分配**：
    *   **Qwen3-4B**：SFT 阶段使用 16 张 A100（约 6.5 小时）；RL 阶段使用 8 张 A100（约 60 小时）。
    *   **Qwen3-8B**：SFT 阶段使用 16 张 A100（约 12 小时）；RL 阶段使用 32 张 A100（异步模式，约 40 小时）。
*   **框架**：基于 SLIME 强化学习框架实现。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个主流基准上进行了全面测试，涵盖了从 4B 到 8B 的模型参数量。
*   **消融实验**：非常充分。包括对交互协议（EC vs EGC vs EPGC）的先导研究、奖励权重 $\lambda$ 的平衡分析、模式奖励的设计维度（耦合 vs 稀疏）、训练/推理轮数预算的影响，以及冷启动 SFT 的必要性分析。
*   **客观性**：实验对比了在“未知模式”下的 TRUST-SQL 与在“全模式预填”下的强基准模型，证明了即使不给任何先验信息，该方法也能达到甚至超越特权模型，对比具有说服力。

### 6. 主要结论与发现
*   **性能飞跃**：TRUST-SQL 使 4B 和 8B 模型在未知模式下的平均绝对准确率分别提升了 **30.6%** 和 **16.6%**。
*   **超越预填**：在 BIRD 等复杂任务上，TRUST-SQL 在完全不预加载模式的情况下，表现优于那些预加载了完整模式的 SOTA 模型。
*   **抑制幻觉**：引入 Propose 阶段作为认知检查点，将幻觉错误率降低了 **9.4 倍**。
*   **信用分配有效性**：双轨 GRPO 相比标准 GRPO 实现了 9.9% 的相对性能提升，证明了分离探索与执行信号的必要性。

### 7. 优点
*   **范式创新**：打破了 Text-to-SQL 依赖全模式注入的固有思维，更符合工业界真实场景。
*   **结构化约束**：通过四阶段协议为强化学习提供了清晰的结构边界，有效解决了长轨迹下的奖励稀疏和信用分配问题。
*   **高效性**：相比于 CHESS 等依赖复杂 Prompt 工程的方法，TRUST-SQL 在推理延迟和 Token 消耗上降低了百倍以上。

### 8. 不足与局限
*   **推理开销**：多轮交互模式虽然准确，但相比单轮翻译，其推理延迟和数据库调用次数更高。
*   **方言限制**：目前主要针对 SQLite 进行优化和评估，尚未在 PostgreSQL 或 MySQL 等其他 SQL 方言上进行广泛验证。
*   **固定轮数预算**：训练时使用了固定的最大轮数（10 轮），对于极度复杂的数据库模式，可能需要更动态的轮数分配策略。

（完）
