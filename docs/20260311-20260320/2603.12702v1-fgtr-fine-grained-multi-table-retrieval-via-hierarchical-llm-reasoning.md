---
title: "FGTR: Fine-Grained Multi-Table Retrieval via Hierarchical LLM Reasoning"
title_zh: FGTR：基于分层大语言模型推理的细粒度多表检索
authors: "Chaojie Sun, Bin Cao, Tiantian Li, Chenyu Hou, Ruizhe Li, Qing Fan"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12702v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 通过分层LLM推理进行细粒度多表检索
tldr: 针对现有表格检索方法在处理多表查询和大型表格时存在的粗粒度编码、效率低下及推理能力不足等问题，本文提出了一种基于大语言模型（LLM）的层级化细粒度多表检索方法FGTR。该方法模拟人类推理过程，通过先识别模式元素再检索单元格内容，构建与查询高度相关的子表。实验表明，FGTR在Spider和BIRD数据集上显著提升了检索精度，为下游表格任务提供了更精准的数据支持。
selection_source: fresh_fetch
motivation: 现有表格检索方法多关注单表且编码粒度过粗，难以高效处理大型表格及复杂的多表查询需求。
method: 提出FGTR框架，利用LLM进行层级化推理，依次定位关键模式元素和单元格内容，从而构建精简准确的子表。
result: "在Spider和BIRD基准测试中，FGTR的F2指标分别比现有最优方法提升了18%和21%。"
conclusion: FGTR通过细粒度检索有效提升了多表查询的准确性，展现了优化表格相关下游任务端到端性能的巨大潜力。
---

## 摘要
随着大语言模型（LLMs）的飞速发展，基于 LLM 的表格检索研究日益增多。然而，现有研究通常侧重于单表查询，并通过对整个表格进行编码后的相似度匹配来实现。由于这些方法采用粗粒度编码，包含了大量与查询无关的数据，通常导致准确率较低，且在处理大型表格时效率低下，未能充分利用 LLM 的推理能力。此外，多表查询在检索任务中尚未得到充分探索。为此，我们提出了一种基于 LLM 的分层多表查询方法：细粒度多表检索（FGTR），这是一种采用类人推理策略的新型检索范式。通过分层推理，FGTR 首先识别相关的模式（schema）元素，然后检索相应的单元格内容，最终构建出一个与给定查询相匹配的简洁且准确的子表。为了全面评估 FGTR 的性能，我们基于 Spider 和 BIRD 构建了两个新的基准数据集。实验结果表明，FGTR 优于以往的最先进方法，在 Spider 上的 F2 指标提升了 18%，在 BIRD 上提升了 21%，证明了其在增强细粒度检索方面的有效性，以及在提高基于表格的下游任务端到端性能方面的潜力。

## Abstract
With the rapid advancement of large language models (LLMs), growing efforts have been made on LLM-based table retrieval. However, existing studies typically focus on single-table query, and implement it by similarity matching after encoding the entire table. These methods usually result in low accuracy due to their coarse-grained encoding which incorporates much query-irrelated data, and are also inefficient when dealing with large tables, failing to fully utilize the reasoning capabilities of LLM. Further, multi-table query is under-explored in retrieval tasks. To this end, we propose a hierarchical multi-table query method based on LLM: Fine-Grained Multi-Table Retrieval FGTR, a new retrieval paradigm that employs a human-like reasoning strategy. Through hierarchical reasoning, FGTR first identifies relevant schema elements and then retrieves the corresponding cell contents, ultimately constructing a concise and accurate sub-table that aligns with the given query. To comprehensively evaluate the performance of FGTR, we construct two new benchmark datasets based on Spider and BIRD . Experimental results show that FGTR outperforms previous state-of-the-art methods, improving the F_2 metric by 18% on Spider and 21% on BIRD, demonstrating its effectiveness in enhancing fine-grained retrieval and its potential to improve end-to-end performance on table-based downstream tasks.

---

## 论文详细总结（自动生成）

这是一份关于论文《FGTR: Fine-Grained Multi-Table Retrieval via Hierarchical LLM Reasoning》的结构化深度总结：

### 1. 核心问题与研究动机
*   **研究背景**：在医疗、金融等领域，大语言模型（LLM）处理结构化表格数据的需求日益增长。然而，LLM 面临上下文窗口限制、缺乏表格结构先验知识以及序列化导致的结构紊乱等挑战。
*   **核心问题**：现有的表格检索方法多针对**单表**，且采用**粗粒度**编码（对整个表格或整行/整列编码），导致检索结果包含大量无关噪声，处理大型表格时效率低下，且难以处理涉及多表关联（Join）的复杂查询。
*   **研究动机**：模拟人类阅读表格的认知过程——先看表头（Schema）理解框架，再定位相关列，最后寻找具体数值（Cell），从而实现精准的细粒度检索。

### 2. 方法论：FGTR 框架
FGTR（Fine-Grained Multi-Table Retrieval）采用分层推理策略，分为离线预处理和在线检索两个阶段：

*   **离线预处理**：
    *   **模式语义化**：利用 LLM 为表名和列名生成自然语言描述。
    *   **连接路径发现**：计算列间的语义相似度和 Jaccard 相似度，预估潜在的外键关系，以支持多表关联。
    *   **列式存储与索引**：对非数值单元格进行向量编码，并使用 HNSW 算法构建索引，实现高效检索。
*   **在线分层检索**：
    *   **第一阶段：模式检索（Schema Retrieval）**
        1.  **问题解析**：LLM 提取查询中的关键实体和需求。
        2.  **模式映射**：通过投票机制（K 次迭代）将需求映射到具体列，确保高召回率。
        3.  **模式填充**：自动补全主外键，确保多表连接的逻辑完整性。
    *   **第二阶段：单元格检索（Cell Retrieval）**
        1.  **范围解析**：LLM 确定各列的约束条件（如数值范围或特定字符串）。
        2.  **单元格映射**：对非数值列进行语义相似度匹配，对数值列进行精确匹配。
        3.  **单元格合并**：取各约束条件的并集，构建最终的精简子表。

### 3. 实验设计
*   **数据集**：
    *   **新构建基准**：基于 Spider 和 BIRD 数据集构建了 **SpiderQA** 和 **BirdQA**，通过解析 SQL 语句提取 Ground-truth 列和单元格。
    *   **下游任务验证**：使用 WikiTableQuestions (WTQ) 和 TabFACT。
*   **对比方法**：
    *   **模式级**：C3, CHESS, RSL-SQL, TableRAG。
    *   **单元格级**：TableRAG（将其扩展至多表场景作为主要基准）。
*   **评估指标**：Precision, Recall, $F_2$ Score（侧重召回）, Strict Recall (SR)。

### 4. 资源与算力
*   **模型使用**：实验涵盖了闭源模型 **GPT-4o** 和开源模型 **Llama3-8B**。
*   **算力细节**：文中提到对 Llama3-8B 进行了**知识蒸馏（Knowledge Distillation）**以提升其检索性能，但**未明确说明**具体的 GPU 型号、数量及训练时长。

### 5. 实验数量与充分性
*   **实验规模**：在四个主流数据集上进行了测试，涵盖了模式检索、单元格检索以及端到端下游任务。
*   **消融实验**：针对列解释、问题解析、模式映射、模式填充、范围解析等每个核心组件都做了消融测试，验证了各部分的必要性。
*   **鲁棒性测试**：分析了不同表格规模（Token 数量从 $10^2$ 到 $10^7$）对性能的影响。
*   **评价**：实验设计较为全面，通过构建新基准解决了细粒度检索缺乏评估数据的问题，对比实验涵盖了当前最先进的方案，结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能领先**：FGTR 在 Spider 和 BIRD 上的 $F_2$ 指标分别提升了 **18%** 和 **21%**。
*   **多表优势**：通过“模式填充”步骤，FGTR 在处理隐式多表连接查询时表现远超基准方法。
*   **下游增益**：精准的检索显著提升了后续 QA 和事实核查任务的准确率，证明了“检索-推理”链路的有效性。
*   **规模抗压**：当表格规模达到千万级 Token 时，传统方法性能骤降，而 FGTR 凭借分层过滤保持了极高的稳定性。

### 7. 优点与亮点
*   **认知模拟**：将复杂的表格理解分解为类人的层级化步骤，降低了 LLM 的推理难度。
*   **细粒度控制**：不仅定位到表，还能定位到具体的列和行切片，极大地节省了上下文窗口（Token 消耗减少）。
*   **多表支持**：首次系统性地解决了检索阶段的跨表关联键补全问题。
*   **自适应匹配**：结合了语义搜索（处理拼写错误/同义词）和精确匹配，增强了实用性。

### 8. 不足与局限
*   **推理延迟**：由于采用了多步推理（解析-映射-填充等）和多次投票机制，在线检索的响应时间可能高于单次编码匹配的方法。
*   **依赖 LLM 质量**：第一步“问题解析”和“范围解析”的效果高度依赖 LLM 的逻辑能力，若初次解析出错，会产生连锁反应。
*   **数值推理限制**：虽然支持数值匹配，但对于涉及复杂计算（如“平均值的两倍”）的检索约束，解析难度依然较大。
*   **算力成本**：多次调用 LLM API（尤其是 GPT-4o）进行检索会增加系统运行成本。

（完）
