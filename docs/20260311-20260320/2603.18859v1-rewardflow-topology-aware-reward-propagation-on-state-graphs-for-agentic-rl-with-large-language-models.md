---
title: "RewardFlow: Topology-Aware Reward Propagation on State Graphs for Agentic RL with Large Language Models"
title_zh: RewardFlow：大语言模型智能体强化学习中基于状态图的拓扑感知奖励传播
authors: "Xiao Feng, Bo Han, Zhanke Zhou, Jiaqi Fan, Jiangchao Yao, Ka Ho Li, Dahai Yu, Michael Kwok-Po Ng"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18859v1"
tags: ["query:agent"]
score: 9.0
evidence: 使用强化学习为智能体推理任务进行状态级奖励估计
tldr: 针对大语言模型在智能体推理任务中面临的奖励稀疏和过程奖励模型成本高的问题，本文提出 RewardFlow。该方法通过构建状态图并利用拓扑感知传播机制，从推理轨迹中估计状态级奖励。实验表明，RewardFlow 作为稠密奖励能显著提升强化学习的性能、鲁棒性和训练效率，在四个基准测试中均优于现有基线。
selection_source: fresh_fetch
motivation: 强化学习在提升 LLM 推理能力时受限于终端奖励的稀疏性，而训练专门的过程奖励模型成本过高。
method: 通过构建状态图并利用拓扑结构进行奖励传播，量化每个状态对最终成功的贡献，从而生成客观的状态级奖励。
result: 在四个智能体推理基准测试中，RewardFlow 在性能、鲁棒性和训练效率方面均显著优于之前的强化学习基线。
conclusion: RewardFlow 为智能体强化学习提供了一种轻量且高效的状态级奖励估计方案，有效解决了稀疏奖励带来的优化难题。
---

## 摘要
强化学习（RL）在增强大语言模型（LLMs）与外部环境交互的智能体推理能力方面具有巨大潜力。然而，终端奖励固有的稀疏性阻碍了细粒度的状态级优化。尽管过程奖励建模（process reward modeling）提供了一种有前景的替代方案，但训练专门的奖励模型通常涉及巨大的计算成本和扩展难题。为了应对这些挑战，我们提出了 RewardFlow，这是一种专为智能体推理任务量身定制的轻量级状态级奖励估计方法。RewardFlow 通过构建状态图，利用了推理轨迹中状态的内在拓扑结构。这使得能够分析各状态对成功的贡献，随后通过拓扑感知图传播来量化贡献并产生客观的状态级奖励。当作为密集奖励集成到强化学习优化中时，RewardFlow 在四个智能体推理基准测试中显著优于先前的强化学习基准，展示了卓越的性能、鲁棒性和训练效率。RewardFlow 的实现已在 https://github.com/tmlr-group/RewardFlow 公开。

## Abstract
Reinforcement learning (RL) holds significant promise for enhancing the agentic reasoning capabilities of large language models (LLMs) with external environments. However, the inherent sparsity of terminal rewards hinders fine-grained, state-level optimization. Although process reward modeling offers a promising alternative, training dedicated reward models often entails substantial computational costs and scaling difficulties. To address these challenges, we introduce RewardFlow, a lightweight method for estimating state-level rewards tailored to agentic reasoning tasks. RewardFlow leverages the intrinsic topological structure of states within reasoning trajectories by constructing state graphs. This enables an analysis of state-wise contributions to success, followed by topology-aware graph propagation to quantify contributions and yield objective, state-level rewards. When integrated as dense rewards for RL optimization, RewardFlow substantially outperforms prior RL baselines across four agentic reasoning benchmarks, demonstrating superior performance, robustness, and training efficiency. The implementation of RewardFlow is publicly available at https://github.com/tmlr-group/RewardFlow.

---

## 论文详细总结（自动生成）

以下是对论文《RewardFlow: Topology-Aware Reward Propagation on State Graphs for Agentic RL with Large Language Models》的深度结构化总结：

# 论文总结：RewardFlow 拓扑感知状态图奖励传播技术

## 1. 核心问题与整体含义（研究动机和背景）
在利用强化学习（RL）提升大语言模型（LLM）智能体（Agent）的推理能力时，核心障碍在于**奖励极其稀疏**。大多数环境仅在任务结束时提供终端奖励（成功/失败），而长程推理轨迹包含大量中间步骤，导致信用分配（Credit Assignment）困难。
*   **现有方案局限**：现有的过程奖励建模（PRM）通常依赖昂贵的人工标注数据或需要训练额外的奖励模型，这带来了巨大的计算开销和扩展性挑战。
*   **研究动机**：能否在不训练额外奖励模型的情况下，利用推理轨迹中状态之间的**内在拓扑关系**，客观地估计中间状态的过程奖励？

## 2. 方法论：核心思想与技术细节
RewardFlow 的核心思想是将多条推理轨迹聚合为一个**状态图（State Graph）**，通过图算法将终端奖励反向传播至中间状态。

### 关键技术流程：
1.  **状态图构建（Graph Construction）**：
    *   **状态归一化**：将语义等效的状态（如描述略有差异但环境实质相同）映射到统一的规范表示。
    *   **噪声剪枝**：识别并移除 LLM 生成的无效动作（如环境不接受的指令），确保图结构的纯净。
    *   **节点聚合**：将多条采样轨迹中的相同状态合并为节点，动作作为有向边，形成一个反映任务结构的子图。
2.  **拓扑感知奖励传播（Graph Propagation）**：
    *   利用**多源逆向广度优先搜索（BFS）**，从所有“成功”终端节点开始，根据跳数距离（Hop Distance）计算中间节点的奖励值 $R(s)$。
    *   距离成功越近的状态，获得的传播奖励越高。
3.  **动作级奖励映射（Reverse Back）**：
    *   将图中的节点奖励映射回原始轨迹。
    *   计算动作前后的奖励差值 $\tilde{r} = R(s_{t+1}) - R(s_t)$，作为该动作的密集奖励信号。
4.  **协同优势估计（Synergistic Advantages）**：
    *   结合**局部动作级优势**（基于状态图）和**全局轨迹级优势**（基于终端结果），通过 PPO 风格的裁剪目标函数更新策略。

## 3. 实验设计
论文在四个具有代表性的智能体基准测试中进行了验证，涵盖了文本和视觉模态：
*   **数据集/场景**：
    *   **ALFWorld**：文本交互式家庭任务（包含 6 类子任务）。
    *   **WebShop**：大规模在线购物交互。
    *   **Sokoban（推箱子）**：视觉空间推理任务（6x6 网格）。
    *   **DeepResearch**：基于搜索引擎的知识问答（单跳与多跳 QA）。
*   **对比基线（Benchmarks）**：
    *   **RLOO / GRPO**：主流的基于组采样的策略优化算法。
    *   **GiGPO**：专门针对智能体环境的状态级优势估计方法。
    *   **Search-R1 / ZeroSearch**：针对搜索增强推理的最新 RL 方法。

## 4. 资源与算力
论文详细记录了实验配置：
*   **模型规模**：使用了 Qwen2.5 系列（1.5B, 3B, 7B）以及视觉语言模型 Qwen2.5-VL（3B, 7B）。
*   **硬件资源**：
    *   1.5B/3B 模型训练：使用 **2 张 NVIDIA A100 或 H20 GPU**。
    *   7B 模型训练：使用 **4 张 NVIDIA A100 或 H20 GPU**。
*   **训练效率**：RewardFlow 引入的额外开销极低。实验显示，每步训练中图构建与奖励传播仅需 **1.5 到 2.4 秒**，相比于轨迹采样和模型更新的时间几乎可以忽略不计。

## 5. 实验数量与充分性
论文进行了多维度的实验验证，具有较高的客观性和说服力：
*   **主实验**：在 4 个基准测试、3 种模型规模上进行了全面对比。
*   **分布外（OOD）测试**：在 ALFWorld 和 DeepResearch 上验证了模型在未见任务/环境下的泛化能力。
*   **消融实验**：验证了状态归一化、噪声动作剪枝对性能的贡献。
*   **鲁棒性分析**：测试了在不同采样预算（Rollout 数量）下的表现，证明即使在低采样量下 RewardFlow 依然稳健。
*   **充分性评价**：实验设计严谨，涵盖了从文本到视觉、从简单到复杂推理的多种场景，对比基线均为当前最先进（SOTA）的方法。

## 6. 主要结论与发现
*   **性能显著提升**：在 ALFWorld 上比最强基线提升了 12.3%，在 Sokoban 上提升了 27.7%。
*   **小模型受益更大**：RewardFlow 显著降低了优化难度，使得 1.5B/3B 的小模型也能展现出极强的智能体推理能力。
*   **拓扑信号的有效性**：证明了状态间的拓扑距离是过程奖励的极佳代理信号，无需昂贵的外部奖励模型。
*   **高效性**：该方法在不增加显著计算负担的前提下，提供了比传统 PRM 更精准的监督信号。

## 7. 优点（亮点）
*   **无需训练奖励模型**：完全基于轨迹拓扑结构生成奖励，解决了 PRM 训练成本高和数据依赖的问题。
*   **拓扑感知**：通过状态聚合发现关键路径（如瓶颈状态），比单纯的轨迹内传播（如 GiGPO）更具全局观。
*   **通用性强**：同时适用于文本和视觉模态，且对随机性环境（如 DeepResearch）通过语义聚类展现了良好的适应性。

## 8. 不足与局限
*   **状态表示依赖**：该方法的效果高度依赖于状态表示的质量。如果环境反馈的信息过于贫乏，难以区分不同状态，则状态图的构建会失效。
*   **复杂开放环境挑战**：在完全开放、状态空间近乎无限的环境中，如何进行高效的状态聚合仍是一个挑战。
*   **推理痕迹缺失**：目前主要基于环境反馈的状态，尚未充分利用模型生成的思维链（CoT）等内部推理痕迹来构建更细粒度的图。

（完）
