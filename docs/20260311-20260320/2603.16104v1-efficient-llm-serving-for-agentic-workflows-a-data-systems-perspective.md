---
title: "Efficient LLM Serving for Agentic Workflows: A Data Systems Perspective"
title_zh: 面向智能体工作流的高效 LLM 推理服务：数据系统视角
authors: "Noppanat Wadlom, Junyi Shen, Yao Lu"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16104v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 针对具有重叠提示词的智能体工作流优化LLM推理服务
tldr: 针对智能体工作流中存在的跨调用冗余和依赖问题，本文从数据系统视角提出了Helium框架。Helium将智能体工作负载建模为查询计划，将LLM调用视为一级算子，并通过主动缓存和缓存感知调度技术，最大化Prompt和KV状态的复用。实验表明，Helium在多种工作负载下比现有最先进系统提速达1.56倍，证明了端到端工作流优化对提升LLM智能体效率的重要性。
selection_source: fresh_fetch
motivation: 现有的LLM推理系统主要优化单次调用，忽视了智能体工作流中由于并行探索和重叠提示词导致的跨调用冗余。
method: 提出了Helium框架，将工作流建模为查询计划，并集成主动缓存与缓存感知调度以实现跨调用的资源复用。
result: 在多种智能体工作负载下，Helium相比现有最先进的智能体服务系统实现了高达1.56倍的加速。
conclusion: 跨工作流的端到端优化是实现高效、可扩展的LLM智能体服务的关键。
---

## 摘要
智能体工作流由一系列相互依赖的大语言模型（LLM）调用组成，已成为现代 AI 系统中的主流工作负载。由于推测性执行和并行探索，这些工作流在重叠的提示词（prompts）和中间结果中表现出大量的冗余。现有的 LLM 推理服务系统（如 vLLM）专注于优化单个推理调用，而忽略了跨调用的依赖关系，导致了显著的效率低下。本文从数据系统的视角重新思考了 LLM 和智能体推理服务，并引入了 Helium。Helium 是一个感知工作流的推理服务框架，它将智能体工作负载建模为查询计划，并将 LLM 调用视为一等算子（first-class operators）。Helium 集成了主动缓存和感知缓存的调度，以最大限度地提高提示词、KV 状态和工作流之间的复用。通过这些技术，Helium 将经典的查询优化原则与 LLM 推理服务相结合，在各种工作负载上相比最先进的智能体推理服务系统实现了高达 1.56 倍的加速。我们的结果表明，跨工作流的端到端优化对于构建可扩展且高效的基于 LLM 的智能体至关重要。

## Abstract
Agentic workflows are composed of sequences of interdependent Large Language Model (LLM) calls, and they have become a dominant workload in modern AI systems. These workflows exhibit extensive redundancy from overlapping prompts and intermediate results due to speculative and parallel exploration. Existing LLM serving systems, such as vLLM, focus on optimizing individual inference calls and overlook cross-call dependencies, leading to significant inefficiencies. This paper rethinks LLM and agent serving from a data systems perspective and introduces Helium, a workflow-aware serving framework that models agentic workloads as query plans and treats LLM invocations as first-class operators. Helium integrates proactive caching and cache-aware scheduling to maximize reuse across prompts, KV states, and workflows. Through these techniques, Helium bridges classic query optimization principles with LLM serving, achieving up to 1.56x speedup over state-of-the-art agent serving systems on various workloads. Our results demonstrate that end-to-end optimization across workflows is essential for scalable and efficient LLM-based agents.

---

## 论文详细总结（自动生成）

以下是对论文《Efficient LLM Serving for Agentic Workflows: A Data Systems Perspective》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：AI 智能体（Agents）正从单次对话转向复杂的“智能体工作流”（Agentic Workflows），即由多个相互依赖的 LLM 调用组成的序列。
*   **核心问题**：
    *   **冗余严重**：并行探索（如多智能体辩论）和推测性执行导致大量重叠的提示词（Prompts）和中间结果。
    *   **系统孤岛**：现有的推理引擎（如 vLLM）是“算子近视”的，仅优化单次调用；而编排框架（如 LangGraph）将 LLM 视为黑盒 UDF，无法进行底层 KV 缓存优化。
*   **研究动机**：从**数据系统（数据库）**的视角重新审视 LLM 推理，将工作流视为查询计划，通过全局优化消除冗余。

### 2. 方法论：核心思想与关键技术
Helium 框架将 LLM 调用视为查询计划中的“一级算子”，核心技术包括：
*   **查询优化器（Query Optimizer）**：
    *   **计划剪枝与 CSE**：应用死代码消除和公共子图提取（Common Subgraph Elimination），合并结构相同且输入一致的算子。
    *   **提示词缓存（Prompt Caching）**：对于确定性算子（如贪婪采样），若输入已见，则直接用 `CacheFetch` 算子替换 LLM 推理，实现结果级复用。
*   **模板化基数树（Templated Radix Tree, TRT）**：
    *   一种创新的数据结构，同时建模提示词的**前缀结构**（静态文本+占位符）和算子间的**数据依赖**。
*   **主动缓存管理（Proactive Caching）**：
    *   不同于 vLLM 的被动 LRU 缓存，Helium 在执行前识别静态前缀并提前“预热”KV 缓存，避免重复的 Prefill 计算。
*   **感知缓存的调度算法（Cache-Aware Scheduling）**：
    *   将调度建模为最小化 Makespan 的优化问题（NP-Hard）。
    *   采用基于成本的贪心算法，利用 TRT 引导算子在多 GPU 间的分配，确保具有相同前缀的请求被连续调度到同一 Worker，最大化缓存命中率。

### 3. 实验设计
*   **场景与工作流**：
    *   **微基准测试**：Map-Reduce、多智能体辩论（Debate）、多智能体反思（Reflect）、迭代改进（Iterative）、并行链（Parallel）。
    *   **端到端场景**：复杂的“金融交易分析（Trading）”工作流，包含 19 个智能体和 88 个 LLM 算子。
*   **数据集**：MMLU、TAT-QA、Amazon Reviews、自定义金融数据集。
*   **对比方法（Baselines）**：
    *   **推理引擎**：vLLM（原生）。
    *   **编排框架**：LangGraph、AgentScope。
    *   **学术前沿系统**：Parrot（感知前缀）、KVFlow（SOTA 智能体缓存系统）。
    *   **传统模式**：OpWise（类似 Spark 的算子逐层执行）。

### 4. 资源与算力
*   **硬件环境**：一台配备 2 张 **NVIDIA H100 NVL (94GB)** GPU 的服务器，CPU 为 AMD EPYC 9554（64 核）。
*   **模型使用**：主要使用 **Qwen3-8B/14B/32B** 和 **Llama-3.1-8B**。
*   **训练/推理时长**：论文未提及模型训练（属于 Serving 研究），推理实验在不同 Batch Size（8 到 80）下进行。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 5 种原始模式的微基准测试。
    *   进行了 1 组大规模端到端复杂工作流实验。
    *   **消融实验**：分别验证了 KV 缓存、计划剪枝、提示词缓存和调度算法的贡献。
    *   **敏感性分析**：测试了不同 Batch Size、并行分支数、辩论轮数、Worker 数量、模型大小及内存限制下的表现。
*   **充分性评价**：实验设计非常全面且客观。通过对比多种不同类型的基准系统，并深入分析了调度开销和内存占用，有力证明了 Helium 的高效性。

### 6. 主要结论与发现
*   **性能提升**：Helium 在微基准测试中比 SOTA 系统（KVFlow）快达 **1.56 倍**，在复杂金融工作流中比 LangGraph 快 **1.49 倍**。
*   **缓存效率**：通过主动调度，Helium 的 KV 缓存命中率显著高于被动缓存系统（如 Parrot 和 vLLM）。
*   **调度优势**：Helium 的调度算法接近理论最优解（与 MILP 求解器对比，差距仅 0.9%），且调度开销极低（毫秒级）。
*   **规模效应**：随着 Batch Size 和模型规模的增大，Helium 的优化收益更加显著。

### 7. 优点与亮点
*   **视角独特**：将 LLM 推理问题转化为经典的数据库查询优化问题，理论基础扎实。
*   **主动性**：打破了现有系统“请求驱动”的被动模式，利用工作流的先验知识进行“主动”缓存和调度。
*   **系统性强**：不仅优化了 KV 缓存，还通过算子合并和结果缓存减少了不必要的 LLM 调用。
*   **低开销**：TRT 结构比传统的基数树更节省内存，且优化器运行速度极快。

### 8. 不足与局限
*   **动态控制流限制**：目前的 DAG 抽象对包含复杂循环（Loop）和动态分支（运行时决定分支数）的工作流支持有限。
*   **外部 API 干扰**：实验主要集中在本地 LLM 调用，未深入探讨外部工具调用（如搜索 API）带来的不可预测延迟对调度的影响。
*   **同质模型假设**：假设工作流使用相同的基座模型，未充分讨论多模型混合部署下的优化。

（完）
