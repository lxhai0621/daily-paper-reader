---
title: "ProRL Agent: Rollout-as-a-Service for RL Training of Multi-Turn LLM Agents"
title_zh: ProRL Agent：面向多轮 LLM 智能体强化学习训练的 Rollout 即服务
authors: "Hao Zhang, Mingjie Liu, Shaokun Zhang, Songyang Han, Jian Hu, Zhenghui Jin, Yuchi Zhang, Shizhe Diao, Ximing Lu, Binfeng Xu, Zhiding Yu, Jan Kautz, Yi Dong"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18815v1"
tags: ["query:agent"]
score: 9.0
evidence: 多轮LLM智能体强化学习训练的Rollout服务
tldr: 本研究针对多轮LLM智能体在强化学习（RL）训练中面临的轨迹生成难题，提出了ProRL Agent。该系统采用“Rollout-as-a-Service”理念，通过API服务解耦了轨迹采样与训练循环，解决了现有架构难以迁移和维护的问题。它提供了可扩展的沙盒环境，支持在无根权限的HPC设置下运行多种任务。ProRL Agent已集成至NVIDIA NeMo Gym，显著提升了智能体在软件工程、数学及编程等复杂任务上的训练效率。
selection_source: fresh_fetch
motivation: 现有的LLM智能体强化学习框架通常将轨迹采样与训练循环紧密耦合，导致系统难以维护且难以在不同计算环境间迁移。
method: 提出了ProRL Agent架构，通过API提供全生命周期的轨迹生成服务，并构建了支持HPC环境的标准化、可扩展沙盒。
result: 在软件工程、数学、STEM和编程等多种任务上的强化学习训练验证了该系统的可扩展性和有效性。
conclusion: ProRL Agent为多轮LLM智能体提供了一个解耦且高效的训练基础设施，并作为开源工具集成到了NVIDIA NeMo Gym中。
---

## 摘要
多轮大语言模型（LLM）智能体在解决复杂的交互式任务中变得越来越重要，而强化学习（RL）是改善其长程行为的关键要素。然而，强化学习训练需要生成大量的沙箱化 rollout 轨迹，且现有的基础设施通常将 rollout 编排与训练循环耦合在一起，导致系统难以迁移和维护。秉持“rollout 即服务”的理念，我们提出了 ProRL Agent，这是一个可扩展的基础设施，通过 API 服务为完整的智能体 rollout 生命周期提供支持。ProRL Agent 还提供了标准化且可扩展的沙箱环境，支持在无根权限（rootless）高性能计算（HPC）设置下执行多种智能体任务。我们通过在软件工程、数学、STEM 和编程任务上的强化学习训练验证了 ProRL Agent。ProRL Agent 已开源，并作为 NVIDIA NeMo Gym 的一部分进行了集成。

## Abstract
Multi-turn LLM agents are increasingly important for solving complex, interactive tasks, and reinforcement learning (RL) is a key ingredient for improving their long-horizon behavior. However, RL training requires generating large numbers of sandboxed rollout trajectories, and existing infrastructures often couple rollout orchestration with the training loop, making systems hard to migrate and maintain. Under the rollout-as-a-service philosophy, we present ProRL Agent , a scalable infrastructure that serves the full agentic rollout lifecycle through an API service. ProRL Agent also provides standardized and extensible sandbox environments that support diverse agentic tasks in rootless HPC settings. We validate ProRL Agent through RL training on software engineering, math, STEM, and coding tasks. ProRL Agent is open-sourced and integrated as part of NVIDIA NeMo Gym.

---

## 论文详细总结（自动生成）

### ProRL Agent: 面向多轮 LLM 智能体强化学习训练的 Rollout 即服务

#### 1. 论文的核心问题与整体含义
*   **研究动机**：多轮 LLM 智能体在处理复杂任务（如软件工程、网页浏览）时，需要通过强化学习（RL）来优化长程行为。然而，现有的 RL 训练框架通常将 **Rollout（轨迹采样/环境交互）** 与 **训练循环（模型更新）** 紧密耦合。
*   **核心问题**：
    1.  **资源冲突**：Rollout 是 I/O 密集型（涉及沙箱创建、工具调用），而训练是 GPU 密集型，耦合会导致资源效率低下。
    2.  **维护困难**：Rollout 逻辑嵌入训练器中，导致迁移到不同训练后端或支持新任务时需要重构大量代码。
    3.  **环境限制**：现有的沙箱（如 Docker）通常需要 root 权限，难以在高性能计算（HPC）集群的无根（rootless）环境中部署。

#### 2. 论文提出的方法论
论文提出了 **ProRL Agent**，核心思想是 **“Rollout 即服务”（Rollout-as-a-Service, RaaS）**，将 Rollout 生命周期完全解耦为一个独立的 HTTP 服务。
*   **三阶段异步流水线**：将 Rollout 分为 **INIT（环境初始化）**、**RUN（智能体执行）** 和 **EVAL（结果评估）** 三个阶段，每个阶段由独立的线程池管理，实现任务重叠执行，最大化吞吐量。
*   **HPC 兼容沙箱**：基于 **Singularity** 构建容器运行时，支持无根权限执行，并为每个容器分配独立的环回 IP 和 Unix 域套接字（UDS）通信，以降低工具调用延迟。
*   **Token-in/Token-out 通信**：在整个流水线中使用 Token ID 而非纯文本进行传输，彻底消除重分词漂移（re-tokenization drift）问题，确保训练的稳定性。
*   **LLM 后端管理**：内置基于最小堆（Min-heap）的负载均衡器，支持动态注册推理服务器和 Checkpoint 自动切换。
*   **高效 DAPO 调度**：针对动态采样策略优化（DAPO）算法，实现了异步补充机制，减少了等待“有效样本”时的计算资源浪费。

#### 3. 实验设计
*   **实验场景与数据集**：
    *   **软件工程**：使用 SWE-Bench Verified 评估模型解决真实 GitHub Issue 的能力。
    *   **STEM 领域**：使用 SCP-116K 数据集，智能体配备网页搜索、Bash 和 IPython 工具。
    *   **数学**：使用 DeepScaleR 数据和 AMC 评估，配备 IPython 验证工具。
    *   **编程**：使用 Codeforces 题目，配备文件编辑器和测试脚本执行工具。
*   **对比方法**：主要对比了基础指令微调模型（如 Qwen3-Instruct）以及现有的智能体 RL 框架（如 SkyRL-v0）。
*   **模型规模**：涵盖了 Qwen3-4B、8B 和 14B 三种参数规模。

#### 4. 资源与算力
*   **GPU 资源**：所有强化学习训练均在 **32 台 NVIDIA H100 GPU** 上完成。
*   **训练参数**：Batch size 为 32，Mini-batch 为 8，每个实例生成 8 条 Rollout 轨迹。
*   **训练时长**：文中未明确给出总训练时长，但提供了训练曲线（通常在 60-100 个 Step 内收敛）。

#### 5. 实验数量与充分性
*   **实验覆盖面**：论文在四个完全不同的智能体领域（软件、数学、STEM、代码）进行了端到端训练验证，证明了系统的通用性。
*   **消融实验**：针对负载均衡、高效 Bash 实现、过期任务清理等关键组件进行了消融实验，量化了各部分对吞吐量和 GPU 利用率的贡献。
*   **扩展性测试**：进行了多节点扩展性实验，证明了 Rollout 吞吐量随计算节点增加呈近线性增长。
*   **评价**：实验设计较为充分且客观，涵盖了从系统性能（吞吐量、延迟）到模型效果（Benchmark 分数）的全方位评估。

#### 6. 论文的主要结论与发现
*   **性能提升**：通过 ProRL Agent 进行 RL 训练，Qwen3 系列模型在 SWE-Bench Verified 上取得显著进步。例如，8B 模型的分数从 9.6 提升至 18.0，接近翻倍。
*   **系统优势**：解耦设计使得 Rollout 节点和训练节点可以独立优化。优化的工具后端（如高效 Bash）显著降低了动作执行时间（从 0.78s 降至 0.42s）。
*   **算法效率**：异步 DAPO 调度显著减少了 GPU 的空闲等待时间，提高了硬件利用率。

#### 7. 优点
*   **架构领先**：首次在智能体 RL 领域系统性地实践了“Rollout 即服务”理念，极大地提升了基础设施的可移植性和可维护性。
*   **HPC 友好**：解决了工业界和学术界在共享集群（如 Slurm 环境）中运行复杂智能体沙箱的痛点。
*   **工程严谨**：对 Token 漂移、负载均衡、异步流水线等底层细节的处理非常到位，具有很强的实用价值。
*   **开源贡献**：作为 NVIDIA NeMo Gym 的一部分开源，对社区具有重要意义。

#### 8. 不足与局限
*   **环境多样性**：虽然支持多种任务，但目前主要集中在代码和文本交互类任务，对于需要复杂图形界面（GUI）或超长时程（如数天）的任务支持尚待验证。
*   **偏差风险**：RL 训练高度依赖于奖励函数的设计（如测试用例的覆盖率），如果奖励信号存在偏差，模型可能会产生“刷分”行为而非真正的能力提升。
*   **集群鲁棒性**：在大规模集群（数百个节点）下的长时运行稳定性（如处理节点掉线、网络抖动）仍有进一步优化的空间。

（完）
