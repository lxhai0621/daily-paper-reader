---
title: Hypothesis-Conditioned Query Rewriting for Decision-Useful Retrieval
title_zh: 针对决策有用检索的假设条件查询重写
authors: "Hangeol Chang, Changsun Lee, Seungjoon Rho, Junho Yeo, Jong Chul Ye"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19008v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 基于假设条件的查询重写，用于RAG中面向证据的检索
tldr: 针对检索增强生成（RAG）在多选项决策任务中检索内容缺乏区分度的问题，本文提出了一种无需训练的预检索框架HCQR。该方法通过生成初步假设，并将其重写为支持假设、区分选项和验证线索的三类针对性查询，将检索目标从主题相关转向证据导向。实验证明，HCQR在医学问答任务上显著提升了准确率，有效辅助模型做出更精准的决策。
selection_source: fresh_fetch
motivation: 传统的RAG方法倾向于检索主题相关的背景信息，往往无法提供足以区分不同候选答案的关键决策证据。
method: 提出HCQR框架，通过初步假设生成三类特定查询，分别用于支持假设、区分备选方案以及验证问题中的关键线索。
result: 在MedQA和MMLU-Med数据集上，HCQR相比基础RAG分别提升了5.9和3.6个百分点，表现优于重排序和过滤等基准方法。
conclusion: 通过假设驱动的查询重写，可以将RAG从主题导向转变为证据导向，从而显著增强大语言模型在复杂决策任务中的表现。
---

## 摘要
检索增强生成 (RAG) 通过将生成过程建立在外部非参数化知识的基础上，提升了大型语言模型 (LLMs) 的性能。然而，当任务需要在竞争选项中做出选择时，仅将生成建立在广泛相关的上下文基础上，往往不足以驱动最终决策。现有的 RAG 方法通常依赖于单个初始查询，这往往更倾向于主题相关性而非决策相关证据，因此检索到的背景信息可能无法区分不同的答案选项。为解决这一问题，我们提出了假设条件查询重写 (HCQR)，这是一个无需训练的预检索框架，它将 RAG 从面向主题的检索转向面向证据的检索。HCQR 首先从输入问题和候选选项中推导出一个轻量级的工作假设，然后将检索重写为三个有针对性的查询，以寻求证据来：(1) 支持该假设，(2) 将其与竞争替代方案区分开来，以及 (3) 验证问题中的显著线索。这种方法使上下文检索能够更直接地与答案选择对齐，允许生成器根据检索到的证据确认或推翻初始假设。在 MedQA 和 MMLU-Med 上的实验表明，HCQR 始终优于单查询 RAG 以及重排序/过滤基准，相比 Simple RAG，平均准确率分别提高了 5.9 和 3.6 个百分点。代码可在 https://anonymous.4open.science/r/HCQR-1C2E 获取。

## Abstract
Retrieval-Augmented Generation (RAG) improves Large Language Models (LLMs) by grounding generation in external, non-parametric knowledge. However, when a task requires choosing among competing options, simply grounding generation in broadly relevant context is often insufficient to drive the final decision. Existing RAG methods typically rely on a single initial query, which often favors topical relevance over decision-relevant evidence, and therefore retrieves background information that can fail to discriminate among answer options. To address this issue, here we propose Hypothesis-Conditioned Query Rewriting (HCQR), a training-free pre-retrieval framework that reorients RAG from topic-oriented retrieval to evidence-oriented retrieval. HCQR first derives a lightweight working hypothesis from the input question and candidate options, and then rewrites retrieval into three targeted queries that seek evidence to: (1) support the hypothesis, (2) distinguish it from competing alternatives, and (3) verify salient clues in the question. This approach enables context retrieval that is more directly aligned with answer selection, allowing the generator to confirm or overturn the initial hypothesis based on the retrieved evidence. Experiments on MedQA and MMLU-Med show that HCQR consistently outperforms single-query RAG and re-rank/filter baselines, improving average accuracy over Simple RAG by 5.9 and 3.6 points, respectively. Code is available at https://anonymous.4open.science/r/HCQR-1C2E.

---

## 论文详细总结（自动生成）

以下是对论文《Hypothesis-Conditioned Query Rewriting for Decision-Useful Retrieval》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的检索增强生成（RAG）系统通常依赖单一的初始查询，这导致检索结果往往倾向于“主题相关性”（Topical Relevance）而非“决策有用性”（Decision Usefulness）。
*   **研究动机**：在需要从多个竞争选项中做出选择的任务（如医学考试）中，仅提供背景知识是不够的。如果检索到的内容无法区分候选答案之间的细微差别，反而会引入无关信息干扰模型推理。
*   **整体含义**：本文旨在将 RAG 从“面向主题的检索”转向“面向证据的检索”，通过预设一个工作假设来引导检索过程，从而获取能够支持、区分或验证特定选项的关键证据。

### 2. 方法论：核心思想与技术细节
HCQR 是一个**无需训练（Training-free）**的预检索框架，包含两个主要阶段：

*   **阶段 1：假设制定器（Hypothesis Formulator）**
    *   输入问题和所有候选选项。
    *   生成一个结构化的“工作假设状态”，包含：工作假设（初步预测）、区分性特征（区分选项的关键点）、确认性证据（若假设正确应具备的事实）以及简短推理。
*   **阶段 2：查询重写器（Query Rewriter）**
    *   将上述状态转化为三个针对性的查询：
        1.  **支持查询 ($q_1$)**：寻找直接支持工作假设的证据。
        2.  **区分查询 ($q_2$)**：寻找能够区分领先候选选项的准则或对比信息。
        3.  **特征查询 ($q_3$)**：验证题干中的显著线索，确保即使假设错误也能找回关键事实。
*   **检索与融合**：
    *   对三个查询分别进行检索（各取 Top-5），去重并合并后（总预算限制为 15 篇文档）输入给最终生成器。
    *   **关键设计**：工作假设仅用于引导检索，默认情况下不直接展示给最终生成器，以避免“答案锚定”偏差。

### 3. 实验设计
*   **数据集**：
    *   **MedQA**：大规模医学开放域问答。
    *   **MMLU-Med**：MMLU 数据集中的医学子集。
*   **基准方法（Baselines）**：
    *   **No-RAG (CoT)**：直接思维链推理。
    *   **Simple RAG**：使用原始问题进行单次检索。
    *   **Rewriting / HyDE**：查询改写和假设文档检索。
    *   **Rerank-RAG / MAIN-RAG**：检索后重排序或多智能体过滤。
*   **模型**：使用了 Llama 3.2-3B、Llama 3.1-8B、Qwen3-4B 和 Qwen3-30B 四种不同规模的指令微调模型。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **推理细节**：提到使用 `vLLM` 进行推理，上下文长度设为 8192 token，采用贪婪解码（Greedy Decoding）。由于该方法是无需训练的（Training-free），其主要开销在于多次 LLM 调用（生成假设和重写查询）。

### 5. 实验数量与充分性
*   **实验规模**：在 2 个数据集上针对 4 种模型进行了全面测试，并对比了 6 种主流基准方法。
*   **消融实验**：
    *   测试了移除 $q_1, q_2, q_3$ 中任意一个查询的影响。
    *   对比了“仅暴露选项”与“完整 HCQR 流程”的差异。
*   **分析深度**：引入了独特的**上下文效用分析（Context-Utility Analysis）**，使用 GPT-4 作为裁判，将检索结果分为“蕴含（Entailed）”、“有用（Useful）”和“无用（Not Useful）”，从定性角度验证了检索质量的提升。
*   **评价**：实验设计非常充分且客观，通过多模型、多维度的对比证明了方法的稳健性。

### 6. 主要结论与发现
*   **性能领先**：HCQR 在所有模型和数据集上均取得最佳表现。在 MedQA 上比 Simple RAG 平均提高 5.9%，在 MMLU-Med 上提高 3.6%。
*   **检索质量提升**：HCQR 显著提高了“决策有用率”（DUR），大幅减少了“无用”上下文的比例。
*   **首选预检索**：实验表明，优化“第一跳”查询的构建比增加候选池大小或事后过滤更有效。如果初始检索没能找回决策证据，后续的重排序也难以挽救。
*   **模型规模无关性**：无论是在 3B 还是 30B 模型上，HCQR 都能带来一致的增益。

### 7. 优点
*   **决策导向**：精准捕捉了 RAG 在决策任务中的痛点，将检索目标从模糊的相关性转向明确的证据验证。
*   **即插即用**：无需对模型进行微调，通过 Prompt 工程即可实现，易于集成到现有 RAG 系统中。
*   **结构化推理**：通过三路查询（支持、区分、验证）构建了严密的逻辑闭环，增强了检索的覆盖面和深度。

### 8. 不足与局限
*   **假设依赖性**：如果初始的“工作假设”严重偏离事实，可能会导致检索方向出现偏差（尽管 $q_3$ 旨在缓解此问题）。
*   **领域局限**：实验主要集中在医学领域，虽然医学是决策任务的典型代表，但在其他更具开放性或缺乏明确选项的任务中的泛化能力有待验证。
*   **推理成本**：相比 Simple RAG，HCQR 需要额外的 LLM 调用来生成假设和查询，增加了首字延迟（TTFT）和计算成本。
*   **裁判偏差**：效用分析依赖于 LLM 裁判，可能存在模型偏好或评估偏差。

（完）
