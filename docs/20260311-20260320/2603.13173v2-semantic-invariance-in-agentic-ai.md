---
title: Semantic Invariance in Agentic AI
title_zh: 代理式人工智能中的语义不变性
authors: "I. de Zarzà, J. de Curtò, Jordi Cabot, Pietro Manzoni, Carlos T. Calafate"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.13173v2"
tags: ["query:agent"]
score: 9.0
evidence: 基于LLM的自主智能体的鲁棒性与语义不变性
tldr: 本研究探讨了智能体AI中的语义不变性，即大语言模型在面对语义等价但表述不同的输入时，其推理过程是否保持稳定。作者提出了一个变态测试框架，通过八种语义保持转换对七种主流大模型在八个科学领域的推理能力进行评估。研究发现，模型规模并非鲁棒性的决定因素，较小的模型在某些情况下表现出更高的稳定性，揭示了当前大模型在可靠性方面的局限。
selection_source: fresh_fetch
motivation: 现有的基准测试仅评估固定表述下的准确性，无法衡量LLM智能体在面对语义等价的输入变体时推理的稳定性。
method: 提出一种变态测试框架，应用包括改写、事实重排、语境变换等在内的八种语义保持转换，对四类架构的七个基础模型进行系统评估。
result: 实验显示模型规模与鲁棒性并不成正比，较小的Qwen3-30B-A3B稳定性最高，而部分大型模型反而表现出更高的脆弱性。
conclusion: 语义不变性是衡量智能体AI可靠性的关键维度，未来的模型开发需超越单纯的规模扩张，重点提升推理的语义稳定性。
---

## 摘要
大语言模型（LLMs）越来越多地在决策支持、科学问题解决和多智能体协作系统中充当自主推理智能体。然而，在重大应用中部署 LLM 智能体需要确保其推理在语义等价的输入变化下保持稳定，我们将这一特性称为语义不变性（semantic invariance）。标准基准评估通常在固定且规范的问题表述上评估准确性，无法捕捉到这一关键的可靠性维度。为解决这一缺陷，本文提出了一个蜕变测试（metamorphic testing）框架，用于系统地评估 LLM 推理智能体的鲁棒性。我们在涵盖四个不同架构家族的七个基础模型上应用了八种语义保持转换（恒等、改写、事实重排、扩充、缩减、学术语境、商业语境和对比表述），这些模型包括：Hermes (70B, 405B)、Qwen3 (30B-A3B, 235B-A22B)、DeepSeek-R1 以及 gpt-oss (20B, 120B)。我们的评估涵盖了八个科学领域的 19 个多步推理问题。结果表明，模型规模并不能预测鲁棒性：较小的 Qwen3-30B-A3B 实现了最高的稳定性（79.6% 的不变响应，语义相似度为 0.91），而较大的模型则表现出更强的脆弱性。

## Abstract
Large Language Models (LLMs) increasingly serve as autonomous reasoning agents in decision support, scientific problem-solving, and multi-agent coordination systems. However, deploying LLM agents in consequential applications requires assurance that their reasoning remains stable under semantically equivalent input variations, a property we term semantic invariance. Standard benchmark evaluations, which assess accuracy on fixed, canonical problem formulations, fail to capture this critical reliability dimension. To address this shortcoming, in this paper we present a metamorphic testing framework for systematically assessing the robustness of LLM reasoning agents, applying eight semantic-preserving transformations (identity, paraphrase, fact reordering, expansion, contraction, academic context, business context, and contrastive formulation) across seven foundation models spanning four distinct architectural families: Hermes (70B, 405B), Qwen3 (30B-A3B, 235B-A22B), DeepSeek-R1, and gpt-oss (20B, 120B). Our evaluation encompasses 19 multi-step reasoning problems across eight scientific domains. The results reveal that model scale does not predict robustness: the smaller Qwen3-30B-A3B achieves the highest stability (79.6% invariant responses, semantic similarity 0.91), while larger models exhibit greater fragility.

---

## 论文详细总结（自动生成）

这篇论文对代理式人工智能（Agentic AI）中的“语义不变性”进行了深入探讨，以下是对该研究的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：研究大语言模型（LLM）作为自主推理智能体时，其推理结果在面对“语义等价但表述不同”的输入时是否保持稳定（即**语义不变性**）。
*   **研究动机**：
    *   目前的基准测试（如 MMLU, GSM8K）侧重于固定表述下的准确率，忽略了模型对输入微小变化的敏感性。
    *   在医疗、金融等高风险领域，如果智能体因为问题换了一种说法（如改变事实顺序或语境）就给出不同的答案，其可靠性将面临严重挑战。
    *   现有研究多关注对抗性攻击，而忽略了日常应用中常见的语义保持转换。

### 2. 方法论
*   **核心思想**：引入**蜕变测试（Metamorphic Testing）**框架。该方法不需要预先知道每个变体问题的标准答案，而是通过检查原始输入与转换后输入的输出之间是否满足特定的“蜕变关系”（即输出应保持一致）来评估鲁棒性。
*   **关键技术细节**：
    *   **八种蜕变关系（MRs）**：
        1.  **结构转换**：恒等（Identity）、改写（Paraphrase）、事实重排（Reorder Facts）。
        2.  **冗余度转换**：扩充（Expand，增加解释）、缩减（Contract，精简语言）。
        3.  **语境转换**：学术语境（Academic）、商业语境（Business）、对比表述（Contrastive，引入干扰项作为负控制）。
    *   **评估指标**：
        *   **语义相似度得分**：利用 `all-MiniLM-L6-v2` 模型计算推理轨迹和答案的向量余弦相似度。
        *   **得分增量（$\Delta$）**：衡量转换前后模型表现的变化。
        *   **平均绝对增量（MAD）**：衡量模型的不稳定性。
        *   **稳定性率**：输出变化极小（$\Delta < 0.05$）的实例比例。

### 3. 实验设计
*   **数据集与场景**：构建了一个包含 19 个多步推理问题的语料库，涵盖物理、数学、化学、经济、统计、生物、微积分和优化 8 个科学领域，分为易、中、难三个等级。
*   **对比模型**：选取了 4 个架构家族的 7 个模型：
    *   **Hermes 家族** (70B, 405B)
    *   **Qwen3 家族** (30B-A3B, 235B-A22B，采用 MoE 架构)
    *   **DeepSeek 家族** (DeepSeek-R1-0528)
    *   **gpt-oss 家族** (20B, 120B)
*   **基准（Benchmark）**：以原始问题的推理轨迹和最终答案作为基准，对比不同转换下的输出一致性。

### 4. 资源与算力
*   **算力平台**：实验通过 **Nebius AI** 平台进行。
*   **推理参数**：统一设置为 Temperature 0.7, Top-p 0.95, Max tokens 1024。
*   **说明**：文中未明确提及具体的 GPU 型号、数量或总训练时长，因为该研究侧重于推理评估而非模型训练。

### 5. 实验数量与充分性
*   **实验规模**：总计进行了 1,064 次独立推理任务（19 个问题 $\times$ 8 种转换 $\times$ 7 个模型）。
*   **充分性与客观性**：
    *   实验覆盖了多种主流架构和不同参数规模的模型。
    *   使用了 Mann-Whitney U 和 Kruskal-Wallis 等统计检验方法来验证模型间差异的显著性。
    *   引入了“对比表述”作为负控制组，增强了实验设计的严密性。

### 6. 主要结论与发现
*   **规模-鲁棒性反转**：模型规模并不预示鲁棒性。较小的 **Qwen3-30B-A3B**（仅 3B 激活参数）表现出最高的稳定性（79.6%），而参数量巨大的模型（如 405B 或 235B）反而更脆弱。
*   **家族特征签名**：不同架构有特定的弱点。例如，Hermes 对对比干扰敏感；DeepSeek-R1 对事实顺序和缩减转换敏感。
*   **普遍的对比脆弱性**：所有模型在面对“对比表述”（引入无关但具干扰性的信息）时，性能均显著下降，这是目前 LLM 推理的共性弱点。
*   **语境适应性**：大多数模型能较好地处理学术或商业语境的切换，这表明简单的语境重构对语义不变性影响较小。

### 7. 优点
*   **视角独特**：超越了单纯的准确率评估，提出了“语义不变性”这一衡量智能体可靠性的关键维度。
*   **过程评估**：不仅看最终答案，还通过语义相似度分析推理轨迹（Reasoning Trace）的连贯性。
*   **实用性强**：为开发者在特定应用场景下选择合适的模型提供了量化依据。

### 8. 不足与局限
*   **样本量限制**：19 个问题虽然涵盖多领域，但样本绝对数量较小，可能无法完全代表所有复杂的推理场景。
*   **单次推理偏差**：实验采用单次推理（Single-inference），未考虑 LLM 采样随机性带来的波动，未来可引入多次采样取平均值。
*   **转换偏差**：部分转换（如改写、扩充）是由 LLM 辅助生成的，可能带有生成模型自身的风格偏好，从而影响评估结果。

（完）
