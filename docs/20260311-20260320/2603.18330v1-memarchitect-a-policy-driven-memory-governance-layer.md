---
title: "MemArchitect: A Policy Driven Memory Governance Layer"
title_zh: MemArchitect：一种策略驱动的内存治理层
authors: "Lingavasan Suresh Kumar, Yang Ba, Rong Pan"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18330v1"
tags: ["query:agent"]
score: 10.0
evidence: 智能体上下文管理的记忆治理层
tldr: 针对持久化大模型智能体在内存管理中存在的冲突、隐私及过时信息等治理难题，本文提出MemArchitect。这是一个将内存生命周期管理与模型权重解耦的治理层，通过引入衰减、冲突解决和隐私控制等显式规则策略，显著提升了自主系统的可靠性与安全性。
selection_source: fresh_fetch
motivation: 现有的RAG框架将内存视为被动存储，缺乏处理矛盾、保护隐私及清理过时信息的有效治理机制。
method: 提出MemArchitect治理层，通过解耦内存管理并实施基于规则的显式策略（如内存衰减和冲突解决）来管控内存。
result: 实验证明，在智能体场景下，经过治理的内存表现持续优于未经管理的内存。
conclusion: 结构化的内存治理对于构建可靠且安全的自主大模型系统至关重要。
---

## 摘要
持久性大语言模型 (LLM) 智能体揭示了内存管理中一个关键的治理空白。标准的检索增强生成 (RAG) 框架将内存视为被动存储，缺乏解决矛盾、执行隐私保护或防止过时信息（“僵尸记忆”）污染上下文窗口的机制。我们引入了 MemArchitect，这是一个将内存生命周期管理与模型权重解耦的治理层。MemArchitect 执行显式的、基于规则的策略，包括内存衰减、冲突解决和隐私控制。我们证明，在智能体场景下，受治理的内存表现始终优于未管理的内存，这强调了结构化内存治理对于构建可靠且安全的自主系统的重要性。

## Abstract
Persistent Large Language Model (LLM) agents expose a critical governance gap in memory management. Standard Retrieval-Augmented Generation (RAG) frameworks treat memory as passive storage, lacking mechanisms to resolve contradictions, enforce privacy, or prevent outdated information ("zombie memories") from contaminating the context window.   We introduce MemArchitect, a governance layer that decouples memory lifecycle management from model weights. MemArchitect enforces explicit, rule-based policies, including memory decay, conflict resolution, and privacy controls.   We demonstrate that governed memory consistently outperforms unmanaged memory in agentic settings, highlighting the necessity of structured memory governance for reliable and safe autonomous systems.

---

## 论文详细总结（自动生成）

以下是对论文《MemArchitect: A Policy Driven Memory Governance Layer》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：随着大语言模型（LLM）从简单的聊天机器人演变为持久运行的智能体，它们面临严重的“内存幻觉”和“治理空白”。
*   **研究动机**：现有的检索增强生成（RAG）框架将内存视为被动的、只增不减的存储桶，缺乏处理信息冲突、执行隐私保护（如“被遗忘权”）或清理过时事实（“僵尸记忆”）的机制。这导致上下文窗口被无关或矛盾的信息污染，降低了智能体在长程任务中的可靠性。

### 2. 方法论
MemArchitect 提出了一种**主动治理中间件层**，将内存生命周期管理与模型权重解耦。其核心思想是建立一个“分拣与竞价”（Triage & Bid）经济体系，让信息竞争进入上下文窗口。
*   **四大治理支柱**：
    1.  **生命周期与卫生（Lifecycle & Hygiene）**：引入 **FSRS v4**（自由间隔重复调度器）模拟生物遗忘曲线；通过**熵触发整合**技术，在系统空闲时压缩冗余信息并删除低可检索性的噪声。
    2.  **一致性与真实性（Consistency & Truth）**：利用**卡尔曼滤波器（Kalman Filter）**追踪每条记忆的“信任分数”；使用**交叉编码器（Cross-Encoder）**作为“否决门”，过滤语义不相关的噪声。
    3.  **自适应检索（Adaptive Retrieval）**：将复杂查询分解为子问题；根据任务类型（事实、时间、推理）动态调整检索权重；实施**赫布图扩展（Hebbian Graph Expansion）**，实现关联记忆的自动提取。
    4.  **效率（Efficiency）**：根据检索置信度动态分配 Token 预算，平衡“推理预留区”和“召回预留区”。
*   **执行路径**：分为**读取路径**（检索与竞价）、**反射路径**（根据生成结果更新信任度）和**后台路径**（清理与整合）。

### 3. 实验设计
*   **数据集/场景**：使用 **LoCoMo-10** 基准测试，这是一个包含 1,986 个 QA 对的长程对话数据集，涵盖单跳、多跳、时间推理和开放域任务。
*   **对比方法（Benchmark）**：
    *   **MemOS**：代表系统级治理的“极大主义”方法，将内存视为操作系统资源。
    *   **SimpleMem**：代表算法效率方法，侧重于语义压缩。
*   **实验模型**：Qwen-2.5-3B-Instruct 和 Llama-3.1-8B-Instruct。

### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   作者强调 MemArchitect 是一个**模型无关（Model-agnostic）且轻量级**的外部层，不需要修改模型参数或进行大规模重训练。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同规模的主流模型（3B 和 8B）上进行了测试，并针对 LoCoMo-10 的四个核心维度进行了详细对比。
*   **充分性与客观性**：实验设计较为客观，通过与“压缩派”（SimpleMem）和“全量保留派”（MemOS）的对比，清晰地展示了治理策略的权衡（Trade-off）。虽然实验涵盖了主要的性能指标，但关于“冲突解决”和“隐私保护”等部分规划中的策略尚未提供详尽的定量消融实验。

### 6. 主要结论与发现
*   **整体提升**：MemArchitect 在所有任务上的平均准确率提升了 **7.45%**。
*   **治理优于压缩**：相比 SimpleMem，MemArchitect 在时间推理上提升了 **39.2%**，证明主动治理比盲目语义压缩更能保持上下文的忠实度。
*   **治理的代价**：相比 MemOS，MemArchitect 在原始召回任务（如单跳和时间戳检索）中表现较弱（下降约 44.9%），这是因为其激进的遗忘策略会修剪掉未重复出现的“一次性”事实。
*   **核心发现**：治理将系统的失败模式从“不可控的幻觉”转变为“可调优的过度修剪”，后者在工程上更具可控性。

### 7. 优点
*   **架构解耦**：作为中间件层，无需微调模型即可提升现有智能体的内存管理能力。
*   **生物启发**：引入 FSRS 遗忘曲线，使智能体的记忆模式更接近人类，提高了长期运行的效率。
*   **主动性**：改变了 RAG 被动检索的现状，引入了反馈循环和后台维护机制。

### 8. 不足与局限
*   **过度修剪风险**：目前的衰减策略可能导致智能体丢失一些虽然出现频率低但至关重要的特定细节（如具体的日期或时间戳）。
*   **策略完整性**：论文中提到的部分高级策略（如基于 NLI 的冲突解决、GDPR 合规删除）仍处于“计划中”或初步实现阶段。
*   **评估局限**：目前仅在 LoCoMo 数据集上验证，尚未在更复杂的个性化（Persona）或超长程（LongMemEval）基准上进行全面压力测试。

（完）
