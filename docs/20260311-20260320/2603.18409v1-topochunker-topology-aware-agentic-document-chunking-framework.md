---
title: "TopoChunker: Topology-Aware Agentic Document Chunking Framework"
title_zh: TopoChunker：拓扑感知的智能体文档分块框架
authors: Xiaoyu Liu
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18409v1"
tags: ["query:ma-kf"]
score: 10.0
evidence: 旨在提高RAG检索质量的智能体文档分块
tldr: 针对检索增强生成（RAG）中传统线性分块导致语义碎片化的问题，本文提出TopoChunker框架。该框架通过结构化中间表示（SIR）保留文档的拓扑层级，并采用双代理架构（Inspector和Refiner）在保证结构完整性的同时优化计算成本。实验表明，该方法在生成准确率和检索召回率上均显著优于现有LLM基线，且大幅降低了Token开销，为结构感知型RAG提供了高效方案。
selection_source: fresh_fetch
motivation: 传统的线性分块方法破坏了文档内在的拓扑结构，导致语义碎片化并降低了检索质量。
method: 提出一种基于双代理架构的框架，利用结构化中间表示（SIR）显式保留跨段落依赖关系，并通过动态路由和容量审计优化提取路径。
result: "在GutenQA和GovReport数据集上，生成准确率提升了8.0%，Recall@3达到83.26%，同时减少了23.5%的Token开销。"
conclusion: TopoChunker为结构感知型RAG提供了一种高效且可扩展的文档分块方案，实现了性能与成本的平衡。
---

## 摘要
现有的检索增强生成（RAG）文档分块方法通常将文本线性化。这种强制线性化剥离了内在的拓扑层级结构，导致“语义碎片化”，从而降低了下游检索的质量。在本文中，我们提出了 TopoChunker，这是一个智能体框架，它将异构文档映射到结构化中间表示（SIR）上，以显式保留跨段依赖关系。为了平衡结构保真度与计算成本，TopoChunker 采用了双智能体架构。检查员智能体（Inspector Agent）通过成本优化的提取路径动态路由文档，而精炼者智能体（Refiner Agent）则执行容量审计和拓扑上下文消歧，以重建层级谱系。在非结构化叙事（GutenQA）和复杂报告（GovReport）上的评估表明，TopoChunker 展示了最先进的性能。它在绝对生成准确率上比最强的基于大语言模型（LLM）的基准高出 8.0%，并实现了 83.26% 的 Recall@3，同时减少了 23.5% 的 Token 开销，为结构感知型 RAG 提供了一种可扩展的方法。

## Abstract
Current document chunking methods for Retrieval-Augmented Generation (RAG) typically linearize text. This forced linearization strips away intrinsic topological hierarchies, creating ``semantic fragmentation'' that degrades downstream retrieval quality. In this paper, we propose TopoChunker, an agentic framework that maps heterogeneous documents onto a Structured Intermediate Representation (SIR) to explicitly preserve cross-segment dependencies. To balance structural fidelity with computational cost, TopoChunker employs a dual-agent architecture. An Inspector Agent dynamically routes documents through cost-optimized extraction paths, while a Refiner Agent performs capacity auditing and topological context disambiguation to reconstruct hierarchical lineage. Evaluated on unstructured narratives (GutenQA) and complex reports (GovReport), TopoChunker demonstrates state-of-the-art performance. It outperforms the strongest LLM-based baseline by 8.0% in absolute generation accuracy and achieves an 83.26% Recall@3, while simultaneously reducing token overhead by 23.5%, offering a scalable approach for structure-aware RAG.

---

## 论文详细总结（自动生成）

以下是对论文《TopoChunker: Topology-Aware Agentic Document Chunking Framework》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：检索增强生成（RAG）的效果高度依赖于文档分块（Chunking）。传统的线性分块方法（将文档视为一维文本流）会破坏文档内在的**拓扑层级结构**（如章节、嵌套表格、标题关联），导致“语义碎片化”和“语义孤岛”现象。
*   **核心挑战**：现有的 LLM 分块器（如 LumberChunker）虽然提升了语义边界识别，但计算成本高昂，且依然无法显式保留跨段落的层级依赖关系。
*   **整体含义**：本文提出 TopoChunker 框架，旨在通过**结构化中间表示（SIR）**和**双智能体架构**，在保留文档复杂拓扑结构的同时，实现高性能检索与低计算成本的平衡。

### 2. 方法论
TopoChunker 采用“感知-执行-审计”的三层智能体架构：
*   **核心思想**：不直接线性化文档，而是将其映射为内存中的树状拓扑模型（SIR），并根据文档复杂度动态选择处理路径。
*   **关键技术细节**：
    *   **检查员智能体（Inspector Agent）**：通过“主动探测”机制（调用轻量级 API 采样元数据和布局），生成“拓扑指纹”，将文档路由至三条路径之一：
        1.  *结构规则路径*：适用于层级清晰的数字文档（低成本）。
        2.  *语义流路径*：适用于非结构化叙事文本（侧重语义连续性）。
        3.  *视觉拓扑路径*：调用 VLM（如 MinerU）处理扫描件或复杂布局（高保真）。
    *   **结构化中间表示（SIR）**：构建内存中的对象树，包含**拓扑谱系**（记录绝对路径如 /Chap1/Sec3）、**原子性锁定**（防止表格等不可分割单元被切分）和 **Token 审计**。
    *   **精炼者智能体（Refiner Agent）**：
        *   *容量审计*：对超过阈值（500 tokens）的节点进行语义切分。
        *   *语义签名*：为每个块生成 3-8 字的主题标题，增强检索辨识度。
        *   *上下文消歧*：通过查询 SIR 树，自动补全块内的代词（如“它”、“该公司”）或缩写，消除语义孤岛。

### 3. 实验设计
*   **数据集**：
    *   **GutenQA**：包含 100 本公有领域叙事书籍，3000 个问答对（代表非结构化文本）。
    *   **GovReport**：复杂的政府报告，包含深层嵌套结构（代表高度结构化文本）。
*   **Benchmark 与对比方法**：
    *   **对比方法**：固定大小分块（FC200）、语义分块器（Semantic Chunker）、命题级分块（Proposition-Level）、以及最强的 LLM 基准 LumberChunker。
    *   **评估指标**：检索质量（Recall@k, DCG@k）、端到端生成准确率（GPT-4 作为裁判 + 人工审计）。
    *   **底层模型**：嵌入模型使用 BGE-m3，LLM 后端统一使用 DeepSeek-R1。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **推理成本**：论文重点分析了推理阶段的 Token 开销。TopoChunker 通过动态路由，在混合复杂度语料库上比 LumberChunker 减少了 **23.5%** 的 Token 消耗。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同性质的大规模数据集上进行了详尽测试，涵盖了检索性能和生成准确率。
*   **消融实验**：分别验证了 Refiner Agent（提升语义质量）和 Inspector Agent（提升路由效率）的贡献。
*   **客观性**：引入了人工审计环节（随机抽取 100 组样本），人工与 GPT-4 裁判的一致性达到 92%，证明了自动化评估的可靠性。实验对比了多种主流分块范式，具有较好的公平性。

### 6. 主要结论与发现
*   **性能领先**：TopoChunker 在生成准确率上比最强基准高出 **8.0%**，在 GovReport 上的 Recall@3 达到 **83.26%**。
*   **成本优化**：通过动态路由避免了对简单文档进行昂贵的 VLM 或全量 LLM 处理，实现了 1.26x 的平均 Token 开销（显著低于 LumberChunker 的 1.65x）。
*   **拓扑重要性**：显式注入拓扑元数据（如父级标题、实体定义）能显著减少 RAG 系统中的幻觉，使生成的回答更具结构化支撑。

### 7. 优点
*   **拓扑感知**：解决了传统方法将文档“拍扁”导致的上下文丢失问题。
*   **成本自适应**：Inspector Agent 的路由机制非常实用，能够根据文档难度自动匹配算力，适合大规模工业应用。
*   **上下文补全**：Refiner Agent 的消歧功能解决了 RAG 中常见的代词指代不明问题，极大提升了检索块的自包含性。

### 8. 不足与局限
*   **上游依赖**：视觉路径高度依赖外部解析工具（如 MinerU），若 OCR 或布局识别出错，错误会向下游传播。
*   **推理延迟**：由于采用了多步智能体（Agentic）流程和多次 API 调用，其推理延迟显著高于传统的确定性启发式分块器，不适用于对实时性要求极高的场景。
*   **模型依赖**：性能高度依赖于底层推理模型（如 DeepSeek-R1）的指令遵循能力。

（完）
