---
title: "UT-ACA: Uncertainty-Triggered Adaptive Context Allocation for Long-Context Inference"
title_zh: UT-ACA：面向长上下文推理的不确定性触发自适应上下文分配
authors: "Lang Zhou, Shuxuan Li, Zhuohao Li, Shi Liu, Zhilin Zhao, Wei-Shi Zheng"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18446v1"
tags: ["query:ma-kf"]
score: 9.0
evidence: 长上下文推理的自适应上下文窗口分配
tldr: 针对大语言模型长文本推理中注意力稀释和固定上下文预算的局限性，本文提出UT-ACA框架。该框架通过学习不确定性检测器，结合语义嵌入和概率置信度，在推理时动态调整上下文窗口。当检测到证据不足时，系统会自动回退并扩大上下文重新生成，从而在保证生成质量的同时显著降低平均上下文开销。
selection_source: fresh_fetch
motivation: 现有的长文本推理方法通常分配固定的上下文预算，无法适应不同生成步骤中非均匀的上下文需求。
method: 提出一种基于不确定性触发的自适应分配框架，通过检测Token级不确定性来动态触发上下文回退与扩展。
result: 实验表明，UT-ACA在保持长文本生成质量的前提下，大幅减少了推理过程中的平均上下文使用量。
conclusion: 动态调整上下文窗口是优化长文本推理效率的有效途径，能平衡计算资源消耗与模型输出质量。
---

## 摘要
由于注意力稀释和分布外退化，长上下文推理对于大语言模型而言仍具挑战性。上下文选择通过关注键值缓存（KV cache）条目的子集来缓解这一限制，但尽管 token 级别的上下文需求高度不均匀，大多数方法在整个解码过程中仍分配固定的上下文预算。为了解决这一问题，我们提出了不确定性触发的自适应上下文分配（UT-ACA），这是一个在推理阶段根据逐 token 不确定性动态调整上下文窗口的框架。UT-ACA 学习了一个不确定性检测器，该检测器结合了语义嵌入与基于 logit 的置信度，同时考虑了跨解码步骤的不确定性累积。当显示证据不足时，UT-ACA 会选择性地回退，扩大上下文窗口，并在额外支持下重新生成 token。实验表明，在长上下文设置下，UT-ACA 在保持生成质量的同时，大幅减少了平均上下文使用量。

## Abstract
Long-context inference remains challenging for large language models due to attention dilution and out-of-distribution degradation. Context selection mitigates this limitation by attending to a subset of key-value cache entries, yet most methods allocate a fixed context budget throughout decoding despite highly non-uniform token-level contextual demands. To address this issue, we propose Uncertainty-Triggered Adaptive Context Allocation (UT-ACA), an inference-time framework that dynamically adjusts the context window based on token-wise uncertainty. UT-ACA learns an uncertainty detector that combines semantic embeddings with logit-based confidence while accounting for uncertainty accumulation across decoding steps. When insufficient evidence is indicated, UT-ACA selectively rolls back, expands the context window, and regenerates the token with additional support. Experiments show that UT-ACA substantially reduces average context usage while preserving generation quality in long-context settings.

---

## 论文详细总结（自动生成）

### UT-ACA：面向长上下文推理的不确定性触发自适应上下文分配 论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
大语言模型（LLM）在处理长文本时面临两大挑战：**注意力稀释**（冗余信息干扰关键证据检索）和**分布外（OOD）退化**（长文本模式偏离预训练分布）。现有的上下文管理方法（如 KV Cache 压缩或选择）通常在整个解码过程中分配**固定的上下文预算**。然而，不同 Token 的生成难度并不均匀：有些 Token 仅依赖局部上下文，而有些则需要检索长程证据。固定窗口在简单步骤中浪费计算资源，在困难步骤中则因证据不足导致幻觉。

#### 2. 论文提出的方法论
论文提出了 **UT-ACA** 框架，其核心思想是**根据 Token 级的不确定性动态调整上下文窗口**。
*   **不确定性检测器（Uncertainty Detector）**：
    *   **输入信号**：结合了轻量级的 **Logit Margin**（最高与次高概率之差）和从模型隐藏层提取的**语义嵌入（Semantic Embedding）**。
    *   **时序建模**：使用 **LSTM** 捕捉解码过程中的不确定性累积效应。
    *   **三分类输出**：将生成状态分为“正确（Correct）”、“未知（Unknown）”和“幻觉（Hallucination）”。
*   **自适应分配流程**：
    1.  **尝试生成**：首先使用较小的上下文窗口生成一个候选 Token。
    2.  **不确定性检查**：检测器评估该 Token 的生成难度。
    3.  **回退与重生成（Rollback & Regeneration）**：若检测到高不确定性（证据不足），则撤销该 Token 及其 KV Cache，扩大上下文窗口（检索更多相关块），并重新生成。
    4.  **窗口收缩**：若生成置信度高，则维持或缩小窗口以节省资源。

#### 3. 实验设计
*   **数据集**：
    *   **合成传记数据集**：用于训练不确定性检测器，包含 10,000 条训练样本，涵盖多种属性提取任务。
    *   **长文本测试集**：长度在 171k 到 400k Token 之间，平均 252k。
    *   **公开基准测试**：$\infty$-Bench、LongBench 和 RULER。
*   **对比方法**：包括 InfLLM（主要基线）、RetrievalAttention、SnapKV、OmniKV、LLMLingua-2、TokenSelect 和 H2O 等。
*   **评估指标**：除了常规的 F1 和准确率，还提出了**概念准确率评分（Conceptual Accuracy Score, CAS）**，利用强模型（如 GPT-OSS-120B）评估语义一致性。

#### 4. 资源与算力
*   **硬件**：实验在单台 **NVIDIA H100 GPU** 上运行。
*   **训练时长**：不确定性检测器的训练大约需要 1.5 小时（共进行 8 次独立运行以选取最佳模型）。
*   **推理开销**：检测器本身非常轻量（LSTM 前向传播仅需约 20ms），主要的额外开销来自于触发回退时的重新生成步骤。

#### 5. 实验数量与充分性
*   **实验规模**：论文在合成数据集、三个主流长文本 Benchmark 上进行了广泛测试，并针对 Llama-3.1-8B 和 Qwen2-7B 两个主流模型分别训练了检测器。
*   **消融实验**：详细分析了 Logit Margin、语义嵌入和 LSTM 模块各自的贡献，验证了三分类指标优于二分类指标。
*   **充分性评价**：实验设计较为全面，不仅对比了生成质量，还详细分析了 Token 使用量、解码延迟和不同回退策略的影响，结果具有较强的说服力。

#### 6. 主要结论与发现
*   **效率与质量平衡**：UT-ACA 在显著减少平均上下文 Token 使用量的同时，保持甚至提升了长文本生成的准确率。
*   **动态性必要性**：实验证明 Token 级别的上下文需求确实存在高度非均匀性，动态分配比固定分配更具鲁棒性。
*   **不确定性作为信号**：不确定性不仅是诊断工具，更可作为推理时决策的主动信号，有效缓解长文本下的幻觉问题。

#### 7. 优点
*   **推理时自适应**：无需对原始 LLM 进行微调，通过轻量级插件即可实现动态上下文控制。
*   **回退机制**：引入回退重生成机制，允许模型在发现“走错路”时利用更多证据进行自我修正。
*   **语义感知**：检测器结合了隐藏层语义信息，比单纯依赖 Logit 的方法更能识别深层的不确定性。

#### 8. 不足与局限
*   **延迟开销**：虽然减少了 Token 计算量，但频繁的回退和重生成会增加单 Token 的平均解码延迟（Latency）。
*   **检测器依赖**：框架的效果高度依赖于不确定性检测器的准确性，若检测器失效（漏检或误检），可能导致生成质量下降或无效的计算浪费。
*   **任务局限**：目前主要在信息提取和摘要类任务上验证，对于逻辑极其严密的超长程推理任务（如复杂代码生成），其回退策略的有效性仍待进一步验证。

（完）
