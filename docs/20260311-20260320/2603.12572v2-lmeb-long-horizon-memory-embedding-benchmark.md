---
title: "LMEB: Long-horizon Memory Embedding Benchmark"
title_zh: LMEB：长跨度记忆嵌入基准
authors: "Xinping Zhao, Xinshuo Hu, Jiaxin Xu, Danyu Tang, Xin Zhang, Mengjia Zhou, Yan Zhong, Yao Zhou, Zifei Shan, Meishan Zhang, Baotian Hu, Min Zhang"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12572v2"
tags: ["query:agent"]
score: 9.0
evidence: 针对记忆增强系统中长程记忆检索的基准测试
tldr: 本研究针对现有文本嵌入基准在长程记忆检索评估方面的不足，提出了LMEB基准。LMEB涵盖22个数据集和193个零样本任务，分为情景、对话、语义和程序四类记忆类型。通过对15个主流模型的评估，发现现有模型在处理碎片化、上下文相关及跨时间信息的长程记忆任务上仍有提升空间，且该基准与传统检索基准具有正交性，为长程记忆增强系统提供了重要的评估框架。
selection_source: fresh_fetch
motivation: 现有文本嵌入基准局限于传统段落检索，难以评估模型在处理碎片化、上下文相关及长时依赖的长程记忆任务中的表现。
method: 构建了包含22个数据集和193个零样本任务的LMEB基准，涵盖四种记忆类型，并对15个不同规模的主流嵌入模型进行了系统评估。
result: 实验发现LMEB具有较高挑战性，模型性能并不随参数量增加而线性提升，且其评估维度与传统MTEB基准具有正交性。
conclusion: LMEB填补了长程记忆嵌入评估的空白，揭示了传统检索能力无法直接推广到长程记忆检索，为未来模型优化提供了新方向。
---

## 摘要
记忆嵌入对于 OpenClaw 等记忆增强系统至关重要，但在当前的文本嵌入基准测试中，对其评估的研究尚不充分。目前的基准测试主要局限于传统的段落检索，未能评估模型处理涉及碎片化、上下文相关且时间跨度较大的信息的长跨度记忆检索任务的能力。为此，我们推出了长跨度记忆嵌入基准（LMEB），这是一个评估嵌入模型处理复杂长跨度记忆检索任务能力的综合框架。LMEB 涵盖了 22 个数据集和 193 个零样本检索任务，涉及四种记忆类型：情景记忆、对话记忆、语义记忆和程序记忆，包含 AI 生成和人工标注的数据。这些记忆类型在抽象程度和时间依赖性方面有所不同，捕捉了记忆检索的不同方面，反映了现实世界中的多样化挑战。我们评估了 15 个广泛使用的嵌入模型，参数量从数亿到百亿不等。结果显示：(1) LMEB 提供了合理的难度水平；(2) 更大的模型并不总是表现更好；(3) LMEB 与 MTEB 表现出正交性。这表明该领域尚未收敛出一个能够在所有记忆检索任务中表现出色的通用模型，且传统段落检索的性能可能无法泛化到长跨度记忆检索。总之，通过提供标准化且可复现的评估框架，LMEB 填补了记忆嵌入评估中的关键空白，推动了文本嵌入在处理长期、上下文相关的记忆检索方面的进一步发展。LMEB 已在 https://github.com/KaLM-Embedding/LMEB 开源。

## Abstract
Memory embeddings are crucial for memory-augmented systems, such as OpenClaw, but their evaluation is underexplored in current text embedding benchmarks, which narrowly focus on traditional passage retrieval and fail to assess models' ability to handle long-horizon memory retrieval tasks involving fragmented, context-dependent, and temporally distant information. To address this, we introduce the Long-horizon Memory Embedding Benchmark (LMEB), a comprehensive framework that evaluates embedding models' capabilities in handling complex, long-horizon memory retrieval tasks. LMEB spans 22 datasets and 193 zero-shot retrieval tasks across 4 memory types: episodic, dialogue, semantic, and procedural, with both AI-generated and human-annotated data. These memory types differ in terms of level of abstraction and temporal dependency, capturing distinct aspects of memory retrieval that reflect the diverse challenges of the real world. We evaluate 15 widely used embedding models, ranging from hundreds of millions to ten billion parameters. The results reveal that (1) LMEB provides a reasonable level of difficulty; (2) Larger models do not always perform better; (3) LMEB and MTEB exhibit orthogonality. This suggests that the field has yet to converge on a universal model capable of excelling across all memory retrieval tasks, and that performance in traditional passage retrieval may not generalize to long-horizon memory retrieval. In summary, by providing a standardized and reproducible evaluation framework, LMEB fills a crucial gap in memory embedding evaluation, driving further advancements in text embedding for handling long-term, context-dependent memory retrieval. LMEB is available at https://github.com/KaLM-Embedding/LMEB.

---

## 论文详细总结（自动生成）

# LMEB: 长跨度记忆嵌入基准（Long-horizon Memory Embedding Benchmark）深度总结

### 1. 核心问题与整体含义（研究动机和背景）
论文指出，当前的文本嵌入（Text Embedding）基准测试（如 MTEB）主要集中在传统的段落检索任务上，这些任务通常处理组织良好的静态信息。然而，在实际的记忆增强系统（如 AI Agent）中，模型需要处理**长跨度记忆（Long-horizon Memory）**。这类记忆具有碎片化、高度依赖上下文、时间跨度大且具有时间依赖性等特点。现有的基准测试无法有效评估嵌入模型在这些复杂场景下的检索能力，导致开发者难以选择最适合构建长期记忆系统的模型。

### 2. 论文提出的方法论
论文提出了 **LMEB** 基准框架，其核心思想是将记忆检索根据**抽象程度（Level of Abstraction）**和**时间依赖性（Temporal Dependency）**两个维度划分为四种类型：
*   **情景记忆（Episodic Memory）：** 低抽象、高时间依赖，关注特定事件及其发生顺序。
*   **对话记忆（Dialogue Memory）：** 高抽象、高时间依赖，涉及多轮交互中的上下文和用户偏好。
*   **语义记忆（Semantic Memory）：** 低抽象、低时间依赖，涉及稳定的通用知识和事实。
*   **程序记忆（Procedural Memory）：** 高抽象、低时间依赖，关注技能、动作序列和经验。

**技术细节与流程：**
*   **统一格式：** 将 22 个来源各异的数据集转换为标准的 IR 风格格式，包含 `queries`（查询）、`corpus`（语料库）、`qrels`（相关性标注）和可选的 `candidates`（受限候选池，用于模拟真实的对话历史约束）。
*   **时间锚点：** 针对含有相对时间描述的查询（如“两天前”），在查询文本中追加显式的时间戳锚点以消除歧义。
*   **评估协议：** 基于 MTEB v2 框架构建，支持零样本（Zero-shot）评估，主要指标为 NDCG@10 和 Capped Recall@k。

### 3. 实验设计
*   **数据集：** 涵盖 22 个数据集，共计 193 个检索任务。数据来源包括 AI 生成（如 EPBench）和人工标注（如 QASPER, SciFact）。
*   **对比方法（模型）：** 评估了 15 个主流开源嵌入模型，参数规模跨度从 239M 到 12B，包括：
    *   **大参数模型（>1B）：** KaLM-Embedding-Gemma3 (12B), bge-multilingual-gemma2 (9B), Qwen3-Embedding-8B, NV-Embed-v2 (7B) 等。
    *   **小参数模型（<1B）：** jina-v5-text-small, bge-m3, EmbeddingGemma-300M 等。
*   **场景设置：** 分为“无指令（w/o inst.）”和“带指令（w/ inst.）”两种模式，探究任务描述对模型性能的影响。

### 4. 资源与算力
论文**未明确说明**评估过程的具体算力消耗（如 GPU 型号、数量或总时长）。由于该论文侧重于基准测试的构建和对现有公开模型的评估，而非从头训练模型，其算力需求主要集中在推理阶段。文中提到开发了开源评估工具包，支持 Transformers、Sentence-Transformers 和 vLLM 等后端，旨在提高评估的复现性和效率。

### 5. 实验数量与充分性
*   **实验规模：** 论文在 22 个数据集上对 15 个模型进行了详尽测试，涵盖了 4 种记忆类型，并计算了 Pearson 和 Spearman 相关系数来分析 LMEB 与 MTEB 的关系。
*   **充分性：** 实验设计非常充分。它不仅对比了模型规模，还分析了指令遵循能力、数据集多样性（通过加权 Jaccard 相似度分析）以及不同记忆类型的性能差异。
*   **客观性：** 采用零样本评估确保了公平性，避免了任务特定微调带来的偏差；同时引入了多样化的数据来源（AI 与人工混合），增强了结果的客观性。

### 6. 论文的主要结论与发现
*   **难度合理：** 顶尖模型的平均得分在 61.41 (NDCG@10)，说明基准既有挑战性又非不可逾越。
*   **规模悖论：** **更大的模型并不总是表现更好**。例如，300M 的 EmbeddingGemma 在某些任务上优于 12B 的模型，说明架构和训练数据质量比参数量更关键。
*   **正交性：** LMEB 与 MTEB 的相关性接近 0。这意味着在传统段落检索（MTEB）中表现优异的模型，在长程记忆检索中可能表现平平。
*   **指令敏感性：** 不同模型对指令的偏好不同，NV-Embed-v2 等模型对指令不敏感，而 BGE 系列在带指令时提升明显。

### 7. 优点
*   **填补空白：** 首次系统性地定义并评估了“长程记忆嵌入”，为 Agent 系统的记忆模块提供了科学的选型依据。
*   **分类严谨：** 提出的四象限记忆分类法（抽象度 vs 时间依赖）具有很强的理论指导意义。
*   **易于扩展：** 兼容 MTEB 框架，提供统一的数据格式和评估工具，方便社区贡献新模型和新数据集。
*   **真实性强：** 引入了 `candidates` 约束，模拟了真实应用中只能在特定对话历史中检索的限制。

### 8. 不足与局限
*   **语言局限：** 目前主要集中在**英文**数据集，缺乏对多语言（如中文）长程记忆能力的深入评估。
*   **合成数据偏差：** 部分数据集（如 EPBench）完全由 AI 生成，可能无法完全模拟人类真实记忆中的复杂噪声和非逻辑性。
*   **动态更新不足：** 记忆系统通常涉及动态更新（增删改），而 LMEB 目前仍侧重于静态语料库的检索评估，未充分涉及记忆更新后的检索一致性测试。

（完）
