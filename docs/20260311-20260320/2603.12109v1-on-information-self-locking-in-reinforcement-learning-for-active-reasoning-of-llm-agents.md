---
title: On Information Self-Locking in Reinforcement Learning for Active Reasoning of LLM agents
title_zh: 论大语言模型智能体主动推理强化学习中的信息自锁现象
authors: "Deyu Zou, Yongqiang Chen, Fan Feng, Mufei Li, Pan Li, Yu Gong, James Cheng"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12109v1"
tags: ["query:agent"]
score: 9.0
evidence: 用于智能体主动推理和信息获取的强化学习
tldr: "本研究揭示了强化学习在训练LLM进行主动推理时存在的“信息自锁”现象，即模型因动作选择与信念追踪能力的相互限制而陷入低信息获取的负反馈循环。为解决此问题，作者提出通过注入定向评论来重新分配学习信号，引导模型跳出陷阱。实验表明，该方法在7个数据集上显著提升了模型获取和处理信息的能力，性能最高提升达60%。"
selection_source: fresh_fetch
motivation: 旨在解决LLM在主动推理任务中因强化学习导致的停止探索和无法内化信息的“信息自锁”难题。
method: 提出一种注入定向评论的方法来重新分配学习信号，帮助模型在动作选择和信念追踪上实现协同进化。
result: "在7个不同领域的实验中，该方法成功缓解了自锁现象，使模型推理性能提升了高达60%。"
conclusion: 通过定向评论打破动作与信念间的负反馈循环，是提升LLM主动推理和信息处理能力的有效途径。
---

## 摘要
基于结果奖励的强化学习（RL）在训练大语言模型（LLM）智能体处理复杂推理任务方面取得了显著成功。然而，在智能体需要策略性地提问以获取任务相关信息的主动推理中，我们发现通过强化学习训练的 LLM 智能体经常遭受“信息自锁”：智能体停止提出具有信息量的疑问，并难以内化已获得的信息。为了理解这一现象，我们将主动推理分解为两项核心能力：动作选择（AS），通过查询决定观测流；以及信念追踪（BT），根据收集到的证据更新智能体的信念。我们表明，不足的 AS 和 BT 能力会限制强化学习训练期间的信息探索。此外，探索不足反过来又阻碍了 AS 和 BT 的提升，从而形成一个反馈循环，将智能体锁定在低信息状态。为解决这一问题，我们提出了一种简单而有效的方法，通过注入易于获取的方向性评论来重新分配学习信号，帮助智能体摆脱自锁。在 7 个数据集上进行的广泛实验表明，我们的方法显著缓解了信息自锁，带来了高达 60% 的提升。

## Abstract
Reinforcement learning (RL) with outcome-based rewards has achieved significant success in training large language model (LLM) agents for complex reasoning tasks. However, in active reasoning where agents need to strategically ask questions to acquire task-relevant information, we find that LLM agents trained with RL often suffer from information self-locking: the agent ceases to ask informative questions and struggles to internalize already-obtained information. To understand the phenomenon, we decompose active reasoning into two core capabilities: Action Selection (AS), which determines the observation stream through queries, and Belief Tracking (BT), which updates the agent's belief based on collected evidence. We show that deficient AS and BT capabilities will limit the information exploration during RL training. Furthermore, insufficient exploration in turn hinders the improvement of AS and BT, creating a feedback loop that locks the agent in a low-information regime. To resolve the issue, we propose a simple yet effective approach that reallocates the learning signal by injecting easy- to-obtain directional critiques to help the agent escape self-locking. Extensive experiments with 7 datasets show that our approach significantly mitigates the information self-locking, bringing up to 60% improvements.

---

## 论文详细总结（自动生成）

这篇论文深入探讨了大语言模型（LLM）智能体在主动推理（Active Reasoning）任务中，使用强化学习（RL）训练时出现的一种关键失效模式——“信息自锁”。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在需要通过多轮交互获取信息的任务中，仅基于结果奖励（Outcome-based Reward）的强化学习会导致智能体陷入**信息自锁（Information Self-Locking, SeL）**。
*   **现象描述**：智能体在训练过程中会逐渐停止提出有意义的问题（动作选择 AS 失效），并且无法有效内化已获得的证据（信念追踪 BT 失效）。
*   **研究动机**：现有的 RL 训练虽然在数学等静态推理上成功，但在动态交互中，AS 和 BT 之间存在负反馈循环：弱 BT 掩盖了高质量提问的奖励信号，而低质量提问（AS）又让 BT 缺乏学习所需的有效数据。

### 2. 方法论：AReW 框架
*   **核心思想**：引入**优势重加权（Advantage Reweighting）**机制，通过注入易于获取的“定向评论”（Directional Critiques）来打破 AS 和 BT 的负耦合。
*   **关键技术细节**：
    *   **动作选择评论 ($z^Q_t$)**：判断查询是否具有信息量（例如：用户是否提供了新证据）。
    *   **信念追踪评论 ($z^U_t$)**：判断智能体在接收信息后，其内部信念（对正确答案的置信度）是否向正确方向移动。
    *   **优势重加权公式**：在策略梯度算法（如 PPO）中，将原始优势值 $A_t$ 修改为 $\hat{A}_t \leftarrow A_t + \lambda u_t$。其中 $u_t$ 是根据评论方向（+1, 0, -1）计算的权重，用于在轨迹内重新分配信用。
    *   **辅助目标**：构建一个边际感知（Margin-aware）的似然目标函数，鼓励模型增加正向评论步骤的概率，减少负向评论步骤的概率。

### 3. 实验设计
*   **数据集/场景**：涵盖 3 个领域共 7 个任务：
    *   **偏好估计 (PE-G, PE-F)**：通过对比电影属性推断用户隐藏偏好。
    *   **医疗诊断 (MediQ)**：通过询问症状识别最佳假设。
    *   **故障排除 (FloDial-Easy/Hard)**：通过对话诊断设备故障。
*   **对比方法**：
    *   **直接推理**：o4-mini, Qwen-2.5-7B, Llama-3.1-8B。
    *   **标准 RL 基准**：Vanilla PPO, GRPO, GSPO。
*   **消融变体**：AReW (AS-ONLY) 和 AReW (AS+BT)。

### 4. 资源与算力
*   **算力设备**：使用了 **8 张 NVIDIA B200 GPU** 的单节点集群。
*   **训练框架**：基于 Verl（分布式 RLHF 框架），采用 FSDP（完全分片数据并行）和 BFloat16 精度。
*   **训练细节**：PPO 训练 200 步，GRPO/GSPO 训练 100 步；推理使用 vLLM 引擎。

### 5. 实验数量与充分性
*   **实验规模**：在 7 个数据集上进行了测试，涵盖了 Qwen 和 Llama 两个主流模型家族。
*   **充分性**：
    *   不仅对比了最终性能，还通过 AS/BT 代理指标分析了训练动力学。
    *   进行了**鲁棒性分析**：测试了当评论信号存在噪声（随机翻转标签）时算法的稳定性。
    *   验证了方法在不同 RL 算法（PPO, GRPO, GSPO）上的通用性。
*   **评价**：实验设计全面，通过跨模型、跨算法、跨领域的对比，客观地证明了方法的有效性。

### 6. 主要结论与发现
*   **打破自锁**：AReW 在所有任务中均显著优于标准 RL，性能提升最高达 60%。
*   **能力耦合**：仅提升 AS 也能带动 BT 的部分提升，但同时优化 AS 和 BT 效果最好。
*   **鲁棒性**：只要定向评论的准确率高于 0.5，AReW 就能产生正向收益，证明了该方法对低质量反馈的容忍度。
*   **行为转变**：标准 RL 训练后的智能体往往变得“固执”（无视反馈），而 AReW 恢复了智能体的信息寻求模式。

### 7. 优点与亮点
*   **理论深刻**：通过数学证明（定理 3.4）揭示了自锁现象的本质是 AS 和 BT 的双向耦合。
*   **实现简单**：不需要训练复杂的奖励模型（Reward Model），仅需简单的规则或轻量级 Prompt 即可生成评论信号。
*   **即插即用**：可以轻松集成到现有的各种策略梯度 RL 算法中。

### 8. 不足与局限
*   **超参数敏感性**：重加权强度 $\lambda$ 需要精细调优，过大会导致训练不稳定。
*   **评论获取**：虽然论文称评论“易于获取”，但在某些极端复杂的开放域任务中，定义什么是“有信息量的查询”可能仍具挑战。
*   **应用范围**：目前主要集中在结构化程度较高的主动推理任务，在更广泛的通用对话智能体上的表现尚待验证。

（完）
