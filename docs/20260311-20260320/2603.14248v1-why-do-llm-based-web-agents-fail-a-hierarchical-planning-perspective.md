---
title: Why Do LLM-based Web Agents Fail? A Hierarchical Planning Perspective
title_zh: 基于大语言模型（LLM）的网络智能体为何失败？分层规划视角
authors: "Mohamed Aghzal, Gregory J. Stein, Ziyu Yao"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14248v1"
tags: ["query:agent"]
score: 9.0
evidence: 基于LLM的网络智能体分层规划框架
tldr: 本研究针对 LLM 网络智能体在复杂长程任务中可靠性不足的问题，提出了一个分层规划框架，从高层规划、底层执行和重新规划三个维度对失败原因进行深入分析。通过对比 PDDL 和自然语言规划，研究发现虽然结构化规划能产生更优策略，但底层执行阶段的感知接地和自适应控制才是制约性能的核心瓶颈。该框架为诊断和改进网络智能体提供了系统性的方法论。
selection_source: fresh_fetch
motivation: 现有的评估方法主要关注端到端的成功率，无法深入揭示 LLM 网络智能体在处理复杂任务时具体的失败环节。
method: 提出一种分层规划框架，将智能体行为分解为高层规划、底层执行和重新规划，并对比了 PDDL 与自然语言规划的表现。
result: 实验表明 PDDL 规划在策略制定上优于自然语言，但底层执行阶段的感知与控制能力是导致任务失败的主要瓶颈。
conclusion: 要实现人类水平的可靠性，网络智能体不仅需要强大的高层推理能力，更需在感知接地和自适应控制方面取得突破。
---

## 摘要
基于大语言模型（LLM）的网络智能体越来越多地被用于网页导航，但在现实的长程任务中，其可靠性仍远低于人类。现有的评估主要关注端到端的成功率，对于失败发生的原因提供的见解有限。我们提出了一个分层规划框架，从三个层面（即高层规划、底层执行和重新规划）分析网络智能体，从而实现对推理、接地（grounding）和恢复能力的基于过程的评估。我们的实验表明，结构化的规划领域定义语言（PDDL）规划比自然语言（NL）规划能产生更简洁且目标导向的策略，但底层执行仍然是主要的瓶颈。这些结果表明，要达到人类水平的可靠性，不仅需要提高高层推理能力，改进感知接地和自适应控制也至关重要。这种分层视角为诊断和推进 LLM 网络智能体提供了原则性的基础。

## Abstract
Large language model (LLM) web agents are increasingly used for web navigation but remain far from human reliability on realistic, long-horizon tasks. Existing evaluations focus primarily on end-to-end success, offering limited insight into where failures arise. We propose a hierarchical planning framework to analyze web agents across three layers (i.e., high-level planning, low-level execution, and replanning), enabling process-based evaluation of reasoning, grounding, and recovery. Our experiments show that structured Planning Domain Definition Language (PDDL) plans produce more concise and goal-directed strategies than natural language (NL) plans, but low-level execution remains the dominant bottleneck. These results indicate that improving perceptual grounding and adaptive control, not only high-level reasoning, is critical for achieving human-level reliability. This hierarchical perspective provides a principled foundation for diagnosing and advancing LLM web agents.

---

## 论文详细总结（自动生成）

以下是对论文《Why Do LLM-based Web Agents Fail? A Hierarchical Planning Perspective》的深入结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：尽管大语言模型（LLM）在推理能力上取得了显著进步，但在处理真实、动态的网页导航任务时，其可靠性仍远低于人类。
*   **核心问题**：现有的评估方法大多依赖“端到端成功率”这一粗粒度指标，这像一个“黑盒”，无法揭示失败的具体根源——是任务理解错了（高层规划失败）？是无法将计划转化为具体的UI操作（底层执行失败）？还是在环境变化时无法自我修正（重新规划失败）？
*   **整体含义**：本研究旨在通过**分层规划（Hierarchical Planning）**的视角，将网络智能体的能力拆解为三个维度进行系统性诊断，从而为提升智能体的可靠性提供明确的改进方向。

### 2. 论文提出的方法论
论文提出了一个三层评估框架，模拟人类解决复杂任务的逻辑：
*   **高层规划（High-level Planning）**：将自然语言指令分解为一系列抽象子目标（Subgoals）。研究对比了两种表示形式：
    *   **自然语言（NL）**：灵活但容易产生冗余或模糊的步骤。
    *   **PDDL（规划领域定义语言）**：一种结构化符号语言，强制要求明确的前置条件（Preconditions）和效果（Effects）。
*   **底层执行（Low-level Execution）**：将每个子目标转化为具体的网页操作序列（如点击、滚动、输入）。
*   **后置条件检查（Postcondition Checking）**：引入“LLM作为裁判（LLM-as-judge）”，在每个子目标执行后判断其是否真正达成，作为反馈信号。
*   **重新规划（Replanning）**：当子目标执行失败时，智能体需决定是“局部调整”（从上个成功点继续）还是“全局重排”（彻底抛弃旧计划）。

### 3. 实验设计
*   **数据集与场景**：使用了 **Mind2Web-Live** 评估集，包含 104 个在真实、实时网站上运行的任务。
*   **基准（Benchmark）**：基于专家标注的“关键节点（Key-nodes）”构建了人类水平的高层计划作为金标准（Gold Standard）。
*   **对比模型**：评估了三个主流模型：**gpt-5-nano**（主要模型）、**claude-haiku-4.5** 和 **gemini-flash-2.5**。
*   **动作空间对比**：
    *   **Expanded**：包含高级动作（如 google_search, goto）。
    *   **Action Object**：直接预测完整的动作规范。
    *   **Action ID**：从页面候选动作列表中选择 ID。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或训练时长。
*   **原因**：该研究主要基于商业 LLM 的 API 进行推理和评估，而非从头训练模型。实验重点在于 Prompt Engineering（提示工程）和框架设计，而非模型训练。

### 5. 实验数量与充分性
*   **实验规模**：针对 104 个复杂任务，在 3 种模型、2 种高层表示（NL/PDDL）、3 种动作空间下进行了多轮交叉实验。
*   **充分性评价**：
    *   **客观性**：通过引入“人类金标准计划”来隔离规划和执行的错误，实验设计非常严谨。
    *   **公平性**：所有模型使用统一的 DOM 表示和评估标准（gpt-5-nano 作为统一裁判）。
    *   **局限性**：104 个样本虽然在实时网页任务中已属不小的规模，但相比离线数据集，样本量仍显略小，且仅限于文本模态。

### 6. 论文的主要结论与发现
*   **执行是核心瓶颈**：即使提供完美的人类高层计划，LLM 在底层执行上的成功率也仅为 36.4% 左右。失败主因是**感知接地（Perceptual Grounding）**不足，如幻觉链接、重复无效操作等。
*   **PDDL 优于自然语言**：结构化的 PDDL 能够产生更简洁、目标导向的计划，减少了自然语言计划中常见的冗余和过度细化。
*   **重新规划显著增效**：仅需一轮基于环境反馈的重新规划，就能大幅提升子目标的完成率和最终任务成功率。
*   **模型差异**：gpt-5-nano 表现最强；gemini-flash-2.5 在底层执行上最为挣扎，存在大量冗余动作。

### 7. 优点与亮点
*   **诊断深度**：跳出了“成功/失败”的二元论，通过分层视角精确定位了 LLM 智能体在“感知-决策-执行”链条上的薄弱环节。
*   **引入符号规划**：首次将 PDDL 这种经典 AI 规划方法引入现代 LLM 网络智能体的评估，证明了结构化约束的价值。
*   **过程化评估指标**：提出了一套细粒度的对齐指标（如 Perfect Match, Decomposed Rate 等），为后续研究提供了标准工具。

### 8. 不足与局限
*   **模态限制**：实验主要基于文本 DOM，未充分利用视觉信息（VLM），而视觉对于网页接地至关重要。
*   **标注成本**：高层计划的对齐评估仍需要高质量的人类标注或强大的模型辅助，难以完全自动化大规模扩展。
*   **实时性挑战**：在实时网站上测试存在环境不可控性（如页面更新、网络延迟），可能引入实验噪声。
*   **样本覆盖**：虽然涵盖了多个领域，但 104 个任务可能无法完全代表互联网极其复杂的长尾场景。

（完）
