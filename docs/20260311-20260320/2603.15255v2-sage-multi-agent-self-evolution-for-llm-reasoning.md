---
title: "SAGE: Multi-Agent Self-Evolution for LLM Reasoning"
title_zh: SAGE：大语言模型推理的多智能体自我演化
authors: "Yulin Peng, Xinxin Zhu, Chenxing Wei, Nianbo Zeng, Leilei Wang, Ying Tiffany He, F. Richard Yu"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15255v2"
tags: ["query:ma-kf"]
score: 9.0
evidence: 多智能体自我进化推理
tldr: SAGE是一个多智能体自我进化框架，旨在提升大语言模型的推理能力。针对现有方法依赖人工标注或缺乏规划控制的问题，SAGE通过挑战者、规划者、执行者和评论者四个角色的协同演化，实现了从少量种子数据出发的闭环自我训练。该框架通过显式规划和质量过滤，在数学和代码生成任务上显著提升了模型性能，为实现自主进化的通用推理提供了新路径。
selection_source: fresh_fetch
motivation: 现有的强化学习推理方法过度依赖人工标注，且自我博弈缺乏显式规划和质量控制，导致长程推理不稳定。
method: 提出SAGE框架，由挑战者生成任务、规划者制定步骤、执行者求解、评论者过滤，实现多智能体闭环自我进化。
result: "在Qwen-2.5-7B模型上，SAGE在LiveCodeBench和OlympiadBench上分别实现了8.9%和10.7%的性能提升。"
conclusion: SAGE通过多智能体协同和严格的质量控制，证明了闭环自我进化在提升大语言模型泛化推理能力方面的有效性。
---

## 摘要
具有可验证奖励的强化学习提升了大语言模型（LLMs）的推理能力，但许多方法仍依赖于大规模的人工标注数据集。虽然自我博弈（self-play）减少了这种依赖，但它往往缺乏显式的规划和强大的质量控制，限制了在长程多步推理中的稳定性。我们提出了 SAGE（用于广义推理演化的自我演化智能体），这是一个闭环框架，其中四个智能体：挑战者（Challenger）、规划者（Planner）、求解者（Solver）和评论者（Critic），仅使用少量的种子集，从共享的 LLM 主干网络中共同演化。挑战者不断生成难度递增的任务；规划者将每个任务转换为结构化的多步计划；求解者遵循该计划生成答案，其正确性由外部验证器确定。评论者对生成的题目和计划进行评分和过滤，以防止课程漂移（curriculum drift）并保持训练信号的质量，从而实现稳定的自我训练。在数学和代码生成基准测试中，SAGE 在不同模型规模上均实现了持续的提升，使 Qwen-2.5-7B 模型在 LiveCodeBench 上提升了 8.9%，在 OlympiadBench 上提升了 10.7%。

## Abstract
Reinforcement learning with verifiable rewards improves reasoning in large language models (LLMs), but many methods still rely on large human-labeled datasets. While self-play reduces this dependency, it often lacks explicit planning and strong quality control, limiting stability in long-horizon multi-step reasoning. We present SAGE (Self-evolving Agents for Generalized reasoning Evolution), a closed-loop framework where four agents: Challenger, Planner, Solver, and Critic, co-evolve from a shared LLM backbone using only a small seed set. The Challenger continuously generates increasingly difficult tasks; the Planner converts each task into a structured multi-step plan; and the Solver follows the plan to produce an answer, whose correctness is determined by external verifiers. The Critic scores and filters both generated questions and plans to prevent curriculum drift and maintain training signal quality, enabling stable self-training. Across mathematics and code-generation benchmarks, SAGE delivers consistent gains across model scales, improving the Qwen-2.5-7B model by 8.9% on LiveCodeBench and 10.7% on OlympiadBench.

---

## 论文详细总结（自动生成）

以下是对论文《SAGE: Multi-Agent Self-Evolution for LLM Reasoning》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：虽然带有可验证奖励的强化学习（RLVR）能显著提升大语言模型（LLM）的推理能力，但现有方法（如 DeepSeek-R1）通常依赖大规模人工标注数据。现有的“自我博弈”（Self-play）方法虽减少了数据依赖，但往往缺乏显式的规划（Planning）和严格的质量控制，导致在处理长程、多步推理任务时表现不稳定，且容易出现“课程漂移”（即生成的题目质量下降）。
*   **核心目标**：构建一个能够从极少量种子数据出发，通过多智能体协同演化，在数学和代码等可验证领域实现自主、稳定进化的推理框架。

### 2. 论文提出的方法论
SAGE 框架的核心思想是将推理过程拆解为四个由同一 LLM 主干网络扮演的专业化角色，形成闭环演化：
*   **核心组件（四个智能体）**：
    1.  **挑战者 (Challenger)**：负责生成新任务及其验证器（如测试用例或标准答案）。它通过“难度奖励”进行优化——如果求解者失败，则挑战者获得奖励，从而推动课程向更难但可解的方向演进。
    2.  **规划者 (Planner)**：将任务分解为结构化的多步执行计划，为求解提供策略指导。
    3.  **求解者 (Solver)**：根据规划者的计划执行具体推理并输出最终答案。
    4.  **评论者 (Critic)**：对生成的题目质量和计划的逻辑性进行评分和过滤，确保训练信号的纯净。
*   **关键技术细节**：
    *   **任务相关 REINFORCE++ (Task-Relative REINFORCE++)**：采用基于角色的优势归一化（Per-role advantage normalization），稳定多智能体联合训练。
    *   **复合奖励设计**：结合了格式奖励（Format reward）、质量评分（Critic score）、难度奖励（Difficulty reward）和最终的正确性验证（Verifier-based correctness）。
    *   **质量过滤机制**：只有通过评论者高分评估且验证器有效的题目才会被加入训练池，防止数据集退化。

### 3. 实验设计
*   **数据集与场景**：
    *   **种子数据**：仅使用 500 条来自 MATH、GSM8K、HumanEval 和 MBPP 的样本。
    *   **数学基准 (Math)**：GSM8K、MATH（分布内）；AIME 2024/2025、OlympiadBench、AMC 2023（分布外/竞赛级）。
    *   **代码基准 (Code)**：HumanEval+、MBPP+（分布内）；LiveCodeBench v1-v5（分布外/实时更新）。
*   **对比方法**：
    *   基础模型（Qwen-2.5-3B/7B-Instruct, Qwen-3-4B-Base）。
    *   **AZR (Absolute Zero Reasoning)**：一种无数据自我博弈推理方法。
    *   **MAE (Multi-Agent Evolve)**：现有的多智能体演化框架。

### 4. 资源与算力
*   **算力说明**：论文提到该框架基于 **VeRL** 强化学习框架实现。
*   **训练配置**：使用了 **LoRA** 微调技术（Rank=128，Alpha=256），学习率为 3e-6，训练步数为 200 步。
*   **未明确信息**：文中**未明确说明**具体的 GPU 型号（如 A100 或 H100）以及具体的显卡数量和总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：在 3B、4B、7B 三种不同规模和系列的模型上进行了验证，涵盖了从基础模型到指令微调模型的跨度。
*   **消融实验**：专门设计了针对“挑战者”、“求解者”和“评论者”的消融实验，证明了每个角色在系统中的必要性。
*   **充分性评价**：实验设计较为充分，不仅关注分布内（ID）性能，更强调分布外（OOD）的泛化能力。通过 200 步训练动态分析，展示了模型从知识获取到潜在过拟合的过程，体现了客观性。

### 6. 论文的主要结论与发现
*   **性能显著提升**：SAGE 在所有模型规模上均优于基准。在 Qwen-2.5-7B 上，LiveCodeBench 提升了 **8.9%**，OlympiadBench 提升了 **10.7%**。
*   **强泛化能力**：相比 AZR 和 MAE，SAGE 在竞赛级数学题和实时代码任务（OOD）上表现出更强的鲁棒性，证明了“规划+过滤”机制的有效性。
*   **自主数据扩展**：模型能将 500 条种子数据自主扩展至超过 20,000 条有效题目，实现了 18 倍的数据增长。

### 7. 优点（亮点）
*   **角色解耦**：通过将“出题、规划、解题、审核”拆分，解决了单一模型在自我训练时容易陷入的逻辑死循环。
*   **闭环稳定性**：引入评论者（Critic）作为守门人，有效缓解了自我演化中常见的“奖励崩溃”和题目质量退化问题。
*   **极低数据依赖**：仅需极少量种子样本即可启动，具有极高的扩展潜力和成本优势。

### 8. 不足与局限
*   **领域局限性**：目前仅限于数学和代码等具有“客观验证器”（Verifiable rewards）的领域，难以直接推广到创意写作或主观问答等开放领域。
*   **种子依赖**：虽然需求量小，但仍需 500 条高质量种子数据，未能实现完全的“零样本”启动。
*   **过拟合风险**：训练动态显示，在 120 步后性能可能出现下滑，说明模型对自我生成的课程存在过专业化（Over-specialization）的风险，需要精细的早停策略。

（完）
